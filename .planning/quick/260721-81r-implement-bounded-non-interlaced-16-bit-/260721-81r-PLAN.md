---
name: implement-bounded-non-interlaced-16-bit-grayscale-alpha-and-rgba-png-decode
quick_id: 260721-81r
created: 2026-07-21
phase: quick-260721-81r
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
autonomous: true
requirements: [PNGX-01]
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/deflate_inflate.mbt
  - modules/mb-image/png/raster_decode.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - modules/mb-image/png/raster_decode_wbtest.mbt
  - modules/mb-image/png/png_test.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
must_haves:
  truths:
    - A valid bounded, non-interlaced PNG of colour type 4 at depth 16 decodes through PngDecoder to straight RGBA8: the high byte of its big-endian grayscale sample is copied to red, green, and blue, and the high byte of its big-endian native alpha sample is copied to alpha.
    - A valid bounded, non-interlaced PNG of colour type 6 at depth 16 decodes through PngDecoder to straight RGBA8 by copying the high byte of each big-endian red, green, blue, and alpha sample, with no scaling, rounding, premultiplication, dithering, or colour transform.
    - Type-4/type-6 source rows are filtered and budgeted in encoded bytes: type 4 uses four bytes and filter bpp 4 per pixel, while type 6 uses eight bytes and filter bpp 8 per pixel; output remains four RGBA8 bytes per pixel.
    - tRNS remains a typed semantic failure for type 4 and type 6, and malformed, over-limit, or hostile data never exposes a DecodeResult image.
    - The independent PowerShell oracle, corpus/manifest, and generated public vectors compare each accepted and hostile payload's unsplit baseline with every nonempty contiguous two-IDAT split on js, wasm, wasm-gc, and native.
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: type-4/type-6 depth-16 legality, source-byte transport facts, and checked image/source-row/filtered/work resource accounting.
    - path: modules/mb-image/png/raster_decode.mbt
      provides: bytewise reconstruction and explicit high-byte straight-RGBA8 writers for 16-bit native-alpha source rows.
    - path: modules/mb-image/png/deflate_inflate.mbt
      provides: live IDAT row reconstruction with the same type-4/type-6 source bpp and writer selection as eager raster decoding.
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: independent big-endian native-alpha PNG oracle and generated unsplit/two-IDAT-split evidence.
    - path: fixtures/png/decode-cases.json
      provides: accepted, type/depth-invalid, native-alpha-tRNS, malformed, and resource-limit 16-bit type-4/type-6 cases.
    - path: modules/mb-image/png/generated_decode_vectors_test.mbt
      provides: generated public PngDecoder vectors and split-baseline assertions for every portable target.
  key_links:
    - from: modules/mb-image/png/structural.mbt
      to: modules/mb-image/png/png.mbt
      via: PngStreamTransport carries legal depth, source channel count, source bytes per pixel, RGBA output channels, and a child budget into descriptor construction and row allocation.
      pattern: PngStreamTransport
    - from: modules/mb-image/png/deflate_inflate.mbt
      to: modules/mb-image/png/raster_decode.mbt
      via: live IDAT and eager filtered-raster paths reconstruct complete encoded samples with bpp 4 or 8 before their writers emit each channel's high byte.
      pattern: _png_write_raster|_png_inflate_zlib_to_raster
    - from: fixtures/png/decode-cases.json
      to: modules/mb-image/png/generated_decode_vectors_test.mbt
      via: the independent generator validates decoded scanlines, emits split variants, and validates the png-decode-vectors manifest digest.
      pattern: Generate-PngDecodeVectors
---

<objective>
Implement strict, bounded decoding of non-interlaced 16-bit PNG grayscale-with-alpha (type 4) and RGBA (type 6) into the existing straight-RGBA8 image model. Every raw big-endian component maps by selecting its high byte, including the native alpha component.

Purpose: complete the remaining native-alpha portion of the bounded, pure-MoonBit 16-bit PNG decode profile without weakening authenticated transport, source-byte filtering, resource accounting, atomic result visibility, or arbitrary-IDAT-boundary behavior.
Output: private type-4/type-6 depth-16 transport/raster support plus independently generated portable evidence. Excludes type-0/type-2 changes, tRNS support for type 4/6, Adam7, colour management, encoding, public streaming, QOI, release, and configuration work.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260721-7d5-implement-bounded-non-interlaced-16-bit-/260721-7d5-SUMMARY.md
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/png.mbt
@modules/mb-image/png/deflate_inflate.mbt
@modules/mb-image/png/raster_decode.mbt
@scripts/fixtures/Generate-PngDecodeVectors.ps1
@fixtures/png/decode-cases.json
</context>

## Scope coverage audit

| Source | ID | Item | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Bounded non-interlaced 16-bit type-4/type-6 decode to straight RGBA8 using each raw component's high byte | 1-2 | COVERED | Task-scoped quick goal. |
| REQ | PNGX-01 | Decode grayscale, transparency, and 16-bit PNG profiles with explicit image-model mapping | 1-2 | COVERED | Extends only the remaining native-alpha 16-bit profiles. |
| RESEARCH | — | Existing 7d5/6k0 transport, generated-fixture, manifest, hostile-baseline, and all-target patterns | 1-2 | COVERED | Level 0: no dependency, integration, package, or new library is needed. |
| CONTEXT | — | Legal depth pairs; bpp 4/8 filters; source/output/budget checks; straight-alpha high-byte mapping; no-image failures; arbitrary split evidence; independent oracle; scope fences | 1-2 | COVERED | Supplied directly by the quick-task request; no task-local D-XX decisions exist. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode 16-bit native-alpha scanlines with encoded-byte filtering and checked resources</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png.mbt, modules/mb-image/png/deflate_inflate.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/structural_wbtest.mbt, modules/mb-image/png/raster_decode_wbtest.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - Colour types 4 and 6 accept exactly bit depths 8 and 16; their currently illegal depths retain the established png bit-depth error, and other colour-type/depth combinations preserve existing behavior.
    - For type 4/depth 16, a reconstructed source pixel is `(gray_hi, gray_lo, alpha_hi, alpha_lo)`: it emits `(gray_hi, gray_hi, gray_hi, alpha_hi)` with straight-alpha descriptor metadata.
    - For type 6/depth 16, a reconstructed source pixel is four big-endian pairs and emits `(red_hi, green_hi, blue_hi, alpha_hi)`; all low bytes are retained for filtering but never become output components.
    - Both eager `_png_write_raster` and live IDAT inflation reconstruct the encoded source bytes using bpp 4 for type 4 and bpp 8 for type 6, so Sub, Up, Average, and Paeth use the preceding complete encoded pixel and source-row state rather than the RGBA8 output view.
    - Checked preflight and decode reservations calculate source-row bytes, two private source rows, filtered output, expanded RGBA8 image bytes, allocation size, and work before allocation; exact envelopes pass while one-unit-short output/work/budget input, malformed compressed/filter input, and all semantic failures return an error without an observable image.
    - A tRNS chunk for either native-alpha profile remains rejected before image success, including normal, duplicate, post-IDAT, and CRC-corrupt chunk forms.
  </behavior>
  <action>Write focused white-box and public tests first, then extend the existing private 16-bit path. Permit depth 16 only for PNG colour types 4 and 6, retaining their legal depth-8 handling and every existing type-0/type-2, low-bit, indexed, and rejection path. Keep source channels distinct from output channels: derive source bytes per pixel as four for type 4 and eight for type 6; select the existing checked 16-bit image-plus-two-source-row budget path for all four 16-bit colour types, passing four output channels for native-alpha images. Ensure preflight and stream transport use the same checked source-row width, filtered-row/output and work calculations, and child allocation accounting; do not use the expanded RGBA8 stride as a filter bpp. Add dedicated private 16-bit grayscale-alpha and RGBA writers, or equivalent explicit branches, which read every big-endian pair after row reconstruction and emit only `gray_hi` to RGB plus `alpha_hi`, or `red_hi`, `green_hi`, `blue_hi`, and `alpha_hi`. Preserve the decoded descriptor's straight alpha and do not premultiply or apply a colour transform. Route both the eager helper and `_png_inflate_zlib_to_raster` through shared source-byte facts and the matching native-alpha writer. Keep tRNS rejected for types 4/6 through the established semantic-chunk path; do not introduce a transparency key, override native alpha, or broaden unrelated PNG support. Cover all five filters, multi-row predictor neighbours, source bpp 4/8, values with equal high and different low bytes, alpha high/low distinctions, descriptor metadata, legal-depth and invalid-depth matrices, tRNS order/duplicate/CRC failures, malformed filter/DEFLATE input, exact and one-unit-short image/output/work/allocation envelopes, and error paths with no DecodeResult image access. Do not modify type-0/type-2 mapping or transparency, add Adam7 or colour-management handling, change encoding, expose streaming, or perform QOI/release/configuration work.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>Legal non-interlaced 16-bit type-4/type-6 images follow private bounded paths with source bpp 4/8 and emit exact straight RGBA8 high-byte values for every channel; invalid, native-alpha-tRNS, malformed, and over-limit input cannot expose an image.</done>
</task>

<task type="auto">
  <name>Task 2: Generate independent native-alpha 16-bit fixtures, split baselines, and portable evidence</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/png_test.mbt</files>
  <action>Extend the existing PNG decode corpus and schema with depth-16 colour-type-4 and colour-type-6 cases while preserving every existing record. Add accepted multi-row cases for both profiles using filter tags 0 through 4, nonzero left/above/upper-left source bytes, varying grayscale/RGB/alpha high and low bytes, and expected straight-RGBA8 pixels derived solely from each component's high byte. Include values that share a high byte but differ in a low byte, including native alpha, to prove that filtering preserves full source samples even though output maps high bytes. Add hostile records for depth-invalid type 4/type 6 input; tRNS presence, duplicate, post-IDAT, and CRC corruption; invalid filters; malformed compressed data; and source-row/image/output/work/allocation limit envelopes. Mark every accepted and hostile canonical payload with a comparison group so the generator emits one unsplit baseline plus every nonempty contiguous two-IDAT split; generated public tests must compare successful dimensions/channels/pixels to their baseline and hostile category/code/context to their baseline while error results remain unobservable as images. Extend `Assert-Oracle` independently of MoonBit decoder helpers: validate type-4/type-6 depth legality, independently inflate zlib, reconstruct source rows bytewise with strides `width * 4` and bpp 4 or `width * 8` and bpp 8, and produce RGBA output from the first byte of each big-endian pair including alpha. It must reject native-alpha tRNS corpus inputs as hostile evidence rather than model it as a key. Update the required-ID/schema guard, regenerate the MoonBit vector table instead of editing it, and update only the `png-decode-vectors` SHA-256 record in the fixture manifest; retain provenance, unrelated manifest entries, earlier vectors, and their split-baseline evidence.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated></verify>
  <done>The independent PowerShell oracle, corpus, regenerated public table, manifest integrity check, accepted and hostile unsplit/two-IDAT-split baselines, and four-target test suite reproduce the complete native-alpha 16-bit contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted IHDR/tRNS to private PNG transport | Colour/depth legality, native-alpha tRNS rejection, source stride, output format, and resource envelopes determine allocation and pixel semantics. |
| compressed IDAT to reconstructed 16-bit rows | Attacker-controlled filter bytes reconstruct full source components before high-byte emission, including alpha. |
| fixture JSON/generator to generated tests | Corpus declarations and an independent oracle become portable conformance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260721-81r-01 | Denial of service | native-alpha 16-bit preflight/allocation | high | mitigate | Apply checked source-row, two-row, image, filtered-output, allocation, and work calculations before child-budget allocation; test exact and one-unit-short envelopes. |
| T-260721-81r-02 | Tampering | bytewise filter reconstruction and IDAT segmentation | high | mitigate | Reconstruct type-4/type-6 rows at encoded bpp 4/8 in both eager and live paths; compare every generated two-IDAT variant with its unsplit baseline. |
| T-260721-81r-03 | Information integrity | straight alpha and high-byte component mapping | high | mitigate | Emit high bytes for grayscale/RGB and alpha only after full-pair reconstruction; test high-byte collisions with distinct low bytes and preserve straight-alpha metadata. |
| T-260721-81r-04 | Tampering | native-alpha transparency semantics | medium | mitigate | Retain the semantic rejection of tRNS for types 4/6 and generate normal/order/duplicate/CRC hostile cases with no image outcome. |
| T-260721-81r-05 | Tampering | generated fixture evidence | medium | mitigate | Independently inflate/reconstruct source rows in PowerShell, validate generated bytes against the manifest, and execute the public suite on all four targets. |
</threat_model>

<verification>
1. `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- Non-interlaced depth-16 type-4/type-6 PNG data decodes to the existing straight-RGBA8 model through an explicit big-endian high-byte mapping for grayscale, red, green, blue, and alpha.
- Source row sizing, bpp 4/8 filtering, live/eager reconstruction, resource bounds, and atomic error behavior agree; tRNS for type 4/6 remains rejected and never produces an image.
- Independent fixture reconstruction, manifest verification, exhaustive nonempty two-IDAT success/hostile baseline comparisons, and js/wasm/wasm-gc/native tests make the contract reproducible.
- The change leaves type-0/type-2 decoding, Adam7, colour management, PNG encoding, public streaming, QOI, release, and configuration outside scope.
</success_criteria>

<output>
Create `.planning/quick/260721-81r-implement-bounded-non-interlaced-16-bit-/260721-81r-SUMMARY.md` when done
</output>
