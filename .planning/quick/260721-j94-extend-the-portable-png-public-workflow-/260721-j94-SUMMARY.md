---
quick_id: 260721-j94
phase: quick-260721-j94
plan: "01"
subsystem: png portable example and quality evidence
tags: [png, bilinear, portable, quality, moonbit]
requires: [canonical PNG encode, resize_bilinear]
provides: [public decode-resize-encode evidence, PNG-lane four-target enforcement]
affects: [examples/png-portable, png-quality]
tech_stack:
  added: []
  patterns: [exact byte evidence, target-neutral rolling digest, isolated quality lane]
key_files:
  created: []
  modified:
    - examples/png-portable/main/main.mbt
    - scripts/quality/Invoke-MoonQuality.ps1
decisions:
  - Preserve the 75-byte source PNG and use a separately budgeted 78-byte expected output buffer.
  - Require one exact public-evidence line from each portable target inside the PNG-only lane.
metrics:
  tasks_completed: 2
  files_modified: 2
  verification: Four direct portable runs and the isolated PNG quality lane passed.
status: complete
---

# Quick 260721-j94: Portable PNG Bilinear Workflow Summary

The public portable PNG example now decodes its fixed 2×1 RGB input, resizes it with alpha-correct bilinear interpolation to 3×1, and verifies the exact 78-byte canonical PNG plus digest `626208771` on every supported portable target.

## Delivered

- Replaced flip-only processing with `@ops.resize_bilinear(3UL, 1UL, ...)`, using the tight 9-byte/one-allocation/12-work budget.
- Preserved the 75-byte source allocation budget and added an independently budgeted 78-byte expected PNG literal.
- Asserted all three RGB output pixels, completed read/write lengths, canonical bytes, digest, and the frozen evidence line.
- Added the `PNG portable bilinear public workflow` stage after generated-vector freshness checks; it runs each portable target independently and accepts exactly one frozen line per target.

## Verification

- `moon -C examples/png-portable run main --target js --frozen` — passed
- `moon -C examples/png-portable run main --target wasm --frozen` — passed
- `moon -C examples/png-portable run main --target wasm-gc --frozen` — passed
- `moon -C examples/png-portable run main --target native --frozen` — passed
- `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png` — passed, including PNG-lane isolation proof and four-target PNG tests.

## Commits

- `f0dc364` — `test(quick-260721-j94): specify bilinear PNG workflow evidence`
- `c372c5f` — `feat(quick-260721-j94): run bilinear PNG workflow`
- `a8d2b78` — `test(quick-260721-j94): require PNG bilinear workflow lane`
- `76ef821` — `test(quick-260721-j94): verify PNG bilinear workflow`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected PowerShell target interpolation in the quality failure message**

- **Found during:** Task 2 quality-lane verification.
- **Issue:** `$target:` was parsed as an invalid scoped variable reference when PowerShell loaded the new stage.
- **Fix:** Delimited the variable as `${target}:` in the diagnostic message.
- **Files modified:** `scripts/quality/Invoke-MoonQuality.ps1`
- **Commit:** `76ef821`

## Known Stubs

None.

## Self-Check: PASSED

Both scoped files and all four task commits exist. The scoped stub scan found no placeholder, TODO, FIXME, or empty-data patterns.
