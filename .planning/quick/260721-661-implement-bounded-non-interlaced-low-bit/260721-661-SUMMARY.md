---
phase: quick-260721-661
plan: 01
subsystem: png-decode
tags: [moonbit, png, indexed-colour, packed-scanlines, plte, trns, fixtures]
requires:
  - phase: quick-260721-3xb
    provides: 8-bit indexed PLTE decode
  - phase: quick-260721-4vk
    provides: indexed tRNS RGBA8 transport
provides:
  - Indexed PNG decode for depths 1, 2, 4, and 8 using packed scanline filters.
  - Independently checked low-bit indexed PNG fixture evidence across four targets.
affects: [png-decode, generated-fixtures, PNGX-01]
tech-stack:
  added: []
  patterns: [packed-byte filtering with bpp 1, MSB-first visible-sample expansion]
key-files:
  modified:
    - modules/mb-image/png/structural.mbt
    - modules/mb-image/png/raster_decode.mbt
    - modules/mb-image/png/deflate_inflate.mbt
    - scripts/fixtures/Generate-PngDecodeVectors.ps1
    - fixtures/png/decode-cases.json
key-decisions:
  - "Reuse PngPackedRows for indexed pixels so filters operate on encoded packed bytes before palette lookup."
  - "Treat depth-specific PLTE cardinality and packed-row reservations as pre-image validation requirements."
patterns-established:
  - "Low-bit indexed rows use ceil(width * depth / 8), bpp 1 filtering, then MSB-first expansion through width only."
requirements-completed: []
requirements-progress:
  - PNGX-01 low-bit indexed palette subset
coverage:
  - id: D1
    description: Indexed type-3 PNG decode at depths 1, 2, 4, and 8 with RGB8/RGBA8 palette output.
    requirement: PNGX-01
    verification:
      - kind: integration
        ref: moon -C modules/mb-image test png --target all --frozen
        status: pass
    human_judgment: false
  - id: D2
    description: Independent low-bit indexed fixture oracle, generated table, and manifest digest verification.
    requirement: PNGX-01
    verification:
      - kind: integration
        ref: pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check
        status: pass
    human_judgment: false
duration: 6min
completed: 2026-07-21
status: complete
---

# Quick 260721-661: Low-Bit Indexed PNG Decode Summary

**Bounded non-interlaced indexed PNG decode now accepts depths 1, 2, 4, and 8, filters packed bytes with bpp 1, and emits validated RGB8 or straight RGBA8 palette pixels.**

## Accomplishments

- Retained indexed bit depth through transport, enforced depth-specific PLTE limits, and reserved image storage plus exactly two packed source rows.
- Unified eager and streaming indexed paths on packed-row reconstruction and MSB-first expansion without consuming padding samples.
- Added low-bit indexed fixtures for depths 1/2/4, partial tRNS alpha, exhaustive two-IDAT splits, hostile depth/PLTE/index cases, and an independent PowerShell packed-index oracle.

## Scope Completion

This task completes only the non-interlaced low-bit indexed-palette subset of
PNGX-01. Grayscale-alpha, 16-bit samples, Adam7 interlace, and colour
management remain future work.

## Task Commits

1. Task 1 RED: `ebf73c9` — failing low-bit indexed IHDR coverage.
2. Task 1 GREEN: `90ce1a9` — packed indexed row decode implementation.
3. Task 2: `cf856ce` — generated low-bit indexed fixture vectors and manifest digest.
4. Task 1 resource evidence: `e956784` — exact and one-unit-short packed-row budget coverage.

## Verification

- PASS — `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check` (413 executable cases).
- PASS — `moon -C modules/mb-image test png --target all --frozen` (26/26 tests on wasm, wasm-gc, js, and native).
- PASS — `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png` (PNG quality and lane isolation).
- BLOCKED PRE-EXISTING — `moon -C modules/mb-image check --target all --deny-warn --frozen` fails on 26 existing unused-field warnings in `generated_vectors.mbt` and the unused legacy `PngTransport.idat` / `consumed` fields. This task introduced no new deny-warn diagnostics and did not modify the generated structural-vector source.

## Deviations from Plan

None - plan executed within the bounded indexed PNG scope.

## Known Stubs

None.

## Self-Check: PASSED

Confirmed all five key artifacts exist and all four task commits are present in git history.
