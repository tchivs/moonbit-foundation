---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 05
description: Prove four-target workspace deny-warn and Required
type: execute
wave: 5
depends_on: ["260726-vpu-04"]
autonomous: true
files_modified:
  - .planning/quick/260726-vpu-eliminate-full-workspace-deny-warn-diagn/260726-vpu-SUMMARY.md
must_haves:
  truths:
    - "Full workspace check and test pass under deny-warn on all four targets from fresh bounded target directories."
    - "Both PNG generators, foundation policy, Png lane, and formatting pass."
    - "The opaque compatibility alias is exact and no warning configuration is added or expanded from Plan 01."
    - "Only the three enumerated font warning-7 declaration scopes are introduced."
    - "The complete Required lane passes from a clean detached commit."
  artifacts:
    - path: .planning/quick/260726-vpu-eliminate-full-workspace-deny-warn-diagn/260726-vpu-SUMMARY.md
      provides: zero-warning, test, policy, generator, and Required evidence
  key_links:
    - from: scripts/quality.ps1
      to: scripts/quality/Invoke-MoonQuality.ps1
      via: Required lane
      pattern: Required
---

<objective>
Prove WORK-05 and the complete repository Required lane after Plans 01-04.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Run exact fresh four-target warning and behavior matrices</name>
  <files>verification only</files>
  <action>
Resolve the committed Plan04 SHA. Under exact temp task root `[IO.Path]::GetFullPath((Join-Path ([IO.Path]::GetTempPath()) 'mnf-260726-vpu-targets'))`, use one GUID leaf matching `matrix-[0-9a-f]{32}`. For each target create separate `check-$target` and `test-$target` children and run:

- `moon check --target $target --deny-warn --frozen --target-dir <check-dir>`;
- `moon test --target $target --deny-warn --frozen --target-dir <test-dir>`.

Fail fast on nonzero exit. In finally, revalidate root containment and leaf regex, then remove only the exact GUID matrix directory with `Remove-Item -LiteralPath -Recurse`; never remove the task root or another target directory.

Also run exactly:

- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `pwsh -NoProfile -Command '. ./scripts/quality/Assert-Policy.ps1; Assert-FoundationPolicy -PolicyPath policy/foundation.json'`
- `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
- `moon fmt --check`
- `git diff --check`

Prove the compatibility migration exactly:

- exactly one repository call to `ProfileIdentity::opaque(` exists, in the
  profile black-box compatibility test;
- all other repository calls use `ProfileIdentity::from_opaque`;
- the generated profile semantic interface and `policy/foundation.json` both
  contain `#alias(opaque)` and the canonical `from_opaque` method;
- warning configuration has no additions or expansions relative to the exact
  Plan 01 commit.

Parse `rg -n '#warnings' modules -g '*.mbt'`: the only new source annotations
relative to Plan 01 are warning 7 on the three exact font retained-state
declarations. Reject any other newly introduced code, file, package, or global
warning suppression. The profile package's pre-existing `warnings = "-35"`
setting is the documented MoonBit v0.10.4 compatibility boundary for publishing
and exercising the reserved-word alias.
  </action>
  <verify>All eight fresh workspace commands exit zero; both GUID target children and matrix leaf are removed; generators, policy, Png lane, formatting, diff, exact alias inventory, and no-expanded-suppression comparison pass.</verify>
  <done>The repository is zero-warning and behavior-green on every supported target.</done>
</task>

<task type="auto">
  <name>Task 2: Run Required in a bounded detached worktree and summarize</name>
  <files>.planning/quick/260726-vpu-eliminate-full-workspace-deny-warn-diagn/260726-vpu-SUMMARY.md</files>
  <action>
Create a GUID detached worktree under exact task root `[IO.Path]::GetFullPath((Join-Path ([IO.Path]::GetTempPath()) 'mnf-260726-vpu-required'))`, leaf `required-[0-9a-f]{32}`, from the final committed source SHA. Run `pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/local`; require exit 0 and `Required quality lane passed.`.

In finally require exact porcelain membership, task-root containment, leaf regex, and clean worktree before `git worktree remove`. Preserve and report any worktree failing those checks. Never prune or remove another worktree.

If a later unrelated gate fails, report the exact first blocker and do not write a complete SUMMARY. On success write SUMMARY with `status: complete`, merge/remediation commits, 80-to-0 inventory, four-target test counts, generator/policy/Png evidence, and Required marker. Do not commit SUMMARY or edit STATE.
  </action>
  <verify>Required marker exists, referenced commits resolve, disposable worktree membership is zero, and only requested SUMMARY is untracked.</verify>
  <done>vpu is complete and ready for independent verification.</done>
</task>
</tasks>

<verification>
- Four fresh check and four fresh test commands pass under deny-warn.
- Required exits zero with exact terminal marker.
- No unsafe cleanup or false completion on a later blocker.
</verification>

<success_criteria>
- WORK-05 and full Required pass from committed source.
- Summary records all exact evidence.
</success_criteria>
