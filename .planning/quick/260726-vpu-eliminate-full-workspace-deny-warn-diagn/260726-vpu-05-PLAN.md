---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 05
description: Remove hidden production and Canvas deprecations
type: execute
wave: 5
depends_on: ["260726-vpu-04"]
autonomous: true
files_modified:
  - examples/mb-svg-demo/main/main.mbt
  - modules/mb-canvas/canvas/stroke.mbt
  - modules/mb-canvas/canvas/render_wbtest.mbt
  - modules/mb-svg/svg/color.mbt
  - modules/mb-svg/svg/length.mbt
  - modules/mb-svg/svg/path_data.mbt
  - modules/mb-svg/svg/scene.mbt
must_haves:
  truths:
    - "Current Boolean, StringView, slice, and character APIs replace deprecated spellings without behavior changes."
    - "PaintContext is explicitly private and no public SVG interface changes."
    - "Canvas render assertions avoid deprecated aggregate Show while retaining exact expected values."
    - "The demo, Canvas, and SVG production packages pass every target under deny-warn."
  artifacts:
    - path: modules/mb-canvas/canvas/render_wbtest.mbt
      provides: typed render assertions without aggregate snapshots
    - path: modules/mb-svg/svg/scene.mbt
      provides: explicit private paint context
  key_links:
    - from: modules/mb-svg/svg/path_data.mbt
      to: MoonBit v0.10.4 character conversion
      via: ASCII-lowercase-guarded unsafe_to_char
      pattern: unsafe_to_char
---

<objective>
Clear the 14 hidden production/example/Canvas diagnostics that become visible
after the original 80-warning compiler limit is removed.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Migrate production and example deprecated APIs</name>
  <files>examples/mb-svg-demo/main/main.mbt, modules/mb-canvas/canvas/stroke.mbt, modules/mb-svg/svg/color.mbt, modules/mb-svg/svg/length.mbt, modules/mb-svg/svg/path_data.mbt, modules/mb-svg/svg/scene.mbt</files>
  <action>
Replace the two `not(expr)` uses with `!expr`. Replace StringView
`to_string()` with `to_owned()`. Replace `substring(end=...)` with the exact
StringView slice followed by `to_owned()`. After the existing ASCII lowercase
range proof, replace `Char::from_int(v - 32)` with
`Int::unsafe_to_char(v - 32)`. Mark `PaintContext` as `priv struct`.
Do not alter public signatures, parsing rules, warning configuration, or policy.
  </action>
  <verify>
Run only
`moon check examples/mb-svg-demo/main/main.mbt --target all --deny-warn --frozen`.
The demo dependency graph compiles the six owned production/example sites
without compiling dependency test files. Empirical Moon CLI behavior compiles
all same-package tests even for an SVG source-file selector, so do not use
same-package SVG/Canvas file checks before later test plans complete.
  </verify>
  <done>All six production/example deprecations are absent on every target.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Canvas aggregate snapshots with typed assertions</name>
  <files>modules/mb-canvas/canvas/render_wbtest.mbt</files>
  <action>
Replace the eight deprecated aggregate `inspect` assertions with typed
`assert_eq` assertions or explicit scalar assertions. Preserve the exact
expected values and test names. Do not add snapshots or warning suppression.
  </action>
  <verify>
Run `moon check modules/mb-canvas/canvas --target all --deny-warn --frozen`,
`moon test modules/mb-canvas/canvas --target all --deny-warn --frozen`, rerun
the demo check from Task 1, and run `git diff --check`. Do not run the full SVG
package yet; Plans 06-07 own its remaining test warnings.
  </verify>
  <done>Canvas production and tests are behavior-green and warning-free on all targets.</done>
</task>
</tasks>

<verification>
- Exactly seven owned files change.
- No new `#warnings` or package warning configuration is introduced.
- Demo dependency-production proof plus full Canvas checks/tests pass target-all deny-warn.
</verification>

<success_criteria>
- The first 14 hidden WORK-05 diagnostics are eliminated without policy or API changes.
</success_criteria>
