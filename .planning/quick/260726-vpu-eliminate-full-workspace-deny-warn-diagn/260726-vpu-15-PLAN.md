---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 15
description: Reconcile remaining publication-file allowlists
type: execute
wave: 15
depends_on: ["260726-vpu-13"]
autonomous: true
files_modified:
  - policy/foundation.json
must_haves:
  truths:
    - "All five registered module package lists are audited against the pinned package command."
    - "Only nine omitted, already-tracked publication files are added: eight mb-image ops files and one mb-canvas white-box test."
    - "No source, manifest, script, interface array, dependency, target, or other policy changes."
  artifacts:
    - path: policy/foundation.json
      provides: exact publication_files allowlists for all five registered modules
  key_links:
    - from: modules/mb-image/ops
      to: policy/foundation.json
      via: WORK-04 package allowlist
      pattern: "geometry|processing"
---

<objective>
Repair the final package-allowlist blocker by synchronizing every remaining
registered publication file in one bounded policy-only change.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Audit five package lists and register the nine omissions</name>
  <files>policy/foundation.json</files>
  <action>
From the clean Plan 13 commit, run pinned
`moon -C modules/&lt;module&gt; package --frozen --list` for mb-core, mb-color,
mb-image, mb-canvas, and mb-font. Normalize path separators exactly as
`Assert-PackageList` does.

Require core 91/91, color 43/43, and font 14/14 to be exact. Require image to
have its 93 existing entries plus exactly these eight omissions and no missing
entries:

- `ops/geometry.mbt`
- `ops/geometry_test.mbt`
- `ops/geometry_wbtest.mbt`
- `ops/processing.mbt`
- `ops/processing_pipeline_test.mbt`
- `ops/processing_pipeline_wbtest.mbt`
- `ops/processing_test.mbt`
- `ops/processing_wbtest.mbt`

Require canvas to have its 14 existing entries plus exactly
`canvas/stroke_wbtest.mbt` and no missing entries.

Add only those nine strings beside their existing package groups without
reordering or rewriting existing entries. Do not change semantic interfaces or
any other policy key.
  </action>
  <verify>
Re-run all five package lists and require exact set equality: core 91, color
43, image 101, canvas 15, font 14, with zero extras or missing entries. Run
Foundation policy, `git diff --check`, and workspace four-target deny-warn
check. Structurally compare HEAD and worktree policy after removing
`publication_files`; require zero drift, exactly two changed arrays, and the
exact nine additions with no removals.

Bind Plan 14's 16/16 successful 1,035/1,035 summaries and 25/25 D-15 results to
the unchanged source and interface policy; Plan 16 re-runs the complete gate.
  </verify>
  <done>Every registered module package list is exact and the policy-only change is committed.</done>
</task>
</tasks>

<verification>
- Five package-list inventories are exact at 91/43/101/15/14.
- Only mb-image and mb-canvas publication_files change, by the exact nine additions.
- Foundation policy and workspace four-target deny-warn check pass.
</verification>

<success_criteria>
- No known package allowlist mismatch remains before the final Required rerun.
</success_criteria>
