---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 10
description: Prove final formatted source and detached Required
type: execute
wave: 10
depends_on: ["260726-vpu-09"]
autonomous: true
files_modified:
  - .planning/quick/260726-vpu-eliminate-full-workspace-deny-warn-diagn/260726-vpu-SUMMARY.md
must_haves:
  truths:
    - "The final committed source is zero-warning and behavior-green on all four targets."
    - "All 227 root-workspace MoonBit source paths are canonical under direct pinned moonfmt comparison."
    - "Both PNG generators, foundation policy, exact alias inventory, and warning-suppression inventory pass."
    - "The complete Required lane passes from a clean detached worktree at the final source commit."
  artifacts:
    - path: .planning/quick/260726-vpu-eliminate-full-workspace-deny-warn-diagn/260726-vpu-SUMMARY.md
      provides: final 207-to-0, source-format, generator, policy, and Required evidence
  key_links:
    - from: scripts/quality.ps1
      to: scripts/quality/Invoke-MoonQuality.ps1
      via: Required lane
      pattern: Required
---

<objective>
Complete WORK-05 after the source-only formatter remediation and prove the final
committed source through the detached Required lane.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Revalidate final source, inventories, generators, and policy</name>
  <files>verification only</files>
  <action>
Resolve the committed Plan 09 SHA and require a clean worktree.

Resolve `(Get-Command moon -ErrorAction Stop).Source`, require
`moon --version` to report `moon 0.1.20260713 (75c7e1f 2026-07-13)`, derive
and require its sibling absolute `moonfmt.exe`, and use only that absolute
formatter path.

Repeat Plan 09's direct pinned-moonfmt SHA-256 comparison in a separately
bounded GUID leaf for the exact 227 tracked paths below root `moon.work`
members. Require zero mismatches and safe leaf-only cleanup. Do not use
`moon fmt`; the root command is incompatible with STACK.md's deliberate
`moon.mod.json` retention and would measure an architectural migration rather
than source layout.

Run exactly:

- `git diff --check`
- `moon check --target all --deny-warn --frozen`
- `moon test --target all --deny-warn --frozen`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `pwsh -NoProfile -Command '. ./scripts/quality/Assert-Policy.ps1; Assert-FoundationPolicy -PolicyPath policy/foundation.json'`

Require 1,035/1,035 tests per target. Re-run the exact compatibility inventory:

- exactly one repository call to `ProfileIdentity::opaque(`, in the profile
  black-box compatibility test;
- every other call uses `ProfileIdentity::from_opaque`;
- the generated semantic interface and foundation policy both contain
  `#alias(opaque)` and canonical `from_opaque`;
- no warning configuration is added or expanded relative to the Plan 01
  integration commit `11cf8eeac854e3831eabfda94b77f15c0fed6cc8`;
- the only new source warning annotations relative to that Plan 01 integration
  commit are warning 7 on
  the three exact retained font declarations.

Bind the successful Plan 08 pre-format evidence to exact tested source commit
`4fd895f7ac879b35407ce87a410cdbe4dcfa1d1e`. Require the only paths changed
between that commit and `Plan09Commit^` to be the two planning files
`260726-vpu-09-PLAN.md` and `260726-vpu-10-PLAN.md`; reject any source,
manifest, policy, or script delta. Then use Plan 08 as pre-format proof: its fresh isolated
matrix passed all eight commands with 1,035/1,035 tests per target; both
generators and Foundation policy passed; the full Png lane passed with both
terminal markers and 320/320 on every target. Plan 09 is layout-only, and this
task's final four-target matrix plus detached Required below must validate the
post-format commit.
  </action>
  <verify>Source-format, diff, four-target warning/behavior, generators, policy, alias, and suppression inventories all pass from the committed Plan 09 source.</verify>
  <done>The final source is ready for the detached repository gate.</done>
</task>

<task type="auto">
  <name>Task 2: Run Required from final detached commit and summarize</name>
  <files>.planning/quick/260726-vpu-eliminate-full-workspace-deny-warn-diagn/260726-vpu-SUMMARY.md</files>
  <action>
Create a GUID detached worktree under exact task root
`[IO.Path]::GetFullPath((Join-Path ([IO.Path]::GetTempPath()) 'mnf-260726-vpu-required'))`,
leaf `required-[0-9a-f]{32}`, from the final committed Plan 09 source SHA. Run:

`pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/local`

Require exit 0 and exact marker `Required quality lane passed.`.

In `finally`, require exact porcelain membership, task-root containment, leaf
regex, and clean detached worktree before `git worktree remove`. Preserve and
report any worktree that fails those safety checks. Never prune or remove
another worktree.

If any gate fails, report the exact first blocker and do not write a complete
SUMMARY. On total success write the requested SUMMARY with `status: complete`,
all implementation/remediation commit SHAs, original 80 plus hidden 127
(207-to-0), Plan 08 fresh matrix and Png evidence, Plan 09 227-source formatting
evidence, final four-target 1,035/1,035 counts, generator/policy/alias evidence,
and the Required marker. Do not commit SUMMARY or edit STATE.
  </action>
  <verify>Required marker exists, referenced commits resolve, disposable worktree membership is zero, and only the requested SUMMARY is untracked.</verify>
  <done>vpu is complete and ready for independent verification.</done>
</task>
</tasks>

<verification>
- Final committed source passes direct pinned-moonfmt comparison for 227 files.
- Final check/test pass all four targets under deny-warn.
- Detached Required exits zero with its exact terminal marker.
- No unsafe cleanup or false completion occurs on a later blocker.
</verification>

<success_criteria>
- WORK-05 and the full Required lane pass from the final formatted commit.
- SUMMARY records exact 207-to-0 evidence without changing STATE.
</success_criteria>
