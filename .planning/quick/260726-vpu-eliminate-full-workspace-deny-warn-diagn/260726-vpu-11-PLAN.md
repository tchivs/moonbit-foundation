---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 11
description: Reconcile the mb-core bytes semantic-interface baseline
type: execute
wave: 11
depends_on: ["260726-vpu-09"]
autonomous: true
files_modified:
  - policy/foundation.json
must_haves:
  truths:
    - "The D-15 bytes mismatch is an omitted policy baseline for the existing public mutation revision contract, not a new implementation change."
    - "The policy records the exact generated ByteView::mutation_revision signature in generated-interface order."
    - "No implementation, manifest, warning configuration, or unrelated policy entry changes."
  artifacts:
    - path: policy/foundation.json
      provides: exact 32-line tchivs/mb-core/bytes semantic-interface baseline
  key_links:
    - from: modules/mb-core/bytes/views.mbt
      to: policy/foundation.json
      via: generated pkg interface classification
      pattern: "ByteView::mutation_revision"
---

<objective>
Repair the first Plan 10 Required blocker by synchronizing the foundation
semantic-interface baseline with the existing public bytes contract.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Prove provenance and update the exact bytes baseline</name>
  <files>policy/foundation.json</files>
  <action>
From the clean Plan 09 source commit, generate the mb-core all-target interface
with the pinned Moon toolchain. Compare its nonblank, non-comment semantic
lines with `tchivs/mb-core/bytes.semantic_interface`.

Require the only difference to be the generated line
`pub fn ByteView::mutation_revision(Self) -> UInt64` immediately after
`ByteView::length`. Prove by history that the API was introduced in Phase 48
commit `f555f01f6faf9c89abe4cad963fbe16cf57b6c02`, is consumed by retained-source
mutation guards, and predates this remediation.

Insert that exact line at the exact generated-interface position. Do not change
MoonBit source, manifests, scripts, warning configuration, or any other policy
entry.
  </action>
  <verify>
Parse `policy/foundation.json`, regenerate `modules/mb-core/bytes/pkg.generated.mbti`,
and require exact ordinal equality for all 32 semantic lines. Run the foundation
policy assertion, `git diff --check`, mb-core all-target deny-warn check, and
mb-core all-target tests.
  </verify>
  <done>D-15 classifies mb-core/bytes exactly and the narrow policy fix is green.</done>
</task>
</tasks>

<verification>
- The committed diff is exactly one added JSON string in the bytes semantic interface.
- Generated and expected bytes interfaces are exactly equal at 32 semantic lines.
- Foundation policy and mb-core four-target warning/behavior checks pass.
</verification>

<success_criteria>
- The stale D-15 bytes baseline is corrected without changing implementation behavior.
</success_criteria>
