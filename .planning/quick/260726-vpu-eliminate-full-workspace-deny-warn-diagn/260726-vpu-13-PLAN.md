---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 13
description: Reconcile every remaining generated-interface baseline
type: execute
wave: 13
depends_on: ["260726-vpu-11"]
autonomous: true
files_modified:
  - policy/foundation.json
must_haves:
  truths:
    - "All 27 registered public packages are audited against freshly generated pinned-toolchain interfaces, rather than chasing one Required failure at a time."
    - "Only the 10 stale semantic_interface arrays are synchronized; the 17 already-exact package baselines remain byte-identical."
    - "No MoonBit implementation, manifest, script, warning configuration, inventory, dependency, or publication policy changes."
  artifacts:
    - path: policy/foundation.json
      provides: exact generated semantic interfaces for every registered public package
  key_links:
    - from: modules/*/*/pkg.generated.mbti
      to: policy/foundation.json
      via: D-15 exact ordinal semantic-interface classification
      pattern: "semantic_interface"
---

<objective>
Repair the systemic stale generated-interface baselines exposed after Plan 11,
so D-15 classifies every registered package in one bounded remediation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Audit all public interfaces and synchronize only stale arrays</name>
  <files>policy/foundation.json</files>
  <action>
From the clean Plan 11 remediation commit, run pinned
`moon -C modules/&lt;module&gt; info --target all --frozen` for mb-core,
mb-color, mb-image, mb-canvas, and mb-font. For every registered public package,
derive semantic lines exactly as D-15 does: trim line endings, remove blank and
comment lines, and preserve leading whitespace.

Require exactly 27 packages and exactly these 10 stale baselines:

- `tchivs/mb-core/math` 96 -> 145
- `tchivs/mb-core/bits` 24 -> 24
- `tchivs/mb-core/text` 29 -> 38
- `tchivs/mb-core/unicode` 35 -> 45
- `tchivs/mb-color/model` 50 -> 54
- `tchivs/mb-color/blend` 34 -> 34
- `tchivs/mb-image/model` 92 -> 96
- `tchivs/mb-image/storage` 39 -> 43
- `tchivs/mb-image/ops` 21 -> 30
- `tchivs/mb-canvas/canvas` 70 -> 85

Require the other 17 packages, including the repaired bytes and current
profile compatibility alias interface, to be exact before editing.

Replace only each listed package's `semantic_interface` array with its freshly
generated exact line sequence. This includes leading-whitespace normalization
emitted by the pinned toolchain as well as previously omitted established API
signatures. Do not copy a whole policy file from another branch and do not
change non-interface keys.
  </action>
  <verify>
Regenerate all five module interfaces and require exact count, ordinal, and
case-sensitive equality for all 27 packages with zero mismatches. Parse the
JSON, run the foundation policy assertion, `git diff --check`, and the
workspace four-target deny-warn check. Require a structural diff audit to show
that only the 10 named `semantic_interface` arrays changed.

Bind Plan 12's 16 successful 1,035/1,035 target/module test summaries to the
same source commit: Plan 13 changes policy only, and Plan 14 re-runs the entire
Required lane after commit.
  </verify>
  <done>All generated interfaces classify exactly and the systemic policy-only remediation is committed.</done>
</task>
</tasks>

<verification>
- 27/27 package interfaces are exact under D-15 semantics.
- Exactly 10 named semantic-interface arrays change; 17 exact arrays and all non-interface policy remain unchanged.
- Foundation policy and workspace four-target deny-warn check pass.
</verification>

<success_criteria>
- No known D-15 baseline mismatch remains before the final Required rerun.
</success_criteria>
