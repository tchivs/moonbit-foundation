---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 02
description: Restore color API compatibility and reconcile PNG exact policy
type: execute
wave: 2
depends_on: ["260726-vpu-01"]
autonomous: true
files_modified:
  - modules/mb-color/profile/profile.mbt
  - modules/mb-color/profile/profile_test.mbt
  - modules/mb-image/model/model_test.mbt
  - modules/mb-image/ops/processing_test.mbt
  - modules/mb-image/ops/resize_convert_wbtest.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/generated_decode_vectors_test.mbt
  - modules/mb-image/png/generated_vectors_test.mbt
  - modules/mb-image/png/generated_vectors_wbtest.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - policy/foundation.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - scripts/fixtures/Generate-PngStructuralVectors.ps1
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
must_haves:
  truths:
    - "ProfileIdentity::from_opaque is the warning-free canonical API and ProfileIdentity::opaque remains a published source-compatible alias."
    - "Production and ordinary tests use the canonical spelling; no package/global suppression is added or expanded."
    - "PNG structural vectors are a white-box file and absent from production_sources."
    - "Generated PNG tests are reproducible, moonfmt-stable, and loadable by the default Node runner."
    - "Foundation semantic, publication, production, directory, negative-fixture, and lane inventories agree exactly."
  artifacts:
    - path: policy/foundation.json
      provides: compatible color semantic interface and exact PNG file classification
    - path: scripts/quality/Assert-Policy.ps1
      provides: exact PNG source/file and negative inventory
  key_links:
    - from: policy/foundation.json
      to: modules/mb-color/profile/profile.mbt
      via: exact generated semantic interface
      pattern: ProfileIdentity::(from_opaque|opaque)
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: modules/mb-image/png/generated_vectors_wbtest.mbt
      via: Png lane exact package allowlist
      pattern: png/generated_vectors_wbtest[.]mbt
---

<objective>
Keep def3f87's internal PNG cleanup while restoring the stable color API and closing its exact-policy omissions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@AGENTS.md
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1

Seven call sites were changed by def3f87: one profile test, two model tests,
one processing test, one resize white-box test, and two PNG metadata helpers.
The profile test is the single compatibility exercise for the old alias; every
other repository-owned call uses the canonical warning-free method. The
profile package's pre-existing `warnings = "-35"` setting is required to
publish and exercise the reserved-word alias on MoonBit v0.10.4.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Preserve ProfileIdentity::opaque through the canonical alias mechanism</name>
  <files>modules/mb-color/profile/profile.mbt, modules/mb-color/profile/profile_test.mbt, modules/mb-image/model/model_test.mbt, modules/mb-image/ops/processing_test.mbt, modules/mb-image/ops/resize_convert_wbtest.mbt, modules/mb-image/png/png.mbt, policy/foundation.json</files>
  <action>
Keep `ProfileIdentity::from_opaque` as the warning-free canonical method and add
MoonBit's native `#alias(opaque)` compatibility alias to that single
implementation. Keep one profile-package compatibility assertion that calls the
old alias; migrate the other six repository-owned call sites to
`ProfileIdentity::from_opaque`. Do not add or expand warning suppression.

The repository already contains package warning settings, including `-35` in
the profile package; this is a discovered pre-existing toolchain compatibility
boundary, not a suppression introduced by this plan. MoonBit v0.10.4 documents
that `#warnings` works for only some warnings, and experiments prove W0035 is
not declaration-scopable while expression attributes are invalid syntax.
Therefore remove the ineffective declaration annotations introduced during
execution and leave existing package manifests unchanged.

Generate/inspect the semantic interface and update foundation policy to match it
exactly. If the alias and canonical method are both emitted, record both.
  </action>
  <verify>
For each target js, wasm, wasm-gc, native run, fail-fast:

- `moon test modules/mb-color/profile --target $target --deny-warn --frozen`
- `moon test modules/mb-image/model --target $target --deny-warn --frozen`
- `moon test modules/mb-image/ops --target $target --deny-warn --frozen`
- `moon check modules/mb-image/png --target $target --deny-warn --frozen`

Require exactly one repository compatibility call to
`ProfileIdentity::opaque`, all production calls to use
`ProfileIdentity::from_opaque`, and the semantic policy to match `moon info`
exactly. Require no manifest warning-setting diff. The full PNG test matrix is
deferred to Task 3 because execution discovered a pre-existing generated-JS
loader overflow.
  </verify>
  <done>The old public spelling remains callable through a native alias, the canonical spelling is warning-free, and no suppression scope is widened.</done>
</task>

<task type="auto">
  <name>Task 2: Reclassify PNG structural vectors in exact policy</name>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <action>
Replace PNG publication/directory references with `generated_vectors_wbtest.mbt`; remove the old file from PNG production_sources and `$sources`; update the Png lane package allowlist. Rewrite the PNG wrong-source-order negative as a same-cardinality swap of two actual production sources and keep extra-source/file negatives fail-closed. Do not alter PPM/QOI generated-vector classifications or historical planning.
  </action>
  <verify>
Run exactly:

- `pwsh -NoProfile -Command '. ./scripts/quality/Assert-Policy.ps1; Assert-FoundationPolicy -PolicyPath policy/foundation.json; Assert-PngQualificationNegativeFixtures -PolicyPath policy/foundation.json'`
- `moon check modules/mb-image/png --target all --deny-warn --frozen`
- `git diff --check`

Generator checks and the complete Png lane are deferred to Task 3 because
execution proved both generated artifacts were stale before this task's policy
change.

  </verify>
  <done>PNG policy and generated evidence match the actual production/test boundary exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Make PNG generated evidence reproducible and JS-loader bounded</name>
  <files>scripts/fixtures/Generate-PngDecodeVectors.ps1, scripts/fixtures/Generate-PngStructuralVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/generated_vectors_test.mbt, modules/mb-image/png/generated_vectors_wbtest.mbt, modules/mb-image/png/structural_wbtest.mbt</files>
  <action>
After Tasks 1-2 are committed, cherry-pick exact concurrent main commit
`279b7415caf73648135a357ddd84054c387fef2c` to retain its WORK-04 decode-vector
formatting and structural white-box correction. Do not merge a moving branch
name.

Repair `Split-MoonBytesLiteral` to compare a PowerShell char with `[char]'\'`.
Add a generated private MoonBit helper that joins `Array[Bytes]` by computing
the exact capacity, appending each `part.iter()` with `push_iter`, and returning
`Bytes::from_array(output)`. Keep the ordinary literal/concat rendering for at
most 64 pieces; render fields above 64 pieces as one flat bytes array passed to
the helper. Regenerate the decode vectors. This must bound the generated JS AST
depth without changing any vector bytes, pixels, order, limits, or budgets.

Rewrite the structural generator's struct and record emitters as canonical
multi-line `Rows.Add` output matching moonfmt. Keep one shared `Render-Table`
path for public and white-box files, differing only by function name.
Regenerate both artifacts; do not call moonfmt from the generator and do not
change fixture JSON or manifest digests.
  </action>
  <verify>
Run exactly:

- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngStructuralVectors.ps1`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`
- `moon fmt --check modules/mb-image/png`
- `git diff --check`
- `git diff --exit-code -- fixtures/png fixtures/manifest.json`

Normalize only the two generated structural function names and require the
public and white-box artifact texts to be byte-identical afterward.

For every target js, wasm, wasm-gc, native, run
`moon test modules/mb-image/png --target $target --deny-warn --frozen`.
Then run `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
The default Node invocation must pass; do not use a wrapper, `NODE_OPTIONS`, or
an enlarged stack.
  </verify>
  <done>Both PNG generators reproduce canonical artifacts and the default runner passes the complete four-target PNG suite.</done>
</task>
</tasks>

<verification>
- Canonical from_opaque plus the source-compatible opaque alias are both proven.
- No warning suppression is added or widened.
- Both PNG generators, foundation policy, negative fixtures, default-Node Png lane, four-target PNG tests, and target-all check pass.
</verification>

<success_criteria>
- Compatible color API and green PNG exact policy.
- No new broad warning suppression or unrelated policy change.
</success_criteria>
