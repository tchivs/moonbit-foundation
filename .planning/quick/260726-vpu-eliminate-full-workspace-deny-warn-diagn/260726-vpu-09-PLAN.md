---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 09
description: Normalize workspace MoonBit sources without manifest migration
type: execute
wave: 9
depends_on: ["260726-vpu-07"]
autonomous: true
files_modified:
  - examples/mb-svg-demo/main/main.mbt
  - examples/png-portable/main/main.mbt
  - examples/ppm-portable/main/main.mbt
  - examples/qoi-portable/main/main.mbt
  - modules/mb-canvas/README.mbt.md
  - modules/mb-canvas/canvas/coverage_wbtest.mbt
  - modules/mb-canvas/canvas/path_builder.mbt
  - modules/mb-canvas/canvas/rasterize.mbt
  - modules/mb-canvas/canvas/render_wbtest.mbt
  - modules/mb-canvas/canvas/stroke_wbtest.mbt
  - modules/mb-canvas/canvas/stroke.mbt
  - modules/mb-canvas/canvas/style.mbt
  - modules/mb-color/README.mbt.md
  - modules/mb-core/README.mbt.md
  - modules/mb-core/text/utf8_wbtest.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/metrics.mbt
  - modules/mb-image/README.mbt.md
  - modules/mb-svg/svg/bounds_wbtest.mbt
  - modules/mb-svg/svg/color.mbt
  - modules/mb-svg/svg/conformance_wbtest.mbt
  - modules/mb-svg/svg/geometry_wbtest.mbt
  - modules/mb-svg/svg/geometry.mbt
  - modules/mb-svg/svg/length.mbt
  - modules/mb-svg/svg/lower_wbtest.mbt
  - modules/mb-svg/svg/lower.mbt
  - modules/mb-svg/svg/numeric_contract_wbtest.mbt
  - modules/mb-svg/svg/parse_wbtest.mbt
  - modules/mb-svg/svg/path_data_wbtest.mbt
  - modules/mb-svg/svg/path_data.mbt
  - modules/mb-svg/svg/portable_qualification_wbtest.mbt
  - modules/mb-svg/svg/scene_wbtest.mbt
  - modules/mb-svg/svg/scene.mbt
  - modules/mb-svg/svg/svg_bench.mbt
  - modules/mb-svg/svg/svg_test.mbt
  - modules/mb-svg/svg/svg.mbt
  - modules/mb-svg/svg/transform_wbtest.mbt
  - modules/mb-svg/svg/transform.mbt
must_haves:
  truths:
    - "All 227 tracked MoonBit source and literate-source files in root moon.work members are canonical under the pinned moonfmt."
    - "Formatting does not migrate or alter moon.mod.json, moon.mod, moon.pkg, or moon.pkg.json."
    - "The exact 38-file formatter mismatch inventory is changed mechanically and no other file is changed."
    - "The formatted workspace remains zero-warning and behavior-green on all four targets."
  artifacts:
    - path: examples/png-portable/main/main.mbt
      provides: canonical source layout without manifest migration
    - path: modules/mb-svg/svg/lower_wbtest.mbt
      provides: canonical layout retaining fail-closed expectations
  key_links:
    - from: moon.work
      to: modules/**/*.mbt
      via: tracked root-workspace source inventory
      pattern: members
---

<objective>
Remediate Plan 08's formatter blocker without violating the project's explicit
decision to retain `moon.mod.json`.

`moon fmt --check` is not an admissible gate with the pinned toolchain: it has
no flag to disable manifest migration, proposes eleven `moon.mod.json` to
`moon.mod` migrations, and expands explicit source paths to package
configuration. Use the pinned `moonfmt` executable directly so only source
bytes are read and written.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Prove and normalize the exact workspace source inventory</name>
  <files>the exact 38 files in frontmatter</files>
  <action>
Build the source inventory from tracked paths below the exact root `moon.work`
member families:

- `git ls-files 'modules/**/*.mbt' 'modules/**/*.mbt.md'`
- `git ls-files 'examples/**/*.mbt' 'examples/**/*.mbt.md'`

Ordinal-sort and require exactly 227 unique paths. Do not include the separate
`benchmarks/moon.work` or qualification fixtures.

Resolve `(Get-Command moon -ErrorAction Stop).Source`, require
`moon --version` to report `moon 0.1.20260713 (75c7e1f 2026-07-13)`, derive
the sibling absolute path `moonfmt.exe`, and require that exact file to exist.
Use only that absolute formatter path below; never invoke a PATH-dependent
`moonfmt`.

Under exact temp task root
`[IO.Path]::GetFullPath((Join-Path ([IO.Path]::GetTempPath()) 'mnf-260726-vpu-moonfmt'))`,
create one GUID leaf matching `format-[0-9a-f]{32}`. For each source run the
pinned `moonfmt` directly with the source input and a unique output file under
the leaf, then compare input/output SHA-256 bytes. Require the pre-format
mismatch inventory to equal exactly the 38 `files_modified` paths above.

Run the absolute pinned `moonfmt.exe -w` on exactly those 38 paths. Run the bounded 227-file
byte-comparison again and require zero mismatches. In `finally`, revalidate task
root containment and the leaf regex, then remove only the exact GUID leaf.
Never call `moon fmt`, and never edit a manifest.
  </action>
  <verify>The first audit reports 227 files and 38 exact mismatches; the second reports 227 files and zero mismatches; the GUID leaf is removed and its root is preserved.</verify>
  <done>The root workspace's source and literate-source files are canonical without manifest migration.</done>
</task>

<task type="auto">
  <name>Task 2: Prove format-only scope and preserve four-target behavior</name>
  <files>the exact 38 files in frontmatter</files>
  <action>
Require the diff name inventory to equal the 38 planned files exactly. Reject
any `moon.mod`, `moon.mod.json`, `moon.pkg`, or `moon.pkg.json` change. Inspect
the diff and require layout-only changes from `moonfmt`; no literal, identifier,
visibility, API, test expectation, warning directive, or policy change.

Run:

- `git diff --check`
- `moon check --target all --deny-warn --frozen`
- `moon test --target all --deny-warn --frozen`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`
- `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`

Require four-target tests to remain 1,035/1,035 per target and both generated
files to remain canonical. Commit the 38-file mechanical formatting change
atomically. Do not write SUMMARY or edit STATE.
  </action>
  <verify>Exact diff scope, no manifest changes, zero-warning four-target check, 4,140/4,140 aggregate tests, both generator checks, and a clean post-commit worktree.</verify>
  <done>Plan 08's source-format blocker is remediated by a committed format-only change.</done>
</task>
</tasks>

<verification>
- Direct moonfmt comparison covers exactly 227 root-workspace source paths.
- The only changed files are the 38 independently audited mismatches.
- No manifest or behavior change is present.
</verification>

<success_criteria>
- Root-workspace MoonBit sources are canonical under the pinned formatter.
- The project keeps the documented `moon.mod.json` compatibility decision.
- Final proof can run from the new committed source SHA.
</success_criteria>
