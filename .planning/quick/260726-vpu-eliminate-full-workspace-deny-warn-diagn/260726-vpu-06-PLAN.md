---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 06
description: Strengthen SVG lower warning-clean tests
type: execute
wave: 6
depends_on: ["260726-vpu-05"]
autonomous: true
files_modified:
  - modules/mb-svg/svg/lower_wbtest.mbt
must_haves:
  truths:
    - "All lowerer expectations use typed/scalar assertions instead of deprecated aggregate Show."
    - "Every formerly unused fallback now fails the test instead of silently passing."
    - "SVG lower tests pass all targets and their owned diagnostics are absent."
    - "The only remaining SVG diagnostics are the exact 64 W0020 sites owned by Plan 07."
  artifacts:
    - path: modules/mb-svg/svg/lower_wbtest.mbt
      provides: fail-closed typed lowerer conformance tests
  key_links:
    - from: modules/mb-svg/svg/lower_wbtest.mbt
      to: modules/mb-svg/svg/lower.mbt
      via: exact drawing-list variant and scalar assertions
      pattern: fail
---

<objective>
Clear all 49 warnings in lower_wbtest while closing 37 test branches that
currently accept an unexpected variant.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Make SVG lower assertions typed and fallbacks fail-closed</name>
  <files>modules/mb-svg/svg/lower_wbtest.mbt</files>
  <action>
Replace the 12 tuple/Option/aggregate `inspect` assertions that emit W0020 with
typed `assert_eq` or explicit scalar assertions, preserving exact expectations.
For each of the 37 match fallbacks whose `other` binding is unused, replace the
branch with `_ => fail("...")` using a concise expectation-specific message.
Do not merely rename the binding to `_`: an unexpected drawing variant must
fail. Do not add warning suppression or modify production code.
  </action>
  <verify>
For each target js, wasm, wasm-gc, native, run
`moon test modules/mb-svg/svg/lower_wbtest.mbt --target $target --frozen` and
require exit 0. Capture diagnostics and require no W0002/W0020 location in the
owned file. Across each target require exactly 64 W0020 diagnostics, exclusively
in Plan 07's seven declared files, with an identical location/ID fingerprint.
Run `git diff --check`.

Do not use `--deny-warn` in this intermediate plan: Moon CLI compiles all SVG
test files even when one test file is selected, and Plan 07 intentionally owns
the remaining warnings.
  </verify>
  <done>All 49 owned diagnostics are removed, unexpected lowerer variants fail closed, and the remaining 64-warning boundary is exact.</done>
</task>
</tasks>

<verification>
- Exactly one test file changes.
- All 37 previously ignored fallback branches explicitly fail.
- No warning suppression is added.
</verification>

<success_criteria>
- SVG lower conformance is stricter and its owned diagnostics are clean on all targets.
</success_criteria>
