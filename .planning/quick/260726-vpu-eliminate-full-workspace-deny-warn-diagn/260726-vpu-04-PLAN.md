---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 04
description: Reclassify font fixtures and separate retained from obsolete state
type: execute
wave: 4
depends_on: ["260726-vpu-03"]
autonomous: true
files_modified:
  - modules/mb-font/font/directory.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/generated_fonts.mbt
  - modules/mb-font/font/generated_fonts_wbtest.mbt
  - modules/mb-font/font/tables.mbt
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
must_haves:
  truths:
    - "Generated font builders compile only with white-box tests."
    - "Obsolete Phase 97 tracer chains and admission-only stored envelopes are removed without weakening validation."
    - "Font directory, required-table, and cmap facts needed by Phase 98 remain retained."
    - "Warning 7 is suppressed only on documented retained-state declarations."
    - "Font exact production/test/publication/directory inventories agree."
  artifacts:
    - path: modules/mb-font/font/generated_fonts_wbtest.mbt
      provides: deterministic private font fixtures
    - path: modules/mb-font/font/tables.mbt
      provides: validated retained cmap facts without dead name/post storage
    - path: policy/foundation.json
      provides: exact font source classification
  key_links:
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/tables.mbt
      via: Phase 98 retained directory/table/cmap state
      pattern: directory|tables|cmap
---

<objective>
Clear all 31 font diagnostics while preserving Phase 97 validation and Phase 98 input state.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Remove obsolete font state and reclassify test builders</name>
  <files>modules/mb-font/font/directory.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/generated_fonts.mbt -> generated_fonts_wbtest.mbt, modules/mb-font/font/tables.mbt</files>
  <action>
Move the complete builder to generated_fonts_wbtest.mbt and delete only dead `font_wb_put_i16`. Delete `TableWindow::length`, use `length_value` in its two tests, and delete obsolete directory tracer chain `font_find_table`/`font_table_checksum`/`font_source_checksum`/`validate_tracer_tables`. Delete obsolete head tracer `FontHeadFacts`/`font_table_source_offset`/`decode_head_table`. Remove stored Os2Facts.version. Convert name/post envelope validators to Result[Unit], preserve all checks, stop storing RequiredTableFacts.name/post, and remove only their value assertions.

Retain Font.directory, Font.tables, RequiredTableFacts.cmap, and CmapEnvelope.record_count for Phase 98. Add comments naming that consumer and apply `#warnings("-7")` only to the three declarations containing these fields.
  </action>
  <verify>Run `moon fmt` only on the owned mb-font files, then `moon check modules/mb-font/font --target all --deny-warn --frozen` and `moon test modules/mb-font/font --target all --deny-warn --frozen`.</verify>
  <done>Font code is warning-clean with validation preserved and next-phase state explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile exact font policy inventories</name>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <action>
Rename the publication entry; remove generated_fonts.mbt from production_sources; add generated_fonts_wbtest.mbt to test_sources; update exact publication, production, test, and directory arrays. Do not change the generated semantic interface, dependency edges, or other modules.
  </action>
  <verify>
Run exactly:

- `moon -C modules/mb-font info --target all --frozen`
- `pwsh -NoProfile -Command '. ./scripts/quality/Assert-Policy.ps1; Assert-FoundationPolicy -PolicyPath policy/foundation.json'`
- `moon check modules/mb-font/font --target all --deny-warn --frozen`
- `moon test modules/mb-font/font --target all --deny-warn --frozen`
- `git diff --check`

Require `rg -n '#warnings' modules/mb-font/font -g '*.mbt'` returns only the three documented warning-7 declarations.
  </verify>
  <done>Font filesystem, policy, compiler interface, and source classification agree exactly.</done>
</task>
</tasks>

<verification>
- Exactly 31 original font diagnostics are addressed.
- 27/27 font tests remain green on each target.
- Three and only three documented warning-7 declaration scopes exist.
</verification>

<success_criteria>
- No dead tracer/test utility remains in production classification.
- Phase 98 cmap/kern prerequisites remain available.
</success_criteria>
