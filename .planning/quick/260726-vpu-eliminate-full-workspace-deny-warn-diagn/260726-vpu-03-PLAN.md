---
quick_id: 260726-vpu
phase: quick-260726-vpu
plan: 03
description: Remove core dead code and deprecated APIs
type: execute
wave: 3
depends_on: ["260726-vpu-02"]
autonomous: true
files_modified:
  - modules/mb-core/math/bezier.mbt
  - modules/mb-core/text/utf8_wbtest.mbt
  - modules/mb-core/text/xml_escape.mbt
must_haves:
  truths:
    - "Zero-caller private derivative functions are deleted, not exported."
    - "UTF-8 exact bytes use equality rather than deprecated Array Show snapshots."
    - "XML numeric entities use current slicing APIs with unchanged decoding."
    - "mb-core passes all targets under deny-warn."
  artifacts:
    - path: modules/mb-core/math/bezier.mbt
      provides: dead-code-free curve primitives
    - path: modules/mb-core/text/xml_escape.mbt
      provides: current string slicing
  key_links:
    - from: modules/mb-core/text/utf8_wbtest.mbt
      to: modules/mb-core/text/utf8.mbt
      via: exact encoded byte arrays
      pattern: assert_eq
---

<objective>
Clear all five audited core diagnostics with behavior-preserving code/test updates.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Apply the audited core cleanup</name>
  <files>modules/mb-core/math/bezier.mbt, modules/mb-core/text/utf8_wbtest.mbt, modules/mb-core/text/xml_escape.mbt</files>
  <action>
Delete complete private `bezier2_derivative` and `bezier3_derivative` blocks. Replace all five UTF-8 Array snapshots with `assert_eq(actual, expected_bytes.to_array())`. Replace the two deprecated substring calls with `entity[2:entity.length()-1].to_string()` and `inner[1:].to_string()`. Do not change public APIs or warning configuration.
  </action>
  <verify>
Run this exact PowerShell fail-fast matrix:

`pwsh -NoProfile -Command '$ErrorActionPreference="Stop"; foreach($target in @("js","wasm","wasm-gc","native")) { foreach($file in @("modules/mb-core/math/bezier_wbtest.mbt","modules/mb-core/text/utf8_wbtest.mbt","modules/mb-core/text/xml_escape_wbtest.mbt")) { & moon test $file --target $target --deny-warn --frozen; if($LASTEXITCODE -ne 0){ throw "$file $target failed" } }; & moon check modules/mb-core --target $target --deny-warn --frozen; if($LASTEXITCODE -ne 0){ throw "mb-core check $target failed" } }'`

Then run `git diff --check`.
  </verify>
  <done>The five core diagnostics are absent and exact behavior passes four targets.</done>
</task>
</tasks>

<verification>
- No derivative definitions or deprecated substring calls remain.
- Five exact UTF-8 equality cases remain.
- Four-target scoped tests and mb-core checks pass with deny-warn.
</verification>

<success_criteria>
- Core warning cleanup has exactly three file changes and no suppression.
</success_criteria>
