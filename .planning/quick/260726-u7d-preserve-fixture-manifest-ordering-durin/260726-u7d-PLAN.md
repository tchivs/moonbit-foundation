---
quick_id: 260726-u7d
phase: quick-260726-u7d
plan: 01
description: Preserve fixture manifest ordering during PPM vector regeneration
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - "scripts/fixtures/Generate-PpmVectors.ps1"
must_haves:
  truths:
    - "PPM regeneration replaces its owned manifest record at its existing position and never moves foreign records."
    - "A duplicate PPM record fails with its exact ID; a missing PPM record is appended deterministically."
    - "The production merge helper has a no-write self-test for duplicate, missing, identity/order, and idempotence behavior."
    - "Two complete PPM generations are byte-idempotent and `-Check` passes."
    - "The Required IMAG generated-evidence sequence accepts both Image and PPM artifacts."
  artifacts:
    - path: "scripts/fixtures/Generate-PpmVectors.ps1"
      provides: "Position-preserving, idempotent PPM manifest record regeneration"
    - path: ".planning/quick/260726-u7d-preserve-fixture-manifest-ordering-durin/260726-u7d-SUMMARY.md"
      provides: "PPM self-test, idempotence, and focused IMAG evidence"
  key_links:
    - from: "scripts/fixtures/Generate-PpmVectors.ps1"
      to: "fixtures/manifest.json"
      via: "The owned PPM record is replaced in place through a production merge helper"
      pattern: "ppm-p6-conformance-vectors"
---

<objective>
Make PPM fixture regeneration preserve the shared manifest's record order.

Purpose: Required now passes CORE and COLR, then fails in IMAG because PPM regeneration extracts PPM and Color records and rebuilds a different order. Regeneration changes only manifest ordering; record data and digests are identical.

Output: One atomic PPM generator commit plus a verified quick summary.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@scripts/fixtures/Generate-PpmVectors.ps1
@scripts/fixtures/Generate-ImageVectors.ps1
@fixtures/manifest.json
@.planning/quick/260726-toy-preserve-fixture-manifest-ordering-durin/260726-toy-SUMMARY.md

At bd67ac0, Generate-ImageVectors is byte-clean. Generate-PpmVectors -Check alone reports fixtures/manifest.json stale. Regeneration removes the canonical PPM record from index 1 and reinserts it near the end while also moving SVG ahead of PPM/Color; no other file changes.

Execute only in the isolated codex/quick-260726-sss worktree. Do not touch dirty main, PNG work, sss summary/state, Phase 97, or historical worktrees.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the owned PPM manifest record in place</name>
  <files>scripts/fixtures/Generate-PpmVectors.ps1</files>
  <action>
Extract a production `Merge-PpmManifestRecord` helper accepting an in-memory existing record array and the canonical PPM record. Walk existing records in order, copy every foreign object through unchanged, replace the first PPM-owned record at the same index, and fail with the exact ID on a duplicate. Append the canonical PPM record only when absent. Remove the special Color extraction/reassembly logic; do not globally sort.

Add `-ManifestSelfTest`. It must use the production helper without file I/O and assert exact duplicate-ID rejection, deterministic missing append, preservation of foreign object identity/order/serialized values, replacement at the same index, and complete-input idempotence. Print `PPM manifest self-test passed.` only after all assertions.
  </action>
  <verify>
Run `pwsh -NoProfile -File ./scripts/fixtures/Generate-PpmVectors.ps1 -ManifestSelfTest` and require exit 0 plus the success marker.

From a disposable exact checkout, run this fail-fast hash matrix:

`pwsh -NoProfile -Command '$ErrorActionPreference="Stop"; $files=@("fixtures/manifest.json","modules/mb-image/ppm/generated_vectors.mbt"); function Snapshot { @($files | ForEach-Object { "$_=$((Get-FileHash -Algorithm SHA256 -LiteralPath $_).Hash)" }) }; $before=Snapshot; & pwsh -NoProfile -File ./scripts/fixtures/Generate-PpmVectors.ps1; if($LASTEXITCODE -ne 0){throw "first PPM generation failed"}; $first=Snapshot; if(Compare-Object $before $first){throw "first PPM generation changed canonical artifacts"}; & pwsh -NoProfile -File ./scripts/fixtures/Generate-PpmVectors.ps1; if($LASTEXITCODE -ne 0){throw "second PPM generation failed"}; $second=Snapshot; if(Compare-Object $first $second){throw "second PPM generation was not idempotent"}; & pwsh -NoProfile -File ./scripts/fixtures/Generate-PpmVectors.ps1 -Check; if($LASTEXITCODE -ne 0){throw "PPM artifact check failed"}'`

Require no diff in both artifacts and run `git diff --check`.
  </verify>
  <done>PPM generation is byte-idempotent and composable with every foreign manifest record.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the focused IMAG generator sequence and record completion</name>
  <files>.planning/quick/260726-u7d-preserve-fixture-manifest-ordering-durin/260726-u7d-SUMMARY.md</files>
  <action>
Run the exact focused sequence `Generate-ImageVectors.ps1 -Check`, then `Generate-PpmVectors.ps1 -Check`, then fixture-policy validation. Commit only Generate-PpmVectors.ps1 with `260726-u7d` in the subject. Write SUMMARY.md with `status: complete`, source commit, self-test, before/after hashes, two-pass idempotence, Image/PPM checks, and fixture-policy evidence. Do not commit summary or update STATE.
  </action>
  <verify>Both generator checks and Test-FixturePolicy.ps1 exit zero, the isolated source worktree contains only untracked SUMMARY after the source commit, and SUMMARY frontmatter is complete.</verify>
  <done>The exact IMAG generated-evidence sequence no longer rejects fixtures/manifest.json.</done>
</task>

</tasks>

<verification>
- PPM owned record stays at its original index; all foreign records keep identity and order.
- Duplicate and missing cases use the production no-write helper.
- Two generation passes and focused Image/PPM checks are clean.
- Fixture policy remains fail-closed and passes canonical data.
</verification>

<success_criteria>
- PPM `-Check` accepts the canonical manifest.
- PPM generation does not change canonical manifest or generated Moon bytes.
- Only the PPM generator source is committed.
</success_criteria>

<output>
After completion, create `.planning/quick/260726-u7d-preserve-fixture-manifest-ordering-durin/260726-u7d-SUMMARY.md`.
</output>
