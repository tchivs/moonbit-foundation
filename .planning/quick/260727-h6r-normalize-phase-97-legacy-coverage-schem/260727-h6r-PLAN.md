---
quick_id: 260727-h6r
phase: quick-260727-h6r
plan: 01
description: Normalize Phase 97 legacy coverage schema and close automated UAT
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [FONT-01]
files_modified:
  - ".planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md"
  - ".planning/phases/97-font-admission-and-metrics/97-UAT.md"
must_haves:
  truths:
    - "The three 97-01 coverage entries use the current id/description/requirement/verification/human_judgment schema and `uat.classify-coverage` reports all three as automated passes with no validation errors."
    - "Phase 97 UAT is complete with all nine tests recorded as automated passes and zero issues, pending tests, skipped tests, or blocked tests."
    - "The completed UAT remains grounded in the three plan summaries and the canonical passed 10/10 Phase 97 verification with behavior_unverified equal to zero."
    - "No production source, ROADMAP.md, REQUIREMENTS.md, canonical verification report, or unrelated worktree path changes."
  artifacts:
    - path: ".planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md"
      provides: "Current-schema automated coverage metadata for the three Plan 97-01 deliverables"
    - path: ".planning/phases/97-font-admission-and-metrics/97-UAT.md"
      provides: "Completed nine-test automated UAT record for Phase 97"
  key_links:
    - from: ".planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md"
      to: "uat.classify-coverage"
      via: "D1-D3 entries each provide a non-empty description and passing verification array with human_judgment false"
      pattern: "all_auto_covered.*true"
    - from: "uat.classify-coverage"
      to: ".planning/phases/97-font-admission-and-metrics/97-UAT.md"
      via: "The three newly classified 97-01 entries become automated UAT tests 1-3; existing 97-02 and 97-03 entries remain tests 4-9"
      pattern: "result: pass"
    - from: ".planning/phases/97-font-admission-and-metrics/97-VERIFICATION.md"
      to: ".planning/phases/97-font-admission-and-metrics/97-UAT.md"
      via: "Canonical passed status, 10/10 score, and zero behavior_unverified corroborate that no human-only checkpoint remains"
      pattern: "status: passed"
---

<objective>
Normalize the three legacy coverage entries in the Phase 97 Plan 01 summary, then close the existing Phase 97 UAT as a fully automated nine-test session.

Purpose: The implementation and canonical phase verification already pass, but the older 97-01 metadata lacks fields required by `uat.classify-coverage`, leaving three UAT tests pending for a schema-only reason.

Output: A current-schema 97-01 coverage block and a truthful completed `97-UAT.md`; the executor also creates the quick SUMMARY with atomic commit evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md
@.planning/phases/97-font-admission-and-metrics/97-02-SUMMARY.md
@.planning/phases/97-font-admission-and-metrics/97-03-SUMMARY.md
@.planning/phases/97-font-admission-and-metrics/97-VERIFICATION.md
@.planning/phases/97-font-admission-and-metrics/97-UAT.md

`97-VERIFICATION.md` is the canonical outcome record: status `passed`, score `10/10 must-haves verified`, `behavior_unverified: 0`, and no human verification required. Its reconciled Required-lane evidence records one exact success marker and sixteen exact `1048/1048` summaries. The untracked `97-UAT.md` is owned by this quick and currently has tests 1-3 pending only because 97-01 uses the older deliverable/result/evidence shape; tests 4-9 are already automated passes from 97-02 and 97-03.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize Plan 97-01 coverage metadata</name>
  <files>.planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md</files>
  <action>
Replace only the `coverage:` list in the YAML frontmatter of `97-01-SUMMARY.md`; preserve every other frontmatter field and the complete Markdown body byte-for-byte.

Express the three existing deliverables in the same current schema used by 97-02 and 97-03. Give them stable IDs `D1`, `D2`, and `D3`, set `requirement: FONT-01` and `human_judgment: false`, and use these descriptions in order:

1. `tchivs/mb-font` is a portable, independently consumable module with an opaque Font, explicit FontLimits, checked standalone TrueType admission, and units-per-em access.
2. A generated checksum-correct standalone TrueType font opens successfully and reports its exact units-per-em value.
3. Malformed, unsupported, over-limit, over-budget, and retained-source-mutated inputs fail closed deterministically without partial budget mutation.

For D1, record passing integration references to `moon -C modules/mb-font test --target all --frozen` and `moon -C modules/mb-font info --target all --frozen`. For D2, record the passing unit reference `modules/mb-font/font/font_test.mbt#generated standalone TrueType opens and returns exact units per em` plus the same all-target test command. For D3, record the passing unit reference `modules/mb-font/font/font_test.mbt and modules/mb-font/font/font_wbtest.mbt boundary suites` plus the same all-target test command. Each verification item must contain `kind`, `ref`, and `status: pass`.

These references re-express evidence already present in 97-01 and corroborated by the canonical Phase 97 verification; do not invent new commands, change historical counts, rerun or rewrite production tests, or modify 97-02/97-03 summaries. Run the classifier before committing and keep this metadata-only edit in one atomic task commit.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$ErrorActionPreference="Stop"; $tool="C:/Users/Admin/.codex/gsd-core/bin/gsd-tools.cjs"; $summary=".planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md"; $raw=(node $tool query uat.classify-coverage --summary $summary | Out-String); if ($LASTEXITCODE -ne 0) { throw "uat.classify-coverage exited $LASTEXITCODE" }; $coverage=$raw | ConvertFrom-Json; if ($coverage.mode -ne "coverage" -or $coverage.total -ne 3 -or -not $coverage.all_auto_covered -or @($coverage.auto_passed).Count -ne 3 -or @($coverage.present).Count -ne 0 -or @($coverage.errors).Count -ne 0) { throw "97-01 coverage did not classify as three clean automated passes" }; $ids=@($coverage.auto_passed | ForEach-Object { $_.id }); if (($ids -join ",") -ne "D1,D2,D3") { throw "Unexpected 97-01 coverage IDs: $($ids -join ",")" }; foreach ($entry in $coverage.auto_passed) { if ($entry.requirement -ne "FONT-01" -or $entry.human_judgment -ne $false -or @($entry.verification).Count -eq 0 -or @($entry.verification | Where-Object { $_.status -ne "pass" }).Count -ne 0) { throw "Invalid automated coverage entry $($entry.id)" } }; git diff --check -- $summary; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }'</automated>
  </verify>
  <done>`uat.classify-coverage` returns `total: 3`, `all_auto_covered: true`, D1-D3 in order, three auto-passed entries, and empty `present` and `errors` arrays; only the 97-01 frontmatter coverage block changed.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile and complete the nine-test Phase 97 UAT</name>
  <files>.planning/phases/97-font-admission-and-metrics/97-UAT.md</files>
  <action>
Rerun `uat.classify-coverage` separately for 97-01, 97-02, and 97-03 and require each summary to report exactly three automated passes, `all_auto_covered: true`, and no present entries or validation errors before editing the UAT.

Preserve the UAT frontmatter phase, source list, and original started timestamp. Set frontmatter `status: complete` and set `updated` to the actual UTC completion timestamp. Replace the Current Test body with `[testing complete]`. Change tests 1-3 to `result: pass`, `source: automated`, and `coverage_id: D1`, `D2`, and `D3` respectively; keep their names and expected behavior aligned with the normalized 97-01 descriptions. Retain tests 4-9 as automated passes with their existing D1-D3 coverage mappings and wording sourced from 97-02 and 97-03.

Set the Summary to exactly nine total and nine passed, with `issues`, `pending`, `skipped`, and `blocked` each equal to zero. Set Gaps to `[none]`. Do not represent any result as a human response: the source is the classifier-recognized passing verification metadata, corroborated by the unchanged canonical `97-VERIFICATION.md`. Do not modify that verification report, any production file, ROADMAP.md, REQUIREMENTS.md, or another planning artifact. Commit the completed UAT as the second atomic task commit; the quick orchestrator owns the quick SUMMARY and STATE update.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command '$ErrorActionPreference="Stop"; $tool="C:/Users/Admin/.codex/gsd-core/bin/gsd-tools.cjs"; foreach ($summary in @(".planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md",".planning/phases/97-font-admission-and-metrics/97-02-SUMMARY.md",".planning/phases/97-font-admission-and-metrics/97-03-SUMMARY.md")) { $raw=(node $tool query uat.classify-coverage --summary $summary | Out-String); if ($LASTEXITCODE -ne 0) { throw "Classifier failed for $summary" }; $coverage=$raw | ConvertFrom-Json; if ($coverage.total -ne 3 -or -not $coverage.all_auto_covered -or @($coverage.auto_passed).Count -ne 3 -or @($coverage.present).Count -ne 0 -or @($coverage.errors).Count -ne 0) { throw "Coverage is not fully automated for $summary" } }; $uatPath=".planning/phases/97-font-admission-and-metrics/97-UAT.md"; $uat=Get-Content -LiteralPath $uatPath -Raw; foreach ($required in @("(?m)^status: complete$","(?m)^source: 97-01-SUMMARY[.]md, 97-02-SUMMARY[.]md, 97-03-SUMMARY[.]md$","(?ms)^## Current Test\\s*\\r?\\n\\s*\\[testing complete\\]","(?m)^total: 9$","(?m)^passed: 9$","(?m)^issues: 0$","(?m)^pending: 0$","(?m)^skipped: 0$","(?m)^blocked: 0$","(?ms)^## Gaps\\s*\\r?\\n\\s*\\[none\\]")) { if ($uat -notmatch $required) { throw "UAT invariant missing: $required" } }; $results=[regex]::Matches($uat,"(?m)^result:\\s*(.+)$"); if ($results.Count -ne 9 -or @($results | Where-Object { $_.Groups[1].Value.Trim() -ne "pass" }).Count -ne 0) { throw "UAT does not contain exactly nine pass results" }; if ([regex]::Matches($uat,"(?m)^source: automated$").Count -ne 9 -or [regex]::Matches($uat,"(?m)^coverage_id: D[1-3]$").Count -ne 9) { throw "UAT automated sources or coverage IDs are incomplete" }; $verification=(node $tool query frontmatter.get .planning/phases/97-font-admission-and-metrics/97-VERIFICATION.md status | Out-String) | ConvertFrom-Json; if ($verification.status -ne "passed" -or $verification.score -ne "10/10 must-haves verified" -or [int]$verification.behavior_unverified -ne 0) { throw "Canonical Phase 97 verification is not passed 10/10 with zero unverified behavior" }; git diff --check -- .planning/phases/97-font-admission-and-metrics/97-01-SUMMARY.md $uatPath; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }'</automated>
  </verify>
  <done>All three summaries classify as three automated passes each; `97-UAT.md` has complete status, `[testing complete]`, nine automated pass results with coverage IDs, zero issues/pending/skipped/blocked, no gaps, and its source list remains truthful; canonical verification remains passed 10/10 with zero unverified behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUMMARY frontmatter -> UAT classifier | Hand-maintained YAML metadata is parsed as executable verification state by `uat.classify-coverage`. |
| Classifier output -> UAT session | Automated pass records determine whether the three formerly pending UAT cases can close without human judgment. |
| Dirty planning worktree -> atomic commits | The untracked UAT belongs to this quick while production, roadmap, requirement, verification, and unrelated files remain outside ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-H6R-01 | Tampering | 97-01 coverage normalization | high | mitigate | Restrict Task 1 to the existing coverage block, use D1-D3 and FONT-01, preserve the summary body, and require the classifier to return three clean automated passes. |
| T-H6R-02 | Repudiation | Verification references | medium | mitigate | Reuse only references already recorded in 97-01 and corroborated by the canonical verification report; retain explicit `kind`, `ref`, and `status` fields. |
| T-H6R-03 | Tampering | UAT completion counts | high | mitigate | Assert nine total/pass results, nine automated sources, nine D1-D3 mappings, zero non-pass counters, and the completed Current Test sentinel. |
| T-H6R-04 | Information Disclosure | Planning records | low | accept | The edited files contain no secrets or external user data; they record repository-local verification metadata only. |
| T-H6R-05 | Denial of Service | Scope drift into implementation or governance | medium | mitigate | Limit task ownership to 97-01-SUMMARY.md and 97-UAT.md and explicitly exclude production code, ROADMAP.md, REQUIREMENTS.md, and 97-VERIFICATION.md. |
</threat_model>

<source_audit>

| Source | ID | Feature / Requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | H6R-01 | Normalize the three legacy Phase 97 Plan 01 coverage entries | 1 | COVERED | Current D1-D3 schema and classifier assertions are explicit |
| GOAL | H6R-02 | Close automated Phase 97 UAT with all nine tests passing | 2 | COVERED | Completion status, automated sources, counts, and no-gap state are asserted |
| GOAL | H6R-03 | Preserve production, roadmap, requirements, and canonical verification artifacts | 1, 2 | COVERED | Only the two named planning records are task-owned |
| REQ | FONT-01 | Bounded static TrueType admission and stable font-wide/per-glyph metrics | 1, 2 | COVERED | Coverage entries retain FONT-01 and UAT records the already verified behavior |
| RESEARCH | — | No quick-task RESEARCH.md was provided | — | N/A | Existing current-schema summaries and live classifier output establish Level 0 discovery |
| CONTEXT | — | No quick-task CONTEXT.md or D-XX decisions were provided | — | N/A | The supplied quick constraints are represented by H6R-01 through H6R-03 |
</source_audit>

<verification>
1. Run `uat.classify-coverage` against 97-01 and require exactly three D1-D3 automated passes with no present/error records.
2. Run the same classifier against 97-02 and 97-03 and require three automated passes from each, giving nine classified automated cases in total.
3. Inspect `97-UAT.md` for complete status, `[testing complete]`, nine pass results, nine automated sources, nine D1-D3 mappings, and exact zero counts for issues, pending, skipped, and blocked.
4. Inspect `97-VERIFICATION.md` frontmatter and require canonical status `passed`, score `10/10 must-haves verified`, and `behavior_unverified: 0`.
5. Run `git diff --check` for the two owned files and confirm no production, ROADMAP, REQUIREMENTS, verification-report, or unrelated path is part of either task commit.
</verification>

<success_criteria>
- The 97-01 coverage block is current-schema metadata with stable D1-D3 IDs, FONT-01 mapping, evidence-grounded passing verification arrays, and no classifier validation failures.
- Phase 97 UAT is complete with all nine tests marked `pass` from automated sources and zero issues, pending, skipped, or blocked cases.
- The canonical Phase 97 verification remains passed at 10/10 with zero behavior left unverified.
- Only `97-01-SUMMARY.md` and the existing untracked `97-UAT.md` are changed by execution; production code and project scope ledgers are untouched.
</success_criteria>

<output>
After both atomic task commits succeed, create `.planning/quick/260727-h6r-normalize-phase-97-legacy-coverage-schem/260727-h6r-SUMMARY.md` with `status: complete`, both task commit hashes, classifier results for 97-01/02/03, final UAT counts, canonical verification status/score/behavior_unverified values, and explicit confirmation that no production, ROADMAP, or REQUIREMENTS file changed.
</output>
