---
name: implement-bounded-non-interlaced-8-bit-grayscale-alpha-png-decode
quick_id: 260721-6k0
created: 2026-07-21
phase: quick-260721-6k0
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
autonomous: true
requirements: [PNGX-01]
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/deflate_inflate.mbt
  - modules/mb-image/png/raster_decode.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - modules/mb-image/png/raster_decode_wbtest.mbt
  - modules/mb-image/png/png_test.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
must_haves:
  truths:
    - A valid bounded non-interlaced PNG with colour type 4 and bit depth 8 decodes through PngDecoder as straight RGBA8: each grayscale sample is replicated into red, green, and blue while its encoded alpha byte is preserved exactly.
    - Colour type 4 accepts no bit depth other than 8, uses exactly two filtered source bytes per pixel and filter bpp 2, and treats a tRNS chunk as invalid rather than as a replacement for native alpha.
    - Checked preflight, allocation, filtered-output, and work accounting reject limits and malformed type-4 transport before a DecodeResult image can be exposed; generated unsplit baselines and every nonempty contiguous two-IDAT boundary prove that accepted vectors preserve pixels and hostile vectors preserve the identical typed failure with no image result.
    - The fixture corpus is reconstructed by a PowerShell oracle independent of MoonBit decoder helpers, generates the MoonBit table, matches its manifest digest, and passes PNG tests on js, wasm, wasm-gc, and native.
  artifacts:
    - modules/mb-image/png/structural.mbt
    - modules/mb-image/png/raster_decode.mbt
    - modules/mb-image/png/deflate_inflate.mbt
    - scripts/fixtures/Generate-PngDecodeVectors.ps1
    - fixtures/png/decode-cases.json
    - modules/mb-image/png/generated_decode_vectors_test.mbt
  key_links:
    - IHDR colour-type/depth validation flows into PngStreamTransport descriptor and budget selection, so type-4 source stride is width times two while the exposed descriptor is straight RGBA8.
    - The live IDAT inflater and private eager raster helper use the same bpp-2 filter reconstruction and grayscale-alpha writer, preventing streamed and eager paths from disagreeing on predictor or alpha behavior.
    - The JSON cases are independently filtered and expanded by PowerShell before they become generated MoonBit vectors; generated boundary groups connect each hostile split to an unsplit typed-error baseline, and the SHA-256 record in fixtures/manifest.json makes fixture drift observable.
---

<objective>
Implement strict bounded decoding of non-interlaced, 8-bit PNG grayscale-with-alpha (colour type 4) into straight RGBA8, with portable generated evidence.

Purpose: complete this PNG interchange profile while preserving the decoder's checked resource, authenticated transport, atomic-result, and four-target guarantees.
Output: private type-4 preflight/transport/raster support plus independently generated fixtures and portable tests. The scope excludes release, publishing, configuration, QOI, low-bit type 4, palette changes, Adam7, 16-bit samples, colour management, and public streaming.
</objective>

## Scope coverage audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Bounded non-interlaced 8-bit grayscale-alpha PNG decode into straight RGBA8 | Tasks 1-2 |
| REQ | PNGX-01 strict eager RGB/RGBA PNG interchange | Tasks 1-2 |
| CONTEXT | No task-local CONTEXT.md; the explicit task fixes type 4 at depth 8, native-alpha semantics, resource bounds, generated evidence, and four portable targets | Tasks 1-2 |
| RESEARCH | Level 0: existing type-0/type-3 packed-row, type-6 RGBA, generated-fixture, and all-target PNG patterns are sufficient; no package or external integration is needed | Tasks 1-2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode legal type-4 scanlines through checked straight-RGBA transport</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png.mbt, modules/mb-image/png/deflate_inflate.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/structural_wbtest.mbt, modules/mb-image/png/raster_decode_wbtest.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - Type 4 accepts bit depth 8 only; bit depths 1, 2, 4, and 16 return the existing PNG bit-depth data error, and a tRNS chunk for type 4 returns a semantic PNG data error before decode success.
    - A source pair `(gray, alpha)` decodes to `(gray, gray, gray, alpha)` in a descriptor whose alpha mode is Straight; alpha is copied from the scanline and never derived from PngTransparency/tRNS state.
    - Each encoded type-4 row is checked as `width * 2` bytes and every PNG filter reconstructs bytes using bpp 2, so Sub, Average, and Paeth use the preceding complete gray-alpha pixel rather than one byte or four output channels.
    - Streamed IDAT consumption and `_png_write_raster` produce identical pixels and errors for all filters, multi-row predictors, invalid filter tags, truncated/decompression-invalid data, and generated type-4 IDAT boundary groups.
    - Exact and one-unit-short output/work/budget limits, dimensions, malformed IHDR/chunks, and type-4 tRNS rejection fail before a result image is visible; existing type-0/2/3/6 behaviors remain covered.
  </behavior>
  <action>First add focused white-box and public failure tests, then implement the smallest private transport and raster changes that make them pass. Extend PNG IHDR validation to recognize colour type 4 only at depth 8, derive two encoded source channels and four exposed channels, and retain those values in both eager and streamed transport so descriptor construction selects RGBA8 with straight alpha. Select or add a checked type-4 decode-budget path that computes `width * 2`, filtered row/output, exposed `width * height * 4` image storage, and work with checked arithmetic before allocation; it must use the established child-budget accounting and preserve decode atomicity. Ensure tRNS is rejected for colour type 4 rather than entering grayscale/RGB/indexed transparency handling. Add a private grayscale-alpha raster writer that expands the grayscale byte into RGB and writes the source alpha unchanged. Route both `_png_inflate_zlib_to_raster` and `_png_write_raster` through equivalent type-4 row reconstruction with filter bpp 2, including correct left/above/upper-left byte locations, without using output-channel count as the bpp. Test legal/depth-invalid IHDRs, all five filters, rows with changing alpha and gray values, cross-pixel predictors, descriptor alpha metadata, tRNS rejection, invalid filters, malformed compressed input, exact/below resource limits, no successful image result on failure, and regression coverage for the existing PNG profiles. Do not add public APIs, palette changes, low-bit type 4, Adam7, 16-bit, colour-management, QOI, or streaming work.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>Only legal non-interlaced 8-bit type-4 input reaches the private raster path; it has checked two-byte source-row/filter semantics, yields exact straight RGBA8 bytes, and all error paths return before image visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Generate independent grayscale-alpha vectors, table, and manifest evidence</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/png_test.mbt</files>
  <action>Extend the PNG decode corpus and generator schema with colour-type-4 depth-8 cases. Add accepted multi-row vectors that use all five filter tags, varying gray and alpha values, predictor-sensitive byte pairs, and exact RGBA8 expected pixels. For each canonical accepted zlib payload, generate one unsplit one-IDAT baseline and every nonempty contiguous two-IDAT split `(offset, payload_length - offset)` for offsets `1..payload_length-1`, then have the generated PNG test compare every split image's dimensions, channel count, and pixels with its unsplit baseline. Retain all existing corpus families. Add hostile type-4 cases for each illegal depth, tRNS presence/order/CRC failure, invalid filter bytes, plus at least two exhaustive-boundary hostile payloads: one malformed compressed stream and one limit-sensitive stream whose declared output/work/budget ceiling is crossed. For each exhaustive hostile payload, generate the same unsplit baseline and every nonempty contiguous two-IDAT split, attach a generated comparison-group identifier and explicit expected error category, code, and context, then make the public `PngDecoder` test decode the unsplit and every split independently. It must assert each hostile variant returns `Err` before any DecodeResult/image access, matches its declared typed error, and has the same category, code, and context as the unsplit baseline; do not treat a generic error outcome as sufficient evidence. Strengthen `Assert-Oracle` independently of MoonBit implementation: calculate source stride as `width * 2`, reconstruct current and previous source bytes with bpp 2 for each PNG filter, expand every pair to `gray, gray, gray, alpha`, and compare the independently produced RGBA bytes to the corpus expectation. It must not import, call, or duplicate MoonBit decoder helpers. Generate `generated_decode_vectors_test.mbt` from the corpus rather than editing it manually, update only the `png-decode-vectors` SHA-256 record in `fixtures/manifest.json`, and preserve fixture provenance plus unrelated records and PNG vectors.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image check --target all --deny-warn --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated></verify>
  <done>The independent PowerShell oracle, JSON corpus, generated MoonBit table, manifest check, and js/wasm/wasm-gc/native PNG suite establish the legal type-4 RGBA8 contract; every generated hostile malformed-compression and limit-sensitive boundary variant returns the same declared typed error as its unsplit baseline without yielding a DecodeResult image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted IHDR/tRNS to type-4 transport | Colour type, depth, source stride, and transparency-chunk legality determine allocations and alpha semantics. |
| compressed IDAT to filtered source pairs | Untrusted byte pairs and filters drive predictor state before grayscale expansion and alpha emission. |
| fixture JSON/generator to generated portable tests | Corpus declarations and oracle results become four-target conformance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260721-6k0-01 | Denial of service | type-4 preflight and allocation | high | mitigate | Apply checked source-row, image, filtered-output, and work calculations before child-budget allocation; prove exact and one-unit-short cases. |
| T-260721-6k0-02 | Tampering | filter reconstruction and hostile IDAT segmentation | high | mitigate | Reconstruct type-4 source bytes with bpp 2 in both eager and streamed paths; for generated accepted and hostile groups, compare every nonempty two-IDAT boundary against an unsplit baseline, including typed malformed-compression and resource-limit failures. |
| T-260721-6k0-03 | Information integrity | grayscale-alpha pixel emission | high | mitigate | Copy encoded alpha directly while expanding grayscale to RGB; reject type-4 tRNS so native alpha cannot be overridden or conflated. |
| T-260721-6k0-04 | Tampering | generated fixture evidence | medium | mitigate | Independently reconstruct filters and RGBA bytes in PowerShell, check the generated table, validate the manifest digest, and run all four targets. |
</threat_model>

<verification>
1. `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `moon -C modules/mb-image check --target all --deny-warn --frozen`
4. `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- Type-4 PNG accepts only non-interlaced depth-8 input and exposes exact straight RGBA8 pixels with the encoded alpha preserved.
- Its filtered rows are bounded and reconstructed as two-byte source pixels in both decoder paths; unsupported/malformed/over-limit input cannot yield an image.
- Independent fixture reconstruction, the generated table, manifest integrity, unsplit-baseline comparisons across every nonempty two-IDAT boundary for accepted and hostile type-4 payloads, and all four portable targets provide deterministic evidence.
</success_criteria>
