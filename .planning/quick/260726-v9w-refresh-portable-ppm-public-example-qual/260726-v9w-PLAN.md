---
quick_id: 260726-v9w
phase: quick-260726-v9w
plan: 01
description: Refresh portable PPM public example qualification evidence
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - "scripts/quality/Test-PublicExamples.ps1"
  - "release/qualification/example-consumers-schema.json"
must_haves:
  truths:
    - "The portable public-example audit requires exactly the eight unique public packages currently imported by examples/ppm-portable/main/moon.pkg."
    - "Workspace and copied-source runs require the Phase 12 strict-P6 output line on js, wasm, wasm-gc, and native."
    - "Qualification reports and their schema freeze portable digest 714923673 while retaining native digest 806175100."
    - "Portable expected output and digest have one canonical definition inside the qualification script."
    - "The example and all library sources remain unchanged."
  artifacts:
    - path: "scripts/quality/Test-PublicExamples.ps1"
      provides: "Current exact portable import, output, source-isolation, and report contract"
    - path: "release/qualification/example-consumers-schema.json"
      provides: "Current portable qualification digest contract"
    - path: ".planning/quick/260726-v9w-refresh-portable-ppm-public-example-qual/260726-v9w-SUMMARY.md"
      provides: "Four-target workspace and qualification evidence"
  key_links:
    - from: "examples/ppm-portable/main/main.mbt"
      to: "scripts/quality/Test-PublicExamples.ps1"
      via: "Exact Phase 12 semantic output and rolling digest"
      pattern: "714923673"
    - from: "scripts/quality/Test-PublicExamples.ps1"
      to: "release/qualification/example-consumers-schema.json"
      via: "Exact portable/native report digest assertions"
      pattern: "rolling257-mod1000000007"
---

<objective>
Synchronize QUAL-02 with the already verified Phase 12 portable PPM example without weakening public-import or source-isolation checks.

Purpose: Required reaches QUAL-02 but the qualification script still freezes the Phase 5 flip-only example. The live public example has legitimately imported `mb-core/io` since Phase 6 and `mb-image/model` since Phase 12, and emits the verified 29-byte strict-P6 pipeline identity.

Output: One atomic qualification-contract commit plus focused workspace, copied-source, schema/report, and full-Required evidence; no example or library change.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@scripts/quality/Test-PublicExamples.ps1
@release/qualification/example-consumers-schema.json
@examples/ppm-portable/main/moon.pkg
@examples/ppm-portable/main/main.mbt
@examples/ppm-native-cli/main/main.mbt
@.planning/milestones/v0.4-phases/12-strict-ppm-end-to-end-filter-coverage/12-01-PLAN.md
@.planning/milestones/v0.4-phases/12-strict-ppm-end-to-end-filter-coverage/12-VERIFICATION.md

Locked live facts:

- portable imports: mb-core budget/bytes/error/io and mb-image codec/model/ops/ppm;
- portable output: `example=portable bytes_read=38+29 bytes_written=29 width=3 height=2 crop rotate_90 rgb8_to_straight_rgba8 grayscale box_blur composite_source_over straight_rgba8_to_rgb8 digest=714923673 sha256=005700d6602b144bafcf3d869deee85619c8279c749bf33ca6fea8b43dbe78bf`;
- portable report digest: `rolling257-mod1000000007:714923673`;
- native output and report digest remain the independent Phase 5 values ending in `806175100`.

Run only in the isolated `codex/quick-260726-sss` worktree. Do not modify examples, modules, STATE, sss/qdh/Phase97, dirty main, or historical worktrees.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh and de-duplicate the exact portable qualification contract</name>
  <files>scripts/quality/Test-PublicExamples.ps1, release/qualification/example-consumers-schema.json</files>
  <action>
At script initialization define exact canonical constants for portable output, portable report digest, native output, and native report digest. Replace every repeated workspace/source-isolation output and report/round-trip digest literal with those constants.

Add a case-sensitive exact-set helper that rejects missing, extra, and duplicate imports with deterministic messages. Extend `Assert-PublicImports` with an explicit exact-set mode and enable it only for the portable example. Extend that portable expected set with `tchivs/mb-core/io` and `tchivs/mb-image/model`, requiring exactly the eight unique live packages. Preserve the generic unexpected-import rejection for existing non-exact callers; do not change native or QOI allowlists or their call semantics.

Add a top-level `-PublicImportContractSelfTest` switch which runs without example execution or report writes. It must call the production exact-set helper with the canonical eight-package array, require the positive case to pass, and require exact expected failures for: one missing import, a ninth import, and one duplicate import. Print `Public import contract self-test passed.` only after all cases pass.

Strengthen `Assert-QualificationSchema` so it verifies the schema's portable digest equals the canonical portable digest and its native digest equals the canonical native digest, in addition to the existing frozen outcome assertions. Update only the portable digest const in `release/qualification/example-consumers-schema.json` to `rolling257-mod1000000007:714923673`; keep schema version 1.0.0 and native digest unchanged.

Do not modify example source, example manifests, modules, registry behavior, source-isolation behavior, qualification outcome semantics, or compile-only semantics.
  </action>
  <verify>
Run the exact portable workspace audit on all four targets:

`pwsh -NoProfile -File ./scripts/quality/Test-PublicExamples.ps1 -Example portable -Mode workspace -Target all`

Require exit 0 and the exact Phase 12 line on js, wasm, wasm-gc, and native.

Run:

`pwsh -NoProfile -File ./scripts/quality/Test-PublicExamples.ps1 -PublicImportContractSelfTest`

Require exit 0 and the exact success marker, with the production helper proving missing, ninth, and duplicate import rejection.

Run qualification to an absolute GUID JSON file under the OS temp directory, using `try/finally` and `Remove-Item -LiteralPath` only for that exact file:

`pwsh -NoProfile -File ./scripts/quality/Test-PublicExamples.ps1 -Example all -Mode qualify -Target all -Report <absolute-temp-report>`

Require exit 0, `source_isolation: pass`, and `registry_resolution: blocked_unpublished_namespace`. Parse both the report and schema and require portable digest `714923673`, native digest `806175100`, source audit/isolation pass, and the registry blocked outcome. Require the temporary report is removed.

Run scoped searches proving the old portable output and old portable report digest no longer occur in the script, while the native constants still do. Run `git diff --check`.
  </verify>
  <done>QUAL-02 audits and executes the current portable example exactly, copied-source isolation uses the same canonical contract, and schema/report evidence cannot drift silently.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Required and record completion</name>
  <files>.planning/quick/260726-v9w-refresh-portable-ppm-public-example-qual/260726-v9w-SUMMARY.md</files>
  <action>
Commit only the two qualification-contract files with `260726-v9w` in the subject. From that commit, create a bounded GUID detached disposable worktree using the same containment, exact membership, leaf-regex, cleanliness, and `try/finally` rules as parent quick 260726-sss Task 3. Run the complete Required lane and require exit 0 plus `Required quality lane passed.`.

If another unrelated later gate fails, safely remove the clean detached worktree, report the exact first blocker, and do not claim this quick complete or create a complete SUMMARY.

On success, write SUMMARY.md with `status: complete`, the source commit, exact import/output/digest contract, four-target workspace results, copied-source/report/schema results, and detached Required evidence. Do not commit SUMMARY or edit STATE.
  </action>
  <verify>The source commit changes exactly two files, the full Required lane passes from the committed detached state, and only the requested untracked SUMMARY remains.</verify>
  <done>The current public PPM example is fully represented in release qualification and the repository Required lane passes.</done>
</task>

</tasks>

<verification>
- Portable import audit requires exactly the live eight-package public set; missing, ninth, and duplicate import fixtures fail.
- Workspace and copied-source portable executions match the exact Phase 12 output on four targets.
- Schema and report agree on portable digest 714923673 and native digest 806175100.
- Full Required passes from a clean detached commit.
- No example, module, unrelated planning, or user-owned path changes.
</verification>

<success_criteria>
- QUAL-02 no longer false-positives on the verified Phase 12 consumer.
- Qualification evidence is centralized and exact rather than weakened.
- Full Required lane passes, or the next exact unrelated blocker is reported without false completion.
</success_criteria>

<output>
After completion, create `.planning/quick/260726-v9w-refresh-portable-ppm-public-example-qual/260726-v9w-SUMMARY.md`.
</output>
