---
quick_id: 260726-r3a
phase: quick-260726-r3a
plan: 01
description: Normalize the exact WORK-04 MoonBit source and literate-document set for mb-core, mb-color, and mb-image, then verify all three modules
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - "modules/mb-core/**/*.mbt"
  - "modules/mb-core/**/*.mbt.md"
  - "modules/mb-color/**/*.mbt"
  - "modules/mb-color/**/*.mbt.md"
  - "modules/mb-image/**/*.mbt"
  - "modules/mb-image/**/*.mbt.md"
must_haves:
  truths:
    - "Every file selected by the Required lane's WORK-04 predicate under mb-core, mb-color, and mb-image is normalized by the pinned MoonBit formatter."
    - "Independent `moon fmt --check` runs pass for the exact 67 mb-core, 29 mb-color, and 83 mb-image files observed during planning."
    - "Checks and tests scoped to the explicit package directories of mb-core, mb-color, and mb-image exit zero across all supported targets, with zero test failures."
    - "The five pre-existing mb-core compiler warnings remain visible but are not promoted to errors or treated as formatting regressions; warning cleanup is outside this mechanical quick."
    - "The source commit contains only formatter changes to WORK-04-selected `.mbt` and `.mbt.md` files; manifests, other modules, paused governance edits, and unrelated/untracked paths are unchanged."
  artifacts:
    - path: "modules/mb-core"
      provides: "WORK-04-formatted mb-core MoonBit sources and README.mbt.md"
    - path: "modules/mb-color"
      provides: "WORK-04-formatted mb-color MoonBit sources and README.mbt.md"
    - path: "modules/mb-image"
      provides: "WORK-04-formatted mb-image MoonBit sources and README.mbt.md"
    - path: ".planning/quick/260726-r3a-normalize-moonbit-formatting-for-the-req/260726-r3a-SUMMARY.md"
      provides: "Quick execution record with formatter, format-check, module check/test, and atomic commit evidence"
  key_links:
    - from: "scripts/quality/Invoke-MoonQuality.ps1"
      to: "modules/mb-core"
      via: "WORK-04 recursively selects filenames matching `[.]mbt(?:[.]md)?$`; the task reuses that predicate for both formatting and checking"
      pattern: "WORK-04 independent format check"
    - from: "scripts/quality/Invoke-MoonQuality.ps1"
      to: "modules/mb-color"
      via: "The Required module list includes mb-color and the same source-only selection contract"
      pattern: "mb-core.*mb-color.*mb-image"
    - from: "scripts/quality/Invoke-MoonQuality.ps1"
      to: "modules/mb-image"
      via: "The Required module list includes mb-image and the same source-only selection contract"
      pattern: "mb-core.*mb-color.*mb-image"
    - from: "moon fmt"
      to: "moon fmt --check"
      via: "The identical sorted per-module source array is passed first to the mechanical formatter and then to the fail-closed check"
      pattern: "fmt.*--check"
---

<objective>
Normalize only the MoonBit source and literate-document files that WORK-04 checks in `modules/mb-core`, `modules/mb-color`, and `modules/mb-image`, then prove those modules remain valid across their supported targets.

Purpose: The paused Required-quality parent gate cannot advance while its source-only formatter check reports drift. This quick removes that drift without absorbing the parent's uncommitted governance work or expanding into manifests and unrelated modules.

Output: One atomic source commit containing only pinned-formatter changes to the exact WORK-04 set, followed by a quick summary recording passing format checks and independent module checks/tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@scripts/quality/Invoke-MoonQuality.ps1
@.planning/quick/260726-qdh-fix-required-quality-wrapper-lane-forwar/260726-qdh-PLAN.md

WORK-04 defines `$modules = @('mb-core', 'mb-color', 'mb-image')` and, independently for each module, recursively enumerates files whose names match `[.]mbt(?:[.]md)?$`, then invokes `moon fmt --check` with those absolute paths. Planning-time enumeration found 67, 29, and 83 selected files respectively. This task uses that live predicate as the canonical exact source list; `moon.mod.json`, `moon.pkg.json`, generated interface files, and every other suffix are outside it.

The parent quick is paused with tracked edits in `docs/governance/rfc-process.md`, `docs/rfcs/0001-moonbit-native-foundation.md`, and `docs/rfcs/README.md`, plus unrelated untracked paths. Those are context-only and are not owned, staged, committed, deleted, or rewritten by this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format and verify the exact Required WORK-04 module set</name>
  <precondition>The installed toolchain reports `moon 0.1.20260713 (75c7e1f)`, `moonc v0.10.4+2cc641edf`, and `moonrun 0.1.20260713 (75c7e1f)`; any current WORK-04-selected diffs are the mechanical formatter output already produced by this quick.</precondition>
  <files>modules/mb-core/**/*.mbt, modules/mb-core/**/*.mbt.md, modules/mb-color/**/*.mbt, modules/mb-color/**/*.mbt.md, modules/mb-image/**/*.mbt, modules/mb-image/**/*.mbt.md</files>
  <action>Capture `git status --short` and the tracked diff for every path outside the WORK-04 selection so the paused governance state can be compared after verification; retain the current mechanical formatter output if this resumed execution has already produced it. For each module in the exact order `mb-core`, `mb-color`, `mb-image`, reproduce the WORK-04 enumeration with `Get-ChildItem -LiteralPath "modules/$module" -Recurse -File`, filter filenames with `[.]mbt(?:[.]md)?$`, convert to absolute paths, and sort the array. Fail closed if a module is empty, if the planning-time counts are no longer 67/29/83, or if any selected path falls outside its named module. Pass each module's complete array as explicit arguments following `moon fmt`; use the formatter command for these bulk mechanical edits and make no hand-authored semantic changes. Do not run unscoped `moon fmt`, because the pinned toolchain would propose the deliberately deferred manifest migration. After formatting, pass the same arrays to `moon fmt --check`. For compile/test evidence, enumerate only the directories beneath the current module that contain `moon.pkg` or `moon.pkg.json`, sort them uniquely, fail if that package list is empty, then pass those explicit directory paths to `moon check --target all --frozen` and `moon test --target all --frozen`. Do not use `--deny-warn`: mb-core has five pre-existing warnings whose cleanup is not part of a source-formatting quick, and the explicit package paths prevent an unscoped check from scanning other repository modules. Require both scoped commands to exit zero and the test command to report zero failures. Confirm `git status --short -- modules/mb-core modules/mb-color modules/mb-image` names only files accepted by the WORK-04 suffix predicate, `git diff --check` passes for the three owned module roots, and the captured outside-scope status and tracked diff are byte-identical to their baselines. Leave all manifest files, other modules, parent-governance documents, planning ledgers, and unrelated/untracked paths untouched. Keep the formatter changes in one task so the executor creates one atomic source commit containing only changed files from the selected arrays.</action>
  <verify>
    <automated>pwsh -NoProfile -Command '$ErrorActionPreference="Stop"; $versions=((& moon version --all) -join "`n"); foreach ($required in @("moon 0.1.20260713 (75c7e1f 2026-07-13)","moonc v0.10.4+2cc641edf (2026-07-15)","moonrun 0.1.20260713 (75c7e1f 2026-07-13)")) { if (-not $versions.Contains($required)) { throw "Pinned toolchain identity mismatch: $required" } }; $expected=@{"mb-core"=67;"mb-color"=29;"mb-image"=83}; foreach ($module in @("mb-core","mb-color","mb-image")) { $files=@(Get-ChildItem -LiteralPath "modules/$module" -Recurse -File | Where-Object { $_.Name -match "[.]mbt(?:[.]md)?$" } | ForEach-Object { $_.FullName } | Sort-Object); if ($files.Count -ne $expected[$module]) { throw "WORK-04 inventory drift for $module`: expected $($expected[$module]), got $($files.Count)" }; & moon fmt --check @files; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $packages=@(Get-ChildItem -LiteralPath "modules/$module" -Recurse -File | Where-Object { $_.Name -in @("moon.pkg","moon.pkg.json") } | ForEach-Object { $_.Directory.FullName } | Sort-Object -Unique); if ($packages.Count -eq 0) { throw "No explicit package directories found for $module verification" }; & moon check --target all --frozen @packages; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon test --target all --frozen @packages; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; $moduleStatus=@(git status --short -- modules/mb-core modules/mb-color modules/mb-image); foreach ($line in $moduleStatus) { $path=($line.Substring(3).Trim() -split " -> ")[-1].Replace("\","/"); if ($path -notmatch "^modules/(?:mb-core|mb-color|mb-image)/.+[.]mbt(?:[.]md)?$") { throw "Out-of-scope module change: $path" } }; git diff --check -- modules/mb-core modules/mb-color modules/mb-image; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }'</automated>
  </verify>
  <done>The pinned toolchain identity and unchanged 67/29/83 inventories are proven; all three exact format checks and all explicit-package all-target check/test commands exit zero with zero test failures; the five pre-existing mb-core warnings are documented as a non-goal rather than promoted to errors; the executor's atomic task commit contains only mechanically formatted WORK-04 source/document paths; and the paused governance state plus every unrelated path remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pinned `moon fmt` -> repository sources | A tool capable of broad rewrites receives an explicitly constrained source-file array. |
| Dirty worktree -> task commit | Owned mechanical source changes coexist with paused governance edits and unrelated untracked paths. |
| Formatted sources -> module verification | Mechanical text rewrites must preserve compilation and test behavior across supported targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-R3A-01 | Tampering | Formatter input scope | high | mitigate | Reuse WORK-04's exact recursive filename predicate per module, assert the observed inventory counts, and never invoke the formatter without explicit paths. |
| T-R3A-02 | Tampering | Module manifests and unrelated modules | high | mitigate | Reject any module-root status entry outside the selected suffixes and compare outside-scope status/diff baselines before the task commit. |
| T-R3A-03 | Repudiation | Atomic source commit and summary | medium | mitigate | Keep all edits in one task, stage only changed members of the selected arrays, and record the exact formatter/check/test evidence in the quick summary. |
| T-R3A-04 | Denial of Service | Cross-target module regression | medium | mitigate | Pass only each module's explicit package directories to all-target check and test commands, require exit zero and zero test failures, and do not conflate the five existing mb-core warnings with formatter regressions. |
</threat_model>

<source_audit>

| Source | ID | Feature / Requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | R3A-01 | Normalize MoonBit formatting for the Required WORK-04 module set | 1 | COVERED | Pinned mechanical formatter receives the same per-module source predicate as WORK-04 |
| GOAL | R3A-02 | Verify mb-core, mb-color, and mb-image | 1 | COVERED | Exact format check plus explicit-package all-target check/test for every named module; existing mb-core warnings are a documented non-goal |
| GOAL | R3A-03 | Preserve paused governance docs and unrelated worktree paths | 1 | COVERED | Outside-scope baseline comparison and selected-array-only atomic commit |
| REQ | — | No ROADMAP requirement IDs apply to this quick task | — | N/A | Quick workflow is outside phase requirement mapping |
| RESEARCH | — | No quick-task RESEARCH.md was provided | — | N/A | Existing WORK-04 implementation and pinned local toolchain establish Level 0 discovery |
| CONTEXT | — | No quick-task CONTEXT.md or D-XX decisions were provided | — | N/A | The supplied quick constraints are fully represented by R3A-01 through R3A-03 |
</source_audit>

<verification>
1. Confirm the formatter and checker use the identical sorted arrays produced by WORK-04's module-local `.mbt` / `.mbt.md` filename predicate.
2. Confirm the observed inventory remains exactly 67 mb-core, 29 mb-color, and 83 mb-image files, with zero manifest or other-module paths.
3. Run `moon fmt --check` independently for each array, then pass only the current module's explicit package directories to `moon check --target all --frozen` and `moon test --target all --frozen`; require exit zero and zero test failures without elevating the five pre-existing mb-core warnings.
4. Confirm the one task commit contains only selected source/document files and that the parent governance diff, untracked-path inventory, and all other repository state are unchanged.
</verification>

<success_criteria>
- The exact WORK-04 formatter checks for mb-core, mb-color, and mb-image exit zero under the pinned toolchain.
- Each required module's explicit package set passes all-supported-target checks and tests with frozen dependencies, exit code zero, and zero test failures; the five pre-existing mb-core warnings are recorded as outside the mechanical-formatting goal.
- No manifest, generated interface, other module, governance document, planning ledger, or unrelated/untracked path is modified or included in the source commit.
- One atomic source commit and `260726-r3a-SUMMARY.md` provide reproducible evidence for the paused parent gate.
</success_criteria>

<output>
Create `.planning/quick/260726-r3a-normalize-moonbit-formatting-for-the-req/260726-r3a-SUMMARY.md` with `status: complete`, the atomic source commit hash, the exact per-module inventory counts, the successful formatter/explicit-package check/test commands, and the five pre-existing mb-core warnings recorded as a non-goal after Task 1 succeeds.
</output>
