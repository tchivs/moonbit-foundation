# v0.19 GrayAlpha8 Adam7 PNG — Milestone Integration Audit

**Audited:** 2026-07-23  
**Scope:** Phases 59–61; `GRAYA8A7-01`, `GRAYA8A7-02`, and `GRAYA8A7-03`  
**Integration score:** **100/100 functional wiring** — 0 blockers, 1 documentation warning.

## Integration Check Complete

### Wiring Summary

**Connected:** 8 required cross-phase links verified.  
**Orphaned:** 0 phase-owned public exports.  
**Missing:** 0 expected runtime connections.

| From | To | Verified connection | Status |
|---|---|---|---|
| Phase 59 eager factories | Shared eager adapter | `PngEncoder::new_graya8_with_interlace_strategy` delegates to the all-strategy factory, preserving `GrayAlpha8` and selected interlace until `PngEncoder::encode` constructs `PngEncodeMachine::new_with_profile`. | WIRED |
| Phase 59 chunk factories | Shared chunk machine | `PngChunkEncoder::new_graya8_with_interlace_strategy` delegates to its all-strategy factory, which directly constructs `PngEncodeMachine::new_with_profile(... GrayAlpha8 ...)`. | WIRED |
| Eager and chunk facades | One preflight ledger | The two entry routes both call `_png_encode_preflight_with_interlace_profile` through `new_with_profile`; no GrayAlpha8-specific encoder/machine exists. | WIRED |
| GrayAlpha8 profile | Adam7 traversal and wire bytes | Profile admission returns two U8 channels; Adam7 coordinates call `_png_wire_byte`, whose U8 branch reads `position % channels`, yielding source-order `G,A`. | WIRED |
| Adam7 traversal | Adaptive filtering | `PngFilteredCursor::next` resolves a pass-local `(pass,row)` and the predictor reads only the current pass's preceding row. | WIRED |
| Phase 60 replay guard | Caller lease emission | `PngChunkEncoder::pull` calls `validate_replay_revision()` before its first reachable `destination.set`; failure becomes sticky `Failed` with zero writes. | WIRED |
| Phase 61 public wire result | Independent public decode | The non-symmetric Stored/None public output is compared with an independently enumerated seven-pass raster, then decoded by `ImageDecoder::decode(PngDecoder::new(), ...)`; the Type-4 decoder maps two-source-channel rows to `(G,G,G,A)`. | WIRED |
| Phase 61 hostile schedules | All six public selector pairs | Fresh eager/chunk peers cover Stored, FixedOrStored, DynamicOrFixedOrStored × None, Adaptive under zero, one-byte, and ragged leases. | WIRED |

### API Coverage

There are no HTTP routes in this portable library milestone. The relevant public API is the additive GrayAlpha8 Adam7 factory surface.

| Public contract | Consumer evidence | Status |
|---|---|---|
| `PngEncoder::new_graya8_with_interlace_strategy` | Public narrow-selector Type-4/8/pass-wire test | CONSUMED |
| `PngEncoder::new_graya8_with_all_strategies` | Six-pair framing, independent wire/decode, atomic-admission, and eager peer tests | CONSUMED |
| `PngChunkEncoder::new_graya8_with_interlace_strategy` | Public narrow-selector chunk parity test | CONSUMED |
| `PngChunkEncoder::new_graya8_with_all_strategies` | Six-pair parity, atomic admission, replay mutation, and hostile-lease matrix | CONSUMED |

### Auth Protection

Not applicable: this is a portable MoonBit image library with no routes, sessions, or authentication boundary.

### E2E Flows

**Complete:** 4  
**Broken:** 0

1. **Explicit eager Adam7 encode:** public eager factory → legal GrayAlpha8 profile admission → shared preflight → seven-pass `G,A` filtered traversal → selected compression → Type-4/8 Adam7 PNG. The independent 61-byte all-pass oracle and framing checks are wired into the public factory test.
2. **Caller-buffered Adam7 encode:** public chunk factory → same shared machine/preflight → `pull` → caller lease write/acknowledge → accepted-only totals → finished stream identical to a separately fresh eager peer.
3. **Mutation-safe bounded replay:** acknowledged prefix → caller source mutation → pre-lease revision guard → zero-write sticky plan-specific error on this and later leases. This is exercised for every compression/filter pair.
4. **Public wire-to-decode fidelity:** explicit Stored/None Adam7 encoder → independent literal pass-raster extraction → public `PngDecoder` / `ImageDecoder` → 25 coordinate-derived canonical RGBA8 assertions.

### Detailed Findings

#### Orphaned Exports

None.

#### Missing Connections

None.

#### Broken Flows

None.

#### Unprotected Routes

Not applicable.

#### Warnings

- **WARNING — requirements traceability is stale.** `.planning/REQUIREMENTS.md` checks all three GRAYA8A7 requirements as complete, but its Traceability table still labels `GRAYA8A7-01`, `-02`, and `-03` “Not started.” This does not break code or the user flows, but it makes milestone status reporting internally inconsistent. Affects: `GRAYA8A7-01`, `GRAYA8A7-02`, `GRAYA8A7-03`.

#### Scope / Anti-Pattern Audit

- No staging buffer, second encoder, target wrapper, copied source tree, FFI, release, or registry change was introduced. Phase 59–60 production changes are limited to the existing PNG files; Phase 61 changes only public-evidence tests and planning records.
- `PngEncodeMachine` is the only owned output state. Its source, scalar cursors, fixed/dynamic replay state, and comments explicitly exclude output-sized staging.
- `modules/mb-image/png/moon.pkg` and `modules/mb-image/moon.mod.json` declare `+js+wasm+wasm-gc+native`; Phase 61 records `moon -C modules/mb-image test png --target all --frozen` passing 227 tests on each target.

### Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| GRAYA8A7-01 | Phase 59 eager/chunk public selectors → `PngEncodeMachine::new_with_profile(GrayAlpha8)` → profile admission → `_png_adam7_passes` / `_png_wire_byte` → Type-4/8 IHDR and independent `G,A` pass-raster test | WIRED | Requirements Traceability table is stale. |
| GRAYA8A7-02 | Phase 59 shared machine → Phase 60 profile-neutral preflight/replay guard → `PngChunkEncoder::pull` before `destination.set` → six-pair atomic-admission and sticky replay tests | WIRED | Requirements Traceability table is stale. |
| GRAYA8A7-03 | Phase 60 public all-strategy path → Phase 61 independent wire extractor → public `PngDecoder` canonical RGBA decode; fresh hostile schedules; frozen vectors; four-target PNG package declaration and recorded qualification | WIRED | Requirements Traceability table is stale. |

**Requirements with no cross-phase wiring:** None.

### Runtime Evidence Note

Audit-time rerunning of the complete `--target all` command was not possible because the workspace `_build/.moon-lock` (timestamp 10:17) is held while a pre-existing `moon` process from 2026-07-22 remains active. Its command line is unavailable, so this audit did not terminate a process it does not own. This is an environment limitation, not a product-flow failure. The Phase 61 verification records the exact unwrapped command passing 227 PNG tests on js, wasm, wasm-gc, and native; the source/package wiring above independently confirms the four-target path.

### Audit Verdict

The phases are not silos. The additive Phase 59 public factories reach the Phase 60 shared bounded profile/machine and replay guard, and Phase 61 drives that public path through independent wire/decode, hostile lease, frozen-vector, and portable-target evidence. No cross-phase integration blocker was found.
