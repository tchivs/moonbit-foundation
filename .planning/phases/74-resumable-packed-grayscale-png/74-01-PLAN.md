---
phase: 74-resumable-packed-grayscale-png
plan: 01
type: execute
wave: 1
depends_on: [73-01]
files_modified:
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYPACK-03
must_haves:
  truths:
    - "Callers can select `PngChunkEncoder::new_gray1`, `new_gray2`, or `new_gray4` and obtain the matching eager packed Type-0 PNG bytes through caller-owned leases."
    - "Each low-bit chunk selector remains fixed to Stored DEFLATE, filter None, and non-interlaced output while using the sole bounded PNG machine."
    - "For every packed depth, zero, one-byte, and ragged caller capacities preserve accepted-byte accounting and lease-tail ownership."
    - "Invalid inputs and resource budgets reject before a lease exists or budget state changes, and a released lease yields the same sticky typed terminal without changing a later lease."
  artifacts:
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Explicit fixed-profile Gray1, Gray2, and Gray4 caller-buffered factories.
      exports: ["PngChunkEncoder::new_gray1", "PngChunkEncoder::new_gray2", "PngChunkEncoder::new_gray4"]
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public all-depth eager-parity, atomic-admission, and released-lease lifecycle evidence.
  key_links:
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: Each new factory passes its existing private packed profile to PngEncodeMachine::new_with_profile before constructing Active state.
      pattern: "PngEncodeProfile::Gray(1|2|4)"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: Public chunk drains compare bytes from each new selector with its matching public eager selector across hostile leases.
      pattern: "PngChunkEncoder::new_gray(1|2|4)"
---

<objective>
Deliver GRAYPACK-03 and D-01 through D-05: expose the three explicit packed-grayscale caller-buffered selectors through the established bounded encoder, then prove their public lease behavior matches Phase 73 eager output.

Purpose: make the already-admitted low-bit grayscale profile available to callers who own the output buffers without changing PNG packing or transport architecture.
Output: three direct chunk factories and focused all-depth lifecycle coverage in the existing PNG stream tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-resumable-packed-grayscale-png/74-CONTEXT.md
@.planning/phases/74-resumable-packed-grayscale-png/74-RESEARCH.md
@.planning/phases/73-explicit-packed-grayscale-png/73-01-PLAN.md
@.planning/phases/73-explicit-packed-grayscale-png/73-01-SUMMARY.md
@.planning/phases/73-explicit-packed-grayscale-png/73-VERIFICATION.md
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Establish the Gray1 caller-buffered path through the existing machine</name>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/stream_encode.mbt:18-96 — public fixed Gray8/Gray16 selector shape; use the direct `new_with_profile` body rather than a strategy overload.
    - modules/mb-image/png/stream_encode.mbt:542-622 and 767-855 — the shared pull/acknowledgement state and atomic profile-aware construction seam that remain unchanged.
    - modules/mb-image/png/stream_encode_test.mbt:755-806, 1040-1088, and 1721-1811 — owner/drain, public eager oracle, and hostile-capacity patterns.
  </read_first>
  <behavior>
    - RED: a width-9 canonical Gray/U8 source of legal one-bit levels completes through `PngChunkEncoder::new_gray1` with bytes equal to `PngEncoder::new_gray1` under a zero-prefixed one-byte schedule.
    - RED: an empty lease makes zero progress and leaves its sentinel byte unchanged; accepted-byte totals and every unaccepted sentinel tail remain intact while draining.
    - GREEN: a completed Gray1 encoder returns `Finished` with zero writes on a later sentinel lease.
  </behavior>
  <action>Per D-01 and D-02, add only `PngChunkEncoder::new_gray1` near the explicit Gray8 selector. Construct `PngEncodeMachine::new_with_profile(source, PngEncodeProfile::Gray1, PngCompressionStrategy::Stored, PngFilterStrategy::None, PngInterlaceStrategy::None, limits, budget, diagnostics)` first, propagate its error directly, then create the existing `Active(machine)` state with total zero. Do not alter `PngChunkEncoder::pull`, `PngEncodeMachine::new_with_profile`, private profiles, row providers, source models, or packing logic: the constructor call must retain shared admission, accounting, and transport semantics (D-02, D-03). Add the smallest public Gray1 fixture and eager helper required by the stream tests. Drive a fresh encoder through an empty lease followed by `[0UL, 1UL]`, compare emitted bytes only with the public eager Gray1 result, and assert per-pull capacity, running totals, untouched lease tails, and the zero-write Finished replay lease (D-04). Keep the test oracle at the public eager/chunk boundary; do not invoke packing helpers.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>`new_gray1` is a direct Stored/None/non-interlaced factory for the established Gray1 profile, and public hostile-lease output is eager-identical with unchanged unaccepted and terminal leases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Gray2/Gray4 and prove all-depth admission and sticky terminals</name>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/stream_encode.mbt — Task 1 Gray1 factory and the unchanged direct Gray16 factory template.
    - modules/mb-image/png/stream_encode_test.mbt:63-110, 755-806, 1721-1811, 2797-2849, and 3069-3100 — canonical Gray/U8 fixtures, hostile drains, typed-error comparison, and released-lease replay assertions.
    - modules/mb-image/png/encode_test.mbt:1302-1395 — Phase 73's independent wire and exact-level cases; retain that qualification boundary rather than copying its wire parser here.
  </read_first>
  <behavior>
    - RED: canonical Gray2 width-5 and Gray4 width-3 sources match their respective eager selectors under `[0UL, 1UL]`, `[1UL]`, and the established ragged schedule, with accepted-byte totals and lease-tail sentinels preserved.
    - RED: Gray1 sample `0x01`, Gray2 sample `0x01`, and Gray4 sample `0x02` each reject from their chunk factory before a lease can be pulled; every budget resource field equals its snapshot. Legal all-depth sources with an insufficient work budget also leave every budget resource field unchanged.
    - RED: releasing the first one-byte lease on each valid low-bit encoder gives zero writes and a typed failure; a later sentinel lease receives zero writes and the same category, code, operation, context, requested, completed, and limit.
    - GREEN: existing generic, Gray8, Gray16, GrayAlpha, RGBA16, and Phase 73 eager tests still pass.
  </behavior>
  <action>Per D-01 and D-02, add only `PngChunkEncoder::new_gray2` and `PngChunkEncoder::new_gray4`, each using the exact direct fixed-profile construction established in Task 1 with its matching existing profile and Stored/None/non-interlaced tuple. Do not expose compression, filter, interlace, generic, or transport variants, and leave the shared pull machine and Phase 73 packing/preflight implementation intact (D-01, D-02, D-05). Expand the public stream tests with exact canonical Gray/U8 fixtures and matching eager helpers for depths 2 and 4. For each of depths 1, 2, and 4, use fresh chunk encoders for zero-prefixed-one-byte, one-byte-only, and ragged schedules; assert eager byte parity, `written <= capacity`, `total_written == bytes previously accepted + written`, and that every lease byte after `written` stays sentinel-valued (D-03, D-04). Add all-depth pre-lease rejection cases for the stated nonrepresentable samples and separate valid-source insufficient-budget cases; snapshot and compare every `ResourceLimits` field, and assert no chunk instance is used to obtain a lease. For every depth, release a first one-byte lease before pulling, replay on a new sentinel lease, and compare the complete typed error with `png_chunk_test_same_error` while both leases remain unchanged (D-03, D-04). Keep independent packed-wire/decode vectors and the final qualification audit outside this plan per D-05.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Exactly three low-bit chunk factories exist; all depths preserve eager parity and caller lease ownership under hostile capacities, fail invalid and budget admission without budget change or lease exposure, and replay released-lease terminals unchanged.</done>
</task>

</tasks>

<source_audit>

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Caller-owned low-bit output with existing atomic and sticky semantics | 74-01 | COVERED | Tasks 1-2 bind existing profiles to the established machine and prove public lifecycle behavior.
REQ | GRAYPACK-03 | Bounded-machine low-bit chunks, eager-identical hostile bytes, lease ownership, sticky typed terminals | 74-01 | COVERED | Task 1 proves one complete Gray1 slice; Task 2 covers all depths, admission, and terminal replay.
RESEARCH | — | Direct fixed-profile factory only | 74-01 | COVERED | Both tasks require `new_with_profile` with the locked Stored/None/non-interlaced tuple.
RESEARCH | — | Existing preflight, provider, pull, and terminal cache remain authoritative | 74-01 | COVERED | Task actions name these seams as unchanged and test their public results.
RESEARCH | — | Public eager parity plus hostile leases, atomic admission, and released lease evidence | 74-01 | COVERED | Test behaviors cover every prescribed schedule and terminal condition.
CONTEXT | D-01 | Only `new_gray1`, `new_gray2`, `new_gray4`; no public strategy/generic/new transport | 74-01 | COVERED | Tasks cite D-01 and restrict the factory surface.
CONTEXT | D-02 | Phase 73 profiles and `new_with_profile`; no staging, provider, model, or machine change | 74-01 | COVERED | Tasks cite D-02 and retain the existing construction seam.
CONTEXT | D-03 | Atomic admission and exact lease ownership/sticky terminals | 74-01 | COVERED | Tasks cite D-03 and prescribe budget, lease-tail, and replay checks.
CONTEXT | D-04 | All-depth eager identity, hostile capacities, atomic rejection, typed terminal evidence | 74-01 | COVERED | Tasks cite D-04 with fixed fixtures and public-oracle tests.
CONTEXT | D-05 | Defer strategy/interlace/palette/model/qualification scope | 74-01 | COVERED | Tasks cite D-05 and exclude those additions.

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller Gray/U8 source → profile-aware factory | Caller-controlled samples, descriptors, limits, and budget enter preflight before an encoder value or lease exists. |
| Bounded machine → caller-owned byte lease | The shared machine previews bytes; the chunk adapter writes and acknowledges only bytes accepted by the lease. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01 | Tampering / Denial of service | `new_gray1/2/4` admission | high | mitigate | Construct the existing profile-aware machine before Active state and test invalid levels plus insufficient budgets for unchanged resource limits and absent lease exposure. |
| T-74-02 | Tampering | `PngChunkEncoder::pull` lease handoff | medium | mitigate | Reuse the sole pull path; all-depth hostile schedules assert capacity bounds, accepted totals, eager parity, and sentinel tails. |
| T-74-03 | Repudiation / Tampering | cached `Failed` terminal | medium | mitigate | Released-lease tests compare complete typed errors across first and replay pulls and assert neither caller lease mutates. |
| T-74-SC | Tampering | package installation | low | accept | The plan installs no dependencies or external package. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target all --frozen`. The ordinary PNG package suite must pass with all-depth public eager-parity, pre-lease atomicity, and released-lease terminal cases included.
</verification>

<success_criteria>
- `PngChunkEncoder::new_gray1`, `new_gray2`, and `new_gray4` are the only added public low-bit chunk APIs and each binds Stored/None/non-interlaced output to the matching Phase 73 profile.
- For legal canonical Gray/U8 sources, every depth emits exactly the matching eager selector bytes under zero-prefixed, one-byte, and ragged capacities without changing unaccepted lease bytes.
- Invalid selected samples and insufficient resource budgets leave all resource-limit fields unchanged before a caller lease can be exposed.
- A released lease produces a zero-write sticky typed failure at every depth and cannot alter a later caller-owned sentinel lease.
</success_criteria>

<output>
Create `.planning/phases/74-resumable-packed-grayscale-png/74-01-SUMMARY.md` when done.
</output>
