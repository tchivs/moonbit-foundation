---
phase: 38-adaptive-filter-compatibility
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - policy/foundation.json
autonomous: true
requirements: [PNGF-01]
must_haves:
  truths:
    - "A library user can choose PngFilterStrategy::Adaptive through documented eager and caller-buffered factories."
    - "PngEncoder::new(), PngChunkEncoder::new(...), and every existing compression-strategy factory retain their exact pre-Phase-38 filter-None PNG bytes."
    - "The Adaptive selection is a filter-None compatibility shim: it changes no scanline byte, compression selection, preflight admission, checksum, progress, or terminal behavior."
    - "Frozen Stored, FixedOrStored, and strict Dynamic winners are exercised through eager and hostile caller-buffered output on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Public equality-comparable PngFilterStrategy and documented parallel eager filter factory.
    - path: modules/mb-image/png/encode.mbt
      provides: Shared eager construction that carries a filter selection to the existing filter-None encode machine without changing compression planning.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Documented caller-buffered filter factory that uses the same atomic machine construction and acknowledgement path.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Immutable complete eager PNG compatibility vectors for legacy and Adaptive routes.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Immutable complete caller-buffered compatibility vectors drained through hostile capacities.
    - path: policy/foundation.json
      provides: Exact PNG semantic-interface registration for PngFilterStrategy and the two additive configured filter factories.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: PngEncoder retains PngFilterStrategy and ImageEncoder::encode passes it to the one private combined constructor.
      pattern: PngFilterStrategy|new_with_filter_strategy|PngEncodeMachine
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: PngChunkEncoder::new_with_filter_strategy feeds the shared machine and hostile drains copy only accepted caller-lease bytes.
      pattern: PngChunkEncoder::new_with_filter_strategy|PngEncodeMachine|png_chunk_test_drain_encoder
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/encode_test.mbt
      via: Unchanged _png_encode_preflight compression branches are protected by independent Stored, FixedOrStored, and strict Dynamic complete-PNG vectors.
      pattern: _png_encode_preflight|PngCompressionStrategy|PngFilterStrategy
    - from: modules/mb-image/png/png.mbt
      to: policy/foundation.json
      via: The generated PNG public semantic interface registers PngFilterStrategy and both configured filter factories consistently on every declared target.
      pattern: PngFilterStrategy|new_with_filter_strategy|semantic_interface
---

<objective>
Add the explicit adaptive PNG filter-selection API as a documented filter-None compatibility shim, preserving exact bytes from every legacy constructor and compression route. PNGF-01.

Purpose: Freeze a narrow, additive public seam for Phase 39 row-filter work without letting an opt-in API alter existing portable PNG output or encoder safety contracts.
Output: Documented eager and caller-buffered filter factories, one private combined construction seam, and all-target public frozen-vector evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-adaptive-filter-compatibility/RESEARCH.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
From modules/mb-image/png/png.mbt:
- `pub(all) enum PngCompressionStrategy { Stored; FixedOrStored; DynamicOrFixedOrStored } derive(Eq)` is the existing configured compression surface.
- `PngEncoder::new()` is a direct Stored constructor and `PngEncoder::new_with_compression_strategy(strategy)` retains an explicit compression selection.
- `PngChunkEncoder::new(source, limits, budget, diagnostics)` and `PngChunkEncoder::new_with_compression_strategy(source, strategy, limits, budget, diagnostics)` are the existing caller-buffered routes.

From modules/mb-image/png/encode.mbt and stream_encode.mbt:
- `_png_encode_preflight(source, strategy, limits, budget)` is the sole atomic admission point for capability, dimensions, output, work, and budget checks.
- `_png_fixed_scanline_byte(...)` provides the current filter-None scanline representation.
- `PngEncodeMachine::new_with_compression_strategy(...)` feeds the shared present/acknowledge lifecycle used by both eager and caller-buffered adapters.

From modules/mb-image/png/stream_encode_test.mbt:
- `png_chunk_test_drain_encoder(encoder, schedule)` and `png_chunk_test_drain_hostile(image, schedule)` already enforce exact accepted-byte progress, zero-capacity handling, eager parity, and sticky terminals.
- The existing `[0UL, 1UL, 3UL, 2UL, 5UL]` schedule is the hostile caller-buffered compatibility schedule.
</interfaces>
</context>

<feature>
  <name>Additive filter strategy with filter-None byte compatibility</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - `PngFilterStrategy::{None, Adaptive}` is public, documented, and derives `Eq`; eager and caller-buffered `new_with_filter_strategy` factories accept it for supported RGB8 and straight-RGBA8 images. PNGF-01.
    - `PngEncoder::new()`, `PngChunkEncoder::new(...)`, and both existing `new_with_compression_strategy` factories explicitly use `PngFilterStrategy::None` and retain exact complete PNG vectors for Stored, FixedOrStored, and a strict Dynamic winner. PNGF-01.
    - `new_with_filter_strategy(Adaptive)` uses Stored compression and the current filter-None scanline provider, producing the same immutable Stored vector for RGB8 and straight-RGBA8 fixtures through eager and hostile caller-buffered routes. PNGF-01.
    - The existing `PngCompressionStrategy` branches, `_png_encode_preflight` call contract, DEFLATE plan selection, IDAT framing, CRC-32, Adler-32, output acknowledgement, exact progress, and sticky terminals remain unchanged. PNGF-01.
  </behavior>
</feature>

<resolved_decisions>
- RESEARCH.md resolves the Phase 38 public shape: add `PngFilterStrategy::{None, Adaptive}` and parallel eager/caller-buffered `new_with_filter_strategy` factories; do not expose a combined compression/filter factory in this phase.
- RESEARCH.md resolves the strict-Dynamic vector: use the existing 128x1 periodic RGB8 corpus, which is a strict Dynamic winner over FixedOrStored while retaining filter-None scanlines.
- RESEARCH.md resolves the implementation boundary: Adaptive is carried through the shared machine but maps to the current filter-None scanline provider; standard row filters and scoring belong to Phase 39.
</resolved_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write public frozen-vector tests for the filter compatibility seam</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: An eager test locks immutable complete pre-change PNG byte literals for RGB8 and straight-RGBA8 through each legacy/default Stored route, explicit Stored, FixedOrStored, and DynamicOrFixedOrStored strict-winner route; it decodes every selected product through public `PngDecoder` with complete input required.
    - Test 2: A caller-buffered test drains the same RGB8/RGBA8 routes through `[0UL, 1UL, 3UL, 2UL, 5UL]`, compares only the copied accepted prefixes to the same immutable complete vectors, and retains zero-capacity, exact-total, and terminal invariants through the established drain helper.
    - Test 3: Eager and caller-buffered `new_with_filter_strategy(PngFilterStrategy::Adaptive)` equal the independent Stored/filter-None complete vectors for both source formats, while the configured FixedOrStored and strict Dynamic outputs still equal their own pre-change vectors.
    - Test 4: The strict Dynamic corpus remains strictly smaller than FixedOrStored and retains its existing Dynamic DEFLATE block marker; these assertions establish that the compatibility test freezes an actual existing filter-None Dynamic winner rather than a fallback route.
  </behavior>
  <action>Per PNGF-01, begin with RED in the existing black-box eager and caller-buffered test files. Capture the current complete output before production changes and encode it as immutable `Bytes` literals: RGB8 and straight-RGBA8 for the legacy/default and configured Stored routes, the present repetitive FixedOrStored vectors, and the present periodic strict-Dynamic winner vectors. Never derive a compatibility oracle from a second encoder route at assertion time. Add one focused eager test in `encode_test.mbt` and one focused chunk test in `stream_encode_test.mbt`; use the existing public image construction and public complete-input decoder oracle. Exercise default constructors, both configured compression factories for Stored, FixedOrStored, and DynamicOrFixedOrStored, and the new eager/caller-buffered filter factories with `Adaptive`. For the chunk test, construct the chosen configured/filter encoder, call `png_chunk_test_drain_encoder` with `[0UL, 1UL, 3UL, 2UL, 5UL]`, and retain its accepted-prefix/progress/sticky-terminal checks. Do not add filter transforms, a filter score, a new compression corpus, a combined public compression/filter factory, private-machine assertions, policy/config edits, scripts, or non-phase documentation. Confirm the focused native test is RED only because the new filter type/factories do not yet exist, then commit the test stage as `test(38-01): add PNG filter compatibility vectors`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --target-dir _build/phase38-red --frozen -f '*filter strategy*'  # expected to fail in RED before PngFilterStrategy exists</automated>
  </verify>
  <done>The two new public tests fail solely for the absent filter-selection API and already freeze independent complete filter-None vectors for legacy/default Stored, explicit Stored, FixedOrStored, and strict Dynamic eager/chunk output across RGB8 and straight-RGBA8 fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the additive eager and caller-buffered filter factories</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, policy/foundation.json</files>
  <behavior>
    - Test 1: `PngFilterStrategy::{None, Adaptive}` and both `new_with_filter_strategy` factories compile as public API on all declared targets.
    - Test 2: Every old constructor and existing compression configuration satisfies Task 1's exact immutable complete-PNG vector, including the strict Dynamic winner.
    - Test 3: Adaptive eager and caller-buffered encoding satisfy the immutable Stored/filter-None vector under the hostile schedule and public complete-input decode checks.
    - Test 4: Existing chunk zero-capacity, exact-progress, acknowledgement, and sticky-terminal evidence remains green because the new factory enters the same private machine and no existing state transition changes.
  </behavior>
  <action>Implement PNGF-01 with exactly one public enum and two parallel public factories. In `png.mbt`, add `pub(all) enum PngFilterStrategy { None; Adaptive } derive(Eq)`, document that `Adaptive` is an explicit temporary filter-None compatibility shim in this phase, add `PngEncoder::new_with_filter_strategy(strategy) -> PngEncoder`, and document/add `PngChunkEncoder::new_with_filter_strategy(source, strategy, limits, budget, diagnostics) -> Result[PngChunkEncoder, @error.CoreError]`. Keep `PngEncoder::new()`, `PngChunkEncoder::new(...)`, and both existing compression-strategy factories source-compatible and make each pass `PngFilterStrategy::None` explicitly through a private combined construction helper. The two filter factories must use `PngCompressionStrategy::Stored` and carry their requested filter selection into that helper. In `encode.mbt` and `stream_encode.mbt`, add the private combined factory/constructor seam around the existing `PngEncodeMachine` setup; match both `None` and `Adaptive` to the present `_png_fixed_scanline_byte` filter-None provider. Leave `_png_encode_preflight` and every `PngCompressionStrategy` match unchanged: no compression candidate, winner rule, work charge, plan, emitter, framing, or checksum behavior may change. Regenerate the public PNG semantic interface through the established repository workflow, then update only the PNG `semantic_interface` registration in `policy/foundation.json` for `PngFilterStrategy` and the eager and caller-buffered `new_with_filter_strategy` declarations; preserve every other policy declaration, source inventory, import allowlist, target declaration, and policy entry. Do not implement None/Sub/Up/Average/Paeth candidate generation, row scoring, adaptive byte changes, a combined public API, new scripts, unrelated policy/config changes, QOI work, release work, or documentation outside the public source doc comments and phase artifacts. Run the RED test to GREEN and commit the implementation as `feat(38-01): add PNG filter compatibility factories`.</action>
  <verify>
    <automated>$required = @('PNG filter strategy eager frozen compatibility vectors','PNG filter strategy chunk frozen compatibility vectors'); foreach ($target in @('js','wasm','wasm-gc','native')) { $targetDir = "_build/phase38-$target"; $outline = @(moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen --outline 2&gt;&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "focused PNG outline has no test entry on $target" }; foreach ($name in $required) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing required PNG test on ${target}: $name" } }; moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen -f '*filter strategy*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; moon -C modules/mb-image test png --target all --target-dir _build/phase38-all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>PngFilterStrategy and both documented configured factories are additive public API; Adaptive emits the current Stored/filter-None representation; all legacy/default and existing compression routes retain their exact frozen complete PNG bytes, and the named public tests pass independently on js, wasm, wasm-gc, and native.</done>
</task>

</tasks>

<scope_fences>
- PNGF-01 covers the documented enum and parallel eager/caller-buffered filter factories, the private combined construction seam, and frozen public compatibility evidence only.
- Preserve the current filter-None scanline provider, PngCompressionStrategy selection, `_png_encode_preflight`, bounded matcher, DEFLATE plans, zlib/PNG framing, CRC-32, Adler-32, resource admission, caller-lease ownership, acknowledgement-only commits, exact progress, and sticky terminals.
- Exclude standard filter implementation and scoring; compression-selection modifications; combined public APIs; new scripts; policy/config changes other than the exact required PNG semantic-interface registration; QOI; release/registry/CI work; FFI; benchmarks; and documentation outside PNG public API comments and phase artifacts.
</scope_fences>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library caller → eager/caller-buffered filter factory | A caller-controlled selection must enter the established atomic encode construction without changing a legacy route. |
| Filter selection → shared encode machine | Adaptive must map to the proven filter-None provider while retaining all compression and checksum behavior. |
| Caller-owned output lease → chunk encoder | The configured chunk factory must preserve accepted-prefix ownership, exact progress, and sticky terminal behavior under hostile capacities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | Legacy/default Stored and configured compression byte representations | high | mitigate | Task 1 freezes independent complete RGB8/RGBA8 Stored, FixedOrStored, and strict Dynamic vectors; Task 2 explicitly supplies None to every old route. |
| T-38-02 | Tampering | Adaptive selection and scanline emission | high | mitigate | Task 2 maps both enum cases to the existing filter-None provider and Task 1 requires Adaptive to equal the independent Stored vectors. |
| T-38-03 | Denial of service | Factory admission and filter dispatch | high | mitigate | Task 2 retains `_png_encode_preflight` and all compression-plan/resource paths unchanged; all-target package regression covers existing bounded behavior. |
| T-38-04 | Tampering | Caller-buffered output acknowledgement | medium | mitigate | Task 1 uses the established zero/ragged hostile schedule and drain helper, which checks written prefixes, totals, and sticky terminal leases. |
| T-38-05 | Spoofing | Public API documentation | low | mitigate | Task 2 documents Adaptive's exact filter-None compatibility behavior rather than representing it as row-filter selection implementation. |
| T-38-06 | Tampering | PNG semantic-interface policy registration | medium | mitigate | Task 2 updates only the enum and two configured factory declarations, then runs the existing PNG quality lane to enforce the contract. |
</threat_model>

<verification>
- RED: the focused native filter-strategy selector fails before Task 2 because the planned public enum and factories are absent.
- GREEN: named eager and chunk frozen-vector tests run independently on js, wasm, wasm-gc, and native in isolated target directories.
- Regression: the complete PNG package suite runs on all targets after the focused evidence, then the existing PNG quality lane verifies the semantic-interface policy contract.
- Commit order is atomic: `test(38-01): add PNG filter compatibility vectors` before `feat(38-01): add PNG filter compatibility factories`.
</verification>

<success_criteria>
- Library users can select `PngFilterStrategy::Adaptive` through documented eager and caller-buffered factories without a new combined public factory.
- Adaptive uses Stored compression and the established filter-None scanline representation; it does not implement filters or scoring.
- Every legacy/default constructor and existing Stored, FixedOrStored, and DynamicOrFixedOrStored route equals its independently frozen full PNG vector under eager and hostile caller-buffered execution.
- The strict Dynamic vector remains a true Dynamic compression winner, proving the filter seam did not alter its compression selection.
- The public frozen-vector tests pass on js, wasm, wasm-gc, and native, followed by the complete all-target PNG test suite.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Explicit adaptive row-filter selection without changing legacy filter-None constructor or compression-route bytes | 38-01 | COVERED | Tasks 1-2 add only the documented selection seam and freeze all old routes as complete bytes. |
| REQ | PNGF-01 | Eager and caller-buffered opt-in with filter-None legacy compatibility | 38-01 | COVERED | Task 2 supplies both factories; Task 1 proves eager/chunk equality to immutable legacy vectors. |
| RESEARCH | API seam | Equality-comparable two-case public selection and parallel factories | 38-01 | COVERED | Task 2 creates PngFilterStrategy and exactly two new public configured factories. |
| RESEARCH | Compatibility | Adaptive maps to the current None provider while old routes explicitly select None | 38-01 | COVERED | Task 2 requires explicit legacy None construction and a two-case private provider mapping. |
| RESEARCH | Safety | Preserve one shared machine, atomic preflight, acknowledgement, exact progress, and sticky terminals | 38-01 | COVERED | Task 2 preserves preflight/compression code; Task 1 uses established hostile-drain invariants. |
| RESEARCH | Evidence | Immutable complete vectors across Stored, FixedOrStored, strict Dynamic winner, eager/chunk hostile execution, and four targets | 38-01 | COVERED | Task 1 locks vector families; Task 2's verifier executes named public tests on each declared target. |
| RESEARCH | Public contract | Register the additive enum and two configured factories in the PNG semantic-interface policy | 38-01 | COVERED | Task 2 updates only the required PNG policy declarations and runs the established PNG quality lane. |
| RESEARCH | Out of scope | Standard filter transforms/scoring and combined public factory | 38-01 | COVERED | Task 2 and scope fences forbid these Phase 39 and later concerns. |
| CONTEXT | — | No Phase 38 CONTEXT.md or D-NN locked decisions exist | 38-01 | COVERED | ROADMAP, REQUIREMENTS, RESEARCH, and the delegated task scope define the work. |

<output>
Create `.planning/phases/38-adaptive-filter-compatibility/38-01-SUMMARY.md` when done.
</output>
