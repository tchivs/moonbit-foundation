---
phase: 47-gray16-factory-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/storage/views.mbt
  - modules/mb-image/storage/storage_test.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAY16-01]
---

<objective>
Implement the explicit Stored/non-interlaced Gray16 PNG baseline with exact PNG big-endian sample serialization, atomic eager/chunk admission, and frozen legacy compatibility.
</objective>

<tasks>

<task type="tracer" tdd="true">
<name>Task 1: Expose checked packed-component byte access needed by Gray16 encoding</name>
<files>modules/mb-image/storage/views.mbt, modules/mb-image/storage/storage_test.mbt</files>
<action>D-03: Add the smallest public, bounds-checked ImageView accessor that reads a named byte within a packed U8 or U16 component without weakening `get_byte`'s existing packed-U8 contract. It must reject unsupported component/layout, invalid coordinates/channels/component-byte index, and stale/empty views through existing typed errors. Write U16 tests that prove both component bytes are observable in storage order and U8 compatibility is unchanged. Commit the red and green work atomically.</action>
<verify>moon -C modules/mb-image test storage --target native --frozen</verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add explicit Gray16 Stored factories and profile-aware big-endian PNG emission</name>
<files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
<action>D-01, D-02, D-04, D-05: Add private `Gray16` profile plus matching explicit eager/chunk `new_gray16` constructors, bound to Stored/None/non-interlaced. Generalize profile-aware source admission and scalar row-byte facts for packed, tightly packed U16 Gray inputs; retain canonical metadata/no-alpha checks and reject Gray16 Adam7 before the ledger. Use the new component-byte accessor to emit each U16 source sample as high byte then low byte in PNG scanlines, regardless of local storage order. Keep legacy and Gray8 paths byte-for-byte unchanged and reuse the same preflight/machine/replay path. Add native black-box tests with non-symmetric U16 values asserting PNG signature/IHDR bit depth 16, colour type 0, no interlace, decompressed Stored scanline big-endian sample bytes, eager/chunk exact identity, atomic bad-profile rejection, and existing legacy/Gray8 frozen vectors. Commit implementation and tests atomically.</action>
<verify>moon -C modules/mb-image test png --target native --frozen</verify>
</task>

</tasks>

<success_criteria>
- Explicit eager and caller-buffered Gray16 factories emit valid type-0/16-bit/non-interlaced Stored PNGs with big-endian samples.
- U16 Gray admission and every rejected input preserve the existing atomic output/lease boundary.
- Gray8, RGB8, and straight-RGBA8 output remains guarded in the same native suite.
</success_criteria>

<output>Create `47-01-SUMMARY.md` after verification.</output>
