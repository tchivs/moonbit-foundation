---
phase: 108-public-contract-and-transaction-skeleton
plan: "02"
type: execute
wave: 2
depends_on:
  - 108-01
files_modified:
  - modules/mb-core/checked/checked.mbt
  - modules/mb-core/checked/checked_test.mbt
  - modules/mb-core/checked/checked_wbtest.mbt
autonomous: true
requirements:
  - TXT-02
must_haves:
  truths:
    - "D-06: checked signed Int64 addition accepts exact MIN/MAX boundaries and rejects one-step overflow with a structured CoreError."
    - "D-06: checked signed negation rejects only Int64::MIN, and UInt64-to-Int64 conversion proves the numeric maximum before any reinterpretation."
    - "D-16: shared arithmetic failures retain stable ArithmeticOverflow facts that mb-text can rebind to its stage-specific operation and context."
    - "TXT-02 boundary/precision: MIN, MAX, zero, exact maximum conversion, and one-over conversion have explicit portable results with no wrapping or rounding."
  artifacts:
    - path: "modules/mb-core/checked/checked.mbt"
      provides: "Portable checked signed Int64 arithmetic"
      exports: ["checked_add_int64", "checked_neg_int64", "checked_uint64_to_int64"]
    - path: "modules/mb-core/checked/checked_test.mbt"
      provides: "Public exact-boundary and overflow behavior"
    - path: "modules/mb-core/checked/checked_wbtest.mbt"
      provides: "Internal sign-branch and range-proof coverage"
  key_links:
    - from: "modules/mb-core/checked/checked.mbt"
      to: "modules/mb-text/text/run.mbt"
      via: "Plan 108-04 consumes the shared add, negate, and narrowing helpers for signed projection and totals"
      pattern: "checked_(add|neg|uint64_to)_int64"
  prohibitions:
    - { statement: "No ordinary signed addition, minimum negation, lossy conversion, or bit reinterpretation is accepted as a range proof.", status: unverified, flagged: true }
---

<objective>
Deliver the shared checked Int64 capability independently of font identity and transaction lifetime work.

Purpose: Give D-06/TXT-02 one portable arithmetic authority with exact boundary evidence before GlyphId hardening and shaped-run projection consume it.
Output: Three focused mb-core checked files containing the additive public helpers and their black-box/white-box boundary matrices.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-CONTEXT.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-RESEARCH.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-PATTERNS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-01-SUMMARY.md

<interfaces>
Existing authorities:
- `modules/mb-font/font/outline.mbt` contains the proven sign-branch checked-add algorithm to promote into mb-core.
- `modules/mb-core/checked/checked.mbt` already owns portable checked integer helpers and returns `Result[..., @error.CoreError]`.
- Plan 108-04 will consume exactly `checked_add_int64`, `checked_neg_int64`, and `checked_uint64_to_int64`; this plan does not modify mb-font or mb-text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact portable checked Int64 helpers</name>
  <reversibility rating="costly">D-06 makes these shared helper semantics part of the public arithmetic contract consumed by later shaping phases; changing them requires coordinated mb-core and mb-text updates.</reversibility>
  <read_first>
    - modules/mb-core/checked/checked.mbt
    - modules/mb-core/checked/checked_test.mbt
    - modules/mb-core/checked/checked_wbtest.mbt
    - modules/mb-font/font/outline.mbt
    - modules/mb-core/error/core_error.mbt
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-PATTERNS.md
  </read_first>
  <files>modules/mb-core/checked/checked.mbt, modules/mb-core/checked/checked_test.mbt, modules/mb-core/checked/checked_wbtest.mbt</files>
  <behavior>
    - "checked_add_int64 accepts MAX+0, MIN+0, mixed-sign cancellation, and exact in-range edges; it rejects MAX+1 and MIN+(-1) with ArithmeticOverflow."
    - "checked_neg_int64 accepts zero, positive values, negative non-MIN values, and MAX; it rejects only MIN with ArithmeticOverflow."
    - "checked_uint64_to_int64 accepts zero and Int64::MAX as UInt64 and rejects one-over and UInt64::MAX before reinterpretation."
    - "All three helpers behave identically under js, wasm, wasm-gc, and native checks/tests."
  </behavior>
  <action>
Write the failing public boundary tests first. Promote the existing sign-branch algorithm from `outline.mbt` into `mb-core/checked` as `checked_add_int64`, then add `checked_neg_int64` and `checked_uint64_to_int64`. Preserve the established CoreError arithmetic convention and exact public names expected by D-06. For addition, branch on operand sign so no intermediate negation of `Int64::MIN` occurs. For conversion, compare the UInt64 value with the exact Int64 maximum before any representation conversion; a reinterpretation is allowed only after that proof.

Add white-box cases for every sign branch and exact boundary, plus public tests for zero, MIN/MAX, exact maximum conversion, and one-step overflow. Do not modify GlyphId, Font, transaction, run, policy, or documentation files in this capability.
  </action>
  <acceptance_criteria>
    - "D-06: signed add, negate, and UInt64 narrowing are exact at MIN/MAX and reject every one-step overflow."
    - "D-16: failures are structured CoreError values suitable for stage-specific rebinding."
    - "TXT-02 receives no wrapping, rounding, unchecked negation, or representation-only range test."
    - "The task changes exactly the three declared mb-core checked files."
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'
foreach ($target in 'js','wasm','wasm-gc','native') {
  moon -C modules/mb-core check --target $target --deny-warn --frozen
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  moon -C modules/mb-core test checked --target $target --frozen
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}</automated>
  </verify>
  <done>The three checked Int64 helpers pass exact boundary and overflow matrices on all four supported targets and are ready for Plan 108-04 projection use.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan contributes:

- `checked_add_int64`, `checked_neg_int64`, and `checked_uint64_to_int64` in `modules/mb-core/checked/checked.mbt`
- public boundary evidence in `modules/mb-core/checked/checked_test.mbt`
- internal sign/range branch evidence in `modules/mb-core/checked/checked_wbtest.mbt`

<threat_model>
## Assets

| Asset | Security property |
|---|---|
| Signed design-unit arithmetic | No overflow, wrap, lossy narrowing, or unproved reinterpretation |
| Shared CoreError facts | Deterministic failure identity for downstream rebinding |

## Trust Boundaries

| Boundary | Description |
|---|---|
| hostile integer extremes -> shared checked helpers | MIN/MAX and cross-sign operands can overflow intermediate or final values |
| UInt64 charge/metric fact -> signed run coordinate | Values above Int64::MAX must fail before representation conversion |

## ASVS L1 mapping

| Control area | Applies | Mitigation / task | Verification |
|---|---|---|---|
| Input validation | yes | Task 1 proves all signed and unsigned boundaries before arithmetic/conversion | MIN/MAX/zero/exact/one-over matrix |
| Architecture / business logic | yes | One mb-core authority supplies all later signed shaping operations | exact additive public interface |
| Error handling / data protection | yes | Structured ArithmeticOverflow values replace wrap/round behavior | exact error assertions |
| Resource / DoS | yes | Extreme values fail deterministically without loops or partial publication | four-target focused tests |
| Build / config / supply chain | yes | No package install; frozen repository module on four targets | explicit check/test loop |
| Browser, session, authentication, HTML, transport, database | no | Pure local arithmetic has no such boundary | source inventory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test |
|---|---|---|---|---|---|---|
| T-108-07 | Tampering | checked Int64 addition | high | mitigate | Sign-branch bounds checks before addition | MAX+1, MIN-1, mixed-sign matrix |
| T-108-08 | Tampering | checked negation | high | mitigate | Reject MIN before unary negation | MIN/MAX/zero matrix |
| T-108-09 | Spoofing | UInt64-to-Int64 projection | high | mitigate | Numeric maximum proof before conversion | exact max and one-over |
| T-108-10 | Repudiation | arithmetic error identity | medium | mitigate | Stable structured overflow facts | exact CoreError assertions |
</threat_model>

<verification>
- Run the focused mb-core checked suite on js, wasm, wasm-gc, and native.
- Confirm the generated mb-core interface adds only the three planned helper names.
- Confirm `files_modified` exactly equals the task `<files>` union.
</verification>

<success_criteria>
- Every D-06/TXT-02 integer boundary has an explicit portable result.
- No GlyphId or transaction capability is mixed into this independently committable plan.
- No deferred feature or UI surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/108-public-contract-and-transaction-skeleton/108-02-SUMMARY.md` when done.
</output>
