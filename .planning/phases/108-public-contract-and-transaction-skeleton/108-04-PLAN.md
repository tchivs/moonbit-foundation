---
phase: 108-public-contract-and-transaction-skeleton
plan: "04"
type: execute
wave: 4
depends_on:
  - 108-01
  - 108-02
  - 108-03
files_modified:
  - modules/mb-text/text/tags.mbt
  - modules/mb-text/text/options.mbt
  - modules/mb-text/text/limits.mbt
  - modules/mb-text/text/run.mbt
  - modules/mb-text/text/shape.mbt
  - modules/mb-text/text/contract_test.mbt
  - modules/mb-text/text/contract_wbtest.mbt
autonomous: true
requirements:
  - TXT-01
  - TXT-02
must_haves:
  truths:
    - "D-02: Input length and identity are Unicode scalar count/value, never bytes, graphemes, normalized text, or borrowed caller storage; the fully validated array is snapshotted before Font work."
    - "TXT-01 adjacency: private Phase 108 staging preserves adjacent/equal scalar records and never merges them implicitly; only an explicit ligature outcome may reduce record count."
    - "D-03/D-04: ShapedRun and PositionedGlyph are opaque immutable values exposing only metadata, length, indexed records, same-Font GlyphId, scalar cluster, signed advance/offsets, units_per_em, direction, and checked total."
    - "D-05/D-06/D-07: equal values retain stable logical order; LTR publishes logical pen order, while RTL reverses only final records and negates advances, with x/y design-space offsets unchanged."
    - "D-08: one-to-one records retain source scalar indices, a ligature uses the minimum consumed index, positioning never changes clusters, and RTL final reversal preserves those clusters."
    - "D-09/D-10: checked four-byte tags, default-or-exact language, explicit direction, and closed liga/kern booleans are the only public choices; required feature and supported rlig behavior are not disableable."
    - "ShapeLimits is exactly a validated new(max_input_scalars~, max_output_glyphs~) contract with nonzero UInt64 values and only max_input_scalars() / max_output_glyphs() accessors."
    - "D-11: empty input retains the validated-options/full-authority/exact-work=1 behavior established by the tracer."
    - "D-12: invalid scalars/tags/choices/limits and indexed access fail as caller-contract errors; nonempty public shaping remains a Capability failure."
    - "D-15/D-16: InvalidInput -> State -> Data -> Capability -> Resource precedence and stable operation/context strings are executable through private generated contract facts."
    - "The call is synchronous, shares no persistent cache, and promises no safe concurrent mutation of caller Budget or retained Font bytes; snapshot and named revision-probe tests make violations observable."
    - "TXT-02 boundary/precision: zero/exact/one-over limits and signed MIN/MAX/one-step overflow return explicit results with no rounding, partial run, or partial charge."
  artifacts:
    - path: "modules/mb-text/text/tags.mbt"
      provides: "Checked four-byte ScriptTag and LanguageTag"
      exports: ["ScriptTag", "LanguageTag"]
    - path: "modules/mb-text/text/options.mbt"
      provides: "Closed language, direction, feature, and shaping option values"
      exports: ["LanguageChoice", "Direction", "FeaturePolicy", "ShapingOptions"]
    - path: "modules/mb-text/text/limits.mbt"
      provides: "Exact two-field nonzero ShapeLimits public contract"
      exports: ["ShapeLimits"]
    - path: "modules/mb-text/text/run.mbt"
      provides: "Immutable positioned glyph and shaped-run projection"
      exports: ["PositionedGlyph", "ShapedRun"]
    - path: "modules/mb-text/text/contract_wbtest.mbt"
      provides: "Generated adjacency, LTR/RTL, ligature-cluster, overflow, snapshot, precedence, and mutation fixtures"
  key_links:
    - from: "modules/mb-text/text/shape.mbt"
      to: "modules/mb-font/font/shape_transaction.mbt"
      via: "validated scalar snapshot and private staged value enter the opaque Font authority"
      pattern: "with_shape_transaction"
    - from: "modules/mb-text/text/shape.mbt"
      to: "modules/mb-core/checked/checked.mbt"
      via: "base plus adjustment, RTL negation, and total accumulation use shared checked signed helpers"
      pattern: "checked_(add|neg|uint64_to)_int64"
    - from: "modules/mb-text/text/run.mbt"
      to: "modules/mb-font/font/font.mbt"
      via: "PositionedGlyph stores only owner-bound opaque GlyphId values"
      pattern: "GlyphId"
  prohibitions:
    - { statement: "No mutable caller array, backing record array, ArrayView, raw layout fact, or font source capability is returned or retained as public run state.", status: unverified, flagged: true }
    - { statement: "No input reversal, offset negation, byte/grapheme cluster claim, implicit adjacent-record merge, or unchecked signed projection is allowed.", status: unverified, flagged: true }
    - { statement: "No successful public nonempty run is synthesized without selected layout authority.", status: unverified, flagged: true }
---

<objective>
Freeze the complete format-neutral text input, immutable run, numeric projection, cluster, and public error contracts.

Purpose: Make TXT-01/TXT-02 executable without crossing the Phase 108 format boundary, so later layout phases consume a stable public value model instead of redefining it.
Output: Fully validated closed input values, immutable run access, private generated LTR/RTL/ligature fixtures, and the complete stage/error matrix while the real public nonempty route remains fail-closed.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-CONTEXT.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-RESEARCH.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-PATTERNS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-UI-SPEC.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-01-SUMMARY.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-02-SUMMARY.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-03-SUMMARY.md

<interfaces>
Consumed foundations:
- `Font::with_shape_transaction[T](self : Font, budget : @budget.Budget, body : (FontShapeScope) -> Result[(T, @budget.ResourceCharge), @error.CoreError]) -> Result[T, @error.CoreError]` is the exact compile-proved seam and owns entry/final source guards plus the sole combined Budget commit. Generic `T` can nominally carry `FontShapeScope`; shared-cell `defer` invalidation makes post-callback operations return `State` / `InvalidRange`, operation `font-shape-scope`, context `font-shape-scope-closed`.
- `GlyphId` is opaque and owner-bound; `GlyphId::value` remains the qualified numeric accessor.
- `checked_add_int64`, `checked_neg_int64`, and `checked_uint64_to_int64` return `Result[..., CoreError]`.
- The public call is exactly `@text.shape(font, scalars, options, limits, budget)` and its empty behavior already exists from the tracer; this plan completes every value and generated nonempty contract around it.
- `ShapeLimits::new(max_input_scalars~ : UInt64, max_output_glyphs~ : UInt64) -> Result[ShapeLimits, @error.CoreError]`, `ShapeLimits::max_input_scalars(self : ShapeLimits) -> UInt64`, and `ShapeLimits::max_output_glyphs(self : ShapeLimits) -> UInt64` are the complete Phase 108 public limit surface; both constructor values are nonzero.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete scalar, tag, closed-option, and ShapeLimits contracts</name>
  <reversibility rating="costly">D-02 and D-09/D-10 freeze public input semantics consumed by every shaping phase; changing them requires coordinated downstream contract updates, while no published-data migration exists.</reversibility>
  <read_first>
    - modules/mb-text/text/tags.mbt
    - modules/mb-text/text/options.mbt
    - modules/mb-text/text/limits.mbt
    - modules/mb-text/text/shape.mbt
    - modules/mb-text/text/contract_test.mbt
    - modules/mb-font/font/limits.mbt
    - modules/mb-core/error/core_error.mbt
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-CONTEXT.md
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-RESEARCH.md
  </read_first>
  <files>modules/mb-text/text/tags.mbt, modules/mb-text/text/options.mbt, modules/mb-text/text/limits.mbt, modules/mb-text/text/shape.mbt, modules/mb-text/text/contract_test.mbt</files>
  <behavior>
    - "Every scalar is checked against 0..0x10FFFF excluding surrogates; the entire array is validated before Font access, then copied."
    - "Caller mutation after the validation/snapshot probe cannot change private staged scalar count, values, adjacency, or clusters."
    - "ScriptTag/LanguageTag require exactly four printable OpenType bytes; LanguageTag rejects reserved default spellings because Default is a closed enum case."
    - "LanguageChoice and Direction enumerate exactly the locked cases; FeaturePolicy accepts only liga and kern booleans and exposes no arbitrary tags/values/ranges."
    - "ShapeLimits::new(max_input_scalars~, max_output_glyphs~) preserves two nonzero UInt64 values; zero input wins over zero output and binds stable text-shape-limits-new context."
    - "The generated interface exposes only ShapeLimits::new, max_input_scalars(), and max_output_glyphs(); it exposes no default, max_work, layout, allocation, or numeric limit group."
    - "Empty input still validates malformed tags/options/each zero limit before font authority and retains exact work=1 success."
  </behavior>
  <action>
Finish private copied four-byte storage for `ScriptTag` and `LanguageTag`, the exact closed enums and private boolean policy in `options.mbt`, and `ShapingOptions` named accessors without automatic script/language/direction inference. Validate printable tag bytes and reserve default language selection exclusively for `LanguageChoice::Default` per D-09/D-12.

Implement `ShapeLimits` with exactly two private `UInt64` fields and the exact public surface `ShapeLimits::new(max_input_scalars~ : UInt64, max_output_glyphs~ : UInt64) -> Result[ShapeLimits, @error.CoreError]`, `ShapeLimits::max_input_scalars(self : ShapeLimits) -> UInt64`, and `ShapeLimits::max_output_glyphs(self : ShapeLimits) -> UInt64`. Both arguments are required and nonzero; validate input before output and return `InvalidInput` / `InvalidRange`, operation `text-shape-limits-new`, requested `0`, limit `1`, with context `max-input-scalars` or `max-output-glyphs`. Expose no default, setters, public fields, `max_work`, layout/count/byte, allocation, or numeric-magnitude limit APIs.

Make scalar validation a complete pass and take a request-owned `Array[Int]` copy before any Font operation per D-02/D-15. Add a private post-snapshot mutation probe used only by white-box tests so mutation of caller storage proves the request snapshot. Keep the public wrapper synchronous and probe-free.
  </action>
  <acceptance_criteria>
    - "D-02/D-09/D-10/D-12: all public inputs are explicit, validated, closed, copied where mutable, and fail before Font work."
    - "Unicode identity is scalar count/value; no normalization, byte offset, grapheme, locale, discovery, fallback, or I/O behavior enters the contract."
    - "ShapeLimits exposes exactly ShapeLimits::new(max_input_scalars~ : UInt64, max_output_glyphs~ : UInt64) -> Result[ShapeLimits, @error.CoreError], ShapeLimits::max_input_scalars(self : ShapeLimits) -> UInt64, and ShapeLimits::max_output_glyphs(self : ShapeLimits) -> UInt64, with input-before-output nonzero validation and no other limit API."
    - "D-11 remains true for valid and invalid empty requests."
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'
moon -C modules/mb-text check --target native --deny-warn --frozen && moon -C modules/mb-text test text --target native --frozen</automated>
  </verify>
  <done>All TXT-01 inputs and the exact two-field ShapeLimits contract are explicit and stable, caller scalar mutation cannot affect the request, and empty validation/charging behavior remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze immutable run projection, scalar clusters, and stage precedence</name>
  <reversibility rating="costly">D-03 through D-08 and D-15/D-16 define the public run and error compatibility contract; changing them requires coordinated downstream contract updates, while no published-data migration exists.</reversibility>
  <read_first>
    - modules/mb-text/text/run.mbt
    - modules/mb-text/text/shape.mbt
    - modules/mb-text/text/contract_test.mbt
    - modules/mb-core/checked/checked.mbt
    - modules/mb-font/font/shape_transaction.mbt
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-RESEARCH.md
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-PATTERNS.md
  </read_first>
  <files>modules/mb-text/text/run.mbt, modules/mb-text/text/shape.mbt, modules/mb-text/text/contract_test.mbt, modules/mb-text/text/contract_wbtest.mbt</files>
  <behavior>
    - "Opaque PositionedGlyph exposes same-Font glyph, UInt64 scalar cluster, Int64 advance/x_offset/y_offset; ShapedRun exposes copied records, units_per_em, direction, len, glyph_at, and checked total only."
    - "The generated logical A/B fixture publishes LTR A/B with advances +550/+500, offsets (+20,-10)/(-30,+15), clusters 0/1, total +1050."
    - "The same fixture publishes RTL B/A with advances -500/-550, identical offsets/clusters attached to their records, total -1050."
    - "Adjacent equal scalar records remain distinct and stable; a generated ligature consuming indices 1,2,3 has cluster 1 before and after RTL reversal."
    - "Base advance plus adjustment, RTL negation, and total sum each fail before publication on signed overflow; offsets remain unchanged signed design-space values."
    - "glyph_at returns exact first/last values and stable InvalidInput/InvalidRange at one-past; no accessor returns a backing array/view."
    - "Multi-fault generated cases enforce InvalidInput -> entry State -> Data -> Capability -> Resource, with immediate State at every named mutation seam and zero budget delta."
  </behavior>
  <action>
Complete private immutable `PositionedGlyph` and `ShapedRun` storage. Copy the staged record array at publication; return records only by checked indexed value access. Use owner-bound GlyphId and shared checked narrowing for `glyph_at`, stable operation `text-shaped-run-glyph-at`, and context `run-index` per D-03/D-04/D-16.

Add a package-private generated-contract staging seam containing only logical glyph facts, source scalar indices, base advances, admitted adjustments, offsets, charge facts, and stage fault/probe selectors. It must contain no font bytes or selected format records. Preserve adjacent and equal scalar records unless the fixture explicitly represents a ligature outcome. For ligatures compute the minimum consumed scalar index. Use checked Int64 add before projection, negate only advances for RTL, reverse only the completed record array, retain offsets/clusters unchanged, and checked-sum the published total per D-05 through D-08.

Execute the full D-15 multi-fault matrix through the private staging seam and the real font transaction: InvalidInput, entry State, Data, Capability, Resource; immediate State at named mutation probes; exact success as one combined charge. Rebind generic arithmetic failures to stable text operation/context values. Keep the public nonempty `shape` path Capability fail-closed and expose none of the fixture or probe machinery.
  </action>
  <acceptance_criteria>
    - "D-03/D-04: public run access is immutable and limited to the locked fields/accessors."
    - "D-05/D-06/D-07: exact LTR/RTL record order, advance signs, offset preservation, and totals match the hand-derived fixture."
    - "D-08: scalar-origin clusters, explicit ligature minimum, adjacency, and equal-value stability are executable."
    - "D-15/D-16: complete multi-fault stage precedence and stable structured errors are tested with zero failure-side authority mutation."
    - "Public nonempty input still returns Capability and cannot reach the generated staging seam."
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'
foreach ($target in 'js','wasm','wasm-gc','native') {
  moon -C modules/mb-text check --target $target --deny-warn --frozen
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  moon -C modules/mb-text test text --target $target --frozen
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}</automated>
  </verify>
  <done>The immutable run, adjacency, clusters, signed LTR/RTL projection, overflow behavior, and complete stage precedence are frozen by public/private executable contracts.</done>
</task>

</tasks>

## Assumption delta decision

`assumption-delta scan 108 --json` detected the primary signal noun `fallback` (and a secondary `second` signal) in explicit prohibitions. Decision: `no-change`. The invariant test is the public nonempty request: it must return Capability with zero Budget delta and cannot invoke any fallback route or a second commit. This preserves the locked scope rather than promoting or adding an alternate capability.

## Artifacts this phase produces

This plan completes:

- `ScriptTag` and `LanguageTag` in `modules/mb-text/text/tags.mbt`
- `LanguageChoice`, `Direction`, `FeaturePolicy`, and `ShapingOptions` in `modules/mb-text/text/options.mbt`
- exact two-field `ShapeLimits::new`, `max_input_scalars()`, and `max_output_glyphs()` in `modules/mb-text/text/limits.mbt`
- `PositionedGlyph`, `ShapedRun`, and `ShapedRun::glyph_at` in `modules/mb-text/text/run.mbt`
- complete public `shape` validation/fail-closed route plus private generated staging/projection helpers in `modules/mb-text/text/shape.mbt`
- public contract tests in `modules/mb-text/text/contract_test.mbt`
- private generated adjacency, projection, cluster, overflow, snapshot, precedence, and mutation fixtures in `modules/mb-text/text/contract_wbtest.mbt`

<threat_model>
## Assets

| Asset | Security property |
|---|---|
| Scalar identity and order | No aliasing, normalization, adjacency loss, or hidden reordering |
| Positioned run | Immutable, same-Font, exact signed values and scalar provenance |
| Public feature/options surface | Closed choices cannot smuggle unsupported capability |
| Error and Budget observations | Deterministic stage precedence and atomic failure |

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller scalar/tag/limits -> shape | Hostile length, values, tags, zero ceilings, mutable arrays |
| private generated facts -> run publication | Test-only facts exercise projection without becoming public capability |
| signed facts -> checked projection | Extreme values can overflow adjustment, negation, or total |
| private run storage -> public accessors | Mutable arrays/raw layout facts must not escape |

## ASVS L1 mapping

| Control area | Applies | Mitigation / task | Verification |
|---|---|---|---|
| Input validation | yes | Task 1 validates every scalar/tag/choice and the exact two nonzero ShapeLimits values before Font access, then snapshots scalars | invalid scalar/tag/zero-limit precedence and mutate-after-snapshot tests |
| Architecture / business logic | yes | Tasks 1-2 retain top-level shape, exact font callback, logical staging, final-only RTL projection, and fail-closed public nonempty path | generated A/B, ligature, precedence, and nonempty Capability tests |
| Access / identity boundary | yes | PositionedGlyph accepts owner-bound GlyphId only; generated fixtures and probes remain package-private; escaped font scopes remain runtime-inert | foreign-glyph regression, `moon info`, and closed-scope dependency tests |
| Error handling / data protection | yes | Stable CoreError tuples, copied scalar/record storage, checked indexed access, and no raw array/table exposure | simultaneous-fault matrix, `glyph_at`, and interface inspection |
| Resource / DoS | yes | Exact input/output ceilings, checked signed projection, combined charge preflight, and no publication on overflow | zero/exact/one-over, MIN/MAX, overflow, and budget-delta matrices |
| Build / config / supply chain | yes | No dependency/config expansion in this plan; frozen explicit four-target builds consume only approved local mb-core/mb-font modules | per-target check/test loop and generated interface |
| Browser, session, authentication, HTML, transport, database | no | Pure synchronous library values and local capability checks; no browser/session/auth/markup/network/database surface | source inventory and approved no-UI contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test |
|---|---|---|---|---|---|---|
| T-108-14 | Tampering | caller scalar array | high | mitigate | Complete validation then request-owned copy before Font work | mutate-after-snapshot plus adjacency/equal-value fixture |
| T-108-15 | Spoofing | PositionedGlyph GlyphId | high | mitigate | Accept only owner-bound GlyphId from mb-font authority | distinct same-range foreign glyph rejection |
| T-108-16 | Denial of Service | ShapeLimits / Budget | high | mitigate | Nonzero semantic ceilings, checked charges, exact complete preflight | zero/exact/one-over matrix |
| T-108-17 | Tampering | RTL/total arithmetic | high | mitigate | Shared checked add/negate/narrow and no fallible work after commit | MIN/MAX and advance/total overflow fixtures |
| T-108-18 | Information Disclosure | ShapedRun / `.mbti` | medium | mitigate | Private copied arrays and named scalar accessors only | `glyph_at` plus interface negative gate |
| T-108-19 | Repudiation | error-stage winner | medium | mitigate | Literal D-15 pipeline and stable D-16 operation/context tuples | simultaneous-fault precedence matrix |
| T-108-20 | Elevation of Privilege | public nonempty route | high | mitigate | Capability fail-closed before private generated facts | black-box nonempty no-publication/no-charge test |
| T-108-21 | Tampering | retained Font / shared Budget | high | mitigate | Synchronous transaction, revision probes, no cache, one commit | named drift probes and caller/ancestor snapshots |
</threat_model>

<verification>
- Run focused native `mb-text` check and tests after each task.
- Run `moon -C modules/mb-text test text --target js --frozen`, then wasm, wasm-gc, and native explicitly.
- Run `moon -C modules/mb-text info --target all --frozen` and verify only the locked closed input/run surface is emitted.
- Run workspace `moon test --target js|wasm|wasm-gc|native --frozen` as four explicit commands after the complete text contract lands; record these as contract portability, not Phase 113 semantic qualification.
- Confirm the approved no-UI contract: no frontend, style, visual, screenshot, responsive, or accessibility-widget path appears in this plan's changes.
</verification>

<success_criteria>
- TXT-01 input/options/limits/empty/fail-closed behavior is complete and deterministic.
- TXT-02 immutable run, same-Font identity, clusters, order, signs, offsets, totals, and overflow behavior is executable.
- The complete stage precedence matrix is stable and failure-atomic.
- No format parser/executor or UI surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/108-public-contract-and-transaction-skeleton/108-04-SUMMARY.md` when done.
</output>
