---
phase: 108-public-contract-and-transaction-skeleton
plan: "05"
type: execute
wave: 5
depends_on:
  - 108-01
  - 108-02
  - 108-03
  - 108-04
files_modified:
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - modules/mb-core/README.mbt.md
  - modules/mb-core/CHANGELOG.md
  - modules/mb-font/README.mbt.md
  - modules/mb-text/README.mbt.md
  - modules/mb-text/CHANGELOG.md
autonomous: true
requirements:
  - TXT-01
  - TXT-02
must_haves:
  truths:
    - "D-01/D-03/D-09/D-10: generated interface evidence exposes exactly @text.shape(font, scalars, options, limits, budget), closed typed inputs, and opaque immutable run accessors only."
    - "The sealed font interface contains exactly Font::with_shape_transaction[T](self : Font, budget : @budget.Budget, body : (FontShapeScope) -> Result[(T, @budget.ResourceCharge), @error.CoreError]) -> Result[T, @error.CoreError]; nominal scope escape is possible but post-callback operations fail with the exact closed-scope State error."
    - "The sealed ShapeLimits interface contains exactly new(max_input_scalars~, max_output_glyphs~), max_input_scalars(), and max_output_glyphs(), with both UInt64 values nonzero and no default or other limit group."
    - "D-13: policy enforces mb-text -> mb-font -> mb-core plus direct mb-text -> mb-core and forbids reverse mb-font -> mb-text imports."
    - "D-14/D-15/D-16: policy and required-lane tests bind one-commit authority, stage precedence, and stable CoreError contexts by named evidence."
    - "The complete v0.34 mb-font semantic interface remains present with only planned additive scope symbols and private GlyphId strengthening."
    - "mb-text builds and tests on js, wasm, wasm-gc, and native under explicit per-target lanes; Phase 113 retains semantic qualification authority."
    - "The approved no-UI contract holds: no frontend, visual, screenshot, responsive, registry, or accessibility-widget artifact exists."
    - "No external API, service, SDK, FFI, or registry package is integrated."
  artifacts:
    - path: "policy/foundation.json"
      provides: "Closed mb-text module/package/dependency/source/interface/publication inventory and additive mb-core/mb-font interface facts"
    - path: "scripts/quality/Assert-Policy.ps1"
      provides: "Fail-closed workspace, dependency, source, interface, and no-leakage gates"
    - path: "scripts/quality/Invoke-MoonQuality.ps1"
      provides: "Explicit four-target mb-text Required-lane check/test/doc/info/package execution"
    - path: "modules/mb-text/README.mbt.md"
      provides: "Literate public shaping contract and executable empty/fail-closed examples"
    - path: "modules/mb-text/CHANGELOG.md"
      provides: "Candidate 0.1.0 contract and explicit Phase 108 boundary"
  key_links:
    - from: "policy/foundation.json"
      to: "modules/mb-text/moon.mod.json"
      via: "exact identity, targets, dependencies, sources, publication files, and public package"
      pattern: "tchivs/mb-text"
    - from: "scripts/quality/Assert-Policy.ps1"
      to: "generated mb-core/mb-font/mb-text interfaces"
      via: "semantic allowlists preserve v0.34 and reject unplanned public authority"
      pattern: "mb-text|FontShapeScope|ResourceCharge"
    - from: "scripts/quality/Invoke-MoonQuality.ps1"
      to: "modules/mb-text"
      via: "independent module check/test/doc/info/package commands for each required target"
      pattern: "mb-text"
  prohibitions:
    - { statement: "No reverse module edge, path dependency, manifest migration, native FFI, external SDK, or registry code is introduced.", status: unverified, flagged: true }
    - { statement: "No public interface inventory contains raw arrays, source bytes, table facts, lookup indices, mutation probes, scope constructors, or commit handles.", status: unverified, flagged: true }
    - { statement: "No documentation or quality claim describes Phase 108 as semantic four-target qualification or successful nonempty real-font shaping.", status: unverified, flagged: true }
---

<objective>
Seal the Phase 108 module, interface, dependency, documentation, and explicit four-target contract gates.

Purpose: Turn the implemented contract into a fail-closed repository-owned publication boundary without overstating nonempty capability or Phase 113 qualification.
Output: Exact policy inventories, Required-lane execution for mb-text, literate documentation, additive v0.34-compatible interface evidence, and a complete four-source coverage audit.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-CONTEXT.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-RESEARCH.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-PATTERNS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-UI-SPEC.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-01-SUMMARY.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-02-SUMMARY.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-03-SUMMARY.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-04-SUMMARY.md

<interfaces>
The policy seal inventories:
- mb-core additions: `ResourceCharge::checked_add`, `checked_add_int64`, `checked_neg_int64`, and `checked_uint64_to_int64`.
- mb-font additions: public-abstract `FontShapeScope` and exactly `Font::with_shape_transaction[T](self : Font, budget : @budget.Budget, body : (FontShapeScope) -> Result[(T, @budget.ResourceCharge), @error.CoreError]) -> Result[T, @error.CoreError]`, while preserving all existing Font/GlyphId signatures.
- mb-text closed surface: ScriptTag, LanguageTag, LanguageChoice, Direction, FeaturePolicy, ShapingOptions, exactly the two-field ShapeLimits constructor/accessors, PositionedGlyph, ShapedRun, and exactly `@text.shape(font, scalars, options, limits, budget)`.
- No private generated fixture, probe, backing array, owner identity, source/table fact, charge ledger, or commit capability may appear in generated interfaces.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the exact module DAG, source inventories, and semantic interfaces</name>
  <reversibility rating="costly">Changing policy-owned publication or semantic-interface inventories requires coordinated module, quality-script, and downstream package updates.</reversibility>
  <read_first>
    - policy/foundation.json
    - scripts/quality/Assert-Policy.ps1
    - scripts/quality/Invoke-MoonQuality.ps1
    - moon.work
    - modules/mb-core/moon.mod.json
    - modules/mb-font/moon.mod.json
    - modules/mb-text/moon.mod.json
    - modules/mb-text/text/moon.pkg
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-PATTERNS.md
  </read_first>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - "Policy recognizes exactly tchivs/mb-text@0.1.0, its path, candidate stability, four targets, direct mb-core/mb-font dependencies, publication files, public package, allowed imports, and exact source/test inventory."
    - "Policy preserves the complete mb-core/mb-font interface and accepts only the planned additive public symbols."
    - "Policy requires the exact generic tuple callback and exact two-field ShapeLimits constructor/accessors; alternate carriers/defaults/limit groups fail."
    - "A reverse edge, extra import, omitted source, extra public fixture/probe symbol, or changed v0.34 signature fails policy."
    - "Required quality independently checks/tests mb-text and checks its literate docs/info/package on js, wasm, wasm-gc, and native."
    - "No UI, host/FFI, bytes, image, canvas, registry, or external API dependency is allowed for mb-text."
  </behavior>
  <action>
Add the exact `tchivs/mb-text` module and public-package record to `policy/foundation.json`, including final source/test/publication inventories, four targets, direct dependencies, allowed imports, and generated semantic interface. Update mb-core and mb-font semantic allowlists additively for Plans 108-01 through 108-03 while preserving every qualified v0.34 signature. Seal the exact generic tuple callback with no separate outcome carrier, the exact two-field ShapeLimits constructor/accessors with no alternate limit API, and exactly `@text.shape(font, scalars, options, limits, budget)`.

Extend `Assert-Policy.ps1` expected module/path/workspace sets and exact dependency/interface/source checks. Add fail-closed negatives for reverse dependency, unplanned import, public raw record/table/source/probe/commit authority, public fixture seams, and public nonempty-success claims. Scope checks to semantic regions where legitimate private test names exist.

Add mb-text to the Required-lane independent module loop and literate README/info/package checks in `Invoke-MoonQuality.ps1`, preserving explicit js, wasm, wasm-gc, and native execution. Do not add a UI lane or claim semantic target equivalence.
  </action>
  <acceptance_criteria>
    - "D-13 dependency ownership is machine-enforced and acyclic."
    - "D-01/D-03/D-09/D-10 interfaces contain exactly the closed shape/limits/run surface and no private authority."
    - "The callback signature and runtime closed-scope behavior are policy-bound with no alternate carrier."
    - "v0.34 mb-font compatibility is an exact assertion."
    - "Required quality runs mb-text independently on all four supported targets."
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'
./scripts/quality/Assert-Policy.ps1 && moon -C modules/mb-text info --target all --frozen</automated>
  </verify>
  <done>The repository recognizes one exact mb-text publication unit, its allowed DAG/interfaces are closed, and any unplanned public/dependency/source drift fails policy.</done>
</task>

<task type="auto">
  <name>Task 2: Document the contract and execute the complete Phase 108 portability gates</name>
  <read_first>
    - modules/mb-core/README.mbt.md
    - modules/mb-core/CHANGELOG.md
    - modules/mb-font/README.mbt.md
    - modules/mb-font/CHANGELOG.md
    - modules/mb-text/moon.mod.json
    - modules/mb-text/text/contract_test.mbt
    - modules/mb-text/text/contract_wbtest.mbt
    - .planning/phases/108-public-contract-and-transaction-skeleton/108-UI-SPEC.md
    - .planning/ROADMAP.md
  </read_first>
  <files>modules/mb-core/README.mbt.md, modules/mb-core/CHANGELOG.md, modules/mb-font/README.mbt.md, modules/mb-text/README.mbt.md, modules/mb-text/CHANGELOG.md</files>
  <action>
Document checked charge composition and signed helpers in mb-core; owner-bound glyph compatibility, exact generic tuple callback, and runtime scope invalidation in mb-font; and the complete closed shape/options/two-field-limits/run/empty/fail-closed contract in a new literate mb-text README. State nominal generic-T scope escape and the exact post-callback State/InvalidRange error without claiming static lifetime enforcement. State scalar-index clusters, exact empty work=1, logical LTR/final-only RTL signed projection, immutable access, one combined commit, synchronous/no-cache behavior, no concurrent mutation promise for one retained Font/Budget authority, and stable error precedence. Record candidate-compatible additions in the mb-core and mb-text changelogs; keep the mb-font README consistent with the mb-font changelog entry already committed by Plan 108-03.

Keep claims exact: Phase 108 demonstrates contract portability/build/tests on four targets but does not claim Phase 113 semantic qualification. Make the format boundary explicit without adding parsers/executors. Preserve the approved no-UI determination.

Run focused module tests, explicit workspace tests for each target, `moon info`, policy, Required lane, and FontQualification regression. Treat every named contract test as required evidence; do not certify from aggregate counts alone. Verify the worktree diff is limited to planned production/governance/docs files plus executing phase summaries.
  </action>
  <acceptance_criteria>
    - "Documentation exactly describes D-01 through D-16 and the public nonempty Capability boundary."
    - "TXT-01/TXT-02 named tests pass on js, wasm, wasm-gc, and native."
    - "Required and FontQualification lanes pass without v0.34 regression."
    - "No UI artifact or semantic qualification claim is introduced."
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'
moon check --target all --deny-warn --frozen && moon test --target all --frozen && ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/phase108 && ./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory artifacts/release-qualification/phase108-font</automated>
  </verify>
  <done>The contract is accurately documented, policy-sealed, v0.34-compatible, and green under all focused/workspace/interface/quality portability gates.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan contributes:

- the `tchivs/mb-text` module/package/dependency/source/publication/interface record in `policy/foundation.json`
- exact module, dependency, interface, source, workspace, and no-leakage gates in `scripts/quality/Assert-Policy.ps1`
- explicit four-target mb-text Required-lane execution in `scripts/quality/Invoke-MoonQuality.ps1`
- `modules/mb-text/README.mbt.md` and `modules/mb-text/CHANGELOG.md`
- additive contract documentation in the existing mb-core README/changelog and mb-font README, consistent with Plan 108-03's mb-font changelog entry

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Stable format-neutral values cross opaque font/text boundary and publish after one combined authority commit | 108-01..05 | COVERED | Tracer proves the path; later plans harden and seal it |
| REQ | TXT-01 | Explicit bounded scalar/font/options/limits/Budget shape contract without ambient policy | 108-01, 108-03, 108-04, 108-05 | COVERED | Empty route, transaction, closed inputs, and policy |
| REQ | TXT-02 | Immutable same-Font run, scalar clusters, checked signed projection and total | 108-02, 108-03, 108-04, 108-05 | COVERED | Arithmetic, owner identity, generated projection, and policy |
| RESEARCH | — | Checked immutable ResourceCharge composition | 108-01, 108-03 | COVERED | Public composition plus per-dimension hardening |
| RESEARCH | — | Compile-proven public-abstract callback-scoped FontShapeScope | 108-01, 108-03 | COVERED | Tracer compile proof plus lifetime/mutation hardening |
| RESEARCH | — | Exact two-field nonzero ShapeLimits and no alternate surface | 108-01, 108-04, 108-05 | COVERED | Validation, generated contract, and interface seal |
| RESEARCH | — | Same-Font GlyphId ownership preserving v0.34 signatures | 108-03, 108-05 | COVERED | Private owner plus exact interface gate |
| RESEARCH | — | Checked signed Int64 helpers and final-only RTL projection | 108-02, 108-04 | COVERED | Independent helpers plus hand-derived fixtures |
| RESEARCH | — | Empty exact work=1 and public nonempty fail-closed | 108-01, 108-04 | COVERED | Black-box production behavior |
| RESEARCH | — | Frozen stage/mutation/error precedence and one commit | 108-01, 108-03, 108-04 | COVERED | Named probes and multi-fault matrix |
| RESEARCH | — | Module/workspace/policy/docs/four-target integration | 108-01, 108-05 | COVERED | Local module and Required lane |
| RESEARCH | — | No external package, FFI, UI, format parser/executor, or semantic qualification claim | 108-01..05 | COVERED | Prohibitions plus policy gates |
| CONTEXT | D-01 | One closed shape operation | 108-01, 108-05 | COVERED | Tracer and interface seal |
| CONTEXT | D-02 | Scalar array validation and owned snapshot | 108-04 | COVERED | Snapshot mutation test |
| CONTEXT | D-03 | Opaque immutable run and indexed values | 108-01, 108-04, 108-05 | COVERED | Run implementation and interface gate |
| CONTEXT | D-04 | Same-Font GlyphId and signed positioned fields | 108-03, 108-04 | COVERED | Owner identity and run fixtures |
| CONTEXT | D-05 | Logical shaping and final-only RTL record reversal | 108-04 | COVERED | Hand-derived fixture |
| CONTEXT | D-06 | Signed advances and checked total | 108-02, 108-04 | COVERED | Shared checked helpers |
| CONTEXT | D-07 | Design-space offsets unchanged by RTL | 108-04 | COVERED | Exact offset fixture |
| CONTEXT | D-08 | Scalar clusters and ligature minimum | 108-04 | COVERED | Adjacency/ligature fixtures |
| CONTEXT | D-09 | Checked tags, language choice, direction | 108-01, 108-04 | COVERED | Closed value constructors |
| CONTEXT | D-10 | Closed liga/kern and non-disableable required/rlig | 108-01, 108-04 | COVERED | FeaturePolicy contract |
| CONTEXT | D-11 | Validated, guarded, charged empty success | 108-01, 108-04 | COVERED | Production tracer |
| CONTEXT | D-12 | Caller failures and foreign glyph rejection | 108-03, 108-04 | COVERED | Error and ownership tests |
| CONTEXT | D-13 | mb-text -> mb-font -> mb-core ownership | 108-01, 108-03, 108-05 | COVERED | Scope and DAG policy |
| CONTEXT | D-14 | One combined charge/guard/publication | 108-01, 108-03 | COVERED | Authority atomicity |
| CONTEXT | D-15 | Frozen stage precedence | 108-01, 108-03, 108-04 | COVERED | Multi-fault matrix |
| CONTEXT | D-16 | Stable CoreError operations/contexts | 108-01..05 | COVERED | Tests, docs, and policy |

No source item is missing. CONTEXT deferred ideas and research items assigned to Phases 109-113 are excluded rather than implemented here.

<threat_model>
## Assets

| Asset | Security property |
|---|---|
| Module DAG and publication inventory | No reverse dependency or untracked source |
| Public semantic interfaces | No private authority or mutable-state leakage |
| v0.34 compatibility | Existing qualified Font behavior remains intact |
| Four-target evidence claims | Exact scope; no false semantic qualification |

## Trust Boundaries

| Boundary | Description |
|---|---|
| source tree -> policy inventory | New or omitted files/imports can bypass review |
| private implementation -> generated interface | Owner, probes, arrays, bytes, or commits may leak |
| module manifest -> workspace/quality runner | Dependency/target drift can create an untested route |
| test output -> documentation claim | Aggregate green output can overstate nonempty/target semantics |

## ASVS L1 mapping

| Control area | Applies | Mitigation / task | Verification |
|---|---|---|---|
| Input validation | yes | Task 1 seals named scalar/tag/choice/ShapeLimits tests and interface | policy plus Required-lane tests |
| Architecture / business logic | yes | Task 1 enforces DAG, exact callback, one-commit path, and closed API | policy, info, named atomicity tests |
| Access / identity boundary | yes | Policy retains owner-bound GlyphId and public-abstract scope while excluding constructors/probes/raw authority | interface negatives and FontQualification |
| Error handling / data protection | yes | Stable CoreError facts and opaque copied values are policy/doc requirements | named matrix and interface inventory |
| Resource / DoS | yes | Required lanes include exact limits, checked arithmetic/charge, preflight, and one commit | four-target boundary suites |
| Build / config / supply chain | yes | Exact local dependency/source inventories, frozen commands, four-target lanes, no install | policy, Required, info/package/doc |
| Browser, session, authentication, HTML, transport, database | no | No web/session/auth/network/database surface and no UI/SDK | source inventory and UI-SPEC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test |
|---|---|---|---|---|---|---|
| T-108-22 | Tampering | module DAG/policy | high | mitigate | Exact dependency/import/workspace/source/publication inventories | reverse-edge and extra-import negatives |
| T-108-23 | Information Disclosure | generated interfaces | high | mitigate | Exact semantic allowlists for closed values and opaque scope | raw-authority negative fixtures |
| T-108-24 | Spoofing | v0.34 compatibility claim | high | mitigate | Preserve existing mb-font signatures and run FontQualification | interface diff and regression lane |
| T-108-25 | Repudiation | error/atomicity evidence | medium | mitigate | Bind named tests and stable contexts in policy/docs | named contract matrix |
| T-108-26 | Denial of Service | hostile scalars/limits/overflow | high | mitigate | Require Plans 108-01 through 108-04 boundary suites on every target | focused Required-lane tests |
| T-108-27 | Elevation of Privilege | nonempty/public authority | high | mitigate | Reject public fixture/probe/commit symbols and nonempty success claims | info plus policy negatives |
| T-108-28 | Tampering | retained Font/Budget authority | high | mitigate | Execute named mutation/ancestor/one-commit tests | font/text white-box matrices |
| T-108-29 | Information Disclosure | UI/external scope | low | accept | Pure local library with approved no-UI contract and no external integration | source inventory and UI-SPEC |
</threat_model>

<verification>
- Run `moon -C modules/mb-text info --target all --frozen`.
- Run explicit package checks/tests on js, wasm, wasm-gc, and native for mb-core budget/checked, mb-font font, and mb-text text.
- Run workspace `moon check --target all --deny-warn --frozen` and `moon test --target all --frozen`.
- Run `./scripts/quality/Assert-Policy.ps1`, Required, and FontQualification lanes.
- Verify from local manifests/package/policy that no external API, service, SDK, network transport, or registry dependency exists.
</verification>

<success_criteria>
- Every GOAL/REQ/RESEARCH/CONTEXT item is covered with no deferred implementation.
- Exact interfaces and module DAG are fail-closed and v0.34-compatible.
- Focused, per-target, workspace, policy, Required, and FontQualification gates pass.
- Documentation states only the Phase 108 contract portability and fail-closed capability delivered.
</success_criteria>

<output>
Create `.planning/phases/108-public-contract-and-transaction-skeleton/108-05-SUMMARY.md` when done.
</output>
