---
phase: 49-portable-gray16-public-evidence
plan: "01"
type: tdd
wave: 1
depends_on: [48-01]
autonomous: true
requirements: [GRAY16-03]
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
---

<objective>
Add portable public evidence for the completed Gray16 PNG route without changing any production contract. The tests prove exact two-byte PNG wire samples independently of source endianness, documented public RGB canonicalization, hostile caller-lease parity, frozen legacy vectors, and all four supported targets.

Purpose: close `GRAY16-03` with generated semantic fixtures rather than opaque Gray16 snapshots or target-specific behavior.
Output: public-evidence-only regressions in the two existing PNG test files.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-portable-gray16-public-evidence/49-CONTEXT.md
@.planning/phases/49-portable-gray16-public-evidence/49-RESEARCH.md
@.planning/phases/48-bounded-gray16-encoder-path/48-VERIFICATION.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<source_audit>
SOURCE | ID | Required evidence | Coverage
ROADMAP | GRAY16-03 | Wire preservation, hostile chunk parity, legacy vectors, four independent targets | Tasks 1-2
CONTEXT | D-01 | Explicit public Gray16 eager/chunk constructors only | Tasks 1-2
CONTEXT | D-02 | Both U16 storage endiannesses, inflated high/low bytes, public RGB canonicalization | Task 1
CONTEXT | D-03 | Zero/one/ragged leases, accepted-only progress, terminal stickiness across all six pairs | Task 2
CONTEXT | D-04 | js, wasm, wasm-gc, native commands and frozen Gray8/RGB/RGBA vectors | Tasks 1-2
</source_audit>

<guardrails>
- Modify exactly `modules/mb-image/png/encode_test.mbt` and `modules/mb-image/png/stream_encode_test.mbt`.
- Exercise `PngEncoder::new_gray16_with_strategies` and `PngChunkEncoder::new_gray16_with_strategies`; do not use private constructors or add APIs.
- Do not modify production PNG/storage code, build configuration, scripts, fixtures, directories, STATE, ROADMAP, REQUIREMENTS, CONTEXT, or RESEARCH.
- The test-local wire oracle may parse only the known `Stored`/`None` zlib stored-block shape. It is bounded by the small generated fixture and must not become a general inflater, reusable encoder buffer, or new stream driver.
- Keep Gray16 Adam7, Gray+alpha, RGB/RGBA16, palette/low-bit formats, release automation, and target-specific fallbacks out of scope.
</guardrails>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD public eager Gray16 wire and decoder evidence</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - `encode_test.mbt` — reuse `png_encode_gray16_image`, `png_encode_gray16_big_image`, `png_encode_prefix`, public decoder helpers, and frozen compatibility vectors.
    - `49-RESEARCH.md` — use the specified 3×2 non-symmetric U16 sample grid and exact inflated Stored/None scanlines.
    - `48-VERIFICATION.md` — preserve Phase 48's public-constructor and wire-order baseline; do not re-open production implementation scope.
  </read_first>
  <behavior>
    - A 3×2 generated packed Gray16 fixture written through `set_component_byte` produces the same public eager PNG for little- and big-endian source storage, with IHDR `[24,25,28] == [0x10,0x00,0x00]`.
    - The public `Stored`/`None` output inflates to the exact two rows `00 12 34 ab cd 00 ff` and `00 7f 01 80 02 fe 10`, proving high-byte/low-byte preservation for every non-symmetric sample.
    - Public `PngDecoder` canonicalizes those samples to RGB8 by repeating only each wire high byte (`12, ab, 00, 7f, 80, fe`) across all RGB channels; the low byte is deliberately not expected at this API boundary.
    - All six explicit compression/filter pairs complete through `PngEncoder::new_gray16_with_strategies` and retain valid Gray16 IHDR plus public decoder canonicalization.
    - Literal frozen Stored vectors for explicit Gray8, legacy RGB8, and straight-RGBA8 remain byte-identical and are not derived from the current encoder.
  </behavior>
  <implementation>
    1. **RED:** Add test names/assertions that reference a missing generated public-fidelity fixture and known-`Stored`/`None` IDAT parser; run the focused native tests and record the expected compile/test failure caused by the missing test helpers.
    2. **GREEN:** Add a local fixture builder parameterized by `@model.Endianness`. Construct the exact 3×2 descriptor with tight rows and canonical metadata; write the six samples using storage-order component bytes: `12 34`, `ab cd`, `00 ff`, `7f 01`, `80 02`, `fe 10`.
    3. Add a small local PNG chunk walker that concatenates IDAT payload bytes only for the known public `Stored`/`None` case. Parse and validate the zlib header, the final stored-block marker, `LEN`/one's-complement `NLEN`, and exactly 14 uncompressed scanline bytes (optionally validate the trailing Adler-32). Assert `00 12 34 ab cd 00 ff 00 7f 01 80 02 fe 10`, both-endian whole-PNG equality, and the explicit IHDR fields. This parser must not use private inflater APIs, and Fixed/Dynamic/Adaptive payloads remain semantic decode/identity tests rather than snapshots.
    4. Decode only through `@codec.ImageDecoder::decode(PngDecoder::new(), ...)`, assert `Rgb`/three U8 channels, and compare each output RGB triplet to the corresponding wire high byte. Loop all six strategy/filter pairs with public `PngEncoder::new_gray16_with_strategies` for complete decode/canonicalization evidence.
    5. Extend the existing frozen eager-vector test with the explicit Gray8 literal while retaining the established RGB8/RGBA8 literals and legacy/default/configured route assertions.
  </implementation>
  <verification>
    - `moon -C modules/mb-image test png --target native --frozen --filter "Gray16 public eager"`
    - `moon -C modules/mb-image test png --target native --frozen --filter "frozen compatibility"`
  </verification>
  <done>Public eager tests prove every U16 high/low wire byte for both storage endiannesses, documented RGB canonicalization, six-pair constructor behavior, and frozen legacy/Gray8 compatibility without production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD caller-buffered hostile-capacity and portable target evidence</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - `stream_encode_test.mbt` — copy the Gray8 hostile-capacity public pattern, one-byte pull helpers, accepted-progress assertions, terminal sentinel checks, and frozen chunk vectors.
    - `encode_test.mbt` — keep the same six-sample Gray16 semantic corpus and public eager-oracle expectations; do not share a new test module.
    - `49-RESEARCH.md` — use the exact zero/one/ragged schedules and independent four-target commands.
  </read_first>
  <behavior>
    - Every `Stored` / `FixedOrStored` / `DynamicOrFixedOrStored` × `None` / `Adaptive` Gray16 pair uses `PngChunkEncoder::new_gray16_with_strategies` and is byte-identical to a fresh eager public oracle under `[0UL,1UL]`, `[1UL]`, and `[0UL,8UL,4UL,1UL,13UL,2UL,5UL,3UL,21UL]` schedules.
    - An explicit empty first lease reports `NeedOutput`, `written==0UL`, `total_written==0UL`, and leaves its sentinel intact. Nonempty drains advance `total_written` only by accepted `written` bytes, finish, and a later sentinel lease returns zero bytes with sticky `Finished`.
    - The public chunk target-level evidence retains literal Gray8, RGB8, and straight-RGBA8 compatibility vectors alongside the new Gray16 matrix.
    - The complete PNG suite runs independently on js, wasm, wasm-gc, and native; no target uses another target's result as evidence.
  </behavior>
  <implementation>
    1. **RED:** Add the caller-buffered public-evidence test invocation/expectations before its local Gray16 fidelity helper and schedule driver exist; run the focused native test and capture the expected missing-helper failure.
    2. **GREEN:** Add a local 3×2 little-endian Gray16 fidelity fixture matching Task 1's logical wire samples and a focused drain helper that records each pull's accepted-only progress, completes, and verifies one later sentinel lease. Use fresh encoders for zero-prefixed, one-byte, and ragged schedules.
    3. For each of the six public strategy/filter pairs, build the eager oracle with `PngEncoder::new_gray16_with_strategies`, inspect the zero-capacity lease directly, then require every independent drain to equal that oracle and preserve terminal stickiness. Do not reuse Phase 48 mutation encoders or add a second stream implementation.
    4. Extend the existing frozen chunk compatibility test with explicit Gray8 literal coverage while preserving legacy RGB8/RGBA8 literal vectors and their configured/default assertions.
    5. Run focused native tests during iteration, then execute all four full package commands independently. Treat any target failure as a portability defect; do not skip it or introduce target branches.
  </implementation>
  <verification>
    - `moon -C modules/mb-image test png --target native --frozen --filter "Gray16 chunk public evidence"`
    - `moon -C modules/mb-image test png --target js --frozen`
    - `moon -C modules/mb-image test png --target wasm --frozen`
    - `moon -C modules/mb-image test png --target wasm-gc --frozen`
    - `moon -C modules/mb-image test png --target native --frozen`
  </verification>
  <done>All public Gray16 strategy pairs retain eager-byte identity under zero/one/ragged leases with accepted-only and sticky terminal semantics, legacy vectors remain frozen, and the exact same public suite passes independently on every supported target.</done>
</task>

</tasks>

<verification>
1. Confirm the scoped diff changes only `modules/mb-image/png/encode_test.mbt` and `modules/mb-image/png/stream_encode_test.mbt`.
2. Confirm tests use only public Gray16 factories and public decoder entry points; the sole structural oracle is the test-local known-`Stored`/`None` IDAT parser, which validates zlib header, stored-block `LEN`/`NLEN`, and the 14-byte scanline fixture without private inflater APIs.
3. Run all four independent package commands:
   - `moon -C modules/mb-image test png --target js --frozen`
   - `moon -C modules/mb-image test png --target wasm --frozen`
   - `moon -C modules/mb-image test png --target wasm-gc --frozen`
   - `moon -C modules/mb-image test png --target native --frozen`
</verification>

<success_criteria>
- [ ] Both source storage endiannesses produce identical exact Gray16 Stored/None inflated wire scanlines with all six high/low sample pairs preserved.
- [ ] Public decoder evidence documents RGB8 high-byte canonicalization without falsely claiming U16 low-byte round-trip.
- [ ] All six explicit Gray16 public strategy/filter pairs have eager and zero/one/ragged chunk identity, accepted-only progress, completion, and sticky terminal evidence.
- [ ] Explicit Gray8 plus legacy RGB8/RGBA8 frozen literals remain in both eager and caller-buffered target-level evidence.
- [ ] The complete public PNG test package passes independently on js, wasm, wasm-gc, and native.
- [ ] No production code, public API, script, fixture directory, staging buffer, or Phase 49-excluded feature is added.
</success_criteria>
