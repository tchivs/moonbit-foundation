---
phase: 34-portable-png-compression-corpus-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [PNGC-04]

must_haves:
  truths:
    - "The stable corpus test generates exactly flat 32x1 RGB8 and straight-RGBA8 sources whose components are 0xaa, then proves FixedOrStored is never larger than explicit Stored and strictly smaller for both records."
    - "Two FixedOrStored eager encodes and a configured caller-buffered encode drained under [0, 1, 3, 2, 5] produce identical bytes for each corpus record."
    - "Both optimized byte sequences complete-input decode to dimensions, channel count, and every component of their generated source on js, wasm, wasm-gc, and native."
    - "Each target's outline names the one corpus test before its isolated filtered execution, and the existing PNG quality lane remains the broader regression gate."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Named public PNG compression corpus evidence with source-component decode oracle and FixedOrStored/Stored relative-size assertions."
  key_links:
    - from: "png_stream_test_fixed_or_stored_corpus_case"
      to: "PngEncoder::new_with_compression_strategy and PngChunkEncoder::new_with_compression_strategy"
      via: "Public Stored baseline, repeated FixedOrStored eager construction, and configured chunk drain."
    - from: "optimized eager and chunk Bytes"
      to: "PngDecoder::new complete-input decode"
      via: "Decoder result dimensions/channel count and every source component are compared."
---

<objective>
Add reproducible, portable PNGC-04 corpus evidence through one stable package test.

Purpose: Prove the opt-in FixedOrStored route remains valid, deterministic, never larger than explicit Stored, and strictly wins on the declared repetitive RGB8 and straight-RGBA8 sources without changing encoder behavior.
Output: One test-only source update plus executable four-target and full PNG regression commands.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-portable-png-compression-corpus-evidence/34-RESEARCH.md
@.planning/phases/32-png-compression-strategy-and-compatibility/32-01-SUMMARY.md
@.planning/phases/33-fixed-or-stored-png-planning-and-emission/33-02-SUMMARY.md
@.planning/phases/33-fixed-or-stored-png-planning-and-emission/33-VERIFICATION.md
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/moon.pkg
</context>

<source_audit>
| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Four-target reproducible evidence of valid, deterministic intended-case compression | 34-01 | COVERED | Tasks 1-2 implement the public corpus and isolated target execution. |
| REQ | PNGC-04 | Decode round trips; FixedOrStored never larger than Stored; declared flat RGB8/RGBA8 wins | 34-01 | COVERED | Task 1 supplies component-level decode, relative-size, repeat-eager, and chunk-parity assertions; Task 2 invokes both declared cases. |
| RESEARCH | — | Two direct 32x1 0xaa records; public APIs only; [0,1,3,2,5] chunk schedule; stable outlined selector | 34-01 | COVERED | Tasks cite the exact helper paths, test name, and runner protocol. |
| RESEARCH | — | No external corpus, benchmark, runner, implementation, public API, default-behavior, or quality-lane changes | 34-01 | COVERED | The plan modifies only the existing test source and uses commands solely as verification. |
| CONTEXT | — | No CONTEXT.md and no D-XX locked decisions exist | 34-01 | COVERED | Research confirms the approved roadmap and requirement are the governing constraints. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable public corpus and decode-oracle helpers</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Add test-only helpers beside the existing selected-strategy eager, flat-image, and chunk-drain helpers. The decode helper must materialize the completed Bytes as OwnedBytes with the existing test budget, call PngDecoder through ImageDecoder::decode with require_complete_input=true, then assert decoded width, height, and format channel count equal the source and compare every x/channel component in nested loops. The corpus-case helper must accept one generated source, encode it once with explicit Stored, encode it twice with FixedOrStored through png_stream_test_eager_with_strategy, construct PngChunkEncoder::new_with_compression_strategy with FixedOrStored and drain it through png_chunk_test_drain_encoder using [0UL, 1UL, 3UL, 2UL, 5UL]. Assert optimized eager length is at most Stored length and is strictly smaller, assert the two eager byte sequences and chunk byte sequence are equal, and pass both optimized byte sequences through the new decoder/source oracle. Keep this entirely at the public encoder/decoder boundary: do not inspect DEFLATE bits, private plans, or byte-count constants.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The reusable corpus helper measures explicit Stored versus FixedOrStored output, proves repeated eager and hostile-capacity chunk determinism, and independently decodes each optimized product back to every source component.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the exact two-record corpus and prove it on every portable target</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Add exactly one test named `PNG fixed-or-stored corpus evidence is deterministic, valid, never-larger, and wins flat RGB8/RGBA8`. Its body must invoke the corpus-case helper exactly twice: first with png_stream_test_flat_image(3UL, 32UL), then with png_stream_test_flat_image(4UL, 32UL). These existing constructors establish the required 32x1 flat RGB8 and straight-RGBA8 sources with every component set to 0xaa. Preserve the test-only scope: edit no encoder, decoder, public declaration, policy, CI, quality-lane, script, fixture, benchmark, or corpus-generation file. Execute the target-isolated outline-and-filter sequence in verification, requiring each outline to contain this one exact name before accepting the filtered result.</action>
  <verify>
    <automated>$filter = '*PNG fixed-or-stored corpus evidence*'; $testName = 'PNG fixed-or-stored corpus evidence is deterministic, valid, never-larger, and wins flat RGB8/RGBA8'; foreach ($target in 'js','wasm','wasm-gc','native') { $outline = &amp; moon -C modules/mb-image test png --target $target --frozen --outline -f $filter 2&gt;&amp;1; if ($LASTEXITCODE -ne 0) { throw "outline failed for $target" }; if (@($outline | Select-String -SimpleMatch $testName).Count -ne 1) { throw "outline did not name exactly one corpus test for $target" }; &amp; moon -C modules/mb-image test png --target $target --target-dir "_build/png-fixed-or-stored-corpus/$target" --frozen -f $filter; if ($LASTEXITCODE -ne 0) { throw "filtered corpus failed for $target" } }</automated>
  </verify>
  <done>The stable test exercises only the declared RGB8 and straight-RGBA8 flat sources, every target outline names it exactly once, and each isolated filtered execution passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated source image to public encoder | Deterministic in-test pixels cross into the configured Stored and FixedOrStored public encoding routes. |
| Optimized encoded bytes to public decoder | Completed eager and caller-buffered bytes are treated as decoder input before evidence is accepted. |
| Named test selector to target runner | A MoonBit glob controls which assertions run on each portable target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-34-01 | Tampering | Corpus size oracle | medium | mitigate | Compare completed public FixedOrStored eager bytes against an explicit public Stored eager baseline for each of the two declared records using relative predicates. |
| T-34-02 | Tampering | Optimized output parity evidence | medium | mitigate | Decode eager and hostile-capacity chunk bytes in complete-input mode and compare dimensions, channel count, and every source component. |
| T-34-03 | Tampering | Filtered portable test execution | medium | mitigate | On js, wasm, wasm-gc, and native, require the target outline to contain the exact stable test name once before running the same filter in its dedicated target directory. |
| T-34-04 | Tampering / Denial of Service | Corpus inputs | low | mitigate | Construct both images in memory with existing bounded helpers; introduce no downloaded, binary, generated-file, or network fixture source. |
</threat_model>

<verification>
Run the per-target outline-and-filter command from Task 2, then run the complete portable PNG regression and the unchanged scoped quality lane:

<automated>moon -C modules/mb-image test png --target all --frozen</automated>
<automated>pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>

The filtered target commands are evidence only; do not add them as a repository script or quality-lane stage.
</verification>

<success_criteria>
1. The one exact stable test name declares two direct 32x1 flat records: RGB8 and straight-RGBA8, each with 0xaa components.
2. For both records, explicit FixedOrStored eager output is no larger than explicit Stored eager output and strictly smaller than it.
3. Repeated FixedOrStored eager output equals configured [0,1,3,2,5] chunk output, and eager plus chunk optimized bytes complete-input decode to all generated components.
4. The outline guard and filtered command pass independently on js, wasm, wasm-gc, and native with isolated target directories; the complete PNG suite and existing PNG quality lane pass.
</success_criteria>

<output>
Create `.planning/phases/34-portable-png-compression-corpus-evidence/34-01-SUMMARY.md` when done.
</output>
