---
phase: 10-alpha-correct-pixel-processing
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - modules/mb-image/ops/processing_test.mbt
  - modules/mb-image/ops/processing_wbtest.mbt
autonomous: true
gap_closure: true
requirements:
  - RASTER-01
  - RASTER-02
must_haves:
  truths:
    - "Translucent source-over, Rec.709 grayscale, and clamp-to-edge blur outputs match a test-local linear-premultiplied decode/encode/ties-even oracle rather than an encoded-byte calculation."
    - "Every supported processing operation rejects unsupported representations, incompatible composite metadata/extents, radius-derived overflow, and each insufficient resource ceiling with a typed CoreError before changing any Budget remaining counter."
    - "The added public and white-box processing evidence passes unchanged on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/ops/processing_test.mbt
      provides: Public translucent output, metadata-boundary, grayscale, and edge-blur behavioral assertions.
    - path: modules/mb-image/ops/processing_wbtest.mbt
      provides: Independent color/quantization oracle plus hostile-input and complete-budget-atomicity matrix.
  key_links:
    - from: modules/mb-image/ops/processing_test.mbt
      to: modules/mb-image/ops/processing.mbt
      via: Public composite_source_over, grayscale, and box_blur calls assert the documented result and metadata contract.
    - from: modules/mb-image/ops/processing_wbtest.mbt
      to: modules/mb-image/ops/processing.mbt
      via: Test-local conversion/oracle and rejection matrices independently validate the production public APIs without calling their private pixel helpers.
---

<objective>
Close the Phase 10 verification blocker by supplying executable public and white-box evidence for the already-implemented raster-processing contract.

Purpose: prove RASTER-01 and RASTER-02 behavior rather than change their processing algorithm, public API, documentation, package configuration, or release state.
Output: expanded processing public and white-box tests only.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-alpha-correct-pixel-processing/10-CONTEXT.md
@.planning/phases/10-alpha-correct-pixel-processing/10-RESEARCH.md
@.planning/phases/10-alpha-correct-pixel-processing/10-01-SUMMARY.md
@.planning/phases/10-alpha-correct-pixel-processing/10-VERIFICATION.md
@modules/mb-image/ops/processing.mbt
@modules/mb-image/ops/processing_test.mbt
@modules/mb-image/ops/processing_wbtest.mbt
@modules/mb-image/ops/geometry_wbtest.mbt
@modules/mb-image/ops/resize_convert_wbtest.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public alpha, metadata, grayscale, and edge-blur behavior vectors</name>
  <files>modules/mb-image/ops/processing_test.mbt</files>
  <action>Extend only the public processing test file. Per D-01 and D-02, add a translucent source-over fixture with nontrivial source and destination RGB/alpha values; assert exact straight RGBA8 bytes against expected values supplied by the test-local independent reference established in Task 2, a fresh tight owned output, and source-over operand order. Per D-03, construct metadata variants through the existing test helpers so composite accepts only paired built-in-sRGB profiles, equal orientation, and empty opaque metadata; assert each differing orientation, non-built-in/custom profile, or nonempty opaque metadata pair returns the exact typed composite metadata error before budget charge. Per D-04, D-05, D-07, and D-08, add nontrivial-alpha grayscale and multi-pixel clamp-to-edge blur public vectors that assert exact expected bytes, alpha semantics, the radius-zero identity, and no visible color contribution from transparent saturated samples. Per D-06, keep this evidence focused on deterministic reference semantics rather than performance, kernel, or acceleration behavior. Keep expected-byte/oracle logic out of production source and preserve the existing public API and README; do not modify processing.mbt, moon.pkg, package dependencies, or documentation.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js</automated>
  </verify>
  <done>Public tests distinguish translucent linear-premultiplied processing from opaque smoke behavior and exercise the composite metadata boundary, Rec.709 grayscale, clamp-to-edge blur, transparent-color handling, and radius-zero identity.</done>
</task>

<task type="auto">
  <name>Task 2: Add independent oracle and complete rejection/atomic-budget matrix</name>
  <files>modules/mb-image/ops/processing_wbtest.mbt</files>
  <action>Extend only the white-box processing test file with test-local helpers patterned after geometry_wbtest.mbt. Implement the reference pipeline independently of processing.mbt private functions: byte dequantization, typed sRGB decode, normalized linear premultiplication, source-over or Rec.709/clamp-to-edge box averaging, unpremultiplication, sRGB encode, and deterministic ties-even quantization. Use it for quantization-sensitive translucent composite, grayscale, and blur fixtures so an encoded-domain blend, direct byte rounding, or straight-RGB blur cannot satisfy the assertions. Add an unchanged-remaining comparator covering bytes, allocations, allocation_size, width, height, pixels, depth, and work. Use it for all known pre-allocation failures: representable unsupported component, layout, channel order, transfer, and alpha combinations across every applicable public operation; unequal composite extents; each composite metadata incompatibility; radius-derived add/multiply/work overflow using the largest constructible UInt64 values; and each individual insufficient resource ceiling for composite, grayscale, and blur. Assert the precise applicable CoreError category/code/operation/context and complete budget snapshot for every rejection. Retain a success control for blur radius zero. Per D-03, D-05, and D-08, these tests must prove rejection occurs without implicit conversion, resizing, cropping, allocation, or partial charge; do not alter the processing algorithm or introduce new source helpers.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon test modules/mb-image/ops --target wasm; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon test modules/mb-image/ops --target wasm-gc; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon test modules/mb-image/ops --target native; exit $LASTEXITCODE</automated>
  </verify>
  <done>White-box tests independently prove alpha/quantization results and cover all required hostile-input, overflow, and complete-budget-atomicity paths on every supported target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller ImageView to processing APIs | Caller-controlled pixel representation, metadata, dimensions, and alpha values select processing or rejection paths. |
| caller Budget to output allocation | Caller-provided resource ceilings must reject unsafe work atomically before owned output allocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | alpha/transfer result path | medium | mitigate | Test-local linear-premultiplied and ties-even oracle checks translucent composite/filter bytes independently of private production helpers. |
| T-10-02-02 | Tampering | processing capability and metadata gates | medium | mitigate | Matrix tests assert each representable unsupported form and metadata incompatibility yields the documented typed error before a charge. |
| T-10-02-03 | Denial of service | blur radius/window/work and Budget preflight | high | mitigate | Overflow and every resource-limit test compares all remaining counters before and after rejection. |
</threat_model>

<verification>
- `moon test modules/mb-image/ops --target js`
- `moon test modules/mb-image/ops --target wasm`
- `moon test modules/mb-image/ops --target wasm-gc`
- `moon test modules/mb-image/ops --target native`
</verification>

<success_criteria>
RASTER-01 and RASTER-02 have independent, four-target executable evidence for linear-premultiplied/ties-even output bytes, metadata and capability rejection, blur arithmetic overflow, and every Budget resource ceiling's atomic rejection. No processing implementation, package, public documentation, release, or unpublished artifact changes are made.
</success_criteria>

<output>
Create `.planning/phases/10-alpha-correct-pixel-processing/10-02-SUMMARY.md` when done.
</output>
