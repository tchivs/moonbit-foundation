---
phase: 10-alpha-correct-pixel-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/ops/moon.pkg
  - modules/mb-image/ops/processing.mbt
  - modules/mb-image/ops/processing_test.mbt
  - modules/mb-image/ops/processing_wbtest.mbt
  - modules/mb-image/README.mbt.md
autonomous: true
requirements:
  - RASTER-01
  - RASTER-02
must_haves:
  truths:
    - "A caller can composite equal-size straight encoded-sRGB RGBA8 images with source-over calculated in linear-premultiplied space and receives a new straight encoded-sRGB RGBA8 result."
    - "A caller can grayscale and clamp-to-edge box-blur straight encoded-sRGB RGBA8 images without transparent-color halos, with deterministic Rec.709 semantics."
    - "Unsupported representations, unequal composite extents, invalid or overflowing blur work, and insufficient resources return typed CoreError values without changing the caller budget."
    - "The same public and adversarial processing behavior passes on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/ops/processing.mbt
      provides: Public source-over, grayscale, and box-blur APIs plus private typed linear-premultiplied conversion helpers.
    - path: modules/mb-image/ops/processing_test.mbt
      provides: Black-box raster-operation behavior and metadata tests.
    - path: modules/mb-image/ops/processing_wbtest.mbt
      provides: Independent alpha/quantization, hostile-input, and atomic-budget tests.
    - path: modules/mb-image/README.mbt.md
      provides: Stable public processing contract and checked usage example.
  key_links:
    - from: modules/mb-image/ops/processing.mbt
      to: tchivs/mb-color/transfer and tchivs/mb-color/quantize
      via: Typed decode/encode and deterministic byte conversion around the linear-premultiplied calculation.
    - from: modules/mb-image/ops/processing.mbt
      to: modules/mb-image/storage/owned_image.mbt
      via: Preflight followed by exactly one OwnedImage::new_operation allocation and with_mut_view write.
    - from: modules/mb-image/ops/processing_test.mbt
      to: modules/mb-image/ops/processing.mbt
      via: Public calls cover composite_source_over, grayscale, and box_blur on all configured targets.
---

<objective>
Deliver the Phase 10 portable reference raster API: alpha-correct source-over compositing and deterministic grayscale/box-blur filters for straight encoded-sRGB RGBA8 images.

Purpose: establish a correct, reproducible processing contract on top of Phase 9's checked allocation and diagnostic boundary.
Output: public ops implementation, behavioral/adversarial tests, and documented semantics; no codec, SIMD/GPU, quality-kernel, benchmark, or release-automation work.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-alpha-correct-pixel-processing/10-CONTEXT.md
@.planning/phases/10-alpha-correct-pixel-processing/10-RESEARCH.md
@.planning/phases/09-checked-image-geometry-and-diagnostics/09-VERIFICATION.md
@modules/mb-color/alpha/alpha.mbt
@modules/mb-color/transfer/transfer.mbt
@modules/mb-color/quantize/quantize.mbt
@modules/mb-image/ops/copy_flip.mbt
@modules/mb-image/ops/convert.mbt
@modules/mb-image/storage/owned_image.mbt
@modules/mb-image/ops/resize_convert_wbtest.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the public raster contract and independent hostile-input oracles</name>
  <files>modules/mb-image/ops/processing_test.mbt, modules/mb-image/ops/processing_wbtest.mbt</files>
  <behavior>
    - Public tests: opaque source-over returns the source pixel; a translucent source-over fixture asserts documented straight RGBA8 output, result metadata, and a fresh tight output.
    - Public tests: grayscale has equal RGB output channels and preserves representable alpha; blur uses clamp-to-edge samples and returns straight RGBA8 metadata.
    - White-box tests: independently perform byte dequantization, sRGB decode, linear premultiplication, source-over or Rec.709/box average, unpremultiplication, encoding, and ties-even quantization for quantization-sensitive fixtures.
    - White-box tests: unsupported alpha/format/transfer forms, unequal composite dimensions, zero/overflowing blur-window arithmetic, and every relevant insufficient budget ceiling return typed errors with the complete Budget.remaining snapshot unchanged.
    - White-box tests: a transparent saturated neighbor contributes no visible color halo after the alpha-aware blur.
  </behavior>
  <action>Create the new public and white-box test files before production implementation, following the adjacent ops helpers for controlled RGBA8 images, metadata, allocator limits, typed CoreError assertions, and four-target-safe MoonBit syntax. Per D-01 and D-02, name the expected public calls `composite_source_over`, `grayscale`, and `box_blur`, use source-over with source first, and assert a newly owned straight encoded-sRGB RGBA8 result. Per D-03, require operation-specific CapabilityUnavailable or dimension diagnostics without conversion, cropping, resizing, or any preflight budget charge. Per D-04, D-05, D-07, and D-08, make exact vector oracles cover Rec.709 linear luminance coefficients, clamp-to-edge fixed-window sampling, alpha-aware premultiplied averaging, checked radius/window/work arithmetic, and unchanged resource counters. Keep test arithmetic independent of the production helpers so an encoded-space blend or straight-RGB blur cannot satisfy both paths.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js</automated>
  </verify>
  <done>Public behavior tests and independent adversarial tests compile as the executable contract for all three operations, including alpha correctness, typed rejection, and full budget atomicity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the straight-RGBA8 linear-premultiplied processing path</name>
  <files>modules/mb-image/ops/moon.pkg, modules/mb-image/ops/processing.mbt</files>
  <behavior>
    - Valid equal-dimension straight RGBA8 encoded-sRGB inputs composite as linear-premultiplied source-over and store a straight RGBA8 encoded-sRGB result using deterministic ties-even bytes.
    - Grayscale uses linear Rec.709 luminance and preserves normalized alpha through the common store boundary.
    - Blur clamps each coordinate to the source extent, averages linear-premultiplied RGBA over the checked fixed window, and has no intermediate image allocation.
    - All deterministic validation and checked work preflight happen before the sole output allocation and its atomic resource charge.
  </behavior>
  <action>Add the existing `tchivs/mb-color/transfer` and `tchivs/mb-color/quantize` imports to the `ops` package, then create `processing.mbt` in the same package as the existing operation helpers. Implement the three public APIs from Task 1 over only packed U8 RGBA, sRGB color space, encoded-sRGB transfer, and `Some(Straight)` alpha; reject every other representation, empty input, incompatible composite extent, and failed checked preflight with the established `operation_error`/CoreError conventions (D-01, D-03). Use private load/store helpers with the verified contracts: byte -> `EncodedSrgb8Component`/`EncodedAlpha8` -> `dequantize_encoded_srgb`/`dequantize_alpha` -> `decode_srgb` -> `StraightNormalizedSrgba::linear` -> `premultiply_normalized`; after each calculation, construct `PremultipliedNormalizedSrgba::linear`, call `unpremultiply_normalized`, extract linear RGB, call `encode_srgb`, and use `quantize_encoded_srgb` plus `quantize_alpha`. This realizes D-02 without encoded-domain alpha arithmetic. Implement source-over as source plus destination times one minus source alpha; implement grayscale as Rec.709 linear luminance followed by premultiplication; implement blur by clamp-to-edge sampling and averaging all four linear-premultiplied components over `(2r+1)^2` (D-04, D-05, D-07, D-08). Reuse tight descriptor/result/disposition conventions, calculate output pixels and blur window/work with `@checked` before the one `OwnedImage::new_operation` call, and write only via `with_mut_view`; charge one work per output pixel for composite/grayscale and checked visited-window work for blur. Do not use premultiply_encoded/unpremultiply_encoded, byte-domain blends, independent straight RGB averaging, intermediate images, SIMD/GPU paths, sliding-window/separable kernels, new codecs, or an arbitrary public radius cap (D-06).</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js</automated>
  </verify>
  <done>All three APIs use one typed linear-premultiplied processing pipeline, produce straight encoded-sRGB RGBA8 outputs, and preserve typed failure plus atomic-budget behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the processing semantics in the checked module contract</name>
  <files>modules/mb-image/README.mbt.md</files>
  <action>Extend the existing operations section and its `mbt check` examples to document the straight encoded-sRGB RGBA8-only processing capability, exact source-over operand order, straight encoded-sRGB output, linear-light premultiplied conversion boundary, Rec.709 grayscale coefficients, clamp-to-edge alpha-aware box blur, checked-radius/work and caller-budget failure semantics, and metadata disposition. Include a small public example calling the three new ops with a supplied budget. State the reference-quality scope per D-06 without introducing performance claims, SIMD/GPU work, extra filters, codecs, examples/benchmarks assigned to Phase 11, registry changes, or release automation.</action>
  <verify>
    <automated>moon -C modules/mb-image check README.mbt.md --target js --frozen</automated>
  </verify>
  <done>The public docs describe exactly the implemented alpha/filter contract and its checked example compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller ImageView -> processing operations | Caller-controlled representation, dimensions, pixels, and metadata enter portable pixel loops. |
| caller Budget -> owned output allocation | Resource limits constrain byte storage, allocation count, dimensions, pixels, and declared work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | processing capability gate | medium | mitigate | Require straight packed U8 RGBA with sRGB/encoded-sRGB metadata and return operation-specific CapabilityUnavailable before allocation. |
| T-10-02 | Denial of service | box_blur radius/window/work preflight | high | mitigate | Checked add/multiply computes window and total sample work before the single owned allocation; supplied Budget atomically rejects insufficient limits. |
| T-10-03 | Tampering | pixel conversion/store boundary | medium | mitigate | Use typed dequantize/decode/premultiply and unpremultiply/encode/ties-even quantize helpers; independent oracle tests reject encoded-space arithmetic. |
| T-10-04 | Information disclosure | owned output write | low | accept | Fresh tightly packed output is fully written per output pixel; no external I/O, secrets, or shared mutable backing are introduced. |
</threat_model>

<verification>
Run the complete ops suite on every supported target:

- `moon test modules/mb-image/ops --target js`
- `moon test modules/mb-image/ops --target wasm`
- `moon test modules/mb-image/ops --target wasm-gc`
- `moon test modules/mb-image/ops --target native`
- `moon -C modules/mb-image check README.mbt.md --target js --frozen`
- `moon -C modules/mb-image check README.mbt.md --target wasm --frozen`
- `moon -C modules/mb-image check README.mbt.md --target wasm-gc --frozen`
- `moon -C modules/mb-image check README.mbt.md --target native --frozen`
</verification>

<success_criteria>
RASTER-01 and RASTER-02 are complete when the three public operations are callable for their documented straight-RGBA8 scope, calculations follow the verified typed linear-premultiplied transfer/quantization path, all deterministic rejections leave the budget unchanged, and public plus adversarial evidence passes on js, wasm, wasm-gc, and native.
</success_criteria>

<output>
Create `.planning/phases/10-alpha-correct-pixel-processing/10-01-SUMMARY.md` when done.
</output>
