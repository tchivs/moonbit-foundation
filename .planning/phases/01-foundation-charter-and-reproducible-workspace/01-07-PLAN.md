---
phase: 01-foundation-charter-and-reproducible-workspace
plan: "07"
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-04", "01-05", "01-06"]
files_modified:
  - scripts/quality.ps1
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Assert-Toolchain.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .github/workflows/quality.yml
autonomous: true
requirements: [WORK-02, WORK-03, WORK-04, WORK-05, GOV-02, GOV-03, GOV-04]
must_haves:
  truths:
    - "D-14: moon, moonc, and moonrun are printed and compared exactly before build work."
    - "D-16 edge WORK-04 adjacency/empty/ordering: every non-empty required stage is specifically wired, ordered, and contextual on failure."
    - "D-15/D-16 edge WORK-05 adjacency/empty/ordering: every declared target has check/test evidence and the four-target lane alone determines success."
    - "Assert-PhaseSourceAudit fails closed unless goal=1, requirements=9, decisions=16, research=29, edges=17, prohibitions=5 and every exact expected ID is present once with no extras."
    - "LLVM is isolated, visible, experimental, and non-blocking."
  artifacts:
    - path: "scripts/quality/Assert-Policy.ps1"
      provides: "Foundation and exact Phase 1 source-audit validators"
      contains: "Assert-PhaseSourceAudit"
    - path: "scripts/quality.ps1"
      provides: "Sole public Required and LlvmExperimental controller"
      contains: "Invoke-MoonQuality"
    - path: ".github/workflows/quality.yml"
      provides: "Pinned read-only required and non-blocking LLVM jobs"
      contains: "-Lane Required"
  key_links:
    - from: "scripts/quality/Assert-Policy.ps1"
      to: "policy/phase-01-source-audit.json"
      via: "Assert-PhaseSourceAudit compares exact expected arrays, counts, uniqueness, covered status, source, description, and plan mapping"
      pattern: 'phase-01-source-audit[.]json'
    - from: "scripts/quality/Invoke-MoonQuality.ps1"
      to: "scripts/quality/Assert-Policy.ps1"
      via: "policy and source-audit validation precede format, targets, docs, info, and packaging"
      pattern: 'Assert-FoundationPolicy|Assert-PhaseSourceAudit'
    - from: ".github/workflows/quality.yml"
      to: "scripts/quality.ps1"
      via: "both jobs call the same controller with explicit lanes"
      pattern: 'scripts/quality[.]ps1 -Lane (Required|LlvmExperimental)'
  assumptions:
    - id: EDGE-WORK-02-UNCLASSIFIED
      statement: "Manually compare normalized output from all three binaries with policy and CI logs on the pinned runner."
      verification: manual-review
      flagged: true
  prohibitions:
    - id: PROH-WORK-05-LLVM-SUPPORT
      requirement_id: WORK-05
      category: transparency
      statement: "MUST NOT advertise LLVM as supported or let its result determine required-lane success."
      status: unverified
      flagged: true
---

<objective>
Implement the fail-closed PowerShell 7 policy, no-silent-drop, target, package, and CI quality contract required by D-02/D-03/D-06/D-07/D-09/D-12/D-14/D-15/D-16.

Purpose: Give local maintainers and CI one deterministic command that proves both implementation quality and exact source coverage.
Output: Root controller, three helpers, and pinned required/LLVM workflow.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-CONTEXT.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-RESEARCH.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-PATTERNS.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-01-SUMMARY.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-02-SUMMARY.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-04-SUMMARY.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-05-SUMMARY.md
@.planning/phases/01-foundation-charter-and-reproducible-workspace/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build exact-toolchain, policy, and source-audit validators</name>
  <read_first>policy/foundation.json, policy/phase-01-source-audit.json, fixtures/manifest.json, docs/rfcs/0001-moonbit-native-foundation.md, moon.work, modules/mb-core/moon.mod.json, modules/mb-core/moon.pkg, .planning/phases/01-foundation-charter-and-reproducible-workspace/01-RESEARCH.md, .planning/phases/01-foundation-charter-and-reproducible-workspace/01-PATTERNS.md</read_first>
  <files>scripts/quality/Assert-Toolchain.ps1, scripts/quality/Assert-Policy.ps1</files>
  <action>01-PATTERNS.md assigns no PowerShell analog; implement the exact fixed-command, `ConvertFrom-Json`, and fail-closed patterns from `01-RESEARCH.md`. `Assert-Toolchain` requires PowerShell 7 and exact normalized outputs from all three binaries. `Assert-FoundationPolicy` validates RFC evidence, labels, identities, targets as sets, fixture records, publication block, package rules, and acyclic allowed DAG. Add `Assert-PhaseSourceAudit`: hard-code expected exact ID arrays for GOAL-01; GOV-01..04/WORK-01..05; D-01..16; all 29 RESEARCH IDs; all 17 EDGE IDs; and five PROH IDs from Plan 01-02. Require exact counts, unique IDs, no extras, non-empty source/description/covering plan, and `status=covered`. Invoke no policy-derived command and mutate nothing.</action>
  <verify><automated>pwsh -NoProfile -Command ". ./scripts/quality/Assert-Toolchain.ps1;. ./scripts/quality/Assert-Policy.ps1;Assert-Toolchain -PolicyPath policy/foundation.json;Assert-FoundationPolicy -PolicyPath policy/foundation.json;Assert-PhaseSourceAudit -AuditPath policy/phase-01-source-audit.json"</automated></verify>
  <acceptance_criteria>Any missing, duplicate, or extra source ID; count mismatch; empty mapping; policy drift; malformed fixture; invalid DAG; or wrong tool identity fails closed.</acceptance_criteria>
  <done>The same validator proves policy integrity and exact 1/9/16/29/17/5 source inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate required and LLVM quality lanes</name>
  <read_first>scripts/quality/Assert-Toolchain.ps1, scripts/quality/Assert-Policy.ps1, .planning/phases/01-foundation-charter-and-reproducible-workspace/01-RESEARCH.md, .planning/phases/01-foundation-charter-and-reproducible-workspace/01-PATTERNS.md</read_first>
  <files>scripts/quality/Invoke-MoonQuality.ps1, scripts/quality.ps1</files>
  <action>01-PATTERNS.md assigns no controller analog; use the exact stage order and explicit exit handling from `01-RESEARCH.md`. The sole public controller accepts only Required or LlvmExperimental. Required order: toolchain; foundation policy plus exact source audit; RFC/fixtures/inventory/DAG; `moon fmt --check`; explicit js/wasm/wasm-gc/native check and test with frozen state; doc; per-module `moon -C modules/<module> info --target all --frozen` followed by the exact semantic-line classifier from Plans 01-04..06; per-module package list allowlist; clean tracked diff. Use fixed commands/arguments, contextual throws, and never run `moon work sync`. LLVM stays isolated and visible.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <acceptance_criteria>One root command proves WORK-04, every D-15 target, exact interfaces, package contents, DAG, and no-silent-drop inventory.</acceptance_criteria>
  <done>The required lane passes without mutation and failures name stage/member/target/source ID.</done>
</task>

<task type="auto">
  <name>Task 3: Run the identical quality contract in pinned read-only CI</name>
  <read_first>scripts/quality.ps1, policy/foundation.json, .planning/phases/01-foundation-charter-and-reproducible-workspace/01-RESEARCH.md, .planning/phases/01-foundation-charter-and-reproducible-workspace/01-PATTERNS.md</read_first>
  <files>.github/workflows/quality.yml</files>
  <action>01-PATTERNS.md assigns no workflow analog; use the exact pinned CI shape in `01-RESEARCH.md`. Create read-only push/PR jobs with no publication secrets. Pin `hustcer/setup-moonbit@bdc8c076af1f4c5012a6ac3451a2009ec75bf921` and version `0.1.20260713+75c7e1f`; retain three-binary validation. Required is blocking. LlvmExperimental calls the same controller with `continue-on-error: true`. If a real run proves the immutable action cannot fetch the bundle, replace only setup with the official exact archive while retaining every gate.</action>
  <verify><automated>pwsh -NoProfile -Command "$y=Get-Content -Raw .github/workflows/quality.yml;@('bdc8c076af1f4c5012a6ac3451a2009ec75bf921','0.1.20260713+75c7e1f','-Lane Required','-Lane LlvmExperimental','continue-on-error: true')|ForEach-Object{if(-not$y.Contains($_)){throw ('CI '+$_)}};if($y-match'moon(cakes)?(_|-)?token'){throw 'secret'}"</automated></verify>
  <acceptance_criteria>CI is immutable, read-only, credential-free, version-gated, and separates required support from LLVM.</acceptance_criteria>
  <done>Local and CI quality behavior share one root implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository JSON -> process execution | Data must never become executable text. |
| Third-party action -> runner | External code must be immutable and independently version-gated. |
| Pull request -> workflow permissions | Untrusted changes receive no writes or publication secrets. |
| Source inventory -> completion claim | Omission, duplication, or substitution must fail closed. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | setup action | high | mitigate | Full SHA plus exact three-binary gate. |
| T-07-02 | Elevation/Disclosure | Actions | high | mitigate | Read-only, no tokens, no publication. |
| T-07-03 | Tampering | policy parser | high | mitigate | Structured JSON and fixed commands only. |
| T-07-04 | Spoofing | source coverage | high | mitigate | Exact arrays/counts/uniqueness/no-extras validator in Required lane. |
| T-07-05 | Spoofing | target evidence | medium | mitigate | Explicit check/test per target and exact generated interfaces. |
## OWASP ASVS L1 Control Mapping
| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V2 Authentication | Not applicable | No application authentication. | N/A |
| V3 Session Management | Not applicable | No sessions. | N/A |
| V4 Access Control | Applicable | Read-only CI, no secrets/publication. | Task 3 |
| V5 Validation, Sanitization and Encoding | Applicable | Structured parsing, exact sets/counts, fixed arguments, fail-closed stages. | Tasks 1-2 |
| V6 Stored Cryptography | Limited | Validate fixture SHA-256 metadata only. | Task 1 |
</threat_model>

## Artifacts this phase produces
- `scripts/quality/Assert-Toolchain.ps1` — exact tool gate.
- `scripts/quality/Assert-Policy.ps1` — foundation plus source-audit validators.
- `scripts/quality/Invoke-MoonQuality.ps1` — fixed-stage runner.
- `scripts/quality.ps1` — sole root entry point.
- `.github/workflows/quality.yml` — pinned required and LLVM jobs.

<verification>
Run Required, confirm exact source audit passes, no tracked diff appears, and CI has immutable/read-only/no-secret properties.
</verification>
<success_criteria>
- WORK-02..05 have deterministic root evidence.
- All six WORK-04/05 edges, WORK-02 assumption, and LLVM prohibition remain explicit.
- Exact 1/9/16/29/17/5 inventory is enforced by the same policy validator and root lane.
</success_criteria>
<output>Create `.planning/phases/01-foundation-charter-and-reproducible-workspace/01-07-SUMMARY.md` when done.</output>
