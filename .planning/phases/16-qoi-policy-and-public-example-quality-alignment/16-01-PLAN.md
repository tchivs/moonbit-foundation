---
phase: 16-qoi-policy-and-public-example-quality-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - scripts/quality/Test-PublicExamples.ps1
autonomous: true
requirements: [QOI-05, QOI-06]
must_haves:
  truths:
    - "The six-member workspace, including qoi-portable, passes an exact foundation-policy assertion while each existing member remains required."
    - "The existing tchivs/mb-image/qoi public package has an exact, compiler-derived policy record for imports, public interface, all supported targets, source order, and its QOI-only source/test/vector inventory."
    - "A QOI-specific quality command fails closed for QOI package drift and runs qoi-portable's one-line evidence on js, wasm, wasm-gc, and native without entering release qualification or registry-resolution paths."
  artifacts:
    - path: policy/foundation.json
      provides: "Exact QOI public-package and mb-image publication inventory"
      contains: "tchivs/mb-image/qoi"
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Fail-closed workspace and QOI policy assertions"
      contains: "mb-image qoi"
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: "Bounded QOI policy quality lane and QOI negative fixtures"
      contains: "Qoi"
    - path: scripts/quality/Test-PublicExamples.ps1
      provides: "Four-target qoi-portable public consumer verification"
      contains: "qoi"
  key_links:
    - from: policy/foundation.json
      to: scripts/quality/Assert-Policy.ps1
      via: "Assert-FoundationPolicy reads the QOI public-package record and exact QOI files"
      pattern: "Assert-FoundationPolicy"
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: scripts/quality/Test-PublicExamples.ps1
      via: "the QOI lane invokes the qoi workspace example selection"
      pattern: "Example qoi"
    - from: scripts/quality/Test-PublicExamples.ps1
      to: examples/qoi-portable/main/main.mbt
      via: "moon run verifies the exact QOI status line on every required target"
      pattern: "examples/qoi-portable/main"
---

<objective>
Close the QOI-only foundation-policy and public-example quality gap identified by the v0.4 audit.

Purpose: Make the already-delivered QOI package and portable public consumer part of the repository's exact, fail-closed quality contract without touching unrelated release or operations inventory.
Output: A QOI public-package policy record, QOI-specific fail-closed assertions and negatives, and a bounded four-target public-example quality lane.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v0.4-v0.4-MILESTONE-AUDIT.md
@.planning/phases/16-qoi-policy-and-public-example-quality-alignment/16-CONTEXT.md
@.planning/phases/14-canonical-qoi-encode-and-four-target-vectors/14-VERIFICATION.md
@.planning/phases/15-public-qoi-processing-example/15-VERIFICATION.md
@policy/foundation.json
@moon.work
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1
@scripts/quality/Test-PublicExamples.ps1

<interfaces>
From modules/mb-image/qoi/moon.pkg: the QOI package imports mb-core budget/bytes/checked/error/io, mb-color model/profile, and mb-image codec/metadata/model/storage; it declares +js+wasm+wasm-gc+native.

From modules/mb-image/qoi/pkg.generated.mbti after moon -C modules/mb-image info --target all --frozen: QoiDecoder and QoiEncoder each expose new() -> Self and implement the corresponding @codec ImageDecoder or ImageEncoder trait. The policy semantic_interface must be copied from this compiler output after removing generated comments and blank lines.

From examples/qoi-portable/main/moon.pkg: the public consumer imports only mb-core budget/bytes/error/io and mb-image codec/ops/qoi. Its sole runtime line is example=qoi-portable bytes_read=27 bytes_written=24 width=2 height=1 flip_horizontal digest=750514177 sha256=5dc3abfe81e722b211af255f6f96805225f98435f1f9525c46df48217f858df2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the exact QOI record to the foundation policy</name>
  <files>policy/foundation.json</files>
  <action>Per D-02, append tchivs/mb-image/qoi after ppm in the tchivs/mb-image public_packages sequence. Before editing, run moon -C modules/mb-image info --target all --frozen and derive the semantic_interface line-for-line from qoi/pkg.generated.mbti, excluding generator comments and blank lines. Record qoi's eleven moon.pkg imports exactly, all four required targets, candidate stability, and the production_sources order moon.pkg, qoi.mbt, decode.mbt, encode.mbt, generated_vectors.mbt. Extend only the mb-image publication_files list with qoi and its existing nine QOI files: moon.pkg, qoi.mbt, decode.mbt, decode_test.mbt, decode_wbtest.mbt, encode.mbt, encode_test.mbt, encode_wbtest.mbt, and generated_vectors.mbt. Do not alter any ops policy entry, module manifest policy, publication block, or release/registry artifact.</action>
  <verify>
    <automated>moon -C modules/mb-image info --target all --frozen; pwsh -NoProfile -Command "&amp; { $p = Get-Content -Raw policy/foundation.json | ConvertFrom-Json; $q = @($p.modules | Where-Object name -ceq 'tchivs/mb-image' | ForEach-Object public_packages | Where-Object path -ceq 'qoi'); if ($q.Count -ne 1 -or @($q[0].production_sources).Count -ne 5 -or @($q[0].semantic_interface).Count -eq 0) { throw 'QOI policy record is incomplete.' } }"</automated>
  </verify>
  <done>The policy has one exact QOI package record and includes only the approved QOI files in the existing mb-image publication list; the direct foundation-policy assertion reaches the QOI-aware policy checks.</done>
</task>

<task type="auto">
  <name>Task 2: Make foundation policy assertions fail closed for the QOI inventory</name>
  <files>scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <action>Per D-01 through D-03, update Assert-FoundationPolicy's expected moon.work set to retain the five present members and require examples/qoi-portable as the sixth. Extend the mb-image spine to end in qoi, assert its exact allowed-import set, exact production-source sequence, and exact QOI directory file set using the policy record; validate the semantic baseline through the existing Assert-GeneratedInterface path after moon info. In Assert-ImageQualificationNegativeFixtures, add QOI-specific artificial drift cases for package presence, missing and extra imports, a missing portable target, missing and extra QoiDecoder/QoiEncoder interface entries, wrong production ordering, and missing or extra QOI production content. Each fixture must prove the same exact-set or exact-sequence helper rejects with its expected mismatch pattern. Keep the pre-existing ops assertions and inventory untouched.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { . ./scripts/quality/Assert-Policy.ps1; . ./scripts/quality/Invoke-MoonQuality.ps1; Assert-FoundationPolicy -PolicyPath policy/foundation.json; Assert-ImageQualificationNegativeFixtures }"</automated>
  </verify>
  <done>Workspace membership and every QOI policy dimension are checked against exact data, and QOI-specific synthetic negatives demonstrate deterministic rejection of each required drift category.</done>
</task>

<task type="auto">
  <name>Task 3: Add the bounded QOI public-example quality path</name>
  <files>scripts/quality/Invoke-MoonQuality.ps1, scripts/quality/Test-PublicExamples.ps1</files>
  <action>Per D-04 and D-05, add a Qoi lane to Invoke-MoonQuality that runs only the QOI foundation-policy assertion, QOI interface generation/classification, QOI image negative-fixture matrix, exact mb-image package allowlist verification, and Test-PublicExamples with the qoi workspace selection. Add qoi to Test-PublicExamples' selectable workspace examples with qoiRoot, its three required source files, the exact public-import allowlist from examples/qoi-portable/main/moon.pkg, and the fixed one-line evidence on js, wasm, wasm-gc, and native. Ensure the qoi workspace selection does not call Assert-QualificationSchema, Invoke-SourceIsolation, Invoke-RegistryResolutionProbe, Write-QualificationReport, or any publication/credential path; preserve the existing portable/native qualification behavior for its existing selections.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Test-PublicExamples.ps1 -Example qoi -Mode workspace -Target all; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Qoi</automated>
  </verify>
  <done>qoi-portable's exact public workflow is checked on all four targets through the existing public-example mechanism, and the Qoi lane completes without release qualification or registry-resolution activity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy JSON → quality scripts | Checked repository policy is interpreted as the authoritative allowlist for workspace, package, and public-interface facts. |
| public example source → MoonBit runner | The quality path executes an independently declared consumer and validates its emitted deterministic evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | policy/foundation.json and Assert-Policy | high | mitigate | Require exact QOI workspace/package/import/target/interface/source lists and exercise artificial missing, extra, and reorder fixtures in Assert-ImageQualificationNegativeFixtures. |
| T-16-02 | Spoofing | qoi-portable public evidence | medium | mitigate | Test-PublicExamples must run examples/qoi-portable/main on each required target and require exactly its one semantic status line. |
| T-16-03 | Elevation of privilege | Qoi lane | medium | mitigate | Keep the lane limited to local policy, compiler, package, negative, and workspace-example functions; the qoi selection bypasses qualification schema, isolation, registry, publication, and credential functions. |
</threat_model>

<verification>
Run the focused Qoi lane after the direct policy assertion. It must compile interface metadata, reject every synthetic QOI drift fixture, list exactly the policy packages, and execute qoi-portable on js, wasm, wasm-gc, and native with its one expected line. Inspect the stage names to confirm no release qualification, registry-resolution, publication, credentials, or unrelated operations-inventory stage ran.
</verification>

<success_criteria>
- The six approved workspace members and seven mb-image public packages, including qoi, are governed by exact policy data.
- QOI drift in imports, targets, semantic interface, source sequence, or production content is rejected fail closed.
- The Qoi quality lane proves the unchanged public example on all four portable targets without release-governance side effects.
- No policy or script change expands scope to unrelated ops inventory, registry artifacts, release-governance removal, publication, credentials, or generic cleanup.
</success_criteria>

<source_audit>
SOURCE  | ID     | Feature/Requirement | Plan | Status | Notes
GOAL    | —      | QOI policy and public example quality alignment | 01 | COVERED | Goal is derived from the locked Context because ROADMAP still says To be planned.
REQ     | QOI-05 | Four-target QOI conformance quality evidence | 01 | COVERED | QOI interface/package policy and Qoi lane retain four-target evidence.
REQ     | QOI-06 | Deterministic portable QOI public example | 01 | COVERED | qoi public-example selection runs the exact line on all targets.
RESEARCH| —      | No RESEARCH.md; this run deliberately skips research | 01 | COVERED | Audit and verification artifacts supply the required technical evidence.
CONTEXT | D-01   | Six-member workspace inventory | 01 | COVERED | Task 2.
CONTEXT | D-02   | Exact QOI policy package inventory | 01 | COVERED | Tasks 1 and 2.
CONTEXT | D-03   | Deterministic QOI policy drift rejection | 01 | COVERED | Task 2.
CONTEXT | D-04   | qoi-portable quality path and exact output | 01 | COVERED | Task 3.
CONTEXT | D-05   | QOI-only command with no release operations | 01 | COVERED | Task 3.
</source_audit>

<output>
Create `.planning/phases/16-qoi-policy-and-public-example-quality-alignment/16-01-SUMMARY.md` when done.
</output>
