---
phase: 20-png-structural-safety-gate
plan: "03"
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - modules/mb-image/png/generated_vectors.mbt
  - modules/mb-image/png/generated_vectors_test.mbt
  - fixtures/png/cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngStructuralVectors.ps1
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
gap_closure: true
requirements: [PNG-01, PNG-02, PNG-03]
must_haves:
  truths:
    - "A complete 57-byte supported PNG accepted at max_input_bytes=57 reaches the typed deflate-and-raster-pending capability terminal; only a genuinely consumed byte beyond that ceiling receives the input-limit failure."
    - "The provenance-tagged generated corpus drives every Phase-20 structural, profile, metadata, resource, budget, and caller-state branch through the public PngDecoder test boundary and the private structural assertions on js, wasm, wasm-gc, and native."
    - "The isolated Png quality lane fails closed when the actual package interface, imports, target set, source order, production inventory, or directory inventory drifts from the PNG-only policy."
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: "A byte-metered structural validator whose terminal EOF probe does not charge an absent byte."
      contains: "_png_require_eof"
    - path: modules/mb-image/png/generated_vectors_test.mbt
      provides: "Generated black-box-only PNG fixture records with bytes, options, limits, Budget envelope, and expected terminal context."
    - path: fixtures/png/cases.json
      provides: "Complete small provenance-tagged PNG structural and resource corpus."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "PNG-scoped actual-interface and negative-policy assertions."
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: "An isolated staged Png quality lane that invokes the real PNG assertions."
  key_links:
    - from: modules/mb-image/png/png_test.mbt
      to: modules/mb-image/png/generated_vectors_test.mbt
      via: "Black-box test records invoke only the public ImageDecoder implementation on PngDecoder."
      pattern: "_generated_png_public_cases"
    - from: modules/mb-image/png/structural_wbtest.mbt
      to: modules/mb-image/png/generated_vectors.mbt
      via: "White-box tests use generated internal records to prove CRC, state, and preflight internals."
      pattern: "_generated_png_structural_cases"
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: scripts/quality/Assert-Policy.ps1
      via: "The Png lane executes PNG-specific interface and fail-closed negative checks before fixture and four-target test stages."
      pattern: "Assert-PngFoundationPolicy"
---

<objective>
Close every outstanding Phase-20 verification blocker: make strict EOF compatible with an exact input ceiling, complete and execute the promised generated hostile corpus, and turn the isolated Png lane into a real PNG-only policy and interface gate.

Purpose: make the structural safety result both correct at the input boundary and independently evidenced on all portable targets before Phase 21 changes the terminal decode capability.
Output: corrected EOF accounting, generated public/private fixture tables and consumers, and a fail-closed PNG quality lane.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-png-structural-safety-gate/20-CONTEXT.md
@.planning/phases/20-png-structural-safety-gate/20-RESEARCH.md
@.planning/phases/20-png-structural-safety-gate/20-VERIFICATION.md
@.planning/phases/20-png-structural-safety-gate/20-02-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/structural_wbtest.mbt
@modules/mb-image/qoi/decode_test.mbt
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1

<interfaces>
- `PngDecoder` remains the sole public PNG type. `ImageDecoder::decode` can return `CapabilityUnavailable("png-decode", "deflate-and-raster-pending")` only after full Phase-20 transport validation.
- `_png_read_one` meters bytes actually consumed from the forward reader. EOF detection after IEND must distinguish an `EndOfStream` outcome from a byte that would require consuming and charging another input byte.
- `CodecLimits` has independent input, output, width, height, pixels, and work ceilings. `Budget::child` is a non-mutating envelope preflight; `Diagnostics` must remain unchanged on every Phase-20 terminal path.
- Black-box `*_test.mbt` code may call only the public decoder. Generated black-box fixture data must therefore be test-only data, not a new PNG production export; `*_wbtest.mbt` remains the place for private state/CRC assertions.
</interfaces>
</context>

<source_audit>
SOURCE   | ID     | Feature / constraint | Coverage | Status
GOAL     | --     | Safely identify and structurally validate the supported PNG subset before image output. | Tasks 1-3 | COVERED
REQ      | PNG-01 | Bounded non-consuming deterministic PNG probe. | Task 2 | COVERED
REQ      | PNG-02 | Typed framing, order, CRC, unsupported-chunk, IEND, and trailing-input rejection. | Tasks 1-2 | COVERED
REQ      | PNG-03 | Checked input, geometry, output, work, allocation, Budget, and metadata enforcement before output. | Tasks 1-2 | COVERED
RESEARCH | --     | Fixed scratch; every processed chunk CRCs type plus payload; strict EOF and no whole-chunk allocation. | Tasks 1-2 | COVERED
RESEARCH | --     | Provenance/digest-checked fixtures must execute in black-box and white-box all-target suites. | Task 2 | COVERED
RESEARCH | --     | Policy must prevent test, public-surface, target, inventory, and fixture drift. | Task 3 | COVERED
CONTEXT  | D-01   | Only positive RGB8/RGBA8, non-interlaced, methods zero are accepted. | Task 2 | COVERED
CONTEXT  | D-02   | Retain the eager `ImageDecoder` public seam; no PNG streaming API. | Task 3 | COVERED
CONTEXT  | D-03   | Signature, first/only IHDR, contiguous IDAT, one empty IEND, CRCs, and EOF are mandatory. | Tasks 1-2 | COVERED
CONTEXT  | D-04   | CRC before critical/semantic/opaque metadata policy; preserve requests fail. | Task 2 | COVERED
CONTEXT  | D-05   | Checked limits, Budget, and diagnostics/state immutability precede output. | Tasks 1-2 | COVERED
CONTEXT  | D-06   | Small provenance-tagged hostile structural/limit fixtures; later decode/encode work stays excluded. | Task 2 | COVERED
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make strict EOF meter only consumed PNG input bytes</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt</files>
  <behavior>
    - A complete 57-byte RGB structural transport with `max_input_bytes=57` returns `CapabilityUnavailable` with the `deflate-and-raster-pending` context and leaves Budget/Diagnostics unchanged.
    - The same complete transport at one byte below its real length reports the input-byte resource failure; a real byte following IEND still receives the existing strict trailing-input rejection or, when the configured ceiling is already exhausted before that byte can be read, the input-byte resource failure.
    - The IEND terminal probe accepts only an actual end-of-stream outcome without incrementing `consumed`; reader failure and no-progress outcomes retain their typed propagation, and a returned byte remains trailing input.
  </behavior>
  <action>Start with failing public and white-box boundary tests. Per D-03 and D-05, refactor only `_png_require_eof` so it performs one direct one-byte `Reader` outcome probe using the existing fixed scratch, rather than routing an absent-byte check through `_png_read_one`. Map actual end of stream to success without advancing or charging `consumed`; map an actual progress byte to the existing trailing-input error; and preserve the reader's typed failed/no-progress result. Keep `_png_read_one` as the sole accounting path for signature, headers, payload, and stored CRC bytes, so any real additional PNG byte still encounters checked `consumed + 1` and `max_input_bytes` enforcement. Do not add buffering, a seek requirement, a public streaming surface, DEFLATE, raster construction, or a new terminal result.

Add an exact public 57-byte accepted RGB transport test that constructs a forward-only `MemoryReader`, applies exactly 57 input bytes, asserts the existing capability operation/context, and snapshots every relevant Budget remaining field plus `Diagnostics::length()`. Add companion below-boundary and post-IEND-byte tests, and white-box scripted-reader checks proving the EOF probe neither precharges an absent byte nor masks non-EOF reader outcomes.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>A valid exact-ceiling PNG reaches only the Phase-20 capability terminal, while streams that truly need an additional input byte still fail closed and all four targets execute the regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate and execute the complete PNG structural and envelope corpus</name>
  <files>fixtures/png/cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngStructuralVectors.ps1, modules/mb-image/png/generated_vectors.mbt, modules/mb-image/png/generated_vectors_test.mbt</files>
  <behavior>
    - Every generated record has a unique ID, byte-exact compact fixture, expected typed outcome, options/limit/Budget profile, and retains the required manifest provenance and digest identity.
    - Generated cases include signature/truncation; chunk-length/header/type-form failures; first, duplicate, misordered, and malformed IHDR; each rejected IHDR field/profile; every IDAT/IEND state transition; CRC corruption for IHDR, IDAT, semantic, ancillary, and IEND chunks; trailing bytes; unknown critical; every known semantic family; and both opaque ancillary dispositions.
    - The generated profiles prove the 57-byte exact input boundary and over-limit behavior plus every width, height, pixels, image/output bytes, work, allocation, and Budget resource ceiling, including accepted-at-ceiling and rejected-below-ceiling behavior where the same valid transport exercises that envelope.
    - The generator emits a private white-box record table and a separate test-only black-box record table; neither changes the generated production interface or adds a public PNG type.
  </behavior>
  <action>Expand the JSON source of truth and deterministic generator per D-01, D-04, D-05, and D-06. Retain small generated chunks and manifest provenance rather than opaque fixtures. Give each case sufficient structured expectation metadata for the test harness: expected error code/context or capability terminal, `preserve_opaque_metadata` choice, a named `CodecLimits` profile, a named `Budget` envelope, and whether caller Budget/Diagnostics must remain immutable. Validate that schema, unique IDs, byte encoding, known profile names, and manifest digest/required provenance fields in generator Check mode before emitting either MoonBit table.

Cover the full verifier and Plan-20 contract rather than only representative rows: malformed and truncated signatures; declared-length and four-character type gates; IHDR first/only/length and every depth/colour/compression/filter/interlace/zero-dimension rule; IDAT-required and contiguity transitions; early, duplicate, nonempty, truncated, and post-terminal IEND paths; CRC-before-discard failures for IHDR, IDAT, ancillary, semantic, and IEND chunks; unknown critical; each palette, transparency, colour-management/HDR, and animation semantic family; and unknown ancillary discard versus preservation refusal. Include the exact 57-byte valid RGB boundary, valid RGBA, input over-limit, all codec ceilings, all inherited Budget dimensions used by the preflight, and immutable caller-state assertions. Generate `generated_vectors.mbt` for private structural tests and `generated_vectors_test.mbt` as test-only black-box fixture data so package production exports remain unchanged per D-02. Regenerate the JSON digest in `fixtures/manifest.json` through the repository fixture policy.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1; pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check</automated>
  </verify>
  <done>The generated fixture data is fresh, provenance-valid, complete for every required Phase-20 guard, and available to both test layers without widening the PNG public interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire generated evidence into both test boundaries and enforce the PNG-only quality gate</name>
  <files>modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - `png_test.mbt` executes every generated public record through `ImageDecoder::decode(PngDecoder)` and observes only public outcomes, while `structural_wbtest.mbt` executes the corresponding private records for CRC scope, state transitions, preflight, reader-progress, and immutable-caller-state assertions.
    - The Png policy assertion reads `moon.pkg`, actual directory contents, the generated package interface after four-target `moon info`, and package-list output; it compares each against the existing PNG policy for imports, targets, semantic interface, production order, and exact inventory.
    - PNG-scoped negative fixtures prove the assertion rejects missing/extra imports, missing target, extra public decoder/encoder/stream type, changed source order, missing/extra production source, and extra package file without exercising QOI or broad release policy.
    - `Invoke-MoonQuality.ps1 -Lane Png` records and checks a Png-only stage trace: PNG foundation/interface policy, PNG scoped negative fixtures, exact PNG package allowlist, vector freshness, and the four-target PNG tests.
  </behavior>
  <action>Consume the generated test-only records in `png_test.mbt` so every generated case invokes only the public `PngDecoder` trait seam through a forward reader; assert the declared public error/capability outcome and caller Budget/Diagnostics invariants from record metadata. Keep private introspection in `structural_wbtest.mbt`: consume the private generated table for CRC type-plus-payload order, header gates before payload access, state transitions, exact boundary accounting, reader failure/no-progress propagation, and resource/preflight invariants. This realizes D-03 through the public API and D-04/D-05 through both appropriate test layers, without exposing fixture helpers as production API.

Mirror the QOI quality architecture only within PNG scope. In `Assert-Policy.ps1`, add PNG-specific foundation/interface and negative-fixture functions that select exactly `tchivs/mb-image/png`; validate candidate stability, policy imports, four target names, policy source order, `moon.pkg` imports/targets, the exact PNG directory file list including the generated test fixture, package-list membership, and the actual generated `pkg.generated.mbti` semantic interface. Generate the interface with `moon -C modules/mb-image info --target all --frozen` before comparison. Use temporary in-memory/on-disk PNG policy mutations to prove each scoped negative is rejected, including an extra public encoder or stream declaration; do not call the broad foundation or QOI policy/negative functions from these tests.

In `Invoke-MoonQuality.ps1`, replace the current JSON-only Png checks with named staged invocations of those PNG-specific policy and negative functions, an exact PNG package allowlist command, fixture Check mode, and `moon -C modules/mb-image test png --target all --frozen`. Add a Png lane-isolation guard and exact stage-trace assertion analogous to QOI's isolation mechanism, but omit a public-example stage because public PNG decode success belongs to Phase 22. Keep the lane confined to PNG policy and all four portable targets; do not add DEFLATE, raster decode success, encoding, FFI, release work, or public PNG streaming.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>Every generated hostile case has real black-box and appropriate white-box execution on js, wasm, wasm-gc, and native, and the isolated Png lane fails closed on actual public-interface and scoped-policy drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller PNG reader to structural validator | Untrusted transport can exploit input accounting, malformed framing, resource ceilings, and terminal-state checks. |
| Fixture JSON/generator to all-target tests | Generated evidence can otherwise omit guards or silently diverge from its provenance source. |
| PNG package sources to quality lane | Public API or package-policy drift can otherwise pass a lane that only reads policy JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-20-07 | Denial of service | `_png_require_eof` input accounting | high | mitigate | Accept actual EOF without charging a hypothetical byte; retain checked per-byte limits for all bytes that are actually read. |
| T-20-08 | Tampering | Generated structural corpus and expected outcomes | high | mitigate | Schema-validate complete named cases, verify manifest digest/provenance, and execute generated records through public and white-box all-target suites. |
| T-20-09 | Elevation of privilege | PNG public package surface | medium | mitigate | Generate and compare the actual interface; negative fixtures reject encoder or stream additions and inventory/import/target drift in PNG scope. |
| T-20-10 | Supply chain | PNG dependencies | low | accept | No package installation is authorized; the closure uses only existing workspace modules and scripts. |
</threat_model>

<verification>
Run `pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`, `moon -C modules/mb-image test png --target all --frozen`, and `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`. Confirm the four-target test run names js, wasm, wasm-gc, and native; inspect the public test source to ensure every generated public record calls `PngDecoder` through `ImageDecoder::decode`; and confirm the Png lane’s stage trace contains only PNG policy, negatives, package, fixture, and test stages.
</verification>

<success_criteria>
The verifier’s three failed truths become true: exact input-limit transport reaches the existing pending capability terminal without charging an absent EOF byte; complete provenance-tagged generated cases prove all Phase-20 structural/profile/metadata/resource/caller-state rules through both appropriate test layers; and the isolated PNG lane checks actual package policy and surface rather than merely reading JSON. The closure preserves the eager-only PngDecoder boundary and excludes DEFLATE, raster output, encoding, public streaming, FFI, release, and registry scope.
</success_criteria>

<output>
Create `.planning/phases/20-png-structural-safety-gate/20-03-SUMMARY.md` when done.
</output>
