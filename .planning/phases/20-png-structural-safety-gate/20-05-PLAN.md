---
phase: 20-png-structural-safety-gate
plan: "05"
type: execute
wave: 3
depends_on: ["20-04"]
files_modified: []
autonomous: true
requirements: [PNG-01, PNG-02, PNG-03]
must_haves:
  truths:
    - "The current PngDecoder continues to classify caller-owned PNG prefixes without consuming its Reader and enforces structural failures before a decoded image can be returned."
    - "Malformed framing, chunk order, CRC, semantic metadata, IEND, trailing-input, input-budget, and resource-envelope cases retain deterministic typed errors across js, wasm, wasm-gc, and native."
    - "The generated structural corpus, package policy, and isolated Png lane provide reproducible evidence of the safety gate without introducing release, registry, CI, QOI, or encoder work."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Current public probe/decode seam whose later decode success remains downstream of structural validation."
    - path: modules/mb-image/png/structural.mbt
      provides: "Private CRC, chunk-state, metadata-policy, and resource-preflight gate."
    - path: fixtures/png/cases.json
      provides: "Generated source corpus for the Phase-20 structural matrix."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/structural.mbt
      via: "PngDecoder decode path performs structural transport validation before DEFLATE/raster/image construction."
      pattern: "_png_read_transport"
    - from: fixtures/png/cases.json
      to: modules/mb-image/png/generated_vectors.mbt
      via: "Generate-PngStructuralVectors.ps1 validates and emits the structural case tables consumed by public and white-box tests."
      pattern: "Generate-PngStructuralVectors"
---

<objective>
Audit the already-implemented Phase-20 PNG structural safety gate against the current, later-phase PNG decoder and record reproducible all-target evidence rather than reopening completed production work.

Purpose: distinguish genuine Phase-20 safety debt from intended Phase 21-25 decoding, encoding, and colour-semantic expansion.
Output: a verification summary containing the three command results and any concrete regression found; no production, policy, release, CI, registry, QOI, or roadmap change.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-png-structural-safety-gate/20-CONTEXT.md
@.planning/phases/20-png-structural-safety-gate/20-RESEARCH.md
@.planning/phases/20-png-structural-safety-gate/20-04-SUMMARY.md
@.planning/phases/20-png-structural-safety-gate/20-VERIFICATION.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/structural_wbtest.mbt
@scripts/fixtures/Generate-PngStructuralVectors.ps1
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<source_audit>
SOURCE   | ID     | Feature / constraint | Plan | Status | Notes
GOAL     | --     | Safely identify and structurally validate a PNG before invalid input can expose an image. | 20-05 | COVERED | Audit asserts structural rejection precedes the later decode-success path.
REQ      | PNG-01 | Bounded, deterministic, non-consuming PNG signature probe. | 20-05 | COVERED | Re-run public all-target test coverage.
REQ      | PNG-02 | Typed rejection for framing, order, CRC, semantic chunks, IEND, and trailing input. | 20-05 | COVERED | Generated 89-case corpus and white-box/public paths are checked.
REQ      | PNG-03 | Checked input, geometry, output, work, allocation, Budget, and metadata policy before image visibility. | 20-05 | COVERED | Existing limits and budget cases run through the current decoder.
RESEARCH | --     | Fixed private scratch, CRC over type plus payload, checked arithmetic, CodecLimits/Budget preflight, and generated provenance. | 20-05 | COVERED | Commands prove the existing implementation and generated corpus; no new implementation is planned.
CONTEXT  | D-01   | Original accepted profile was non-interlaced 8-bit RGB/RGBA. | 20-05 | COVERED | Treat Phases 21-24's documented profile expansion as downstream scope, while confirming malformed IHDR/order remains rejected.
CONTEXT  | D-02   | Eager ImageDecoder seam; no public PNG streaming API. | 20-05 | COVERED | Verify current public interface through the Png policy lane.
CONTEXT  | D-03   | Signature, IHDR, contiguous IDAT, empty IEND, CRC, and strict EOF. | 20-05 | COVERED | Structural vector check plus public/private all-target execution.
CONTEXT  | D-04   | Critical/semantic chunks and opaque metadata policy are explicit. | 20-05 | COVERED | Current generated cases assert typed outcomes before a result is visible.
CONTEXT  | D-05   | Checked limits, budget, and diagnostics before exposure. | 20-05 | COVERED | Current generated limits/Budget assertions run on all targets.
CONTEXT  | D-06   | Small provenance-tagged hostile fixtures; no Phase-20 legal DEFLATE/filter/public-workflow evidence. | 20-05 | COVERED | Limit evidence to cases.json and structural-generator outputs; leave later-phase decode/encode evidence untouched.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the generated structural matrix on every portable target</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/structural.mbt, modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt, fixtures/png/cases.json, modules/mb-image/png/generated_vectors.mbt, modules/mb-image/png/generated_vectors_test.mbt, scripts/fixtures/Generate-PngStructuralVectors.ps1</files>
  <action>Perform a read-only Phase-20 regression audit. Per D-01 through D-06, first run the structural-vector generator in Check mode, then run the PNG package test suite with `--target all --frozen`. Inspect the test names and generated case routes to confirm the caller-owned prefix probe, framing/type/order/CRC/IEND/EOF checks, semantic and opaque-metadata branches, checked geometry/input/output/work/allocation limits, and immutable caller-Budget assertions still execute through the public and white-box paths. Attribute legal DEFLATE, raster output, encoder, and expanded colour-profile successes to Phases 21-25; do not modify their code or recast those delivered scopes as a Phase-20 failure. If a Phase-20 structural case fails or is no longer routed through PngDecoder, record its exact case ID, target, typed outcome, and causal source location in the summary before proposing any follow-up.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Generator freshness succeeds and all PNG tests pass on wasm, wasm-gc, js, and native, with Phase-20 structural cases demonstrably retaining their generated public and white-box coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the isolated structural-policy evidence and record the audit boundary</name>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <action>Run the existing isolated `Png` quality lane without editing policy or quality scripts. Per D-02 and D-06, confirm its trace checks the actual PNG interface/import/target/source/file inventory, structural vector freshness, and four-target tests while remaining isolated from broad qualification, release, registry, credential, CI, and QOI routes. In the summary, state that historical Phase-20 plans 01 and 03 are not open production debt: Plan 02, Plan 04, and the passed Phase-20 verification document the completed safety work; public PngEncoder and successful decoded images are later Phase 21-25 behavior. If the lane fails, capture the first failing stage and command output, and create no repair outside a separately authorized gap plan.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The isolated lane passes and the summary gives an evidence-backed boundary between retained Phase-20 structural safety and intentional later PNG capabilities, or precisely identifies the first reproducible safety-gate regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller bytes to PngDecoder | Untrusted prefix, chunk framing, type bytes, payload, CRC, metadata, and declared resource envelope enter the decoder. |
| Structural gate to later decode/raster stages | Structural acceptance supplies bounded transport state; later code may create an image only after its own decoding checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05-01 | Denial of service | Lengths, IHDR arithmetic, and resource budgets | high | mitigate | Re-run generated exact/below limit cases and all-target tests covering checked ceilings and caller-Budget atomicity. |
| T-20-05-02 | Tampering | Chunk type, CRC, ordering, and terminal framing | high | mitigate | Re-run structural corpus cases through public and white-box paths, including type-form, CRC, IDAT/IEND, and EOF cases. |
| T-20-05-03 | Tampering | Package policy and generated evidence | medium | mitigate | Run the isolated Png lane, including vector freshness and scoped policy/interface checks. |
| T-20-05-04 | Elevation of privilege | Structural-to-image handoff | medium | mitigate | Confirm rejected structural inputs never reach later decode success; classify later legal decoding as Phase 21-25 scope rather than bypassing validation. |
| T-20-05-SC | Tampering | Package installs | low | accept | This audit installs no packages and changes no dependency state. |
</threat_model>

<verification>
Run the generator check, all-target PNG package tests, and the isolated Png quality lane. Preserve their complete exit status and target/stage evidence in `20-05-SUMMARY.md`.
</verification>

<success_criteria>
The current repository supplies reproducible evidence that PNG-01, PNG-02, and PNG-03 remain enforced at the structural boundary on all four portable targets. The audit neither duplicates completed production work nor expands into Phase 21-25 decoding, encoding, colour, release, CI, registry, or QOI scope.
</success_criteria>

<output>
Create `.planning/phases/20-png-structural-safety-gate/20-05-SUMMARY.md` when done.
</output>
