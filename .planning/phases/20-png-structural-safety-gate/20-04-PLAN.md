---
phase: 20-png-structural-safety-gate
plan: "04"
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - fixtures/png/cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngStructuralVectors.ps1
  - modules/mb-image/png/generated_vectors.mbt
  - modules/mb-image/png/generated_vectors_test.mbt
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/structural_wbtest.mbt
autonomous: true
gap_closure: true
requirements: [PNG-01, PNG-02, PNG-03]
must_haves:
  truths:
    - "The PNG generated corpus has a finite, provenance-checked record for every structural, semantic, integrity, and resource boundary promised by 20-03, with byte construction and exact typed expectation encoded as data."
    - "Every record runs through PngDecoder at the public ImageDecoder boundary and the matching white-box structural assertion on js, wasm, wasm-gc, and native."
    - "The corpus proves both accepted-at-ceiling and rejected-below-ceiling behavior for every Phase-20 CodecLimits and Budget envelope without adding PNG parser behavior."
  artifacts:
    - path: fixtures/png/cases.json
      provides: "Complete finite PNG transport matrix with mutation phase, construction, typed outcome, options, limits, Budget profile, and test-layer routing per record."
    - path: scripts/fixtures/Generate-PngStructuralVectors.ps1
      provides: "Strict schema, completeness, provenance, digest, and deterministic private/public table generation."
    - path: modules/mb-image/png/png_test.mbt
      provides: "Public PngDecoder matrix execution and caller-state assertions."
    - path: modules/mb-image/png/structural_wbtest.mbt
      provides: "White-box matrix execution for state, CRC-before-policy, and resource-preflight assertions."
  key_links:
    - from: fixtures/png/cases.json
      to: scripts/fixtures/Generate-PngStructuralVectors.ps1
      via: "The generator rejects any missing declared matrix ID, malformed metadata, unknown profile, stale provenance record, or stale digest."
      pattern: "requiredCaseIds"
    - from: modules/mb-image/png/generated_vectors_test.mbt
      to: modules/mb-image/png/png_test.mbt
      via: "Every public record invokes ImageDecoder::decode with PngDecoder and checks the generated typed expectation."
      pattern: "_generated_png_public_cases"
    - from: modules/mb-image/png/generated_vectors.mbt
      to: modules/mb-image/png/structural_wbtest.mbt
      via: "Every private record drives the matching structural, CRC ordering, and preflight assertion."
      pattern: "_generated_png_structural_cases"
---

<objective>
Close the remaining evidence gap in 20-03 by replacing the 17-case representative corpus with the complete finite structural/semantic/CRC/resource matrix that 20-03 promised.

Purpose: make every Phase-20 safety guard reproducibly observable through the existing public decoder and white-box proof boundaries.
Output: provenance-tagged matrix fixtures, schema-complete deterministic generation, and all-target public/white-box matrix tests. No parser, API, DEFLATE, raster, encoder, or quality-lane behavior is changed unless a new case exposes a real parser defect.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-png-structural-safety-gate/20-CONTEXT.md
@.planning/phases/20-png-structural-safety-gate/20-RESEARCH.md
@.planning/phases/20-png-structural-safety-gate/20-VERIFICATION.md
@.planning/phases/20-png-structural-safety-gate/20-03-PLAN.md
@.planning/phases/20-png-structural-safety-gate/20-02-SUMMARY.md
@fixtures/png/cases.json
@fixtures/manifest.json
@scripts/fixtures/Generate-PngStructuralVectors.ps1
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/structural_wbtest.mbt
@modules/mb-image/png/generated_vectors.mbt
@modules/mb-image/png/generated_vectors_test.mbt

<interfaces>
- `PngDecoder` and `ImageDecoder::decode` are the only public execution seam; a structurally accepted Phase-20 transport returns `CapabilityUnavailable("png-decode", "deflate-and-raster-pending")`.
- `_png_error` creates `Data` / `InvalidEncoding` errors with the supplied PNG context. `_png_limit` creates `Resource` / `BudgetExceeded` errors with the supplied resource context. Truncation is asserted by `UnexpectedEndOfStream` code, and opaque-preservation refusal is the existing `CapabilityUnavailable` context.
- `CodecLimits` controls input, width, height, pixels, output, and work. PNG preflight derives image bytes, filtered output, and work, then makes a non-mutating `Budget::child` request for bytes, allocations, allocation size, width, height, pixels, and work.
- The generated `*_test.mbt` helper remains test-only; `generated_vectors.mbt` remains package-private. Per D-02, this plan must not expose a helper or introduce a public streaming seam.
</interfaces>
</context>

<case_matrix>
All rows below are currently absent from the 17-record corpus. `P+W` means the generated public `png_test.mbt` loop and generated/private `structural_wbtest.mbt` loop must both run the record; W additionally asserts the named branch ordering or preflight invariant. `base-rgb` means the existing 57-byte 1x1 RGB structural transport, with every appended chunk CRC recomputed over its type and payload.

| Case ID | Mutation phase | Byte construction / variation | Expected typed outcome (category / code / context) | Runs |
| --- | --- | --- | --- | --- |
| empty-signature | signature | Zero input bytes. | IO / `UnexpectedEndOfStream` | P+W |
| truncated-signature-7 | signature | First seven valid signature bytes only. | IO / `UnexpectedEndOfStream` | P+W |
| truncated-chunk-length | header | Valid signature followed by one through three bytes of the first chunk length; use one named three-byte boundary record. | IO / `UnexpectedEndOfStream` | P+W |
| truncated-chunk-type | header | Valid signature, a complete zero length, and only three IHDR type bytes. | IO / `UnexpectedEndOfStream` | P+W |
| ihdr-not-first | header | Insert a CRC-valid unknown ancillary chunk before the first IHDR. | Data / `InvalidEncoding` / `png-ihdr-first` | P+W |
| duplicate-ihdr | header | Append a second CRC-valid 13-byte IHDR immediately after the first. | Data / `InvalidEncoding` / `png-ihdr-order` | P+W |
| ihdr-length-12 | header | First IHDR declares and carries 12 payload bytes with a valid CRC. | Data / `InvalidEncoding` / `png-ihdr-length` | P+W |
| ihdr-zero-width | payload | Base IHDR with width zero, CRC recomputed. | Data / `InvalidEncoding` / `png-dimensions` | P+W |
| ihdr-zero-height | payload | Base IHDR with height zero, CRC recomputed. | Data / `InvalidEncoding` / `png-dimensions` | P+W |
| ihdr-bit-depth-1 | payload | Base IHDR bit depth set to one, CRC recomputed. | Data / `InvalidEncoding` / `png-bit-depth` | P+W |
| ihdr-colour-grayscale | payload | Base IHDR colour type zero, CRC recomputed. | Data / `InvalidEncoding` / `png-colour-type` | P+W |
| ihdr-colour-invalid-1 | payload | Base IHDR colour type one, CRC recomputed. | Data / `InvalidEncoding` / `png-colour-type` | P+W |
| ihdr-colour-indexed | payload | Base IHDR colour type three, CRC recomputed. | Data / `InvalidEncoding` / `png-colour-type` | P+W |
| ihdr-colour-grayscale-alpha | payload | Base IHDR colour type four, CRC recomputed. | Data / `InvalidEncoding` / `png-colour-type` | P+W |
| ihdr-colour-invalid-5 | payload | Base IHDR colour type five, CRC recomputed. | Data / `InvalidEncoding` / `png-colour-type` | P+W |
| ihdr-compression-method | payload | Base IHDR compression method set nonzero, CRC recomputed. | Data / `InvalidEncoding` / `png-ihdr-method` | P+W |
| ihdr-filter-method | payload | Base IHDR filter method set nonzero, CRC recomputed. | Data / `InvalidEncoding` / `png-ihdr-method` | P+W |
| ihdr-interlace-method | payload | Base IHDR interlace method set nonzero, CRC recomputed. | Data / `InvalidEncoding` / `png-ihdr-method` | P+W |
| ihdr-crc | CRC | Flip one stored CRC bit on an otherwise valid IHDR. | Data / `InvalidEncoding` / `png-crc` | P+W (W proves CRC precedes IHDR policy) |
| idat-before-ihdr | order | Valid empty IDAT as first chunk after signature. | Data / `InvalidEncoding` / `png-ihdr-first` | P+W |
| split-contiguous-idat | order | Split the existing empty IDAT into two adjacent CRC-valid IDAT chunks. | Capability / `CapabilityUnavailable` / `deflate-and-raster-pending` | P+W |
| ancillary-after-idat | order | Insert one CRC-valid unknown ancillary chunk after IDAT and before IEND. | Capability / `CapabilityUnavailable` / `deflate-and-raster-pending` | P+W |
| idat-after-semantic | order | Insert a CRC-valid known semantic chunk between two IDAT chunks. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| iend-before-idat | IEND | IHDR followed immediately by empty CRC-valid IEND. | Data / `InvalidEncoding` / `png-iend-order` | P+W |
| iend-before-ihdr | IEND | Empty CRC-valid IEND immediately after signature. | Data / `InvalidEncoding` / `png-ihdr-first` | P+W |
| truncated-iend-crc | IEND / EOF | Complete IHDR and IDAT then empty IEND header with fewer than four CRC bytes. | IO / `UnexpectedEndOfStream` | P+W |
| iend-crc | CRC / IEND | Flip one stored CRC bit on an otherwise valid empty IEND. | Data / `InvalidEncoding` / `png-crc` | P+W (W proves CRC precedes IEND acceptance) |
| duplicate-iend | IEND / EOF | Valid completed transport followed by a second complete empty IEND chunk. | Data / `InvalidEncoding` / `png-trailing-data` | P+W |
| post-iend-idat | IEND / EOF | Valid completed transport followed by a complete CRC-valid IDAT chunk. | Data / `InvalidEncoding` / `png-trailing-data` | P+W |
| ancillary-crc | CRC | Insert unknown ancillary chunk with corrupted stored CRC before IDAT while discard is selected. | Data / `InvalidEncoding` / `png-crc` | P+W (W proves CRC before discard) |
| ancillary-preserve | metadata | Insert CRC-valid unknown ancillary chunk and set `preserve_opaque_metadata=true`. | Capability / `CapabilityUnavailable` / `opaque-metadata-preservation` | P+W |
| semantic-crc | CRC | Insert a PLTE chunk with corrupted stored CRC. | Data / `InvalidEncoding` / `png-crc` | P+W (W proves CRC before semantic rejection) |
| semantic-trns | metadata | Insert CRC-valid zero-payload `tRNS` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-chrm | metadata | Insert CRC-valid zero-payload `cHRM` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-gama | metadata | Insert CRC-valid zero-payload `gAMA` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-iccp | metadata | Insert CRC-valid zero-payload `iCCP` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-sbit | metadata | Insert CRC-valid zero-payload `sBIT` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-srgb | metadata | Insert CRC-valid zero-payload `sRGB` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-cicp | metadata | Insert CRC-valid zero-payload `cICP` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-mdcv | metadata | Insert CRC-valid zero-payload `mDCv` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-clli | metadata | Insert CRC-valid zero-payload `cLLi` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-actl | metadata | Insert CRC-valid zero-payload `acTL` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-fctl | metadata | Insert CRC-valid zero-payload `fcTL` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| semantic-fdat | metadata | Insert CRC-valid zero-payload `fdAT` before IDAT. | Data / `InvalidEncoding` / `png-semantic-chunk` | P+W |
| rgba-at-ceiling | resource | Existing valid 1x1 RGBA transport with its exact computed width, height, pixel, image/output byte, and work ceilings. | Capability / `CapabilityUnavailable` / `deflate-and-raster-pending` | P+W |
| width-at-ceiling / width-below | resource | `base-rgb` with `max_width=1`, then an otherwise identical profile with zero. | Capability terminal; then Resource / `BudgetExceeded` / `width` | P+W |
| height-at-ceiling / height-below | resource | `base-rgb` with `max_height=1`, then an otherwise identical profile with zero. | Capability terminal; then Resource / `BudgetExceeded` / `height` | P+W |
| pixels-at-ceiling / pixels-below | resource | `base-rgb` with `max_pixels=1`, then an otherwise identical profile with zero. | Capability terminal; then Resource / `BudgetExceeded` / `pixels` | P+W |
| image-bytes-at-ceiling / image-bytes-below | resource | `base-rgb` with output ceiling equal to its three image bytes, then two. | Capability terminal; then Resource / `BudgetExceeded` / `image-bytes` | P+W |
| output-bytes-at-ceiling / output-bytes-below | resource | `base-rgb` with output ceiling equal to its four filtered bytes, then three. | Capability terminal; then Resource / `BudgetExceeded` / `output-bytes` | P+W |
| work-at-ceiling / work-below | resource | `base-rgb` with work ceiling equal to seven, then six. | Capability terminal; then Resource / `BudgetExceeded` / `work` | P+W |
| budget-bytes-at-ceiling / budget-bytes-below | resource | `base-rgb` with child Budget bytes exactly three, then two. | Capability terminal; then Resource / `BudgetExceeded` / `bytes` | P+W |
| budget-allocations-at-ceiling / budget-allocations-below | resource | `base-rgb` with child Budget allocations exactly one, then zero. | Capability terminal; then Resource / `BudgetExceeded` / `allocations` | P+W |
| budget-allocation-size-at-ceiling / budget-allocation-size-below | resource | `base-rgb` with child allocation-size exactly three, then two. | Capability terminal; then Resource / `BudgetExceeded` / `allocation_size` | P+W |
| budget-width-at-ceiling / budget-width-below | resource | `base-rgb` with child Budget width exactly one, then zero. | Capability terminal; then Resource / `BudgetExceeded` / `width` | P+W |
| budget-height-at-ceiling / budget-height-below | resource | `base-rgb` with child Budget height exactly one, then zero. | Capability terminal; then Resource / `BudgetExceeded` / `height` | P+W |
| budget-pixels-at-ceiling / budget-pixels-below | resource | `base-rgb` with child Budget pixels exactly one, then zero. | Capability terminal; then Resource / `BudgetExceeded` / `pixels` | P+W |
| budget-work-at-ceiling / budget-work-below | resource | `base-rgb` with child Budget work exactly seven, then six. | Capability terminal; then Resource / `BudgetExceeded` / `work` | P+W |

Retain and execute the existing records for malformed signature, overlong chunk length, non-letter and reserved-bit type form, missing and separated IDAT, nonempty IEND, trailing byte, unknown critical, PLTE semantic rejection, unknown ancillary discard, RGB/RGBA validity, and the exact/over 57-byte input pair. The retained input pair is the `input-bytes-at-ceiling / input-bytes-below` resource boundary and must receive the same schema metadata and P+W execution as every added row.
</case_matrix>

<source_audit>
SOURCE   | ID     | Feature / constraint | Coverage | Status
GOAL     | --     | Safely identify and structurally validate the supported PNG subset before image output. | Tasks 1-2 | COVERED
REQ      | PNG-01 | Bounded non-consuming deterministic PNG probe evidence remains preserved in the generated malformed-signature records. | Task 2 | COVERED
REQ      | PNG-02 | Typed framing, order, CRC, unsupported/semantic chunk, IEND, and EOF rejection evidence. | Tasks 1-2 | COVERED
REQ      | PNG-03 | Checked input, geometry, output, work, allocation, Budget, metadata, and caller-state evidence. | Tasks 1-2 | COVERED
RESEARCH | --     | Fixed scratch, CRC over type plus payload, strict EOF, and no whole-chunk allocation. | Tasks 1-2 | COVERED
RESEARCH | --     | Fixture records have manifest provenance, digest, deterministic generation, and all-target public/white-box proof. | Task 1 | COVERED
CONTEXT  | D-01   | RGB8/RGBA8, non-interlaced, method-zero profile is the only accepted profile. | Tasks 1-2 | COVERED
CONTEXT  | D-02   | Eager PngDecoder boundary only; no public streaming surface. | Tasks 1-2 | COVERED
CONTEXT  | D-03   | Signature, first/only IHDR, contiguous IDAT, one empty IEND, CRCs, and EOF. | Tasks 1-2 | COVERED
CONTEXT  | D-04   | CRC-before-policy for critical/semantic/ancillary chunks and preserve-or-fail opaque metadata. | Tasks 1-2 | COVERED
CONTEXT  | D-05   | Checked limits, Budget preflight, and immutable caller state before output. | Tasks 1-2 | COVERED
CONTEXT  | D-06   | Small provenance-tagged hostile structural and limit fixtures; no decode/encode work. | Task 1 | COVERED
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the complete finite PNG corpus contract and generate both record tables</name>
  <files>fixtures/png/cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngStructuralVectors.ps1, modules/mb-image/png/generated_vectors.mbt, modules/mb-image/png/generated_vectors_test.mbt</files>
  <behavior>
    - Every existing and matrix-listed case has a unique stable ID; a mutation phase; reproducible compact bytes or a declarative construction source; expected error category, code, and context or pending capability terminal; decode options; named CodecLimits and Budget profiles; immutable-caller-state requirement; and P+W routing.
    - Generator Check mode rejects missing required IDs, duplicate IDs, malformed byte encodings, incomplete typed expectations, unknown profiles, missing layer routing, missing provenance metadata, stale manifest digest, and stale generated tables.
    - The generated private and test-only public tables preserve all metadata necessary to run every record, including nonstandard resource and Budget profiles; they do not add a production PNG export.
  </behavior>
  <action>Implement the matrix above as the finite source-of-truth fixture schema, retaining all current 17 records and adding every listed missing record. Per D-01, build only accepted RGB8/RGBA8 non-interlaced method-zero transports; rejected IHDR records mutate one named field from a CRC-valid base. Per D-03, construct chunks from explicit length, type, payload, and recomputed CRC material so every signature/header/payload/CRC/IEND/EOF row reaches its named stage. Per D-04, keep the five distinct CRC corruption paths and all thirteen known semantic families separate, and encode discard and preservation dispositions as distinct records. Per D-05, define named complete `CodecLimits` and `ResourceLimits` profiles for every paired at-ceiling/below-ceiling row, including input, width, height, pixels, image/output bytes, work, bytes, allocations, allocation size, Budget width, Budget height, Budget pixels, and Budget work. Preserve source transport immutability by using the existing bounded reader setup; no parser source file is in scope.

Upgrade generator validation from its count threshold to an exact required-ID set and required per-record schema: construction/mutation phase, bytes, structured typed expectation, options, limits profile, Budget profile, immutable-state flag, and public/white-box routes. Validate profile objects contain every `CodecLimits` and `ResourceLimits` field used by the harness, and reject records that attempt to name unimplemented parser behavior. Render rich tuples for both generated tables so tests do not duplicate profile mapping. Retain all manifest required fields, recompute only the PNG fixture SHA-256 after corpus change, and make Check mode compare both generated artifacts byte-for-byte.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1; pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check</automated>
  </verify>
  <done>The fixture JSON, manifest digest, and both generated MoonBit tables represent every row of the explicit matrix with complete schema/provenance metadata and pass deterministic freshness checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute every corpus record through public and white-box PNG proof boundaries</name>
  <files>modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt, modules/mb-image/png/generated_vectors_test.mbt, modules/mb-image/png/generated_vectors.mbt</files>
  <behavior>
    - `png_test.mbt` runs every P-routed generated record through `ImageDecoder::decode(PngDecoder)` and distinguishes Data/InvalidEncoding, Resource/BudgetExceeded, IO/UnexpectedEndOfStream, and CapabilityUnavailable terminals by generated category, code, and context.
    - Every record marked immutable snapshots all relevant caller Budget dimensions and `Diagnostics::length()` before the decode and asserts unchanged state after every terminal, accepted-at-ceiling capability, and below-ceiling resource result.
    - `structural_wbtest.mbt` runs every W-routed record and explicitly proves header/state branch selection, CRC-before-IHDR/semantic/ancillary/IEND policy, contiguous-IDAT transition, EOF/trailing handling, and each CodecLimits/Budget preflight dimension.
  </behavior>
  <action>Replace positional fixture indexing and the two hard-coded public profile branches with generated record metadata. In `png_test.mbt`, construct the reader, `DecodeOptions`, `CodecLimits`, and `Budget` solely from the public generated tuple; call only `ImageDecoder::decode(PngDecoder)`; assert the generated category/code/context or the existing pending capability operation/context; and snapshot/compare every `Budget::remaining()` field plus diagnostics for each immutable record. This is the public proof required by D-02, D-03, D-04, and D-05.

In `structural_wbtest.mbt`, consume the private generated tuple instead of hand-selecting array offsets or maintaining a separate hand-written envelope list. For every W-routed row, assert the same typed terminal and add focused named assertions that confirm the intended precedence: corrupted IHDR, semantic, ancillary, and IEND chunks stop at CRC before their later policy/state errors; IHDR/order/IDAT/IEND/EOF mutations stop at their declared state; and all limit/Budget pairs accept at equality and reject only below it. Assert caller Budget and Diagnostics remain unchanged for every preflight/policy terminal. Do not modify `structural.mbt`, `png.mbt`, MoonBit package configuration, or quality-lane scripts. If a matrix row demonstrates a real existing-parser mismatch, report it with the row ID and failing typed assertion; change parser behavior only in a separately authorized repair.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Every matrix record is executed through the public decoder and white-box proof boundary on js, wasm, wasm-gc, and native, with exact typed outcomes and immutable caller state demonstrated by data-driven tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted PNG bytes to structural validator | Matrix records must exercise hostile framing, integrity, policy, and resource boundaries without becoming parser changes. |
| Fixture JSON to generated MoonBit tests | Missing, mistyped, or stale records could falsely certify safety coverage. |
| Generated profiles to caller Budget/Diagnostics | A profile/harness mismatch could hide an incorrect limit or state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Tampering | `fixtures/png/cases.json` and generator | high | mitigate | Exact required-ID/schema validation, deterministic table comparison, and manifest provenance/digest verification in Task 1. |
| T-20-12 | Denial of service | Structural resource preflight | high | mitigate | Paired equality/below boundary records execute each codec and inherited Budget dimension through both test layers in Task 2. |
| T-20-13 | Tampering | Chunk CRC/policy sequencing | high | mitigate | Separate IHDR, IDAT, semantic, ancillary, and IEND corruption records verify CRC terminal precedence in Task 2. |
| T-20-14 | Repudiation | Corpus coverage claim | medium | mitigate | Stable IDs, construction metadata, routes, and all-target automated runs create reproducible evidence. |
| T-20-15 | Supply chain | Fixture generation | low | accept | No package installation or external execution is introduced; generation uses existing repository PowerShell and MoonBit tooling. |
</threat_model>

<verification>
Run generator write mode followed by Check mode, then `moon -C modules/mb-image test png --target all --frozen`. Confirm the test output covers js, wasm, wasm-gc, and native. Inspect generated records to confirm every case ID in the matrix is emitted to both tables with structured typed expectations and profile names, and verify the public test references only `PngDecoder` through `ImageDecoder::decode`.
</verification>

<success_criteria>
The 17-case representative corpus is replaced by the exact finite 20-03 matrix: every structural transition, all unsupported IHDR/profile forms, all 13 semantic chunk families, five CRC corruption families, opaque dispositions, IEND/EOF variants, and every CodecLimits/Budget equality-versus-below boundary has a provenance-tagged record and P+W all-target proof. No new parser/API/decode/encode behavior is added unless a named matrix case first demonstrates a defect.
</success_criteria>

<output>
Create `.planning/phases/20-png-structural-safety-gate/20-04-SUMMARY.md` when done.
</output>
