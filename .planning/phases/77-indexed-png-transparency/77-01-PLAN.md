---
phase: 77-indexed-png-transparency
plan: 01
type: execute
wave: 1
depends_on: [76-01]
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
autonomous: true
requirements:
  - INDEX-03
must_haves:
  truths:
    - "A library user can construct an immutable Indexed8 PNG source whose palette alpha has exactly one entry for every RGB palette entry."
    - "An all-opaque indexed source produces the exact Phase 76 eager bytes and contains no tRNS chunk."
    - "A source with non-opaque palette entries emits one canonical tRNS chunk after PLTE, whose payload ends at the last non-255 alpha, and public generic decode returns exact RGBA8 pixels."
    - "Invalid alpha sources and indexed transparency limit or budget rejection expose no writer bytes and leave the affected preflight budget unchanged."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Immutable, validated, single-allocation Indexed8 source containing indices, RGB palette bytes, and palette alpha bytes.
    - path: modules/mb-image/png/encode.mbt
      provides: Atomic Indexed8 preflight with canonical optional tRNS frame facts.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: One acknowledged eager byte machine that emits PLTE and optional tRNS with independent CRC state.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Independent wire/CRC, public RGB8/RGBA8 decode, opaque compatibility, and atomicity evidence.
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Private variable-frame and tRNS acknowledgement-timing evidence.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: PngIndexedImage supplies palette length and per-entry alpha to indexed preflight.
      pattern: "PngIndexedImage|alpha_at|trns_length"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: Indexed preflight derives one PngFrameFacts layout consumed by PngEncodeMachine.
      pattern: "PngFrameFacts|trns_start|trns_length"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode_test.mbt
      via: Emitted Type-3 chunks are independently parsed as IHDR, PLTE, optional tRNS, IDAT, and IEND and decoded with PngDecoder.
      pattern: "tRNS|PngDecoder|png_indexed_crc32"
---

<objective>
Extend the eager Indexed8 PNG path with canonical palette transparency while preserving the owning source boundary and every all-opaque Phase 76 byte.

Purpose: deliver INDEX-03 without widening generic image models or creating another PNG traversal.
Output: palette-alpha ownership, optional canonical tRNS frame facts and acknowledged emission, plus independent wire, public decode, compatibility, and atomicity proof.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/76-indexed8-source-eager-plte/76-01-SUMMARY.md
@.planning/phases/77-indexed-png-transparency/77-CONTEXT.md
@.planning/phases/77-indexed-png-transparency/77-RESEARCH.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/raster_decode.mbt

<decisions>
D-01: Extend the immutable owning `PngIndexedImage` with one alpha byte per palette entry while preserving its validated source boundary and all all-opaque Phase 76 bytes.
D-02: Emit no tRNS when all alpha entries are 255; otherwise emit every alpha byte through the last non-255 entry, including any intermediate 255 values.
D-03: Reject alpha-count mismatches before ownership, preflight budget admission, or writer exposure, preserving atomicity.
D-04: Reuse the private variable-frame facts and one acknowledged byte machine to emit IHDR → PLTE → tRNS → IDAT → IEND with an independent tRNS CRC and public generic RGBA8 decode evidence.
D-05: Keep scope to eager Indexed8; caller-buffered parity, Indexed low bit depths, Adam7, strategy families, model widening, and quantization are deferred.
</decisions>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one palette-alpha Indexed8 source through canonical eager tRNS output</name>
  <reversibility rating="costly">The extended public `PngIndexedImage` constructor establishes the palette-alpha source contract consumed by later indexed transport work.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - Test 1: a four-entry Indexed8 source with alpha `[0, 255, 128, 255]` eagerly emits the full `IHDR → PLTE → tRNS → IDAT → IEND` path, with the canonical three-byte tRNS payload `[0, 255, 128]`.
    - Test 2: public generic decode of that output returns RGBA8, preserving RGB triples and alpha for transparent, intermediate-opaque, partial-alpha, and implicit-tail-opaque palette indices.
    - Test 3: an all-255 alpha table follows the existing no-tRNS branch and yields the frozen Phase 76 bytes.
  </behavior>
  <action>Per D-01 and D-03, extend `PngIndexedImage::new` to accept palette-alpha bytes, validate alpha count equals the already validated RGB palette-entry count before allocation or charge, then retain indices, RGB palette, and alpha in one checked `OwnedBytes` layout. Store explicit segment lengths or offsets so `palette_length`, RGB lookup, and a new internal per-entry alpha lookup remain correct after the alpha segment is added; update all existing constructor call sites to provide all-opaque alpha. Keep validation-before-copy ordering, checked allocation arithmetic, index-range validation, PNG-U32 geometry checks, and the PNG-only immutable API boundary; do not widen `ImageView`, `ImageFormat`, or `ImageEncoder`. Per D-02 and D-03, have `_png_encode_indexed_preflight` scan the owned alpha table before any limit check or budget charge, derive `trns_length` as zero or the last `alpha != 255` index plus one, and extend `PngFrameFacts` with the explicit optional tRNS start/length so all-opaque facts retain the Phase 76 PLTE/IDAT/IEND positions. Per D-04, consume those facts in the existing `PngEncodeMachine`: add type-seeded tRNS CRC state; generate length, type, payload, and CRC only for a nonzero tRNS span; keep the chunk order IHDR, PLTE, tRNS, IDAT, IEND; and update tRNS CRC only after an accepted payload byte is acknowledged, exactly as PLTE and IDAT do. Retain the single eager byte traversal, Stored/None/non-interlaced Indexed8 profile, and existing writer lifecycle. First add the public end-to-end test for a four-entry palette with alpha `[0, 255, 128, 255]`; encode it, establish its canonical tRNS payload, and decode it through `@codec.ImageDecoder::decode(PngDecoder::new(), ...)` to assert four channels plus exact RGB and alpha semantics, including the omitted trailing alpha becoming 255.</action>
  <verify>
    <automated>Set-Location modules/mb-image/png; moon test --target all</automated>
  </verify>
  <done>A valid palette-alpha source is fully owned and eagerly emits one canonical optional tRNS chunk through the existing acknowledged machine; public generic decode returns exact RGBA8 pixels, while an all-opaque source follows the unchanged no-tRNS layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove transparency framing, CRC acknowledgement, opaque compatibility, and atomic admission</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt</files>
  <behavior>
    - Test 1: a test-local wire walker finds exactly IHDR, PLTE, tRNS, IDAT, and IEND for a transparent source, validates every chunk CRC independently, and confirms the tRNS payload `[0, 255, 128]` retains the intermediate opaque byte.
    - Test 2: all-opaque alpha output contains no tRNS, has the Phase 76 fixed bytes and length, and continues to decode as RGB8.
    - Test 3: a mismatched alpha table fails source construction with unchanged budget; output/work limit and budget requests that reject the transparent frame leave writer position zero and the preflight budget state unchanged.
    - Test 4: private frame facts place tRNS after PLTE and before IDAT, and tRNS rolling CRC changes only when its payload byte is acknowledged.
  </behavior>
  <action>In `encode_test.mbt`, extend the Phase 76 test-local U32/slice/CRC helpers into a variable-offset indexed chunk walker that computes CRC-32 over each chunk type and payload without production frame, CRC, scanline, or decode helpers. Use the transparent four-entry source to assert the exact chunk sequence, one PLTE payload, the canonical tRNS payload `[0, 255, 128]`, independent CRCs for IHDR/PLTE/tRNS/IDAT/IEND, and the fixed Stored/filter-None indexed raster. Keep its public generic decode assertion separate from this wire oracle and explicitly inspect RGB8/RGBA8 channel counts and all palette-index alpha outcomes. Per D-01 and D-02, retain a literal frozen Phase 76 all-opaque vector plus its 89-byte length, prove no tRNS appears, and prove public decode remains RGB8. Per D-03, add alpha-count mismatch cases alongside source budget snapshots, then derive the larger transparent frame under output and work budget/limit failures and assert no writer progress or budget mutation. In `encode_wbtest.mbt`, assert explicit transparent `plte_start`, `trns_start`, `trns_length`, `idat_start`, `iend_start`, and total-length values; step the one machine across tRNS length/type/payload/CRC boundaries to demonstrate preview does not mutate `trns_crc` and each accepted payload byte advances it once. Preserve existing zero-PLTE legacy fact assertions and add all-opaque indexed fact/byte regressions, limiting coverage to D-05 eager Indexed8 behavior.</action>
  <verify>
    <automated>Set-Location modules/mb-image/png; moon test --target all</automated>
  </verify>
  <done>Independent tests validate canonical tRNS order, payload, and CRC; public tests prove exact RGB8/RGBA8 semantics; malformed alpha and rejected transparent preflight remain atomic; and every opaque Phase 76 indexed byte stays frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller alpha bytes → `PngIndexedImage::new` | Untrusted palette-alpha cardinality joins the owned index and RGB palette source. |
| owned indexed source → preflight/frame facts | Validated alpha determines optional chunk length, output accounting, and budget admission. |
| byte machine → writer | Chunk bytes and CRC state cross a potentially failing acknowledgement boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-01 | Tampering, Denial of Service | `PngIndexedImage::new` | high | mitigate | Verify alpha cardinality against validated palette entries before checked allocation, defensive copy, source budget charge, preflight, or writer access. |
| T-77-02 | Denial of Service | `_png_encode_indexed_preflight` | high | mitigate | Derive optional frame size with checked arithmetic before output/work limits and the single work-budget admission. |
| T-77-03 | Tampering | `PngFrameFacts` and `PngEncodeMachine` | medium | mitigate | Use one scalar layout for PLTE/tRNS/IDAT/IEND, emit tRNS only when canonical length is nonzero, and verify chunk order and payload through a local wire oracle. |
| T-77-04 | Tampering | `trns_crc` acknowledgement state | medium | mitigate | Seed CRC from the tRNS type and advance it only after successful payload acknowledgement; inspect timing privately and validate final chunk CRC independently. |
| T-77-05 | Tampering | generic indexed decode | medium | mitigate | Decode emitted bytes publicly and assert explicit, intermediate, and implicit-tail alpha values with their RGB palette entries. |
| T-77-06 | Tampering | opaque Indexed8 compatibility | medium | mitigate | Preserve zero-tRNS frame arithmetic and compare the all-opaque eager result to the frozen Phase 76 vector. |
</threat_model>

<verification>
From `modules/mb-image/png`, run `moon test --target all`. The suite must cover the public transparent encode/decode tracer, independent per-chunk framing and CRC proof, all-opaque byte-for-byte compatibility, constructor/preflight atomicity, and white-box tRNS acknowledgement timing on wasm, wasm-gc, js, and native.
</verification>

<success_criteria>
- INDEX-03 accepts one owned alpha byte for each palette entry and rejects every cardinality mismatch before resource or writer exposure.
- Canonical Type-3 output omits tRNS for all-opaque palettes, otherwise writes it after PLTE with payload ending at the last non-255 alpha and a separately valid CRC.
- Opaque indexed output is byte-identical to Phase 76; transparent output independently validates on the wire and publicly decodes as exact RGBA8.
- Limits and budgets reject the complete transparent frame atomically before output.
</success_criteria>

## Source Audit

| Source | ID | Required item | Plan | Status |
|--------|----|---------------|------|--------|
| GOAL | Phase 77 | Canonical optional tRNS plus exact RGB/RGBA decode evidence | 77-01 Tasks 1-2 | COVERED |
| REQ | INDEX-03 | Palette alpha emits canonical optional tRNS and decodes publicly as RGB8 or RGBA8 | 77-01 Tasks 1-2 | COVERED |
| RESEARCH | — | Retain one owned source and shared preflight/frame/machine seam | 77-01 Task 1 | COVERED |
| RESEARCH | — | Optional tRNS is last non-255 alpha plus one, after PLTE and before IDAT | 77-01 Tasks 1-2 | COVERED |
| RESEARCH | — | Independent per-chunk CRC/wire oracle and public RGBA8 proof | 77-01 Task 2 | COVERED |
| RESEARCH | — | Atomic alpha validation and frame/budget admission | 77-01 Tasks 1-2 | COVERED |
| CONTEXT | D-01 | Owning per-palette alpha and frozen opaque Phase 76 bytes | 77-01 Tasks 1-2 | COVERED |
| CONTEXT | D-02 | Canonical optional tRNS payload | 77-01 Tasks 1-2 | COVERED |
| CONTEXT | D-03 | Alpha-count validation and preflight atomicity | 77-01 Tasks 1-2 | COVERED |
| CONTEXT | D-04 | Shared framing, acknowledged CRC, chunk order, and public RGBA8 proof | 77-01 Tasks 1-2 | COVERED |
| CONTEXT | D-05 | Eager Indexed8-only scope | 77-01 Tasks 1-2 | COVERED |

<output>
Create `.planning/phases/77-indexed-png-transparency/77-01-SUMMARY.md` when done.
</output>
