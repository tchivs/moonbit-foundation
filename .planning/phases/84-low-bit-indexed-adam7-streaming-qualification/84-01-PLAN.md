---
phase: 84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [INDEXLOWADAM7-05, INDEXLOWADAM7-06]
must_haves:
  truths:
    - "For PngIndexedBitDepth One, Two, and Four, the admitted Adam7 chunk selector reproduces a fresh explicit eager result under zero-capacity, one-byte, and ragged caller-owned leases, while totals include only accepted bytes and sentinel tails survive unchanged."
    - "A released first lease replays the same zero-write Failed outcome, and a completed selector replays zero-write Finished without touching a later destination."
    - "Collected low-bit Adam7 stream bytes are independently parsed for legal Type-3 Adam7 PNG framing, CRCs, palette chunks, Stored scanlines, local Adam7 pass packing facts, tail-zero bits, and public RGB8/RGBA8 palette decode."
    - "Existing Type-3 low-bit non-interlaced and Type-3/8 Adam7 vectors remain frozen, and the PNG package passes on wasm, wasm-gc, js, and native."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Test-local hostile-lease, independent Adam7 stream parser, terminal replay, and compatibility qualification for selected Indexed1/2/4 routes."
      contains: "new_indexed_with_interlace_strategy"
  key_links:
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngChunkEncoder::new_indexed_with_interlace_strategy(..., PngInterlaceStrategy::Adam7, ...) followed by pull on caller-owned leases"
      pattern: "new_indexed_with_interlace_strategy"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/encode_test.mbt
      via: "test-local png_indexed_crc32, png_indexed_u32, png_indexed_slice, and png_encode_public_stored_scanlines parse collected bytes without encoder packing or preflight helpers"
      pattern: "png_encode_public_stored_scanlines"
---

<objective>
Qualify the Phase 83 selected-depth Type-3 Adam7 route through hostile caller-owned chunk leases and independent collected-byte evidence, without altering production architecture.

Purpose: Prove the one admitted acknowledged machine preserves caller ownership, terminal semantics, PNG wire correctness, compatibility vectors, and four-target portability for Indexed1, Indexed2, and Indexed4 Adam7 output.
Output: Focused test-only qualification in `modules/mb-image/png/stream_encode_test.mbt` for INDEXLOWADAM7-05 and INDEXLOWADAM7-06.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/84-low-bit-indexed-adam7-streaming-qualification/84-CONTEXT.md
@.planning/phases/84-low-bit-indexed-adam7-streaming-qualification/84-RESEARCH.md
@.planning/phases/83-low-bit-indexed-adam7-machine-and-eager-contract/83-01-SUMMARY.md
@.planning/phases/83-low-bit-indexed-adam7-machine-and-eager-contract/83-VERIFICATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TDD tracer for one caller-owned Indexed1 Adam7 stream</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: read the Indexed8 Adam7 qualification helpers and the current non-interlaced low-bit helpers before adding adjacent selected-depth Adam7-specific helpers.
    - `modules/mb-image/png/encode_test.mbt`: read the Phase 83 5x5 Type-3/1 literal source, palette, tRNS, raw Adam7 bytes, and test-local CRC/u32/slice/Stored-IDAT helpers.
    - `.planning/phases/83-low-bit-indexed-adam7-machine-and-eager-contract/83-VERIFICATION.md`: retain the corrected Type-3/2 `B1` packed-pass fact and the Phase 83/84 ownership boundary.
  </read_first>
  <behavior>
    - RED: A new Indexed1 Adam7 stream qualification test describes a 5x5 non-symmetric transparent source drained through a `[0UL, 1UL]` schedule; it expects a zero-capacity sentinel to survive, every accepted byte to advance the total exactly once, and the completed bytes to satisfy a test-local Type-3 Adam7 frame/raster/decode oracle.
    - GREEN: The tracer constructs only `PngChunkEncoder::new_indexed_with_interlace_strategy(source, PngIndexedBitDepth::One, PngInterlaceStrategy::Adam7, ...)`, drains caller-owned leases, and passes the registered test without changing an encoder, model, filter, compression mode, FFI boundary, wrapper, or source tree.
    - Terminal: A seven-byte sentinel lease after completion reports `written == 0`, retains the final total, reports `Finished`, and remains byte-for-byte untouched.
  </behavior>
  <action>
    Start with the RED test registration and run the native PNG package command; confirm it fails because the selected-depth Adam7 stream qualifier is absent, rather than because of an unrelated package failure. Then implement the test-only tracer in this file. Add a dedicated 5x5 Indexed1 fixture matching Phase 83's literal source/palette/tRNS data, a fresh explicit Adam7 eager parity producer, and a hostile drain helper that owns one `Z` byte while lending a zero-length view for capacity zero. Assert `written <= capacity`, `total_written == previously_collected + written`, collected bytes consist only of accepted positions, every remaining leased position is still `Z`, and the completed replay lease is untouched. Per D-01 and D-02, construct the existing selected-depth chunk selector with explicit `PngInterlaceStrategy::Adam7`; do not call `new_indexed` or a non-interlaced eager helper for the route under test. Per D-03, before terminal replay, run a test-local parser over collected bytes: use the existing test-only CRC, chunk-slice, and Stored-IDAT extraction utilities, verify PNG signature, Type-3/1 Adam7 IHDR, ordered PLTE/tRNS/IDAT/IEND chunks and CRCs, compare the Stored raw scanlines to the fixed Phase 83 literal, and decode the collected transparent stream through the public decoder as RGBA8. Keep eager byte equality as an additional lifecycle comparison, never as the parser input or sole wire oracle. Commit the RED and GREEN stages using the Phase-84 TDD commit names.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <acceptance_criteria>
    The registered Indexed1 tracer runs through the admitted Adam7 selector under a zero/one lease schedule; its independently collected bytes prove signature, Type-3 Adam7 IHDR, PLTE, tRNS, IDAT, IEND, CRC, Stored raster, and public RGBA8 palette decode. Accepted-only progress, unaccepted sentinel preservation, and zero-write untouched `Finished` replay are asserted in the same end-to-end path.
  </acceptance_criteria>
  <done>One production-quality caller-owned Indexed1 Adam7 path is test-qualified end-to-end, with no production file modification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the qualification to every low-bit depth, sticky failure, and frozen targets</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: inspect the completed tracer plus the Indexed8 released-lease helper so depth expansion retains the existing caller-lease conventions.
    - `modules/mb-image/png/encode_test.mbt`: reuse the established Type-3/1, /2, and /4 5x5 literal sources, palettes, alpha data, full raw rasters, and RGB/RGBA decoder assertions as independent test data.
    - `modules/mb-image/png/moon.pkg`: confirm the declared `+js+wasm+wasm-gc+native` package targets before the final frozen gate.
  </read_first>
  <behavior>
    - For One, Two, and Four, schedules `[0UL, 1UL]`, `[1UL]`, and `[0UL, 1UL, 3UL, 2UL, 5UL]` reproduce fresh explicit Adam7 eager bytes, preserve zero-capacity and unaccepted tails, and account only acknowledged bytes.
    - For every depth, releasing the first one-byte lease yields `Failed` with zero writes and zero total; a later lease yields the equivalent cached failure with no sentinel mutation.
    - For every depth, independently parsed collected stream bytes prove literal local Adam7 coordinates, row-byte boundaries, MSB-first packed values, zeroed unused low tail bits, Type-3 Adam7 chunk framing/CRCs, PLTE/tRNS, Stored IDAT data, transparent RGBA8 decode, and opaque RGB8 decode.
    - Existing Type-3 low-bit non-interlaced and Type-3/8 Adam7 byte-vector tests continue to run unchanged in the ordinary all-target frozen package gate.
  </behavior>
  <action>
    Extend only the new test-local selected-depth Adam7 harness from Task 1. Parameterize its fixed 5x5 non-symmetric source, palette, canonical alpha, expected frame size, and exact raw raster for `PngIndexedBitDepth::One`, `Two`, and `Four`; retain Phase 83's corrected depth-two packed byte `B1` in its literal vector. Exercise all three hostile schedules at every depth and keep the one-byte owner for capacity-zero views so zero-capacity destination preservation stays observable. Add the released-first-lease helper and registered test loop for all depths, asserting both cached failures are zero-write, totals remain zero, errors compare through the existing stream test error-equivalence helper, and neither original nor later sentinel changes. Strengthen the collected-byte parser so it is independent of eager output and every production packer/preflight routine: validate signature; IHDR dimensions, selected bit depth, colour type 3, compression/filter zero, and Adam7 flag; ordered PLTE/tRNS/IDAT/IEND declared lengths and CRCs; Stored-IDAT bounds and literal raw raster. Use a test-local Adam7 pass table and integer row-byte calculation for the 5x5 geometry to segment the literal rows, check local pass coordinates against the canonical source codes, verify filter-None at each row, verify MSB-first packed output, and require unused low bits of every partial final data byte to be zero. Decode collected transparent stream bytes through `PngDecoder` and assert RGBA8 palette/alpha values, then collect an opaque caller-buffered stream and assert public RGB8 palette values. Per D-03, do not feed eager bytes to this qualification parser and do not call production Adam7 geometry, row-byte, packing, or preflight helpers. Per D-04, leave the established non-interlaced low-bit and Indexed8 Adam7 vector tests untouched; the final all-target package invocation is their freeze gate. Keep scope test-only: do not edit encoder/machine/model/filter/compression/FFI/wrapper/release files, add scripts, copy trees, or introduce another stream path.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <acceptance_criteria>
    All One/Two/Four routes satisfy every required hostile schedule, sticky completed and released-lease terminal condition, independently parsed collected-byte wire fact, literal pass-local packing fact, RGBA8/RGB8 public decode fact, and the package gate passes on wasm, wasm-gc, js, and native with frozen vector tests intact.
  </acceptance_criteria>
  <done>INDEXLOWADAM7-05 and INDEXLOWADAM7-06 are fully qualified by a single focused stream test file; no production architecture or deferred scope has changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned lease → `PngChunkEncoder::pull` | A mutable destination and its capacity are supplied by the test; qualification must detect writes beyond accepted bytes and terminal mutation. |
| collected PNG bytes → test-local parser | Stream output is interpreted by a parser that must bound its chunk access and validate framing before certifying wire facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-01 | Tampering | Caller-owned lease lifecycle | medium | mitigate | Sentinel-backed zero, one, ragged, released, and post-finish leases assert accepted-only totals and untouched unaccepted bytes in `stream_encode_test.mbt`. |
| T-84-02 | Tampering | Collected PNG qualification parser | medium | mitigate | Test-local bounded chunk reads validate signature, chunk ordering/lengths, CRCs, Stored payload bounds, local pass-row facts, and public decode before accepting stream bytes. |
| T-84-SC | Tampering | Package supply chain | low | accept | No dependency or package-manager operation is part of this test-only plan; the existing frozen MoonBit package gate is the sole tool invocation. |
</threat_model>

<verification>
Run the native PNG package gate after the tracer RED/GREEN cycle, then run `moon -C modules/mb-image test png --target all --frozen` after expansion. The all-target command must exercise the added hostile-lease qualification and the already registered Type-3/1, /2, /4 non-interlaced plus Type-3/8 Adam7 frozen vectors on wasm, wasm-gc, js, and native.
</verification>

<success_criteria>
- INDEXLOWADAM7-05: all selected depths use the existing explicit Adam7 chunk selector under zero, one-byte, and ragged leases; accepted progress, untouched tails, released failure replay, and finished replay are observable from caller-owned buffers.
- INDEXLOWADAM7-06: every selected-depth collected stream is verified by an independent test-local PNG and Adam7 raster oracle plus public RGBA8/RGB8 decode, while the frozen all-target package gate passes.
- The only changed implementation artifact is `modules/mb-image/png/stream_encode_test.mbt`.
</success_criteria>

## Artifacts this phase produces

- `modules/mb-image/png/stream_encode_test.mbt` — registered selected-depth Adam7 hostile-lease, terminal replay, independent wire/raster/decode, and compatibility-portability qualification tests.

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Safe caller-owned, independently wire-qualified, compatibility-frozen low-bit Adam7 streaming across four targets | 84-01 | COVERED | Tracer proves one complete path; Task 2 covers all selected depths and the frozen package gate. |
| REQ | INDEXLOWADAM7-05 | Hostile leases, accepted-only totals, tail preservation, sticky released failure, and zero-write completion | 84-01 | COVERED | D-02 is implemented by both tracer and depth expansion. |
| REQ | INDEXLOWADAM7-06 | Independent Type-3 Adam7 wire/raster/decode proof and frozen four-target portability | 84-01 | COVERED | D-03/D-04 map to Task 2's parser and all-target verification. |
| RESEARCH | — | Existing selected-depth chunk facade and acknowledged lifecycle are the sole route | 84-01 | COVERED | Task actions require the explicit existing Adam7 selector and prohibit replacement transport. |
| RESEARCH | — | Use test-local CRC/chunk/Stored parsing and Phase 83 literal vectors | 84-01 | COVERED | Parser consumes collected bytes and fixed local vectors, never production packer/preflight functions. |
| RESEARCH | — | Four-target frozen PNG package gate | 84-01 | COVERED | Task 2 runs the ordinary `--target all --frozen` command. |
| CONTEXT | D-01 | Existing selected low-bit chunk API only | 84-01 | COVERED | Both task actions cite D-01 and use the explicit selected-depth selector. |
| CONTEXT | D-02 | All lease schedules and sticky terminals | 84-01 | COVERED | Tracer starts the lifecycle path; Task 2 completes all depths and failure replay. |
| CONTEXT | D-03 | Independent collected-byte frame/raster/decode evidence | 84-01 | COVERED | Both task actions cite D-03; Task 2 expands the oracle to all depths and RGB/RGBA routes. |
| CONTEXT | D-04 | Frozen vectors and ordinary all-target gate | 84-01 | COVERED | Task 2 leaves vector registrations unchanged and proves them through the package gate. |

<output>
Create `.planning/phases/84-low-bit-indexed-adam7-streaming-qualification/84-01-SUMMARY.md` when done.
</output>
