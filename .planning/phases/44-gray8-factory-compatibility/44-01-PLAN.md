---
phase: 44-gray8-factory-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYPNG-01
must_haves:
  truths:
    - "Users can encode a valid packed U8 Gray image through explicit eager and caller-buffered default-Stored factories and receive a complete non-interlaced Gray8 PNG."
    - "Gray8 factories accept only the locked Gray8 source contract and reject wrong profile/source pairs before eager output or chunk construction."
    - "Existing RGB8 and straight-RGBA8 factories retain their existing admission behavior and exact bytes."
    - "Gray8 exposes no public filter, Fixed/Dynamic, or Adam7 factory variant."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Explicit default-Stored `new_gray8` profile selection for eager and chunk surfaces."
    - path: modules/mb-image/png/encode.mbt
      provides: "Profile-aware Gray8 source admission and shared one-channel Stored preflight facts."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "One-channel Stored IHDR, scanline traversal, byte emission, and acknowledgement-safe caller-buffered replay."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Real eager Stored Gray8 output, rejection, and frozen legacy-vector tests."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Real chunk Stored Gray8/eager identity, rejection, and frozen legacy-vector tests."
  key_links:
    - from: "PngEncoder::new_gray8 and PngChunkEncoder::new_gray8"
      to: "PngEncodeMachine::new_with_all_strategies"
      via: "Private PngEncodeProfile::Gray8 selects Stored/None/None through the existing atomic construction seam."
      pattern: "PngEncodeProfile::Gray8"
    - from: "_png_encode_source"
      to: "PngEncodeMachine byte emission"
      via: "Profile-specific channel count, row facts, and IHDR colour type stay aligned for the same Stored traversal."
      pattern: "channels.*row_bytes"
---

<objective>
Deliver working explicit eager and caller-buffered default-Stored Gray8 PNG encoding while preserving legacy RGB8/RGBA8 behavior.

Purpose: Satisfy `GRAYPNG-01` with the smallest real one-channel route: PNG colour type 0, bit depth 8, and non-interlaced output.
Output: Two explicit `new_gray8` factories, profile-aware admission, one-channel Stored emission/replay, and focused public tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-gray8-factory-compatibility/44-CONTEXT.md
@.planning/phases/44-gray8-factory-compatibility/44-RESEARCH.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the explicit default-Stored Gray8 encoder route</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <read_first>
    - modules/mb-image/png/png.mbt — `PngEncoder` state and default public construction surfaces.
    - modules/mb-image/png/encode.mbt — `_png_encode_source`, shared preflight, and filter-none traversal inputs.
    - modules/mb-image/png/stream_encode.mbt — chunk construction, `PngEncodeMachine`, Stored zlib emission, IHDR assembly, and acknowledgement/replay state.
    - .planning/phases/44-gray8-factory-compatibility/44-CONTEXT.md — locked decisions D-01 through D-04.
    - .planning/phases/44-gray8-factory-compatibility/44-RESEARCH.md — narrowed Stored-only implementation boundary.
  </read_first>
  <action>
    Add private `PngEncodeProfile::{LegacyRgbOrRgba, Gray8}` and carry it through `PngEncoder`, `PngChunkEncoder::new_gray8`, the private machine constructor, `_png_encode_source`, and the resulting preflight/machine facts. Per D-01, add exactly one explicit `PngEncoder::new_gray8()` and one matching `PngChunkEncoder::new_gray8(source, limits, budget, diagnostics)`; both select `Gray8`, `PngCompressionStrategy::Stored`, `PngFilterStrategy::None`, and `PngInterlaceStrategy::None`. Keep every existing constructor, field default, signature, and delegation on `LegacyRgbOrRgba` so RGB8/straight-RGBA8 admission and output remain frozen per D-03. Do not expose Gray8 strategy, filter, all-strategy, or interlace constructors.

Make `_png_encode_source` profile-aware while retaining the single geometry, resource-limit, budget, and Stored-preflight path. For `Gray8`, accept only `ChannelOrder::Gray`, `U8`, packed layout, row bytes and stride equal to width, top-left orientation, no opaque metadata, no alpha, and the existing canonical built-in encoded-sRGB metadata contract; retain legacy RGB/RGBA branches and their existing capability contexts exactly. Per D-02 and D-04, make a valid Gray8 profile produce bit depth 8, colour type 0, and interlace method 0 in IHDR, then extend only the existing filter-None/Stored scanline traversal, zlib payload, checksum, byte-at/emitter, and acknowledged caller-buffered replay to consume one source byte per pixel. The one-channel path must use the same bounded preflight and atomic construction rules as Stored RGB/RGBA. Reject RGB/RGBA under the Gray8 profile, U16, planar rows, alpha/transparency metadata, and any private Gray8-plus-Adam7 request with stable typed capability errors before writer output, budget charge, or a chunk encoder is returned. Do not generalize Gray8 to adaptive filtering, FixedOrStored, DynamicOrFixedOrStored, Adam7, palette, low-bit, Gray16, or conversion.
  </action>
  <verify>
    <automated>moon test modules/mb-image/png</automated>
  </verify>
  <acceptance_criteria>
    - `PngEncoder::new_gray8()` and `PngChunkEncoder::new_gray8(...)` are the only public Gray8 factories and always select Stored/None/None.
    - A valid Gray U8 input produces a complete PNG with IHDR bit depth 8, colour type 0, and interlace method 0 through both eager and chunk paths.
    - The Gray8 Stored path emits and replays one-channel scanlines without image-sized staging and advances chunk state only for acknowledged bytes.
    - Unsupported Gray/profile combinations fail before observable output or caller-buffered state, and all legacy RGB/RGBA paths retain their existing behavior.
  </acceptance_criteria>
  <done>Working default-Stored Gray8 PNG encoding shares the existing bounded/atomic route, with RGB/RGBA compatibility and all later Gray8 strategy work fenced out.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real Gray8 Stored output and freeze compatibility boundaries</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/encode_test.mbt — eager image helpers, byte fixtures, and atomic-admission tests.
    - modules/mb-image/png/stream_encode_test.mbt — caller-buffered drain helpers, sentinel leases, and frozen compatibility cases.
    - .planning/phases/44-gray8-factory-compatibility/44-CONTEXT.md — verification decision D-05.
  </read_first>
  <behavior>
    - A packed canonical U8 Gray image encoded with `PngEncoder::new_gray8()` has a complete PNG signature/IHDR, reports depth 8, colour type 0, and non-interlaced output, and contains its Stored scanline sample bytes.
    - `PngChunkEncoder::new_gray8(...)` drains a valid Gray source through an ordinary fixed caller capacity and produces bytes identical to the eager Gray8 result with exact accepted progress.
    - Gray8 factories reject RGB/RGBA profile mismatches, Gray16, planar rows, alpha/transparency metadata, and a private Gray8 Adam7 combination before output or a chunk encoder; budgets and sentinel leases remain unchanged on rejection.
    - Existing RGB8 and straight-RGBA8 eager/chunk factory vectors remain byte-for-byte equal to their established stored and explicit-None fixtures.
  </behavior>
  <action>
    Add packed Gray-U8 test helpers using `ImageFormat::new` with `ChannelOrder::Gray`, canonical metadata, and exact one-channel plane geometry. Per D-05, add eager tests that encode real Gray8 Stored output and assert PNG framing plus IHDR depth/type/interlace bytes, a deterministic small-image output fixture or equivalent complete Stored payload assertions, and no unintended RGB/RGBA byte change. Add caller-buffered tests that construct `PngChunkEncoder::new_gray8`, drain it with one ordinary nonzero capacity, assert `written`/`total_written` count accepted bytes only, and compare the completed byte stream with eager Gray8 output.

Add focused D-04 rejection cases for wrong profile/source pair, U16, planar Gray, alpha/transparency metadata, and private Gray8 Adam7 construction. Assert typed capability context plus zero writer bytes; for chunk construction assert `Err`, unchanged budget, and untouched sentinel lease. Retain or extend exact legacy RGB8/straight-RGBA8 vectors across default and explicit-None eager/chunk factories. Do not add Gray8 adaptive, Fixed/Dynamic, multi-schedule hostile-capacity, decode-fidelity, or four-target cases.
  </action>
  <verify>
    <automated>moon test modules/mb-image/png</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove real eager and caller-buffered default-Stored Gray8 PNG output, not merely factory construction.
    - Eager and ordinary-capacity chunk output are byte-identical and show a valid Gray8 IHDR.
    - Rejections are atomic and deterministic, while RGB/RGBA compatibility bytes remain frozen.
  </acceptance_criteria>
  <done>The public Stored Gray8 route, source-rejection boundary, and RGB/RGBA compatibility contract are regression-locked without adding later-phase strategy or portability coverage.</done>
</task>

</tasks>

<artifacts_produced>
  - Explicit eager/chunk default-Stored Gray8 factories and profile-aware bounded construction.
  - Minimum one-channel Stored PNG emission and replay support.
  - Real Gray8 output, atomic rejection, and frozen RGB/RGBA compatibility tests.
</artifacts_produced>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Explicit working Gray8 eager/chunk output with frozen RGB/RGBA behavior | 44-01 | COVERED | Tasks 1-2 deliver and prove the Stored-only route.
REQ | GRAYPNG-01 | Explicit non-interlaced Gray8 factories; legacy bytes unchanged | 44-01 | COVERED | Both public factories, real output, and compatibility locks.
RESEARCH | — | Private profile, one default factory per surface, one-channel Stored emission | 44-01 | COVERED | Task 1 implements the minimal route; Task 2 proves it.
CONTEXT | D-01 | One symmetric eager/chunk `new_gray8` factory | 44-01 | COVERED | Task 1.
CONTEXT | D-02 | Working Stored type-0/8-bit/None output only | 44-01 | COVERED | Task 1 and Gray8 output tests.
CONTEXT | D-03 | Strict Gray source admission and frozen RGB/RGBA behavior | 44-01 | COVERED | Tasks 1-2.
CONTEXT | D-04 | Deterministic pre-output/profile rejection | 44-01 | COVERED | Tasks 1-2.
CONTEXT | D-05 | Real Stored tests; defer strategy schedules and target evidence | 44-01 | COVERED | Task 2 scope fence.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView to PNG factory | Caller-controlled format, metadata, layout, and profile choice enter source admission. |
| Caller lease to chunk encoder | Caller-owned mutable memory receives bytes only after successful construction and acknowledgement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Tampering | Profile/source admission | medium | mitigate | Require the locked Gray U8 packed/tight/metadata contract and reject all profile mismatches before construction. |
| T-44-02 | Denial of service | Stored preflight and replay | medium | mitigate | Reuse checked dimensions, declared limits, resource budget charging, and scalar replay facts; do not introduce image-sized staging. |
| T-44-03 | Tampering | PNG framing | medium | mitigate | Derive IHDR colour type and one-channel traversal from the same private profile and assert complete framing in tests. |
| T-44-04 | Information disclosure | In-process codec | low | accept | No external service, credential, or new I/O boundary is introduced. |
</threat_model>

<verification>
Run `moon test modules/mb-image/png`. Confirm real Gray8 eager/chunk Stored output, atomic rejection assertions, and frozen RGB/RGBA vectors pass.
</verification>

<success_criteria>
1. `GRAYPNG-01` has one explicit working eager and caller-buffered default-Stored Gray8 factory, producing type-0, 8-bit, non-interlaced PNGs.
2. Gray8 source admission and rejection are deterministic and atomic; no implicit conversion or unsupported Gray form enters the emitter.
3. Existing RGB8 and straight-RGBA8 bytes and behavior remain regression-locked.
4. Gray8 filtering, Fixed/Dynamic planning, Adam7, broad hostile schedules, and four-target public evidence remain absent from this plan.
</success_criteria>

<output>
Create `.planning/phases/44-gray8-factory-compatibility/44-01-SUMMARY.md` when done.
</output>
