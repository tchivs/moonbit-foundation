---
phase: 05-reference-codec-and-release-qualification
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [modules/mb-image/ppm/moon.pkg, modules/mb-image/ppm/decode.mbt, modules/mb-image/ppm/decode_test.mbt, modules/mb-image/ppm/decode_wbtest.mbt]
autonomous: true
requirements: [QUAL-01, QUAL-03]
must_haves:
  truths:
    - "Decode validates all dimensions, payload, limits, and work before one authoritative image allocation."
    - "Payload fills one OwnedImage directly through callback-scoped mutable authority, without whole-input buffering."
    - "Exactly one image is accepted: truncation and the one-byte trailing-data probe have stable structured failures."
  artifacts:
    - path: modules/mb-image/ppm/decode.mbt
      provides: ImageDecoder payload/allocation/EOF implementation
    - path: modules/mb-image/ppm/decode_test.mbt
      provides: Black-box decode and adversarial progress evidence
  key_links:
    - from: modules/mb-image/ppm/decode.mbt
      to: modules/mb-image/storage/owned_image.mbt
      via: OwnedImage::new_operation then with_mut_view
    - from: modules/mb-image/ppm/decode.mbt
      to: modules/mb-image/codec/contracts.mbt
      via: the complete impl ImageDecoder for PpmDecoder, including probe and decode
    - from: modules/mb-image/ppm/decode.mbt
      to: modules/mb-core/io/exact.mbt
      via: Reader progress and exact payload fill
---

<objective>Implement strict allocation-safe P6 decode and exact single-image completion per D-02, D-04, D-05, and D-08 through D-10.</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@modules/mb-core/io/traits.mbt
@modules/mb-core/io/exact.mbt
@modules/mb-image/model/descriptor.mbt
@modules/mb-image/storage/owned_image.mbt
@modules/mb-image/codec/contracts.mbt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Preflight descriptor and one atomic output allocation</name>
  <files>modules/mb-image/ppm/moon.pkg, modules/mb-image/ppm/decode.mbt, modules/mb-image/ppm/decode_wbtest.mbt</files>
  <behavior>
    - Checked width*height*3 and parser+payload work reject overflow or any CodecLimits/Budget ceiling before allocation.
    - Successful metadata is tight packed ImageFormat::rgb8, encoded sRGB, no alpha, built-in sRGB profile, TopLeft, no opaque metadata.
    - Exactly one OwnedImage::new_operation charge occurs; malformed or underfunded input preserves documented budget state.
  </behavior>
  <action>Implement the complete `impl @codec.ImageDecoder for PpmDecoder` in `decode.mbt`: delegate `probe` to Plan 01's pure prefix helper and implement the full `decode` method here, with no decoder trait method left in `ppm.mbt`. Add the exact decoder imports to `moon.pkg`. After Plan 01 header completion, compute pixels/payload/work only with checked full-width helpers, validate max_input/output/width/height/pixels/work, construct the exact descriptor, and call `OwnedImage::new_operation` once per D-02/D-04/D-09. Test independent allocation, allocation-size, bytes, pixels, dimensions, and work rejection before mutable access.</action>
  <verify><automated>moon test --frozen --target native --package moonbit-foundation/mb-image/ppm</automated></verify>
  <done>The complete ImageDecoder implementation is owned by decode.mbt; all invalid sizes and budgets fail before allocation and a valid header produces one exact owned output charge.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Fill exact payload and require EOF</name>
  <files>modules/mb-image/ppm/decode.mbt, modules/mb-image/ppm/decode_test.mbt, modules/mb-image/ppm/decode_wbtest.mbt</files>
  <behavior>
    - Payload bytes copy directly into one mutable view under arbitrary short progress.
    - One-byte-short payload -> UnexpectedEndOfStream with requested/completed; Progress(0) -> NoProgress; backend failure preserves exact completed count.
    - After payload, one read accepts EndOfStream only; any byte, including another P6 magic, -> Data/InvalidEncoding with context ppm-trailing-data.
  </behavior>
  <action>Fill the exact tight raster through callback-scoped `MutImageView::set_byte`/bounded Reader progress, always releasing authority on every result. Probe exactly one byte after payload per D-05; do not seek, buffer the input, accept concatenation, or classify raster `#`/whitespace specially. Return `DecodeResult` with exact bytes_read and metadata disposition.</action>
  <verify><automated>moon test --frozen --target all --package moonbit-foundation/mb-image/ppm</automated></verify>
  <done>Canonical decode, every payload split, zero/failed progress, truncation, extra byte, and concatenated image have exact stable outcomes on four targets.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04 | Denial of Service | decode allocation | high | mitigate | All checked limits and one atomic charge precede allocation/mutation. |
| T-05-05 | Tampering | payload progress | high | mitigate | Exact counts, no-progress rejection, and EOF probe enforce one raster only. |
| T-05-06 | Information Disclosure | mutable lease | medium | mitigate | Callback scope invalidates authority on every exit and payload length is exact. |
</threat_model>

<verification>Focused black-box and white-box PPM decode suites pass on native, then all declared targets.</verification>
<success_criteria>Decode is forward-only, one-allocation, exact-payload, strict-EOF, budgeted, and structured-error complete.</success_criteria>
<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-02-SUMMARY.md` when done.</output>
