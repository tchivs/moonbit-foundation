---
phase: 05-reference-codec-and-release-qualification
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - examples/ppm-portable/moon.mod.json
  - examples/ppm-portable/main/moon.pkg
  - examples/ppm-portable/main/main.mbt
  - examples/ppm-native-cli/moon.mod.json
  - examples/ppm-native-cli/main/moon.pkg
  - examples/ppm-native-cli/main/adapter.mbt
  - examples/ppm-native-cli/main/main.mbt
  - scripts/quality/Test-PublicExamples.ps1
  - release/qualification/example-consumers-schema.json
autonomous: true
requirements: [QUAL-02, QUAL-03]
must_haves:
  truths:
    - "The portable consumer executes memory stream to decode to meaningful Phase 4 transform to encode on all four targets."
    - "The Native CLI-shaped adapter receives Reader, Writer, options, budget, diagnostics, and transform explicitly and opens no ambient path."
    - "Both examples execute normally in the repository workspace using public imports and pass a private-import source audit."
    - "A clean copied-source workspace reports source_isolation=pass, while a separate no-workspace named-dependency probe reports registry_resolution=blocked_unpublished_namespace."
  artifacts:
    - path: examples/ppm-portable/main/main.mbt
      provides: Four-target in-memory public example
    - path: examples/ppm-native-cli/main/adapter.mbt
      provides: Injected Native CLI-shaped public adapter
    - path: scripts/quality/Test-PublicExamples.ps1
      provides: Workspace execution, source-isolation, and separate registry-resolution probe qualification
  key_links:
    - from: examples/ppm-portable/main/main.mbt
      to: modules/mb-image/ops/copy_flip.mbt
      via: decode then flip_horizontal then encode
    - from: examples/ppm-native-cli/main/adapter.mbt
      to: modules/mb-image/codec/contracts.mbt
      via: explicit Reader/Writer/options/results only
---
<objective>Deliver and independently qualify the two executable public examples required by D-14 and D-15.</objective>
<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@modules/mb-core/io/memory.mbt
@modules/mb-image/codec/contracts.mbt
@modules/mb-image/ops/copy_flip.mbt
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the four-target portable public consumer</name>
  <files>examples/ppm-portable/moon.mod.json, examples/ppm-portable/main/moon.pkg, examples/ppm-portable/main/main.mbt</files>
  <behavior>
    - Canonical bytes through MemoryReader decode, flip_horizontal, and MemoryWriter encode to the expected canonical digest.
    - Example uses only public APIs and explicit limits/budget/diagnostics.
    - Identical semantic result passes native, js, wasm, and wasm-gc.
  </behavior>
  <action>Create the in-memory executable with normal named dependencies and public imports per D-14/D-15. Execute it in the normal repository workspace on all four targets, and source-audit imports against the exact public package allowlist. Verify descriptor/disposition/count/digest rather than merely exit success. Do not claim that an unpublished downstream module resolves in a no-workspace root.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-PublicExamples.ps1 -Example portable -Mode workspace -Target all</automated></verify>
  <done>The public stream-transform-stream flow executes identically on all four required targets.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build the injected Native CLI-shaped adapter</name>
  <files>examples/ppm-native-cli/moon.mod.json, examples/ppm-native-cli/main/moon.pkg, examples/ppm-native-cli/main/adapter.mbt, examples/ppm-native-cli/main/main.mbt, scripts/quality/Test-PublicExamples.ps1, release/qualification/example-consumers-schema.json</files>
  <behavior>
    - Adapter takes Reader, Writer, DecodeOptions, EncodeOptions, CodecLimits, Budget, Diagnostics, and transform selection.
    - Short-progress doubles prove exact completed counts and canonical output.
    - Source/import audit rejects file, argv, environment, global registry, seeker, and private-package use.
    - Clean copied-source workspace execution emits source_isolation=pass; no-workspace named dependency resolution emits blocked_unpublished_namespace, not pass.
  </behavior>
  <action>Implement the CLI-shaped library function and a tiny native main that injects memory doubles only. Execute both examples normally in the repository workspace with public-import source audit. Then copy the complete required module sources plus examples into a clean temporary workspace and require `source_isolation: pass`. Separately remove `moon.work` and probe the unchanged named downstream dependencies; require `registry_resolution: blocked_unpublished_namespace` and reject any fabricated pass or unrelated failure. Do not attempt an exact mb-image artifact consumer; Plan 07 reserves no-workspace artifact-consumer pass exclusively for mb-core.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-PublicExamples.ps1 -Example all -Mode qualify -Report artifacts/release-qualification/examples.json</automated></verify>
  <done>Workspace examples pass, copied-source workspace isolation passes, and the distinct no-workspace named-dependency probe records the honest unpublished-namespace blocker.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-10 | Elevation of Privilege | native adapter | high | mitigate | All I/O/options/budgets are injected; source audit bans ambient host access. |
| T-05-11 | Spoofing | public consumer proof | medium | mitigate | Source audit proves public imports; copied-source workspace and no-workspace registry probes have separate machine-readable outcomes. |
</threat_model>
<verification>Normal workspace examples pass with exact digests; copied-source workspace reports source_isolation=pass; the separate no-workspace probe reports registry_resolution=blocked_unpublished_namespace.</verification>
<success_criteria>Both examples are executable, meaningful, public-only, explicit-capability consumers without an impossible downstream artifact-resolution claim.</success_criteria>
<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-04-SUMMARY.md` when done.</output>
