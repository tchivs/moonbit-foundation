---
phase: 05-reference-codec-and-release-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/ppm/moon.pkg
  - modules/mb-image/ppm/ppm.mbt
  - modules/mb-image/ppm/parser.mbt
  - modules/mb-image/ppm/parser_wbtest.mbt
autonomous: true
requirements: [QUAL-01, QUAL-03]
must_haves:
  truths:
    - "The public PPM package recognizes only the MNF strict P6/sRGB subset through the existing codec contract."
    - "Every pre-raster byte, token, and comment is bounded and chunk-independent before allocation."
    - "Malformed decimal syntax, overflow, and each parser limit have an exact structured error classification."
  artifacts:
    - path: modules/mb-image/ppm/ppm.mbt
      provides: Public PpmDecoder, PpmEncoder, and PpmParserLimits contracts
    - path: modules/mb-image/ppm/parser.mbt
      provides: Forward-only bounded P6 header state machine
    - path: modules/mb-image/ppm/parser_wbtest.mbt
      provides: Parser transition, grammar, limit, and chunk-boundary evidence
  key_links:
    - from: modules/mb-image/ppm/ppm.mbt
      to: modules/mb-image/codec/contracts.mbt
      via: ImageDecoder/ImageEncoder implementations and CodecLimits
    - from: modules/mb-image/ppm/parser.mbt
      to: modules/mb-core/checked/checked.mbt
      via: checked_add and checked_mul decimal accumulation
---

<objective>
Define the public strict PPM P6 codec model and prove a bounded incremental header parser.

Purpose: Establish D-01 through D-04, D-08 through D-10, and D-25 before allocation or payload behavior can exist.
Output: A portable `mb-image/ppm` package with closed parser limits, stable errors, and TDD parser evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@modules/mb-core/io/traits.mbt
@modules/mb-core/checked/checked.mbt
@modules/mb-core/error/core_error.mbt
@modules/mb-image/codec/contracts.mbt

<interfaces>
Use `Reader::read(Self, ReadWindow) -> ReadOutcome`, `ImageDecoder::probe/decode`, `ImageEncoder::encode`, and the existing `CodecLimits` accessors exactly. `PpmParserLimits` adds only `max_header_bytes`, `max_token_bytes`, `max_comment_bytes`, and `max_comments`; width/height/pixels/input/output/work remain owned by `CodecLimits`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the PPM package and public codec contracts</name>
  <files>modules/mb-image/ppm/moon.pkg, modules/mb-image/ppm/ppm.mbt, modules/mb-image/ppm/parser_wbtest.mbt</files>
  <behavior>
    - Probe prefixes: empty or `P` -> NeedMore(2), `P6` -> Match, every other two-byte magic -> NoMatch.
    - Constructor rejects zero parser ceilings with Resource/BudgetExceeded and the exact bounded dimension token.
    - The package imports only declared core safety/I/O packages and image codec/model/storage/metadata packages, and supports +js+wasm+wasm-gc+native.
  </behavior>
  <action>Write black-box-first contract tests, then add `PpmParserLimits`, `PpmDecoder::new`, `PpmEncoder::new`, prefix-only probe, and trait implementations per D-01 and D-04. Keep codec selection explicit; do not add a registry, seeker, host, or filesystem import per D-08 and D-25. Use `CapabilityUnavailable` only for unsupported codec capability and `BudgetExceeded` with one of `header-bytes`, `token-bytes`, `comment-bytes`, or `comments` for declared ceilings.</action>
  <verify><automated>moon test --frozen --target native --package moonbit-foundation/mb-image/ppm</automated></verify>
  <done>The public type surface compiles, prefix probing is reader-independent, and package/target boundaries are closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded byte-state header parser</name>
  <files>modules/mb-image/ppm/parser.mbt, modules/mb-image/ppm/parser_wbtest.mbt</files>
  <behavior>
    - Accept P6, positive ASCII decimal width/height, maxval 255, permitted ASCII whitespace/comments, and exactly one raster separator.
    - Reject signs, locale digits, empty tokens, zero dimensions, invalid comment positions, numeric overflow, maxval other than 255, and every independent ceiling.
    - All deterministic chunk schedules yield the same state, bytes consumed, error code/context, and unchanged authoritative Budget on header failure.
  </behavior>
  <action>Implement the closed one-byte transition machine recommended by research, with checked `value * 10 + digit`, total-header/token/comment counters, comment count, and no string/Int parsing per D-02, D-03, D-04, and D-09. Map malformed grammar to Data/InvalidEncoding, checked overflow to ArithmeticOverflow, unsupported maxval to Capability/CapabilityUnavailable with bounded `ppm-maxval`, and limit failure to Resource/BudgetExceeded. Once the single whitespace delimiter after maxval is consumed, return header completion without examining the first raster byte.</action>
  <verify><automated>moon test --frozen --target all --package moonbit-foundation/mb-image/ppm</automated></verify>
  <done>Parser tests cover every transition, byte split, syntax class, overflow, and limit before any allocation seam is reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reader bytes -> P6 parser | Untrusted binary input controls grammar, counters, and dimensions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Denial of Service | parser.mbt | high | mitigate | Checked scalar accumulation and independent header/token/comment ceilings fail before allocation. |
| T-05-02 | Tampering | parser state transitions | medium | mitigate | Closed states and byte-split equivalence tests prevent chunk-dependent grammar. |
| T-05-03 | Information Disclosure | diagnostics | low | accept | Context is fixed bounded tokens and contains no input payload or host detail. |
</threat_model>

<verification>
Focused package tests pass on all four required targets; no package imports host/filesystem/registry state.
</verification>

<success_criteria>
- PPM public contracts and parser limits are explicit and portable.
- Header completion proves every D-02/D-03 grammar rule and D-04 ceiling before allocation.
- Error category/code/operation/context tuples are stable test expectations.
</success_criteria>

<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-01-SUMMARY.md` when done.</output>
