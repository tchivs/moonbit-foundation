---
phase: 05-reference-codec-and-release-qualification
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - modules/mb-image/ppm/encode.mbt
  - modules/mb-image/ppm/ppm_test.mbt
  - modules/mb-image/ppm/reference_vectors_wbtest.mbt
  - modules/mb-image/ppm/generated_vectors.mbt
  - fixtures/ppm/cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PpmVectors.ps1
autonomous: true
requirements: [QUAL-01, QUAL-03]
must_haves:
  truths:
    - "Encode emits exactly P6 LF width-space-height LF 255 LF plus logical tight RGB rows."
    - "Unsupported format/layout/orientation/alpha/profile interpretation fails before the first write."
    - "Generated conformance, adversarial, chunk, short-progress, and metamorphic evidence is provenance-linked and byte reproducible."
  artifacts:
    - path: modules/mb-image/ppm/encode.mbt
      provides: Canonical streaming ImageEncoder implementation
    - path: fixtures/ppm/cases.json
      provides: Official-spec-derived and project-derived canonical case source
    - path: scripts/fixtures/Generate-PpmVectors.ps1
      provides: Deterministic formatter-clean generator and -Check mode
  key_links:
    - from: modules/mb-image/ppm/encode.mbt
      to: modules/mb-core/io/exact.mbt
      via: write_all header and each logical row
    - from: modules/mb-image/ppm/generated_vectors.mbt
      to: fixtures/ppm/cases.json
      via: exact generated bytes and fixture IDs
---

<objective>Complete canonical P6 encoding and deterministic conformance/metamorphic evidence per D-06, D-07, and D-11 through D-13.</objective>
<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@modules/mb-core/io/exact.mbt
@modules/mb-image/codec/contracts.mbt
@modules/mb-image/storage/views.mbt
@modules/mb-image/ops/copy_flip.mbt
@modules/mb-image/ops/orientation.mbt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement canonical row-safe encoding</name>
  <files>modules/mb-image/ppm/encode.mbt, modules/mb-image/ppm/ppm_test.mbt</files>
  <behavior>
    - Supported tight or padded packed TopLeft encoded-sRGB Rgb8 views emit identical canonical bytes using logical row_bytes only.
    - Alpha, non-RGB/U8/packed, non-TopLeft, non-sRGB, or meaningful profile/opaque metadata requirements fail CapabilityUnavailable before output.
    - Short writes succeed; zero progress and failure retain total exact completed count.
  </behavior>
  <action>Validate the complete image capability first per D-07, build the bounded canonical ASCII header, then call `write_all` for it and for each logical RGB row per D-06/D-11. Never write row_stride/storage padding. Use checked bytes_written accumulation and return exact `EncodeResult` disposition/count.</action>
  <verify><automated>moon test --frozen --target all --package moonbit-foundation/mb-image/ppm</automated></verify>
  <done>Canonical bytes are exact for supported views and every unsupported capability or writer progress class has a stable result.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Generate conformance and metamorphic proof</name>
  <files>fixtures/ppm/cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PpmVectors.ps1, modules/mb-image/ppm/generated_vectors.mbt, modules/mb-image/ppm/reference_vectors_wbtest.mbt, modules/mb-image/ppm/ppm_test.mbt</files>
  <behavior>
    - Generator -Check compares exact UTF-8 no-BOM LF output that is formatter-clean.
    - Evidence covers canonicalization, semantic round trip, every header/payload split, short I/O, limits, trailing data, and fixed chunk schedules.
    - Double horizontal flip and TopLeft orientation application preserve canonical output; appending one byte flips success to the exact trailing-data error.
  </behavior>
  <action>Register a minimal spec-derived corpus separately from project-derived adversarial cases with license/source/digest metadata per D-12. Generate one package-local table and execute the complete D-13 relations, including 1x1 endpoints, 1xN/Nx1, padded encode views, orientation/flip pipeline, allocation/work rejection, maxval variants, and deterministic pseudo-random chunk schedules. Do not add fuzz infrastructure.</action>
  <verify><automated>pwsh -NoProfile -File scripts/fixtures/Generate-PpmVectors.ps1 -Check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; moon test --frozen --target all --package moonbit-foundation/mb-image/ppm</automated></verify>
  <done>Fixture provenance, generated MoonBit bytes, canonical encoding, and all conformance/metamorphic relations pass deterministically.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-07 | Information Disclosure | row encoder | high | mitigate | Encoder writes logical row_bytes only; padded-view tests use nonzero sentinels. |
| T-05-08 | Tampering | fixture generator | medium | mitigate | Provenance digests and exact -Check output link source cases to package-local tables. |
| T-05-09 | Denial of Service | Writer loop | high | mitigate | Existing write_all rejects zero/invalid progress and preserves counts. |
</threat_model>
<verification>Generator check and PPM package tests pass on all declared targets.</verification>
<success_criteria>Encoder bytes are canonical, padding-safe, capability-closed, and supported by deterministic conformance/metamorphic proof.</success_criteria>
<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-03-SUMMARY.md` when done.</output>
