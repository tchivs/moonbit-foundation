---
phase: 05-reference-codec-and-release-qualification
plan: 08
type: execute
wave: 8
depends_on: [05-07]
files_modified:
  - qualification/negative/path-dependency/mb-color.moon.mod.json
  - qualification/negative/higher-layer-dependency/mb-core.moon.pkg
  - qualification/negative/unexpected-package/package-list.txt
  - qualification/negative/false-registry-pass/report.json
  - scripts/quality/Test-ReleaseQualification.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
  - scripts/quality.ps1
  - .github/workflows/quality.yml
  - release/qualification/v0.1-requirements.json
autonomous: true
requirements: [WORK-06, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "Every Phase 5 requirement maps to an executable focused or Required evidence selector."
    - "Release policy fails closed for path substitution, higher-layer dependency, extra package content, and fabricated downstream registry success."
    - "Two complete Required runs from the same clean committed baseline pass and have identical deterministic evidence digests."
    - "Each module qualifies independently; downstream registry resolution remains blocked without failing the honest candidate qualification contract."
  artifacts:
    - path: scripts/quality/Test-ReleaseQualificationNegative.ps1
      provides: Exact fail-closed release-policy mutation suite
    - path: release/qualification/v0.1-requirements.json
      provides: WORK-06 and QUAL-01..06 evidence map and deterministic run digests
    - path: scripts/quality.ps1
      provides: Required-lane integration of examples, docs, benchmarks, packaging, consumers, and read-only checks
  key_links:
    - from: scripts/quality.ps1
      to: scripts/quality/Invoke-ReleaseQualification.ps1
      via: Required lane after generation/check tests
    - from: release/qualification/v0.1-requirements.json
      to: artifacts/release-qualification/required-run-1
      via: selector and deterministic evidence digest mapping repeated for required-run-2
---

<objective>
Close Phase 5 with fail-closed policy evidence, independent module qualification, and two clean deterministic Required runs.

Purpose: Fulfill D-23 and D-24 while proving WORK-06 and QUAL-01 through QUAL-06 without overstating registry publication or codec scope.
Output: Exact negative fixtures, Required-lane wiring, independent requirement map, and two-run clean-baseline evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@policy/foundation.json
@policy/release-qualification.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove exact release-policy negatives fail closed</name>
  <files>qualification/negative/path-dependency/mb-color.moon.mod.json, qualification/negative/higher-layer-dependency/mb-core.moon.pkg, qualification/negative/unexpected-package/package-list.txt, qualification/negative/false-registry-pass/report.json, scripts/quality/Test-ReleaseQualificationNegative.ps1</files>
  <behavior>
    - A local/path dependency in mb-color is rejected as workspace substitution.
    - An mb-core import of mb-color or mb-image is rejected as reverse dependency.
    - One unexpected package/archive entry is rejected against the closed ordered allowlist.
    - A downstream report that claims registry pass while namespace/dependencies are unpublished is rejected.
  </behavior>
  <action>Create the four named immutable negative fixtures and a table-driven test that copies each to a temporary root, requires the exact expected rule ID, and fails if a mutation passes or fails for another reason. Keep production manifests untouched. Also test missing version/status/license/provenance/checksum, reordered publication, a changed artifact after hashing, and tracked-source mutation through temporary derived cases per D-19 through D-23.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-ReleaseQualificationNegative.ps1</automated></verify>
  <done>Every exact policy boundary has a positive owner and a negative fixture proving fail-closed classification.</done>
</task>

<task type="auto">
  <name>Task 2: Wire independent module and requirement qualification into Required</name>
  <files>scripts/quality/Test-ReleaseQualification.ps1, scripts/quality.ps1, .github/workflows/quality.yml, release/qualification/v0.1-requirements.json</files>
  <action>Add ordered independent selectors for mb-core, mb-color, and mb-image: format, check/test on +js+wasm+wasm-gc+native, runnable docs/examples, semantic interfaces, exact package contents/DAG, fixture generation/provenance, benchmark correctness/baseline check, external consumers, and tracked read-only assertion. Map WORK-06 and each QUAL-01..06 exactly once to concrete commands/artifacts, while cross-reference evidence may be listed separately. Required accepts only the policy-declared `blocked_unpublished_namespace` for downstream registry resolution and never converts another failure into that state. CI runs the same Required entrypoint with read-only permissions and no credentials/network publication per D-23/D-24.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-ReleaseQualification.ps1 -Focused</automated></verify>
  <done>Focused qualification proves all seven requirement selectors, exact module order, exact target/support policies, docs/examples/benchmarks/packages, and the expected downstream blocked contract.</done>
</task>

<task type="auto">
  <name>Task 3: Produce two clean-baseline Required run proofs</name>
  <files>release/qualification/v0.1-requirements.json</files>
  <action>After Tasks 1 and 2 are committed, require no tracked or staged diff, record the exact HEAD/toolchain, run `pwsh -NoProfile -File scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/required-run-1`, assert clean tracked state again, then repeat into `required-run-2` without changing HEAD. Canonicalize both machine-readable reports excluding run-local path/timestamp fields and require identical evidence digests. Record both run IDs, shared commit, result, digest, and the independent verifier mapping in the checked requirement ledger; rerun ledger check without source rewrites. This is the only phase-close route per D-24.</action>
  <verify><automated>pwsh -NoProfile -Command "if (git status --porcelain --untracked-files=no) { exit 1 }; & ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/required-run-1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; if (git status --porcelain --untracked-files=no) { exit 1 }; & ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/required-run-2; if ($LASTEXITCODE) { exit $LASTEXITCODE }; & ./scripts/quality/Test-ReleaseQualification.ps1 -VerifyTwoRuns artifacts/release-qualification/required-run-1,artifacts/release-qualification/required-run-2"</automated></verify>
  <done>Two full Required runs pass from one clean commit, leave tracked source unchanged, and produce matching deterministic evidence digests with all seven requirements independently mapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Required runner -> phase-close ledger | Generated evidence can authorize release-candidate completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-22 | Elevation of Privilege | blocked-state classifier | high | mitigate | Only exact policy-declared downstream namespace misses map to blocked; mutation tests reject fabricated pass. |
| T-05-23 | Tampering | Required evidence | high | mitigate | Clean HEAD checks, two runs, canonical digest comparison, and read-only tracked-source assertion. |
| T-05-24 | Repudiation | requirement ledger | medium | mitigate | Exact command/artifact selectors, commit/toolchain identities, and both run IDs are recorded. |
</threat_model>

<verification>
Focused negative and qualification suites pass, then two Required runs pass from the same clean commit with identical deterministic digests and no tracked changes.
</verification>

<success_criteria>
- Exact positive policies and negative fixtures cover module order, dependencies, contents, metadata, provenance, hashes, and blocked resolution.
- WORK-06 and QUAL-01..06 each have executable independent evidence.
- Two Required runs, not one, close the phase from a clean committed baseline.
</success_criteria>

<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-08-SUMMARY.md` when done.</output>
