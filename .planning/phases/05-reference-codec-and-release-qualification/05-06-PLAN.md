---
phase: 05-reference-codec-and-release-qualification
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - benchmarks/ppm/moon.pkg
  - benchmarks/ppm/ppm_bench.mbt
  - scripts/benchmarks/Invoke-PpmBenchmarks.ps1
  - release/qualification/benchmark-schema.json
  - release/qualification/ppm-native-release-baseline.json
autonomous: true
requirements: [QUAL-05]
must_haves:
  truths:
    - "A maintainer can reproduce PPM decode, encode, rejection, and transform-pipeline measurements from public APIs."
    - "Every timing record carries raw samples, environment identity, corpus/correctness digests, and aggregate variance."
    - "Correctness gates before timing and the only performance gate detects catastrophic local regression."
  artifacts:
    - path: benchmarks/ppm/ppm_bench.mbt
      provides: Named public-API MoonBit benchmark workloads
    - path: scripts/benchmarks/Invoke-PpmBenchmarks.ps1
      provides: Reproducible capture, validation, aggregation, and check mode
    - path: release/qualification/ppm-native-release-baseline.json
      provides: Checked local native release baseline with raw evidence
  key_links:
    - from: scripts/benchmarks/Invoke-PpmBenchmarks.ps1
      to: benchmarks/ppm/ppm_bench.mbt
      via: pinned moon bench --release --target native --frozen invocation
    - from: release/qualification/ppm-native-release-baseline.json
      to: fixtures/ppm
      via: exact corpus and correctness SHA-256 digests
---

<objective>
Create reproducible, correctness-gated benchmark evidence for the strict PPM proof.

Purpose: Satisfy D-17 and D-18 without turning noisy hosted timing into a product claim.
Output: Public-API workloads, a deterministic capture/check harness, schema, raw records, and a conservative local baseline.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@modules/mb-image/codec/contracts.mbt
@modules/mb-image/ops/copy_flip.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add correctness-gated public benchmark workloads</name>
  <files>benchmarks/ppm/moon.pkg, benchmarks/ppm/ppm_bench.mbt</files>
  <behavior>
    - Named workloads cover canonical decode and encode at 64x64, 256x256, and 1024x1024, one bounded-header rejection, and decode-flip-encode.
    - Each workload verifies expected pixel/byte counts and canonical digest before entering the timed body.
    - Workloads import only public codec, image operation, and core I/O/budget APIs.
  </behavior>
  <action>Create `@bench.T` workloads using `b.keep(...)` and canonical generated corpus bytes per D-17. Separate untimed correctness setup from the timed public operation, preserve exact budgets/options, and fail rather than record a fast incorrect result. Do not add timing or statistics logic to the portable codec.</action>
  <verify><automated>moon bench --release --target native --frozen benchmarks/ppm</automated></verify>
  <done>Every named workload runs through public APIs and refuses to time unless semantic and digest checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Capture raw environment-complete baseline evidence</name>
  <files>scripts/benchmarks/Invoke-PpmBenchmarks.ps1, release/qualification/benchmark-schema.json, release/qualification/ppm-native-release-baseline.json</files>
  <action>Implement one warmup plus seven captured native release invocations. Preserve each complete raw MoonBit summary and record exact moon/moonc/moonrun identities, commit, target, optimization mode, Windows/runtime/CPU/logical-core/memory data, corpus and correctness digests, workload names, repetitions, timestamp, mean/median/standard deviation/coefficient of variation/min/max. Validate against a closed schema and support `-Check` without rewriting. Per D-18, gate only local comparable hardware with `current_median <= max(4 * baseline_median, baseline_median + 5ms)` after correctness succeeds; hosted results are explicitly informational.</action>
  <verify><automated>pwsh -NoProfile -File scripts/benchmarks/Invoke-PpmBenchmarks.ps1 -Check</automated></verify>
  <done>The checked baseline contains raw samples and complete environment/correctness evidence; schema/check mode rejects missing, reordered, mismatched, or marketing-claim fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Benchmark process -> checked baseline | Host timing/environment output becomes release evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-16 | Tampering | baseline JSON | high | mitigate | Closed schema, corpus/correctness digests, commit identity, and check mode reject edited or mismatched records. |
| T-05-17 | Repudiation | environment capture | medium | mitigate | Raw run summaries and exact toolchain/hardware/timestamp fields accompany aggregates. |
| T-05-18 | Denial of Service | performance threshold | low | accept | Loose local-only catastrophic gate avoids blocking on ordinary noise; hosted timing is informational. |
</threat_model>

<verification>
Run the focused benchmark correctness checks, one capture, then `-Check`; confirm the checked record contains seven raw samples for every workload.
</verification>

<success_criteria>
- QUAL-05 fields are schema-required and populated from actual runs.
- Correctness precedes timing and remains independently verifiable by digest.
- Threshold policy is conservative, local-machine-scoped, and explicitly not a marketing claim.
</success_criteria>

<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-06-SUMMARY.md` when done.</output>
