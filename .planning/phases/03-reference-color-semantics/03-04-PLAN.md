---
phase: 03-reference-color-semantics
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: [modules/mb-color/quantize/moon.pkg, modules/mb-color/quantize/quantize.mbt, modules/mb-color/quantize/quantize_test.mbt, modules/mb-color/quantize/quantize_wbtest.mbt, modules/mb-color/quantize/reference_vectors_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [COLR-02, COLR-04]
must_haves:
  truths:
    - "D-06: encoded-sRGB-normalized -> encoded-sRGB-8 and normalized-alpha -> alpha-8 are distinct public operations with ties-to-even and no implicit saturation; linear-sRGB cannot enter either accidentally."
    - "The floating and exact-ratio rounding helpers make decisions before narrowing and never delegate policy to Double::round or backend casts."
    - "All 256 encoded values, exact half ties, near-half neighbors, endpoints, and quantization bounds are target-conformant."
  artifacts:
    - { path: "modules/mb-color/quantize/quantize.mbt", provides: "explicit normalized/encoded-8 conversion and shared exact ties-to-even decisions" }
    - { path: "modules/mb-color/quantize/quantize_wbtest.mbt", provides: "exhaustive and halfway rounding invariant evidence" }
    - { path: "modules/mb-color/quantize/reference_vectors_wbtest.mbt", provides: "deterministic package-local quantization/ratio vectors" }
  key_links:
    - { from: "modules/mb-color/quantize/quantize.mbt", to: "modules/mb-color/model", via: "identity-preserving EncodedSrgbComponent/EncodedSrgb8Component and alpha-specific types", pattern: "mb-color/model" }
    - { from: "modules/mb-color/quantize/quantize.mbt", to: "modules/mb-core/checked", via: "checked widened numerator arithmetic", pattern: "checked_mul" }
---
<!-- phase-source-audit: GOAL,COLR-02,COLR-04,D-06,D-08,D-14 -->
<objective>Make 8-bit quantization and integer ratio rounding exact and portable.
Purpose: prevent target casts or half-up library behavior from becoming color semantics, and provide the arithmetic seam used by encoded alpha.
Output: `quantize` package, exhaustive evidence, and exact package policy.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/03-reference-color-semantics/03-01-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-02-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-03-SUMMARY.md
@modules/mb-core/checked/pkg.generated.mbti</context>
<interfaces>
Consume `@model.EncodedSrgbComponent`, `@model.EncodedSrgb8Component`, `@model.NormalizedAlpha`, `@model.EncodedAlpha8`, and `@checked.checked_mul`. Create exactly `quantize_encoded_srgb(@model.EncodedSrgbComponent) -> @model.EncodedSrgb8Component`, `dequantize_encoded_srgb(@model.EncodedSrgb8Component) -> @model.EncodedSrgbComponent`, `quantize_alpha(@model.NormalizedAlpha) -> @model.EncodedAlpha8`, `dequantize_alpha(@model.EncodedAlpha8) -> @model.NormalizedAlpha`, and the intentionally public lower-layer helper `round_ratio_ties_even(UInt64, UInt64) -> Result[UInt64, @error.CoreError]` for alpha conversion. Keep floating classification private. No function accepts `LinearSrgbComponent` or an identity-erasing scalar, and quantize imports no transfer package.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement portable ties-to-even quantization</name>
  <files>modules/mb-color/quantize/moon.pkg, modules/mb-color/quantize/quantize.mbt, modules/mb-color/quantize/quantize_test.mbt, modules/mb-color/quantize/quantize_wbtest.mbt, modules/mb-color/quantize/reference_vectors_wbtest.mbt</files>
  <read_first>D-06/D-08; research Portable ties-to-even, Pattern 3, Pitfall 2; model and checked interfaces; derived edge vectors</read_first>
  <behavior>Generate the quantize table, then write failing tests for typed-domain rejection/acceptance, scaled 0.5->0, 1.5->2, 2.5->2, neighboring halves, endpoints, both encoded-sRGB and alpha dequantize/requantize for all 256 codes, error at most 0.5/255, zero denominator, exact ratio ties, and checked multiplication overflow.</behavior>
  <action>Run the selective generator with `-Artifacts quantize` before RED and check it after GREEN. Create a four-target package importing model, mb-core/error, and mb-core/checked but not transfer. For each validated domain-specific value, scale by 255 and classify floor/fraction/parity explicitly; construct only its matching 8-bit identity type. Dequantization constructs the corresponding encoded-sRGB normalized or normalized-alpha type. For exact ratios, use quotient/remainder and checked twice-remainder before narrowing. Never accept linear-sRGB, call the pinned half-up rounding API, silently saturate, or let a cast choose rounding per D-06/D-08.</action>
  <acceptance_criteria>Every encoded value round-trips exactly; all halfway decisions are even; invalid ratio arithmetic is structured; four target results are identical.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-color test quantize --target all --frozen</automated></verify>
  <done>The rounding portion of COLR-02 and quantization invariants of COLR-04 are executable.</done>
</task>
<task type="auto">
  <name>Task 2: Register quantize and prove exact policy alignment</name>
  <files>policy/foundation.json</files>
  <read_first>generated quantize interface; model/transfer policy entries; package import output</read_first>
  <action>Append `quantize` after `transfer` in ordered publication inventory, but record its exact dependency DAG edges only to model, mb-core/error, and mb-core/checked. Include the generated vector table in publication contents. Expose only the four typed conversions and documented ratio helper needed by alpha; add no transfer/profile/image import.</action>
  <acceptance_criteria>Policy proves the intended package DAG and public surface exactly.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Quantization exits green with all earlier packages and fixtures unchanged.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-07 | Tampering | halfway decisions | high | mitigate | floor/remainder/parity classification and named exact cases on all targets |
| T-03-08 | Denial of Service | integer ratio arithmetic | medium | mitigate | zero-denominator and overflow rejection before division/multiplication |
| T-03-09 | Tampering | narrowing | medium | mitigate | prove value range before `to_byte`; encoded exhaustive test |
</threat_model>
<verification>All-target quantize tests and complete Required lane.</verification>
<success_criteria>D-06 and the rounding seam required by D-08 are exact, exhaustive, and independent of backend rounding behavior.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-04-SUMMARY.md` when done.</output>
