---
phase: 03-reference-color-semantics
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [modules/mb-color/transfer/moon.pkg, modules/mb-color/transfer/transfer.mbt, modules/mb-color/transfer/transfer_test.mbt, modules/mb-color/transfer/transfer_wbtest.mbt, modules/mb-color/transfer/reference_vectors_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [COLR-02, COLR-04]
must_haves:
  truths:
    - "D-05/D-06: encoded-sRGB and linear-sRGB conversion uses the published inclusive low branches and documented absolute tolerances on every target."
    - "D-14: the package consumes the registered reference dataset through its deterministic package-local MoonBit table without filesystem access."
    - "Endpoints, exact thresholds and adjacent values, monotonicity, finite/range preservation, and round trips away from threshold discontinuities are executable invariants."
  artifacts:
    - { path: "modules/mb-color/transfer/transfer.mbt", provides: "standards-literal sRGB transfer oracle and named public tolerances" }
    - { path: "modules/mb-color/transfer/reference_vectors_wbtest.mbt", provides: "generated MoonBit table binding the registered vectors to four-target tests without ambient file I/O" }
  key_links:
    - { from: "modules/mb-color/transfer/transfer.mbt", to: "modules/mb-color/model", via: "EncodedSrgbComponent -> LinearSrgbComponent and inverse", pattern: "mb-color/model" }
    - { from: "modules/mb-color/transfer/reference_vectors_wbtest.mbt", to: "fixtures/color/srgb-reference-vectors.json", via: "same deterministic generator emits matching target-native test table", pattern: "srgb-reference-vectors" }
---
<!-- phase-source-audit: GOAL,COLR-02,COLR-04,D-05,D-06,D-14 -->
<objective>Implement the portable sRGB reference transfer oracle and its provenance-recorded evidence.
Purpose: supply Phase 4 with reproducible encoded/linear semantics without backend-specific equality assumptions.
Output: generated package-local vector table, transfer package, and exact policy entry.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/03-reference-color-semantics/03-01-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-02-SUMMARY.md
@fixtures/manifest.json</context>
<interfaces>
Consume distinct validated `@model.EncodedSrgbComponent` and `@model.LinearSrgbComponent`. Create `decode_srgb(@model.EncodedSrgbComponent) -> @model.LinearSrgbComponent`, `encode_srgb(@model.LinearSrgbComponent) -> @model.EncodedSrgbComponent`, and public contract constants as zero-argument functions `decode_absolute_tolerance() -> Double` = `1.0e-12`, `encode_absolute_tolerance() -> Double` = `1.0e-12`, `roundtrip_absolute_tolerance() -> Double` = `2.0e-12`. Use `@math.pow`; decoding selects the low branch for `e <= 0.04045`, encoding for `l <= 0.0031308`. No public conversion accepts or returns a bare ambiguous normalized scalar.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Generate package-local vectors and implement identity-preserving transfer</name>
  <files>modules/mb-color/transfer/moon.pkg, modules/mb-color/transfer/transfer.mbt, modules/mb-color/transfer/transfer_test.mbt, modules/mb-color/transfer/transfer_wbtest.mbt, modules/mb-color/transfer/reference_vectors_wbtest.mbt</files>
  <read_first>D-05/D-06; generated vectors; research Exact Numerical Contract, tolerances, Pitfall 1; model interface</read_first>
  <behavior>Generate the transfer table, then write failing tests first for typed input/output identity, vectors, endpoints, exact inclusive thresholds, immediate neighbors, monotonic ordered grids, finite/range preservation, and round trips away from the published threshold discontinuities. Compare `pow` results only with the named operation tolerance; branch tests assert the selected formula directly.</behavior>
  <action>Run the selective generator with `-Artifacts transfer` before RED and check it after GREEN. Create a four-target `transfer` package importing only model and standard `math`. Implement the standards-literal piecewise formulas and construct the opposite explicit component type. Do not clamp, compare power results for exact equality, invent continuity at rounded thresholds, use a polynomial approximation, or expose any raw/ambiguous Double conversion seam per D-05/D-06.</action>
  <acceptance_criteria>All target outputs stay finite/in-range and satisfy the exact branch/tolerance contract; the generated interface exposes the three locked tolerance values.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-color test transfer --target all --frozen</automated></verify>
  <done>COLR-02 conversion and the conversion portion of COLR-04 are deterministic across the declared targets.</done>
</task>
<task type="auto">
  <name>Task 2: Register transfer topology and prove the green exit</name>
  <files>policy/foundation.json</files>
  <read_first>generated transfer interface and package output; current model policy; updated fixture manifest</read_first>
  <action>Append `transfer` after `model` with exact imports, targets, semantic interface, files, and publication inventory. Preserve the temporary non-reexport root scaffold and reject any undeclared dependency.</action>
  <acceptance_criteria>Exact policy plus fixture digest validation accepts only the implemented transfer surface.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Transfer code, evidence, and policy leave the complete repository green and read-only.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-20 | Tampering | branch thresholds | high | mitigate | named constants plus exact-threshold and neighbor tests |
| T-03-21 | Tampering | component identity | high | mitigate | distinct encoded/linear argument and result types in the public interface |
| T-03-22 | Tampering | backend floating variation | medium | mitigate | absolute operation tolerances and no exact power-result equality |
</threat_model>
<verification>Generator check, fixture-policy matrix, four-target transfer tests, and root Required.</verification>
<success_criteria>COLR-02/COLR-04 transfer evidence and D-05/D-06/D-13/D-14 are covered without copied opaque fixtures or invented numerical guarantees.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-03-SUMMARY.md` when done.</output>
