---
phase: 03-reference-color-semantics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [modules/mb-color/model/moon.pkg, modules/mb-color/model/components.mbt, modules/mb-color/model/identities.mbt, modules/mb-color/model/model_test.mbt, modules/mb-color/model/model_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [COLR-01]
must_haves:
  truths:
    - "D-01/D-03: callers can name sRGB space, encoded-sRGB or linear-sRGB transfer, normalized-double or encoded-8 representation, and straight or premultiplied alpha without ambient defaults."
    - "D-02/D-04: EncodedSrgbComponent, LinearSrgbComponent, NormalizedAlpha, EncodedSrgb8Component, and EncodedAlpha8 are distinct opaque validated values; non-finite/out-of-range input is rejected rather than clamped or wrapped."
    - "The first real mb-color package is portable, candidate, exact-policy classified, and does not become a universal Color bag or root re-export."
  artifacts:
    - { path: "modules/mb-color/model/components.mbt", provides: "identity-bearing encoded/linear sRGB components and domain-specific normalized/8-bit alpha" }
    - { path: "modules/mb-color/model/identities.mbt", provides: "explicit ColorSpaceIdentity, TransferIdentity, ComponentRepresentation, and AlphaMode values" }
    - { path: "policy/foundation.json", provides: "exact root-scaffold-plus-model transitional package contract" }
  key_links:
    - { from: "modules/mb-color/model/moon.pkg", to: "moonbit-foundation/mb-core/error", via: "sole inward dependency for structured constructor rejection", pattern: "mb-core/error" }
    - { from: "policy/foundation.json", to: "modules/mb-color/model/pkg.generated.mbti", via: "ordered closed semantic interface", pattern: "mb-color/model" }
---
<!-- phase-source-audit: GOAL,COLR-01,D-01,D-02,D-03,D-04 -->
<objective>Create the explicit validated color vocabulary that every later Phase 3 package consumes.
Purpose: make ambiguous component, space, transfer, representation, and alpha state unrepresentable before numerical operations exist.
Output: portable `model` package, black/white-box tests, and exact transitional policy topology.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/02-bounded-core-primitives/02-01-SUMMARY.md
@modules/mb-core/error/pkg.generated.mbti
@modules/mb-color/moon.mod.json
@policy/foundation.json</context>
<interfaces>
Consume `@error.CoreError::new(ErrorCategory, ErrorCode, ... context?)`; use `InvalidInput/InvalidRange` with bounded stable context tokens rather than prose parsing. Create exactly these public opaque value contracts: `EncodedSrgbComponent::new(Double) -> Result[Self, @error.CoreError]`, `value`, `space()->Srgb`, `transfer()->EncodedSrgb`, `representation()->NormalizedDouble`; `LinearSrgbComponent::new(Double) -> Result[Self, @error.CoreError]` with corresponding linear identity accessors; `NormalizedAlpha::new(Double) -> Result[Self, @error.CoreError]` and `value`; `EncodedSrgb8Component::new(UInt64) -> Result[Self, @error.CoreError]`, `from_byte`, `value`, and encoded-sRGB/Encoded8 identity accessors; `EncodedAlpha8::new(UInt64) -> Result[Self, @error.CoreError]`, `from_byte`, `value`. Also expose equatable closed enums `ColorSpaceIdentity { Srgb }`, `TransferIdentity { EncodedSrgb, LinearSrgb }`, `ComponentRepresentation { NormalizedDouble, Encoded8 }`, and `AlphaMode { Straight, Premultiplied }`. Do not expose a generic `NormalizedComponent` or `EncodedComponent8` alias that erases identity.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify opaque component validation and explicit identities</name>
  <files>modules/mb-color/model/moon.pkg, modules/mb-color/model/components.mbt, modules/mb-color/model/identities.mbt, modules/mb-color/model/model_test.mbt, modules/mb-color/model/model_wbtest.mbt</files>
  <read_first>D-01..D-04; research Normalized domain and Pattern 1/2; mb-core error interface; current mb-color target manifest</read_first>
  <behavior>Write failing public tests first for every distinct type at 0, 1/255, 1, representative interior values, NaN, both infinities, negative finite, above-one finite, encoded 0/255/256/UInt64 maximum, exact value/space/transfer/representation accessors, and all identity enums. Add compile-time/public-interface assertions that encoded and linear values cannot be substituted and no generic normalized/encoded component alias exists. White-box tests prove invalid state cannot be constructed and errors have stable category/code/context.</behavior>
  <action>Create a four-target package importing only `moonbit-foundation/mb-core/error`. Store each domain/identity scalar behind its own opaque field. Check NaN and infinity before the closed `[0,1]` range; reject instead of changing input. Guard `UInt64 <= 255` before `to_byte`. Color component methods return their fixed sRGB/transfer/representation identities; alpha types deliberately carry only coverage representation, never a color transfer. Do not create a raw normalized conversion seam or public generic record that freely combines component, space, transfer, and alpha fields per D-01/D-02/D-03/D-04.</action>
  <acceptance_criteria>All invalid values return deterministic structured errors; all valid values round-trip exactly; the generated interface exposes no fields or universal color container.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-color test model --target all --frozen</automated></verify>
  <done>COLR-01 has an executable explicit component and identity foundation on all four targets.</done>
</task>
<task type="auto">
  <name>Task 2: Register the first exact mb-color package contract</name>
  <files>policy/foundation.json</files>
  <read_first>generated model interface; current mb-color policy entry; Phase 2 exact-interface and topology classifier pattern</read_first>
  <action>Update only the mb-color policy entry to list the legacy private root scaffold followed by `model`, with exact targets, imports, generated semantic lines, and publication files. The root remains temporarily only as the non-public Phase 1 scaffold and must import/re-export nothing; it is removed in Plan 03-07. Preserve candidate status, module dependency direction, and all other module policy.</action>
  <acceptance_criteria>Policy rejects missing/extra interface lines or imports and records no image/reverse dependency.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>The model package and transitional scaffold topology exit the plan atomically green.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering | normalized constructors | high | mitigate | reject non-finite values before range comparison and preserve no public field escape |
| T-03-02 | Tampering | encoded constructor | medium | mitigate | compare full-width UInt64 to 255 before narrowing |
| T-03-03 | Elevation of Privilege | semantic identity | medium | mitigate | distinct opaque components and explicit closed identities; no generic freely-combinable bag |
</threat_model>
<verification>Four-target model tests followed by the root Required lane.</verification>
<success_criteria>COLR-01 and D-01..D-04 are represented by exact portable public contracts with no hidden default, clamping, root facade, or dependency reversal.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-01-SUMMARY.md` when done.</output>
