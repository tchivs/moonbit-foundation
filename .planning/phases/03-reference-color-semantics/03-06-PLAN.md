---
phase: 03-reference-color-semantics
plan: "06"
type: execute
wave: 6
depends_on: ["03-05"]
files_modified: [modules/mb-color/profile/moon.pkg, modules/mb-color/profile/profile.mbt, modules/mb-color/profile/profile_test.mbt, modules/mb-color/profile/profile_wbtest.mbt, modules/mb-color/profile/reference_vectors_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [COLR-05, COLR-04]
must_haves:
  truths:
    - "D-10/D-12: built-in sRGB is direct identity; opaque profile bytes carry an explicit bounded tag and round-trip exactly without header inspection, transforms, digest, or ICC-conformance claim."
    - "D-11: the caller supplies the payload maximum and Budget; oversize/tag/budget failures occur deterministically before copying or allocation."
    - "Empty payload and exact-limit payload succeed when budgets permit; one-over-limit and each independent budget dimension fail without charge/copy side effects."
  artifacts:
    - { path: "modules/mb-color/profile/profile.mbt", provides: "ProfileFormatTag, ProfileLimits, OpaqueProfile, and ProfileIdentity" }
    - { path: "modules/mb-color/profile/profile_wbtest.mbt", provides: "preallocation ordering and no-header-inspection proof" }
    - { path: "modules/mb-color/profile/reference_vectors_wbtest.mbt", provides: "generated package-local tag/limit/budget/opaque-byte cases" }
  key_links:
    - { from: "modules/mb-color/profile/profile.mbt", to: "modules/mb-core/bytes", via: "OwnedBytes::from_bytes after explicit size/tag validation", pattern: "OwnedBytes::from_bytes" }
    - { from: "modules/mb-color/profile/profile.mbt", to: "modules/mb-core/budget", via: "caller-supplied Budget charges bytes/allocation/allocation-size", pattern: "@budget.Budget" }
---
<!-- phase-source-audit: GOAL,COLR-04,COLR-05,D-10,D-11,D-12,D-14 -->
<objective>Add a bounded opaque profile identity seam that preserves metadata without implementing color management.
Purpose: allow future codecs to retain profile bytes while keeping allocation policy explicit and ICC semantics outside this phase.
Output: independent `profile` package, adversarial limit tests, and exact policy entry.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/03-reference-color-semantics/03-02-SUMMARY.md
@.planning/phases/02-bounded-core-primitives/02-03-SUMMARY.md
@.planning/phases/02-bounded-core-primitives/02-04-SUMMARY.md
@modules/mb-core/budget/pkg.generated.mbti
@modules/mb-core/bytes/pkg.generated.mbti</context>
<interfaces>
Create `ProfileFormatTag::new(String) -> Result[Self, @error.CoreError]`, `ProfileFormatTag::icc() -> Self`, `ProfileFormatTag::value(Self) -> String`; grammar is ASCII `[A-Za-z0-9][A-Za-z0-9._+-]{0,31}` and accepted spelling/case is preserved. Create `ProfileLimits::new(UInt64) -> Self`, `max_payload_bytes(Self) -> UInt64`; `OpaqueProfile::from_bytes(ProfileFormatTag, Bytes, ProfileLimits, @budget.Budget) -> Result[Self, @error.CoreError]`, plus `tag`, `length`, `view`. Create opaque `ProfileIdentity::builtin_srgb()`, `ProfileIdentity::opaque(OpaqueProfile)`, `is_builtin_srgb`, and `opaque_profile() -> OpaqueProfile?`. No digest API exists.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement caller-bounded opaque profile preservation</name>
  <files>modules/mb-color/profile/moon.pkg, modules/mb-color/profile/profile.mbt, modules/mb-color/profile/profile_test.mbt, modules/mb-color/profile/profile_wbtest.mbt, modules/mb-color/profile/reference_vectors_wbtest.mbt</files>
  <read_first>D-10..D-12; research Opaque Profile Seam, Pitfall 4 and Don't Hand-Roll; mb-core bytes/budget interfaces; derived profile vectors</read_first>
  <behavior>Generate the profile table, then write failing tests for builtin sRGB identity, canonical `icc`, accepted tag boundaries/case, empty/overlong/invalid-ASCII tags, empty payload, exact maximum, one-over maximum, insufficient bytes, zero allocation, insufficient allocation-size, exact byte view round-trip, source independence after copy, and arbitrary `icc`-tagged bytes remaining opaque. White-box tests prove tag/limit rejection occurs before `OwnedBytes::from_bytes` and budget state remains unchanged.</behavior>
  <action>Run the selective generator with `-Artifacts profile` before RED and check it after GREEN. Create a four-target package importing only mb-core/error, budget, and bytes. Validate tag, then compare source length to the required caller limit, then call `OwnedBytes::from_bytes` directly so its checked narrowing, allocation approval, atomic charge, allocation, and copy order remain authoritative. Store the resulting owner privately and expose only length plus retained immutable view. Allow empty bytes within limits. Treat the tag as caller identity metadata only: do not inspect an ICC header/size, validate contents, compute transforms, compute a digest, infer semantic equivalence, or introduce a global maximum per D-10/D-11/D-12.</action>
  <acceptance_criteria>Opaque bytes are preserved exactly and independently, every rejection is structured/preallocation, and no API or prose implies profile validity.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-color test profile --target all --frozen</automated></verify>
  <done>COLR-05 and the bounded profile invariant portion of COLR-04 are executable.</done>
</task>
<task type="auto">
  <name>Task 2: Register the independent profile leaf</name>
  <files>policy/foundation.json</files>
  <read_first>generated profile interface; package imports; current color package topology</read_first>
  <action>Append `profile` as an independent leaf after alpha in the ordered publication inventory, but record DAG edges only to mb-core error/budget/bytes; do not force dependencies on model, transfer, quantize, or alpha. Add the package-local vector file, exact interface and publication contents while retaining candidate/four-target metadata.</action>
  <acceptance_criteria>Policy proves the opaque profile seam is bounded by core primitives and does not pull in unrelated color or image layers.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Profile behavior and policy leave all previous target contracts green.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-13 | Denial of Service | profile payload | high | mitigate | explicit caller maximum before budgeted OwnedBytes copy |
| T-03-14 | Spoofing | format tag | medium | mitigate | validated bounded token documented as identity only, never conformance |
| T-03-15 | Information Disclosure | profile storage | medium | mitigate | private owned copy and validated immutable ByteView only |
</threat_model>
<verification>All-target profile tests, budget-state white-box tests, and root Required.</verification>
<success_criteria>COLR-05 and D-10..D-12 are satisfied without a parser, implicit resource policy, digest semantics, or external dependency.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-06-SUMMARY.md` when done.</output>
