---
phase: 03-reference-color-semantics
plan: "08"
type: execute
wave: 8
depends_on: ["03-07"]
files_modified: [scripts/fixtures/Generate-ColorVectors.ps1, modules/mb-color/profile/reference_vectors_wbtest.mbt, modules/mb-color/profile/profile_wbtest.mbt, modules/mb-color/README.mbt.md]
autonomous: true
gap_closure: true
requirements: [COLR-04]
must_haves:
  truths:
    - "README provenance language says published formulas are primary sources while the sampled numeric points are project-selected formula-derived reference points; it never calls those generated points official samples."
    - "Render-ProfileMoon consumes every canonical profile.payload_cases entry and deterministically emits package-local payload plus applicable budget-rejection cases."
    - "Profile white-box tests consume every emitted case and behaviorally prove exact-limit/empty/opaque-byte success, one-over limit rejection before charge, independent budget-dimension atomic rejection, and byte-for-byte preservation."
    - "Generator byte identity, fixture policy, profile tests, four-target README checks, color negative fixtures, full Required, and tracked-read-only proof all pass without public API expansion."
  artifacts:
    - { path: "scripts/fixtures/Generate-ColorVectors.ps1", provides: "payload_cases-aware Render-ProfileMoon and deterministic derived budget cases" }
    - { path: "modules/mb-color/profile/reference_vectors_wbtest.mbt", provides: "generated private payload and budget-rejection tables" }
    - { path: "modules/mb-color/profile/profile_wbtest.mbt", provides: "behavioral consumer for every generated profile case" }
    - { path: "modules/mb-color/README.mbt.md", provides: "provenance-accurate evidence wording" }
  key_links:
    - { from: "fixtures/color/derived-edge-vectors.json", to: "scripts/fixtures/Generate-ColorVectors.ps1", via: "canonical profile_vectors.payload_cases from New-CanonicalData", pattern: "payload_cases" }
    - { from: "scripts/fixtures/Generate-ColorVectors.ps1", to: "modules/mb-color/profile/reference_vectors_wbtest.mbt", via: "Render-ProfileMoon stable MoonBit serialization", pattern: "generated_profile_payload_cases" }
    - { from: "modules/mb-color/profile/profile_wbtest.mbt", to: "modules/mb-color/profile/reference_vectors_wbtest.mbt", via: "package-private generated table iteration", pattern: "generated_profile_payload_cases" }
---
<!-- phase-source-audit: GAP-03-PROVENANCE-WORDING,GAP-03-PROFILE-PAYLOAD-WIRING,COLR-04,D-13,D-14,D-15 -->
<objective>Close the two Phase 3 verification gaps without changing the public color/profile API.
Purpose: align public provenance claims with actual derivation and connect the already-canonical profile payload evidence to executable package-local behavior.
Output: payload-aware deterministic profile generation, complete generated-case white-box proof, corrected README wording, and green final qualification.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-VERIFICATION.md
@.planning/phases/03-reference-color-semantics/03-06-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-07-SUMMARY.md
@scripts/fixtures/Generate-ColorVectors.ps1
@fixtures/color/derived-edge-vectors.json
@modules/mb-color/profile/profile_test.mbt
@modules/mb-color/profile/profile_wbtest.mbt
@modules/mb-color/profile/reference_vectors_wbtest.mbt
@modules/mb-color/README.mbt.md</context>
<interfaces>
Do not modify `profile.mbt`, any public `.mbti` surface, package imports, or policy. Extend generated package-private evidence only: `generated_profile_payload_cases() -> Array[(String, Bytes, UInt64, Bool)]` carries id, exact bytes, caller maximum, and expected success for every canonical `profile.payload_cases` item. `generated_profile_budget_rejection_cases() -> Array[(String, Bytes, UInt64, UInt64, UInt64, UInt64, String)]` deterministically derives applicable underfunded bytes/allocation/allocation-size cases from successful canonical payloads and carries the expected budget context token. Keep accepted/rejected tag tables and their completeness test.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire canonical profile payload cases into executable generated evidence</name>
  <files>modules/mb-color/profile/profile_wbtest.mbt, scripts/fixtures/Generate-ColorVectors.ps1, modules/mb-color/profile/reference_vectors_wbtest.mbt</files>
  <read_first>03-VERIFICATION gaps[1]; New-CanonicalData profile.payload_cases; current Render-ProfileMoon; profile constructors/budget helpers/manual tests; existing generated tag table</read_first>
  <behavior>RED first: add a focused white-box test that calls the absent generated payload/budget table functions, asserts all four canonical IDs are present exactly once, and therefore fails before generator changes. GREEN: iterate every generated payload case. Successful cases receive exact sufficient budget, construct an opaque profile, assert length and every byte; this includes empty, exact-limit, and ICC-shaped opaque bytes. The one-over case receives sufficient storage budget but fails at the caller limit, then all budget counters equal their pre-call values. Iterate every generated underfunded-budget case and assert `BudgetExceeded`, the exact dimension context, and unchanged bytes/allocations/allocation-size. Assert emitted counts match canonical input/derived applicability so unused or duplicate cases fail.</behavior>
  <action>Commit the RED test before changing the renderer and confirm `moon -C modules/mb-color test profile --target all --frozen` fails because payload generation is absent. Extend `Render-ProfileMoon` to read `Data.profile.payload_cases`, serialize IDs, byte literals, UInt64 maxima, and booleans in stable canonical order, and derive only applicable independent budget-rejection variants from successful payloads. Preserve the formatter-stable generated header/tag functions. Regenerate with `-Artifacts profile`, make the behavioral loops green, format only the two MoonBit files, and run `-Artifacts profile -Check` to prove the checked-in bytes are exactly renderer-owned. Reuse existing private/public budget helpers and `OpaqueProfile::from_bytes`; do not add public API, change canonical JSON cases, inspect ICC headers, or duplicate profile implementation logic.</action>
  <acceptance_criteria>The initial RED failure proves the missing link; GREEN consumes every canonical payload and every generated budget case with exact bytes and atomic rejection behavior on all four targets.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -Command '&amp; ./scripts/fixtures/Generate-ColorVectors.ps1 -Artifacts profile -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; &amp; moon -C modules/mb-color test profile --target all --frozen; exit $LASTEXITCODE'</automated></verify>
  <done>The canonical `profile.payload_cases -> Render-ProfileMoon -> reference_vectors_wbtest -> profile_wbtest` path is complete and behavioral.</done>
</task>
<task type="auto">
  <name>Task 2: Correct provenance wording and requalify Phase 3 evidence</name>
  <files>modules/mb-color/README.mbt.md</files>
  <read_first>03-VERIFICATION gaps[0]; README Evidence and provenance section; srgb-reference-vectors classification/notes; fixture manifest source record</read_first>
  <action>Replace the unsupported sample attribution with provenance-accurate wording: the W3C/ICC formulas are published primary sources, while endpoints, thresholds, adjacent values, and other numeric points are project-selected and computed by the repository generator. Retain the separate repository-derived adversarial classification and the no-copied-external-bytes statement. Then run generator `-Artifacts all -Check`, fixture policy, profile tests on all targets, README checks on js/wasm/wasm-gc/native, and the root Required lane. Confirm Required executes its color provenance/source negative fixtures and ends with an unchanged tracked checkout. No semantic/API/policy/fixture expansion is authorized.</action>
  <acceptance_criteria>README and fixture classifications agree exactly; all generated artifacts are byte-stable; positive and negative qualification plus read-only proof pass.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -Command 'function Invoke-Checked([scriptblock]$Command) { &amp; $Command; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; Invoke-Checked { &amp; ./scripts/fixtures/Generate-ColorVectors.ps1 -Artifacts all -Check }; Invoke-Checked { &amp; ./scripts/quality/Test-FixturePolicy.ps1 }; Invoke-Checked { &amp; moon -C modules/mb-color test profile --target all --frozen }; foreach ($target in @("js", "wasm", "wasm-gc", "native")) { Invoke-Checked { &amp; moon -C modules/mb-color check README.mbt.md --target $target --frozen } }; Invoke-Checked { &amp; ./scripts/quality.ps1 -Lane Required }'</automated></verify>
  <done>Both verification gaps are closed and COLR-04 has honest, complete, four-target executable evidence.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-23 | Repudiation | README provenance | high | mitigate | distinguish published formulas from project-selected generated points |
| T-03-24 | Tampering | generated profile completeness | high | mitigate | count/ID checks and behavioral iteration over every canonical payload case |
| T-03-25 | Denial of Service | profile budgets | high | mitigate | generated independent underfunded cases assert atomic unchanged counters |
</threat_model>
<verification>Fail-first/green profile evidence, selective and all-artifact generator checks, fixture-policy matrix, four-target README/profile checks, and full Required negative/read-only qualification.</verification>
<success_criteria>The two actionable gaps in 03-VERIFICATION.md are closed with no public API, package DAG, policy, or canonical fixture-scope change.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-08-SUMMARY.md` when done.</output>
