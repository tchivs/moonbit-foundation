---
phase: 03-reference-color-semantics
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified: [modules/mb-color/alpha/moon.pkg, modules/mb-color/alpha/alpha.mbt, modules/mb-color/alpha/alpha_test.mbt, modules/mb-color/alpha/alpha_wbtest.mbt, modules/mb-color/alpha/reference_vectors_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [COLR-01, COLR-03, COLR-04]
must_haves:
  truths:
    - "D-07: straight and premultiplied normalized/encoded states are distinct opaque public types with explicit space, transfer, representation, and alpha identity."
    - "D-08/D-09: both domains use the common ties-to-even rule, canonical zero-alpha output, checked widened arithmetic, and structured rejection of nonzero encoded p>a."
    - "Round-trip identity is claimed only for proven cases; exhaustive encoded evidence establishes the actual one-pair error bound before documentation."
  artifacts:
    - { path: "modules/mb-color/alpha/alpha.mbt", provides: "four explicit SRGBA state types and named conversions" }
    - { path: "modules/mb-color/alpha/alpha_wbtest.mbt", provides: "exhaustive encoded alpha pair and invariant proof" }
    - { path: "modules/mb-color/alpha/reference_vectors_wbtest.mbt", provides: "generated package-local zero/boundary/tie/profile-independent alpha vectors" }
  key_links:
    - { from: "modules/mb-color/alpha/alpha.mbt", to: "modules/mb-color/model", via: "validated components and explicit identities", pattern: "TransferIdentity" }
    - { from: "modules/mb-color/alpha/alpha.mbt", to: "modules/mb-color/quantize", via: "one ties-to-even arithmetic policy", pattern: "round_ratio_ties_even" }
---
<!-- phase-source-audit: GOAL,COLR-01,COLR-03,COLR-04,D-07,D-08,D-09,D-14 -->
<objective>Deliver explicit straight/premultiplied alpha states and deterministic conversions in normalized and encoded-8 domains.
Purpose: prevent image code from guessing alpha mode or repairing invalid premultiplied bytes.
Output: `alpha` package, exhaustive boundary evidence, and exact policy entry.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/03-reference-color-semantics/03-01-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-02-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-04-SUMMARY.md</context>
<interfaces>
Create exactly four opaque states. `StraightNormalizedSrgba::encoded(EncodedSrgbComponent, EncodedSrgbComponent, EncodedSrgbComponent, NormalizedAlpha) -> Self` and `::linear(LinearSrgbComponent, LinearSrgbComponent, LinearSrgbComponent, NormalizedAlpha) -> Self`; `PremultipliedNormalizedSrgba::encoded(...) -> Result[Self,@error.CoreError]` and `::linear(...) -> Result[Self,@error.CoreError]` reject any channel greater than alpha, including nonzero RGB at zero alpha. Both normalized states expose `encoded_rgb() -> (EncodedSrgbComponent,EncodedSrgbComponent,EncodedSrgbComponent)?`, `linear_rgb() -> (...) ?`, `alpha() -> NormalizedAlpha`, and fixed identity accessors `space/transfer/representation/alpha_mode`. `StraightEncodedSrgba8::new(EncodedSrgb8Component, EncodedSrgb8Component, EncodedSrgb8Component, EncodedAlpha8) -> Self`; `PremultipliedEncodedSrgba8::new(...) -> Result[Self,@error.CoreError]` is the exact public p>a rejection entry point and also rejects noncanonical nonzero RGB at alpha zero. Encoded states expose typed `r/g/b/a` plus fixed sRGB/encoded-sRGB/Encoded8/alpha-mode accessors. Create all directional conversions with explicit outcomes: `premultiply_normalized(StraightNormalizedSrgba) -> Result[PremultipliedNormalizedSrgba,@error.CoreError]`, `unpremultiply_normalized(PremultipliedNormalizedSrgba) -> Result[StraightNormalizedSrgba,@error.CoreError]`, `premultiply_encoded(StraightEncodedSrgba8) -> Result[PremultipliedEncodedSrgba8,@error.CoreError]`, and `unpremultiply_encoded(PremultipliedEncodedSrgba8) -> Result[StraightEncodedSrgba8,@error.CoreError]`. No Boolean mode toggle or identity-erasing component parameter exists.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify distinct alpha-state constructors and conversions</name>
  <files>modules/mb-color/alpha/moon.pkg, modules/mb-color/alpha/alpha.mbt, modules/mb-color/alpha/alpha_test.mbt, modules/mb-color/alpha/alpha_wbtest.mbt, modules/mb-color/alpha/reference_vectors_wbtest.mbt</files>
  <read_first>D-07..D-09; research Alpha Contract and Pitfall 3; model/quantize interfaces; derived alpha vectors</read_first>
  <behavior>Generate the alpha table, then write failing tests for every exact constructor/conversion signature, encoded-vs-linear normalized variants, identity accessors and mismatched optional RGB accessors, zero alpha with nonzero straight input -> canonical zero output, constructor rejection of noncanonical premultiplied zero, p>a rejection per channel at both premultiplied constructors, encoded alpha 0/1/254/255, alpha-255 identity, ties, and exhaustive `(component,alpha)` encoded enumeration. Calculate the maximum one-pair error before naming its bound.</behavior>
  <action>Run the selective generator with `-Artifacts alpha` before RED and check it after GREEN. Create a four-target package importing exactly model, quantize, mb-core/error, and mb-core/checked. Implement all signatures from the interfaces block verbatim and classify encoded/linear normalized state internally without exposing raw fields. Premultiplied constructors are the sole public state-validation entry points and reject p>a before storing. Normalized conversions multiply/divide within the carried transfer domain; all zero-alpha conversions construct zero RGB in the same domain. Encoded operations use checked numerators plus `@quantize.round_ratio_ties_even`; unpremultiply accepts only already validated premultiplied state. Do not clamp, toggle modes, infer a transfer, or promise arbitrary round-trip identity per D-07/D-08/D-09.</action>
  <acceptance_criteria>Types prevent alpha-mode confusion, zero is canonical in both directions, invalid encoded state is structured, and exhaustive evidence supports every documented bound.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-color test alpha --target all --frozen</automated></verify>
  <done>COLR-03 and the alpha-state portion of COLR-01/COLR-04 are satisfied.</done>
</task>
<task type="auto">
  <name>Task 2: Register alpha package and its exact dependency direction</name>
  <files>policy/foundation.json</files>
  <read_first>generated alpha interface; package imports; exact preceding color package order</read_first>
  <action>Append `alpha` after `quantize` in publication order with exact generated constructor/conversion/accessor lines, the package-local vector file, targets, and dependency edges only to model, quantize, mb-core/error, and mb-core/checked. No transfer, root, profile, image, codec, or reverse edge may appear.</action>
  <acceptance_criteria>Exact policy and Required prove alpha consumes only earlier portable contracts.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Alpha semantics and topology leave the repository green and tracked-read-only.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10 | Tampering | premultiplied input | high | mitigate | opaque state constructors and explicit p<=a rejection before unpremultiply |
| T-03-11 | Tampering | encoded numerator/rounding | high | mitigate | checked widened multiplication plus shared exact ratio rounding |
| T-03-12 | Repudiation | round-trip claims | medium | mitigate | exhaustive enumeration and documentation limited to proven identities/bounds |
</threat_model>
<verification>Four-target alpha tests, exhaustive white-box matrix, and root Required.</verification>
<success_criteria>COLR-03, D-07..D-09, and alpha invariants from D-14 are explicit, deterministic, and fail closed.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-05-SUMMARY.md` when done.</output>
