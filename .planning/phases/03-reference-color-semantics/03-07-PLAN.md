---
phase: 03-reference-color-semantics
plan: "07"
type: execute
wave: 7
depends_on: ["03-06"]
files_modified: [modules/mb-color/README.mbt.md, modules/mb-color/CHANGELOG.md, modules/mb-color/moon.pkg, modules/mb-color/scaffold.mbt, modules/mb-color/scaffold_wbtest.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1]
autonomous: true
requirements: [COLR-01, COLR-02, COLR-03, COLR-04, COLR-05]
must_haves:
  truths:
    - "D-15: all five public packages and standalone README examples check on js, wasm, wasm-gc, and native; exact interfaces/imports/contents/DAG/fixtures and tracked-read-only behavior are Required gates."
    - "D-16/D-17: publication order is exactly model, transfer, quantize, alpha, profile, while the separately checked DAG keeps quantize independent of transfer and profile independent of all color packages; root scaffold is absent."
    - "D-18: Required rejects catch-all roots, hidden defaults/clamping/backend rounding, reverse/image dependencies, fixture provenance drift, and out-of-phase parser/render/codec surface."
  artifacts:
    - { path: "modules/mb-color/README.mbt.md", provides: "four-target public examples and exact numerical/profile contracts" }
    - { path: "scripts/quality/Invoke-MoonQuality.ps1", provides: "mb-color README, prohibition, negative-fixture, and final qualification stages" }
    - { path: "policy/foundation.json", provides: "closed five-package publication and semantic-interface inventory without root" }
  key_links:
    - { from: "modules/mb-color/README.mbt.md", to: "modules/mb-color/{model,transfer,quantize,alpha,profile}", via: "explicit literate imports and runnable examples", pattern: "moonbit" }
    - { from: "scripts/quality/Invoke-MoonQuality.ps1", to: "policy/foundation.json", via: "same exact classifiers used by positive and synthetic negative cases", pattern: "mb-color" }
---
<!-- phase-source-audit: GOAL,COLR-01,COLR-02,COLR-03,COLR-04,COLR-05,D-13,D-14,D-15,D-16,D-17,D-18 -->
<objective>Replace the private scaffold with independently consumable documentation and fail-closed final qualification for the complete color oracle.
Purpose: make every Phase 3 claim executable on the four declared targets and freeze an acyclic candidate surface for Phase 4.
Output: checked README/changelog, final five-package topology, negative fixtures/prohibitions, and a green read-only Required lane.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/03-reference-color-semantics/03-01-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-02-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-03-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-04-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-05-SUMMARY.md
@.planning/phases/03-reference-color-semantics/03-06-SUMMARY.md
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1</context>
<tasks>
<task type="auto">
  <name>Task 1: Replace scaffold prose with executable public color contracts</name>
  <files>modules/mb-color/README.mbt.md, modules/mb-color/CHANGELOG.md</files>
  <read_first>D-05/D-06/D-09..D-18; every Phase 3 generated interface; Phase 2 standalone README pattern; verified fixture records</read_first>
  <action>Rewrite the README as standalone literate MoonBit with explicit imports for all five packages and examples covering EncodedSrgbComponent -> LinearSrgbComponent conversion, encoded-sRGB/alpha-specific quantization, every exact alpha constructor/conversion including canonical zero and p>a rejection, builtin profile identity, and caller-bounded opaque byte round-trip. Record exact transfer formulas, inclusive thresholds, absolute tolerances, quantization rule, proven alpha identity/error bounds, 32-byte tag grammar, explicit payload limit/budget ordering, arbitrary-byte/no-ICC-conformance boundary, target matrix, candidate status, publication order, exact dependency DAG, and publication block. Cite registered primary sources and distinguish generated reference from derived adversarial vectors per D-13/D-14. Update Unreleased changelog with five candidate packages, four package-local generated vector tables, and scaffold removal; claim no stable/public release.</action>
  <acceptance_criteria>A consumer can execute and understand every COLR contract without hidden defaults, overstated round-trip/ICC claims, or reference to private APIs.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -Command '$targets = @("js", "wasm", "wasm-gc", "native"); foreach ($target in $targets) { &amp; moon -C modules/mb-color check README.mbt.md --target $target --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }'</automated></verify>
  <done>All public semantics and evidence are independently runnable documentation on each declared target.</done>
</task>
<task type="auto">
  <name>Task 2: Remove the root scaffold and close the exact package boundary</name>
  <files>modules/mb-color/moon.pkg, modules/mb-color/scaffold.mbt, modules/mb-color/scaffold_wbtest.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <read_first>D-15..D-18; final generated package interfaces/imports/contents; current policy module entry and mb-core spine assertion pattern</read_first>
  <action>Delete the root package file and both scaffold sources. Replace transitional policy with the ordered publication inventory `model`, `transfer`, `quantize`, `alpha`, `profile`, including every source/test/generated-vector file, while separately enumerating the exact allowed package-import DAG: model -> mb-core/error; transfer -> model; quantize -> model + mb-core/error + mb-core/checked (explicitly no transfer); alpha -> model + quantize + mb-core/error + mb-core/checked; profile -> mb-core/error + mb-core/budget + mb-core/bytes (no color package). Retain independent 0.1.0 candidate/module metadata and the sole module-level mb-core dependency. Add assertions for publication order, each exact edge set, quantize no-transfer, profile independence, and obsolete-root rejection. No root prelude, image/reverse import, additional space, CSS, rendering, codec, or ICC parser surface may enter per D-16/D-17/D-18.</action>
  <acceptance_criteria>Filesystem and ordered publication inventory agree on five packages; the independently asserted DAG matches every listed edge exactly and contains no order-implied dependency.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Assert-Policy.ps1</automated></verify>
  <done>The Phase 1 scaffold is gone and the final candidate DAG is closed.</done>
</task>
<task type="auto">
  <name>Task 3: Add color negative fixtures and run final Required qualification</name>
  <files>scripts/quality/Invoke-MoonQuality.ps1</files>
  <read_first>D-13..D-18; existing core negative-fixture/README/read-only stages; all color tests and fixture manifest records</read_first>
  <action>Add explicit mb-color Required stages for each target's standalone README and synthetic negative cases using the same exact sequence/set/interface/publication classifiers: root/extra/missing package, reverse/image import, forbidden quantize->transfer edge, forbidden profile->color edge, semantic-interface drift, publication/generated-vector drift, missing target, and fixture digest/redistribution failures. Add scoped portable-source checks rejecting backend half-up rounding, silent clamp, and identity-erasing generic normalized conversion APIs; verify README owns exact tolerance/zero-alpha/opaque-profile statements. Keep commands fixed and JSON non-executable. Run the sole root Required lane twice, ensure every negative fails for the intended reason, all package-local generated vector checks/tests/docs pass on four targets, and tracked diff is unchanged per D-15.</action>
  <acceptance_criteria>Required fails closed for every declared prohibition and passes from the committed final tree without tracked mutation.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -Command '1..2 | ForEach-Object { &amp; ./scripts/quality.ps1 -Lane Required; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }'</automated></verify>
  <done>COLR-01..05 and D-13..D-18 have final four-target, provenance, topology, negative, documentation, and read-only evidence.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16 | Tampering | package/interface/DAG | high | mitigate | exact closed policy classifiers plus synthetic drift cases |
| T-03-17 | Repudiation | numerical and profile claims | medium | mitigate | executable docs tied to named constants, proven bounds, and manifest provenance |
| T-03-18 | Elevation of Privilege | deferred surface/root facade | medium | mitigate | exact five-package spine, root deletion, source/import prohibitions |
| T-03-19 | Tampering | qualification process | high | mitigate | fixed commands, fail-closed negatives, and tracked read-only proof |
</threat_model>
<verification>Four direct README checks, policy validation, then two complete root Required runs.</verification>
<success_criteria>Every COLR requirement and D-13..D-18 is independently consumable and continuously enforced; no root catch-all, external package, hidden rounding/clamping, ICC parser, or out-of-phase surface remains.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-07-SUMMARY.md` when done.</output>
