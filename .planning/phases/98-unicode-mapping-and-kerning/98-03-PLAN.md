---
phase: 98-unicode-mapping-and-kerning
plan: 03
type: execute
wave: 3
depends_on:
  - 98-01
  - 98-02
files_modified:
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/generated_fonts_wbtest.mbt
  - modules/mb-font/font/pkg.generated.mbti
  - policy/foundation.json
  - modules/mb-font/README.mbt.md
  - modules/mb-font/CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - FONT-02
  - FONT-04
must_haves:
  truths:
    - "The complete Unicode and kern public outcome matrices pass with identical semantic facts on js, wasm, wasm-gc, and native (D-15, D-16)."
    - "The generated interface exposes exactly Font::glyph_for_scalar, Font::kerning, two kern-limit accessors, and the expanded FontLimits::new signature; no private cmap/kern fact leaks (D-02, D-12, D-16)."
    - "Policy keeps `tchivs/mb-font -> tchivs/mb-core` as the only runtime dependency, preserves all four targets, and registers cmap.mbt/kern.mbt in exact source/publication inventories."
    - "Documentation explains one signed scalar, one opaque glyph result, deterministic format-12/4 selection, optional basic kern outcomes, mutation guards, and explicit limits without claiming deferred text/outline/real-font features."
    - "Invalid input, malformed bytes, unsupported capability, mutation drift, and resource exhaustion remain distinguishable; neither glyph zero nor kern zero masks an error."
  flagged_assumptions:
    - "FONT-02 accepts one signed scalar only; empty/null/string normalization, byte/code-point/grapheme length, and plural glyph output are not part of the contract."
    - "FONT-04 accepts one glyph pair only; the locked result taxonomy is absence/miss zero, signed supported hit, unsupported capability, or malformed data."
    - "Assumption delta is no-change: BMP and supplementary scalars are invariant cases through the same public method and same opaque GlyphId type."
  prohibitions:
    - statement: "MUST NOT silently reinterpret malformed or unsupported mapping/kerning data as a successful glyph-zero or neutral-zero result."
      status: resolved
      verification: test
  artifacts:
    - path: modules/mb-font/font/pkg.generated.mbti
      provides: "Generated exact public Phase 98 interface."
    - path: policy/foundation.json
      provides: "Exact source, publication, dependency, target, description, and semantic-interface policy."
    - path: modules/mb-font/README.mbt.md
      provides: "Literate public Unicode/kerning contract, limits, error taxonomy, and exclusions."
    - path: modules/mb-font/CHANGELOG.md
      provides: "Candidate release record for the Phase 98 additions."
    - path: README.md
      provides: "Equivalent English and Chinese mb-font responsibility/status descriptions."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Complete portable public outcome/resource/mutation evidence."
    - path: modules/mb-font/font/font_wbtest.mbt
      provides: "Complete private ranking/offset/search/envelope/taxonomy evidence."
  key_links:
    - from: modules/mb-font/font/pkg.generated.mbti
      to: policy/foundation.json
      via: "Generated signatures are copied verbatim into the exact semantic_interface allowlist after all-target info."
    - from: modules/mb-font/font/font_test.mbt
      to: modules/mb-font/font/font.mbt
      via: "Black-box package tests exercise only public Font/GlyphId/FontLimits APIs on every target."
    - from: policy/foundation.json
      to: modules/mb-font/font/moon.pkg
      via: "Exact imports and supported targets remain unchanged while source/publication inventories add cmap/kern."
---

<objective>
Qualify and publish the complete Phase 98 contract across all portable targets and repository policy surfaces.

Purpose: The new queries are complete only when portable semantics, generated interface, exact inventories, dependency closure, and public documentation agree.
Output: All-target black-/white-box evidence, regenerated `.mbti`, exact policy updates, literate docs/changelog, and bilingual discovery text.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/98-unicode-mapping-and-kerning/98-CONTEXT.md
@.planning/phases/98-unicode-mapping-and-kerning/98-RESEARCH.md
@.planning/phases/98-unicode-mapping-and-kerning/98-PATTERNS.md
@.planning/phases/98-unicode-mapping-and-kerning/98-01-SUMMARY.md
@.planning/phases/98-unicode-mapping-and-kerning/98-02-SUMMARY.md
@.planning/phases/97-font-admission-and-metrics/97-03-SUMMARY.md

<interfaces>
Expected public delta after Plans 01-02:
- `pub fn Font::glyph_for_scalar(Self, Int) -> Result[GlyphId, @error.CoreError]`
- `pub fn Font::kerning(Self, GlyphId, GlyphId) -> Result[Int, @error.CoreError]`
- `pub fn FontLimits::max_kern_subtables(Self) -> UInt64`
- `pub fn FontLimits::max_kern_pairs(Self) -> UInt64`
- `FontLimits::new` retains all existing named parameters and adds `max_kern_subtables~ : UInt64` and `max_kern_pairs~ : UInt64`

`moon -C modules/mb-font info --target all --frozen` is the only writer of `pkg.generated.mbti`; never hand-edit the generated file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the portable public/private qualification matrix</name>
  <files>modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/generated_fonts_wbtest.mbt</files>
  <read_first>
    - .planning/phases/98-unicode-mapping-and-kerning/98-01-SUMMARY.md
    - .planning/phases/98-unicode-mapping-and-kerning/98-02-SUMMARY.md
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-font/font/generated_fonts_wbtest.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/cmap.mbt
    - modules/mb-font/font/kern.mbt
  </read_first>
  <behavior>
    - "BMP and supplementary scalars pass through the same glyph_for_scalar method; scalar boundaries, canonical ranks, precedence, aliases/conflicts, direct/array/raw-zero format-4, and format-12 edges are all public or private executable cases."
    - "Kern absence, empty set, first/middle/last hit, miss, positive/negative adjustment, foreign IDs, unsupported profiles, malformed pairs, and exact resource edges are executable cases."
    - "Every public query participates in the all-query mutation/mutate-back helper; successful queries are repeatable, order-independent, cross-font isolated, and budget-neutral."
    - "The same public values and stable CoreError fields pass on js, wasm, wasm-gc, and native."
  </behavior>
  <action>
Audit the generated test matrix against D-15/D-16 and add only missing cases. Public black-box coverage must include the explicit specless assumptions: BMP and supplementary values through the same `glyph_for_scalar` call shape; no string, empty, null, length, grapheme, or normalization contract; exactly one kern glyph pair with absence/miss zero and unsupported/malformed nonzero error paths. Assert exact semantic values and stable category/code/operation/context/requested/limit facts, not rendered prose or implementation layout.

Ensure public cmap coverage includes direct and glyph-array format 4, raw zero, format-12 BMP/supplementary groups, first/last/gaps, all ranks, record-order independence, format-12 precedence, shared aliases, distinct consistent/conflicting candidates, no eligible record, no per-scalar fallback, invalid scalar endpoints, glyph-range failure, revision drift, exact semantic/work/budget fit, and one-short atomic failure. Ensure public kern coverage includes absent, empty, first/middle/last/miss, signed values, foreign IDs, unsupported version/count/coverage/format profiles, malformed recognized lengths/helpers/order/duplicates/ranges, revision drift, exact limits/work/budget, and one-short atomic failure.

Complete white-box rank, format-4 base math, format-12/4 lower-bound, kern exact-length/helper/key/signed-FWORD, and error-taxonomy tables. Extend the existing all-query mutation helper with both methods. Run all targets only after the native cycle is green; every target must execute the same semantic cases.
  </action>
  <verify>
    <automated>moon -C modules/mb-font test --target all --frozen</automated>
  </verify>
  <acceptance_criteria>
    - "Every D-15 generated case and D-16 public/private boundary is represented by executable assertions."
    - "The BMP/supplementary same-method invariant and both flagged specless assumptions are explicit in tests."
    - "All four targets pass identical semantic values/error facts with no target-specific branch or dependency."
  </acceptance_criteria>
  <done>The full FONT-02/FONT-04 semantic, hostile, mutation, determinism, and resource matrix passes on every supported target.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the interface and close policy/documentation publication</name>
  <files>modules/mb-font/font/pkg.generated.mbti, policy/foundation.json, modules/mb-font/README.mbt.md, modules/mb-font/CHANGELOG.md, README.md</files>
  <read_first>
    - modules/mb-font/font/moon.pkg
    - modules/mb-font/font/pkg.generated.mbti
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/limits.mbt
    - policy/foundation.json:2181
    - modules/mb-font/README.mbt.md
    - modules/mb-font/CHANGELOG.md
    - README.md:28
    - README.md:127
    - scripts/quality/Assert-Policy.ps1
  </read_first>
  <action>
Run `moon -C modules/mb-font info --target all --frozen` after production and tests compile; accept the generated update to `pkg.generated.mbti` and do not hand-edit it. Review the delta against D-02/D-16: only `Font::glyph_for_scalar`, `Font::kerning`, two kern-limit accessors, and the expanded `FontLimits::new` signature may be added. Private cmap/kern records, tables, offsets, ranks, state enums, and helpers must remain absent.

Update the existing `tchivs/mb-font` entry in `policy/foundation.json` in one scoped change: describe deterministic Unicode mapping and basic legacy horizontal kerning; add `font/cmap.mbt` and `font/kern.mbt` to publication files and the exact accepted production-source order; copy generated signatures verbatim into `semantic_interface`; preserve `allowed_imports`, supported targets, and the sole `mb-font -> mb-core` dependency. No schema, UI, external API, package, FFI, or host-font entry is added.

Update `modules/mb-font/README.mbt.md` with the explicit constructor limits, one signed scalar contract, invalid-scalar versus glyph-zero miss, exact selection rank, one pair kern contract, absence/miss/unsupported/malformed distinctions, allocation-free admitted lookups, and retained-source guards. Narrow the exclusions to keep outlines, text shaping/GPOS/GSUB, normalization, discovery, rasterization, and licensed real-font qualification in their owning phases. Extend the existing candidate CHANGELOG entry and update equivalent English/Chinese `mb-font` responsibility/status text in root README. Do not claim Phase 99 outlines or Phase 100 real-font evidence.
  </action>
  <verify>
    <automated>moon -C modules/mb-font info --target all --frozen &amp;&amp; moon -C modules/mb-font check README.mbt.md --target all --frozen &amp;&amp; moon -C modules/mb-font test --target all --frozen &amp;&amp; ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/phase-98</automated>
  </verify>
  <acceptance_criteria>
    - "Generated `.mbti` and policy contain exactly the intended public additions and no private/deferred surface."
    - "Policy inventories both new sources/publication files in exact order while imports, targets, and mb-core-only dependency remain unchanged."
    - "Literate, candidate changelog, English, and Chinese documentation agree on delivered behavior and exclusions."
    - "All-target info/tests/literate checks and the repository Required lane pass."
  </acceptance_criteria>
  <done>Phase 98 is portable, discoverable, documented, and fail-closed under exact repository interface/dependency/source policy.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact group | Every new file/symbol delivered by Phase 98 |
|---|---|
| Unicode implementation | `font/cmap.mbt`; `CmapLookupFacts`, `CmapFormat4Facts`, `CmapFormat12Facts`; `font_cmap_rank`, `font_admit_cmap_lookup`, `font_lookup_cmap` |
| Kerning implementation | `font/kern.mbt`; `KernState`, `KernFormat0Facts`; `font_optional_table_window`, `font_admit_kern`, `font_lookup_kern`, `font_kern_pair_key` |
| Public API | `Font::glyph_for_scalar(Int)`, `Font::kerning(GlyphId, GlyphId)`, `FontLimits::max_kern_subtables`, `FontLimits::max_kern_pairs`, expanded `FontLimits::new` |
| Retained wiring | selected cmap descriptor and kern state in `RequiredTableFacts`/`Font`; no public raw facts |
| Generated/publication evidence | updated `pkg.generated.mbti`, `policy/foundation.json`, `font_test.mbt`, `font_wbtest.mbt`, optional shared generated fixture helpers |
| Documentation | updated module README/changelog and equivalent English/Chinese root README text |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| malicious font bytes -> all-target qualification | Generated hostile cmap/kern structures exercise checked admission and error classes. |
| public values -> generated interface/policy | A private fact or wrong result shape could become a compatibility promise. |
| retained source -> every public query | Mutation must fail before any value is published on every target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-98-03-01 | Tampering | malicious cmap/kern fixture bytes | high | mitigate | Run exact malformed length/offset/order/range/helper cases through public open/query paths on all targets and assert no partial Font/result. |
| T-98-03-02 | Denial of Service | attacker-declared record/group/segment/subtable/pair work | high | mitigate | Execute exact-fit/one-short ceiling/max_work/budget tests proving pre-loop rejection and unchanged budget across targets. |
| T-98-03-03 | Tampering | retained source mutation | high | mitigate | Extend one all-query mutation and mutate-back matrix to both new methods and run it on four targets. |
| T-98-03-04 | Tampering | glyph-range/key correctness | high | mitigate | Public foreign-ID and private pair-key/mapped-glyph cardinality tests prove receiving-font validation before publication. |
| T-98-03-05 | Spoofing | error-taxonomy confusion | high | mitigate | Freeze invalid-input/data/capability/state/resource facts in black-box tests and document that zero is only valid miss/absence. |
| T-98-03-06 | Tampering | partial publication/interface leak | high | mitigate | Assert failed opens expose no Font, regenerate interface only after green tests, and exact-policy reject private cmap/kern symbols and deferred APIs. |
| T-98-SC | Tampering | package/dependency publication | low | mitigate | Use frozen MoonBit commands and policy-enforce unchanged four targets, exact sources, and mb-core-only imports/dependency. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Deterministic Unicode scalar and basic legacy kerning queries over admitted Font | 98-01, 98-02, 98-03 | COVERED |
| REQ | FONT-02 | Any valid scalar, canonical format-12/4 mapping, zero miss, invalid/malformed error | 98-01, 98-03 | COVERED |
| REQ | FONT-04 | Legacy format-0 pair lookup with absence/miss zero and distinct unsupported/malformed | 98-02, 98-03 | COVERED |
| RESEARCH | — | Retained table-local facts; allocation-free binary searches; exact rank; optional kern tri-state | 98-01, 98-02 | COVERED |
| RESEARCH | — | Checked exact envelopes, pre-loop limits/work/budget, all-target public/private verification | 98-01, 98-02, 98-03 | COVERED |
| RESEARCH | — | Generated interface, source/publication policy, README/changelog integration | 98-03 | COVERED |
| CONTEXT | D-01,D-02,D-03 | Signed singular scalar, opaque in-range ID, pre/post mutation guards | 98-01, 98-03 | COVERED |
| CONTEXT | D-04,D-05,D-06,D-07,D-07a | Exact canonical selection, peer consistency, structural proof, capability/no-fallback outcomes | 98-01, 98-03 | COVERED |
| CONTEXT | D-08,D-09,D-10,D-10a,D-11 | Exact kern profile, receiving-font pair, zero/capability/data taxonomy, strict keys | 98-02, 98-03 | COVERED |
| CONTEXT | D-12,D-13,D-14 | Atomic compact facts, explicit kern ceilings/preflight, allocation-free budget-neutral lookup | 98-01, 98-02 | COVERED |
| CONTEXT | D-15,D-16 | Complete generated public/private hostile and four-target interface evidence | 98-01, 98-02, 98-03 | COVERED |

The edge probes are carried as flagged assumptions rather than dismissed. Prohibition recall retained one safety/transparency constraint, resolved by descriptor-less executable tests. Deferred outlines, real-font qualification, shaping/GPOS/GSUB, normalization, host discovery, rasterization, extended containers, and authoring are exclusions, not gaps.
</source_audit>

<verification>
1. Run `moon -C modules/mb-font test --target all --frozen` and require identical public/private semantic cases on js, wasm, wasm-gc, and native.
2. Run `moon -C modules/mb-font info --target all --frozen`; review and commit the generated interface.
3. Run `moon -C modules/mb-font check README.mbt.md --target all --frozen`.
4. Run `./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/phase-98`.
5. Confirm no new imports, external package/API, FFI, UI, schema, host discovery, shaping, outline, or real-font evidence entered the phase.
</verification>

<success_criteria>
- FONT-02 and FONT-04 pass complete all-target public and private evidence.
- BMP and supplementary scalars use one signed-scalar-to-opaque-GlyphId method.
- Kerning absence/miss zero remains distinct from unsupported capability and malformed data.
- All attacker-driven work is preflighted; mutation, glyph-range/key, taxonomy, and partial-publication threats have executable mitigations.
- Generated interface, exact policy, literate docs/changelog, and bilingual README describe only the delivered pure-MoonBit `mb-font -> mb-core` contract.
</success_criteria>

<output>
Create `.planning/phases/98-unicode-mapping-and-kerning/98-03-SUMMARY.md` when done.
</output>
