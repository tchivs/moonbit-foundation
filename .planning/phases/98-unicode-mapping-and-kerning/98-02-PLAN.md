---
phase: 98-unicode-mapping-and-kerning
plan: 02
type: execute
wave: 2
depends_on:
  - 98-01
files_modified:
  - modules/mb-font/font/directory.mbt
  - modules/mb-font/font/kern.mbt
  - modules/mb-font/font/limits.mbt
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/generated_fonts_wbtest.mbt
  - modules/mb-font/README.mbt.md
autonomous: true
requirements:
  - FONT-04
must_haves:
  truths:
    - "Font::kerning accepts one opaque glyph pair, revalidates both IDs against the receiving Font, and returns the exact signed format-0 adjustment (D-09)."
    - "Kern absence and a supported pair miss return zero; a well-formed out-of-profile table is retained as unsupported and errors only when kerning is queried; malformed recognized data fails atomic opening (D-08, D-10, D-10a)."
    - "The supported profile is exactly OpenType version 0, one subtable, subtable version 0, coverage 0x0001, format 0, exact lengths, canonical search helpers, and strictly sorted unique in-range pair keys (D-08, D-11)."
    - "The optional kern directory-record scan, kern subtable scans, and pair scans are each preflighted before iteration and charged exactly once under explicit non-zero max_kern_subtables/max_kern_pairs, max_work, and the shared budget (D-13)."
    - "Successful lookups are allocation-free, repeatable, order-independent, budget-neutral, and protected by pre/post revision guards (D-12, D-14)."
  flagged_assumptions:
    - "FONT-04 unclassified probe: the public input is exactly one left/right GlyphId pair; outcomes are absence/miss zero, supported signed hit, unsupported capability, or malformed data."
    - "Non-version-0 kern headers are safely recognized as unsupported; data errors are reserved for truncation or inconsistency within the recognized version-0 envelope."
  prohibitions:
    - statement: "MUST NOT silently reinterpret malformed or present-but-unsupported kerning data as neutral zero."
      status: resolved
      verification: test
  artifacts:
    - path: modules/mb-font/font/kern.mbt
      provides: "Private absent/supported/unsupported state, strict format-0 admission, and allocation-free pair lookup."
    - path: modules/mb-font/font/limits.mbt
      provides: "Explicit non-zero kern-subtable and kern-pair semantic ceilings."
    - path: modules/mb-font/font/directory.mbt
      provides: "Optional normalized table-window lookup without weakening required-table errors."
    - path: modules/mb-font/font/font.mbt
      provides: "Guarded Font::kerning facade with receiving-font validation and distinct outcomes."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Public absence/miss/hit/error/mutation/resource evidence."
    - path: modules/mb-font/font/font_wbtest.mbt
      provides: "Private exact-length, search-helper, pair-key, signed-value, and taxonomy evidence."
  key_links:
    - from: modules/mb-font/font/directory.mbt
      to: modules/mb-font/font/kern.mbt
      via: "font_optional_table_window supplies None or one normalized private TableWindow."
    - from: modules/mb-font/font/tables.mbt
      to: modules/mb-font/font/kern.mbt
      via: "font_admit_required_tables retains KernState before atomic RequiredTableFacts publication."
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/kern.mbt
      via: "Font::kerning validates both opaque IDs, dispatches tri-state lookup, revision-checks again, and publishes signed units."
---

<objective>
Add bounded legacy horizontal kerning without weakening atomic admission or conflating absence, unsupported capability, and malformed bytes.

Purpose: Library authors need an exact pair adjustment over admitted glyph identities while hostile counts, cross-font IDs, and optional-table taxonomy remain deterministic.
Output: Optional kern discovery, strict version-0/format-0 facts, `Font::kerning`, explicit kern limits, and generated black-/white-box evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/98-unicode-mapping-and-kerning/98-CONTEXT.md
@.planning/phases/98-unicode-mapping-and-kerning/98-RESEARCH.md
@.planning/phases/98-unicode-mapping-and-kerning/98-PATTERNS.md
@.planning/phases/98-unicode-mapping-and-kerning/98-01-SUMMARY.md
@.planning/phases/97-font-admission-and-metrics/97-03-SUMMARY.md

<interfaces>
Preserve existing `DirectoryFacts`, `TableWindow`, required `font_table_window`, `RequiredTableFacts`, `font_admit_required_tables`, `Font::require_revision`, `font_glyph_id_error`, and `read_i16`.

New symbols owned here:
- `font_optional_table_window(DirectoryFacts, UInt64) -> TableWindow?`
- `KernState::{Absent, Supported(KernFormat0Facts), Unsupported}`
- `font_admit_kern`, `font_lookup_kern`, `font_kern_pair_key`
- `Font::kerning(GlyphId, GlyphId) -> Result[Int, @error.CoreError]`
- `FontLimits::max_kern_subtables()`, `FontLimits::max_kern_pairs()`, corresponding `FontLimits::new` named parameters and private fields
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one supported kern pair through optional admission and public lookup</name>
  <files>modules/mb-font/font/directory.mbt, modules/mb-font/font/kern.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    - modules/mb-font/font/directory.mbt:18
    - modules/mb-font/font/directory.mbt:34
    - modules/mb-font/font/directory.mbt:613
    - modules/mb-font/font/tables.mbt:57
    - modules/mb-font/font/tables.mbt:1763
    - modules/mb-font/font/font.mbt:217
    - modules/mb-font/font/font.mbt:257
    - modules/mb-font/font/font_test.mbt:923
    - modules/mb-font/font/font_test.mbt:2086
  </read_first>
  <behavior>
    - "A font with no kern table opens and Font::kerning returns 0 for two admitted GlyphIds."
    - "A supported one-subtable format-0 table returns exact positive and negative signed adjustments; an absent pair returns 0."
    - "A GlyphId produced by a larger font and passed to a smaller receiving Font is rejected before pair-key construction."
    - "Mutation before/during lookup returns revision drift and publishes no adjustment."
  </behavior>
  <action>
Write the public RED cases first using small checksum-correct kern payload builders colocated with the existing generated font helpers. Create `font_optional_table_window` as a private normalized scan returning `Some(TableWindow)` or `None`; keep required `font_table_window` and its missing-required-table contract unchanged. Before this one new scan of `directory.tables`, add the retained directory `numTables` count to declared discovery work, preflight it against `max_work` and the caller budget, and include it exactly once in the aggregate admission charge (D-13). The scan must not begin until that preflight succeeds.

Create `kern.mbt` with private `KernState`, `KernFormat0Facts`, exact checked version-0/one-subtable/coverage-0x0001/format-0 envelope admission, strict pair-key validation, signed `read_i16` values, and count-derived binary search. Integrate the state into `font_admit_required_tables` and the retained Font facts before the single atomic Font publication (D-08, D-11, D-12). Table absence retains `Absent`; the supported tracer retains compact table-local facts.

Add `Font::kerning(left, right)` beside `horizontal_metrics`. Follow the same guard sequence: pre-read revision check, revalidate both opaque numeric values against the receiving font's `num_glyphs`, dispatch state/lookup, post-read revision check, publish exact signed units. Both Absent and supported miss return zero; no query charges or mutates the opening budget (D-09, D-14). Do not add GPOS, accumulation, override, format 2, vertical/cross-stream/minimum semantics, shaping, scaling, or glyph sequences.
  </action>
  <verify>
    <automated>moon -C modules/mb-font test --target native --frozen</automated>
  </verify>
  <acceptance_criteria>
    - "Absent table, pair miss, positive hit, negative hit, foreign-font ID, and mutation cases execute through the public API."
    - "Supported bytes are validated/retained during the existing open transaction and lookup uses count-derived binary search."
    - "Native tests pass without changing package imports or targets."
  </acceptance_criteria>
  <done>A library author can query one admitted glyph pair and receive exact signed legacy kerning or neutral zero through the guarded public API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish explicit kern semantic ceilings across every constructor call site</name>
  <reversibility rating="costly">D-13 intentionally expands the public FontLimits constructor and every consumer; removing these named ceilings later would require coordinated API and documentation changes and weaken the resource contract.</reversibility>
  <files>modules/mb-font/font/limits.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/generated_fonts_wbtest.mbt, modules/mb-font/README.mbt.md</files>
  <read_first>
    - modules/mb-font/font/limits.mbt
    - modules/mb-font/font/font_test.mbt:19
    - modules/mb-font/font/font_test.mbt:696
    - modules/mb-font/font/font_wbtest.mbt:84
    - modules/mb-font/font/generated_fonts_wbtest.mbt:585
    - modules/mb-font/README.mbt.md:70
  </read_first>
  <behavior>
    - "FontLimits::new rejects zero max_kern_subtables and zero max_kern_pairs with stable InvalidInput contexts."
    - "Successful construction preserves both exact values through public accessors."
    - "Every repository FontLimits::new call supplies explicit non-zero values; no hidden defaults alter existing callers."
  </behavior>
  <action>
Add private `max_kern_subtables_value` and `max_kern_pairs_value`, named constructor parameters, non-zero validation contexts `max-kern-subtables`/`max-kern-pairs`, initialization, and public accessors in `limits.mbt` per D-13. Update every exact call site found in `font_test.mbt`, `font_wbtest.mbt`, `generated_fonts_wbtest.mbt`, and the literate README; do not add defaults or overloads that hide the public resource choice.

Write RED constructor/accessor/zero-limit cases before implementation. Preserve all existing limit fields and error facts. Use explicit values suited to each fixture so later exact-fit/one-short tests can isolate subtable and pair ceilings without accidentally failing an unrelated limit.
  </action>
  <verify>
    <automated>moon -C modules/mb-font test --target native --frozen &amp;&amp; moon -C modules/mb-font check README.mbt.md --target native --frozen</automated>
  </verify>
  <acceptance_criteria>
    - "Both named limits are non-zero, publicly observable, and supplied explicitly at all MoonBit and literate call sites."
    - "Existing limit behavior remains unchanged and native package/literate checks pass."
    - "The costly D-13 constructor change is recorded without a redundant checkpoint because the decision is already locked."
  </acceptance_criteria>
  <done>Callers explicitly bound kern subtable and pair work in every FontLimits construction path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close unsupported/malformed taxonomy and hostile kern resource edges</name>
  <files>modules/mb-font/font/kern.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/kern.mbt
    - modules/mb-font/font/tables.mbt:103
    - modules/mb-font/font/tables.mbt:263
    - modules/mb-font/font/tables.mbt:736
    - modules/mb-font/font/font_test.mbt:1124
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-font/font/cursor.mbt
  </read_first>
  <behavior>
    - "A structurally valid classic OpenType table outside the single coverage-0x0001 format-0 profile, including multiple supported subtables, retains Unsupported and does not block metrics/cmap."
    - "Exact Apple version 0x00010000 is envelope-validated through its 8-byte table/subtable headers and retained Unsupported without parsing format bodies; other complete nonzero/unknown version prefixes retain Unsupported without attacker-driven traversal."
    - "Truncation, undersized headers/lengths, checked cumulative-end failure, non-exact final exhaustion, noncanonical search helpers, unsorted/duplicate/out-of-range keys, and malformed values fail Font::open as Data."
    - "Zero, one, non-power-of-two, and power-of-two pair counts validate canonical helper fields and first/middle/last/miss lookup positions."
    - "Exact optional-directory-record work, kern limits, max_work, and budget admit; each one-short case rejects before its loop with the budget unchanged."
  </behavior>
  <action>
Implement the full D-08/D-10/D-10a byte-level classifier boundary. Fewer than 4 table bytes is Data. When the top-level `uint16` version is 0, require the complete 4-byte classic OpenType header, preflight `nTables`, and structurally walk every subtable: require a contained 6-byte header, `uint16 length >= 6`, checked cumulative end, and final cumulative end exactly equal to the table length. Any envelope failure is Data. Only after this structural pass classify subtable version/coverage/format/profile: exactly one version-0 coverage-`0x0001` format-0 subtable may be Supported; multiple supported subtables or any other structurally valid composition retains Unsupported.

Detect Apple v1 only by the exact top-level `uint32` value `0x00010000`. Require its complete 8-byte header, bound and preflight its `uint32 nTables`, then structurally walk every Apple subtable using a contained 8-byte header, `uint32 length >= 8`, checked cumulative end, and exact final table exhaustion. Envelope failure is Data; a structurally valid Apple table retains Unsupported, and no Apple format body is interpreted. Any other nonzero/unknown version with a complete 4-byte prefix retains Unsupported without attacker-driven traversal; truncation remains Data. Only `Font::kerning` turns retained Unsupported into Capability/CapabilityUnavailable with a stable kern-specific context; metrics and cmap remain usable.

For the supported profile, checked-derive exact subtable length `14 + 6*nPairs` and exact whole-table exhaustion, validate canonical searchRange/entrySelector/rangeShift for zero/one/non-power-of-two/power-of-two counts, require strictly increasing unique `(left << 16) | right` keys with both glyphs below `numGlyphs`, and decode signed adjustments. Navigation derives only from admitted `nPairs`.

Extend the existing declared-work discovery/aggregate charge so the optional lookup's one `directory.tables` scan adds retained `numTables` exactly once, is preflighted before iteration, and is charged once with kern subtable/pair work (D-13). Preflight classic and Apple `nTables` before their structural walks; preflight supported-profile `nPairs` before its pair walk. Add exact-fit/one-short tests dedicated to the directory-record scan, subtable count, pair count, `max_work`, and caller budget, asserting unchanged budget on atomic rejection and after successful queries.

Add D-15 public cases and D-16 white-box exact-length/helper/key/value/taxonomy cases for: classic tables shorter than 4 bytes; classic 4-byte header and `nTables`; each 6-byte subtable header, minimum length, cumulative end, and exact exhaustion; single supported versus multiple supported/out-of-profile compositions; exact Apple-v1 8-byte header, bounded count, each 8-byte subtable header/minimum length/cumulative end/exhaustion; structurally valid Apple Unsupported without body parsing; and complete unknown-version Unsupported without traversal.
  </action>
  <verify>
    <automated>moon -C modules/mb-font test --target native --frozen &amp;&amp; moon -C modules/mb-font info --target native --frozen</automated>
  </verify>
  <acceptance_criteria>
    - "Absent/miss zero, signed hit, unsupported capability, and malformed data are stable, mutually distinguishable public outcomes."
    - "The directory-record, classic/Apple subtable, and supported pair loops are preflighted and charged exactly once; exact-fit/one-short evidence proves atomicity."
    - "Search fields, lengths, pair ordering/uniqueness/ranges, and signed values are fully covered in private tests."
    - "Native tests pass and no private kern facts appear in the generated interface."
  </acceptance_criteria>
  <done>FONT-04 is complete on native with exact optional-table taxonomy, bounded admission, and deterministic pair lookup.</done>
</task>

</tasks>

## Artifacts this phase produces

| New symbol/file owned by this plan | Contract |
|---|---|
| `modules/mb-font/font/kern.mbt` | New private optional kern admission and lookup implementation |
| `font_optional_table_window` | Optional normalized lookup without required-table error conversion |
| `KernState`, `KernFormat0Facts` | Absent/supported/unsupported retained state and compact pair facts |
| `font_admit_kern`, `font_lookup_kern`, `font_kern_pair_key` | Atomic classifier, binary search, and exact key construction |
| `Font::kerning(GlyphId, GlyphId)` | Exact signed adjustment or neutral/distinct structured outcome |
| `FontLimits::max_kern_subtables`, `FontLimits::max_kern_pairs` | Explicit non-zero semantic ceilings and constructor parameters |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller font bytes -> optional kern admission | Attacker-controlled headers, lengths, counts, helper fields, keys, and values affect reads/work. |
| caller GlyphIds -> receiving Font | Opaque IDs can originate from another font and must be range-checked before key construction. |
| retained ByteView -> kerning result | Backing storage can mutate after admission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-98-02-01 | Tampering | malicious kern bytes in `kern.mbt` | high | mitigate | Require contained recognized envelopes, checked exact lengths, canonical helpers, strict unique key order, signed reads, and exact table exhaustion. |
| T-98-02-02 | Denial of Service | optional directory-record, declared subtable, and pair loops | high | mitigate | Add retained numTables once for the optional lookup, enforce explicit ceilings plus max_work/budget preflights before every loop, charge each scan once, and test exact fit/one-short. |
| T-98-02-03 | Tampering | retained source mutation | high | mitigate | Guard before validation/read and after lookup before publication; extend mutation and mutate-back coverage to kerning. |
| T-98-02-04 | Tampering | glyph-range/pair-key correctness | high | mitigate | Revalidate both opaque values against the receiving Font before `(left << 16) | right`; validate every admitted pair key and navigate from nPairs only. |
| T-98-02-05 | Spoofing | error-taxonomy confusion | high | mitigate | Retain Absent/Supported/Unsupported separately; zero only for absence/miss, capability only at query for well-formed unsupported, and data error during open for malformed recognized bytes. |
| T-98-02-06 | Tampering | partial publication | high | mitigate | Discover/classify/validate kern inside existing atomic `Font::open`; malformed paths return before the sole Font construction and consume no partial budget charge. |
| T-98-SC | Tampering | dependency surface | low | accept | No package is installed and `moon.pkg` remains unchanged with frozen `mb-core`-only imports. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Resolve basic legacy horizontal kerning deterministically | Tasks 1-3 | COVERED |
| REQ | FONT-04 | Version-0 format-0 pair query with zero absence/miss and distinct unsupported/malformed outcomes | Tasks 1-3 | COVERED |
| RESEARCH | — | Optional tri-state, exact envelope, pair binary search, signed FWORD, explicit limits | Tasks 1-3 | COVERED |
| CONTEXT | D-08,D-09 | Exact supported profile, two opaque IDs, signed hit, absence/miss zero | Task 1 | COVERED |
| CONTEXT | D-10,D-10a,D-11 | Capability/data split, deferred unsupported state, strict exact pair facts | Tasks 1,3 | COVERED |
| CONTEXT | D-12,D-13,D-14 | Atomic compact facts, explicit preflighted ceilings, allocation-free budget-neutral lookup | Tasks 1-3 | COVERED |
| CONTEXT | D-15,D-16 | Generated public hostile cases and private boundary/taxonomy cases | Tasks 1-3 | COVERED |

GPOS/GSUB, format 2, Apple extension semantics, multiple-subtable accumulation, vertical/minimum/cross-stream/override behavior, shaping, and scaling are explicit exclusions rather than gaps.
</source_audit>

<verification>
1. Run native package tests after each RED/GREEN cycle.
2. Compile the literate README after every FontLimits constructor update.
3. Inspect native generated interface for exactly the kerning method, two limit accessors, and expanded constructor; private states/facts must remain absent.
4. Confirm exact-fit/one-short semantic/max_work/budget cases leave rejection atomic and successful queries budget-neutral.
</verification>

<success_criteria>
- FONT-04 passes native public/private evidence for absent, miss, signed hit, unsupported, malformed, mutation, foreign IDs, and resource boundaries.
- Optional kern admission remains inside the single Font opening transaction and unsupported state does not block metrics/cmap.
- All public limit call sites explicitly supply non-zero kern ceilings.
- No external dependency, FFI, UI, schema, host discovery, shaping, GPOS, or outline capability is introduced.
</success_criteria>

<output>
Create `.planning/phases/98-unicode-mapping-and-kerning/98-02-SUMMARY.md` when done.
</output>
