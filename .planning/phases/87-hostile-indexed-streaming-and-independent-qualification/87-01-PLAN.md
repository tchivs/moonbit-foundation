---
phase: 87
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
requirements: [INDEXCOMP-04, INDEXCOMP-05]
must_haves:
  truths:
    - "D-01: A Fixed-winner and a Stored-fallback non-interlaced indexed selector reproduce fresh eager bytes under zero-capacity, one-byte, and ragged caller-owned leases; total_written advances only by accepted bytes and every rejected sentinel tail remains unchanged."
    - "D-02: Released leases, source-revision/replay-accounting mismatches, and terminal replay are sticky zero-write outcomes; Finished and Failed pulls leave later destinations unchanged."
    - "D-03: A test-local parser independently validates eager and collected chunk-origin Type-3 bytes, PLTE/tRNS canonicalisation, Fixed-or-Stored DEFLATE selection, filter-None packed rows/tails, Adler-32, CRCs, and public RGB8/RGBA8 decode without production planning/matching/packing/frame helpers."
    - "D-04: The qualification retains a compact deterministic 512-pixel Fixed/Stored corpus plus odd/narrow partial-alpha fixtures and no external binary fixtures."
    - "D-05: Legacy non-interlaced Indexed1/2/4/8 Stored vectors and all existing indexed Adam7 Stored/None vectors remain byte-frozen, and the ordinary PNG package gate passes for native, wasm, wasm-gc, and js."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Hostile lease matrix, independent eager and chunk-origin parser/decode oracle, compatibility vectors, and public Indexed1/2/4/8 qualification."
      contains: "new_indexed_with_compression_strategy"
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: "Mandatory white-box replay/fingerprint and terminal-accounting assertions for the admitted indexed machine."
      contains: "indexed"
  key_links:
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngChunkEncoder indexed compression constructors feed caller-owned leases through present/acknowledge."
      pattern: "new_indexed_with_compression_strategy"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/encode_wbtest.mbt
      via: "Test-local corpus independently parses fresh eager bytes and separately collected chunk-origin bytes without production planning helpers."
      pattern: "png_indexed_compression_matrix_source"
    - from: modules/mb-image/png/stream_encode_wbtest.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "White-box checks observe sticky replay revision/work errors at EOB without changing the machine."
      pattern: "replay"
---

<objective>
Qualify the Phase 86 admitted non-interlaced indexed Fixed-or-Stored machine
under hostile caller leases and independent wire/decode evidence, preserving
all frozen compatibility routes and declared target coverage.

Purpose: turn the existing eager/chunk parity and atomic-admission evidence
into externally observable lifecycle, framing, raster, decode, and portability
proof without adding production architecture.

Output: test-only qualification in `stream_encode_test.mbt` and, only where
existing white-box seams require it, `stream_encode_wbtest.mbt`.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-hostile-indexed-streaming-and-independent-qualification/87-CONTEXT.md
@.planning/phases/87-hostile-indexed-streaming-and-independent-qualification/87-RESEARCH.md
@.planning/phases/87-hostile-indexed-streaming-and-independent-qualification/87-PATTERNS.md
@.planning/phases/86-ancillary-aware-preflight-and-shared-machine-integration/86-VERIFICATION.md
@.planning/milestones/v0.27-phases/84-low-bit-indexed-adam7-streaming-qualification/84-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TDD tracer for one Fixed-winner hostile indexed stream</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: read `png_chunk_test_pull`, `png_chunk_test_drain_encoder`, sentinel-owner helpers, and existing sticky/released-lease tests.
    - `modules/mb-image/png/encode_test.mbt`: read the Phase 85/86 Fixed-winner Indexed2 source and eager compression helper.
    - `modules/mb-image/png/stream_encode.mbt`: read public indexed compression constructors and acknowledged `present`/`acknowledge` lifecycle; do not edit this file.
  </read_first>
  <behavior>
    - RED: register the exact focused test `PNG indexed compression hostile tracer FixedOrStored` that currently fails because this qualification is absent.
    - GREEN: construct only the existing `new_indexed_with_compression_strategy` route, drain zero-capacity, one-byte, and a short ragged schedule, compare accepted bytes to a fresh eager result, and assert untouched sentinel tails and accepted-only totals.
    - Terminal: after completion, a later sentinel lease reports zero-write `Finished`; a released first lease reports zero-write `Failed` and the same cached failure on replay.
  </behavior>
  <action>
    Per D-01 and D-02, add the smallest end-to-end test fixture for the canonical 512-pixel Fixed
    winner. Use the existing sentinel-backed owner and borrowed zero-capacity
    lease pattern; never allocate a second stream buffer that could hide
    acknowledgement bugs. For each pull assert `written <= capacity`,
    `total_written == previous_total + written`, only accepted positions enter
    the collected bytes, and all unaccepted positions remain the sentinel.
    Compare the collected result with a fresh eager `FixedOrStored` output only
    as a lifecycle identity check. Add terminal `Finished` replay and released
    lease sticky-failure checks. If a white-box replay seam is already exposed,
    record the typed error but keep parser work for Task 2. Keep the change
    test-only and do not call production packing/planning helpers as an oracle.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter "PNG indexed compression hostile tracer FixedOrStored"</automated>
  </verify>
  <acceptance_criteria>
    One Fixed-winner Indexed2 stream passes zero/one/ragged hostile schedules,
    accepted-only progress, untouched tails, released-lease sticky failure, and
    zero-write terminal replay against fresh eager bytes.
  </acceptance_criteria>
  <done>One production-quality hostile lifecycle tracer exists with no production file changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand independent qualification, compatibility, and targets</name>
  <files>modules/mb-image/png/stream_encode_test.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: inspect Task 1 and Phase 84 independent parser/lease analogs before extending helpers.
    - `modules/mb-image/png/encode_wbtest.mbt`: reuse only literal corpus facts; do not call production preflight, matcher, packer, frame-facts, or row helpers from the oracle.
    - `modules/mb-image/png/png_test.mbt`: read test-local CRC/Adler/chunk parsing and public RGB8/RGBA8 decoder assertions.
    - `modules/mb-image/png/moon.pkg`: confirm `+js+wasm+wasm-gc+native` target declaration.
  </read_first>
  <behavior>
    - Indexed1/2/4/8 Fixed winners and Stored fallbacks survive zero-capacity, one-byte, and ragged schedules with accepted-only totals, sentinel tails, sticky release/replay failures, and zero-write Finished/Failed pulls.
    - A test-local parser consumes fresh eager bytes and separately collected chunk-origin bytes and proves signature, IHDR Type-3 fields, chunk order/lengths, PLTE, shortest canonical tRNS, IDAT DEFLATE block type, Adler-32, CRCs, filter-None rows, MSB-first packed values, and zero low tail bits before parity comparison.
    - Public RGB8 decode is checked for opaque sources and RGBA8 for partial-alpha sources at every source coordinate, including odd/narrow final rows.
    - Explicit Stored output equals its corresponding legacy non-interlaced API byte-for-byte; existing indexed Adam7 Stored/None vectors stay frozen.
    - Named ordinary package gates pass with the new tests registered for native, wasm, wasm-gc, and js; an `--target all` aggregate is supplementary, and any unavailable target is recorded with concrete toolchain evidence.
  </behavior>
  <action>
    Per D-03, D-04, and D-05, parameterize the hostile harness over the canonical Fixed-winner/Stored-
    fallback matrix and retain a compact odd/narrow partial-alpha fixture for
    packed tails and RGBA8 semantics. Parse fresh eager bytes independently,
    then parse separately collected chunk-origin bytes, and only then compare
    them for parity. Implement bounded test-local chunk slicing, CRC-32,
    Adler-32, zlib header and BTYPE inspection, and a minimal test-local
    Stored/Fixed-Huffman inflater sufficient to reconstruct the raw packed rows;
    expected values must be local literals or local arithmetic and must not call
    production planning/matching/packing/frame-facts/preflight functions. Extend
    parser checks through IEND and public decode. Add compatibility assertions
    for legacy non-interlaced Indexed1/2/4/8 and existing Indexed Adam7
    Stored/None vectors without changing those vectors. In
    `stream_encode_wbtest.mbt`, make indexed replay-work/fingerprint drift a
    mandatory white-box test: induce the existing ledger mismatch, assert the
    typed error is sticky, zero-write, and leaves later sentinel destinations
    unchanged. Record explicitly if the immutable indexed source-revision guard
    cannot be induced without production mutation. Keep all edits in the two
    planned test files; no second encoder, staging, FFI, wrapper, target
    adapter, copied source tree, release script, or new public API.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
    <automated>moon -C modules/mb-image test png --target wasm --frozen</automated>
    <automated>moon -C modules/mb-image test png --target wasm-gc --frozen</automated>
    <automated>moon -C modules/mb-image test png --target js --frozen</automated>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <acceptance_criteria>
    All four non-interlaced indexed depths have independent eager and
    chunk-origin framing/DEFLATE/raster/CRC/Adler/decode proof, hostile lifecycle and sticky
    terminal coverage, frozen legacy/Adam7 compatibility, and a passing
    four-target package gate.
  </acceptance_criteria>
  <done>INDEXCOMP-04 and INDEXCOMP-05 are satisfied by test-only evidence; production architecture and deferred scope are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned lease → `PngChunkEncoder::pull` | Mutable destination/capacity must not be written beyond acknowledged bytes or after terminal failure. |
| collected PNG bytes → test-local parser | Parser must bound all chunk lengths and validate CRC/DEFLATE/raster facts before certification. |
| source revision/replay ledger → terminal state | Drift must fail closed and remain sticky rather than emit a wrong final stream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Tampering | Caller lease | medium | mitigate | Zero/one/ragged schedules, sentinel tails, accepted-only totals, and terminal replay checks. |
| T-87-02 | Tampering | Test-local parser | medium | mitigate | Independent bounded chunk/CRC/Adler/DEFLATE/raster checks and public decode. |
| T-87-03 | Tampering | Replay accounting | medium | mitigate | Released/revision/work drift must produce sticky zero-write failures; no wrong final bytes accepted. |
</threat_model>

<verification>
Run the focused hostile tracer first on native, then the named ordinary PNG
package gate separately for `native`, `wasm`, `wasm-gc`, and `js`, followed by
the `--target all` aggregate. Record each command's pass/failure or concrete
unavailable-target evidence. Every gate must exercise the new qualification and
the frozen non-interlaced/Adam7 indexed vectors.
</verification>

<success_criteria>
- INDEXCOMP-04: accepted-only hostile lifecycle, untouched tails, sticky
  release/replay failures, and zero-write terminal pulls are proven.
    - INDEXCOMP-05: independent eager and collected-byte Type-3/DEFLATE/raster/decode proof,
  compatibility vectors, and all four target gates pass.
- Only planned test artifacts change; no production architecture or deferred
  capability changes.
</success_criteria>

## Artifacts this phase produces

- `modules/mb-image/png/stream_encode_test.mbt` — hostile lease, parser,
  compatibility, and public decode qualification.
- `modules/mb-image/png/stream_encode_wbtest.mbt` — mandatory white-box replay
  accounting and sticky drift checks using existing private seams.

<output>
Create `.planning/phases/87-hostile-indexed-streaming-and-independent-qualification/87-01-SUMMARY.md` when done.
</output>
