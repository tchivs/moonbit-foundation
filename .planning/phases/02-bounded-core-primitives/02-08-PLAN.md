---
phase: 02-bounded-core-primitives
plan: "08"
type: execute
wave: 8
depends_on: ["02-07"]
files_modified:
  - modules/mb-core/bytes/views.mbt
  - modules/mb-core/bytes/bytes_test.mbt
  - modules/mb-core/bytes/bytes_wbtest.mbt
  - modules/mb-core/README.mbt.md
  - policy/foundation.json
autonomous: true
gap_closure: true
requirements: [CORE-02]
must_haves:
  truths:
    - "D-04/D-06: callback exit invalidates the entire mutable lease group, including every live child or deeper descendant created by split_mut."
    - "Normal and structured-error callback exits restore owner availability exactly once even when split children are retained and never explicitly released."
    - "Every retained descendant rejects get, set, split_mut, and repeated release after callback cleanup, while a later OwnedBytes::with_mut call succeeds."
    - "The documented split-lease flow proves post-callback reacquisition and the root Required lane, including its fail-closed negative fixtures, passes unchanged on all four targets."
  artifacts:
    - path: "modules/mb-core/bytes/views.mbt"
      provides: "LeaseGroup-wide callback cleanup and descendant invalidation"
      contains: "LeaseGroup"
    - path: "modules/mb-core/bytes/bytes_test.mbt"
      provides: "Public normal/error split-callback lifecycle regressions"
    - path: "modules/mb-core/bytes/bytes_wbtest.mbt"
      provides: "Internal nested descendant, release-once, stale-handle, and reacquisition invariants"
    - path: "modules/mb-core/README.mbt.md"
      provides: "Executable split example that demonstrates later mutable reacquisition"
  key_links:
    - from: "modules/mb-core/bytes/views.mbt"
      to: "LeaseGroup"
      via: "LeaseOwner::with_mut defers group cleanup rather than parent-handle cleanup"
      pattern: "defer.*group"
    - from: "modules/mb-core/bytes/views.mbt"
      to: "MutByteLease::checked_index"
      via: "every descendant access checks both handle activity and group scope activity"
      pattern: "group"
    - from: "modules/mb-core/bytes/bytes_test.mbt"
      to: "OwnedBytes::with_mut"
      via: "normal and Err split callbacks retain children, verify staleness, then reacquire"
      pattern: "with_mut"
---

<objective>
Close the verified callback-scoped split-lease lifecycle gap without changing the public byte API.

Purpose: A split inside `OwnedBytes::with_mut` must not leave the owner permanently leased when callers omit explicit child release, and no descendant may remain usable after callback scope ends.
Output: Group-wide cleanup in `views.mbt`, public/internal lifecycle regression tests, an honest executable README example, and a passing root Required gate with negative fixtures.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-core-primitives/02-CONTEXT.md
@.planning/phases/02-bounded-core-primitives/02-VERIFICATION.md
@.planning/phases/02-bounded-core-primitives/02-04-SUMMARY.md
@modules/mb-core/bytes/views.mbt
@modules/mb-core/bytes/bytes_test.mbt
@modules/mb-core/bytes/bytes_wbtest.mbt
@modules/mb-core/bytes/pkg.generated.mbti
@modules/mb-core/README.mbt.md
</context>

## Artifacts this phase produces

- Modified `views.mbt`; no new public symbol is expected. `LeaseGroup`, `LeaseState`, `LeaseOwner::with_mut`, `MutByteLease::checked_index`, `release`, and `split_mut` gain coordinated group-scope lifecycle behavior behind the existing public API.
- Modified `bytes_test.mbt` and `bytes_wbtest.mbt` with normal-exit, `Err`-exit, nested split-descendant, stale operation, idempotent release, exact-once owner restoration, and successful reacquisition regressions.
- Modified `README.mbt.md` so the public split example performs and verifies a later `with_mut` acquisition.
- `policy/foundation.json` and package classifiers are not modified unless the generated public interface or package contents actually change; this internal fix is expected to preserve both exactly.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make callback cleanup invalidate the full lease group</name>
  <files>modules/mb-core/bytes/views.mbt, modules/mb-core/bytes/bytes_test.mbt, modules/mb-core/bytes/bytes_wbtest.mbt</files>
  <read_first>02-VERIFICATION.md gaps/reason/missing sections; views.mbt LeaseOwner::with_mut, LeaseGroup, checked_index, release, split_mut; existing callback and split tests</read_first>
  <behavior>
    - Public normal exit: split inside `OwnedBytes::with_mut`, retain both children, omit child release, exit `Ok`, verify both children reject access/split, then reacquire and mutate successfully.
    - Public error exit: split, retain children, return a structured `Err`, verify the original error is preserved, all descendants are stale, repeated release is harmless, and a later `with_mut` succeeds.
    - Internal nested split: split a child again, leave every descendant unreleased, ensure callback cleanup invalidates the whole shared group and restores `owner.leased` exactly once.
    - Mixed cleanup: explicitly release zero, one, or all children before callback exit; group cleanup remains idempotent and never underflows live-handle accounting or reactivates a stale handle.
  </behavior>
  <action>Write the failing lifecycle regressions first. Refactor the private lease state so the callback owns a cleanup operation on the shared `LeaseGroup`, not on the parent handle that `split_mut` consumes. The group must track whether its callback scope is active and whether owner availability has already been restored; cleanup atomically marks the scope inactive, invalidates all current and future descendant operations through the shared group state, normalizes live-handle accounting, and clears `owner.leased` once. Make `checked_index`, `split_mut`, and `release` honor group-scope inactivity as well as per-handle activity. Preserve standalone private `acquire_mut`/explicit-release behavior used by white-box tests, preserve D-04/D-06 opaque backing and exclusivity, and preserve the existing public signatures.</action>
  <acceptance_criteria>Normal and `Err` callback exits invalidate all split descendants, stale get/set/split operations return the existing structured inactive-lease error, repeated child/parent releases are no-ops, nested descendants cannot escape, and immediate subsequent mutable acquisition succeeds. No live-handle count becomes negative and owner availability changes at most once per group cleanup.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test bytes --target all --frozen</automated></verify>
  <done>The verified CORE-02 lifecycle truth holds for unsplit, split, nested-split, explicitly released, unreleased, normal, and error callback paths.</done>
</task>

<task type="auto">
  <name>Task 2: Prove public documentation, exact artifacts, and negative gates remain truthful</name>
  <files>modules/mb-core/README.mbt.md, policy/foundation.json</files>
  <read_first>README split-lease example; generated bytes interface; policy bytes interface/package records; Required negative fixture stage</read_first>
  <action>Extend the existing README split example with a second `OwnedBytes::with_mut` call after the first callback exits, and assert that reacquisition and mutation succeed without explicit release of the split children. Regenerate/compare the bytes interface and package list before touching policy: because the fix is private, keep exact interface/package policy unchanged when outputs match; update those records only if the implementation necessarily changes an intentional public artifact, and document that reason in the summary. Run the exact target-qualified README checks, bytes tests, and the root Required lane so its topology, surface, backing, narrowing, ambient-host, OOM-prose, and missing-README negative fixtures all reject as expected.</action>
  <acceptance_criteria>The README demonstrates post-split reacquisition on js, wasm, wasm-gc, and native; generated bytes interface/package contents remain exact or have a justified matching policy update; root Required reaches and passes the CORE fail-closed negative-fixture stage and tracked-read-only proof.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>The public example catches the original defect, exact policy remains truthful, and the complete Required plus negative-fixture contract passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Callback body -> retained lease descendants | Callback-controlled children must lose mutation authority on every scope exit. |
| Lease group -> owner availability | Cleanup must restore exclusivity exactly once without premature or repeated release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-08-01 | Tampering / Elevation of Privilege | retained split descendants | high | mitigate | shared group-scope activity checked by every access and split operation |
| T-02-08-02 | Denial of Service | owner leased state | high | mitigate | deferred group-wide exact-once cleanup on normal and structured-error exits |
| T-02-08-03 | Tampering | live-handle accounting | medium | mitigate | idempotent mixed-release and nested-split regression matrix |
</threat_model>

<verification>
First run the focused bytes suite on all four targets. Then run the target-qualified README checks and the root Required lane; confirm its fail-closed negative fixtures execute and reject every injected invalid topology/surface/source/docs case. Re-run phase verification after execution to close the sole `gaps:` entry in `02-VERIFICATION.md`.
</verification>

<success_criteria>
- Callback cleanup targets the shared lease group rather than the consumed parent handle.
- Normal and error exits invalidate every retained split descendant and permit reacquisition.
- Regression tests fail on the verified old implementation and pass on all required targets after the fix.
- Public API, exact interface, package contents, and unrelated Phase 2 behavior remain unchanged.
- Root Required and its negative fixtures pass without modifying unrelated governance, state, roadmap, or caches.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-core-primitives/02-08-SUMMARY.md` when done.
</output>
