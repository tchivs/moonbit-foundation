---
phase: 02-bounded-core-primitives
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [modules/mb-core/budget/moon.pkg, modules/mb-core/budget/budget.mbt, modules/mb-core/budget/budget_test.mbt, modules/mb-core/budget/budget_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [CORE-07]
must_haves:
  truths: ["D-15/D-16: multidimensional charges preflight completely, commit atomically, reject without consumption, and child scopes share parent state with balanced depth.", "Budget package and exact policy/classifiers leave root Required green."]
  artifacts:
    - { path: "modules/mb-core/budget/budget.mbt", provides: "ResourceLimits, ResourceCharge, Budget, BudgetScope, charge, enter_depth, leave" }
  key_links:
    - { from: "modules/mb-core/budget/moon.pkg", to: "modules/mb-core/checked", via: "checked logical counters and structured errors", pattern: "mb-core/checked" }
  assumptions:
    - { id: EDGE-CORE-07-THRESHOLD, statement: "Exact remaining charge succeeds; next positive unit fails unchanged.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-07-ADJACENCY, statement: "Adjacent charges compose to the limit and multidimensional failure rolls back all fields.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-07-EMPTY, statement: "Zero charge and empty balanced scope are no-ops.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-07-ORDERING, statement: "All counters/depth validate before ledger mutation or work.", verification: white-box-review, flagged: true }
    - { id: EDGE-CORE-07-PRECISION, statement: "UInt64 precision and boundaries match across all targets.", verification: four-target-test, flagged: true }
---
<!-- phase-source-audit: CORE-07,D-15,D-16,EDGE-CORE-07-THRESHOLD,EDGE-CORE-07-ADJACENCY,EDGE-CORE-07-EMPTY,EDGE-CORE-07-ORDERING,EDGE-CORE-07-PRECISION -->
<objective>Deliver the third locked package: atomic hierarchical resource budgets before byte allocation/work.
Purpose: hostile workloads cannot partially consume or reset limits.
Output: public `budget`, tests, exact policy entry, green Required.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-core-primitives/02-CONTEXT.md
@.planning/phases/02-bounded-core-primitives/02-RESEARCH.md
@.planning/phases/02-bounded-core-primitives/02-02-SUMMARY.md</context>
## Artifacts this phase produces

- `budget/moon.pkg`, `budget.mbt` (`ResourceLimits`, `ResourceCharge`, `Budget`, `BudgetScope`, `charge`, `enter_depth`, `leave`), public/internal tests, updated policy entry.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement atomic shared resource budgets</name>
  <files>modules/mb-core/budget/moon.pkg, modules/mb-core/budget/budget.mbt, modules/mb-core/budget/budget_test.mbt, modules/mb-core/budget/budget_wbtest.mbt</files>
  <read_first>D-15/D-16; research Pattern 4/Pitfalls 4-5; checked/error interfaces</read_first>
  <behavior>Tests first cover all five edge rows, bytes/allocation count/size/dimensions-pixels/depth/work, child narrowing, parent sharing, and balanced success/error/early-exit cleanup.</behavior>
  <action>Implement one opaque shared ledger. Validate the complete `ResourceCharge` before changing any field, share it with child scopes, permit children only to narrow allowance, and balance nesting deterministically with cleanup per D-15/D-16.</action>
  <acceptance_criteria>Rejected charges mutate no field, children cannot reset/bypass parents, and every edge row passes on all targets.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test budget --target all --frozen</automated></verify>
  <done>CORE-07 pre-work limits are deterministic and atomic.</done>
</task>
<task type="auto">
  <name>Task 2: Register budget and prove the green exit</name>
  <files>policy/foundation.json</files>
  <read_first>budget interface/package list; current incremental policy</read_first>
  <action>Add exact budget identity, targets, earlier-package imports, interface, and contents; retain prior entries; run root Required.</action>
  <acceptance_criteria>Policy lists root/error/checked/budget exactly and Required passes all four targets plus exact artifacts.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Plan 02-03 exits green atomically.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Denial of Service | budget ledger | high | mitigate | atomic preflight/commit and shared child state |
</threat_model>
<verification>All-target budget tests then root Required.</verification>
<success_criteria>CORE-07 and all five budget edge probes are covered without partial consumption or precision drift.</success_criteria>
<output>Create `.planning/phases/02-bounded-core-primitives/02-03-SUMMARY.md` when done.</output>
