---
phase: 02-bounded-core-primitives
plan: "06"
type: execute
wave: 6
depends_on: ["02-05"]
files_modified: [modules/mb-core/host/moon.pkg, modules/mb-core/host/capabilities.mbt, modules/mb-core/host/fakes.mbt, modules/mb-core/host/host_test.mbt, modules/mb-core/host/host_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [CORE-08]
must_haves:
  truths: ["D-17/D-18: files/resources, diagnostics, clock, cancellation, and resolution are granular explicit optional traits with deterministic portable doubles and no ambient fallback/native adapter.", "Host topology and classifiers leave Required green."]
  artifacts:
    - { path: "modules/mb-core/host/capabilities.mbt", provides: "FileCapability, DiagnosticSink, Clock, Cancellation, ResourceResolver" }
    - { path: "modules/mb-core/host/fakes.mbt", provides: "deterministic independent capability doubles" }
  key_links:
    - { from: "modules/mb-core/host/fakes.mbt", to: "modules/mb-core/host/capabilities.mbt", via: "one explicit fake per open trait", pattern: "impl" }
  assumptions:
    - { id: EDGE-CORE-08-CONCURRENCY, statement: "Explicit instances compose under deterministic reentrant/interleaved calls without global state or unproven cross-thread guarantee.", verification: adversarial-test, flagged: true }
  prohibitions:
    - { id: PROH-CORE-08-AMBIENT-FALLBACK, requirement_id: CORE-08, category: capability-boundary, statement: "MUST NOT read process-global host state or define concrete native adapters or a mandatory all-capabilities singleton.", status: unverified, verification: "Required exact interface/source scans and explicit-fake tests", flagged: true }
---
<!-- phase-source-audit: CORE-08,D-17,D-18,EDGE-CORE-08-CONCURRENCY,PROH-CORE-08-AMBIENT-FALLBACK -->
<objective>Deliver the sixth locked package: granular explicitly injected host capability contracts and portable doubles.
Purpose: portable consumers receive only authorized effects without ambient process state.
Output: public `host`, exact policy entry, green Required.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-core-primitives/02-CONTEXT.md
@.planning/phases/02-bounded-core-primitives/02-RESEARCH.md
@.planning/phases/02-bounded-core-primitives/02-PATTERNS.md
@.planning/phases/02-bounded-core-primitives/02-05-SUMMARY.md</context>
## Artifacts this phase produces

- `host/moon.pkg`; `capabilities.mbt` (`FileCapability`, `DiagnosticSink`, `Clock`, `Cancellation`, `ResourceResolver`); `fakes.mbt` per-capability doubles; public/internal tests; updated policy.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement granular host traits and deterministic doubles</name>
  <files>modules/mb-core/host/moon.pkg, modules/mb-core/host/capabilities.mbt, modules/mb-core/host/fakes.mbt, modules/mb-core/host/host_test.mbt, modules/mb-core/host/host_wbtest.mbt</files>
  <read_first>D-17/D-18; research responsibility map/security domain; io/error interfaces</read_first>
  <behavior>Tests first instantiate/omit each capability independently, compose clock/log/cancellation/resource fakes, map failures, bound context, and interleave instances without shared state.</behavior>
  <action>Define the listed small open traits and explicit bounded portable fakes, one capability apiece. Consumers receive only traits they need. Map errors at adapter boundaries. Do not add a global default, Host aggregate, or concrete filesystem/clock/process/network adapter per D-17/D-18.</action>
  <acceptance_criteria>Capabilities are individually optional/composable, omitted capabilities have no fallback, fakes are instance-local/deterministic, and deferred native scope is absent.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test host --target all --frozen</automated></verify>
  <done>CORE-08 explicit capability injection is complete.</done>
</task>
<task type="auto">
  <name>Task 2: Register host and prove the green exit</name>
  <files>policy/foundation.json</files>
  <read_first>host interface/package output; current policy</read_first>
  <action>Add exact host identity, targets, earlier imports, interface, and contents; retain root plus all five prior packages; run Required.</action>
  <acceptance_criteria>Policy exactly includes host, ambient/native surfaces are absent, and root Required passes.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Plan 02-06 exits green atomically with all six spine packages present.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-10 | Elevation of Privilege / Disclosure | host boundary | high | mitigate | granular explicit traits, no ambient fallback, bounded error mapping |
</threat_model>
<verification>All-target host tests plus root Required.</verification>
<success_criteria>CORE-08, concurrency probe, and ambient prohibition are explicit; I/O and host remain independent sequential contexts.</success_criteria>
<output>Create `.planning/phases/02-bounded-core-primitives/02-06-SUMMARY.md` when done.</output>
