---
phase: 02-bounded-core-primitives
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [modules/mb-core/checked/moon.pkg, modules/mb-core/checked/checked.mbt, modules/mb-core/checked/range.mbt, modules/mb-core/checked/dimensions.mbt, modules/mb-core/checked/checked_test.mbt, modules/mb-core/checked/checked_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "D-01: addition, subtraction/underflow, multiplication, alignment, offsets, narrowing, dimensions, ranges, and allocation sizes return structured failures before operators/effects."
    - "D-02/D-03: half-open empty ranges and explicit-width logical quantities remain portable; backend narrowing never truncates."
    - "Checked package, policy inventory, and root Required classifiers exit green atomically."
  artifacts:
    - { path: "modules/mb-core/checked/checked.mbt", provides: "checked_add, checked_sub, checked_mul, align_up, checked_narrow_int, checked_allocation_size, CheckedOffset" }
    - { path: "modules/mb-core/checked/range.mbt", provides: "opaque CheckedRange" }
    - { path: "modules/mb-core/checked/dimensions.mbt", provides: "opaque Dimensions and checked pixel count" }
  key_links:
    - { from: "modules/mb-core/checked/moon.pkg", to: "modules/mb-core/error", via: "sole structured failure dependency", pattern: "mb-core/error" }
  assumptions:
    - { id: EDGE-CORE-01-CHECKED-ADJACENCY, statement: "Below/exact/above arithmetic and cast boundaries are distinct tests.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-01-CHECKED-EMPTY, statement: "Zero operands, zero offset, and zero sizes succeed canonically.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-01-CHECKED-ORDERING, statement: "Guards precede addition, subtraction, multiplication, alignment, narrowing, allocation, and work.", verification: white-box-review, flagged: true }
    - { id: EDGE-CORE-01-RANGE-ADJACENCY, statement: "Touching half-open ranges are non-overlapping and exact end is accepted.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-01-RANGE-EMPTY, statement: "Zero-length ranges/subranges are valid at every legal boundary.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-01-RANGE-ORDERING, statement: "start plus length is checked before range construction.", verification: white-box-review, flagged: true }
---
<!-- phase-source-audit: CORE-01,D-01,D-02,D-03,EDGE-CORE-01-CHECKED-ADJACENCY,EDGE-CORE-01-CHECKED-EMPTY,EDGE-CORE-01-CHECKED-ORDERING,EDGE-CORE-01-RANGE-ADJACENCY,EDGE-CORE-01-RANGE-EMPTY,EDGE-CORE-01-RANGE-ORDERING -->

<objective>Implement the second locked package: complete checked arithmetic, offset, range, dimension, and narrowing contracts.
Purpose: no later access/allocation/work can rely on wrapping or backend-width behavior.
Output: public `checked`, exact policy entry, and green root Required.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-core-primitives/02-CONTEXT.md
@.planning/phases/02-bounded-core-primitives/02-RESEARCH.md
@.planning/phases/02-bounded-core-primitives/02-PATTERNS.md
@.planning/phases/02-bounded-core-primitives/02-01-SUMMARY.md</context>

## Artifacts this phase produces

- `checked/moon.pkg`; `checked.mbt` (`checked_add`, `checked_sub`, `checked_mul`, `align_up`, `checked_narrow_int`, `checked_allocation_size`, `CheckedOffset`); `range.mbt` (`CheckedRange`); `dimensions.mbt` (`Dimensions`); public/internal tests; updated policy entry.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement checked arithmetic, subtraction, offsets, and narrowing</name>
  <files>modules/mb-core/checked/moon.pkg, modules/mb-core/checked/checked.mbt, modules/mb-core/checked/checked_test.mbt, modules/mb-core/checked/checked_wbtest.mbt</files>
  <read_first>D-01..D-03; research Pattern 1/Pitfall 1; error interface; pinned UInt64/Int constants</read_first>
  <behavior>Tests first cover zero subtraction, exact equality, one-unit underflow, zero/max offsets, addition/multiplication overflow, alignment validity, allocation products, and exact/one-over backend narrowing.</behavior>
  <action>Implement guard-before-operator UInt64 functions including concrete `checked_sub` and opaque validated `CheckedOffset` arithmetic. Underflow and invalid offsets use structured error codes distinct from overflow/range errors. Only `checked_narrow_int` may perform direct logical-to-index conversion. Cite and enforce D-01/D-03.</action>
  <acceptance_criteria>Zero and exact-boundary subtraction succeed, underflow fails before subtraction, offset movement never wraps, and every operator/conversion follows a non-overflowing precondition guard.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test checked --target all --frozen</automated></verify>
  <done>Structured overflow, underflow, invalid offset/alignment, and narrowing APIs are complete.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add half-open ranges and dimensions</name>
  <files>modules/mb-core/checked/range.mbt, modules/mb-core/checked/dimensions.mbt, modules/mb-core/checked/checked_test.mbt, modules/mb-core/checked/checked_wbtest.mbt</files>
  <read_first>Task 1 APIs; D-02; six recorded CORE-01 edge assumptions</read_first>
  <behavior>Tests first cover empty boundaries, exact adjacency, relative subranges, overflowed start-plus-length, zero/invalid dimensions, and checked pixel/allocation size.</behavior>
  <action>Implement opaque `CheckedRange` and `Dimensions`; derive range end only through checked addition and pixel/allocation products only through checked multiplication/narrowing. Preserve valid empty ranges and explicit UInt64 logical values per D-02/D-03.</action>
  <acceptance_criteria>Invalid states are not publicly constructible and every recorded checked/range edge row has a named test.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test checked --target all --frozen</automated></verify>
  <done>CORE-01 range/dimension/size behavior is portable and effect-free on failure.</done>
</task>
<task type="auto">
  <name>Task 3: Register checked and preserve a green root exit</name>
  <files>policy/foundation.json</files>
  <read_first>checked generated interface/package list; current incremental policy schema</read_first>
  <action>Add the exact checked identity, targets, error-only import, public symbols, and contents to policy, retaining root and error entries. Run Required; do not accept broad wildcards or an undeclared topology interval.</action>
  <acceptance_criteria>Policy lists root/error/checked exactly and root Required passes all four targets plus interfaces/packages/docs/read-only proof.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Plan 02-02 exits green with checked code and classifiers synchronized.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering / DoS | arithmetic/ranges | high | mitigate | guard-before-operator and exact boundary tests |
| T-02-04 | Tampering | package DAG | medium | mitigate | exact error-only import policy |
</threat_model>
<verification>All-target checked tests plus root Required.</verification>
<success_criteria>CORE-01 includes explicit subtraction/underflow and offset artifacts/tests; six edge probes remain explicit; atomic green exit holds.</success_criteria>
<output>Create `.planning/phases/02-bounded-core-primitives/02-02-SUMMARY.md` when done.</output>
