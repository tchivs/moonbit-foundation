---
phase: 94-svg-benchmark-evidence
plan: 02
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1
  - docs/benchmarks/mb-svg-native-release-baseline.md
autonomous: true
requirements: [SVGPR-04]
must_haves:
  truths:
    - "A maintainer can generate one native release warmup and seven separately recorded native release captures using one exact frozen command."
    - "The baseline binds each workload's corpus and correctness digests, benchmark/package source digests, toolchain facts, truthful host facts, raw outputs, and per-workload summary statistics."
    - "The script's read-only -Audit mode accepts only a clean worktree, re-parses the generated Markdown, and independently validates every stated digest, runner summary, and seven-sample aggregate."
    - "A future record is comparable only when its workload identity, command, source/corpus/correctness digests, toolchain, release target, and host facts agree."
  artifacts:
    - path: scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1
      provides: "PowerShell-native capture and Markdown rendering command"
      contains: "moon bench modules/mb-svg/svg --release --target native --frozen"
    - path: docs/benchmarks/mb-svg-native-release-baseline.md
      provides: "Actual native release warmup and seven-capture evidence"
      contains: "path-parse/1000-line-to"
  key_links:
    - from: scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1
      to: modules/mb-svg/svg/svg_bench.mbt
      via: "source digest plus deterministic corpus/correctness-contract records"
      pattern: "svg_bench\\.mbt"
    - from: scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1
      to: docs/benchmarks/mb-svg-native-release-baseline.md
      via: "UTF-8 Markdown generation and read-only audit after one warmup and seven successful native release invocations"
      pattern: "Capture 7"
---

<objective>
Capture and independently audit a reproducible, native-only release baseline for the completed SVG workload suite without turning evidence into a threshold or CI policy.

Purpose: Preserve the exact local conditions and outputs necessary for a future like-for-like native observation.
Output: A narrow PowerShell capture/audit generator and the generated Markdown baseline with one excluded warmup and seven retained captures.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-svg-benchmark-evidence/94-CONTEXT.md
@.planning/phases/94-svg-benchmark-evidence/94-RESEARCH.md
@.planning/phases/94-svg-benchmark-evidence/94-01-PLAN.md
@docs/policies/toolchain.md
@policy/foundation.json
@modules/mb-svg/svg/svg_bench.mbt
@modules/mb-svg/svg/moon.pkg
@scripts/benchmarks/Invoke-PpmBenchmarks.ps1
@benchmarks/ppm/phase-11-resize-composite-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a PowerShell 5.1-safe native capture and read-only audit generator</name>
  <reversibility rating="costly">Per D-04, the fixed capture schema and comparison identity become the durable reference for future like-for-like native observations.</reversibility>
  <files>scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1</files>
  <action>Per D-03: keep native release observations distinct from the four-target runnable qualification. Per D-04: create `Invoke-SvgNativeBenchmarkBaseline.ps1` as a self-contained evidence generator with `[CmdletBinding()] param([switch]$Audit)`, modeled only on the PPM script's safe process-output and statistics techniques. Resolve the repository root from `$PSScriptRoot`; use the literal native command `moon bench modules/mb-svg/svg --release --target native --frozen` with no caller-supplied command text; retain complete merged stdout/stderr as normalized UTF-8 text; require exit code zero and the three fixed workload summaries in their declared order. Use a no-BOM `System.Text.UTF8Encoding` instance and implement lower-case SHA-256 exclusively with `[Security.Cryptography.SHA256]::Create().ComputeHash($bytes)` rendered as `[BitConverter]::ToString(...).Replace('-', '').ToLowerInvariant()`, disposing the hash instance in `finally`. Prohibit `Convert::ToHexString`, static `SHA256.HashData`, and other PowerShell-7-only hashing APIs. Build literal contract records for the three frozen corpus and canonical correctness texts/digests in `94-RESEARCH.md`, calculate all UTF-8 digests outside runner timing, and record individual `svg_bench.mbt`, `moon.pkg`, and combined-source digests. Read `policy/foundation.json`, record raw `moon --version`, `moonc -v`, and `moonrun --version`, and refuse capture on a policy mismatch. Before either capture or audit, require `git status --porcelain=v1 --untracked-files=all` to be empty; capture records `worktree: (clean)` as a comparison-identity field, while audit refuses any other state. Collect UTC timestamps, git commit, PowerShell version, OS/architecture, CPU, physical/logical cores, physical memory, and active power scheme using available host commands. Use .NET runtime facts and conditional `Get-CimInstance`/`powercfg` probes where present; when a probe cannot run, write literal `unavailable` plus the attempted command rather than guessing. In normal mode, perform one successful warmup excluded from aggregate statistics, then seven distinct captures, parse each ordered workload summary, calculate arithmetic mean, median, sample standard deviation, minimum, maximum, and coefficient of variation from only those seven means, and render one Markdown document. In `-Audit` mode, do not run MoonBit, write Markdown, alter files, accept an alternate command, or make any timing decision: parse the existing generated Markdown and current fixed inputs; recompute every stated `svg_bench.mbt`, `moon.pkg`, combined-source, three corpus, three correctness, and warmup-plus-captures-1-through-7 output digest; validate every warmup/capture exit status, fixed workload order, and parsed runner summary against its retained raw output; recompute all seven-sample aggregates for every workload and fail on any mismatch, absent capture, or malformed field. Its result is evidence-only: it adds neither threshold enforcement nor CI configuration.</action>
  <verify>
    <automated>powershell.exe -NoProfile -Command '$path = "scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1"; [void][scriptblock]::Create((Get-Content -Raw -LiteralPath $path)); if ((Select-String -LiteralPath $path -SimpleMatch "moon bench modules/mb-svg/svg --release --target native --frozen").Count -ne 1) { throw "native command count mismatch" }'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -Command '$path = "scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1"; [void][scriptblock]::Create((Get-Content -Raw -LiteralPath $path)); if ((Select-String -LiteralPath $path -SimpleMatch "moon bench modules/mb-svg/svg --release --target native --frozen").Count -ne 1) { throw "native command count mismatch" }'</automated>
  </verify>
  <done>The capture/audit generator is syntactically loadable in Windows PowerShell 5.1 and PowerShell 7, uses one exact native-release command, records only observed host/toolchain values, and contains a clean-tree, read-only audit path with no comparison decision path.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the clean-tree native-release baseline</name>
  <files>docs/benchmarks/mb-svg-native-release-baseline.md</files>
  <action>Per D-03: include the four-target qualification command and four successful target labels only as functional qualification context, keeping target-runner timing values out of that context. Per D-04: after this plan's script has been committed and `git status --porcelain=v1 --untracked-files=all` is empty, execute the generator from the repository root to create `docs/benchmarks/mb-svg-native-release-baseline.md` from the current checked workload suite. Its clean preflight is part of comparison identity, so do not generate a baseline from a dirty tree. The document must state its scope as native-host, like-for-like evidence; record the exact command and working directory; list all three workload names in fixed order with their frozen corpus and canonical-correctness SHA-256 values; record source/package/combined SHA-256, git commit, `worktree: (clean)`, pinned-policy and observed toolchain lines, host probes, and the exact comparison identity rule. Embed the complete normalized UTF-8 output for the warmup and every capture in collapsible Markdown blocks with each output's SHA-256, UTC start/end, exit status, parsed runner summaries, and fixed workload order. Mark the warmup as excluded, retain captures 1 through 7 individually, and publish native-host-specific summary statistics for every workload. Do not treat the generated but uncommitted document as audit evidence; commit this generated record as the task artifact so the next task can enforce its audit clean preflight. The record adds no threshold, CI gate, ranking, or performance claim.</action>
  <verify>
    <automated>$clean = & git status --porcelain=v1 --untracked-files=all; if ($clean) { throw "capture preflight requires a clean worktree" }; powershell.exe -NoProfile -File scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $p = "docs/benchmarks/mb-svg-native-release-baseline.md"; $t = Get-Content -Raw -LiteralPath $p; foreach($n in @("path-parse/1000-line-to", "transform-composition/50-segment", "parse-to-lower/50-rect")) { if(([regex]::Matches($t, [regex]::Escape($n))).Count -lt 9) { throw ("missing recorded workload evidence: " + $n) } }; if(([regex]::Matches($t, "Capture [1-7]")).Count -ne 7) { throw "capture count mismatch" }; if($t -notmatch "Warmup.*excluded") { throw "warmup exclusion missing" }; if($t -notmatch "worktree:\s*\(clean\)") { throw "clean identity missing" }; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>The committed Markdown record contains a truthful toolchain/host/provenance record, explicit clean-worktree comparison identity, one excluded warmup, seven complete native captures with full-output digests, and summaries that future maintainers can classify as comparable or not comparable from exact recorded identity facts.</done>
</task>

<task type="auto">
  <name>Task 3: Audit the committed baseline under both supported PowerShell engines</name>
  <precondition>The generated Markdown record from Task 2 is committed and `git status --porcelain=v1 --untracked-files=all` produces no output.</precondition>
  <files>scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1, docs/benchmarks/mb-svg-native-release-baseline.md</files>
  <action>Per D-04: from the clean committed tree, invoke `-Audit` once through `powershell.exe` and once through `pwsh`. The read-only audit must parse the generated Markdown rather than rerunning the benchmark or regenerating the document. It must recompute and match the stated two source-file digests, combined-source digest, all three frozen corpus digests, all three canonical-correctness digests, and all eight normalized output digests; parse and validate every warmup/capture workload summary and its zero exit status in the fixed three-workload order; and recompute arithmetic mean, median, sample standard deviation, minimum, maximum, and coefficient of variation from the seven retained samples for every workload, failing any mismatch. Treat the clean worktree preflight and recorded `(clean)` worktree identity as required comparison evidence. The audit makes no threshold, regression, ranking, CI, or cross-target conclusion.</action>
  <verify>
    <automated>$clean = & git status --porcelain=v1 --untracked-files=all; if ($clean) { throw "audit preflight requires a clean worktree" }; powershell.exe -NoProfile -File scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1 -Audit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1 -Audit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Both Windows PowerShell 5.1 and PowerShell 7 complete the clean-tree, read-only audit without modifying files; every documented digest, summary, and seven-capture aggregate is independently verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository files and host probes to evidence generator | Source hashes, command output, policy data, and host facts become a durable human-readable record. |
| Native benchmark process to Markdown record | A local process produces untrusted textual runner output that is parsed and preserved as evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-04 | Tampering | baseline source/corpus/output provenance | high | mitigate | D-04 records separate source, corpus, correctness, and full-output SHA-256 values with commit and worktree state before rendering the document. |
| T-94-05 | Repudiation | toolchain and host provenance | medium | mitigate | D-04 preserves raw version/probe results and records unavailable values with attempted commands instead of inferred substitutions. |
| T-94-06 | Elevation of Privilege | PowerShell process invocation | medium | mitigate | The script resolves its repository root and invokes one literal MoonBit command without accepting command text or loading external packages. |
| T-94-07 | Integrity | comparison conclusion | medium | mitigate | D-03/D-04 require exact identity matching for a like-for-like comparison; the generator only records observations and contains no automatic decision policy. |
</threat_model>

<verification>
Run the generator once with the pinned toolchain, then independently recompute source and output hashes from the Markdown record. Confirm the single native command succeeds and the document provides exactly one warmup and seven retained captures for each of the three workloads.
</verification>

<success_criteria>
- The baseline can be regenerated by one checked-in PowerShell command on Windows PowerShell or PowerShell 7, with unavailable platform-specific facts recorded honestly.
- It retains no cross-target timing evidence; the all-target command is represented only as runnable qualification.
- It defines no timing threshold, CI gate, ranking, or performance claim.
</success_criteria>

<output>
Create `.planning/phases/94-svg-benchmark-evidence/94-02-SUMMARY.md` when done.
</output>
