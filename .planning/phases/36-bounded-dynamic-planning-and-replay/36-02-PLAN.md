---
phase: 36-bounded-dynamic-planning-and-replay
plan: "02"
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
gap_closure: true
requirements: [PNGD-02, PNGD-03]
must_haves:
  truths:
    - "A Dynamic-selected plan with exactly its selected work admission succeeds and consumes that work once, while one less work fails before either eager output or caller-buffered output can be observed."
    - "After a Dynamic source replay drift reaches PngChunkEncoder, the first error remains sticky and a fresh sentinel-filled caller lease is not changed by a later pull."
  artifacts:
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Dynamic selected-work exact/one-less shared-preflight evidence.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public eager/chunk atomic-admission and Dynamic replay-drift lease-isolation evidence.
  key_links:
    - from: modules/mb-image/png/encode_wbtest.mbt
      to: modules/mb-image/png/encode.mbt
      via: _png_encode_preflight exposes the Dynamic selected plan and single charged-work ledger.
      pattern: "_png_encode_preflight|PngDeflatePlan::Dynamic"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: Public PngEncoder and PngChunkEncoder factories use the same admission path; PngChunkEncoder latches its first replay failure before later lease pulls.
      pattern: "new_with_compression_strategy|PngChunkEncoder::pull|png-encode-dynamic-replay-drift"
---

<objective>
Close the two Phase 36 behavioral-evidence gaps without altering the bounded Dynamic implementation.

Purpose: Prove the exact Dynamic admission boundary and the public chunk replay-drift terminal composition that the verification report found unexercised.
Output: Stable, focused tests covering Dynamic selected-work atomicity and sticky public caller-lease isolation on all four supported targets.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-bounded-dynamic-planning-and-replay/36-01-SUMMARY.md
@.planning/phases/36-bounded-dynamic-planning-and-replay/36-VERIFICATION.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- `_png_encode_preflight(source, strategy, limits, budget)` is the sole shared eager/caller admission path. Its selected Dynamic ledger is `total_length + matcher_work + matcher_work`, and its limit checks and one `budget.charge` happen only after selection.
- The existing 128-pixel periodic RGB8 strict-winning fixture has a deterministic Dynamic selected-work cost of `9388UL`. Task 1 derives and asserts it from `_png_encode_preflight` as `total_length == 202UL` plus two `matcher_work == 4593UL` walks; Task 2 treats `9388UL` as a documented test fixture constant and calls only public APIs.
- `PngEncoder::new_with_compression_strategy` and `PngChunkEncoder::new_with_compression_strategy` both route `DynamicOrFixedOrStored` through that preflight before creating output state.
- `PngChunkEncoder::pull` latches the first machine error as `Failed(error)`, then returns it with zero newly written bytes and unchanged total progress for subsequent caller leases.
- `png_stream_test_dynamic_image()` is the existing compact periodic RGB8 fixture that already demonstrates a final Dynamic (`BTYPE=10`) strict winner. It is Phase 36 focused evidence, not the RGB8/RGBA8 generated corpus reserved for Phase 37.
</interfaces>
</context>

<source_audit>
SOURCE   | ID      | Feature / constraint | Plan | Status | Coverage
---------|---------|----------------------|------|--------|---------
GOAL     | —       | Exact preflight plus acknowledgement-safe eager/chunk Dynamic replay. | 36-01, 36-02 | COVERED | Plan 01 implemented the behavior; Tasks 1-2 exercise its two previously unproven transitions.
REQ      | PNGD-02 | Strict Dynamic selection after exact atomic admission, with no observed output on rejection. | 36-02 | COVERED | Tasks 1-2 prove Dynamic selected-work exact/one-less behavior through shared and public paths.
REQ      | PNGD-03 | Sticky Dynamic eager/caller behavior and acknowledgement-safe caller leases. | 36-02 | COVERED | Task 2 proves the public replay-drift terminal is sticky and isolates a fresh lease.
RESEARCH | bounded replay | Retain fixed-capacity Dynamic facts, existing matcher, one IDAT, zlib, CRC, Adler, and sticky terminal semantics. | 36-01, 36-02 | COVERED | This test-only plan asserts existing behavior and does not alter implementation or scope boundaries.
CONTEXT  | —       | No Phase 36 CONTEXT.md exists; there are no locked D-XX decisions. | 36-01, 36-02 | COVERED | Existing roadmap, requirements, and verification report govern the gap closure.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Dynamic selected-work exact and one-less preflight evidence</name>
  <files>modules/mb-image/png/encode_wbtest.mbt</files>
  <behavior>
    - Test 1: The compact periodic Dynamic fixture selects `PngDeflatePlan::Dynamic` under a generous budget and derives its selected work from the one preflight charge.
    - Test 2: The fixture-specific Dynamic facts are `total_length == 202UL`, `matcher_work == 4593UL`, and selected work `202UL + 4593UL + 4593UL == 9388UL`; an exact work budget succeeds and reaches zero remaining work.
    - Test 3: One less Dynamic selected-work unit returns the existing work-limit error and leaves every resource-budget field unchanged.
  </behavior>
  <action>Add the stable white-box test named `PNG dynamic selected work boundary charges once` beside the existing FixedOrStored boundary test. Reuse or add a compact package-private 128-pixel periodic RGB8 fixture byte-for-byte equivalent to the existing public Dynamic strict winner; do not introduce a corpus generator, RGBA matrix, benchmark, or Phase 37 evidence. Run `_png_encode_preflight` with `DynamicOrFixedOrStored` and a generous work budget, assert the selected branch is `PngDeflatePlan::Dynamic`, then calculate selected work from the budget delta. Assert the source facts `total_length == 202UL` and `matcher_work == 4593UL`, and assert the selected ledger and budget delta are exactly `9388UL`. This `9388UL` fixture constant is the documented bridge used by the public-only Task 2 tests. Repeat with exactly `9388UL` work and assert success plus zero remaining work. Repeat with `9387UL` and assert the normal work rejection plus `png_wb_same_remaining` equality against the pre-call resource limits. The test is evidence for the existing atomic preflight; do not change `encode.mbt`, plan selection, limits, budgets, or compression behavior unless this new test demonstrates a real regression that requires a separately approved repair.</action>
  <verify>
    <automated>$names = @('PNG dynamic selected work boundary charges once'); foreach ($target in @('js','wasm','wasm-gc','native')) { $outline = @(moon -C modules/mb-image test png --target $target --frozen --outline 2>&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "PNG outline has no entries on $target" }; foreach ($name in $names) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing named Dynamic test on ${target}: $name" } }; moon -C modules/mb-image test png --target $target --frozen -f '*dynamic*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }</automated>
  </verify>
  <done>The selected Dynamic ledger has direct exact/one-less coverage with the asserted 128-pixel fixture constant `9388UL`: exact admission charges once, while `9387UL` fails atomically and does not consume any budget field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove public Dynamic admission and replay-drift lease isolation</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: Using only the documented `9388UL` fixture work constant, both eager and caller-buffered strategy factories are admitted once before output; fresh exact budgets succeed and exhaust exactly, while their independent `9387UL` cases expose no eager writer byte or caller-lease byte and preserve all eight resource-limit fields.
    - Test 2: After admission, mutating the Dynamic-winning source makes `PngChunkEncoder` fail with `png-encode-dynamic-replay-drift`; a subsequent pull into a fresh sentinel-filled lease returns the same error, writes zero bytes, preserves total progress, and retains the sentinel.
  </behavior>
  <action>Add two stable public tests named `PNG dynamic public exact and one-less admission is atomic` and `PNG dynamic replay drift is sticky through chunk encoder`. Build small helpers from the existing FixedOrStored public-admission test pattern, but use `png_stream_test_dynamic_image()` and `DynamicOrFixedOrStored` throughout. Define a test-local documented fixture constant `9388UL`, citing Task 1's white-box assertions (`total_length 202UL`, `matcher_work 4593UL`, selected work 9388UL). Do not call `_png_encode_preflight`, inspect `PngDeflatePlan`, or use any other private API from these public tests. Exercise the public eager factory with a `MemoryWriter` and the public chunk factory separately. Give each exact case a fresh `png_stream_test_budget(work=9388UL)` and assert construction succeeds with exactly zero remaining work; compare the other seven remaining fields against the pre-call snapshot so the one charge is isolated to work. Give each one-less case a fresh `png_stream_test_budget(work=9387UL)` and assert factory failure before output is observable: eager writer position remains zero, the caller-buffered constructor produces no encoder to pull from, a prefilled candidate lease remains unchanged, and all eight remaining fields—bytes, allocations, allocation size, width, height, pixels, depth, and work—exactly equal their pre-call snapshots. For the replay test, admit the Dynamic encoder, mutate a source component after admission, drain valid one-byte scratch leases until the first failed pull, and assert the state error context is `png-encode-dynamic-replay-drift`. Then call `pull` with a new sentinel-filled lease and assert zero written bytes, the same total progress and error identity as the first failure, and every sentinel byte unchanged. Keep existing `Finished`, released-lease, fixed-or-stored, eager parity, and BTYPE tests intact. Do not change `stream_encode.mbt`, emit new PNG bytes, alter public APIs, add staging or dependencies, or extend this focused fixture into Phase 37's corpus, cross-format matrix, or compression evidence.</action>
  <verify>
    <automated>$names = @('PNG dynamic public exact and one-less admission is atomic','PNG dynamic replay drift is sticky through chunk encoder'); foreach ($target in @('js','wasm','wasm-gc','native')) { $outline = @(moon -C modules/mb-image test png --target $target --frozen --outline 2>&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "PNG outline has no entries on $target" }; foreach ($name in $names) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing named Dynamic test on ${target}: $name" } }; moon -C modules/mb-image test png --target $target --frozen -f '*dynamic*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Both output adapters directly prove `9388UL` exact and `9387UL` one-less Dynamic atomic admission using public APIs only, and public PngChunkEncoder replay drift is proved sticky with a later sentinel lease untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutable caller ImageView → Dynamic replay | The caller can change source components after preflight but before Dynamic replay reaches its admitted terminal checks. |
| Public encoder factory → eager writer/caller lease | Rejected admission must not create observable output, and a latched terminal must not alter a later caller-owned lease. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02-01 | Tampering | Dynamic selected-work admission | high | mitigate | Tasks 1-2 assert exact single charge and one-less atomic rejection through the shared and both public factory paths. |
| T-36-02-02 | Tampering | PngChunkEncoder Dynamic replay | high | mitigate | Task 2 mutates an admitted source, asserts the Dynamic drift terminal, and proves a fresh lease is untouched by the latched failure. |
| T-36-02-03 | Denial of service | Dynamic replay/admission bounds | medium | accept | No implementation or input-boundary changes are introduced; existing fixed-capacity planning and preflight limits are exercised as-is. |
| T-36-02-SC | Tampering | Package/dependency supply chain | low | accept | The plan changes only MoonBit tests and adds no package installation or dependency. |
</threat_model>

<verification>
- Run every named test through the per-target outline guard before relying on the `*dynamic*` filter, so an empty selector cannot pass as evidence.
- Run the focused Dynamic filter independently on js, wasm, wasm-gc, and native, then run the full PNG package suite across all supported targets.
- These tests close only the two Phase 36 verification gaps. The generated RGB8/RGBA8 corpus, cross-case Dynamic win claims, and Phase 37 evidence remain out of scope.
</verification>

<success_criteria>
- `PNG dynamic selected work boundary charges once` proves the strict-winning 128-pixel fixture's asserted Dynamic selected-work constant is `9388UL` and has exact/one-less atomic shared-preflight behavior.
- `PNG dynamic public exact and one-less admission is atomic` uses only the documented `9388UL` constant and public APIs to prove exact exhaustion and one-less rejection before eager and caller-buffered output observation.
- `PNG dynamic replay drift is sticky through chunk encoder` proves the public Dynamic composition latches the first drift error and leaves a new sentinel lease intact.
- All three stable named tests appear in the outline and pass on js, wasm, wasm-gc, and native; the full PNG package suite remains green on all four targets.
</success_criteria>

<output>
Create `.planning/phases/36-bounded-dynamic-planning-and-replay/36-02-SUMMARY.md` when done.
</output>
