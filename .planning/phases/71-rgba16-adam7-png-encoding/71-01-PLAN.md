---
phase: 71-rgba16-adam7-png-encoding
plan: 01
type: execute
wave: 1
depends_on: [70-01]
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [RGBA16ENC-03]
requirements_addressed: [RGBA16ENC-03]
must_haves:
  truths:
    - "D-01 and D-02: Exactly two additive eager and two additive caller-buffered RGBA16 Adam7 selector APIs are public; every existing RGBA16 selector remains explicitly non-interlaced."
    - "D-03: Both new selector families forward PngEncodeProfile::Rgba16 and the selected PngInterlaceStrategy into the existing bounded profile-aware machine and its single Adam7 planner."
    - "D-04: A non-symmetric packed little-endian 5x5 RGBA16 source produces legal seven-pass Type-6/16 PNG data and decode_rgba16 restores all stored component bytes at their original coordinates."
    - "D-05: Fresh eager and caller-buffered Adam7 bytes are identical for Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive filtering, including zero-capacity, one-byte, and ragged leases with established atomic and sticky lifecycle semantics."
    - "D-06: Generic constructors, established RGB8/RGBA8 and Gray/GrayAlpha interlace routes, RGBA16 non-interlaced bytes, colour identity gates, and source layout remain unchanged; Phase 72 retains broad portability qualification."
  artifacts:
    - "modules/mb-image/png/png.mbt exposes PngEncoder::new_rgba16_with_interlace_strategy and PngEncoder::new_rgba16_with_all_strategies."
    - "modules/mb-image/png/stream_encode.mbt exposes PngChunkEncoder::new_rgba16_with_interlace_strategy and PngChunkEncoder::new_rgba16_with_all_strategies."
    - "modules/mb-image/png/encode_test.mbt contains the independent 5x5 Type-6/16 seven-pass raster and explicit decode evidence."
    - "modules/mb-image/png/stream_encode_test.mbt contains RGBA16 Adam7 eager/chunk identity and hostile caller-owned lease evidence."
  key_links:
    - "PngEncoder::new_rgba16_with_all_strategies -> PngEncodeProfile::Rgba16 with caller-selected PngInterlaceStrategy -> existing PngEncodeMachine"
    - "PngChunkEncoder::new_rgba16_with_all_strategies -> PngEncodeMachine::new_with_profile(Rgba16, strategy, filter_strategy, interlace_strategy, ...)"
    - "Rgba16 profile -> existing Adam7 pass cursor and pass-local filtering -> Type-6/16 IHDR and Rhi,Rlo,Ghi,Glo,Bhi,Blo,Ahi,Alo wire lanes"
    - "PngChunkEncoder::pull -> existing acknowledgement, revision validation, lease isolation, and cached terminal lifecycle"
  prohibitions:
    - statement: "Do not alter generic constructors, existing non-interlaced RGBA16 factory delegation, colour identity checks, source storage layout, the private Adam7 planner, filtering/compression machinery, or established RGB8/RGBA8 and Gray/GrayAlpha routes. Do not add staging, an alternate machine, FFI, copied source trees, release work, or Phase 72 qualification suites."
      status: required
      verification: "Focused source review and the RGBA16 JS PNG test family."
---

<objective>
Add explicit eager and caller-buffered Type-6/16 RGBA16 Adam7 selection through the already proven profile-aware encoder path, with an independent seven-pass lane-fidelity oracle and bounded eager/chunk parity evidence.

Purpose: Fulfil RGBA16ENC-03 without weakening the non-interlaced compatibility contract or duplicating any emitter, pass planner, filter, compression, admission, acknowledgement, or terminal logic.

Output: Four locked RGBA16 interlace selectors and focused public PNG-package evidence for seven-pass source fidelity and caller-owned streaming parity.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

## Source Audit

| SOURCE | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 71 | Explicit Type-6/16 Adam7 output retains exact source lanes, profile, filter, and compression semantics. | 01 / Tasks 1-2 | COVERED |
| REQ | RGBA16ENC-03 | Explicit legal RGBA16 Adam7 output preserves every U16 lane, encoder options, and frozen non-interlaced behavior. | 01 / Tasks 1-2 | COVERED |
| RESEARCH | public factory seam | Add only the matched eager/chunk interlace and all-strategies façades; preserve literal None in prior RGBA16 families. | 01 / Tasks 1-2 | COVERED |
| RESEARCH | shared machine seam | Forward Rgba16 and PngInterlaceStrategy into the existing profile-aware preflight/machine; retain the single pass planner. | 01 / Tasks 1-2 | COVERED |
| RESEARCH | independent fidelity oracle | Assert the literal seven-pass 5x5 uncompressed raster, Type-6/16/Adam7 IHDR, and decode_rgba16 storage-lane restoration. | 01 / Task 1 | COVERED |
| RESEARCH | bounded chunk contract | Cross six compression/filter pairs with zero, one-byte, and ragged schedules; retain tail, acknowledgement, and terminal checks. | 01 / Task 2 | COVERED |
| CONTEXT | D-01 | Add exactly the two eager and two chunk explicit-interlace/all-strategies names. | 01 / Tasks 1-2 | COVERED |
| CONTEXT | D-02 | Freeze the four existing eager and four existing chunk RGBA16 non-interlaced routes; Adam7 remains opt-in. | 01 / Tasks 1-2 | COVERED |
| CONTEXT | D-03 | Reuse Rgba16, the shared encoder machine, and the established Adam7 traversal/pass planner. | 01 / Tasks 1-2 | COVERED |
| CONTEXT | D-04 | Use a non-symmetric 5x5 packed RGBA16 source, legal seven-pass Type-6/16 output, and explicit decode reconstruction. | 01 / Task 1 | COVERED |
| CONTEXT | D-05 | Prove fresh eager/chunk equality for the six strategy pairs plus atomic admission, acknowledged progress, lease isolation, and sticky terminals. | 01 / Task 2 | COVERED |
| CONTEXT | D-06 | Keep other routes, generic APIs, colour/layout contracts, and qualification boundaries frozen. | 01 / Tasks 1-2 | COVERED |

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-rgba16-adam7-png-encoding/71-CONTEXT.md
@.planning/phases/71-rgba16-adam7-png-encoding/71-RESEARCH.md
@.planning/phases/71-rgba16-adam7-png-encoding/71-PATTERNS.md
@.planning/phases/69-explicit-rgba16-png-encoding/69-01-SUMMARY.md
@.planning/phases/69-explicit-rgba16-png-encoding/69-VERIFICATION.md
@.planning/phases/70-resumable-rgba16-png-encoding/70-01-SUMMARY.md
@.planning/phases/70-resumable-rgba16-png-encoding/70-VERIFICATION.md

<interfaces>
From modules/mb-image/png/png.mbt: PngEncoder::new_rgba16_with_strategies(strategy, filter_strategy) is the frozen eager compatibility route and fixes PngInterlaceStrategy::None; the GrayAlpha16 interlace/all-strategies pair is the exact additive public-factory shape.

From modules/mb-image/png/stream_encode.mbt: PngChunkEncoder::new_rgba16_with_strategies(source, strategy, filter_strategy, limits, budget, diagnostics) is the frozen caller-buffered compatibility route and fixes None. PngChunkEncoder::new_graya16_with_all_strategies(source, strategy, filter_strategy, interlace_strategy, limits, budget, diagnostics) demonstrates forwarding into PngEncodeMachine::new_with_profile.

From modules/mb-image/png/encode.mbt: PngEncodeProfile::Rgba16 already maps a packed little-endian source to Type-6/16 wire bytes in Rhi,Rlo,Ghi,Glo,Bhi,Blo,Ahi,Alo order and shares the Adam7 pass cursor and pass-local filtering.

From modules/mb-image/png/stream_encode.mbt: PngChunkEncoder::pull is the sole lease/acknowledgement, revision-validation, destination-failure, total-written, and cached Finished/Failed lifecycle owner; new factories must return its existing state unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
<name>Task 1: Red-green the eager RGBA16 Adam7 path with an independent seven-pass fidelity oracle</name>
<reversibility rating="costly">These additive public factory names become part of the encoder API, but D-01 and D-02 have already fixed their exact shape and the implementation remains behind the existing profile-aware machine.</reversibility>
<files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode_test.mbt</files>
<read_first>
- modules/mb-image/png/png.mbt: `PngEncoder::new_graya16_with_interlace_strategy` and `PngEncoder::new_graya16_with_all_strategies` (around lines 370-390), then the four `PngEncoder::new_rgba16*` compatibility factories (around lines 395-431).
- modules/mb-image/png/encode_test.mbt: GrayAlpha16 5x5 fixture and literal seven-pass oracle (around lines 293-348), RGBA16 non-interlaced source-lane/decode tests (around lines 1343-1445), and GrayAlpha16 Adam7 selector/strategy tests (around lines 1451-1721).
- modules/mb-image/png/encode.mbt: `PngEncodeProfile::Rgba16` wire-byte mapping and the existing Adam7 cursor/pass-local filtering branches (pattern-map anchors around lines 438-458 and 569-616).
- .planning/phases/71-rgba16-adam7-png-encoding/71-CONTEXT.md
- .planning/phases/71-rgba16-adam7-png-encoding/71-PATTERNS.md
- .planning/phases/70-resumable-rgba16-png-encoding/70-01-SUMMARY.md
</read_first>
<behavior>
- A checked 5x5 packed little-endian, straight-alpha RGBA16 source whose coordinate, component, high, and low storage lanes all differ produces an uncompressed Adam7 raster matching the independently enumerated seven standard tuples `(0,0,8,8)`, `(4,0,8,8)`, `(0,4,4,8)`, `(2,0,4,4)`, `(0,2,2,4)`, `(1,0,2,2)`, and `(0,1,1,2)`.
- Every populated pass row has one None filter byte and each selected pixel is independently expected in PNG wire order `Rhi,Rlo,Ghi,Glo,Bhi,Blo,Ahi,Alo`; the full 5x5 raster is exactly 211 bytes.
- `PngEncoder::new_rgba16_with_interlace_strategy(Adam7)` and `PngEncoder::new_rgba16_with_all_strategies(Stored, None, Adam7)` choose legal IHDR depth `0x10`, colour type `0x06`, and interlace method `0x01`, then explicit `PngDecoder::decode_rgba16` restores each storage byte at its source coordinate.
- The all-strategies eager selector succeeds for Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive filters; every result retains Type-6/16 Adam7 IHDR framing.
</behavior>
<action>
Begin RED by creating an RGBA16-specific 5x5 fixture and an independent expected-pass helper in `encode_test.mbt`, modeled on the GrayAlpha16 oracle but calculating all 200 source-wire bytes directly from the fixture coordinates. Enumerate the seven literal pass tuples and append a None filter tag per emitted pass row; assert the entire 211-byte decompressed IDAT raster so expected output cannot originate from either new encoder façade. Add public explicit-decode checks that iterate all 25 pixels, four components, and both packed storage lanes via `PngDecoder::decode_rgba16` and component-byte access.

After the tests demonstrate the missing API, implement D-01 by adding exactly `PngEncoder::new_rgba16_with_interlace_strategy(interlace_strategy)` and `PngEncoder::new_rgba16_with_all_strategies(strategy, filter_strategy, interlace_strategy)` immediately beside the existing RGBA16 family. The narrow selector delegates to the all-strategies selector with Stored and None, matching GrayAlpha16. The all-strategies selector returns the standard PngEncoder record with `PngEncodeProfile::Rgba16` and the caller-provided interlace value. Per D-03, do not modify `encode.mbt`, the private traversal, or the filtering/compression paths: this façade must cause the one existing profile-aware encoder machine and Adam7 planner to run.

Add selector and six-pair strategy assertions for IHDR `0x10/0x06/0x01`, independent raster equality where Stored/None permits it, and complete explicit decode fidelity. Per D-02 and D-06, leave the prior four `new_rgba16*` declarations and their literal None delegation unchanged; retain generic constructors and all unrelated profiles without edits. Limit validation to the focused package test family because Phase 72 owns broad target qualification.
</action>
<acceptance_criteria>
- `png.mbt` exposes exactly the two D-01 eager RGBA16 interlace selectors and forwards `Rgba16` plus the requested interlace strategy without adding a new encoder implementation.
- A 5x5 RGBA16 Adam7 Stored/None encoding matches the independently derived 211-byte seven-pass raster, declares IHDR depth/type/interlace `0x10/0x06/0x01`, and `decode_rgba16` restores all 200 packed storage bytes at their original coordinates.
- All three compression strategies crossed with None and Adaptive filters produce legal Type-6/16 Adam7 framing through the new all-strategies selector.
- Existing eager RGBA16 non-interlaced factory regression remains present and still observes IHDR interlace byte `0x00`.
</acceptance_criteria>
<verify>
<automated>moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'</automated>
</verify>
<done>A library user can explicitly request eager Type-6/16 RGBA16 Adam7 output, and a public independent oracle proves exact wire lanes and decoded source-lane reconstruction.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Red-green caller-buffered RGBA16 Adam7 selectors and hostile-schedule eager parity</name>
<files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
<read_first>
- modules/mb-image/png/stream_encode.mbt: frozen `PngChunkEncoder::new_rgba16*` factories (around lines 265-324), GrayAlpha16 interlace/all-strategies factory family (around lines 329-360), `PngChunkEncoder::pull` (around lines 503-587), and `PngEncodeMachine::new_with_profile` preflight construction (around lines 728-800).
- modules/mb-image/png/stream_encode_test.mbt: GrayAlpha16 selector identity checks (around lines 1357-1385), frozen RGBA16 non-interlaced factory regression (around lines 1502-1552), shared public drain helpers (around lines 943-996 and 1089-1149), and GrayAlpha16 Adam7 eager/chunk drain matrix (around lines 4048-4144).
- modules/mb-image/png/encode_test.mbt: Task 1's 5x5 RGBA16 source and eager strategy expectations.
- .planning/phases/71-rgba16-adam7-png-encoding/71-CONTEXT.md
- .planning/phases/71-rgba16-adam7-png-encoding/71-RESEARCH.md
- .planning/phases/70-resumable-rgba16-png-encoding/70-VERIFICATION.md
</read_first>
<behavior>
- `PngChunkEncoder::new_rgba16_with_interlace_strategy(source, Adam7, ...)` and `PngChunkEncoder::new_rgba16_with_all_strategies(source, strategy, filter_strategy, Adam7, ...)` start the same `Rgba16` profile-aware machine as eager output after preflight.
- For Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive filters, a fresh eager RGBA16 Adam7 encoder is the byte oracle for a fresh chunk encoder under `[0UL, 1UL]`, `[1UL]`, and `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]` capacities.
- A zero-capacity lease reports NeedOutput with no accepted bytes or total progress and leaves its sentinel unchanged; every acknowledged pull changes only its accepted prefix; a post-finish pull writes no byte and preserves its sentinel.
- An Adam7 request rejected by profile/resource preflight exposes neither eager nor caller-buffered bytes and does not reserve the caller budget; a post-construction source mutation or released lease reaches the shared zero-write typed terminal and returns that identical terminal on a later pull without changing either sentinel.
</behavior>
<action>
Begin RED by adding RGBA16 Adam7 source, fresh eager-oracle, and caller-buffered drain helpers beside the existing GrayAlpha16 Adam7 helpers. Reuse the same public lease harness, but construct independent eager and chunk encoders for each assertion. Cover the narrow and all-strategies D-01 APIs, then cross Stored, FixedOrStored, and DynamicOrFixedOrStored with None and Adaptive using the exact zero/one/ragged capacity schedules. D-05 requires checks for NeedOutput on a zero-capacity lease, acknowledged-only totals, capacity-bounded writes, untouched lease tails, aggregate equality to fresh eager bytes, and a zero-write Finished pull afterward. Adapt the established RGBA16 admission, replay-mutation, and released-destination helpers to at least one new Adam7 all-strategies construction: compare rejected construction with fresh eager error/no-output behavior, then prove first and later failed pulls accept zero bytes, retain the pre-failure total, replay one typed terminal, and leave caller sentinels unchanged.

Once those tests fail, add exactly `PngChunkEncoder::new_rgba16_with_interlace_strategy(source, interlace_strategy, limits, budget, diagnostics)` and `PngChunkEncoder::new_rgba16_with_all_strategies(source, strategy, filter_strategy, interlace_strategy, limits, budget, diagnostics)` next to the existing RGBA16 family. The narrow form delegates with Stored/None; the all-strategies form must use the same `PngEncodeMachine::new_with_profile` match/Err propagation/active-state initialization as the GrayAlpha16 family, substituting only `PngEncodeProfile::Rgba16` and forwarding the supplied strategy values. This is D-03: leave `pull`, preflight, pass planning, filter choice, compression, source validation, and terminal handling untouched.

Per D-02 and D-06, preserve the four prior chunk RGBA16 constructors with their literal None forwarding, keep generic selection unchanged, and retain all RGB8/RGBA8 and Gray/GrayAlpha routes. Reuse the existing public hostile lifecycle harness as RGBA16 evidence; no broad multi-target run, separate qualification matrix, staging buffer, second planner, or alternate encoder belongs in this task.
</action>
<acceptance_criteria>
- `stream_encode.mbt` exposes exactly the two D-01 RGBA16 chunk interlace selectors; both select `Rgba16`, preserve the existing Result propagation and active state, and forward the caller-supplied interlace strategy to `PngEncodeMachine::new_with_profile`.
- Fresh eager and caller-buffered RGBA16 Adam7 bytes match for all six compression/filter pairs and all three prescribed lease schedules.
- Zero-capacity, acknowledged, sentinel-tail, post-finish, rejected-admission, source-mutation, and released-destination checks demonstrate the established atomic/sticky lease-progress lifecycle for the new chunk selectors.
- Existing four RGBA16 chunk non-interlaced factories still assert IHDR interlace `0x00`; generic and other colour-profile constructors are not changed.
</acceptance_criteria>
<verify>
<automated>moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'</automated>
</verify>
<done>A library user can select caller-buffered RGBA16 Adam7 output that is byte-identical to fresh eager output under hostile capacities while retaining the one shared bounded encoder lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller image/selector arguments → profile-aware encoder preflight | Caller controls source descriptor, interlace, compression/filter choices, limits, budget, and later source revision. |
| Shared encoder machine → caller-owned chunk lease | Canonical PNG bytes cross into mutable caller memory only through existing destination acceptance and acknowledgement. |
| Explicit encoded PNG → explicit decoder oracle | Type-6/16 Adam7 wire lanes are reconstructed into packed little-endian RGBA16 storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-71-01 | Tampering | RGBA16 U16 wire-lane and Adam7 coordinate mapping | medium | mitigate | Task 1 independently enumerates the seven passes, 211-byte raster, all Type-6/16 wire lane orders, and public coordinate/storage-lane reconstruction. |
| T-71-02 | Tampering | caller-selected interlace profile routing | medium | mitigate | Tasks 1-2 require both facades to forward `Rgba16` and PngInterlaceStrategy to the same shared machine and prove Adam7 IHDR framing. |
| T-71-03 | Denial of service | source/resource admission and hostile caller capacities | medium | mitigate | Task 2 reuses public zero/one/ragged lease checks and the profile-aware preflight route, confirming no accepted bytes before acknowledgement. |
| T-71-04 | Tampering | source mutation and destination lifecycle after chunk construction | medium | mitigate | Task 2 keeps the existing revision/destination/cached-terminal owner and exercises the same public hostile lifecycle harness for the new selector family. |
| T-71-05 | Elevation of privilege | generic or default interlace selection | low | accept | This in-process codec has no authorization surface; D-02/D-06 preserve explicit opt-in APIs and keep generic/default constructors frozen. |
</threat_model>

<verification>
Run the focused RGBA16 public PNG test family after each red/green cycle and after both tasks:

<automated>moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'</automated>

Inspect the final diff to confirm production changes are limited to the two public factory files and that all test evidence resides in the two existing PNG test files. Confirm the pre-existing RGBA16 non-interlaced factory tests still assert interlace `0x00`, while the new selectors assert `0x01`. Do not perform Phase 72's broad four-target qualification in this plan.
</verification>

<success_criteria>
1. The four D-01 explicit RGBA16 Adam7 selector names are public and route `Rgba16` plus caller-selected interlace through the existing encoder machine without changing any legacy selector.
2. The independent 5x5 seven-pass Type-6/16 raster and explicit decode establish exact source-lane preservation at every coordinate.
3. Fresh eager and caller-buffered Adam7 output are byte-identical for the six required compression/filter pairs and all required hostile lease schedules, with established progress, isolation, atomicity, and sticky terminal behavior.
4. The focused RGBA16 JS PNG test family passes, while broader Phase 72 qualification remains outside this phase.
</success_criteria>

<output>
Create `.planning/phases/71-rgba16-adam7-png-encoding/71-01-SUMMARY.md` when complete.
</output>
