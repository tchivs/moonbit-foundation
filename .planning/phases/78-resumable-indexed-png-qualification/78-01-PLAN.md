---
phase: 78-resumable-indexed-png-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/encode_test.mbt
autonomous: true
requirements:
  - INDEX-04
  - INDEX-05
must_haves:
  truths:
    - "A caller can drain an Indexed8 source through caller-owned leases and receive the same bytes as eager encoding for opaque and transparent palettes."
    - "Zero-byte, one-byte, and ragged leases commit only accepted bytes, preserve caller tails and borrow ownership, and retain sticky success and failure terminals."
    - "Indexed8 chunk construction rejects invalid limits or budget admission before any lease is exposed or resource counter is charged."
    - "Chunk-produced Type-3/8 PNGs have independently checked frame ordering and CRCs, decode publicly to RGB8 or RGBA8 as applicable, retain the Phase 76 opaque vector, and pass on every supported target."
  artifacts:
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "PngChunkEncoder::new_indexed8 direct adapter to the shared indexed PngEncodeMachine"
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Hostile lease, atomic admission, and sticky-terminal coverage for Indexed8 chunk output"
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Independent chunk-origin wire/CRC, public decode, and frozen opaque compatibility qualification"
  key_links:
    - from: PngChunkEncoder::new_indexed8
      to: PngEncodeMachine::new_with_indexed
      via: "Direct construction after the shared indexed preflight returns its checked frame facts."
    - from: PngChunkEncoder::pull
      to: caller-owned lease
      via: "Lease write is followed by acknowledgement, which alone advances the machine, CRCs, and total_written."
    - from: chunk-produced Indexed8 bytes
      to: test-local PNG parser and PngDecoder
      via: "Independent structural assertions and public RGB8/RGBA8 decode exercise the emitted frame."
---

<objective>
Expose the established bounded Indexed8 PNG frame through the existing caller-buffered encoder and qualify the public lifecycle, wire, decode, compatibility, and portability contracts.

Purpose: Complete INDEX-04 and INDEX-05 without introducing a second encoder or widening the generic image model.
Output: One explicit Indexed8 chunk factory plus hostile lifecycle and independent public qualification tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/78-resumable-indexed-png-qualification/78-CONTEXT.md
@.planning/phases/78-resumable-indexed-png-qualification/78-RESEARCH.md
@.planning/phases/76-indexed8-source-eager-plte/76-01-SUMMARY.md
@.planning/phases/77-indexed-png-transparency/77-01-SUMMARY.md
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/encode_test.mbt

<interfaces>
From modules/mb-image/png/stream_encode.mbt: PngChunkEncoder::new_gray1(source, limits, budget, diagnostics) is the closest public four-argument factory shape; PngChunkEncoder::pull(destination) owns the caller lease and returns the established pull outcome; PngEncodeMachine::new_with_indexed(source, limits, budget, diagnostics) performs the checked Indexed8 preflight and constructs the shared pending-byte machine.

From modules/mb-image/png/encode.mbt: PngEncoder::encode_indexed8(PngEncoder::new(), source, writer, limits, budget, diagnostics) supplies the eager reference bytes.

From modules/mb-image/png/encode_test.mbt: png_indexed_crc32, png_indexed_u32, and png_indexed_slice are test-local structural-oracle helpers; PngDecoder through @codec.ImageDecoder::decode is the public decode path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the shared-machine Indexed8 chunk path and prove one transparent end-to-end lease drain</name>
  <reversibility rating="costly">D-01 and D-03 add a public streaming constructor and lifecycle contract that later callers will rely on, so changing its shape or acknowledgement behavior needs coordinated consumer changes.</reversibility>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: A partial-alpha PngIndexedImage drains through PngChunkEncoder::new_indexed8 with a ragged lease schedule to the exact PngEncoder::encode_indexed8 byte sequence.
    - Test 2: A zero-capacity lease reports NeedOutput with zero writes and leaves its sentinel byte state unchanged; one-byte and ragged leases collect only accepted bytes, retain untouched tail sentinels, and report total_written equal to collected output.
    - Test 3: A completed Indexed8 chunk encoder leaves a later lease untouched and replays its completed terminal; a released first lease creates a zero-write failure that replays unchanged into a fresh caller-owned lease.
    - Test 4: Indexed output-limit, zero-pixel, and insufficient-work admissions return errors from construction while each captured budget remaining value remains unchanged and no pullable encoder exists.
  </behavior>
  <action>Start RED by adding focused stream tests beside the packed-grayscale hostile-drain helpers. Add PngChunkEncoder::new_indexed8(source : PngIndexedImage, limits, budget, diagnostics) per D-01, matching the direct match-and-return style of new_gray1 and wrapping only PngEncodeMachine::new_with_indexed in Active with total_written zero. Keep the fixed Type-3/8, Stored DEFLATE, filter-None, non-interlaced profile, RGB PLTE, and canonical optional tRNS supplied by the shared machine per D-02. Exercise the transparent tracer with zero, one, and ragged caller leases; assert accepted-only progress, tail sentinels, borrowed-lease ownership, sticky completed/error terminals, and construction-time atomic admissions per D-03 and D-04. Keep framing, CRC progression, and traversal entirely in PngEncodeMachine and the existing pull/acknowledge path.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --filter "PNG Indexed8 chunk" --frozen</automated>
  </verify>
  <done>PngChunkEncoder::new_indexed8 exists as a direct shared-machine adapter, and focused stream tests prove a transparent caller-buffered PNG has eager-identical bytes with hostile lease, ownership, atomicity, and terminal behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify opaque and transparent chunk bytes through independent wire and public decode oracles</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - Test 1: Chunk-drained opaque Indexed8 bytes equal both eager bytes and the frozen 89-byte Phase 76 vector, contain IHDR, PLTE, IDAT, and IEND in order, have independently valid CRCs, and decode as public RGB8 palette pixels.
    - Test 2: Chunk-drained partial-alpha Indexed8 bytes equal eager bytes, contain IHDR, PLTE, canonical tRNS, IDAT, and IEND in order with independently valid CRCs, and decode as public RGBA8 palette pixels and alpha values.
    - Test 3: The ordinary PNG package command executes the complete suite with wasm, wasm-gc, js, and native selected by --target all.
  </behavior>
  <action>Start RED by adding a test-local caller-buffered drain helper in encode_test.mbt that uses the public Indexed8 chunk constructor and a hostile schedule, then feed its collected bytes into the existing png_indexed_crc32/png_indexed_u32/png_indexed_slice oracle and public PngDecoder path. Preserve the literal opaque 89-byte Phase 76 vector and compare both opaque and transparent chunk output directly with their eager references per D-03 and D-05. Independently assert opaque IHDR→PLTE→IDAT→IEND and transparent IHDR→PLTE→tRNS→IDAT→IEND framing, each chunk CRC, Stored scanline bytes, and public RGB8/RGBA8 values. Keep the evidence in the ordinary source tree and run the unfiltered all-target PNG package command per D-05.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Opaque and transparent chunk-origin PNGs are eager-identical, structurally independently qualified, publicly decodable as RGB8/RGBA8, preserve the frozen opaque vector, and pass the ordinary four-target package suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Indexed source → constructor | Caller-supplied indexed source and limits/budget request enter checked frame admission. |
| Encoder → caller-owned lease | The encoder writes only accepted bytes to a borrowed destination, then commits shared state. |
| Chunk bytes → decoder/structural oracle | Produced PNG bytes are interpreted by independent structural checks and public decode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-01 | Tampering | Indexed source and indexed preflight | medium | mitigate | Route the constructor directly through PngEncodeMachine::new_with_indexed so existing source validation, checked frame facts, and pre-output limit/work admission remain authoritative; test rejected construction with unchanged budget state. |
| T-78-02 | Denial of service | Indexed8 frame admission | high | mitigate | Reuse indexed preflight's checked dimensions, pixels, output, and work charge ordering; cover output-limit, zero-pixel, and insufficient-work requests before any lease can be pulled. |
| T-78-03 | Tampering | PngChunkEncoder::pull acknowledgement boundary | medium | mitigate | Use the existing present → destination write → acknowledge sequence; hostile tests cover zero/one/ragged capacities, tails, released leases, CRC-safe progress, and sticky terminals. |
| T-78-04 | Repudiation | Indexed PNG frame evidence | low | accept | The deterministic test-local CRC parser and literal compatibility vector make byte-level regressions reproducible; no identity or audit system is in scope. |
| T-78-SC | Tampering | Package installation | low | accept | Phase 78 adds no package-manager dependency or installation task. |
</threat_model>

<verification>
- `moon -C modules/mb-image test png --filter "PNG Indexed8 chunk" --frozen`
- `moon -C modules/mb-image test png --target all --frozen`
</verification>

<success_criteria>
- INDEX-04: The explicit Indexed8 chunk factory reuses the indexed preflight and acknowledged machine, produces eager-identical opaque and transparent bytes under hostile leases, and preserves atomic/sticky ownership semantics.
- INDEX-05: Chunk-origin bytes receive test-local frame/CRC qualification and public RGB8/RGBA8 decode coverage, retain the Phase 76 opaque vector, and pass the ordinary four-target PNG package command.
- Scope remains limited to explicit Indexed8 Type-3/8 Stored/filter-None non-interlaced output and its qualification evidence.
</success_criteria>

## Source Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Caller-buffered parity, hostile leases, independent wire vectors, and four-target proof | 78-01 | COVERED | Tasks 1–2 provide the transport and qualification slices. |
| REQ | INDEX-04 | Shared bounded machine, eager parity, lease ownership, sticky terminals | 78-01 | COVERED | Task 1 directly implements and proves the lifecycle contract. |
| REQ | INDEX-05 | Independent wire/decode vectors, lifecycle evidence, compatibility, portability | 78-01 | COVERED | Task 2 supplies independent/public evidence and the all-target gate. |
| RESEARCH | — | Direct `new_indexed8` wrapper over `new_with_indexed`; accepted-only acknowledgement; admission ordering | 78-01 | COVERED | Task 1 preserves each established seam. |
| RESEARCH | — | Test-local CRC/wire parser, public decode, frozen 89-byte opaque vector | 78-01 | COVERED | Task 2 consumes the existing independent oracle pattern. |
| CONTEXT | D-01 | One explicit Indexed8 constructor sharing the state machine | 78-01 | COVERED | Task 1 action cites D-01. |
| CONTEXT | D-02 | Fixed Type-3/8 Stored/filter-None non-interlaced profile with PLTE/tRNS | 78-01 | COVERED | Task 1 action cites D-02. |
| CONTEXT | D-03 | Eager identity, accepted-only progress, hostile leases | 78-01 | COVERED | Tasks 1–2 actions cite D-03. |
| CONTEXT | D-04 | Atomic preflight and sticky terminal behavior | 78-01 | COVERED | Task 1 action cites D-04. |
| CONTEXT | D-05 | Independent wire/CRC, public decode, compatibility, ordinary all-target test | 78-01 | COVERED | Task 2 action cites D-05. |

<output>
Create `.planning/phases/78-resumable-indexed-png-qualification/78-01-SUMMARY.md` when done.
</output>
