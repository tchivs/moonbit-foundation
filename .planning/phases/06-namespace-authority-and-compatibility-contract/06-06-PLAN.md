---
phase: 06-namespace-authority-and-compatibility-contract
plan: "06"
type: execute
wave: 5
depends_on: [06-01, 06-03, 06-05]
files_modified:
  - release/qualification/phase-06-requirements.json
  - scripts/quality/Test-Phase06Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
requirements: []
must_haves:
  truths:
    - "A separate closed ledger reciprocally maps exactly the eight Phase 6 requirements to focused selectors and content-addressed artifacts without editing the locked v0.1 ledger."
    - "Both focused qualification and the real `scripts/quality.ps1 -Lane Required -EvidenceDirectory <untracked>` entrypoint pass while tracked state remains byte-for-byte unchanged."
    - "Static orchestration and dynamic Required evidence prove the operator observation collector is never invoked and `credentials_read` remains false."
  artifacts:
    - path: "release/qualification/phase-06-requirements.json"
      provides: "Static exact eight-requirement selector/artifact contract"
    - path: "scripts/quality/Test-Phase06Qualification.ps1"
      provides: "Independent focused reciprocal verifier"
    - path: "scripts/quality/Invoke-MoonQuality.ps1"
      provides: "Credential-free Required integration"
  key_links:
    - from: "scripts/quality/Invoke-MoonQuality.ps1"
      to: "release/qualification/phase-06-requirements.json"
      via: "ordered selectors and stable artifact digests"
      pattern: "phase-06-requirements[.]json"
---

<!-- phase-source-audit: GOAL-06,REG-01,REG-02,REG-03,COMP-01,COMP-02,COMP-03,COMP-04,PROV-03,D-01,D-12,RESEARCH-REQUIRED-INTEGRATION,CONTEXT-REQUIRED-BOUNDARY -->

<objective>
Bind all Phase 6 contracts into the real credential-free Required entrypoint.

Purpose: Close reciprocal evidence and prove the actual orchestration, not only a focused surrogate, remains read-only and credential-free.
Output: Static ledger, focused verifier, Required stages, and untracked real-run evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@release/qualification/v0.1-requirements.json
@scripts/quality.ps1
@scripts/quality/ReleaseQualification.Common.ps1
@scripts/quality/Invoke-MoonQuality.ps1
@scripts/quality/Test-RegistryAuthority.ps1
@scripts/quality/Test-PublicInterfaceBaseline.ps1
@scripts/quality/Test-PublicCompatibility.ps1
@scripts/quality/Test-CandidateDocumentation.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze exact reciprocal Phase 6 coverage</name>
  <read_first>release/qualification/v0.1-requirements.json, scripts/quality/ReleaseQualification.Common.ps1, .planning/REQUIREMENTS.md</read_first>
  <files>release/qualification/phase-06-requirements.json, scripts/quality/Test-Phase06Qualification.ps1</files>
  <action>Using the v0.1 ledger only as a closed reciprocal analog, map exactly REG-01, REG-02, REG-03, COMP-01, COMP-02, COMP-03, COMP-04, and PROV-03 to focused commands, stable rules, and content-addressed artifacts. Reject extras, missing reverse mappings, dynamic fields, credentials, PROV-05, and post-publication claims. Do not edit the v0.1 ledger.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-Phase06Qualification.ps1 -LedgerOnly</automated></verify>
  <acceptance_criteria>Exactly eight Phase 6 requirements map once in both directions; PROV-05 remains exclusively Phase 8.</acceptance_criteria>
  <done>The static Phase 6 evidence contract is frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate and execute the real credential-free Required entrypoint</name>
  <read_first>scripts/quality.ps1, scripts/quality/Invoke-MoonQuality.ps1, scripts/quality/Test-Phase06Qualification.ps1, scripts/quality/ReleaseQualification.Common.ps1</read_first>
  <files>scripts/quality/Invoke-MoonQuality.ps1, scripts/quality/Test-Phase06Qualification.ps1</files>
  <action>Add ordered Phase 6 stages before the final tracked-diff snapshot for sanitized authority-readiness evidence, exact 68-record baseline checks, compatibility policy/negatives, source documentation, and reciprocal ledger/artifact binding. Extend the focused verifier to statically reject any Required call/reference to `Invoke-RegistryObservation.ps1`, credential-path reads, environment-secret enumeration, authentication, publication, or registry mutation, and to inspect the real Required report for `credentials_read=false`, `performed=false`, no observation selector, and unchanged tracked state. Preserve the actual existing entrypoint `pwsh -NoProfile -File scripts/quality.ps1 -Lane Required -EvidenceDirectory <untracked-evidence-directory>`, locked v0.1 report meaning, and CI behavior. Add no OIDC, destructive recovery, 1.0, new modules, UI, or COVERAGE.md work.</action>
  <verify><automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; $e='artifacts/release-qualification/phase-06-plan-06'; $before=(git status --porcelain --untracked-files=no) -join \"`n\"; & ./scripts/quality/Test-Phase06Qualification.ps1 -Focused; if($LASTEXITCODE){exit $LASTEXITCODE}; $requiredSource=(Get-Content -Raw scripts/quality.ps1)+(Get-Content -Raw scripts/quality/Invoke-MoonQuality.ps1); if($requiredSource -match 'Invoke-RegistryObservation|Get-Content[^`n]*(credentials|token|cookie|authorization)|Get-ChildItem[^`n]*(env:|credentials)|moon\s+(login|publish)'){throw 'Required contains observation or credential/publication access'}; & ./scripts/quality.ps1 -Lane Required -EvidenceDirectory $e; if($LASTEXITCODE){exit $LASTEXITCODE}; $after=(git status --porcelain --untracked-files=no) -join \"`n\"; if($after -cne $before){throw 'Required changed tracked state'}; if(-not (git check-ignore -q $e)){throw 'Required evidence directory is not untracked/ignored'}; $report=Get-Content -Raw (Join-Path $e 'report.json')|ConvertFrom-Json -Depth 100; if($report.publication.credentials_read -ne $false -or $report.publication.performed -ne $false){throw 'Required report crossed credential/publication boundary'}; $dynamic=Get-Content -Raw (Join-Path $e 'report.json'); if($dynamic -match 'Invoke-RegistryObservation|CaptureAuthority'){throw 'Required dynamically invoked operator observation'}"</automated></verify>
  <acceptance_criteria>Focused and real Required both pass; the real report is untracked, performed/credentials_read are false, no observation selector appears, static orchestration has no collector/credential/auth/publish path, and tracked state is unchanged.</acceptance_criteria>
  <done>The actual Required entrypoint proves every Phase 6 credential-free contract without invoking the operator seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase artifacts -> Required authorization | Missing mappings or privileged calls could bypass gates. |
| Required execution -> untracked report | A surrogate-only check could hide tracked mutation or credential access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-12 | Elevation of Privilege | Required orchestration | high | mitigate | Static forbidden-call scan plus real entrypoint and dynamic report assertions. |
| T-06-13 | Tampering | tracked source and evidence | high | mitigate | Before/after tracked-state equality, ignored evidence directory, stable artifact digests. |
| T-06-14 | Information Disclosure | Required evidence | high | mitigate | Assert credentials_read=false and no collector/auth/credential access path. |

## OWASP ASVS L1 Control Mapping

| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V2 Authentication | Limited | Real Required statically and dynamically proves it never authenticates. | Task 2 |
| V4 Access Control | Applicable | Exact gates run before later publication authorization. | Tasks 1-2 |
| V5 Validation, Sanitization and Encoding | Applicable | Closed ledger, reciprocal mappings, report/schema assertions. | Tasks 1-2 |
| V6 Stored Cryptography | Limited | Existing SHA-256 helpers bind artifact integrity. | Task 2 |
</threat_model>

## Artifacts this phase produces

- `release/qualification/phase-06-requirements.json` — exact static ledger.
- `scripts/quality/Test-Phase06Qualification.ps1` — focused and real-run verifier.
- `scripts/quality/Invoke-MoonQuality.ps1` — Required integration.
- `artifacts/release-qualification/phase-06-plan-06/report.json` — untracked real Required evidence.

<verification>Run Task 2's combined command exactly: focused suite, static boundary scan, real Required with the actual entrypoint, tracked-state comparison, ignored-evidence assertion, and dynamic report checks.</verification>

<success_criteria>
- All eight Phase 6 requirements are mapped exactly once.
- Focused and real Required entrypoints pass with unchanged tracked state.
- Required does not invoke observation, credentials, authentication, publication, or registry mutation.
- PROV-05 remains mapped only to Phase 8.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-06-SUMMARY.md` when done.</output>

