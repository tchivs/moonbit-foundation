---
phase: 06-namespace-authority-and-compatibility-contract
plan: "12"
type: execute
wave: 6
depends_on: [06-07]
files_modified:
  - modules/mb-core/moon.mod.json
  - modules/mb-color/moon.mod.json
  - modules/mb-image/moon.mod.json
  - modules/mb-core/bytes/moon.pkg
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
must_haves:
  truths:
    - "D-02/D-13: all three module roots project the exact ordered tchivs/* 0.1.0 policy DAG."
    - "D-10/D-11/D-13: this unpublished bootstrap correction preserves 0.1.0 and the four compatibility classes."
    - "D-14/D-15: the root change neither rewrites history nor creates an umbrella or additional module."
  artifacts:
    - path: "modules/mb-core/moon.mod.json"
      provides: "Canonical leaf module root"
      contains: "tchivs/mb-core"
    - path: "modules/mb-color/moon.mod.json"
      provides: "Canonical middle module root with exact core floor"
      contains: "tchivs/mb-core"
    - path: "modules/mb-image/moon.mod.json"
      provides: "Canonical top module root with exact core/color floors"
      contains: "tchivs/mb-color"
    - path: "modules/mb-core/bytes/moon.pkg"
      provides: "One bounded root-adjacent smoke package moved out of the 15-file source plan"
  key_links:
    - from: "policy/release-qualification.json"
      to: "modules/mb-image/moon.mod.json"
      via: "ordinal module/version/dependency projection"
      pattern: "tchivs/mb-image"
---

<!-- phase-source-audit: GOAL-06,COMP-01,COMP-02,COMP-03,COMP-04,D-02,D-10,D-11,D-13,D-14,D-15,RESEARCH-MODULE-ROOT-BOUNDARY -->

<objective>
Rebase only the three MoonBit module roots and one bounded root-adjacent package.

Purpose: Separate manifest authority from the wider 17-package import migration while keeping every pending plan within its explicit file budget.
Output: Three canonical module manifests plus the `mb-core/bytes` smoke package on `tchivs/*` at 0.1.0.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-07-SUMMARY.md
@policy/release-qualification.json
@moon.work
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase the exact three module roots</name>
  <read_first>policy/release-qualification.json, modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json</read_first>
  <files>modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json</files>
  <action>Set the roots to `tchivs/mb-core`, `tchivs/mb-color`, and `tchivs/mb-image`. Set mb-color's core dependency and mb-image's core/color dependencies to those exact identities at 0.1.0. Preserve targets, descriptions, license, version, and all other closed metadata. Do not touch any package file except the separately declared bytes smoke package.</action>
  <verify><automated>pwsh -NoProfile -Command "$e=@{'mb-core'='tchivs/mb-core';'mb-color'='tchivs/mb-color';'mb-image'='tchivs/mb-image'}; foreach($m in $e.Keys){$x=Get-Content -Raw ('modules/'+$m+'/moon.mod.json')|ConvertFrom-Json -Depth 100; if($x.name -cne $e[$m] -or $x.version -cne '0.1.0'){throw ('manifest drift: '+$m)}}"</automated></verify>
  <acceptance_criteria>The three manifests exactly project policy and retain candidate version 0.1.0.</acceptance_criteria>
  <done>The module-root authority boundary is canonical.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate one root-adjacent smoke package and prove bounded closure</name>
  <read_first>modules/mb-core/moon.mod.json, modules/mb-core/bytes/moon.pkg, policy/release-qualification.json, policy/phase-01-source-audit.json</read_first>
  <files>modules/mb-core/bytes/moon.pkg</files>
  <action>Replace any positive old-owner package/import identity in `modules/mb-core/bytes/moon.pkg` with the exact `tchivs/mb-core/*` identity while preserving API, behavior, tests, and targets. Run the leaf-module four-target check as a bounded smoke test. Do not broaden into the other 16 package files, docs, consumers, baselines, generated outputs, or history.</action>
  <verify><automated>pwsh -NoProfile -Command "foreach($t in @('js','wasm','wasm-gc','native')){& moon -C modules/mb-core check --target $t --frozen; if($LASTEXITCODE){exit $LASTEXITCODE}}; $h=(Get-FileHash -Algorithm SHA256 policy/phase-01-source-audit.json).Hash.ToLowerInvariant(); if($h -cne '52f118333892cfe1044b8105a6ea5d03f1ab087d3f7875d13b79c4e5b7640a7a'){throw 'historical audit changed'}"</automated></verify>
  <acceptance_criteria>The roots and bytes smoke package agree with policy, the leaf module checks on four targets, and historical evidence is unchanged.</acceptance_criteria>
  <done>The bounded root migration is ready for the remaining package graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Policy -> module roots | Mixed owners or dependency floors could authorize the wrong graph. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-25 | Spoofing/Tampering | module manifests | high | mitigate | Exact ordinal root/dependency projection and four-target smoke check. |

## OWASP ASVS L1 Control Mapping
| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Applicable | Policy-owned roots cannot infer live authority. | Tasks 1-2 |
| V5 Validation | Applicable | Exact JSON and bounded target checks. | Tasks 1-2 |
</threat_model>

## Artifacts this phase produces
- Three canonical module roots and one bounded smoke package.

<verification>Run both automated commands and confirm only the four declared files are modified.</verification>

<success_criteria>
- Three roots exactly match the policy DAG at 0.1.0.
- The bytes package is canonical and the leaf module passes four targets.
- No broader source, evidence, documentation, or history scope is touched.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-12-SUMMARY.md` when done.</output>
