---
phase: 06-namespace-authority-and-compatibility-contract
plan: "13"
type: execute
wave: 9
depends_on: [06-09]
files_modified:
  - qualification/consumers/mb-core/moon.mod.json
  - qualification/consumers/mb-core/main/moon.pkg
  - qualification/negative/higher-layer-dependency/mb-core.moon.pkg
  - qualification/negative/path-dependency/mb-color.moon.mod.json
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/Test-ReleaseQualification.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, PROV-03]
must_haves:
  truths:
    - "D-02/D-13/D-14: packaged-copy consumers resolve only the canonical tchivs/* graph; old identities survive only in named drift negatives."
    - "D-07/D-08/D-10/D-11: positive release qualification, package consumers, archive checks, and exact negative ownership remain intact."
    - "D-18: all paths remain credential-free and publication stays blocked."
  artifacts:
    - path: "qualification/consumers/mb-core/moon.mod.json"
      provides: "Canonical isolated leaf consumer"
    - path: "qualification/negative/higher-layer-dependency/mb-core.moon.pkg"
      provides: "Named dependency-direction negative retaining explicit drift input"
    - path: "scripts/quality/Test-ReleaseQualification.ps1"
      provides: "Positive packaged-copy consumer proof"
    - path: "scripts/quality/Test-ReleaseQualificationNegative.ps1"
      provides: "Exact negative rule ownership"
  key_links:
    - from: "scripts/quality/Invoke-ReleaseQualification.ps1"
      to: "qualification/consumers/mb-core/moon.mod.json"
      via: "isolated packaged-copy consumer execution"
      pattern: "consumer"
    - from: "scripts/quality/Test-ReleaseQualificationNegative.ps1"
      to: "qualification/negative/path-dependency/mb-color.moon.mod.json"
      via: "named path-dependency failure"
      pattern: "path-dependency"
---

<!-- phase-source-audit: GOAL-06,COMP-01,COMP-02,COMP-03,COMP-04,PROV-03,D-02,D-07,D-08,D-10,D-11,D-13,D-14,D-18,RESEARCH-PACKAGE-CONSUMER-MIGRATION -->

<objective>
Migrate the isolated package consumers and preserve exact release-qualification negatives.

Purpose: Prove the canonical graph outside ordinary workspace resolution without mixing example or benchmark ownership.
Output: Passing positive packaged-copy qualification plus exact named negative fixtures on `tchivs/*`.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-09-SUMMARY.md
@policy/release-qualification.json
@scripts/quality/Invoke-ReleaseQualification.ps1
@scripts/quality/Test-ReleaseQualification.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the bounded positive package consumer</name>
  <read_first>qualification/consumers/mb-core/moon.mod.json, qualification/consumers/mb-core/main/moon.pkg, qualification/consumers/mb-core/main/main.mbt, qualification/consumers/downstream-public/main.mbt, policy/release-qualification.json</read_first>
  <files>qualification/consumers/mb-core/moon.mod.json, qualification/consumers/mb-core/main/moon.pkg</files>
  <action>Rewrite only the leaf consumer manifest and package import to exact `tchivs/mb-core` identities. Inspect both behavior sources but preserve them byte-for-byte. Preserve isolated copy semantics, exact version 0.1.0, public behavioral assertion, no path dependency, and no credential/global Moon-home use.</action>
  <verify><automated>pwsh -NoProfile -Command "$m=Get-Content -Raw qualification/consumers/mb-core/moon.mod.json|ConvertFrom-Json -Depth 100; if($m.deps.'tchivs/mb-core' -ne '0.1.0'){throw 'consumer dependency drift'}; if((Get-Content -Raw qualification/consumers/mb-core/main/moon.pkg) -notmatch 'tchivs/mb-core'){throw 'consumer import drift'}"</automated></verify>
  <acceptance_criteria>The bounded tracked consumer uses the exact personal identity and its behavior source is unchanged.</acceptance_criteria>
  <done>The positive leaf consumer is canonical.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile positive release qualification</name>
  <read_first>scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-ReleaseQualification.ps1, policy/release-qualification.json, qualification/consumers/mb-core/moon.mod.json</read_first>
  <files>scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-ReleaseQualification.ps1</files>
  <action>Update positive identity expectations and generated packaged-copy consumer templates to the exact ordered `tchivs/*` graph. Preserve module order, exact versions and dependency floors, package/archive checks, isolated copy execution, four-target behavioral assertions, blocked publication state, and credential-free operation. Run the real non-static positive path.</action>
  <verify><automated>pwsh -NoProfile -Command "$e='artifacts/release-qualification/phase-06-plan-13'; & ./scripts/quality/Invoke-ReleaseQualification.ps1 -Check -OutputDirectory $e; if($LASTEXITCODE){exit $LASTEXITCODE}; & ./scripts/quality/Test-ReleaseQualification.ps1"</automated></verify>
  <acceptance_criteria>Positive release qualification and all package consumers pass on the canonical graph without registry mutation.</acceptance_criteria>
  <done>The positive packaged-copy path is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve exact negative fixture ownership</name>
  <read_first>qualification/negative/higher-layer-dependency/mb-core.moon.pkg, qualification/negative/path-dependency/mb-color.moon.mod.json, scripts/quality/Test-ReleaseQualificationNegative.ps1</read_first>
  <files>qualification/negative/higher-layer-dependency/mb-core.moon.pkg, qualification/negative/path-dependency/mb-color.moon.mod.json, scripts/quality/Test-ReleaseQualificationNegative.ps1</files>
  <action>Reconcile the two explicitly named identity-bearing fixtures and their owning rules. Retain old identities only where the fixture is an intentional drift input expected to fail; otherwise use `tchivs/*`. Preserve all other exact negatives, unique ownership, failure precedence, and no broad regex/path allowlist.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-ReleaseQualificationNegative.ps1</automated></verify>
  <acceptance_criteria>Every release-qualification negative still fails under one exact owning rule and no old identity becomes positive truth.</acceptance_criteria>
  <done>The negative boundary remains exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Workspace -> packaged copy | Local resolution could hide stale consumer identities. |
| Drift fixture -> positive truth | An old identity retained for testing could escape its named negative. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-26 | Spoofing | packaged consumers | high | mitigate | Isolated copy, exact versions, and real positive execution. |
| T-06-27 | Elevation of Privilege | negative fixtures | high | mitigate | Named fixtures, unique rule ownership, and exact failure assertions. |

## OWASP ASVS L1 Control Mapping
| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Applicable | Consumers accept only canonical identities. | Tasks 1-2 |
| V5 Validation | Applicable | Positive execution and exact negative ownership. | Tasks 1-3 |
</threat_model>

## Artifacts this phase produces
- Canonical packaged-copy consumer and release-qualification positive/negative proof.

<verification>Run all three commands and confirm only the seven declared files can change.</verification>

<success_criteria>
- Positive isolated consumers and release qualification pass.
- Old identities occur only in explicit named negatives.
- No example, benchmark, documentation, support route, or baseline is modified.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-13-SUMMARY.md` when done.</output>
