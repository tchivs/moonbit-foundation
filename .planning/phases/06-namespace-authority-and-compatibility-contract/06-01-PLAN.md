---
phase: 06-namespace-authority-and-compatibility-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - policy/registry-authority.json
  - release/registry/authority-observation-schema.json
  - release/registry/capability-matrix-schema.json
  - release/registry/authority-observation.json
  - release/registry/capability-matrix.json
  - scripts/quality/Invoke-RegistryObservation.ps1
  - scripts/quality/Test-RegistryAuthority.ps1
  - scripts/quality/Test-RegistryAuthorityNegative.ps1
autonomous: true
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "D-01/D-02: one closed machine-readable policy owns intended namespace, exact module identities, pinned toolchain, required-current facts, and dispositions; a mismatch blocks and never guesses a replacement identity."
    - "D-03/D-04: tracked evidence contains only allowlisted sanitized fields and classifies every capability as documented, safely_observed, or unknown with a source and disposition."
    - "D-05/D-06: the operator seam is read-only and separate from Required; the three production modules are never published or mutated by this phase."
    - "REG-01/REG-03: this plan cannot complete until an allowlisted authenticated/read-only observation proves the exact moonbit-foundation namespace and all three canonical identities; unavailable or ambiguous auth/evidence blocks honestly."
  artifacts:
    - path: "policy/registry-authority.json"
      provides: "Canonical intended identities, freshness rules, required facts, and unknown dispositions"
      contains: "moonbit-foundation/mb-core"
    - path: "release/registry/authority-observation.json"
      provides: "Credential-redacted repository-bound authority evidence with explicit unknown state"
      contains: "credentials_read"
    - path: "release/registry/capability-matrix.json"
      provides: "Closed documented/safely_observed/unknown registry capability matrix"
      contains: "destructive_recovery"
    - path: "scripts/quality/Test-RegistryAuthority.ps1"
      provides: "Credential-free fail-closed authority and freshness validator"
  key_links:
    - from: "scripts/quality/Test-RegistryAuthority.ps1"
      to: "policy/registry-authority.json"
      via: "exact identity, freshness, required-fact, and disposition validation"
      pattern: "registry-authority[.]json"
    - from: "scripts/quality/Invoke-RegistryObservation.ps1"
      to: "release/registry/authority-observation-schema.json"
      via: "sanitize into allowlisted scalar fields before any persistence"
      pattern: "authority-observation-schema[.]json"
  assumptions:
    - { id: EDGE-REG-01-BOUNDARY, statement: "[FLAGGED-UNVERIFIED] Review authority-evidence freshness thresholds at the exact maximum age and immediately beyond it.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-ADJACENCY, statement: "[FLAGGED-UNVERIFIED] Review whether authenticated account identity equal to the intended owner string is still insufficient without an explicit namespace-authorization fact.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-EMPTY, statement: "[FLAGGED-UNVERIFIED] Review missing, null, or single-module authority evidence and require a precise blocked result.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-ENCODING, statement: "[FLAGGED-UNVERIFIED] Review canonical identity equality as ordinal UTF-8 text with no normalization or case folding.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-ORDERING, statement: "[FLAGGED-UNVERIFIED] Review stable ordering of the three module authority records and capability rows.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-PRECISION, statement: "[FLAGGED-UNVERIFIED] Review UTC timestamp parsing and freshness arithmetic for truncation, overflow, and clock-boundary ambiguity.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-02-UNCLASSIFIED, statement: "[FLAGGED-UNVERIFIED] Manually review the capability matrix for a registry semantic omitted by the deterministic taxonomy.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-03-UNCLASSIFIED, statement: "[FLAGGED-UNVERIFIED] Manually review publication-readiness facts and unknown dispositions for a fail-open omission.", verification: manual-review, flagged: true }
  prohibitions:
    - { id: PROH-REG-CREDENTIALS, requirement_id: REG-01, category: confidentiality, statement: "MUST NOT read, record, hash, copy, or derive evidence from tokens, cookies, authorization headers, credential files, or credential paths.", status: unverified, flagged: true }
    - { id: PROH-REG-MUTATION, requirement_id: REG-02, category: safety, statement: "MUST NOT publish, overwrite, delete, unpublish, yank, or otherwise mutate mb-core, mb-color, mb-image, or guess a replacement module identity.", status: unverified, flagged: true }
---

<!-- phase-source-audit: GOAL-06,REG-01,REG-02,REG-03,D-01,D-02,D-03,D-04,D-05,D-06,RESEARCH-AUTHORITY-PLANE,RESEARCH-CAPABILITY-UNKNOWNS,RESEARCH-CREDENTIAL-BOUNDARY,CONTEXT-REGISTRY-AUTHORITY,CONTEXT-SAFE-PROBING -->

<objective>
Establish the fail-closed namespace-authority contract and safe observation boundary before any credentialed publication.

Purpose: Give the sole maintainer a repository-bound truth source for registry authority and capability uncertainty without weakening the credential-free Required lane.
Output: Closed policy/schemas, explicit initial evidence and capability matrix, an operator-only sanitizer, and exact positive/negative validators.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-RESEARCH.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-PATTERNS.md
@policy/release-qualification.json
@release/qualification/package-schema.json
@scripts/quality/ReleaseQualification.Common.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze closed authority and capability contracts</name>
  <read_first>policy/release-qualification.json, release/qualification/package-schema.json, .planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md, .planning/phases/06-namespace-authority-and-compatibility-contract/06-PATTERNS.md</read_first>
  <files>policy/registry-authority.json, release/registry/authority-observation-schema.json, release/registry/capability-matrix-schema.json, release/registry/authority-observation.json, release/registry/capability-matrix.json</files>
  <action>Using `policy/release-qualification.json` as the exact centralized-policy analog and `release/qualification/package-schema.json` as the exact closed-schema analog, define versioned, `additionalProperties: false` contracts for D-01 through D-04. Preserve `moonbit-foundation/mb-core`, `mb-color`, and `mb-image` exactly; record the pinned moon/moonc/moonrun triplet, source commit, UTC observation time, allowlisted command identifier/argument shape, sanitized result, stable digest, freshness, and separate run-local fields. Seed current unproven registry facts as explicit `unknown` values with blocking or forward-only dispositions, including registry-render metadata; never fabricate live authority. The stable digest excludes absolute paths and run-local time fields except the authority observation timestamp that is itself freshness evidence.</action>
  <verify><automated>pwsh -NoProfile -Command "$paths=@('policy/registry-authority.json','release/registry/authority-observation-schema.json','release/registry/capability-matrix-schema.json','release/registry/authority-observation.json','release/registry/capability-matrix.json'); foreach($p in $paths){Get-Content -Raw $p|ConvertFrom-Json -Depth 100|Out-Null}; $m=Get-Content -Raw release/registry/capability-matrix.json|ConvertFrom-Json -Depth 100; if(@($m.capabilities).Count -lt 7){throw 'capability matrix incomplete'}"</automated></verify>
  <acceptance_criteria>D-01 through D-04 are represented by separate policy and evidence objects; all current live gaps are explicit unknowns, all objects are closed/versioned, and no credential material or guessed identity exists.</acceptance_criteria>
  <done>REG-01 and REG-02 have reviewable tracked contracts even while live authority remains honestly blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the operator-only sanitized observation seam</name>
  <read_first>scripts/quality/ReleaseQualification.Common.ps1, policy/registry-authority.json, release/registry/authority-observation-schema.json, release/registry/capability-matrix-schema.json</read_first>
  <files>scripts/quality/Invoke-RegistryObservation.ps1</files>
  <action>Create a strict-mode operator-only collector using `ReleaseQualification.Common.ps1` hashing, exact-set, UTF-8-no-BOM, and guarded-write patterns. Accept only a closed command allowlist for local CLI help, authenticated identity/status, and read-only registry observations; never open a credential path, enumerate environment secrets, or invoke publish/mutation commands. Capture command output in memory, project only allowlisted scalars, reject token/header/cookie/path/secret-shaped values before writing, and write atomically only after schema-shaped validation. Keep this script unreachable from `scripts/quality.ps1`, `Invoke-MoonQuality.ps1`, and CI. Scope API-coverage contribution to the external registry observation seam only; do not create or broaden `COVERAGE.md`.</action>
  <verify><automated>pwsh -NoProfile -Command "$s=Get-Content -Raw scripts/quality/Invoke-RegistryObservation.ps1; @('Set-StrictMode','safely_observed','unknown','SHA256')|ForEach-Object{if(-not $s.Contains($_)){throw ('collector missing '+$_)}}; if($s -match 'Get-Content.+credentials|moon\s+publish|Invoke-MoonQuality'){throw 'unsafe collector linkage'}"</automated></verify>
  <acceptance_criteria>The collector sanitizes before persistence, invokes only safe read-only commands, never reads credentials, and remains outside Required/CI.</acceptance_criteria>
  <done>D-05 and D-06 have an executable but deliberately operator-invoked observation seam.</done>
</task>

<task type="auto">
  <name>Task 3: Capture exact namespace proof and enforce authority readiness</name>
  <read_first>scripts/quality/Test-ReleaseQualificationNegative.ps1, scripts/quality/ReleaseQualification.Common.ps1, policy/registry-authority.json, release/registry/authority-observation.json, release/registry/capability-matrix.json</read_first>
  <files>release/registry/authority-observation.json, scripts/quality/Test-RegistryAuthority.ps1, scripts/quality/Test-RegistryAuthorityNegative.ps1</files>
  <action>First implement the credential-free validator and table-driven derived-negative suite using the exact `Confirm-ExactRule` and guarded-temp-copy analog. Then run `Invoke-RegistryObservation.ps1` explicitly as an operator checkpoint using only its closed authenticated/read-only allowlist. It may use the CLI's existing authenticated session but must never ask for, open, enumerate, hash, or persist credential files, environment secrets, tokens, raw output, or secret-derived values. Persist only sanitized schema-valid evidence proving the authenticated account's exact authority over `moonbit-foundation` and the three exact module identities. If existing authentication is unavailable, namespace authority is not observable, or any fact is ambiguous, stop the plan as blocked with the unknown artifact intact; do not mark REG-01 complete. After successful capture, validate closed properties, identities/order, toolchain, freshness, stable digest, command shape, capability dispositions, and separate account-versus-namespace authorization; `-AssertPublishReady` passes only for the exact current proof. Add exact negatives for missing/extra facts, identity drift, stale evidence, digest tampering, secret/header/cookie/path-shaped values, unsupported state, required unknown treated as pass, and optional unknown without disposition.</action>
  <verify><automated>pwsh -NoProfile -Command "& ./scripts/quality/Invoke-RegistryObservation.ps1 -CaptureAuthority -OutputPath release/registry/authority-observation.json; if($LASTEXITCODE){throw 'REG-01 blocked: safe authenticated read-only authority evidence unavailable'}; & ./scripts/quality/Test-RegistryAuthority.ps1 -AssertPublishReady; if($LASTEXITCODE){exit $LASTEXITCODE}; & ./scripts/quality/Test-RegistryAuthorityNegative.ps1"</automated></verify>
  <acceptance_criteria>Every failure has one stable owning rule, and the plan completes only with fresh sanitized proof of exact moonbit-foundation authority plus all three identities; unavailable/ambiguous evidence blocks without credential access, publication, mutation, or guessing.</acceptance_criteria>
  <done>REG-01 and REG-03 are proven by safe dynamic authorization evidence, or Phase 6 remains honestly blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated/read-only CLI output -> tracked observation | Untrusted external text must be sanitized before persistence. |
| Tracked authority evidence -> publication readiness | Evidence can authorize later privileged publication and therefore must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Spoofing/Elevation of Privilege | account and namespace facts | high | mitigate | Require separate exact namespace authorization evidence; account identity equality alone never passes. |
| T-06-02 | Information Disclosure | observation collector | high | mitigate | Allowlist scalar projection, reject secret/header/cookie/path shapes, never read credential files, and persist no raw output. |
| T-06-03 | Tampering/Repudiation | observation and capability artifacts | high | mitigate | Closed schemas, source commit/toolchain/time binding, stable SHA-256 recomputation, freshness checks, and exact negative ownership. |

## OWASP ASVS L1 Control Mapping

| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V2 Authentication | Applicable | Observe only allowlisted authenticated status without handling credential material. | Task 2 |
| V3 Session Management | Limited | Expiry/revocation/non-interactive behavior remains explicit unknown and blocks unattended readiness. | Tasks 1, 3 |
| V4 Access Control | Applicable | Exact namespace authority is distinct from local authentication and gates publication. | Tasks 1, 3 |
| V5 Validation, Sanitization and Encoding | Applicable | Closed schemas, ordinal identities, allowlisted fields, pre-persistence sanitization, and exact negatives. | Tasks 1-3 |
| V6 Stored Cryptography | Limited | Platform SHA-256 provides integrity only and is never presented as authority authentication. | Tasks 1, 3 |
</threat_model>

## Artifacts this phase produces

- `policy/registry-authority.json` — canonical authority/freshness/disposition policy.
- `release/registry/authority-observation.json` — sanitized repository-bound evidence with honest unknowns.
- `release/registry/capability-matrix.json` — credential-redacted capability facts table.
- `scripts/quality/Invoke-RegistryObservation.ps1` — operator-only read-only observation seam.
- `scripts/quality/Test-RegistryAuthority.ps1` and `Test-RegistryAuthorityNegative.ps1` — fail-closed validation contract.

<verification>
Run the explicit safe observation checkpoint, authority readiness validator, and negative suite; confirm exact namespace/identity proof exists and no Required/CI path invokes the operator collector.
</verification>

<success_criteria>
- REG-01, REG-02, and REG-03 are fully represented without credentials, publication, mutation, or identity guessing.
- D-01 through D-06 and all eight registry edge-probe items remain explicit.
- The two flagged safety prohibitions remain unverified until implementation proves them.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-01-SUMMARY.md` when done.</output>
