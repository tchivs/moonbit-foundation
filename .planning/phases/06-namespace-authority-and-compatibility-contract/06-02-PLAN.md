---
phase: 06-namespace-authority-and-compatibility-contract
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - compatibility/schema/baseline-schema.json
  - compatibility/baselines/0.1.0/manifest.json
  - compatibility/baselines/0.1.0/mb-core
  - compatibility/baselines/0.1.0/mb-color
  - compatibility/baselines/0.1.0/mb-image
  - scripts/quality/New-PublicInterfaceBaseline.ps1
  - scripts/quality/Test-PublicInterfaceBaseline.ps1
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "D-07/D-09: all 17 policy-owned public packages emit exactly 68 normalized package-target records across js, wasm, wasm-gc, and native, each bound to canonical raw evidence, pinned toolchain, schema, and digests."
    - "Two independent frozen clean copies produce byte-identical normalized records; generation is mechanical, atomic, and check mode never mutates tracked source."
    - "The baseline proves public-interface text stability only and makes no behavioral, semantic, resource, layout, or performance claim."
  artifacts:
    - path: "compatibility/schema/baseline-schema.json"
      provides: "Closed raw/normalized package-target baseline contract"
    - path: "compatibility/baselines/0.1.0/manifest.json"
      provides: "Exact 17-package and 68-record mechanical inventory with digests"
      contains: "normalization_schema_version"
    - path: "scripts/quality/New-PublicInterfaceBaseline.ps1"
      provides: "Bounded two-clean-copy generator and check mode"
  key_links:
    - from: "scripts/quality/New-PublicInterfaceBaseline.ps1"
      to: "compatibility/schema/baseline-schema.json"
      via: "closed normalization and stable digest serialization"
      pattern: "baseline-schema[.]json"
    - from: "compatibility/baselines/0.1.0/manifest.json"
      to: "policy/release-qualification.json"
      via: "exact 6+5+6 public package inventory"
      pattern: "mb-core"
  assumptions:
    - { id: EDGE-COMP-01-ADJACENCY, statement: "[FLAGGED-UNVERIFIED] Review declaration equality and additions adjacent to overloads or identities for ambiguous matching.", verification: manual-review, flagged: true }
    - { id: EDGE-COMP-01-EMPTY, statement: "[FLAGGED-UNVERIFIED] Review empty interfaces, missing raw files, and single-declaration packages as distinct cases.", verification: manual-review, flagged: true }
    - { id: EDGE-COMP-01-ENCODING, statement: "[FLAGGED-UNVERIFIED] Review LF/CRLF, UTF-8, and identifier normalization while preserving raw-byte evidence.", verification: manual-review, flagged: true }
    - { id: EDGE-COMP-01-ORDERING, statement: "[FLAGGED-UNVERIFIED] Review declaration ordering and stable ties without erasing meaningful order.", verification: manual-review, flagged: true }
---

<!-- phase-source-audit: GOAL-06,COMP-01,D-07,D-08,D-09,RESEARCH-INTERFACE-BASELINES,RESEARCH-CLEAN-COPY-PROBE,CONTEXT-PUBLIC-INTERFACE -->

<objective>
Generate the bounded canonical public-interface baseline inventory.

Purpose: Freeze reproducible raw and normalized evidence for exactly 17 packages × 4 targets before delta policy is applied.
Output: One closed schema, one manifest, mechanical module baseline trees, and focused generation tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-RESEARCH.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-PATTERNS.md
@policy/release-qualification.json
@scripts/quality/ReleaseQualification.Common.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the bounded baseline schema and generator</name>
  <read_first>policy/release-qualification.json, release/qualification/package-schema.json, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-MoonQuality.ps1</read_first>
  <files>compatibility/schema/baseline-schema.json, scripts/quality/New-PublicInterfaceBaseline.ps1</files>
  <action>Using the release schema and common helpers as exact analogs, define a closed D-07/D-09 record binding commit, exact moon/moonc/moonrun, module/package/target, normalization version, canonical raw digest, normalized digest, target inspection, and two-run equality. Implement one bounded parser/generator script for the currently emitted `.mbti` grammar only. Unknown syntax, target divergence, missing records, or toolchain drift returns unknown and stops generation. Run canonical `moon info` plus separate frozen target inspections in two independently guarded clean copies; sanitize paths, write UTF-8 without BOM atomically, and keep check mode read-only.</action>
  <verify><automated>pwsh -NoProfile -Command "$s=Get-Content -Raw compatibility/schema/baseline-schema.json|ConvertFrom-Json -Depth 100; & ./scripts/quality/New-PublicInterfaceBaseline.ps1 -CheckSchema"</automated></verify>
  <acceptance_criteria>The schema is closed and the single generator has explicit bounded grammar, unknown handling, two-copy equality, safe cleanup, and no compatibility classifier or registry behavior.</acceptance_criteria>
  <done>The mechanical generation contract is executable without scope from comparator/version policy.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize and prove exactly 68 mechanical records</name>
  <read_first>scripts/quality/New-PublicInterfaceBaseline.ps1, compatibility/schema/baseline-schema.json, policy/release-qualification.json, scripts/quality/Test-ReleaseQualificationNegative.ps1</read_first>
  <files>compatibility/baselines/0.1.0/manifest.json, compatibility/baselines/0.1.0/mb-core, compatibility/baselines/0.1.0/mb-color, compatibility/baselines/0.1.0/mb-image, scripts/quality/Test-PublicInterfaceBaseline.ps1</files>
  <action>Generate raw evidence and normalized records mechanically for the policy-owned 6 mb-core + 5 mb-color + 6 mb-image packages at js, wasm, wasm-gc, and native: exactly 17 packages and 68 package-target records. The module directories are generated output, not hand-authored files; the manifest owns their exact paths, counts, order, and digests. Add focused positives and exact negatives for 67/69 records, duplicate/missing package-target pairs, unknown syntax, target divergence, toolchain mismatch, unstable second run, digest drift, partial output, and tracked mutation.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-PublicInterfaceBaseline.ps1</automated></verify>
  <acceptance_criteria>The manifest enumerates exactly 17 packages and 68 unique records, two clean generations match, all raw/normalized digests validate, and every partial/ambiguous case fails closed.</acceptance_criteria>
  <done>COMP-01 is proven by a bounded mechanical inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Compiler text -> normalized baseline | Parser omissions could erase public surface. |
| Generated tree -> authoritative manifest | Partial or tampered output could masquerade as complete evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-04 | Tampering | 68-record baseline inventory | high | mitigate | Closed manifest, raw/normalized digests, exact counts/order, two clean copies, and tracked-mutation proof. |
| T-06-05 | Elevation of Privilege | bounded parser | high | mitigate | Unknown syntax and target/toolchain ambiguity stop generation rather than normalize away. |

## OWASP ASVS L1 Control Mapping

| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Limited | Only complete policy-owned evidence becomes authoritative. | Task 2 |
| V5 Validation, Sanitization and Encoding | Applicable | Closed schema, UTF-8 rules, bounded grammar, exact inventory, and negatives. | Tasks 1-2 |
| V6 Stored Cryptography | Limited | Platform SHA-256 binds evidence integrity only. | Tasks 1-2 |
</threat_model>

## Artifacts this phase produces

- `compatibility/schema/baseline-schema.json` — closed baseline record.
- `compatibility/baselines/0.1.0/` — mechanical 17-package × 4-target evidence.
- `scripts/quality/New-PublicInterfaceBaseline.ps1` — bounded generator/checker.
- `scripts/quality/Test-PublicInterfaceBaseline.ps1` — exact inventory and failure tests.

<verification>Run the focused baseline suite and confirm exactly 68 records, two-clean-run equality, no tracked mutation, and no semantic-compatibility claim.</verification>

<success_criteria>
- COMP-01 and D-07 through D-09 are executable.
- All four COMP-01 edge assumptions remain explicit.
- Generated inventory is explicitly mechanical and bounded to 17 × 4 records.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-02-SUMMARY.md` when done.</output>

