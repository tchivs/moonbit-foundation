---
phase: 06-namespace-authority-and-compatibility-contract
plan: "07"
type: execute
wave: 5
depends_on: [06-05]
files_modified:
  - policy/foundation.json
  - policy/registry-authority.json
  - policy/release-qualification.json
  - policy/compatibility.json
  - release/qualification/package-schema.json
  - release/registry/authority-observation-schema.json
  - release/registry/authority-observation.json
  - scripts/quality/Test-RegistryAuthority.ps1
autonomous: true
requirements: [REG-01, REG-02, REG-03, COMP-03, COMP-04]
must_haves:
  truths:
    - "D-02/D-13: policy and closed schemas name exactly tchivs/mb-core, tchivs/mb-color, and tchivs/mb-image at 0.1.0 before module roots are changed."
    - "D-10/D-11/D-13: the unpublished owner correction is a bootstrap identity rebase, not a fifth compatibility class, migration note, or SemVer bump."
    - "D-14/D-15: policy preserves MoonBit Native Foundation branding and does not rewrite archived v0.1 evidence."
    - "D-16/D-17: future ownership is forward-only and repository metadata separates intended from verified-live state."
  artifacts:
    - path: "policy/registry-authority.json"
      provides: "Canonical personal owner and exact module identities"
      contains: "tchivs/mb-core"
    - path: "release/registry/authority-observation.json"
      provides: "Sanitized tracked seed for the personal owner with every unobserved live fact still unknown"
      contains: "tchivs"
    - path: "policy/release-qualification.json"
      provides: "Ordered candidate module and dependency authority"
      contains: "tchivs/mb-image"
  key_links:
    - from: "policy/registry-authority.json"
      to: "policy/release-qualification.json"
      via: "ordinal owner/module identity equality"
      pattern: "tchivs/mb-core"
    - from: "policy/registry-authority.json"
      to: "release/registry/authority-observation.json"
      via: "intended owner, namespace, pinned toolchain, unknown-first facts, and stable projection equality"
      pattern: "tchivs"
  prohibitions:
    - { id: PROH-IDENTITY-HISTORY-REWRITE, requirement_id: COMP-01, category: integrity, statement: "MUST NOT rewrite archived v0.1 evidence, completed Phase 6 summaries, or policy/phase-01-source-audit.json to make the personal namespace appear historical.", status: unverified, flagged: true }
---

<!-- phase-source-audit: GOAL-06,REG-01,REG-02,REG-03,COMP-03,COMP-04,D-02,D-10,D-11,D-13,D-14,D-15,D-16,D-17,RESEARCH-POLICY-FIRST-REBASE -->

<objective>
Rebase the canonical machine policy and closed evidence contracts onto the personal Mooncakes owner.

Purpose: Establish one exact identity authority before changing module roots, package imports, consumers, documentation, or generated evidence.
Output: Closed `tchivs/*` policies, schemas, blocked authority seed, and policy validator at candidate version 0.1.0.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-RESEARCH.md
@policy/phase-01-source-audit.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase closed policy and schema identities</name>
  <read_first>policy/foundation.json, policy/registry-authority.json, policy/release-qualification.json, policy/compatibility.json, release/qualification/package-schema.json, release/registry/authority-observation-schema.json, release/registry/authority-observation.json, release/registry/capability-matrix.json, scripts/quality/Invoke-RegistryObservation.ps1, scripts/quality/Test-RegistryAuthority.ps1</read_first>
  <files>policy/foundation.json, policy/registry-authority.json, policy/release-qualification.json, policy/compatibility.json, release/qualification/package-schema.json, release/registry/authority-observation-schema.json, release/registry/authority-observation.json, scripts/quality/Test-RegistryAuthority.ps1</files>
  <action>Apply D-02 and D-13 through D-17 from the policy owner outward. Set owner `tchivs`, the exact ordered module identities, their package identities and dependency floors, candidate 0.1.0, and intended-but-unverified repository metadata. Update the closed observation schema and `Test-RegistryAuthority.ps1` positive owner/module expectations to `tchivs` and the exact three canonical identities, then regenerate `release/registry/authority-observation.json` as a deterministic sanitized `tracked_seed`: intended owner and namespace are `tchivs`; `exact_module_identities` is empty while namespace authority is unknown; session authentication, authenticated account, exact-version availability, publish seam, registry observation, and registry resolution remain unknown/blocking; only the pinned toolchain remains documented; timestamps are null/not-observed; credentials and mutation remain false. Recompute `stable_sha256` from the same stable projection used by `Test-RegistryAuthority.ps1` (excluding only `stable_sha256` and `run_local`) and prove closed-schema validation. Keep publish-ready assertions fail-closed; 06-10 may later extend repository-liveness/documentation negatives but must not be needed for this blocked-state validator pass. Inspect `release/registry/capability-matrix.json`; its stable projection is identity-neutral and already states username-prefixed ownership with authority unknown, so do not rewrite or claim a new digest unless the inspection proves an actual policy projection mismatch. Preserve the four compatibility classes and forward-only future-owner rule. Do not preserve the stale operator observation as current proof, edit archived v0.1 files, completed summaries, user-dirty governance files, or `policy/phase-01-source-audit.json`.</action>
  <verify><automated>pwsh -NoProfile -Command "$p=Get-Content -Raw policy/registry-authority.json|ConvertFrom-Json -Depth 100; $o=Get-Content -Raw release/registry/authority-observation.json|ConvertFrom-Json -Depth 100; if($p.intended_owner -cne 'tchivs' -or $o.intended_owner -cne 'tchivs' -or $o.namespace_authority.namespace -cne 'tchivs'){throw 'owner drift'}; if($o.namespace_authority.state -cne 'unknown' -or @($o.namespace_authority.exact_module_identities).Count -ne 0 -or $o.authenticated_account.state -cne 'unknown' -or $o.freshness.status -cne 'not_observed' -or $o.run_local.captured_by -cne 'tracked_seed'){throw 'tracked seed overclaims live authority'}; @('policy/foundation.json','policy/release-qualification.json','policy/compatibility.json','release/qualification/package-schema.json','release/registry/authority-observation-schema.json')|ForEach-Object{Get-Content -Raw $_|ConvertFrom-Json -Depth 100|Out-Null}; & ./scripts/quality/Test-RegistryAuthority.ps1; if($LASTEXITCODE){exit $LASTEXITCODE}"</automated></verify>
  <acceptance_criteria>All closed machine facts and the sanitized tracked observation seed agree on one exact personal identity family; `Test-RegistryAuthority.ps1` passes in credential-free blocked-state mode before 06-01, every unobserved live fact remains unknown/blocking, the stable digest/schema close, and the identity-neutral capability matrix is unchanged unless a demonstrated projection mismatch requires otherwise.</acceptance_criteria>
  <done>Policy and schemas have one canonical owner.</done>
</task>

<task type="auto">
  <name>Task 2: Prove bounded policy closure without touching roots or historical evidence</name>
  <read_first>policy/registry-authority.json, policy/release-qualification.json, release/registry/authority-observation.json, policy/phase-01-source-audit.json</read_first>
  <files>none</files>
  <action>Compare the exact owner/module/version/dependency facts across the closed policies and tracked blocked seed with ordinal equality, while accepting that roots are migrated only in 06-12 and package imports only in 06-08. Verify the historical Phase 1 source-audit digest and archived/completed paths remain unchanged. Do not weaken blocked publication or execute broad validators before their source consumers are migrated.</action>
  <verify><automated>pwsh -NoProfile -Command "$h=(Get-FileHash -Algorithm SHA256 policy/phase-01-source-audit.json).Hash.ToLowerInvariant(); if($h -cne '52f118333892cfe1044b8105a6ea5d03f1ab087d3f7875d13b79c4e5b7640a7a'){throw 'historical audit changed'}; $a=Get-Content -Raw policy/registry-authority.json|ConvertFrom-Json -Depth 100; if(@($a.module_order).Count -ne 3){throw 'identity inventory drift'}"</automated></verify>
  <acceptance_criteria>Policy and blocked evidence close exactly, historical digest is unchanged, and no module-root or broad source-validator pass is claimed.</acceptance_criteria>
  <done>The bounded canonical policy contract is ready for module-root migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Policy -> later manifests | A later root migration could diverge from the closed authority contract. |
| Historical evidence -> active truth | Rewriting history could erase the reason for the correction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-15 | Spoofing/Elevation of Privilege | identity policy | high | mitigate | Closed ordinal identities, exact roots, and fail-closed external facts. |
| T-06-16 | Tampering/Repudiation | historical source audit | high | mitigate | Immutable history prohibition and later digest-bound closure. |

## OWASP ASVS L1 Control Mapping

| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Applicable | Exact owner/module identities remain distinct from authentication status. | Tasks 1-2 |
| V5 Validation | Applicable | Closed JSON, ordinal equality, and exact negatives. | Tasks 1-2 |
</threat_model>

## Artifacts this phase produces

- Canonical `tchivs/*` policies, schemas, blocked authority evidence, and policy-validation rules.

<verification>Run both task commands and verify module roots, `policy/phase-01-source-audit.json`, and archived/completed history have no diff.</verification>

<success_criteria>
- Canonical policy and closed evidence use exactly `tchivs/*` at 0.1.0 while roots remain owned by 06-12.
- External account and repository liveness remain honestly unverified.
- No historical artifact is rewritten.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-07-SUMMARY.md` when done.</output>
