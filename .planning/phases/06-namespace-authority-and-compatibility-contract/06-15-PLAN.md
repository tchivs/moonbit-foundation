---
phase: 06-namespace-authority-and-compatibility-contract
plan: "15"
type: execute
wave: 12
depends_on: [06-14]
files_modified:
  - compatibility/source-snapshots/0.1.0.json
  - scripts/quality/New-PublicInterfaceBaseline.ps1
  - scripts/quality/Test-PublicInterfaceBaseline.ps1
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
must_haves:
  truths:
    - "The 0.1.0 source snapshot is captured once immediately after 06-14 in compatibility/source-snapshots/0.1.0.json and immutably binds canonical tchivs/* modules, the pinned toolchain, the exact 06-14 source commit, and a deterministic module-tree digest."
    - "D-07/D-09/D-13: exact package batches are independently reproducible and cannot publish the final manifest."
    - "D-08/D-10/D-11: batch generation and finalization reject incomplete, extra, stale, mixed-identity, anchor-mismatched, or non-reproducible output; neither the obsolete manifest source_commit nor a later HEAD can influence evidence."
  artifacts:
    - path: "compatibility/source-snapshots/0.1.0.json"
      provides: "Immutable pre-generation source anchor outside the generated 0.1.0 baseline tree"
      contains: "module_tree_sha256"
    - path: "scripts/quality/New-PublicInterfaceBaseline.ps1"
      provides: "Bounded baseline batching/finalization contract"
    - path: "scripts/quality/Test-PublicInterfaceBaseline.ps1"
      provides: "Bounded baseline batching/finalization contract"
  key_links:
    - from: "compatibility/source-snapshots/0.1.0.json"
      to: "scripts/quality/New-PublicInterfaceBaseline.ps1"
      via: "required exact anchor path, SHA-256, source_commit, module-tree digest, canonical modules, and pinned toolchain"
      pattern: "source_commit"
    - from: "scripts/quality/New-PublicInterfaceBaseline.ps1"
      to: "compatibility/baselines/0.1.0/manifest.json"
      via: "Finalize-only atomic publication after exact complete-tree validation"
      pattern: "Finalize"
---

<!-- phase-source-audit: GOAL-06,COMP-01,COMP-02,COMP-03,COMP-04,D-07,D-08,D-09,D-10,D-11,D-13,RESEARCH-BASELINE-REGENERATION -->

<objective>
Prepare deterministic exact-package baseline batching and manifest finalization.

Purpose: Make package batches isolated and make premature manifest publication impossible.
Output: One immutable tracked source-snapshot anchor plus a tested generator/test interface that separates package generation from finalization.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-14-SUMMARY.md
@policy/release-qualification.json
@compatibility/schema/baseline-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact package batching and guarded finalization</name>
  <read_first>scripts/quality/New-PublicInterfaceBaseline.ps1, scripts/quality/Test-PublicInterfaceBaseline.ps1, policy/release-qualification.json, modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json</read_first>
  <files>compatibility/source-snapshots/0.1.0.json, scripts/quality/New-PublicInterfaceBaseline.ps1, scripts/quality/Test-PublicInterfaceBaseline.ps1</files>
  <action>Immediately after 06-14, capture compatibility/source-snapshots/0.1.0.json exactly once. The canonical JSON records baseline_version 0.1.0, the exact ordered module identities tchivs/mb-core, tchivs/mb-color, tchivs/mb-image, the exact pinned moon/moonc/moonrun identities, source_commit as the 40-hex commit at the completed 06-14 boundary before any 06-15 edit, and module_tree_sha256 computed deterministically from sorted tracked paths plus bytes for the three modules at that commit. The anchor lives outside compatibility/baselines/0.1.0, must not be derived from that tree's obsolete manifest, and an existing non-byte-identical anchor is an error rather than an overwrite. Extend the generator with required -SourceSnapshot compatibility/source-snapshots/0.1.0.json for both batch and Finalize modes. Validate the literal path, canonical JSON SHA-256, identities, toolchain, commit existence, and module-tree digest; materialize generation input from the anchored commit, never mutable HEAD or working-tree module bytes. Every package baseline record and the final manifest must embed the exact anchor SHA-256 and exact anchor source_commit. Batch mode accepts -Packages as a nonempty exact ordered array of canonical full package identities and -BaselineVersion 0.1.0; it validates against the 17-package policy inventory, rejects duplicates, generates two clean temporary trees from the anchored commit for only those packages, compares them byte-for-byte, atomically replaces only selected package subtrees, and cannot mutate manifest.json or the anchor. -Check remains read-only. Finalize rejects anything other than the exact 102 package files whose embedded anchor digest/source_commit match the anchor, then atomically writes/checks the single manifest. Make parameter sets prevent mixed semantics. Add exact negatives for duplicate/unknown/empty packages, cross-batch or manifest mutation, incomplete/extra/stale output, premature finalization, non-read-only check, mutated or mismatched anchor, an obsolete manifest source_commit attempting to override the anchor, and a later synthetic HEAD attempting to alter batch records or finalization. Preserve unknown-syntax failure and two-clean-run proof.</action>
  <verify><automated>pwsh -NoProfile -Command "& ./scripts/quality/Test-PublicInterfaceBaseline.ps1 -ToolingOnly; if($LASTEXITCODE){exit $LASTEXITCODE}; $a='compatibility/source-snapshots/0.1.0.json'; $j=Get-Content -Raw $a|ConvertFrom-Json; if($j.source_commit -notmatch '^[0-9a-f]{40}$' -or $j.module_tree_sha256 -notmatch '^[0-9a-f]{64}$' -or @($j.canonical_modules).Count -ne 3){throw 'invalid immutable source anchor'}; $s=Get-Content -Raw scripts/quality/New-PublicInterfaceBaseline.ps1; foreach($term in @('SourceSnapshot','source_commit','module_tree_sha256','Finalize')){if($s -notmatch $term){throw ('missing anchored interface: '+$term)}}"</automated></verify>
  <acceptance_criteria>The tracked anchor is created once from the exact completed 06-14 commit and cannot be rewritten; the tested interface regenerates any exact package subset only from that anchor, every record binds its path/digest/source_commit, obsolete manifest metadata and later HEAD are proven irrelevant, anchor mismatch/mutation fails closed, and finalization is impossible until the complete exact 102-file package tree exists.</acceptance_criteria>
  <done>Exact-package batching and guarded finalization are ready for the nine bounded generation plans.</done>
</task>

</tasks>

<verification>Run the tooling-only positive and negative suite and confirm only the three declared files can change.</verification>

<success_criteria>
- Batch mode owns only explicitly selected six-file package subtrees.
- Every batch and final record binds compatibility/source-snapshots/0.1.0.json by exact SHA-256 and source_commit, independent of obsolete manifest metadata and later HEAD.
- No partial run can write or replace the final manifest.
- Finalize mode rejects any tree other than exactly 17 packages, 102 package files, and 68 target records.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-15-SUMMARY.md` when done.</output>
