---
phase: 86-ancillary-aware-preflight-and-shared-machine-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - INDEXCOMP-03
must_haves:
  truths:
    - Each non-interlaced selected Type-3 depth has a Fixed winner and a Stored fallback whose selected PngFrameFacts contain the source PLTE length and the shortest one-byte canonical tRNS length.
    - Selected output and work facts admit at their exact limits through one post-check budget charge; one-less output or work rejects before a writer byte or caller-buffered encoder can be exposed.
    - The eager and chunk selectors both traverse the admitted PngEncodeMachine and produce identical ordinary-drain bytes for the selected profile.
    - Palette-capacity and source-construction arithmetic rejections do not consume their supplied budget.
  artifacts:
    - modules/mb-image/png/encode_wbtest.mbt
    - modules/mb-image/png/encode_test.mbt
    - modules/mb-image/png/stream_encode_test.mbt
  key_links:
    - _png_encode_indexed_preflight_with_profile_and_strategy retains the chosen PngFrameFacts and selected_work before PngEncodeMachine::new_with_indexed_profile_and_strategy constructs either facade.
    - PngEncoder::encode_indexed8_with_compression_strategy and PngEncoder::encode_indexed_with_compression_strategy present only a successfully admitted machine.
    - PngChunkEncoder::new_indexed8_with_compression_strategy and PngChunkEncoder::new_indexed_with_compression_strategy return an active encoder only after that same admission succeeds.
---

<objective>
Prove that the existing selected indexed preflight admits one ancillary-aware Stored-or-Fixed plan exactly once and that both public output façades use that admitted machine.

Purpose: Close INDEXCOMP-03 at the admission boundary without changing the Phase 85 compression contract or creating another encoder path.
Output: A focused all-depth selected-facts matrix plus eager and caller-buffered admission evidence using repository test helpers.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-ancillary-aware-preflight-and-shared-machine-integration/86-CONTEXT.md
@.planning/phases/86-ancillary-aware-preflight-and-shared-machine-integration/86-RESEARCH.md
@.planning/phases/86-ancillary-aware-preflight-and-shared-machine-integration/86-PATTERNS.md
@.planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-VERIFICATION.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one admitted ancillary-aware profile through selected facts, eager output, and chunk construction</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Extend the existing indexed-compression matrix helpers, not a second frame or compression oracle. Starting from `png_indexed_compression_matrix_source`, drive a real Type-3 source with its actual palette and one non-opaque alpha entry through `_png_encode_indexed_preflight_with_profile_and_strategy` using `PngCompressionStrategy::FixedOrStored`. Assert that the retained `PngEncodePreflight.frame` is the selected `PngFrameFacts`: its `plte_length` equals `source.palette_length()`, its `trns_length` is the shortest canonical prefix (1), and its selected plan is Fixed for the known winner or Stored for the literal fallback. Then use the public eager selector and the matching `PngChunkEncoder` selector for that same source and strategy; drain the chunk encoder with the ordinary established drain helper and assert equality with fresh eager bytes. This is the production-quality tracer through `_png_encode_indexed_preflight_with_profile_and_strategy` and `PngEncodeMachine::new_with_indexed_profile_and_strategy` (D-01, D-03). Do not add a stream implementation, output collection path, planning formula, Dynamic selection, adaptive filter, Adam7 selector, or lease-hostility test.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter "*indexed compression admission tracer*"</automated>
  </verify>
  <done>A real palette/transparency source has selected facts and fresh eager/chunk byte parity through the one existing machine; the assertion reads the retained frame facts rather than reconstructing PNG framing in tests.</done>
</task>

<task type="auto">
  <name>Task 2: Expand selected-facts admission to every depth and every atomic boundary</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Expand the tracer into the complete matrix of `PngIndexedWireProfile::{One, Two, Four, Eight}` times both known candidate outcomes from `png_indexed_compression_matrix_source`. In `encode_wbtest.mbt`, obtain the selected `total_length` and `selected_work` only from a generous real preflight, then prove four distinct selected-work admission cases against that same selected fact: (1) exact `CodecLimits.max_work` with a generous supplied budget succeeds; (2) one-less `CodecLimits.max_work` with a generous supplied budget returns `Err` and retains the complete pre-call budget snapshot via `png_wb_same_remaining`; (3) exact supplied budget work with generous `CodecLimits` succeeds, exhausts that exact work allowance, and charges the selected work once; and (4) one-less supplied budget work with generous `CodecLimits` returns `Err` and retains the complete pre-call budget snapshot via `png_wb_same_remaining`. Keep exact and one-less `max_output_bytes` as separate selected-frame cases with complete budget snapshots on rejection. In every success case assert the selected plan/frame facts and positive selected work, so the sole charge is demonstrably for the post-check selected plan. Keep the existing selected-depth palette-cap source loop but route it through `FixedOrStored`; assert `indexed-palette-cap` and an unchanged complete budget snapshot (D-01, D-02, D-04, D-05). For each one-less selected-work rejection, exercise both public façades: in eager tests reuse `@io.MemoryWriter`, `png_encode_prefix`, and the full remaining-resource comparator to prove the writer has zero bytes and the complete admission budget snapshot is unchanged; in chunk tests use the real `new_indexed8_with_compression_strategy` and `new_indexed_with_compression_strategy` constructors to prove `Err`, the same complete budget snapshot, and absence of any caller-visible active encoder or lease. Apply the same eager/chunk atomic observations to selected one-less output and palette-cap failures. Exact eager and chunk construction uses the same real selectors and succeeds before ordinary draining. Add or retain the checked `PngIndexedImage::new` overflowing-geometry construction test using its constructor budget snapshot; it must reject before an encodable source exists. Keep this as source-construction evidence rather than manufacturing an invalid image for an unreachable encoder branch. Do not introduce diagnostics assertions unless an existing stable query supports them, and leave hostile pull schedules, sentinel-tail checks, independent parsing, portability gates, staging, FFI, release work, Dynamic, adaptive filters, and Adam7 compression selection to their assigned scope.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter "*indexed compression ancillary admission*"</automated>
  </verify>
  <done>For Fixed winners and Stored fallbacks at depths 1, 2, 4, and 8, actual PLTE/shortest-tRNS selected facts prove all four selected-work boundaries: exact `CodecLimits.max_work` with generous budget succeeds, one-less `CodecLimits.max_work` with generous budget rejects atomically, exact budget work with generous limits succeeds and charges once, and one-less budget work with generous limits rejects atomically. Every one-less selected-work rejection retains its complete budget snapshot, produces zero eager writer bytes, and exposes neither an active chunk encoder nor a lease; selected one-less output, palette-cap overflow, and checked source construction failure retain their required atomicity evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Indexed source to selected preflight | Caller-provided dimensions, palette data, alpha entries, and limits determine arithmetic, resource admission, and output framing. |
| Selected preflight to acknowledged machine | Retained frame and work facts cross from pre-output validation into both user-visible facades. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-01 | Denial of Service | `_png_encode_indexed_preflight_with_profile_and_strategy` geometry and `PngFrameFacts` arithmetic | medium | mitigate | Preserve checked geometry/frame calculations and prove rejected source construction and selected limit failures consume no admission budget. |
| T-86-02 | Denial of Service | Type-3 palette sizing | medium | mitigate | Exercise cap-plus-one palette rejection through the strategy-aware preflight and both public constructor observations before output state exists. |
| T-86-03 | Tampering | selected plan/frame/work handoff to `PngEncodeMachine` | medium | mitigate | Assert real retained PLTE/tRNS facts, exact selected limits, one post-check charge, and eager/chunk traversal of the same machine constructors. |
</threat_model>

<verification>
Run the two focused native PNG test filters from the tasks. Then run `moon -C modules/mb-image test png --target native --frozen --filter "*indexed compression*"` to cover the existing Phase 85 selection baseline together with the Phase 86 admission matrix.</verification>

<success_criteria>
INDEXCOMP-03 is satisfied: selected-depth non-interlaced indexed compression computes complete ancillary-aware facts before observable output, charges once only after chosen output/work limits pass, and both public façades reach the existing acknowledged machine from that one admission decision.</success_criteria>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
------ | -- | ------------------- | ---- | ------ | -----
GOAL | — | Preflight actual palette/transparency facts and admit once into the acknowledged eager/chunk machine | 86-01 | COVERED | Tasks 1-2 exercise selected facts plus both public façades.
REQ | INDEXCOMP-03 | Atomic selected-depth geometry, PLTE/tRNS, exact output/work, and one-less/cap/arithmetic rejection | 86-01 | COVERED | Task 2 covers the complete all-depth boundary matrix.
RESEARCH | — | Keep `PngFrameFacts` as framing owner and selected preflight as sole charge seam | 86-01 | COVERED | Tasks name and test the existing selected-facts path without a new oracle.
RESEARCH | — | Verify no eager writer byte and no chunk constructor result on admission rejection | 86-01 | COVERED | Task 2 uses existing writer and chunk helpers.
CONTEXT | D-01 | Build both candidate facts and retain selected frame/output/work facts | 86-01 | COVERED | Tasks 1-2 cite D-01 and assert retained selected facts.
CONTEXT | D-02 | One charge after selected limit checks; every rejection is atomic | 86-01 | COVERED | Task 2 cites D-02 and verifies exact/one-less budgets and façade observability.
CONTEXT | D-03 | Use the sole `PngEncodeMachine` lifecycle for eager and chunks | 86-01 | COVERED | Task 1 cites D-03 and proves eager/chunk parity through public selectors.
CONTEXT | D-04 | Direct exact/one-less/cap/arithmetic evidence | 86-01 | COVERED | Task 2 cites D-04.
CONTEXT | D-05 | Fixed winner and Stored fallback with actual ancillary facts at every depth | 86-01 | COVERED | Task 2 cites D-05 and loops all four selected profiles.

<output>
Create `.planning/phases/86-ancillary-aware-preflight-and-shared-machine-integration/86-01-SUMMARY.md` when done.
</output>
