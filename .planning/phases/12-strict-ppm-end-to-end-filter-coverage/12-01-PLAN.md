---
phase: 12-strict-ppm-end-to-end-filter-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ppm-portable/main/main.mbt
  - modules/mb-image/ops/processing_pipeline_test.mbt
  - modules/mb-image/ops/processing_pipeline_wbtest.mbt
autonomous: true
requirements: [INTEG-01, INTEG-02, RASTER-02, RASTER-03]
must_haves:
  truths:
    - "The portable strict-P6 example demonstrably decodes, crops, rotates, converts, grayscales, radius-one blurs, composites, converts, and encodes one fixed vector on every supported target."
    - "The resulting strict-P6 payload is exactly 29 bytes and is proven by exact bytes, rolling digest, SHA-256 identity, and intermediate semantic assertions."
    - "An otherwise successful crop-to-filter route rejects a 53-work blur budget atomically with the typed box-blur budget error on every supported target."
  artifacts:
    - path: examples/ppm-portable/main/main.mbt
      provides: "Runnable strict-P6 crop/rotate/filter/source-over/encode vector with explicit stage budgets and byte oracle"
    - path: modules/mb-image/ops/processing_pipeline_test.mbt
      provides: "Named public semantic coverage for the crop, rotation, conversion, grayscale, blur, and source-over route"
    - path: modules/mb-image/ops/processing_pipeline_wbtest.mbt
      provides: "Named hostile radius-one blur preflight and complete budget-atomicity proof"
  key_links:
    - from: examples/ppm-portable/main/main.mbt
      to: tchivs/mb-image/ppm and tchivs/mb-image/ops
      via: "PpmDecoder -> crop -> rotate_90 -> RGB8/RGBA8 conversion -> grayscale -> box_blur -> source_over -> RGB8 conversion -> PpmEncoder"
      pattern: "PpmDecoder|rotate_90|box_blur|composite_source_over|PpmEncoder"
    - from: modules/mb-image/ops/processing_pipeline_wbtest.mbt
      to: modules/mb-image/ops/processing.mbt
      via: "public box_blur invoked after successful precursor stages with work=53"
      pattern: "box_blur.*1UL"
---

<objective>
Close the v0.3 audit's sole partial strict-PPM integration path with one portable, deterministic crop/rotate/filter/source-over/encode evidence vector and one hostile blur-budget boundary.

Purpose: Make the already-delivered geometry and alpha-aware filter contracts visible in the real public strict-P6 route, without broadening product scope.
Output: Updated portable example/documentation evidence and named four-target public/white-box tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.3-v0.3-MILESTONE-AUDIT.md
@.planning/phases/12-strict-ppm-end-to-end-filter-coverage/12-CONTEXT.md
@.planning/phases/12-strict-ppm-end-to-end-filter-coverage/12-RESEARCH.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-VERIFICATION.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-01-SUMMARY.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-02-SUMMARY.md
@examples/ppm-portable/main/main.mbt
@modules/mb-image/ops/processing_pipeline_test.mbt
@modules/mb-image/ops/processing_pipeline_wbtest.mbt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the portable PPM route with the audited geometry-and-filter vector</name>
  <files>examples/ppm-portable/main/main.mbt</files>
  <action>Implement D-01, D-02, D-03, and D-05 by changing only the existing portable executable. Decode a fixed strict P6 3x3 RGB source, then call the public APIs in this exact order: crop rectangle (1,0,2,3), named rotate_90, rgb8_to_straight_rgba8, grayscale, box_blur with radius 1UL, composite_source_over over a separately strict-decoded and converted opaque 3x2 background, straight_rgba8_to_rgb8, and PpmEncoder. Give every decode, allocation-producing operation, writer, and encoder a fresh explicit Budget; preserve the researched stage ceilings, including blur work 54UL and writer/encode capacity/work 29UL. Assert post-rotation 3x2 extent and first/last neutral RGB values 0x80 and 0xff; assert grayscale channel equality and alpha 0xff; assert blurred pixels (0,0)=72 72 72 ff and (2,1)=c6 c6 c6 ff; assert empty diagnostics and the final output's complete 29-byte P6 payload. Require rolling-257 digest 714923673 and print the fixed SHA-256 identity 005700d6602b144bafcf3d869deee85619c8279c749bf33ca6fea8b43dbe78bf only after all executable assertions pass. The implementation scope is confined to this approved portable artifact; do not add an API or dependency edge.</action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $markers = @('bytes_written=29', 'digest=714923673', 'sha256=005700d6602b144bafcf3d869deee85619c8279c749bf33ca6fea8b43dbe78bf'); foreach ($target in 'js','wasm','wasm-gc','native') { $out = &amp; moon -C examples/ppm-portable run main --target $target --frozen; if ($LASTEXITCODE -ne 0) { throw "portable example failed on $target: exit $LASTEXITCODE" }; $text = $out -join "`n"; foreach ($marker in $markers) { if (-not $text.Contains($marker)) { throw "portable example on $target omitted required marker: $marker" } } }</automated>
  </verify>
  <done>The sole public portable executable performs the required strict-P6 crop/rotate/filter/source-over/encode route with independent byte, digest, SHA, diagnostics, and intermediate semantic proof on js, wasm, wasm-gc, and native.</done>
</task>

<task type="auto">
  <name>Task 2: Add discriminating public and hostile filter-path tests</name>
  <files>modules/mb-image/ops/processing_pipeline_test.mbt, modules/mb-image/ops/processing_pipeline_wbtest.mbt</files>
  <action>Implement D-02, D-04, and D-05 with two newly named tests that cannot be satisfied by the pre-existing resize/composite vector. In the public test file, use the existing compatible owned-RGB helper and exported ops APIs to construct the 3x3 fixed vector, crop (1,0,2,3), rotate_90, convert RGB8 to straight RGBA8, grayscale, blur radius 1UL, source-over a compatible opaque 3x2 converted destination, then strictly convert back to RGB8. Assert 3x2 orientation-normalized geometry, decisive rotated/cropped pixel positions, grayscale alpha/channel semantics, radius-one clamp-edge output pixels, opaque source-over result, and strict RGB conversion success. In the existing white-box hostile path, run the same successful owned RGB crop -> rotate_90 -> conversion -> grayscale precursor route, then call box_blur at radius 1UL with all output resources sufficient except work=53UL. Assert Resource/BudgetExceeded, operation image-box-blur, context image-box-blur-output-budget, and that bytes, allocations, allocation_size, width, height, pixels, depth, and work all equal the pre-call ResourceLimits snapshot. Keep codec parsing out of ops tests, do not modify ops implementation or public APIs, and leave the existing composite-budget test intact.</action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; $markers = @('public processing pipeline crop-rotate-grayscale-blur-source-over vector', 'processing pipeline box blur insufficient work leaves budget unchanged'); foreach ($target in 'js','wasm','wasm-gc','native') { $out = &amp; moon test modules/mb-image/ops --target $target --frozen -v; if ($LASTEXITCODE -ne 0) { throw "ops test suite failed on $target: exit $LASTEXITCODE" }; $text = $out -join "`n"; foreach ($marker in $markers) { if (-not $text.Contains($marker)) { throw "ops suite on $target omitted required test marker: $marker" } } }</automated>
  </verify>
  <done>Both new named test paths execute and pass on each required target: one proves the real geometry/filter composition with semantic assertions, and one proves radius-one box-blur work preflight rejects atomically at 53 units.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strict P6 bytes -> decoder | Bounded untrusted image data enters the public codec with explicit parser/codec limits. |
| caller budget -> image operations | Requested crop/rotation/filter work is constrained before allocation or mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | D | radius-one box blur | medium | mitigate | Exercise the 54-work success path and 53-work typed preflight with a full unchanged-budget snapshot. |
| T-12-02 | T | strict-P6 integration evidence | low | mitigate | Check complete 29-byte payload, digest, SHA identity, and intermediate geometry/filter pixels on all four targets. |
| T-12-03 | I | codec input limits | low | accept | Existing strict decoder remains bounded; this phase adds no parser, path, or host input surface. |
</threat_model>

<verification>
Run the portable executable and the ops test suite with --frozen on js, wasm, wasm-gc, and native. Confirm the new names appear in verbose test output. After all Phase-12 implementation commits and its summary exist, run this PowerShell scope gate against the plan baseline commit: `$ErrorActionPreference = 'Stop'; $allowed = @('examples/ppm-portable/main/main.mbt', 'modules/mb-image/ops/processing_pipeline_test.mbt', 'modules/mb-image/ops/processing_pipeline_wbtest.mbt', '.planning/phases/12-strict-ppm-end-to-end-filter-coverage/12-01-SUMMARY.md'); $changed = @(&amp; git diff --name-only c5b0344569c51433eb8b1c14b60cfa9f0cd272d3..HEAD); if ($LASTEXITCODE -ne 0) { throw "cannot inspect Phase-12 changed paths: exit $LASTEXITCODE" }; $unexpected = @($changed | Where-Object { $_ -notin $allowed }); if ($unexpected.Count -ne 0) { throw ('Phase-12 scope violation: ' + ($unexpected -join ', ')) }; &amp; git diff --check c5b0344569c51433eb8b1c14b60cfa9f0cd272d3..HEAD; if ($LASTEXITCODE -ne 0) { throw "Phase-12 diff check failed: exit $LASTEXITCODE" }`. This gate permits only the three approved implementation artifacts and the plan's own summary; it rejects production ops/API/codec, release, benchmark, manifest, and configuration changes.
</verification>

<success_criteria>
1. The audit's crop/rotate and grayscale/box-blur integration debt is demonstrably closed in the existing strict-P6 public consumer.
2. The deterministic 3x2 result is exactly the stated 29-byte payload with digest 714923673 and the stated SHA identity on every supported target.
3. The tests prove both discriminating positive operation sequencing and atomic radius-one blur resource rejection without changing product APIs or release/benchmark scope.
</success_criteria>

<source_audit>
SOURCE   | ID | Feature/requirement | Plan | Status | Notes
GOAL     | -- | Audit closure for strict-P6 geometry/filter E2E coverage | 12-01 | COVERED | Public executable and four-target evidence.
REQ      | INTEG-01 | Existing public PPM composition evidence | 12-01 | COVERED | Extends, without creating, the satisfied integration requirement.
REQ      | INTEG-02 | Existing four-target behavioral/adversarial evidence | 12-01 | COVERED | Adds discriminating named vectors on all targets.
REQ      | RASTER-02 | Grayscale and bounded box blur behavior | 12-01 | COVERED | Radius-one semantic and work-boundary assertions.
REQ      | RASTER-03 | Typed deterministic resource-limit error | 12-01 | COVERED | 53-work BudgetExceeded plus complete atomicity snapshot.
RESEARCH | -- | Fixed 3x3 -> 3x2 sequence, 29-byte oracle, no new dependency/API edge | 12-01 | COVERED | Task 1 and scope fence.
RESEARCH | -- | Radius-one 54/53 blur boundary in existing white-box test | 12-01 | COVERED | Task 2.
CONTEXT  | D-01 | Strict PPM decode-to-encode sequence | 12-01 | COVERED | Task 1.
CONTEXT  | D-02 | Explicit budgets and byte/digest/semantic evidence | 12-01 | COVERED | Tasks 1-2.
CONTEXT  | D-03 | Named rotation and non-degenerate blur | 12-01 | COVERED | Tasks 1-2.
CONTEXT  | D-04 | Four targets and hostile boundary | 12-01 | COVERED | Task 2 verification.
CONTEXT  | D-05 | Phase-11 evidence files only; no release/API/benchmark work | 12-01 | COVERED | Both task scope fences.
</source_audit>

<output>
Create `.planning/phases/12-strict-ppm-end-to-end-filter-coverage/12-01-SUMMARY.md` when done.
</output>
