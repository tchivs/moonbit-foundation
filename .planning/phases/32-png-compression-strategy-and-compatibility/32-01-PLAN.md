---
phase: 32-png-compression-strategy-and-compatibility
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - policy/foundation.json
autonomous: true
requirements: [PNGC-01]
must_haves:
  truths:
    - "A library user can explicitly select either Stored or FixedOrStored through documented additive eager and caller-buffered PNG factories."
    - "The public strategy and configured-factory documentation states that FixedOrStored currently emits the Stored baseline, offers no optimization or size guarantee, and receives fixed-or-stored behavior in Phase 33."
    - "PngEncoder::new() and PngChunkEncoder::new(...) continue to produce their independently frozen complete stored-DEFLATE PNG byte sequences for the same compatible RGB8 and straight-RGBA8 images."
    - "The generated public PNG interface exactly matches the registered semantic interface while retaining the existing source inventory, imports, and four portable targets."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Public PngCompressionStrategy declaration and documented additive configured eager factory.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Additive configured chunk factory and strategy-aware construction of the shared private encode machine.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Public eager strategy construction and legacy stored-byte regression coverage.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public caller-buffered strategy construction and legacy complete stored-byte regression coverage.
    - path: policy/foundation.json
      provides: Exact normalized PNG public-interface registration for the enum and both configured factories.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: PngEncoder retains its selected PngCompressionStrategy and supplies it to the authoritative PngEncodeMachine constructor used by ImageEncoder::encode.
      pattern: PngCompressionStrategy|new_with_compression_strategy|PngEncodeMachine::new
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: The configured chunk factory and legacy constructor create the shared machine, while tests drain only accepted caller-lease prefixes and compare each legacy aggregate to its frozen byte baseline.
      pattern: PngChunkEncoder::new_with_compression_strategy|PngChunkEncoder::new|png_chunk_test_drain
    - from: policy/foundation.json
      to: modules/mb-image/png/pkg.generated.mbti
      via: The PNG quality lane generates package interfaces on every declared target and exact-compares the registered semantic interface.
      pattern: PngCompressionStrategy|new_with_compression_strategy
---

<objective>
Publish the additive PNG compression-selection contract while making the established stored-DEFLATE eager and caller-buffered constructors independently byte-stable.

Purpose: Give callers an explicit, auditable selection seam without changing the canonical stored representation they already receive.
Output: A tested `PngCompressionStrategy`, configured eager/chunk factories, frozen legacy full-byte regressions, and exact policy registration for PNGC-01.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-png-compression-strategy-and-compatibility/32-RESEARCH.md
@.planning/milestones/v0.9-phases/29-pausable-png-encode-substrate/29-01-SUMMARY.md
@.planning/milestones/v0.9-phases/31-portable-png-encode-evidence/31-VERIFICATION.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@policy/foundation.json
</context>

<feature>
  <name>Additive PNG compression strategy with legacy stored-byte compatibility</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt, policy/foundation.json</files>
  <behavior>
    - `PngCompressionStrategy::Stored` and `PngCompressionStrategy::FixedOrStored` are public, equality-comparable, PNG-specific strategy cases whose public documentation says that FixedOrStored currently emits the Stored baseline, offers no optimization or size guarantee, and receives fixed-or-stored behavior in Phase 33.
    - `PngEncoder::new_with_compression_strategy(strategy)` and `PngChunkEncoder::new_with_compression_strategy(source, strategy, limits, budget, diagnostics)` are public additive factories; their documentation repeats the FixedOrStored Phase 32 behavior boundary, and both cases successfully use the existing supported RGB8/straight-RGBA8 encode route.
    - Legacy `PngEncoder::new()` still emits the frozen full RGB8 and straight-RGBA8 stored-DEFLATE PNG byte sequences.
    - Legacy `PngChunkEncoder::new(...)` still drains its frozen full RGB8 and straight-RGBA8 stored-DEFLATE PNG byte sequences under an irregular caller-owned capacity schedule.
    - `FixedOrStored` remains a selected construction policy in this phase; its output is not asserted equal to Stored, so Phase 33 can implement bounded fixed-or-stored planning/emission without another public API change.
    - The exact generated PNG interface is registered in the existing semantic-interface policy; package source inventory, import allowlist, supported targets, and compatibility policy are unchanged.
  </behavior>
  <implementation>Use the one existing PngEncodeMachine and its existing stored byte emitter for both strategies in this phase. The public enum and both configured factories must state that FixedOrStored currently emits the Stored baseline, offers no optimization or size guarantee, and receives fixed-or-stored behavior in Phase 33. Preserve its preflight, framing, CRC, Adler, acknowledgement, progress, and terminal behavior. Phase 33 owns exact optimized admission, fixed-Huffman planning/emission, and eager/chunk optimized parity; Phase 34 owns corpus, four-target compression measurements, never-larger evidence, and flat-image win claims.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing public-contract and independent legacy-byte tests</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: Each new eager/chunk configured factory accepts Stored and FixedOrStored for a compatible RGB8 image; each encoder completes through its ordinary public API and emits a structurally valid PNG.
    - Test 2: Legacy PngEncoder::new() exactly equals the existing complete fixed RGB8 and straight-RGBA8 stored-byte vectors, independently of a configured encoder result.
    - Test 3: Legacy PngChunkEncoder::new(...) drains RGB8 and straight-RGBA8 through an irregular capacity schedule to those complete fixed vectors, independently of the eager encoder.
  </behavior>
  <action>Create the RED tests in the existing package-private black-box test files, using only public `PngEncoder`, `PngChunkEncoder`, `ImageEncoder`, and caller-scoped `MutByteLease` operations. In `encode_test.mbt`, call the missing eager configured factory with both required strategy cases and validate the resulting bytes with the package's existing PNG decode/assertion helper; keep the current exact RGB8/RGBA8 stored vectors as the legacy `PngEncoder::new()` oracle. In `stream_encode_test.mbt`, add a helper that accepts a constructor result and drains only `[0, written)` from each output lease; call the missing configured chunk factory with both cases, then add separate legacy-constructor tests that compare the complete drained RGB8 and RGBA8 aggregates to fixed stored vectors rather than to eager output. Use an irregular non-empty schedule already established by the package. Do not add an equality assertion between FixedOrStored and Stored, private-machine inspection, optimized-size expectation, corpus fixture, target runner, or public workflow change. Run the focused native selector and confirm RED fails because the public enum/factories do not yet exist, then commit the test stage as `test(32-01): add failing PNG compression strategy contract`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG compression strategy*'  # expected to fail in RED before the public declarations exist</automated>
  </verify>
  <done>The focused selector fails for the missing public strategy/factories, while the tests already state successful configured construction and complete independently frozen legacy eager/chunk stored-byte behavior for RGB8 and straight RGBA8.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strategy-aware construction without changing stored emission</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <behavior>
    - Test 1: Both configured factories compile and encode the supported test image for Stored and FixedOrStored.
    - Test 2: Both legacy constructors retain their explicit Stored choice and satisfy the byte baselines from Task 1.
    - Test 3: Existing preflight, byte acknowledgement, exact pull progress, and sticky-terminal regressions continue to pass through the shared machine.
  </behavior>
  <action>Add public `PngCompressionStrategy` with exactly `Stored` and `FixedOrStored` cases plus `derive(Eq)` in `png.mbt`. Public documentation on the enum and on both `new_with_compression_strategy` factories must state exactly that FixedOrStored currently emits the Stored baseline, provides no optimization or size guarantee, and receives fixed-or-stored behavior in Phase 33; it must also state that the strategy excludes dynamic Huffman and adaptive filters. Change `PngEncoder` from an empty value to retain the selected private strategy, keep `PngEncoder::new()` directly constructing `Stored`, and add `PngEncoder::new_with_compression_strategy(strategy)`. Update `ImageEncoder::encode` in `encode.mbt` to pass that retained selection into one strategy-aware `PngEncodeMachine::new` call. Keep `PngChunkEncoder::new(...)` directly constructing `Stored`, add `PngChunkEncoder::new_with_compression_strategy(source, strategy, limits, budget, diagnostics)`, and route both chunk construction paths through the same private strategy-aware machine constructor. Retain the selected strategy in the machine construction state so the configured branch is explicit and available to Phase 33, but keep the existing stored zlib/header/block/CRC/Adler byte path, preflight order, acknowledgement discipline, exact total length, pull semantics, and terminal behavior unchanged for both cases. Do not make a legacy constructor delegate through a configurable default, add a generic codec option or global setting, alter the decoder, hand-roll another PNG emitter, add dependencies or FFI, or implement fixed-Huffman planning, emission, size promises, corpus evidence, adaptive filtering, host streaming, APNG, colour changes, or metadata expansion. Make the focused RED selector pass, run the complete native PNG package suite, and commit the implementation as `feat(32-01): add PNG compression strategy factories`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>Users can construct eager and caller-buffered PNG encoders with either named strategy, and the public enum plus both configured factories state that FixedOrStored currently emits the Stored baseline, provides no optimization or size guarantee, and receives fixed-or-stored behavior in Phase 33; both legacy constructors explicitly select Stored and still pass their complete frozen-byte tests while all established native PNG regressions remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Register the exact additive interface and run portable compatibility gates</name>
  <files>policy/foundation.json, modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Generate the PNG package interface using the repository quality workflow and copy the resulting normalized declarations exactly into only the PNG `semantic_interface` array in `policy/foundation.json`: the public compression-strategy enum with its two cases, the eager configured factory, and the caller-buffered configured factory. Preserve every existing declaration and retain the PNG production-source list, allowed-import list, supported target set, and all non-PNG policy entries verbatim; do not edit `policy/compatibility.json`, because this is an additive surface under its existing classifier. Run the repository's exact policy/PNG lane and the four target-isolated package test commands. If refactoring improves only duplication introduced by the strategy-aware constructors, make it after GREEN without changing observable behavior and commit it as `refactor(32-01): clarify PNG strategy routing`; otherwise record that no refactor was necessary in the summary.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
    <automated>moon -C modules/mb-image test png --target js --frozen; moon -C modules/mb-image test png --target wasm --frozen; moon -C modules/mb-image test png --target wasm-gc --frozen; moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The generated public PNG interface and its policy registration match exactly on js, wasm, wasm-gc, and native; the change is additive, legacy complete byte baselines remain locked, and no Phase 33 or Phase 34 responsibility is implemented or claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library caller → public eager/chunk factory | Caller selects a public strategy and supplies image capability, limits, budget, diagnostics, and caller-owned output leases. |
| Public strategy selection → private PngEncodeMachine | The selected policy must enter the authoritative preflight/emitter route without bypassing resource checks or changing legacy construction. |
| Generated PNG interface → foundation policy gate | Public declarations become a compatibility-governed contract that must exactly match the checked semantic snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | `PngEncoder::new` and `PngChunkEncoder::new` construction routes | high | mitigate | Task 1 locks each legacy path independently to its complete stored RGB8/RGBA8 bytes; Task 2 makes each constructor explicitly select Stored instead of using a mutable default. |
| T-32-02 | Denial of service | configured eager/chunk factories and PngEncodeMachine admission | high | mitigate | Task 2 routes both factories through the existing authoritative preflight and machine; focused and full PNG regressions retain capability, limits, budget, exact-progress, and terminal checks. |
| T-32-03 | Spoofing | `PngCompressionStrategy::FixedOrStored` public contract | medium | mitigate | Task 2 documents the precise named policy and exclusions; Task 1 avoids a size/equality promise that would misrepresent the Phase 32 seam, and Task 3 runs the exact generated-interface policy gate. |
</threat_model>

<verification>
- RED: `moon -C modules/mb-image test png --target native --frozen -f '*PNG compression strategy*'` fails for missing declarations before the GREEN implementation.
- GREEN: the same focused selector passes, followed by `moon -C modules/mb-image test png --target native --frozen`.
- Portable/policy: run `Invoke-MoonQuality.ps1 -Lane Png` and the four target-specific PNG package test commands from Task 3.
- TDD history: confirm `test(32-01): ...` precedes `feat(32-01): ...`; retain a `refactor(32-01): ...` commit only when behavior-preserving cleanup is made.
</verification>

<success_criteria>
- `PngCompressionStrategy::{Stored, FixedOrStored}` and the two `new_with_compression_strategy` factories are the only public compression additions.
- Public enum and configured-factory documentation states that FixedOrStored currently emits the Stored baseline, provides no optimization or size guarantee, and receives fixed-or-stored behavior in Phase 33.
- Existing `PngEncoder::new()` and `PngChunkEncoder::new(...)` return their exact established stored-DEFLATE bytes for the locked RGB8 and straight-RGBA8 fixtures.
- The configured factories share the existing bounded, atomic preflight and private emitter without changing present/acknowledge, caller-buffered progress, or terminal semantics.
- The PNG semantic interface is exact on every supported target and policy inventory/compatibility boundaries are preserved.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Explicit documented strategy choice with byte-for-byte stored eager/chunk defaults preserved | 32-01 | COVERED | Tasks 1-2 add the two factories and independently freeze both legacy byte routes. |
| REQ | PNGC-01 | Explicit compression strategy while existing eager/chunk constructors retain byte-for-byte stored-DEFLATE output | 32-01 | COVERED | Feature behavior and Tasks 1-2 cover additive construction plus full legacy RGB8/RGBA8 byte baselines. |
| RESEARCH | — | Exact `Stored`/`FixedOrStored` enum and eager/chunk configured factory shape | 32-01 | COVERED | Task 2 implements the specified public surface; Task 3 registers its generated normalized form. |
| RESEARCH | — | Public documentation says FixedOrStored currently emits Stored, has no optimization/size guarantee, and receives fixed-or-stored behavior in Phase 33 | 32-01 | COVERED | Feature behavior, Task 2, its done criterion, and success criteria require the enum and both configured factories to state this boundary. |
| RESEARCH | — | Retain selection in the shared private route; do not alter stored emission/preflight/progress | 32-01 | COVERED | Task 2 specifies one strategy-aware machine constructor and preserves existing emitter and lifecycle behavior. |
| RESEARCH | — | Exact semantic-interface policy registration only; no compatibility-policy rewrite | 32-01 | COVERED | Task 3 updates only the PNG semantic interface and runs the policy gate. |
| RESEARCH | — | Fixed-or-stored planning/emission, corpus, portable compression measurements, and size claims are other-phase scope | 32-01 | COVERED | Feature boundary and Tasks 1-3 reserve these responsibilities to Phases 33 and 34 without adding an unplanned feature. |
| CONTEXT | — | No Phase 32 CONTEXT.md and no D-NN decisions exist | 32-01 | COVERED | ROADMAP, REQUIREMENTS, and verified research control the phase scope. |

<output>
Create `.planning/phases/32-png-compression-strategy-and-compatibility/32-01-SUMMARY.md` when done.
</output>
