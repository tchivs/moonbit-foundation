---
phase: 67-resumable-rgba16-png-preservation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_decode.mbt
  - modules/mb-image/png/stream_decode_test.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
autonomous: true
requirements:
  - RGBA16DEC-03
must_haves:
  truths:
    - "PngChunkDecoder::new_rgba16 selects the established Rgba16 machine and produces component-byte-identical results to fresh eager decode_rgba16 for empty, one-byte, and ragged schedules, including Adam7."
    - "Chunk accounting stays accepted-only; no caller ByteView or partial image is exposed, and finish is the sole successful result transfer."
    - "Malformed, truncated, profile-invalid, and resource-limited RGBA16 chunk streams retain the existing sticky typed terminal behavior; generic chunk Type-6/16 remains RGBA8 high-byte compatible."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Additive new_rgba16 constructor selecting PngDecodeProfile::Rgba16 through the existing chunk facade.
      contains: "new_rgba16"
    - path: modules/mb-image/png/stream_decode_test.mbt
      provides: Public eager/chunk schedule parity, result visibility, and generic compatibility evidence.
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: Internal accepted-only, atomic profile/resource, and sticky terminal evidence.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: PngDecodeProfile::Rgba16
      via: new_rgba16 constructs PngChunkDecoder using the same private byte-fed machine as Phase 66 eager decode.
      pattern: "new_rgba16.*Rgba16"
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: fresh decode_rgba16 is the exact component-byte oracle for chunk schedules.
      pattern: "decode_rgba16"
---

<objective>
Expose the completed explicit RGBA16 profile through the existing resumable decoder without changing its lifecycle contract.

Purpose: make Type-6/16 exact preservation usable by callers who supply input incrementally.
Output: one additive `new_rgba16` constructor and executable parity/lifecycle/terminal tests.

Decision coverage: D-01 adds only the constructor and existing Rgba16 machine; D-02 proves accepted-only progress, private lifecycle and sole finish transfer; D-03 proves sticky malformed/truncated/profile/resource terminals; D-04 carries forward Phase 66 strict profile/exact storage/generic freeze; D-05 excludes a second decoder, copied sources, release automation and broad Phase 68 qualification.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/67-resumable-rgba16-png-preservation/67-CONTEXT.md
@.planning/phases/67-resumable-rgba16-png-preservation/67-RESEARCH.md
@.planning/phases/66-explicit-rgba16-png-preservation/66-01-SUMMARY.md
@.planning/phases/66-explicit-rgba16-png-preservation/66-VERIFICATION.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/stream_decode_test.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Red-green the additive resumable RGBA16 constructor and eager parity</name>
  <reversibility rating="costly">The public chunk selector extends the explicit high-precision contract to incremental consumers and must remain byte-identical to Phase 66 eager decode.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - RED: public tests cannot construct `PngChunkDecoder::new_rgba16` or prove component-byte parity with `PngDecoder::decode_rgba16`.
    - GREEN: declaration-free normal and sRGB Adam7 Type-6/16 literals feed empty, one-byte, and ragged schedules; the successful finish result has the same descriptor, metadata, component bytes and bytes-read as a fresh eager result.
    - GREEN: generic chunk decoding of the same Type-6/16 data remains the established RGBA8 high-byte output.
  </behavior>
  <action>Add `new_rgba16` beside `new_graya16` in png.mbt and select only `PngDecodeProfile::Rgba16` through the existing private machine; do not add state, buffering, parser, raster or eager changes. Add compact public schedule helpers/tests using Phase 66 independent normal and Adam7 literals. Compare all observable component bytes through existing views, not opaque backing buffers. Assert zero-byte pushes, accepted counts and successful finish transfer. Retain generic chunk construction unchanged.</action>
  <verify><automated>moon -C modules/mb-image test png --target js --frozen --filter '*rgba16*'</automated></verify>
  <done>new_rgba16 is the sole new public surface and every successful schedule returns the same exact RGBA16 result as eager decoding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green chunk lifecycle, atomic failures and sticky terminals</name>
  <files>modules/mb-image/png/stream_decode_wbtest.mbt, modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - RED: Rgba16 chunks lack explicit accepted-only, no-result-before-finish, sticky-terminal and resource/profile failure evidence.
    - GREEN: wrong profile, malformed/truncated data, and one-less output/budget limits fail before a result; later pushes and repeated finish calls return the first typed terminal error with zero consumed bytes.
    - GREEN: valid resources preserve Phase 66 eight-byte accounting and no caller source view/partial image escapes before finish.
  </behavior>
  <action>Extend the closest GrayAlpha16 white-box lifecycle fixtures rather than duplicating chunk machinery. Feed CRC-authenticated profile-invalid and resource-boundary input through new_rgba16, assert accepted-only counts, absent private outcome before successful finish, stable error identity on replay, and unchanged caller-visible source ownership. Keep all admission and output accounting in the Phase 66 machine; add no special chunk allocation path. Run targeted RGBA16 plus existing GrayAlpha16 checks before full package validation.</action>
  <verify><automated>moon -C modules/mb-image test png --target js --frozen --filter '*rgba16*'; moon -C modules/mb-image test png --target js --frozen --filter '*graya16*'</automated></verify>
  <done>Rgba16 chunk decoding has the same bounded visibility and sticky typed failure contract as the established facade.</done>
</task>

</tasks>

<verification>
Run focused JS checks during RED/GREEN, then serially run the ordinary package command for wasm, wasm-gc, js and native:

`moon -C modules/mb-image test png --target <target> --frozen`
</verification>

<success_criteria>
RGBA16DEC-03 is complete when caller-owned chunk schedules reproduce eager RGBA16 bytes, only successful finish exposes one result, terminal failures remain typed and sticky with accepted-only accounting, generic chunk behavior is unchanged, and all four package targets pass.
</success_criteria>

<output>
Create `.planning/phases/67-resumable-rgba16-png-preservation/67-01-SUMMARY.md` when done.
</output>
