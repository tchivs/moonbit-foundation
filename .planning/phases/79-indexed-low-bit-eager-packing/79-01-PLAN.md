---
phase: 79-indexed-low-bit-eager-packing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
autonomous: true
requirements:
  - INDEXLOW-01
  - INDEXLOW-02
  - INDEXLOW-03
must_haves:
  truths:
    - "A caller can select PngIndexedBitDepth::One, ::Two, or ::Four in PngEncoder::encode_indexed and receive a bounded non-interlaced Type-3 PNG from the existing PngIndexedImage."
    - "Selected-depth output has checked packed row/frame sizing, a filter-None byte followed by MSB-first palette codes, deterministic zero-filled tail bits, and the existing PLTE plus canonical optional tRNS framing."
    - "A palette exceeding 2, 4, or 16 entries for the selected depth, or a resource-limit/budget rejection, occurs before writer progress and before the single encode-work charge changes caller budget state."
    - "The retained encode_indexed8 route and its frozen Indexed8 bytes still pass the PNG package suite."
    - "PngChunkEncoder::new_indexed8 retains its established Type-3/8 bytes through the fixed-8 private machine wrapper; Phase 79 exposes no low-bit chunk API."
  artifacts:
    - "modules/mb-image/png/png.mbt exposes PngIndexedBitDepth."
    - "modules/mb-image/png/encode.mbt exposes PngEncoder::encode_indexed and performs low-bit indexed admission."
    - "modules/mb-image/png/stream_encode.mbt emits selected Type-3 IHDR depth and packed indexed scanline bytes through PngEncodeMachine."
    - "modules/mb-image/png/encode_test.mbt and encode_wbtest.mbt prove eager wire, decode, and admission behavior."
  key_links:
    - "PngIndexedBitDepth maps once to a private indexed depth/profile fact consumed by _png_encode_indexed_preflight, PngEncodeMachine::new_with_indexed, PngEncodeMachine::scanline_byte, and PngEncodeMachine::byte_at."
    - "The legacy _png_encode_indexed_preflight and PngEncodeMachine::new_with_indexed signatures remain fixed-8 wrappers for encode_indexed8, PngChunkEncoder::new_indexed8, and existing white-box callers; selector-aware helpers serve only encode_indexed."
    - "The existing PngIndexedImage index_at/palette_byte_at/alpha_at accessors remain the single source for raster, PLTE, and tRNS bytes."
---

<objective>
Deliver the eager-only Type-3 low-bit indexed encoder described by D-01 through D-05, retaining the shipped Indexed8 source and one acknowledged PNG byte machine.

Purpose: Let library users choose exact 1-, 2-, or 4-bit indexed PNG wire output without changing canonical source ownership or legacy Indexed8 behavior.
Output: Public depth selector and eager factory, depth-aware atomic admission and scalar packing, plus independent wire/decode/private-fact evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/79-indexed-low-bit-eager-packing/79-CONTEXT.md
@.planning/phases/79-indexed-low-bit-eager-packing/79-RESEARCH.md
@.planning/research/v025-INDEXED-LOW-BIT-ENCODE.md
@.planning/milestones/v0.24-phases/78-resumable-indexed-png-qualification/78-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one transparent 2-bit indexed image through the eager encoder</name>
  <precondition>Phase 78 artifacts PngIndexedImage, PngEncoder::encode_indexed8, and PngEncodeMachine::new_with_indexed exist in the PNG package.</precondition>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - RED: a 5-pixel PngIndexedImage with palette codes 0,1,2,3,0 and PngIndexedBitDepth::Two is accepted by PngEncoder::encode_indexed, emits Type-3/2 IHDR, PLTE followed by canonical three-byte tRNS, and has Stored scanline bytes 00 1B 00.
    - GREEN: public PngDecoder decoding of that emitted stream exposes every RGB and alpha value from the selected palette, including the odd-width final index.
    - Compatibility: PngEncoder::encode_indexed8 remains callable with its existing signature and preserves its established Type-3/8 bytes.
  </behavior>
  <action>Start with the failing public tracer in encode_test.mbt, using the existing test-local PNG chunk/CRC helpers and public decoder assertions rather than encoder packing helpers. Per D-01, add public PngIndexedBitDepth with exactly One, Two, and Four in png.mbt, then add PngEncoder::encode_indexed(source, bit_depth, writer, limits, budget, diagnostics) in encode.mbt while preserving encode_indexed8 unchanged. Add a private indexed wire-profile/depth fact with One, Two, Four, and fixed Eight cases; map the public selector only to the low-bit cases. Preserve _png_encode_indexed_preflight(source, limits, budget) and PngEncodeMachine::new_with_indexed(source, limits, budget, diagnostics) as fixed-Eight wrappers so existing encode_indexed8, PngChunkEncoder::new_indexed8, and white-box callers keep their signature and Type-3/8 bytes; add selector-aware private helper variants for encode_indexed. Per D-02 and D-03, derive packed row bytes in the selected helper by checked width-times-depth arithmetic, retain the existing Stored/None/non-interlaced plan and one budget charge, and keep PngIndexedImage as the only raster owner. Make PngEncodeMachine::scanline_byte build each payload byte from zero and place only visible source.index_at codes at the MSB-first shifts; make byte_at emit Type-3 and the selected IHDR bit depth while retaining existing PLTE/tRNS, CRC, Adler, pending-byte, and acknowledgement paths. Do not add a PngChunkEncoder low-bit entry point: caller-buffered lifecycle work belongs to Phase 80.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The tracer test first fails because the public selector/factory is absent, then passes with a real Type-3/2 transparent odd-width PNG whose stored payload and public RGBA8 decode are exact; existing Indexed8 tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Expand low-bit indexed admission and independent eager evidence across all selected depths</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt</files>
  <action>Expand the completed eager path only. Per D-04, implement the admission matrix below for each selected depth: use a source with the exact 2/4/16-entry palette to prove an accepted call consumes precisely the preflight selected-work charge, then use an otherwise valid source with palette cap plus one entry and limits set one below requested width, height, pixels, frame total output bytes, and selected work. Add a one-less-work-budget case as well. For every rejection, snapshot every @budget.ResourceRemaining field before the call, assert the complete snapshot is unchanged afterward, and assert MemoryWriter.position() remains zero; prove the accepted case charges exactly once. Keep private facts explicit: checked packed row bytes, scanlines, Stored IDAT length, frame offsets, total length, and selected work. Per D-05, add public independent odd-width Stored vectors for One, Two, and Four with scanlines 00 55 80, 00 1B 00, and 00 01 20; assert IHDR depth/type, PLTE-before-optional-tRNS-before-IDAT ordering, and CRCs via test-local helpers. Decode opaque output through RGB8 and transparent output through RGBA8, asserting every palette value and the final visible pixel; retain frozen encode_indexed8 and PngChunkEncoder::new_indexed8 Type-3/8 vectors through their fixed-8 wrappers. Keep tests within eager APIs and existing fixed-8 chunk compatibility assertions—do not add a low-bit chunk factory, caller lease schedules, terminal-state tests, or Phase 80 qualification.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>All 1/2/4-bit eager paths have independent MSB-first zero-tail wire proof, public RGB8/RGBA8 decode-back, atomic depth-specific admission coverage, and passing frozen Indexed8 compatibility evidence.</done>
</task>

</tasks>

<evidence_matrix>
| Selected depth | Accepted proof | Atomic rejection cases | Required unchanged observables |
|---|---|---|---|
| One / cap 2 | Exact-cap source succeeds; private facts show `row_bytes = ceil(width / 8)` and budget work falls by exactly `selected_work`. | 3-entry palette; `max_width = width - 1`; `max_height = height - 1`; `max_pixels = pixels - 1`; `max_output_bytes = frame.total_length - 1`; `max_work = selected_work - 1`; budget work remaining one below `selected_work`. | `MemoryWriter.position() == 0` for each failure and every `ResourceRemaining` field equals its pre-call snapshot. |
| Two / cap 4 | Exact-cap source succeeds; private facts show `row_bytes = ceil(width * 2 / 8)` and budget work falls by exactly `selected_work`. | 5-entry palette and the same one-below width, height, pixels, output, limit-work, and budget-work cases. | `MemoryWriter.position() == 0` for each failure and every `ResourceRemaining` field equals its pre-call snapshot. |
| Four / cap 16 | Exact-cap source succeeds; private facts show `row_bytes = ceil(width * 4 / 8)` and budget work falls by exactly `selected_work`. | 17-entry palette and the same one-below width, height, pixels, output, limit-work, and budget-work cases. | `MemoryWriter.position() == 0` for each failure and every `ResourceRemaining` field equals its pre-call snapshot. |
| Fixed Eight compatibility | Existing encode_indexed8 and new_indexed8 inputs drain through unchanged fixed-8 wrappers and match their frozen Type-3/8 bytes. | Existing legacy rejection coverage remains green. | Existing eager and chunk vector assertions remain byte-identical. |
</evidence_matrix>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned indexed source to eager preflight | Raster dimensions, palette cardinality, selected depth, limits, and budget enter the encoder. |
| preflight facts to acknowledged PNG machine | Exact row/frame facts control IHDR, bufferless scanline emission, CRC/Adler accounting, and writer-visible bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Denial of service | _png_encode_indexed_preflight | high | mitigate | Per D-04, checked width-depth/frame arithmetic and all resource limits precede the single budget charge and machine construction; tests cover exact and rejected limits. |
| T-79-02 | Tampering | PngEncodeMachine::scanline_byte and byte_at | medium | mitigate | Per D-03 and D-05, pack unscaled indices at explicit MSB-first shifts into zero-initialized bytes; independent odd-width scanline, IHDR, chunk-order, and CRC assertions catch framing drift. |
| T-79-03 | Tampering | palette admission and ancillary framing | medium | mitigate | Per D-04, enforce selected-depth PLTE caps before output; retain existing canonical PLTE/tRNS accessors and acknowledged CRC progression, then prove RGB8/RGBA8 decode-back. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target all --frozen`. The package suite must exercise the new public eager surface, independent Stored wire assertions, atomicity tests, decoder round trips, and existing Indexed8 regressions on wasm, wasm-gc, js, and native.
</verification>

<success_criteria>
- INDEXLOW-01: PngEncoder::encode_indexed accepts only PngIndexedBitDepth::One, ::Two, and ::Four and produces non-interlaced Type-3 eager output from PngIndexedImage.
- INDEXLOW-02: The selected palette cap and all packed resource facts are admitted atomically before output and the sole encode-work charge; every one-below width, height, pixels, output, work, and budget case leaves writer position and the complete budget snapshot unchanged.
- INDEXLOW-03: Raw odd-width vectors prove MSB-first zero-tailed payloads, PLTE/canonical tRNS framing, CRC correctness, and public RGB8/RGBA8 decoding.
- Compatibility and scope: encode_indexed8 and new_indexed8 preserve frozen Type-3/8 eager and chunk bytes through fixed-8 wrappers, while Phase 80 remains untouched: no caller-buffered indexed-low-bit surface or lifecycle qualification is added.
</success_criteria>

## Source Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explicit bounded Type-3/1, /2, /4 eager output with exact packed rows and PLTE/tRNS | 01 | COVERED | Tracer implements the public route; expansion verifies all depths. |
| REQ | INDEXLOW-01 | Explicit Type-3 1/2/4 eager output from PngIndexedImage | 01 | COVERED | Task 1. |
| REQ | INDEXLOW-02 | Atomic palette/resource admission | 01 | COVERED | Tasks 1 and 2. |
| REQ | INDEXLOW-03 | MSB-first zero tails, PLTE/tRNS, RGB8/RGBA8 decode | 01 | COVERED | Tasks 1 and 2. |
| RESEARCH | — | One depth fact feeds preflight, IHDR, and scalar packing | 01 | COVERED | Task 1 names the consuming functions. |
| RESEARCH | — | Checked packed frame math and one existing charge | 01 | COVERED | Task 1 implementation and Task 2's complete depth-by-depth admission matrix. |
| RESEARCH | — | Independent Stored vectors plus public decoder oracle | 01 | COVERED | Task 2. |
| CONTEXT | D-01 | Finite public selector and additive eager API; Indexed8 frozen | 01 | COVERED | Task 1. |
| CONTEXT | D-02 | Canonical unpacked source and emission-only packing | 01 | COVERED | Task 1. |
| CONTEXT | D-03 | Single Stored/None/non-interlaced machine, MSB-first tails, PLTE/tRNS | 01 | COVERED | Task 1. |
| CONTEXT | D-04 | Atomic palette caps, checked facts, limits, one charge | 01 | COVERED | Tasks 1 and 2. |
| CONTEXT | D-05 | Independent vectors, decoder proof, retained legacy and Indexed8 bytes, Phase 80 excluded | 01 | COVERED | Task 2, including fixed-8 eager and chunk compatibility checks. |

<output>
Create `.planning/phases/79-indexed-low-bit-eager-packing/79-01-SUMMARY.md` when done.
</output>
