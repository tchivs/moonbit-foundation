---
phase: 11-portable-processing-pipeline-evidence
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/ops/processing_pipeline_test.mbt
  - modules/mb-image/ops/processing_pipeline_wbtest.mbt
autonomous: true
requirements: [INTEG-02]
must_haves:
  truths:
    - "Public behavioral tests prove the composed public PPM-compatible processing route on js, wasm, wasm-gc, and native."
    - "Adversarial evidence proves a pipeline failure boundary returns a typed error without partial budget mutation."
  artifacts:
    - "modules/mb-image/ops/processing_pipeline_test.mbt"
    - "modules/mb-image/ops/processing_pipeline_wbtest.mbt"
  key_links:
    - "Tests invoke resize_nearest, RGB/RGBA conversion, composite_source_over, and RGBA/RGB conversion through the public ops package."
---

<objective>
Add four-target behavioral and adversarial evidence for the finished public geometry-plus-raster pipeline, per D-03 and D-04.

Purpose: prevent the consumer route from being considered proven merely because its individual Phase 9 and Phase 10 operations passed in isolation.
Output: direct public composition vectors and white-box failure/budget-atomicity coverage, both included by the existing ops package test commands.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-CONTEXT.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-RESEARCH.md
@.planning/phases/09-checked-image-geometry-and-diagnostics/09-VERIFICATION.md
@.planning/phases/10-alpha-correct-pixel-processing/10-VERIFICATION.md
@modules/mb-image/ops/processing_test.mbt
@modules/mb-image/ops/processing_wbtest.mbt
@modules/mb-image/ops/resize_convert_wbtest.mbt

<interfaces>
Public composed route:
  resize_nearest(source.view(), 2UL, 1UL, budget)
  rgb8_to_straight_rgba8(image.view(), budget)
  composite_source_over(source_rgba.view(), destination_rgba.view(), budget)
  straight_rgba8_to_rgb8(composite.view(), budget)

Prior test conventions: public *_test.mbt tests invoke exported APIs with fixed byte vectors; *_wbtest.mbt creates hostile representable inputs and compares every Budget remaining field to demonstrate rejection atomicity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public fixed-vector evidence for the composed processing route</name>
  <files>modules/mb-image/ops/processing_pipeline_test.mbt</files>
  <action>Create a public black-box ops test file per D-03 and D-04 that builds only documented compatible RGB8 images through established test helpers, invokes resize_nearest, both explicit conversion calls, and composite_source_over in the same order as the portable example, and asserts the 2x1 opaque RGB result. Use the deterministic foreground bytes 0c 22 38 and assert both result pixels/channels plus output dimensions, source-over operand order, and output metadata/disposition relevant to the public contracts. Include a second direct public boundary assertion that the RGB/RGBA conversion or composite route returns the established typed error for an incompatible representable input; do not duplicate implementation math, access package-private conversion/processing helpers, or introduce a new processing API.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js --frozen; moon test modules/mb-image/ops --target wasm --frozen; moon test modules/mb-image/ops --target wasm-gc --frozen; moon test modules/mb-image/ops --target native --frozen</automated>
  </verify>
  <done>Public tests directly prove the composed resize-to-source-over route and a documented typed failure behavior on all four required targets.</done>
</task>

<task type="auto">
  <name>Task 2: Add adversarial pipeline budget-atomicity coverage</name>
  <files>modules/mb-image/ops/processing_pipeline_wbtest.mbt</files>
  <action>Create a white-box test file per D-04 using the existing local image and Budget snapshot patterns. Drive the real composed operation sequence to a resource boundary chosen from an actual charged stage, assert its stable typed CoreError, and compare bytes, allocations, allocation_size, width, height, pixels, depth, and work remaining before and after rejection. Keep the successful path’s expected data independent and fixed; do not add an alternative resize/composite algorithm, relax an existing API precondition, or test filesystem/host behavior.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js --frozen; moon test modules/mb-image/ops --target wasm --frozen; moon test modules/mb-image/ops --target wasm-gc --frozen; moon test modules/mb-image/ops --target native --frozen</automated>
  </verify>
  <done>Adversarial Phase 11 evidence demonstrates a deterministic composed-pipeline failure boundary with an unchanged complete budget snapshot across every supported target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public ImageView input → ops pipeline | Caller-controlled image descriptor, metadata, bytes, and budget cross into resize/conversion/composite APIs. |
| test budget → individual operation allocation | Tests must distinguish preflight rejection from partial resource charging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | composed output assertions | medium | mitigate | Use fixed semantic RGB channel vectors, dimensions, operand order, and public metadata assertions rather than a self-derived production oracle. |
| T-11-05 | Denial of service | pipeline resource limits | high | mitigate | Exercise a real insufficient-resource boundary and assert the complete Budget snapshot is unchanged after the typed error. |
| T-11-06 | Repudiation | portable target coverage | medium | mitigate | Run the full public ops suite explicitly with --target js, wasm, wasm-gc, and native. |
</threat_model>

<verification>
Run the ops suite on all four targets. Confirm the new public file calls exported APIs only and the white-box file checks every Budget resource field around its selected failure boundary.
</verification>

<success_criteria>
INTEG-02 is met by direct public composition assertions and adversarial error/budget evidence that execute on js, wasm, wasm-gc, and native.
</success_criteria>

<output>
Create `.planning/phases/11-portable-processing-pipeline-evidence/11-02-SUMMARY.md` when done.
</output>
