---
phase: 11-portable-processing-pipeline-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ppm-portable/main/main.mbt
  - modules/mb-image/README.mbt.md
autonomous: true
requirements: [INTEG-01]
must_haves:
  truths:
    - "The portable public example decodes strict PPM P6 foreground and background inputs, resizes and composites them through public operations, then encodes an RGB PPM result."
    - "The example succeeds identically on js, wasm, wasm-gc, and native with exact 17-byte output evidence and semantic pixel checks."
  artifacts:
    - "examples/ppm-portable/main/main.mbt"
    - "modules/mb-image/README.mbt.md"
  key_links:
    - "main.mbt calls PpmDecoder/PpmEncoder through codec traits and calls resize_nearest, rgb8_to_straight_rgba8, composite_source_over, and straight_rgba8_to_rgb8 in that order."
---

<objective>
Turn the existing public four-target PPM example into the real Phase 9+10 processing pipeline required by D-01 through D-03.

Purpose: give library users a runnable consumer-level proof that strict PPM I/O, deterministic nearest resize, explicit RGB/RGBA representation conversion, and alpha-correct source-over compose without internal shortcuts.
Output: an updated portable executable and documentation that identifies its cross-target command and pipeline semantics.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-CONTEXT.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-RESEARCH.md
@.planning/phases/09-checked-image-geometry-and-diagnostics/09-VERIFICATION.md
@.planning/phases/10-alpha-correct-pixel-processing/10-VERIFICATION.md
@examples/ppm-portable/main/main.mbt
@modules/mb-image/README.mbt.md

<interfaces>
From modules/mb-image/ops/pkg.generated.mbti:
  pub fn resize_nearest(@storage.ImageView, UInt64, UInt64, @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
  pub fn rgb8_to_straight_rgba8(@storage.ImageView, @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
  pub fn composite_source_over(@storage.ImageView, @storage.ImageView, @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
  pub fn straight_rgba8_to_rgb8(@storage.ImageView, @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]

Existing main.mbt supplies MemoryReader/MemoryWriter, CodecLimits, PpmParserLimits, and a rolling-257 digest. Keep that portable in-memory boundary and public codec invocation pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the flip-only portable example with the public resize-and-source-over flow</name>
  <files>examples/ppm-portable/main/main.mbt</files>
  <behavior>
    - The example decodes strict opaque foreground P6 1x1 RGB bytes 0c 22 38 and a compatible opaque background P6 2x1 input through PpmDecoder and ImageDecoder.
    - It resizes foreground to 2x1, converts both decoded RGB8 images through rgb8_to_straight_rgba8, composites foreground over background, converts the opaque result through straight_rgba8_to_rgb8, and encodes with PpmEncoder and ImageEncoder.
    - The encoded P6 result is exactly 17 bytes: header for 2x1 plus two RGB pixels 0c 22 38; semantic output pixels and rolling-257 digest 9386158 are asserted before success output carries the precomputed SHA-256 cf8f36752d62cd88334bfa8fc45c55bdbf0f70275180bc6d2b14bf3810676464 identity of that fully asserted vector.
    - All decode/operation/encode diagnostics are empty and each operation receives adequate fresh budget/codec limits.
  </behavior>
  <action>Modify the existing executable, per D-01, D-02, and D-03, rather than adding a second example. Retain only the real strict PPM decoder/encoder, MemoryReader/MemoryWriter, and public @ops calls; remove the flip-only transform from the success route. Decode two deterministic P6 inputs, resize the 1x1 foreground to 2x1 with @ops.resize_nearest, explicitly bridge RGB8 to representable straight RGBA8 for both operands, call @ops.composite_source_over with foreground as source, bridge the known-opaque result back to RGB8, and encode it. Keep built-in sRGB, TopLeft orientation, and empty opaque metadata by relying on strict PPM decode and the public conversion APIs. Verify exact output byte positions as well as dimensions, byte counts, pixel channel values, diagnostics, and rolling digest; after that full-byte assertion succeeds, print its precomputed SHA-256 identity in the success line so each target command can prove this new route ran without adding a runtime SHA implementation. Do not construct image storage/descriptors directly, add an algorithm, use filesystem/CLI state, or alter package manifests.</action>
  <verify>
    <automated>$targets = @('js','wasm','wasm-gc','native'); foreach ($target in $targets) { $output = moon -C examples/ppm-portable run main --target $target --frozen 2>&1; if ($LASTEXITCODE -ne 0) { throw "$target example failed" }; $line = ($output | Out-String); foreach ($required in @('bytes_written=17','digest=9386158','sha256=cf8f36752d62cd88334bfa8fc45c55bdbf0f70275180bc6d2b14bf3810676464','resize_nearest','composite_source_over')) { if ($line -notmatch [regex]::Escape($required)) { throw "$target missing $required" } } }</automated>
  </verify>
  <done>The sole portable example prints its success evidence on all four supported targets only after it has exercised decode, resize, both representation bridges, source-over, and encode, with the 17-byte vector, semantic pixels, and deterministic digest checked in process.</done>
</task>

<task type="auto">
  <name>Task 2: Document the runnable portable processing proof</name>
  <files>modules/mb-image/README.mbt.md</files>
  <action>Update the standalone-consumer documentation per D-01 through D-03 to describe the existing ppm-portable executable as strict PPM decode to nearest resize to explicit RGB8/straight-RGBA8 conversion to source-over to RGB8/PPM encode. Include the four exact frozen Moon commands that run it for js, wasm, wasm-gc, and native, and state that its deterministic proof checks complete encoded bytes plus semantic pixels. Keep the Native CLI adapter description separate, and do not state performance results, add release instructions, or change the package’s algorithm contract.</action>
  <verify>
    <automated>moon -C modules/mb-image check README.mbt.md --target js --frozen; moon -C modules/mb-image check README.mbt.md --target wasm --frozen; moon -C modules/mb-image check README.mbt.md --target wasm-gc --frozen; moon -C modules/mb-image check README.mbt.md --target native --frozen</automated>
  </verify>
  <done>The public package README gives users an accurate four-target command path to the updated example and names its real codec, geometry, and raster-operation boundaries without changing release or benchmark documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded PPM bytes → decoder | Example input remains untrusted codec data until strict parser/codec limits validate it. |
| RGB8 decoded images → RGBA compositing | Public conversion and composite capability/metadata gates protect the strict processing representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | portable PPM vector | medium | mitigate | Assert the complete encoded 17-byte payload, semantic pixel channels, decoded/encoded counts, empty diagnostics, and rolling digest before success output. |
| T-11-02 | Denial of service | strict decode and operation allocations | medium | mitigate | Retain bounded CodecLimits, parser limits, and fresh caller budgets at each decode/operation/encode boundary. |
| T-11-03 | Elevation of privilege | portable example host boundary | low | accept | The example is in-memory only and imports no host capability or native adapter; no privileged boundary exists in scope. |
</threat_model>

<verification>
Run the example on all four required targets and execute the literate package README checks on those same targets. Inspect the source route to confirm the public decoder, resize, conversion, source-over, conversion, and encoder calls occur in order.
</verification>

<success_criteria>
INTEG-01 is met by one documented public example using the actual strict codec and Phase 9/10 APIs, with deterministic encoded-byte and semantic output proof across all supported targets.
</success_criteria>

<output>
Create `.planning/phases/11-portable-processing-pipeline-evidence/11-01-SUMMARY.md` when done.
</output>
