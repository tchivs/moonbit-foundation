---
phase: 51-bounded-gray-alpha-png-encoding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYA-02
  - GRAYA-03
must_haves:
  truths:
    - "A packed U8 GrayAlpha source with straight alpha, encoded builtin sRGB, and top-left orientation encodes through explicit eager and caller-buffered graya8 factories as a non-interlaced type-4, 8-bit PNG."
    - "The new route preserves distinct, non-symmetric gray and alpha components at the PNG raster boundary and through the existing decoder's local eager-fidelity path."
    - "Every graya8 factory uses the existing profile-aware bounded preflight and replay machine; legacy, Gray8, and Gray16 factory selections retain their existing profile values."
  artifacts:
    - "modules/mb-image/png/png.mbt exposes the four explicit PngEncoder graya8 factory shapes and the private GrayAlpha8 profile."
    - "modules/mb-image/png/encode.mbt admits only the locked GrayAlpha descriptor identity and derives a two-byte U8 raster layout without a conversion buffer."
    - "modules/mb-image/png/stream_encode.mbt exposes the four PngChunkEncoder graya8 factory shapes and emits the GrayAlpha8 IHDR through PngEncodeMachine."
    - "modules/mb-image/png/encode_test.mbt and modules/mb-image/png/stream_encode_test.mbt contain focused GrayAlpha eager and caller-buffered regressions."
  key_links:
    - "PngEncoder::new_graya8_with_strategies selects PngEncodeProfile::GrayAlpha8, which _png_encode_source validates before shared bounded preflight reads source pixels or charges the budget."
    - "PngChunkEncoder::new_graya8_with_strategies reaches PngEncodeMachine::new_with_profile before a PngChunkEncoder value or callback-scoped mutable lease can exist."
    - "The generic U8 _png_wire_byte fallback receives channels=2 and emits source component 0 then component 1, while IHDR writes bit depth 8, colour type 4, and interlace 0."
---

<objective>
Deliver the production-quality Gray+Alpha8 PNG tracer: explicit eager and caller-buffered factory families connected to the existing bounded encoder, with one real source-to-PNG-to-decoder path.

Purpose: satisfy the new two-component PNG profile without altering any legacy PNG route or introducing another encoder pipeline.
Output: private GrayAlpha8 profile support, eight public factory entry points, and focused baseline regressions.
</objective>

## Artifacts this phase produces

- Explicit `PngEncoder::new_graya8*` and `PngChunkEncoder::new_graya8*` families.
- One private `PngEncodeProfile::GrayAlpha8` flowing through admission, preflight, scalar raster replay, and IHDR emission.
- Focused package regressions for Gray+Alpha source-pair fidelity and ordinary eager/chunk construction.

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-CONTEXT.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-RESEARCH.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-PATTERNS.md
@.planning/phases/50-gray-alpha-image-model/50-01-SUMMARY.md

<interfaces>
From `modules/mb-image/png/png.mbt`:
- `PngEncoder` stores `strategy`, `filter_strategy`, `interlace_strategy`, and private `profile`.
- The Gray16 family establishes the required shapes: `new_gray16()`, `new_gray16_with_compression_strategy(strategy)`, `new_gray16_with_filter_strategy(filter_strategy)`, and `new_gray16_with_strategies(strategy, filter_strategy)`.

From `modules/mb-image/png/stream_encode.mbt`:
- `PngChunkEncoder::new_gray16_with_strategies(source, strategy, filter_strategy, limits, budget, diagnostics)` calls `PngEncodeMachine::new_with_profile` and returns `Err` before constructing the public encoder.
- `PngEncodeMachine::new_with_profile(source, profile, strategy, filter_strategy, interlace_strategy, limits, budget, diagnostics)` is the shared preflight/replay seam.

From `modules/mb-image/png/encode.mbt`:
- `_png_encode_source(source, profile)` returns the raster channel-byte count after format and metadata admission.
- `_png_wire_byte(source, profile, row, channels, position)` already uses generic `get_byte(pixel, row, component)` behavior for every non-Gray16 U8 profile.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire one default Gray+Alpha8 image through eager and caller-buffered PNG encoding</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/png.mbt`: the private profile enum and complete Gray16 eager family.
    - `modules/mb-image/png/encode.mbt`: `_png_encode_source`, `_png_wire_byte`, and `_png_encode_preflight_with_interlace_profile`.
    - `modules/mb-image/png/stream_encode.mbt`: Gray16 chunk construction, `PngEncodeMachine::new_with_profile`, and IHDR byte emission.
    - `modules/mb-image/png/encode_test.mbt`: Gray16 fixture, memory-writer helper, and public decoder-fidelity helper patterns.
  </read_first>
  <action>Per D-01, add `PngEncoder::new_graya8()` and `PngChunkEncoder::new_graya8()` as the default Stored/None entry points, then add their compression-only, filter-only, and combined-strategy siblings by mirroring the Gray16 signatures and delegation shape. Add only private `PngEncodeProfile::GrayAlpha8`; each combined graya8 factory fixes `PngInterlaceStrategy::None`. Per D-02, add an exhaustive GrayAlpha8 admission arm in `_png_encode_source` that requires `ChannelOrder::GrayAlpha`, `ComponentType::U8`, and `Some(AlphaMode::Straight)`, uses `graya8-required` for a channel-order mismatch, and retains the existing packed, encoded builtin-sRGB, top-left, tight-row, checked-geometry, limit, and budget gates. Per D-03, make profile matching exhaustive: reject a non-None interlace selection before preflight work and map GrayAlpha8 to IHDR colour type 4 with bit depth 8 and methods/interlace bytes 0. Per D-05, retain the generic U8 scalar-wire path with `channels = 2UL` so component order is gray then alpha at the PNG boundary. Per D-04, route the caller-buffered combined factory directly through `PngEncodeMachine::new_with_profile`; preserve the one-machine bounded filter, planner, and acknowledgement-safe replay ownership. Per D-06, make this an additive profile/factory change: retain the current legacy, Gray8, and Gray16 factory/profile arms and their scalar behavior. Add a compact packed `graya8` test fixture with non-symmetric pairs; exercise the default eager and chunk routes through real memory output, assert PNG signature plus IHDR depth/type/interlace fields, inspect the stored/None raster bytes in gray-then-alpha order, and use the existing decoder path to assert local eager round-trip component fidelity.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The default eager and caller-buffered graya8 routes are real end-to-end paths for a valid packed source; emitted IHDR is 8-bit/type-4/non-interlaced, the non-symmetric source pairs survive the tested path, and invalid profile admission still occurs before output.</done>
</task>

<task type="auto">
  <name>Task 2: Prove every graya8 factory shape selects the shared ordinary bounded route</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A compression-only graya8 factory fixes the None filter, and a filter-only graya8 factory fixes Stored compression.
    - Each of Stored, FixedOrStored, and DynamicOrFixedOrStored combined with None and Adaptive emits an 8-bit, type-4, non-interlaced eager PNG.
    - The ordinary caller-buffered drain of each combined pair equals its eager output and all four caller-buffered factory forms construct from the valid source.
  </behavior>
  <action>Per D-01 and D-04, extend the focused GrayAlpha fixture tests with the exact Gray16 family matrix: cover each eager convenience factory and the six compression/filter combinations, then cover each caller-buffered convenience constructor and ordinary drain parity for the same combinations. Reuse the existing package helpers for real eager encoding, real chunk draining, and IHDR inspection. Assert only semantic framing and byte identity for these ordinary drains; retain the separate hostile-capacity terminal and cross-target qualification boundary for Phase 52. Keep fixture pairs non-symmetric and preserve the established test-local naming and typed error conventions.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>All default, compression-only, filter-only, and combined graya8 factories compile and execute; all six bounded strategy pairs retain correct framing, and ordinary chunk output equals eager output.</done>
  <reversibility rating="reversible">The regression layout is package-local test coverage and can evolve without changing the public contract.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView to profile admission | A caller-controlled descriptor, metadata, geometry, and packed backing are accepted only when they meet the GrayAlpha8 contract. |
| PngEncodeMachine to writer or lease | Planned bytes become observable through eager writes or a caller-owned mutable lease only after complete preflight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | `_png_encode_source` GrayAlpha8 profile arm | high | mitigate | Task 1 implements D-02's closed descriptor predicates and typed capability errors before source reads, output, or budget charge. |
| T-51-02 | Tampering | `_png_wire_byte` and IHDR emission | high | mitigate | Task 1 uses the existing generic two-component U8 mapping and explicit type-4 IHDR branch; non-symmetric source-pair tests detect component swaps. |
| T-51-03 | Denial of Service | profile-aware preflight and compression planning | high | mitigate | Task 1 routes GrayAlpha8 through the existing checked geometry, resource-limit, budget, filter, and compression planner path rather than allocating a raster staging buffer. |
| T-51-04 | Tampering | caller-buffered construction | medium | mitigate | Task 1 keeps `Err` before public encoder construction; Plan 51-02 exercises every strategy pair's pre-exposure result and lease sentinel. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target native --frozen`. The completed package run must cover the added eager and ordinary caller-buffered GrayAlpha regressions without changing public evidence ownership assigned to Phase 52.
</verification>

<success_criteria>
- GRAYA-02 has explicit eager and caller-buffered factory families that emit type-4, 8-bit, non-interlaced PNGs from the locked packed GrayAlpha8 descriptor.
- The source gray/alpha component order is observable in emitted raster bytes and locally verified through existing decode behavior.
- GRAYA-03's three compression choices and two filter choices use the existing bounded machine in both public routes.
</success_criteria>

## Source Coverage Audit

| Source | Required item | Coverage |
|--------|---------------|----------|
| GOAL | compatible Gray+Alpha8 sources flow through bounded eager and caller-buffered encoding | Tasks 1-2 |
| REQ | GRAYA-02 explicit factories, type-4/8-bit/non-interlaced output, pair preservation | Tasks 1-2 |
| REQ | GRAYA-03 shared preflight/filter/planner/replay path | Tasks 1-2 and Plan 51-02 |
| RESEARCH | private profile, generic two-U8 wire mapping, Gray16 analogue, no new dependency | Task 1 |
| CONTEXT | D-01 factory families | Tasks 1-2 |
| CONTEXT | D-02 closed descriptor admission before exposure | Task 1 and Plan 51-02 Task 2 |
| CONTEXT | D-03 private type-4/non-interlaced profile | Task 1 |
| CONTEXT | D-04 shared bounded pipeline | Tasks 1-2 |
| CONTEXT | D-05 gray/alpha wire order | Task 1 |
| CONTEXT | D-06 additive legacy preservation | Task 1 |

<output>
Create `.planning/phases/51-bounded-gray-alpha-png-encoding/51-01-SUMMARY.md` when done.
</output>
