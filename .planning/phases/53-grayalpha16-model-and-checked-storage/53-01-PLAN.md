---
phase: 53-grayalpha16-model-and-checked-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/model/descriptor.mbt
  - modules/mb-image/model/model_test.mbt
  - modules/mb-image/storage/storage_test.mbt
autonomous: true
requirements:
  - GRAYA16-01
must_haves:
  truths:
    - "A caller can construct a packed U16 GrayAlpha descriptor with exactly two components, straight alpha, encoded builtin sRGB, and top-left metadata."
    - "A caller can write and read distinct storage-order bytes for both U16 gray and alpha components through the existing checked packed views."
    - "Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 descriptor and storage behavior remains stable, and GrayAlpha remains outside reference operations."
  artifacts:
    - path: "modules/mb-image/model/descriptor.mbt"
      provides: "Public graya16 factory and exact U8-or-U16 GrayAlpha descriptor admission."
      contains: "ImageFormat::graya16"
    - path: "modules/mb-image/model/model_test.mbt"
      provides: "Public GrayAlpha16 descriptor, malformed-identity, and legacy-behavior regressions."
      contains: "graya16"
    - path: "modules/mb-image/storage/storage_test.mbt"
      provides: "Checked GrayAlpha16 U16 component-byte storage regression."
      contains: "graya16_descriptor"
  key_links:
    - from: "modules/mb-image/model/descriptor.mbt"
      to: "modules/mb-image/storage/storage_test.mbt"
      via: "The descriptor's U16 component size and GrayAlpha channel count derive the packed four-byte pixel stride consumed by generic checked views."
      pattern: "bytes_per_component() * channel_count()"
    - from: "modules/mb-image/model/descriptor.mbt"
      to: "modules/mb-image/ops/copy_flip.mbt"
      via: "The retained GrayAlpha false capability branch keeps the new descriptor outside reference and copy semantics."
      pattern: "ChannelOrder::GrayAlpha => false"
---

<objective>
Implement the additive public U16 GrayAlpha descriptor identity and prove it travels through existing checked packed storage without changing established image or operation contracts.

Purpose: Establish the exact source-model contract required by GRAYA16-01 before later phases consume it.
Output: A public `ImageFormat::graya16()` factory, exact descriptor admission, and all-target model/storage compatibility evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-grayalpha16-model-and-checked-storage/53-CONTEXT.md
@.planning/phases/53-grayalpha16-model-and-checked-storage/53-RESEARCH.md
@.planning/phases/53-grayalpha16-model-and-checked-storage/53-PATTERNS.md
@.planning/milestones/v0.15-phases/47-gray16-factory-compatibility/47-01-SUMMARY.md
@.planning/milestones/v0.16-phases/50-gray-alpha-image-model/50-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Admit one GrayAlpha16 descriptor through generic checked storage</name>
  <reversibility rating="one-way">D-01 establishes a public format spelling and canonical metadata contract; changing it after downstream encoder adoption breaks a published API contract.</reversibility>
  <read_first>
    - modules/mb-image/model/descriptor.mbt — factory style, GrayAlpha identity predicate, checked plane shape, and retained reference-operation gate.
    - modules/mb-image/model/model_test.mbt — GrayAlpha8 descriptor and invalid-identity matrix.
    - modules/mb-image/storage/storage_test.mbt — Gray16 component-byte and GrayAlpha8 two-channel storage controls.
    - modules/mb-image/storage/views.mbt and modules/mb-image/storage/owned_image.mbt — existing packed U8/U16 component-byte and callback-scoped mutation contracts to reuse unchanged.
    - modules/mb-image/ops/copy_flip.mbt — established fail-closed copy capability boundary that remains unchanged.
  </read_first>
  <behavior>
    - A 1x1 `ImageFormat::graya16()` descriptor with one four-byte packed plane accepts only `Some(AlphaMode::Straight)`, encoded builtin sRGB, and top-left metadata; it exposes U16, GrayAlpha, Packed, Little, two channels, one plane, four row bytes, and no reference-operation support.
    - A real `OwnedImage` made from that descriptor writes gray bytes `34,12` and alpha bytes `C5,A7` through `set_component_byte`, then its immutable view returns all four distinct bytes from the same component indices.
    - Checked storage rejects channel index 2 and U16 component-byte index 2, while the existing U16-only `get_byte` boundary remains unavailable.
  </behavior>
  <files>modules/mb-image/model/descriptor.mbt, modules/mb-image/model/model_test.mbt, modules/mb-image/storage/storage_test.mbt</files>
  <action>Start RED with focused public model and storage tests for the stated behavior, then commit the failing test as `test(53-01): specify GrayAlpha16 model and storage`. Per D-01: add `ImageFormat::graya16()` adjacent to `graya8()` with `U16`, `GrayAlpha`, `Packed`, and `Little` fields. Per D-02: broaden only `validate_gray_alpha_identity` to the explicit U8-or-U16 component set; preserve the existing straight-alpha, packed, little-endian, sRGB, encoded transfer, builtin-profile, and top-left clauses and their typed error boundary. Per D-03: add a `graya16_descriptor()` storage helper and exercise `OwnedImage` plus existing component-byte view APIs with the four non-symmetric byte values, without a representation, buffer, view, or ownership change. Per D-04: keep the current `GrayAlpha => false` reference-operation behavior intact and assert it through the valid descriptor. Run the all-target suite, then commit the implementation and passing regressions as `feat(53-01): add GrayAlpha16 descriptor admission`.</action>
  <verify>
    <automated>moon test --target all modules/mb-image/model modules/mb-image/storage modules/mb-image/ops; moon check --target all</automated>
  </verify>
  <done>The public GrayAlpha16 descriptor creates a four-byte packed U16 pixel, generic checked views preserve both U16 component byte pairs, and the package model/storage/ops suite plus workspace all-target check pass.</done>
</task>

<task type="auto">
  <name>Task 2: Lock malformed GrayAlpha16 rejection and legacy model/storage controls</name>
  <read_first>
    - modules/mb-image/model/descriptor.mbt — final exact GrayAlpha admission predicate and typed `InvalidEncoding` paths.
    - modules/mb-image/model/model_test.mbt — current GrayAlpha8 controls and the U16 GrayAlpha case being converted from invalid to valid.
    - modules/mb-image/storage/storage_test.mbt — existing Gray16 and GrayAlpha8 read/write regression values that protect legacy access behavior.
  </read_first>
  <files>modules/mb-image/model/model_test.mbt, modules/mb-image/storage/storage_test.mbt</files>
  <action>Per D-02: expand the black-box matrix by replacing the former canonical U16 GrayAlpha rejection with the accepted `graya16()` identity, then retain invalid U16 GrayAlpha controls for F32, planar layout, big-endian storage, missing or premultiplied alpha, linear or unknown colour metadata, a non-builtin profile, rotated orientation, and a malformed four-byte row shape. Assert the existing typed descriptor category and retain the GrayAlpha8 invalid matrix. Per D-04: preserve and run the existing Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 model/storage controls without altering their expected values; preserve the existing GrayAlpha reference-operation rejection check. Commit the expanded regression evidence as `test(53-01): lock GrayAlpha16 admission compatibility`.</action>
  <verify>
    <automated>moon test --target all modules/mb-image/model modules/mb-image/storage modules/mb-image/ops; moon check --target all</automated>
  </verify>
  <done>Only the canonical packed U16 GrayAlpha identity joins the existing U8 identity; every malformed GrayAlpha16 variant fails through the established descriptor boundary, while existing format and operation controls pass unchanged on js, wasm, wasm-gc, and native.</done>
</task>

</tasks>

## Source coverage audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Construct and inspect packed U16 GrayAlpha with explicit straight alpha while retaining existing descriptor, storage, and operation behavior | 53-01 | COVERED | Task 1 creates the admitted descriptor and real checked-storage path; Task 2 protects all stated compatibility controls. |
| REQ | GRAYA16-01 | One gray and one straight-alpha U16 component with stable existing formats | 53-01 | COVERED | Frontmatter truths and both task behaviors test the public model, backing access, malformed admission, and legacy behavior. |
| RESEARCH | — | Add only the U16 factory and exact identity predicate; reuse descriptor-derived packed U16 view access | 53-01 | COVERED | Task 1 changes only the documented model seam and proves the existing generic storage path. |
| RESEARCH | — | Retain the fail-closed reference/copy boundary | 53-01 | COVERED | Task 1 asserts retained descriptor operation support is false; both all-target checks execute the existing ops controls. |
| CONTEXT | D-01 | Public `graya16` U16/GrayAlpha/Packed/Little identity with canonical metadata | 53-01 | COVERED | Task 1 implements and verifies the locked public contract. |
| CONTEXT | D-02 | Admit exactly canonical U16 GrayAlpha and reject malformed variants | 53-01 | COVERED | Tasks 1–2 retain the identity predicate and exhaustive typed rejection matrix. |
| CONTEXT | D-03 | Reuse generic checked storage for distinct gray and alpha U16 byte pairs | 53-01 | COVERED | Task 1 uses real owned storage and all four component-byte positions without production storage changes. |
| CONTEXT | D-04 | Preserve legacy formats and retain unsupported GrayAlpha reference operations | 53-01 | COVERED | Task 2 keeps existing controls intact; Task 1 asserts the retained false operation capability. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller descriptor metadata → `ImageDescriptor::new` | Caller-supplied component, layout, endianness, alpha, colour, profile, and orientation facts decide whether storage can be admitted. |
| Validated descriptor → owned storage/view | Descriptor channel count and component size drive allocation size, row bytes, checked channel bounds, and component-byte offsets. |
| Image model → reference operations | A storable format is not automatically authorized for reference or copy semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-01 | Tampering | `validate_gray_alpha_identity` | high | mitigate | Tasks 1–2 limit admission to the two explicit component types and prove unsupported representation, alpha, colour, profile, orientation, and row-shape variants fail through the established typed boundary. |
| T-53-02 | Tampering | descriptor-derived checked component-byte offsets | high | mitigate | Task 1 writes and reads distinct bytes in both U16 lanes and proves out-of-range channel and component-byte indices fail before storage access. |
| T-53-03 | Denial of Service | descriptor plane shape and `OwnedImage` allocation | medium | mitigate | Reuse checked descriptor shape arithmetic and existing budgeted owned allocation; Task 1 exercises allocation only from the accepted four-byte descriptor. |
| T-53-04 | Elevation of Privilege | reference/copy capability boundary | medium | mitigate | Task 1 asserts the descriptor retains false reference-operation support, while all-target ops regressions retain the existing fail-closed GrayAlpha behavior. |
| T-53-05 | Repudiation | compatibility evidence | low | accept | Deterministic public package tests and all-target compilation provide project-standard executable evidence for the model-only change. |
</threat_model>

<verification>
- `moon test --target all modules/mb-image/model modules/mb-image/storage modules/mb-image/ops`
- `moon check --target all`
- Confirm `git diff --check` is clean and the implementation diff is limited to the three frontmatter file paths.
</verification>

<success_criteria>
- `ImageFormat::graya16()` is publicly constructible and admits only the canonical packed U16 straight-alpha GrayAlpha descriptor identity.
- Checked generic storage exposes separate storage-order bytes for both U16 components and rejects invalid component addressing.
- Existing model, storage, and operation evidence remains green across js, wasm, wasm-gc, and native.
</success_criteria>

<output>
Create `.planning/phases/53-grayalpha16-model-and-checked-storage/53-01-SUMMARY.md` when done.
</output>
