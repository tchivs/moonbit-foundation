---
phase: 19-portable-streaming-qoi-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/qoi/cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-QoiVectors.ps1
  - modules/mb-image/qoi/generated_vectors.mbt
  - modules/mb-image/qoi/stream_decode_test.mbt
  - modules/mb-image/qoi/stream_decode_wbtest.mbt
  - modules/mb-image/qoi/stream_encode_test.mbt
  - modules/mb-image/qoi/stream_encode_wbtest.mbt
  - examples/qoi-portable/main/main.mbt
  - modules/mb-image/README.mbt.md
  - scripts/quality/Test-PublicExamples.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
requirements:
  - QSTR-06
  - QSTR-07
must_haves:
  truths:
    - "Maintainers can run every generated QOI decode and encode vector through named hostile streaming schedules on js, wasm, wasm-gc, and native, with exact decoded pixels, canonical bytes, per-call progress, cumulative counters, finish failures, and sticky terminal behavior asserted."
    - "A user can run the existing qoi-portable executable unchanged as the sole public example; it feeds fixed QOI chunks to QoiStreamDecoder, flips the decoded image horizontally, drains QoiStreamEncoder through fixed caller-owned output leases, and prints one deterministic evidence line."
    - "The QOI quality lane remains restricted to the QOI package policy, QOI negative fixtures, exact package allowlist, and the QOI public example; it does not enter qualification, registry, release, publication, credential, PNG/DEFLATE, or FFI routes."
  artifacts:
    - path: fixtures/qoi/cases.json
      provides: "Named repository-derived streaming input and output schedules with zero/one-byte and QOI boundary coverage."
    - path: fixtures/manifest.json
      provides: "QOI vector provenance whose expected-use contract explicitly includes QSTR-06 streaming evidence."
    - path: modules/mb-image/qoi/generated_vectors.mbt
      provides: "Generated named schedules consumed by portable stream vector tests."
    - path: modules/mb-image/qoi/stream_decode_wbtest.mbt
      provides: "Generated-vector hostile decode schedule and progress evidence."
    - path: modules/mb-image/qoi/stream_encode_wbtest.mbt
      provides: "Generated-vector hostile output-capacity and canonical-byte evidence."
    - path: examples/qoi-portable/main/main.mbt
      provides: "Runnable public streaming decode-flip-encode workflow with exact deterministic evidence."
    - path: scripts/quality/Test-PublicExamples.ps1
      provides: "Exact qoi-portable status-line contract on all four supported targets."
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: "QOI lane entry point that executes and verifies its exact stage trace and workspace isolation probe."
    - path: modules/mb-image/README.mbt.md
      provides: "Public documentation of the streaming QOI decode-flip-stream-encode example."
  key_links:
    - from: fixtures/qoi/cases.json
      to: modules/mb-image/qoi/generated_vectors.mbt
      via: scripts/fixtures/Generate-QoiVectors.ps1
      pattern: "generated schedule identities and capacities are fail-closed by generator check"
    - from: modules/mb-image/qoi/generated_vectors.mbt
      to: modules/mb-image/qoi/stream_decode_wbtest.mbt and modules/mb-image/qoi/stream_encode_wbtest.mbt
      via: "named schedules drive both stream directions"
      pattern: "all generated valid vectors prove exact progress and semantic output"
    - from: examples/qoi-portable/main/main.mbt
      to: scripts/quality/Test-PublicExamples.ps1 and scripts/quality/Invoke-MoonQuality.ps1
      via: "exact one-line evidence plus the QOI lane's stage-trace and isolation-probe checks"
      pattern: "public stream workflow is exercised only in the QOI workspace lane"
---

<objective>
Deliver portable, adversarial evidence for the completed QOI streaming APIs and upgrade the existing public QOI consumer to prove the real decode → flip → encode route.

Purpose: Close QSTR-06 and QSTR-07 with reproducible, four-target evidence rather than a second example or an eager-codec demonstration.
Output: Generated hostile schedule fixtures, strengthened stream tests, and an in-place qoi-portable streaming workflow with an exact QOI-only quality contract.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-portable-streaming-qoi-evidence/19-CONTEXT.md
@.planning/phases/17-resumable-qoi-chunk-decode/17-01-SUMMARY.md
@.planning/phases/18-resumable-qoi-buffer-encode/18-01-SUMMARY.md
@fixtures/qoi/cases.json
@scripts/fixtures/Generate-QoiVectors.ps1
@modules/mb-image/qoi/stream_decode.mbt
@modules/mb-image/qoi/stream_encode.mbt
@modules/mb-image/qoi/stream_decode_test.mbt
@modules/mb-image/qoi/stream_decode_wbtest.mbt
@modules/mb-image/qoi/stream_encode_test.mbt
@modules/mb-image/qoi/stream_encode_wbtest.mbt
@examples/qoi-portable/main/main.mbt
@scripts/quality/Test-PublicExamples.ps1
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate boundary-complete hostile streaming schedules</name>
  <files>fixtures/qoi/cases.json, fixtures/manifest.json, scripts/fixtures/Generate-QoiVectors.ps1, modules/mb-image/qoi/generated_vectors.mbt</files>
  <action>Extend the repository-derived QOI fixture schema and its deterministic generator so generated vectors expose separate named input-chunk and output-capacity schedules. Per D-04, include a zero-length input push, a zero-capacity output turn, one-byte turns, header splits, partial RGB/RGBA/luma token splits, opcode boundaries, and the eight-byte end-marker boundary; preserve the existing valid QOI and canonical encode vectors. Generate the MoonBit helper functions from the fixture rather than duplicating schedule arrays in tests, then regenerate the checked artifact and fixture-manifest digest. Update the generator's QOI manifest record so its expected-use contract explicitly includes QSTR-06. Keep all schedule identities compact, visible, and deterministic; do not introduce a new codec, package, workspace member, host I/O adapter, source snapshot mechanism, or non-QOI fixture route.</action>
  <verify>
    <automated>pwsh -File scripts/fixtures/Generate-QoiVectors.ps1 -Check</automated>
  </verify>
  <done>Generated vectors provide named hostile decode and encode schedules covering zero/one-byte calls and every required header, token, opcode, and marker boundary; the checked artifact and manifest digest are current, and the manifest states QSTR-06 as an expected use.</done>
</task>

<task type="auto">
  <name>Task 2: Prove all generated vectors under adversarial stream progress schedules</name>
  <files>modules/mb-image/qoi/stream_decode_test.mbt, modules/mb-image/qoi/stream_decode_wbtest.mbt, modules/mb-image/qoi/stream_encode_test.mbt, modules/mb-image/qoi/stream_encode_wbtest.mbt</files>
  <action>Consume the generated schedule helpers in both stream directions. For every generated valid decode vector and every named hostile input schedule, assert each push call's accepted byte count and NeedInput result, the cumulative consumed count, finish-only completion, exact descriptor and pixel bytes, and zero-consumption sticky behavior after terminal completion or a terminal finish failure. For every generated encode vector and every named hostile output schedule, assert each pull's written count, total-written counter, NeedOutput or Finished transition, exact canonical eager-equivalent bytes, and zero-written sticky pulls after terminal state. Retain and extend the existing malformed, incomplete-marker, trailing-data, budget-preflight, and pending-token tests so their typed finish errors and terminal follow-ups are explicit. Per D-04 and D-05, run this portable test evidence on all four targets; do not weaken strict finish semantics or change eager Reader/Writer contracts.</action>
  <verify>
    <automated>moon -C modules/mb-image test qoi --target all --frozen</automated>
  </verify>
  <done>Every generated vector is exercised through generated hostile schedules with exact progress and semantic assertions, malformed completion paths remain finish-gated and sticky, and the full QOI package suite passes on js, wasm, wasm-gc, and native.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Upgrade qoi-portable to the public streaming workflow and freeze its QOI-only evidence</name>
  <files>examples/qoi-portable/main/main.mbt, modules/mb-image/README.mbt.md, scripts/quality/Test-PublicExamples.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - Test 1: The quality script accepts exactly one qoi-portable status line on js, wasm, wasm-gc, and native that identifies the fixed input/output schedules and their observable stream progress counters alongside the existing width, height, flip, rolling digest, and SHA-256 evidence.
    - Test 2: The executable feeds the fixed source through QoiStreamDecoder using the visible header-token-marker input schedule, explicitly finishes the decoder, applies the existing horizontal flip, and drains QoiStreamEncoder using the visible zero-header-token-marker output schedule to the unchanged canonical byte sequence.
    - Test 3: The QOI workspace isolation probe still records only source, dependency, public-import, and example-verification stages.
    - Test 4: Selecting `-Lane Qoi` runs the exact four-stage QOI trace and its workspace isolation proof, including the direct public-example isolation-probe invocation.
  </behavior>
  <action>First update the QOI-only public-example expected status contract, then replace the eager decode and writer path in the existing qoi-portable executable with the public QoiStreamDecoder and QoiStreamEncoder APIs. Per D-01, modify this single example in place and do not add a workspace member or duplicate executable. Per D-02, use fixed schedules: input `header-token-marker` capacities `[4, 10, 1, 3, 8]` cycling to complete the 27-byte source, and output `zero-header-token-marker` capacities `[0, 14, 1, 3, 6]`; assert per-call outcomes and counters, explicit decoder finish, final encoder completion, the existing horizontal-flip pixels, unchanged canonical 24-byte output, rolling digest, and SHA-256. Per D-03, preserve the exact public import allowlist, four portable targets, one deterministic status line, and pure public MoonBit boundary; the updated line must expose schedule identities plus input push/output pull counts and cumulative bytes. Update the mb-image README's portable QOI consumer description from eager codec terminology to the fixed streaming decode → flip → streaming encode flow and its caller-owned output leases. Per D-06, make the public `Qoi` lane dispatch execute `Assert-QoiLaneIsolation` (or an equivalent scoped wrapper) so the selected lane verifies the exact four-stage QOI trace and directly runs `Test-PublicExamples.ps1 -Example qoi -Mode workspace -Target all -IsolationProbe`; preserve the trap functions and exact trace assertion. Only this QOI workspace route may run, with no qualification, registry, release, publication, credential, PNG/DEFLATE, FFI, or source-snapshot behavior.</action>
  <verify>
    <automated>pwsh -File scripts/quality/Test-PublicExamples.ps1 -Example qoi -Mode workspace -Target all -IsolationProbe; pwsh -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Qoi</automated>
  </verify>
  <done>The existing qoi-portable example is the sole public streaming consumer, succeeds unchanged on all four targets with one exact status line proving fixed schedules and stream counters, its public documentation describes streaming decode → flip → streaming encode, and selecting the QOI lane itself passes the exact stage-trace plus isolation proof without entering any broader quality route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated fixture → streaming test | Fixture-defined hostile capacities become untrusted incremental input/output drives for QOI state machines. |
| public example → QOI stream API | Caller-owned byte views and mutable leases cross into stateful decode/encode APIs. |
| QOI quality lane → workspace command | A scoped quality entry point selects the public example and must not reach broader release or registry controllers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | `fixtures/qoi/cases.json` and generated schedules | medium | mitigate | Task 1 regenerates `generated_vectors.mbt` from fixture-defined schedules and runs the generator's stale-artifact/provenance check. |
| T-19-02 | Denial of service | `QoiStreamDecoder` incomplete or split QOI input | medium | mitigate | Task 2 asserts bounded progress, strict finish failures, and sticky terminal behavior across hostile chunk boundaries on every portable target. |
| T-19-03 | Tampering | `QoiStreamEncoder` caller-owned output leases | medium | mitigate | Task 2 asserts zero/one-byte capacity behavior, per-pull counters, terminal behavior, and exact canonical bytes for every generated encode vector. |
| T-19-04 | Elevation of privilege | QOI public-example quality route | high | mitigate | Task 3 preserves the QOI-only workspace invocation and executes the existing lane isolation probe, which traps qualification, registry, release, publication, and credential routes. |
| T-19-05 | Repudiation | cross-target public evidence | low | mitigate | Task 3 freezes one exact status line containing fixed schedule identities, stream counters, byte evidence, rolling digest, and SHA-256 on all four targets. |
</threat_model>

<verification>
Run the checked fixture generator, then the QOI package test suite on `--target all`. Run `Test-PublicExamples.ps1 -Example qoi -Mode workspace -Target all -IsolationProbe`, then select `Invoke-MoonQuality.ps1 -Lane Qoi`; the selected QOI lane must itself assert the exact four-stage trace and the same isolation probe. Confirm no policy inventory changes are needed because this phase adds no module source file, package, public type, public import, supported target, workspace member, release action, registry operation, FFI boundary, PNG/DEFLATE capability, or source-snapshot contract.
</verification>

<success_criteria>
1. QSTR-06: Generated valid decode and encode vectors have deterministic hostile schedules covering zero/one-byte and header/token/end-marker boundaries; all four targets prove exact pixels, canonical bytes, per-call and cumulative progress, strict finish failures, and sticky terminal results.
2. QSTR-07: `examples/qoi-portable` alone demonstrates public streaming decode → horizontal flip → streaming canonical encode with fixed schedules and one exact four-target evidence line.
3. The QOI quality invocation remains a scoped portable evidence lane and retains its isolation evidence without widening into unrelated system or release work.
</success_criteria>

<source_audit>
| Source | Required item | Coverage | Plan task |
|--------|---------------|----------|-----------|
| GOAL | Public four-target adversarial streaming evidence and one public decode → operation → encode workflow | COVERED | Tasks 1-3 |
| REQ | QSTR-06: hostile generated decode/encode vectors on all portable targets and fixture expected-use provenance | COVERED | Tasks 1-2 |
| REQ | QSTR-07: one portable public streaming example with an existing operation and deterministic output | COVERED | Task 3 |
| RESEARCH | No Phase 19 RESEARCH.md exists; local Phase 17/18 implementation context is authoritative by task assignment | N/A | N/A |
| CONTEXT | D-01: upgrade qoi-portable in place | COVERED | Task 3 |
| CONTEXT | D-02: fixed visible schedules, real horizontal flip, deterministic evidence | COVERED | Task 3 |
| CONTEXT | D-03: retain import allowlist, portable targets, no-FFI boundary, one status line | COVERED | Task 3 |
| CONTEXT | D-04: generated vectors use hostile input/output schedules at every boundary | COVERED | Tasks 1-2 |
| CONTEXT | D-05: exact pixels/bytes/progress/counters/failures/sticky behavior on four targets | COVERED | Task 2 |
| CONTEXT | D-06: retain QOI-only quality isolation | COVERED | Task 3 |
</source_audit>

<output>
Create `.planning/phases/19-portable-streaming-qoi-evidence/19-01-SUMMARY.md` when done.
</output>
