---
phase: 23-png-colour-declaration-and-srgb-semantics
plan: "01"
type: execute
wave: 1
depends_on: ["22-01"]
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - modules/mb-image/png/png_test.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
autonomous: true
requirements: [PNGCM-01, PNGCM-02]
must_haves:
  truths:
    - "The eager PNG decoder validates each recognised colour chunk as a singleton before PLTE and IDAT, and rejects malformed, duplicate, or late declarations before exposing a DecodeResult image."
    - "A valid sRGB chunk produces built-in encoded-sRGB ImageMetadata and preserves its rendering intent in bounded metadata on every supported target."
    - "gAMA, cHRM, and iCCP never cause raw samples to be labelled sRGB: valid but unsupported non-sRGB declarations return a typed capability result after structural validation."
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: "Recognised colour-chunk state, ordering/duplicate/payload validation, and strict transport facts."
    - path: modules/mb-image/png/png.mbt
      provides: "sRGB-aware descriptor metadata and explicit non-sRGB capability boundary."
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: "Independent colour-chunk fixture construction and generated portable assertions."
  key_links:
    - from: modules/mb-image/png/structural.mbt
      to: modules/mb-image/png/png.mbt
      via: "PngStreamTransport carries only validated colour declaration facts into descriptor construction."
      pattern: "PngStreamTransport|PngColour|_png_descriptor"
    - from: fixtures/png/decode-cases.json
      to: modules/mb-image/png/generated_decode_vectors_test.mbt
      via: "The independent PowerShell generator emits accepted sRGB and hostile colour-declaration cases."
      pattern: "Generate-PngDecodeVectors|srgb|gAMA|cHRM|iCCP"
---

<objective>
Implement strict recognised PNG colour-declaration parsing and honest sRGB metadata mapping without colour transforms.

Purpose: complete PNGCM-01 and PNGCM-02 while ensuring non-sRGB declarations cannot be silently discarded or misrepresented as sRGB.
Output: private semantic-chunk state, built-in sRGB metadata with retained rendering intent, and generated hostile/portable evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/PNG-COLOUR-SPEC.md
@.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-CONTEXT.md
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/png.mbt
@modules/mb-image/model/descriptor.mbt
@modules/mb-image/metadata/metadata.mbt
@modules/mb-color/profile/profile.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse recognised colour declarations and map only validated sRGB to image metadata</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png.mbt, modules/mb-image/png/structural_wbtest.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - `sRGB`, `gAMA`, `cHRM`, and `iCCP` are each accepted at most once and only before `PLTE` and `IDAT`; malformed lengths, duplicate chunks, invalid sRGB intents, zero gamma, malformed iCCP envelope, or an unsupported pre-IDAT colour declaration fail before `OwnedImage` construction. A post-IDAT declaration is rejected by terminal transport validation before any `DecodeResult` becomes visible.
    - A valid `sRGB` payload has exactly one intent byte in 0..3. The decoder produces `ColorSpaceIdentity::Srgb`, `TransferIdentity::EncodedSrgb`, `ProfileIdentity::builtin_srgb()`, and one bounded opaque metadata entry recording that intent.
    - If `sRGB` is accompanied by legacy `gAMA` or `cHRM`, accept only the exact standard sRGB compatibility values; a conflicting legacy declaration fails deterministically rather than changing the sRGB interpretation.
    - iCCP names are 1–79 valid Latin-1 printable bytes with no leading, trailing, or consecutive spaces, followed by one NUL, method zero, and at least one compressed-profile byte; empty/overlong/control/space-rule/no-NUL/no-data/bad-method envelopes fail as malformed without retaining compressed profile bytes.
    - A valid non-sRGB `gAMA`/`cHRM` declaration, or a valid iCCP envelope, reaches an explicit `png-colour-transform-unavailable` capability result. It must not fall through to the old empty-sRGB metadata path.
  </behavior>
  <action>Start with red public and white-box tests. Extend the private PNG semantic state instead of classifying every colour chunk as one generic unsupported semantic chunk: track seen/order facts and minimal validated payload facts in all structural and live streaming transport paths. Reuse existing CRC-authenticated forward reading and exact reader accounting; do not buffer IDAT. Declarations occurring before PLTE/IDAT must settle policy before image allocation; a declaration after IDAT is necessarily discovered by the existing forward-only terminal transport and must fail before a `DecodeResult` is returned, without claiming allocation rollback. Define one private helper for “before PLTE/IDAT” and use it for all four chunks. Validate `sRGB` length/intent, `gAMA` length/nonzero integer, `cHRM` length, and the complete iCCP envelope: 1–79 printable Latin-1 name bytes, no leading/trailing/consecutive spaces, exactly one NUL, method zero, and at least one following compressed-profile byte. Parse iCCP with fixed scratch plus CRC/discard accounting, retaining only bounded name/method state; never append compressed profile bytes to an Array or any budget-untracked buffer in this phase. Defer ICC zlib expansion and profile inspection to Phase 24. Model a valid sRGB declaration as a private transport fact and create descriptor metadata from it with `OpaqueMetadata::from_entries`. Before `OwnedImage::new_operation`, extend every PNG preflight budget route (ordinary, packed, 16-bit, indexed, and Adam7) by the exact one intent byte and one additional allocation required by owned metadata; prove exact and one-byte/one-allocation-below caller budgets atomically reject. Use a stable token namespace/key/tag such as `png`, `srgb`, `intent` and the exact one-byte value. Preserve the old default built-in sRGB mapping only when no recognised colour declaration exists. When a legal declaration has higher or unknown semantics that Phase 23 cannot transform, return `@codec.capability_unavailable("png-decode", "png-colour-transform-unavailable")`; do not expose an image or change caller-owned source semantics. Ensure a valid `sRGB` with matching legacy compatibility chunks still chooses sRGB, while inconsistent values and an iCCP+sRGB combination get deterministic typed outcomes. Keep Adam7, palette, tRNS, DEFLATE, encoder bytes, public APIs, and modified QOI files unchanged.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>All recognised colour declarations have a strict authenticated state path; only validated sRGB produces executable sRGB metadata, and every non-sRGB route fails explicitly before image visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate independent colour declaration positives and hostile vectors</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - Generated accepted sRGB RGB/RGBA, palette, grayscale, and Adam7 samples retain one intent byte and built-in encoded-sRGB metadata across every IDAT segmentation used by the corpus.
    - Generated hostile cases cover wrong payload sizes, out-of-range intent, duplicate and post-PLTE/post-IDAT occurrences, zero gamma, malformed cHRM/iCCP envelopes (empty/80-byte/control/space-rule/no-NUL/no-profile-data/bad-method), conflicting sRGB legacy values, and iCCP precedence without a transform.
    - Tight sRGB vectors prove the extra one metadata byte/allocation is reserved in every source-profile preflight route; a large in-limit iCCP payload proves Phase 23 does not buffer compressed profile bytes before its capability result.
    - Generator validation independently checks chunk assembly/CRC/order and manifest integrity; all target tests compare category, code, context, descriptor metadata, intent bytes, and no-image-on-error behaviour.
  </behavior>
  <action>Extend the PNG fixture schema and PowerShell builder with an ordered `colour_chunks` description that writes complete CRC-checked ancillary chunks before `PLTE`/`IDAT`; retain all existing default vectors byte-for-byte. Add compact accepted cases for each sRGB rendering intent and representative existing profile/layout combinations, including a split-IDAT case so the metadata parser is proven independent of compressed raster boundaries. Add hostile records for each structural failure described in behavior, specifically every iCCP name grammar failure, missing compressed byte, method failure, and a large but in-input-limit compressed iCCP payload that reaches the explicit capability boundary without retention. Add exact/one-byte-below/one-allocation-below budget records for sRGB in each ordinary, packed/indexed, 16-bit, and Adam7 preflight path. Add a post-IDAT declaration case that asserts no `DecodeResult` even though terminal validation may occur after internal image construction. Extend the independent generator oracle to validate chunk order, lengths, and expected errors without calling MoonBit code. Emit public generated tests that inspect `DecodeResult.descriptor().metadata()` and the bounded opaque intent entry for accepted sRGB cases, and assert failure occurs before a result for hostile/non-sRGB cases. Regenerate derived MoonBit vectors and update only the PNG vector manifest digest; preserve provenance records, the existing non-colour corpus, and all unrelated fixture manifest entries.</action>
  <verify><automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated></verify>
  <done>Independent, manifest-backed accepted and hostile colour vectors prove intent retention, strict order/grammar handling, non-sRGB capability boundaries, arbitrary IDAT segmentation, and four-target equivalence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PNG ancillary payload → colour state | Untrusted chunk order, multiplicity, syntax, and declared colour meaning select metadata or failure. |
| colour state → image descriptor | A semantic declaration must not make raw samples appear to have an unsupported transform applied. |
| JSON fixture/generator → generated target tests | Independent declared chunk construction and expectations guard the portable contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|---|---|---|---|---|
| T-23-01 | Tampering | colour chunk parser | high | Enforce singleton/order/length/field checks before descriptor or image allocation. |
| T-23-02 | Information integrity | sRGB mapping | high | Map only valid sRGB to built-in sRGB; validate conflicting legacy values and preserve rendering intent. |
| T-23-03 | Information integrity | non-sRGB declarations | high | Return an explicit capability result rather than discarding metadata or inventing a transform. |
| T-23-04 | Denial of service | iCCP envelope | medium | Keep profile parsing bounded to structural envelope in this phase; no unbounded decompression. |
| T-23-05 | Tampering | generated evidence | medium | Assemble and validate ancillary chunks independently, then run generated vectors on all targets. |
</threat_model>

<verification>
1. `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
PNGCM-01 and PNGCM-02 are met: recognised colour declarations are strictly validated before image exposure, valid sRGB produces executable built-in metadata plus intent, and no unsupported declaration is silently relabelled as sRGB.
</success_criteria>

<output>
Create `.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-01-SUMMARY.md` when done.
</output>
