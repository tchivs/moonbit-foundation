---
phase: 23-png-colour-declaration-and-srgb-semantics
plan: "02"
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
autonomous: true
gap_closure: true
requirements: [PNGCM-01]
must_haves:
  truths:
    - "The eager PNG decoder rejects any declared sRGB, gAMA, or cHRM length other than its fixed legal size before it reads or retains a payload byte, so hostile declarations cannot grow an attacker-controlled buffer."
    - "Hostile PNG vectors with a 2 GiB fixed-colour chunk length and no declared payload reach their typed malformed-length result on every supported target rather than an end-of-stream or allocation path."
    - "Valid fixed-size colour chunks still traverse CRC-authenticated parsing and preserve the existing singleton, ordering, sRGB metadata, and non-sRGB capability behavior."
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: "Pre-payload fixed-length gates and bounded CRC parsing for sRGB, gAMA, and cHRM colour chunks."
      contains: "_png_read_colour_chunk"
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: "Independent header-only hostile-colour vector construction and oracle checks."
      contains: "Generate-PngDecodeVectors"
    - path: modules/mb-image/png/generated_decode_vectors_test.mbt
      provides: "Regenerated executable all-target PNG decode records for hostile declared-length cases."
      contains: "PngDecodeVector"
  key_links:
    - from: modules/mb-image/png/structural.mbt
      to: modules/mb-image/png/png_test.mbt
      via: "PngDecoder drives generated header-only fixed-colour records through _png_read_colour_chunk and returns the expected typed error."
      pattern: "_png_read_colour_chunk|png_test_decode_vector"
    - from: fixtures/png/decode-cases.json
      to: modules/mb-image/png/generated_decode_vectors_test.mbt
      via: "The PowerShell generator encodes each declared-length record into the executable PngDecodeVector corpus."
      pattern: "declared_length|PngDecodeVector"
---

<objective>
Close the sole Phase 23 verifier gap: fixed-length sRGB, gAMA, and cHRM chunks must be rejected from their header length before the forward reader can grow a payload collection.

Purpose: complete the bounded structural-validation guarantee of PNGCM-01 without changing the established valid-colour or non-sRGB semantics.
Output: a bounded fixed-colour parser path and manifest-backed hostile vectors that prove header-only oversized declarations fail deterministically on all targets.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-CONTEXT.md
@.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-01-SUMMARY.md
@.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-VERIFICATION.md
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/structural_wbtest.mbt
@modules/mb-image/png/png_test.mbt
@fixtures/png/decode-cases.json
@scripts/fixtures/Generate-PngDecodeVectors.ps1
</context>

<source_audit>
SOURCE   | ID       | Feature / constraint | Coverage | Status
GOAL     | --       | Strict validated PNG colour declarations before image visibility. | Tasks 1-2 | COVERED
REQ      | PNGCM-01 | Typed deterministic rejection for malformed recognised colour chunks before an image is exposed. | Tasks 1-2 | COVERED
RESEARCH | --       | Recognised sRGB, gAMA, and cHRM have fixed 1, 4, and 32 byte payload grammars; structural parsing must be bounded. | Task 1 | COVERED
CONTEXT  | D-01     | Keep this a strict eager decoder extension with no public streaming API, FFI, release work, or ICC transform engine. | Tasks 1-2 | COVERED
CONTEXT  | D-02     | Recognise the four colour chunks before PLTE/IDAT and reject malformed declarations before output. | Tasks 1-2 | COVERED
CONTEXT  | D-03     | Preserve valid sRGB's built-in encoded-sRGB metadata and one-byte rendering intent. | Task 1 | COVERED
CONTEXT  | D-04     | Keep valid non-sRGB declarations on the existing typed capability boundary. | Task 1 | COVERED
CONTEXT  | D-05     | Retain the existing actual-encoded-sRGB requirement of reference image operations. | Task 1 | COVERED
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate fixed colour lengths before forward payload reads</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/structural_wbtest.mbt</files>
  <behavior>
    - The recognised-chunk reader returns `png-srgb-length`, `png-gama-length`, or `png-chrm-length` immediately when the chunk header declares a size other than 1, 4, or 32 bytes respectively; this occurs before `_png_read_one`, CRC-payload accumulation, or payload storage for that chunk.
    - Valid fixed-size sRGB, gAMA, and cHRM declarations still consume exactly their legal bytes through the existing CRC path and retain the prior intent, legacy-compatibility, duplicate, ordering, and capability outcomes.
    - iCCP remains on its existing streaming envelope path, which records bounded grammar facts and discards profile bytes; valid non-sRGB declarations remain typed `png-colour-transform-unavailable` results and do not produce an image.
    - White-box coverage proves the expected-length selector and each pre-payload malformed-size path, including the maximum legal global PNG chunk size, without changing public decoder or reference-operation APIs.
  </behavior>
  <action>Write failing white-box cases first. In `_png_read_colour_chunk`, classify the recognised kind and apply the singleton and fixed-length gates for sRGB, gAMA, and cHRM at function entry, before constructing an `Array[Byte]`, entering the payload loop, or calling `_png_read_one` for payload data. Per D-02, map only the three fixed chunk kinds to exact header sizes 1UL, 4UL, and 32UL and return their established typed malformed-length contexts when the declaration size differs; retain the existing global chunk ceiling in the outer transport reader. Refactor the valid fixed-size route so CRC is still seeded with the chunk type and advanced for each legal byte, while retained data is bounded by the fixed grammar rather than the declared length. Keep iCCP's streaming grammar state and byte discard route unchanged. Run duplicate, ordering, intent, gamma, chromaticity, CRC, and colour-declaration policy checks on the same accepted fixed-size values as the current implementation so D-03, D-04, and D-05 behavior is preserved. Do not add a public stream surface, FFI, ICC transform, encoder change, QOI edit, or new dependency (D-01). Add focused `structural_wbtest.mbt` assertions for the fixed-size mapping and maximum declared-size rejection; the generated end-to-end header-only cases in Task 2 prove no payload read is attempted.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>No recognised fixed-colour chunk can make `_png_read_colour_chunk` allocate or append according to an attacker-controlled declared size; legal fixed-size declarations retain their existing authenticated semantic results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate header-only oversized fixed-colour regressions</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt</files>
  <behavior>
    - The fixture corpus contains one hostile header-only declaration for each of sRGB, gAMA, and cHRM with declared length `2147483647`, the maximum accepted by the outer PNG chunk-length gate, and expects its corresponding fixed-length error.
    - Each hostile record omits the claimed payload and CRC by construction; receiving its declared malformed-length context from `PngDecoder` proves the live parser rejected the header before attempting a payload read, rather than reporting end-of-stream or retaining bytes.
    - The generator independently validates the intentional header-only form, prevents it from being treated as an ordinary complete chunk, emits its actual compact bytes into `PngDecodeVector`, and preserves validation of every ordinary chunk's CRC, grammar, ordering, generated-source freshness, and manifest digest.
    - The existing generated PNG test loop executes all three records through `PngDecoder` on js, wasm, wasm-gc, and native, asserting their category, code, context, and no-success result path.
  </behavior>
  <action>Add three compact `colour_chunks` records to `fixtures/png/decode-cases.json` for sRGB, gAMA, and cHRM with an explicit `declared_length` of 2147483647 and a dedicated header-only marker; use the current smallest RGB PNG scaffold and the exact malformed-length contexts. Extend `Generate-PngDecodeVectors.ps1` so its colour-chunk builder can write a declared header length independently of the byte body only for that marker, and make the independent assembly oracle validate this deliberate header-only malformed form without trying to materialize or CRC a 2 GiB payload. Keep ordinary colour descriptions bound to matching header/payload lengths and CRCs, reject unsupported use of the new fields, and append all three IDs to the corpus completeness list. Generate compact records whose bytes end immediately after the colour type (or otherwise contain no declared colour payload); subsequent decoding must therefore reach the declared-length error before any payload read. Regenerate `generated_decode_vectors_test.mbt` through the script, update only the PNG decode vector SHA-256 entry in `fixtures/manifest.json`, and preserve existing provenance metadata and unrelated manifest records. The existing `png_test_decode_vector`/generated test loop already asserts an error rather than a `DecodeResult`; keep that wiring intact and do not hand-edit the generated MoonBit file.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated>
  </verify>
  <done>Manifest-backed hostile vectors independently prove that 2 GiB declared sRGB, gAMA, and cHRM payloads are rejected from their headers on every supported target without a payload-read, allocation, or image-success path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PNG colour-chunk header → forward parser | An attacker controls chunk kind and declared length before any ancillary payload bytes are read. |
| Fixture schema/generator → generated MoonBit test corpus | A declarative hostile-header representation becomes executable decoder input on all portable targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-23-06 | Denial of service | `_png_read_colour_chunk` fixed-colour path | high | mitigate | Validate the 1/4/32-byte header lengths before payload reads or collections, then retain at most the fixed legal byte count while computing CRC. |
| T-23-07 | Tampering | recognised colour semantics | medium | mitigate | Preserve existing CRC-authenticated parsing and singleton/order/semantic checks for all valid fixed-size declarations. |
| T-23-08 | Tampering | hostile-vector generator | medium | mitigate | Make declared-length/header-only records explicit, independently validated, generated into tests, and bound to the existing manifest digest. |
| T-23-09 | Supply chain | PNG package dependencies | low | accept | The fix uses only existing MoonBit, PowerShell, and fixture-generator facilities; no package installation occurs. |
</threat_model>

<verification>
1. `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`

Confirm the three generated header-only records use `2147483647` and receive `png-srgb-length`, `png-gama-length`, and `png-chrm-length`, respectively. Their intentionally absent payload makes any regression to payload-first parsing surface as an end-of-stream path instead of the expected fixed-length error.
</verification>

<success_criteria>
PNGCM-01 is fully closed: recognised fixed-size colour chunks cannot produce attacker-controlled buffering, malformed declared lengths reject before payload consumption, valid colour semantics remain CRC-authenticated and unchanged, and generated all-target evidence exercises the live `PngDecoder` path.
</success_criteria>

<output>
Create `.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-02-SUMMARY.md` when done.
</output>
