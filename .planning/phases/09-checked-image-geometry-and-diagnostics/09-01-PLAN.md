---
phase: 09-checked-image-geometry-and-diagnostics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/ops/geometry.mbt
  - modules/mb-image/ops/geometry_test.mbt
  - modules/mb-image/ops/geometry_wbtest.mbt
  - modules/mb-image/ops/resize.mbt
  - modules/mb-image/ops/resize_convert_wbtest.mbt
  - modules/mb-image/README.mbt.md
  - examples/ppm-portable/main/main.mbt
autonomous: true
requirements:
  - GEOM-01
  - GEOM-02
  - GEOM-03
  - RASTER-03
must_haves:
  truths:
    - "A caller can crop a supported image into a fresh tightly packed owned result and receives stable typed failures for invalid rectangles or resource limits."
    - "A caller can request explicit clockwise 90-, 180-, and 270-degree rotations and observe the prescribed pixels, dimensions, metadata normalization, and deterministic capability failures."
    - "Nearest-neighbor resize remains the documented integer-floor reference mapping with identical public geometry evidence on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/ops/geometry.mbt
      provides: "Public crop and explicit rotate_90, rotate_180, and rotate_270 operations"
      exports: ["crop", "rotate_90", "rotate_180", "rotate_270"]
    - path: modules/mb-image/ops/geometry_test.mbt
      provides: "Black-box owned-output, mapping, metadata, and typed-diagnostic tests"
    - path: modules/mb-image/ops/geometry_wbtest.mbt
      provides: "Adversarial capability, checked-rectangle, and atomic resource-limit proof"
    - path: modules/mb-image/ops/resize_convert_wbtest.mbt
      provides: "Two-dimensional nearest-neighbor floor-mapping regression"
    - path: modules/mb-image/README.mbt.md
      provides: "Executable public geometry and resize contract"
    - path: examples/ppm-portable/main/main.mbt
      provides: "Four-target public crop and rotation consumer"
  key_links:
    - from: modules/mb-image/ops/geometry.mbt
      to: modules/mb-image/storage/owned_image.mbt
      via: "Validated tight descriptors flow into one OwnedImage::new_operation charge before with_mut_view writes output bytes."
      pattern: "OwnedImage::new_operation"
    - from: modules/mb-image/ops/geometry.mbt
      to: modules/mb-image/ops/copy_flip.mbt
      via: "Geometry uses the shared capability gate, deterministic CoreError constructor, result type, and metadata-disposition policy."
      pattern: "supports_copy_flip|ImageOperationResult|operation_error"
    - from: modules/mb-image/README.mbt.md
      to: modules/mb-image/ops/geometry.mbt
      via: "The checked example calls @ops.crop and @ops.rotate_90 with caller-owned budgets."
      pattern: "@ops\\.(crop|rotate_90)"
    - from: examples/ppm-portable/main/main.mbt
      to: modules/mb-image/ops/geometry.mbt
      via: "The portable public executable composes crop then rotate_90 before the later raster and codec stages."
      pattern: "@ops\\.(crop|rotate_90)"
---

<objective>
Audit the already-present Phase 9 geometry implementation against its locked historical contract, then make only evidence-backed repairs if a public API, diagnostic, resource-bound, or portable-target gate fails.

Purpose: preserve a proven portable geometry baseline while ensuring the archived Phase 9 requirements are still executable in the current workspace rather than reimplementing settled behavior.
Output: a current source-and-test conformance audit, narrowly scoped regression repairs only when a gate finds a mismatch, and current four-target public evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v0.4-phases/09-checked-image-geometry-and-diagnostics/09-CONTEXT.md
@.planning/milestones/v0.4-phases/09-checked-image-geometry-and-diagnostics/09-RESEARCH.md
@modules/mb-image/ops/copy_flip.mbt
@modules/mb-image/ops/geometry.mbt
@modules/mb-image/ops/geometry_test.mbt
@modules/mb-image/ops/geometry_wbtest.mbt
@modules/mb-image/ops/orientation.mbt
@modules/mb-image/ops/orientation_test.mbt
@modules/mb-image/ops/orientation_wbtest.mbt
@modules/mb-image/ops/resize.mbt
@modules/mb-image/ops/resize_convert_test.mbt
@modules/mb-image/ops/resize_convert_wbtest.mbt
@modules/mb-image/model/descriptor.mbt
@modules/mb-image/storage/owned_image.mbt
@modules/mb-image/README.mbt.md
@examples/ppm-portable/main/main.mbt

<interfaces>
From modules/mb-image/ops/geometry.mbt:
```moonbit
pub fn crop(source : @storage.ImageView, region : @model.Rect, budget : @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
pub fn rotate_90(source : @storage.ImageView, budget : @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
pub fn rotate_180(source : @storage.ImageView, budget : @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
pub fn rotate_270(source : @storage.ImageView, budget : @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
```

From modules/mb-image/ops/resize.mbt:
```moonbit
pub fn resize_nearest(source : @storage.ImageView, width : UInt64, height : UInt64, budget : @budget.Budget) -> Result[ImageOperationResult, @error.CoreError]
```

From modules/mb-image/ops/copy_flip.mbt and modules/mb-image/storage/owned_image.mbt:
```moonbit
pub struct ImageOperationResult
pub fn ImageOperationResult::image(self : ImageOperationResult) -> @storage.OwnedImage
pub fn ImageOperationResult::disposition(self : ImageOperationResult) -> @metadata.MetadataDisposition
pub fn OwnedImage::new_operation(descriptor : @model.ImageDescriptor, budget : @budget.Budget, allocator : &@bytes.Allocator, work : UInt64) -> Result[OwnedImage, @error.CoreError]
```

From modules/mb-image/model/descriptor.mbt:
```moonbit
pub fn Rect::new(x : UInt64, y : UInt64, width : UInt64, height : UInt64) -> Result[Rect, @error.CoreError]
pub fn Rect::right(self : Rect) -> UInt64
pub fn Rect::bottom(self : Rect) -> UInt64
pub fn Rect::is_empty(self : Rect) -> Bool
```
</interfaces>
</context>

<source_audit>
SOURCE   | ID        | Required behavior | Plan task | Status
---------|-----------|-------------------|-----------|-------
GOAL     | Phase 9   | Composable portable crop, reorientation, and resize with deterministic invalid-work diagnostics | 1, 2 | COVERED
REQ      | GEOM-01   | Checked fresh rectangular crop without out-of-bounds or overflow allocation | 1 | COVERED
REQ      | GEOM-02   | Existing flips plus explicit right-angle rotations with correct pixels and dimensions | 1 | COVERED
REQ      | GEOM-03   | Documented deterministic nearest-neighbor output across targets | 1, 2 | COVERED
REQ      | RASTER-03 | Typed deterministic unsupported-format, invalid-dimension/region, and resource-limit failures | 1 | COVERED
RESEARCH | —         | Reuse the packed sRGB U8 RGB/RGBA capability gate, checked descriptors, and one authoritative owned-output charge | 1 | COVERED
RESEARCH | —         | Preserve the existing integer floor resize baseline; prove public behavior across all four targets | 1, 2 | COVERED
CONTEXT  | D-01      | Crop is a fresh tightly packed OwnedImage | 1 | COVERED
CONTEXT  | D-02      | Explicit requested 90/180/270 rotations remain distinct from metadata-driven apply_orientation | 1 | COVERED
CONTEXT  | D-03      | Crop preserves metadata; physical rotation normalizes orientation to TopLeft | 1 | COVERED
CONTEXT  | D-04      | Unsupported formats fail as CapabilityUnavailable without conversion | 1 | COVERED
CONTEXT  | D-05      | Checked crop endpoints and atomic typed invalid/budget failures | 1 | COVERED
CONTEXT  | D-06      | Nearest neighbor is the only deterministic reference resampling algorithm | 1, 2 | COVERED
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit and, only on a failing witness, repair the checked geometry contract</name>
  <files>modules/mb-image/ops/geometry.mbt, modules/mb-image/ops/geometry_test.mbt, modules/mb-image/ops/geometry_wbtest.mbt, modules/mb-image/ops/resize.mbt, modules/mb-image/ops/resize_convert_wbtest.mbt</files>
  <behavior>
    - A valid Rect returns a fresh, tightly packed owned crop with the selected stored-coordinate bytes and preserved format/metadata, while empty, outside, overflowed, unsupported, and under-budget requests return typed CoreError values before observable budget consumption.
    - rotate_90, rotate_180, and rotate_270 map every RGB and straight/premultiplied RGBA source channel clockwise as (h - 1 - y, x), (w - 1 - x, h - 1 - y), and (y, w - 1 - x); 90/270 swap dimensions and all physical rotations emit TopLeft orientation with only that metadata field transformed.
    - resize_nearest maps both axes by min(source_extent - 1, floor(destination * source_extent / destination_extent)) and its zero-axis, unsupported-format, overflow, and resource failures retain deterministic typed and atomic behavior.
  </behavior>
  <action>Read the archived Phase 9 context/research and audit the current geometry, orientation, resize, and public/white-box test seams against D-01 through D-06. First execute the named behavior tests before changing production code. If all witnesses pass, retain the current production implementation and record the conformance evidence in the plan summary; do not rewrite equivalent crop, rotation, orientation, flip, or nearest-neighbor code. If a witness exposes a mismatch, add the smallest failing public or white-box regression first, then repair only the named geometry or resize seam. Per D-01 and D-05, require valid Rect endpoints/source bounds and a fully checked tight descriptor before exactly one OwnedImage::new_operation allocation; assert every ResourceLimits remaining field is unchanged for all rejection paths. Per D-02 and D-03, preserve direct requested rotation mappings and TopLeft metadata normalization without routing requested rotations through apply_orientation. Per D-04, retain the shared packed U8 encoded-sRGB RGB/RGBA capability gate and stable CoreError category, code, operation, and context rather than conversion fallback. Per D-06, retain the integer floor mapping and add a two-dimensional regression only if the existing coverage does not exercise every output channel. Do not modify QOI code, module manifests, release/CI/registry files, or unrelated processing operations.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$targets=@('js','wasm','wasm-gc','native'); foreach($target in $targets){ &amp; moon test modules/mb-image/ops --target $target; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } }"</automated>
  </verify>
  <done>GEOM-01, GEOM-02, GEOM-03, and RASTER-03 have a current source-level audit with passing black-box and adversarial witnesses on all four supported targets; any code change is paired with the specific regression that exposed it.</done>
</task>

<task type="auto">
  <name>Task 2: Audit executable public geometry documentation and portable consumer evidence</name>
  <files>modules/mb-image/README.mbt.md, examples/ppm-portable/main/main.mbt</files>
  <action>Audit the public README and the portable PPM consumer against the confirmed Phase 9 surface. The README must distinguish borrowed ImageView::crop from D-01 owned @ops.crop, describe D-02 explicit clockwise rotations separately from metadata-driven apply_orientation, state D-03 TopLeft normalization and metadata disposition, define D-04/D-05 typed capability and resource diagnostics, and document D-06's checked integer-floor nearest-neighbor formula. The portable example must invoke the public crop then rotate_90 route with caller-owned bounded budgets and emit its current deterministic success identity. Compile the README and run the consumer on js, wasm, wasm-gc, and native. Preserve content that matches these checks; if a public statement, example call, budget, output assertion, or target behavior differs from the audited API, correct only the documentation or example and rerun all targets. Do not add release, registry, CI, benchmark, interpolation, color-conversion, or alpha-processing scope.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$targets=@('js','wasm','wasm-gc','native'); $expected='example=portable bytes_read=38+29 bytes_written=29 width=3 height=2 crop rotate_90 rgb8_to_straight_rgba8 grayscale box_blur composite_source_over straight_rgba8_to_rgb8 digest=714923673 sha256=005700d6602b144bafcf3d869deee85619c8279c749bf33ca6fea8b43dbe78bf'; foreach($target in $targets){ &amp; moon -C modules/mb-image check README.mbt.md --target $target --frozen; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; $output=((&amp; moon -C examples/ppm-portable run main --target $target --frozen) | Out-String).Trim(); if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; if($output -cne $expected){ throw ('unexpected public geometry output on '+$target) } }"</automated>
  </verify>
  <done>The documented API and a real portable public consumer prove the checked crop and explicit rotation route with the same exact result on js, wasm, wasm-gc, and native.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller ImageView, Rect, dimensions, and Budget to portable operations | Untrusted geometry, pixel representation, and limits determine allocation and byte traversal. |
| validated descriptor to OwnedImage allocation | Checked output shape and declared work enter the one authoritative resource charge. |
| public docs/example to four target runtimes | Public statements and callable APIs cross into independently compiled target artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | crop endpoint and stored-coordinate mapping | high | mitigate | Task 1 checks Rect overflow/bounds before allocation and asserts selected output bytes through public and white-box tests. |
| T-09-02 | Denial of service | descriptor arithmetic and owned output allocation | high | mitigate | Task 1 verifies checked scalar calculations, the single OwnedImage::new_operation boundary, and unchanged complete budget snapshots on each rejected limit. |
| T-09-03 | Tampering | format/capability and orientation metadata | medium | mitigate | Task 1 preserves the shared capability gate and tests stable CoreError diagnostics plus TopLeft physical-rotation metadata/disposition. |
| T-09-04 | Repudiation | public geometry documentation and example | low | mitigate | Task 2 compiles the README and compares the public executable's complete deterministic output on all four targets. |
| T-09-SC | Tampering | package installation | low | accept | No package-manager install is in scope; the plan uses existing workspace MoonBit packages only. |
</threat_model>

<verification>
Run the Task 1 ops suite on js, wasm, wasm-gc, and native. Then compile the mb-image README and run the PPM portable consumer on the same four targets, requiring its complete deterministic result line. Treat any failure as an evidence-backed repair trigger within the task-owned files; do not broaden scope.
</verification>

<success_criteria>
- The current workspace proves every Phase 9 requirement with a concrete public or adversarial witness rather than relying on the archived completion marker.
- Crop, explicit rotations, and nearest resize preserve their locked API, metadata, diagnostic, checked-allocation, and portable-target semantics.
- No release, CI, registry, QOI, benchmark, interpolation, conversion, or unrelated processing work is introduced.
</success_criteria>

<output>
Create `.planning/phases/09-checked-image-geometry-and-diagnostics/09-01-SUMMARY.md` when done.
</output>
