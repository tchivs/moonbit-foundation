---
phase: 09-checked-image-geometry-and-diagnostics
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - modules/mb-image/ops/geometry_wbtest.mbt
  - modules/mb-image/ops/resize_convert_wbtest.mbt
  - modules/mb-image/README.mbt.md
autonomous: true
requirements:
  - GEOM-03
must_haves:
  truths:
    - "All supported targets give the same nearest-neighbor output for the documented integer-floor reference mapping."
    - "Adversarial geometry requests expose typed deterministic errors and leave every observable budget counter unchanged when allocation is rejected."
    - "Public documentation distinguishes fresh operation crop from borrowed view crop and describes explicit rotations and the single nearest-neighbor algorithm."
  artifacts:
    - path: modules/mb-image/ops/geometry_wbtest.mbt
      provides: "White-box crop and rotation boundary, coordinate-oracle, capability, and atomic-budget evidence"
    - path: modules/mb-image/ops/resize_convert_wbtest.mbt
      provides: "Nearest-neighbor deterministic regression coverage"
    - path: modules/mb-image/README.mbt.md
      provides: "Four-target checked examples and documented geometry/resize semantics"
  key_links:
    - from: modules/mb-image/README.mbt.md
      to: modules/mb-image/ops/geometry.mbt
      via: "mbt check example invokes @ops.crop and explicit rotate functions against a caller-provided Budget"
      pattern: "@ops\\.(crop|rotate_90|rotate_180|rotate_270)"
    - from: modules/mb-image/ops/resize_convert_wbtest.mbt
      to: modules/mb-image/ops/resize.mbt
      via: "Generated nearest-axis vectors and full resize output assert the unchanged integer-floor mapping"
      pattern: "nearest_source_index|resize_nearest"
---

<objective>
Prove adversarial geometry diagnostics and make the existing nearest-neighbor reference contract explicit without replacing established operations.

Purpose: make Phase 9's portable behavior, resource safety, and one allowed resampling algorithm executable and discoverable before raster processing builds on it.
Output: white-box geometry evidence, nearest-neighbor regression coverage, and checked public documentation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-checked-image-geometry-and-diagnostics/09-CONTEXT.md
@.planning/phases/09-checked-image-geometry-and-diagnostics/09-RESEARCH.md
@.planning/phases/09-checked-image-geometry-and-diagnostics/09-01-SUMMARY.md
@modules/mb-image/ops/geometry.mbt
@modules/mb-image/ops/copy_flip_wbtest.mbt
@modules/mb-image/ops/orientation_wbtest.mbt
@modules/mb-image/ops/resize.mbt
@modules/mb-image/ops/resize_convert_wbtest.mbt
@modules/mb-image/README.mbt.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adversarial crop and rotation diagnostic evidence</name>
  <files>modules/mb-image/ops/geometry_wbtest.mbt</files>
  <behavior>
    - Empty, out-of-bounds, endpoint-overflow, unsupported-layout, and insufficient resource requests produce the expected typed CoreError category/code without a panic.
    - Every pre-allocation rejection and every OwnedImage::new_operation resource rejection preserves bytes, allocations, pixels, dimensions, and work remaining in the supplied Budget.
    - A non-square source exercises every source coordinate for all three requested rotations and validates its destination coordinate and normalized metadata disposition.
  </behavior>
  <action>Create geometry_wbtest.mbt using the established wb_operation_limits, unsupported_image, and orientation-source patterns. Per D-04 and D-05, cover U16, planar, Gray, non-sRGB, and incompatible alpha input at the shared capability boundary; assert CapabilityUnavailable plus the public operation context and an unchanged complete Budget::remaining snapshot. Exercise Rect::new overflow separately as its checked constructor contract, then cover a valid-but-source-outside Rect as InvalidRange and an empty Rect as InvalidDimensions through @ops.crop. For crop and every rotation, constrain bytes, allocation count, allocation size, width, height, pixels, and work one at a time so each resource failure propagates BudgetExceeded and leaves the full remaining snapshot unchanged. Add complete source-to-destination coordinate-oracle loops for 90/180/270 on a non-square image, including RGB and RGBA, output dimensions, TopLeft orientation, and one transformed disposition field. Do not modify generated vector tables unless the fixture generator is deliberately updated in a future authorized task.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js</automated>
  </verify>
  <done>Adversarial tests demonstrate that all geometry validation and budget failures are typed, deterministic, atomic, and portable, while coordinate semantics are exhaustively asserted on a non-square source.</done>
</task>

<task type="auto">
  <name>Task 2: Document geometry and retain nearest-neighbor as the tested reference baseline</name>
  <files>modules/mb-image/README.mbt.md, modules/mb-image/ops/resize_convert_wbtest.mbt</files>
  <action>Update the existing operations section of modules/mb-image/README.mbt.md to distinguish zero-copy ImageView::crop from the new @ops.crop owned, tightly packed operation; document D-01 through D-06: packed U8 encoded-sRGB RGB/RGBA capability, checked rectangle bounds, explicit clockwise 90/180/270 operations, TopLeft orientation normalization for physical rotations, typed CoreError failures, and the fixed nearest-neighbor formula min(source_extent - 1, floor(destination * source_extent / destination_extent)). Extend the checked README example with crop and at least one explicit rotation plus observable descriptor/metadata assertions and caller-provided budgets. Add a resize regression in resize_convert_wbtest.mbt that consumes a two-dimensional source/output mapping in addition to the existing generated axis vectors, asserts every channel is mapped by the documented floor formula, and verifies an unsupported/incompatible request remains a typed failure with unchanged budget. Per D-06, leave resize.mbt, nearest_source_index, existing resize vector generation, flip implementations, and orientation realization behavior unchanged; do not introduce interpolation, conversion fallback, alpha processing, or release automation.</action>
  <verify>
    <automated>moon test modules/mb-image/ops --target js; moon test modules/mb-image/ops --target wasm; moon test modules/mb-image/ops --target wasm-gc; moon test modules/mb-image/ops --target native; moon -C modules/mb-image check README.mbt.md --target js --frozen; moon -C modules/mb-image check README.mbt.md --target wasm --frozen; moon -C modules/mb-image check README.mbt.md --target wasm-gc --frozen; moon -C modules/mb-image check README.mbt.md --target native --frozen</automated>
  </verify>
  <done>GEOM-03 is documented and regression-tested as the existing deterministic integer-floor nearest-neighbor reference algorithm on js, wasm, wasm-gc, and native; public documentation exposes the new geometry API and its error/metadata contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial test input to public geometry operations | Format, region, dimensions, and budget values must not cause partial charging, invalid access, or non-deterministic diagnostics. |
| public documentation to checked example compiler | Documentation examples are executable public API consumers across four targets and can detect exported-contract drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Denial of service | geometry allocation-limit paths | high | mitigate | Test each budget dimension's atomic rejection through OwnedImage::new_operation and assert all remaining counters are preserved. |
| T-09-06 | Tampering | operation diagnostics | medium | mitigate | Assert stable CoreError categories, codes, and public contexts for capability and invalid-region requests. |
| T-09-07 | Tampering | nearest-neighbor cross-target mapping | medium | mitigate | Execute generated and two-dimensional floor-mapping regressions on js, wasm, wasm-gc, and native. |
| T-09-08 | Repudiation | public geometry contract | low | mitigate | Compile README examples on all supported targets so documented APIs, metadata behavior, and required caller budgets remain executable. |
</threat_model>

<verification>
Run the full ops package suite and the mb-image README check on js, wasm, wasm-gc, and native. Confirm no planned file is a release script, workflow, registry artifact, or generated vector table.
</verification>

<success_criteria>
- GEOM-03 appears once in plan ownership and is proven by a documented, unchanged nearest-neighbor floor mapping on all four targets.
- The Phase 9 geometry family has public and adversarial proof for valid pixels, invalid regions, unsupported formats, incompatible dimensions, and atomic resource limits.
- Public docs make view cropping, owned operation cropping, explicit requested rotations, and metadata-driven orientation application unambiguous.
</success_criteria>

<output>
Create `.planning/phases/09-checked-image-geometry-and-diagnostics/09-02-SUMMARY.md` when done.
</output>
