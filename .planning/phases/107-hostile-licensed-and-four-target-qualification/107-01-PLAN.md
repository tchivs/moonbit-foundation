---
phase: 107-hostile-licensed-and-four-target-qualification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json
  - fixtures/font/cff/host-toolchain.lock.json
  - fixtures/font/cff-oracle-tools.json
  - fixtures/font/cff-qualification-cases.json
  - scripts/fixtures/Provision-CffQualificationTools.ps1
  - scripts/fixtures/oracles/fonttools_cff_oracle.py
  - scripts/fixtures/oracles/Invoke-AfdkoCffOracle.ps1
  - scripts/fixtures/Generate-FontQualification.ps1
autonomous: false
requirements:
  - CFF-06
must_haves:
  truths:
    - "Per D-01/D-02, qualification adds no production API, dependency, import, parser, profile, limit, or target-specific runtime path."
    - "Per D-03/D-04/D-05/D-06, one ordered canonical corpus freezes hand-derived generated name-keyed, multi-FD CID, shared-CFF collection, compatibility, workload, and exact case identities."
    - "Per D-09, fontTools 4.63.0 and AFDKO 5.0.1 remain independent semantic readers, while OTS commit 3b26b2e8b571a33739ee0c3553cb01c17ed62e23 supplies structural acceptance only."
    - "Before Task 1, a blocking execution preflight supplies and validates one exact caller-owned absolute host-toolchain manifest; Task 1 then emits a fixed execution-handoff artifact instead of consulting ambient environment state."
    - "The host toolchain contract accepts only caller-supplied absolute canonical identities and fails closed when CPython, Meson, Ninja, C/C++ compilers, linker, SDK identities, or any invoked build dependency is unavailable or mismatched."
    - "Per D-11/D-12/D-13, every hostile case owns a traced literal outcome, nullable failing GID, publication facts, and caller/ancestor before/after B8 snapshots with exact State, Resource, Capability, or Data category."
  artifacts:
    - path: "artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json"
      provides: "Ignored run-scoped handoff containing the validated caller manifest path/digest, lock digest, provisioned-tools root, SDK inventory digest, and invoked identity digest set for Plan 107-02."
    - path: "fixtures/font/cff/host-toolchain.lock.json"
      provides: "Closed required-role, identity, version, SHA-256, provenance, invocation, and containment contract for every host build input."
    - path: "fixtures/font/cff-oracle-tools.json"
      provides: "Exact wheel/source/adapter/invoked-executable identities and structural-versus-semantic role separation."
    - path: "fixtures/font/cff-qualification-cases.json"
      provides: "Ordered canonical generated, hostile, compatibility, licensed-intake, public-workflow, target, and benchmark-correctness contracts."
    - path: "scripts/fixtures/Provision-CffQualificationTools.ps1"
      provides: "Isolated fail-closed wheel installation and OTS build/test from caller-supplied absolute identities."
  key_links:
    - from: "artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json"
      to: "fixtures/font/cff/host-toolchain.lock.json"
      via: "blocking preflight path/digest followed by Task 1 full schema, identity, version, hash, SDK inventory, and invocation validation"
      pattern: "mnf-phase107-host-toolchain-handoff/1.0.0"
    - from: "fixtures/font/cff/host-toolchain.lock.json"
      to: "scripts/fixtures/Provision-CffQualificationTools.ps1"
      via: "exact required-role schema and caller manifest validation before any process invocation"
      pattern: "host-toolchain-lock/1.0.0"
    - from: "scripts/fixtures/Provision-CffQualificationTools.ps1"
      to: "fixtures/font/cff-oracle-tools.json"
      via: "isolated wheel/source verification plus invoked executable and adapter digests"
      pattern: "cff-oracle-tools/1.0.0"
    - from: "fixtures/font/cff-qualification-cases.json"
      to: "scripts/fixtures/Generate-FontQualification.ps1"
      via: "closed key order, exact IDs, source-assertion trace, formulas, and byte-canonical checks"
      pattern: "cff-qualification-cases/1.0.0"
  prohibitions:
    - "No PATH discovery, global package/tool use, symlink/reparse/hardlink alias, registry fallback, floating dependency, or unverified executable invocation."
    - "No self-oracle, shared semantic backend, OTS semantic facts, guessed outcome category, unresolved B8 value, or fabricated Resource pair."
    - "No licensed asset promotion, evidence module, production MoonBit, public API, dependency, license, policy, CI, documentation, or benchmark mutation in this plan."
---

<objective>
Implement D-23 slice 1a: close the oracle/tool adapters and canonical generated/hostile corpus before any asset or MoonBit mirror consumes them.

Purpose: Make all later plans consume exact producer-owned facts and exact host identities rather than discover tools or complete schemas downstream.
Output: A blocking caller-manifest preflight, ignored validated execution handoff, fail-closed host-toolchain contract, isolated provisioner, two independent adapters, closed tool identities, and canonical generated/hostile corpus.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
@scripts/fixtures/Generate-FontQualification.ps1
@modules/mb-font/font/cff_name_keyed_fixture_wbtest.mbt
@modules/mb-font/font/cff_cid_fixture_wbtest.mbt
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Preflight checkpoint: supply one exact caller-owned host-toolchain manifest</name>
  <action>
Before Task 1, the executing agent may act as the caller and choose or provision the optimal local CPython, Meson, Ninja, C compiler, C++ compiler, linker, SDK, and invoked build-dependency identities outside the repository. Prepare one out-of-repository JSON manifest that names every identity as an absolute regular-file path and records its exact version, SHA-256, permitted parent root, purpose, provenance, and the closed SDK regular-file inventory. Do not use the production provisioner to discover identities: no PATH lookup, registry lookup, command-name resolution, global package lookup, or undeclared environment expansion is permitted.

Validate the proposed manifest before resuming: the manifest itself is an absolute regular file; every declared path exists and is an absolute canonical regular file; no declared path or ancestor is a symlink/reparse point; declared versions match direct invocation by the declared absolute executable; SHA-256 values match; role and SDK inventories are closed with no missing or extra entries; and every build dependency needed by the literal OTS configure/build/test chain is present. If any required identity cannot be provisioned or validated, stop the plan rather than substituting ambient state.

After validation, create the ignored run-scoped `artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json` with schema `mnf-phase107-host-toolchain-handoff/1.0.0`, the exact manifest absolute path, manifest SHA-256, preflight timestamp, ordered role IDs, ordered SDK inventory digest, and `preflight_validated: true`. This checkpoint authorizes only that exact path/digest pair for Task 1.
  </action>
  <instructions>
1. Acting as the caller, the executing agent chooses or provisions every required local host identity outside the repository and writes the closed manifest outside the repository.
2. Invoke each declared executable by its absolute path to capture and compare its exact version; hash every declared regular file and the closed SDK inventory; reject missing/extra roles, aliases, link/reparse components, undeclared dependencies, and non-canonical paths.
3. Write `artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json` only after all checks pass, using the exact schema and fields in the action above.
4. Inspect the handoff and manifest once more by literal path. If any identity is unavailable or mismatched, do not approve or begin Task 1.
  </instructions>
  <verification>The literal handoff path exists as a regular file; its schema is exactly `mnf-phase107-host-toolchain-handoff/1.0.0`; `preflight_validated` is true; its manifest path is absolute and resolves to a regular file; the manifest SHA-256 matches; ordered role IDs cover CPython, Meson, Ninja, C compiler, C++ compiler, linker, all SDK roots, and every invoked build dependency; and the closed SDK inventory digest matches the validated input.</verification>
  <resume-signal>Type `host-toolchain preflight approved` only after the fixed handoff artifact exists and every required identity/version/hash/SDK-inventory check above passes; otherwise report the unavailable identity and stop.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one generated CFF face through an exact locked host chain and two independent readers</name>
  <precondition>The blocking preflight is approved and `artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json` names one existing absolute regular-file manifest whose SHA-256 and `preflight_validated: true` value match; no environment variable may replace this handoff.</precondition>
  <files>artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json, fixtures/font/cff/host-toolchain.lock.json, scripts/fixtures/Provision-CffQualificationTools.ps1, scripts/fixtures/oracles/fonttools_cff_oracle.py, scripts/fixtures/oracles/Invoke-AfdkoCffOracle.ps1, scripts/fixtures/Generate-FontQualification.ps1</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
    - scripts/fixtures/Generate-FontQualification.ps1
    - fixtures/font/collection-qualification-cases.json
  </read_first>
  <behavior>
    - "Missing/extra roles, relative/non-canonical paths, non-regular files, symlink/reparse/hardlink aliases, unexpected SDK members, or version/SHA-256/provenance mismatch fails before use."
    - "The required roles include CPython, Meson, Ninja, C compiler, C++ compiler, linker, every SDK root with a closed regular-file identity inventory, and every executable/library/script invoked by the build."
    - "The tracer installs only the two exact wheels in an isolated tree and runs the exact OTS configure/build/test commands with no ambient fallback."
    - "The two adapters emit equal closed semantic values from distinct implementations; OTS emits only acceptance and diagnostics."
  </behavior>
  <action>
Per D-09, create `host-toolchain-lock/1.0.0` as an exact ordered contract. Require a caller manifest with no missing or extra roles. Every executable or build dependency record contains logical role, caller-supplied absolute path, expected version, expected SHA-256, acquisition provenance when it is not supplied by the environment, invocation purpose, and permitted parent root. Represent each SDK root by its canonical absolute root plus a closed ordered inventory of absolute regular-file identities used by the build. Reject relative paths, environment expansion, command names, PATH lookup, registry lookup, global/user/machine package locations, file-ID aliases, symlink/reparse components, undeclared child processes, and any path outside the declared isolated staging or SDK roots. Validate the full set before the first process starts and record every actually invoked executable digest afterward. If the caller input cannot satisfy the contract, fail closed.

Read the caller manifest path only from the fixed preflight handoff artifact and revalidate its recorded digest before parsing it; do not read `MNF_CFF_HOST_TOOLCHAIN_INPUT` or any other ambient variable. After the full provisioner and reader tracer pass, atomically enrich the same handoff with the committed lock digest, provisioned-tools absolute root, complete ordered invoked-executable digest set, SDK inventory digest, adapter digests, OTS executable digest, and `provisioning_validated: true`. Keep this file under the already ignored `artifacts/release-qualification/` tree; it is an execution contract, not canonical fixture evidence.

Provision fontTools 4.63.0 only from `https://files.pythonhosted.org/packages/87/36/cccb9bc2a6ab63d1b2980374f0dca72ce95ae267c9b4cfe77455bb70d0d4/fonttools-4.63.0-cp312-cp312-win_amd64.whl`, length 2343211, SHA-256 `59ac449f8cca9b4ffa08d2e7bbadad87ce710d69d1eda5c3c1ce579baa987272`; provision AFDKO 5.0.1 only from `https://files.pythonhosted.org/packages/de/08/0d5046bd4d4dfc10d0d44feb3fa44886fcf8fa55161f4a4721ea3b90c340/afdko-5.0.1-cp312-cp312-win_amd64.whl`, length 2721468, SHA-256 `c5aca7e9c8ed8ad3f411ba9e60cdcc5e820ca2326efc8332168fec995600c15a`. Invoke the caller-supplied absolute CPython directly and install only these staged wheel paths into the isolated tree with index and dependency resolution disabled.

Provision OTS only from `https://codeload.github.com/khaledhosny/ots/zip/3b26b2e8b571a33739ee0c3553cb01c17ed62e23`, commit `3b26b2e8b571a33739ee0c3553cb01c17ed62e23`, length 46943415, SHA-256 `11e2c24e2c2d13627f389ca811041cb31022fe7747e5eb44ccff92757a6b63f1`. Extract link-free under the staging root, then wire the exact caller-supplied Meson, Ninja, compilers, linker, SDK, and dependency paths to the literal operations `meson build`, `ninja -C build`, and `ninja -C build test`; disable wrap downloads and sanitize the process environment so none of those names resolve ambiently. Hash the built `ots-sanitize`.

Implement the fontTools adapter without AFDKO imports and the AFDKO adapter through only the isolated `tx` executable without fontTools imports. Extend the generator with a hand-derived generated name-keyed tracer and compare mapping/GID, metrics, bounds, and ordered MoveTo/LineTo/CubicTo/Close facts across both adapters. Add provisioner `-Check` negatives for every missing role, extra role, path/global/PATH injection, alias/reparse, version/hash mismatch, undeclared child, dependency omission, wrap download, and reader disagreement. The check must prove no fallback and preserve all canonical files.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; $handoffPath='artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json'; if (-not (Test-Path -LiteralPath $handoffPath -PathType Leaf)) { throw '107-01 host-toolchain handoff is missing' }; $handoff=Get-Content -Raw -LiteralPath $handoffPath | ConvertFrom-Json; if ($handoff.schema -cne 'mnf-phase107-host-toolchain-handoff/1.0.0' -or $handoff.preflight_validated -ne $true) { throw '107-01 host-toolchain preflight is not validated' }; $input=(Resolve-Path -LiteralPath $handoff.manifest_path).Path; $inputHash=(Get-FileHash -Algorithm SHA256 -LiteralPath $input).Hash.ToLowerInvariant(); if ($inputHash -cne $handoff.manifest_sha256) { throw '107-01 caller manifest digest drifted' }; $stage=Join-Path ([System.IO.Path]::GetTempPath()) ('mnf-cff-host-'+[guid]::NewGuid().ToString('N')); ./scripts/fixtures/Provision-CffQualificationTools.ps1 -HostToolchainInputPath $input -ExecutionHandoffPath $handoffPath -StagingRoot $stage; ./scripts/fixtures/Provision-CffQualificationTools.ps1 -HostToolchainInputPath $input -ExecutionHandoffPath $handoffPath -StagingRoot $stage -Check; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckGeneratedTracer -ExecutionHandoffPath $handoffPath -ProvisionedToolsRoot $stage; $validated=Get-Content -Raw -LiteralPath $handoffPath | ConvertFrom-Json; if ($validated.provisioning_validated -ne $true -or -not $validated.lock_sha256 -or -not $validated.invoked_identities_sha256 -or -not $validated.sdk_inventory_sha256) { throw '107-01 validated execution handoff is incomplete' }; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-04/D-09 one hand-derived generated face crosses the complete locked host chain and two independent semantic readers."
    - "No process starts until every caller-supplied absolute identity validates, and every invoked executable digest is recorded."
    - "Unavailable caller inputs stop the plan; no ambient or alternate tool can satisfy the gate."
  </acceptance_criteria>
  <done>The exact locked toolchain and independent-reader tracer pass with no fallback path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze canonical generated, public, licensed-intake, compatibility, target, and workload contracts</name>
  <files>fixtures/font/cff-oracle-tools.json, fixtures/font/cff-qualification-cases.json, scripts/fixtures/Generate-FontQualification.ps1</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
    - fixtures/font/cff/host-toolchain.lock.json
    - scripts/fixtures/Provision-CffQualificationTools.ps1
    - scripts/fixtures/Generate-FontQualification.ps1
    - modules/mb-font/font/cff_name_keyed_fixture_wbtest.mbt
    - modules/mb-font/font/cff_cid_fixture_wbtest.mbt
  </read_first>
  <behavior>
    - "Missing, extra, duplicate, or reordered schema keys, case IDs, target IDs, or workload IDs fail closed."
    - "Generated name-keyed, two-FD CID, and two-face shared-CFF collection facts remain hand-derived, including mapping/GID, selected FD/environment, face-local metrics/kern, bounds, and ordered cubic paths."
    - "The resolved Source Sans and Source Han archive/member/license/profile/FD/high-GID facts are complete producer inputs for Wave 2."
    - "Target order is exactly js, wasm, wasm-gc, native; workload order is Latin admission, CJK admission, Latin outline batch, CJK high-GID multi-FD outline batch."
  </behavior>
  <action>
Per D-03/D-04/D-05/D-06, create the ordered `cff-qualification-cases/1.0.0` corpus. Freeze deterministic Apache-2.0 recipes and hand-derived facts for name-keyed standalone/selected collection, two-FD CID standalone/selected collection, and a shared-CFF two-face collection whose cmap/hmtx/kern differ by face while CFF bytes are identical. Freeze exact public workflow IDs, static-glyf semantic-lock IDs, target order, fixed benchmark GID lists, and fixture/workload/correctness digest inputs.

Freeze the research-resolved licensed intake records without promoting bytes: Source Sans tag 3.052R archive length 2387997 and SHA-256 `a4ebbdea20b08ccbd7bf3665a9462454eefdd01d9a6307129d3b3d4672981074`, member length 334924 and SHA-256 `08df266400933d3178d081a45f94a08814c3e55b4b7dd2e0ff69cb1329f13ab6`, license length 4579 and SHA-256 `89ad2c4f66dd29127527493e729c31e731f111cf10faf5774c3db9275ed0c22c`; Source Han tag 2.003R archive length 36831708 and SHA-256 `c5a3bbc213980cea04932457899c9fc2da4784d3d1d7cae469c41909dd112230`, member length 6210796 and SHA-256 `e5f502bb193c28829895b098498f0f9dd8f658c760b0f83656ad41c1137a8785`, license length 4463 and SHA-256 `9ff5bb567e1b92c801fc1069e5fbf992ff8efccacb9db94e5959a5b3ba9bb903`. Retain CFF1 name/CID keying, 17923 glyphs, all 18 used FDs, exact local-Subr counts, 1599 global Subrs, and non-empty GID 17922 on FD 17.

Create `cff-oracle-tools/1.0.0` from the exact package/source identities plus the validated caller/invoked digest set. Close every nested key/order/cardinality and add byte-canonical LF UTF-8-no-BOM, locale, one-field, adapter-alias, tool-substitution, and schema-drift negatives.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckContracts; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckGeneratedRecipes; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckOracleAdapters; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckSchemaNegatives; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-03-D-06 all generated/public/compatibility identities and expected values are ordered, closed, and producer-owned."
    - "D-07-D-10 all exact licensed and oracle inputs needed by Wave 2 are frozen without promoting assets."
    - "D-19/D-20 correctness workloads and digests contain no timing."
  </acceptance_criteria>
  <done>Every non-hostile downstream identity has an exact canonical producer record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze complete hostile outcomes, exact limits, B8 snapshots, and precedence</name>
  <files>fixtures/font/cff-qualification-cases.json, scripts/fixtures/Generate-FontQualification.ps1</files>
  <read_first>
    - .planning/phases/104-cff1-profile-and-bounded-data-model/104-VERIFICATION.md
    - .planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-VERIFICATION.md
    - fixtures/font/cff-qualification-cases.json
    - modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
    - modules/mb-font/font/cff_type2_fixture_wbtest.mbt
    - modules/mb-font/font/cff_type2_wbtest.mbt
    - modules/mb-font/font/cff_type2_bounds_wbtest.mbt
    - modules/mb-font/font/cff_type2_path_wbtest.mbt
  </read_first>
  <behavior>
    - "Every D-11 structural, Type 2, semantic-limit, caller/ancestor resource, mutation, and multi-fault case has one literal ordered ID and exact source assertion locator."
    - "Every outcome stores exact category/code/operation/payload/context, nullable smallest GID, publication state, and four B8 snapshots ordered bytes, allocations, allocation_size, width, height, pixels, depth, work."
    - "Exact/one-short Resource pairs exist only for independently adjustable ceilings; non-applicable pairs retain exact reasons and structural Data evidence."
    - "State -> Resource -> Capability -> Data precedence and smallest-failing-GID order are literal facts, not consumer rules."
  </behavior>
  <action>
Per D-11/D-12/D-13, populate the ordered groups `structural`, `type2-program`, `semantic-limit`, `resource`, `mutation`, and `multi-fault-precedence` from the named Phase 104-106 assertions. Cover Header, all INDEX families, DICT, String, CharStrings, Private/Subrs, charset, Encoding, ROS, FDArray, FDSelect, table ranges; Type 2 number/stack/arity/arithmetic/logical/storage/transient/random/width/path/flex/stem/mask/subroutine/operator/termination behavior; every applicable structural/VM/geometry/admission ceiling; caller and ancestor authority; admission, selected-face, fetch, staged-path, and final-commit mutation windows; and all required competing-fault pairs.

Trace each literal outcome instead of applying a family default. Preserve Data for malformed/cardinality/framing outcomes, Capability for recognized unsupported profiles/operators, Resource only for independently adjustable limits, and State for revision/authority mutation. Store resolved literal B8 values even when a separate formula audit recomputes their inputs. Add negatives for untraced rows, wrong category or error field, unresolved snapshot values, invented pairs, non-adjacent exact/one-short pairs, incomplete eight-field snapshots, wrong publication/GID, and consumer completion of expected facts.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckHostileInventory; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckOutcomeTrace; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckBoundaryApplicability; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckSchemaNegatives; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-11 the complete hostile and exact/one-short applicability surface is present."
    - "D-12 every error, GID, publication state, and caller/ancestor B8 value is literal and complete."
    - "D-13 mutation and multi-fault rows preserve exact precedence without a consumer-side rule."
  </acceptance_criteria>
  <done>Downstream consumers can execute every hostile row without inventing any expected fact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries and STRIDE

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-01-01 | Spoofing | caller host identities | high | mitigate | Task 1 closed roles, canonical paths, versions, SHA-256, provenance, and invoked-digest reconciliation. |
| T-107-01-02 | Tampering | oracle independence | high | mitigate | Distinct adapters, alias/import/process negatives, exact semantic agreement, structural-only OTS. |
| T-107-01-03 | Tampering | canonical outcomes | high | mitigate | Task 3 assertion trace and literal error/GID/publication/B8 rows. |
| T-107-01-04 | Denial of service | hostile corpus | high | mitigate | Closed bounded recipes and explicit exact/one-short applicability. |
| T-107-01-05 | Information disclosure | local paths | low | accept | Canonical evidence and commits retain roles/digests only; the absolute caller path exists solely in the ignored run-scoped execution handoff. |

## OWASP ASVS L1 V1-V14

| Family | Applies | Control |
|---|---|---|
| V1 Architecture | Yes | Host adapters, canonical producers, and later portable consumers are separate. |
| V2 Authentication | N/A | No authentication. |
| V3 Session | N/A | No sessions. |
| V4 Access Control | Yes | Caller-rooted staging and closed executable/file authority. |
| V5 Validation | Yes | Exact schemas, identities, ranges, assertion traces, and outcomes. |
| V6 Cryptography | Yes | Platform SHA-256 binds every source/tool/adapter/evidence input. |
| V7 Errors | Yes | Any mismatch aborts before canonical publication. |
| V8 Data Protection | Yes | No caller absolute path enters committed evidence; the ignored execution handoff carries the path only between dependent plan runs. |
| V9 Communications | Yes | Only exact pinned acquisition URLs are permitted; checks are offline. |
| V10 Malicious Code | Yes | No ambient executable or undeclared child may run. |
| V11 Business Logic | Yes | Hand-derived authority, two-reader agreement, and exact precedence. |
| V12 Files/Resources | Yes | Link-free bounded staging and exact hostile limits. |
| V13 API/Web | N/A | No web API. |
| V14 Configuration | Yes | Closed host roles, versions, commands, case IDs, targets, and workloads. |

No HIGH threat is accepted or left open.
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | Phase 107 goal | Exact producer contracts begin reproducible interoperable/hostile-safe four-target closure. | COVERED |
| REQ | CFF-06 | Generated, hostile, oracle, compatibility, workload, and target identities are frozen for all six waves. | COVERED |
| RESEARCH | Six-plan architecture | Wave 1 owns adapters/toolchain and canonical generated/hostile corpus. | COVERED |
| CONTEXT | D-01/D-02 | Qualification-only boundary and complete evidence outcome are invariant. | COVERED |
| CONTEXT | D-03/D-04/D-05/D-06 | Canonical generated recipes, facts, workflows, and closed inventory are implemented here. | COVERED |
| CONTEXT | D-07/D-08/D-09/D-10 | Licensed intake facts and exact oracle/tool boundary are frozen here; promotion/mirror follow in Waves 2-3. | COVERED |
| CONTEXT | D-11/D-12/D-13/D-14 | Hostile outcomes are frozen here; execution and glyf lock follow in Wave 4. | COVERED |
| CONTEXT | D-15/D-16/D-17/D-18 | Target/policy identities are frozen here and implemented in Wave 5. | COVERED |
| CONTEXT | D-19/D-20/D-21/D-22 | Workload identities are frozen here and native observation is implemented in Wave 6. | COVERED |
| CONTEXT | D-23 | Strict Wave 1 producer precedes Waves 2-6. | COVERED |
</source_audit>

<verification>
The blocking preflight is approved before Task 1; all three automated task gates pass; the host-chain check proves no fallback; the final execution handoff is complete; every canonical row is byte-canonical and source-traced.
</verification>

<success_criteria>
Wave 2 can read the exact validated host-toolchain execution handoff named by the 107-01 summary and promote only the exact licensed assets through the locked adapters; no later consumer must discover a tool, assume an ambient environment variable, or invent a case, outcome, budget value, or workload identity.
</success_criteria>

<output>
Create `.planning/phases/107-hostile-licensed-and-four-target-qualification/107-01-SUMMARY.md` when done. Its `Execution handoff` section MUST record the literal path `artifacts/release-qualification/phase-107/107-01-host-toolchain-handoff.json`, that file's SHA-256, schema `mnf-phase107-host-toolchain-handoff/1.0.0`, the validated caller-manifest SHA-256, lock SHA-256, SDK-inventory SHA-256, invoked-identities SHA-256, provisioned-tools root, and `provisioning_validated: true`; Plan 107-02 consumes this section and the referenced artifact without consulting ambient environment variables.
</output>
