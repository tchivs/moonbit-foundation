---
phase: 107-hostile-licensed-and-four-target-qualification
plan: "04"
type: execute
wave: 4
depends_on:
  - 107-03
files_modified:
  - benchmarks/font-cff/cff_qualification_wbtest.mbt
  - modules/mb-font/font/cff_cid_fixture_wbtest.mbt
  - modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
  - modules/mb-font/font/cff_type2_fixture_wbtest.mbt
  - modules/mb-font/font/font_qualification_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/collection_wbtest.mbt
autonomous: true
requirements:
  - CFF-06
must_haves:
  truths:
    - "Per D-05, every generated and licensed face is exercised through standalone Font and selected FontCollection workflows using only observable public mapping/GID, metrics, bounds, kerning, and exact ordered paths."
    - "The evidence-package wbtest uses private visibility only for its carrier and calls only imported public mb-font APIs; it never exposes or compares private FD/local environment facts."
    - "Direct FD/local-environment comparison exists only in an existing mb-font white-box file and consumes the generator-owned oracle projection."
    - "Per D-11/D-12/D-13, every canonical hostile/mutation row executes with its literal error, smallest GID, publication state, and four B8 snapshots; existing hooks preserve State -> Resource -> Capability -> Data."
    - "Per D-14, static-glyf standalone/collection mappings, metrics, kerning, paths, errors, mutation ordering, and resource facts retain one semantic fingerprint without a total-test constant."
  artifacts:
    - path: "benchmarks/font-cff/cff_qualification_wbtest.mbt"
      provides: "Carrier-visible public-only generated/licensed standalone/collection observations and timing-free correctness facts."
    - path: "modules/mb-font/font/cff_cid_fixture_wbtest.mbt"
      provides: "Private FD/local-environment comparison against generator-owned independent facts."
    - path: "modules/mb-font/font/cff_hostile_fixture_wbtest.mbt"
      provides: "Complete structural/admission hostile dispatcher and exact outcomes."
    - path: "modules/mb-font/font/cff_type2_fixture_wbtest.mbt"
      provides: "Complete Type 2/limit/resource/precedence dispatcher and exact outcomes."
    - path: "modules/mb-font/font/font_qualification_test.mbt"
      provides: "Static-glyf semantic compatibility fingerprint."
  key_links:
    - from: "benchmarks/font-cff/generated_cff_evidence.mbt"
      to: "benchmarks/font-cff/cff_qualification_wbtest.mbt"
      via: "package-private carrier access followed only by public Font/FontCollection operations"
      pattern: "font-cff1-v3 carrier-public"
    - from: "scripts/fixtures/Generate-FontQualification.ps1"
      to: "modules/mb-font/font/cff_cid_fixture_wbtest.mbt"
      via: "generator-delimited private oracle region checked by `-CheckPrivateOracleFacts`"
      pattern: "font-cff1-v3 private fd oracle"
    - from: "scripts/fixtures/Generate-FontQualification.ps1"
      to: "modules/mb-font/font/cff_hostile_fixture_wbtest.mbt"
      via: "generator-delimited exact hostile rows checked by `-CheckPrivateEvidenceMirrors`"
      pattern: "font-cff1-v3 private hostile"
  prohibitions:
    - "No public FD/local-environment/raw-CFF query, public fixture export, licensed payload duplication, or private mb-font call from the evidence package."
    - "No consumer-authored expected category, Resource pair, GID, publication value, formula completion, or symbolic budget."
    - "No new runtime hook, parser/profile/limit, dependency/import, file/network/process/FFI seam, or target-specific path."
    - "No deferred capability, timing comparison/verdict, aggregate test-count lock, production/API/dependency/license expansion."
---

<objective>
Implement D-23 slice 2: execute public carrier-backed observations and private FD/hostile/mutation/glyf assertions against the immutable Wave 1-3 contracts.

Purpose: Separate public format-neutral behavior from private implementation evidence while proving complete hostile atomicity and compatibility.
Output: One evidence-package public-observation suite plus exact generator-delimited mb-font white-box assertions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-03-SUMMARY.md
@benchmarks/font-cff/generated_cff_evidence.mbt
@benchmarks/font-cff/cff_qualification_wbtest.mbt
@scripts/fixtures/Generate-FontQualification.ps1
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Qualify one generated face end to end, then expand every carrier-backed public workflow</name>
  <files>benchmarks/font-cff/cff_qualification_wbtest.mbt</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-03-SUMMARY.md
    - benchmarks/font-cff/generated_cff_evidence.mbt
    - benchmarks/font-cff/cff_qualification_wbtest.mbt
    - benchmarks/font-cff/moon.mod.json
    - benchmarks/font-cff/moon.pkg
    - modules/mb-font/font/font_qualification_test.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/collection_test.mbt
  </read_first>
  <behavior>
    - "The generated name-keyed tracer opens standalone and selected collection and matches exact public mapping/GID, metrics, bounds, kern, and ordered MoveTo/LineTo/CubicTo/Close commands."
    - "Generated CID/shared-CFF, licensed Latin, and licensed CJK cases use the same public-only projection."
    - "The shared-CFF collection proves distinct face-local cmap/hmtx/kern with shared path bytes."
    - "Before/after caller-byte mutation and all four correctness workload digests are exact and timing-free."
    - "The source contains no private mb-font symbol or FD/local-environment assertion and exports no evidence symbol."
  </behavior>
  <action>
Per D-04/D-05/D-10, expand `cff_qualification_wbtest.mbt` from the Wave 3 scaffold. Start with focused identity `font-cff1-v3 carrier-public generated-name standalone and selected tracer`; obtain bytes/expected public values only from the package-private carrier, then call only imported public `Font::open`, scalar-to-glyph, metrics, bounds, kerning, outline, `FontCollection::open`, and `open_face` operations. Compare every path command and coordinate field in order.

Use the same public projection for generated name/CID/shared-CFF, Source Sans, and Source Han standalone/selected workflows. For shared CFF, assert face-local cmap/hmtx/kern distinctions and shared outline semantics. Add public before/after byte-mutation cases with exact structured outcomes and budgets. Add timing-free correctness assertions in frozen order for Latin full admission, CJK full admission, Latin fixed outline batch, and CJK high-GID multi-FD outline batch; bind fixture, workload, GID-list, and correctness digests.

Keep every FD, Private DICT, local Subrs, descriptor, matrix, and raw offset fact out of this file. Use `-CheckEvidencePackage` for each target so the test always runs against tracked local mb-font under frozen resolution.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1 -Check; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPublicPrivateBoundary; foreach($t in @('js','wasm','wasm-gc','native')){./scripts/fixtures/Generate-FontQualification.ps1 -CheckEvidencePackage -Target $t; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}}; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-04/D-05 every canonical face has exact standalone and selected public evidence."
    - "Carrier visibility is confined to this package; every mb-font call is public."
    - "D-19/D-20 all four correctness workloads are ordered and contain no timing."
  </acceptance_criteria>
  <done>All generated/licensed public workflows pass through the opaque API on every supported target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind private FD/local facts and freeze static-glyf compatibility</name>
  <files>modules/mb-font/font/cff_cid_fixture_wbtest.mbt, modules/mb-font/font/font_qualification_test.mbt</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-03-SUMMARY.md
    - scripts/fixtures/Generate-FontQualification.ps1
    - modules/mb-font/font/cff_cid_fixture_wbtest.mbt
    - modules/mb-font/font/cff_name_keyed_fixture_wbtest.mbt
    - modules/mb-font/font/font_qualification_test.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/collection_test.mbt
  </read_first>
  <behavior>
    - "The generator materializes only its delimited fact region before the exact private oracle check is invoked."
    - "Existing private CID builders compare actual descriptor/FD/local-environment/matrix facts to independent generator-owned expected rows."
    - "No licensed payload or fixture accessor enters mb-font."
    - "The static-glyf fingerprint covers semantic content rather than suite totals."
  </behavior>
  <action>
Per D-04/D-14, invoke the already implemented generator rendering path to materialize the closed private FD/local-environment region in `cff_cid_fixture_wbtest.mbt`, then extend the existing CID assertion to compare actual per-GID descriptor, selected FD, Private/local-Subrs environment identity, and separate Top/FD matrix facts against that region. The expected facts remain traced to hand-derived/two-reader canonical rows and may not be copied from runtime output.

In `font_qualification_test.mbt`, freeze the Phase 106 static-glyf standalone/collection projection: mappings, metrics, kerning, ordered paths, errors, mutation ordering, and exact caller/ancestor resource facts. Use existing public fixtures/helpers and named focused identity; do not freeze a discovered total. Only after both files are complete invoke `-CheckPrivateOracleFacts`.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPrivateOracleFacts; moon -C modules/mb-font test --target native --frozen --filter 'font-cff1-v3 private fd oracle'; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; moon -C modules/mb-font test --target native --frozen --filter 'font-cff1-v3 freezes static glyf semantic compatibility'; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "Direct FD/local facts remain private and independently anchored."
    - "D-14 static-glyf semantics are frozen without a test-count constant."
    - "No public API, payload, dependency, import, or production source changes."
  </acceptance_criteria>
  <done>Private FD truth and static-glyf public compatibility are exact and generator-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute every hostile outcome and existing mutation window with exact atomic budgets</name>
  <files>modules/mb-font/font/cff_hostile_fixture_wbtest.mbt, modules/mb-font/font/cff_type2_fixture_wbtest.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-01-SUMMARY.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-03-SUMMARY.md
    - scripts/fixtures/Generate-FontQualification.ps1
    - modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
    - modules/mb-font/font/cff_type2_fixture_wbtest.mbt
    - modules/mb-font/font/cff_type2_wbtest.mbt
    - modules/mb-font/font/cff_type2_bounds_wbtest.mbt
    - modules/mb-font/font/cff_type2_path_wbtest.mbt
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-font/font/collection_wbtest.mbt
  </read_first>
  <behavior>
    - "Every canonical hostile ID executes exactly once or asserts its producer-owned non-applicability reason while retaining structural Data evidence."
    - "Actual error/null, nullable GID, publication, formula audit, and four B8 snapshots compare field-for-field."
    - "Existing admission, selected-face, fetch, staged-path, and final-commit hooks fire exactly once."
    - "State -> Resource -> Capability -> Data and zero uncommitted charge remain exact."
  </behavior>
  <action>
Per D-11/D-12/D-13, use the already implemented generator rendering path to materialize only the delimited hostile/mutation fact regions in these four existing white-box files. Route each row to its existing Phase 104-106 builder or hook; compare all fields literally. For a non-applicable Resource pair, assert the exact reason and execute its retained structural exact/invalid case rather than creating another outcome. Require exact pair adjacency where applicability is true.

Exercise public before/after mutation plus private admission, selected-face, Type 2 fetch, staged-path, and final-commit windows using the existing no-op-in-production hooks. Assert callback count, exact State identity, smallest GID, publication flags, and all eight caller/ancestor before/after fields. Add no hook or production path. Only after all regions/dispatchers are complete invoke `-CheckPrivateEvidenceMirrors`.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPrivateEvidenceMirrors; moon -C modules/mb-font test --target native --frozen --filter 'font-cff1-v3 private hostile outcomes'; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; moon -C modules/mb-font test --target native --frozen --filter 'font-cff1-v3 private mutation windows and atomic budgets'; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; moon -C modules/mb-font test --target all --frozen; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-11 every producer row is executed or its explicit non-applicability is asserted."
    - "D-12 error/GID/publication/four-B8 facts match without consumer completion."
    - "D-13 every existing mutation window preserves precedence and atomicity."
  </acceptance_criteria>
  <done>The full hostile/private matrix passes with exact outcomes and unchanged runtime boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries and STRIDE

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-04-01 | Spoofing | carrier/public facts | high | mitigate | Single carrier and field-by-field public observations. |
| T-107-04-02 | Information disclosure | private FD facts | high | mitigate | Existing mb-font white-box file only; no public/evidence export. |
| T-107-04-03 | Tampering | hostile outcomes | high | mitigate | Generator-delimited literal rows and exact private check modes. |
| T-107-04-04 | Elevation | mutation hooks | high | mitigate | Existing no-op hooks only, callback once, exact State precedence. |
| T-107-04-05 | Repudiation | budget/publication | high | mitigate | Four complete B8 snapshots and publication fields on every failure. |

## OWASP ASVS L1 V1-V14

| Family | Applies | Control |
|---|---|---|
| V1 Architecture | Yes | Public observations and private implementation assertions remain separate. |
| V2 Authentication | N/A | No authentication. |
| V3 Session | N/A | No sessions. |
| V4 Access Control | Yes | Package/private boundaries and existing hook access. |
| V5 Validation | Yes | Exact case IDs, fields, paths, limits, and mutation windows. |
| V6 Cryptography | Yes | Canonical fixture/workload digests checked before use. |
| V7 Errors | Yes | Complete structured errors and precedence are asserted. |
| V8 Data Protection | Yes | Licensed bytes remain only in the evidence carrier. |
| V9 Communications | N/A | Tests are offline and subprocess-free at runtime. |
| V10 Malicious Code | Yes | No install/dependency/runtime expansion. |
| V11 Business Logic | Yes | Public parity, private FD truth, atomicity, and compatibility. |
| V12 Files/Resources | Yes | Exact/one-short applicability and complete B8 snapshots. |
| V13 API/Web | N/A | No web API. |
| V14 Configuration | Yes | Exact case/assertion/GID/workload/target orders. |

No HIGH threat is accepted or left open.
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | Phase 107 goal | Public interoperability, hostile safety, and compatibility are executable. | COVERED |
| REQ | CFF-06 | Generated/licensed workflows, hostile/resource/mutation cases, and glyf locks execute here. | COVERED |
| RESEARCH | Six-plan architecture | Wave 4 owns evidence wbtest public observations and mb-font private assertions. | COVERED |
| CONTEXT | D-01/D-02 | No runtime/API expansion; canonical failure is the only permitted defect signal. | COVERED |
| CONTEXT | D-03/D-04/D-05/D-06 | All generated facts/workflows are consumed exactly. | COVERED |
| CONTEXT | D-07/D-08/D-09/D-10 | Licensed/oracle facts are consumed from one carrier with no host access. | COVERED |
| CONTEXT | D-11/D-12/D-13/D-14 | Full hostile, atomicity, precedence, and glyf compatibility are implemented here. | COVERED |
| CONTEXT | D-15/D-16/D-17/D-18 | Focused identities become Wave 5 equality/policy inputs. | COVERED |
| CONTEXT | D-19/D-20/D-21/D-22 | Correctness workloads execute here; native timing remains Wave 6. | COVERED |
| CONTEXT | D-23 | Strict Wave 4 consumes frozen producers before v3 serialization. | COVERED |
</source_audit>

<verification>
All public evidence-package targets, exact private generator modes, focused private assertions, complete mb-font all-target suite, and whitespace checks pass.
</verification>

<success_criteria>
Wave 5 can serialize exact public/private/hostile/glyf/correctness facts without creating a test, payload, expected value, or private visibility seam.
</success_criteria>

<output>
Create `.planning/phases/107-hostile-licensed-and-four-target-qualification/107-04-SUMMARY.md` when done.
</output>
