---
phase: 107-hostile-licensed-and-four-target-qualification
plan: "05"
type: execute
wave: 5
depends_on:
  - 107-04
files_modified:
  - scripts/quality/Invoke-FontQualification.ps1
  - scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
  - scripts/quality/Assert-Policy.ps1
  - policy/foundation.json
  - .github/workflows/quality.yml
  - modules/mb-font/moon.mod.json
  - modules/mb-font/README.mbt.md
  - modules/mb-font/CHANGELOG.md
  - docs/policies/licensing-and-fixtures.md
autonomous: true
requirements:
  - CFF-06
must_haves:
  truths:
    - "Per D-15/D-16, fresh v3 ownership emits exactly four explicit isolated records in js, wasm, wasm-gc, native order and compares every equality-bearing field after removing only top-level target and runner."
    - "The marker is preserved byte-for-byte and cleanup owns only js.json, wasm.json, wasm-gc.json, native.json, and comparison.json; earlier markers or unrelated files never authorize deletion."
    - "Every target runs the evidence-package public observations against tracked local mb-font, then the existing private focused assertions and complete discovered mb-font suite."
    - "Per D-17/D-18, policy retains the exact 85-line interface, sole `tchivs/mb-core@0.1.0` dependency, five imports, pure runtime, exact source/fixture/oracle/tool/test inventories, one existing 20-minute CI job, and documentation boundaries."
    - "Wave 6 owns the final benchmark/workspace source inventory refresh and post-baseline v3/policy seal."
  artifacts:
    - path: "scripts/quality/Invoke-FontQualification.ps1"
      provides: "Fresh v3 isolated four-target runner, closed records, local resolution, explicit semantic projection, and comparison."
    - path: "scripts/quality/Test-FontQualificationEvidenceBoundary.ps1"
      provides: "Ownership, containment, marker, cleanup, schema, order, resolution, and one-field divergence negatives."
    - path: "policy/foundation.json"
      provides: "Exact pre-benchmark runtime/evidence/source/API/dependency/import/tool/fixture inventories."
    - path: ".github/workflows/quality.yml"
      provides: "Existing single 20-minute FontQualification v3 CI job and success-only upload."
  key_links:
    - from: "benchmarks/font-cff/cff_qualification_wbtest.mbt"
      to: "scripts/quality/Invoke-FontQualification.ps1"
      via: "exact public-observation focused identities executed once per target in a frozen local workspace"
      pattern: "font-cff1-v3 carrier-public"
    - from: "modules/mb-font/font/cff_hostile_fixture_wbtest.mbt"
      to: "scripts/quality/Invoke-FontQualification.ps1"
      via: "exact private FD/hostile/mutation assertions and complete package execution"
      pattern: "font-cff1-v3 private"
    - from: "scripts/quality/Invoke-FontQualification.ps1"
      to: "scripts/quality/Test-FontQualificationEvidenceBoundary.ps1"
      via: "import-only marker, five-product cleanup, closed validator, and comparison seams"
      pattern: "mnf-font-qualification-evidence/v3"
    - from: "scripts/quality/Invoke-FontQualification.ps1"
      to: "policy/foundation.json"
      via: "exact workflow/schema/normalization/toolchain/source/API/dependency/import identities"
      pattern: "font-complete-public-v3"
  prohibitions:
    - "No public/runtime API, dependency/import, parser/profile/limit, license, or target-specific runtime expansion."
    - "No runtime ambient I/O/FFI/tool access, registry fallback, self-oracle, recursive cleanup, marker rewrite/removal, or directory-enumeration-derived record membership."
    - "No timing in semantic records and no threshold/comparison/verdict/ranking/superiority claim."
    - "No deferred font capability or competing correctness lane."
---

<objective>
Implement D-23 slice 3a: advance the existing font qualification lane to v3, close its evidence boundary, refresh policy/CI, and document qualified static CFF1.

Purpose: Serialize all frozen Wave 1-4 facts into exactly four equal isolated target records without reopening producers or runtime behavior.
Output: v3 runner/boundary, pre-benchmark policy inventories, retained CI lane, and accurate module/licensing documentation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-04-SUMMARY.md
@scripts/quality/Invoke-FontQualification.ps1
@scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
@scripts/quality/Assert-Policy.ps1
@policy/foundation.json
@.github/workflows/quality.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one public CFF path through fresh v3 ownership, then close exact four-target execution</name>
  <files>scripts/quality/Invoke-FontQualification.ps1, scripts/quality/Test-FontQualificationEvidenceBoundary.ps1</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-01-SUMMARY.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-02-SUMMARY.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-03-SUMMARY.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-04-SUMMARY.md
    - scripts/quality/Invoke-FontQualification.ps1
    - scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
    - scripts/fixtures/Generate-FontQualification.ps1
  </read_first>
  <behavior>
    - "Missing/corrupt/earlier marker, outside/unowned/linked path, unrelated entry, write-swap, registry-resolved mb-font, or schema/order divergence fails before deletion or record publication."
    - "The generated-name tracer crosses the package-private carrier and public mb-font API on each target under tracked local resolution."
    - "Exactly four literal record paths validate before write and after read-back; comparison removes only top-level target and runner."
    - "Cleanup preserves the exact marker and may replace only five named products."
  </behavior>
  <action>
Per D-15/D-16/D-17, advance the existing runner to workflow `font-complete-public-v3`, schema `3.0.0`, marker `mnf-font-qualification-evidence/v3`, local root `artifacts/release-qualification/font-v3`, CI root `artifacts/release-qualification/ci-font-v3`, and target order `js,wasm,wasm-gc,native`. Earlier ownership markers must fail before any cleanup and remain unchanged.

For each target create a fresh link-free isolated target root and temporary workspace with explicit canonical paths to tracked mb-core, mb-font, and font-cff. Use `--frozen`, an empty task cache, exact module manifest digests, and canonical source-root checks. Execute each exact evidence-package public-observation identity once, then exact private FD/hostile/mutation identities, then the discovered complete mb-font suite without freezing its total. Validate every focused pass identity and remove the temporary workspace only after record validation.

Freeze exact top-level key order `schema_version,workflow_id,target,toolchain,fixtures,oracle_facts,generated_cff_facts,licensed_cff_facts,public_workflow_facts,cff_hostile_outcomes,mutation_atomicity_facts,glyf_compatibility_facts,benchmark_correctness_facts,boundary_facts,dependency_facts,source_identities,focused_assertions,runner,pass`. Close every nested schema. Construct the semantic payload explicitly from this list while omitting only top-level `target` and `runner`; do not recursively normalize nested fields.

Treat the marker as a regular non-link file in a strict child of the managed root and preserve it byte-for-byte. Cleanup owns only the four literal target filenames and `comparison.json`; record order comes from the literal list. Add boundary negatives for containment/link/reparse/write-swap, unrelated files, marker/workflow/schema, product membership/order, fixture/license/provenance/oracle/tool/host lock digest, GID/FD/private-public boundary, cubic command, error/GID/publication/B8, glyf/workload/API/dependency/import/source/toolchain/assertion/local-resolution/pass, and semantic divergence.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/quality/Test-FontQualificationEvidenceBoundary.ps1; ./scripts/quality/Invoke-FontQualification.ps1 -ContractOnly; ./scripts/quality/Invoke-FontQualification.ps1 -Target native -TracerOnly -EvidenceDirectory artifacts/release-qualification/font-v3-smoke; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-15 fresh ownership and five-product cleanup reject every unsafe authority."
    - "D-16 records compare every field except top-level target/runner."
    - "D-17 every target and focused/full invocation is explicit and locally resolved."
  </acceptance_criteria>
  <done>The native tracer proves the complete v3 public/private/record path and all ownership negatives pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh exact pre-benchmark policy and retain the single 20-minute CI lane</name>
  <files>scripts/quality/Assert-Policy.ps1, policy/foundation.json, .github/workflows/quality.yml</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-04-SUMMARY.md
    - scripts/quality/Assert-Policy.ps1
    - policy/foundation.json
    - .github/workflows/quality.yml
    - modules/mb-font/moon.mod.json
    - modules/mb-font/font/moon.pkg
    - benchmarks/font-cff/moon.mod.json
    - benchmarks/font-cff/moon.pkg
  </read_first>
  <behavior>
    - "Policy owns exact ordered live production, mb-font test, evidence-module, fixture, provenance/oracle, adapter/tool-lock, generator, runner, and boundary inventories."
    - "Independent classifier remains exactly 85 public interface lines; production dependency/import/target facts remain unchanged."
    - "Only the existing font-qualification job runs v3 and uploads its five products after success with timeout exactly 20 minutes."
    - "Policy contains an explicit Wave 6 refresh contract for benchmark/workspace/script/test/baseline identities."
  </behavior>
  <action>
Per D-01/D-17/D-18, refresh exact ordered paths and digests after Wave 4 assertion identities are frozen. Lock the live Phase 106 production surface, all relevant mb-font tests, the evidence module/package/carrier/wbtest, canonical corpus/assets/notices/qualification records/manifest, host-toolchain lock, oracle tools/adapters, generator, v3 runner, and boundary test. Preserve the independent hard-coded 85-line public-interface classifier, supported targets `+js+wasm+wasm-gc+native`, sole production dependency `tchivs/mb-core@0.1.0`, and exact imports budget/bytes/checked/error/math.

Add executable one-fact negatives for public CFF/fixture/evidence symbols, production source/dependency/import/target expansion, file/network/subprocess/GUI/FFI, target-specific runtime branches, deferred profiles/capabilities, duplicate licensed payload, unowned fixture/oracle/tool, host lock substitution, hostile outcome/applicability/B8 drift, public/private FD boundary drift, glyf/workload drift, v3 root/marker/schema/normalization/local-resolution drift, and toolchain substitution. Record that Wave 6 must refresh exact benchmark/workspace/script/test/baseline paths before final v3; do not claim that final seal here.

Update only the existing `font-qualification` job to invoke the v3 lane at the new CI root and upload only the five evidence products after success. Preserve pinned actions/toolchain, selector, and `timeout-minutes: 20`.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json; ./scripts/quality/Test-FixturePolicy.ps1; ./scripts/quality/Test-FontQualificationEvidenceBoundary.ps1; $y=Get-Content -Raw .github/workflows/quality.yml; if($y-notmatch 'timeout-minutes:\s*20'){throw 'font qualification timeout drift'}; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-01/D-18 API/dependency/import/target/runtime purity and exact pre-benchmark inventories fail closed."
    - "Single carrier ownership, host-toolchain identity, and public/private boundaries are policy facts."
    - "D-17 one existing CI job owns v3 with the exact retained timeout."
  </acceptance_criteria>
  <done>Policy and CI close every current source/evidence identity and expose only the exact Wave 6 final-refresh seam.</done>
</task>

<task type="auto">
  <name>Task 3: Document qualified static CFF1 and exact licensed evidence without broader claims</name>
  <files>modules/mb-font/moon.mod.json, modules/mb-font/README.mbt.md, modules/mb-font/CHANGELOG.md, docs/policies/licensing-and-fixtures.md</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
    - modules/mb-font/moon.mod.json
    - modules/mb-font/README.mbt.md
    - modules/mb-font/CHANGELOG.md
    - docs/policies/licensing-and-fixtures.md
    - fixtures/manifest.json
  </read_first>
  <action>
Per D-08/D-18/D-22, update only module description/narrative, the current changelog candidate section, and licensing policy. Describe qualified static CFF1 through the existing opaque standalone and selected-collection workflows, canonical generated vectors, exact-upstream Source Sans/Source Han specimens and retained licenses, two-reader semantic agreement, structural-only OTS, v3 exactly-four-target records, and the separate observation-only native baseline that Wave 6 records.

State that `benchmarks/font-cff` is non-published evidence, its payload is package-private, and production mb-font contains no licensed CFF bytes or fixture API. Preserve module name/version/candidate status/license/repository/readme/targets/dependency. Retain every deferred exclusion and make no threshold, cross-target/library comparison, superiority, registry publication, release, or stability-promotion claim.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; moon -C modules/mb-font check --target all --frozen; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; foreach($t in @('js','wasm','wasm-gc','native')){moon -C modules/mb-font test --target $t --frozen --doc; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}}; ./scripts/quality/Test-FixturePolicy.ps1; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-18 documentation matches the exact opaque qualified contract."
    - "D-08 upstream licensing/provenance remains explicit."
    - "D-22 no observation is converted into a threshold, verdict, or broader claim."
  </acceptance_criteria>
  <done>Module and policy documentation state exactly the qualified static-CFF1 and evidence boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries and STRIDE

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-05-01 | Spoofing | target/toolchain/module | high | mitigate | Exact targets/toolchain/host lock/local module roots and substitution negatives. |
| T-107-05-02 | Tampering | records/normalization | high | mitigate | Closed schemas, read-back hashes, explicit target/runner-only projection. |
| T-107-05-03 | Elevation | evidence cleanup | high | mitigate | Strict child/link-free exact marker/five-product/write-swap gates. |
| T-107-05-04 | Tampering | API/dependency/source | high | mitigate | Independent interface/import/dependency/source/capability policy checks. |
| T-107-05-05 | Repudiation | documentation claims | medium | mitigate | Exact claim boundary and fixture/licensing policy tests. |
| T-107-05-06 | Information disclosure | local paths | low | accept | Records retain repository-relative identities and approved host facts only. |

## OWASP ASVS L1 V1-V14

| Family | Applies | Control |
|---|---|---|
| V1 Architecture | Yes | Runner, ownership, policy, CI, and docs remain separate. |
| V2 Authentication | N/A | No authentication. |
| V3 Session | N/A | No sessions. |
| V4 Access Control | Yes | Strict managed-root and tracked-local module authority. |
| V5 Validation | Yes | Exact nested schemas/orders/counts and one-field negatives. |
| V6 Cryptography | Yes | SHA-256 binds records, semantics, fixtures, tools, sources, and interface. |
| V7 Errors | Yes | Validation precedes cleanup/write/comparison. |
| V8 Data Protection | Yes | Relative identities, retained licenses, no secrets. |
| V9 Communications | Yes | Pinned CI actions/toolchain and success-only upload. |
| V10 Malicious Code | Yes | Source/import/toolchain/action substitution gates. |
| V11 Business Logic | Yes | Exactly four targets and target/runner-only normalization. |
| V12 Files/Resources | Yes | Managed ownership, explicit products, isolated roots, 20-minute job. |
| V13 API/Web | N/A | No web API. |
| V14 Configuration | Yes | Exact workflow/root/marker/targets/API/dependency/import/source/timeout. |

No HIGH threat is accepted or left open.
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | Phase 107 goal | Exactly-four-target reproducibility and compatibility-preserving policy are executable. | COVERED |
| REQ | CFF-06 | Isolated equal records, policy/toolchain/source/API locks, CI, and docs are implemented. | COVERED |
| RESEARCH | Six-plan architecture | Wave 5 owns v3 runner/boundary/policy/CI/docs; Wave 6 owns final benchmark seal. | COVERED |
| CONTEXT | D-01/D-02 | Runtime/API boundary remains fixed while evidence closes. | COVERED |
| CONTEXT | D-03/D-04/D-05/D-06 | Generated facts/workflows and exact IDs serialize unchanged. | COVERED |
| CONTEXT | D-07/D-08/D-09/D-10 | Licensed/oracle/tool/carrier facts remain equality-bearing and policy-owned. | COVERED |
| CONTEXT | D-11/D-12/D-13/D-14 | Hostile/atomicity/precedence/glyf facts serialize unchanged. | COVERED |
| CONTEXT | D-15/D-16/D-17/D-18 | Fresh v3, exact normalization, runner/boundary/policy/CI/docs are implemented here. | COVERED |
| CONTEXT | D-19/D-20/D-21/D-22 | Correctness digests enter v3; native timing/final seal follow in Wave 6. | COVERED |
| CONTEXT | D-23 | Strict Wave 5 consumes frozen assertions before the final benchmark wave. | COVERED |
</source_audit>

<verification>
  <automated>$ErrorActionPreference='Stop'; ./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory artifacts/release-qualification/font-v3; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; . ./scripts/quality/Invoke-FontQualification.ps1 -ImportOnly; $expected=@('js','wasm','wasm-gc','native'); if((@($Targets)-join "`0")-cne($expected-join "`0")){throw 'target order drift'}; $comparison=Get-Content -Raw artifacts/release-qualification/font-v3/comparison.json|ConvertFrom-Json; if(-not $comparison.equal-or(@($comparison.targets)-join "`0")-cne($expected-join "`0")){throw 'comparison drift'}; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json; git diff --check</automated>
</verification>

<success_criteria>
Exactly four isolated equal records prove the public evidence package and existing private/full mb-font behavior under safe ownership, with policy/CI/docs ready for Wave 6 final source/baseline refresh.
</success_criteria>

<output>
Create `.planning/phases/107-hostile-licensed-and-four-target-qualification/107-05-SUMMARY.md` when done.
</output>
