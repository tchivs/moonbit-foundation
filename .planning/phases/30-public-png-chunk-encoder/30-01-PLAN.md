---
phase: 30-public-png-chunk-encoder
plan: "01"
type: execute
wave: 1
depends_on: [29-03]
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
autonomous: true
requirements: [PNGE-02, PNGE-03]
must_haves:
  truths:
    - "A library user can create PngChunkEncoder for an admitted RGB8 or straight-RGBA8 image, supply empty, one-byte, and short irregular caller-owned mutable leases, and receive exact per-call and cumulative progress until one PNG completes."
    - "Concatenating accepted bytes from the public pull API produces exactly the canonical eager PngEncoder byte sequence once, with no duplicate or omitted byte and no retained output lease or staged PNG buffer."
    - "A successful final pull and every later pull report Finished with zero new bytes; a failed pull and every later pull report the original Failed(CoreError) with zero new bytes."
    - "The PNG public interface policy exposes the approved PngChunkEncoder pull contract exactly and rejects unapproved PNG stream-surface drift."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Public PngChunkEncoder, PngChunkPullOutcome, and PngChunkPullResult declarations with constructor, pull, and exact progress accessors."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Thin sticky public lifecycle adapter over the Phase 29 PngEncodeMachine without caller-output retention or a second PNG emitter."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Native public API evidence for focused empty/tiny/irregular progress, eager-byte parity, caller-lease ownership, preflight, and sticky terminals."
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: "Private acknowledgement-order and machine/wrapper accounting regressions."
    - path: policy/foundation.json
      provides: "Exact generated semantic interface including the approved public PNG chunk encoder contract."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Fail-closed required and negative checks for PngChunkEncoder and its pull result/outcome types."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngChunkEncoder::new constructs exactly one private PngEncodeMachine and pull delegates all canonical byte presentation and acknowledgement to it."
      pattern: "PngChunkEncoder::new|PngChunkEncoder::pull|PngEncodeMachine::new"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-core/bytes/views.mbt
      via: "pull receives the current callback-scoped MutByteLease as a parameter, writes through set, and stores no lease, view, owner, or output byte buffer."
      pattern: "MutByteLease|destination\.set|PngChunkEncoderState"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: "The tests collect each accepted caller lease after its callback, compare the aggregate with eager output, and replay both success and the original lease-write failure."
      pattern: "PngChunkEncoder|PngChunkPullOutcome|Finished|Failed"
    - from: policy/foundation.json
      to: modules/mb-image/png/pkg.generated.mbti
      via: "Assert-PngFoundationPolicy regenerates and exactly compares the public semantic interface."
      pattern: "PngChunkEncoder|PngChunkPullOutcome|PngChunkPullResult"
---

<objective>
Publish the public caller-buffered PNG encoder as a thin lifecycle adapter over the verified Phase 29 machine.

Purpose: Give library users deterministic canonical PNG output through their own short-lived mutable buffers without changing eager bytes, preflight, ownership, or terminal semantics.
Output: Public PngChunkEncoder pull API, focused executable native contract evidence, and an exact fail-closed interface-policy update for PNGE-02 and PNGE-03.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-public-png-chunk-encoder/30-RESEARCH.md
@.planning/phases/29-pausable-png-encode-substrate/29-03-SUMMARY.md
@.planning/phases/29-pausable-png-encode-substrate/29-VERIFICATION.md
@.planning/milestones/v0.8-phases/27-public-png-chunk-decoder/27-03-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt
@modules/mb-image/qoi/qoi.mbt
@modules/mb-image/qoi/stream_encode.mbt
@modules/mb-image/qoi/stream_encode_test.mbt
@modules/mb-core/bytes/views.mbt
@modules/mb-core/bytes/owned_bytes.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the public PNG pull, ownership, parity, and sticky-terminal contract</name>
  <files>modules/mb-image/png/stream_encode_test.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - Test 1: `PngChunkEncoder::new(image.view(), limits, budget, diagnostics)` returns a public encoder only after the same compatible-image preflight used by eager encoding; incompatible source and insufficient budget results are typed constructor errors before a caller lease can be supplied.
    - Test 2: An empty `OwnedBytes::with_mut` lease returns `written() == 0`, `total_written() == 0`, and `NeedOutput`; one-byte and a short fixed irregular sequence of fresh leases each report exactly the bytes copied in that call and a cumulative total equal to the collected prefix length.
    - Test 3: The focused RGB8 and straight-RGBA8 schedules concatenate every byte copied after each callback and equal the existing eager `PngEncoder` bytes exactly; the pull that accepts the final byte reports `Finished` and its total equals the eager byte length.
    - Test 4: After a partial pull returns, mutating and reusing that completed caller owner cannot alter future output; finish through fresh owners and compare the recorded aggregate with eager bytes. A terminal success pull must leave a sentinel-filled new destination unchanged and repeatedly return zero written, the same final total, and `Finished`.
    - Test 5: Releasing a lease inside its `with_mut` callback before passing it to `pull` makes its first `set` return the existing typed lease error. The wrapper reports that exact `CoreError` as `Failed`, reports zero written and no advanced cumulative total, and later pulls with fresh sentinel-filled destinations return the same error without mutation or progress.
    - Test 6: White-box regressions prove the public adapter invokes private `acknowledge` only after a successful lease `set`, adopts `machine.completed()` only after acknowledgment, and transitions to sticky Finished exactly when the final acknowledged byte reaches `machine.total_length()`.
  </behavior>
  <action>Start with RED tests in `stream_encode_test.mbt` and `stream_encode_wbtest.mbt`, following the existing `png_stream_test_eager`, `png_stream_wb_drain`, QOI pull-result, and `OwnedBytes::with_mut` patterns. Establish the exact intended public names in compilation expectations: `PngChunkEncoder`, `PngChunkPullOutcome::{NeedOutput, Finished, Failed(CoreError)}`, `PngChunkPullResult`, `PngChunkEncoder::new`, `PngChunkEncoder::pull`, and the `written`, `total_written`, and `outcome` accessors. Build a compact native helper that allocates a fresh caller `OwnedBytes` for each pull, copies just the reported prefix out after the callback closes, and records result accounting; cover a zero capacity, one-byte capacities, and one short fixed irregular capacity sequence for each RGB8 and straight-RGBA8 fixture. Use the existing eager helper as the only oracle. For ownership evidence, mutate the first completed output owner only after its callback returns, then continue with new owners; verify the preserved collected prefix plus all later output still equals eager bytes. Create the executable failure with the existing `MutByteLease::release` API inside the active callback so the production wrapper receives the ordinary range-checked lease failure, then compare category, code, operation, context, requested, completed, and limit across repeated failures. Retain focused Phase 29 preflight parity checks at construction. Do not alter production code, policy, package inventory, QOI, the eager writer facade, test fixtures, or Phase 31 four-target hostile scheduling and public workflow evidence in this task.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk encoder*'</automated>
  </verify>
  <done>Focused native public and white-box tests compile against the complete planned API and make exact progress, no-retained-lease behavior, eager byte parity, preflight, acknowledgement order, and sticky success/failure behavior executable; they fail only because the public PNG encoder does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the thin sticky public adapter over PngEncodeMachine</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <behavior>
    - `PngChunkEncoder::new` calls `PngEncodeMachine::new` exactly once and returns its original preflight `CoreError` unchanged; no output lease, output byte, or public encoder is produced on rejection.
    - While active, `pull` calls `present`, calls the current lease's `set` for that exact byte, then calls `acknowledge` for that same byte. It increments per-call and cumulative progress only after both operations succeed and stops when the lease is full or the machine reaches its exact total.
    - The durable public state contains only the active private machine or a terminal discriminator plus scalar completed accounting. It does not retain a `MutByteLease`, `ByteView`, `OwnedBytes`, destination owner, or complete/public PNG byte buffer.
    - A zero-length active lease returns NeedOutput and zero written. A final acknowledged byte makes the same pull return Finished; each later pull returns zero written, the preserved total, and Finished without inspecting or mutating its destination.
    - A failure from `present`, current-lease `set`, or `acknowledge` is stored as the original `CoreError`; that call and every later pull return Failed with zero new output after the failure, without substituting a terminal state error.
  </behavior>
  <action>Declare the public values in `png.mbt` using the established `PngChunk*` family and QOI pull shape: `PngChunkEncoder` with private lifecycle/scalar fields; `PngChunkPullOutcome` with `NeedOutput`, `Finished`, and `Failed(@error.CoreError)`; and `PngChunkPullResult` with private written, total-written, and outcome fields plus accessors. Define `PngChunkEncoder::new(@storage.ImageView, @codec.CodecLimits, @budget.Budget, @error.Diagnostics) -> Result[PngChunkEncoder, @error.CoreError]` and `PngChunkEncoder::pull(@bytes.MutByteLease) -> PngChunkPullResult` exactly.

In `stream_encode.mbt`, introduce one private lifecycle such as `Active(PngEncodeMachine)`, `Finished`, and `Failed(CoreError)` and implement the constructor/pull methods as the thin byte-transfer adapter mandated by the locked Phase 30 decisions. Constructor delegates directly once to `PngEncodeMachine::new`; do not duplicate source validation, checked geometry, output/work limits, disposition, budget charge, framing, compression, CRC, Adler, or byte generation. In the active loop, obtain only the current private byte through `present`, write it with the current `MutByteLease::set(written, byte)`, then acknowledge that exact byte. Update cumulative accounting from `machine.completed()` only after acknowledgement, and turn final acknowledgement into sticky `Finished` when it reaches `machine.total_length()`. For any returned error, persist the original typed value in `Failed` before forming the result. Terminal branches return zero written and never inspect the supplied destination. Make all Task 1 tests pass on native. Do not retain a caller lease/view/owner, allocate or stage the PNG output, modify `PngEncoder` or its direct Writer error behavior, add FFI/dependencies/host streams, alter compression or colour semantics, or implement Phase 31's broad hostile-capacity or decode-process-encode evidence.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk encoder*'</automated>
  </verify>
  <done>PngChunkEncoder is a public pure-MoonBit caller-buffered adapter over the sole Phase 29 byte machine: each accepted byte is copied then acknowledged exactly once, progress is exact, eager bytes/preflight are preserved, no caller output capability is retained, and both success and failure terminal outcomes are sticky.</done>
</task>

<task type="auto">
  <name>Task 3: Register and verify the exact PNG chunk-encoder public surface</name>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <action>After Task 2 compiles, run the existing MoonBit interface-generation invocation and copy the generated `modules/mb-image/png/pkg.generated.mbti` semantic sequence exactly into the PNG `semantic_interface` entry in `policy/foundation.json`. Preserve the existing package name, imports, four declared targets, production-source order, and directory inventory; this public adapter belongs in existing `png.mbt` and `stream_encode.mbt`, so do not add package files. Extend `Assert-PngFoundationPolicy` to require the public `PngChunkEncoder`, `PngChunkPullOutcome`, and `PngChunkPullResult` declarations in addition to the retained decoder and eager interfaces. Extend its scoped negative fixtures' expected public-type sequence so a missing pull-result type, an extra obsolete `PngStreamEncoder`, or an unapproved additional public PNG stream type fails closed. Verify the generated interface, focused native chunk-encoder tests, and direct PNG policy/negative-fixture command. Do not run or modify the Phase 31 cross-target hostile-schedule suite, portable public workflow, release/registry automation, QOI policy, or unrelated quality-lane stages.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& {
$ErrorActionPreference = 'Stop'
& moon -C modules/mb-image info --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
. ./scripts/quality/Assert-Policy.ps1
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Assert-PngFoundationPolicy -PolicyPath policy/foundation.json
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Assert-PngQualificationNegativeFixtures -PolicyPath policy/foundation.json
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
& moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk encoder*'
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}"</automated>
  </verify>
  <done>The generated MBTI and policy are byte-for-byte semantically aligned for the approved PngChunk encoder API, policy negatives reject missing or extra stream declarations, and the focused native behavior suite plus direct policy checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Immutable ImageView → PngChunkEncoder::new | Caller-selected image, limits, and budget enter the canonical preflight boundary before any byte is exposed. |
| PngChunkEncoder → current MutByteLease | A callback-scoped caller-owned mutable destination is writable only during one pull and must not be retained. |
| PngChunkEncoder → PngEncodeMachine | Public accounting and terminal state must preserve the private machine's canonical pending-byte and acknowledgment protocol. |
| Source declarations → public MBTI policy | A generated public surface must not silently grow beyond the approved candidate API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | `PngChunkEncoder::pull` byte transfer | high | mitigate | Present exactly one private byte, write it to the current lease, then acknowledge it; update counters only after both succeed and compare concatenated output to eager bytes. |
| T-30-02 | Information Disclosure / Tampering | Caller `MutByteLease` lifetime | high | mitigate | Store no lease, view, owner, or output buffer; use only the pull parameter and prove post-callback owner mutation/reuse cannot affect later output. |
| T-30-03 | Denial of Service | Constructor admission | high | mitigate | Delegate once to Phase 29's checked capability, dimension, limit, disposition, and budget preflight before exposing a public encoder or accepting a destination. |
| T-30-04 | Tampering / Repudiation | Terminal lifecycle | medium | mitigate | Persist original errors unchanged and persist Finished; terminal pulls have zero progress and do not touch a supplied destination. |
| T-30-05 | Tampering | Candidate public interface | medium | mitigate | Regenerate MBTI, exact-compare policy, and extend required/negative public-type checks for the approved chunk encoder family. |
</threat_model>

<verification>
- RED/GREEN contract suite: `moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk encoder*'`.
- Public interface and fail-closed policy: `moon -C modules/mb-image info --target all --frozen`; `pwsh -NoProfile -Command ". ./scripts/quality/Assert-Policy.ps1; Assert-PngFoundationPolicy -PolicyPath policy/foundation.json; Assert-PngQualificationNegativeFixtures -PolicyPath policy/foundation.json"`.
- Final focused Phase 30 regression: `moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk encoder*'`.
</verification>

<success_criteria>
- A public caller can make deterministic empty, one-byte, and short irregular lease pulls, receive exact `written` and `total_written`, and collect the sole canonical eager-equivalent PNG without a retained destination capability.
- Constructor reuses the private Phase 29 admission/canonical machine; public output does not introduce a second emitter, output staging allocation, altered eager Writer path, or changed compression/colour semantics.
- The final successful pull and all later pulls report zero additional bytes plus sticky Finished; a typed failed pull and all later pulls report zero additional bytes plus the identical stored CoreError.
- The public MBTI policy permits exactly PngChunkEncoder/PngChunkPullOutcome/PngChunkPullResult alongside the existing PNG API and rejects missing or unapproved stream-surface changes.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Drain one canonical eager-equivalent PNG through arbitrary caller-owned mutable buffers with exact progress and sticky terminals. | 30-01 | COVERED | Tasks 1-2 implement and execute focused empty/tiny/short-irregular pull, parity, ownership, and terminal evidence. |
| REQ | PNGE-02 | Exact deterministic progress through caller-owned output, emitted once without retained buffers or byte loss/duplication. | 30-01 | COVERED | Tasks 1-2 specify caller-lease transfer, acknowledgement ordering, accounting, aggregate eager parity, and no-retention evidence. |
| REQ | PNGE-03 | Eager-equivalent canonical bytes and failure behavior; completion and failure stay sticky with no later output. | 30-01 | COVERED | Tasks 1-2 preserve machine preflight/bytes and test repeated Finished and original Failed(CoreError) outcomes; Task 3 freezes the public contract. |
| RESEARCH | — | Keep PngEncodeMachine as the only preflight and byte source. | 30-01 | COVERED | Task 2 requires one direct constructor delegation and forbids a duplicate emitter, staging, and duplicated preflight. |
| RESEARCH | — | Use callback-scoped MutByteLease only for the current pull. | 30-01 | COVERED | Tasks 1-2 require fresh-owner collection/mutation tests and prohibit retained leases/views/owners. |
| RESEARCH | — | `PngChunkPullOutcome`/`PngChunkPullResult`, sticky Finished and original Failed. | 30-01 | COVERED | Task 2 chooses and defines the recommended established naming and exact terminal semantics; Task 3 records them in MBTI policy. |
| RESEARCH | — | Exact interface-policy registration and fail-closed negatives. | 30-01 | COVERED | Task 3 regenerates the interface and changes only PNG required/negative policy assertions. |
| CONTEXT | — | Public type is PngChunkEncoder; PNGE-02/03 only; preserve Phase 29 machine; pure MoonBit/no packages or FFI. | 30-01 | COVERED | Task 2 implements every locked scope constraint; Tasks 1 and 3 provide focused evidence and policy without external dependencies. |
| CONTEXT | — | Four-target hostile schedules, broad resource parity, and public decode → operation → chunk encode stay in Phase 31. | 30-01 | COVERED (excluded) | Every task expressly excludes those Phase 31 deliverables while retaining the focused Phase 30 native contract tests required for this public API. |

<output>
Create `.planning/phases/30-public-png-chunk-encoder/30-01-SUMMARY.md` when done.
</output>
