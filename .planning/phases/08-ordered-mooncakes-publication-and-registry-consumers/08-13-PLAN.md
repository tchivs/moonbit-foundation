---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "13"
type: execute
wave: 13
depends_on: ["08-12"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "The exact r4-capable committed HEAD is pushed before r4 creation; origin/main, annotated peel, LF clone, and locator bind one SHA."
    - "A new GUID ExecutionRoot/StateRoot is used; no prior attempt state or fixed handoff is reused."
    - "Attempt-zero/r1/r2/r3 are revalidated and all four digests/set bind fresh r4 preparation, packet, receipt, authority, and handoff."
    - "Exact-existing completes automatically without packet/receipt; absent returns a structured dynamic checkpoint:human-action and only the user's same-checkpoint-turn verbatim literal authorize-core may resume; no automation may approve it and no branch calls PublishOne."
    - "Fresh continuation reloads every displayed untrusted recovery input before receipt/handoff."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r4"
      provides: "Annotated r4 exact boundary"
    - path: "%TEMP%/mnf-phase08-r4-handoff.json"
      provides: "Eligible r4 AuthorityUnion handoff"
  key_links:
    - from: "four histories"
      to: "r4 union"
      via: "individual/set digests"
      pattern: "historical_r3_sha256"
    - from: "untrusted checkpoint"
      to: "literal receipt"
      via: "fresh disk/remote/live revalidation"
      pattern: "untrusted_recovery_inputs"
---

<objective>Establish the exact LF-clean r4 boundary and produce one non-mutating four-history AuthorityUnion.

Purpose: Cross the next live boundary without publishing after r3's pre-run parity failure.
Output: Exact r4 tag/clone/state, exclusive authority, optional receipt, and fixed handoff.</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-workflow-receipt-input.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-12-SUMMARY.md
@release/qualification/phase-08-handoff-schema.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fail-close four histories and empty r4 namespace</name>
  <files>None (read-only local, GitHub, registry, refs, runs, and temp checks)</files>
  <action>Require 08-11/12 summaries and owned paths committed/clean without touching unrelated dirt. Verify immutable attempt-zero, r1, r2, and r3 peeled to 67b1fbc9dd62288d19018c46a44c1e3293212b76. Prove r3 PrepareAttempt+confirmed_absent then pre-run exact14 receipt parity failure, with no hosted run/mutation/authority. Verify r4 absent locally/remotely and `%TEMP%/mnf-phase08-r4-handoff.json` absent as file/directory. Any collision is preserved and stops; never overwrite/delete/reuse it.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r4-handoff.json'; if(Test-Path -LiteralPath $h){throw 'P08-R4-HANDOFF-COLLISION'}; if(@(git ls-remote --tags origin refs/tags/modules-v0.1.0-r4 'refs/tags/modules-v0.1.0-r4^{}').Count -ne 0){throw 'P08-R4-EXISTS'}; $r3=@(git ls-remote --tags origin 'refs/tags/modules-v0.1.0-r3^{}'); if($r3.Count -ne 1 -or (@($r3[0]-split '\s+'))[0] -cne '67b1fbc9dd62288d19018c46a44c1e3293212b76'){throw 'P08-R3-HISTORY'} }"</automated></verify>
  <done>Four histories are intact and r4/fixed handoff are absent; collisions stop without mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Create LF r4 boundary and eligible AuthorityUnion</name>
  <files>None (exact push, annotated r4, unique state, sanitized artifacts, optional receipt, fixed handoff)</files>
  <action>Push exact committed HEAD containing planning and r4 static implementation; require remote equality. Create GUID-unique ExecutionRoot/StateRoot; clone with `git -c core.autocrlf=false clone --no-tags`, set local false, detach exact SHA, verify LF bytes/clean status/blobs/empty state. InitializeBoundary with UTC digest, create/push annotated r4 without force, verify peel, and PrepareAttempt with all four histories/set and fresh root/genesis/prepared/index/store.

Run clone-rooted ObserveOnly mb-core. Exact performs structured observation plus one-node four-target/native cold proof, indexes them, selects packet/receipt-free exact authority, validates union, then writes fixed handoff. Absent alone runs HostedPreflight and PublisherDryRun using the exact controller/workflow 14-field contract, assembles packet, then returns a structured `dynamic checkpoint:human-action` accepting only literal `authorize-core` or `stop`. `autonomous: true`, executor auto mode, workflow auto-advance, general prior approval, login success, push permission, defaults, heuristics, or any agent/subagent may not approve, select, infer, normalize, quote, replay, or synthesize `authorize-core`. Authorization exists only when the user responds in that exact checkpoint's current continuation turn with the verbatim case-sensitive literal `authorize-core`; any surrounding text or response from another turn/source is non-authorizing and stops. Literal `stop` terminates the plan and writes neither receipt nor handoff. Display under `untrusted_recovery_inputs`: ExecutionRoot, StateRoot, BoundaryLocatorPath, BoundaryLocatorSha256, ActiveAttemptPath, ActiveAttemptFileSha256, ActiveAttemptLocatorSha256, ArtifactRoot, PacketPath, PacketSha256, ReleaseRef, BoundarySha, SourceSha, AttemptZeroHistorySha256, R1HistorySha256, R2HistorySha256, R3HistorySha256, HistoricalAttemptSetSha256, PreparedManifestSha256, RootIntentSha256, IntentSha256. Mark all sanitized but untrusted and show no receipt/handoff yet.

Fresh authorize-core continuation trusts no memory: reload locator/active/packet/four histories/prepared/root/current/preflight/dry actor/new absence; revalidate schemas, file/content/locator/set digests, paths/cross-bindings, clean LF HEAD/blobs, remote r4 peel, immutable histories, actor tchivs, exact14 packet/receipt propagation, dry success/credential removal, and immediately fresh absent. Any drift/ambiguity stops. Only then persist/reload receipt, update/reload active attempt, validate mutation union, and create fixed handoff first time. Stop or mismatch/unknown/disagreement/timeout writes no receipt/handoff. Never PublishOne. Record all paths/digests/union/histories in summary.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r4-handoff.json'; if(-not(Test-Path -LiteralPath $p -PathType Leaf)){throw 'P08-R4-HANDOFF-MISSING'}; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; $x=[string]$h.execution_root; $s=Join-Path $x 'release/qualification/phase-08-handoff-schema.json'; if(-not((Get-Content -LiteralPath $p -Raw)|Test-Json -SchemaFile $s -ErrorAction Stop)){throw 'P08-R4-HANDOFF-SCHEMA'}; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -Handoff -HandoffPath $p }"</automated></verify>
  <done>One eligible four-history r4 union exists; mutation has literal receipt, exact none, and no publication occurred.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Ambient checkout to LF r4 clone | Only exact pushed bytes execute. |
| Four histories to r4 state | Prior attempts are evidence only. |
| Checkpoint to fresh continuation | All recovery values are untrusted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-13-01 | Spoofing | clone/tag | critical | mitigate | Exact remote/peel/blob equality, no-tags LF clone. |
| T-08-13-02 | Tampering | histories/state | critical | mitigate | Four individual/set digests, unique roots, no reuse. |
| T-08-13-03 | Elevation of Privilege | continuation | critical | mitigate | Structured dynamic human-action gate accepts only the user's same-turn verbatim literal; automation/default/inference is forbidden, followed by full fresh reload/revalidation. |
| T-08-13-04 | Information Disclosure | checkpoint | high | mitigate | Closed sanitized fields only. |
| T-08-13-05 | Repudiation | r3 termination | high | mitigate | Exact pre-run parity failure/no-run/no-mutation digest evidence. |
</threat_model>

<verification>Validate four histories, exact r4 boundary/LF state, complete untrusted checkpoint, fresh continuation, receipt union, fixed handoff, and no PublishOne.</verification>
<success_criteria>08-13 ends only with a valid r4 exact-existing or literal-receipt mutation AuthorityUnion.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-13-SUMMARY.md` when done.</output>
