---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "07"
type: execute
wave: 7
depends_on: ["08-06"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Completed 08-05 and 08-06 summaries plus ROADMAP/STATE/COVERAGE tracking are committed before one exact local HEAD is pushed to origin/main; unrelated dirty user files are neither staged, stashed, destroyed, nor required to be clean."
    - "Remote main is verified byte-for-byte at the pushed SHA before a fresh disposable clone is created; its exact path and boundary SHA are persisted in the durable attempt locator, and every later helper/selector runs by absolute path under that clone."
    - "The old tag/run remain terminal; annotated r1 is created once at the clean boundary; fresh r1 intent/root/genesis/prepared identities are produced without reusing attempt zero (D-01, D-15)."
    - "Core absence alone proceeds through secret-free HostedPreflight, actor/dry-only PublisherDryRun, MutationAuthorizationPacket, and literal authorize-core; core exact instead produces a validated packet-free ExactExistingAuthority and completes without an authorization checkpoint (D-03, D-04, D-07, D-18, D-19)."
    - "Task 3 accepts exactly one core-entry authority: MutationAuthorizationPacket or ExactExistingAuthority, never both or neither; no PublishOne occurs in this plan."
  artifacts:
    - path: "refs/remotes/origin/main"
      provides: "Exact pushed completion boundary containing Plans 05 and 06 summaries/tracking"
    - path: "<system-temp>/mnf-phase08/boundary-locator.json"
      provides: "Durable execution_root and boundary_sha locator"
    - path: "refs/tags/modules-v0.1.0-r1"
      provides: "Immutable annotated r1 ref peeled to the verified boundary"
    - path: "<artifact-root>/authorization/authorize-core.json or <artifact-root>/authorities/mb-core/exact-existing.json"
      provides: "Exclusive core-entry MutationAuthorizationPacket or ExactExistingAuthority"
  key_links:
    - from: "committed 08-05/08-06 completion HEAD"
      to: "origin/main"
      via: "exact SHA refspec push and ls-remote equality"
      pattern: "refs/heads/main"
    - from: "boundary-locator.json"
      to: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      via: "ExecutionRoot HEAD/blob validation immediately before every mode"
      pattern: "execution_root"
    - from: "ObserveOnly core outcome"
      to: "core-entry authority union"
      via: "absent uses preflight/dry-run packet assembly; exact uses observation/cold ExactExistingAuthority selection"
      pattern: "MutationAuthorizationPacketPath|ExactExistingAuthorityPath"
---

<objective>
Push the completed static boundary, establish and persist the clean execution clone, create r1, observe core, and either stop at literal absence-path authorization or close exact-existing core without mutation authorization.

Purpose: Ensure no live operation can select unpushed or ambient working-tree code and preserve the explicit first-mutation decision.
Output: Verified remote boundary, durable clean-clone locator, r1 tag, fresh attempt store, and exactly one validated core-entry authority.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/hosted-toolchain-setup-failure.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-06-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
@release/qualification/phase-08-authority-schema.json

<interfaces>
The only permitted source push is the already committed current HEAD to refs/heads/main. It stages nothing and examines cleanliness only for Plan 05/06 owned source and tracking paths. The durable locator is initialized by the helper from the clean clone and records execution_root plus boundary_sha. Every subsequent helper or selector is invoked as `&amp; (Join-Path $ExecutionRoot 'scripts/quality/...')`; the invoked script validates ExecutionRoot HEAD and its own/required blobs against the locator immediately before use.
The old protected tag remains at 198436a45b7403a3c28c98d5fa0d5ed6a958455f and run 29652468948/1 remains terminal setup failure. The new annotated tag is refs/tags/modules-v0.1.0-r1 at the verified completion boundary.
The closed branch order is: ObserveOnly core first; `absent` then runs HostedPreflight, PublisherDryRun, assembles MutationAuthorizationPacket, and reaches literal `authorize-core`; `exact` runs the Plan 06 production observer/cold template and `SelectExactExistingAuthority`, writes no packet, and completes Plan 07; `mismatch` records terminal forward correction and stops; `unknown`, `disagreement`, and `timeout` stop. `active-attempt.json` contains exactly one of `mutation_authorization_packet_path` or `exact_existing_authority_path` and explicitly nulls the other.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the exact completion HEAD and persist the clean execution boundary</name>
  <files>None (one exact main push, disposable clone, and system-temp locator only)</files>
  <action>First prove 08-05-SUMMARY.md and 08-06-SUMMARY.md exist at current HEAD and that the committed ROADMAP, STATE, and COVERAGE record eight plans with 05 and 06 complete and 07 next. Check staged/unstaged status only for the two plans' owned source files and their planning/tracking paths; fail if those scoped paths differ from HEAD. Do not require a globally clean worktree and do not stage, stash, reset, checkout, delete, or otherwise touch unrelated user files. Push the exact committed SHA directly to origin/main, verify the remote branch SHA, clone that remote into a new disposable directory, detach at the exact SHA, require empty clone status, and initialize the durable locator by invoking the helper from that clone.

PowerShell command contract:

    $RepoRoot=(git rev-parse --show-toplevel).Trim()
    if ($LASTEXITCODE) { exit $LASTEXITCODE }
    $LocalHead=(git -C $RepoRoot rev-parse HEAD).Trim()
    if ($LocalHead -notmatch '^[0-9a-f]{40}$') { throw 'P08-LOCAL-HEAD' }
    $RequiredCommitted=@('.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md','.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-06-SUMMARY.md','.planning/ROADMAP.md','.planning/STATE.md','.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md','release/qualification/phase-08-authority-schema.json')
    foreach($Path in $RequiredCommitted){ git -C $RepoRoot cat-file -e "${LocalHead}:$Path"; if($LASTEXITCODE){throw "P08-COMMITTED-MISSING: $Path"} }
    $RoadmapAtHead=(git -C $RepoRoot show "${LocalHead}:.planning/ROADMAP.md") -join "`n"; $StateAtHead=(git -C $RepoRoot show "${LocalHead}:.planning/STATE.md") -join "`n"; $CoverageAtHead=(git -C $RepoRoot show "${LocalHead}:.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md") -join "`n"
    if($RoadmapAtHead -notmatch '(?m)^\*\*Plans:\*\* 6/8 plans executed$' -or $RoadmapAtHead -notmatch '(?m)^- \[x\] 08-05-PLAN[.]md$' -or $RoadmapAtHead -notmatch '(?m)^- \[x\] 08-06-PLAN[.]md$' -or $RoadmapAtHead -notmatch '(?m)^- \[ \] 08-07-PLAN[.]md$'){throw 'P08-COMMITTED-ROADMAP'}
    if($StateAtHead -notmatch '(?m)^Plan: 7 of 8$' -or $StateAtHead -notmatch 'Completed 08-06-PLAN[.]md'){throw 'P08-COMMITTED-STATE'}
    if($CoverageAtHead -notmatch '08-01[.][.]08-08' -or $CoverageAtHead -notmatch 'exact-existing'){throw 'P08-COMMITTED-COVERAGE'}
    $ScopedPaths=@('release/qualification/phase-08-authority-schema.json','policy/release-control.json','release/intent/schema.json','release/prepared/schema.json','scripts/quality/New-ReleaseIntent.ps1','scripts/quality/ReleaseQualification.Common.ps1','scripts/quality/Invoke-ReleaseQualification.ps1','scripts/quality/Invoke-ReleasePublisher.ps1','scripts/quality/Invoke-MooncakesLiveMutation.ps1','scripts/quality/Test-ReleaseIntent.ps1','scripts/quality/Test-ReleasePublisherNegative.ps1','scripts/quality/Invoke-Phase08HostedRun.ps1','scripts/quality/Test-Phase08Qualification.ps1','scripts/quality/Test-Phase08LiveSeam.ps1','.github/workflows/publish-modules.yml','scripts/quality/Get-MooncakesObservation.ps1','scripts/quality/Test-MooncakesObservation.ps1','scripts/quality/Invoke-ColdRegistryConsumer.ps1','scripts/quality/Test-ColdRegistryConsumer.ps1') + $RequiredCommitted
    if (@(git -C $RepoRoot status --porcelain=v1 --untracked-files=all -- $ScopedPaths).Count -ne 0) { throw 'P08-PLAN-OWNED-DIRTY' }
    git -C $RepoRoot push origin "${LocalHead}:refs/heads/main"
    if ($LASTEXITCODE) { exit $LASTEXITCODE }
    $RemoteRows=@(git -C $RepoRoot ls-remote origin refs/heads/main | Where-Object{-not [string]::IsNullOrWhiteSpace($_)})
    if($RemoteRows.Count -ne 1 -or (@($RemoteRows[0]-split '\s+'))[0] -cne $LocalHead){throw 'P08-REMOTE-MAIN-SHA'}
    $ExecutionRoot=[IO.Path]::GetFullPath((Join-Path ([IO.Path]::GetTempPath()) ('mnf-phase08-boundary-'+[Guid]::NewGuid().ToString('N'))))
    git clone --no-tags https://github.com/tchivs/moonbit-foundation.git $ExecutionRoot
    if ($LASTEXITCODE) { exit $LASTEXITCODE }
    git -C $ExecutionRoot checkout --detach $LocalHead
    if ($LASTEXITCODE -or @(git -C $ExecutionRoot status --porcelain=v1 --untracked-files=all).Count -ne 0) { throw 'P08-CLEAN-CLONE' }
    $StateRoot=[IO.Path]::GetFullPath((Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08'))
    $Boundary=&amp; (Join-Path $ExecutionRoot 'scripts/quality/Invoke-Phase08HostedRun.ps1') -Mode InitializeBoundary -Repository tchivs/moonbit-foundation -Workflow publish-modules.yml -BoundarySha $LocalHead -ExecutionRoot $ExecutionRoot -StateRoot $StateRoot
    if ($LASTEXITCODE) { exit $LASTEXITCODE }

The helper writes `<StateRoot>/boundary-locator.json` atomically. Reload it and require its execution_root and boundary_sha equal the clean clone and exact remote main SHA before completing the task.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json'; $l=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; if((git -C ([string]$l.execution_root) rev-parse HEAD).Trim() -cne [string]$l.boundary_sha){throw 'P08-BOUNDARY-VERIFY'}; if(@(git -C ([string]$l.execution_root) status --porcelain=v1 --untracked-files=all).Count -ne 0){throw 'P08-BOUNDARY-DIRTY'} }"</automated></verify>
  <done>Both completed plan summaries/tracking are in the pushed SHA, origin/main equals that SHA, unrelated dirt was untouched, and the durable locator names a clean exact clone.</done>
</task>

<task type="auto">
  <name>Task 2: Create r1 and produce exactly one core-entry authority</name>
  <files>None (one immutable tag, hosted sanitized artifacts, and system-temp store only)</files>
  <action>Per D-01, D-03, D-04, D-07, and D-13 through D-19, reload the boundary locator and set `$ExecutionRoot` only from its validated value. Invoke every helper and selector through an absolute Join-Path under that root. Prove attempt-zero tag/run identity, prove r1 absent, create one annotated r1 tag at the boundary without force, push it, and verify the quoted peeled ref. Run PrepareAttempt, then ObserveOnly for core before any preflight, dry-run, packet, or mutation authorization. Use the exclusive closed switch below. Only `absent` may run HostedPreflight and PublisherDryRun and assemble a MutationAuthorizationPacket. `exact` must run the complete clone-rooted structured-surface observer/cold sequence from 08-06 Task 3, then `SelectExactExistingAuthority`; it must prove the exact record binds r1 ref/boundary SHA, clean root/current/prepared intent, observation/archive/manifest/checksum/docs, cold four-target/native sentinel, pinned toolchain, actor-free credential-free status, reducer, locator/root/index, and historical 29652468948/1 negative evidence, with mutation_authorization_required/used and publisher_dry_run_used all false. It writes no packet and reaches no authorize-core checkpoint. Mismatch records terminal forward-correction evidence; unknown/disagreement/timeout stop.

Command-shape contract after locator reload:

    $BoundaryLocator=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100
    $ExecutionRoot=[IO.Path]::GetFullPath([string]$BoundaryLocator.execution_root)
    $Hosted=Join-Path $ExecutionRoot 'scripts/quality/Invoke-Phase08HostedRun.ps1'
    $Selector=Join-Path $ExecutionRoot 'scripts/quality/Test-Phase08Qualification.ps1'
    $BoundarySha=[string]$BoundaryLocator.boundary_sha
    if((git -C $ExecutionRoot rev-parse HEAD).Trim() -cne $BoundarySha -or @(git -C $ExecutionRoot status --porcelain=v1 --untracked-files=all).Count -ne 0){throw 'P08-EXECUTION-BOUNDARY'}
    if(@(git -C $ExecutionRoot ls-remote --tags origin refs/tags/modules-v0.1.0-r1 'refs/tags/modules-v0.1.0-r1^{}'|Where-Object{-not [string]::IsNullOrWhiteSpace($_)}).Count -ne 0){throw 'P08-R1-EXISTS'}
    git -C $ExecutionRoot tag -a modules-v0.1.0-r1 $BoundarySha -m 'MNF modules v0.1.0 initial attempt r1'
    if($LASTEXITCODE){exit $LASTEXITCODE}; git -C $ExecutionRoot push origin refs/tags/modules-v0.1.0-r1
    if($LASTEXITCODE){exit $LASTEXITCODE}
    $PeeledRows=@(git -C $ExecutionRoot ls-remote --tags origin 'refs/tags/modules-v0.1.0-r1^{}'|Where-Object{-not [string]::IsNullOrWhiteSpace($_)})
    if($PeeledRows.Count -ne 1){throw 'P08-R1-PEELED-COUNT'}; $PeeledFields=@($PeeledRows[0]-split '\s+')
    if($PeeledFields.Count -ne 2 -or $PeeledFields[0] -cne $BoundarySha -or $PeeledFields[1] -cne 'refs/tags/modules-v0.1.0-r1^{}'){throw 'P08-R1-PEELED-SHA'}
    $Prepared=&amp; $Hosted -Mode PrepareAttempt -BoundaryLocatorPath ([string]$BoundaryLocator.locator_path) -ReleaseRef refs/tags/modules-v0.1.0-r1 -HistoricalRunId 29652468948 -HistoricalRunAttempt 1 -HistoricalReleaseRef refs/tags/modules-v0.1.0 -HistoricalSourceSha 198436a45b7403a3c28c98d5fa0d5ed6a958455f
    if($LASTEXITCODE){exit $LASTEXITCODE}
    $Common=@{BoundaryLocatorPath=[string]$BoundaryLocator.locator_path;LocatorPath=[string]$Prepared.locator_path;ArtifactRoot=[string]$Prepared.artifact_root;ReleaseRef='refs/tags/modules-v0.1.0-r1';SourceSha=$BoundarySha;RootIntentSha256=[string]$Prepared.root_intent_sha256;IntentSha256=[string]$Prepared.intent_sha256;PreparedManifestSha256=[string]$Prepared.prepared_manifest_sha256;TargetModule='mb-core'}
    $Observed=&amp; $Hosted -Mode ObserveOnly @Common -ObservationPurpose CoreEntry
    if($LASTEXITCODE){exit $LASTEXITCODE}
    if([string]$Observed.outcome -ceq 'absent'){
      $Preflight=&amp; $Hosted -Mode HostedPreflight @Common
      if($LASTEXITCODE){exit $LASTEXITCODE}
      $Dry=&amp; $Hosted -Mode PublisherDryRun @Common -PreflightRecordPath ([string]$Preflight.artifact_path)
      if($LASTEXITCODE){exit $LASTEXITCODE}
      $Packet=&amp; $Hosted -Mode AssembleAuthorizationPacket @Common -HistoricalRecordPath ([string]$Prepared.historical_record_path) -PreflightRecordPath ([string]$Preflight.artifact_path) -DryRunRecordPath ([string]$Dry.artifact_path) -ObservationRecordPath ([string]$Observed.artifact_path)
      if($LASTEXITCODE){exit $LASTEXITCODE}; $PacketPath=[IO.Path]::GetFullPath([string]$Packet.packet_path); $ExactAuthorityPath=''
      &amp; $Selector -MutationAuthorizationPacket -LocatorPath ([string]$Prepared.locator_path) -ArtifactRoot ([string]$Prepared.artifact_root) -MutationAuthorizationPacketPath $PacketPath
      if($LASTEXITCODE){exit $LASTEXITCODE}
    } elseif([string]$Observed.outcome -ceq 'exact') {
      $Surface=&amp; $Hosted -Mode MaterializePublicSurface @Common -ObservationPhase exact-existing -PriorAuthorityRecordPath ''
      if($LASTEXITCODE){exit $LASTEXITCODE}; $FixturePath=[IO.Path]::GetFullPath([string]$Surface.fixture_path)
      $PolicyPath=Join-Path $ExecutionRoot 'policy/phase-08-distribution.json'; $ObservationSchemaPath=Join-Path $ExecutionRoot 'release/registry/module-observation-schema.json'; $ColdSchemaPath=Join-Path $ExecutionRoot 'release/consumers/proof-schema.json'
      $ObservationPath=[IO.Path]::GetFullPath((Join-Path ([string]$Prepared.artifact_root) 'observations/mb-core/exact-existing/observation.json'))
      &amp; (Join-Path $ExecutionRoot 'scripts/quality/Get-MooncakesObservation.ps1') -FixturePath $FixturePath -OutputPath $ObservationPath -Module mb-core -PolicyPath $PolicyPath -SchemaPath $ObservationSchemaPath
      if($LASTEXITCODE){exit $LASTEXITCODE}; $ObservationJson=Get-Content -LiteralPath $ObservationPath -Raw; if(-not ($ObservationJson|Test-Json -SchemaFile $ObservationSchemaPath -ErrorAction Stop)){throw 'P08-CORE-OBSERVATION-SCHEMA'}; $ObservationRecord=$ObservationJson|ConvertFrom-Json -Depth 100; $ObservationDigest=(Get-FileHash -LiteralPath $ObservationPath -Algorithm SHA256).Hash.ToLowerInvariant()
      &amp; $Hosted -Mode IndexSanitizedArtifact @Common -ArtifactKind Observation -ArtifactPath $ObservationPath -ArtifactFileSha256 $ObservationDigest -ArtifactContentSha256 ([string]$ObservationRecord.content_sha256) -PriorAuthorityRecordPath ''
      if($LASTEXITCODE){exit $LASTEXITCODE}; $ColdPath=[IO.Path]::GetFullPath((Join-Path ([string]$Prepared.artifact_root) 'cold/mb-core/exact-existing/proof.json')); $ColdArgs=@{Module='mb-core';OutputPath=$ColdPath;ToolchainRoot=[string]$Prepared.toolchain_root;ObservationFixturePath=$FixturePath;PolicyPath=$PolicyPath;SchemaPath=$ColdSchemaPath}; if(-not [string]::IsNullOrWhiteSpace([string]$Prepared.native_toolchain_bin)){$ColdArgs.NativeToolchainBin=[string]$Prepared.native_toolchain_bin}
      &amp; (Join-Path $ExecutionRoot 'scripts/quality/Invoke-ColdRegistryConsumer.ps1') @ColdArgs
      if($LASTEXITCODE){exit $LASTEXITCODE}; $ColdJson=Get-Content -LiteralPath $ColdPath -Raw; if(-not ($ColdJson|Test-Json -SchemaFile $ColdSchemaPath -ErrorAction Stop)){throw 'P08-CORE-COLD-SCHEMA'}; $ColdRecord=$ColdJson|ConvertFrom-Json -Depth 100; if((@($ColdRecord.targets.name)-join ',') -cne 'js,wasm,wasm-gc,native' -or [string]$ColdRecord.targets[3].runtime -cne 'pass'){throw 'P08-CORE-COLD-NATIVE'}; $ColdDigest=(Get-FileHash -LiteralPath $ColdPath -Algorithm SHA256).Hash.ToLowerInvariant()
      &amp; $Hosted -Mode IndexSanitizedArtifact @Common -ArtifactKind ColdProof -ArtifactPath $ColdPath -ArtifactFileSha256 $ColdDigest -ArtifactContentSha256 ([string]$ColdRecord.content_sha256) -ObservationRecordPath $ObservationPath -PriorAuthorityRecordPath ''
      if($LASTEXITCODE){exit $LASTEXITCODE}; $Exact=&amp; $Hosted -Mode SelectExactExistingAuthority @Common -ObservationRecordPath $ObservationPath -ColdProofPath $ColdPath -HistoricalRecordPath ([string]$Prepared.historical_record_path) -PriorAuthorityRecordPath ''
      if($LASTEXITCODE){exit $LASTEXITCODE}; $ExactAuthorityPath=[IO.Path]::GetFullPath([string]$Exact.authority_path); $PacketPath=''
      &amp; $Selector -ExactExistingAuthority -LocatorPath ([string]$Prepared.locator_path) -ArtifactRoot ([string]$Prepared.artifact_root) -ExactExistingAuthorityPath $ExactAuthorityPath
      if($LASTEXITCODE){exit $LASTEXITCODE}
    } elseif([string]$Observed.outcome -ceq 'mismatch') { throw 'P08-CORE-FORWARD-CORRECTION-REQUIRED' }
    else { throw 'P08-CORE-OBSERVATION-STOP' }
    &amp; $Selector -AuthorityUnion -LocatorPath ([string]$Prepared.locator_path) -ArtifactRoot ([string]$Prepared.artifact_root) -MutationAuthorizationPacketPath $PacketPath -ExactExistingAuthorityPath $ExactAuthorityPath
    if($LASTEXITCODE){exit $LASTEXITCODE}

Persist both nullable path fields in active-attempt.json and their authority-union selector digest. The executor may correct harmless PowerShell line wrapping but must not change branch semantics or replace clone-rooted commands with ambient paths.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $b=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100; $a=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/active-attempt.json') -Raw|ConvertFrom-Json -Depth 100; $x=[IO.Path]::GetFullPath([string]$b.execution_root); $p=[string]$a.mutation_authorization_packet_path; $e=[string]$a.exact_existing_authority_path; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -AuthorityUnion -LocatorPath ([string]$a.locator_path) -ArtifactRoot ([string]$a.artifact_root) -MutationAuthorizationPacketPath $p -ExactExistingAuthorityPath $e }"</automated></verify>
  <done>r1 peels to the pushed boundary and exactly one validated core-entry authority exists: an absent-path mutation packet or a packet-free exact-existing authority; no PublishOne occurred.</done>
</task>

<task type="auto">
  <name>Task 3: Close exact core or authorize absent core</name>
  <files>None (conditional completion evidence or dynamic authorization checkpoint only)</files>
  <action>Per D-04 and D-07, reload the durable boundary and active-attempt locators, derive every path from the validated ExecutionRoot, require its clean HEAD to equal boundary_sha, require the remote peeled refs/tags/modules-v0.1.0-r1 SHA to equal that boundary, and run the clone-rooted AuthorityUnion selector immediately before branching. Reject both/neither, stale, mixed, or ambiguous authority input.

If AuthorityUnion reports source `exact-existing`, rerun final ExactExistingAuthority verification against the authority schema and require the actor-free, credential-free, mutation_authorization_required=false, mutation_authorization_used=false, and publisher_dry_run_used=false flags plus exact r1 ref/boundary SHA, root/current/prepared intent, observation/archive/manifest/checksum/docs, cold four-target/native sentinel, pinned toolchain, reducer, locator/root/index, and historical 29652468948/1 bindings. Record exactly `Core entry authority: exact-existing` in the normal 08-07 completion evidence, create and commit the normal 08-07-SUMMARY through the execute-plan completion flow, and complete 08-07 automatically. This branch returns no checkpoint, requests no authorize-core signal, and never calls PublishOne.

If AuthorityUnion reports source `mutation-authorization-packet`, revalidate the packet against the authority schema and require its exact packet path and digest plus r1 ref, boundary SHA, root/current/prepared intent, history, preflight, actor, dry-run, and absence-observation bindings. On the initial execution, stop and return the following structured executor state exactly as a dynamic checkpoint contract: heading `## CHECKPOINT REACHED`; a Completed Tasks table for Tasks 1 and 2 with commit hashes/files; Current Task `Task 3: Close exact core or authorize absent core`; Checkpoint Details containing `type: human-action`, the verified packet/ref/SHA/digest display, the statement that Plan 08 alone owns PublishOne, and the statement that no registry mutation has occurred; Awaiting containing only `Enter exactly authorize-core for this unchanged packet, or stop.` This is not a decision checkpoint and has no default option. General auto mode, first-option selection, prior permissions, general approval, login, environment setup, `approved`, `done`, `yes`, or any response other than literal `authorize-core` or `stop` cannot satisfy it and must not be normalized into authorization.

When a fresh continuation agent receives literal `stop`, preserve the evidence and leave Task 3 and Plan 08-07 incomplete without writing a completion SUMMARY. When it receives literal `authorize-core`, reload both locators and all packet files from disk, re-prove clean exact ExecutionRoot HEAD, remote r1 peeled ref/SHA, authority-schema validity, AuthorityUnion source `mutation-authorization-packet`, and equality of every displayed packet/ref/SHA/digest and root/current/prepared/history/preflight/actor/dry-run/absence binding to the checkpoint state. Any drift stops. Only after this revalidation, record exactly `Core entry authority: mutation-packet` and `Operator decision: authorize-core` in the normal 08-07 completion evidence, create and commit the normal 08-07-SUMMARY through the execute-plan completion flow, and complete 08-07. Neither the initial agent nor the continuation agent may call PublishOne; 08-08 exclusively owns publication.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { $b=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100; $a=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/active-attempt.json') -Raw|ConvertFrom-Json -Depth 100; $x=[IO.Path]::GetFullPath([string]$b.execution_root); if((git -C $x rev-parse HEAD).Trim() -cne [string]$b.boundary_sha){throw 'P08-CHECKPOINT-BOUNDARY'}; $p=[string]$a.mutation_authorization_packet_path; $e=[string]$a.exact_existing_authority_path; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -AuthorityUnion -LocatorPath ([string]$a.locator_path) -ArtifactRoot ([string]$a.artifact_root) -MutationAuthorizationPacketPath $p -ExactExistingAuthorityPath $e }"</automated>
  </verify>
  <done>Exact-existing authority completes automatically with normal committed evidence and no checkpoint; an absent-core packet completes only after a non-auto-bypassable human-action checkpoint receives literal authorize-core and a continuation agent revalidates the unchanged packet/ref/SHA/digests; stop leaves the plan incomplete; PublishOne remains uncalled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User worktree to origin/main | Only the committed exact HEAD is pushed; unrelated dirt is outside the scoped check. |
| Remote main to execution clone | Remote SHA equality and clean detached checkout establish the live source boundary. |
| Clean clone to hosted services | Only blob-verified clone-rooted helpers may dispatch or select evidence. |
| Core-entry authority union to operator | Exact-existing closes without authorization; only one exact absence packet can reach the first-mutation decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07-01 | Tampering | completion push/clone | critical | mitigate | Exact SHA refspec, remote verification, clean detached clone, durable SHA/root locator, and scoped dirty checks. |
| T-08-07-02 | Elevation of Privilege | ambient helper bypass | critical | mitigate | Every command uses clone-rooted Join-Path and immediate HEAD/blob validation. |
| T-08-07-03 | Spoofing | actor/ref/run/artifact | critical | mitigate | Exact actor projection, peeled r1 SHA, unique indexed hosted artifacts, and packet selector. |
| T-08-07-04 | Information Disclosure | hosted credential/output | critical | mitigate | Secret-free preflight, dry-run child isolation, raw exclusion, and no PublishOne. |
| T-08-07-05 | Repudiation | operator choice | high | mitigate | Revalidated packet plus one literal recorded decision. |
</threat_model>

<verification>
Verify committed summaries/tracking, exact origin/main SHA, clean durable execution clone, immutable old/r1 refs, hosted evidence, packet selector, and absence of PublishOne.
</verification>

<success_criteria>
The forward attempt is bound to a pushed clean source; exact-existing core closes without mutation authority, while an absent core remains gated by literal authorization for one exact packet.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-07-SUMMARY.md` recording boundary/clone/ref/store/authority-union facts only after an automatic exact-existing completion or literal authorize-core continuation. Record `Core entry authority: exact-existing` with no operator-decision line, or `Core entry authority: mutation-packet` with exactly one line `Operator decision: authorize-core`. Literal stop leaves 08-07 incomplete and creates no completion SUMMARY.
</output>
