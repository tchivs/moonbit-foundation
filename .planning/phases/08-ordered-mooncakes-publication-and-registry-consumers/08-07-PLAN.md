---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "07"
type: execute
wave: 7
depends_on: ["08-06"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "refs/tags/modules-v0.1.0-r2 is the sole current initial forward-retry: module versions remain 0.1.0, intent_kind is initial, correction_sequence is 0, root equals current intent, and predecessor is null (D-01, D-07, D-15)."
    - "Attempt zero and annotated r1 at 09548df948f58ec1bdfff7494757596c03e4c9bd are immutable terminal-negative history and cannot validate as current intent, prepared input, authority, receipt, or handoff."
    - "MutationAuthorizationReceipt and r2 handoff have closed schemas, canonical UTC timestamps, explicit digest fields, and mutually exclusive packet+receipt versus packet-free exact-existing authority variants."
    - "Prepared r2 identity binds both historical-negative content digests without importing an old root, genesis, journal, locator, index, artifact store, or predecessor."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r2 current profile and exact attempt-zero/r1 terminal-negative history"
    - path: "release/qualification/phase-08-authorization-receipt-schema.json"
      provides: "Closed literal authorize-core receipt contract"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "Closed digest-bound live handoff contract"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r2 prepared bundle bound to both historical negatives"
  key_links:
    - from: "release-control r2 current profile"
      to: "intent and prepared schemas/generators"
      via: "one canonical initial ref/root/sequence/no-predecessor contract"
      pattern: "modules-v0.1.0-r2"
    - from: "authorization receipt schema"
      to: "handoff authority union"
      via: "packet digest, r2 ref, boundary SHA, literal response, UTC time, and receipt digest"
      pattern: "authorization_receipt_sha256"
---

<objective>
Define and test the r2 initial forward-retry, historical-negative bindings, authorization receipt, and deterministic handoff contracts.

Purpose: Make the forward retry and its later human authorization mechanically unambiguous before hosted or live work exists.
Output: Closed r2 policy/schemas/generators plus credential-free positive and adversarial tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/prepare-attempt-contract-mismatch.md
@.planning/debug/resolved/phase08-live-locator-time-digest.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@policy/release-control.json
@release/intent/schema.json
@release/prepared/schema.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the r2 attempt-family policy and initial intent</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r2 validates as current initial forward-retry with version 0.1.0, sequence zero, root=current, and no predecessor/correction evidence.
    - Attempt zero and r1 retain exact ref/source/run-presence/mutation/authority/reason facts as terminal-negative history.
    - r1 substitution, old StateRoot identity, a predecessor, sequence drift, or correction-lane fields fail closed.
  </behavior>
  <action>Per D-01, D-07, and D-15, replace the r1 current profile with an explicit attempt family: attempt zero and r1 are immutable terminal-negative entries, r2 is the only current initial forward-retry, and correction-N remains reserved for published-content mismatch with advanced versions. Update policy, initial intent/prepared ref constraints, generator validation, and focused tests. Preserve attempt-zero run 29652468948/1 and r1 SHA 09548df948f58ec1bdfff7494757596c03e4c9bd with no hosted run, mutation, or authority. Commit this five-file unit atomically after tests pass.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r2 is the only valid current initial retry and both prior attempts are immutable terminal-negative policy facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define closed authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Mutation receipt has only schema_version, release_ref r2, boundary_sha, packet_sha256, response authorize-core, created_at_utc in canonical UTC Z form, and receipt_sha256.
    - Handoff has closed paths/digests for boundary locator, active attempt, artifact/index, both history records, exclusive authority variant, optional packet/receipt or exact-existing authority, created_at_utc, and handoff_sha256.
    - Mutation variant requires packet and receipt path/digest; exact-existing variant forbids packet and receipt; stop is never an eligible variant.
  </behavior>
  <action>Per D-03, D-04, D-07, D-14, and D-18, create closed receipt and handoff JSON schemas and extend schema-focused fixtures in Test-ReleaseIntent. Canonicalize timestamps to UTC Z before content hashing. Require the receipt to bind exact packet digest, r2 ref, boundary SHA, literal authorize-core, and creation evidence. Require the handoff to bind its exact files by SHA-256, both terminal-negative histories, and exactly one eligible union variant. Reject unknown properties, relative/out-of-root paths, stale/tampered digests, packet without receipt, receipt on exact-existing, and stop eligibility. Commit this four-file schema/test unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$paths=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($p in $paths){$null=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Receipt and handoff contracts are closed, digest-bound, UTC-canonical, and encode the exact branch eligibility rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate fresh r2 prepared identity and test contract composition</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1</files>
  <behavior>
    - Prepared r2 root/genesis/manifest use fresh identities and bind both historical-negative digests.
    - r1 current input, reused locator/root/index/genesis, missing history, or published-content correction semantics fail.
    - Receipt/handoff schema fixtures accept valid branch projections and reject cross-branch or digest tampering.
  </behavior>
  <action>Extend canonical qualification, prepared-bundle generation, and Phase 8 static fixtures to compose the Task 1 attempt family with Task 2 authority/receipt/handoff schemas. Fresh r2 preparation must not reuse any prior attempt state. Add positive and adversarial tests for both eligible authority variants, UTC-equivalent timestamps, serialized reload, changed instant, history substitution, missing receipt, and receipt on exact-existing. Run the full credential-free focused set and commit this five-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r2 preparation and both handoff variants are composition-tested without network, credentials, tags, dispatch, or mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Historical attempts to r2 current input | Prior attempts are evidence only. |
| Human response to mutation eligibility | Only a closed receipt can carry literal authorization. |
| Mutable live paths to deterministic handoff | Every referenced artifact is schema- and digest-bound. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-07-01 | Tampering | attempt-family policy | critical | mitigate | Exact r2 invariants and immutable attempt-zero/r1 facts with substitution tests. |
| T-08-07-02 | Elevation of Privilege | authorization receipt | critical | mitigate | Closed literal response plus packet/ref/boundary/time/content digest bindings. |
| T-08-07-03 | Spoofing | handoff paths | critical | mitigate | Absolute rooted paths, closed properties, per-file SHA-256, exclusive variant validation. |
| T-08-07-04 | Repudiation | prior failures | high | mitigate | Exact terminal-negative records and content digests required in every r2 projection. |
</threat_model>

<verification>
Run intent, prepared-bundle, and Phase 8 static qualification suites; validate schemas, UTC/digest behavior, branch exclusivity, and no network or mutation.
</verification>

<success_criteria>
r2, both immutable historical negatives, the literal receipt, and the deterministic handoff are closed tested contracts ready for hosted integration.
</success_criteria>

<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-07-SUMMARY.md` when done.</output>
