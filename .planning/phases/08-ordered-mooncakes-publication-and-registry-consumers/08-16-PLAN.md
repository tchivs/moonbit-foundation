---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "16"
type: execute
wave: 16
depends_on: ["08-15"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Only fixed valid r5 union starts: mutation packet+receipt, exact neither, stop ineligible."
    - "All artifacts bind five histories/set and r5 identity."
    - "Core/color/image strictly ordered, one mutation/run, no exact republish."
    - "Each authority has exact metadata/archive/manifest/checksum/graph/four-target/native/behavior/predecessor evidence."
    - "Reciprocal closure proves all requirements offline."
  artifacts:
    - path: "%TEMP%/mnf-phase08-r5-handoff.json"
      provides: "Eligible r5 union"
    - path: "<r5-artifact-root>/authorities/mb-core/*.json"
      provides: "Core authority"
    - path: "<r5-artifact-root>/authorities/mb-color/*.json"
      provides: "Color authority"
    - path: "<r5-artifact-root>/authorities/mb-image/*.json"
      provides: "Image authority"
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Tracked closure"
  key_links:
    - from: "r5 union"
      to: "core"
      via: "receipt-or-exact and five-history replay"
      pattern: "modules-v0.1.0-r5"
    - from: "core/color"
      to: "successor"
      via: "prior authority and cold proof"
      pattern: "PriorAuthorityRecordPath"
---

<objective>Complete ordered r5 publication/exact recovery, cold proofs, and closure.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-15-SUMMARY.md
@policy/phase-08-distribution.json</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate r5 union and resolve core then color</name>
  <files>None (sanitized r5 artifacts)</files>
  <action>Validate fixed handoff schema/digests/files, LF clean root, unique state, r5 identity, five histories/set, union. Mutation requires packet+receipt; exact forbids both. Exact core consumes directly. Mutation core re-observes; only absent one PublishOne; exact zero mutation; ambiguity stops. Produce/index public metadata and outside-checkout one-node cold proof. Resolve color with core predecessor through same switch; color absent one separate PublishOne, exact no republish. Run CoreColorArtifacts.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r5-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -CoreColorArtifacts -HandoffPath $p }"</automated></verify>
  <done>r5 core/color exact authorities and cold proofs exist in order.</done>
</task>
<task type="auto">
  <name>Task 2: Resolve image and full graph</name>
  <files>None (sanitized r5 artifacts)</files>
  <action>Reload handoff/CoreColor/five histories. Resolve image with color predecessor; absent one PublishOne, exact zero mutation, uncertainty stops. Run outside-checkout empty-home credential-free pinned-toolchain consumer; require exact image->color->core 0.1.0, metadata/archive/manifests/checksums/docs, js/wasm/wasm-gc/native, bounded PPM. Index/select/run LiveArtifacts.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r5-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -LiveArtifacts -HandoffPath $p }"</automated></verify>
  <done>Image exact authority/full graph/PPM pass four targets.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r5 closure and Required</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Reciprocal binds r5, five histories/set, union/receipt, authorities.
    - Exact zero mutation; published exactly one valid run.
    - Required preserves snapshot, UTC, exact14, no-tags offline.
  </behavior>
  <action>Run ReciprocalArtifacts; replay union/reducer/predecessor/five-history/receipt and copy closed sanitized projection. Extend tracked Required, preserve equal-empty/nonempty-drift, UTC, exact14 receipt parity, no-tags, handoff isolation. Update COVERAGE for sixteen plans, r5, five histories, branch adjacency, D-01..19, all requirements. Exclude Phase9.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -TrackedClosure; if ($LASTEXITCODE) { exit $LASTEXITCODE }; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All requirements have r5 evidence and offline Required passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r5 union to mutation | Receipt-or-exact replay. |
| Authority to successor | Predecessor/cold gate. |
| Live to tracked | Sanitized only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-16-01 | Elevation of Privilege | union | critical | mitigate | Receipt required; absent only publishes once. |
| T-08-16-02 | Tampering | five-history chain | critical | mitigate | Digest/set/predecessor replay. |
| T-08-16-03 | Spoofing | old state | critical | mitigate | Fixed r5 handoff/LF/unique state. |
| T-08-16-04 | Information Disclosure | tracked | high | mitigate | Sanitized offline projection. |
| T-08-16-05 | Denial of Service | ambiguity | medium | mitigate | Stop, no rerun/republish. |
</threat_model>
<verification>Validate r5 union, five histories, ordered authorities, cold/metadata, reciprocal closure, Required.</verification>
<success_criteria>All modules published or exactly checkpointed without republish and Phase 8 requirements have r5 evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-16-SUMMARY.md` when done.</output>
