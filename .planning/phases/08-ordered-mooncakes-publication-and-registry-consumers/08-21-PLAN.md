---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "21"
type: execute
wave: 21
depends_on: ["08-20"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
  - scripts/quality/Invoke-Phase08R8PreLive.ps1
  - scripts/quality/Test-Phase08R8PreLive.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher, workflow, and hosted controller accept only r8 canonical prepared identity and all eight histories/set (D-02, D-05, D-07)."
    - "Unique workflow environment keys and exact14 controller/declaration/propagation/receipt parity remain mechanical."
    - "Snapshot, UTC, LF, no-tags, LibraryOnly, fixed-handoff isolation, rootless attempt-zero, and remote-tag authority all remain fail-closed."
    - "Zero-write pre-live binds exact r7 failure, canonical archive evidence, committed-clean 08-19..21 ownership, and r8 tag/handoff absence."
    - "No task pushes, tags, dispatches, accesses secrets, writes fixed r8 handoff, or calls PublishOne."
  artifacts:
    - path: ".github/workflows/publish-modules.yml"
      provides: "r8 canonical exact14 workflow"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r8 eight-history hosted controller"
    - path: "scripts/quality/Invoke-Phase08R8PreLive.ps1"
      provides: "Read-only r8 pre-live selector"
  key_links:
    - from: "canonical r8 prepared manifest"
      to: "publisher/workflow/controller validation"
      via: "exact archive and intent digests"
      pattern: "prepared_manifest_sha256"
    - from: "eight validated histories"
      to: "historical_attempts_sha256"
      via: "canonical ordered-set recomputation"
      pattern: "historical_r7_sha256"
---

<objective>Wire r8 canonical prepared identity through publisher, workflow, hosted exact14, and zero-write pre-live seams.

Purpose: Make the corrected r8 HostedPreflight structurally dispatchable without live action.
Output: r8 hosted seam, adversarial tests, and mandatory pre-live selector.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-intent-components.md
@.planning/debug/phase08-prelive-attempt-zero-root.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-20-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce r8 canonical identity and eight histories in publisher and workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - Only r8 canonical prepared requests with eight histories/set are accepted.
    - Raw-container substitution, r7-current/fake success, history drift, correction substitution, and second mutation fail before credentials.
    - Workflow mappings have unique keys and exact14 propagation remains closed.
  </behavior>
  <action>Per D-02, D-03, D-05, D-07, and D-14, advance publisher, adapter, and workflow to r8/eight histories and canonical prepared identity from 08-20. Preserve one-module maximum, ephemeral MOON_HOME, pinned toolchain, actor parsing, raw-output rejection, secret scoping, unique keys, and exact14 receipt parity. Reject raw archive digests, old-current, fabricated r7 success, missing/reordered histories, set mismatch, correction substitution, second mutation, and field drift before credential materialization.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Publisher/workflow accept only canonical r8, eight histories, unique keys, and exact14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advance hosted exact14 while retaining every prior guard</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1</files>
  <behavior>
    - Hosted controller binds canonical archives, r8, eight histories, and exactly 14 fields in identical order.
    - r7 failed prepare/downstream-zero cannot be upgraded into hosted authority.
    - Equal-empty/nonempty-drift, UTC, LF, no-tags, LibraryOnly, raw-output, and fixed-handoff guards remain.
  </behavior>
  <action>Per D-03, D-04, D-07, D-14, and D-18, extend hosted state to r8/eight histories and consume only 08-20 canonical prepared identity. Preserve exact14 declarations/propagation, unique keys, receipt pairing, history cardinality/order/set, snapshot equality/drift, canonical UTC, LF/no-tags, LibraryOnly GUID roots, raw-output rejection, and `%TEMP%/mnf-phase08-r8-handoff.json` absence. Retain exact r7 run/job/failure/downstream-zero form and reject fabricated prepared upload, downstream job, packet, receipt, authority, or successor.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -HostedFieldsOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Hosted r8 seam retains canonical identity, exact14, eight histories, snapshot, UTC/LF/no-tags, and isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add zero-write r8 pre-live selector</name>
  <files>scripts/quality/Invoke-Phase08R8PreLive.ps1, scripts/quality/Test-Phase08R8PreLive.ps1</files>
  <behavior>
    - Selector validates rootless attempt-zero plus persisted r1-r7, remote refs/peels, eight digests/set, exact r7 failure, and downstream zero.
    - Canonical archive evidence, all historical paths, 08-19..21 summaries/ownership, and r8 tag/handoff absence are mandatory.
    - Success emits one closed sanitized object and performs no mutation.
  </behavior>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, add `Invoke-Phase08R8PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin`. Preserve rootless attempt-zero canonical evidence, strict r1-r7 path containment/digests, and single fail-closed remote tag query. Bind exact r7 source/tag/run/job/error/raw-container mismatch/downstream-zero and canonical archive proof from 08-20. Require complete 08-19/20/21 owned files and summary ancestry, remote/local r8 tag absence, and fixed r8 handoff absence. Emit one sanitized JSON object with no output write. Add isolated fixtures for historical/ref/path/archive/summary/tag/handoff/output-write drift and positive case.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08R8PreLive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>The selector proves canonical r8 pre-live eligibility and all eight histories without side effects.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Canonical prepared evidence to hosted seam | Raw container identity cannot substitute for qualified canonical bytes. |
| Controller to workflow | Unique keys and exact14 prevent shadowing or dropped authority. |
| Remote/history state to live execution | Selector validates immutable evidence without writing or trusting local tags. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-21-01 | Tampering | canonical prepared propagation | critical | mitigate | Exact archive/intent digest parity across all seams. |
| T-08-21-02 | Tampering | exact14/env mappings | critical | mitigate | Mechanical equality and unique-key tests. |
| T-08-21-03 | Spoofing | histories/remote refs | critical | mitigate | Eight-record set plus remote tag authority. |
| T-08-21-04 | Elevation of Privilege | r7 failed run | critical | mitigate | Reject fabricated downstream authority. |
| T-08-21-05 | Information Disclosure | hosted evidence | high | mitigate | Credential isolation and raw-output rejection. |
</threat_model>

<verification>Run publisher, workflow, hosted, qualification, observation, and r8 pre-live suites; confirm canonical identity, eight histories, all prior guards, r8 absence, and zero external effects.</verification>
<success_criteria>The r8 hosted/pre-live seam is ready for one clean non-publishing live execution in 08-22.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-21-SUMMARY.md` when done.</output>
