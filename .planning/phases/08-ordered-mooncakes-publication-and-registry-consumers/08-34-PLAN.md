---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "34"
type: execute
wave: 34
depends_on: ["08-31", "08-32"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-handoff-schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
  - scripts/quality/Test-Phase08R13DirtyBaseline.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "08-33 is quarantined as obsolete r12 publication authority and cannot be routed to execution; publication requires a later r13-specific plan after r13's separate boundary and authorization steps."
    - "r12 remains immutable REL01-REF terminal evidence, while only r13 may be current and every static artifact binds all thirteen ordered histories plus their LF aggregate."
    - "The exact eight-path user-dirty baseline is captured and rechecked by an executable static-only guard; changed allowed paths and any other drift fail closed."
  artifacts:
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md"
      provides: "authoritative r13 recovery route and 08-33 quarantine"
    - path: "scripts/quality/Assert-Phase08R13DirtyBaseline.ps1"
      provides: "closed eight-path status/content-hash baseline guard"
    - path: "policy/release-control.json"
      provides: "r13 current policy with r12 terminal REL01-REF history"
---
<objective>Route r12 publication safely out of execution and establish the static r13 history and dirty-baseline contracts.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-33-PLAN.md
@policy/release-control.json</context>
<tasks>
<task type="auto">
  <name>Task 1: Quarantine obsolete r12 publication routing before recovery work</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md</files>
  <action>Perform routing-only work: create or verify the concise r13 recovery route record and update the Phase 8 route/state so 08-33 is marked obsolete and quarantined, never executable or reusable as publication authority. State that r12 is immutable terminal REL01-REF evidence, Plan 08-34 then Plan 08-35 are the active static-only recovery route, a later plan must separately create and verify an immutable r13 boundary, and a still-later r13-specific plan may seek explicit authorization and attempt one ordered module operation. This task runs before the baseline guard exists and therefore must not edit, stage, delete, reset, clean, stash, or otherwise touch any of the eight baseline paths. Do not claim an r13 tag, authorization, Mooncakes observation, cold proof, registry mutation, or publication. Preserve all historical 08-01 through 08-33 files unchanged.</action>
  <verify><automated>pwsh -NoProfile -Command "$state=Get-Content -Raw ./.planning/STATE.md; $road=Get-Content -Raw ./.planning/ROADMAP.md; $route=Get-Content -Raw ./.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md; if($route -notmatch '08-33' -or $route -notmatch 'quarantined' -or $route -notmatch 'r13'){throw 'P08-R13-ROUTE'}; if($state -match '(?im)^.*08-33.*(active|resume|operator authorization|authorize-core|dispatch).*$'){throw 'P08-R13-STATE-33-ACTIVE'}; if($state -notmatch '08-34 then 08-35'){throw 'P08-R13-STATE-RECOVERY'}; if($road -notmatch '\*\*Plans:\*\* 32/35 plans executed' -or $road -notmatch '\*\*Wave 33\*\* \*\(quarantined; do not execute\)\*' -or $road -notmatch '08-34-PLAN.md' -or $road -notmatch '08-35-PLAN.md'){throw 'P08-R13-ROADMAP-ROUTE'}"</automated></verify>
  <done>GSD routing explicitly blocks 08-33 and points to r13 recovery without asserting a live release result.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add the closed eight-path dirty-baseline guard</name>
  <files>scripts/quality/Assert-Phase08R13DirtyBaseline.ps1, scripts/quality/Test-Phase08R13DirtyBaseline.ps1</files>
  <behavior>
    - Capture records only these allowed paths: `.planning/config.json`, `docs/governance/decisions/0001-sole-owner-bootstrap.md`, `docs/governance/rfc-process.md`, `.codebase-memory/`, `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/research-plan-input.json`, `.planning/research/.cache/`, `.github/workflows/quality.yml`, and `.planning/quick/260719-fix-github-actions-ci/PLAN.md`.
    - The guard records normalized porcelain status and SHA-256 content/tree hashes for every protected path, and Verify rejects changed protected status/hash or any dirty path outside the union of the eight protected paths and the exact current task-owned file list.
    - Capture and verify are local static operations; they never stage, edit, reset, clean, stash, include a baseline path in an artifact, access a credential, or contact GitHub/Mooncakes.
  </behavior>
  <action>Write RED tests first, then implement one parameterized Capture/Verify guard with a closed JSON schema/version, deterministic path ordering, and mandatory `-TaskOwnedPath` input. Capture Git status for the whole worktree, compute a content hash for files and a deterministic tree hash for directories, and persist only the eight protected path/status/hash records under an executor-owned temporary path. Verify must recapture the protected projection and fail on any mismatch, new/deleted protected member, or dirty entry outside the exact union of the protected paths and the supplied current task-owned paths; it must not allow a wildcard, undeclared task path, or another task's path. Make tests prove positive equality plus status drift, content drift, directory drift, unexpected non-task drift, malformed baseline, attempted ninth protected path, and task-owned allowlist negatives. Only after tests pass, run Capture to `%TEMP%/mnf-phase08-r13-user-dirty-baseline.json` and immediately run Verify with exactly `scripts/quality/Assert-Phase08R13DirtyBaseline.ps1` and `scripts/quality/Test-Phase08R13DirtyBaseline.ps1` as Task 2-owned paths. Thereafter each task passes only its own `<files>` paths to Verify before and after work; this is not permission to stage, edit, delete, reset, clean, stash, or include any protected path in source, tag, clone, prepared, publisher, evidence, or planning artifacts.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08R13DirtyBaseline.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Capture -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath scripts/quality/Assert-Phase08R13DirtyBaseline.ps1,scripts/quality/Test-Phase08R13DirtyBaseline.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath scripts/quality/Assert-Phase08R13DirtyBaseline.ps1,scripts/quality/Test-Phase08R13DirtyBaseline.ps1</automated></verify>
  <done>A tested fail-closed eight-path baseline guard captures and immediately verifies the required baseline, then can prove unchanged user dirties and reject every other worktree drift.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Advance static schemas to r13 and preserve r12 terminal evidence</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, release/qualification/phase-08-handoff-schema.json, release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Valid r13 contracts contain attempt-zero then r1 through r12, thirteen individual digest fields, and their canonical LF aggregate.
    - r12 contains only its attested REL01-REF terminal facts and cannot supply current state, locator, receipt, handoff, credentials, mutation, or publication authority.
  </behavior>
  <action>After the dirty guard passes before/after the task, write RED vectors then update release policy and closed initial/prepared/authority/receipt/handoff schemas to r13. Preserve prior history exactly; record r12 only from its attested terminal REL01-REF checkpoint and reject missing, reordered, altered, stale, or compatibility-path evidence. Per D-01, D-07, D-14, D-15, and D-18, do not create or fetch a production tag, dispatch a workflow, access credentials, or contact the registry.</action>
  <verify><automated>$taskPaths=@('policy/release-control.json','release/intent/schema.json','release/prepared/schema.json','release/qualification/phase-08-handoff-schema.json','release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','scripts/quality/Test-ReleaseIntent.ps1'); pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths</automated></verify>
  <done>Only r13 static artifacts with complete thirteen-history binding pass, with unchanged baseline evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| obsolete r12 route to executor | 08-33 must not be mistaken for current publication authority. |
| user dirties to release artifacts | Eight acknowledged paths remain unchanged and excluded; all other drift stops. |
| r12 record to r13 policy | Terminal REL01-REF facts cannot become active authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-34-01 | Elevation of Privilege | 08-33 routing | critical | mitigate | Quarantine in route/state before any recovery execution. |
| T-08-34-02 | Tampering | user-dirty baseline | high | mitigate | Closed status/hash capture and equality checks before/after every task. |
| T-08-34-03 | Tampering | r12 terminal history | critical | mitigate | Closed thirteen-history schemas and adversarial tests. |
</threat_model>
<verification>Run route, baseline, and intent suites; verify baseline before and after every task. No source/tag/workflow/registry/credential operation is authorized.</verification>
<success_criteria>08-33 is unusable for release, r13 static authority is closed, and the eight-path baseline guard is executable and fail-closed.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-34-SUMMARY.md` when done.</output>
