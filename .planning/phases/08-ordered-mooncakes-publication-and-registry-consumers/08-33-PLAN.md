---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "33"
type: execute
wave: 33
depends_on: ["08-32"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Core requires revalidated r12 receipt or exact authority; color follows core proof and image follows color proof, each in a distinct run."
    - "Each module has exact structured metadata/identity/graph and fresh credential-free four-target cold proof."
    - "Closure binds r12 and attempt-zero+r1..r11 plus sanitized evidence and unchanged user-dirty baseline."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "r12 reciprocal DIST/PROV-05 evidence"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "final r12 Phase 8 evidence map"
---
<objective>Publish or exactly recognize modules in order through r12, prove cold registry consumption, and close Phase 8 evidence.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-SUMMARY.md
@policy/release-qualification.json
@policy/registry-authority.json
@release/qualification/phase-08-requirements.json
@scripts/quality/Invoke-Phase08HostedRun.ps1</context>
<tasks>
<task type="auto">
  <name>Task 1: Revalidate r12 and complete core cold registry proof</name>
  <files>None (sanitized publisher/observation/core-consumer artifacts)</files>
  <action>Before/after every action compare the exact six-path dirty baseline from 08-32; stop on drift or any other dirty path and never alter allowed user files. Revalidate canonical wrapper result, r12 tag/source/twelve histories/prepared artifacts/receipt/actor/current observation. Exact core performs no mutation; absent with same-turn receipt receives one protected core-scoped publisher run and no retry. Require credential-free structured metadata/index/archive/manifest/checksum agreement, then a new external empty-MOON_HOME consumer with pinned toolchain, one-node graph, deterministic core behavior, and js/wasm/wasm-gc/real native results. Persist sanitized evidence; any failure stops before color.</action>
  <verify><automated>pwsh -NoProfile -Command "$h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r12-handoff.json'; $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r12-preauthorization.json'; if(Test-Path $h){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $h; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $p -HandoffPath $h; exit $LASTEXITCODE"</automated></verify>
  <done>Core exact metadata/identity/cold proof exists before color and baseline is unchanged.</done>
</task>
<task type="auto">
  <name>Task 2: Publish or recognize color only after core proof</name>
  <files>None (sanitized publisher/observation/color-consumer artifacts)</files>
  <action>Recheck baseline before/after each action. Require core checkpoint digest, exact observation, identity, graph, behavior, and all targets. Exact color performs no mutation; absent color gets one new journal-selected color run only. Require exact color+core graph, structured metadata/archive/manifest/checksum, then a distinct new cold consumer and real four-target public color proof. Any drift, unknown, timeout, or failure stops before image.</action>
  <verify><automated>pwsh -NoProfile -Command "$h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r12-handoff.json'; $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r12-preauthorization.json'; if(Test-Path $h){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $h; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $p -HandoffPath $h; exit $LASTEXITCODE"</automated></verify>
  <done>Color exact two-node cold proof is tied to core proof with unchanged baseline.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Complete image proof and reciprocal r12 closure</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Closure rejects missing r11 history, missing predecessor proof, raw outputs/credentials, or dirty-baseline drift.
    - Valid image proof has exact full graph and bounded PPM behavior across all targets.
  </behavior>
  <action>Write closure tests first. Revalidate color and baseline, then exact-recognize image or publish confirmed-absent image in one separate journal-selected run. Require structured metadata and archive/manifest/checksum plus a new external cold consumer with exact three-module graph and bounded public PPM encode/decode across all targets. Bind r12/tag/source/twelve history values and sanitized authority/observation/identity/graph/toolchain/targets/behavior/baseline evidence into reciprocal qualification and coverage. Never persist raw response or credentials; Phase 9 owns immutable provenance ledger closure.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Check</automated></verify>
  <done>Image full graph PPM proof and all Phase 8 r12 DIST/PROV-05 evidence are complete with unchanged baseline.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r12 receipt to core | Only fresh authorized core can mutate. |
| registry to successor | Metadata/identity/graph must agree before advance. |
| user baseline to evidence | Dirty files remain isolated and unchanged. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-33-01 | Elevation of Privilege | core receipt | critical | mitigate | Revalidate then one core run. |
| T-08-33-02 | Tampering | registry identity | critical | mitigate | Structured identity/graph/archive comparisons. |
| T-08-33-03 | Information Disclosure | consumers | critical | mitigate | New empty homes and sanitized records. |
| T-08-33-04 | Tampering | dirty baseline | high | mitigate | Compare before/after every action. |
</threat_model>
<verification>Verify each one-module run, exact observation/identity, fresh cold graph/four targets, baseline equality, then offline closure.</verification>
<success_criteria>All modules are published once or exact-recognized in order with r12-bound cold proof ready for Phase 9.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-33-SUMMARY.md` when done.</output>
