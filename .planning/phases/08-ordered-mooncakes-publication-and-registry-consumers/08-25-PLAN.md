---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "25"
type: execute
wave: 25
depends_on: ["08-24"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "r9 handoff admits exactly one nine-history union; mutation has packet+receipt and exact-existing has neither (D-04, D-07, D-15)."
    - "Core, color, image resolve strictly; each absent module gets at most one PublishOne and exact gets zero mutation (D-05, D-06, D-14)."
    - "Every module has structured metadata, exact canonical archive/manifest/checksum, and cold four-target proof before successor (D-08..D-18)."
    - "Tracked closure binds r9, nine histories/set, canonical-copy seam/fixture, authorities, observations, graphs, behavior, targets, and sanitized digests."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Reciprocal r9 DIST-01..04 and PROV-05 evidence"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "Offline qualification gates"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "25-plan final coverage"
  key_links:
    - from: "verified authority"
      to: "successor eligibility"
      via: "exact observation plus cold predecessor proof"
      pattern: "predecessor_checkpoint_sha256"
    - from: "sanitized artifacts"
      to: "tracked closure"
      via: "reciprocal digests"
      pattern: "historical_r8_sha256"
---

<objective>Complete ordered r9 publication or exact recovery, cold proofs, metadata proof, and reciprocal Phase 8 closure.

Purpose: Turn r9 handoff into dependency-ordered exact authorities without retry or republish.
Output: Core/color/image authorities plus tracked all-requirement closure.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-24-SUMMARY.md
@release/qualification/phase-08-requirements.json</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate r9 union and resolve core then color</name>
  <files>None (sanitized r9 artifacts under ArtifactRoot)</files>
  <action>Per D-04 through D-19, validate fixed r9 handoff, paths/digests, LF root, unique state, remote identity, canonical-copy seam/fixture, nine histories/set, prepared identity, and exclusive union. Mutation requires 08-24 packet+receipt; exact forbids both. Core exact performs no mutation; fresh absent permits one separate PublishOne; every timeout/mismatch/unknown/conflict/ambiguity/nonzero/stale receipt stops. Prove core structured metadata, canonical archive/downloaded manifest/checksum/docs, cold one-node graph, pinned toolchain, four targets, checked behavior. Select core authority. Require it as color predecessor and apply absent-one-run/exact-zero branch; prove exact color-core graph, metadata, targets, behavior; select CoreColorArtifacts.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode CoreColorArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r9-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>Core/color exact authorities and cold proofs exist in strict order without exact republish.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove full graph</name>
  <files>None (sanitized r9 artifacts under ArtifactRoot)</files>
  <action>Per D-05 through D-18, reload r9 handoff, nine histories/set, seam/fixture, and CoreColorArtifacts. Require color/core predecessors. Image absent permits one PublishOne run; exact zero mutation; ambiguity/mismatch/timeout/nonzero/conflict stops. Run a new outside-checkout consumer with empty MOON_HOME, pinned toolchain, and no credentials/workspace/path/Git/copied source/cache/target state. Require exact image-color-core graph, structured API/index/canonical archive/downloaded manifest/checksum/assets/docs, four targets, and bounded PPM behavior. Select LiveArtifacts before closure.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode LiveArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r9-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>Image authority, full graph, metadata, targets, and PPM behavior pass cold registry-only proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r9 closure and Required evidence</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Closure and evidence reciprocally bind r9 source/tag, union, nine histories/set, canonical-copy seam/fixture, and canonical archives.
    - All requirements pass offline; missing predecessor/graph/target/metadata/behavior/digest fails.
    - No raw network/authentication/credential/secret output is tracked.
  </behavior>
  <action>Per D-05 through D-18, replay union, reducer, predecessor chain, nine histories/set, optional receipt, seam/fixture, hosted identities, canonical archives, observations, cold graphs, targets, and sanitized digests. Copy only closed projection into tracked requirements. Retain canonical ZIP, LF dual-clone, unique env, exact14, snapshot, UTC, no-tags, fixed-handoff, rootless attempt-zero, remote tags, and all negatives. Update COVERAGE to 25/25 only after all evidence passes, recording r8 PREP15 no-run/downstream zero and all requirements. Exclude Phase 9 closure.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ReciprocalArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r9-handoff.json" -TrackedClosurePath ./release/qualification/phase-08-requirements.json; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Profile Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All requirements have reciprocal r9 evidence and offline Required passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| AuthorityUnion to PublishOne | Only receipt-backed absent authority mutates once. |
| Observation to successor | Exact observation and cold predecessor proof are mandatory. |
| Evidence to Git | Only closed projections and reciprocal digests cross. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-25-01 | Elevation of Privilege | PublishOne | critical | mitigate | Union, receipt, predecessor, one-run gates. |
| T-08-25-02 | Tampering | graph/archive/seam | critical | mitigate | Exact graph and canonical-copy artifact agreement. |
| T-08-25-03 | Spoofing | public package | critical | mitigate | Structured metadata/index/archive proof. |
| T-08-25-04 | Information Disclosure | closure | high | mitigate | Sanitized projection and raw-output rejection. |
| T-08-25-05 | Repudiation | requirements | high | mitigate | Reciprocal digests and offline replay. |
</threat_model>

<verification>Validate r9 union, nine histories, ordered transitions, seam/fixture, metadata, cold graphs/behavior, reciprocal closure, and offline Required.</verification>
<success_criteria>All modules are exact without republish and all Phase 8 requirements have reciprocal r9 evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-25-SUMMARY.md` when done.</output>
