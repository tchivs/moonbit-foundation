---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "23"
type: execute
wave: 23
depends_on: ["08-22"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "r8 handoff admits exactly one eight-history union; mutation has packet+receipt and exact-existing has neither (D-04, D-07, D-15)."
    - "Core, color, image resolve strictly in order; absent gets at most one separate PublishOne and exact gets zero mutation (D-05, D-06, D-14)."
    - "Each accepted module has structured metadata, exact canonical archive/manifest/checksum, and credential-free cold four-target proof before successor (D-08..D-18)."
    - "Tracked closure binds r8, eight histories/set, authorities, canonical archives, observations, graphs, behavior, targets, and sanitized digests."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Reciprocal DIST-01..04 and PROV-05 r8 evidence"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "Offline qualification gates"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "23-plan final coverage matrix"
  key_links:
    - from: "verified module authority"
      to: "successor eligibility"
      via: "exact observation plus cold predecessor proof"
      pattern: "predecessor_checkpoint_sha256"
    - from: "sanitized live artifacts"
      to: "tracked closure"
      via: "reciprocal digests"
      pattern: "historical_r7_sha256"
---

<objective>Complete ordered r8 publication or exact recovery, cold registry proofs, metadata proof, and reciprocal Phase 8 closure.

Purpose: Turn non-mutating r8 handoff into three dependency-ordered exact public authorities without retry or republish.
Output: Core/color/image authorities plus tracked DIST-01..04 and PROV-05 closure.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-22-SUMMARY.md
@release/qualification/phase-08-requirements.json</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate r8 union and resolve core then color</name>
  <files>None (sanitized r8 live artifacts under ArtifactRoot)</files>
  <action>Per D-04 through D-19, validate fixed handoff closed schema, paths/digests, LF root, unique state, remote r8 identity, canonical archives, eight histories/set, prepared identity, and exclusive union. Mutation requires 08-22 packet+receipt; exact forbids both. Core exact performs no mutation; mutation authority re-observes and only fresh absent permits one separate PublishOne. Timeout, mismatch, unknown, conflict, ambiguity, nonzero, or stale receipt stops without rerun. Prove core structured metadata, canonical archive/downloaded manifest/checksum/docs, cold exact one-node graph, pinned toolchain, four targets, and checked behavior. Select core authority. Require it as color predecessor; apply absent-one-run/exact-zero branch; prove exact color-core graph, metadata/artifact agreement, four targets, and color behavior; select CoreColorArtifacts.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode CoreColorArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r8-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>Core and color exact authorities/cold proofs exist in strict order without exact republish.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove full exact public graph</name>
  <files>None (sanitized r8 live artifacts under ArtifactRoot)</files>
  <action>Per D-05 through D-18, reload handoff, eight histories/set, and CoreColorArtifacts. Require color/core predecessors. Re-observe image: absent permits one PublishOne run, exact zero mutation, and ambiguity/mismatch/timeout/nonzero/conflict stops. Run new outside-checkout consumer with empty MOON_HOME, pinned toolchain, no credentials/workspace/path/Git/copied source/cache/target state. Require exact image-color-core graph; structured API/index/canonical archive/downloaded manifests/checksums/assets/docs; four targets; bounded PPM behavior. Select LiveArtifacts before closure.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode LiveArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r8-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>Image authority, full graph, metadata, targets, and PPM behavior pass cold registry-only proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r8 closure and Required evidence</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Closure and live evidence reciprocally bind r8 source/tag, union, canonical archives, and eight histories/set.
    - All five requirements pass offline; any missing predecessor/graph/target/metadata/behavior/digest fails.
    - Raw network/authentication/credential/secret output never enters tracked evidence.
  </behavior>
  <action>Per D-05 through D-18, run ReciprocalArtifacts and replay union, reducer, predecessor chain, eight histories/set, optional receipt, hosted identities, canonical archives, public observations, cold graphs, targets, and sanitized digests. Copy only closed projection into tracked requirements. Retain canonical ZIP, LF dual-clone, unique env, exact14, snapshot, UTC, no-tags, fixed-handoff, rootless attempt-zero, remote-tag, and all existing negatives. Update COVERAGE to 23/23 only after all evidence passes, recording exact r7 failure/downstream zero and all five requirements. Exclude Phase 9 closure.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ReciprocalArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r8-handoff.json" -TrackedClosurePath ./release/qualification/phase-08-requirements.json; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Profile Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All Phase 8 requirements have reciprocal r8 evidence and offline Required passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| AuthorityUnion to PublishOne | Only receipt-backed absent authority mutates once. |
| Observation to successor | Exact observation and cold predecessor proof are mandatory. |
| Live evidence to Git | Only closed projection and reciprocal digests cross. |
| Cold consumer to registry | No credential, checkout, alternate source, or warm state crosses. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-23-01 | Elevation of Privilege | PublishOne | critical | mitigate | Exclusive union, receipt, predecessor, one-run gates. |
| T-08-23-02 | Tampering | graph/archive | critical | mitigate | Exact graph and canonical archive agreement. |
| T-08-23-03 | Spoofing | package identity | critical | mitigate | Structured metadata/index/archive proof. |
| T-08-23-04 | Information Disclosure | closure | high | mitigate | Sanitized projection and raw-output rejection. |
| T-08-23-05 | Repudiation | requirements | high | mitigate | Reciprocal digests and offline replay. |
</threat_model>

<verification>Validate r8 union, eight histories, ordered authority transitions, canonical archives, metadata, cold graphs/behavior, reciprocal closure, and offline Required.</verification>
<success_criteria>All modules are exact without republish and DIST-01..04 plus PROV-05 have reciprocal r8 evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-23-SUMMARY.md` when done.</output>
