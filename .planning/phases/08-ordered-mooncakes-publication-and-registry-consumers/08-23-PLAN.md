---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "23"
type: execute
wave: 23
depends_on: ["08-22"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
  - scripts/quality/Invoke-Phase08R9PreLive.ps1
  - scripts/quality/Test-Phase08R9PreLive.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher, workflow, controller accept only r9 canonical prepared-copy identity and nine histories/set (D-02, D-05, D-07)."
    - "Unique env keys and exact14 controller/workflow/receipt parity remain mechanical."
    - "All prior snapshot, UTC, LF, no-tags, LibraryOnly, fixed handoff, rootless attempt-zero, and remote-tag guards remain fail-closed."
    - "r9 pre-live binds canonical-copy seam version and actual PrepareAttempt fixture, exact r8 pre-locator terminal facts, committed-clean 08-22/23 ownership, and r9 tag/handoff absence."
    - "No task pushes, tags, dispatches, accesses secrets, writes handoff, or calls PublishOne."
  artifacts:
    - path: ".github/workflows/publish-modules.yml"
      provides: "r9 exact14 canonical-copy workflow"
    - path: "scripts/quality/Invoke-Phase08R9PreLive.ps1"
      provides: "Read-only r9 pre-live selector"
  key_links:
    - from: "actual canonical-copy PrepareAttempt evidence"
      to: "publisher/workflow/controller"
      via: "prepared manifest and intent digest equality"
      pattern: "prepared_manifest_sha256"
    - from: "nine validated histories"
      to: "historical_attempts_sha256"
      via: "canonical ordered set"
      pattern: "historical_r8_sha256"
---

<objective>Wire r9 canonical prepared-copy identity through publisher, hosted exact14, and zero-write pre-live seams.

Purpose: Make r9 HostedPreflight structurally dispatchable after r8's local prepare seam failure.
Output: r9 hosted seam, adversarial tests, and mandatory pre-live selector.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-prepare-canonicalization-seam.md
@.planning/debug/phase08-prelive-attempt-zero-root.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-22-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce r9 canonical prepared-copy identity in publisher and hosted workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1, scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1</files>
  <behavior>
    - Only r9 requests with canonical derived prepared copies and nine histories/set pass before credentials.
    - r8-current, raw-copy substitution, fake locator/run/success, history drift, and second mutation fail closed.
    - Workflow env names are unique and controller/declaration/propagation retains exact14 parity.
  </behavior>
  <action>Per D-02, D-03, D-05, D-07, and D-14, advance publisher, adapter, workflow, and hosted controller to r9/nine histories. Require the canonical-copy seam version and actual prepared fixture digest from 08-22 in prepared validation and all exact14 fields. Preserve one-module maximum, isolated MOON_HOME, pinned toolchain, actor parsing, secret scoping, raw-output rejection, snapshot behavior, and unique keys. Reject raw archive handoff, r8-current, fabricated locator/run/downstream success, missing/reordered histories/set, correction substitution, receipt field drift, and second mutation before credential materialization.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r9 publisher/workflow/controller accepts only canonical-copy identity, nine histories, unique keys, and exact14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add zero-write r9 pre-live gate retaining all prior safeguards</name>
  <files>scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1, scripts/quality/Invoke-Phase08R9PreLive.ps1, scripts/quality/Test-Phase08R9PreLive.ps1</files>
  <behavior>
    - Selector validates rootless attempt-zero plus r1-r8, remote tag objects/peels, nine digests/set, exact r8 PREP15 no-run facts, and downstream zero.
    - Canonical-copy seam version and actual actual-PrepareAttempt fixture are required; r8's raw failure cannot substitute.
    - 08-22/23 summaries/ownership are committed-clean and r9 tag/fixed handoff absent; success writes nothing.
  </behavior>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, add `Invoke-Phase08R9PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin`. Preserve rootless attempt-zero evidence, strict r1-r8 path/locator/index/store containment where applicable, one fail-closed remote-tag query, exact14, snapshot, UTC/LF/no-tags, and LibraryOnly isolation. Bind r8 source/tag/PREP15/REL-XPLAT error/no locator/no run/downstream zero and require current canonical-copy seam version plus actual r9 PrepareAttempt fixture metadata/digests. Require clean 08-22/23 ownership and summary ancestry, r9 tag absence, and `%TEMP%/mnf-phase08-r9-handoff.json` absence. Emit exactly one sanitized JSON object. Add remote/GUID fixtures for history, ref, seam version, fixture, path, summary, tag, handoff, and output-write drift plus positive case.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08R9PreLive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r9 selector proves canonical prepared-copy seam and nine histories before any live side effect.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Canonical-copy prepared evidence to hosted seam | Raw copies cannot substitute for qualified derived canonical bytes. |
| Controller to workflow | Exact14 and unique keys prevent authority field loss or shadowing. |
| Historical/remote state to live execution | Selector validates immutable evidence without writes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-23-01 | Tampering | prepared-copy propagation | critical | mitigate | Fixture/seam/digest equality across seams. |
| T-08-23-02 | Tampering | exact14/env | critical | mitigate | Mechanical equality and unique-key tests. |
| T-08-23-03 | Spoofing | r8 terminal state | critical | mitigate | Closed PREP15/no-locator/no-run/downstream-zero record. |
| T-08-23-04 | Elevation of Privilege | r9 authority | critical | mitigate | Nine-history/set and pre-live gate before secrets. |
</threat_model>

<verification>Run publisher, hosted, qualification, observation, and r9 pre-live suites; confirm canonical-copy identity, nine histories, all prior guards, r9 absence, and zero external effects.</verification>
<success_criteria>r9 hosted/pre-live seam is ready for one clean non-publishing live execution in 08-24.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-23-SUMMARY.md` when done.</output>
