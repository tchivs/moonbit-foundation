---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "14"
type: execute
wave: 14
depends_on: ["08-13"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher/workflow/controller accept only r5 and bind five histories/set."
    - "Exact ordered 14-field controller/workflow/propagation receipt parity remains."
    - "Clean snapshot equal-empty passes and nonempty drift fails in focused and hosted fixtures."
    - "UTC/LF/no-tags/LibraryOnly fixed-handoff isolation remain fail-closed."
  artifacts:
    - path: ".github/workflows/publish-modules.yml"
      provides: "r5 exact14 receipt-aware workflow"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r5 five-history hosted seam"
    - path: "scripts/quality/Test-ReleaseQualificationNegative.ps1"
      provides: "Equal-empty acceptance and nonempty drift rejection"
  key_links:
    - from: "controller 14 fields"
      to: "workflow declarations/propagation"
      via: "exact name/order/count equality"
      pattern: "authorization_receipt_sha256"
    - from: "five histories"
      to: "historical_attempts_sha256"
      via: "recomputed ordered set"
      pattern: "historical_attempts_sha256"
---

<objective>Wire r5 through publisher/workflow/hosted seam while retaining snapshot, exact14, UTC, LF, and fixture regressions.

Purpose: Ensure r5 HostedPreflight crosses the clean qualification and dispatch contracts safely.
Output: r5 hosted implementation and credential-free adversarial tests.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/resolved/phase08-clean-tracked-snapshot.md
@.planning/debug/phase08-workflow-receipt-input.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-13-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce r5/five histories in publisher and workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - r5 request binds root/current/prepared and five histories/set.
    - r4-current/history drift/correction/second mutation fails before credentials.
    - Exact14 packet/receipt propagation remains closed.
  </behavior>
  <action>Advance publisher/adapter/workflow to r5/five histories, preserving one-module max, isolated MOON_HOME/toolchain, actor/raw/secret rules, and exact14 controller/workflow packet+receipt parity. Add r4-current, history/set, second mutation, correction, and parity negatives. Commit four-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Publisher/workflow accepts only r5 and retains exact14 parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve clean snapshot, exact14, UTC, LF, no-tags, and handoff isolation</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1, scripts/quality/Test-ReleaseQualificationNegative.ps1</files>
  <behavior>
    - Equal empty Before/After snapshots pass; unequal nonempty snapshots throw REL14 tracked mutation.
    - Controller/workflow exact 14 names include packet+receipt for start-empty/resume-valid propagation.
    - History set derives from five individually validated records.
    - UTC/LF/--no-tags and LibraryOnly GUID fixtures never touch fixed r5 handoff.
  </behavior>
  <action>Extend hosted state to r5/five histories. Preserve clean snapshot fix in focused qualification and hosted fixtures: equal-empty passes, nonempty drift fails. Retain exact14 count/order/names/propagation and old 17-vs-14/missing-receipt/ungrouped/collapse negatives. Preserve UTC offset/reload/tamper/stale, core.autocrlf=false LF, no-tags clone, and LibraryOnly independent GUID roots. Production `%TEMP%/mnf-phase08-r5-handoff.json` is non-overridable; tests assert absent before/after and clean only owned paths. Run all focused/adjacent suites and commit atomically without live actions.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseQualificationNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -HostedFieldsOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r5 passes snapshot equality/drift, exact14, UTC/LF/no-tags, five-history, and isolation regressions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Clean checkout snapshot to qualification | Empty baseline valid; mutation drift rejected. |
| Controller to workflow | Exact14 parity required. |
| Tests to production state | Fixtures cannot touch fixed handoff/tags. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-14-01 | Tampering | tracked snapshot | critical | mitigate | Equal-empty and unequal-nonempty permanent regressions. |
| T-08-14-02 | Tampering | exact14 parity | critical | mitigate | Mechanical declaration/propagation equality. |
| T-08-14-03 | Spoofing | history set | critical | mitigate | Recompute from five validated histories. |
| T-08-14-04 | Elevation of Privilege | packet/receipt | critical | mitigate | Paired digest validation and stale alias rejection. |
| T-08-14-05 | Information Disclosure | hosted evidence | high | mitigate | Credential isolation and raw rejection. |
</threat_model>

<verification>Run qualification-negative, HostedFieldsOnly, WorkflowOnly, live seam, qualification, observation; confirm snapshot and exact14 regressions and no live effects.</verification>
<success_criteria>r5 seam is ready for one clean hosted preflight and later authorized resume.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-14-SUMMARY.md` when done.</output>
