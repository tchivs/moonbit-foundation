---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "14"
type: execute
wave: 14
depends_on: ["08-13"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Only fixed valid r4 AuthorityUnion starts publication: mutation has packet+receipt, exact neither, stop ineligible."
    - "All artifacts bind attempt-zero/r1/r2/r3 individual and ordered-set digests plus r4 identity."
    - "Core, color, image resolve strictly with one mutation per run and no exact republish (D-05 through D-07)."
    - "Each authority has exact metadata/archive/manifest/checksum/graph/four-target/native/behavior/predecessor evidence (D-08 through D-18)."
    - "Tracked reciprocal closure proves all five requirements while Required is offline and credential-free."
  artifacts:
    - path: "%TEMP%/mnf-phase08-r4-handoff.json"
      provides: "Eligible r4 start union"
    - path: "<r4-artifact-root>/authorities/mb-core/*.json"
      provides: "Exact core authority"
    - path: "<r4-artifact-root>/authorities/mb-color/*.json"
      provides: "Exact color authority"
    - path: "<r4-artifact-root>/authorities/mb-image/*.json"
      provides: "Exact image full-graph authority"
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Tracked reciprocal closure"
  key_links:
    - from: "r4 union"
      to: "core eligibility"
      via: "receipt-or-exact and four-history replay"
      pattern: "modules-v0.1.0-r4"
    - from: "core/color authority"
      to: "successor eligibility"
      via: "prior authority digest and cold proof"
      pattern: "PriorAuthorityRecordPath"
---

<objective>Complete ordered r4 publication/exact recovery, cold proofs, and Phase 8 closure.

Purpose: Deliver genuine Mooncakes usability without republish or history loss.
Output: Three exact authorities and tracked evidence for all requirements.</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-13-SUMMARY.md
@policy/phase-08-distribution.json
@release/qualification/phase-08-handoff-schema.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate r4 union and resolve core then color</name>
  <files>None (sanitized r4 hosted artifacts)</files>
  <action>Load fixed r4 handoff and validate schema/digests/files, LF clean root, unique state, r4 identity, four histories/set, and union. Mutation requires packet+literal receipt; exact forbids both. Exact core is consumed directly without preflight/dry/packet/receipt/re-observation. Mutation core re-observes and only absent may invoke one PublishOne; exact selects zero-mutation authority; ambiguity stops. Produce/index explicit public metadata and outside-checkout exact one-node cold proof, then resolve color using core predecessor through the same switch. Color absent may use one separate PublishOne; exact never republishes. Run CoreColorArtifacts.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r4-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -CoreColorArtifacts -HandoffPath $p }"</automated></verify>
  <done>r4 core/color each have one exact authority and cold proof in order.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and exact full graph</name>
  <files>None (sanitized r4 hosted artifacts)</files>
  <action>Reload r4 handoff/CoreColorArtifacts/four histories. Resolve image with color predecessor through exhaustive switch; absent may one PublishOne, exact zero mutation, uncertainty stops. Run outside-checkout empty-home credential-free consumer with pinned toolchain; require exact image->color->core 0.1.0 graph, structured metadata/archive/manifests/checksums/docs, js/wasm/wasm-gc/native, and bounded PPM behavior. Index, select image authority, run LiveArtifacts.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r4-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -LiveArtifacts -HandoffPath $p }"</automated></verify>
  <done>Image has exact authority and full graph/PPM proof on four targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r4 closure and offline Required</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Reciprocal record binds r4, four histories/set, union/receipt, and three authorities.
    - Exact has zero mutation; published-now exactly one predecessor-valid run.
    - Required preserves CRLF/UTC/exact14 receipt parity/no-tags regressions offline.
  </behavior>
  <action>Invoke ReciprocalArtifacts with fixed r4 handoff and authorities; replay union/reducer/predecessor/four-history/receipt rules and copy only closed sanitized projection to tracked record. Extend tracked qualification/Required, preserve CRLF, UTC, controller/workflow exact14 receipt parity, no-tags, and fixed-handoff isolation. Update COVERAGE for fourteen plans, r4 current, four terminal histories, absent packet+receipt versus exact, mutation-only preflight, D-01..19, and all requirements. Exclude Phase 9.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -TrackedClosure; if ($LASTEXITCODE) { exit $LASTEXITCODE }; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All requirements have reciprocal r4 evidence and offline Required passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fixed r4 union to mutation | Receipt-or-exact and histories replay. |
| Module authority to successor | Exact predecessor/cold proof gates. |
| Live artifacts to tracked closure | Only sanitized projection crosses. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-14-01 | Elevation of Privilege | union switch | critical | mitigate | Mutation requires receipt; only absent publishes once. |
| T-08-14-02 | Tampering | four-history chain | critical | mitigate | Individual/set/ref/observation/cold/predecessor digest replay. |
| T-08-14-03 | Spoofing | old state as r4 | critical | mitigate | Fixed r4 handoff, LF clone, unique state. |
| T-08-14-04 | Information Disclosure | tracked evidence | high | mitigate | Sanitized projection and offline Required. |
| T-08-14-05 | Denial of Service | ambiguity | medium | mitigate | Bounded stop, no rerun/republish. |
</threat_model>

<verification>Validate r4 union, four histories, ordered authorities, exact cold/metadata proofs, reciprocal closure, and offline Required.</verification>
<success_criteria>All modules are published or exactly checkpointed without republish and all Phase 8 requirements have r4 evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-14-SUMMARY.md` when done.</output>
