---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "11"
type: execute
wave: 11
depends_on: ["08-10"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "The committed r3-capable HEAD is pushed exactly before r3 tag creation; origin/main, annotated peel, LF clean clone, and locator bind one SHA."
    - "ExecutionRoot uses core.autocrlf=false and StateRoot is a new GUID-unique empty root; no r1/r2 locator, index, store, artifact, or fixed handoff is reused."
    - "Attempt-zero, r1, and r2 terminal-negative records are revalidated from immutable source/remote facts and bound into fresh r3 PrepareAttempt, store, packet, receipt, authority, and handoff."
    - "Exact-existing automatically yields packet/receipt-free AuthorityUnion; absent may reach packet then literal authorize-core|stop; Plan 08-11 never calls PublishOne."
    - "Fresh continuation treats the complete checkpoint display as untrusted and revalidates disk/remote/live absence before receipt or handoff."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r3"
      provides: "Annotated r3 tag peeled to exact pushed boundary"
    - path: "%TEMP%/mnf-phase08-r3-handoff.json"
      provides: "Closed eligible r3 AuthorityUnion handoff"
    - path: "<unique-r3-state-root>/authorization/authorize-core-receipt.json"
      provides: "Absent-branch literal receipt; absent on exact-existing"
  key_links:
    - from: "exact pushed HEAD"
      to: "r3 tag, LF clone, and unique state"
      via: "remote/peel/blob equality and UTC locator digest"
      pattern: "boundary_sha"
    - from: "three terminal histories"
      to: "fresh r3 attempt and union"
      via: "individual and ordered-set digests"
      pattern: "historical_r2_sha256"
    - from: "untrusted checkpoint packet"
      to: "literal receipt"
      via: "fresh disk/remote/schema/digest/LF/history/actor/dry-run/absence revalidation"
      pattern: "untrusted_recovery_inputs"
---

<objective>
Establish the exact LF-clean r3 boundary and produce one non-mutating, fully history-bound core-entry AuthorityUnion handoff.

Purpose: Cross the next live boundary without publishing and preserve the first-mutation decision after the r2 pre-dispatch failure.
Output: Exact r3 tag/clone/state, exclusive core authority, optional literal receipt, and fixed validated handoff.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-hosted-fields.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-10-SUMMARY.md
@release/qualification/phase-08-handoff-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail-close prior history and the empty r3 namespace</name>
  <files>None (read-only local, GitHub, registry, tag, run, and temp checks)</files>
  <action>Require 08-09/10 summaries and all their owned paths committed and clean without touching unrelated user dirt. Verify attempt-zero/run facts, annotated r1 peeled to 09548df948f58ec1bdfff7494757596c03e4c9bd, and annotated r2 peeled to 73a3af920fc3938f49e93d14f16f79f116475f1e. Prove r1 and r2 have no hosted workflow run, mutation, or authority; prove r2 terminal history includes PrepareAttempt, confirmed_absent, and failure before HostedPreflight dispatch due the old field-array helper. Verify r3 is absent locally/remotely and `%TEMP%/mnf-phase08-r3-handoff.json` is absent as file or directory. Any old/stale/malformed fixed-path object stops and is preserved; never overwrite/delete/rename/reuse it.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r3-handoff.json'; if(Test-Path -LiteralPath $h){throw 'P08-R3-HANDOFF-COLLISION'}; if(@(git ls-remote --tags origin refs/tags/modules-v0.1.0-r3 'refs/tags/modules-v0.1.0-r3^{}').Count -ne 0){throw 'P08-R3-EXISTS'}; $r2=@(git ls-remote --tags origin 'refs/tags/modules-v0.1.0-r2^{}'); if($r2.Count -ne 1 -or (@($r2[0]-split '\s+'))[0] -cne '73a3af920fc3938f49e93d14f16f79f116475f1e'){throw 'P08-R2-HISTORY'} }"</automated></verify>
  <done>All three prior histories are intact, r3/fixed handoff are absent, and any collision stops without mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Create the LF r3 boundary and persist eligible AuthorityUnion</name>
  <files>None (exact push, annotated r3 tag, unique clone/state, sanitized artifacts, optional receipt, and fixed handoff only)</files>
  <action>Require Task 1 facts unchanged. Push exact committed current HEAD, including planning and r3 static implementation, to refs/heads/main and require remote equality. Create independent GUID-suffixed ExecutionRoot and StateRoot; clone using `git -c core.autocrlf=false clone --no-tags`, set local core.autocrlf=false, detach at the exact SHA, and verify LF bytes, clean status, exact required blobs, and empty state. InitializeBoundary and validate canonical UTC locator. Create annotated r3 once without force, push it, and verify tag object plus peeled SHA. PrepareAttempt with exact attempt-zero/r1/r2 terminal records; require fresh r3 root/genesis/prepared/index/store and three individual plus ordered-set digests.

Invoke all helpers from the clean clone and run ObserveOnly mb-core first. Exact runs explicit structured observation and cold one-node js/wasm/wasm-gc/native proof, indexes both, selects packet/receipt-free ExactExistingAuthority, validates exact AuthorityUnion, then writes the fixed handoff. Absent alone may run HostedPreflight and PublisherDryRun, assemble one sanitized packet, and return a structured human-action checkpoint accepting only literal authorize-core or stop. Display these fields under `untrusted_recovery_inputs`: ExecutionRoot, StateRoot, BoundaryLocatorPath, BoundaryLocatorSha256, ActiveAttemptPath, ActiveAttemptFileSha256, ActiveAttemptLocatorSha256, ArtifactRoot, PacketPath, PacketSha256, ReleaseRef, BoundarySha, SourceSha, AttemptZeroHistorySha256, R1HistorySha256, R2HistorySha256, HistoricalAttemptSetSha256, PreparedManifestSha256, RootIntentSha256, IntentSha256. Label every value sanitized but untrusted and state that receipt/handoff do not yet exist.

On fresh literal authorize-core continuation, trust no in-memory object. Reload locator, active attempt, packet, all three histories, prepared/root/current identities, preflight, dry-run actor, and a newly obtained absence observation from disk/remote. Revalidate schemas, file/content/locator/ordered-set digests, paths, cross-bindings, r3 ref/source, clean LF ExecutionRoot HEAD/blobs, remote annotated r3 peel, immutable histories, actor exactly tchivs, dry-run success, credential isolation/removal, and immediately fresh confirmed_absent. Any drift/ambiguity/nonliteral/change stops. Only then persist the closed receipt, reload it and active attempt, validate receipt path/digest/locator update and mutation AuthorityUnion, and create fixed handoff for the first time. Stop writes neither receipt nor handoff. Mismatch/unknown/disagreement/timeout also stop. Never call PublishOne. Record handoff, union, packet/receipt/exact paths and digests, three histories, and ordered-set digest in 08-11-SUMMARY.md.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r3-handoff.json'; if(-not(Test-Path -LiteralPath $p -PathType Leaf)){throw 'P08-R3-HANDOFF-MISSING'}; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; $x=[string]$h.execution_root; $schema=Join-Path $x 'release/qualification/phase-08-handoff-schema.json'; if(-not((Get-Content -LiteralPath $p -Raw)|Test-Json -SchemaFile $schema -ErrorAction Stop)){throw 'P08-R3-HANDOFF-SCHEMA'}; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -Handoff -HandoffPath $p }"</automated></verify>
  <done>Exactly one eligible r3 AuthorityUnion exists, bound to three terminal histories; mutation has literal receipt, exact has none, and zero publication occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Ambient checkout to LF r3 clone | Only exact pushed bytes may execute. |
| Three prior attempts to r3 state | Histories are evidence only and cannot be reused as state. |
| Checkpoint display to fresh continuation | Every recovery input is untrusted until revalidated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-11-01 | Spoofing | clone/tag boundary | critical | mitigate | Exact remote/peel/blob equality, no-tags LF clone, clean status. |
| T-08-11-02 | Tampering | three histories/state | critical | mitigate | Individual/set digests, unique roots, closed schemas, no reuse. |
| T-08-11-03 | Elevation of Privilege | authorization continuation | critical | mitigate | Full fresh reload/revalidation before literal receipt and handoff. |
| T-08-11-04 | Information Disclosure | checkpoint/evidence | high | mitigate | Sanitized closed fields only; raw/secret-shaped data rejected. |
| T-08-11-05 | Repudiation | r2 termination | high | mitigate | Exact pre-dispatch failure stage and no-run/no-mutation facts are digest-bound. |
</threat_model>

<verification>Validate prior histories, exact push/tag/LF clone/state, structured checkpoint fields, fresh continuation, branch-exclusive receipt, fixed handoff, and absence of PublishOne.</verification>
<success_criteria>Plan 08-11 ends only with a schema/digest-valid r3 exact-existing or literal-receipt mutation AuthorityUnion.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-11-SUMMARY.md` when done.</output>
