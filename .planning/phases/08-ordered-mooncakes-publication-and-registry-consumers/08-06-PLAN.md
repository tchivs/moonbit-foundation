---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "06"
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Three separate authorized workflow runs publish at most one module each in canonical order: core, then color only after verified core observation/cold proof, then image only after verified color observation/cold proof (D-05, D-06)."
    - "Core passes exact one-node graph and checked behavior; color passes exact core-color graph and color behavior; image passes exact full graph and bounded PPM behavior on all four targets before the next state is verified (D-08 through D-12)."
    - "Any timeout, nonzero exit, absence after attempt, checksum disagreement, metadata drift, missing proof, or unknown stops the plan without automatic retry or downstream dispatch; exact existing matches checkpoint without republish and mismatches require forward correction (D-13 through D-15)."
    - "Final PROV-05 evidence compares exact qualified metadata for all three modules across structured public API/index/archive/manifest/assets/docs and contains no secret or raw response (D-16 through D-18)."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Reciprocal sanitized DIST-01..04 and PROV-05 evidence bindings"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "Phase 8 requirement, graph, prohibition, and artifact verification"
    - path: "scripts/quality/Invoke-MoonQuality.ps1"
      provides: "Credential-free Required integration for static Phase 8 evidence validation"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "Final requirement/decision/prohibition/API capability coverage"
    - path: "<system-temp>/mnf-phase08/phase-08-live-locator.json"
      provides: "Durable sanitized cross-process pointer to the one authorized artifact root and bound index"
  key_links:
    - from: "verified core checkpoint artifact"
      to: "color workflow eligibility"
      via: "exact journal predecessor plus core observation and four-target proof digests"
      pattern: "mb-core"
    - from: "verified color checkpoint artifact"
      to: "image workflow eligibility"
      via: "exact journal predecessor plus two-node observation and four-target proof digests"
      pattern: "mb-color"
    - from: "release/qualification/phase-08-requirements.json"
      to: "scripts/quality/Test-Phase08Qualification.ps1"
      via: "closed reciprocal requirement and artifact-digest validation"
      pattern: "DIST-01"
    - from: "<system-temp>/mnf-phase08/phase-08-live-locator.json"
      to: "scripts/quality/Test-Phase08Qualification.ps1"
      via: "each process recomputes the locator path, validates locator/root/index/run bindings, and passes the resolved root with explicit -ArtifactRoot"
      pattern: "ArtifactRoot"
---

<objective>
Execute the authorized ordered publication as three isolated one-module runs, prove each published graph through cold consumers, and close Phase 8 with sanitized reciprocal evidence.

Purpose: Make all three modules genuinely available from Mooncakes while preserving strict dependency gates, ambiguity stops, and the credential-free Required boundary.
Output: Verified core/color/image checkpoints, public metadata proof, Phase 8 reciprocal evidence, and Required integration.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md
@policy/phase-08-distribution.json
@.github/workflows/publish-modules.yml
@release/qualification/phase-07-requirements.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish and prove core, then publish and prove color</name>
  <files>None (use the helper unchanged from the authorized tagged source; Git tag/workflow runs and sanitized checkpoint artifacts are live outputs)</files>
  <read_first>.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md, .github/workflows/publish-modules.yml, policy/phase-08-distribution.json, scripts/quality/Get-MooncakesObservation.ps1, scripts/quality/Invoke-ColdRegistryConsumer.ps1</read_first>
  <action>Require the exact unchanged `authorize-core` packet from 08-05 and use the tagged `Invoke-Phase08HostedRun.ps1` contract without modifying tracked source. In this fresh process recompute `$locatorPath` only as `Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/phase-08-live-locator.json'`; require the locator, canonical root `<system-temp>/mnf-phase08/artifacts/<root-intent-sha256>/`, closed index, and `authorization/authorize-core.json` to exist from 08-05, then validate and resume them without deleting, replacing, truncating, or reinitializing evidence. Never rely on a process environment variable for handoff. Before dispatch, run the actual authorization-packet selector with explicit `-LocatorPath $locatorPath`, `-ArtifactRoot $artifactRoot`, and `-PacketPath $packetPath` to recompute every packet and evidence digest/current fact, including the unique bound PublisherDryRun run/artifact, exact prepared core, zero exit, credential teardown, fresh post-dry-run absence observation, hosted-native evidence, and local/remote `modules-v0.1.0` resolving to the authorized source SHA; do not create, move, force-update, or push the tag. Before each selector or helper invocation, recompute the deterministic locator path, reopen the locator, validate its digest and exact repository/workflow/tag/source/root/current-intent/authorization-packet bindings, canonicalize its artifact-root value, require that value equals the root-intent-derived expected root and the index root binding, and pass both `-LocatorPath $locatorPath` and `-ArtifactRoot $artifactRoot` explicitly. Re-observe core; ambiguity stops, exact existing content records a no-republish checkpoint and prevents a core mutation, mismatch requires forward correction, and only fresh exact absence permits the one authorized attempt. Invoke the helper in `PublishOne` mode for core with the literal unchanged packet and explicit locator/root. It snapshots run IDs before `gh workflow run`, captures exactly one new run by repository `tchivs/moonbit-foundation`, workflow `publish-modules.yml`, event `workflow_dispatch`, head tag/ref `modules-v0.1.0`, and authorized head SHA, persists sanitized `gh run view` metadata, waits for terminal success, and downloads only the exact checkpoint artifact into `runs/<run-id>/artifacts/checkpoint/`. Before selection, re-resolve and validate locator/root/index and require artifact/run metadata bind repository, workflow, event, tag/ref, source SHA, root/current intent, run ID/attempt, `mb-core`, start intent, journal predecessor, and packet digest. Reject stale timestamps, unindexed or colliding files, symlinks/path escape, duplicate names, mixed run IDs, extra module attempts, partial state, and raw/secret-shaped output. Accept core only on exact observation and a brand-new four-target one-node cold proof. Re-resolve and validate the same locator/root/index, then repeat the unique dispatch/download/selection flow for color using a distinct run and the exact verified core predecessor; require `mb-color`, resume intent, and core checkpoint/proof bindings before its exact two-node/four-target proof counts. Persist each new artifact by exclusive temporary write plus atomic rename and update `index.json` atomically with content digests after each run; exact indexed immutable evidence may be resumed only after digest/binding validation and is never overwritten. On absent/unknown/mismatch/incomplete evidence, stop without another mutation or retry. Never print environment values, request headers, credential paths/content, CLI authentication output, or raw live responses.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { $locatorPath = Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/phase-08-live-locator.json'; if (-not (Test-Path -LiteralPath $locatorPath -PathType Leaf)) { throw 'P08-LOCATOR-MISSING' }; $locator = Get-Content -LiteralPath $locatorPath -Raw | ConvertFrom-Json -Depth 30; $artifactRoot = [IO.Path]::GetFullPath([string]$locator.artifact_root); $packetPath = Join-Path $artifactRoot 'authorization/authorize-core.json'; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -AuthorizationPacket -LocatorPath $locatorPath -ArtifactRoot $artifactRoot -PacketPath $packetPath; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -CoreColorArtifacts -LocatorPath $locatorPath -ArtifactRoot $artifactRoot; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }"</automated>
  </verify>
  <acceptance_criteria>Two distinct runs attempted no more than core and color respectively; color was dispatched only from a complete verified core checkpoint; both exact graphs and four-target behaviors are verified.</acceptance_criteria>
  <done>The core/color artifact selector exits zero, proves the pre-existing authorized tag remained bound to the unchanged source and packet, and proves separate core and color runs with color gated by exact core observation and four-target proof.</done>
</task>

<task type="auto">
  <name>Task 2: Publish image and prove the complete public graph and metadata</name>
  <files>None (workflow run and sanitized checkpoint artifacts only)</files>
  <read_first>The verified core/color artifact root from Task 1, policy/phase-08-distribution.json, qualification/registry-consumers/mb-image/main/main.mbt, scripts/quality/Get-MooncakesObservation.ps1, scripts/quality/Invoke-ColdRegistryConsumer.ps1</read_first>
  <action>Per D-05, D-06, and D-13 through D-18, start this new process by deterministically recomputing `$locatorPath` as `<system-temp>/mnf-phase08/phase-08-live-locator.json`, reopening it, validating its digest and authorization bindings, canonicalizing its artifact-root value, and validating locator/root/index equality before reading any predecessor or invoking any selector/helper. Pass `-LocatorPath $locatorPath` and `-ArtifactRoot $artifactRoot` explicitly every time; do not read or depend on `PHASE08_ARTIFACT_ROOT`. Require the index to contain exactly the distinct bound core and color runs from Task 1 and revalidate the exact color predecessor. Re-observe image immediately before dispatch: ambiguity stops, exact existing content records a no-republish checkpoint, mismatch requires forward correction, and only fresh exact absence permits one image attempt. Use the same helper with explicit locator/root to snapshot run IDs, dispatch image only, capture exactly one new matching workflow-dispatch run, wait for terminal success, and download the exact image checkpoint into `runs/<image-run-id>/artifacts/checkpoint/`. Before selection and before atomically updating `index.json`, reopen and revalidate locator/root/index plus repository/workflow/event/tag/ref/SHA/root/current intent/run/attempt/`mb-image`/resume/predecessor bindings; reject stale, duplicate, mixed-run, extra-module, ambiguous, raw, or secret-shaped evidence. Accept image only after public observation and a newly empty registry-only consumer prove the exact core-color-image 0.1.0 graph and bounded PPM encode/decode on js, wasm, wasm-gc, and real native. Re-resolve and validate locator/root/index again before the final `-LiveArtifacts` selector and structured public observation across user/module, registry index, archive/checksum, downloaded manifest, and versioned assets/docs for all three modules. Compare every required qualified metadata field exactly. Missing, empty, drifted, stale, mixed, or ambiguous facts block completion without registry mutation. Persist only sanitized content-addressed checkpoint artifacts; immutable ledger/provenance closure remains outside this phase.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { $locatorPath = Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/phase-08-live-locator.json'; if (-not (Test-Path -LiteralPath $locatorPath -PathType Leaf)) { throw 'P08-LOCATOR-MISSING' }; $locator = Get-Content -LiteralPath $locatorPath -Raw | ConvertFrom-Json -Depth 20; $artifactRoot = [IO.Path]::GetFullPath([string]$locator.artifact_root); &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -LiveArtifacts -LocatorPath $locatorPath -ArtifactRoot $artifactRoot; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }"</automated>
  </verify>
  <acceptance_criteria>The image run attempted no other module; the exact three-node graph and PPM behavior pass on four real targets; all three public metadata projections exactly match qualified source with no secret/raw response.</acceptance_criteria>
  <done>The live-artifact selector exits zero and proves the image-only run, exact three-node graph, four real target PPM behavior, and exact sanitized metadata agreement for all three modules.</done>
</task>

<task type="auto">
  <name>Task 3: Bind reciprocal evidence and preserve credential-free Required</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <read_first>The complete sanitized Phase 8 artifact root from Tasks 1-2, release/qualification/phase-07-requirements.json, scripts/quality/Test-Phase07Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</read_first>
  <action>Extend the already-created `Test-Phase08Qualification.ps1` with a `-ReciprocalArtifacts` live-closure selector plus reciprocal/static tracked-record validation; do not first-create, rename, weaken, or replace its fixture-tested `-CoreColorArtifacts`, `-LiveArtifacts`, `-AuthorizationPacket`, `-LocatorPath`, `-ArtifactRoot`, or `-PacketPath` contracts. At the start of this fresh process, recompute `$locatorPath` only from system temp, reopen it, canonicalize `$artifactRoot`, derive `$packetPath`, validate exact locator/root/index/repository/workflow/tag/SHA/root-intent/current-intent/packet equality and all indexed digests, and pass explicit `-LocatorPath` and `-ArtifactRoot` to `-ReciprocalArtifacts` before consuming any live evidence; also pass explicit `-PacketPath` to the authorization selector. Never depend on process environment persistence. Only after those live selectors close successfully, create a closed tracked reciprocal Phase 8 requirements record that binds DIST-01 through DIST-04 and PROV-05 to the sanitized observation, archive/manifest, graph, toolchain, target, behavior, journal, run, and artifact digests without copying raw live output. The reciprocal/static extension checks all requirement-to-evidence adjacencies, exact canonical order, empty/missing failure, all prohibitions, and API capability decisions in COVERAGE.md. Integrate only validation of those tracked reciprocal records and their static schemas/digests into credential-free Required; Required must never read the outside-checkout live locator/root, contact live Mooncakes, access a secret, dispatch, create a tag, or publish. Do not add Phase 9 immutable ledger, release provenance, or milestone-audit fields.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { $locatorPath = Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/phase-08-live-locator.json'; if (-not (Test-Path -LiteralPath $locatorPath -PathType Leaf)) { throw 'P08-LOCATOR-MISSING' }; $locator = Get-Content -LiteralPath $locatorPath -Raw | ConvertFrom-Json -Depth 30; $artifactRoot = [IO.Path]::GetFullPath([string]$locator.artifact_root); $packetPath = Join-Path $artifactRoot 'authorization/authorize-core.json'; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -AuthorizationPacket -LocatorPath $locatorPath -ArtifactRoot $artifactRoot -PacketPath $packetPath; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -ReciprocalArtifacts -LocatorPath $locatorPath -ArtifactRoot $artifactRoot; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; &amp; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }"</automated>
  </verify>
  <acceptance_criteria>All five requirements have reciprocal exact evidence, every prohibition/API decision is checked, full Required remains credential-free and passes, and tracked release source remains free of secrets and raw live responses.</acceptance_criteria>
  <done>The Phase 8 qualification selector and full Required both exit zero, all five requirement bindings validate reciprocally, and scans find no tracked credential or raw live response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Explicit authorization to Git tag/workflow dispatch | Exact immutable inputs govern the first production mutation. |
| One verified module checkpoint to successor dispatch | External exact observation and cold proof are mandatory authorization dependencies. |
| Live workflow artifacts to tracked reciprocal evidence | Only sanitized content digests/classifications may enter the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Elevation of Privilege | ordered workflow dispatch | critical | mitigate | Revalidate explicit core authorization and exact verified predecessor before each separate one-module run. |
| T-08-06-02 | Tampering | registry module/archive/graph | critical | mitigate | Independent structured observation, archive checksum, manifest, exact graph, and four-target behavior before checkpoint. |
| T-08-06-03 | Information Disclosure | live artifacts/evidence | high | mitigate | Consumer/observer jobs receive no secret; raw output is discarded; closed reciprocal records contain allowlisted digests only. |
| T-08-06-04 | Repudiation | workflow run/journal chain | high | mitigate | Bind repository, run/artifact identity, sequence, prior digest, source, root/current intent, and terminal observation/proof digests. |
| T-08-06-05 | Denial of Service | propagation/target proof | medium | mitigate | Bounded polling and terminal stop; no automatic retry or successor dispatch on uncertainty. |
</threat_model>

<artifacts_this_phase_produces>
- Three verified one-module workflow checkpoints in strict dependency order.
- Exact cold registry-only proofs for core, core-color, and the full graph on four targets.
- Credential-free structured public metadata proof for all three published modules.
- Closed reciprocal Phase 8 requirement evidence and Required integration.
</artifacts_this_phase_produces>

<verification>
Validate live artifacts, reciprocal coverage, all prohibitions, Phase 7 regressions, and full credential-free Required; confirm no tracked secret/raw output and no Phase 9 ledger scope.
</verification>

<success_criteria>
Core, color, and image are genuinely published in order, independently consumable from cold registry state with exact graphs and behavior, and publicly render exact qualified metadata.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-06-SUMMARY.md` when done.
</output>
