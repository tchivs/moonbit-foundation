---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "32"
type: execute
wave: 32
depends_on: ["08-31"]
files_modified: []
autonomous: false
requirements: [DIST-01, DIST-04]
user_setup:
  - service: GitHub Actions / Mooncakes
    why: "Only future protected publisher dry-run and same-turn authorization use the configured environment; the credential value is never inspected."
must_haves:
  truths:
    - "r12 source/tag object/peel/detached remote-clone HEAD/clone policy/boundary bind one tested SHA, and PrepareAttempt is launched only by the canonical clone-policy wrapper."
    - "The exact eight-path user-dirty baseline stays unchanged; every other worktree drift fails closed and artifacts use exact committed SHA or clean clone."
    - "Non-publishing r12 ends exact-existing without packet/receipt/handoff or absent with one packet and no receipt/handoff, both zero mutation."
    - "Only absent displays same-turn `authorize-core`; exact skips and stop leaves no receipt/handoff."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r12"
      provides: "unique r12 immutable tag"
    - path: "%TEMP%/mnf-phase08-r12-user-dirty-baseline.json"
      provides: "closed allowed-dirty baseline"
    - path: "%TEMP%/mnf-phase08-r12-preauthorization.json"
      provides: "closed r12 branch projection"
    - path: "%TEMP%/mnf-phase08-r12-handoff.json"
      provides: "authorized-absent-only handoff"
---
<objective>Create and non-publishing-qualify r12 through its canonical remote-clone boundary invocation, then stop at the absent-only core decision.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-29-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-30-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-31-SUMMARY.md
@scripts/quality/Invoke-Phase08R12Boundary.ps1
@scripts/quality/Invoke-Phase08R12PreLive.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1</context>
<tasks>
<task type="auto">
  <name>Task 1: Establish canonical non-publishing r12 boundary</name>
  <files>None (Git refs, disposable remote clone, GUID-owned state, and sanitized temporary artifacts)</files>
  <action>Baseline and preserve exactly `.planning/config.json`, `docs/governance/decisions/0001-sole-owner-bootstrap.md`, `docs/governance/rfc-process.md`, `.codebase-memory/`, `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/research-plan-input.json`, `.planning/research/.cache/`, `.github/workflows/quality.yml`, and `.planning/quick/260719-fix-github-actions-ci/PLAN.md` by Git status/content hash before/after every side effect and task; all other drift stops. Allowed paths are never staged, edited, deleted, reset, cleaned, stashed, or included in any committed source, tag, clone, prepared artifact, publisher artifact, or evidence payload. Re-run 08-29 through 08-31 gates, including the real remote-clone canonical-ref test. Push exact tested committed HEAD by SHA, create/push one non-force r12 annotated tag, and verify the remote annotated tag object and peel.

Create the required disposable `--no-local --no-tags` remote clone. Explicitly fetch `refs/tags/modules-v0.1.0-r12` into that clone before any boundary call, then require the clone-local tag object and peeled source, detached clone HEAD, clone-local policy literal, and InitializeBoundary SHA all equal the intended r12 identity. Invoke `Invoke-Phase08R12Boundary.ps1` with clone/root/state/tag identity inputs only; it must derive the release ref from clone policy, so no caller ReleaseRef may be supplied. Require its closed invocation record and all twelve histories before preparation. Run one hosted preflight and one publisher dry run with publisher-only temporary Moon home and teardown, then fresh credential-free core observation. Emit pre-authorization: exact authority with packet/receipt/handoff absent or absent with one packet and receipt/handoff absent, zero mutation. No PublishOne, mutation dispatch, or registry-mutating command.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R12PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath "$env:TEMP/mnf-phase08-r12-preauthorization.json" -HandoffPath "$env:TEMP/mnf-phase08-r12-handoff.json"</automated></verify>
  <done>r12 canonical clone boundary has one valid zero-mutation branch and unchanged dirty baseline.</done>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Dynamic absent-only authorize-core checkpoint</name>
  <files>None (only authorized absent branch may emit receipt/handoff)</files>
  <decision>Authorize one core mutation from revalidated r12 absent evidence, or stop.</decision>
  <context>Emit only after confirmed absence and display clone-policy canonical ref, r12 tag object/peel/source, r11 terminal facts/twelve histories, wrapper proof, prepared/preflight/dry/actor/teardown, observation/packet/projection digests, zero mutation, and unchanged baseline. Exact-existing has no packet/receipt/handoff and ends without this prompt. No Mooncakes authority is inferred.</context>
  <action>Recompute baseline and all displayed identity/authority facts. Accept only same-turn literal `authorize-core` or `stop`. Authorized absent creates receipt/handoff then ValidateHandoff; stop/exact retain no receipt/handoff and ValidatePreAuthorization zero mutation. Any drift, stale observation, mismatch, or malformed response stops without mutation; recompare baseline after the branch.</action>
  <options><option id="authorize-core"><name>authorize-core</name><pros>Enables only Plan 08-33’s one core run.</pros><cons>Next plan can make one irreversible operation after revalidation.</cons></option><option id="stop"><name>stop</name><pros>Preserves r12 zero-mutation evidence.</pros><cons>Publication remains blocked.</cons></option></options>
  <resume-signal>Type exactly `authorize-core` or `stop`.</resume-signal>
  <verify><automated>pwsh -NoProfile -Command "$h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r12-handoff.json'; $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r12-preauthorization.json'; if(Test-Path $h){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $h; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $p -HandoffPath $h; exit $LASTEXITCODE"</automated></verify>
  <done>Only same-turn absent authorization creates receipt/handoff; all other outcomes leave zero mutation and baseline unchanged.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| caller to clone wrapper | Canonical policy ref cannot be overridden. |
| user dirties to artifacts | Baseline preserves user state and excludes it from release identity. |
| pre-authorization to decision | Fresh absent evidence is required for receipt. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-32-01 | Spoofing | r12 caller ref | critical | mitigate | Explicit clone tag fetch plus wrapper-only clone-policy ref derivation. |
| T-08-32-02 | Tampering | eight-path dirty baseline | high | mitigate | Hash/status before and after every side effect; forbid every baseline path from artifact inclusion. |
| T-08-32-03 | Information Disclosure | dry credential | critical | mitigate | Publisher-only temporary Moon home. |
| T-08-32-04 | Elevation of Privilege | core receipt | critical | mitigate | Same-turn absent-only revalidation. |
</threat_model>
<verification>Re-run static/seam/remote-ref/pre-live gates, prove wrapper identity, zero-mutation branch, and baseline equality. No registry mutation.</verification>
<success_criteria>Only a canonical r12 clone boundary can reach a future human authorization; no ref override or dirty-state drift can enter it.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-SUMMARY.md` when done.</output>
