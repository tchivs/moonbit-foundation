---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "17"
type: execute
wave: 17
depends_on: ["08-16"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Exact r6-capable committed HEAD is pushed before the r6 tag; remote main, annotated tag peel, LF clean clone, boundary locator, and source blobs bind one SHA (D-01, D-07)."
    - "GUID-unique ExecutionRoot and StateRoot reuse no r5 state, active attempt, locator, index, store, tag, or fixed handoff."
    - "All six terminal histories and the ordered set bind fresh r6 prepare, packet, optional receipt, authority, and handoff."
    - "Exact-existing auto-completes without preflight, dry run, packet, or receipt; absent returns a non-auto-approvable dynamic human-action checkpoint accepting only same-turn literal authorize-core or stop; neither branch calls PublishOne (D-04, D-14, D-15)."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R6PreLive.ps1"
      provides: "Mandatory zero-write selector that gates every live side effect"
    - path: "refs/tags/modules-v0.1.0-r6"
      provides: "Annotated r6 boundary"
    - path: "%TEMP%/mnf-phase08-r6-handoff.json"
      provides: "Eligible six-history r6 AuthorityUnion"
  key_links:
    - from: "attempt-zero plus r1 through r5 histories"
      to: "r6 AuthorityUnion"
      via: "six individual digests and canonical ordered-set digest"
      pattern: "historical_r5_sha256"
    - from: "dynamic checkpoint"
      to: "authorization receipt"
      via: "same-checkpoint-turn user literal followed by full fresh reload and revalidation"
      pattern: "untrusted_recovery_inputs"
---

<objective>Establish the LF-clean r6 boundary and select a non-mutating six-history AuthorityUnion.

Purpose: Cross the corrected hosted boundary after r5's pre-run validation failure while preserving the explicit first-mutation checkpoint.
Output: r6 tag, clean clone, unique state, exclusive authority, optional literal receipt, and fixed handoff; no PublishOne.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-workflow-duplicate-env.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-16-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute the zero-write pre-live gate before every live side effect</name>
  <files>None (read-only local, GitHub, registry, ref, run, and temporary-state checks)</files>
  <action>Per D-01, D-04, D-07, D-14, D-15, and D-19, make this task a strict side-effect boundary: before any push, tag, ExecutionRoot/StateRoot or other root creation, dispatch, secret access, or registry action, execute `scripts/quality/Invoke-Phase08R6PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin` from the committed HEAD. Do not replace any part with prose or ad hoc checks. Require its closed stdout result to prove attempt-zero and r1 through r5 refs/peels/digests/set; exact r5 source/tag; completed PrepareAttempt; fresh `confirmed_absent`; duplicate-env HostedPreflight rejection before run creation; r5 publish workflow runs zero; PublisherDryRun/packet/receipt/handoff/PublishOne/mutation/successor effects zero; every historical root/index/store/locator present with immutable digest; exact 08-15/16 file ownership committed-clean; both summaries committed with ancestor commits at HEAD; and r6 tag/fixed handoff absent. Recompute the result digest in memory, record only its sanitized closed fields after the gate passes, and stop on nonzero exit, extra output, missing field, disagreement, or ambiguity. This task performs no writes or live action.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R6PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin</automated></verify>
  <done>The executable zero-write selector passes completely; only then may Task 2 begin push/tag/root/dispatch work.</done>
</task>

<task type="auto">
  <name>Task 2: Create the LF-clean r6 boundary and eligible dynamic AuthorityUnion</name>
  <files>None (exact push/tag, unique state, sanitized artifacts, optional receipt, and fixed handoff)</files>
  <action>Only after Task 1's selector passes, per D-01 through D-07 and D-13 through D-19, push the exact committed r6 implementation and planning HEAD to origin/main and require local/remote equality before tagging. Treat any rerun of this task as requiring a fresh successful selector execution before the first side effect. Create new GUID-owned ExecutionRoot and StateRoot; clone with `git -c core.autocrlf=false clone --no-tags`, detach the exact remote SHA, and verify clean LF bytes, tracked blobs, no tags, empty owned state, and no path overlap with any historical root reported by the selector. Initialize the UTC boundary locator. Create and push one annotated `modules-v0.1.0-r6` tag without force, verify tag object and peel, then PrepareAttempt with initial 0.1.0, sequence zero, root=current, no predecessor, all six histories/set, and a fresh root/genesis/prepared/index/store.

Run fresh credential-free ObserveOnly for core. If exact, run the structured public observation and outside-checkout empty-MOON_HOME four-target/native cold proof, index/select packet-and-receipt-free ExactExistingAuthority, validate the six-history union, and first-write the fixed handoff. If absent, and only if absent, run HostedPreflight and PublisherDryRun; require exactly one non-mutating hosted run per selector, clean equal-empty snapshots, unique workflow env keys, exact14 receipt parity, actor `tchivs`, dry success, credential removal, fresh absence, and zero PublishOne. Generate the sanitized mutation packet, then emit a dynamic `checkpoint:human-action` that cannot be auto-approved by autonomous mode, auto-advance, defaults, agents, prior login, prior push permission, or earlier general consent.

The checkpoint must display exactly the sanitized recovery binding needed for an independent decision: ExecutionRoot, StateRoot, BoundaryLocatorPath, BoundaryLocatorSha256, ActiveAttemptPath, ActiveAttemptFileSha256, ActiveAttemptLocatorSha256, ArtifactRoot, PacketPath, PacketSha256, ReleaseRef, BoundarySha, SourceSha, AttemptZeroHistorySha256, R1HistorySha256, R2HistorySha256, R3HistorySha256, R4HistorySha256, R5HistorySha256, HistoricalAttemptSetSha256, PreparedManifestSha256, RootIntentSha256, IntentSha256, HostedPreflightRunId/Attempt, PublisherDryRunRunId/Attempt, observed actor, dry-run disposition, core absence observation digest/timestamp, and confirmation that PublishOne/mutation count is zero. Only the user's verbatim case-sensitive `authorize-core` in the same checkpoint turn resumes; literal `stop` terminates without receipt or handoff; all other text remains paused.

On authorize-core, perform a fresh same-turn continuation from persisted paths: reload locator, active attempt, packet, six history files/set, prepared bundle, root/current intent, hosted run receipts, actor evidence, dry evidence, and a new absence observation. Revalidate every schema, file digest, locator digest, ordered set, path containment, cross-binding, clean LF HEAD/blob identity, origin/main equality, r6 tag peel, unique env mappings, exact14 propagation, equal-empty snapshots, exact actor, successful credential cleanup, fresh absence, and zero PublishOne. Any drift, timeout, missing artifact, ambiguity, or exact/mismatch transition stops without receipt/handoff. Only after all checks pass, persist and reload the literal receipt, update and reload active state, validate the exclusive mutation AuthorityUnion, and first-write the fixed handoff. Record exact/existing or authorized-absent branch evidence in the summary. This plan never invokes PublishOne.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r6-handoff.json'; if(-not(Test-Path -LiteralPath $p -PathType Leaf)){throw 'P08-R6-HANDOFF-MISSING'}; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; $x=[string]$h.execution_root; $s=Join-Path $x 'release/qualification/phase-08-handoff-schema.json'; if(-not((Get-Content -LiteralPath $p -Raw)|Test-Json -SchemaFile $s -ErrorAction Stop)){throw 'P08-R6-HANDOFF-SCHEMA'}; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -Handoff -HandoffPath $p }"</automated></verify>
  <done>One eligible six-history r6 union exists: exact-existing has no packet/receipt, absent has a same-turn literal receipt, and publication count remains zero.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Ambient checkout to LF r6 clone | Only exact pushed tracked bytes may establish the boundary. |
| Six terminal histories to r6 state | Histories are evidence and cannot be reused as current state. |
| Dynamic checkpoint to continuation | Every displayed value remains untrusted until fresh same-turn reload. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-17-01 | Spoofing | clone/tag/main | critical | mitigate | Remote main, annotated peel, clean LF blobs, and locator bind one SHA. |
| T-08-17-02 | Tampering | histories/state | critical | mitigate | Six digests/set and GUID roots with r5 path non-overlap. |
| T-08-17-03 | Elevation of Privilege | authorization checkpoint | critical | mitigate | Same-turn verbatim user literal followed by complete fresh reload. |
| T-08-17-04 | Tampering | hosted receipts | high | mitigate | Unique env keys, exact14 parity, run identity, snapshot, actor, and absence revalidation. |
| T-08-17-05 | Information Disclosure | checkpoint | high | mitigate | Closed sanitized field list; no secret or raw output. |
</threat_model>

<verification>Validate six histories, exact r5 no-run terminal facts, r6 LF boundary, unique state, complete dynamic checkpoint, same-turn recovery, receipt-or-exact union, fixed handoff, and zero PublishOne.</verification>
<success_criteria>08-17 ends only with a valid r6 exact-existing union or a literal-receipt mutation union ready for Plan 08-18; it performs no registry mutation.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-17-SUMMARY.md` when done.</output>
