---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "12"
type: execute
wave: 12
depends_on: ["08-11"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher/workflow/controller accept only r4 and bind four histories/set."
    - "Controller fields, workflow_dispatch declarations, and downstream propagation are the identical ordered 14-name contract including authorization_receipt_sha256."
    - "Start-empty and resume-valid packet/receipt parity regressions preserve the 04704b4 fix; obsolete 17-input fields are rejected."
    - "CRLF/LF, UTC digest, no-tags fixture, and LibraryOnly handoff isolation regressions remain active."
  artifacts:
    - path: ".github/workflows/publish-modules.yml"
      provides: "Exact r4 14-input workflow contract and packet/receipt propagation"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r4 four-history hosted state and exact 14-field dispatch"
    - path: "scripts/quality/Test-Phase08LiveSeam.ps1"
      provides: "Controller/workflow parity, receipt, UTC, LF, and isolation tests"
  key_links:
    - from: "controller 14 fields"
      to: "workflow declarations and job environments"
      via: "exact count/order/names and packet+receipt propagation"
      pattern: "authorization_receipt_sha256"
    - from: "four histories"
      to: "hosted 14-field vector"
      via: "canonical historical_attempts_sha256"
      pattern: "historical_attempts_sha256"
---

<objective>Wire r4 through publisher/workflow/hosted seams while preserving exact 14-field receipt parity and all isolation regressions.

Purpose: Ensure r4 HostedPreflight reaches GitHub run creation with the same closed contract on both sides.
Output: r4-capable hosted implementation and credential-free parity/adversarial tests.</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-hosted-fields.md
@.planning/debug/phase08-workflow-receipt-input.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-11-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce r4 and four histories in publisher and workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - r4 request binds current/root/prepared plus four histories/set.
    - r3-current, history drift, correction semantics, or second mutation fail before credentials.
    - Workflow keeps exact 14 inputs and packet/receipt propagation to prepare/publisher/LiveOneStep.
  </behavior>
  <action>Advance publisher, adapter, and workflow to r4/four histories while preserving one-module maximum, isolated MOON_HOME/toolchain, exact actor, and raw/secret rejection. Keep controller/workflow 14-input parity and packet/receipt pair; reject obsolete mutation-prefixed/legacy inputs. Add r3-current, history replacement/aggregate, second mutation, correction, and parity negatives. Commit this four-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Publisher/workflow accepts only r4 and retains exact 14-input receipt parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve exact14 receipt parity, UTC, LF, no-tags, and handoff isolation</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1</files>
  <behavior>
    - Exact ordered fields are operation_mode, run_mode, release_ref, source_sha, root_intent_sha256, intent_sha256, prepared_manifest_sha256, historical_attempts_sha256, target_module, live_authorization, prior_run_id, prior_artifact_name, authorization_packet_sha256, authorization_receipt_sha256.
    - Workflow declarations equal those 14 names exactly; start uses empty receipt, resume uses valid packet/receipt, and both propagate without stale aliases.
    - History set is recomputed from four individually validated records.
    - UTC, core.autocrlf=false/LF, --no-tags, and test-only GUID handoff isolation remain fail-closed.
  </behavior>
  <action>Extend hosted preparation/receipt/active attempt/union/handoff to r4/four histories. Preserve the grouped 14-field builder and add exact mechanical parity between controller names, workflow declarations, and job propagation for start-empty and resume-valid receipt cases. Retain regression for old controller14/workflow17 mismatch, missing receipt declaration, stale aliases, ungrouped-if, and collapsed vector. Preserve UTC offset/reload/tamper/stale, LF clone, no-tags fixture, and LibraryOnly-only HandoffPath/TempRoot tests. Production fixed `%TEMP%/mnf-phase08-r4-handoff.json` is non-overridable; all GUID fixtures clean only owned paths and assert the real path absent before/after. Run focused and adjacent suites and commit atomically without live actions.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -HostedFieldsOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r4 hosted state passes exact14 controller/workflow/receipt parity plus UTC/LF/no-tags/handoff regressions.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Controller fields to workflow parser | Exact declaration parity is required before run creation. |
| Four histories to aggregate dispatch field | Aggregate cannot replace individual artifact bindings. |
| Tests to production refs/handoff | Fixtures cannot inherit or mutate permanent state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-12-01 | Tampering | 14-field parity | critical | mitigate | Mechanical controller/declaration/propagation equality and start/resume fixtures. |
| T-08-12-02 | Spoofing | four-history aggregate | critical | mitigate | Recompute from four schema/digest-valid records. |
| T-08-12-03 | Elevation of Privilege | packet/receipt propagation | critical | mitigate | Paired digest validation; obsolete aliases rejected. |
| T-08-12-04 | Tampering | fixture isolation | high | mitigate | no-tags, GUID roots, LibraryOnly injection, fixed-path absence. |
| T-08-12-05 | Information Disclosure | hosted evidence | high | mitigate | Isolated credential and raw/secret rejection. |
</threat_model>

<verification>Run publisher, HostedFieldsOnly, WorkflowOnly, full live seam, qualification, and observation suites; confirm exact14 parity and no live effects.</verification>
<success_criteria>The r4 hosted seam is statically ready for one new live boundary with exact receipt-aware dispatch parity.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-12-SUMMARY.md` when done.</output>
