---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "12"
type: execute
wave: 12
depends_on: ["08-11"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "08-12 accepts only the fixed schema/digest-valid r3 AuthorityUnion: mutation has packet+literal receipt; exact-existing has neither; stop is ineligible."
    - "All authority/observation/cold/receipt records bind attempt-zero, r1, r2, their ordered set digest, and r3 boundary/current/prepared identity."
    - "Core, color, and image resolve strictly in order with at most one module mutation per hosted run and no exact-content republish (D-05 through D-07)."
    - "Every module has exact structured metadata, archive/manifest/checksum, dependency graph, four targets/native runtime, public behavior, and predecessor proof (D-08 through D-18)."
    - "Reciprocal tracked closure proves DIST-01..04 and PROV-05 while Required remains offline, credential-free, and non-mutating."
  artifacts:
    - path: "%TEMP%/mnf-phase08-r3-handoff.json"
      provides: "Eligible r3 union start"
    - path: "<r3-artifact-root>/authorities/mb-core/*.json"
      provides: "Exact core authority"
    - path: "<r3-artifact-root>/authorities/mb-color/*.json"
      provides: "Exact color authority with core predecessor"
    - path: "<r3-artifact-root>/authorities/mb-image/*.json"
      provides: "Exact image authority with full graph"
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Tracked reciprocal closure"
  key_links:
    - from: "r3 handoff"
      to: "core eligibility"
      via: "receipt-or-exact union and three-history replay"
      pattern: "modules-v0.1.0-r3"
    - from: "core then color authority"
      to: "successor mutation eligibility"
      via: "prior_authority_record_sha256 plus exact cold proof"
      pattern: "PriorAuthorityRecordPath"
---

<objective>
Complete ordered r3 core/color/image publication or exact-existing recovery, prove cold consumption, and close Phase 8 evidence.

Purpose: Deliver genuine Mooncakes usability without ambiguity-driven republish or loss of attempt history.
Output: Three ordered exact authorities and tracked evidence for all five requirements.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-11-SUMMARY.md
@policy/phase-08-distribution.json
@release/qualification/phase-08-handoff-schema.json
@release/qualification/phase-08-authority-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate r3 union and resolve core then color</name>
  <files>None (sanitized r3 hosted artifacts only)</files>
  <action>Load `%TEMP%/mnf-phase08-r3-handoff.json` and validate closed schema, canonical digest, every referenced file/path, clean LF ExecutionRoot, unique StateRoot, r3 ref/boundary/current/prepared identity, all three terminal histories and ordered set, and AuthorityUnion. Mutation requires unchanged packet and literal receipt; exact forbids both; reject stop/other. Exact core is consumed directly without preflight/dry/packet/receipt/re-observation. Mutation core is freshly observed and only absent may invoke one PublishOne; exact selects zero-mutation authority; mismatch/unknown/disagreement/timeout stop. After core public observation and outside-checkout one-node cold proof, resolve color with exact core predecessor. Color absent may invoke one separate PublishOne run; exact never republishes. For both modules explicitly materialize/index structured metadata and exact js/wasm/wasm-gc/native cold proof, then select authority and run CoreColorArtifacts.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r3-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -CoreColorArtifacts -HandoffPath $p }"</automated></verify>
  <done>Eligible r3 core and color each have one exact authority and four-target cold proof in strict order.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove the exact full graph</name>
  <files>None (sanitized r3 hosted artifacts only)</files>
  <action>Reload and revalidate the r3 handoff plus CoreColorArtifacts and all three histories. Resolve image with color as predecessor through the same exhaustive switch; absent may invoke one separate PublishOne, exact has zero mutation, and every uncertainty branch stops. Run an outside-checkout consumer with new empty MOON_HOME, pinned MOON_TOOLCHAIN_ROOT, no credential/moon.work/path/Git/copied source/warm state. Require exact graph mb-image@0.1.0 -> mb-color@0.1.0 -> mb-core@0.1.0, structured metadata/archive/manifests/checksums/docs, four real targets/native, and bounded PPM behavior. Index proof, select image authority, and run LiveArtifacts.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r3-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -LiveArtifacts -HandoffPath $p }"</automated></verify>
  <done>Image has one exact authority and the full exact graph plus PPM proof passes all four targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r3 closure and offline Required</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - ReciprocalArtifacts binds all requirements to r3, three histories/set, union variant, optional receipt, and three ordered authorities.
    - Exact-existing has zero mutation; published-now has exactly one mutation and one predecessor-valid hosted run.
    - Required validates only closed tracked evidence and CRLF/UTC/14-field/no-tags regressions without live state/network/credentials.
  </behavior>
  <action>Invoke clone-rooted ReciprocalArtifacts with the fixed r3 handoff and all authority paths, replay union/reducer/predecessor/three-history/receipt rules, and copy only its closed sanitized projection into the tracked record. Extend tracked qualification and Required integration, preserving CRLF-safe, UTC digest, exact 14-field vector, and no-tags fixture regressions. Update COVERAGE for twelve plans, r3 current authority, attempt-zero/r1/r2 terminal history, absent packet+literal receipt versus packet-free exact, mutation-only HostedPreflight, all D-01..D-19, and all five requirements. Keep Phase 9 work excluded.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -TrackedClosure; if ($LASTEXITCODE) { exit $LASTEXITCODE }; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All requirements have reciprocal r3 evidence and offline credential-free Required passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fixed r3 handoff to mutation | Receipt-or-exact and three histories must replay. |
| Module authority to successor | Exact predecessor/cold proof gates each mutation. |
| Live artifacts to tracked closure | Only sanitized projection crosses. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-12-01 | Elevation of Privilege | union/outcome switch | critical | mitigate | Mutation requires literal receipt; only absent calls one PublishOne. |
| T-08-12-02 | Tampering | three-history successor chain | critical | mitigate | Individual/set/ref/observation/cold/predecessor/index digest replay. |
| T-08-12-03 | Spoofing | r1/r2 state as r3 | critical | mitigate | Fixed r3 handoff, exact LF clone, unique StateRoot, history-only prior attempts. |
| T-08-12-04 | Information Disclosure | tracked evidence | high | mitigate | Closed sanitized projection and offline Required. |
| T-08-12-05 | Denial of Service | ambiguity | medium | mitigate | Bounded stop without rerun or republish. |
</threat_model>

<verification>Validate r3 union, three histories, ordered authorities, exact metadata/graphs/cold proofs, reciprocal closure, and offline Required.</verification>
<success_criteria>All three modules are genuinely published or exactly checkpointed without republish and all Phase 8 requirements have r3-bound evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-12-SUMMARY.md` when done.</output>
