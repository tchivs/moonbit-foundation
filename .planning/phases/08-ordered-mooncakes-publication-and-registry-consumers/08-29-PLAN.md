---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "29"
type: execute
wave: 29
depends_on: ["08-28"]
files_modified: []
autonomous: false
requirements: [DIST-01, DIST-04]
user_setup:
  - service: GitHub Actions / Mooncakes
    why: "Only the future protected publisher dry run and explicit core authorization use the existing environment; its credential value must never be inspected or copied."
must_haves:
  truths:
    - "The r11 source is an exact tested committed SHA, pushed normally, then bound to one new non-force annotated r11 tag, remote peel, and clean clone (D-01, D-07)."
    - "The exact six-path user-dirty baseline stays status/content-identical while all other worktree drift remains fail-closed; release artifacts come only from exact committed HEAD or the clean clone."
    - "Non-publishing r11 qualification ends only in exact-existing without packet/receipt/handoff or confirmed-absent with one packet and no receipt/handoff, both zero mutation (D-03, D-04, D-13, D-19)."
    - "Only confirmed absence may display same-turn `authorize-core`; exact-existing skips the checkpoint and `stop` leaves receipt/handoff absent."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r11"
      provides: "unique r11 immutable boundary tag"
    - path: "%TEMP%/mnf-phase08-r11-user-dirty-baseline.json"
      provides: "closed status/content baseline for allowed user dirties"
    - path: "%TEMP%/mnf-phase08-r11-preauthorization.json"
      provides: "closed r11 pre-authorization projection"
    - path: "%TEMP%/mnf-phase08-r11-handoff.json"
      provides: "handoff created only after same-turn authorized absence branch"
  key_links:
    - from: "r11 static/seam tests and clean clone"
      to: "authorization packet"
      via: "same SHA, eleven histories, prepared equality, hosted dry run, fresh observation"
      pattern: "modules-v0.1.0-r11"
---
<objective>Create and non-publishing-qualify the unique r11 boundary, then stop at the sole maintainer’s dynamic core decision.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-26-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-27-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-28-SUMMARY.md
@scripts/quality/Invoke-Phase08R11PreLive.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1
</context>
<tasks>
<task type="auto">
  <name>Task 1: Establish and branch-qualify the non-publishing r11 boundary</name>
  <files>None (Git refs, clean clone, GUID-owned temporary state, and sanitized r11 artifacts only)</files>
  <action>Create and hash a closed baseline for exactly these known user paths: `.planning/config.json`, `docs/governance/decisions/0001-sole-owner-bootstrap.md`, `docs/governance/rfc-process.md`, `.codebase-memory/`, `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/research-plan-input.json`, and `.planning/research/.cache/`. Require every other tracked/untracked/staged/renamed path to be clean. Do not stage, edit, delete, reset, clean, stash, or otherwise alter an allowed path; compare the baseline before/after every side effect and at task end.

After all 08-26 through 08-28 checks pass, run r11 zero-write pre-live and stop if r10 object/peel/terminal evidence differs, r11 state/tag/handoff exists, or the actual pre-tag PrepareAttempt regression is not green. Push the exact tested committed HEAD by SHA to `origin/main`, require remote equality, create/push a new non-force annotated `modules-v0.1.0-r11` tag, and verify its object and peel. In an LF/no-tags clone of that exact peel with GUID-owned roots, fetch/verify the r11 tag into the clone before InitializeBoundary then PrepareAttempt. Require all eleven histories, policy/ref/tag/source binding, prepared intent/archive equality, and fresh roots. Run exactly one hosted preflight and one publisher dry run using only the publisher-only temporary Moon home; remove it after each. Run fresh credential-free core ObserveOnly and emit the closed pre-authorization projection. Validate exact-existing as authority with packet/receipt/handoff absent, or absent as one valid packet with receipt/handoff absent; both require zero mutation. Record only sanitized artifacts. Never invoke PublishOne, dispatch a mutation run, or call a registry-mutating command. Recompare the user baseline before task completion.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R11PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath "$env:TEMP/mnf-phase08-r11-preauthorization.json" -HandoffPath "$env:TEMP/mnf-phase08-r11-handoff.json"</automated></verify>
  <done>One r11 tag/clean clone/source identity has exactly one valid zero-mutation pre-authorization branch and the user-dirty baseline is unchanged.</done>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Dynamic authorize-core checkpoint for confirmed-absent r11 only</name>
  <files>None (receipt and fixed handoff may be written only for an authorized absent branch)</files>
  <decision>Authorize one `tchivs/mb-core@0.1.0` mutation from the revalidated r11 packet, or stop without mutation.</decision>
  <context>Emit this checkpoint only when Task 1 reports confirmed absence. First display/revalidate r11 tag object/peel/source, r10 terminal record and eleven-history set, clone-local tag evidence, prepared artifacts, hosted preflight/dry identifiers, actor and credential teardown, fresh observation/packet/projection digests, zero mutation, and unchanged user-dirty baseline. Exact-existing has no packet/receipt/handoff and completes without this prompt. Publication authority is not inferred from any other fact.</context>
  <action>Recompute the allowed user-dirty baseline and stop on any drift or any path outside it. Reload all displayed r11 identities and artifacts. Accept only same-turn literal `authorize-core` or `stop`. Only an authorized confirmed-absent branch may create receipt/handoff then run ValidateHandoff; `stop` and exact-existing retain no receipt/handoff and re-run pre-authorization validation with zero mutation. Recompare the baseline after the decision. Any stale, ambiguous, malformed, or mismatched input stops without mutation.</action>
  <options><option id="authorize-core"><name>authorize-core</name><pros>Creates a receipt for only Plan 08-30’s one core mutation.</pros><cons>Plan 08-30 may perform one irreversible publisher command after a fresh revalidation.</cons></option><option id="stop"><name>stop</name><pros>Retains zero-mutation r11 evidence.</pros><cons>Publication remains blocked.</cons></option></options>
  <resume-signal>Type exactly `authorize-core` or `stop`.</resume-signal>
  <verify><automated>pwsh -NoProfile -Command "$handoff=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r11-handoff.json'; $preauth=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r11-preauthorization.json'; if(Test-Path -LiteralPath $handoff -PathType Leaf){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $handoff; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $preauth -HandoffPath $handoff; exit $LASTEXITCODE"</automated></verify>
  <done>Only same-turn authorization of confirmed absence produces receipt/handoff; stop/exact paths preserve zero mutation and unchanged user dirties.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| dirty checkout to committed source | User data must not enter artifacts or block safe work when it is unchanged. |
| r11 tag/clone to qualification | Tag object, peel, source, policy, and clone-local ref must agree. |
| pre-authorization to human decision | Only fresh absent evidence can create a receipt. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-29-01 | Tampering | user-dirty baseline | high | mitigate | Exact path/status/content hashes before/after every side effect and task. |
| T-08-29-02 | Tampering | r11 tag/clone | critical | mitigate | SHA/object/peel/clone/policy equality and fetched local tag verification. |
| T-08-29-03 | Information Disclosure | dry-run credential | critical | mitigate | Temporary publisher-only Moon home and teardown. |
| T-08-29-04 | Elevation of Privilege | core authorization | critical | mitigate | Same-turn absent-only checkpoint and full revalidation. |
</threat_model>
<verification>Re-run static/seam/pre-live gates before side effects; prove tag/clone/ref equality, branch-aware zero mutation, and user-baseline equality. No registry mutation is permitted.</verification>
<success_criteria>Only r11 can become a qualified boundary, and only its confirmed-absent branch can reach a human authorization receipt after all static and non-publishing gates pass.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-29-SUMMARY.md` when done.</output>
