---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "22"
type: execute
wave: 22
depends_on: ["08-21"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Exact r8-capable committed HEAD is pushed before r8 tag; remote main, tag peel, canonical prepared bytes, clean clone, and source bind one SHA (D-01, D-07)."
    - "GUID roots reuse no r7 state, locator, store, tag, raw/canonical artifact, or handoff."
    - "Eight histories/set bind fresh r8 preparation and exclusive AuthorityUnion."
    - "Exact-existing needs no preflight/dry/packet/receipt; absent requires dynamic human-action accepting only same-turn literal authorize-core or stop (D-04, D-14, D-15)."
    - "No branch calls PublishOne or mutates Mooncakes."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R8PreLive.ps1"
      provides: "Mandatory zero-write gate"
    - path: "refs/tags/modules-v0.1.0-r8"
      provides: "Annotated immutable r8 boundary"
    - path: "%TEMP%/mnf-phase08-r8-handoff.json"
      provides: "Eligible eight-history AuthorityUnion"
  key_links:
    - from: "eight histories and canonical prepared identity"
      to: "r8 AuthorityUnion"
      via: "individual/set/archive/intent digests"
      pattern: "historical_r7_sha256"
---

<objective>Establish the canonical cross-OS r8 boundary and select a non-mutating eight-history AuthorityUnion.

Purpose: Cross the corrected hosted boundary while preserving the first-mutation checkpoint.
Output: r8 tag, clean clone, unique state, exclusive authority, optional literal receipt, and fixed handoff; no PublishOne.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-intent-components.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-21-SUMMARY.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute zero-write r8 gate before every live side effect</name>
  <files>None (read-only local, GitHub, registry, ref, run, and temporary-state checks)</files>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, before any push, tag, root, dispatch, secret, registry, or handoff action, run `Invoke-Phase08R8PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin` from committed HEAD. Require its single sanitized object to prove rootless attempt-zero plus persisted r1-r7 refs/peels/digests/set; exact r7 source/tag/run/job/failure/raw-container mismatch/downstream-zero; canonical archive invariants; immutable paths/digests; committed-clean 08-19..21 summaries/ownership; and r8 tag/handoff absence. Recompute result digest and stop on disagreement, ambiguity, or write. Repeat in the same executor turn before the first side effect of any resumed Task 2.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R8PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin</automated></verify>
  <done>Zero-write selector passes completely before Task 2 may act.</done>
</task>

<task type="auto">
  <name>Task 2: Create canonical r8 boundary and dynamic AuthorityUnion</name>
  <files>None (exact push/tag, unique state, sanitized artifacts, optional receipt, fixed handoff)</files>
  <action>Only after same-turn Task 1, per D-01 through D-07 and D-13 through D-19, push exact committed HEAD and require local/remote equality. Create GUID-owned roots; clean LF/no-tags clone exact SHA; verify no historical overlap. Create/push annotated `modules-v0.1.0-r8` without force, verify object/peel, then PrepareAttempt as initial 0.1.0 sequence zero root=current no predecessor with eight histories/set and fresh state. Require local/hosted canonical archive order/path/payload/fixed attributes/stored compression/digests, prepared manifest, and intent to agree.

Fresh ObserveOnly core. Exact creates packet/receipt-free ExactExistingAuthority after structured observation and cold four-target proof. Absent permits one HostedPreflight and one PublisherDryRun; require snapshots, unique keys, exact14, actor `tchivs`, dry success, credential removal, fresh absence, zero PublishOne. Generate packet and emit dynamic `checkpoint:human-action`, never auto-approvable. Display roots; locator paths/file/digests; ArtifactRoot; PacketPath/Sha256; release/boundary/source SHA; AttemptZero and R1..R7 digests plus set; canonical archives/prepared/root/intent digests; hosted ids/attempts; actor/disposition; absence digest/timestamp; zero mutation. Accept only verbatim case-sensitive `authorize-core` or `stop` in the same checkpoint turn. Stop writes nothing. Authorization triggers fresh full reload/revalidation of every displayed field, canonical archives, remote refs, checkout, histories/set, hosted artifacts, actor, dry-run, absence, packet, locators/index/store, and zero mutation; drift stops. Only then create reciprocal receipt, mutation union, and fixed handoff. Never call PublishOne in this plan.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r8-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -Mode AuthorityUnion -HandoffPath "$env:TEMP/mnf-phase08-r8-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>One eligible r8 union exists; exact has no packet/receipt, absent has same-turn receipt, and publication remains zero.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Source/tag/canonical archive | Exact SHA and canonical bytes agree across hosts. |
| Observation to authority | Only exact or confirmed absent selects a branch. |
| Human literal to receipt | Complete same-turn recovery revalidation is mandatory. |
| Dry-run to mutation | This plan cannot call PublishOne. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-22-01 | Tampering | source/tag/archive | critical | mitigate | SHA/peel/canonical equality. |
| T-08-22-02 | Spoofing | r7 history | critical | mitigate | Remote refs and eight-history set. |
| T-08-22-03 | Elevation of Privilege | authorization | critical | mitigate | Same-turn literal plus full recovery. |
| T-08-22-04 | Information Disclosure | credentials | high | mitigate | Ephemeral home and sanitized projections. |
| T-08-22-05 | Repudiation | no-mutation | high | mitigate | Zero counts and no PublishOne path. |
</threat_model>

<verification>Validate eight histories, r7 terminal evidence, canonical r8 boundary, full dynamic checkpoint/recovery, exclusive union, fixed handoff, and zero PublishOne.</verification>
<success_criteria>08-22 ends only with valid r8 exact-existing or literal-receipt mutation authority ready for 08-23; it performs no registry mutation.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-22-SUMMARY.md` when done.</output>
