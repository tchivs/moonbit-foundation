---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "22"
type: execute
wave: 22
depends_on: ["08-21"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "r9 is sole current initial retry with 0.1.0, sequence 0, root=current, no predecessor (D-01, D-07, D-15)."
    - "Attempt-zero and r1 through r8 are nine immutable terminal-negative histories with individual digests and canonical set."
    - "r8 binds source 8d0f050a2ea2a5f136d87f913987d59ea99a13d4, annotated tag 20907c7bbd11b91d4482dd113d149b3a107c9672, and PREP15-CANONICAL-ARCHIVE / REL-XPLAT-NONCANONICAL before live locator, run, or downstream effects."
    - "r8 has zero locator, active attempt, packet, receipt, handoff, hosted run, secret, PublishOne, mutation, and successor effects."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r9 profile and nine histories"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r9 nine-history AuthorityUnion"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r9 prepared identity"
  key_links:
    - from: "attempt-zero through r8"
      to: "r9 prepared/authority/receipt/handoff"
      via: "nine individual digests and canonical ordered set"
      pattern: "historical_r8_sha256"
---

<objective>Define and test r9 contracts and nine immutable terminal-negative histories.

Purpose: Recover from r8's pre-locator canonical-copy seam failure without moving r8 or reusing state.
Output: Closed r9 policy, intent, prepared, authority, receipt, handoff, and qualification contracts.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-prepare-canonicalization-seam.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-21-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Advance policy, intent, prepared, authority, receipt, and handoff to r9</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r9 is current; order is attempt_zero,r1,r2,r3,r4,r5,r6,r7,r8.
    - r8 records exact source/tag and terminal PREP15/REL-XPLAT failure before locator/run.
    - Authority requires nine histories/set; exact-existing forbids packet/receipt and absent requires same-turn literal receipt.
  </behavior>
  <action>Per D-01, D-04, D-07, D-14, and D-15, preserve attempt-zero through r7 byte-for-byte and add r8's exact source/tag, raw input failure `PREP15-CANONICAL-ARCHIVE / REL-XPLAT-NONCANONICAL`, absence of locator/active attempt/hosted run, and all downstream zero. Advance contracts to r9/nine histories, bind receipt to packet/r9 ref/boundary/source/literal `authorize-core`/UTC/nine histories/set, fix handoff `%TEMP%/mnf-phase08-r9-handoff.json`, and reject r8-current, fake locator/run/success, replacement/reorder/set drift, correction substitution, old handoff, and path reuse.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r9 alone is current and r8 remains an exact pre-locator terminal record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind canonical-copy seam and fresh r9 preparation</name>
  <files>scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Actual PrepareAttempt copies raw archives to temporary prepared input, preserves raw provenance, canonicalizes derived copies, and binds the same canonical digests in intent/manifest.
    - Fresh r9 root/genesis/prepared/index/store bind nine histories and reject all r8 paths/artifacts.
    - The production actual-prepare fixture version and seam contract are digest-bound.
  </behavior>
  <action>Per D-01, D-07, D-14, and D-18, make the production copy-then-canonicalize prepared seam explicit in r9 qualification: raw source copies are never mutated, only temporary prepared-input copies are canonicalized, and canonical copies must match qualification/intent/manifest digests. Require a fixture that executes actual r9 PrepareAttempt with valid noncanonical raw ZIPs and proves original bytes unchanged, derived canonical bytes used, and bundle validates. Reject raw copy handoff, old r8 locator/store/index/archive reuse, seam-version mismatch, fixture substitution, history/set drift, UTC/LF/no-tags/snapshot drift, and correction lane.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-ReleaseQualification.ps1 -Check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r9 prepared identity consumes only derived canonical copies and binds actual PrepareAttempt seam evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Raw package archive to prepared input | Raw provenance remains immutable while only derived copies are canonicalized. |
| r8 terminal evidence to r9 | Pre-locator failure cannot be upgraded into live authority. |
| r8 state to r9 | No locator/store/archive/handoff reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-22-01 | Tampering | canonical-copy seam | critical | mitigate | Raw-preservation and derived-copy digest fixtures. |
| T-08-22-02 | Spoofing | r8 live success | critical | mitigate | Closed pre-locator/no-run/downstream-zero shape. |
| T-08-22-03 | Elevation of Privilege | receipt/handoff | critical | mitigate | Exclusive r9 union bound to nine histories. |
| T-08-22-04 | Tampering | r8 state reuse | high | mitigate | Fresh r9 roots and old-path rejection. |
</threat_model>

<verification>Run intent, prepared, qualification, and actual-PrepareAttempt fixture suites; validate r9, nine histories, r8 no-run facts, canonical derived copies, and zero live effects. Accepted scope warning: 13 files are necessary to atomically advance contract layers; tasks are bounded to 8 and 5 files.</verification>
<success_criteria>r9 contracts and canonical prepared-copy seam are ready for hosted/pre-live integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-22-SUMMARY.md` when done.</output>
