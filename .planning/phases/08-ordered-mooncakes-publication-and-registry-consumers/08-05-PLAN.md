---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "05"
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - .github/workflows/publish-modules.yml
autonomous: false
requirements: [DIST-01]
must_haves:
  truths:
    - "Before authorization, the trusted source is clean and pushed, the exact protected tag `modules-v0.1.0` is created/pushed and verified at that source, the release intent and prepared bundle validate, one uniquely correlated environment-credentialed `PublisherDryRun` executes exact prepared core with zero exit and no mutation, all Phase 6/7/8 credential-free tests pass, hosted settings remain exact, credential state is torn down, and a fresh post-run public observation proves core 0.1.0 absent (D-04, D-19)."
    - "The operator sees the verified immutable tag/commit binding, source SHA, intent SHA-256, root intent SHA-256, prepared digest, dry-run evidence digest, target module, and irreversible effect before deciding (D-04)."
    - "The checkpoint itself dispatches no workflow, reads no secret, performs no publication, and cannot be offered until the exact tag and bound non-mutating dry run are proven."
    - "A separate no-mutation hosted preflight compiles and executes the deterministic native sentinel with the pinned toolchain before authorization; compile-only, nonzero, missing-sentinel, linker/runtime drift, stale, or ambiguous evidence makes `authorize-core` unavailable."
  artifacts:
    - path: "GitHub Actions preflight artifact"
      provides: "Sanitized exact authorization packet bound to the verified immutable tag/commit, source, intent, prepared bundle, and publisher dry-run evidence"
    - path: "08-05-SUMMARY.md"
      provides: "Explicit operator authorization or stop decision without a mutation"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "Exact gh dispatch/run-correlation/artifact-retrieval contract for PublisherDryRun, HostedPreflight, and later one-module runs"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "Fixture-tested ArtifactRoot/locator/index selectors required before any Phase 8 live task"
  key_links:
    - from: "authorization packet"
      to: "08-06 live inputs"
      via: "exact tag/source/root/current/prepared digests"
      pattern: "modules-v0.1.0"
    - from: "hosted native preflight artifact"
      to: "authorization packet"
      via: "run identity, pinned toolchain digest, linker/runtime identity, expected sentinel, zero exit, and artifact SHA-256"
      pattern: "native_runtime_verified"
    - from: ".github/workflows/publish-modules.yml"
      to: "authorization packet"
      via: "unique PublisherDryRun run/artifact bound to exact tag, SHA, intent, prepared core, zero exit, credential teardown, and fresh absence proof"
      pattern: "PublisherDryRun"
---

<objective>
Create and verify the exact protected release tag, run every bound non-mutating gate, and stop for explicit operator authorization immediately before the first irreversible `tchivs/mb-core@0.1.0` mutation.

Purpose: Preserve the locked human boundary between a proven executable seam and consumption of the first production version.
Output: Verified immutable tag/commit binding, bound dry-run evidence, sanitized preflight packet, and a recorded authorize-or-stop decision.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-04-SUMMARY.md
@.planning/phases/07-release-safety-intent-and-recovery-automation/07-03-SUMMARY.md
@.github/workflows/publish-modules.yml
@policy/phase-08-distribution.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the exact no-registry-mutation authorization packet</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, .github/workflows/publish-modules.yml</files>
  <read_first>.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-01-SUMMARY.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-02-SUMMARY.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-03-SUMMARY.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-04-SUMMARY.md, release/intent/intent.json, .github/workflows/publish-modules.yml</read_first>
  <action>Per D-04, D-11, D-12, and D-19, before any release tag or live dispatch exists, write fixtures and implement the complete live-seam contract. Add explicit workflow/helper modes `PublisherDryRun`, `HostedPreflight`, and `PublishOne`; the helper accepts `-Repository tchivs/moonbit-foundation`, `-Workflow publish-modules.yml`, exact tag/ref, source SHA, root/current intent SHA-256, authorization-packet path, target module, prior run/artifact when resuming, plus mandatory explicit `-LocatorPath` and `-ArtifactRoot`. Every mode snapshots existing workflow run IDs before `gh workflow run`, then polls `gh run list`/`gh run view` and requires exactly one new run bound to repository, workflow path, `workflow_dispatch`, tag/ref, head SHA, run ID/attempt, target module, root/current intent, and prepared artifact digest; zero or multiple candidates fail instead of selecting the newest. `PublisherDryRun` is a distinct environment-gated job/path which may receive `MOONCAKES_TOKEN` only in its isolated step, materializes minimum ephemeral Mooncakes credential state, validates the exact prepared `mb-core` payload, invokes only `moon publish --frozen --dry-run`, records zero exit, removes credential state in `finally`, and uploads a closed sanitized dry-run artifact. Workflow conditions must make the non-dry publication step unreachable in this mode. The artifact binds mode, run/artifact identity and digest, exact protected tag, source SHA, root/current intent digests, prepared manifest/module/archive digests, literal command classification, target module, zero exit, teardown success, and timestamps without secret values, hashes of secret values, headers, credential contents/paths, or raw CLI output. `HostedPreflight` never receives the secret and uses pinned MoonBit `0.1.20260713+75c7e1f` plus the hosted linker/runtime to compile and execute the same native sentinel as the cold proof, explicitly without compile-only execution; its artifact binds expected/observed sentinel digest, zero exit, toolchain/linker/runtime identities, source/ref/intent/run, and timestamps. Add fail-closed fixtures for wrong mode/job reachability, a non-dry publisher command, missing prepared binding, nonzero exit, absent teardown, secret-shaped output, stale/mixed/ambiguous runs or artifacts, compile-only evidence, and sentinel/runtime drift. Also create `Test-Phase08Qualification.ps1` now, before tag creation, with fixture-tested `-CoreColorArtifacts`, `-LiveArtifacts`, and runnable `-AuthorizationPacket` modes and mandatory explicit `-LocatorPath` plus `-ArtifactRoot` parameters; `-AuthorizationPacket` also requires explicit `-PacketPath`. Define the durable locator at the deterministically recomputable outside-checkout path `<system-temp>/mnf-phase08/phase-08-live-locator.json` and the canonical root `<system-temp>/mnf-phase08/artifacts/<root-intent-sha256>/`. Before persisting any `PublisherDryRun`, `HostedPreflight`, absence-observation, or authorization-packet evidence, recompute those paths from system temp and the validated root intent. If the locator is absent, create the root, `runs/`, `observations/`, `authorization/`, and closed empty `index.json`, then atomically create the locator once; if it exists, validate and resume it without deleting, replacing, truncating, or reinitializing any evidence. The locator allowlist binds schema version, canonical artifact-root and index paths, repository, workflow, exact tag/ref, source SHA, root/current intent, authorization-packet path/digest when present, creation timestamp, and locator digest. Initialization/resume must validate exact locator/root/index equality and canonical absolute paths, root-intent derivation, immutable repository/workflow/tag/SHA/intent bindings, indexed content digests, and absence of symlink/path escape; collision, drift, partial state, an unindexed file, duplicate logical evidence, mixed runs, or ambiguous recovery fails closed. Persist every downloaded or derived live artifact beneath the bound root by exclusive sibling temporary file plus atomic rename, then atomically replace the closed index with its path/digest/run/kind binding; an exact already-indexed immutable record may be resumed only after digest and binding revalidation, never overwritten. Task 3 of 08-06 may extend reciprocal/static closure only and must not first-create or weaken these live selector modes. After every fixture passes, run full credential-free Required, Phase 8 selectors, workflow static checks, and credential-redacted HostedSettings; require clean tracked source at the pushed default-branch SHA; rebuild and validate the exact intent/prepared bundle; then create and push the protected tag `modules-v0.1.0` at that exact SHA and require local/remote equality. Recompute and validate the locator/root/index immediately before every hosted helper or selector invocation and pass both explicit paths every time; do not depend on process environment persistence. Only after that binding exists, dispatch one `PublisherDryRun` against the tag with `live_authorization` false and target `mb-core`, explicit locator/root, and indexed destination; retrieve exactly its uniquely correlated sanitized artifact, require terminal success and zero dry-run exit, prove teardown success, and immediately make a fresh credential-free structured observation that still classifies `tchivs/mb-core@0.1.0` absent, persisting it atomically under the same root/index. Dispatch the separate secret-free `HostedPreflight` against the same tag/SHA/intent/prepared inputs with explicit locator/root and persist its unique native sentinel artifact identically. Assemble `authorization/authorize-core.json` atomically and index it as one closed authorization packet that binds both exact run IDs/attempts/artifact names/digests, tag/SHA, root/current intent, prepared manifest/module/archive, target module, dry-run command classification/zero exit/teardown, fresh post-run absence digest/timestamp, native sentinel/toolchain/linker/runtime evidence, and all gate digests. Fixture-test and then run `Test-Phase08Qualification.ps1 -AuthorizationPacket -LocatorPath $locatorPath -ArtifactRoot $artifactRoot -PacketPath $packetPath`; it independently recomputes indexed digests and rejects stale, mixed, missing, ambiguous, mutation-indicating, unbound, or changed PublisherDryRun, HostedPreflight, tag/SHA/intent/module/zero-exit/teardown/fresh-absence evidence. Any such failure prevents the checkpoint. No `PublishOne` or other non-dry mutation dispatch is permitted before literal `authorize-core`, and no step prints or inspects secret values.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { $locatorPath = Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/phase-08-live-locator.json'; if (-not (Test-Path -LiteralPath $locatorPath -PathType Leaf)) { throw 'P08-LOCATOR-MISSING' }; $locator = Get-Content -LiteralPath $locatorPath -Raw | ConvertFrom-Json -Depth 30; $artifactRoot = [IO.Path]::GetFullPath([string]$locator.artifact_root); $packetPath = Join-Path $artifactRoot 'authorization/authorize-core.json'; &amp; ./scripts/quality/Test-Phase08LiveSeam.ps1 -PreflightOnly -LocatorPath $locatorPath -ArtifactRoot $artifactRoot; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly -LocatorPath $locatorPath -ArtifactRoot $artifactRoot; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -AuthorizationPacket -LocatorPath $locatorPath -ArtifactRoot $artifactRoot -PacketPath $packetPath; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }"</automated>
  </verify>
  <acceptance_criteria>Fixtures prove callable PublisherDryRun and hosted-preflight paths plus the live selector root/locator/index contract fail closed before any tag exists; one exact sanitized packet later proves every prerequisite and current absence.</acceptance_criteria>
  <done>The fixture selectors exit zero and prove PublisherDryRun can access the environment credential only for exact prepared core dry-run while non-dry mutation is unreachable, teardown is mandatory, and both live selector modes reject missing/mixed/stale roots; after the exact protected tag is created, a unique dry-run run and artifact complete with zero exit, teardown succeeds, fresh post-run observation still classifies core absent, hosted native execution passes, and the packet binds every exact fact or `authorize-core` is unavailable.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize the first irreversible core publication</name>
  <files>None (operator decision only)</files>
  <read_first>The exact sanitized authorization packet produced by Task 1 and the passing Task 1 verification output.</read_first>
  <action>Per D-04, immediately before presenting the choice, recompute the deterministic locator path from system temp, reopen it, derive the canonical artifact root and `authorization/authorize-core.json` packet path, validate exact locator/root/index/repository/workflow/tag/SHA/root-intent/current-intent equality, and run `Test-Phase08Qualification.ps1 -AuthorizationPacket` with explicit `-LocatorPath`, `-ArtifactRoot`, and `-PacketPath`. Present the exact immutable values only from that freshly passing selector, including the already-pushed tag/commit binding; PublisherDryRun run ID/attempt/artifact name/digest, exact prepared core bindings, zero exit, credential teardown, and fresh post-run absence proof; and hosted native preflight run/artifact digest with expected sentinel, zero exit, pinned toolchain, and linker/runtime identity. State plainly that the next plan will revalidate those unchanged inputs, dispatch one environment-gated `PublishOne` run, and may irreversibly consume `tchivs/mb-core@0.1.0`. Offer only `authorize-core` or `stop`; if locator/root/index/packet equality fails or any packet field or hosted evidence is unavailable, stale, compile-only, drifted, mutation-indicating, mixed, unbound, or ambiguous, offer only `stop`. Do not infer approval from previous login, tag push, dry-run/preflight success, environment setup, auto-advance, or general permission to push code.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "&amp; { $locatorPath = Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/phase-08-live-locator.json'; if (-not (Test-Path -LiteralPath $locatorPath -PathType Leaf)) { throw 'P08-LOCATOR-MISSING' }; $locator = Get-Content -LiteralPath $locatorPath -Raw | ConvertFrom-Json -Depth 30; $artifactRoot = [IO.Path]::GetFullPath([string]$locator.artifact_root); $packetPath = Join-Path $artifactRoot 'authorization/authorize-core.json'; &amp; ./scripts/quality/Test-Phase08Qualification.ps1 -AuthorizationPacket -LocatorPath $locatorPath -ArtifactRoot $artifactRoot -PacketPath $packetPath; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }"</automated>
    <human-check>Confirm the displayed tag and verified commit, source SHA, intent digests, prepared digest, dry-run evidence/no-mutation classification, hosted preflight run/artifact digest, native sentinel and zero exit, pinned toolchain/linker/runtime identities, target module, and fresh absent observation, then explicitly choose `authorize-core` or `stop`.</human-check>
  </verify>
  <acceptance_criteria>Execution resumes only on the exact `authorize-core` signal bound to the unchanged packet; any other response stops before all irreversible operations.</acceptance_criteria>
  <done>The operator has selected exactly `authorize-core` for the displayed unchanged packet, or selected/stated anything else and execution has stopped without any `PublishOne`/non-dry mutation dispatch or Mooncakes publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated preflight to human authorization | Sanitized evidence must faithfully identify the exact irreversible action. |
| Authorization packet to later live dispatch | Any source, intent, prepared, observation, or hosted-setting drift invalidates approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Spoofing | operator authorization | critical | mitigate | Explicit exact packet and `authorize-core` signal; no inherited or implicit approval. |
| T-08-05-02 | Tampering | packet/source | high | mitigate | Clean pushed SHA plus content-addressed intent/prepared/observation bindings and revalidation in the live plan. |
| T-08-05-03 | Information Disclosure | preflight output | high | mitigate | Allowlisted digests/classifications only; no secret values, headers, credential paths, or raw responses. |
| T-08-05-04 | Repudiation | irreversible decision | medium | mitigate | Record the exact packet digest and explicit decision in the plan summary, without credential material. |
</threat_model>

<artifacts_this_phase_produces>
- A complete sanitized authorization packet bound to the verified immutable tag/commit and no-mutation publisher dry run.
- One explicit decision governing the first core mutation.
</artifacts_this_phase_produces>

<verification>
All automated gates pass immediately before the checkpoint and are bound to the unchanged exact packet.
</verification>

<success_criteria>
The phase cannot reach its first irreversible action without fresh exact evidence and explicit operator authorization.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md` only after the operator decision is recorded.
</output>
