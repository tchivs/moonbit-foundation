---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "05"
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - .github/workflows/publish-modules.yml
autonomous: false
requirements: [DIST-01]
must_haves:
  truths:
    - "Before authorization, the trusted source is clean and pushed, the exact protected tag `modules-v0.1.0` is created/pushed and verified at that source, the release intent and prepared bundle validate, the exact bound `moon publish --frozen --dry-run` passes without mutation, all Phase 6/7/8 credential-free tests pass, hosted settings remain exact, and fresh public observation proves core 0.1.0 absent (D-04, D-19)."
    - "The operator sees the verified immutable tag/commit binding, source SHA, intent SHA-256, root intent SHA-256, prepared digest, dry-run evidence digest, target module, and irreversible effect before deciding (D-04)."
    - "The checkpoint itself dispatches no workflow, reads no secret, performs no publication, and cannot be offered until the exact tag and bound non-mutating dry run are proven."
    - "A separate no-mutation hosted preflight compiles and executes the deterministic native sentinel with the pinned toolchain before authorization; compile-only, nonzero, missing-sentinel, linker/runtime drift, stale, or ambiguous evidence makes `authorize-core` unavailable."
  artifacts:
    - path: "GitHub Actions preflight artifact"
      provides: "Sanitized exact authorization packet bound to the verified immutable tag/commit, source, intent, prepared bundle, and publisher dry-run evidence"
    - path: "08-05-SUMMARY.md"
      provides: "Explicit operator authorization or stop decision without a mutation"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "Exact gh dispatch/run-correlation/artifact-retrieval contract shared by hosted preflight and later one-module runs"
  key_links:
    - from: "authorization packet"
      to: "08-06 live inputs"
      via: "exact tag/source/root/current/prepared digests"
      pattern: "modules-v0.1.0"
    - from: "hosted native preflight artifact"
      to: "authorization packet"
      via: "run identity, pinned toolchain digest, linker/runtime identity, expected sentinel, zero exit, and artifact SHA-256"
      pattern: "native_runtime_verified"
---

<objective>
Create and verify the exact protected release tag, run every bound non-mutating gate, and stop for explicit operator authorization immediately before the first irreversible `tchivs/mb-core@0.1.0` mutation.

Purpose: Preserve the locked human boundary between a proven executable seam and consumption of the first production version.
Output: Verified immutable tag/commit binding, bound dry-run evidence, sanitized preflight packet, and a recorded authorize-or-stop decision.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-04-SUMMARY.md
@.planning/phases/07-release-safety-intent-and-recovery-automation/07-03-SUMMARY.md
@.github/workflows/publish-modules.yml
@policy/phase-08-distribution.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the exact no-registry-mutation authorization packet</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, .github/workflows/publish-modules.yml</files>
  <read_first>.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-01-SUMMARY.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-02-SUMMARY.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-03-SUMMARY.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-04-SUMMARY.md, release/intent/intent.json, .github/workflows/publish-modules.yml</read_first>
  <action>Per D-04, D-11, D-12, and D-19, first extend the Phase 8 workflow/live-seam fixtures with an explicit no-mutation `hosted-preflight` path and create `Invoke-Phase08HostedRun.ps1` before the release source is committed, pushed, and tagged. The helper accepts only `-Mode HostedPreflight|PublishOne`, `-Repository tchivs/moonbit-foundation`, `-Workflow publish-modules.yml`, exact tag/ref, source SHA, root/current intent SHA-256, authorization-packet path, target module, prior run/artifact when resuming, and artifact root. It snapshots existing workflow run IDs, invokes `gh workflow run` with only declared inputs, then polls `gh run list`/`gh run view` and requires exactly one new run whose repository, workflow identity, `workflow_dispatch` event, tag branch/ref, and head SHA match; zero or multiple candidates fail instead of selecting the newest. `HostedPreflight` sets live authorization false, never enters the environment-secret publisher step, and uses pinned MoonBit `0.1.20260713+75c7e1f` plus the hosted system linker/runtime to compile and execute the same deterministic native consumer sentinel used by Phase 8 cold proofs, explicitly without `--build-only`. Its closed sanitized artifact binds the literal expected sentinel, observed sentinel digest, process exit code zero, Moon/moonc/moonrun identities and toolchain digest, compiler/linker path+version digest, runtime/OS identity digest, source/ref/intent bindings, run ID/attempt, and start/end timestamps. Missing runtime components, compile-only evidence, nonzero exit, sentinel mismatch/absence, drift, stale timestamps, or multiple/ambiguous reports fails closed; add adversarial fixtures for every case. Then run full credential-free Required, Phase 8 selectors, workflow static checks, and credential-redacted HostedSettings; verify clean tracked source at the pushed default-branch SHA; rebuild/validate the exact intent and prepared bundle; create/push protected tag `modules-v0.1.0` at that source and require local/remote equality. Materialize minimum ephemeral publisher credential state only inside the isolated dry-run step, run exact prepared `mb-core` as `moon publish --frozen --dry-run`, tear it down, and prove no mutation by fresh structured absence observation. Dispatch hosted preflight against the exact tag, retrieve its exact artifact through the helper, and produce one allowlisted authorization packet containing tag/commit, source, root/current intent, prepared and dry-run evidence digests, target module, fresh absence digest/timestamp, hosted run/artifact digest and native sentinel/zero-exit/toolchain/linker/runtime identities, plus gate digests. Bind every field into the packet digest. Any missing, stale, empty, mismatched, mutation-indicating, compile-only, or ambiguous fact prevents the checkpoint and makes `authorize-core` unavailable. Do not invoke non-dry-run publication, inspect/expose the secret value, or let hosted preflight access it.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Test-Phase08LiveSeam.ps1 -PreflightOnly</automated>
  </verify>
  <acceptance_criteria>One exact sanitized packet proves every prerequisite and current absence; any weak or ambiguous evidence prevents the checkpoint from being offered.</acceptance_criteria>
  <done>The preflight selector exits zero only after the protected tag resolves to the exact source SHA, the publisher dry run completes without mutation, a uniquely identified hosted run genuinely compiles and executes the bound native sentinel with zero exit under the pinned toolchain/linker/runtime, and the sanitized packet binds every fresh prerequisite; otherwise `authorize-core` is unavailable.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize the first irreversible core publication</name>
  <files>None (operator decision only)</files>
  <read_first>The exact sanitized authorization packet produced by Task 1 and the passing Task 1 verification output.</read_first>
  <action>Per D-04, present the exact immutable values, including the already-pushed tag/commit binding, bound no-mutation dry-run evidence, and hosted native preflight run/artifact digest with expected sentinel, zero exit, pinned toolchain, and linker/runtime identity. State plainly that the next plan will revalidate those unchanged inputs, dispatch one environment-gated run, and may irreversibly consume `tchivs/mb-core@0.1.0`. Offer only `authorize-core` or `stop`; if any packet field or hosted evidence is unavailable, stale, compile-only, drifted, or ambiguous, offer only `stop`. Do not infer approval from previous login, tag push, dry-run/preflight success, environment setup, auto-advance, or general permission to push code.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Test-Phase08LiveSeam.ps1 -PreflightOnly</automated>
    <human-check>Confirm the displayed tag and verified commit, source SHA, intent digests, prepared digest, dry-run evidence/no-mutation classification, hosted preflight run/artifact digest, native sentinel and zero exit, pinned toolchain/linker/runtime identities, target module, and fresh absent observation, then explicitly choose `authorize-core` or `stop`.</human-check>
  </verify>
  <acceptance_criteria>Execution resumes only on the exact `authorize-core` signal bound to the unchanged packet; any other response stops before all irreversible operations.</acceptance_criteria>
  <done>The operator has selected exactly `authorize-core` for the displayed unchanged packet, or selected/stated anything else and execution has stopped without workflow dispatch or Mooncakes publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated preflight to human authorization | Sanitized evidence must faithfully identify the exact irreversible action. |
| Authorization packet to later live dispatch | Any source, intent, prepared, observation, or hosted-setting drift invalidates approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Spoofing | operator authorization | critical | mitigate | Explicit exact packet and `authorize-core` signal; no inherited or implicit approval. |
| T-08-05-02 | Tampering | packet/source | high | mitigate | Clean pushed SHA plus content-addressed intent/prepared/observation bindings and revalidation in the live plan. |
| T-08-05-03 | Information Disclosure | preflight output | high | mitigate | Allowlisted digests/classifications only; no secret values, headers, credential paths, or raw responses. |
| T-08-05-04 | Repudiation | irreversible decision | medium | mitigate | Record the exact packet digest and explicit decision in the plan summary, without credential material. |
</threat_model>

<artifacts_this_phase_produces>
- A complete sanitized authorization packet bound to the verified immutable tag/commit and no-mutation publisher dry run.
- One explicit decision governing the first core mutation.
</artifacts_this_phase_produces>

<verification>
All automated gates pass immediately before the checkpoint and are bound to the unchanged exact packet.
</verification>

<success_criteria>
The phase cannot reach its first irreversible action without fresh exact evidence and explicit operator authorization.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md` only after the operator decision is recorded.
</output>
