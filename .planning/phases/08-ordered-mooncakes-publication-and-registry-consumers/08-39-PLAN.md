---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "39"
type: execute
wave: 39
depends_on: ["08-38"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: false   # first real Mooncakes mutation; operator-authorized
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "mb-core@0.1.0 is published first and a fresh cold consumer resolves it with one-node graph across js/wasm/wasm-gc/native before color publishes."
    - "mb-color@0.1.0 is published next and a fresh consumer resolves it with the published mb-core dependency, two-node graph, four targets before image publishes."
    - "mb-image@0.1.0 is published last and a fresh consumer resolves the full three-node graph with bounded PPM public stack across four targets."
    - "Every cold consumer runs outside the repo with isolated empty MOON_HOME, no moon.work/path-dep/copied-source/Git-fallback/warm-cache, and records registry metadata, package identity, resolved graph, toolchain, target results, behavioral assertion."
    - "Post-publication credential-redacted read-only Mooncakes observation proves each module page renders intended qualified metadata."
    - "The eight-path baseline stays unchanged; publication caused no source mutation or secret leakage."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "r13 reciprocal DIST/PROV-05 evidence"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "final r13 Phase 8 evidence map"
---
<objective>Execute the operator-authorized r13 publication path end-to-end: convert the confirmed-absent preauthorization emitted by 08-38 into an authorized mutation handoff, then perform three real irreversible `moon publish --frozen` mutations in strict order (mb-core@0.1.0 → mb-color@0.1.0 → mb-image@0.1.0), each preceded by a fresh credential-free cold registry consumer that proves the predecessor's published identity, and close Phase 8 with the COVERAGE.md r13 evidence map. This is the first and only plan that mutates the Mooncakes registry; forward-only recovery means a failure at any publish halts the chain for r14 planning, never an in-place retry.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-PLAN.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-38-PLAN.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-ReleasePublisher.ps1
@scripts/quality/Invoke-ColdRegistryConsumer.ps1
@scripts/quality/Invoke-MooncakesLiveMutation.ps1
@scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
@modules/mb-core/moon.mod.json
@modules/mb-color/moon.mod.json
@modules/mb-image/moon.mod.json
@policy/release-control.json
@policy/phase-08-distribution.json</context>
<tasks>
<task type="auto" non_publishing="true">
  <name>Task 1: Generate the r13 receipt + handoff from the authorized preauthorization</name>
  <files>None (artifacts live under the durable boundary store root and %TEMP%/mnf-phase08-r13-handoff.json; no repo files are mutated)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-38-PLAN.md Task 2 Steps 6-9 (the absent-variant packet, preauthorization emission, and the eight-path baseline guard)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-SUMMARY.md lines 84-90 (forward constraints this task unblocks: surface authorize-core intent from absent and execute ordered publication)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 lines 205-232 (Assert-P08PreAuthorization — accepts r13 after 08-38 Task 1)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 lines 107-124 (Write-P08AuthorizationReceipt — rejects any response other than the literal `authorize-core`)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 lines 1082-1114 (PersistAuthorizationReceipt and WriteHandoff mode bodies — required bindings, fixed r10 handoff override, all thirteen histories)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 (the eight-path guard and its task-owned path normalization)
  <action>
    The goal of this task is to convert the 08-38 absent preauthorization (`authority_variant=confirmed_absent`, one packet, zero mutation, no receipt/handoff) into an authorized r13 mutation handoff WITHOUT yet performing any Mooncakes mutation. This is a non-mutating state transition: the only outputs are a `PersistAuthorizationReceipt` record and a `WriteHandoff` record produced by the existing `Invoke-Phase08HostedRun.ps1` modes against the durable boundary store populated by 08-38. No `moon publish`, no `Invoke-MooncakesLiveMutation`, no `gh workflow run`, no credential read for mutation.

    Operator intent recorded by 08-38's checkpoint was `authorize-core`. This task consumes that same-turn authorization by invoking the HostedRun receipt and handoff modes with `$AuthorizationResponse='authorize-core'`. The receipt writer (line 112) refuses any other literal, so a misrouted intent fails closed.

    **Step 1 — Baseline Verify (before).** Confirm the eight-path user-dirty baseline captured by 08-38 still holds:
    ```
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify `
      -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" `
      -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
    ```
    The three task-owned paths are the only files Task 4 will edit; the eight protected paths MUST be unchanged. If the baseline file is absent or the captured head differs from current HEAD, re-run with `-Mode Capture` first (the 08-38 baseline may have been consumed by intervening plans), then Verify again.

    **Step 2 — Reconfirm the absent preauthorization and r13 boundary facts.** Read `%TEMP%/mnf-phase08-r13-preauthorization.json`. Require `schema_version='mnf-phase08-pre-authorization/1'`, `release_ref='refs/tags/modules-v0.1.0-r13'`, `authority_variant='confirmed_absent'`, `mutation_count=0`, `output_write_count=0`, exactly one digest-valid `mutation_authorization_packet_path` + matching `mutation_authorization_packet_sha256`, `authorization_receipt_path=$null`, `active_attempt_path=$null`. Re-run `ValidatePreAuthorization` to assert the projection is still digest-closed:
    ```
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization `
      -PreAuthorizationPath "$env:TEMP/mnf-phase08-r13-preauthorization.json" `
      -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json"
    ```
    Also re-verify the r13 tag identity is unchanged: `$localObject=(& git rev-parse refs/tags/modules-v0.1.0-r13).Trim()` MUST equal `611fb2862533ffd959ed830a222da663df80af49`; `$localPeel=(& git rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim()` MUST equal `003b604978a06c87858fe2cdc0a1969fd20159f7`; `& git merge-base --is-ancestor $localPeel HEAD` MUST succeed. If any of these drift, the boundary established by 08-37/08-38 has changed under us — STOP.

    **Step 3 — Locate the durable boundary store populated by 08-38.** The 08-38 Task 2 Step 4-6 sequence populated a store under `%TEMP%/mnf-phase08/...` keyed by `root_intent_sha256`. Re-resolve the boundary locator and artifact root from the preauthorization's packet (which binds `boundary_locator_path`, `artifact_root`, `index_path`, `root_intent_sha256`, `intent_sha256`, `prepared_manifest_sha256`):
    ```
    $p   = Get-Content -LiteralPath "$env:TEMP/mnf-phase08-r13-preauthorization.json" -Raw | ConvertFrom-Json -Depth 100
    $pkt = Get-Content -LiteralPath ([string]$p.mutation_authorization_packet_path) -Raw | ConvertFrom-Json -Depth 100
    $boundaryLocatorPath = [string]$pkt.boundary_locator_path   # or derive from boundary store index
    $artifactRoot        = [string]$pkt.artifact_root
    $locatorPath         = Join-Path $artifactRoot '../phase-08-live-locator.json'   # canonical durable locator path
    ```
    If the packet shape does not bind these paths, fall back to a `Get-ChildItem -Recurse` search of `%TEMP%/mnf-phase08/` for a `mnf-phase08-live-locator/2` document whose `boundary_sha` equals `$localPeel` AND whose `mutation_authorization_packet_sha256` equals `$p.mutation_authorization_packet_sha256`. Exactly one such store must exist. If zero or more than one, STOP — Task 2-4 cannot proceed without an unambiguous durable store.

    **Step 4 — PersistAuthorizationReceipt (the literal authorize-core conversion).** Invoke the HostedRun receipt mode with `$AuthorizationResponse='authorize-core'`. All thirteen terminal-negative history paths must be supplied (they live under `$artifactRoot/historical/`):
    ```
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode PersistAuthorizationReceipt `
      -BoundaryLocatorPath $boundaryLocatorPath -LocatorPath $locatorPath -ArtifactRoot $artifactRoot `
      -ReleaseRef 'refs/tags/modules-v0.1.0-r13' -SourceSha $localPeel -BoundarySha $localPeel `
      -Repository tchivs/moonbit-foundation -Workflow publish-modules.yml `
      -RootIntentSha256 $pkt.root_intent_sha256 -IntentSha256 $pkt.intent_sha256 `
      -PreparedManifestSha256 $pkt.prepared_manifest_sha256 -TargetModule mb-core `
      -MutationAuthorizationPacketPath ([string]$p.mutation_authorization_packet_path) `
      -AuthorizationResponse 'authorize-core' `
      -AttemptZeroHistoryPath "$artifactRoot/historical/attempt-zero.json" `
      -R1HistoryPath "$artifactRoot/historical/r1.json" -R2HistoryPath "$artifactRoot/historical/r2.json" `
      -R3HistoryPath "$artifactRoot/historical/r3.json" -R4HistoryPath "$artifactRoot/historical/r4.json" `
      -R5HistoryPath "$artifactRoot/historical/r5.json" -R6HistoryPath "$artifactRoot/historical/r6.json" `
      -R7HistoryPath "$artifactRoot/historical/r7.json" -R8HistoryPath "$artifactRoot/historical/r8.json" `
      -R9HistoryPath "$artifactRoot/historical/r9.json" -R10HistoryPath "$artifactRoot/historical/r10.json" `
      -R11HistoryPath "$artifactRoot/historical/r11.json" -R12HistoryPath "$artifactRoot/historical/r12.json"
    ```
    The receipt writer (lines 107-124) requires `$AuthorizationResponse -ceq 'authorize-core'`, then validates the packet (`schema_version='mnf-phase08-mutation-authorization-packet/1'`, `release_ref='refs/tags/modules-v0.1.0-r13'`, `boundary_sha=$localPeel`, digest-valid `packet_sha256`) and writes `$artifactRoot/authorization/authorization-receipt.json` + `$artifactRoot/active-attempt.json` (authority_variant=`mutation_authorized`). The output object binds `receipt_path`, `receipt_sha256`, `active_attempt_path`, `active_attempt_sha256`. **STOP condition:** if this call throws any `P08-RECEIPT-*` rule, the packet, the boundary, or the authorization response drifted — do NOT proceed to Task 2.

    **Step 5 — WriteHandoff (produce %TEMP%/mnf-phase08-r13-handoff.json).** With the active attempt now persisted under `$artifactRoot/active-attempt.json`, invoke WriteHandoff. The mode body (lines 1099-1114) hard-codes the fixed handoff path `$env:TEMP/mnf-phase08-r10-handoff.json` internally — but 08-38 Task 2 Step 8 fixed the preauthorization's `fixed_handoff_path` to `mnf-phase08-r13-handoff.json`. Use the same `-HandoffPath` argument shape 08-38 used so the resulting file lands at the path `ValidateHandoff` expects:
    ```
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode WriteHandoff `
      -BoundaryLocatorPath $boundaryLocatorPath -LocatorPath $locatorPath -ArtifactRoot $artifactRoot `
      -ReleaseRef 'refs/tags/modules-v0.1.0-r13' -SourceSha $localPeel -BoundarySha $localPeel `
      -Repository tchivs/moonbit-foundation -Workflow publish-modules.yml `
      -RootIntentSha256 $pkt.root_intent_sha256 -IntentSha256 $pkt.intent_sha256 `
      -PreparedManifestSha256 $pkt.prepared_manifest_sha256 -TargetModule mb-core `
      -AttemptZeroHistoryPath "$artifactRoot/historical/attempt-zero.json" `
      -R1HistoryPath "$artifactRoot/historical/r1.json" -R2HistoryPath "$artifactRoot/historical/r2.json" `
      -R3HistoryPath "$artifactRoot/historical/r3.json" -R4HistoryPath "$artifactRoot/historical/r4.json" `
      -R5HistoryPath "$artifactRoot/historical/r5.json" -R6HistoryPath "$artifactRoot/historical/r6.json" `
      -R7HistoryPath "$artifactRoot/historical/r7.json" -R8HistoryPath "$artifactRoot/historical/r8.json" `
      -R9HistoryPath "$artifactRoot/historical/r9.json" -R10HistoryPath "$artifactRoot/historical/r10.json" `
      -R11HistoryPath "$artifactRoot/historical/r11.json" -R12HistoryPath "$artifactRoot/historical/r12.json"
    ```
    The fixed path internal to `Write-P08HostedHandoff` is `mnf-phase08-r10-handoff.json`; if the 08-38 fixed preauthorization path was `mnf-phase08-r13-handoff.json`, the resulting handoff MUST be copied (or the internal path patched via a follow-up seam) so that `ValidateHandoff` resolves it. The exact mechanics are owned by the existing HostedRun handoff writer — Task 1's job is to make `%TEMP%/mnf-phase08-r13-handoff.json` exist with `schema_version='mnf-phase08-handoff/1'`, `release_ref='refs/tags/modules-v0.1.0-r13'`, the digest-valid `active_attempt_sha256`, and the closed property inventory asserted by `Assert-ReleasePhase08Handoff`. **STOP condition:** if WriteHandoff throws, the receipt or active attempt drifted — do NOT proceed.

    **Step 6 — ValidateHandoff.** Validate the produced handoff against the r13 expectation:
    ```
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff `
      -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json"
    ```
    Require exit 0. The validator (`Assert-ReleasePhase08Handoff`) checks the closed schema, the r13 release_ref, the active-attempt digest, and the historical history-set digest `961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5`.

    **Step 7 — Baseline Verify (after).** Re-run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the same three-path task-owned list as Step 1. The eight protected paths MUST still be unchanged. Task 1 produced no source-tree mutation; only durable state files under `%TEMP%` and the boundary store changed.

    **Task 1 MUST NOT** invoke `PublishOne`, `Invoke-MooncakesLiveMutation`, `Invoke-ReleasePublisher -Mode LiveOneStep`, `gh workflow run`, `gh run create`, or read any credential. Task 1 only converts the absent preauthorization into an authorized mutation handoff; Task 2 performs the first real mutation.
  </action>
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$h=Get-Content -LiteralPath \"$env:TEMP/mnf-phase08-r13-handoff.json\" -Raw | ConvertFrom-Json -Depth 100; if ($h.release_ref -cne 'refs/tags/modules-v0.1.0-r13') { exit 1 }; if ($h.boundary_sha -cne '003b604978a06c87858fe2cdc0a1969fd20159f7') { exit 1 }; $lo=(& git rev-parse refs/tags/modules-v0.1.0-r13).Trim(); if ($lo -cne '611fb2862533ffd959ed830a222da663df80af49') { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
  </automated></verify>
  <done>%TEMP%/mnf-phase08-r13-handoff.json exists with schema `mnf-phase08-handoff/1`, `release_ref='refs/tags/modules-v0.1.0-r13'`, the digest-closed active attempt, and the thirteen-history set digest `961780e0...`; the durable boundary store at the 08-38 location now carries an `authorization-receipt.json` and an `active-attempt.json` (authority_variant=`mutation_authorized`); `ValidateHandoff` exits 0; the eight-path baseline is unchanged before and after; no Mooncakes mutation has yet been performed.</done>
</task>
<task type="auto">
  <name>Task 2: Publish mb-core@0.1.0 + cold consumer proof (core only)</name>
  <files>None (real registry mutation is external; sanitized observation + cold proof live under %TEMP%)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-38-PLAN.md Task 3 (the authorize-core checkpoint this task consumes)
    - .planning/REQUIREMENTS.md lines 32 and 35 (DIST-01 and DIST-04 exact acceptance — exact core 0.1.0 identity, one-node graph, four targets, outside-checkout credential-free evidence)
    - scripts/quality/Invoke-ColdRegistryConsumer.ps1 lines 1-200 (parameter set, isolation facts, four-target runtime requirement, one-node expected graph, `moon publish --frozen` is NOT called by the consumer; only `moon tree`, `moon check`, `moon test`, `moon run` against the freshly resolved registry dependency)
    - scripts/quality/Invoke-MooncakesLiveMutation.ps1 lines 220-261 (the live mutation adapter that performs exactly one `moon publish --frozen` in an isolated publisher Moon home with `MOONCAKES_TOKEN` materialized only for that single call and removed in `finally`)
    - scripts/quality/Get-MooncakesObservation.ps1 (the credential-free structured observer used by the cold consumer's `-ObservationFixturePath` adapter)
    - modules/mb-core/moon.mod.json (identity `tchivs/mb-core`, version `0.1.0`, four-target `+js+wasm+wasm-gc+native`, NO deps)
    - policy/phase-08-distribution.json (the cold-consumer policy: `module_order=[mb-core,mb-color,mb-image]`, `required_targets=[js,wasm,wasm-gc,native]`, the canonical graph nodes/edges)
    - ~/.moon/credentials.json (the credential at `username='tchivs'`; never read into a repo artifact, only materialized inside the publisher's ephemeral Moon home)
  <action>
    This task performs the FIRST real Mooncakes mutation of Phase 8: `moon publish --frozen` of `tchivs/mb-core@0.1.0`. After it succeeds, a fresh credential-free cold registry consumer resolves `tchivs/mb-core@0.1.0` from the public registry and proves one-node graph + four-target behavior. No `mb-color` or `mb-image` publication may happen until this task passes end-to-end.

    The publication itself is invoked by `Invoke-MooncakesLiveMutation`'s adapter inside an isolated GUID-owned publisher Moon home. The adapter performs exactly ONE `moon -C <source> publish --frozen` call (the count is enforced by `LIVE09-ONE-CALL` at line 248). The `MOONCAKES_TOKEN` is read from the step environment, written to `<moonHome>/credentials.json` with `username='tchivs'`, used for the single publish, and the entire Moon home is removed in `finally` (`LIVE11-TEARDOWN`).

    **Step 1 — Baseline Verify (before).** Same eight-path guard as Task 1 Step 1. The eight protected paths MUST be unchanged.

    **Step 2 — Reconfirm the r13 handoff and the authorized active attempt.** Re-run `Invoke-Phase08HostedRun -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json"`. Require exit 0. This is the gate Task 2-4 all hang off: the mutation is authorized only while the handoff remains closed and unchanged.

    **Step 3 — Locate prepared inputs and the pinned toolchain.** The prepared bundle lives under `$artifactRoot/prepared/` (populated by 08-37 Task 3's PrepareAttempt). The pinned toolchain root is bound by `$pkt.toolchain_root` (from the active attempt record) and the native toolchain bin (clang) by the active attempt's `native_toolchain_bin`. Both MUST be revalidated against the pinned version policy (`moon` `0.1.20260713`, `moonc` `v0.10.4+2cc641edf`, `moonrun` `0.1.20260713` per AGENTS.md stack). The prepared mb-core archive is at `$artifactRoot/prepared/archives/mb-core.zip` with `canonical_sha256` bound to the active attempt's prepared manifest.

    **Step 4 — The real publish: one authorized `moon publish --frozen` for mb-core.** Materialize the publisher-only Moon home, write the credential, expand the prepared archive, and call `moon publish --frozen` exactly once:
    ```
    $env:MOONCAKES_TOKEN = <operator-supplied step token>   # never logged; never written into a repo artifact
    pwsh -NoProfile -File ./scripts/quality/Invoke-ReleasePublisher.ps1 -Mode LiveOneStep `
      -RequestPath "$artifactRoot/prepared/request.json" `
      -PreparedRoot "$artifactRoot/prepared" -JournalRoot "$artifactRoot/journal" `
      -ToolchainRoot $pkt.toolchain_root -ProofPaths @("$env:TEMP/mnf-phase08-r13-handoff.json") `
      -ExplicitLiveAuthorization
    # finally: Remove-Item Env:\MOONCAKES_TOKEN
    ```
    `Invoke-ReleasePublisher -Mode LiveOneStep` calls `Invoke-MooncakesLiveMutation`, which:
      - Asserts `-ExplicitLiveAuthorization` is set and an adapter is available (`PUB14-LIVE-GUARD`).
      - Resolves the journal-selected next module as `mb-core` (first eligible; no published checkpoint yet).
      - Validates the prepared mb-core archive digest matches the active attempt's prepared manifest.
      - Creates a GUID-owned publisher Moon home under `%TEMP%/mnf-live-publisher-<guid>/`.
      - Writes `<moonHome>/credentials.json` with `username='tchivs'` and `token=$MOONCAKES_TOKEN`.
      - Expands `$artifactRoot/prepared/archives/mb-core.zip` into `<moonHome>/source/`.
      - Calls `moon -C <moonHome>/source publish --frozen` exactly once.
      - Removes the entire publisher Moon home in `finally`.
    The result object binds `classification` (one of `attempted`, `timeout`, `nonzero`, `not_eligible`), `module='mb-core'`, `mutation_count` (0 if not_eligible, else 1), `reobservation_required`, `raw_output_persisted=false`, `credential_state_removed=true`. **Require `classification -ceq 'attempted' AND mutation_count -eq 1`.** A `nonzero` or `timeout` classification is a partial-publish outcome — STOP, do not retry, do not proceed to mb-color. See the recovery contract below.

    **Step 5 — Post-publish reobservation (PROV-05 + reobservation contract).** `Invoke-MooncakesLiveMutation` sets `reobservation_required=true` whenever `classification -cin 'attempted','timeout','nonzero'`. Run a credential-free read-only Mooncakes observation bounded by STATE.md (20 attempts at 15-second cadence) for `tchivs/mb-core@0.1.0`:
    ```
    & ./scripts/quality/Get-MooncakesObservation.ps1 -Module mb-core `
      -OutputPath "$env:TEMP/mnf-phase08-r13-observation-mb-core.json" `
      -PolicyPath ./policy/phase-08-distribution.json `
      -SchemaPath ./release/registry/module-observation-schema.json
    ```
    Require `outcome='exact'`, `observed.identity='tchivs/mb-core@0.1.0'`, `observed.archive_sha256` matching the prepared archive's `canonical_sha256`, `observed.manifest_sha256` matching the cold-consumer policy's `modules[?module=='mb-core'].manifest_sha256`, and a one-node observed graph. Persist only the sanitized projection (no credential, no token, no cookie-shaped value).

    **Step 6 — Cold registry consumer proof for mb-core (DIST-01 + DIST-04).** Outside the checkout, with a freshly generated empty MOON_HOME, no `moon.work`, no path/git/local dependency, no copied source, no warm registry cache, no credential, pinned toolchain only:
    ```
    & ./scripts/quality/Invoke-ColdRegistryConsumer.ps1 -Module mb-core `
      -OutputPath "$env:TEMP/mnf-phase08-r13-cold-proof-mb-core.json" `
      -ToolchainRoot $pkt.toolchain_root -NativeToolchainBin $pkt.native_toolchain_bin `
      -ObservationFixturePath "$env:TEMP/mnf-phase08-r13-observation-mb-core.json" `
      -PolicyPath ./policy/phase-08-distribution.json -SchemaPath ./release/consumers/proof-schema.json
    ```
    The consumer (`Invoke-ColdRegistryConsumer.ps1` Live mode) generates a GUID-owned root `%TEMP%/mnf-cold-registry-<guid>/`, an empty `moon-home/`, a consumer module `mnf-registry-consumer/mb-core@0.0.0` that depends only on `tchivs/mb-core@0.1.0`, then runs `moon tree`, `moon check`, `moon test`, `moon run` on each of `js`, `wasm`, `wasm-gc`, `native`. It asserts: 15 isolation facts (including `moon_home_initially_empty`, `credentials_absent`, `workspace_absent`, `source_copy_absent`, `path_dependency_absent`, `git_dependency_absent`, `registry_cache_initially_empty`, `archive_cache_absent`, `mooncakes_state_absent`, `pinned_toolchain_explicit`, `ambient_toolchain_ignored`), exact `outcome='exact'` observation, archive + manifest digest agreement, exact one-node graph (`tchivs/mb-core@0.1.0` only, no edges), pinned toolchain identity, four target results each with `check='pass'`, `test='pass'`, `runtime='pass'`, and identical `output_sha256` equal to the behavior contract's `output='consumer=mb-core sum=42 bytes=4 position=0 clock=42'` digest.

    **Step 7 — Record checkpoint into journal (success branch only).** If Step 6 exits 0, write the `core_checkpoint_verified` journal record via the publisher reducer so the active attempt's journal carries the verified mb-core checkpoint digest. This digest is the DIST-01 predecessor evidence that Task 3 will pass to `mb-color` as `PriorAuthorityRecordPath`.

    **Step 8 — Baseline Verify (after).** Re-run `Assert-Phase08R13DirtyBaseline -Mode Verify`. The eight protected paths MUST be unchanged — publication caused no source mutation.

    **STOP conditions (forward-only recovery):**
      - Step 4 classification is `timeout`, `nonzero`, or `not_eligible` → STOP. Do NOT retry `moon publish`. Do NOT proceed to mb-color. A new r14 plan is required (new boundary tag, new absent preauthorization, new authorize-core).
      - Step 5 observation is `outcome='absent'` or `outcome='mismatch'` → STOP. The publish outcome is ambiguous; reobservation alone cannot resolve it. Mooncakes has no unpublish/yank; the published version (if any) is immutable. r14 planning required.
      - Step 5 observation is `outcome='unknown'` → STOP. Same as above.
      - Step 6 cold consumer throws any `COLD01-...` through `COLD13-...` → STOP. The published module does not satisfy the cold registry contract. r14 planning required.
      - Step 8 baseline guard fails → STOP. Something mutated a protected path during publication; investigate before any further work.

    **Task 2 MUST NOT** publish mb-color or mb-image, MUST NOT retry a failed publish, MUST NOT call `moon publish` outside the `Invoke-MooncakesLiveMutation` adapter's single count-bounded call, MUST NOT log the credential token, MUST NOT leave the publisher Moon home alive after the finally block, MUST NOT copy published source back into the repo.
  </action>
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$p=Get-Content -LiteralPath \"$env:TEMP/mnf-phase08-r13-cold-proof-mb-core.json\" -Raw | ConvertFrom-Json -Depth 100; if ($p.evidence_mode -cne 'live_registry') { exit 1 }; if ($p.module -cne 'mb-core' -or $p.identity -cne 'tchivs/mb-core@0.1.0') { exit 1 }; if ($p.resolved_graph.nodes.Count -ne 1 -or $p.resolved_graph.edges.Count -ne 0) { exit 1 }; if ($p.targets.Count -ne 4) { exit 1 }; foreach($t in $p.targets){ if ($t.check -cne 'pass' -or $t.test -cne 'pass' -or $t.runtime -cne 'pass') { exit 1 } }; if (($p.targets.name -join ',') -cne 'js,wasm,wasm-gc,native') { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "if ((Get-Content -LiteralPath \"$env:TEMP/mnf-phase08-r13-observation-mb-core.json\" -Raw) -match '(?i)(token|secret|password|cookie|authorization|bearer|MOONCAKES_TOKEN)') { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
  </automated></verify>
  <acceptance_criteria>
    - `Invoke-ReleasePublisher -Mode LiveOneStep -ExplicitLiveAuthorization` returns `classification='attempted'` and `mutation_count=1` for `module='mb-core'`.
    - Post-publish sanitized observation has `outcome='exact'`, `observed.identity='tchivs/mb-core@0.1.0'`, archive and manifest digest agreement with the prepared bundle and policy.
    - Cold registry consumer proof `%TEMP%/mnf-phase08-r13-cold-proof-mb-core.json` has `evidence_mode='live_registry'`, `verified=true`, one-node zero-edge graph, four targets each `check/test/runtime='pass'`, identical `output_sha256`, `moon_home_initially_empty=true`, `credentials_absent=true`, `workspace_absent=true`, `path_dependency_absent=true`, `git_dependency_absent=true`, `registry_cache_initially_empty=true`, `archive_cache_absent=true`, `mooncakes_state_absent=true`, `pinned_toolchain_explicit=true`.
    - Journal carries `core_checkpoint_verified` with the active attempt's `root_intent_sha256` and `intent_sha256`.
    - The eight-path baseline is unchanged before and after.
    - No credential-shaped value appears in any artifact under `%TEMP%/mnf-phase08-r13-*`.
  </acceptance_criteria>
  <done>tchivs/mb-core@0.1.0 is published to Mooncakes and a fresh credential-free cold consumer outside the repo with an isolated empty MOON_HOME resolves it with a one-node zero-edge graph across js/wasm/wasm-gc/native, all four targets runtime-pass with identical output digests, the post-publish sanitized observation is `outcome='exact'` with archive/manifest digest agreement, the journal carries the verified `core_checkpoint_verified` record, and the eight-path baseline is unchanged. DIST-01 and DIST-04 evidence is satisfied for mb-core.</done>
</task>
<task type="auto">
  <name>Task 3: Publish mb-color@0.1.0 + cold consumer proof (core→color)</name>
  <files>None</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 33 (DIST-02 exact acceptance — color 0.1.0 plus core 0.1.0 edge, two-node graph, four targets)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md lines 117-125 (DIST-02 requires verified DIST-01 checkpoint as predecessor)
    - modules/mb-color/moon.mod.json (identity `tchivs/mb-color@0.1.0`, deps `{ "tchivs/mb-core": "0.1.0" }`)
    - scripts/quality/Invoke-ColdRegistryConsumer.ps1 lines 167-189 (`Get-BehaviorContract` for mb-color: two dependencies, three imports, `output='consumer=mb-color sum=128 encoded=128 alpha=128 roundtrip=128'`)
    - policy/phase-08-distribution.json (the canonical graph edges: `tchivs/mb-color@0.1.0 -> tchivs/mb-core@0.1.0`)
  <action>
    This task performs the SECOND real Mooncakes mutation: `moon publish --frozen` of `tchivs/mb-color@0.1.0`, which depends on the now-published `tchivs/mb-core@0.1.0`. After it succeeds, a fresh cold consumer resolves `tchivs/mb-color@0.1.0` plus its published `tchivs/mb-core@0.1.0` dependency with a two-node one-edge graph and four-target behavior. No `mb-image` publication may happen until this task passes.

    **Step 1 — Baseline Verify (before).** Same eight-path guard.

    **Step 2 — Reconfirm the r13 handoff AND the Task 2 mb-core checkpoint.** Re-run `ValidateHandoff` for the r13 handoff. Then re-load the journal's `core_checkpoint_verified` record from Task 2 Step 7 — its `record_sha256` is the `PriorAuthorityRecordPath` for mb-color's selection. If the core checkpoint record is missing or its digest drifted, the predecessor gate is unsatisfied — STOP.

    **Step 3 — Reobserve mb-core (predecessor liveness).** Run one credential-free read-only Mooncakes observation for `tchivs/mb-core@0.1.0` exactly as in Task 2 Step 5. Require `outcome='exact'` with the SAME archive and manifest digests recorded in Task 2's checkpoint. A drift here means the registry state changed under us — STOP.

    **Step 4 — The real publish: one authorized `moon publish --frozen` for mb-color.** Same publisher-only Moon home pattern as Task 2 Step 4, but with `-TargetModule mb-color` and the core checkpoint record passed as `PriorAuthorityRecordPath`:
    ```
    $env:MOONCAKES_TOKEN = <operator-supplied step token>
    pwsh -NoProfile -File ./scripts/quality/Invoke-ReleasePublisher.ps1 -Mode LiveOneStep `
      -RequestPath "$artifactRoot/prepared/request.json" `
      -PreparedRoot "$artifactRoot/prepared" -JournalRoot "$artifactRoot/journal" `
      -ToolchainRoot $pkt.toolchain_root -ProofPaths @("$env:TEMP/mnf-phase08-r13-handoff.json","$env:TEMP/mnf-phase08-r13-cold-proof-mb-core.json") `
      -ExplicitLiveAuthorization
    # finally: Remove-Item Env:\MOONCAKES_TOKEN
    ```
    The publisher's `Resolve-MooncakesLiveMutationTarget` consumes the journal-selected next module as `mb-color` (the only module whose predecessor `mb-core` has a `core_checkpoint_verified` record and whose own checkpoint is absent). The mutation adapter expands `$artifactRoot/prepared/archives/mb-color.zip`, writes the credential, and calls `moon -C <moonHome>/source publish --frozen` exactly once. The adapter enforces `LIVE09-ONE-CALL`. **Require `classification='attempted'` AND `mutation_count=1` AND `module='mb-color'`.** Any other outcome is a partial-publish — STOP, do not proceed to mb-image.

    **Step 5 — Post-publish reobservation for mb-color.** Credential-free observation:
    ```
    & ./scripts/quality/Get-MooncakesObservation.ps1 -Module mb-color `
      -OutputPath "$env:TEMP/mnf-phase08-r13-observation-mb-color.json" `
      -PolicyPath ./policy/phase-08-distribution.json `
      -SchemaPath ./release/registry/module-observation-schema.json
    ```
    Require `outcome='exact'`, `observed.identity='tchivs/mb-color@0.1.0'`, archive + manifest digest agreement, and a two-node one-edge observed graph `tchivs/mb-color@0.1.0 -> tchivs/mb-core@0.1.0`. Persist only the sanitized projection.

    **Step 6 — Cold registry consumer proof for mb-color (DIST-02 + DIST-04).** Fresh empty MOON_HOME, no moon.work/path-dep/git-dep/copied-source/warm-cache/credential:
    ```
    & ./scripts/quality/Invoke-ColdRegistryConsumer.ps1 -Module mb-color `
      -OutputPath "$env:TEMP/mnf-phase08-r13-cold-proof-mb-color.json" `
      -ToolchainRoot $pkt.toolchain_root -NativeToolchainBin $pkt.native_toolchain_bin `
      -ObservationFixturePath "$env:TEMP/mnf-phase08-r13-observation-mb-color.json" `
      -PolicyPath ./policy/phase-08-distribution.json -SchemaPath ./release/consumers/proof-schema.json
    ```
    The consumer generates a new `%TEMP%/mnf-cold-registry-<guid>/` and a consumer module `mnf-registry-consumer/mb-color@0.0.0` that depends on `tchivs/mb-color@0.1.0` (which itself depends on `tchivs/mb-core@0.1.0`). It asserts: all 15 isolation facts, exact `outcome='exact'` observation, archive + manifest digest agreement, exact two-node one-edge graph (both core and color nodes present, one edge color→core), pinned toolchain identity, four targets each runtime-pass with identical `output_sha256` equal to the behavior contract's `output='consumer=mb-color sum=128 encoded=128 alpha=128 roundtrip=128'`.

    **Step 7 — Record `color_checkpoint_verified` into the journal.** Same reducer pattern as Task 2 Step 7. The `color_checkpoint_verified` record's digest is Task 4's `PriorAuthorityRecordPath` for mb-image.

    **Step 8 — Baseline Verify (after).** Eight-path guard.

    **STOP conditions:** same as Task 2 (forward-only; no retry, no in-place recovery; any publish failure or cold-consumer failure or observation drift means r14 planning). Additionally, if Task 2's core checkpoint is missing or its observation drifted, STOP at Step 2.
  </action>
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$p=Get-Content -LiteralPath \"$env:TEMP/mnf-phase08-r13-cold-proof-mb-color.json\" -Raw | ConvertFrom-Json -Depth 100; if ($p.evidence_mode -cne 'live_registry') { exit 1 }; if ($p.module -cne 'mb-color' -or $p.identity -cne 'tchivs/mb-color@0.1.0') { exit 1 }; if ($p.resolved_graph.nodes.Count -ne 2 -or $p.resolved_graph.edges.Count -ne 1) { exit 1 }; $edge=$p.resolved_graph.edges[0]; if ($edge.from -cne 'tchivs/mb-color@0.1.0' -or $edge.to -cne 'tchivs/mb-core@0.1.0') { exit 1 }; if ($p.targets.Count -ne 4) { exit 1 }; foreach($t in $p.targets){ if ($t.check -cne 'pass' -or $t.test -cne 'pass' -or $t.runtime -cne 'pass') { exit 1 } }; if (($p.targets.name -join ',') -cne 'js,wasm,wasm-gc,native') { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
  </automated></verify>
  <acceptance_criteria>
    - Publisher LiveOneStep returns `classification='attempted'`, `mutation_count=1`, `module='mb-color'`.
    - Post-publish sanitized observation: `outcome='exact'`, `observed.identity='tchivs/mb-color@0.1.0'`, two-node one-edge observed graph, archive + manifest digest agreement.
    - Cold proof `%TEMP%/mnf-phase08-r13-cold-proof-mb-color.json`: `evidence_mode='live_registry'`, `verified=true`, two-node one-edge graph `color->core`, four targets each runtime-pass with identical output digests, all 15 isolation facts true.
    - Journal carries `color_checkpoint_verified` with the active attempt digests.
    - Eight-path baseline unchanged.
    - No credential-shaped value in any artifact.
  </acceptance_criteria>
  <done>tchivs/mb-color@0.1.0 is published to Mooncakes with its published `tchivs/mb-core@0.1.0` dependency and a fresh credential-free cold consumer resolves the two-node one-edge graph across all four targets with identical behavior digests; the post-publish observation is `outcome='exact'`; the journal carries `color_checkpoint_verified`; the eight-path baseline is unchanged. DIST-02 is satisfied.</done>
</task>
<task type="auto">
  <name>Task 4: Publish mb-image@0.1.0 + cold consumer proof (full graph) + Phase 8 closure</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 34 (DIST-03 exact acceptance — full graph, bounded PPM public stack, four targets) and line 43 (PROV-05 — read-only Mooncakes observation proves each module page renders intended metadata)
    - .planning/REQUIREMENTS.md lines 62-71 (Definition of Done — items 3 and 6 are what this task closes)
    - modules/mb-image/moon.mod.json (identity `tchivs/mb-image@0.1.0`, deps `tchivs/mb-core@0.1.0` AND `tchivs/mb-color@0.1.0`)
    - scripts/quality/Invoke-ColdRegistryConsumer.ps1 lines 182-189 (`Get-BehaviorContract` for mb-image: three dependencies, six imports spanning `mb-core/budget`, `mb-core/bytes`, `mb-core/error`, `mb-core/io`, `mb-color/model`, `mb-image/codec`, `mb-image/ppm`; `output='consumer=mb-image bytes_read=17 bytes_written=17 width=2 height=1 digest=237717273'` — the bounded PPM public stack)
    - policy/phase-08-distribution.json (three-node graph with edges `image->core`, `image->color`, `color->core`)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md lines 96-116 (the r12 audit table this task replaces with the r13 audit table; lines 117-125 adjacency rules; lines 127-144 capability matrix; lines 145-163 prohibitions and artifacts)
    - release/qualification/phase-07-requirements.json (the reciprocal-evidence JSON schema this task mirrors for phase-08)
  <action>
    This task performs the THIRD and final Mooncakes mutation: `moon publish --frozen` of `tchivs/mb-image@0.1.0`, then proves the full three-node two-edge graph cold-consumption and closes Phase 8 by writing the r13 evidence map.

    **Step 1 — Baseline Verify (before).** Eight-path guard.

    **Step 2 — Reconfirm r13 handoff AND Task 3 mb-color checkpoint.** `ValidateHandoff` for the r13 handoff. Reload the journal's `color_checkpoint_verified` from Task 3 Step 7 — its `record_sha256` is the `PriorAuthorityRecordPath` for mb-image. If missing or drifted, STOP.

    **Step 3 — Reobserve mb-color (predecessor liveness).** Credential-free observation for `tchivs/mb-color@0.1.0`. Require `outcome='exact'` with the same archive and manifest digests recorded in Task 3's checkpoint.

    **Step 4 — The real publish: one authorized `moon publish --frozen` for mb-image.** Same publisher-only Moon home pattern with `-TargetModule mb-image` and the color checkpoint as predecessor:
    ```
    $env:MOONCAKES_TOKEN = <operator-supplied step token>
    pwsh -NoProfile -File ./scripts/quality/Invoke-ReleasePublisher.ps1 -Mode LiveOneStep `
      -RequestPath "$artifactRoot/prepared/request.json" `
      -PreparedRoot "$artifactRoot/prepared" -JournalRoot "$artifactRoot/journal" `
      -ToolchainRoot $pkt.toolchain_root -ProofPaths @("$env:TEMP/mnf-phase08-r13-handoff.json","$env:TEMP/mnf-phase08-r13-cold-proof-mb-color.json") `
      -ExplicitLiveAuthorization
    # finally: Remove-Item Env:\MOONCAKES_TOKEN
    ```
    `Resolve-MooncakesLiveMutationTarget` selects `mb-image` (only module whose predecessors `mb-core` AND `mb-color` have checkpoint records and whose own is absent). The adapter expands `$artifactRoot/prepared/archives/mb-image.zip`, calls `moon -C <moonHome>/source publish --frozen` exactly once. **Require `classification='attempted'` AND `mutation_count=1` AND `module='mb-image'`.** Any other outcome — STOP. There is no r14 yet; further work requires new planning.

    **Step 5 — Post-publish reobservation for mb-image.** Credential-free observation:
    ```
    & ./scripts/quality/Get-MooncakesObservation.ps1 -Module mb-image `
      -OutputPath "$env:TEMP/mnf-phase08-r13-observation-mb-image.json" `
      -PolicyPath ./policy/phase-08-distribution.json `
      -SchemaPath ./release/registry/module-observation-schema.json
    ```
    Require `outcome='exact'`, `observed.identity='tchivs/mb-image@0.1.0'`, archive + manifest digest agreement, three-node two-edge observed graph.

    **Step 6 — Cold registry consumer proof for mb-image (DIST-03 + DIST-04).** Fresh empty MOON_HOME:
    ```
    & ./scripts/quality/Invoke-ColdRegistryConsumer.ps1 -Module mb-image `
      -OutputPath "$env:TEMP/mnf-phase08-r13-cold-proof-mb-image.json" `
      -ToolchainRoot $pkt.toolchain_root -NativeToolchainBin $pkt.native_toolchain_bin `
      -ObservationFixturePath "$env:TEMP/mnf-phase08-r13-observation-mb-image.json" `
      -PolicyPath ./policy/phase-08-distribution.json -SchemaPath ./release/consumers/proof-schema.json
    ```
    The consumer asserts three-node two-edge graph (image, color, core all present; edges image→core, image→color, color→core), pinned toolchain identity, four targets each runtime-pass with identical `output_sha256` equal to the bounded PPM public stack output digest. This is the bounded PPM public stack requirement of DIST-03.

    **Step 7 — PROV-05 final read-only observation across all three module pages.** Reobserve all three module pages credential-free in one bounded sweep (each bounded by STATE.md 20×15s) and assert each renders the intended qualified metadata: identity, version `0.1.0`, four-target support, license `Apache-2.0`, repository `https://github.com/tchivs/moonbit-foundation`, README pointer `README.mbt.md`, description text, and the intended dependency graph. Persist three sanitized observations:
    ```
    & ./scripts/quality/Get-MooncakesObservation.ps1 -Module mb-core  -OutputPath "$env:TEMP/mnf-phase08-r13-observation-final-mb-core.json"  -PolicyPath ./policy/phase-08-distribution.json -SchemaPath ./release/registry/module-observation-schema.json
    & ./scripts/quality/Get-MooncakesObservation.ps1 -Module mb-color -OutputPath "$env:TEMP/mnf-phase08-r13-observation-final-mb-color.json" -PolicyPath ./policy/phase-08-distribution.json -SchemaPath ./release/registry/module-observation-schema.json
    & ./scripts/quality/Get-MooncakesObservation.ps1 -Module mb-image -OutputPath "$env:TEMP/mnf-phase08-r13-observation-final-mb-image.json" -PolicyPath ./policy/phase-08-distribution.json -SchemaPath ./release/registry/module-observation-schema.json
    ```
    Each MUST have `outcome='exact'` and match the qualified source documentation set per PROV-03.

    **Step 8 — Record `image_checkpoint_verified` into the journal.** Same reducer pattern. This completes the monotonic journal.

    **Step 9 — Write closure tests** in `scripts/quality/Test-Phase08Qualification.ps1` (extend the existing test, do not rewrite): add a `Phase08R13Closure` test that (a) reads `%TEMP%/mnf-phase08-r13-handoff.json` and asserts `release_ref='refs/tags/modules-v0.1.0-r13'` and `ValidateHandoff` exit 0; (b) reads the three cold-proof JSON files and asserts `evidence_mode='live_registry'`, `verified=true`, the one/two/three-node graphs respectively, and four targets each runtime-pass; (c) asserts the three post-publish observations all have `outcome='exact'`; (d) asserts no artifact under `%TEMP%/mnf-phase08-r13-*` matches the secret-shape regex (`(?i)(gh[opusr]_|bearer|MOONCAKES_TOKEN|credentials.json|token[:=])`). The test must `Set-StrictMode -Version Latest` and use the existing `Throw-P08Qualification` helper.

    **Step 10 — Write the r13 reciprocal evidence JSON** at `release/qualification/phase-08-requirements.json` mirroring `phase-07-requirements.json`'s schema. Bind for each of DIST-01..04 + PROV-05: the requirement id, status `satisfied`, the r13 boundary tag (`611fb286`/`003b604`), the source SHA, the cold-proof file SHA-256, the observation file SHA-256, the journal checkpoint digest, and a reference to this plan (08-39). Include the thirteen-history LF aggregate `961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5` as the r13 history-set digest. No credential material may appear.

    **Step 11 — Write the Phase 8 evidence map** at `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md`. Append a new section `## R13 Forward-Recovery Source Audit` mirroring the r10/r11/r12 audit tables (lines 40-115) but for the 08-37 → 08-38 → 08-39 chain. Bind: r13/tag/source/thirteen-history (boundary facts from 08-37); sanitized authority (the absent preauthorization from 08-38 and the receipt+handoff from 08-39 Task 1); observation (the three post-publish observations); identity (the three `tchivs/mb-*@0.1.0` identities); graph (one/two/three-node cold proofs); toolchain (the pinned `0.1.20260713` triple); targets (`js,wasm,wasm-gc,native` per module); behavior (the three contract outputs); baseline (the unchanged eight paths). Mark DIST-01..04 + PROV-05 as COVERED with plan reference 08-39. Update the Capability Matrix decisions to note that the three mutations are now executed (not just contracted). Do NOT delete or rewrite the prior r10/r11/r12 audit tables — they are immutable terminal evidence; only append.

    **Step 12 — Baseline Verify (after).** Eight-path guard. The three task-owned paths (`release/qualification/phase-08-requirements.json`, `scripts/quality/Test-Phase08Qualification.ps1`, `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md`) are permitted to be dirty; the eight protected paths MUST be unchanged.

    **STOP conditions:** same forward-only contract. A failure in mb-image publish (Step 4 nonzero/timeout), observation drift (Step 5/7 absent/mismatch/unknown), or cold consumer failure (Step 6 any COLD rule) halts the chain. Phase 8 closure (Steps 9-11) is ONLY permitted if all three publishes and all three cold consumers pass. If any failure occurs, write a SUMMARY recording exactly where the chain halted and the published-vs-unpublished state; do NOT update COVERAGE.md to claim DIST-* completion; do NOT update STATE.md or ROADMAP.md (orchestrator owns those).

    **Task 4 MUST NOT** update STATE.md or ROADMAP.md, MUST NOT create a new tag, MUST NOT retag r13, MUST NOT call `gh release create`, MUST NOT invoke Phase 9 ledger/provenance work, MUST NOT add a new module family.
  </action>
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$p=Get-Content -LiteralPath \"$env:TEMP/mnf-phase08-r13-cold-proof-mb-image.json\" -Raw | ConvertFrom-Json -Depth 100; if ($p.evidence_mode -cne 'live_registry') { exit 1 }; if ($p.module -cne 'mb-image' -or $p.identity -cne 'tchivs/mb-image@0.1.0') { exit 1 }; if ($p.resolved_graph.nodes.Count -ne 3 -or $p.resolved_graph.edges.Count -ne 2) { exit 1 }; $nodeIds=@($p.resolved_graph.nodes.identity | Sort-Object); if (($nodeIds -join ',') -cne 'tchivs/mb-color@0.1.0,tchivs/mb-core@0.1.0,tchivs/mb-image@0.1.0') { exit 1 }; if ($p.targets.Count -ne 4) { exit 1 }; foreach($t in $p.targets){ if ($t.check -cne 'pass' -or $t.test -cne 'pass' -or $t.runtime -cne 'pass') { exit 1 } }; if (($p.targets.name -join ',') -cne 'js,wasm,wasm-gc,native') { exit 1 }; if ($p.behavior.output_sha256 -cnotmatch '^[0-9a-f]{64}$') { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$j=Get-Content -LiteralPath ./release/qualification/phase-08-requirements.json -Raw | ConvertFrom-Json -Depth 50; if (($j.requirements | Where-Object { $_.id -cin @('DIST-01','DIST-02','DIST-03','DIST-04','PROV-05') -and $_.status -cne 'satisfied' }).Count -ne 0) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "if ((Select-String -Path ./release/qualification/phase-08-requirements.json,./.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md -Pattern '(?i)(gh[opusr]_[A-Za-z0-9_]{12,}|bearer\s+[A-Za-z0-9._-]+|MOONCAKES_TOKEN)').Count -ne 0) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "release/qualification/phase-08-requirements.json,scripts/quality/Test-Phase08Qualification.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
  </automated></verify>
  <acceptance_criteria>
    - Publisher LiveOneStep returns `classification='attempted'`, `mutation_count=1`, `module='mb-image'`.
    - Post-publish observation `outcome='exact'`, three-node two-edge graph.
    - Cold proof three-node two-edge graph (image/color/core), four targets each runtime-pass with identical bounded PPM public stack output digest.
    - All three module pages render intended qualified metadata credential-free (PROV-05).
    - Journal carries `image_checkpoint_verified`, `color_checkpoint_verified`, `core_checkpoint_verified` (monotonic three-checkpoint chain).
    - `Test-Phase08Qualification.ps1` exits 0 including the new `Phase08R13Closure` test.
    - `release/qualification/phase-08-requirements.json` marks DIST-01..04 + PROV-05 as `satisfied` with r13 boundary, source SHA, cold-proof, observation, and checkpoint evidence bound.
    - `COVERAGE.md` has a new `## R13 Forward-Recovery Source Audit` section binding boundary/authority/observation/identity/graph/toolchain/targets/behavior/baseline evidence to 08-37 → 08-38 → 08-39.
    - Eight-path baseline unchanged; no credential-shaped value in any artifact or committed file.
  </acceptance_criteria>
  <done>tchivs/mb-image@0.1.0 is published with the full three-node graph cold-consumed across all four targets with the bounded PPM public stack; all three module pages render intended qualified metadata credential-free; the journal carries the monotonic three-checkpoint chain; `Test-Phase08Qualification.ps1` passes including the new closure test; `release/qualification/phase-08-requirements.json` records DIST-01..04 + PROV-05 as satisfied; `COVERAGE.md` carries the r13 evidence map; the eight-path baseline is unchanged. Phase 8 is closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| absent preauthorization to authorized handoff | Task 1 converts the 08-38 absent preauthorization into a mutation-authorized handoff only via the literal `authorize-core` response; `Write-P08AuthorizationReceipt` (line 112) refuses any other literal. |
| handoff to live mutation | Tasks 2-4 invoke `Invoke-ReleasePublisher -Mode LiveOneStep -ExplicitLiveAuthorization` only after `ValidateHandoff` exits 0; the publisher's `PUB14-LIVE-GUARD` refuses without explicit authorization. |
| publisher Moon home to credential | The `MOONCAKES_TOKEN` is materialized only inside a GUID-owned publisher Moon home as `<moonHome>/credentials.json` for the duration of the single `moon publish --frozen` call, then removed in `finally` (`LIVE11-TEARDOWN` enforces no survival). |
| registry state to cold consumer | Each cold consumer runs in a fresh `%TEMP%/mnf-cold-registry-<guid>/` with an empty MOON_HOME, no `moon.work`, no credentials, no copied source, no warm cache; the consumer enforces 15 isolation facts fail-closed. |
| cold consumer to four-target behavior | `Invoke-ColdRegistryConsumer` requires all of `js`, `wasm`, `wasm-gc`, `native` to pass `check`, `test`, AND `run` with identical `output_sha256`; any drift is a `COLD10-BEHAVIOR` failure. |
| source tree to publication | The eight-path dirty-baseline guard runs before and after every task; the three task-owned paths (`phase-08-requirements.json`, `Test-Phase08Qualification.ps1`, `COVERAGE.md`) are the only permitted source-tree mutations, and only in Task 4. |
| failure to recovery | Forward-only: any publish failure, observation drift, or cold-consumer failure halts the chain; no in-place retry, no overwrite/delete/unpublish/yank, no r14 in this plan. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-39-01 | Tampering | registry identity drift | critical | mitigate | Each publish is followed by a credential-free `Get-MooncakesObservation` requiring `outcome='exact'` with `observed.identity` matching the qualified module name AND `observed.archive_sha256` + `observed.manifest_sha256` matching the prepared bundle and `policy/phase-08-distribution.json`. A drift in identity, archive digest, or manifest digest is a publish outcome mismatch — STOP, do not advance to the successor. Tasks 2/3/4 Steps 5 each gate the cold consumer on this observation. The cold consumer (Step 6) re-derives the graph from `moon tree` output and asserts node/edge equality against the policy graph (`COLD08-GRAPH`), so a substituted dependency or missing edge fails closed. |
| T-08-39-02 | Information Disclosure | consumer warm-cache leak | critical | mitigate | Each `Invoke-ColdRegistryConsumer` call generates a fresh GUID-owned `%TEMP%/mnf-cold-registry-<guid>/` and asserts all 15 isolation facts including `moon_home_initially_empty`, `registry_cache_initially_empty`, `registry_index_cache_absent`, `archive_cache_absent`, `mooncakes_state_absent`, `path_dependency_absent`, `git_dependency_absent`, `local_dependency_absent`, `alternate_dependency_source_absent`, `workspace_absent`, `source_copy_absent`, `credentials_absent`. The generated `moon.mod.json` is regex-scanned for `(path|git|workspace|local)` keys and `..` paths (`COLD03-CONTAMINATION`). Each task uses its own fresh cold root — Task 3's cold consumer cannot reuse Task 2's warm cache because each invokes `Invoke-ColdRegistryConsumer` with a new GUID. |
| T-08-39-03 | Information Disclosure | credential leak | critical | mitigate | The `MOONCAKES_TOKEN` is set only in the step environment for the duration of the publisher LiveOneStep call and removed in a `finally` block. Inside `Invoke-MooncakesLiveMutation` the token is written only to `<moonHome>/credentials.json` where `<moonHome>` is a GUID-owned publisher Moon home, used for exactly one `moon publish --frozen` call, and removed in `finally` (`LIVE11-TEARDOWN` enforces the directory is gone). No artifact under `%TEMP%/mnf-phase08-r13-*` may match the secret-shape regex (`(?i)(gh[opusr]_|bearer|MOONCAKES_TOKEN|credentials.json|token[:=])`); the `Test-Phase08Qualification.ps1` Phase08R13Closure test enforces this. The committed files (`phase-08-requirements.json`, `COVERAGE.md`) are also scanned in Task 4 Step 12's verify block. The credential at `~/.moon/credentials.json` is never read into a repo artifact; it is consumed only via the step environment variable by the publisher adapter. |
| T-08-39-04 | Denial of Service / Tampering | partial-publish recovery | critical | mitigate | Mooncakes has no overwrite/delete/unpublish/yank (AGENTS.md explicit prohibition; REQUIREMENTS.md Out of Scope). A `timeout`, `nonzero`, or `not_eligible` classification from `Invoke-MooncakesLiveMutation` is treated as terminal for this plan. Each task's STOP conditions explicitly forbid in-place retry. The published version (if `moon publish` partially succeeded before failing) is immutable; reobservation alone cannot reverse it. Recovery requires r14 planning: a new boundary tag (r14), a new absent preauthorization, a new `authorize-core`, and a journal-selected forward-corrected intent that accounts for whatever the partial publish left on the registry. The journal reducer (`Resolve-PublisherTransition`) records the ambiguous outcome as terminal evidence. |
| T-08-39-05 | Tampering | dirty-baseline drift | high | mitigate | `Assert-Phase08R13DirtyBaseline -Mode Verify` runs before and after every task (Task 1 Steps 1+7, Task 2 Steps 1+8, Task 3 Steps 1+8, Task 4 Steps 1+12). The eight protected paths (`.planning/config.json`, `docs/governance/decisions/0001-sole-owner-bootstrap.md`, `docs/governance/rfc-process.md`, `.codebase-memory/`, the phase-08 `research-plan-input.json`, `.planning/research/.cache/`, `.github/workflows/quality.yml`, the quick-plan `PLAN.md`) must show no status or sha256 drift. The three task-owned paths (`release/qualification/phase-08-requirements.json`, `scripts/quality/Test-Phase08Qualification.ps1`, the phase-08 `COVERAGE.md`) are permitted dirty ONLY in Task 4; Tasks 1-3 do not touch them. Any other dirty path fails with `P08-R13-UNEXPECTED-DRIFT`. |
| T-08-39-06 | Elevation of Privilege | handoff replay across r-attempts | high | mitigate | The r13 handoff binds `release_ref='refs/tags/modules-v0.1.0-r13'` and `boundary_sha='003b604978a06c87858fe2cdc0a1969fd20159f7'`. `ValidateHandoff` recomputes the active attempt digest and the thirteen-history set digest (`961780e0...`) and rejects any drift. The publisher's `Assert-PublisherRequest` (lines 30-48) requires the exact thirteen `historical_*_sha256` values AND the recomputed set digest to equal `961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5` — a stale r12 handoff or a tampered history fails with `PUB16-HISTORY`. The initial-request source_sha blocklist (line 63) rejects any prior failed attempt's source SHA, so an r12 handoff cannot be replayed. |
| T-08-39-07 | Repudiation | journal monotonicity | high | mitigate | The journal reducer (`Resolve-PublisherTransition`) accepts only one transition per (root_intent_sha256, intent_sha256, module) triple. Each task's checkpoint record (`core_checkpoint_verified`, `color_checkpoint_verified`, `image_checkpoint_verified`) carries a `record_sha256` that the successor task passes as `PriorAuthorityRecordPath`. The publisher's `Resolve-MooncakesLiveMutationTarget` selects the next module by scanning the journal for verified predecessor checkpoints; a missing or digest-drifted predecessor fails with `PUB10-STALE-FORK` or `PUB11-CORRECTION-SEQUENCE`. The monotonic chain is preserved across Tasks 2 → 3 → 4 by Steps 2 of each task reconfirming the predecessor checkpoint before any mutation. |
| T-08-39-08 | Tampering | tag immutability (r13 retag/recreate) | critical | mitigate | The r13 tag was created and pushed in 08-37 (object `611fb2862533ffd959ed830a222da663df80af49`, peel `003b604978a06c87858fe2cdc0a1969fd20159f7`). Tasks 1-4 MUST NOT create, retag, delete, or force-push any tag. Task 1 Step 2 and each subsequent task's baseline reconfirmation re-verify the tag object and peel are byte-identical locally and that the peel is an ancestor of HEAD. `Assert-P08PrepareCloneRefBeforeBoundary` (line 565) would throw `P08-PREPARE-REF` if the peel drifted from the boundary SHA. Mooncakes has no force-tag recovery; an r13 retag would be a critical incident requiring fresh planning, never an in-place fix. |
</threat_model>
<verification>Re-run the eight-path dirty-baseline guard before AND after every task; prove Task 1 produced a `ValidateHandoff` exit 0 against `%TEMP%/mnf-phase08-r13-handoff.json` with `release_ref='refs/tags/modules-v0.1.0-r13'` and `boundary_sha='003b604978a06c87858fe2cdc0a1969fd20159f7'`; prove each of Tasks 2/3/4 returns `classification='attempted' AND mutation_count=1` for the exact intended module from `Invoke-ReleasePublisher -Mode LiveOneStep -ExplicitLiveAuthorization`; prove each post-publish credential-free observation has `outcome='exact'` with the qualified identity and matching archive/manifest digests; prove each cold consumer proof has `evidence_mode='live_registry' AND verified=true` with the exact one/two/three-node graph (no edges for core, color→core for color, image→core + image→color + color→core for image), four targets each `check/test/runtime='pass'` with identical `output_sha256`, and all 15 isolation facts true; prove PROV-05's three final observations all render intended qualified metadata credential-free; prove the journal carries the monotonic three-checkpoint chain; prove `Test-Phase08Qualification.ps1` exits 0 including the new closure test; prove `release/qualification/phase-08-requirements.json` marks DIST-01..04 + PROV-05 as satisfied; prove `COVERAGE.md` carries the new r13 evidence map without rewriting r10/r11/r12 audit tables; prove no credential-shaped value appears in any artifact under `%TEMP%/mnf-phase08-r13-*` or in any committed file; prove the r13 tag object and peel are byte-identical before and after every task. No overwrite/delete/unpublish/yank operation; no r14 tag; no STATE.md or ROADMAP.md update (orchestrator owns them after Task 4).</verification>
<success_criteria>Only an operator-authorized r13 publication chain that converts the 08-38 absent preauthorization into a mutation handoff, then performs three irreversible `moon publish --frozen` mutations in strict order (mb-core → mb-color → mb-image), each gated by a fresh credential-free cold registry consumer that proves the published module resolves with the intended one/two/three-node graph and four-target behavior across js/wasm/wasm-gc/native, and closes with PROV-05 read-only observation of all three module pages plus the reciprocal evidence JSON and the r13 COVERAGE.md audit table — all while the eight-path dirty baseline stays unchanged and no credential material enters any artifact — satisfies DIST-01..04 and PROV-05. Any publish failure, observation drift, or cold-consumer failure halts the chain for r14 planning; no in-place retry, no destructive recovery, no tag recreation, no STATE.md or ROADMAP.md mutation during execution.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-39-SUMMARY.md` when done.</output>
