---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - policy/phase-08-distribution.json
  - release/registry/module-observation-schema.json
  - scripts/quality/Get-MooncakesObservation.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
autonomous: true
requirements: [DIST-04, PROV-05]
must_haves:
  truths:
    - "Credential-free structured observation projects only allowlisted Mooncakes public fields and never treats SPA HTML as authority (D-16)."
    - "An observation is accepted only when identity, exact version, metadata, package inventory, dependency graph, manifest, archive checksum, and versioned assets/docs agree exactly; missing, empty, drifted, or ambiguous facts fail (D-10, D-17)."
    - "Polling is bounded and deterministic, and timeout or disagreement yields unknown/incident without authorizing retry or another mutation (D-13, D-14, D-15)."
  artifacts:
    - path: "policy/phase-08-distribution.json"
      provides: "Canonical core-color-image graph, exact metadata projections, target set, polling bounds, and evidence order"
    - path: "release/registry/module-observation-schema.json"
      provides: "Closed sanitized public observation contract"
    - path: "scripts/quality/Get-MooncakesObservation.ps1"
      provides: "Credential-free structured Mooncakes observer"
    - path: "scripts/quality/Test-MooncakesObservation.ps1"
      provides: "Positive, disagreement, redaction, and bounded-polling tests"
  key_links:
    - from: "scripts/quality/Get-MooncakesObservation.ps1"
      to: "policy/phase-08-distribution.json"
      via: "exact module/version/metadata/graph comparison"
      pattern: "phase-08-distribution.json"
    - from: "scripts/quality/Get-MooncakesObservation.ps1"
      to: "release/registry/module-observation-schema.json"
      via: "closed sanitized result emission"
      pattern: "module-observation-schema.json"
---

<objective>
Create the credential-free registry observer that turns untrusted live Mooncakes surfaces into one closed exact-equality decision.

Purpose: Make public observation authoritative enough to gate downstream publication without leaking raw responses or guessing endpoint semantics.
Output: Distribution policy, observation schema, structured observer, and adversarial selector.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-RESEARCH.md
@policy/release-qualification.json
@policy/registry-authority.json
@release/registry/authority-observation.json
@modules/mb-core/moon.mod.json
@modules/mb-color/moon.mod.json
@modules/mb-image/moon.mod.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define closed observation and exact-equality policy</name>
  <files>policy/phase-08-distribution.json, release/registry/module-observation-schema.json, scripts/quality/Test-MooncakesObservation.ps1</files>
  <read_first>policy/release-qualification.json, policy/registry-authority.json, release/registry/authority-observation.json, modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json, modules/mb-core/README.mbt.md, modules/mb-color/README.mbt.md, modules/mb-image/README.mbt.md</read_first>
  <behavior>
    - Canonical module order is core then color then image, while normalized graph serialization is deterministic and never changes semantic node/edge equality.
    - Exact equality accepts only the exact expected version, dependency graph, metadata projection, package inventory, and strongest available identity.
    - Missing/empty surface data, checksum disagreement, metadata drift, unexpected graph nodes/edges, reordered semantic content, malformed input, or redaction failure stops with a stable classification.
  </behavior>
  <action>Per D-10, D-13, D-16, and D-17, define one closed distribution policy and sanitized observation schema. Include exact module identities/versions, expected graph adjacency, qualified metadata digests/fields, supported targets when exposed, bounded polling cadence/attempts, surface classifications, and terminal dispositions. The dependency source is registry-only; no alternative source is permitted. Write fixture-driven tests before the observer implementation, including adjacent-version, empty, ordering, conflicting-surface, secret-shaped input, and stronger/weaker checksum cases.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Test-MooncakesObservation.ps1 -SchemaOnly</automated>
  </verify>
  <acceptance_criteria>Policy and schema are closed and deterministic; every Phase 8 module, edge, metadata field, surface, polling state, and failure disposition is enumerated.</acceptance_criteria>
  <done>The focused policy/schema selector exits zero and rejects every missing, extra, reordered, adjacent-version, or unrecognized module, edge, metadata field, surface, polling state, and disposition fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded structured public observation</name>
  <files>scripts/quality/Get-MooncakesObservation.ps1, scripts/quality/Test-MooncakesObservation.ps1</files>
  <read_first>policy/phase-08-distribution.json, release/registry/module-observation-schema.json, scripts/quality/Test-RegistryAuthority.ps1, scripts/quality/ReleasePublisher.Common.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</read_first>
  <action>Per D-13 through D-19, implement a credential-free adapter layer for publish preflight/whoami classification, structured public user/module observation, registry index, archive download/checksum, downloaded manifest, and versioned assets/docs. Detect the current endpoint/field shape from sanitized structured responses rather than assuming it; allowlist and canonicalize fields before persistence, discard raw bodies, and record attempt numbers/timestamps/classifications. Any missing required surface or cross-surface disagreement returns `unknown`/incident and never decides that mutation may repeat. Do not scrape application HTML, read a secret, write a credential file, or commit a live raw response.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Test-MooncakesObservation.ps1</automated>
  </verify>
  <acceptance_criteria>All integrated Mooncakes surfaces produce one sanitized content-addressed observation on agreement; every empty, adjacent, drifted, ambiguous, or secret-bearing fixture fails closed.</acceptance_criteria>
  <done>The observation selector exits zero, emits one schema-valid content-addressed observation for the agreement fixture, and rejects every empty, adjacent, drifted, ambiguous, and secret-bearing fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mooncakes public endpoints to observer | Remote metadata, indexes, archives, manifests, and assets are untrusted input. |
| Observer to checkpoint artifact | Only allowlisted canonical fields may cross into persisted evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Spoofing | public module identity | high | mitigate | Exact owner/module/version matching across independent structured surfaces and downloaded artifacts. |
| T-08-02-02 | Tampering | archive/manifest/metadata | high | mitigate | SHA-256 plus node-for-node graph and exact qualified metadata comparison; disagreement is terminal for the run. |
| T-08-02-03 | Information Disclosure | persisted observation | high | mitigate | Closed allowlist projection, secret-pattern rejection, raw-response disposal, credential-free execution. |
| T-08-02-04 | Denial of Service | propagation polling | medium | mitigate | Fixed cadence and attempt bound with deterministic timeout disposition. |
| T-08-02-05 | Repudiation | observation timing | low | accept | Content digest, source surface classifications, and start/end timestamps make later review possible; Phase 9 owns immutable closure. |
</threat_model>

<artifacts_this_phase_produces>
- Closed exact graph and metadata policy.
- Sanitized structured Mooncakes observation records.
- Bounded polling and disagreement evidence with no mutation authority.
</artifacts_this_phase_produces>

<verification>
Run schema-only and full fixture suites; prove endpoint/field discovery persists only sanitized closed projections.
</verification>

<success_criteria>
The observer can distinguish absent, exact, mismatch, and unknown without credentials, SPA scraping, automatic retry, or raw-response persistence.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-02-SUMMARY.md` when done.
</output>
