---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "30"
type: execute
wave: 30
depends_on: ["08-29"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Core mutation requires a revalidated r11 same-turn receipt or exact-existing authority; color follows verified core proof and image follows verified color proof (D-04 through D-07)."
    - "Each module uses a distinct journal-selected run, then exact structured metadata/identity and a fresh credential-free four-target registry consumer proof (D-05 through D-18)."
    - "Closure binds r11, attempt-zero+r1..r10, sanitized authorities/observations/graphs/behavior, and unchanged user-dirty baseline without persisting raw output or credentials."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "reciprocal DIST and PROV-05 evidence bound to r11"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "final r11 source audit and Phase 8 handoff map"
  key_links:
    - from: "predecessor exact cold proof"
      to: "successor mutation eligibility"
      via: "sanitized checkpoint digest"
      pattern: "predecessor_checkpoint_sha256"
---
<objective>Publish or safely recognize all three modules through r11 in order, prove actual cold registry consumption, and close Phase 8 evidence.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-29-SUMMARY.md
@policy/release-qualification.json
@policy/registry-authority.json
@release/qualification/phase-08-requirements.json
@scripts/quality/Invoke-Phase08HostedRun.ps1
</context>
<tasks>
<task type="auto">
  <name>Task 1: Revalidate r11 authority and complete core registry proof</name>
  <files>None (sanitized publisher, observation, and core-consumer artifacts under the r11 ArtifactRoot)</files>
  <action>Before and after every action, compare the exact six-path user-dirty status/content baseline from 08-29; stop on any drift or any non-baselined worktree path, and never stage/edit/delete/clean/reset/stash an allowed path. Revalidate r11 tag/source/eleven histories/prepared artifacts/packet/receipt/actor/current observation. Exact-existing core performs no mutation; confirmed absence with the same-turn r11 receipt may start exactly one protected publisher run scoped only to `mb-core`. No automatic retry is allowed. Then use bounded credential-free structured observation and require qualified metadata, index/archive/manifest/checksum equality. In a new external directory with a new empty exported `MOON_HOME`, pinned toolchain, and no workspace/source/path/Git/cache/credentials/target state, prove the exact one-node graph and deterministic public core behavior across js, wasm, wasm-gc, and real native. Persist only normalized sanitized evidence; any uncertainty/failure stops before color.</action>
  <verify><automated>pwsh -NoProfile -Command "$handoff=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r11-handoff.json'; $preauth=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r11-preauthorization.json'; if(Test-Path -LiteralPath $handoff -PathType Leaf){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $handoff; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $preauth -HandoffPath $handoff; exit $LASTEXITCODE"</automated></verify>
  <done>Core is exact, observed, and cold-proven on four targets with unchanged user dirties before color eligibility.</done>
</task>
<task type="auto">
  <name>Task 2: Publish or recognize color only after core proof</name>
  <files>None (sanitized publisher, observation, and color-consumer artifacts under the r11 ArtifactRoot)</files>
  <action>Recheck the unchanged six-path user baseline before/after every action. Require core’s exact authority, metadata, identity, one-node graph, behavior, and four-target checkpoint digest. Re-observe color credential-free: exact-existing performs no mutation; confirmed absence gets exactly one new journal-selected run scoped to `mb-color`, never a loop or retry. Require exact `mb-color@0.1.0` plus published `mb-core@0.1.0` graph, structured metadata, archive/manifest/checksum agreement, then a distinct new cold consumer root and empty Moon home proving public color behavior on all four pinned targets including real native. Stop before image on any mismatch, unknown, timeout, or baseline drift.</action>
  <verify><automated>pwsh -NoProfile -Command "$handoff=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r11-handoff.json'; $preauth=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r11-preauthorization.json'; if(Test-Path -LiteralPath $handoff -PathType Leaf){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $handoff; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $preauth -HandoffPath $handoff; exit $LASTEXITCODE"</automated></verify>
  <done>Color has an exact two-node registry and four-target cold-consumer checkpoint tied to verified core evidence and unchanged user dirties.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Complete image proof and reciprocal r11 closure</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Closure rejects missing r10 evidence, missing predecessor proof, raw output/credential persistence, or changed user-dirty baseline.
    - Valid image evidence contains exact full graph and bounded public PPM behavior on all four targets.
    - DIST-01..04 and PROV-05 map to sanitized r11 authority, metadata, identity, graph, toolchain, target, and behavior digests.
  </behavior>
  <action>Write closure tests first. Revalidate color proof and user baseline, then recognize exact image without mutation or publish confirmed-absent image in exactly one separate journal-selected run. Require structured metadata and exact archive/manifest/checksum, plus a new outside-checkout cold consumer with the full three-module 0.1.0 graph and bounded public PPM encode/decode on all targets including real native. Update reciprocal qualification and coverage to bind r11 tag/source, all eleven history values/set, authorities, observations, identity, graph, toolchain, targets, behavior, and unchanged baseline attestations. Do not persist raw response bodies or credential material. Phase 9 remains the immutable provenance-ledger owner.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Check</automated></verify>
  <done>Image has exact full-graph PPM proof and Phase 8 has complete r11-bound DIST/PROV-05 evidence with no user-dirty drift.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r11 receipt to core publisher | Only fresh same-turn authorization enables the first mutation. |
| registry data to successor | Untrusted public surfaces require closed projection and identity/graph agreement. |
| user dirties to release evidence | Baseline state must remain isolated from source/artifact identity. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-30-01 | Elevation of Privilege | core receipt | critical | mitigate | Revalidate receipt and permit one core-only run. |
| T-08-30-02 | Tampering | registry graph/identity | critical | mitigate | Structured metadata/index/archive/manifest/checksum/graph equality. |
| T-08-30-03 | Information Disclosure | consumers/evidence | critical | mitigate | New empty homes and sanitized evidence exclude secrets/raw responses. |
| T-08-30-04 | Tampering | user-dirty baseline | high | mitigate | Compare exact baseline before/after every action and task. |
| T-08-30-05 | Denial of Service | propagation | high | mitigate | Bounded polling; incident/stop rather than automatic retry. |
</threat_model>
<verification>For each module confirm one run, exact observation/identity, fresh cold consumer, graph equality, four targets, and unchanged baseline; run offline closure after image.</verification>
<success_criteria>All modules are newly published once or exact-recognized in order with r11-bound cold evidence; Phase 8 is ready for Phase 9 provenance closure.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-30-SUMMARY.md` when done.</output>
