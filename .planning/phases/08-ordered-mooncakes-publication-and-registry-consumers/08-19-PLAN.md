---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "19"
type: execute
wave: 19
depends_on: ["08-18"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Exact r7-capable committed HEAD is pushed before the r7 tag; remote main, annotated tag peel, LF clean clone, boundary locator, and source blobs bind one SHA (D-01, D-07)."
    - "GUID-unique ExecutionRoot and StateRoot reuse no r6 state, active attempt, locator, index, store, tag, prepared artifact, or fixed handoff."
    - "Attempt-zero and r1 through r6 plus their ordered-set digest bind fresh r7 preparation, optional packet/receipt, authority, and handoff."
    - "Exact-existing completes without preflight, dry run, packet, or receipt; absent requires a dynamic human-action checkpoint accepting only same-turn literal authorize-core or stop (D-04, D-14, D-15)."
    - "No branch in this plan calls PublishOne or mutates Mooncakes."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R7PreLive.ps1"
      provides: "Mandatory zero-write gate before every live side effect"
    - path: "refs/tags/modules-v0.1.0-r7"
      provides: "Annotated r7 immutable boundary"
    - path: "%TEMP%/mnf-phase08-r7-handoff.json"
      provides: "Eligible seven-history r7 AuthorityUnion"
  key_links:
    - from: "attempt-zero plus r1 through r6 histories"
      to: "r7 AuthorityUnion"
      via: "seven individual digests and canonical ordered-set digest"
      pattern: "historical_r6_sha256"
    - from: "dynamic absent checkpoint"
      to: "authorization receipt"
      via: "same-turn literal followed by complete fresh reload and revalidation"
      pattern: "authorization_receipt_sha256"
---

<objective>Establish the LF-clean r7 boundary and select a non-mutating seven-history AuthorityUnion.

Purpose: Cross the corrected hosted boundary after r6's prepare failure while preserving the explicit first-mutation checkpoint.
Output: r7 tag, clean clone, unique state, exclusive authority, optional literal receipt, and fixed handoff; no PublishOne.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-prepared-zip.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-18-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute the zero-write r7 gate before every live side effect</name>
  <files>None (read-only local, GitHub, registry, ref, run, and temporary-state checks)</files>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, before any push, tag, root creation, dispatch, secret access, registry action, or handoff write, execute `scripts/quality/Invoke-Phase08R7PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin` from committed HEAD. Require its single closed sanitized stdout object to prove: rootless attempt-zero plus persisted r1-r6 refs/peels/digests/set; exact r6 source `c05cacbc3cfc583205c612f4bf293a4e251ec079`, tag object `cdff825cc870a50c0393d5347f21351011092149`, run `29671691604/1`, prepare job `88151792308`, `P08-PREPARED-INTENT-BINDING`, cross-platform prepared mismatch, and all downstream effects zero; immutable historical roots/locators/index/store paths and digests; committed-clean 08-17/18 ownership and ancestor summaries; and r7 tag/fixed handoff absence. Recompute the selector result digest in memory and stop on nonzero exit, extra output, missing field, disagreement, ambiguity, or any write. Repeat this complete selector in the same executor turn before the first side effect of any resumed Task 2.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R7PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin</automated></verify>
  <done>The executable zero-write selector passes completely; only then may Task 2 perform push/tag/root/dispatch actions.</done>
</task>

<task type="auto">
  <name>Task 2: Create the LF-clean r7 boundary and eligible dynamic AuthorityUnion</name>
  <files>None (exact push/tag, unique state, sanitized artifacts, optional receipt, and fixed handoff)</files>
  <action>Only after a same-turn successful Task 1, per D-01 through D-07 and D-13 through D-19, push exact committed r7 implementation/planning HEAD to origin/main and require local/remote equality. Create GUID-owned nonoverlapping ExecutionRoot and StateRoot; clone with `git -c core.autocrlf=false clone --no-tags`, detach exact remote SHA, verify committed `.gitattributes`, LF source bytes, clean tracked blobs, no tags, empty state, and no historical path overlap. Initialize the canonical UTC boundary. Create and push one annotated `modules-v0.1.0-r7` tag without force, verify tag object and peel, then PrepareAttempt as initial 0.1.0, sequence zero, root=current, no predecessor, with all seven histories/set and fresh root/genesis/prepared/index/store. Require local prepared identity to agree with hosted Linux requalification before any authority branch.

Run fresh credential-free ObserveOnly for core. If exact, run structured public observation and outside-checkout empty-MOON_HOME four-target/native cold proof, then create packet-and-receipt-free ExactExistingAuthority and first-write the fixed handoff. If absent, run exactly one HostedPreflight and one PublisherDryRun; require clean equal-empty snapshots, unique env keys, exact14 parity, actor `tchivs`, dry success, credential removal, fresh absence, and zero PublishOne. Generate the sanitized mutation packet, then emit a dynamic `checkpoint:human-action` that autonomous mode, auto-advance, defaults, agents, prior login, prior push permission, and general consent cannot approve. Display the full recovery binding: ExecutionRoot, StateRoot, boundary/active locator paths and file/digests, ArtifactRoot, PacketPath/Sha256, release/boundary/source SHA, all seven individual history digests and set, prepared/root/intent digests, HostedPreflight and PublisherDryRun ids/attempts, actor, disposition, absence digest/timestamp, and zero mutation confirmation. Accept only verbatim case-sensitive `authorize-core` or `stop` from the user in that same checkpoint turn. `stop` writes no receipt/handoff. On `authorize-core`, freshly reload and revalidate every displayed field, remote ref, LF checkout, seven histories/set, hosted jobs/artifacts, actor, dry-run, absence freshness, packet, locators/index/store, and zero mutation; any stale/missing/drift/ambiguity stops. Only then create the reciprocal literal authorization receipt, mutation AuthorityUnion, and fixed handoff. Never call PublishOne in this plan.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r7-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -Mode AuthorityUnion -HandoffPath "$env:TEMP/mnf-phase08-r7-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>One eligible seven-history r7 union exists: exact-existing has no packet/receipt, absent has a same-turn literal receipt, and publication count remains zero.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Committed HEAD to remote/tag/clone | All source identities and LF bytes must bind one immutable SHA. |
| Public observation to branch choice | Only exact or confirmed-absent may select authority; ambiguity stops. |
| Human literal to receipt | Same-turn input is untrusted until the complete packet and live state are reloaded. |
| Dry-run authority to registry mutation | This plan must remain non-mutating and cannot call PublishOne. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-19-01 | Tampering | source/tag/clone | critical | mitigate | Exact SHA/peel/blob/LF equality and non-force tag creation. |
| T-08-19-02 | Spoofing | r6 history | critical | mitigate | Remote refs plus exact run/job/error and seven digest/set validation. |
| T-08-19-03 | Elevation of Privilege | dynamic authorization | critical | mitigate | Same-turn literal plus complete fresh recovery revalidation. |
| T-08-19-04 | Tampering | packet/receipt/handoff | critical | mitigate | Reciprocal digests, first-write semantics, and exclusive union. |
| T-08-19-05 | Information Disclosure | actor/credential evidence | high | mitigate | Ephemeral MOON_HOME and closed sanitized projections only. |
| T-08-19-06 | Repudiation | no-mutation boundary | high | mitigate | Explicit zero counts and no PublishOne path in plan verification. |
</threat_model>

<verification>Validate seven histories, exact r6 terminal failure, r7 LF boundary, unique state, cross-host prepared agreement, complete dynamic checkpoint, same-turn recovery, exclusive union, fixed handoff, and zero PublishOne.</verification>
<success_criteria>08-19 ends only with a valid r7 exact-existing union or a literal-receipt mutation union ready for 08-20; it performs no registry mutation.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-19-SUMMARY.md` when done.</output>
