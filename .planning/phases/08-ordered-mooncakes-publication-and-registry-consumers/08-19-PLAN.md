---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "19"
type: execute
wave: 19
depends_on: ["08-18"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "r8 is the sole current initial forward retry with module 0.1.0, sequence 0, root=current, and no predecessor (D-01, D-07, D-15)."
    - "Attempt-zero and r1 through r7 are eight distinct immutable terminal-negative histories with individual digests and one canonical ordered-set digest."
    - "r7 binds source 195e08dc1f3a1dc561d98cc660af679926ae0198, annotated tag object 52a47cda33492fa490178ab195ecdca50b1cf382, run 29673849108/1, failed prepare job 88157456895, and P08-PREPARED-INTENT-BINDING caused by raw moon ZIP container differences across operating systems despite LF payloads."
    - "Every r7 downstream publisher, dry-run, exact-existing, observation, hosted-preflight, cold-consumer, packet, receipt, handoff, PublishOne, mutation, and successor effect is zero."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r8 current profile and eight exact histories"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r8 eight-history AuthorityUnion handoff"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r8 root/genesis/prepared/index/store identity"
  key_links:
    - from: "attempt-zero plus r1 through r7 histories"
      to: "r8 eligibility artifacts"
      via: "eight individual SHA-256 digests plus canonical LF-ordered-set SHA-256"
      pattern: "historical_r7_sha256"
---

<objective>Define and test the r8 initial retry and eight immutable terminal-negative histories.

Purpose: Recover forward from r7's cross-OS raw ZIP-container intent mismatch without moving r7, reusing its state, or entering the correction lane.
Output: Closed r8 policy, intent, prepared, authority, receipt, handoff, and qualification contracts.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-intent-components.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-18-SUMMARY.md
@policy/release-control.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the initial attempt family to r8 and preserve eight histories</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r8 is current initial; 0.1.0, root=current, sequence zero, and no-predecessor invariants remain.
    - Exact order is attempt_zero,r1,r2,r3,r4,r5,r6,r7 and each history has distinct terminal facts and digest.
    - r7 binds exact source/tag/run/job/error, raw-container mismatch, and all downstream effects as zero.
  </behavior>
  <action>Per D-01, D-07, D-14, and D-15, advance only current initial from r7 to r8. Preserve attempt-zero through r6 byte-for-byte and add r7 as a terminal prepare-job failure: source `195e08dc1f3a1dc561d98cc660af679926ae0198`, annotated tag object `52a47cda33492fa490178ab195ecdca50b1cf382`, run `29673849108/1`, prepare job `88157456895`, failure `P08-PREPARED-INTENT-BINDING`, and the debug-proven cross-OS raw moon ZIP-container divergence despite LF-stable entry payloads. Require all downstream jobs/effects skipped or zero and forbid uploaded prepared authority, packet, receipt, handoff, PublishOne, mutation, or successor. Define eight individual digests and one canonical ordered-set digest; reject r7-current, replacement, duplication, reorder, set drift, predecessor/sequence drift, state reuse, and correction substitution.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r8 alone is current and eight immutable histories include the exact r7 hosted prepare failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind eight histories into authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Authority, receipt, and handoff require eight individual history digests and their matching set.
    - Mutation authority requires packet plus same-turn literal receipt; exact-existing forbids both; stop is ineligible.
    - Fake r7 success, history substitution/reorder/set drift, stale UTC, and cross-attempt path reuse fail closed.
  </behavior>
  <action>Per D-04, D-07, D-14, and D-15, advance closed authority, receipt, and handoff schemas to r8 and eight histories. Bind r7's exact failed prepare job without allowing fabricated downstream artifact or authority. Make the receipt bind packet, r8 ref/boundary/source, literal `authorize-core`, canonical UTC evidence, every history/set, and reciprocal digest. Fix production handoff at `%TEMP%/mnf-phase08-r8-handoff.json`, bind every path/file/digest, and admit exactly one AuthorityUnion variant. Add exact-existing, absent-receipt, stop, fake-r7-success, stale-history, reordered-set, old-root, and old-handoff adversarial fixtures.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$p=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($f in $p){$null=Get-Content -LiteralPath $f -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All live schemas require r8 plus eight histories and preserve receipt-or-exact exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fresh canonical r8 prepared, index, store, and qualification identity</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r8 root/genesis/prepared/index/store bind eight histories and reject every r7 path, locator, store, prepared artifact, tag, or handoff.
    - Archive identity uses canonical ZIP bytes rather than raw moon container bytes before any digest enters intent or prepared evidence.
    - Snapshot, UTC, LF, no-tags, and duplicate workflow-key protections remain deterministic.
  </behavior>
  <action>Per D-01, D-07, D-14, D-15, and D-18, extend canonical qualification and prepared generation to r8 with eight histories. Require fresh state and prevent r7 StateRoot, locator, index, store, active attempt, raw prepared archive, or handoff reuse. Carry r7 run/job/error/downstream-zero facts as history only. Require qualified archives to use the deterministic canonical container described by the debug artifact before hashing and preserve entry order/path/payload identity. Add positive r8 and adversarial r7-current, raw-container, old-root/index/store, fake upload/successor, missing/reordered history, set mismatch, correction-lane, UTC, LF, no-tags, equal-empty, and nonempty-drift cases.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r8 eligibility is eight-history-bound and cannot inherit r7 raw-container bytes or state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Eight prior attempts to r8 | Histories are immutable evidence only and cannot grant current authority. |
| Raw moon ZIP to canonical archive | Host-dependent container metadata cannot enter release identity. |
| r7 state to r8 preparation | No locator, store, tag, handoff, prepared archive, or StateRoot reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-19-01 | Tampering | attempt family | critical | mitigate | Exact eight facts/digests/set and substitution/reorder tests. |
| T-08-19-02 | Spoofing | r7 downstream success | critical | mitigate | Closed failed-job shape and fabricated-success rejection. |
| T-08-19-03 | Tampering | ZIP release identity | critical | mitigate | Canonical bytes precede all archive digest binding. |
| T-08-19-04 | Elevation of Privilege | receipt/handoff | critical | mitigate | Exclusive r8 union bound to eight histories. |
| T-08-19-05 | Repudiation | r7 failure | high | mitigate | Exact source/tag/run/job/error/downstream-zero facts are digest-bound. |
</threat_model>

<verification>Run intent, prepared, and qualification suites; validate r8, eight histories, exact r7 failure/downstream-zero record, canonical archive identity, fresh state, and zero live effects. Accepted scope warning: this contract plan names 13 files because policy/intent/prepared, authority/receipt/handoff, and qualification composition must advance atomically; its three tasks remain bounded to 5, 4, and 5 files.</verification>
<success_criteria>r8 and eight terminal histories are closed tested contracts ready for hosted seam integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-19-SUMMARY.md` when done.</output>
