---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "08"
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Core, color, and image each resolve through a closed absent/exact/mismatch/unknown outcome switch; exact-existing never republishes and carries the same cold-proof rigor as published-now (D-05 through D-15)."
    - "Core entry accepts the exclusive Phase 07 union: validated ExactExistingAuthority or literally authorized MutationAuthorizationPacket. Color consumes the unique core authority and image consumes the unique color authority; every ObserveOnly and exact branch passes PriorAuthorityRecordPath explicitly (D-04 through D-07)."
    - "Published-now and exact-existing records normalize into one successor authority schema with a source discriminator and identical ref/boundary/root/current/prepared/observation/archive/manifest/checksum/docs/cold/native/toolchain/reducer/locator/root/index/predecessor bindings."
    - "Every hosted helper, observation, cold consumer, and qualification selector is invoked by absolute path beneath the persisted clean execution root after immediate boundary/blob validation."
    - "Every CoreColorArtifacts, LiveArtifacts, and ReciprocalArtifacts call supplies LocatorPath, ArtifactRoot, the applicable authority-record paths, and the packet path only when the core-entry basis is MutationAuthorizationPacket."
    - "The final tracked reciprocal record binds all five requirements to sanitized exact evidence while Required stays credential-free, offline, and non-mutating (D-08 through D-18)."
  artifacts:
    - path: "<artifact-root>/checkpoints/mb-core-*.json"
      provides: "Published-now or exact-existing core authority with exact observation/cold proof"
    - path: "<artifact-root>/checkpoints/mb-color-*.json"
      provides: "Published-now or exact-existing color authority with exact predecessor proof"
    - path: "<artifact-root>/checkpoints/mb-image-*.json"
      provides: "Published-now or exact-existing image authority with complete graph proof"
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Closed reciprocal DIST-01..04 and PROV-05 evidence"
  key_links:
    - from: "08-07 core-entry authority union"
      to: "mb-core outcome switch"
      via: "validated exact-existing authority or literal-authorized mutation packet"
      pattern: "ExactExistingAuthorityPath|MutationAuthorizationPacketPath"
    - from: "verified core/color checkpoint"
      to: "successor eligibility"
      via: "authority_kind plus exact observation/cold-proof and reducer record digests"
      pattern: "exact_existing|published_now"
    - from: "unique predecessor authority"
      to: "all live selectors"
      via: "clone-rooted ObserveOnly/PublishOne and selectors with explicit PriorAuthorityRecordPath"
      pattern: "PriorAuthorityRecordPath"
---

<objective>
Complete ordered publication or exact-existing recovery for all three modules, prove cold registry consumption and public metadata, and bind reciprocal tracked closure.

Purpose: Deliver genuine Mooncakes usability without ambiguity-driven republish or ambient-source execution.
Output: Three exact module checkpoints, one-/two-/three-node cold proofs, structured metadata evidence, and credential-free reciprocal requirement closure.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-07-SUMMARY.md
@policy/phase-08-distribution.json
@release/qualification/phase-08-authority-schema.json
@release/qualification/phase-07-requirements.json

<interfaces>
Reload `<system-temp>/mnf-phase08/boundary-locator.json` and `active-attempt.json` in every task. `$ExecutionRoot` comes only from the boundary locator. The active attempt carries exactly one canonical core-entry path: `$MutationAuthorizationPacketPath` or `$ExactExistingAuthorityPath`. Every invocation uses `&amp; (Join-Path $ExecutionRoot 'scripts/quality/...')`; no `./scripts`, caller-memory path, direct `gh workflow run`, or rerun command is permitted.
Closed per-module outcome switch: absent -> one PublishOne only when the required mutation packet/unique predecessor authority is valid; exact -> verified no-republish checkpoint plus cold proof with mutation_count 0 and no publish/dry-run authorization; mismatch -> terminal mismatch and forward_correction_required, then stop; unknown/disagreement/timeout -> terminal stopped evidence, then stop. All exact-existing and published-now results are validated against `release/qualification/phase-08-authority-schema.json` and normalize to the same successor fields with only `authority_source` differing. Core exact authority has no packet. Color and image always bind `prior_authority_record_sha256` to the unique immediately preceding authority and pass its path on ObserveOnly, materialization/indexing, authority selection, and PublishOne.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve core and color through the closed outcome switch</name>
  <files>None (hosted runs and sanitized content-addressed artifacts only)</files>
  <action>Per D-03 through D-15, validate the exclusive Phase 07 authority union first. When it selects ExactExistingAuthority, require `Core entry authority: exact-existing` and no operator-decision line, validate the exact core record, and use it directly as `$CoreAuthorityPath`; do not observe core again and do not require a packet or authorize-core. When it selects MutationAuthorizationPacket, require `Core entry authority: mutation-packet` plus exactly one `Operator decision: authorize-core`, validate the unchanged packet, then resolve core through the closed switch. Validate the resulting normalized core authority before color. For color, pass `-PriorAuthorityRecordPath $CoreAuthorityPath` to ObserveOnly and to every exact/published-now authority-producing mode. Run the explicit production surface/observer/cold template from 08-06 Task 3 for both exact-existing and post-publish outcomes; supply the complete `-FixturePath/-OutputPath/-Module/-PolicyPath/-SchemaPath` observer parameters and `-Module/-OutputPath/-ToolchainRoot/-ObservationFixturePath/[-NativeToolchainBin]/-PolicyPath/-SchemaPath` cold parameters, then schema/digest/index both outputs before selecting authority. No helper may perform those commands implicitly. After both authorities exist, run CoreColorArtifacts with both authority paths and the optional packet path.

Command contract skeleton:

    $Boundary=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100
    $Active=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/active-attempt.json') -Raw|ConvertFrom-Json -Depth 100
    $ExecutionRoot=[IO.Path]::GetFullPath([string]$Boundary.execution_root)
    $MutationAuthorizationPacketPath=[string]$Active.mutation_authorization_packet_path
    $ExactExistingAuthorityPath=[string]$Active.exact_existing_authority_path
    $Hosted=Join-Path $ExecutionRoot 'scripts/quality/Invoke-Phase08HostedRun.ps1'; $Selector=Join-Path $ExecutionRoot 'scripts/quality/Test-Phase08Qualification.ps1'
    if((git -C $ExecutionRoot rev-parse HEAD).Trim() -cne [string]$Boundary.boundary_sha -or @(git -C $ExecutionRoot status --porcelain=v1 --untracked-files=all).Count -ne 0){throw 'P08-EXECUTION-BOUNDARY'}
    &amp; $Selector -AuthorityUnion -LocatorPath ([string]$Active.locator_path) -ArtifactRoot ([string]$Active.artifact_root) -MutationAuthorizationPacketPath $MutationAuthorizationPacketPath -ExactExistingAuthorityPath $ExactExistingAuthorityPath
    if($LASTEXITCODE){exit $LASTEXITCODE}
    $Common=@{BoundaryLocatorPath=[string]$Boundary.locator_path;LocatorPath=[string]$Active.locator_path;ArtifactRoot=[string]$Active.artifact_root;ReleaseRef='refs/tags/modules-v0.1.0-r1';SourceSha=[string]$Boundary.boundary_sha;RootIntentSha256=[string]$Active.root_intent_sha256;IntentSha256=[string]$Active.intent_sha256;PreparedManifestSha256=[string]$Active.prepared_manifest_sha256}
    $RunPublicProof={param($Module,$ObservationPhase,$PriorAuthorityPath)
      $Surface=&amp; $Hosted -Mode MaterializePublicSurface @Common -TargetModule $Module -ObservationPhase $ObservationPhase -PriorAuthorityRecordPath $PriorAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}
      $FixturePath=[IO.Path]::GetFullPath([string]$Surface.fixture_path); $PolicyPath=Join-Path $ExecutionRoot 'policy/phase-08-distribution.json'; $ObservationSchemaPath=Join-Path $ExecutionRoot 'release/registry/module-observation-schema.json'; $ColdSchemaPath=Join-Path $ExecutionRoot 'release/consumers/proof-schema.json'
      $ObservationPath=[IO.Path]::GetFullPath((Join-Path ([string]$Active.artifact_root) ('observations/'+$Module+'/'+$ObservationPhase+'/observation.json')))
      &amp; (Join-Path $ExecutionRoot 'scripts/quality/Get-MooncakesObservation.ps1') -FixturePath $FixturePath -OutputPath $ObservationPath -Module $Module -PolicyPath $PolicyPath -SchemaPath $ObservationSchemaPath; if($LASTEXITCODE){exit $LASTEXITCODE}
      $ObservationJson=Get-Content -LiteralPath $ObservationPath -Raw; if(-not ($ObservationJson|Test-Json -SchemaFile $ObservationSchemaPath -ErrorAction Stop)){throw 'P08-OBSERVATION-SCHEMA'}; $ObservationRecord=$ObservationJson|ConvertFrom-Json -Depth 100; $ObservationDigest=(Get-FileHash -LiteralPath $ObservationPath -Algorithm SHA256).Hash.ToLowerInvariant()
      &amp; $Hosted -Mode IndexSanitizedArtifact @Common -TargetModule $Module -ArtifactKind Observation -ArtifactPath $ObservationPath -ArtifactFileSha256 $ObservationDigest -ArtifactContentSha256 ([string]$ObservationRecord.content_sha256) -PriorAuthorityRecordPath $PriorAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}
      $ColdPath=[IO.Path]::GetFullPath((Join-Path ([string]$Active.artifact_root) ('cold/'+$Module+'/'+$ObservationPhase+'/proof.json'))); $ColdArgs=@{Module=$Module;OutputPath=$ColdPath;ToolchainRoot=[string]$Active.toolchain_root;ObservationFixturePath=$FixturePath;PolicyPath=$PolicyPath;SchemaPath=$ColdSchemaPath}; if(-not [string]::IsNullOrWhiteSpace([string]$Active.native_toolchain_bin)){$ColdArgs.NativeToolchainBin=[string]$Active.native_toolchain_bin}
      &amp; (Join-Path $ExecutionRoot 'scripts/quality/Invoke-ColdRegistryConsumer.ps1') @ColdArgs; if($LASTEXITCODE){exit $LASTEXITCODE}
      $ColdJson=Get-Content -LiteralPath $ColdPath -Raw; if(-not ($ColdJson|Test-Json -SchemaFile $ColdSchemaPath -ErrorAction Stop)){throw 'P08-COLD-SCHEMA'}; $ColdRecord=$ColdJson|ConvertFrom-Json -Depth 100; if((@($ColdRecord.targets.name)-join ',') -cne 'js,wasm,wasm-gc,native' -or [string]$ColdRecord.targets[3].runtime -cne 'pass'){throw 'P08-COLD-NATIVE'}; $ColdDigest=(Get-FileHash -LiteralPath $ColdPath -Algorithm SHA256).Hash.ToLowerInvariant()
      &amp; $Hosted -Mode IndexSanitizedArtifact @Common -TargetModule $Module -ArtifactKind ColdProof -ArtifactPath $ColdPath -ArtifactFileSha256 $ColdDigest -ArtifactContentSha256 ([string]$ColdRecord.content_sha256) -ObservationRecordPath $ObservationPath -PriorAuthorityRecordPath $PriorAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}
      [pscustomobject]@{observation_path=$ObservationPath;cold_proof_path=$ColdPath}
    }
    if(-not [string]::IsNullOrWhiteSpace($ExactExistingAuthorityPath)){
      $CoreAuthorityPath=[IO.Path]::GetFullPath($ExactExistingAuthorityPath)
      &amp; $Selector -ExactExistingAuthority -LocatorPath ([string]$Active.locator_path) -ArtifactRoot ([string]$Active.artifact_root) -ExactExistingAuthorityPath $CoreAuthorityPath
      if($LASTEXITCODE){exit $LASTEXITCODE}
    } else {
      $Before=&amp; $Hosted -Mode ObserveOnly @Common -TargetModule mb-core -MutationAuthorizationPacketPath $MutationAuthorizationPacketPath -PriorAuthorityRecordPath ''
      if($LASTEXITCODE){exit $LASTEXITCODE}
      switch -CaseSensitive ([string]$Before.outcome) {
        'absent' { $Mutation=&amp; $Hosted -Mode PublishOne @Common -TargetModule mb-core -AuthorizationPacketPath $MutationAuthorizationPacketPath -PreMutationObservationPath ([string]$Before.artifact_path) -PriorAuthorityRecordPath ''; if($LASTEXITCODE){exit $LASTEXITCODE}; $Proof=&amp; $RunPublicProof mb-core post-publish ''; $CoreAuthority=&amp; $Hosted -Mode SelectPublishedNowAuthority @Common -TargetModule mb-core -MutationRecordPath ([string]$Mutation.record_path) -ObservationRecordPath ([string]$Proof.observation_path) -ColdProofPath ([string]$Proof.cold_proof_path) -PriorAuthorityRecordPath ''; if($LASTEXITCODE){exit $LASTEXITCODE}; $CoreAuthorityPath=[string]$CoreAuthority.authority_path }
        'exact' { $Proof=&amp; $RunPublicProof mb-core exact-existing ''; $CoreExact=&amp; $Hosted -Mode SelectExactExistingAuthority @Common -TargetModule mb-core -ObservationRecordPath ([string]$Proof.observation_path) -ColdProofPath ([string]$Proof.cold_proof_path) -PriorAuthorityRecordPath ''; if($LASTEXITCODE){exit $LASTEXITCODE}; $CoreAuthorityPath=[string]$CoreExact.authority_path }
        'mismatch' { throw 'P08-mb-core-FORWARD-CORRECTION-REQUIRED' }
        'unknown' { throw 'P08-mb-core-UNKNOWN-STOP' }
        'disagreement' { throw 'P08-mb-core-DISAGREEMENT-STOP' }
        'timeout' { throw 'P08-mb-core-TIMEOUT-STOP' }
        default { throw 'P08-mb-core-UNRECOGNIZED-STOP' }
      }
    }
    $ColorBefore=&amp; $Hosted -Mode ObserveOnly @Common -TargetModule mb-color -MutationAuthorizationPacketPath $MutationAuthorizationPacketPath -PriorAuthorityRecordPath $CoreAuthorityPath
    if($LASTEXITCODE){exit $LASTEXITCODE}
    switch -CaseSensitive ([string]$ColorBefore.outcome) {
      'absent' { $Mutation=&amp; $Hosted -Mode PublishOne @Common -TargetModule mb-color -PreMutationObservationPath ([string]$ColorBefore.artifact_path) -PriorAuthorityRecordPath $CoreAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}; $Proof=&amp; $RunPublicProof mb-color post-publish $CoreAuthorityPath; $ColorAuthority=&amp; $Hosted -Mode SelectPublishedNowAuthority @Common -TargetModule mb-color -MutationRecordPath ([string]$Mutation.record_path) -ObservationRecordPath ([string]$Proof.observation_path) -ColdProofPath ([string]$Proof.cold_proof_path) -PriorAuthorityRecordPath $CoreAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}; $ColorAuthorityPath=[string]$ColorAuthority.authority_path }
      'exact' { $Proof=&amp; $RunPublicProof mb-color exact-existing $CoreAuthorityPath; $ColorExact=&amp; $Hosted -Mode SelectExactExistingAuthority @Common -TargetModule mb-color -ObservationRecordPath ([string]$Proof.observation_path) -ColdProofPath ([string]$Proof.cold_proof_path) -PriorAuthorityRecordPath $CoreAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}; $ColorAuthorityPath=[string]$ColorExact.authority_path }
      'mismatch' { throw 'P08-mb-color-FORWARD-CORRECTION-REQUIRED' }
      'unknown' { throw 'P08-mb-color-UNKNOWN-STOP' }
      'disagreement' { throw 'P08-mb-color-DISAGREEMENT-STOP' }
      'timeout' { throw 'P08-mb-color-TIMEOUT-STOP' }
      default { throw 'P08-mb-color-UNRECOGNIZED-STOP' }
    }
    &amp; $Selector -CoreColorArtifacts -LocatorPath ([string]$Active.locator_path) -ArtifactRoot ([string]$Active.artifact_root) -MutationAuthorizationPacketPath $MutationAuthorizationPacketPath -CoreAuthorityRecordPath $CoreAuthorityPath -ColorAuthorityRecordPath $ColorAuthorityPath

The shared command block is used for both exact-existing and post-publish and passes `-PriorAuthorityRecordPath $CoreAuthorityPath` for color. Do not replace the switch with truthiness, hidden observer/cold execution, or retry logic.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $b=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100; $a=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/active-attempt.json') -Raw|ConvertFrom-Json -Depth 100; $x=[IO.Path]::GetFullPath([string]$b.execution_root); &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -CoreColorArtifacts -LocatorPath ([string]$a.locator_path) -ArtifactRoot ([string]$a.artifact_root) -MutationAuthorizationPacketPath ([string]$a.mutation_authorization_packet_path) -CoreAuthorityRecordPath ([string]$a.core_authority_record_path) -ColorAuthorityRecordPath ([string]$a.color_authority_record_path) }"</automated></verify>
  <done>Core and color each have exactly one published-now or exact-existing authority, exact cold proofs, and reducer-valid ordering; ambiguity caused no mutation or successor.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove the complete public graph</name>
  <files>None (hosted run and sanitized content-addressed artifacts only)</files>
  <action>Per D-05 through D-18, reload and validate the same boundary, active locator, artifact root, exclusive core-entry authority union, and unique indexed color authority. Invoke clone-rooted CoreColorArtifacts with LocatorPath, ArtifactRoot, MutationAuthorizationPacketPath when present, CoreAuthorityRecordPath, and ColorAuthorityRecordPath before image eligibility. Recreate the Task 1 `$RunPublicProof` scriptblock verbatim so its clone-rooted observer call supplies `-FixturePath/-OutputPath/-Module/-PolicyPath/-SchemaPath`, its cold call supplies `-Module/-OutputPath/-ToolchainRoot/-ObservationFixturePath/[-NativeToolchainBin]/-PolicyPath/-SchemaPath`, and both outputs are schema/digest/four-target/native validated and indexed. Pass `-PriorAuthorityRecordPath $ColorAuthorityPath` to ObserveOnly, MaterializePublicSurface, both index calls, SelectExactExistingAuthority/SelectPublishedNowAuthority, and PublishOne. Require exact three-node 0.1.0 graph, archive/downloaded-manifest identity, four real target checks/tests/runtimes, bounded PPM behavior, and exact structured public metadata. Every helper/selector call must revalidate ExecutionRoot HEAD/blob equality before use.

    $ImageBefore=&amp; $Hosted -Mode ObserveOnly @Common -TargetModule mb-image -MutationAuthorizationPacketPath $MutationAuthorizationPacketPath -PriorAuthorityRecordPath $ColorAuthorityPath
    if($LASTEXITCODE){exit $LASTEXITCODE}
    switch -CaseSensitive ([string]$ImageBefore.outcome) {
      'absent' { $Mutation=&amp; $Hosted -Mode PublishOne @Common -TargetModule mb-image -PreMutationObservationPath ([string]$ImageBefore.artifact_path) -PriorAuthorityRecordPath $ColorAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE}; $Proof=&amp; $RunPublicProof mb-image post-publish $ColorAuthorityPath; $ImageAuthority=&amp; $Hosted -Mode SelectPublishedNowAuthority @Common -TargetModule mb-image -MutationRecordPath ([string]$Mutation.record_path) -ObservationRecordPath ([string]$Proof.observation_path) -ColdProofPath ([string]$Proof.cold_proof_path) -PriorAuthorityRecordPath $ColorAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE} }
      'exact' { $Proof=&amp; $RunPublicProof mb-image exact-existing $ColorAuthorityPath; $ImageAuthority=&amp; $Hosted -Mode SelectExactExistingAuthority @Common -TargetModule mb-image -ObservationRecordPath ([string]$Proof.observation_path) -ColdProofPath ([string]$Proof.cold_proof_path) -PriorAuthorityRecordPath $ColorAuthorityPath; if($LASTEXITCODE){exit $LASTEXITCODE} }
      'mismatch' { throw 'P08-IMAGE-FORWARD-CORRECTION-REQUIRED' }
      'unknown' { throw 'P08-IMAGE-UNKNOWN-STOP' }
      'disagreement' { throw 'P08-IMAGE-DISAGREEMENT-STOP' }
      'timeout' { throw 'P08-IMAGE-TIMEOUT-STOP' }
      default { throw 'P08-IMAGE-UNRECOGNIZED-STOP' }
    }
    $ImageAuthorityPath=[string]$ImageAuthority.authority_path
    &amp; $Selector -LiveArtifacts -LocatorPath ([string]$Active.locator_path) -ArtifactRoot ([string]$Active.artifact_root) -MutationAuthorizationPacketPath $MutationAuthorizationPacketPath -CoreAuthorityRecordPath $CoreAuthorityPath -ColorAuthorityRecordPath $ColorAuthorityPath -ImageAuthorityRecordPath $ImageAuthorityPath
    if($LASTEXITCODE){exit $LASTEXITCODE}</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $b=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100; $a=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/active-attempt.json') -Raw|ConvertFrom-Json -Depth 100; $x=[IO.Path]::GetFullPath([string]$b.execution_root); &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -LiveArtifacts -LocatorPath ([string]$a.locator_path) -ArtifactRoot ([string]$a.artifact_root) -MutationAuthorizationPacketPath ([string]$a.mutation_authorization_packet_path) -CoreAuthorityRecordPath ([string]$a.core_authority_record_path) -ColorAuthorityRecordPath ([string]$a.color_authority_record_path) -ImageAuthorityRecordPath ([string]$a.image_authority_record_path) }"</automated></verify>
  <done>Image has one exact authority, the complete graph and bounded PPM behavior pass all four targets, metadata is exact, and no uncertainty branch mutated or retried.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal closure and preserve offline Required</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <read_first>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md, release/qualification/phase-07-requirements.json</read_first>
  <behavior>
    - Reciprocal live selector binds DIST-01 through DIST-04 and PROV-05 to the exclusive core-entry authority and exact ref/intent/journal/observation/archive/manifest/graph/toolchain/target/behavior/metadata digests; packet/actor/dry-run fields are required only for the mutation-packet source and forbidden for exact-existing core.
    - Each module authority records published_now or exact_existing and mutation count; exact_existing must be zero and published_now at most one.
    - Required validates only the tracked sanitized record/schema/digests and never reads the durable live locator, contacts Mooncakes/GitHub, accesses credentials, dispatches, tags, or publishes.
  </behavior>
  <action>First invoke the clone-rooted ReciprocalArtifacts selector with LocatorPath, ArtifactRoot, the optional MutationAuthorizationPacketPath, and all three explicit authority-record paths to create/validate the closed sanitized reciprocal projection. It must replay the authority union and reducer, prove each successor's prior_authority_record_sha256 equals the unique preceding record, and enforce identical normalized bindings for published-now and exact-existing sources. Copy only that validated projection into `release/qualification/phase-08-requirements.json`; never copy raw hosted/registry output. Extend the tracked selector/static Required integration so the repository record is closed, canonical, and digest-bound, while the live selector remains clone-rooted. Update COVERAGE.md to eight plans and mark 08-08 closure, all D-01..D-19 decisions, all five requirements, the authority union, both normalized authority sources, and the no-republish switch as covered. Keep Phase 9 ledger/provenance/milestone work excluded.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $b=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/boundary-locator.json') -Raw|ConvertFrom-Json -Depth 100; $a=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08/active-attempt.json') -Raw|ConvertFrom-Json -Depth 100; $x=[IO.Path]::GetFullPath([string]$b.execution_root); &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -ReciprocalArtifacts -LocatorPath ([string]$a.locator_path) -ArtifactRoot ([string]$a.artifact_root) -MutationAuthorizationPacketPath ([string]$a.mutation_authorization_packet_path) -CoreAuthorityRecordPath ([string]$a.core_authority_record_path) -ColorAuthorityRecordPath ([string]$a.color_authority_record_path) -ImageAuthorityRecordPath ([string]$a.image_authority_record_path); if($LASTEXITCODE){exit $LASTEXITCODE}; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if($LASTEXITCODE){exit $LASTEXITCODE}; git diff --check }"</automated></verify>
  <done>All five requirements have reciprocal exact evidence, both authority kinds are constrained correctly, COVERAGE is complete, and offline credential-free Required passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Literal packet authorization to core | One exact packet governs the first possible mutation. |
| Module authority to successor | Published-now and exact-existing checkpoints must carry identical exact observation/cold-proof rigor. |
| Registry/network output to sanitized evidence | Raw surfaces cannot become tracked or successor authority. |
| Live artifact store to tracked Required record | Only closed digest-bound reciprocal projection crosses into source control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08-01 | Elevation of Privilege | outcome/resume switch | critical | mitigate | Exhaustive six-branch switch; only absent calls PublishOne and exact-existing requires zero mutation plus cold proof. |
| T-08-08-02 | Tampering | successor authority | critical | mitigate | Packet/ref/intent/observation/cold-proof digest equality and reducer replay before each successor. |
| T-08-08-03 | Spoofing | ambient script or old attempt | critical | mitigate | Persisted clone-rooted absolute paths with immediate HEAD/blob validation and r1-only bindings. |
| T-08-08-04 | Information Disclosure | live/tracked evidence | high | mitigate | Closed sanitized projections, secret-shape rejection, and raw-output exclusion. |
| T-08-08-05 | Repudiation | publication chain | high | mitigate | Run/artifact/authority kind/mutation count/predecessor/packet digests in reciprocal evidence. |
| T-08-08-06 | Denial of Service | propagation ambiguity | medium | mitigate | Bounded observation and terminal stop without rerun, republish, or successor. |
</threat_model>

<verification>
Validate core/color/image authority and cold proofs, exact metadata, reciprocal evidence, mandatory packet parameters, clone-rooted command use, full Required, and absence of secret/raw/destructive operations.
</verification>

<success_criteria>
All three modules are published or exactly checkpointed without republish, independently cold-consumable in strict dependency order, and covered by exact credential-free tracked evidence.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-08-SUMMARY.md` when done.
</output>
