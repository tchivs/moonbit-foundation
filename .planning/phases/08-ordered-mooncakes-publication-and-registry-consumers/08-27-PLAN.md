---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "27"
type: execute
wave: 27
depends_on: ["08-26"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Only a revalidated r10 exact-existing authority or same-turn authorize-core receipt may permit one core mutation; every other state stops (D-04, D-07, D-14, D-15)."
    - "Core is observed and cold-proven before color; color is observed and cold-proven before image; each mutation uses a distinct journal-selected workflow run (D-05, D-06)."
    - "Each successful module has exact structured public metadata, normalized registry graph, downloaded identity evidence, and a no-credential four-target consumer proof (D-08 through D-18)."
    - "Closure binds r10, all ten historical records, sanitized authorities, registry observations, cold proofs, and PROV-05 without treating raw live output or the authorization secret as evidence."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "reciprocal DIST-01..DIST-04 and PROV-05 evidence bound to r10"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "final r10 source audit and Phase 8 evidence map"
  key_links:
    - from: "predecessor cold consumer proof"
      to: "successor mutation eligibility"
      via: "exact metadata/graph/archive/target/behavior checkpoint digest"
      pattern: "predecessor_checkpoint_sha256"
---

<objective>
Publish or safely recognize the three modules through r10 in dependency order, prove real registry consumption, and record reciprocal Phase 8 evidence.

Purpose: Satisfy the distribution and public-metadata outcome only after the separately authorized, non-publishing r10 qualification is complete.
Output: one authority/proof chain per module and a completed sanitized Phase 8 closure; no retry, overwrite, deletion, unpublish, or yank action.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-26-SUMMARY.md
@policy/release-qualification.json
@policy/registry-authority.json
@release/qualification/phase-08-requirements.json
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-ReleasePublisher.ps1

<interfaces>
From the r10 handoff: only `exact_existing` authority without packet/receipt, or `mutation_authorized` authority with the Task 2 same-turn receipt, can select core. Each downstream module requires its predecessor’s exact observation and cold-proof checkpoint digest.

From `policy/release-qualification.json`: module order is `mb-core`, `mb-color`, `mb-image`; every public proof uses the pinned toolchain and `js`, `wasm`, `wasm-gc`, and real `native` targets.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate r10 authority and complete core registry proof</name>
  <files>None (publisher artifacts and sanitized core observation/consumer evidence under the r10 ArtifactRoot)</files>
  <action>First re-run the r10 pre-live validation and validate the handoff/receipt against the displayed Task 2 authorization facts. Per D-04 through D-15 and D-19, stop if the r10 tag/source, ten histories, packet/receipt, prepared manifest, actor, current core observation, or zero prior-mutation state differs. For exact-existing core, perform no mutation and require an exact authority record. For confirmed absent core with the same-turn r10 receipt, start exactly one new protected publisher workflow run that may invoke PublishOne only for `mb-core`; wait for its sanitized journal record and never retry it automatically. After either exact path, perform bounded credential-free structured observations; require qualified metadata, index/archive/manifest/checksum identity, and no disagreement. In a brand-new directory outside the checkout with a new empty exported `MOON_HOME`, pinned `MOON_TOOLCHAIN_ROOT`, no workspace/source/path/Git/cache/credentials/target state, resolve the exact one-node graph and run the deterministic public core behavior on all four targets including real native execution. Persist only normalized sanitized evidence. Any nonzero, timeout, absent/mismatch/conflict/unknown observation, graph drift, or target failure creates incident evidence and stops before color.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r10-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Core has a matching exact registry authority, structured metadata proof, and isolated four-target cold consumer checkpoint before color becomes eligible.</done>
</task>

<task type="auto">
  <name>Task 2: Publish or recognize color only after core proof</name>
  <files>None (publisher artifacts and sanitized color observation/consumer evidence under the r10 ArtifactRoot)</files>
  <action>Require Task 1’s core checkpoint digest, exact registry observation, qualified metadata, exact one-node cold graph, all four target results, and no incident disposition before selecting color. Per D-05, D-06, D-08 through D-15, and D-17, re-observe color through credential-free structured surfaces. Exact-existing color receives no mutation; confirmed absent color receives exactly one new journal-selected publisher workflow run scoped to `mb-color`, never a loop or retry. Require its registry metadata and downloaded identity to match qualified sources and the normalized resolved graph to contain exactly `mb-color@0.1.0` and the intended published `mb-core@0.1.0`. In another brand-new cold consumer root with a distinct empty `MOON_HOME`, prove public color behavior over all four pinned targets including real native execution. Stop on every ambiguous/failing condition before image and retain only sanitized digest-bound checkpoint artifacts.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r10-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Color has exact registry and two-node cold-consumer proof tied to verified core evidence before image becomes eligible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete image full-graph proof and reciprocal r10 closure</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Closure rejects evidence that omits r9 from the ten-history set, lacks a predecessor checkpoint, treats a publish exit as proof, or contains raw live output/credential material.
    - A valid image checkpoint contains the exact full published graph and bounded public PPM encode/decode result across all four targets.
    - Reciprocal requirements evidence maps DIST-01 through DIST-04 and PROV-05 to sanitized r10 authority, metadata, archive, graph, toolchain, target, and behavior digests.
  </behavior>
  <action>Write the closure assertions before changing tracked qualification artifacts. Require the completed color checkpoint before re-observing image. Per D-05, D-06, D-08 through D-18, allow exact-existing image with no mutation or confirmed-absent image with one distinct journal-selected `mb-image` publisher run; never retry automatically. Require structured metadata plus exact archive/manifest/checksum and an isolated new cold consumer whose normalized graph is node-for-node equal to the intended three-module 0.1.0 graph and whose bounded public PPM encode/decode behavior passes on all four targets including real native execution. Then update reciprocal qualification and coverage evidence to bind the r10 tag/source, attempt-zero+r1..r9 values/set, authorities, observations, metadata projections, archive/manifest identities, graphs, toolchain, target results, and behavior sentinels. Treat raw response bodies and all credential material as prohibited persistence. Retain Phase 9 ownership of immutable provenance ledger and milestone audit; this task records only the Phase 8 handoff evidence required by DIST and PROV-05.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Check; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Image has an exact full-graph four-target PPM proof, and tracked Phase 8 closure maps every distribution and public-metadata requirement to sanitized r10 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| r10 receipt to core publisher | Only fresh, fully revalidated receipt can enable the first irreversible command. |
| Registry observation to successor eligibility | Public registry data is untrusted until closed projection and independent archive/graph comparison agree. |
| Publisher artifacts to cold consumer | Consumer proof must not inherit credentials, workspace sources, paths, Git fallback, or caches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-27-01 | Elevation of Privilege | core authorization | critical | mitigate | Revalidate same-turn r10 receipt and allow one core-scoped publisher run only. |
| T-08-27-02 | Tampering | registry identity/graph | critical | mitigate | Compare structured metadata, index, downloaded archive, manifest, checksum, and normalized graph before each successor. |
| T-08-27-03 | Information Disclosure | consumer/evidence | critical | mitigate | New empty Moon homes and sanitized closed artifacts exclude secrets and raw responses. |
| T-08-27-04 | Denial of Service | propagation failures | high | mitigate | Bounded polling; any timeout/unknown records incident evidence and halts rather than retrying. |
| T-08-27-05 | Repudiation | completion closure | high | mitigate | Reciprocal digest-bound evidence links authority, observations, consumer proof, and r10 history set. |
</threat_model>

<verification>
For each module, verify one-module journal selection, exact observation/identity, a fresh outside-checkout consumer, normalized graph equality, and all four target results. Run the tracked offline qualification closure after image. Confirm no source mutation, secret leakage, overwrite/delete/unpublish/yank action, or r9 reuse occurred.
</verification>

<success_criteria>
All three `tchivs/*@0.1.0` modules are either newly published once or independently recognized as exact, in dependency order, and each has sanitized metadata and cold registry-consumer proof. Phase 8 requirements have reciprocal r10 evidence ready for Phase 9 provenance closure.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-27-SUMMARY.md` when done.
</output>
