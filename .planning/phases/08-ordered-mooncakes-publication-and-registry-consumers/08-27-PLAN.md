---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "27"
type: execute
wave: 27
depends_on: ["08-26"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "r11 is the only current initial retry and binds attempt-zero+r1..r10 as eleven immutable terminal histories (D-01, D-07, D-15)."
    - "r10 remains a tagged terminal record with its exact object/peel, REL01-REF clean-clone failure, and documented zero downstream effects; no unlisted fact is inferred."
    - "Prepared, authority, receipt, and handoff contracts require eleven individual history digests plus their LF-ordered aggregate before the publisher boundary (D-01, D-14, D-18)."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r11 current-attempt and immutable r10 terminal policy"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "closed eleven-history r11 authority contract"
  key_links:
    - from: "r10 terminal record"
      to: "r11 prepared/authority artifacts"
      via: "individual r10 digest plus eleven-entry set digest"
      pattern: "historical_r10_sha256"
---
<objective>Advance credential-free static release contracts from r10 to the sole r11 forward retry.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-26-SUMMARY.md
@policy/release-control.json
@release/intent/schema.json
@release/prepared/schema.json
@scripts/quality/ReleaseQualification.Common.ps1
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Record r10 terminal evidence and close r11 schemas</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Valid r11 contracts contain attempt-zero then r1 through r10 in order, all individual digests, and the canonical aggregate.
    - r10 is bound to tag object 0546025 and peel d49edc5 with REL01-REF after clean-clone tag verification plus the asserted zero prepared/packet/receipt/handoff/hosted/dry/observation/PublishOne/registry/credential facts.
    - Missing, reordered, substituted, or altered r10 evidence and r10-as-current variants fail closed.
  </behavior>
  <action>Write RED assertions, then advance only the initial retry policy and closed schemas to `modules-v0.1.0-r11`. Per D-01, D-07, D-14, D-15, and D-18, create the r10 terminal record from the executor’s sanitized checkpoint and exact Git object/peel; record no guessed locator, active-attempt, or other unobserved field. Require eleven individual history values and their LF aggregate in intent, prepared, authority, receipt, and handoff artifacts. Preserve all prior records byte-for-byte and reject any compatibility path that makes fields generally optional.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1</automated></verify>
  <done>Policy and schemas make r11 unique, retain r10 only as bounded terminal evidence, and fail closed on any eleven-history drift.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind fresh r11 prepared qualification to the eleven-history family</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - A fresh r11 genesis prepares deterministic canonical bytes only with all eleven history values.
    - Any r10 root, locator, archive, index, store, tag, or handoff reused as r11 input fails before provider work.
    - The repaired actual PrepareAttempt fixture remains green with its clone-local current tag before any r11 tag creation.
  </behavior>
  <action>After RED, advance shared history serialization and prepared validation to r11 without duplicating serializers. Require a new r11 state/root and canonical prepared bytes bound to the r11 intent; carry r10 only as an immutable digest record. Keep the clean-clone tag-ref regression from 08-26 in the FixtureOnly suite and require it before r11 boundary work. This task is local and credential-free: it must not push, create any tag, dispatch, materialize a secret, call PublishOne, or contact the registry.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly</automated></verify>
  <done>Fresh r11 prepared qualification uses exactly eleven histories, rejects all r10 state reuse, and remains protected by the actual pre-tag PrepareAttempt fixture.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r10 checkpoint to r11 policy | Only attested r10 facts may enter immutable history. |
| policy/schema to prepared artifact | Any historical drift must stop before publisher access. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-27-01 | Tampering | r10 history | critical | mitigate | Bind object, peel, terminal facts, individual digest, and aggregate in tests. |
| T-08-27-02 | Spoofing | retry identity | critical | mitigate | Make r11 sole current and reject r10 root/path/ref reuse. |
| T-08-27-03 | Elevation of Privilege | prepared artifact | high | mitigate | Fresh roots and closed eleven-history schemas before publisher boundary. |
</threat_model>
<verification>Run focused intent, prepared, FixtureOnly, PowerShell parse, and diff checks; no external action is allowed.</verification>
<success_criteria>r11 is the only contractual retry and all preparation artifacts bind r10 as immutable terminal evidence plus the full eleven-history set.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-27-SUMMARY.md` when done.</output>
