---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "20"
type: execute
wave: 20
depends_on: ["08-19"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "The r7 handoff admits exactly one seven-history AuthorityUnion; mutation authority has packet+receipt and exact-existing has neither (D-04, D-07, D-15)."
    - "Core, color, and image resolve in strict order; each absent module gets at most one separate PublishOne run and each exact module gets zero mutation (D-05, D-06, D-14)."
    - "Every accepted module has structured public metadata, exact registry/archive/manifest/checksum identity, and a new credential-free cold four-target proof before its successor is eligible (D-08..D-18)."
    - "Tracked reciprocal closure binds r7, all seven histories/set, authorities, observations, graphs, behavior, target results, and sanitized digests without raw live output."
  artifacts:
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Reciprocal DIST-01..04 and PROV-05 r7 evidence"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "Offline required qualification and adversarial gates"
    - path: ".planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md"
      provides: "20-plan final capability and source coverage matrix"
  key_links:
    - from: "verified module authority"
      to: "successor eligibility"
      via: "exact observation plus cold four-target predecessor proof"
      pattern: "predecessor_checkpoint_sha256"
    - from: "sanitized live artifacts"
      to: "tracked reciprocal closure"
      via: "closed projection and bidirectional digest validation"
      pattern: "historical_r6_sha256"
---

<objective>Complete ordered r7 publication or exact recovery, cold registry proofs, structured metadata proof, and reciprocal Phase 8 closure.

Purpose: Turn the non-mutating r7 handoff into three dependency-ordered exact public authorities without automatic retry or republish.
Output: Core, color, and image authorities plus tracked offline DIST-01..04 and PROV-05 closure.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-19-SUMMARY.md
@release/qualification/phase-08-requirements.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate the r7 union and resolve core then color</name>
  <files>None (sanitized r7 live artifacts under the handoff ArtifactRoot)</files>
  <action>Per D-04 through D-19, load only `%TEMP%/mnf-phase08-r7-handoff.json` and validate its closed schema, every bound file/digest/path, clean LF detached execution root, unique state root, remote r7 source/tag identity, seven histories/set, prepared/root/current identity, and exclusive union. Mutation authority requires packet plus the literal receipt from 08-19; exact-existing forbids both. For core, exact-existing performs no mutation. Mutation authority must first re-observe: only fresh confirmed absent permits exactly one separate PublishOne hosted run; exact skips mutation; timeout, mismatch, unknown, conflicting surface, run ambiguity, nonzero exit, or stale/missing receipt stops without rerun. After publication or exact recovery, use bounded structured observation and a new outside-checkout empty-MOON_HOME credential-free consumer to prove exact one-node core graph, metadata/archive/downloaded-manifest/checksum/docs agreement, pinned toolchain, js/wasm/wasm-gc/native, and checked behavior. Index/select core authority. Require that exact authority as color predecessor, then apply the same branch switch to color: absent permits one separate PublishOne run, exact performs none, ambiguity stops. Prove exact color-to-core graph, metadata identity, four targets, public color behavior, then select CoreColorArtifacts.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode CoreColorArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r7-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>r7 core and color exact authorities and four-target cold proofs exist in strict predecessor order with no exact republish.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove the full exact public graph</name>
  <files>None (sanitized r7 live artifacts under the handoff ArtifactRoot)</files>
  <action>Per D-05 through D-18, reload fixed handoff, seven histories/set, and validated CoreColorArtifacts. Require color authority and its core predecessor before image eligibility. Re-observe image: absent permits exactly one new one-module PublishOne hosted run, exact-existing performs zero mutation, and mismatch/unknown/timeout/nonzero/conflicting/ambiguous results stop without rerun. Run a newly created outside-checkout consumer with empty exported MOON_HOME, explicit pinned MOON_TOOLCHAIN_ROOT, no credentials, workspace, path/Git dependency, copied source, registry cache, `.mooncakes`, or target state. Require exact `mb-image@0.1.0 -> mb-color@0.1.0 -> mb-core@0.1.0` normalized graph; structured API/index/archive/downloaded manifests/checksums/assets/docs agreement; js, wasm, wasm-gc, and real native results; and bounded PPM encode/decode behavior. Index, select, and validate LiveArtifacts before any closure write.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode LiveArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r7-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>Image exact authority, full three-node graph, qualified metadata, four targets, and bounded PPM behavior pass from a cold unauthenticated registry-only consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r7 closure and Required evidence</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Tracked closure and sanitized live evidence reciprocally bind one another and the exact r7 source/tag, union, and seven histories/set.
    - All five Phase 8 requirements pass offline Required validation; any missing predecessor, graph node, target, metadata field, behavior result, or digest fails.
    - Raw network, authentication, credential, and secret output never enters tracked evidence.
  </behavior>
  <action>Per D-05 through D-18, run ReciprocalArtifacts and replay union, reducer, predecessor chain, seven histories/set, optional receipt, hosted-run identities, public observations, cold graphs, target results, and sanitized content digests. Copy only the closed sanitized projection into tracked `phase-08-requirements.json`; never copy raw network, actor, credential, or secret output. Extend tracked qualification and Required gates for r7 and seven histories while retaining cross-platform byte identity, unique env-key validation, exact14 receipt parity, equal-empty/nonempty-drift, UTC, LF, no-tags, fixed-handoff isolation, rootless attempt-zero, remote-tag authority, and all existing adversarial cases. Update COVERAGE to 20/20 executed only after all evidence passes, with r7, seven histories, exact r6 failure/downstream-zero record, adjacency, D-01 through D-19, and all five requirements. Exclude Phase 9 ledger, provenance/release closure, and milestone audit.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ReciprocalArtifacts -HandoffPath "$env:TEMP/mnf-phase08-r7-handoff.json" -TrackedClosurePath ./release/qualification/phase-08-requirements.json; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Profile Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All Phase 8 requirements have reciprocal r7 evidence and credential-free offline Required passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| AuthorityUnion to PublishOne | Only receipt-backed absent authority may mutate once; exact never republishes. |
| Registry observation to successor | Exact observation and cold predecessor proof are both mandatory. |
| Live sanitized artifacts to tracked closure | Only closed projections and reciprocal digests cross into Git. |
| Cold consumer to registry | No checkout, credential, workspace, path/Git source, or warm state may cross. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-20-01 | Elevation of Privilege | PublishOne | critical | mitigate | Exclusive union, receipt, predecessor, and one-run reducer gates. |
| T-08-20-02 | Tampering | ordered graph | critical | mitigate | Exact node/edge equality and predecessor artifact selectors. |
| T-08-20-03 | Spoofing | public package identity | critical | mitigate | Structured metadata/index/archive/manifest/checksum agreement. |
| T-08-20-04 | Information Disclosure | tracked closure | high | mitigate | Closed sanitized projection and raw-output rejection. |
| T-08-20-05 | Repudiation | requirement evidence | high | mitigate | Reciprocal digests and offline Required replay. |
| T-08-20-06 | Tampering | cold consumer | high | mitigate | New empty homes and registry-only source isolation assertions. |
</threat_model>

<verification>Validate r7 union, seven histories, ordered one-module authority transitions, structured metadata, cold exact graphs/behavior, reciprocal tracked closure, and offline Required.</verification>
<success_criteria>All three modules are published or exactly checkpointed without republish, strict predecessor proofs pass, and DIST-01..04 plus PROV-05 have r7 reciprocal evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-20-SUMMARY.md` when done.</output>
