---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "10"
type: execute
wave: 10
depends_on: ["08-09"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "08-10 starts only from the fixed schema/digest-valid r2 handoff: mutation branch has packet plus literal receipt; exact-existing has neither; stop has no eligibility."
    - "Core, color, and image resolve in strict order with at most one module mutation per hosted run and no republish of exact content (D-05 through D-07, D-13 through D-15)."
    - "Every module authority binds r2, both historical terminal negatives, exact structured metadata, archive/manifest/checksum, exact graph, four targets, native runtime, behavior, locator/index, and predecessor."
    - "Final reciprocal evidence closes DIST-01..04 and PROV-05 while Required remains offline, credential-free, and non-mutating."
  artifacts:
    - path: "%TEMP%/mnf-phase08-r2-handoff.json"
      provides: "Validated eligible start authority"
    - path: "<r2-artifact-root>/authorities/mb-core/*.json"
      provides: "Exact core authority"
    - path: "<r2-artifact-root>/authorities/mb-color/*.json"
      provides: "Exact color authority with core predecessor"
    - path: "<r2-artifact-root>/authorities/mb-image/*.json"
      provides: "Exact image authority with full graph"
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Tracked reciprocal closure"
  key_links:
    - from: "fixed r2 handoff"
      to: "core eligibility"
      via: "schema/digest replay and receipt-or-exact branch validation"
      pattern: "authority_union_variant"
    - from: "core/color authorities"
      to: "successor eligibility"
      via: "prior_authority_record_sha256 and exact cold proof"
      pattern: "PriorAuthorityRecordPath"
---

<objective>
Complete ordered r2 core/color/image publication or exact-existing recovery, prove cold consumption, and persist reciprocal Phase 8 closure.

Purpose: Deliver genuine Mooncakes usability without ambiguity-driven republish or loss of retry history.
Output: Three ordered exact authorities and tracked credential-free evidence for all five requirements.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-09-SUMMARY.md
@policy/phase-08-distribution.json
@release/qualification/phase-08-handoff-schema.json
@release/qualification/phase-08-authority-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate handoff and resolve core then color</name>
  <files>None (sanitized r2 hosted artifacts only)</files>
  <action>Load `%TEMP%/mnf-phase08-r2-handoff.json`, validate its closed schema, canonical digest, every referenced file digest, LF clean ExecutionRoot, unique StateRoot, r2 ref/boundary, both history records, and AuthorityUnion. Mutation variant must have unchanged packet and authorize-core receipt binding packet/ref/boundary/literal response/time/digest; exact-existing must have neither. Reject stop or any other variant. Exact core is consumed directly without preflight, dry run, packet, receipt, or re-observation. Mutation core is re-observed and only absent may invoke one PublishOne; exact selects zero-mutation authority, and mismatch/unknown/disagreement/timeout stop. After exact core public observation and cold one-node proof, resolve color with core as PriorAuthorityRecordPath. Color absent may invoke one separate PublishOne run; exact never republishes. For both modules explicitly materialize structured public surface, run and index schema/digest-valid observation, run outside-checkout empty-home exact-graph cold proof on js/wasm/wasm-gc/native, then select normalized authority. Run CoreColorArtifacts with exact paths.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r2-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; $x=[string]$h.execution_root; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -CoreColorArtifacts -HandoffPath $p }"</automated></verify>
  <done>Eligible r2 core and color each have one exact authority and four-target cold proof in strict predecessor order.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove the exact full graph</name>
  <files>None (sanitized r2 hosted artifacts only)</files>
  <action>Per D-05 through D-18, reload and fully revalidate the fixed handoff plus CoreColorArtifacts. Resolve image with color as PriorAuthorityRecordPath through the same closed absent/exact/mismatch/unknown/disagreement/timeout switch; absent may invoke one separate PublishOne, exact has zero mutation, and every uncertainty branch stops. Explicitly observe/index structured metadata and run an outside-checkout cold consumer with new empty MOON_HOME, pinned MOON_TOOLCHAIN_ROOT, no credential/moon.work/path/Git/copied source/warm state. Require exact graph mb-image@0.1.0 -> mb-color@0.1.0 -> mb-core@0.1.0, archive/downloaded manifests/checksums/docs, four real target checks/tests/runtimes, and bounded public PPM behavior. Select image authority and run LiveArtifacts with all predecessor paths and branch-appropriate receipt evidence.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r2-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -LiveArtifacts -HandoffPath $p }"</automated></verify>
  <done>Image has one exact authority and the complete exact graph plus bounded PPM proof passes all four targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal closure and preserve offline Required</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - ReciprocalArtifacts binds all requirements to r2, both histories, handoff variant, optional receipt, and three ordered authorities.
    - Exact-existing has zero mutation; published-now has exactly one mutation and one predecessor-valid hosted run.
    - Required validates only closed sanitized tracked evidence and all UTC digest regressions without live state, credentials, network, tags, dispatch, or publish.
  </behavior>
  <action>Invoke clone-rooted ReciprocalArtifacts with the fixed handoff and three authority paths, replay reducer/predecessor/history/receipt rules, and copy only its closed sanitized projection into the tracked requirements record. Extend tracked qualification and Required integration, including UTC offset-equivalence/reload/changed-instant/tamper/stale regressions. Update COVERAGE for ten plans, r2 current authority, attempt-zero/r1 terminal-negative history, exclusive AuthorityUnion adjacency, mutation packet+literal receipt versus packet-free exact-existing, mutation-only HostedPreflight, all D-01..D-19, and all five requirements. Keep Phase 9 work excluded.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -TrackedClosure; if ($LASTEXITCODE) { exit $LASTEXITCODE }; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All requirements have reciprocal r2 evidence and offline credential-free Required passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Fixed handoff to mutation eligibility | Receipt-or-exact union must replay exactly. |
| Module authority to successor | Exact predecessor and cold proof gate each mutation. |
| Live artifacts to tracked closure | Only sanitized closed projection crosses. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-10-01 | Elevation of Privilege | handoff/outcome switch | critical | mitigate | Schema/digest replay; mutation requires literal receipt; only absent calls one PublishOne. |
| T-08-10-02 | Tampering | successor authority | critical | mitigate | Ref/history/observation/cold/predecessor/index digest equality and reducer replay. |
| T-08-10-03 | Spoofing | old root/current r1 | critical | mitigate | r2 fixed handoff, LF exact clone, unique StateRoot, terminal-history-only r1. |
| T-08-10-04 | Information Disclosure | live/tracked evidence | high | mitigate | Closed sanitized projection, raw/secret exclusion, offline Required. |
| T-08-10-05 | Denial of Service | propagation ambiguity | medium | mitigate | Bounded stop with no automatic rerun or republish. |
</threat_model>

<verification>Validate fixed handoff eligibility, ordered three-module authorities, exact metadata/graphs/cold proofs, reciprocal closure, UTC regressions, and offline Required.</verification>
<success_criteria>All three modules are genuinely published or exactly checkpointed without republish and all five Phase 8 requirements have r2-bound cold registry evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-10-SUMMARY.md` when done.</output>
