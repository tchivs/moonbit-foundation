---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "10"
type: execute
wave: 10
depends_on: ["08-09"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher, workflow, mutation adapter, hosted controller, observer, and selectors accept only r3 current identity and bind three terminal histories."
    - "The debug-fixed hosted field builder preserves an exact ordered 14-field vector for start and resume without evaluating if as a command or collapsing fields."
    - "UTC locator digests, CRLF-safe execution, and no-tags qualification fixtures retain their regressions."
    - "Only LibraryOnly tests may inject handoff/temp paths; production fixed r3 handoff is non-overridable and must remain absent throughout static suites."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r3 three-history hosted state, exact 14-field dispatch, receipt, and handoff seam"
    - path: "scripts/quality/Test-Phase08LiveSeam.ps1"
      provides: "14-field, UTC, receipt/handoff, and isolation regressions"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "r3 no-tags and three-history qualification fixtures"
  key_links:
    - from: "three history records"
      to: "14-field hosted dispatch"
      via: "canonical historical_attempts_sha256 set digest"
      pattern: "historical_attempts_sha256"
    - from: "LibraryOnly seam"
      to: "GUID fixture roots"
      via: "test-only HandoffPath/TempRoot injection"
      pattern: "LibraryOnly"
---

<objective>
Wire r3 and three-history bindings through the publisher/workflow/hosted seam while preserving the confirmed helper, UTC, LF, and no-tags fixes.

Purpose: Ensure the next live attempt reaches hosted preflight using deterministic tested fields and isolated state.
Output: r3-capable hosted implementation with complete credential-free regression coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-hosted-fields.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-09-SUMMARY.md
@policy/release-control.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce r3 in publisher, adapter, and workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - r3 initial request binds current/root/prepared plus three individual history digests and their ordered set digest.
    - r2 current, history drift/mix, correction semantics, or more than one module mutation fail before credentials.
    - Workflow credential isolation, exact tchivs actor proof, and raw/secret rejection remain unchanged.
  </behavior>
  <action>Per D-01 through D-07 and D-13 through D-15, advance the publisher request, one-step adapter, and workflow from r2 to r3. Carry all three histories in closed request/evidence records and verify their ordered aggregate. Preserve one mutation per hosted run, isolated MOON_HOME, pinned MOON_TOOLCHAIN_ROOT, actor evidence, and no raw credential output. Add r2-current, three-history replacement/mix, aggregate drift, second mutation, and correction-lane negatives. Commit this four-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Publisher/workflow accepts only exact r3 with three histories and remains isolated and one-step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the 14-field, UTC, no-tags, and handoff seam regressions</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1</files>
  <behavior>
    - Hosted dispatch returns exactly 14 separately grouped ordered fields for start and resume: operation_mode, run_mode, release_ref, source_sha, root_intent_sha256, intent_sha256, prepared_manifest_sha256, historical_attempts_sha256, target_module, live_authorization, prior_run_id, prior_artifact_name, authorization_packet_sha256, authorization_receipt_sha256.
    - historical_attempts_sha256 is recomputed from the three individually validated history records; it never replaces their schema/index/store/packet/receipt/handoff bindings.
    - UTC-equivalent DateTime/DateTimeOffset/JSON reloads hash equally; changed/tampered/stale inputs fail.
    - Qualification clones with --no-tags and seeds only fixture-owned tags; permanent repository tags cannot collide.
    - Production fixed r3 handoff is non-overridable and absent before/after tests; LibraryOnly mutation/exact/collision fixtures use separate GUID roots and finally delete only owned paths.
  </behavior>
  <action>Per D-03, D-04, D-07, and D-13 through D-19, extend hosted preparation, receipt, active attempt, union, and handoff from r2/two histories to r3/three histories. Preserve the debug fix by constructing the listed 14 fields as distinct grouped array elements; start uses empty packet/receipt and resume uses branch-valid packet/receipt values. Add focused HostedFieldsOnly count/order/value tests for both vectors, including the old ungrouped-if and one-element-collapse failures. Preserve all UTC locator/receipt/handoff offset/reload/tamper/stale cases, core.autocrlf=false/LF checks, and the no-tags fixture clone. LibraryOnly alone may inject HandoffPath/TempRoot; positive mutation, exact, and collision fixtures each own an independent GUID root in try/finally, and suite start/end assert `%TEMP%/mnf-phase08-r3-handoff.json` absent. Production computes the fixed path internally and rejects overrides. Run focused and adjacent suites and commit atomically without live actions.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -HostedFieldsOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r3 hosted state passes exact 14-field, UTC, LF, no-tags, three-history, receipt/handoff, and isolation regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r3 prepared identity to credentialed workflow | Three histories and exact current binding must survive dispatch. |
| PowerShell values to gh field vector | Expression grouping and field boundaries must remain exact. |
| Tests to production fixed handoff/tags | Fixtures cannot touch permanent state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-10-01 | Tampering | hosted field vector | critical | mitigate | Exact 14-name order/count/value tests for start/resume and grouped expressions. |
| T-08-10-02 | Spoofing | history aggregate | critical | mitigate | Recompute from three individually schema/digest-validated records. |
| T-08-10-03 | Elevation of Privilege | publisher request | critical | mitigate | r3/three-history/one-module validation before credential materialization. |
| T-08-10-04 | Tampering | fixtures versus production | high | mitigate | no-tags clones, GUID-owned roots, LibraryOnly injection, fixed-path absence assertions. |
| T-08-10-05 | Information Disclosure | hosted evidence | high | mitigate | Isolated credentials and raw/secret-shape rejection. |
</threat_model>

<verification>Run publisher, HostedFieldsOnly, full live-seam, qualification, and observation suites; confirm r3, exact 14 fields, UTC/LF/no-tags behavior, and no live effects.</verification>
<success_criteria>The r3 hosted seam is fully regression-tested and ready for one new live boundary.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-10-SUMMARY.md` when done.</output>
