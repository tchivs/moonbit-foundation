---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "24"
type: execute
wave: 24
depends_on: ["08-23"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "r10 is the sole current initial retry and binds attempt-zero plus r1 through r9 as ten immutable terminal histories (D-01, D-07, D-15)."
    - "r9 remains a remote immutable tag whose peeled source is 4158dff; it is recorded as a pre-locator StrictMode history-schema terminal record and is never rerun, retagged, or reused."
    - "Prepared, authority, receipt, and handoff artifacts reject a missing, substituted, reordered, or altered r9 history digest before the publisher boundary (D-01, D-14, D-18)."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r10 current-attempt policy and exact ten-history terminal set"
    - path: "release/intent/schema.json"
      provides: "r10-only initial release binding"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "closed r10 authority, receipt, and handoff contracts"
  key_links:
    - from: "policy terminal_negative_history"
      to: "prepared/authority/receipt/handoff request fields"
      via: "ten individual SHA-256 values plus LF-ordered set digest"
      pattern: "historical_r9_sha256"
---

<objective>
Advance the credential-free Phase 8 contracts from the terminated r9 attempt to one forward-only r10 retry.

Purpose: Preserve immutable r9 evidence while making every later qualification artifact prove it is using a fresh r10 state rather than replaying the tag or r9 paths.
Output: r10 policy, schemas, prepared validation, and tests; no network operation, credential access, tag, dispatch, handoff, or registry mutation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-22-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-23-SUMMARY.md
@.planning/debug/phase08-r9-history-schema-debug.md
@policy/release-control.json
@release/intent/schema.json
@release/prepared/schema.json
@release/qualification/phase-08-handoff-schema.json
@scripts/quality/ReleaseQualification.Common.ps1

<interfaces>
From `policy/release-control.json`: `initial_attempt_family.current_attempt`, ordered `terminal_negative_history`, and `history_set_sha256` are the authoritative retry identity; the record must contain r9 as terminal evidence before r10 can be current.

From `scripts/quality/ReleaseQualification.Common.ps1`: the existing initial-history binding is the shared source for prepared requests, authority variants, receipts, and handoffs. Advance that one contract together with its consumers; do not introduce an independent r10 history serializer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the immutable r9-to-r10 history family</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - A valid initial contract identifies r10 and contains attempt-zero followed exactly by r1 through r9, with every individual digest and the canonical aggregate.
    - The r9 record binds the already-pushed annotated tag and peeled 4158dff source, its exact protected terminal record digest, its pre-locator StrictMode schema failure, and zero locator/run/credential/authority/handoff/publication effects.
    - Removing, swapping, or changing r9 evidence, accepting r9 as current, or accepting a r10 artifact with a stale nine-history aggregate fails closed.
  </behavior>
  <action>Write RED assertions first, then update the release-control policy and the intent, prepared, authority, receipt, and handoff schemas. Per D-01, D-07, D-14, D-15, and D-18, advance only the initial retry binding to `modules-v0.1.0-r10`; derive the r9 tag object, peel, source binding, exact field inventory, and digest from the committed/remote facts rather than inventing them. Classify r9 as immutable pre-locator terminal evidence from the verified StrictMode history-schema failure after `InitializeBoundary` and before active-locator creation. Require all ten individual history digest fields and their LF-ordered set digest in every closed artifact. Do not alter historical r8 evidence, tag references, or any prior record, and do not permit a compatibility fallback for arbitrary missing fields.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1</automated>
  </verify>
  <done>Contract tests prove r10 is the only current retry, r9 is exactly bound as terminal evidence, and all stale or mutated history variants fail before authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind fresh r10 prepared qualification to the ten-history contract</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - A fresh r10 request produces a deterministic prepared bundle only from a new r10 root and binds all ten history digests plus the canonical set.
    - A request that points at r9 state, paths, locator, archive, index, store, tag, or handoff is rejected before provider work or side effects.
    - The r8 legacy reader remains narrowly accepted only for its attested immutable record; altered r8 or r9 fields are rejected.
  </behavior>
  <action>After RED, update the shared history binding and prepared-bundle generator to consume the r10 policy contract without duplicating history serialization. Per D-01 and D-07, require a fresh r10 genesis, new state roots, and canonical prepared bytes bound to the qualified r10 intent. Carry r9 only as historical digest evidence; no r9 execution root, locator, archive, index, artifact store, tag creation, or handoff may become an input to r10. Preserve the `3a761ae` narrow legacy-r8 reader behavior validated by `Test-Phase08PrepareHistorySchema.ps1`; it is evidence preservation, not a broad relaxation of history completeness. Repair the qualification fixture so its declared noncanonical raw archive is constructed deterministically and reaches the intended canonical-copy assertion on the current pinned toolchain; do not weaken the production archive validator or bypass the post-history qualification path. Keep this plan credential-free and local: no remote query, push, tag, dispatch, secret materialization, PublishOne call, or registry operation.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1</automated>
  </verify>
  <done>Prepared qualification accepts only a fresh r10 contract with ten immutable histories and retains strict rejection of mutated historical evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Immutable r9 evidence to r10 contract | Historical evidence is untrusted until its exact tag/source/digest/field inventory is revalidated. |
| Policy/schema to prepared bundle | A stale or substituted history field must not reach the publisher seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-24-01 | Tampering | `release-control.json` history family | critical | mitigate | Test exact ordered r9 record, ten individual digests, and canonical aggregate before accepting r10. |
| T-08-24-02 | Spoofing | tag/source binding | critical | mitigate | Bind r9’s existing annotated tag and peeled source to policy and schema tests; no r9 reuse path exists. |
| T-08-24-03 | Elevation of Privilege | prepared request | high | mitigate | Fresh r10 roots and closed schemas reject r9 state reuse before any publisher/credential boundary. |
| T-08-24-04 | Repudiation | terminal history | medium | mitigate | Record the precise pre-locator StrictMode disposition and retain immutable record digests. |
</threat_model>

<verification>
Run the focused intent, prepared-bundle, qualification-fixture, and strict history-schema suites. Confirm `git diff --check` and PowerShell parser success for every edited script. Confirm no local or remote release/tag/registry side effect was attempted.
</verification>

<success_criteria>
r10 is the unique forward retry in source contracts; r9 remains an immutable terminal record with no rerun path; all r10 preparation contracts require the complete ten-history binding and a fresh state.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-24-SUMMARY.md` when done.
</output>
