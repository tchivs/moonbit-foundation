---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "24"
type: execute
wave: 24
depends_on: ["08-23"]
files_modified: []
autonomous: false
requirements: [DIST-01]
must_haves:
  truths:
    - "Exact r9 committed HEAD is pushed before r9 tag; remote/main/tag peel/canonical prepared-copy fixture/clean clone bind one SHA (D-01, D-07)."
    - "GUID roots reuse no r8 state, locator, archive, index, store, tag, or handoff."
    - "Nine histories/set bind fresh r9 preparation and exclusive AuthorityUnion."
    - "Exact-existing needs no preflight/dry/packet/receipt; absent has dynamic human-action accepting only same-turn literal authorize-core or stop (D-04, D-14, D-15)."
    - "No branch calls PublishOne or mutates Mooncakes."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R9PreLive.ps1"
      provides: "Mandatory zero-write gate"
    - path: "refs/tags/modules-v0.1.0-r9"
      provides: "Annotated r9 boundary"
    - path: "%TEMP%/mnf-phase08-r9-handoff.json"
      provides: "Eligible nine-history AuthorityUnion"
  key_links:
    - from: "nine histories and canonical-copy prepared evidence"
      to: "r9 AuthorityUnion"
      via: "history/set/seam/fixture/archive/intent digests"
      pattern: "historical_r8_sha256"
---

<objective>Establish r9 canonical prepared-copy boundary and select a non-mutating nine-history AuthorityUnion.

Purpose: Cross the repaired PrepareAttempt seam while preserving the explicit first-mutation checkpoint.
Output: r9 tag, clean clone, unique state, exclusive authority, optional literal receipt, fixed handoff; no PublishOne.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-prepare-canonicalization-seam.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-23-SUMMARY.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute zero-write r9 gate before every live side effect</name>
  <files>None (read-only local, GitHub, registry, ref, run, and temporary-state checks)</files>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, before push/tag/root/dispatch/secret/registry/handoff action, run `Invoke-Phase08R9PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin`. Require its closed output to prove attempt-zero+r1-r8 refs/peels/digests/set; exact r8 source/tag/PREP15/REL-XPLAT/no locator/no run/downstream zero; canonical-copy seam version and actual-prepare fixture; historical path digests; clean 08-22/23 summaries/ownership; r9 tag/handoff absence. Recompute result digest and stop on disagreement, ambiguity, or any write. Repeat same-turn before any resumed Task 2 side effect.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R9PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin</automated></verify>
  <done>r9 gate passes completely before Task 2 may act.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 2: Create r9 boundary and dynamic non-mutating AuthorityUnion</name>
  <files>None (push/tag/roots/sanitized artifacts/optional receipt/handoff)</files>
  <action>Only after same-turn Task 1, per D-01 through D-07 and D-13 through D-19, push exact HEAD and require local/remote equality. Create GUID-owned roots; LF/no-tags clone exact SHA; verify no historical overlap. Create/push non-force annotated `modules-v0.1.0-r9`, verify object/peel, then PrepareAttempt initial 0.1.0 sequence zero root=current no predecessor with nine histories/set and fresh state. Require actual canonical-copy seam version/fixture and local-hosted prepared manifest/archive/intent equality.

Run fresh credential-free ObserveOnly core. Exact creates packet/receipt-free ExactExistingAuthority after structured observation and cold four-target proof. Absent permits one HostedPreflight and one PublisherDryRun with snapshots, unique keys, exact14, actor `tchivs`, dry success, credential removal, fresh absence, and zero PublishOne. Generate packet and emit non-auto-approvable `checkpoint:human-action`. Display roots; locator paths/file digests; ArtifactRoot; PacketPath/Sha256; release/boundary/source SHA; AttemptZero/R1..R8 digests/set; seam version/actual fixture/canonical archive/prepared/root/intent digests; hosted ids; actor/disposition; absence digest/timestamp; zero mutation. Accept only case-sensitive `authorize-core` or `stop` in same checkpoint turn. Stop writes nothing. Authorization triggers fresh reload/revalidation of every displayed field, refs, clone, histories/set, seam/fixture, hosted artifacts, actor/dry/absence/packet/paths/zero mutation; drift stops. Only then receipt, mutation union, handoff. Never call PublishOne.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath "$env:TEMP/mnf-phase08-r9-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated></verify>
  <done>One r9 union exists; exact has no packet/receipt, absent has same-turn receipt, and publication count remains zero.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Source/tag/seam/fixture | Exact committed identity and actual prepare evidence must agree. |
| Observation to authority | Only exact or confirmed absent selects a branch. |
| Human literal to receipt | Complete same-turn recovery is mandatory. |
| Dry-run to mutation | This plan cannot call PublishOne. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-24-01 | Tampering | source/seam/fixture | critical | mitigate | SHA/peel/seam/fixture digest equality. |
| T-08-24-02 | Spoofing | r8 history | critical | mitigate | Exact no-locator/no-run/nine-set validation. |
| T-08-24-03 | Elevation of Privilege | authorization | critical | mitigate | Same-turn literal and full recovery. |
| T-08-24-04 | Repudiation | no mutation | high | mitigate | Zero counts and no PublishOne path. |
</threat_model>

<verification>Validate nine histories, r8 terminal evidence, r9 seam/fixture boundary, dynamic checkpoint/recovery, exclusive union, handoff, and zero PublishOne.</verification>
<success_criteria>08-24 ends only with valid r9 exact-existing or literal-receipt authority ready for 08-25; it performs no registry mutation.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-24-SUMMARY.md` when done.</output>
