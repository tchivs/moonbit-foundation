---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "31"
type: execute
wave: 31
depends_on: ["08-30"]
files_modified:
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Invoke-Phase08R12PreLive.ps1
  - scripts/quality/Test-Phase08PrepareHistorySchema.ps1
  - scripts/quality/Test-Phase08R12PreLive.ps1
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "Hosted/publisher/workflow accept only r12 with twelve history values and aggregate."
    - "r12 pre-live is credential-free and zero-write, requires r11 tag/peel evidence, r12 absence, canonical wrapper availability, and no r12 handoff."
    - "Pre-authorization remains mutually exclusive and zero-mutation: exact-existing no packet/receipt/handoff; absent one packet and no receipt/handoff."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R12PreLive.ps1"
      provides: "credential-free r12 selector"
    - path: ".github/workflows/publish-modules.yml"
      provides: "twelve-history isolated publisher propagation"
---
<objective>Wire r12 through hosted, publisher, and zero-write qualification seams.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-30-SUMMARY.md
@scripts/quality/Invoke-Phase08R12Boundary.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-ReleasePublisher.ps1
@.github/workflows/publish-modules.yml</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Advance hosted and zero-write r12 qualification seams</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Invoke-Phase08R12PreLive.ps1, scripts/quality/Test-Phase08PrepareHistorySchema.ps1, scripts/quality/Test-Phase08R12PreLive.ps1</files>
  <behavior>
    - r12 with twelve histories and canonical wrapper context reaches fresh preparation; r11 active state/ref reuse fails.
    - selector validates r11 remote identity, r12 absence, canonical wrapper contract, and zero writes.
    - pre-authorization rejects mixed packet/receipt/handoff states and nonzero mutation.
  </behavior>
  <action>Write RED fixtures then advance controller/active-attempt/dispatch history binding to r12/twelve fields. Add r11 terminal checks using only attested object/peel/invocation facts. Implement a read-only r12 selector modeled on r11 but requiring the r12 boundary wrapper and remote-clone test capability before eligibility. Preserve closed pre-authorization validation and make all selector tests fixture-only with no tag, credential, dispatch, or registry action.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08R12PreLive.ps1</automated></verify>
  <done>Only r12/twelve-history canonical-wrapper inputs pass hosted/pre-live qualification with zero writes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Carry r12 authority through the isolated one-module publisher</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1, scripts/quality/Test-Phase08LiveSeam.ps1</files>
  <behavior>
    - stale r11 requests/receipts/handoffs, missing r11 history, and altered aggregate are rejected.
    - only r12 can reach one journal-selected module adapter.
    - only publisher temporary Moon home can materialize the token.
  </behavior>
  <action>Write failing seam tests before replacing r11 history propagation with r12/twelve fields in publisher, live adapter, and workflow. Keep one `moon publish --frozen` module invocation per run, read-only workflow defaults, pinned actions/toolchain, and publisher-only temporary Moon home cleanup. No test or task in this plan may dispatch, tag, read credentials, or mutate Mooncakes.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1</automated></verify>
  <done>Only complete r12 authority can reach an isolated one-module publisher seam.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r11 terminal to r12 controller | Historical tag facts cannot become active state. |
| pre-live to publisher | Credential-free selection cannot authorize mutation. |
| workflow to adapter | Only one isolated publisher path sees token. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-31-01 | Tampering | twelve-history binding | critical | mitigate | Closed individual fields and aggregate tests. |
| T-08-31-02 | Spoofing | ref caller | critical | mitigate | Canonical wrapper requirement in selector/controller. |
| T-08-31-03 | Information Disclosure | publisher credential | critical | mitigate | Publisher-only temporary home and teardown. |
</threat_model>
<verification>Run focused selector, history, publisher-negative, live-seam, parser, and diff checks; no external operation.</verification>
<success_criteria>r12 is fully wired and no stale r11/ref-override path reaches publisher.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-31-SUMMARY.md` when done.</output>
