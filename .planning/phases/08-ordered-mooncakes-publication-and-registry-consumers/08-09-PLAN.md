---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "09"
type: execute
wave: 9
depends_on: ["08-08"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "refs/tags/modules-v0.1.0-r3 is the sole current initial forward-retry with module version 0.1.0, intent_kind initial, correction_sequence 0, root=current, and no predecessor (D-01, D-07, D-15)."
    - "Attempt zero, r1 at 09548df948f58ec1bdfff7494757596c03e4c9bd, and r2 at 73a3af920fc3938f49e93d14f16f79f116475f1e are three distinct immutable terminal-negative histories and can never be substituted, collapsed, reordered, or selected as current authority."
    - "The r2 record states no workflow run and no mutation; it binds successful PrepareAttempt and confirmed_absent, then terminal failure before HostedPreflight dispatch because the old helper field-array construction failed."
    - "Schemas, prepared identity, index/store, packet, receipt, authority, and handoff bind all three individual history digests plus their canonical ordered history-set digest."
    - "Receipt and handoff retain closed UTC/digest and exclusive mutation-versus-exact-existing rules; r3 uses a new fixed handoff namespace."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r3 current profile plus exact attempt-zero/r1/r2 terminal-negative history"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r3 handoff with three-history and receipt-or-exact union bindings"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r3 prepared identity and canonical history set"
  key_links:
    - from: "three terminal-negative records"
      to: "r3 intent/prepared/index/authority contracts"
      via: "individual SHA-256 values plus ordered history-set SHA-256"
      pattern: "historical_r2_sha256"
    - from: "r3 current profile"
      to: "initial intent and prepared bundle"
      via: "root=current, sequence zero, null predecessor"
      pattern: "modules-v0.1.0-r3"
---

<objective>
Define and test the r3 initial forward-retry and its three immutable terminal-negative history bindings.

Purpose: Recover from the r2 pre-dispatch helper defect without rewriting prior attempts or entering the published-content correction lane.
Output: Closed r3 policy/schemas/prepared contracts and credential-free adversarial tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/phase08-hosted-fields.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-08-SUMMARY.md
@policy/release-control.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the initial attempt family to r3</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r3 validates as current initial retry; versions remain 0.1.0 and correction fields remain absent/zero/null.
    - Attempt-zero, r1, and r2 exact facts and order are immutable; each record has a distinct digest.
    - r2 explicitly records PrepareAttempt and confirmed_absent followed by pre-HostedPreflight-dispatch helper failure, with no run/mutation/authority.
    - Any old-current substitution, history collapse/reorder/mix, predecessor, or correction semantics fail.
  </behavior>
  <action>Per D-01, D-07, and D-15, advance only the current initial retry from r2 to r3. Preserve r1 and attempt-zero facts and add the exact r2 terminal-negative facts from the plan context. Define canonical history order attempt-zero, r1, r2 and an ordered history-set digest without replacing the three individual digests. Update policy, schemas, generator, and focused tests; keep correction-N exclusively for published-content mismatch with advanced module versions. Commit this five-file unit atomically after tests pass.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r3 is the sole current initial retry and all three prior attempts are exact, distinct, ordered terminal-negative history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind three histories into authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Authority, receipt, and handoff require attempt-zero, r1, r2, and ordered history-set digests.
    - Mutation handoff requires packet plus literal receipt; exact-existing forbids both; stop is ineligible.
    - r2-as-current, swapped histories, duplicate digest, aggregate mismatch, stale UTC, or unknown property fails.
  </behavior>
  <action>Per D-03, D-04, D-07, D-14, D-15, and D-18, extend the closed Phase 8 schemas and schema fixtures from two histories/r2 to three histories/r3. Receipt continues to bind packet digest, r3 ref, boundary/source SHA, literal authorize-core, canonical UTC creation evidence, and its own digest. Handoff uses `%TEMP%/mnf-phase08-r3-handoff.json`, binds all referenced paths/files by digest, and admits exactly one eligible union branch. Add replacement, mix-and-match, reorder, duplicate, aggregate-drift, packet-without-receipt, receipt-on-exact, and stop tests. Commit this four-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$p=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($f in $p){$null=Get-Content -LiteralPath $f -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All live authority schemas require three exact histories and preserve receipt-or-exact exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fresh r3 prepared, store, and qualification identity</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r3 root/genesis/prepared/index/store bind all three histories and cannot reuse r2 state.
    - Prepared payloads and current intent remain initial/root-equal with no predecessor.
    - Qualification rejects missing, reordered, substituted, or cross-attempt history/store records.
  </behavior>
  <action>Extend canonical qualification and prepared-bundle generation to compose the Task 1 attempt family and Task 2 schemas. Every locator/index/store record that grants eligibility must carry the three individual history digests and the recomputed ordered set digest. Add positive r3 fixtures and adversarial r2-current, old-root, reused-index, missing-history, history-order, aggregate, and correction-lane cases. Preserve UTC canonicalization and no-tags clone isolation. Commit this five-file unit atomically after focused suites pass.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r3 prepared/store identity is fully bound to three terminal histories and passes static qualification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Three terminal attempts to r3 current | Historical evidence cannot become current authority. |
| Individual histories to aggregate | Order and membership must be deterministic. |
| Prepared/store identity to later live seam | No r2 locator, root, index, or genesis may cross. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-09-01 | Tampering | attempt family | critical | mitigate | Exact facts, three individual digests, ordered aggregate, and substitution/reorder tests. |
| T-08-09-02 | Spoofing | r2 state as r3 | critical | mitigate | r3-only ref/root/store schemas and old-root/index rejection. |
| T-08-09-03 | Elevation of Privilege | receipt/handoff | critical | mitigate | Closed mutation receipt versus packet-free exact union bound to all histories. |
| T-08-09-04 | Repudiation | r2 termination | high | mitigate | Persist PrepareAttempt/confirmed_absent/pre-dispatch failure and no-run/no-mutation facts by digest. |
</threat_model>

<verification>Run intent, prepared, and Phase 8 qualification suites; validate r3 invariants, three-history membership/order/digests, UTC behavior, and no live effects.</verification>
<success_criteria>r3 and all three immutable prior attempts are closed tested contracts ready for hosted integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-09-SUMMARY.md` when done.</output>
