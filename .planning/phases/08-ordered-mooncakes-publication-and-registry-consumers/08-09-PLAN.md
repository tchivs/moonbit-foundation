---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "09"
type: execute
wave: 9
depends_on: ["08-08"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "The tested static r2 HEAD is pushed exactly before one annotated r2 tag is created; origin/main, peeled tag, LF clean clone, and locator all bind the same SHA."
    - "ExecutionRoot is cloned with core.autocrlf=false and StateRoot is a new GUID-unique empty directory; no prior locator, active attempt, artifact index, or fixed handoff is reused."
    - "ObserveOnly produces exact-existing authority automatically or an absent packet that requires literal authorize-core and a closed receipt; stop produces no eligible handoff (D-03, D-04, D-19)."
    - "The absent checkpoint displays a complete sanitized but untrusted recovery-input packet; a fresh continuation reloads and revalidates every field from disk and remote state before it may persist a receipt."
    - "The fixed handoff is schema/digest validated, binds both historical negatives and exactly one eligible AuthorityUnion variant, and Plan 08-09 never calls PublishOne."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r2"
      provides: "Annotated r2 tag peeled to exact pushed static boundary"
    - path: "%TEMP%/mnf-phase08-r2-handoff.json"
      provides: "Closed discoverable eligible authority handoff"
    - path: "<unique-state-root>/authorization/authorize-core-receipt.json"
      provides: "Absent-branch literal authorization receipt; forbidden on exact-existing"
  key_links:
    - from: "origin/main exact HEAD"
      to: "r2 tag, LF ExecutionRoot, and boundary locator"
      via: "SHA equality, annotated peel, clean status, and UTC locator digest"
      pattern: "boundary_sha"
    - from: "packet plus literal continuation"
      to: "mutation AuthorityUnion"
      via: "closed receipt and active-attempt/handoff digest update"
      pattern: "authorization_receipt_sha256"
    - from: "absent checkpoint recovery packet"
      to: "fresh continuation"
      via: "disk/remote reload, schema/digest/cross-binding, LF clone, history, actor, dry-run, and absence revalidation"
      pattern: "untrusted_recovery_inputs"
---

<objective>
Establish the exact LF-clean r2 boundary and produce one non-mutating, receipt-qualified core-entry AuthorityUnion handoff.

Purpose: Cross the live boundary without publishing and preserve the explicit first-mutation decision.
Output: Exact pushed/tagged r2 boundary, unique state, exclusive authority, and fixed validated handoff.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-08-SUMMARY.md
@release/qualification/phase-08-handoff-schema.json
@release/qualification/phase-08-authorization-receipt-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail-closed preflight the immutable history and empty r2 namespace</name>
  <files>None (read-only local, GitHub, tag, run, registry, and temp-state checks)</files>
  <action>Require 08-07/08 summaries and all owned code committed and clean without staging, stashing, resetting, deleting, or touching unrelated user dirt. Verify annotated r1 peels to 09548df948f58ec1bdfff7494757596c03e4c9bd, r1 has no hosted run/mutation/authority, r2 is absent locally and remotely, and `%TEMP%/mnf-phase08-r2-handoff.json` does not exist as either file or directory. If the fixed path contains any old, stale, malformed, or otherwise unexpected object, fail closed and preserve it for diagnosis; never overwrite, delete, rename, or reuse it. Confirm no prior r2 StateRoot/ExecutionRoot is selected and record the read-only preflight facts for Task 2.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r2-handoff.json'; if(Test-Path -LiteralPath $h){throw 'P08-R2-HANDOFF-COLLISION'}; if(@(git ls-remote --tags origin refs/tags/modules-v0.1.0-r2 'refs/tags/modules-v0.1.0-r2^{}').Count -ne 0){throw 'P08-R2-EXISTS'}; $r1=@(git ls-remote --tags origin 'refs/tags/modules-v0.1.0-r1^{}'); if($r1.Count -ne 1 -or (@($r1[0]-split '\s+'))[0] -cne '09548df948f58ec1bdfff7494757596c03e4c9bd'){throw 'P08-R1-HISTORY'} }"</automated></verify>
  <done>Immutable r1 history is intact, r2 and its fixed handoff path are absent, and any collision would stop without mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the LF r2 boundary and persist the eligible AuthorityUnion handoff</name>
  <files>None (sanitized hosted artifacts, optional receipt, and fixed handoff only)</files>
  <action>Require Task 1 preflight unchanged. Push the exact committed current HEAD to refs/heads/main and require remote equality. Create independent GUID-suffixed ExecutionRoot and StateRoot. Clone with `git -c core.autocrlf=false clone --no-tags`, set local core.autocrlf=false, detach at the exact SHA, verify LF bytes for tracked PowerShell/workflow inputs, empty status, exact blobs, and no existing state content. InitializeBoundary from the clone, reload and schema/digest-check the UTC-canonical locator, then create and push annotated r2 without force and verify its tag object and peeled SHA. PrepareAttempt with exact attempt-zero/r1 history and require fresh root/genesis/prepared/index identities.

Per D-03, D-04, D-07, and D-13 through D-19, invoke every helper by absolute path beneath that validated clone and run ObserveOnly for mb-core before preflight, dry run, receipt, or mutation. Exact must run explicit structured observation plus cold one-node four-target/native proof, index them, select packet-free ExactExistingAuthority, leave receipt null, validate exact AuthorityUnion, and only then write the exact-existing fixed handoff.

Absent may run HostedPreflight and PublisherDryRun, assemble one sanitized MutationAuthorizationPacket, validate it, and must return a structured dynamic human-action checkpoint. The checkpoint display contains these explicit fields under `untrusted_recovery_inputs`: `ExecutionRoot`, `StateRoot`, `BoundaryLocatorPath`, `BoundaryLocatorSha256`, `ActiveAttemptPath`, `ActiveAttemptFileSha256`, `ActiveAttemptLocatorSha256`, `ArtifactRoot`, `PacketPath`, `PacketSha256`, `ReleaseRef`, `BoundarySha`, `SourceSha`, `AttemptZeroHistorySha256`, `R1HistorySha256`, `PreparedManifestSha256`, `RootIntentSha256`, and `IntentSha256`. It labels every field as sanitized but untrusted recovery input, displays that no receipt or fixed handoff exists, and accepts only literal `authorize-core` or `stop`.

Treat continuation as a fresh process with no trusted in-memory objects. On literal `authorize-core`, first reload the boundary locator, active attempt, packet, histories, prepared manifest, root/current intents, preflight record, dry-run actor record, and fresh absence observation from the displayed disk paths. Revalidate closed schemas, canonical file/content/locator digests, path containment, all cross-record ref/SHA/intent/prepared/history bindings, and packet digest. Revalidate the ExecutionRoot is clean, `core.autocrlf=false`, LF-safe, at BoundarySha/SourceSha with exact required blobs; re-query the remote annotated r2 peeled SHA; revalidate attempt-zero and r1 immutable history; revalidate sanitized actor exactly tchivs, dry-run success, credential isolation/removal, and an immediately fresh absent mb-core observation. Any missing file, drift, stale timestamp, mismatch, ambiguity, nonliteral response, changed remote ref, dirty/root/blob/line-ending change, or no-longer-absent result records terminal evidence and stops. Only after every reload and revalidation succeeds may PersistAuthorizationReceipt atomically write the closed receipt and update active-attempt path/digest/locator digest; then reload both, validate mutation AuthorityUnion, and write the fixed handoff for the first time. Never create the fixed handoff before the receipt exists. On literal `stop`, record terminal stop and write neither receipt nor handoff. Mismatch, unknown, disagreement, timeout, actor/evidence ambiguity, or stale/tampered state also stop. No branch calls PublishOne. Validate the final handoff against the clone-rooted schema, recompute its digest, verify every referenced file digest/path/root, and record the fixed handoff path/digest, union variant, packet path/digest or null, receipt path/digest or null, and exact-existing authority path/digest or null in 08-09-SUMMARY.md.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r2-handoff.json'; if(-not(Test-Path -LiteralPath $p -PathType Leaf)){throw 'P08-HANDOFF-MISSING'}; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; $x=[IO.Path]::GetFullPath([string]$h.execution_root); $schema=Join-Path $x 'release/qualification/phase-08-handoff-schema.json'; if(-not((Get-Content -LiteralPath $p -Raw)|Test-Json -SchemaFile $schema -ErrorAction Stop)){throw 'P08-HANDOFF-SCHEMA'}; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -Handoff -HandoffPath $p }"</automated></verify>
  <done>Exactly one eligible AuthorityUnion is discoverable and digest-valid: mutation has packet plus literal receipt, exact-existing has neither, and zero publication occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Ambient checkout to LF clone | Only pushed exact bytes are executable. |
| Old state to unique r2 roots | Prior state and fixed handoff are non-reusable. |
| User continuation to receipt | Only exact literal authorize-core creates eligibility. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-09-01 | Spoofing | clone/tag boundary | critical | mitigate | Exact remote/peel/blob equality, LF configuration, clean clone. |
| T-08-09-02 | Tampering | state/handoff | critical | mitigate | Unique roots, no overwrite, closed schema, UTC/file/content digests. |
| T-08-09-03 | Elevation of Privilege | authorization continuation | critical | mitigate | Literal response persisted in packet/ref/boundary-bound receipt before eligibility. |
| T-08-09-04 | Information Disclosure | actor/dry evidence | high | mitigate | Closed sanitized projections and secret/raw rejection. |
| T-08-09-05 | Tampering | checkpoint recovery inputs | critical | mitigate | All displayed inputs are explicitly untrusted; fresh continuation reloads disk/remote state and revalidates schemas, digests, cross-bindings, LF clone, histories, actor, dry run, and fresh absence before receipt. |
</threat_model>

<verification>Validate exact remote/tag/LF clone/state identities, both terminal histories, branch-exclusive receipt rules, fixed handoff schema/digest, and absence of PublishOne.</verification>
<success_criteria>Plan 08-09 ends only with a validated exact-existing authority or literal-receipt-qualified mutation authority at the fixed handoff path.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-09-SUMMARY.md` when done.</output>
