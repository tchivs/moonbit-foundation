---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "26"
type: execute
wave: 26
depends_on: ["08-25"]
files_modified: []
autonomous: false
requirements: [DIST-01, DIST-04]
user_setup:
  - service: GitHub Actions / Mooncakes
    why: "The future publisher dry run and explicit core authorization use the preconfigured protected environment without reading its credential outside the isolated publisher step."
    env_vars:
      - name: MOONCAKES_TOKEN
        source: "Existing protected `mooncakes-production` GitHub environment; its value must not be displayed or copied."
must_haves:
  truths:
    - "r10’s source commit is pushed only after static r10 tests pass; its new annotated tag, remote peel, clean clone, state roots, and prepared artifacts bind one SHA (D-01, D-07)."
    - "A non-publishing r10 qualification ends in exactly one pre-authorization state: exact-existing authority with no packet/receipt/handoff, or confirmed-absent authority with one packet and no receipt/handoff (D-03, D-04, D-13, D-19)."
    - "The dynamic authorize-core checkpoint accepts only a same-turn literal authorization after full revalidation; `stop` leaves receipt and handoff absent and performs no registry mutation."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r10"
      provides: "unique r10 immutable boundary tag"
    - path: "%TEMP%/mnf-phase08-r10-handoff.json"
      provides: "r10 authority handoff created only after an explicit authorize-core decision"
    - path: "%TEMP%/mnf-phase08-r10-preauthorization.json"
      provides: "closed branch-aware non-publishing authority projection"
    - path: "r10 ArtifactRoot/authorization/mutation-authorization-packet.json"
      provides: "sanitized non-publishing core authorization packet"
  key_links:
    - from: "r10 pre-live result and static test evidence"
      to: "r10 boundary and authorization packet"
      via: "same-source SHA, ten history digests, canonical prepared manifest, and fresh absent observation"
      pattern: "modules-v0.1.0-r10"
---

<objective>
Create and qualify one new r10 boundary without publishing, then obtain the sole maintainer’s explicit decision about the first core mutation.

Purpose: Ensure that no Mooncakes mutation can occur until a clean r10 source, static contracts, hosted preflight, dry run, and current credential-free observation all agree.
Output: immutable r10 boundary plus either exact-existing authority or an authorization receipt; this plan itself never calls PublishOne or mutates the registry.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-24-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-25-SUMMARY.md
@.planning/debug/phase08-r9-history-schema-debug.md
@scripts/quality/Invoke-Phase08R10PreLive.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-ReleasePublisher.ps1

<interfaces>
From `Invoke-Phase08R10PreLive.ps1`: `-Check -Repository tchivs/moonbit-foundation -Remote origin` must return the closed credential-free eligibility result before any r10 boundary side effect.

From `Invoke-Phase08HostedRun.ps1`: `InitializeBoundary`, `PrepareAttempt`, hosted preflight, publisher dry run, observation, authorization packet, receipt, and handoff modes consume the durable r10 locator and ten-history binding. `ValidatePreAuthorization` reads the closed pre-authorization projection and distinguishes exact-existing from confirmed-absent without writing; `PublishOne` is not an allowed mode in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish and qualify the unique non-publishing r10 boundary</name>
  <files>None (exact Git refs, clean clone, GUID-owned temporary state, sanitized hosted artifacts, and temporary r10 authority paths)</files>
  <action>After re-running every automated verification from 08-24 and 08-25, run the r10 credential-free pre-live check. Per D-01, D-03, D-04, D-07, D-13, D-14, D-18, and D-19, stop if the worktree includes unrelated changes, if `3a761ae` is not an ancestor of the intended r10 source, if r9 tag/peel terminal evidence disagrees, or if any r10 tag/handoff/state already exists. Push only the exact tested source commit to `origin/main`, require local and remote SHA equality, then create and push a new non-force annotated `modules-v0.1.0-r10` tag and verify its object and peeled source. In a new LF/no-tags clean clone at that exact peel and GUID-owned roots, run InitializeBoundary then PrepareAttempt; require the r10 canonical prepared bundle, intent, archives, locator, and ten-history set to agree. Run exactly one hosted preflight and one publisher dry run using the publisher-only ephemeral Moon home and pinned toolchain; remove that home after each use. Run fresh credential-free structured ObserveOnly for core and emit the closed sanitized pre-authorization projection at the fixed temporary path. Validate the branch with `ValidatePreAuthorization`: exact-existing requires exact authority and packet/receipt/handoff all absent; confirmed absent requires one digest-valid packet and receipt/handoff absent. Record only sanitized, digest-bound evidence and stop on timeout, nonzero exit, mismatch, stale observation, unknown, or any side effect outside the permitted non-publishing operations. Do not invoke PublishOne, dispatch a mutation run, or call any registry-mutating command.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R10PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath "$env:TEMP/mnf-phase08-r10-preauthorization.json" -HandoffPath "$env:TEMP/mnf-phase08-r10-handoff.json"; if ($LASTEXITCODE) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>r10 has one verified immutable boundary and exactly one valid pre-authorization branch: exact authority without packet/receipt/handoff or confirmed absence with one packet and no receipt/handoff; no registry mutation occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Dynamic authorize-core checkpoint after r10 qualification</name>
  <files>None (the decision may create only a sanitized receipt and r10 handoff beneath the existing temporary ArtifactRoot)</files>
  <decision>Authorize the first irreversible `tchivs/mb-core@0.1.0` mutation using the qualified r10 packet, or stop without mutation.</decision>
  <context>Per D-04, this checkpoint is emitted only for Task 1’s confirmed-absent packet branch, after it displays the r10 source/tag peels, clean-clone SHA, r9 terminal evidence and ten-history digests, canonical prepared/intent/archive roots, hosted preflight and dry-run identifiers, publisher actor, credential teardown result, current absent observation digest/timestamp, packet path/SHA, pre-authorization projection, and zero-mutation count. Exact-existing completes Task 1 without this prompt because it has no packet, receipt, or handoff. Mooncakes publication authority is intentionally not inferred by this plan; the operator decides only from those fresh, sanitized facts.</context>
  <action>Before accepting either response, reload and validate the displayed r10 tag/source identities, ten-history set, clean clone, prepared artifacts, hosted preflight/dry-run records, actor, credential teardown, observation timestamp/digest, pre-authorization branch, packet path/digest when absent, and zero-mutation evidence. Per D-04, D-07, D-14, and D-15, accept only the literal selected option in the same checkpoint turn. Only `authorize-core` on the confirmed-absent packet branch may create the closed receipt and r10 handoff for Plan 08-27; it must then run `ValidateHandoff`. Exact-existing needs no receipt/handoff and exits with its authority record. `stop` writes neither receipt nor handoff and must verify both are absent with zero mutation. Any disagreement, stale observation, unknown result, malformed response, or additional request stops without mutation.</action>
  <options>
    <option id="authorize-core">
      <name>authorize-core</name>
      <pros>Creates a same-turn receipt that permits only the next plan’s single core mutation.</pros>
      <cons>The following plan can perform one irreversible registry operation after revalidating all displayed facts.</cons>
    </option>
    <option id="stop">
      <name>stop</name>
      <pros>Leaves r10 boundary and non-publishing evidence intact with no registry mutation.</pros>
      <cons>Phase 8 publication remains blocked until a fresh authorized continuation is planned/executed.</cons>
    </option>
  </options>
  <resume-signal>Type exactly `authorize-core` to create the receipt, or `stop` to end without mutation.</resume-signal>
  <verify>
    <automated>pwsh -NoProfile -Command "$handoff=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r10-handoff.json'; $preauth=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r10-preauthorization.json'; if(Test-Path -LiteralPath $handoff -PathType Leaf){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $handoff; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $preauth -HandoffPath $handoff; exit $LASTEXITCODE"</automated>
  </verify>
  <done>Only same-turn `authorize-core` on confirmed absence yields a receipt and validated handoff; exact-existing and `stop` retain no receipt/handoff and every non-authorized outcome leaves the registry unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local tested commit to remote/tag | Git identity must remain one exact SHA before a unique r10 boundary exists. |
| Publisher dry run to authorization packet | Credentialed dry-run evidence cannot itself authorize or mutate a module. |
| Authorization packet to human decision | The packet is untrusted until all fields are revalidated at the same checkpoint turn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-26-01 | Tampering | r10 source/tag/clone | critical | mitigate | Compare local, remote, tag object, tag peel, clone, intent, and prepared artifact identities. |
| T-08-26-02 | Information Disclosure | dry-run credential | critical | mitigate | Materialize only in publisher-only temporary Moon home, never log/upload it, and verify teardown. |
| T-08-26-03 | Spoofing | registry absence | critical | mitigate | Fresh credential-free structured observation and bounded classification gate packet creation. |
| T-08-26-04 | Elevation of Privilege | human authorization | critical | mitigate | Exact same-turn decision with complete revalidation; stop creates no receipt or mutation. |
| T-08-26-05 | Repudiation | pre-publication evidence | high | mitigate | Persist only sanitized digest-bound packet/authority records and zero-mutation evidence. |
</threat_model>

<verification>
Re-run r10 static suites and the pre-live selector before every side effect. Verify the new remote tag’s object and peel, clean clone SHA, ten-history binding, prepared artifact equality, hosted dry-run cleanup, and current observation. Confirm the selected branch creates no PublishOne record or registry mutation.
</verification>

<success_criteria>
The only route to Phase 8 publication is a fresh r10 receipt created from completed static and non-publishing qualification; r9 is never re-executed and no unapproved mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-26-SUMMARY.md` when done.
</output>
