---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "26"
type: execute
wave: 26
depends_on: ["08-25"]
files_modified:
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-Phase08PrepareHistorySchema.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "A clean clone with a fetched, verified dedicated initial tag can run the real PrepareAttempt path without REL01-REF before any r11 tag exists (D-01, D-07)."
    - "Qualification binds its policy, source SHA, and initial release ref to the exact clean-clone tag peel; an ambient checkout policy or a missing local tag ref fails closed."
    - "r10 remains immutable terminal evidence: tag object 0546025 and peel d49edc5, with REL01-REF after clean-clone tag verification and no prepared, packet, receipt, handoff, hosted, dry-run, observation, PublishOne, registry, or credential effect."
  artifacts:
    - path: "scripts/quality/New-ReleaseIntent.ps1"
      provides: "explicit clean-clone policy/ref binding for initial qualification"
    - path: "scripts/quality/Test-Phase08Qualification.ps1"
      provides: "actual PrepareAttempt clean-clone tag-ref regression"
  key_links:
    - from: "clean clone fetched tag"
      to: "New-ReleaseIntent initial binding"
      via: "policy initial_profile.release_ref and tag peel equal SourceSha"
      pattern: "REL01-REF"
---

<objective>
Repair the credential-free clean-clone qualification reference binding that terminated r10, before creating any r11 tag.

Purpose: Make the actual PrepareAttempt prove a dedicated immutable tag is locally materialized and bound to the clone’s policy/source, rather than reading an ambient or missing ref.
Output: focused clean-clone tag-binding implementation and regression evidence only; no r11 policy advance, push, tag, credential, hosted run, handoff, or registry action.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-24-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-25-SUMMARY.md
@scripts/quality/New-ReleaseIntent.ps1
@scripts/quality/ReleaseQualification.Common.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1

<interfaces>
`New-ReleaseIntent.ps1` currently enforces the policy’s `initial_profile.release_ref` and resolves its tag against the supplied source root. The repair must pass an explicit clone-scoped policy/ref context and prove the fetched tag peels to the same SHA as the source root.

`Invoke-Phase08HostedRun.ps1` calls `InitializeBoundary` then `PrepareAttempt`; the regression must use this real path in a disposable clean clone, not a direct intent-builder substitute.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind initial intent validation to its clean clone and fetched tag</name>
  <files>scripts/quality/New-ReleaseIntent.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - A clone-scoped policy and fetched initial tag whose peel equals SourceSha produce a valid initial intent.
    - A missing clone tag, a tag peeling to another SHA, an ambient policy/ref substitution, or a policy/ref mismatch fails closed with the release-ref rule before any provider work.
    - Existing correction and historical-source rejection semantics remain unchanged.
  </behavior>
  <action>Write RED tests for the r10 clean-clone condition first. Make the intent builder and shared qualification helper accept and validate an explicit source-root/policy-path/release-ref context from the clean clone; resolve the exact tag in that clone only after the caller has fetched it, and require its peel and the clone HEAD to equal the bound source SHA. Do not loosen REL01-REF, infer a tag from ambient refs, or use a workspace checkout as a fallback. Keep the repair generic over the policy-selected initial tag so the same code will qualify r11 later, but do not advance any current-attempt policy in this plan.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1</automated></verify>
  <done>Initial intent creation succeeds only with one explicit clone-local policy/ref/tag/SHA binding and rejects all missing or substituted ref variants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reproduce and close the real clean-clone PrepareAttempt ref seam</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-Phase08PrepareHistorySchema.ps1</files>
  <behavior>
    - A disposable clean clone with a locally fetched dedicated test tag reaches the real PrepareAttempt fixture beyond the initial-ref check and reaches its expected pre-provider sentinel/locator state.
    - Removing the fetched tag or changing its peel causes a deterministic release-ref failure before locator/provider/credential/network behavior.
    - The immutable r8 and r9 strict-history regressions remain green.
  </behavior>
  <action>After RED, thread the clone-local policy/ref binding through the actual HostedRun PrepareAttempt call site. Extend the FixtureOnly qualification harness to create a disposable repository/clean clone, fetch and verify the policy-selected dedicated tag into that clone, and invoke InitializeBoundary then PrepareAttempt through the real script path. Make the positive assertion prove the historical reader and initial-ref binding have been crossed before any provider, credential, hosted, or network operation; make tag-absent and tag-peel-drift fixtures fail before those effects. This fixture is the release gate for the bug that ended r10 and must run before any future r11 tag is created. Preserve r10 as evidence only; do not rerun, move, or change its tag or terminal record.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1</automated></verify>
  <done>The exact clean-clone PrepareAttempt regression catches a missing or mismatched immutable tag before r11 exists and passes only with the clone-local verified tag binding.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| clean clone to intent builder | The clone’s policy, HEAD, and fetched tag must be one identity. |
| immutable r10 history to repair | Historical r10 facts are evidence, never a current ref or retry root. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-26-01 | Tampering | clone policy/tag/SHA | critical | mitigate | Explicit path/ref inputs and clone-local peel equality tests. |
| T-08-26-02 | Spoofing | ambient Git refs | high | mitigate | Reject absent/local-mismatched tag and prohibit workspace fallback. |
| T-08-26-03 | Repudiation | r10 failure | high | mitigate | Preserve exact r10 tag object, peel, REL01-REF disposition, and zero-effect facts. |
</threat_model>
<verification>Run intent, FixtureOnly PrepareAttempt, strict-history, PowerShell parse, and diff checks; confirm no tag, push, dispatch, secret, or registry operation occurred.</verification>
<success_criteria>The actual pre-tag PrepareAttempt fixture proves a dedicated clone-local tag binding, eliminating the precise r10 REL01-REF failure without weakening immutable-tag validation.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-26-SUMMARY.md` when done.</output>
