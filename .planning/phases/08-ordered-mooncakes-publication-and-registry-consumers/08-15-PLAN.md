---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "15"
type: execute
wave: 15
depends_on: ["08-14"]
files_modified: []
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Exact r5-capable HEAD is pushed before r5 tag; remote/peel/LF clone/locator bind one SHA."
    - "GUID-unique ExecutionRoot/StateRoot reuse no prior state or fixed handoff."
    - "Five terminal histories/set bind fresh r5 prepare, packet, receipt, authority, handoff."
    - "Exact auto-completes packet/receipt-free; absent returns non-auto-approvable dynamic human-action literal authorize-core|stop; no PublishOne."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r5"
      provides: "Annotated r5 boundary"
    - path: "%TEMP%/mnf-phase08-r5-handoff.json"
      provides: "Eligible r5 AuthorityUnion"
  key_links:
    - from: "five histories"
      to: "r5 union"
      via: "individual/set digests"
      pattern: "historical_r4_sha256"
    - from: "dynamic checkpoint"
      to: "receipt"
      via: "same-turn user literal plus fresh revalidation"
      pattern: "untrusted_recovery_inputs"
---

<objective>Establish LF-clean r5 boundary and non-mutating five-history AuthorityUnion.

Purpose: Cross the next live boundary after r4 clean-snapshot failure without publishing.
Output: r5 tag/clone/state, exclusive authority, optional receipt, fixed handoff.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/resolved/phase08-clean-tracked-snapshot.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-14-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fail-close five histories and empty r5 namespace</name>
  <files>None (read-only local/GitHub/registry/ref/run/temp checks)</files>
  <action>Require 08-13/14 summaries and owned paths committed/clean. Verify attempt-zero/r1/r2/r3 and remote annotated r4 peeled to ee4a8eb9b8dca5d69b404c9a4a1cd81608a5462a. Bind r4 run 29667231047/1 failure in credential-free qualification at clean empty Before binding, with PublisherDryRun/packet/receipt/handoff/mutation zero. Verify r5 absent and `%TEMP%/mnf-phase08-r5-handoff.json` absent. Preserve and stop on any stale/collision; never override/delete/reuse.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r5-handoff.json'; if(Test-Path -LiteralPath $h){throw 'P08-R5-HANDOFF-COLLISION'}; if(@(git ls-remote --tags origin refs/tags/modules-v0.1.0-r5 'refs/tags/modules-v0.1.0-r5^{}').Count -ne 0){throw 'P08-R5-EXISTS'}; $r4=@(git ls-remote --tags origin 'refs/tags/modules-v0.1.0-r4^{}'); if($r4.Count -ne 1 -or (@($r4[0]-split '\s+'))[0] -cne 'ee4a8eb9b8dca5d69b404c9a4a1cd81608a5462a'){throw 'P08-R4-HISTORY'} }"</automated></verify>
  <done>Five histories intact; r5/fixed handoff absent.</done>
</task>

<task type="auto">
  <name>Task 2: Create LF r5 boundary and eligible dynamic AuthorityUnion</name>
  <files>None (exact push/tag, unique state, sanitized artifacts, optional receipt/handoff)</files>
  <action>Push exact committed r5 implementation/planning HEAD and require remote equality. Create GUID roots; clone `git -c core.autocrlf=false clone --no-tags`, detach exact SHA, verify LF/clean/blobs/empty state. Initialize UTC locator, create/push annotated r5 without force, verify peel, PrepareAttempt with five histories/set and fresh root/genesis/prepared/index/store.

ObserveOnly core. Exact runs structured observation+cold four-target/native proof, indexes, selects packet/receipt-free exact union, then writes handoff. Absent alone runs HostedPreflight/PublisherDryRun, proving equal-empty clean snapshots and exact14 receipt parity, then packet and structured `dynamic checkpoint:human-action`. `autonomous`, auto mode/advance, prior approval, login/push permission, defaults, agents cannot approve/infer/synthesize. Only the user's same-checkpoint-turn verbatim case-sensitive `authorize-core` resumes; literal `stop` terminates without receipt/handoff. Display untrusted fields: ExecutionRoot, StateRoot, BoundaryLocatorPath, BoundaryLocatorSha256, ActiveAttemptPath, ActiveAttemptFileSha256, ActiveAttemptLocatorSha256, ArtifactRoot, PacketPath, PacketSha256, ReleaseRef, BoundarySha, SourceSha, AttemptZeroHistorySha256, R1HistorySha256, R2HistorySha256, R3HistorySha256, R4HistorySha256, HistoricalAttemptSetSha256, PreparedManifestSha256, RootIntentSha256, IntentSha256.

Fresh authorize continuation reloads locator/active/packet/five histories/prepared/root/current/preflight/dry actor/new absence and revalidates schemas/digests/set/paths/cross-bindings, clean LF HEAD/blobs, remote r5 peel, histories, clean snapshot equality, actor tchivs, exact14 propagation, dry success/credential removal, fresh absent. Any drift stops. Only then persist/reload receipt, update/reload active, validate union, first-write handoff. No PublishOne. Record all in summary.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r5-handoff.json'; if(-not(Test-Path -LiteralPath $p -PathType Leaf)){throw 'P08-R5-HANDOFF-MISSING'}; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; $x=[string]$h.execution_root; $s=Join-Path $x 'release/qualification/phase-08-handoff-schema.json'; if(-not((Get-Content -LiteralPath $p -Raw)|Test-Json -SchemaFile $s -ErrorAction Stop)){throw 'P08-R5-HANDOFF-SCHEMA'}; &amp; (Join-Path $x 'scripts/quality/Test-Phase08Qualification.ps1') -Handoff -HandoffPath $p }"</automated></verify>
  <done>One eligible five-history r5 union exists; mutation has literal receipt, exact none, zero publication.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Ambient to LF r5 clone | Exact pushed bytes only. |
| Five histories to r5 state | Evidence only. |
| Checkpoint to fresh continuation | Untrusted until reload. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-15-01 | Spoofing | clone/tag | critical | mitigate | Remote/peel/blob/LF equality. |
| T-08-15-02 | Tampering | histories/state | critical | mitigate | Five digests/set, unique roots. |
| T-08-15-03 | Elevation of Privilege | checkpoint | critical | mitigate | Same-turn verbatim user literal only, then full reload. |
| T-08-15-04 | Tampering | clean snapshot | high | mitigate | Equal-empty revalidated; nonempty drift stops. |
| T-08-15-05 | Information Disclosure | checkpoint | high | mitigate | Sanitized fields only. |
</threat_model>

<verification>Validate five histories, r5 LF boundary, complete checkpoint, fresh continuation, receipt union, handoff, no PublishOne.</verification>
<success_criteria>08-15 ends only with valid r5 exact or literal-receipt mutation union.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-15-SUMMARY.md` when done.</output>
