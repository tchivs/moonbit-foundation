---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "18"
type: execute
wave: 18
depends_on: ["08-17"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
  - .gitattributes
  - scripts/quality/Test-CrossPlatformReleaseArchive.ps1
  - scripts/quality/Invoke-Phase08R7PreLive.ps1
  - scripts/quality/Test-Phase08R7PreLive.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher, workflow, and hosted controller accept only r7 and bind all seven histories plus their ordered set (D-02, D-05, D-07)."
    - "Every workflow env mapping has unique keys and the exact 14-field controller/workflow/propagation receipt parity remains mechanical."
    - "Committed LF checkout policy and dual-core.autocrlf clean clones produce byte-identical package ZIPs; snapshot, UTC, no-tags, and LibraryOnly isolation remain fail-closed."
    - "The read-only pre-live selector preserves rootless attempt-zero and remote-tag authority, validates seven histories and r6's hosted prepare failure, and requires r7 tag/fixed handoff absence before any side effect."
    - "No task pushes, tags, dispatches, accesses a secret, creates the fixed r7 handoff, or calls PublishOne."
  artifacts:
    - path: ".github/workflows/publish-modules.yml"
      provides: "r7 exact14 receipt-aware workflow with unique env keys"
    - path: "scripts/quality/Test-CrossPlatformReleaseArchive.ps1"
      provides: "Cross-policy dual-clone byte-identical archive regression"
    - path: "scripts/quality/Invoke-Phase08R7PreLive.ps1"
      provides: "Executable read-only seven-history r7 pre-live gate"
  key_links:
    - from: "seven individually validated histories"
      to: "historical_attempts_sha256"
      via: "canonical LF-ordered-set recomputation"
      pattern: "historical_r6_sha256"
    - from: "controller 14 fields"
      to: "workflow declarations and job propagation"
      via: "exact name, order, and count equality"
      pattern: "authorization_receipt_sha256"
---

<objective>Wire r7 through the publisher, workflow, hosted controller, cross-platform archive regression, and zero-write pre-live selector.

Purpose: Make the r7 HostedPreflight seam structurally dispatchable after the r6 cross-platform failure without performing any live action.
Output: r7 hosted implementation, credential-free adversarial tests, and mandatory read-only pre-live gate.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-prepared-zip.md
@.planning/debug/phase08-prelive-attempt-zero-root.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-17-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce r7 and seven histories in publisher and workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - r7 request binds current initial root/prepared state and all seven histories/set.
    - r6-current, fake successful r6 prepare, history drift, correction substitution, and second mutation fail before credentials.
    - Workflow env mappings have unique keys and exact14 packet/receipt propagation remains closed.
  </behavior>
  <action>Per D-02, D-03, D-05, D-07, and D-14, advance publisher, one-module adapter, and workflow from r6 to r7 and from six to seven histories. Preserve one-module maximum, isolated ephemeral MOON_HOME, pinned MOON_TOOLCHAIN_ROOT, actor parsing, raw-output rejection, secret scoping, and exact14 controller/workflow packet-plus-receipt parity. Require structural uniqueness for every workflow env mapping. Reject old-current requests, fabricated r6 downstream success, missing/reordered histories, set mismatch, correction substitution, second mutation, and field-parity drift before credential materialization.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Publisher and workflow accept only r7, carry seven histories, and expose unique exact14 mappings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve hosted exact14, snapshot, UTC, LF, no-tags, and isolation protections</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1, scripts/quality/Test-ReleaseQualificationNegative.ps1</files>
  <behavior>
    - Equal empty Before/After snapshots pass and unequal nonempty ordinal snapshots fail tracked-mutation validation.
    - Controller/workflow retain exactly 14 names in the same order for start-empty and resume-valid propagation.
    - Seven-history identity retains r6's failed prepare job and zero downstream effects without fabricating authority.
    - UTC/LF/--no-tags and LibraryOnly GUID fixtures never touch the production r7 handoff.
  </behavior>
  <action>Per D-03, D-04, D-07, D-14, and D-18, extend hosted state to r7 and seven histories while retaining r6's exact run/job/failure/downstream-zero form. Preserve exact14 count/order/names/propagation, cardinality, missing-receipt, ungrouped, collapse, vector-drift, and duplicate-key negatives. Preserve canonical UTC offset/reload/tamper/stale detection, LF bytes, `--no-tags` cloning, clean snapshot equality/drift, and LibraryOnly GUID roots. The production `%TEMP%/mnf-phase08-r7-handoff.json` is non-overridable; tests assert it is absent before and after and clean only owned paths.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseQualificationNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -HostedFieldsOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r7 passes exact14, seven-history, snapshot, UTC, LF, no-tags, unique-key, and isolation regressions with zero live effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve cross-platform archive identity and add the zero-write r7 pre-live selector</name>
  <files>.gitattributes, scripts/quality/Test-CrossPlatformReleaseArchive.ps1, scripts/quality/Invoke-Phase08R7PreLive.ps1, scripts/quality/Test-Phase08R7PreLive.ps1</files>
  <behavior>
    - Opposing core.autocrlf clean clones package byte-identical ZIPs with identical entry order, metadata, and payloads under the committed LF policy.
    - Rootless attempt-zero is validated only by immutable tag/run/artifact evidence; r1 through r6 retain their exact persisted root/locator/index/store requirements.
    - Historical tag objects and peels come from one fail-closed read-only remote-tag query, never local tag availability or remote-tracking refs.
    - Selector validates seven refs/digests/set, exact r6 run/job/failure/downstream-zero facts, committed-clean 08-17/18 ownership, and fixed r7 tag/handoff absence without writing.
  </behavior>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, retain `.gitattributes` `text=auto eol=lf` and the dual-policy clean-clone/package byte-identity regression; do not weaken it to manifest-only or decompressed semantic equality. Add `Invoke-Phase08R7PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin` by extending the r6 selector to seven histories. Preserve the rootless attempt-zero exception backed by its canonical immutable terminal artifact, strict persisted path containment/digests for r1-r6, and one injected-or-production `git ls-remote --tags` authority parser with missing/duplicate/peel/source/tag drift rejection. Mechanically bind r6 source/tag/run/attempt/prepare-job/error, mismatched local/hosted prepared intent/archive evidence, skipped downstream jobs, and zero packet/receipt/handoff/PublishOne/mutation/successor effects. Require the complete 08-17/18 owned file sets and summaries committed-clean with ancestor commits; require remote/local r7 tag and `%TEMP%/mnf-phase08-r7-handoff.json` absent. Emit exactly one closed sanitized JSON object and perform no output write or state mutation. Add bare-remote/GUID fixtures for every historical, cross-platform, path, summary, ref, handoff, and output-write negative plus the positive case.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-CrossPlatformReleaseArchive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08R7PreLive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>The r7 selector proves the entire immutable pre-live boundary read-only and the committed LF policy yields byte-identical cross-platform prepared archives.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Windows/Linux checkout to prepared bundle | EOL policy must make archive bytes platform-independent before intent hashing. |
| Controller to workflow parser | Duplicate env keys or field drift must not shadow authority. |
| Remote refs and historical filesystem to live execution | Selector proves exact immutable evidence without writing or trusting local tags. |
| Static tests to production state | Fixtures cannot touch fixed r7 handoff, tags, roots, or external state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-18-01 | Tampering | prepared ZIP bytes | critical | mitigate | Committed LF policy and dual-policy byte/entry regression. |
| T-08-18-02 | Tampering | exact14/env mappings | critical | mitigate | Exact declaration/propagation equality and unique-key tests. |
| T-08-18-03 | Spoofing | history refs/set | critical | mitigate | Remote tag parsing plus seven record/set recomputation. |
| T-08-18-04 | Elevation of Privilege | r6 failed run | critical | mitigate | Reject fabricated downstream artifact, packet, receipt, or authority. |
| T-08-18-05 | Information Disclosure | hosted evidence | high | mitigate | Credential isolation and raw-output rejection. |
| T-08-18-06 | Tampering | pre-live boundary | critical | mitigate | Zero-write selector validates refs, roots, stores, summaries, and r7 absence. |
</threat_model>

<verification>Run publisher, hosted, qualification, observation, cross-platform archive, and r7 pre-live suites; confirm seven histories, exact r6 failure, LF byte identity, unique env keys, exact14, snapshot/UTC/no-tags/isolation, rootless attempt-zero, remote tag authority, r7 absence, and no external effects.</verification>
<success_criteria>The r7 static hosted seam is ready for one clean non-publishing live preflight and later explicitly authorized resume.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-18-SUMMARY.md` when done.</output>
