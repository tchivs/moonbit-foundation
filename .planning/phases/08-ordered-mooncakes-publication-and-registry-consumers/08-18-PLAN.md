---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "18"
type: execute
wave: 18
depends_on: ["08-17"]
files_modified:
  - release/qualification/phase-08-requirements.json
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, PROV-05]
must_haves:
  truths:
    - "Only the fixed valid r6 AuthorityUnion starts closure: absent mutation authority has packet plus literal receipt, exact-existing has neither, and stop is ineligible (D-04, D-07, D-15)."
    - "Every live artifact binds r6, all six terminal histories, and their canonical ordered set."
    - "Core, color, and image resolve strictly in order with at most one module mutation per hosted run and no exact-version republish (D-05, D-06, D-14)."
    - "Each module authority binds exact structured metadata, archive, downloaded manifest, checksum, graph, four-target/native behavior, and predecessor proof from a credential-free cold consumer (D-08 through D-18)."
    - "Reciprocal tracked closure proves DIST-01 through DIST-04 and PROV-05 offline without entering Phase 9 scope."
  artifacts:
    - path: "%TEMP%/mnf-phase08-r6-handoff.json"
      provides: "Eligible six-history r6 union"
    - path: "<r6-artifact-root>/authorities/mb-core/*.json"
      provides: "Exact core authority and cold proof"
    - path: "<r6-artifact-root>/authorities/mb-color/*.json"
      provides: "Exact color/core authority and cold proof"
    - path: "<r6-artifact-root>/authorities/mb-image/*.json"
      provides: "Exact full-graph authority and bounded PPM proof"
    - path: "release/qualification/phase-08-requirements.json"
      provides: "Sanitized reciprocal Phase 8 closure"
  key_links:
    - from: "r6 AuthorityUnion"
      to: "core resolution"
      via: "receipt-or-exact replay plus six-history validation"
      pattern: "modules-v0.1.0-r6"
    - from: "core and color authorities"
      to: "successor eligibility"
      via: "required predecessor authority and cold proof"
      pattern: "PriorAuthorityRecordPath"
---

<objective>Complete ordered r6 publication or exact recovery, cold registry proofs, structured metadata proof, and reciprocal Phase 8 closure.

Purpose: Turn the non-mutating r6 handoff into three dependency-ordered exact public authorities without automatic retry or republish.
Output: Core, color, and image authorities plus tracked offline DIST-01..04 and PROV-05 closure.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-17-SUMMARY.md
@policy/phase-08-distribution.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate the r6 union and resolve core then color</name>
  <files>None (sanitized r6 live artifacts under the handoff ArtifactRoot)</files>
  <action>Per D-04 through D-19, load only `%TEMP%/mnf-phase08-r6-handoff.json` and validate its closed schema, every bound file/digest/path, clean LF detached execution root, unique state root, remote r6 source/tag identity, six histories/set, prepared/root/current identity, and exclusive union. Mutation authority requires packet plus the literal receipt from 08-17; exact-existing forbids both. For core, consume exact-existing authority directly. For mutation authority, re-observe first: only a fresh exact `absent` result permits exactly one separate PublishOne hosted run; exact skips mutation; timeout, mismatch, unknown, conflicting surface, run ambiguity, nonzero exit, or missing receipt stops without rerun. After publication or exact recovery, use bounded structured observation and a new outside-checkout empty-MOON_HOME credential-free consumer to prove the exact one-node core graph, metadata/archive/downloaded-manifest/checksum/docs agreement, pinned toolchain, js/wasm/wasm-gc/native, and checked behavior. Index and select the core authority. Then require that exact core authority as predecessor and resolve color through the same branch switch: color absent permits one separate PublishOne run, exact performs none, and ambiguity stops. Prove the exact color-to-core graph, metadata/artifact agreement, four targets, and public color behavior, then run the CoreColorArtifacts selector.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r6-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -CoreColorArtifacts -HandoffPath $p }"</automated></verify>
  <done>r6 core and color exact authorities and four-target cold proofs exist in strict predecessor order with no exact republish.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve image and prove the full exact public graph</name>
  <files>None (sanitized r6 live artifacts under the handoff ArtifactRoot)</files>
  <action>Per D-05 through D-18, reload the fixed handoff, six histories/set, and validated CoreColorArtifacts from persisted paths. Require the color authority and its core predecessor before image eligibility. Re-observe image: absent permits exactly one new one-module PublishOne hosted run, exact-existing performs zero mutation, and mismatch/unknown/timeout/nonzero/conflicting/ambiguous results stop without rerun. Run a newly created outside-checkout consumer with an empty exported MOON_HOME, explicit pinned MOON_TOOLCHAIN_ROOT, no credentials, workspace, path/Git dependency, copied source, registry cache, `.mooncakes`, or target state. Require the exact `mb-image@0.1.0 -> mb-color@0.1.0 -> mb-core@0.1.0` normalized graph; structured API/index/archive/downloaded manifests/checksums/assets/docs agreement; js, wasm, wasm-gc, and real native results; and bounded PPM encode/decode behavior. Index, select, and validate the LiveArtifacts set before any closure write.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r6-handoff.json'; $h=Get-Content -LiteralPath $p -Raw|ConvertFrom-Json -Depth 100; &amp; (Join-Path ([string]$h.execution_root) 'scripts/quality/Test-Phase08Qualification.ps1') -LiveArtifacts -HandoffPath $p }"</automated></verify>
  <done>Image exact authority, full three-node graph, qualified metadata, four targets, and bounded PPM behavior pass from a cold unauthenticated registry-only consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist reciprocal r6 closure and Required evidence</name>
  <files>release/qualification/phase-08-requirements.json, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Invoke-MoonQuality.ps1, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/COVERAGE.md</files>
  <behavior>
    - Reciprocal closure binds r6, six individual histories/set, union/optional receipt, and all three exact authorities.
    - Exact-existing branches prove zero mutation; each absent branch proves exactly one eligible one-module hosted run and no automatic retry.
    - Required preserves duplicate-key uniqueness, exact14 parity, clean snapshot, UTC, LF, no-tags, and fixed-handoff isolation offline.
  </behavior>
  <action>Per D-05 through D-18, run ReciprocalArtifacts and replay the union, reducer, predecessor chain, six histories/set, optional receipt, hosted-run identities, public observations, cold graphs, target results, and sanitized content digests. Copy only the closed sanitized projection into tracked `release/qualification/phase-08-requirements.json`; never copy raw network, actor, credential, or secret output. Extend tracked qualification and Required gates for r6 and six histories while retaining unique workflow env-key validation, exact14 receipt parity, equal-empty/nonempty-drift, UTC, LF, no-tags, fixed-handoff isolation, and all existing adversarial cases. Update final COVERAGE to **18/18 executed**, r6, six immutable histories, the exact r5 pre-run/no-run terminal record, requirement adjacency, D-01 through D-19, and all five phase requirements. Exclude immutable ledger, provenance/release closure, and milestone audit owned by Phase 9.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -TrackedClosure; if ($LASTEXITCODE) { exit $LASTEXITCODE }; ./scripts/quality/Invoke-MoonQuality.ps1 -Required; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All Phase 8 requirements have reciprocal r6 evidence and credential-free offline Required passes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r6 union to registry mutation | Receipt-or-exact replay governs the only eligible core branch. |
| Module authority to successor | Exact predecessor observation and cold proof gate each successor. |
| Live evidence to tracked closure | Only closed sanitized projections may cross. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-18-01 | Elevation of Privilege | AuthorityUnion | critical | mitigate | Literal receipt required for absent core; exact branch forbids packet and receipt. |
| T-08-18-02 | Tampering | six-history chain | critical | mitigate | Digest/set/predecessor replay before every live selector. |
| T-08-18-03 | Spoofing | exact-existing registry state | critical | mitigate | Structured surface, archive, manifest, checksum, graph, and cold behavior agreement. |
| T-08-18-04 | Information Disclosure | tracked closure | high | mitigate | Closed sanitized offline projection and raw-output rejection. |
| T-08-18-05 | Denial of Service | propagation ambiguity | medium | mitigate | Bounded observation stops without rerun or republish. |
</threat_model>

<verification>Validate the r6 union, six histories, ordered one-module authority transitions, structured metadata, cold exact graphs and behavior, reciprocal tracked closure, and offline Required.</verification>
<success_criteria>All three modules are published or exactly checkpointed without republish, strict predecessor proofs pass, and DIST-01..04 plus PROV-05 have r6 reciprocal evidence.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-18-SUMMARY.md` when done.</output>
