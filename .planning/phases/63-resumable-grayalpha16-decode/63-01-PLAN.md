---
phase: 63-resumable-grayalpha16-decode
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_decode_test.mbt
autonomous: true
requirements:
  - GRA16DEC-02
must_haves:
  truths:
    - "A caller can create PngChunkDecoder::new_graya16, pass a legal declaration-free or sRGB Type-4/16 PNG through empty, one-byte, and ragged schedules, and obtain the exact result of a fresh PngDecoder::decode_graya16 peer only after finish succeeds."
    - "Each active push reports exactly the bytes accepted from the caller-owned view; a result remains unavailable during push, including after complete IEND input, and finish is the sole successful transfer boundary."
    - "An incomplete, malformed, profile-metadata-rejected, or input-limit-rejected explicit chunk stream reports its first typed error atomically and rejects later pushes and finish with the same sticky terminal error."
    - "The existing generic PngChunkDecoder::new route continues returning its frozen RGBA8 high-byte result for the Type-4/16 literal."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: The sole public PngChunkDecoder::new_graya16 selector that constructs the existing PngDecodeMachine with the private GrayAlpha16 profile.
      contains: "PngChunkDecoder::new_graya16"
    - path: modules/mb-image/png/stream_decode_test.mbt
      provides: Public explicit-profile schedule, eager-parity, accepted-progress, terminal-error, and generic-compatibility regressions.
      contains: "PngChunkDecoder::new_graya16"
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: new_graya16 constructs PngDecodeMachine::new_with_profile with PngDecodeProfile::GrayAlpha16, so the shared chunk facade retains framing, accepted-byte accounting, EOF handling, and terminal transfer.
      pattern: "new_graya16.*new_with_profile"
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: Explicit schedule tests feed caller-owned ByteViews into the new factory and compare its result to a fresh decode_graya16 eager peer.
      pattern: "PngChunkDecoder::new_graya16"
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: Public tests exercise existing push and finish behavior to prove accepted-only progress, EOF-only result transfer, and sticky failures without a second state machine.
      pattern: "\.push\(|\.finish\("
---

<objective>
Deliver the explicit GrayAlpha16 preservation profile through the established caller-owned PNG chunk lifecycle as one TDD vertical slice.

Purpose: make the already-proven eager Type-4/16 result usable by resumable callers without changing the parser, output representation, or generic compatibility contract.
Output: one public new_graya16 chunk selector and focused public schedule/terminal regressions that use the existing machine.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-resumable-grayalpha16-decode/63-CONTEXT.md
@.planning/phases/63-resumable-grayalpha16-decode/63-RESEARCH.md
@.planning/phases/62-explicit-grayalpha16-decode-contract/62-01-SUMMARY.md
@.planning/phases/62-explicit-grayalpha16-decode-contract/62-VERIFICATION.md
@.planning/research/v020-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/stream_decode_test.mbt
@modules/mb-image/png/png_test.mbt

<interfaces>
From modules/mb-image/png/png.mbt:
- `PngDecoder::decode_graya16(reader, options, limits, budget, diagnostics) -> Result[@codec.DecodeResult, @error.CoreError]` selects `PngDecodeProfile::GrayAlpha16` through `PngDecodeMachine::new_with_profile`.
- `PngChunkDecoder::new(limits, budget, diagnostics) -> PngChunkDecoder` currently creates the shared machine with strict decode options, starts `consumed_total` at zero, and is the frozen generic route.
- `PngChunkDecoder::push(source) -> PngChunkPushResult` exposes only `consumed()` and `outcome()`; `PngChunkDecoder::finish() -> Result[@codec.DecodeResult, @error.CoreError]` is the result-transfer operation.

From modules/mb-image/png/stream_decode.mbt:
- `PngDecodeMachine::new_with_profile(PngDecodeProfile::GrayAlpha16, options, limits, budget)` is the existing profile-aware private constructor.
- `PngChunkDecoder::push` accepts bytes until the first parser or input-limit failure; its failed and finished states consume zero bytes.
- `PngChunkDecoder::finish` turns `NeedEof` into the sole DecodeResult transfer and otherwise records/replays its first terminal error.

From modules/mb-image/png/stream_decode_test.mbt:
- `png_chunk_public_schedule` is the generic public schedule driver for empty, one-byte, and ragged caller slices.
- `png_chunk_schedule_matches_eager` compares result/error, every remaining budget dimension, and rendered diagnostics against a fresh generic eager peer.
</interfaces>
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Caller-owned chunks produce the same preserved GrayAlpha16 result with the established bounded lifecycle | 63-01 | COVERED | The tracer wires the sole selector to the existing machine and proves eager parity through public schedules. |
| REQ | GRA16DEC-02 | new_graya16 preserves eager-equivalent result, accepted-only progress, atomic failure, and sticky terminal behavior under hostile schedules | 63-01 | COVERED | Task 1 proves successful scheduling and Task 2 proves error and compatibility terminals. |
| RESEARCH | — | Select GrayAlpha16 once through new_with_profile using the strict options of the generic chunk constructor | 63-01 | COVERED | Task 1 adds only the profile-selected factory in png.mbt. |
| RESEARCH | — | Reuse public schedule/error helpers with a fresh explicit eager peer and exact component-byte output | 63-01 | COVERED | Task 1 adds narrow explicit-profile test helpers and compares result, budget, diagnostics, and lanes. |
| RESEARCH | — | Preserve empty push, accepted counts, finish-only transfer, early EOF classification, and sticky replay | 63-01 | COVERED | Tasks 1 and 2 exercise each observable wrapper state through public APIs. |
| RESEARCH | — | Reject incompatible profile metadata before result visibility and retain generic RGBA8 compatibility | 63-01 | COVERED | Task 2 covers the public rejection replay and the frozen generic selector from the same literal. |
| CONTEXT | D-01 | Add only PngChunkDecoder::new_graya16 using the eager profile and DecodeResult shape | 63-01 | COVERED | Task 1 names and implements the sole additive selector. |
| CONTEXT | D-02 | Reuse the one chunk machine's framing, progress, finish, atomic failure, sticky states, and result representation | 63-01 | COVERED | Task 1 calls the existing profile-aware constructor; Task 2 asserts lifecycle behavior instead of adding transport state. |
| CONTEXT | D-03 | Compare hostile schedules with fresh eager peers, including zero/one/ragged, early finish, malformed/metadata rejection, and generic compatibility | 63-01 | COVERED | Tasks 1 and 2 map each named public scenario to focused tests. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Red-green explicit GrayAlpha16 chunk selector and eager-parity schedules</name>
  <reversibility rating="costly">The additive public selector becomes the established resumable entry point for a result contract that Phase 64 qualifies across portable targets.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - RED: public schedule tests invoking PngChunkDecoder::new_graya16 fail because the selector does not exist.
    - GREEN: a fresh explicit chunk decoder accepts a zero-length push, then declaration-free and sRGB Type-4/16 literals through one-byte and fixed ragged `[8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]` schedules; all active turns consume their requested bytes and return NeedInput.
    - GREEN: each successful schedule stays result-free until finish, then matches a separately initialized PngDecoder::decode_graya16 peer in DecodeResult image bytes/metadata, result bytes_read, remaining budget, and diagnostics; unequal wire lanes remain observable as `34,12,c5,a7` and `0f,be,76,5a` in packed component-byte order.
  </behavior>
  <action>Begin in stream_decode_test.mbt by adding a narrow explicit-profile schedule item and sibling public schedule/eager comparison helpers that construct PngChunkDecoder::new_graya16 and PngDecoder::decode_graya16 respectively; retain the generic helpers unchanged because their eager oracle intentionally selects GenericRgba8. Name the public test `PNG graya16 chunk schedules match eager preservation` so the focused verification filter selects it. Use the Phase 62 hand-authored declaration-free and sRGB Type-4/16 literals with fresh caller-owned source allocations for every zero, one-byte, and ragged push. The explicit helper must preserve the existing harness assertions for exact active consumption, source terminal, result/error parity, remaining-budget equality, rendered diagnostics, and push-before-finish result unavailability; add direct component-byte assertions so parity cannot accidentally compare two lossy results. After the RED run, per D-01 add only PngChunkDecoder::new_graya16 beside new in png.mbt. Construct PngDecodeMachine::new_with_profile(PngDecodeProfile::GrayAlpha16, strict DecodeOptions, limits, budget) with the same strict options and field initialization used by new. Per D-02, route this selector through the existing state field so push and finish remain the shared machine behavior. Keep PngChunkDecoder::new constructing PngDecodeMachine::new and leave the existing DecodeResult envelope intact. Run the focused public test while red, implement the selector and test helpers until green, then refactor only duplicated test-local setup if it obscures the two distinct eager peers.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target js --frozen --filter '*graya16 chunk*'</automated>
  </verify>
  <done>PngChunkDecoder::new_graya16 is the sole new public chunk selector and legal default/sRGB Type-4/16 inputs reach the existing GrayAlpha16 profile through zero, one-byte, and ragged caller schedules, returning exact fresh-eager-equivalent DecodeResult data only from finish.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green hostile terminal, atomic rejection, and generic-compatibility evidence</name>
  <files>modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - RED: public new_graya16 tests do not yet prove that an early finish after a partial legal literal produces the established EOF error and that later nonempty push plus repeated finish replay the identical terminal error with zero consumption.
    - GREEN: malformed signature input, a complete profile-incompatible metadata literal, and an input ceiling that rejects the first byte each preserve the first typed error across later push and finish; metadata rejection exposes no DecodeResult and retains the Phase 62 pre-allocation admission boundary.
    - GREEN: the Type-4/16 literal driven through ordinary PngChunkDecoder::new remains the frozen RGBA8 high-byte image (`12,12,12,a7` for its first pixel), independently of the explicit chunk route.
  </behavior>
  <action>Extend stream_decode_test.mbt with public new_graya16 terminal cases before changing any production behavior. Name the test `PNG graya16 chunk terminals retain atomic sticky compatibility` so the focused verification filter selects it. Per D-03, feed a partial legal literal then call finish early; capture its typed UnexpectedEndOfStream-style terminal error and assert a subsequent nonempty caller view consumes zero bytes, reports the identical error shape, and a repeated finish returns that same error. Add hand-authored complete hostile inputs for a malformed parser path and for a Type-4/16 declaration whose colour metadata is rejected by the existing GrayAlpha16 first-IDAT gate; in each case record the first public failure, assert no DecodeResult is returned, and prove later push and finish replay it without admitting bytes. Exercise the existing max-input limit through new_graya16 so the rejected next byte remains unaccepted. Keep the Phase 62 white-box allocation proof as the authority for pre-allocation internals; these public tests prove the wrapper's atomic visibility and terminal contract. Finally, drive the unchanged PngChunkDecoder::new with the asymmetric Type-4/16 literal, finish it, and assert its RGBA8 high-byte channels remain unchanged per D-01 and D-02. Do not introduce a second parser, state holder, source-retention buffer, conversion result, or generic selector change. Run these tests red first, make only test-local additions unless they reveal a defect in the shared lifecycle, and refactor shared assertion code only after the focused suite passes.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target js --frozen --filter '*graya16 chunk*'</automated>
  </verify>
  <done>Early EOF, malformed input, profile-metadata rejection, and input-limit failures through new_graya16 remain atomic and sticky at the public boundary with accepted-only counters; ordinary generic chunk decoding retains its historical RGBA8 high-byte output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-owned ByteView to PngChunkDecoder | Untrusted, mutable-after-return chunks cross into public input accounting and the private PNG framing machine. |
| Authenticated PNG headers/metadata to GrayAlpha16 admission | Type/depth and colour declaration facts determine whether the decoder may allocate and expose the explicit result. |
| NeedEof machine state to DecodeResult | An internal terminal raster outcome becomes externally owned only after the caller explicitly declares EOF. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Tampering | new_graya16 profile selection | high | mitigate | Task 1 directs the sole factory to the existing GrayAlpha16 profile, and Task 2 verifies the profile-gated metadata failure has no public result and stays sticky. |
| T-63-02 | Denial of service | PngChunkDecoder push/finish lifecycle | high | mitigate | Reuse the bounded machine's input ceiling and first-failure state; Task 2 proves rejected bytes are not accepted and replay cannot consume later input. |
| T-63-03 | Information disclosure | caller ByteView and terminal result transfer | medium | mitigate | Task 1 uses fresh caller allocations and proves result transfer occurs only after finish; it relies on the existing owned machine lifecycle rather than retaining a view. |
| T-63-04 | Tampering | generic compatibility path | medium | mitigate | Task 2 asserts the ordinary selector continues selecting its RGBA8 high-byte result for the same asymmetric Type-4/16 wire bytes. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target js --frozen --filter '*graya16 chunk*'`. The focused tests must demonstrate default and sRGB eager equivalence under zero/one/ragged schedules; exact accepted progress and finish-only transfer; sticky early-EOF, malformed, metadata, and limit terminals; and the unchanged ordinary chunk result.
</verification>

<success_criteria>
GRA16DEC-02 is complete when PngChunkDecoder::new_graya16 selects the same private preservation profile as decode_graya16, public hostile schedules preserve exact eager result and existing terminal semantics, and the generic chunk facade remains its frozen compatible RGBA8 route.
</success_criteria>

<output>
Create `.planning/phases/63-resumable-grayalpha16-decode/63-01-SUMMARY.md` when done.
</output>
