---
phase: 102-root-relative-selected-face-admission
plan: 02
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - modules/mb-font/font/collection.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/collection_test.mbt
  - modules/mb-font/font/collection_wbtest.mbt
autonomous: true
requirements:
  - TTC-02
  - TTC-03
must_haves:
  decisions:
    - "D-01 through D-03: FontCollection adds exactly one repeatable non-consuming open_face method returning the existing opaque Font."
    - "D-05 through D-07: selected Font retains the collection root/revision, reparses only its cached directory authority, and ignores unrelated unsupported siblings."
    - "D-08: collection opening and selected-face admission remain separate transactions with fresh limits and caller-owned budgets."
    - "D-09: max_source_bytes bounds the retained root while charge bytes/work cover only selected directory and distinct selected-table extents plus selected semantics."
    - "D-10: selected admission uses cumulative staged preflights and one final aggregate charge without changing standalone helper semantics."
    - "D-11 through D-13: revision, index, profile, authority, wire facts, checksums, budget, final revision, charge, and publication have one stable precedence."
    - "D-14/D-15: collection table checksums remain strict and every existing Font query is container-neutral."
  truths:
    - "TTC-02 / D-01 through D-03: any in-range StaticGlyf face can be opened repeatedly through exactly `FontCollection::open_face(index, FontLimits, Budget)` and each success is the existing opaque Font under an independent caller-owned transaction."
    - "TTC-03 / D-04 through D-07: selected tables before or after a non-zero face directory resolve against collection byte zero, exact shared sibling ranges remain usable, and unsupported siblings do not poison a supported selection."
    - "D-05/D-12: every selected Font retains the original collection root and collection opening revision; mutation before/during selection or after publication fails State, including mutate-then-restore."
    - "D-08 through D-10: max_source_bytes bounds retained root length, selected charge bytes equal directory extent plus selected table lengths, unrelated sibling payloads are unscanned/uncharged, and failures commit no budget."
    - "D-11 through D-14: invalid index, unsupported selected profile, malformed selected bytes/checksums, resource exhaustion, and revision drift return the established distinct CoreError facts in frozen order."
  prohibitions:
    - "No CollectionFace, collection-specific metric/query API, public source/directory/range handle, selected-face cache, consuming selection, sibling semantic rescan, or fresh revision identity may be added."
    - "No selected path may copy the collection, synthesize a standalone SFNT, rebase table-record offsets, disable per-table checksums, or apply the standalone whole-root adjustment rule."
    - "No failed selection may charge any Budget dimension or publish a partial Font."
    - "No CFF/CFF2 or variable execution, WOFF/WOFF2, DSIG trust, eager sibling admission, extraction/materialization, authoring, shaping, bidi, hinting, rasterization, FFI, ambient I/O, new module, or new dependency may be added."
    - "Standalone Font::open bytes, signature, errors, ordering, checksum behavior, and exact charges must remain unchanged."
  artifacts:
    - path: modules/mb-font/font/collection.mbt
      provides: "The single public open_face facade, revision/index/profile gates, and deterministic final-revision test seam."
    - path: modules/mb-font/font/font.mbt
      provides: "Shared private selected admission/publication path constructing the existing Font from the retained collection root."
    - path: modules/mb-font/font/tables.mbt
      provides: "Selected extent/work facts and exact one-charge collection plan."
    - path: modules/mb-font/font/collection_test.mbt
      provides: "Public root-relative, sharing, mixed-profile, repeatability, checksum, charge, and mutation evidence."
    - path: modules/mb-font/font/collection_wbtest.mbt
      provides: "Private selected arithmetic, root/revision identity, and deterministic mid-selection hook evidence."
  key_links:
    - from: modules/mb-font/font/collection.mbt
      to: modules/mb-font/font/collection_parser.mbt
      via: "open_face selects cached CollectionFaceFacts after revision/index/profile gates without rescanning siblings."
      pattern: "CollectionFaceFacts"
    - from: modules/mb-font/font/collection.mbt
      to: modules/mb-font/font/font.mbt
      via: "The facade passes retained root, collection opening revision, cached directory facts, fresh FontLimits, and caller Budget to the shared selected admission seam."
      pattern: "open_face"
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/directory.mbt
      via: "Selected admission invokes the offset-aware parser in collection checksum mode."
      pattern: "font_parse_directory"
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/tables.mbt
      via: "Shared semantic admission uses the deferred ledger and returned exact selected charge before constructing Font."
      pattern: "font_admission_charge"
    - from: modules/mb-font/font/collection_test.mbt
      to: modules/mb-font/font/collection.mbt
      via: "Black-box tests select faces and compare only existing public Font behavior."
      pattern: "open_face"
---

<objective>
Publish one no-copy, root-relative selected-face transaction from an admitted `FontCollection` into the existing opaque `Font`.

Purpose: Complete the Phase 102 user capability over cached Phase 101 authority while preserving exact sharing, mutation identity, collection checksum semantics, selected-only accounting, and atomic publication.
Output: `FontCollection::open_face`, shared selected admission, exact root-backed Font publication, and focused public/private tests for TTC-02/TTC-03 and D-01 through D-15.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
@.planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
@.planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
@.planning/phases/102-root-relative-selected-face-admission/102-01-SUMMARY.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-VERIFICATION.md
@.planning/debug/resolved/ttc-work-precedence-order.md
@modules/mb-font/font/collection.mbt
@modules/mb-font/font/collection_parser.mbt
@modules/mb-font/font/directory.mbt
@modules/mb-font/font/font.mbt
@modules/mb-font/font/tables.mbt
@modules/mb-font/font/collection_test.mbt
@modules/mb-font/font/collection_wbtest.mbt
@modules/mb-core/budget/budget.mbt
@modules/mb-core/bytes/views.mbt

<interfaces>
Create exactly:
- `pub fn FontCollection::open_face(self : FontCollection, index : UInt64, limits : FontLimits, budget : @budget.Budget) -> Result[Font, @error.CoreError]`
- generated spelling `pub fn FontCollection::open_face(Self, UInt64, FontLimits, @budget.Budget) -> Result[Font, @error.CoreError]`

Reuse:
- `CollectionFaceFacts { directory_start, directory_end, table_count, sfnt_version, profile }`
- `FontCollection::{source, opening_revision, faces, require_revision}`
- the Plan 102-01 offset/checksum/ledger modes
- the exact existing `Font` fields and query surface

Add only a private `open_face_after_admit(..., before_final_guard : () -> Unit)`-style deterministic test seam; no hook is public.
</interfaces>
</context>

## Artifacts this plan produces

| Kind | Exact artifact | Contract |
|---|---|---|
| New public method | `FontCollection::open_face` | Non-consuming repeatable selected-face admission returning existing `Font`. |
| Modified file | `modules/mb-font/font/collection.mbt` | Revision/index/profile facade and atomic selected transaction. |
| Modified file | `modules/mb-font/font/font.mbt` | Shared semantic selected admission and exact existing Font construction. |
| Modified file | `modules/mb-font/font/tables.mbt` | `D + T`, collection directory work, exact work, and one final ResourceCharge. |
| Modified file | `modules/mb-font/font/collection_test.mbt` | Public selected-face behavior and hostile focused cases. |
| Modified file | `modules/mb-font/font/collection_wbtest.mbt` | Private accounting, mutation-hook, and retained identity cases. |

`modules/mb-font/font/pkg.generated.mbti` may be regenerated by later policy verification but is never a tracked artifact.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Open one cached static face through the retained collection root</name>
  <reversibility rating="costly">D-01 fixes one additive public method and its exact signature; later renaming changes downstream call sites and the generated interface.</reversibility>
  <files>modules/mb-font/font/collection.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/collection_test.mbt</files>
  <read_first>
    - .planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
    - .planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
    - .planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
    - .planning/phases/102-root-relative-selected-face-admission/102-01-SUMMARY.md
    - modules/mb-font/font/collection.mbt
    - modules/mb-font/font/collection_parser.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-core/bytes/views.mbt
  </read_first>
  <behavior>
    - "open_face(0) and the last in-range StaticGlyf index return the existing Font and can be called again with independent limits/budgets."
    - "Revision drift precedes index; index precedes cached profile; profile rejection occurs before any selected directory or table admission."
    - "A supported selected face can place legal tables before and after its non-zero directory and expose existing units_per_em, bounds, line metrics, glyph mapping, horizontal metrics, kerning, and outline queries."
    - "The returned Font retains the collection root ByteView and the collection opening revision rather than a directory subview or fresh revision."
    - "Only the selected directory is reparsed into semantic DirectoryFacts; unrelated sibling directories and payloads are not semantically admitted."
  </behavior>
  <action>
Start with a public test that admits a collection, selects one valid static `glyf` face whose real tables straddle a non-zero directory, and exercises representative existing `Font` queries. Add exactly the D-01 method with the established receiver spelling. Implement its facade in the D-11 order: `require_revision("font-collection-open-face")`, UInt64 index range and checked narrowing, cached profile gate accepting only `StaticGlyf`, then the shared private selected admission call. Use stable `font-collection-source-revision-drift`, `font-collection-face-index`, and `font-collection-face-profile` contexts with State, InvalidInput, and Capability categories per D-12/D-13.

Pass `self.source`, `self.opening_revision`, and the selected cached `directory_start`/`directory_end`/`table_count`/profile into the Plan 102-01 private seam per D-04 through D-07. Reparse only that directory into fresh semantic `DirectoryFacts`, use collection checksum mode, reuse the existing required-table/cmap/kern/metrics/loca/glyf admission and exact `Font` object literal, and set `Font.source`/`opening_revision` to the collection root/original revision. Keep selection non-consuming and uncached; a second call repeats the transaction with its own limits/budget.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_test.mbt --target native --frozen --target-dir target/phase102-plan02-public-tracer --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase102-plan02-public-regression --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "The exact public method returns the existing opaque Font and adds no selected-face type or collection-specific query."
    - "Revision, index, and cached profile gates return exact D-11/D-13 categories and facts before selected byte work."
    - "A real selected face with tables before and after its directory succeeds only with root-relative table offsets."
    - "Repeated selection is non-consuming, uncached, and independently budgeted."
    - "Representative existing Font queries work without collection provenance, and the complete native package remains green."
  </acceptance_criteria>
  <done>TTC-02 has an end-to-end public selected-face path implementing D-01 through D-07, D-11 through D-13, and the container-neutral core of D-15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close selected accounting, sharing, checksum, and mutation atomicity</name>
  <files>modules/mb-font/font/collection.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - .planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
    - .planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
    - .planning/debug/resolved/ttc-work-precedence-order.md
    - modules/mb-font/font/collection.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/tables.mbt
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-core/budget/budget.mbt
  </read_first>
  <behavior>
    - "max_source_bytes accepts the exact retained collection-root length and rejects one short without charging."
    - "Selected charge bytes equal D + T; allocations remain 3; allocation_size is max(64*R, 8*(num_glyphs+1)); work equals exact selected semantic work."
    - "Every selected stage cumulatively preflights the real caller/ancestors before a declared loop, but only valid semantics after the final revision guard commit one exact aggregate charge."
    - "Two faces with exact shared table ranges independently open against one root/revision without a cache; distinct face-local tables remain distinct."
    - "A supported static face opens beside CFF, CFF2, and variable siblings; selecting an unsupported sibling fails Capability before deep admission."
    - "Mutation before selection, deterministic mutate/restore during selection, and later mutation of any inherited Font fail State; failed selection leaves the entire Budget unchanged."
  </behavior>
  <action>
Implement the selected accounting fixed by D-08 through D-10. Let `R` be cached selected table count, `D = directory_end - directory_start`, and `T` the checked sum of selected record lengths; set committed bytes to checked `D + T`, not root length or sibling bytes. Use collection directory work `R + C2(R) + R + search_selector_work + T`, retain three allocations and `max(64*R, 8*(num_glyphs+1))`, and include the exact existing selected semantic work. Apply `FontLimits.max_source_bytes` to the retained root extent while every other Font limit applies only to the selected face.

Use the deferred ledger from Plan 102-01 for cumulative max_work and real caller/ancestor preflights before directory/checksum/cmap/kern/name/post/glyph/loca/mapping loops. After required tables, per-table checksums, semantic admission, metric index, and exact final caller preflight succeed, invoke a private deterministic callback, recheck the original collection opening revision, charge the one aggregate `ResourceCharge`, and only then construct `Font`. No failure or final-revision drift may charge or publish.

Extend generated collection builders/tests with exact shared `glyf`/`loca`/`hmtx` ranges, distinct face-local tables, and supported StaticGlyf beside CFF, CFF2, and variable siblings per D-06/D-07. Assert no sibling semantic rescan by giving unsupported siblings bounded structural directories but deep bytes that would fail if admitted. Add selected bad-table and non-zero head-adjustment cases proving D-14. Add root/source, D+T, allocations, allocation-size, exact work, and ancestor-budget exact/one-short tests; repeated independent selections; mutate-before, private mid-selection mutate/restore, and every-query post-selection mutation checks per D-12.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase102-plan02-transaction-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font/collection_test.mbt --target native --frozen --target-dir target/phase102-plan02-transaction-public --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase102-plan02-transaction-regression --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "Retained root source authority and selected D+T/allocations/allocation_size/work charges accept exact fit and reject each one-short dimension atomically."
    - "Valid exact cross-face sharing and mixed unsupported siblings do not change the selected face's semantics, work, or charge."
    - "Every selected table checksum is enforced with head zeroing, while collection selection never emits the standalone aggregate-adjustment error."
    - "Mutation before, during, or after selected admission is detected through the shared monotonic root revision; no failed admission charges or publishes."
    - "Standalone Font::open and all existing package behavior remain unchanged."
  </acceptance_criteria>
  <done>TTC-02/TTC-03 and D-01 through D-15 are implemented through one root-relative, checksum-correct, exact-accounting, atomic selected transaction.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS L1 applies at the public collection/index/limits/budget boundary and the retained mutable root. All high-severity threats are mitigated with focused behavioral tests.

## Trust Boundaries

| Boundary | Description |
|---|---|
| `FontCollection` + index -> `open_face` | Revision, index, and cached profile authority must precede untrusted selected-byte work. |
| cached Phase 101 facts -> selected directory parser | Cached root coordinates/count/profile may authorize only the selected face. |
| selected semantic plan -> caller Budget | Cumulative authority is observable, but commit must remain atomic. |
| retained mutable root -> returned Font | Original monotonic revision identity must survive selection and every query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-102-02-01 | Tampering | root-relative selected tables | high | mitigate | Task 1 owns before/after-directory and in-bounds wrong-rebase public cases. |
| T-102-02-02 | Denial of service | unrelated siblings | high | mitigate | Tasks 1-2 reuse cached face facts, admit one directory only, and use mixed-profile poison-sibling tests. |
| T-102-02-03 | Denial of service | selected declared work | high | mitigate | Task 2 owns cumulative max_work and caller/ancestor preflights before every dependent loop. |
| T-102-02-04 | Repudiation | partial selected charge/publication | high | mitigate | Task 2 owns full Budget equality on every failure and one exact aggregate success delta. |
| T-102-02-05 | Tampering | retained-source mutation | high | mitigate | Tasks 1-2 own revision-first, deterministic final-guard, mutate/restore, and inherited-query regressions. |
| T-102-02-06 | Tampering | collection checksum policy | high | mitigate | Task 2 owns per-table/head-zeroing enforcement and collection aggregate-skip evidence. |
| T-102-02-07 | Information disclosure | public selected internals | medium | mitigate | Task 1 returns only existing Font; Plan 102-03 owns the exact interface/private-leak classifier. |
| T-102-02-SC | Tampering | dependency supply chain | low | accept | No package installation or dependency change occurs; all code remains in existing pure-MoonBit packages. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Select one supported static face and use existing opaque Font unchanged | Tasks 1-2 | COVERED |
| REQ | TTC-02 | In-range static-glyf selection and existing Font semantics | Task 1 plus Plan 102-03 equivalence matrix | COVERED |
| REQ | TTC-03 | Root offsets, exact sharing, collection checksums, unsupported siblings | Tasks 1-2 | COVERED |
| RESEARCH | — | Cached-fact facade, retained root/revision, selected D+T, deferred ledger, one final charge | Tasks 1-2 | COVERED |
| CONTEXT | D-01-D-03 | Exact method, existing Font result, repeatable independent transaction | Task 1 | COVERED |
| CONTEXT | D-04-D-07 | Root-relative selected reparse, no copy, cached authority, sharing/mixed siblings | Tasks 1-2 | COVERED |
| CONTEXT | D-08-D-10 | Separate budgets, selected-only accounting, staged preflight and one commit | Task 2 | COVERED |
| CONTEXT | D-11-D-13 | Frozen precedence, revision behavior, exact error categories | Tasks 1-2 plus Plan 102-03 matrix | COVERED |
| CONTEXT | D-14-D-15 | Collection checksum semantics and existing Font behavior | Tasks 1-2 plus Plan 102-03 | COVERED |
| CONTEXT | D-16-D-17 | Scope fence and focused-vs-broad qualification split | Both tasks and Plan 102-03 | COVERED |

All GOAL/REQ/RESEARCH/CONTEXT items are covered by the three-plan set; no deferred capability enters this plan.
</source_audit>

<verification>
1. Exercise public selection, root-relative before/after tables, exact sharing, mixed profiles, and representative existing Font queries on native.
2. Exercise selected D+T/work/allocation formulas, cumulative ancestor preflights, atomic failure, and deterministic mutation in white-box tests.
3. Run the complete native font package and whitespace gate to preserve standalone compatibility.
</verification>

<success_criteria>
- The single additive open_face method returns existing Font for every supported in-range face.
- Selected bytes remain root-relative, shared ranges remain valid, and unsupported siblings are irrelevant.
- Selected limits/work/checksums/budgets follow the locked split rule and one-commit transaction.
- Root mutation invalidates collection selection and every inherited Font query.
- Standalone Font::open remains exact and no excluded capability or public storage fact appears.
</success_criteria>

<output>
Create `.planning/phases/102-root-relative-selected-face-admission/102-02-SUMMARY.md` when done.
</output>
