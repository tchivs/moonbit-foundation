---
phase: 102-root-relative-selected-face-admission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-font/font/directory.mbt
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/kern.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/collection_wbtest.mbt
autonomous: true
requirements:
  - TTC-02
  - TTC-03
must_haves:
  decisions:
    - "D-04: directory-field reads use an absolute selected-directory start while table-record offsets remain collection-root-relative."
    - "D-08 through D-10: standalone and collection admission use separate commit policies over the same semantic pipeline."
    - "D-14: both modes validate every table checksum with head adjustment bytes zeroed, while only standalone validates the whole-source checksum adjustment."
    - "D-16: the refactor stays pure MoonBit, read-only, and within the existing mb-font/font package and dependency boundary."
  truths:
    - "TTC-02/TTC-03 / D-04: one private directory seam can parse at source byte zero or at a non-zero absolute directory start, and every normalized TableWindow remains a no-copy subview of the retained root at the record's unchanged root offset."
    - "D-14: standalone and collection checksum modes share the exact per-table checksum loop and head-byte zeroing, while the standalone wrapper alone retains the existing whole-source 0xB1B0AFBA check."
    - "D-08 through D-10: a private admission ledger preserves every historical standalone preflight/charge point and also supports cumulative ancestor-aware preflight with deferred collection commit."
    - "Existing Font::open accepts the same standalone bytes through the same public signature and returns the same success, Data, Capability, Resource, State, and exact budget-delta observations as before this phase."
  prohibitions:
    - "Font::open must not auto-detect TTC/OTC, change its public signature, weaken standalone checksum adjustment, reorder observable errors, or change successful and failed charge deltas."
    - "Directory base must never be added to a table-record offset; collection mode must never be inferred from directory_start == 0."
    - "The deferred ledger must not use a detached Budget as its sole authority and must not charge the real collection caller before final publication."
    - "No public symbol, new module, new dependency, FFI, ambient I/O, CFF/CFF2 execution, variation execution, WOFF/WOFF2 path, or materialized standalone font may be introduced."
  artifacts:
    - path: modules/mb-font/font/directory.mbt
      provides: "Private offset-aware directory parser and closed standalone/collection checksum mode."
    - path: modules/mb-font/font/tables.mbt
      provides: "Private dual-mode admission plan and cumulative deferred-work ledger."
    - path: modules/mb-font/font/kern.mbt
      provides: "Kern subtable/pair traversal routed through the shared ledger without changing its semantic order."
    - path: modules/mb-font/font/font.mbt
      provides: "Standalone facade routed through neutral private seams with historical behavior intact."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Standalone byte, checksum, precedence, and exact charge compatibility locks."
    - path: modules/mb-font/font/collection_wbtest.mbt
      provides: "Private non-zero-base, checksum-mode, and deferred-ledger arithmetic evidence."
  key_links:
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/directory.mbt
      via: "Font::open calls the zero-base standalone wrapper, which delegates to the offset-aware private parser and standalone checksum mode."
      pattern: "font_parse_directory"
    - from: modules/mb-font/font/directory.mbt
      to: modules/mb-core/bytes/views.mbt
      via: "checked_font_table_view resolves unchanged table offsets against the retained root ByteView."
      pattern: "checked_font_table_view"
    - from: modules/mb-font/font/tables.mbt
      to: modules/mb-core/budget/budget.mbt
      via: "The admission ledger performs real caller preflights and mode-specific charge/record operations."
      pattern: "preflight"
    - from: modules/mb-font/font/kern.mbt
      to: modules/mb-font/font/tables.mbt
      via: "Kern subtable and pair loops use the same admission ledger as cmap declaration loops."
      pattern: "font_admit_kern_bounded"
---

<objective>
Refactor the existing standalone directory, checksum, and semantic-admission pipeline into explicit private modes while keeping every standalone `Font::open` observation unchanged.

Purpose: Prove the riskiest shared seam and atomic-work mechanism before any public collection selection depends on it.
Output: Offset-aware root parsing, an explicit checksum dispatcher, a dual-mode admission ledger, and exhaustive standalone compatibility evidence for TTC-02/TTC-03 and D-04, D-08 through D-10, D-14, and D-16.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
@.planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
@.planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-VERIFICATION.md
@.planning/debug/resolved/ttc-work-precedence-order.md
@modules/mb-font/font/directory.mbt
@modules/mb-font/font/tables.mbt
@modules/mb-font/font/kern.mbt
@modules/mb-font/font/font.mbt
@modules/mb-font/font/font_test.mbt
@modules/mb-font/font/collection_wbtest.mbt
@modules/mb-core/budget/budget.mbt
@modules/mb-core/bytes/views.mbt

<interfaces>
Preserve these existing public contracts exactly:
- `Font::open(ByteView, FontLimits, Budget) -> Result[Font, CoreError]`
- opaque `Font` query surface and all existing CoreError facts

Create only private contracts:
- a closed checksum mode with standalone and collection cases
- an offset-aware directory core accepting root source, absolute directory start, optional cached table count, limits, and checksum mode
- the existing zero-base `font_parse_directory(source, limits)` wrapper
- a closed commit mode with standalone-incremental and collection-deferred cases
- an admission ledger that performs `max_work` checks, real caller cumulative `preflight`, mode-specific virtual recording/charge, and exact-work reporting
</interfaces>
</context>

## Artifacts this plan produces

| Kind | Exact artifact | Contract |
|---|---|---|
| Modified file | `modules/mb-font/font/directory.mbt` | Offset-aware private core, root-backed table windows, half-open directory overlap, and explicit checksum dispatcher. |
| Modified file | `modules/mb-font/font/tables.mbt` | Exact/legacy admission facts and the shared dual-mode work ledger. |
| Modified file | `modules/mb-font/font/kern.mbt` | Ledger-aware subtable and pair preflight/charge staging. |
| Modified file | `modules/mb-font/font/font.mbt` | Existing standalone facade routed through neutral private seams. |
| Modified file | `modules/mb-font/font/font_test.mbt` | Exact standalone compatibility oracle. |
| Modified file | `modules/mb-font/font/collection_wbtest.mbt` | Private offset/checksum/ledger cases without a public selected-face API. |

`modules/mb-font/font/pkg.generated.mbti` remains ignored compiler output. This plan neither edits nor tracks it.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one standalone font through the offset-aware checksum seam</name>
  <files>modules/mb-font/font/directory.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - .planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
    - .planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
    - .planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
    - modules/mb-font/font/directory.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-core/bytes/views.mbt
  </read_first>
  <behavior>
    - "The existing generated standalone TrueType opens with the same public metrics, checksum outcome, error facts, and budget delta."
    - "A private parse at non-zero directory_start reads signature/count/search/records at that absolute start but resolves each table record offset from collection byte zero."
    - "A legal non-empty table immediately before or after the selected directory succeeds, touching endpoints succeed, and a non-empty table intersecting the directory fails Data."
    - "Both checksum modes reject a bad selected table checksum and zero head bytes 8-11 during the head table sum."
    - "Collection mode omits only the whole-root adjustment check; standalone mode retains that check and its exact error context."
  </behavior>
  <action>
Write the non-zero-base, wrong-rebase-stays-in-bounds, before/after-directory, overlap/touching, table-checksum, head-zeroing, and standalone-adjustment tests first. Refactor `parse_font_directory`/`font_parse_directory` into an offset-aware private core per D-04. Use checked additions for every directory-local field (`directory_start`, `+4`, `+6`, `+8`, `+10`, and `directory_start + 12 + 16*i`), compare an optional cached table count before its loop, and keep each record's table offset unchanged when calling `checked_font_table_view(source, table_offset, table_length)`. Replace the zero-base overlap shortcut with a checked half-open intersection against the selected directory range; preserve all established deep `font-*` error contexts and wire order.

Split checksum validation per D-14 into the shared per-table loop, a standalone whole-source helper, and an explicit closed dispatcher. Preserve padded big-endian summation and zero only head bytes 8-11 in both modes. Keep `font_parse_directory(source, limits)` and the existing `Font::open` path as thin standalone wrappers with byte-for-byte input handling, the same public signature, and identical observable error/checksum sequencing. Do not expose the modes or offset-aware parser.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase102-plan01-directory-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase102-plan01-directory-regression --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "Non-zero directory-field addressing and unchanged root-relative table offsets are distinguished by an in-bounds wrong-rebase fixture."
    - "Before, after, touching, intersecting, and checked-overflow directory/table layouts have exact expected outcomes."
    - "Per-table checksums and head zeroing are identical in both modes; only collection mode skips the standalone aggregate check."
    - "All existing standalone checksum and complete native font regressions pass with unchanged public signature and CoreError facts."
  </acceptance_criteria>
  <done>D-04/D-14 have one production-quality shared directory/checksum path, and standalone `Font::open` remains behaviorally exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cumulative deferred work without changing standalone charges</name>
  <files>modules/mb-font/font/tables.mbt, modules/mb-font/font/kern.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - .planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
    - .planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
    - .planning/debug/resolved/ttc-work-precedence-order.md
    - modules/mb-font/font/tables.mbt
    - modules/mb-font/font/kern.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-core/budget/budget.mbt
  </read_first>
  <behavior>
    - "Standalone cmap and kern declaration loops retain limit, cumulative preflight, incremental charge, and traversal order exactly."
    - "Collection-deferred mode checks max_work and cumulatively preflights the real caller Budget and all ancestors before each declared loop while leaving every real budget dimension unchanged."
    - "The admission plan exposes exact total work and the historical standalone remainder without double-counting staged cmap or kern work."
    - "Overflow or one-short max_work/caller-work authority fails before its dependent loop with the existing Resource facts."
    - "Every existing standalone success and failure observes the same exact bytes, allocations, allocation_size, work, and error precedence."
  </behavior>
  <action>
Create the private commit mode and ledger described by D-08 through D-10. Route `font_preflight_admission_work`, `font_charge_admission_work`, cmap record/body stages, and kern subtable/pair stages through the ledger. In standalone mode issue the same request sizes and real charges at the same code points. In collection-deferred mode checked-add virtual staged work to the next request, run cumulative `max_work` and real caller/ancestor preflights before traversal, record the stage only after preflight, and never mutate the caller.

Extend `FontAdmissionPlan` or an equally local private result with exact total work plus the unchanged standalone remaining charge. Keep allocation count and allocation-size calculations unchanged. Route `Font::open` through standalone mode and preserve its current order: discovery authority, directory parse, admission planning and incremental cmap/kern charges, final remainder charge, profile/required/checksum/semantic/metric/revision behavior exactly as the existing tests observe. Add white-box cumulative child/ancestor budget, overflow, exact/one-short, semantic-failure-zero-charge, and no-double-count cases; add surgical black-box locks for any standalone charge or precedence branch not already covered.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase102-plan01-ledger-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase102-plan01-standalone-lock --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font check --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "Cmap and kern loops cannot run before exact semantic and caller authority in either mode."
    - "Collection-deferred white-box failures leave the real root and child Budget dimensions unchanged, including when an ancestor is tighter."
    - "Standalone mode retains every incremental stage delta and final remainder delta with no work counted twice."
    - "The full native font suite and all-target compilation pass without a new dependency, public symbol, or generated interface artifact."
  </acceptance_criteria>
  <done>D-08 through D-10 have a reusable private ledger that enables one future collection commit without changing standalone admission.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS L1 applies at the untrusted-font-byte and attacker-declared-work boundaries. The configured high-severity block threshold is satisfied because every high threat has a concrete mitigation and automated owner.

## Trust Boundaries

| Boundary | Description |
|---|---|
| root `ByteView` -> offset-aware directory core | Untrusted offsets/counts must not confuse directory-local and root-relative coordinates. |
| directory/table bytes -> checksum dispatcher | Collection policy may omit only the inapplicable aggregate rule, never table integrity. |
| attacker declarations -> cmap/kern loops | Exact semantic and caller authority must precede traversal. |
| private refactor -> standalone public facade | Existing caller-visible errors and resource deltas must not drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-102-01-01 | Tampering | directory base and table offsets | high | mitigate | Task 1 owns checked directory-local addressing, unchanged root offsets, and an in-bounds wrong-rebase regression. |
| T-102-01-02 | Tampering | checksum mode | high | mitigate | Task 1 owns shared per-table checks, head zeroing, and standalone aggregate compatibility tests. |
| T-102-01-03 | Denial of service | cmap/kern declared loops | high | mitigate | Task 2 owns max_work plus cumulative real caller/ancestor preflights before every loop. |
| T-102-01-04 | Repudiation | deferred budget accounting | high | mitigate | Task 2 owns exact/one-short and complete Budget::remaining equality evidence for deferred failures. |
| T-102-01-05 | Denial of service | standalone compatibility drift | high | mitigate | Tasks 1-2 run the complete native package and preserve exact staged charges/error order. |
| T-102-01-06 | Information disclosure | private mode leakage | medium | mitigate | Both tasks keep modes/ledger private; Plan 102-03 independently freezes the public interface. |
| T-102-01-SC | Tampering | dependency supply chain | low | accept | No install or dependency change occurs; implementation remains pure MoonBit over the existing mb-core workspace dependency. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Existing opaque Font behavior must be reusable from a no-copy root-relative adapter | Tasks 1-2 establish the shared private seam; Plan 102-02 publishes it | COVERED |
| REQ | TTC-02 | Existing Font semantic pipeline is reused rather than duplicated | Task 2 plus Plan 102-02 | COVERED |
| REQ | TTC-03 | Root-relative tables and collection-specific checksum behavior | Task 1 | COVERED |
| RESEARCH | — | Offset-aware core, explicit checksum mode, dual commit ledger, real cumulative preflight, unchanged standalone wrapper | Tasks 1-2 | COVERED |
| CONTEXT | D-04 | Directory-local base and root-relative table offsets | Task 1 | COVERED |
| CONTEXT | D-08-D-10 | Separate transactions and mode-specific ledger/commit timing | Task 2 | COVERED |
| CONTEXT | D-14 | Shared table checks plus standalone-only aggregate rule | Task 1 | COVERED |
| CONTEXT | D-16 | Pure MoonBit/no-copy/no-new-boundary scope | Tasks 1-2 | COVERED |

No source item is omitted. D-01 through D-03, D-05 through D-07, D-11 through D-13, D-15, and D-17 are owned by Plans 102-02/03.
</source_audit>

<verification>
1. Prove non-zero directory coordinates, root table offsets, half-open overlap, and both checksum policies in white-box tests.
2. Prove standalone incremental and collection-deferred ledger behavior with exact/one-short and ancestor-budget cases.
3. Run the complete native font package, target-all compilation, and whitespace gate.
</verification>

<success_criteria>
- The private directory/checksum seam is offset-aware and root-relative without weakening table integrity.
- The private ledger can defer collection charges while preserving traversal authority and ancestor budgets.
- Existing standalone bytes, public signature, errors, precedence, checksum adjustment, and exact budget deltas are unchanged.
- No public API, dependency, generated interface artifact, or excluded capability is added.
</success_criteria>

<output>
Create `.planning/phases/102-root-relative-selected-face-admission/102-01-SUMMARY.md` when done.
</output>
