---
phase: 102-root-relative-selected-face-admission
plan: 03
type: execute
wave: 3
depends_on:
  - 102-02
files_modified:
  - modules/mb-font/font/collection_test.mbt
  - modules/mb-font/font/collection_wbtest.mbt
  - modules/mb-font/font/font_test.mbt
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
autonomous: true
requirements:
  - TTC-02
  - TTC-03
must_haves:
  decisions:
    - "D-01/D-02: the exact public surface advances by only FontCollection::open_face returning existing Font."
    - "D-11 through D-13: the complete multi-fault precedence and error taxonomy are frozen behaviorally."
    - "D-14/D-15: selected and standalone checksum/equivalence behavior are independently qualified."
    - "D-16/D-17: focused four-target Phase 102 evidence stays inside scope while broad hostile/licensed/release qualification remains Phase 103."
  truths:
    - "TTC-02 / D-15: standalone and collection-selected forms of the same logical font return equal units-per-em, bounds, both line metrics, Unicode mapping, glyph identity, horizontal metrics, kerning hit/miss, and unhinted outline commands/values through public APIs."
    - "D-11 through D-13: every two-fault selection case returns the exact earlier category/code/operation/context/requested/limit, and every failed case preserves all Budget dimensions."
    - "D-14: bad selected table checksums fail before exact-budget shortage; collection mode ignores only whole-root adjustment, while the standalone bad-adjustment oracle remains unchanged."
    - "D-12: mutation before, deterministic mutate/restore during, and later mutation after independent repeated selections all return State without stale Font publication or partial charge."
    - "D-01/D-02/D-16: generated mb-font/font interface has exactly 85 semantic lines—84 inherited lines plus only the exact open_face signature—and independent negatives reject any other selected/parser/storage/deferred surface."
    - "D-17: focused public/private collection selectors pass independently on js, wasm, wasm-gc, and native; full native regression, target-all check/info, policy, dependency, FFI, and whitespace gates pass."
  prohibitions:
    - "No additional public type, method, constructor parameter, import, raw source/directory/range/parser fact, or collection-specific Font query may accompany open_face."
    - "pkg.generated.mbti must remain ignored ephemeral compiler output and must not be hand-edited, force-added, or tracked."
    - "Standalone Font::open bytes, signature, whole-source checksum, error precedence, and exact charge behavior must not change."
    - "No licensed fixture claim, broad release matrix, CFF/CFF2 or variable execution, WOFF/WOFF2, materialization, FFI, ambient I/O, module, or dependency expansion may enter Phase 102."
  artifacts:
    - path: modules/mb-font/font/collection_test.mbt
      provides: "Public equivalence, precedence, checksum, sharing, mutation, and exact/one-short matrix."
    - path: modules/mb-font/font/collection_wbtest.mbt
      provides: "Private final-guard, accounting, and root identity evidence."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Surgical standalone checksum/charge/preference compatibility locks."
    - path: policy/foundation.json
      provides: "Tracked exact ordered 85-line mb-font/font semantic interface."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Independent Phase 102 classifier plus missing/duplicate/reorder/altered/private/deferred negative fixtures."
  key_links:
    - from: modules/mb-font/font/collection_test.mbt
      to: modules/mb-font/font/font.mbt
      via: "The same generated logical font is opened standalone and through FontCollection::open_face, then compared only by public Font methods."
      pattern: "Font::open"
    - from: modules/mb-font/font/collection_test.mbt
      to: modules/mb-font/font/collection.mbt
      via: "Multi-fault and exact/one-short cases invoke the public selected transaction."
      pattern: "open_face"
    - from: scripts/quality/Assert-Policy.ps1
      to: policy/foundation.json
      via: "Assert-FontFoundationPolicy independently classifies and exact-compares regenerated semantic interface lines."
      pattern: "Assert-FontFoundationPolicy"
    - from: policy/foundation.json
      to: modules/mb-font/font/pkg.generated.mbti
      via: "The tracked 85-line golden is compared to ignored moon info output without tracking that output."
      pattern: "semantic_interface"
---

<objective>
Freeze public semantic equivalence, complete selected-face ordering/atomicity, the exact one-method interface advance, and focused portable evidence.

Purpose: Convert the Phase 102 implementation into reproducible TTC-02/TTC-03 proof without absorbing Phase 103's licensed or broad release qualification.
Output: Public/private compatibility matrices, exact 84-to-85 policy gates, and independent focused runs on all four supported targets.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
@.planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
@.planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
@.planning/phases/102-root-relative-selected-face-admission/102-01-SUMMARY.md
@.planning/phases/102-root-relative-selected-face-admission/102-02-SUMMARY.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-VERIFICATION.md
@.planning/debug/resolved/ttc-work-precedence-order.md
@modules/mb-font/font/collection_test.mbt
@modules/mb-font/font/collection_wbtest.mbt
@modules/mb-font/font/font_test.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1

<interfaces>
The only generated-interface addition is exactly:
`pub fn FontCollection::open_face(Self, UInt64, FontLimits, @budget.Budget) -> Result[Font, @error.CoreError]`

Use only existing public Font observations as the equivalence oracle:
- `units_per_em`, `global_bounds`, `hhea_line_metrics`, `typographic_line_metrics`
- `glyph_for_scalar`, `glyph_id`, `horizontal_metrics`
- `kerning`
- `outline` and the existing shared Path2 inspection contract

The tracked semantic interface must contain exactly 85 non-comment/nonblank lines in compiler order.
</interfaces>
</context>

## Artifacts this plan produces

| Kind | Exact artifact | Contract |
|---|---|---|
| Modified file | `modules/mb-font/font/collection_test.mbt` | Complete public selected/standalone equivalence and focused hostile matrix. |
| Modified file | `modules/mb-font/font/collection_wbtest.mbt` | Final private selected accounting/revision assertions. |
| Modified file | `modules/mb-font/font/font_test.mbt` | Standalone aggregate checksum and staged-charge lock where existing coverage is not already exact. |
| Modified file | `policy/foundation.json` | One-line generated interface advance from 84 to 85. |
| Modified file | `scripts/quality/Assert-Policy.ps1` | Independent exact Phase 102 surface classifier and negative fixtures. |

The generated `modules/mb-font/font/pkg.generated.mbti` is verification output only and remains ignored/untracked.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze public equivalence and the complete selected failure matrix</name>
  <files>modules/mb-font/font/collection_test.mbt, modules/mb-font/font/collection_wbtest.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    - .planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
    - .planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
    - .planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
    - .planning/phases/102-root-relative-selected-face-admission/102-02-SUMMARY.md
    - .planning/debug/resolved/ttc-work-precedence-order.md
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-font/font/font_test.mbt
  </read_first>
  <behavior>
    - "Equivalent standalone and selected fonts return equal values for every D-15 public metric, mapping, kerning, glyph, and outline observation."
    - "Revision drift plus invalid index returns State; invalid index plus unsupported profile returns InvalidInput; unsupported profile plus malformed/depleted deep state returns Capability."
    - "Malformed selected directory with sufficient structural authority precedes final budget shortage; missing required table precedes bad checksum; bad table checksum precedes exact final budget one-short."
    - "Valid semantics with final bytes/allocations/allocation_size/work one short returns Resource and leaves all dimensions unchanged."
    - "Collection mode enforces table checks and head zeroing but never standalone whole-root adjustment; the standalone bad-adjustment case retains its exact Data context."
    - "Before/during/after mutation and repeated independent selection preserve one monotonic root identity and atomic budget behavior."
  </behavior>
  <action>
Build one generated standalone logical TrueType and one collection embedding the same tables at absolute root offsets, then compare only public `Font` results per TTC-02/D-15: units-per-em, global bounds, hhea and typographic line metrics, BMP and supplementary scalar mapping, glyph ID/value, horizontal metrics/bounds, kern hit/miss, and exact unhinted outline command/value sequence under equal outline budgets. Include selected tables before and after a non-zero directory, exact shared ranges, and a supported face beside unsupported siblings so the equivalence oracle also exercises TTC-03.

Complete the D-11 through D-13 two-fault table. Assert category, code, operation, context, source_offset, requested, and limit wherever defined for revision versus index; index versus profile; profile versus deep malformed/resource state; selected structural Data versus final budget; required-table versus checksum; selected table checksum versus final budget; and valid semantics versus each final one-short budget dimension. Snapshot every `Budget::remaining` field before every rejected call. Add exact/one-short `max_source_bytes`, selected `D+T`, three allocations, allocation-size, exact work, and child/ancestor work cases.

Freeze D-14 by corrupting a selected table checksum, exercising non-zero `head.checksumAdjustment`, and independently retaining the existing standalone whole-source bad-adjustment error. Add deterministic private mid-selection mutate/restore plus pre-selection and every-inherited-query post-selection mutation cases; verify repeated selections get independent charges/Font handles but share the root revision identity. Reuse existing builders and helpers; do not derive expected semantics by calling private production parsers.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase102-plan03-matrix-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font/collection_test.mbt --target native --frozen --target-dir target/phase102-plan03-matrix-public --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase102-plan03-matrix-regression --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "All required existing Font observations are equal for standalone and selected forms of the same logical font."
    - "Every D-11 precedence pair returns the exact earlier structured outcome and no rejected transaction changes any Budget field."
    - "Every selected authority/charge dimension has exact-fit and one-short evidence, including a tighter live ancestor."
    - "Collection table/head checksum behavior and standalone whole-source adjustment behavior are simultaneously frozen."
    - "Mutation before/during/after selection and repeat selection prove one shared monotonic root identity without a face cache."
  </acceptance_criteria>
  <done>TTC-02/TTC-03 and D-11 through D-15 have complete public semantic, ordering, checksum, mutation, and exact-accounting evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Advance the interface by one line and qualify focused portability</name>
  <reversibility rating="costly">D-01/D-02 advance an exact public compatibility allowlist consumed by downstream packages; accidental extra surface creates lasting compatibility obligations.</reversibility>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <read_first>
    - .planning/phases/102-root-relative-selected-face-admission/102-CONTEXT.md
    - .planning/phases/102-root-relative-selected-face-admission/102-RESEARCH.md
    - .planning/phases/102-root-relative-selected-face-admission/102-PATTERNS.md
    - .planning/phases/102-root-relative-selected-face-admission/102-02-SUMMARY.md
    - policy/foundation.json
    - scripts/quality/Assert-Policy.ps1
    - modules/mb-font/font/collection.mbt
  </read_first>
  <action>
Regenerate the interface with `moon info` but treat `pkg.generated.mbti` only as ignored evidence. Insert exactly the D-01 generated `open_face` line into `policy/foundation.json` after `FontCollection::open` in compiler order, preserving all 84 inherited lines and making the semantic count exactly 85.

Rename/generalize the independent Phase 101 classifier in `Assert-Policy.ps1` to Phase 102, independently add the same exact line, and update the exact count to 85. Remove only that exact signature from the selected-face forbidden fixtures. Retain missing/duplicate/reorder and private-leak negatives; add altered return type, `CollectionFace`, raw source/directory/range handles, parser facts, collection-specific metric/query, and extra selected overload fixtures. Keep dependency, target, source inventory, FFI, ambient-I/O, CFF/variation/WOFF/materialization, and public storage guards fail closed per D-02/D-16.

Run the focused public and white-box collection files independently on `js`, `wasm`, `wasm-gc`, and `native`, then run the complete native font package, target-all check/info, exact policy classifier, ignored-output assertion, and whitespace gate. This is D-17's focused Phase 102 evidence; do not add licensed artifacts or claim Phase 103's broad release matrix.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
foreach ($target in @('js','wasm','wasm-gc','native')) {
  moon -C modules/mb-font test font/collection_test.mbt --target $target --frozen --target-dir "target/phase102-plan03-public-$target" --no-parallelize
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
  moon -C modules/mb-font test font/collection_wbtest.mbt --target $target --frozen --target-dir "target/phase102-plan03-wb-$target" --no-parallelize
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase102-plan03-native-regression --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font check --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font info --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git check-ignore -q -- modules/mb-font/font/pkg.generated.mbti
if ($LASTEXITCODE -ne 0) { throw 'pkg.generated.mbti must remain ignored compiler output.' }
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "The regenerated semantic interface contains exactly 85 ordered lines: all 84 inherited declarations plus only the exact open_face signature."
    - "Independent negative fixtures reject missing, duplicate, reordered, altered, overloaded, parser/storage, collection-query, and deferred-capability surface."
    - "pkg.generated.mbti is ignored and untracked; production/test inventories contain only real repository sources."
    - "Focused public/private collection tests pass independently on js, wasm, wasm-gc, and native; full native regression and target-all compilation pass."
    - "Policy preserves pure MoonBit, mb-font -> mb-core only, no FFI/ambient I/O, and the explicit unsupported-profile/container boundary."
  </acceptance_criteria>
  <done>D-01/D-02/D-16/D-17 are frozen by one exact public line and reproducible focused four-target evidence without absorbing Phase 103.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS L1 applies to hostile multi-fault inputs, checksum integrity, mutable backing, resource atomicity, and the public compatibility boundary. All high-severity threats are mitigated.

## Trust Boundaries

| Boundary | Description |
|---|---|
| hostile selected bytes -> public CoreError | Multi-fault ordering must be stable and bounded. |
| selected semantic plan -> Budget/publication | One-short and mutation failures must be fully atomic. |
| standalone/collection representations -> public Font | Container choice must not change semantic results. |
| generated interface -> policy/downstream callers | Exactly one intended method may cross the public boundary. |
| target runtime -> qualification evidence | Host-width or target-specific behavior must not diverge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-102-03-01 | Tampering | standalone/selected semantic equivalence | high | mitigate | Task 1 compares all TTC-02 public observations from independently opened logical equivalents. |
| T-102-03-02 | Denial of service | multi-fault precedence | high | mitigate | Task 1 owns exact D-11 pairwise ordering and one-short authority cases. |
| T-102-03-03 | Repudiation | failed budget transaction | high | mitigate | Task 1 snapshots every Budget dimension and proves no failed call mutates it. |
| T-102-03-04 | Tampering | selected/standalone checksums | high | mitigate | Task 1 independently freezes table/head behavior and the standalone aggregate error. |
| T-102-03-05 | Tampering | source mutation | high | mitigate | Task 1 owns before/during/after mutate-restoration and inherited-query tests. |
| T-102-03-06 | Information disclosure | accidental public internals | high | mitigate | Task 2 owns exact 85-line allowlists and private/parser/storage negative fixtures. |
| T-102-03-07 | Tampering | target-specific semantics | high | mitigate | Task 2 runs focused suites independently on all four targets plus target-all gates. |
| T-102-03-SC | Tampering | dependency supply chain | low | accept | No install or dependency change occurs; policy independently preserves the existing mb-core-only edge. |
</threat_model>

<decision_coverage>

| Decision | Plan/task owner | Executable evidence |
|---|---|---|
| D-01 | 102-02 Task 1; 102-03 Task 2 | Exact open_face call plus 85-line interface and altered/extra negatives |
| D-02 | 102-02 Task 1; 102-03 Tasks 1-2 | Existing Font public equivalence and no CollectionFace/query/storage surface |
| D-03 | 102-02 Tasks 1-2; 102-03 Task 1 | Repeated independent selections, separate budgets, no cache |
| D-04 | 102-01 Task 1; 102-02 Task 1 | Non-zero directory with before/after tables and in-bounds wrong-rebase case |
| D-05 | 102-02 Tasks 1-2; 102-03 Task 1 | Root/revision identity, exact sharing, mutation of inherited Font |
| D-06 | 102-02 Tasks 1-2 | Cached selected facts and unsupported poison-sibling isolation |
| D-07 | 102-02 Task 2 | Exact shared ranges and Capability-before-deep-admission unsupported selection |
| D-08 | 102-01 Task 2; 102-02 Task 2 | Separate fresh selected limits/budget and no reuse/refund of collection-open charge |
| D-09 | 102-02 Task 2; 102-03 Task 1 | Root source ceiling plus selected D+T/exact work and sibling-size independence |
| D-10 | 102-01 Task 2; 102-02 Task 2; 102-03 Task 1 | Cumulative staged preflights, one final charge, unchanged standalone stage deltas |
| D-11 | 102-02 Tasks 1-2; 102-03 Task 1 | Complete pairwise precedence and final publication order |
| D-12 | 102-02 Task 2; 102-03 Task 1 | Pre/during/post mutation and mutate-restoration with zero failed charge |
| D-13 | 102-02 Task 1; 102-03 Task 1 | Exact Input/Capability/Data/Resource/State facts and ordering |
| D-14 | 102-01 Task 1; 102-02 Task 2; 102-03 Task 1 | Shared per-table/head checks, collection aggregate skip, standalone aggregate lock |
| D-15 | 102-02 Task 1; 102-03 Task 1 | Complete public metrics/mapping/kern/glyph/outline equivalence |
| D-16 | All plans; 102-03 Task 2 | Scope prohibitions plus policy dependency/FFI/I/O/deferred-surface gates |
| D-17 | 102-03 Task 2 | Focused four-target selectors and explicit Phase 103 boundary |
</decision_coverage>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Select a supported static face and use existing opaque Font unchanged | 102-02; 102-03 Task 1 | COVERED |
| REQ | TTC-02 | Selected static glyf face is publicly equivalent to standalone logical font | 102-02 Task 1; 102-03 Task 1 | COVERED |
| REQ | TTC-03 | Root offsets, sharing, collection checksum, unsupported siblings | 102-01 Task 1; 102-02 Tasks 1-2; 102-03 Task 1 | COVERED |
| RESEARCH | — | Offset-aware seam, explicit checksum mode, deferred ledger, selected D+T, exact policy, focused portability | 102-01 through 102-03 | COVERED |
| CONTEXT | D-01-D-03 | Exact repeatable public selected-face contract | 102-02 Task 1; 102-03 Task 2 | COVERED |
| CONTEXT | D-04-D-07 | Root-relative no-copy retained source, cached authority, sharing/mixed siblings | 102-01 Task 1; 102-02 Tasks 1-2 | COVERED |
| CONTEXT | D-08-D-10 | Separate transactions, selected accounting, staged authority and one charge | 102-01 Task 2; 102-02 Task 2; 102-03 Task 1 | COVERED |
| CONTEXT | D-11-D-13 | Stable precedence, mutation, error taxonomy | 102-02; 102-03 Task 1 | COVERED |
| CONTEXT | D-14-D-15 | Collection checksum compatibility and existing Font behavior | 102-01 Task 1; 102-02; 102-03 Task 1 | COVERED |
| CONTEXT | D-16-D-17 | Explicit exclusions and Phase 102/103 qualification boundary | All plans; 102-03 Task 2 | COVERED |

All GOAL, TTC-02/TTC-03, in-scope RESEARCH constraints, and D-01 through D-17 are covered. Phase 103 hostile/licensed/release evidence and every explicit deferred capability are exclusions, not gaps.
</source_audit>

<verification>
1. Compare standalone and selected logical equivalents only through existing public Font methods.
2. Run complete precedence/checksum/mutation/exact-one-short matrices with full structured errors and Budget equality.
3. Regenerate the ignored interface, exact-compare 85 lines through two independent gates, and run negative fixtures.
4. Run focused public/private collection files on js, wasm, wasm-gc, and native; run full native regression, target-all check/info, policy, and whitespace gates.
</verification>

<success_criteria>
- TTC-02 public semantic equivalence is complete across metrics, mapping, kerning, glyph identity, and outlines.
- TTC-03 root offsets, sharing, mixed siblings, and collection checksums are reproducibly enforced.
- Every selected failure follows D-11 precedence and commits/publishes nothing.
- The public interface advances from 84 to exactly 85 lines with no accidental surface.
- Focused behavior is identical on all four targets while standalone and scope/dependency boundaries remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/102-root-relative-selected-face-admission/102-03-SUMMARY.md` when done.
</output>
