---
phase: 14-canonical-qoi-encode-and-four-target-vectors
plan: "01"
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - modules/mb-image/qoi/qoi.mbt
  - modules/mb-image/qoi/encode.mbt
  - modules/mb-image/qoi/encode_test.mbt
  - modules/mb-image/qoi/encode_wbtest.mbt
  - fixtures/qoi/cases.json
  - scripts/fixtures/Generate-QoiVectors.ps1
  - modules/mb-image/qoi/generated_vectors.mbt
  - fixtures/manifest.json
autonomous: true
requirements: [QOI-03, QOI-05]
must_haves:
  truths:
    - "A caller can encode compatible packed U8 RGB and straight-RGBA TopLeft sRGB image views through `@codec.ImageEncoder` and decode the exact original pixels."
    - "For a compatible source, `QoiEncoder` emits one deterministic QOI 1.0 byte sequence: a 14-byte header, canonical chunks, and the exact eight-byte end marker."
    - "Capability, declared-limit, budget, and writer failures use existing typed errors; every validation failure precedes writer output, while writer failures report exact completed bytes."
    - "Repository-owned specification-derived QOI encoder vectors cover chunk selection and byte round trips unchanged on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/qoi/qoi.mbt
      provides: "Public `QoiEncoder` value alongside the existing independent `QoiDecoder`."
    - path: modules/mb-image/qoi/encode.mbt
      provides: "Canonical QOI 1.0 `ImageEncoder` implementation with deterministic prepass, atomic budget charge, and forward Writer output."
    - path: modules/mb-image/qoi/encode_test.mbt
      provides: "Public QOI encoder contract, error-progress, and decode-after-encode behavior tests."
    - path: fixtures/qoi/cases.json
      provides: "Human-reviewable QOI decode and canonical-encode fixture authority."
    - path: scripts/fixtures/Generate-QoiVectors.ps1
      provides: "Deterministic checked materialization of all QOI vector tables and manifest provenance."
    - path: modules/mb-image/qoi/generated_vectors.mbt
      provides: "Generated encoder expectations consumed by white-box conformance tests."
  key_links:
    - from: modules/mb-image/qoi/encode.mbt
      to: modules/mb-image/codec/contracts.mbt
      via: "`@codec.ImageEncoder::encode(QoiEncoder, ...)` retains the existing `EncodeOptions`, `CodecLimits`, `Budget`, Writer, and `EncodeResult` seam."
    - from: modules/mb-image/qoi/encode.mbt
      to: modules/mb-image/qoi/decode.mbt
      via: "The canonical encoder uses the same QOI hash, header channel/color-space representation, chunk state, and marker semantics that `QoiDecoder` consumes."
    - from: scripts/fixtures/Generate-QoiVectors.ps1
      to: modules/mb-image/qoi/generated_vectors.mbt
      via: "A `-Check` generation run maps repository JSON encoder cases to the all-target MoonBit conformance table."
    - from: modules/mb-image/qoi/encode_wbtest.mbt
      to: modules/mb-image/qoi/encode.mbt
      via: "Generated source-image records are encoded, byte-compared to canonical expected streams, and decoded back to exact source pixels."
---

<objective>
Implement one independent pure-MoonBit QOI 1.0 encoder and prove its canonical bytes with generated specification-derived vectors on all four portable targets.

Purpose: Complete QOI-03 and QOI-05 while retaining Phase 13's portable codec, resource, and fixture contracts.
Output: `QoiEncoder`, canonical encode/error/round-trip tests, expanded checked QOI fixtures, and all-target vector evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-canonical-qoi-encode-and-four-target-vectors/14-CONTEXT.md
@.planning/phases/13-qoi-format-core-and-safe-decode/13-CONTEXT.md
@.planning/phases/13-qoi-format-core-and-safe-decode/13-01-SUMMARY.md
@.planning/phases/13-qoi-format-core-and-safe-decode/13-VERIFICATION.md
@modules/mb-image/codec/contracts.mbt
@modules/mb-image/ppm/ppm.mbt
@modules/mb-image/ppm/encode.mbt
@modules/mb-image/ppm/ppm_test.mbt
@modules/mb-image/qoi/qoi.mbt
@modules/mb-image/qoi/decode.mbt
@modules/mb-image/qoi/decode_test.mbt
@modules/mb-image/qoi/decode_wbtest.mbt
@scripts/fixtures/Generate-QoiVectors.ps1
@fixtures/qoi/cases.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the canonical QOI encoder through the unchanged codec seam</name>
  <files>modules/mb-image/qoi/qoi.mbt, modules/mb-image/qoi/encode.mbt, modules/mb-image/qoi/encode_test.mbt</files>
  <read_first>
    - modules/mb-image/codec/contracts.mbt — exact `ImageEncoder`, `EncodeOptions`, `CodecLimits`, `EncodeResult`, and typed capability-error contract.
    - modules/mb-image/ppm/ppm.mbt, modules/mb-image/ppm/encode.mbt, modules/mb-image/ppm/ppm_test.mbt — public encoder value, source validation, atomic `Budget::charge`, `write_encode_part`, MemoryWriter, and scripted Writer progress patterns.
    - modules/mb-image/qoi/qoi.mbt and modules/mb-image/qoi/decode.mbt — public package boundary, QOI pixel hash, 14-byte header decoding, channel/color-space mapping, opcode state, and marker semantics to mirror.
    - modules/mb-image/qoi/decode_test.mbt — QOI Reader helpers, exact limits/budget assertions, and public decoder invocation shape for later round trips.
    - .planning/phases/14-canonical-qoi-encode-and-four-target-vectors/14-CONTEXT.md — locked decisions D-01 through D-04.
  </read_first>
  <behavior>
    - A packed U8 RGB TopLeft built-in-sRGB view with no alpha, and a packed U8 RGBA TopLeft built-in-sRGB view with straight alpha, encode through `@codec.ImageEncoder` and preserve every logical source pixel after `QoiDecoder` decodes the output.
    - Encoded-sRGB maps to QOI header color-space byte 0 and linear-sRGB maps to byte 1; RGB maps to channel byte 3 and straight RGBA maps to channel byte 4.
    - The fixed first pass validates the whole source and limits, selects exactly the canonical chunks, calculates the actual header-plus-chunks-plus-marker byte count, and atomically charges that exact work before the first Writer call.
    - For every pixel, canonical selection is RUN for an eligible repeat, then INDEX, DIFF, LUMA, RGB, or RGBA; the output has the exact 14-byte header and eight-byte QOI end marker.
    - Unsupported component, layout, alpha, color-space, transfer, profile, orientation, opaque metadata, or inconsistent packed row arrangement produces the existing capability error at Writer position zero; declared output/width/height/pixel/work ceilings and exhausted budget also fail before output.
    - A zero-progress Writer and a scripted later host failure preserve the established error category/code and report exact requested and completed bytes; short successful writes complete normally.
  </behavior>
  <action>Add the public `QoiEncoder` value and `QoiEncoder::new()` in `qoi.mbt`, then implement `@codec.ImageEncoder` for it in a new `encode.mbt`, using the unchanged `@codec.ImageEncoder` seam per D-01. Keep this package independently importable: its encoder uses the lower-layer contracts already imported by `mb-image/qoi`, does not alter `modules/mb-image/codec/contracts.mbt`, and has no dependency on PPM or `ops`.

Write private `qoi_encode_*` helpers that first validate the complete `@storage.ImageView` per D-02: non-empty packed U8 RGB with no alpha or packed U8 RGBA with `AlphaMode::Straight`; `ColorSpaceIdentity::Srgb`; transfer limited to `EncodedSrgb` or `LinearSrgb`; `ProfileIdentity::builtin_srgb()`; `Orientation::TopLeft`; no opaque metadata; and `row_bytes == width * channel_count` with a stride that can address each logical row. Map the two transfers directly to the QOI color-space byte and reject each unrepresentable semantic with `@codec.capability_unavailable("qoi-encode", context)` before touching the Writer.

Implement a deterministic `qoi_encode_prepass` that walks the source view in row-major logical-pixel order, maintains the QOI previous pixel and 64-slot hash index, chooses chunks with D-04's exact order (eligible RUN, matching INDEX, DIFF, LUMA, RGB, RGBA), flushes runs at the legal 62-pixel boundary and at source end, and checked-adds the concrete chunk lengths to the 14-byte header and eight-byte marker. Validate width, height, pixels, exact output bytes, and exact prepass work against `CodecLimits` and call `Budget::charge(ResourceCharge::new(... work=exact_output_bytes))` once only after all source and limit validation succeeds, per D-03. The write pass must run the same state transition rules and use a QOI-local equivalent of PPM's `write_encode_part`/error remapping so `@io.write_all` reports total requested/completed bytes exactly. Then write header, canonical chunks, and the marker through the supplied forward-only Writer; return the existing empty metadata disposition and `EncodeResult` only after the complete marker succeeds.

Start with RED tests in `encode_test.mbt`, then implement and refactor. Build source images with controlled RGB/RGBA descriptors, transfer identities, orientation, opaque metadata, logical rows, budgets, and public `MemoryWriter`/scripted Writer calls. Assert public trait invocation, header bytes, `EncodeResult.bytes_written()`, decode-after-encode pixel equality, capability rejection before output, exact declared limit and budget boundaries, no-progress handling, short writes, and later Writer failure progress. Cite D-01, D-02, D-03, and D-04 in test names or nearby comments only when they explain behavior; preserve the repository's target-neutral assertions.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test --target all --frozen</automated>
  </verify>
  <done>`QoiEncoder` is public and satisfies the existing `ImageEncoder` trait; it writes canonical QOI output for all compatible RGB/RGBA source views, rejects unsupported sources and resource failures before output, and exposes exact Writer progress on every portable target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend checked QOI fixtures and prove canonical bytes on four targets</name>
  <files>fixtures/qoi/cases.json, scripts/fixtures/Generate-QoiVectors.ps1, modules/mb-image/qoi/generated_vectors.mbt, fixtures/manifest.json, modules/mb-image/qoi/encode_wbtest.mbt</files>
  <read_first>
    - fixtures/qoi/cases.json, scripts/fixtures/Generate-QoiVectors.ps1, modules/mb-image/qoi/generated_vectors.mbt, and fixtures/manifest.json — current fixture authority, deterministic format/check algorithm, generated helper layout, and QOI provenance record.
    - modules/mb-image/qoi/decode_wbtest.mbt and modules/mb-image/qoi/decode_test.mbt — all-target generated-vector execution and QOI decode byte/pixel assertion patterns.
    - modules/mb-image/qoi/encode.mbt and modules/mb-image/qoi/encode_test.mbt — canonical encoder state rules and public test helpers introduced by Task 1.
    - .planning/phases/14-canonical-qoi-encode-and-four-target-vectors/14-CONTEXT.md — locked D-03 through D-05 fixture and conformance requirements.
  </read_first>
  <behavior>
    - Generated canonical-source records contain RGB and RGBA source bytes, dimensions, channel count, transfer/color-space declaration, and exact expected QOI byte stream for every opcode family.
    - The record set demonstrates canonical INDEX reuse and index collision, DIFF and LUMA selection, byte wraparound, initial pixel behavior, one-pixel plus maximum run boundaries, RGB/RGBA fallback, header color-space mapping, and byte equality rather than only semantic decode equality.
    - Each record writes exact expected bytes, then `QoiDecoder` recovers the original source pixels and descriptor channel/transfer semantics through the existing public contracts.
    - The generator's `-Check` mode fails if the generated encoder table or manifest SHA-256/provenance record does not match `fixtures/qoi/cases.json`; it performs no network acquisition and does not modify artifacts in check mode.
  </behavior>
  <action>Extend the repository-owned `fixtures/qoi/cases.json` with a distinct canonical-encode record section whose reviewable fields describe source pixels, dimensions, channels, transfer/color-space, and expected full QOI stream. Cover D-05 exhaustively: each standard opcode family; hash-index reuse and collision; channel mode; DIFF/LUMA modular-byte behavior; initial state; one-pixel and 62-pixel run boundaries; RGB/RGBA fallback; header color-space bytes; and byte-level `decode(encode(image))` round trips. Keep Phase 13 decode/adversarial records and schedules intact, and retain the existing source/provenance classification.

Extend `Generate-QoiVectors.ps1` so its fixed-order generated MoonBit output also exports `_generated_qoi_encode_cases()` with the complete source image and canonical expected stream for every new record. Preserve its UTF-8-without-BOM, temporary `moon fmt`, byte-for-byte stale-artifact check, source-byte SHA-256 calculation, and ordered manifest rewrite/check. Update the existing `qoi-1.0-conformance-vectors` manifest record's expected-use field to include QOI-03 and QOI-05 while retaining all manifest-required provenance fields and a hash of the exact current JSON bytes. In `-Check`, generator output and the manifest record/hash must be compared only; no mutation is permitted, per D-05.

Create `encode_wbtest.mbt` to consume the generated records, materialize each compatible image through the existing model/storage public constructors, encode it with `QoiEncoder`, and compare its full Writer byte prefix with the expected stream. For each case, feed those bytes through the existing `QoiDecoder` public trait, compare all logical pixel bytes and descriptor channel/transfer semantics to the generated source, and assert output count equals expected-stream length. Include focused generated tests for zero-progress and scripted Writer failure/progress using the same deterministic expected-length data, per D-03. Run the fixture check before the all-target MoonBit suite so the white-box table cannot drift from its JSON authority. This delivers D-05 without adding a registry, streaming API, FFI, benchmark, release artifact, or Phase 15 example.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-QoiVectors.ps1 -Check; moon -C modules/mb-image test --target all --frozen</automated>
  </verify>
  <done>Checked QOI fixture authority and its generated MoonBit table prove exact canonical QOI bytes, all required state/opcode boundaries, Writer progress, and decode-after-encode equality unchanged on js, wasm, wasm-gc, and native.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller `ImageView` → encoder prepass | Caller-supplied descriptors, metadata, strides, and bytes determine permitted QOI representation and exact resource work. |
| validated prepass → forward-only Writer | Canonical byte chunks cross into an I/O implementation that may make partial or failed progress. |
| fixture JSON → generator → MoonBit test table | Repository source data is transformed into executable target-neutral conformance assertions. |
| encoder output → Phase 13 decoder | QOI header and chunk bytes must retain QOI 1.0 state semantics and exact portable interoperability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Denial of Service | `qoi_encode_prepass` and source validation | high | mitigate | Validate descriptor semantics, checked row/pixel/output arithmetic, every `CodecLimits` ceiling, and authoritative budget before the first Writer call. |
| T-14-02 | Tampering | canonical chunk state and QOI byte output | high | mitigate | Use one deterministic prepass/write transition policy with QOI hash/index, RUN boundary, wraparound, header, and marker vector checks. |
| T-14-03 | Repudiation | Writer failure progress | medium | mitigate | Route Writer calls through exact total-progress remapping and public no-progress, short-write, and host-failure assertions. |
| T-14-04 | Tampering | fixture generator and manifest | medium | mitigate | Materialize checked fixture data deterministically, verify the exact JSON SHA-256/provenance record, and run `-Check` before all-target tests. |
| T-14-SC | Tampering | package installation | low | accept | No package-manager installation occurs; this plan uses the existing MoonBit workspace, local generator, and checked-in fixture authority. |
</threat_model>

<verification>
- `pwsh -NoProfile -File scripts/fixtures/Generate-QoiVectors.ps1 -Check`
- `moon -C modules/mb-image test --target all --frozen`
- Inspect that `QoiEncoder` implements only the existing `@codec.ImageEncoder` seam and the plan makes no changes beneath `modules/mb-image/codec/`.
- Confirm generated canonical cases exercise every QOI chunk preference, two channel modes, two QOI color-space bytes, index collision, wraparound, both run boundaries, Writer progress, exact bytes, and decode-after-encode pixels.
</verification>

<success_criteria>
1. Compatible packed U8 RGB and straight-RGBA views encode through the public QOI encoder and decode back to exact source pixels.
2. Canonical prepass and write output produce one deterministic QOI 1.0 byte representation, including the required header and end marker.
3. Typed capability/resource/I-O behavior preserves pre-output atomicity and exact Writer progress.
4. Specification-derived canonical vectors and their fixture freshness gate pass unchanged on js, wasm, wasm-gc, and native.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Lossless canonical QOI bytes with four-target conformance | 14-01 | COVERED | Tasks 1 and 2 implement output and target-neutral evidence. |
| REQ | QOI-03 | Encode compatible RGB/straight-RGBA source and recover exact pixels | 14-01 | COVERED | Task 1 implements the seam and round trip; Task 2 vectorizes it. |
| REQ | QOI-05 | Specification-derived opcode/wraparound/index/run/byte-round-trip evidence on four targets | 14-01 | COVERED | Task 2 expands generator authority and its all-target test. |
| RESEARCH | — | No Phase 14 research artifact exists; checked local codec/fixture patterns are the planning authority | 14-01 | COVERED | Tasks use Phase 13's verified portable decoder and PPM encoder patterns only. |
| CONTEXT | D-01 | Independent `QoiEncoder` using unchanged `ImageEncoder` seam | 14-01 | COVERED | Task 1 action. |
| CONTEXT | D-02 | Supported source view semantics and exact QOI header mappings | 14-01 | COVERED | Task 1 action and behavior. |
| CONTEXT | D-03 | Full validation, exact deterministic prepass, atomic budget, and Writer progress | 14-01 | COVERED | Tasks 1 and 2 actions and tests. |
| CONTEXT | D-04 | Canonical chunk preference, header, and marker | 14-01 | COVERED | Task 1 state machine and Task 2 exact-stream vectors. |
| CONTEXT | D-05 | Repository fixture authority, generator, and comprehensive conformance coverage | 14-01 | COVERED | Task 2 action and behavior. |

<output>
Create `.planning/phases/14-canonical-qoi-encode-and-four-target-vectors/14-01-SUMMARY.md` when done.
</output>
