---
phase: 56-grayalpha16-adam7-factory-and-pass-profile
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16A7-01]
requirements_addressed: [GRAYA16A7-01]
must_haves:
  truths:
    - "A caller can explicitly select eager or caller-buffered GrayAlpha16 Adam7 encoding for a legal packed little-endian U16 GrayAlpha image."
    - "The selected route emits IHDR bit depth 16, colour type 4, compression and filter methods 0, and Adam7 interlace method 1."
    - "Each nonempty Adam7 pass reads GrayAlpha16 source components in PNG wire order Ghi, Glo, Ahi, Alo before the shared filtering and compression route consumes them."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Additive public eager GrayAlpha16 Adam7 selector factories."
    - path: modules/mb-image/png/encode.mbt
      provides: "Profile-aware Adam7 scalar wire reads and GrayAlpha16-only Adam7 admission."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Additive caller-buffered GrayAlpha16 Adam7 selector factories using the existing PngEncodeMachine."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Focused eager Type-4/16 Adam7 framing and seven-pass lane-order regression."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Focused caller-buffered GrayAlpha16 Adam7 eager-parity regression."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: "new_graya16_with_all_strategies stores GrayAlpha16 plus the caller-selected PngInterlaceStrategy, which reaches the profile-aware preflight gate."
      pattern: "new_graya16_with_all_strategies"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "new_graya16_with_all_strategies constructs PngEncodeMachine::new_with_profile with GrayAlpha16 and Adam7."
      pattern: "PngEncodeMachine::new_with_profile"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/encode_test.mbt
      via: "Adam7 raw and candidate reads use _png_wire_byte with GrayAlpha16 rather than direct storage-order byte reads."
      pattern: "_png_wire_byte"
---

<objective>
Deliver the first real end-to-end legal GrayAlpha16 Adam7 path: explicit eager and caller-buffered factory selection, one existing profile/machine, and profile-aware pass-lane traversal.

Purpose: satisfy GRAYA16A7-01 and locked decisions D-01 through D-04 without changing the frozen non-interlaced routes or building another encoder.
Output: additive `graya16` Adam7 factory pairs, a GrayAlpha16 Adam7 preflight/traversal seam, and one focused public eager/chunk proof.

Locked decision coverage: D-01: mirror only the explicit legal eager and chunk `graya16` Adam7 families. D-02: reuse the private GrayAlpha16 profile and sole pass traversal for Type-4/depth-16/interlace-1 Ghi/Glo/Ahi/Alo output. D-03: retain little-endian-only descriptor admission and reject Big-endian before PNG admission. D-04: leave every existing non-interlaced GrayAlpha16 and legacy route unchanged; Adam7 is opt-in.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-CONTEXT.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-PATTERNS.md
@.planning/milestones/v0.13-phases/41-adam7-opt-in-compatibility/41-01-SUMMARY.md
@.planning/milestones/v0.17-phases/54-bounded-type-4-16-encoder/54-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and deliver one legal GrayAlpha16 Adam7 eager-to-chunk path</name>
  <reversibility rating="one-way">D-01's exported factory spellings become a public package compatibility contract once consumers adopt them.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A 5x5 legal `ImageFormat::graya16()` fixture has distinct gray and alpha high/low bytes at every coordinate, making all seven Adam7 passes nonempty and lane mistakes observable.
    - `PngEncoder::new_graya16_with_interlace_strategy(Adam7)` and `PngEncoder::new_graya16_with_all_strategies(Stored, None, Adam7)` each create Type-4/depth-16 Adam7 PNG output through the existing machine.
    - The stored pass stream emits the seven Adam7 pass scanlines in pass order, with each source sample serialized as Ghi, Glo, Ahi, Alo rather than little-endian storage bytes.
    - `PngChunkEncoder` equivalents drain normally to the same complete bytes as their eager selectors and declare the same Type-4/depth-16/Adam7 IHDR.
  </behavior>
  <action>Start RED by adding focused eager and chunk tests around a shared 5x5 legal little-endian GrayAlpha16 fixture; use distinct bytes for both U16 components at every coordinate. In the eager test, select Stored/None Adam7 through both new eager APIs, inspect IHDR depth `0x10`, colour type `0x04`, methods `0`, and interlace `0x01`, then parse the Stored IDAT payload with existing test helpers or a local bounded helper to assert the seven pass scanlines in Adam7 order and every `Ghi,Glo,Ahi,Alo` lane quartet. In the chunk test, call the new narrow and all-strategy chunk APIs and compare an ordinary existing drain to the eager Stored/None oracle. After the tests fail, implement D-01 by adding `PngEncoder::new_graya16_with_interlace_strategy` and `PngEncoder::new_graya16_with_all_strategies`, plus identically shaped `PngChunkEncoder` methods; the narrow method fixes Stored/None and the all-strategy method accepts compression, filter, and interlace. Preserve all existing `new_graya16*` methods as explicit `PngInterlaceStrategy::None` per D-04. Route both chunk selectors through the existing `PngEncodeMachine::new_with_profile` and `PngEncodeProfile::GrayAlpha16`; do not add a machine, staging buffer, target branch, or hand-built IHDR. Per D-02, remove only the GrayAlpha16 non-interlace rejection in `_png_encode_preflight_with_interlace_profile`, leaving Gray8, Gray16, and GrayAlpha8 rejected for Adam7. Thread `self.profile` through `_png_adam7_raw_byte`, `_png_adam7_candidate_byte`, and `_png_adam7_row_winner`, and derive each pass sample through `_png_wire_byte` at the pass-selected source coordinates so the existing profile-aware U16 conversion supplies Ghi/Glo/Ahi/Alo before filters, planning, checksums, and replay. Reuse `_png_adam7_passes` as the sole pass-geometry authority. Per D-03, do not widen model admission: retain the existing Big-endian GrayAlpha16 descriptor rejection before PNG construction.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 eager pass profile'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 chunk parity'; exit $LASTEXITCODE</automated>
  </verify>
  <done>The two additive eager and chunk GrayAlpha16 Adam7 selector pairs use one existing profile/machine; focused Stored/None tests prove Type-4/depth-16/interlace-1 framing and all seven passes' Ghi/Glo/Ahi/Alo samples.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller ImageView to GrayAlpha16 profile admission | Caller-controlled descriptor and metadata must remain within the locked packed little-endian U16 straight-alpha contract. |
| Adam7 pass coordinate to PNG wire byte | A pass-local logical position crosses from checked storage to endian-sensitive Type-4/16 wire lanes. |
| Public factory selection to PngEncodeMachine | Eager and caller-buffered selection must feed one profile-aware bounded construction path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | GrayAlpha16 Adam7 profile gate | high | mitigate | Task 1 permits Adam7 only for GrayAlpha16 and leaves the other grayscale profile guards closed. |
| T-56-02 | Tampering | Adam7 raw/candidate/winner reads | high | mitigate | Task 1 carries the profile to the pass reads and asserts seven-pass Ghi/Glo/Ahi/Alo output with non-symmetric U16 values. |
| T-56-03 | Elevation of Privilege | U16 descriptor admission | medium | mitigate | Tasks 1-2 preserve D-03's descriptor-level Big-endian rejection instead of adding a second encoder variant. |
| T-56-04 | Tampering | legacy GrayAlpha16 constructor routing | high | mitigate | Tasks 1-2 retain explicit None forwarding and existing non-interlaced IHDR assertions while adding opt-in selectors only. |
| T-56-05 | Denial of Service | pass traversal | medium | mitigate | Task 1 retains `_png_adam7_passes`, scalar cursor traversal, and the existing PngEncodeMachine rather than allocating pass or image staging state. |
</threat_model>

<verification>
Run the two named tracer tests after Task 1. The phase-local tests validate the additive constructor/profile/pass-wire seam; Phase 58 owns independent four-target evidence.
</verification>

<success_criteria>
- GRAYA16A7-01 exposes explicit eager and caller-buffered GrayAlpha16 Adam7 factories for the legal little-endian U16 source contract.
- Stored/None Type-4/16 Adam7 output proves all seven pass samples use Ghi/Glo/Ahi/Alo PNG wire ordering through the existing pass geometry and machine.
- Big-endian descriptor rejection and existing non-interlaced GrayAlpha16 routes remain unchanged.
</success_criteria>

## Source Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | Phase 56 | Explicit eager and caller-buffered legal GrayAlpha16 Adam7 encoding produces standards-compliant Type-4/16 PNGs. | 01 | COVERED |
| REQ | GRAYA16A7-01 | Explicit factories, Type-4/depth-16/Adam7 framing, and Ghi/Glo/Ahi/Alo pass samples. | 01 Tasks 1-2 | COVERED |
| RESEARCH | intentionally skipped | Exact code patterns are supplied by `56-PATTERNS.md`; no research feature or constraint is unplanned. | 01 | COVERED |
| CONTEXT | D-01 | Add mirrored eager and chunk legal `graya16` Adam7 factory families. | 01 Task 1 | COVERED |
| CONTEXT | D-02 | Reuse private GrayAlpha16 profile and sole Adam7 traversal with Type-4/16/interlace-1 framing and lane order. | 01 Task 1 | COVERED |
| CONTEXT | D-03 | Keep little-endian-only descriptor admission and pre-PNG Big-endian rejection. | 01 Tasks 1-2 | COVERED |
| CONTEXT | D-04 | Preserve frozen non-interlaced routes with explicit additive opt-in only. | 01 Tasks 1-2 | COVERED |

<output>
Create `.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-01-SUMMARY.md` when done.
</output>
