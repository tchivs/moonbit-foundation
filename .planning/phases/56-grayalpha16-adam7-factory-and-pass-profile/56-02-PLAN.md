---
phase: 56-grayalpha16-adam7-factory-and-pass-profile
plan: "02"
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16A7-01]
requirements_addressed: [GRAYA16A7-01]
must_haves:
  truths:
    - "The explicit GrayAlpha16 Adam7 eager and caller-buffered entry points retain one coherent Stored/None public selection contract."
    - "A 5x5 non-symmetric legal source continues to prove every Adam7 pass's Type-4/16 wire lanes without broadening source descriptor admission."
    - "Existing non-interlaced GrayAlpha16 APIs remain demonstrably separate from the opt-in Adam7 surface."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Focused eager pass-profile and legacy-noninterlace regression coverage."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Focused caller-buffered selector/parity regression coverage."
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "Public new_graya16 Adam7 eager factory calls are asserted against IHDR and stored pass bytes."
      pattern: "new_graya16_with_interlace_strategy"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Public new_graya16 Adam7 chunk selectors drain through the established ordinary parity helper."
      pattern: "new_graya16_with_all_strategies"
---

<objective>
Harden Phase 56's focused public factory/profile boundary without absorbing the bounded-resource, hostile-streaming, or portable-evidence work assigned to later phases.

Purpose: ensure the tracer's production route has compact, independently intelligible eager/chunk selector coverage and preserves the exact source and compatibility fences.
Output: named focused regressions for the two public factory families and the retained non-interlaced/descriptor boundary.

Locked decision coverage: D-01: test only the mirrored legal eager and chunk `graya16` Adam7 families. D-02: test the reused GrayAlpha16 profile's Type-4/depth-16/interlace-1 and Ghi/Glo/Ahi/Alo results. D-03: retain the Big-endian descriptor rejection ahead of PNG admission. D-04: retain the existing non-interlaced GrayAlpha16 factory checks unchanged.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-CONTEXT.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-PATTERNS.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-01-SUMMARY.md
@.planning/milestones/v0.13-phases/41-adam7-opt-in-compatibility/41-01-SUMMARY.md
@.planning/milestones/v0.17-phases/54-bounded-type-4-16-encoder/54-01-SUMMARY.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the eager GrayAlpha16 Adam7 profile regression self-contained</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - Each public eager GrayAlpha16 Adam7 selector is exercised with a legal 5x5 little-endian source, declares depth 16/type 4/interlace 1, and exposes all seven nonempty pass payloads in PNG component order.
    - Existing default, compression-only, filter-only, and combined non-interlaced eager GrayAlpha16 selectors continue to assert interlace 0.
    - The Big-endian GrayAlpha16 descriptor test remains a descriptor-construction failure and does not become an encoder test.
  </behavior>
  <action>Write the named eager profile regression first and execute it as RED if Task 1's desired focused assertion is not already present. Consolidate its 5x5 fixture, Stored IDAT unpacking, and seven-pass expected lanes into local test helpers only when they improve readability of the existing Phase 56 tests; do not copy structural geometry into production code. Invoke both `new_graya16_with_interlace_strategy(Adam7)` and `new_graya16_with_all_strategies(Stored, None, Adam7)`, assert equal complete bytes where that follows from fixed strategies, and inspect their Type-4/depth-16/interlace-1 fields plus every pass's `Ghi,Glo,Ahi,Alo` source lanes. Per D-03 and D-04, retain the pre-PNG Big-endian descriptor rejection and existing `new_graya16*` non-interlace checks without changing their expected bytes or API signatures. Do not add decode canonicalization, frozen vectors, all-target runner work, or strategy/resource matrices.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 eager pass profile'; exit $LASTEXITCODE</automated>
  </verify>
  <done>The eager test names and directly proves both public Adam7 selectors, all seven pass lane quartets, IHDR Type-4/16/Adam7 fields, and the unchanged descriptor/noninterlaced boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make caller-buffered GrayAlpha16 Adam7 parity explicit</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - The narrow and all-strategy caller-buffered GrayAlpha16 Adam7 factories accept a legal 5x5 source and drain to their corresponding eager outputs.
    - The resulting complete PNGs declare Type 4, depth 16, and Adam7 interlace, while the existing non-interlaced chunk factories retain interlace 0 checks.
    - The test uses one ordinary drain schedule and does not claim resource, accepted-only accounting, replay, or hostile-capacity guarantees.
  </behavior>
  <action>Write the named caller-buffered parity regression first and run it RED before filling any missing test wiring. Use fresh encoders for `new_graya16_with_interlace_strategy` and `new_graya16_with_all_strategies(Stored, None, Adam7)`, drain each with the established ordinary helper, and compare each result to the matching eager selected route. Assert their IHDR Type-4/depth-16/interlace-1 fields and retain the existing non-interlaced GrayAlpha16 chunk assertions for old factory shapes per D-04. Per D-01 and D-02, validate public selectors only; do not modify `PngChunkEncoder`, `PngEncodeMachine`, leases, output accounting, source-revision replay, or adaptive/compression matrices in this plan. Those semantics and public hostile schedules are exclusively Phase 57–58 work.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 chunk parity'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen; exit $LASTEXITCODE</automated>
  </verify>
  <done>The caller-buffered public Adam7 selectors have ordinary eager-byte parity and Type-4/depth-16/interlace-1 framing evidence, with the whole native PNG suite protecting existing routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legal descriptor to public factory | A caller's GrayAlpha16 source must retain the locked model identity before any encoder route is chosen. |
| Caller-buffered factory to emitted PNG | Factory selection must preserve the selected profile/interlace identity across eager and chunk construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-06 | Tampering | eager factory/profile selection | high | mitigate | Task 1 asserts both public eager selectors and literal Type-4/16/Adam7 pass framing from a non-symmetric source. |
| T-56-07 | Tampering | chunk factory/profile selection | high | mitigate | Task 2 compares both public chunk selectors with their eager routes and inspects the emitted IHDR identity. |
| T-56-08 | Elevation of Privilege | GrayAlpha16 descriptor boundary | medium | mitigate | Task 1 preserves D-03's Big-endian construction rejection as the only supported model boundary. |
| T-56-09 | Tampering | frozen non-interlaced factory routing | high | mitigate | Tasks 1-2 retain D-04's existing interlace-0 assertions for non-Adam7 GrayAlpha16 factories. |
</threat_model>

<verification>
Run each named focused test after its task and the complete native PNG suite after Task 2. This plan intentionally does not perform Phase 58 portability certification.
</verification>

<success_criteria>
- GRAYA16A7-01's eager/chunk factory behavior is locally verifiable through exact legal source, IHDR, and seven-pass wire-lane assertions.
- The Phase 53 little-endian descriptor contract and existing non-interlaced GrayAlpha16 routes remain visibly protected by their established tests.
- No Phase 57/58 resource/replay matrix, hostile schedule, or public all-target evidence is introduced.
</success_criteria>

## Source Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | Phase 56 | Explicit legal eager/chunk Adam7 Type-4/16 selection remains comprehensible and testable at its public boundary. | 02 | COVERED |
| REQ | GRAYA16A7-01 | Eager/chunk selectors and pass-aware Type-4/16 wire fidelity. | 02 Tasks 1-2 | COVERED |
| RESEARCH | intentionally skipped | `56-PATTERNS.md` supplies exact existing test patterns; no research feature or constraint is unplanned. | 02 | COVERED |
| CONTEXT | D-01 | Public eager and chunk `graya16` Adam7 factory family coverage. | 02 Tasks 1-2 | COVERED |
| CONTEXT | D-02 | Existing GrayAlpha16 profile/machine emits Type-4/16/interlace-1 and profile-aware pass lanes. | 02 Tasks 1-2 | COVERED |
| CONTEXT | D-03 | Little-endian-only model admission remains enforced ahead of PNG selection. | 02 Task 1 | COVERED |
| CONTEXT | D-04 | Existing non-interlaced routes remain opt-out of Adam7 and byte-route stable. | 02 Tasks 1-2 | COVERED |

<output>
Create `.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-02-SUMMARY.md` when done.
</output>
