---
phase: 18-resumable-qoi-buffer-encode
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/qoi/qoi.mbt
  - modules/mb-image/qoi/encode.mbt
  - modules/mb-image/qoi/stream_encode.mbt
  - modules/mb-image/qoi/stream_encode_test.mbt
  - modules/mb-image/qoi/stream_encode_wbtest.mbt
  - modules/mb-image/qoi/pkg.generated.mbti
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
autonomous: true
requirements: [QSTR-04, QSTR-05]
must_haves:
  truths:
    - "A caller can repeatedly supply arbitrary callback-scoped mutable byte leases, including zero- and one-byte capacities, and receives exact per-call bytes written with NeedOutput until the final end-marker byte is copied."
    - "A valid stream produces exactly the eager QOI encoder's canonical byte sequence once, in order, and its Finished result reports the exact complete byte total."
    - "All compatibility, exact-length, limit, metadata, and work-budget checks succeed before the first destination byte can change; construction failures leave caller destinations untouched and expose no stream encoder."
    - "The encoder retains only the caller's immutable ImageView plus private pending token state; it never retains a mutable lease, snapshots source pixels, adds a lock, or creates an output staging allocation."
    - "A terminal pull result is sticky: later pulls report zero bytes and the deterministic typed state error."
  artifacts:
    - path: modules/mb-image/qoi/qoi.mbt
      provides: "Public QoiStreamEncoder and QoiStreamPullResult/QoiStreamPullOutcome contract beside unchanged eager traits."
    - path: modules/mb-image/qoi/encode.mbt
      provides: "Private shared canonical QOI token-generation state used by eager and streaming encoders."
    - path: modules/mb-image/qoi/stream_encode.mbt
      provides: "Zero-copy caller-lease pull encoder with eager-equivalent construction preflight and sticky terminal state."
    - path: modules/mb-image/qoi/stream_encode_test.mbt
      provides: "Public capacity, lease-lifetime, terminal, and preflight-output contract tests."
    - path: modules/mb-image/qoi/stream_encode_wbtest.mbt
      provides: "Generated-vector canonicality, one-byte schedule, pending-token, budget, and source-contract tests."
    - path: policy/foundation.json
      provides: "Exact QOI production/test inventory and compiler-derived stream-encoder public interface."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Both broad and scoped QOI policy assertions and negative fixtures updated for stream-encoder order and contents."
  key_links:
    - from: modules/mb-image/qoi/stream_encode.mbt
      to: modules/mb-image/qoi/encode.mbt
      via: "A shared private token generator preserves the eager encoder's exact header, opcode, deferred-run, and marker decisions."
    - from: modules/mb-image/qoi/stream_encode.mbt
      to: modules/mb-core/bytes/views.mbt
      via: "Each pull writes only through the supplied live MutByteLease and stores no lease after that call returns."
    - from: modules/mb-image/qoi/stream_encode.mbt
      to: modules/mb-image/codec/contracts.mbt
      via: "Construction repeats CodecLimits and ResourceCharge preflight before a pull can expose an output byte."
    - from: policy/foundation.json
      to: scripts/quality/Assert-Policy.ps1
      via: "The broad and scoped QOI validators share the exact policy-ordered stream-encoder source and interface inventory."
---

<objective>
Deliver a bounded, zero-copy QOI stream encoder that drains canonical QOI bytes through caller-owned mutable leases.

Purpose: Give portable callers resumable output without changing ImageEncoder, Writer semantics, or the already-canonical eager representation.
Output: A public QoiStreamEncoder pull contract, shared eager/stream token generation, hostile capacity and resource tests, and exact QOI policy inventory updates.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-resumable-qoi-buffer-encode/18-CONTEXT.md
@.planning/phases/17-resumable-qoi-chunk-decode/17-01-SUMMARY.md
@modules/mb-core/bytes/views.mbt
@modules/mb-image/codec/contracts.mbt
@modules/mb-image/qoi/qoi.mbt
@modules/mb-image/qoi/encode.mbt
@modules/mb-image/qoi/encode_test.mbt
@modules/mb-image/qoi/encode_wbtest.mbt
@modules/mb-image/qoi/generated_vectors.mbt
@modules/mb-image/qoi/stream_decode.mbt
@modules/mb-image/storage/views.mbt
@modules/mb-image/storage/owned_image.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the caller-lease stream encoder and shared canonical token state</name>
  <files>modules/mb-image/qoi/qoi.mbt, modules/mb-image/qoi/encode.mbt, modules/mb-image/qoi/stream_encode.mbt, modules/mb-image/qoi/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: QoiStreamEncoder::new(valid_image.view(), limits, budget, diagnostics) succeeds only after eager-equivalent source validation, exact canonical chunk-length calculation, limit order, metadata disposition setup, and one ResourceCharge; incompatible source, output/work limits, and budget exhaustion return the existing typed error before any caller lease is supplied or any output byte is visible.
    - Test 2: pull on a zero-length MutByteLease returns QoiStreamPullResult with written() equal to zero and NeedOutput, changing neither pending offset nor total bytes; a one-byte lease can advance every header, opcode, deferred-run, and end-marker byte one copy at a time.
    - Test 3: QoiStreamPullResult exposes written(), total_written(), and outcome(); QoiStreamPullOutcome is exactly NeedOutput, Finished, or Failed(CoreError). Finished occurs only on the call that copies the eighth marker byte and reports the complete total.
    - Test 4: A completed pull stream and a stream that reaches a terminal failure both reject every later pull with written() equal to zero and the deterministic stream-state CoreError; no source byte or lease is reprocessed.
    - Test 5: Pull stores no MutByteLease after the callback returns; a fresh lease can serve the next call. The retained ImageView contract is documented at new(): callers must keep its backing image unchanged through terminal state, and the implementation makes no snapshot, lock, or full-output buffer.
  </behavior>
  <action>Write the black-box tests first, then add QoiStreamEncoder beside the Phase 17 types in qoi.mbt per D-01. Lock the public API as QoiStreamEncoder::new(ImageView, CodecLimits, Budget, Diagnostics) -> Result[QoiStreamEncoder, CoreError], pull(Self, MutByteLease) -> QoiStreamPullResult, QoiStreamPullOutcome { NeedOutput, Finished, Failed(CoreError) }, and QoiStreamPullResult accessors written() -> UInt64, total_written() -> UInt64, and outcome() -> QoiStreamPullOutcome. Document D-07 directly on the public constructor/type: the retained ImageView is zero-copy and callers must not alter its backing from successful new through terminal state. Keep QoiEncoder, ImageEncoder, Writer behavior, and Phase 17 decode types unchanged.

Refactor only the private canonical emission mechanics in encode.mbt into a shared private token generator/state consumed by both eager qoi_write_chunks and stream_encode.mbt. It must make the existing eager header, INDEX, DIFF, LUMA, RGB, RGBA, deferred RUN, and eight-byte marker choices exactly once; preserve existing eager byte output and Writer-progress behavior. In stream_encode.mbt, retain the immutable ImageView and only fixed-size private pending header/token/marker bytes plus pending offset, canonical encoder state, total length, and total copied count. Per D-05, advance canonical state when generating a complete pending token, then advance only the pending offset and total count as bytes are copied into the currently supplied lease. Never retain a MutByteLease, allocate a whole-output staging buffer, or regenerate a partially copied token.

Per D-04, perform construction preflight before any pull can write: preserve the eager sequence qoi_encode_source, checked pixel count, exact qoi_chunk_length, checked total, CodecLimits checks in output-bytes/width/height/pixels/work order, then the single eager-equivalent ResourceCharge. Create the metadata disposition and initial private header/pending state before returning the encoder. A failed constructor returns the typed eager-equivalent error, leaves the budget atomic, and provides no encoder to pull. Per D-02, a zero-capacity lease copies nothing and yields NeedOutput; only copying the final end-marker byte yields Finished. Per D-03, store terminal completion/failure and make later pull return Failed(stream-state error) with written zero. Do not add streaming input, a public streaming example, FFI, PNG/DEFLATE, release work, registry work, or source snapshots/locking.</action>
  <verify>
    <automated>moon -C modules/mb-image test qoi --target all --frozen</automated>
  </verify>
  <done>QSTR-04 has a public caller-lease pull interface with deterministic NeedOutput/Finished/Failed results, exact per-call and cumulative progress, no retained lease, and byte-for-byte eager canonical behavior for arbitrary capacities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove canonical capacity schedules and update exact QOI policy guards</name>
  <files>modules/mb-image/qoi/stream_encode_wbtest.mbt, modules/mb-image/qoi/pkg.generated.mbti, policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <behavior>
    - Test 1: Every generated QOI encoder vector drains through zero, one-byte, token-boundary, and deterministic mixed-capacity schedules to precisely the eager canonical bytes, with no duplicate, missing, or reordered byte and a Finished total equal to eager EncodeResult.bytes_written.
    - Test 2: Each private pending header/token/deferred-run/marker state is generated exactly once before any of its bytes are copied; capacities that split each token preserve state, offset, and byte order, and Finished is unavailable until the final marker byte.
    - Test 3: Constructor capability, total-output, work-limit, and underfunded-budget failures preserve all observable budget remaining fields and cannot mutate a destination; successful construction performs the one work charge before zero-capacity and one-byte pulls, with no second charge while draining.
    - Test 4: The compiler-generated interface, policy/foundation.json, broad Assert-FoundationPolicy, scoped Assert-QoiFoundationPolicy, and scoped negative fixtures carry QoiStreamEncoder, QoiStreamPullResult, QoiStreamPullOutcome, stream_encode.mbt, and both stream encode test files in one exact policy order.
  </behavior>
  <action>Start with failing white-box tests in stream_encode_wbtest.mbt. Consume every _generated_qoi_encode_cases record, compare each stream drain against the eager bytes from the existing vector helper, and run zero, one-byte, token-edge, and deterministic mixed-capacity schedules. Assert the complete byte sequence, per-call written, cumulative total, final result timing, and no duplicate/omitted/reordered byte. Use the existing encode budget/limit helpers to assert full ResourceLimits atomicity for failed new, exact successful work charge before the first pull, and no additional charge while output is drained. Cover D-05 internal pending state without a public test hook, and cover D-07 by retaining the existing immutable ImageView rather than creating a copied image or buffer.

Run moon -C modules/mb-image info --target all --frozen and do not hand-edit pkg.generated.mbti. Update policy/foundation.json only for the qoi public package: append stream_encode.mbt after stream_decode.mbt in production_sources; append stream_encode.mbt, stream_encode_test.mbt, and stream_encode_wbtest.mbt to QOI publication_files; and copy the compiler-derived public semantic interface with the new stream encoder types/accessors. In scripts/quality/Assert-Policy.ps1, update both hard-coded production-source lists in broad Assert-FoundationPolicy and scoped Assert-QoiFoundationPolicy to exactly that policy order, update the exact QOI directory/interface assertions, and extend Assert-QoiQualificationNegativeFixtures with missing, extra, and reordered stream-encoder production/file/interface cases. Retain the Phase 17 controlled temporary-policy broad fixture so it exercises the broad source-order rejection while neutralizing only the known unrelated ops-policy mismatch. Preserve QOI imports, targets, candidate stability, current eager and stream-decoder surfaces, the scoped Qoi lane and public-example behavior, and all registry/release artifacts.</action>
  <verify>
    <automated>moon -C modules/mb-image info --target all --frozen; moon -C modules/mb-image test qoi --target all --frozen; pwsh -NoProfile -Command "&amp; { . ./scripts/quality/Assert-Policy.ps1; Assert-QoiFoundationPolicy -PolicyPath policy/foundation.json; Assert-QoiQualificationNegativeFixtures -PolicyPath policy/foundation.json; try { Assert-FoundationPolicy -PolicyPath policy/foundation.json; throw 'expected pre-existing ops policy mismatch' } catch { if ($_.Exception.Message -notmatch 'ops') { throw } } }"; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Qoi</automated>
  </verify>
  <done>QSTR-05 proves eager-equivalent preflight and one-charge behavior before the first output byte, exact canonical output for every adversarial capacity schedule, and an exact compiler/policy inventory checked through both broad and scoped QOI assertion paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| immutable ImageView → stream constructor | Caller-provided source metadata and pixels determine all preflight and canonical-token state; caller must preserve backing stability. |
| stream state → caller MutByteLease | Private pending bytes are copied to a callback-scoped mutable destination that cannot outlive pull. |
| preflight → first output byte | Source compatibility, length, limits, metadata, and budget charge must finish before an output byte can become visible. |
| compiler interface/source inventory → QOI policy | Public stream additions are constrained by exact broad and scoped policy assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Denial of Service | source/preflight | high | mitigate | Preserve eager checked dimensions, canonical-length calculation, output/width/height/pixels/work limit ordering, and one authoritative ResourceCharge before pull. |
| T-18-02 | Tampering | pending canonical token state | high | mitigate | Share eager token selection, generate a private token once, copy with an independent offset, and prove all byte-capacity schedules against eager canonical bytes. |
| T-18-03 | Information Disclosure | mutable caller lease | medium | mitigate | Accept the lease only for one pull, retain no lease/reference, and verify a fresh later lease is required for the next copy. |
| T-18-04 | Tampering | source lifetime | medium | mitigate | Retain only immutable ImageView and document the stable-backing precondition; do not snapshot, lock, or add an allocation path that changes ownership semantics. |
| T-18-05 | Tampering | terminal progress | medium | mitigate | Record terminal success/failure, report zero written after terminal state, and test final-marker timing plus no reprocessing. |
| T-18-06 | Tampering | QOI policy inventory | medium | mitigate | Update both production-source assertions, exact directory/interface checks, and scoped negative fixtures in lockstep with compiler output. |
| T-18-SC | Tampering | package installs | low | accept | No package-manager installation occurs; only existing workspace packages and tools are used. |
</threat_model>

<verification>
- `moon -C modules/mb-image info --target all --frozen` regenerates and validates the public QOI stream-encoder interface on every target.
- `moon -C modules/mb-image test qoi --target all --frozen` runs eager-regression, stream-encoder public, and white-box schedule tests on js, wasm, wasm-gc, and native.
- `Assert-QoiFoundationPolicy`, `Assert-QoiQualificationNegativeFixtures`, and the controlled broad fixture prove both QOI production-source assertion sites use the exact same ordered inventory.
- `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Qoi` retains the existing scoped QOI quality lane without adding a Phase 19 streaming public example or release behavior.
</verification>

<success_criteria>
1. QSTR-04: caller-controlled zero, one, and arbitrary capacities return deterministic per-call progress and produce the eager canonical byte stream exactly once and in order.
2. QSTR-05: all source, length, limits, metadata, and budget failures occur at construction before output, and successful construction charges the defined work once before output starts.
3. Finished reports the exact completed byte count only after the final marker byte; later calls report zero bytes and a deterministic state error.
4. The stream encoder is portable on js, wasm, wasm-gc, and native, and broad/scoped exact QOI policy inventory checks remain fail closed.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan coverage | Status |
|--------|----|---------------|---------------|--------|
| GOAL | — | Resumable canonical QOI output through caller buffers without changing forward-only I/O | Tasks 1-2 | COVERED |
| REQ | QSTR-04 | Arbitrary output capacities and deterministic NeedOutput without byte loss, duplication, or reorder | Tasks 1-2 | COVERED |
| REQ | QSTR-05 | Eager-equivalent preflight before output and exact completed byte total | Tasks 1-2 | COVERED |
| RESEARCH | — | No RESEARCH.md by deliberate orchestration decision; Phase 17 implementation and eager encoder are the canonical local seams | Tasks 1-2 | COVERED |
| CONTEXT | D-01 | Separate QoiStreamEncoder and per-call pull outcome | Task 1 | COVERED |
| CONTEXT | D-02 | No retained lease, zero-capacity progress, final-marker completion | Task 1 | COVERED |
| CONTEXT | D-03 | Sticky terminal pulls with zero written state error | Task 1 | COVERED |
| CONTEXT | D-04 | Exact eager validation/length/limits/metadata/budget before output | Tasks 1-2 | COVERED |
| CONTEXT | D-05 | Private pending tokens and copy-only offset advancement | Tasks 1-2 | COVERED |
| CONTEXT | D-06 | Zero/one/arbitrary schedules equal eager canonical bytes | Tasks 1-2 | COVERED |
| CONTEXT | D-07 | Retained immutable source contract without snapshot, lock, or new allocation path | Tasks 1-2 | COVERED |
</source_audit>

<output>
Create `.planning/phases/18-resumable-qoi-buffer-encode/18-01-SUMMARY.md` when done.
</output>
