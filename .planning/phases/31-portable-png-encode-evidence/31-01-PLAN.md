---
phase: 31-portable-png-encode-evidence
plan: "01"
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
  - examples/png-portable/main/main.mbt
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
requirements: [PNGE-04, PNGE-05]
must_haves:
  truths:
    - "Maintainers can run target-directory-isolated js, wasm, wasm-gc, and native evidence that proves public PngChunkEncoder output under empty, one-byte, and ragged caller capacities has exact progress, canonical eager-byte parity, atomic preflight parity, and non-mutating sticky terminals."
    - "The sole png-portable executable uses only public PngChunkDecoder, resize_bilinear, and PngChunkEncoder contracts to emit the frozen 78-byte PNG with digest 626208771 through its caller-owned output schedule."
    - "The existing scoped PNG lane exact-matches the one public chunk-decode-resize-chunk-encode evidence line on every required target without reaching Required, QOI, release, registry, or credential paths."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Named public hostile-capacity, preflight-parity, progress, canonical-byte, and sticky-terminal evidence selected by the existing isolated target runner."
    - path: examples/png-portable/main/main.mbt
      provides: "The repository's single public portable PNG chunk-decode, bilinear-resize, and chunk-encode workflow with frozen output evidence."
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: "PNG-only exact four-target gate for the chunk-decode-resize-chunk-encode workflow."
  key_links:
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "Black-box tests construct PngChunkEncoder, pass only callback-scoped MutByteLease values to pull, and compare accepted public output with a separate eager PngEncoder oracle."
      pattern: "PngChunkEncoder::new|PngChunkEncoder::pull|PngChunkPullOutcome"
    - from: scripts/quality/Invoke-PngEncodeEvidence.ps1
      to: modules/mb-image/png/stream_encode_test.mbt
      via: "The unchanged isolated runner's shared test filter selects the added public hostile-capacity evidence once per target-specific build directory."
      pattern: "PNG encoder isolated four-target evidence"
    - from: examples/png-portable/main/main.mbt
      to: scripts/quality/Invoke-MoonQuality.ps1
      via: "The existing PNG lane runs the executable once on every portable target and exact-matches its single deterministic evidence line."
      pattern: "PngChunkDecoder|resize_bilinear|PngChunkEncoder|example=png-portable"
---

<objective>
Prove the published caller-buffered PNG encoder under hostile portable runtime schedules and make the established public PNG workflow use it end to end.

Purpose: PNGE-04 and PNGE-05 qualify the existing Phase 29/30 encoder behavior across all required runtimes; they do not alter the PNG API, canonical emitter, policy, or release path.
Output: Four-target selected public contract evidence, an in-place public chunk-decode → resize → chunk-encode executable, and one scoped PNG quality-lane expectation.
</objective>

<prerequisites>
- Phase 30 Plan 01 is complete: `PngChunkEncoder`, its sticky pull outcomes, and the private Phase 29 `PngEncodeMachine` are published and policy-locked.
- `scripts/quality/Invoke-PngEncodeEvidence.ps1` already runs its shared filter against one target and target-specific build directory; this plan extends its selected test set without changing runner routing.
- The Phase 28 `png-portable` source bytes, sixteen input pushes, 3×1 bilinear result, 78-byte output, and digest `626208771` remain the established portable evidence baseline.
</prerequisites>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-portable-png-encode-evidence/31-RESEARCH.md
@.planning/phases/30-public-png-chunk-encoder/30-01-SUMMARY.md
@.planning/phases/30-public-png-chunk-encoder/30-VERIFICATION.md
@.planning/milestones/v0.8-phases/28-portable-png-streaming-evidence/28-01-SUMMARY.md
@.planning/milestones/v0.8-phases/28-portable-png-streaming-evidence/28-01-VERIFICATION.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
@examples/png-portable/main/main.mbt
@scripts/quality/Invoke-PngEncodeEvidence.ps1
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add selected four-target public hostile-output and preflight evidence</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A new public black-box test whose name contains `PNG encoder isolated four-target evidence` runs for RGB8 and straight-RGBA8 images under `empty-then-one-byte` (`[0, 1]`), `one-byte` (`[1]`), and `zero-tiny-ragged` (`[0, 8, 4, 1, 13, 2, 5, 3, 21]`) output capacities.
    - Each active pull reports `written() <= capacity`, and `total_written()` equals the aggregate prefix after copying only the reported callback-scoped destination bytes. Empty capacity produces `0/0/NeedOutput`; every completed aggregate byte-for-byte equals the independent eager PngEncoder oracle.
    - For each successful drain, a fresh sentinel-filled terminal destination receives zero written bytes, preserves the final total and Finished outcome, and remains byte-for-byte unchanged across repeated pulls. A released active lease produces a typed Failed outcome that replays unchanged to a fresh sentinel destination with zero new bytes and no mutation.
    - Direct PngChunkEncoder construction has eager-equivalent rejection and zero-output boundaries for an output-byte limit, exhausted work budget, and unsupported retained metadata: category, code, operation, context, requested, completed, and limit match the eager oracle where present, eager Writer position remains zero, and rejected chunk construction leaves its independent budget work remainder unchanged.
  </behavior>
  <action>Start RED-first in `modules/mb-image/png/stream_encode_test.mbt`. Extend the existing `png_stream_test_image`, `png_stream_test_eager`, `png_chunk_test_owner`, `png_chunk_test_pull`, and public `PngChunkEncoder` helpers; do not create a second encoder, private-machine assertion, fixture family, or test package. Add one named test containing the existing `PNG encoder isolated four-target evidence` selector so `Invoke-PngEncodeEvidence.ps1` runs it beside the established three target-isolated regressions without a runner edit. In that test, drain fresh encoders for both RGB8 and straight-RGBA8 sources under exactly the three named schedules. For every callback-scoped lease, append only indexes below `written()` after `with_mut` returns; assert active accounting before advancing and compare the complete aggregate exactly with the eager oracle. Exercise empty capacity, final/repeated Finished sentinel non-mutation, and the current released-lease Failed replay with field-for-field CoreError equality and sentinel non-mutation. Pair public constructor attempts with independent eager `PngEncoder` calls for output limit, work-budget exhaustion, and opaque-metadata capability rejection; verify error observables, zero eager Writer position, and unchanged rejected-constructor work budget. Preserve the frozen public API, the private canonical machine, eager Writer behavior, package policy, source inventory, compression/colour semantics, FFI boundary, and all release or registry routing. Make this selected evidence pass first on native, then invoke the existing runner separately for every portable target.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target js; pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target wasm; pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target wasm-gc; pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target native</automated>
  </verify>
  <done>One selected public evidence test runs with the existing three tests in independently isolated js, wasm, wasm-gc, and native commands and proves hostile capacity progress, complete eager-byte parity, constructor/resource parity, and non-mutating sticky terminals for the published encoder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the single PNG portable consumer to public chunk output</name>
  <files>examples/png-portable/main/main.mbt</files>
  <behavior>
    - The current fixed 75-byte source still reaches public PngChunkDecoder through its exact sixteen-turn input schedule, `finish()` remains the sole decoded-image transfer, and resize_bilinear retains the frozen 3×1 pixel assertions.
    - The resize result is constructed with public PngChunkEncoder and drained through a reusable caller-owned OwnedBytes owner using the cyclic output schedule `[0, 8, 4, 1, 13, 2, 5, 3, 21]`. Each pull proves `written() <= capacity` and `total_written() == collected_prefix_length + written()` before copying the reported prefix after callback close.
    - The public output drain makes exactly 14 pulls (including the zero-capacity turn), terminates only with Finished, produces exactly 78 bytes equal to the frozen expected PNG, and preserves digest `626208771`.
    - Every portable target prints exactly `example=png-portable input_schedule=workflow-zero-signature-ihdr-idat-deflate-iend pushes=16 bytes_read=75 output_schedule=zero-tiny-ragged output_pulls=14 bytes_written=78 width=3 height=1 resize_bilinear digest=626208771` once.
  </behavior>
  <action>Write the public-output assertions in `examples/png-portable/main/main.mbt` before replacing its final eager writer route. Retain the fixed source/expected bytes, decoder construction, all sixteen input pushes, finish-only decode transfer, limits, resize, semantic pixels, exact byte comparison, and digest. Replace only the `MemoryWriter` plus eager `ImageEncoder::encode(PngEncoder)` section with `@png.PngChunkEncoder::new` and one reusable caller-owned `@bytes.OwnedBytes` owner sized to the schedule maximum of 21 bytes. Cycle exactly `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]`; after each `with_mut` callback returns, validate the public result, append exactly its accepted prefix to a separate output array, and handle NeedOutput, Finished, and Failed explicitly. Require the terminal path to finish in 14 pulls, then materialize the collected bytes only for the pre-existing exact-byte and digest assertions. Freeze the output label and count in the sole status line using the stated `zero-tiny-ragged` / `output_pulls=14` convention, matching the established QOI portable consumer's explicit input/output schedule naming. Do not retain a mutable lease after its callback, retain eager PngEncoder or MemoryWriter in this workflow, add a second example, change public declarations, alter source bytes/pixel operation/output bytes, or extend to release, registry, FFI, compression, or colour work. Run the executable directly on all four targets before changing the quality expectation.</action>
  <verify>
    <automated>moon -C examples/png-portable run main --target js --frozen; moon -C examples/png-portable run main --target wasm --frozen; moon -C examples/png-portable run main --target wasm-gc --frozen; moon -C examples/png-portable run main --target native --frozen</automated>
  </verify>
  <done>The only PNG portable executable visibly uses public caller-buffered decoding and encoding around its existing bilinear operation, and all four direct target runs emit the identical frozen 75-read/78-written/digest status line with the explicit output schedule and pull count.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze chunk-encode evidence in the scoped PNG quality lane</name>
  <files>scripts/quality/Invoke-MoonQuality.ps1</files>
  <action>Update only `Invoke-PngQualityLane` and the matching `Assert-PngLaneIsolation` expected trace in `scripts/quality/Invoke-MoonQuality.ps1`. Rename the public-workflow stage to `PNG portable chunk-decode-resize-chunk-encode public workflow`; for js, wasm, wasm-gc, and native, exact-match the Task 2 status line including `output_schedule=zero-tiny-ragged` and `output_pulls=14`. Keep the existing PNG foundation/interface policy, scoped negatives, exact package allowlist, structural/decode fixture freshness checks, colour README/conformance checks, four-target PNG suite, and ordered isolation proof unchanged. Preserve the fail-closed stubs for broad foundation, QOI, Required, and release routes; the lane remains PNG-scoped and does not add registry, credential, publication, or alternate target controller work. Run the isolated lane after Tasks 1 and 2 are green.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The scoped PNG lane runs all existing checks plus four exact public chunk-decode-resize-chunk-encode evidence runs in its asserted isolation trace, without reaching release or registry automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView, limits, and budget → public PngChunkEncoder construction | Caller-selected resource policy must remain equivalent to eager construction before an output lease exists. |
| Public PngChunkEncoder → callback-scoped MutByteLease | Hostile empty/tiny/ragged capacities and terminal callers must not cause byte loss, counter drift, or a later owner mutation. |
| png-portable evidence → scoped PNG quality lane | A target-neutral executable line must prove the public consumer while the lane remains isolated from release infrastructure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering / Denial of Service | public output capacity scheduling | high | mitigate | Run public RGB8/RGBA8 drains under empty-one-byte, one-byte, and zero-tiny-ragged capacities; assert per-pull/cumulative accounting and full eager-byte equality on every target. |
| T-31-02 | Tampering / Information Disclosure | terminal caller lease ownership | high | mitigate | Use fresh sentinel destinations after Finished and typed Failed outcomes; assert zero new bytes, stable terminal/error state, and byte-for-byte no mutation. |
| T-31-03 | Denial of Service | public encoder admission | high | mitigate | Pair public output-limit, work-budget, and retained-metadata rejection with independent eager results; assert error observables, zero eager output, and unchanged rejected-constructor work budget. |
| T-31-04 | Repudiation | portable workflow evidence | medium | mitigate | Freeze source/read schedule, output schedule label and pull count, 78-byte exact result, digest, and one exact line independently run on js, wasm, wasm-gc, and native. |
| T-31-05 | Elevation of Privilege | quality-lane routing | high | mitigate | Preserve `Assert-PngLaneIsolation` fail-closed broad/QOI/Required/release stubs and exact staged trace; change only its PNG public-workflow stage. |
</threat_model>

<verification>
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target js`
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target wasm`
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target wasm-gc`
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target native`
- `moon -C examples/png-portable run main --target js --frozen`
- `moon -C examples/png-portable run main --target wasm --frozen`
- `moon -C examples/png-portable run main --target wasm-gc --frozen`
- `moon -C examples/png-portable run main --target native --frozen`
- `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- The existing isolated runner supplies runtime proof on each required target that the public encoder preserves exact output progress, full canonical eager-byte parity, atomic preflight/resource behavior, and non-mutating sticky success/failure terminals under hostile caller capacities.
- `png-portable` is the single portable public consumer and executes chunk decode → bilinear resize → chunk encode with exactly the frozen bytes, digest, schedule label, and pull count on all four targets.
- The PNG-only lane exact-matches that consumer evidence and preserves its fail-closed scope, excluding release, registry, credential, API redesign, policy redesign, FFI, and alternate automation paths.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan coverage | Status |
|--------|----|---------------|---------------|--------|
| GOAL | — | Four-target hostile-schedule proof and a public decode-process-encode workflow validate the complete contract. | Tasks 1-3 add the target-selected contract test, convert the existing public consumer, and gate its exact four-target evidence. | COVERED |
| REQ | PNGE-04 | Verify hostile capacities, eager/chunk parity, limits, budgets, and terminal behavior on js, wasm, wasm-gc, and native. | Task 1 exercises all listed public boundaries through the unchanged target-isolated runner; Task 3 retains full package evidence in the PNG lane. | COVERED |
| REQ | PNGE-05 | Run one public portable chunk-decode → image operation → chunk-encode workflow with deterministic evidence. | Task 2 uses PngChunkDecoder, resize_bilinear, and PngChunkEncoder in the existing executable; Task 3 exact-matches its one line per target. | COVERED |
| RESEARCH | — | Reuse the isolated target runner and one named black-box test; do not build a second runner/package. | Task 1 deliberately adds the runner-filtered test to `stream_encode_test.mbt` and leaves `Invoke-PngEncodeEvidence.ps1` unchanged. | COVERED |
| RESEARCH | — | Exercise empty/one-byte/ragged output, exact progress, byte parity, terminal destination non-mutation, and preflight budget parity. | Task 1 specifies all schedules, public observables, sentinel checks, and output/work/metadata constructor pairs. | COVERED |
| RESEARCH | — | Convert png-portable in place and retain the Phase 28 bytes/digest workflow. | Task 2 changes only final output ownership, preserves decode/resize/source/output evidence, and resolves the schedule label and pull count to `zero-tiny-ragged` / 14. | COVERED |
| RESEARCH | — | Scope quality evidence to PNG without release or registry expansion. | Task 3 changes only the existing PNG stage/trace expectation and preserves lane isolation. | COVERED |
| CONTEXT | — | No Phase 31 CONTEXT.md or D-NN locked decisions exist. | Roadmap, requirements, state, and research control scope; deferred ideas do not appear as planned work. | COVERED |

<output>
Create `.planning/phases/31-portable-png-encode-evidence/31-01-SUMMARY.md` when done.
</output>
