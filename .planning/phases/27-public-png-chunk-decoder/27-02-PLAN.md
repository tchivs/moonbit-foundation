---
phase: 27-public-png-chunk-decoder
plan: "02"
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - modules/mb-image/png/stream_decode_test.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
  - modules/mb-image/png/stream_decode.mbt
  - modules/mb-image/png/raster_decode.mbt
autonomous: true
gap_closure: true
requirements: [PNGS-01, PNGS-02]
must_haves:
  truths:
    - "Every active PngChunkDecoder push accepts precisely the bytes it dispatches from arbitrary caller-owned partitions, returns NeedInput without an image, and retains no view that later caller mutation can affect."
    - "finish() has executable public and white-box evidence for every frozen incomplete-state context and precedence rule, and both push and finish replay the first typed terminal error with zero later consumption."
    - "Chunk decode has observable eager equivalence for accepted supported-profile inputs and for structural, DEFLATE, raster, trailing, limit, allocation, and budget failures, including diagnostics and all budget remainders."
    - "The completed public contract passes the generated-vector check, the complete PNG package suite on all four targets, and the isolated PNG quality lane."
  artifacts:
    - path: modules/mb-image/png/stream_decode_test.mbt
      provides: "Black-box public schedule, ownership, exact-consumption, one-result, sticky-terminal, and eager/chunk observable-parity evidence."
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: "Focused frozen EOF-classifier and precedence matrix that exercises the same public finish route and its private machine classifier."
    - path: modules/mb-image/png/stream_decode.mbt
      provides: "Only test-proven minimal corrections to public progress, terminal classification, or sticky-state wiring if the new contract tests find a behavioral mismatch."
  key_links:
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: "Caller-owned OwnedBytes views are pushed through PngChunkDecoder, then mutated or reused only after push returns; public outcomes and finish results are checked against the eager decoder."
      pattern: "PngChunkDecoder::push|PngChunkDecoder::finish|PngChunkPushOutcome"
    - from: modules/mb-image/png/stream_decode_wbtest.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: "Prefix fixtures exercise PngDecodeMachine::chunk_incomplete_error and the public finish path for each state-table row, preserving first-error precedence."
      pattern: "chunk_incomplete_error|zlib-truncated|png-iend-crc"
    - from: modules/mb-image/png/stream_decode.mbt
      to: modules/mb-image/png/raster_decode.mbt
      via: "The classifier queries raster completeness without transferring the private OwnedImage; corrections may not make output visible before finish."
      pattern: "PngRasterSink::is_complete|PngChunkDecoder::finish"
---

<objective>
Close Phase 27's verification gaps by proving the already-published PngChunkDecoder contract at arbitrary public chunk boundaries, across every frozen EOF classification, and against the existing eager decoder's full observable behavior.

Purpose: The public adapter and policy are present, but its progress, ownership, terminal, and parity claims are not yet backed by the behavior evidence required by PNGS-01 and PNGS-02.
Output: Focused public and white-box MoonBit regression coverage, minimal test-driven adapter corrections only where those regressions demonstrate a mismatch, and four-target PNG qualification evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-public-png-chunk-decoder/27-RESEARCH.md
@.planning/phases/27-public-png-chunk-decoder/27-01-PLAN.md
@.planning/phases/27-public-png-chunk-decoder/27-01-SUMMARY.md
@.planning/phases/27-public-png-chunk-decoder/27-01-VERIFICATION.md
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/png/stream_decode_test.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt
@modules/mb-image/png/png_test.mbt
@modules/mb-image/qoi/stream_decode_test.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add black-box arbitrary-partition, ownership, and eager-parity coverage</name>
  <files>modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - Empty input, every one-byte partition, and mixed-size partitions through signature, IHDR length/type/payload/CRC, first and subsequent IDAT headers/payloads/CRCs, DEFLATE output, filtered raster, IEND, and the post-IEND pre-finish state return this-call exact consumption plus NeedInput while active.
    - The same public runner feeds representative stored, fixed-Huffman, dynamic-Huffman, RGB, straight-RGBA, indexed/PLTE/tRNS, 16-bit, Adam7, sRGB, legacy, and ICC accepted generated cases. Only finish transfers a result, and that result matches the eager result's descriptor, metadata, disposition, pixels, and bytes_read.
    - An OwnedBytes view may be mutated or reused after a partial push returns; a subsequent independent source suffix still decodes to the original expected pixel/result, proving that neither the wrapper nor private machine retained a caller view or lease.
    - A parser-dispatched malformed signature/CRC/DEFLATE/raster/trailing byte is counted in the push that detects it. A byte denied by max_input_bytes is not counted or dispatched. All later pushes consume zero and expose exactly the original terminal CoreError through both push and finish.
    - Paired eager and chunk runners constructed with equal independent limits, budgets, and diagnostics compare successful results, diagnostics length/render, and every ResourceLimits remaining field. Representative failure runners compare CoreError category, code, operation, context, requested, completed, and limit along with diagnostics and all remaining budget fields.
  </behavior>
  <action>Start with RED tests in `stream_decode_test.mbt`. Add compact helpers modeled on `qoi_stream_chunk`, `qoi_stream_push`, `png_scheduled_result_matches`, `png_test_decode_vector`, and `png_test_remaining_unchanged`: build caller-owned `OwnedBytes` chunks, push one selected slice at a time, record each returned `consumed()`/outcome, and create an eager counterpart with independently created `Budget` and `Diagnostics`. Use a schedule table that includes empty, one-byte, and mixed chunks whose boundaries are derived from the real valid PNG fixture layout; include all signature, length/type/payload/CRC, IDAT, DEFLATE, scanline/filter, IEND, and trailing positions rather than only a whole-buffer success. Run all accepted `PngDecodeVector` profiles through a one-byte schedule, plus named mixed schedules at the structural boundaries, so profile parity remains broad without inventing a second parser or fixture format. After a partial real token has been accepted, mutate the original owner with `with_mut` and feed the untouched remaining bytes from a new owner; assert the decoded image proves accepted bytes were copied/consumed synchronously. Add public sticky cases for bad signature, bad CRC, malformed DEFLATE, short raster, trailing data, input limit, allocation, and budget exhaustion. Compare the complete visible error shape, diagnostics snapshot, and `Budget::remaining()` dimensions with eager decoding; preserve strict EOF by asserting a valid IEND remains NeedInput until finish. Do not edit production code, policy, public declarations, QOI, generated fixtures, Reader semantics, or documentation in this task. Commit the failing behavioral tests before any implementation correction.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk*'</automated>
  </verify>
  <done>Public tests make every required partition, exact-consumption, no-retained-view, one-result, sticky-error, and eager-equivalence claim executable; they initially fail only where a current public adapter behavior differs from the frozen contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze every EOF classifier row and terminal precedence rule</name>
  <files>modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - finish() on signature prefixes, generic chunk-length prefixes, ancillary payload/CRC prefixes, and each partial IDAT CRC returns the frozen Data/UnexpectedEndOfStream context and completed count, then remains sticky through later public push and finish calls.
    - Complete inflater plus short filtered row/Adam7 pass wins over missing or partial IEND as png-filtered-size; incomplete zlib header, stored/fixed/dynamic token/tree/match, and Adler trailer win over simultaneously incomplete IDAT/IEND framing as zlib-truncated.
    - A stream with completed raster reports png-iend for no IEND bytes, png-iend-length for one through three length bytes or a nonzero IEND declaration, png-iend-type for I, IE, IEN, a wrong fourth type, and another completed zero-length chunk type, and png-iend-crc for zero through three IEND CRC bytes.
    - The attempted nonzero-IEND payload route fails at the fourth type byte with png-iend-length before any payload byte is accepted; a complete IEND followed by input fails as png-trailing-data and stays ahead of later finish classification.
    - Existing structural, CRC, resource, allocation, budget, zlib, raster, and trailing CoreErrors always win over the active-state classifier and are replayed unchanged with zero subsequent push consumption; successful finish moves exactly one result and uses only the distinct state-terminal error afterwards.
  </behavior>
  <action>Write RED-first focused tests in `stream_decode_wbtest.mbt` using its existing private machine/header/IDAT helpers and small purpose-built byte prefixes. Cover every row in the frozen table in `27-01-PLAN.md`: signature indices; generic Length; no/partial IEND length; zero/nonzero IEND type; generic payload/CRC; zero through three IDAT and IEND CRC bytes; zlib header, stored/fixed/dynamic tree/token/match and Adler pauses; and raster/Adam7 incompleteness. For each prefix, inspect both the non-mutating `PngDecodeMachine::chunk_incomplete_error(machine.accepted_bytes())` result and an equivalent public PngChunkDecoder `finish()` result, asserting category, code, context, and completed count. Add table-driven helpers to avoid duplicating CRC construction, but keep expected context literals directly in the test cases. Exercise precedence by deliberately combining incomplete framing with unfinished inflater or raster, then replay every resulting first error through push and finish. Prove the nonzero-IEND type rejection consumes the fourth type byte but no declared payload byte. Do not add test-only public APIs, expose PngMachineOutcome, alter the private result gate, or bypass PngChunkDecoder::finish(). Commit the failing classifier matrix before fixing production behavior.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk*'</automated>
  </verify>
  <done>White-box plus equivalent public-finish regressions cover all frozen EOF contexts, zlib/raster/framing precedence, first-error replay, terminal consumption, and the one-transfer invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct only test-proven adapter drift and qualify the complete public contract</name>
  <files>modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/stream_decode_test.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - All new public and white-box contract tests pass without loosening strict EOF, result privacy, source limits, typed errors, or four-target portability.
    - If tests find an adapter defect, the correction keeps PngDecodeMachine as the sole PNG parser, retains no ByteView/lease/full IDAT buffer, preserves the existing public interface and policy, and changes only the demonstrated behavior.
    - All accepted full-profile cases and the selected structural, DEFLATE, raster, trailing, limit, allocation, and budget failures have eager/chunk parity for every observable result/error, diagnostics, and remaining-budget field.
  </behavior>
  <action>Run the RED suites from Tasks 1 and 2 and diagnose every failure against the contract before modifying production sources. Make the smallest correction in `stream_decode.mbt` only when a failing test proves an adapter defect in per-call consumption, source-limit preflight, first-error storage/replay, one-time outcome transfer, or incomplete-state classification. Touch `raster_decode.mbt` only if a failing raster-precedence test proves its existing non-mutating completeness query is incorrect; any correction must preserve the private OwnedImage until the current terminal finish path succeeds. Re-run the black-box and white-box matrices after each correction, retaining their final parity checks rather than weakening expected outcomes. Do not add buffering, a second parser, FFI, DecodeOptions to PngChunkDecoder, a public stream encoder, new public declarations, policy changes, Reader EOF changes, QOI changes, release/registry work, or Phase 28's separate public workflow/example. Finish by running the generated vector check, the complete all-target PNG package test suite, and the isolated PNG quality lane.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>All Phase 27 gap rows are demonstrably closed: public arbitrary schedules have exact consumption and ownership isolation, all frozen EOF/precedence rows are sticky and typed, eager/chunk observables agree, and the complete portable PNG qualification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-owned ByteView to PngChunkDecoder::push | Mutable, untrusted bytes must be synchronously consumed without retaining a view or accepting bytes after a terminal state. |
| PngChunkDecoder to PngDecodeMachine | Public progress/accounting and explicit EOF must preserve the one private parser's integrity and resource semantics. |
| PngMachineOutcome to DecodeResult | The single allocated image must remain private until final terminal validation and be moved once only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06 | Tampering | caller view ownership and accepted-byte accounting | high | mitigate | Test one-byte/mixed schedules, post-return source mutation, malformed-byte counting, and rejected-limit-byte non-consumption through the public API. |
| T-27-07 | Denial of service | incomplete/hostile PNG prefixes and resource paths | high | mitigate | Exercise input ceilings, allocation and budget failures, and zlib/raster/framing precedence; retain existing checked limits and immediate terminal stop. |
| T-27-08 | Information disclosure | result visibility before EOF | high | mitigate | Assert IEND remains NeedInput, only finish can move the result, and a second terminal operation never replays it. |
| T-27-09 | Tampering | EOF classifier and sticky errors | high | mitigate | Freeze every classifier row and compare CoreError fields on first error and every later push/finish operation. |
| T-27-10 | Tampering | test-driven implementation correction | medium | mitigate | Permit only targeted adapter/raster fixes proven by RED tests; preserve the exact public interface, parser ownership, and existing quality lane. |
</threat_model>

<verification>
- Task 1 RED/GREEN: `moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk*'`.
- Task 2 RED/GREEN: `moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk*'`.
- Final qualification: `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`; `moon -C modules/mb-image test png --target all --frozen`; `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
</verification>

<success_criteria>
- Public chunk tests cover arbitrary caller partitions at framing, IDAT, DEFLATE, filter, IEND, and trailing boundaries; every active push reports exact this-call consumption and no output is visible before finish.
- Mutation/reuse after push proves no caller ByteView or lease is retained, while valid completion transfers exactly one owned eager-equivalent result.
- The EOF classifier matrix covers all frozen suffix contexts and its precedence rules; malformed, resource, and trailing failures preserve the original typed CoreError with zero-consumption terminal pushes.
- Eager and chunk routes agree on successes and representative failures, including diagnostics and every remaining resource dimension, on js, wasm, wasm-gc, and native.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Caller-buffered PNG decode with explicit strict completion and unchanged Reader EOF behavior | 27-02 | COVERED | Tasks 1-3 prove the public adapter rather than changing Reader semantics. |
| REQ | PNGS-01 | Arbitrary caller-owned chunks, deterministic non-terminal progress, no image before completion | 27-02 | COVERED | Task 1 supplies public partitions, exact consumption, ownership mutation, and pre-finish visibility evidence. |
| REQ | PNGS-02 | Explicit finish yields one eager-equivalent result or a sticky typed terminal error for all named failures | 27-02 | COVERED | Tasks 2-3 freeze every incomplete/precedence row and compare success/error observables. |
| RESEARCH | — | Use one private PngDecodeMachine; never buffer or add a second parser | 27-02 | COVERED | Task 3 permits only minimal existing-adapter/raster corrections and forbids alternate transport/parser implementations. |
| RESEARCH | — | Exact consumed-byte contract and no retained ByteView/lease | 27-02 | COVERED | Task 1 checks successful, malformed, and limited bytes plus post-push mutation/reuse. |
| RESEARCH | — | finish-only result transfer, sticky first error, and deterministic zlib/raster/framing precedence | 27-02 | COVERED | Tasks 1-2 cover terminal replay, single transfer, and the frozen full classifier table. |
| RESEARCH | — | Full caller-visible parity: image/result, diagnostics, limits, and budget accounting | 27-02 | COVERED | Tasks 1 and 3 compare all visible fields against independent eager runs. |
| RESEARCH | — | Four-target portable evidence and PNG quality gate | 27-02 | COVERED | Task 3 runs the generated vectors, all targets, and the PNG lane. |
| CONTEXT | — | No 27-CONTEXT.md exists; roadmap/research lock out FFI, public encoder, release/registry work, Reader changes, and Phase 28 workflow scope | 27-02 | COVERED | Every task confines work to test evidence and test-proven existing-adapter fixes. |

<output>
Create `.planning/phases/27-public-png-chunk-decoder/27-02-SUMMARY.md` when done.
</output>
