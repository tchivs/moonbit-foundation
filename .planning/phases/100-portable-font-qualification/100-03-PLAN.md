---
phase: 100-portable-font-qualification
plan: 03
type: execute
wave: 3
depends_on:
  - 100-01
  - 100-02
files_modified:
  - modules/mb-font/font/font_qualification_test.mbt
  - modules/mb-font/font/font_qualification_hostile_test.mbt
  - scripts/quality/Invoke-FontQualification.ps1
autonomous: true
requirements:
  - FONT-05
must_haves:
  truths:
    - "One compact immutable public workflow opens, maps BMP and supplementary scalars, reads named metrics, extracts exact simple/composite paths, and returns nonzero kerning on every target."
    - "The exact DejaVu specimen independently confirms representative mapping, metrics, simple/composite outline facts, and legacy kerning on every target."
    - "Every closed hostile case executes only public entry points with identical structured outcomes and transactional publication rules on js, wasm, wasm-gc, and native."
    - "Four evidence records share one closed schema and have byte-identical semantic payloads after removing only target/runner metadata."
  artifacts:
    - "modules/mb-font/font/font_qualification_test.mbt"
    - "modules/mb-font/font/font_qualification_hostile_test.mbt"
    - "scripts/quality/Invoke-FontQualification.ps1"
    - "artifacts/release-qualification/font/{js,wasm,wasm-gc,native,comparison}.json"
  key_links:
    - "Generated test-private bytes feed only the existing public Font API."
    - "Independent oracle fields are asserted by the target tests and copied into closed evidence only after those tests pass."
    - "qualification-cases.json IDs map one-to-one to public hostile executions and evidence outcomes."
    - "Invoke-FontQualification.ps1 uses isolated target directories and refuses skips or target-specific expectations."
---

<objective>
Freeze the complete generated and real-font public workflows, execute the full hostile matrix, and produce comparable four-target evidence.

Purpose: FONT-05 is satisfied only when real public behavior—not parser internals or native-only shortcuts—is reproducible with identical semantics on every supported target.
Output: Two black-box test suites, a focused four-target evidence runner, four target records, and a normalized comparison record.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/100-portable-font-qualification/100-RESEARCH.md
@.planning/phases/100-portable-font-qualification/100-01-SUMMARY.md
@.planning/phases/100-portable-font-qualification/100-02-SUMMARY.md
@fixtures/font/dejavu-sans-2.37/oracle.json
@fixtures/font/qualification-cases.json
@modules/mb-font/font/generated_font_qualification_test.mbt
@modules/mb-font/font/font.mbt
@modules/mb-font/font/limits.mbt
@modules/mb-font/font/font_test.mbt
@modules/mb-font/font/generated_fonts_wbtest.mbt
@scripts/quality/Invoke-PngEncodeEvidence.ps1

<interfaces>
Use only the existing public workflow:
- `FontLimits::new(...)`
- `Font::open(ByteView, FontLimits, Budget)`
- `Font::units_per_em`, `global_bounds`, `hhea_line_metrics`, `typographic_line_metrics`
- `Font::glyph_for_scalar`, `glyph_id`, `horizontal_metrics`
- `Font::outline(GlyphId, Budget) -> Result[Path2, CoreError]`
- `Font::kerning(GlyphId, GlyphId)`
- public `CoreError` category/code/context/requested/limit accessors

DejaVu exact public facts are fixed in `100-RESEARCH.md`: U+0041->36, U+00E9->171, U+10300->5373; U+0041 metrics 1401/16; U+00E9 metrics 1260/113; U+10300 metrics 1550/100; global/hhea/OS2 facts; and glyph 36/57 kerning -131.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| New file | `modules/mb-font/font/font_qualification_test.mbt` | Black-box compact and DejaVu public workflow assertions. |
| New file | `modules/mb-font/font/font_qualification_hostile_test.mbt` | Black-box closed hostile matrix and transactional assertions. |
| New file | `scripts/quality/Invoke-FontQualification.ps1` | Offline four-target runner, evidence writer, and semantic comparator. |
| New run artifact | `artifacts/release-qualification/font/js.json` | Closed js qualification record. |
| New run artifact | `artifacts/release-qualification/font/wasm.json` | Closed wasm qualification record. |
| New run artifact | `artifacts/release-qualification/font/wasm-gc.json` | Closed wasm-gc qualification record. |
| New run artifact | `artifacts/release-qualification/font/native.json` | Closed native qualification record. |
| New run artifact | `artifacts/release-qualification/font/comparison.json` | Normalization contract, record hashes, and equality result. |
| New MoonBit symbol | `font_qualification_limits` | Test-private explicit comfortable happy-path limits. |
| New MoonBit symbol | `font_qualification_admission_budget` | Test-private authoritative open budget. |
| New MoonBit symbol | `font_qualification_outline_budget` | Test-private authoritative outline budget. |
| New MoonBit symbol | `font_qualification_assert_error` | Test-private exact category/code/context/requested/limit assertion. |
| New PowerShell symbol | `New-FontQualificationEvidenceRecord` | Builds one closed record only after a target passes. |
| New PowerShell symbol | `Compare-FontQualificationEvidence` | Removes only target/runner metadata and byte-compares canonical JSON. |

No production symbol or public API is added. Target evidence is generated output, not a tracked semantic source of truth.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze compact and DejaVu public workflow oracles</name>
  <files>modules/mb-font/font/font_qualification_test.mbt</files>
  <read_first>
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/limits.mbt
    - modules/mb-font/font/font_test.mbt:1347
    - modules/mb-font/font/font_test.mbt:2389
    - modules/mb-font/font/generated_font_qualification_test.mbt
    - fixtures/font/dejavu-sans-2.37/oracle.json
    - .planning/phases/100-portable-font-qualification/100-RESEARCH.md
  </read_first>
  <behavior>
    - "Workflow `font-complete-public` uses explicit limits and budgets and observes stable semantic facts only."
    - "Compact bytes map U+0041 and U+10300, expose named global/per-glyph metrics, return exact simple and composite Path2 commands, and return a nonzero classic kern pair."
    - "An auxiliary compact format-4 font executes the supported format-4 branch without claiming DejaVu selected it."
    - "DejaVu maps U+0041/U+00E9/U+10300, exposes independently frozen public metrics and Path2 facts, and returns -131 for glyph 36/57; component identities remain offline-oracle-only."
    - "No assertion depends on time, debug rendering, allocation identity, filesystem path, or target representation."
  </behavior>
  <action>
Create a black-box `_test.mbt` suite that imports only published mb-font/mb-core APIs and consumes the generated test-private fixture accessors. Define `font_qualification_limits`, admission budget, and outline budget with named comfortable ceilings separate from exact/one-short hostile factories.

For `font-complete-public`, open the compact checksum-correct font and freeze the generator-owned facts: BMP U+0041, supplementary U+10300, named global/hhea/typographic values, horizontal metrics, the exact complete Path2 command sequence for glyph 1 simple and glyph 2 one-level composite, and the nonzero glyph 1/2 classic format-0 kerning pair. Assert the exact command kinds and coordinates rather than debug strings. Add a separate compact format-4 public lookup case so the format-4 implementation remains exercised without implying per-scalar fallback or claiming DejaVu selects it (D-04/D-08).

Open `font_qualification_dejavu_sans_237_bytes` and assert only facts observable through the published D-09 API: units 2048, global bounds `(-2090,-948)..(3673,2524)`, hhea `1901/-483/0`, typographic `1556/-492/410`; U+0041->36 with public horizontal metrics 1401/16/bounds and its public Path2 command count/fingerprint/selected coordinates; U+00E9->171 with public horizontal metrics 1260/113/bounds and its public Path2 command count/fingerprint/selected coordinates; U+10300->5373 with public horizontal metrics 1550/100/bounds and its public Path2 command count/fingerprint/selected coordinates; and glyph 36/57 public kerning `-131`. Component glyph identities, component transforms, raw contour classification, cmap table selection facts, and other parser internals may remain in `oracle.json` for offline intake validation but must not be asserted by the MoonBit black-box test or copied into target `public_facts`. Documentation may state the independently inventoried format-4/12 coexistence and canonical-selection rule, but the public test proves only the resulting mappings.

Run these tests on all four targets before continuing. Do not add convenience APIs, raw cmap/glyf inspection, debug-string or timing oracles, target branches, host file loading, shaping, hinting, CFF, rasterization, or broader composite behavior (D-05/D-07/D-16/D-17).
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
foreach ($target in @('js','wasm','wasm-gc','native')) {
  moon -C modules/mb-font test font --target $target --frozen --target-dir "target/phase100-plan03-workflow-$target" --no-parallelize
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
    </automated>
  </verify>
  <acceptance_criteria>
    - "The compact workflow covers every D-08 operation and freezes exact simple/composite commands plus nonzero kerning."
    - "The DejaVu scenario reproduces only independently curated facts exposed by glyph mapping, named metrics, Path2, and kerning APIs; component identities and raw parser facts remain offline-only."
    - "Format-4 execution is represented honestly and DejaVu BMP lookup is described as selected format 12."
    - "The same test source and expectations pass on js, wasm, wasm-gc, and native."
    - "Only existing public APIs participate; no new format, composite depth, host capability, or public symbol appears."
  </acceptance_criteria>
  <done>Both minimal complete and representative real-font workflows are frozen through the published API on four targets per D-04/D-08/D-09/D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the hostile matrix and emit byte-comparable target evidence</name>
  <files>modules/mb-font/font/font_qualification_hostile_test.mbt, scripts/quality/Invoke-FontQualification.ps1</files>
  <read_first>
    - fixtures/font/qualification-cases.json
    - modules/mb-font/font/generated_font_qualification_test.mbt
    - modules/mb-font/font/font_test.mbt:3324
    - modules/mb-font/font/font_test.mbt:3606
    - modules/mb-font/font/font_test.mbt:4702
    - modules/mb-font/font/font_test.mbt:5032
    - scripts/quality/Invoke-PngEncodeEvidence.ps1
    - modules/mb-font/moon.mod.json
    - modules/mb-font/font/moon.pkg
  </read_first>
  <behavior>
    - "Each generated case ID executes a public operation and matches exact category, code, context, requested, limit, and stage."
    - "Failed open never yields a Font; failed outline never yields a Path2; mutation never yields stale data."
    - "Exact source/budget/outline boundaries pass and their one-short partners fail Resource/BudgetExceeded."
    - "All case IDs execute on every target with no skips and no target-specific expected value."
    - "Normalized semantic evidence differs only in removed target and runner metadata."
  </behavior>
  <action>
Create the black-box hostile suite by iterating `font_qualification_cases()` and dispatching every closed case ID to an existing generated source seam. Freeze public fields, not rendered prose: malformed directory range as Data/InvalidEncoding at open; unsupported outline profile as Capability/CapabilityUnavailable; retained mutation as State/InvalidRange with `font-source-revision-drift`; checked range overflow as its stable Data error; exact/one-short source limits and authoritative open/outline budgets as success versus Resource/BudgetExceeded; and nested composite as Capability/CapabilityUnavailable with `font-outline-nested-composite`. Assert optional requested/limit exactly when declared (D-11).

For each failure, match on Result before publication: an open error must have no Font value, an outline error must have no Path2, and post-open mutation must not publish stale glyph/metric/kern/path data. Keep exact-fit and one-short pairs adjacent and prove budget state follows the existing transactional contract (D-13). The matrix must have an exact set/sequence assertion so adding, dropping, renaming, skipping, or making an outcome target-specific fails (D-12).

Create `Invoke-FontQualification.ps1` with `-EvidenceDirectory` defaulting to `artifacts/release-qualification/font`. It must run generator `-Check`, capture exact `moon/moonc/moonrun` identities and source/fixture hashes, run the complete isolated font package on js, wasm, wasm-gc, and native with unique target directories and `--no-parallelize`, and write one deterministic UTF-8-no-BOM JSON record only after each target passes. Use the closed schema `schema_version`, `workflow_id`, `target`, `toolchain`, `fixtures`, `public_facts`, `hostile_outcomes`, `dependency_facts`, `runner`, `pass`; reject missing/unknown keys. `public_facts` contains only the independent oracle subset observable through mapping, named metrics, Path2, and kerning; it must exclude component identities, raw table/cmap facts, and parser classifications. Hostile outcomes come from the closed independent case inputs whose exact values the target tests assert, never from mb-font as its own certifier.

`Compare-FontQualificationEvidence` may remove only top-level `target` and the target-specific `runner` object before canonical serialization and byte comparison. It must retain and compare toolchain, every fixture digest, every public fact, the complete ordered hostile outcomes, dependency facts, and pass status. Write `comparison.json` with record hashes and equality status. Any skipped target, missing case, false pass, semantic divergence, or extra normalization is a failure (D-10/D-12).
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$evidence = 'artifacts/release-qualification/font-plan03'
./scripts/quality/Invoke-FontQualification.ps1 -EvidenceDirectory $evidence
foreach ($name in @('js','wasm','wasm-gc','native','comparison')) {
  if (-not (Test-Path -LiteralPath (Join-Path $evidence "$name.json") -PathType Leaf)) { throw "Missing $name evidence" }
}
$comparison = Get-Content -Raw (Join-Path $evidence 'comparison.json') | ConvertFrom-Json
if ($comparison.equal -ne $true -or @($comparison.targets).Count -ne 4) { throw 'Four-target semantic evidence differs' }
$expected = @((Get-Content -Raw ./fixtures/font/qualification-cases.json | ConvertFrom-Json).cases.id)
foreach ($target in @('js','wasm','wasm-gc','native')) {
  $record = Get-Content -Raw (Join-Path $evidence "$target.json") | ConvertFrom-Json
  if ($record.pass -ne $true -or @($record.hostile_outcomes.id).Count -ne $expected.Count) { throw "$target evidence is incomplete" }
  if (Compare-Object $expected @($record.hostile_outcomes.id)) { throw "$target hostile IDs drifted" }
}
    </automated>
  </verify>
  <acceptance_criteria>
    - "Every D-11 hostile category and every D-13 transactional pair executes through public entry points."
    - "The closed case order and exact structured fields are identical on all four targets with no skip mechanism."
    - "Each target evidence record binds toolchain, fixture hashes, workflow facts, outcomes, dependency facts, commands, and pass status."
    - "Normalization removes only target and runner metadata and byte-compares the complete remaining semantic payload."
    - "The focused runner is offline after fixture intake and acquires no GUI, host font, filesystem-at-runtime, FFI, canvas, shaping, hinting, CFF, or rasterization capability."
  </acceptance_criteria>
  <done>Complete public and hostile behavior has canonical, byte-equal four-target evidence per D-10 through D-13 and FONT-05.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Crafted input/resource behavior and target-specific evidence bypass are high-severity and blocking.

## Trust Boundaries

| Boundary | Description |
|---|---|
| generated bytes -> public Font API | Untrusted font data must cross the same public limits/budget guards on every target. |
| public Results -> qualification assertions | Errors and publication state must be checked structurally, not through prose. |
| target runner -> evidence JSON | A passing record must correspond to a complete successful isolated test run. |
| four records -> normalized comparison | Normalization must not erase semantic divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-03-01 | Denial of service | hostile font operations | high | mitigate | Exercise explicit FontLimits, max_work, and authoritative exact/one-short budgets through public entry points. |
| T-100-03-02 | Tampering | target-specific outcomes | high | mitigate | Use one closed generated matrix, exact ID sequence, no skip path, and four isolated runs. |
| T-100-03-03 | Spoofing | target evidence | high | mitigate | Write pass records only after the exact target command succeeds and bind source/fixture/toolchain identities. |
| T-100-03-04 | Repudiation | partial publication | medium | mitigate | Pattern-match failure Results and assert no Font/Path2/stale value is published. |
| T-100-03-05 | Tampering | evidence normalization | high | mitigate | Remove only target and runner, then canonicalize and byte-compare every semantic field. |
| T-100-03-SC | Tampering | dependency supply chain | low | accept | No package installation or new runtime dependency occurs; evidence uses repository PowerShell and pinned MoonBit tools. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Complete public workflow and hostile behavior with identical four-target facts | 100-03 Tasks 1-2 | COVERED |
| REQ | FONT-05 | Reproducible licensed/public/hostile qualification on four targets | 100-03 Tasks 1-2 | COVERED |
| RESEARCH | — | Compact oracle, DejaVu oracle, closed hostile matrix, closed evidence schema | 100-03 Tasks 1-2 | COVERED |
| CONTEXT | D-04,D-05,D-07 | Separate compact/real roles and portable licensed transport | Task 1 | COVERED |
| CONTEXT | D-08,D-09 | Complete compact and representative DejaVu workflows | Task 1 | COVERED |
| CONTEXT | D-10 | Per-target records and normalized byte comparison | Task 2 | COVERED |
| CONTEXT | D-11-D-13 | Public hostile matrix, no skips, transactional pairs | Task 2 | COVERED |
| CONTEXT | D-16,D-17 | Negative capability and no-public-expansion boundary | Both tasks | COVERED |

Deferred families, containers, CFF, variations, color/bitmap fonts, deeper composites, shaping, hinting, discovery, rasterization, authoring, and publication promotion are absent.
</source_audit>

<verification>
1. Run both black-box workflow suites and the complete font package independently on all four targets.
2. Require the exact closed hostile ID sequence and structured fields in every target record.
3. Require four records plus comparison.json and byte-identical normalized semantic payloads.
4. Confirm evidence records include no skips, timestamps in semantic payloads, debug strings, or target-specific expected facts.
</verification>

<success_criteria>
- Compact and DejaVu public workflows pass with exact stable semantic facts on four targets.
- Every hostile case and exact/one-short pair passes with identical public outcomes.
- Failed operations never publish Font, Path2, or stale data.
- Canonical target evidence is complete and byte-equal after the narrowly allowed normalization.
</success_criteria>

<output>
Create `.planning/phases/100-portable-font-qualification/100-03-SUMMARY.md` when done.
</output>
