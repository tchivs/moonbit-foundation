---
phase: 100-portable-font-qualification
plan: 06
type: execute
wave: 6
depends_on:
  - 100-05
files_modified:
  - scripts/fixtures/Generate-FontQualification.ps1
  - fixtures/font/dejavu-sans-2.37/oracle.json
  - modules/mb-font/font/generated_font_qualification_test.mbt
  - modules/mb-font/font/font_qualification_test.mbt
  - scripts/quality/Invoke-FontQualification.ps1
  - scripts/quality/Assert-Policy.ps1
autonomous: true
gap_closure: true
requirements:
  - FONT-05
must_haves:
  truths:
    - "The independent DejaVu oracle retains the complete ordered command vector behind every published supported outline fingerprint: U+0041, U+034C, and U+10300."
    - "The deterministic generator binds those complete oracle vectors into test-private MoonBit expectations and `-Check` rejects drift in any command kind, control point, endpoint, close, count, order, or fingerprint."
    - "On js, wasm, wasm-gc, and native, the black-box DejaVu workflow structurally compares every returned PathCommand against the complete generated expectation before qualification evidence can be published."
    - "Each target evidence record retains the independently derived DejaVu outline fingerprints and records a successful focused one-test assertion gate that ran before record construction."
    - "The gap closes without a runtime/public SHA API, production font change, public-interface change, dependency change, target exception, or evidence-normalization expansion."
  artifacts:
    - "fixtures/font/dejavu-sans-2.37/oracle.json contains complete command vectors and their independently derived fingerprints."
    - "modules/mb-font/font/generated_font_qualification_test.mbt contains generated test-private complete DejaVu command expectations."
    - "modules/mb-font/font/font_qualification_test.mbt consumes every generated expectation through structural PathCommand assertions."
    - "scripts/quality/Invoke-FontQualification.ps1 gates record creation on the focused DejaVu assertion test for each target."
    - "scripts/quality/Assert-Policy.ps1 freezes the revised closed oracle schema and its independent implementation identity."
  key_links:
    - "Canonical DejaVu bytes -> Read-FontQualificationSfntOracle -> complete path.commands -> fingerprint_sha256."
    - "Complete oracle path.commands -> Write-FontQualificationGeneratedSource -> test-private expected command arrays."
    - "Expected command arrays -> font_qualification_test.mbt -> exact ordered PathCommand variant and coordinate assertions."
    - "Successful focused target assertion -> New-FontQualificationEvidenceRecord -> retained per-target DejaVu fingerprints."
prohibitions:
  - "Do not add SHA-256, fixture, glyph-inventory, or outline-fingerprint APIs to mb-font, mb-core, or any production/runtime package."
  - "Do not hand-author duplicate DejaVu command vectors in the black-box test or derive expectations from mb-font output."
  - "Do not publish an outline fingerprint unless the corresponding complete generated vector was asserted successfully on that target first."
  - "Do not add U+00E9 path expectations or broaden the existing font-outline-grid-rounding capability boundary."
  - "Do not change target normalization, allow target-specific expectations/skips, add dependencies, or modify the 56-line public semantic interface."
---

<objective>
Close the Phase 100 human-verification evidence-link gap by proving every published supported DejaVu outline fingerprint through the complete ordered PathCommand sequence on every target.

Purpose: The current independent oracle fingerprints complete paths, but persists only eight selected commands; the black-box target test consequently checks only eight commands while the focused runner publishes the complete fingerprints after a broad package pass. FONT-05 requires the evidence contract and executed target assertions to agree.
Output: A complete closed oracle/vector contract, deterministic generated MoonBit expectations, exhaustive black-box command assertions, and per-target evidence provenance showing the focused assertion ran before fingerprint publication.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/100-portable-font-qualification/100-RESEARCH.md
@.planning/phases/100-portable-font-qualification/100-VERIFICATION.md
@.planning/phases/100-portable-font-qualification/100-01-SUMMARY.md
@.planning/phases/100-portable-font-qualification/100-03-SUMMARY.md
@.planning/phases/100-portable-font-qualification/100-04-SUMMARY.md
@.planning/phases/100-portable-font-qualification/100-05-SUMMARY.md
@fixtures/font/dejavu-sans-2.37/oracle.json
@scripts/fixtures/Generate-FontQualification.ps1
@modules/mb-font/font/generated_font_qualification_test.mbt
@modules/mb-font/font/font_qualification_test.mbt
@scripts/quality/Invoke-FontQualification.ps1
@scripts/quality/Assert-Policy.ps1

<interfaces>
Existing oracle pipeline:
- `Convert-GeometryToCommands` returns the complete canonical strings `M:x,y`, `L:x,y`, `Q:cx,cy:x,y`, and `Z`.
- `Read-FontQualificationSfntOracle` already computes SHA-256 over the complete `commands -join '|'`, but persists only `selected_commands = first 8`.
- Supported published DejaVu outlines are U+0041/glyph 36 (13 commands), U+034C/glyph 765 (48 commands), and U+10300/glyph 5373 (13 commands).
- U+00E9/glyph 171 remains `Capability / CapabilityUnavailable / font-outline-grid-rounding` and has no published path fingerprint.

Existing black-box assertion seam:
- `font_qualification_assert_command(path, index, kind, x1, y1, x2, y2)` structurally matches MoveTo, LineTo, QuadTo, and Close; CubicTo fails.
- Test `font-complete-public freezes DejaVu Sans 2.37 public facts` currently checks only indexes 0-7 of each supported DejaVu path.

Existing evidence seam:
- `Get-FontQualificationGlyphFact` copies oracle `command_count`, `fingerprint_sha256`, and selected coordinates into target `public_facts`.
- `New-FontQualificationEvidenceRecord` is called after `moon check` and the complete package test, but the record does not identify a focused complete-outline assertion.
- Moon supports an exact focused invocation: `moon -C modules/mb-font test font/font_qualification_test.mbt -f 'font-complete-public freezes DejaVu Sans 2.37 public facts' --target TARGET --frozen --target-dir TARGET_DIR --no-parallelize`, whose successful output is `Total tests: 1, passed: 1, failed: 0.`
</interfaces>
</context>

## Artifacts this gap plan produces

| Kind | Exact artifact | Contract |
|---|---|---|
| Modified oracle | `fixtures/font/dejavu-sans-2.37/oracle.json` | Stores every command behind the three supported outline fingerprints, in canonical order. |
| Modified generator | `scripts/fixtures/Generate-FontQualification.ps1` | Recomputes the complete independent vectors and fingerprints and renders test-private structured expectations. |
| Modified generated source | `modules/mb-font/font/generated_font_qualification_test.mbt` | Exposes only test-private expected command records and the ordered A/U+034C/U+10300 outline expectations. |
| Modified black-box test | `modules/mb-font/font/font_qualification_test.mbt` | Compares all 74 commands, including every quadratic control/endpoint and every close. |
| Modified evidence runner | `scripts/quality/Invoke-FontQualification.ps1` | Runs and validates exactly one focused DejaVu assertion test per target before constructing that target record. |
| Modified policy gate | `scripts/quality/Assert-Policy.ps1` | Freezes the revised oracle path schema/version and rejects reversion to partial vectors. |

No production MoonBit file, public symbol, runtime SHA implementation, dependency, font-format boundary, CI workflow, README contract, or Required-lane behavior is added or changed.

<root_cause>
The gap is a planning-contract deficiency: Plan 100-03 followed the research recommendation to assert DejaVu counts/fingerprints plus selected coordinates, while the runner published fingerprints derived from complete independent vectors. Verification correctly found that successful target execution did not cover the entire value summarized by each fingerprint. This plan repairs the certification chain itself: independent bytes produce complete vectors, generation binds those vectors into target assertions, and the runner proves those assertions passed before publishing the already-retained fingerprints.
</root_cause>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind complete independent DejaVu vectors into generated test expectations</name>
  <files>scripts/fixtures/Generate-FontQualification.ps1, fixtures/font/dejavu-sans-2.37/oracle.json, modules/mb-font/font/generated_font_qualification_test.mbt, scripts/quality/Assert-Policy.ps1</files>
  <read_first>
    - .planning/phases/100-portable-font-qualification/100-VERIFICATION.md
    - .planning/phases/100-portable-font-qualification/100-CONTEXT.md
    - scripts/fixtures/Generate-FontQualification.ps1:332
    - scripts/fixtures/Generate-FontQualification.ps1:483
    - scripts/fixtures/Generate-FontQualification.ps1:966
    - fixtures/font/dejavu-sans-2.37/oracle.json
    - scripts/quality/Assert-Policy.ps1:1230
  </read_first>
  <behavior>
    - "The independent parser produces 13 ordered commands for U+0041, 48 for U+034C, and 13 for U+10300."
    - "For each supported glyph, command_count equals commands.length and fingerprint_sha256 equals SHA-256 of the UTF-8 canonical commands joined by `|`."
    - "The generated source preserves the exact command order and structured M/L/Q/Z coordinates for all three supported glyphs."
    - "Changing any oracle command, order, control, endpoint, close, count, fingerprint, or generated literal makes generator `-Check` or policy fail."
  </behavior>
  <action>
Per D-03/D-06/D-08/D-09, evolve the closed independent oracle path object from `command_count,fingerprint_sha256,selected_commands` to `command_count,fingerprint_sha256,commands`. Persist the complete `Convert-GeometryToCommands` result for each inventoried glyph, require `command_count == commands.Count`, and compute `fingerprint_sha256` from the same complete ordered array using UTF-8 and `|` separators. Bump the oracle schema and named closed-reader version from `1.0.0` to `1.1.0` so this intentional completeness contract cannot be confused with the partial schema. Keep the canonical TTF identity, parser independence, glyph facts, and fingerprint algorithm unchanged.

Extend `Write-FontQualificationGeneratedSource` to receive the freshly recomputed in-memory oracle from `Read-FontQualificationSfntOracle`; do not source expected vectors from mb-font, target evidence, or hand-written MoonBit literals. Validate the exact supported set/order U+0041/glyph 36, U+034C/glyph 765, U+10300/glyph 5373 and exact counts 13/48/13. Parse only the closed M/L/Q/Z canonical command grammar and render test-private `FontQualificationExpectedCommand` records carrying `kind,x1,y1,x2,y2` plus `FontQualificationOutlineExpectation` records carrying scalar, glyph ID, fingerprint string, and the complete command array. Expose one ordered accessor `font_qualification_dejavu_supported_outlines()`; use zero secondary coordinates for M/L/Z and both control/endpoint coordinates for Q. Reject unknown kinds, malformed coordinates, CubicTo expectations, duplicates, missing glyphs, count drift, and fingerprint drift before writing.

Make normal generation and `-Check` use the same freshly recomputed oracle object. `Test-FontQualificationInputs` must still byte-compare the stable serialized oracle against `oracle.json`, and `Write-FontQualificationGeneratedSource -CheckOnly` must byte-compare the complete regenerated MoonBit file. Update `Assert-FontQualificationArtifacts` to require the `1.1.0` oracle identities, the complete `commands` path schema, the exact three supported counts, count/vector equality, nonempty fingerprints, and the generated expectation/accessor symbols while retaining the existing no-public-symbol and independent-oracle boundaries. Regenerate `oracle.json` and `generated_font_qualification_test.mbt` deterministically. This operator-approved closure intentionally replaces the research's selected-coordinate-only recommendation because verification showed it did not certify the full fingerprints.
  </action>
  <reversibility rating="reversible">The change is confined to closed fixture/oracle/test schemas and can be regenerated from the immutable font bytes without altering production or public contracts.</reversibility>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
./scripts/fixtures/Generate-FontQualification.ps1 -Check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$oracle = Get-Content -Raw ./fixtures/font/dejavu-sans-2.37/oracle.json | ConvertFrom-Json
if ($oracle.schema_version -cne '1.1.0' -or $oracle.oracle.version -cne '1.1.0') { throw 'Complete oracle schema/version is not frozen.' }
$expected = [ordered]@{ 'U+0041' = 13; 'U+034C' = 48; 'U+10300' = 13 }
foreach ($scalar in $expected.Keys) {
  $glyph = @($oracle.glyphs | Where-Object scalar -CEQ $scalar)
  if ($glyph.Count -ne 1) { throw "Missing or duplicate supported oracle glyph $scalar." }
  $commands = @($glyph[0].path.commands)
  if ($commands.Count -ne $expected[$scalar] -or [int]$glyph[0].path.command_count -ne $commands.Count) { throw "$scalar command vector/count drifted." }
  $bytes = [Text.UTF8Encoding]::new($false).GetBytes($commands -join '|')
  $sha = [Convert]::ToHexString([Security.Cryptography.SHA256]::HashData($bytes)).ToLowerInvariant()
  if ($sha -cne [string]$glyph[0].path.fingerprint_sha256) { throw "$scalar complete-vector fingerprint drifted." }
}
$generated = Get-Content -Raw ./modules/mb-font/font/generated_font_qualification_test.mbt
foreach ($symbol in @('struct FontQualificationExpectedCommand','struct FontQualificationOutlineExpectation','fn font_qualification_dejavu_supported_outlines')) {
  if (-not $generated.Contains($symbol, [StringComparison]::Ordinal)) { throw "Generated expectation symbol missing: $symbol" }
}
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
    </automated>
  </verify>
  <acceptance_criteria>
    - "The `1.1.0` independent oracle persists all 74 supported DejaVu commands, with exact 13/48/13 partitioning and fingerprint/count invariants."
    - "Generated test-private expectations are derived only from the freshly recomputed closed PowerShell oracle and preserve every M/L/Q/Z field in order."
    - "Generator `-Check` rejects drift in either `oracle.json` or the generated MoonBit expectations."
    - "Policy freezes the complete path schema and generated symbols while the canonical font identity, license, public interface, and production sources remain unchanged."
    - "No runtime/public SHA function, new package, or mb-font-derived expectation is introduced."
  </acceptance_criteria>
  <done>The immutable font's independent complete path vectors, fingerprints, and generated test expectations form one deterministic fail-closed chain per D-03/D-06/D-08/D-09/D-17.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert every command on four targets before publishing fingerprints</name>
  <files>modules/mb-font/font/font_qualification_test.mbt, scripts/quality/Invoke-FontQualification.ps1</files>
  <read_first>
    - modules/mb-font/font/generated_font_qualification_test.mbt
    - modules/mb-font/font/font_qualification_test.mbt:80
    - modules/mb-font/font/font_qualification_test.mbt:220
    - scripts/quality/Invoke-FontQualification.ps1:100
    - scripts/quality/Invoke-FontQualification.ps1:192
    - scripts/quality/Invoke-FontQualification.ps1:392
    - scripts/quality/Invoke-FontQualification.ps1:423
    - .planning/phases/100-portable-font-qualification/100-CONTEXT.md
  </read_first>
  <behavior>
    - "The DejaVu black-box test maps each generated scalar to the exact generated glyph ID and obtains its public Path2."
    - "Path length equals the generated vector length, and every index structurally matches MoveTo, LineTo, QuadTo control plus endpoint, or Close."
    - "A missing, extra, reordered, changed-kind, changed-coordinate, changed-control, changed-endpoint, changed-close, or CubicTo command fails the target test."
    - "For each target, the focused runner proves exactly one named DejaVu assertion test passed before it constructs a record containing outline fingerprints."
    - "All four records retain identical complete-fingerprint facts and continue to compare equal after removing only target and runner."
  </behavior>
  <action>
Per D-08/D-09/D-12, replace the three partial eight-command assertion blocks in `font-complete-public freezes DejaVu Sans 2.37 public facts` with one exhaustive helper that consumes `font_qualification_dejavu_supported_outlines()`. For each ordered expectation, verify the already mapped public GlyphId equals the generated glyph ID, call `Font::outline`, require `Path2.length()` to equal the expectation length, and iterate every index through `font_qualification_assert_command`. Preserve its structural variant matching: M checks its point, L checks its endpoint, Q checks both control and endpoint, Z requires Close, CubicTo fails, and a missing command fails. Keep all existing mapping, metrics, U+00E9 capability, and kerning assertions. Do not compute SHA in MoonBit; the target proves the complete command value summarized by the independently computed fingerprint.

Per D-10/D-12/D-14, strengthen `Invoke-FontQualification.ps1` so each target runs the exact filtered DejaVu test file/name with its own target directory before the existing complete-package test. Capture and display the focused command output, require exit zero and exactly `Total tests: 1, passed: 1, failed: 0.`, then run the complete package test. Add closed runner fields `outline_assertion_test`, `outline_assertion_command`, and `outline_assertion_passed` to each target record and require the exact test name, target-specific command, and Boolean true in `Assert-FontQualificationEvidenceRecord`. Construct/write no target record until generator/policy checks, target check, focused assertion, and full package test have all passed.

Continue deriving `public_facts` from the independent oracle. Retain `command_count`, `fingerprint_sha256`, and the existing eight readable `selected_commands` in the evidence schema by selecting the first eight from the new complete oracle `commands`; do not copy all vectors into evidence and do not change fingerprint values. Add runner-side closure checks that the supported oracle glyph set is exactly U+0041/U+034C/U+10300, each complete-vector count matches the public count, and each fingerprint is nonempty before entering the target loop. Keep comparison normalization exactly `target` and `runner`, preserve the hostile matrix and dependency facts, and retain all missing-target/divergence negative probes.
  </action>
  <reversibility rating="reversible">The focused test/runner provenance fields are private qualification artifacts; reverting them does not migrate data or alter published runtime APIs.</reversibility>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$evidence = 'artifacts/release-qualification/font-plan06'
./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory $evidence
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$oracle = Get-Content -Raw ./fixtures/font/dejavu-sans-2.37/oracle.json | ConvertFrom-Json
$expectedGlyphs = @($oracle.glyphs | Where-Object {
  $_.scalar -ceq 'U+0041' -or
  $_.scalar -ceq 'U+034C' -or
  $_.scalar -ceq 'U+10300'
})
if ($expectedGlyphs.Count -ne 3) { throw 'Supported complete oracle glyph set drifted.' }
$expectedFingerprints = @{}
foreach ($glyph in $expectedGlyphs) { $expectedFingerprints[[string]$glyph.scalar] = [string]$glyph.path.fingerprint_sha256 }
$testName = 'font-complete-public freezes DejaVu Sans 2.37 public facts'
foreach ($target in @('js','wasm','wasm-gc','native')) {
  $record = Get-Content -Raw (Join-Path $evidence "$target.json") | ConvertFrom-Json
  if ($record.pass -ne $true -or $record.runner.outline_assertion_passed -ne $true -or $record.runner.outline_assertion_test -cne $testName) { throw "$target did not prove the focused DejaVu assertion before evidence." }
  if ([string]$record.runner.outline_assertion_command -cnotmatch [regex]::Escape("--target $target")) { throw "$target focused assertion command is not target-bound." }
  $published = @($record.public_facts.dejavu_sans_237.glyphs | Where-Object { $_.outline.status -ceq 'path' })
  if ($published.Count -ne 3) { throw "$target published supported outline set drifted." }
  foreach ($glyph in $published) {
    if ($glyph.outline.fingerprint_sha256 -cne $expectedFingerprints[[string]$glyph.scalar]) { throw "$target $($glyph.scalar) fingerprint is not bound to the complete oracle vector." }
  }
}
$comparison = Get-Content -Raw (Join-Path $evidence 'comparison.json') | ConvertFrom-Json
if ($comparison.equal -ne $true -or (@($comparison.normalization_removed) -join ',') -cne 'target,runner') { throw 'Four-target semantic comparison or normalization contract drifted.' }
moon -C modules/mb-font info --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$semantic = @(Get-Content ./modules/mb-font/font/pkg.generated.mbti | ForEach-Object { $_.TrimEnd() } | Where-Object { $_ -ne '' -and -not $_.TrimStart().StartsWith('//') })
if ($semantic.Count -ne 56) { throw 'Public mb-font semantic interface changed.' }
    </automated>
  </verify>
  <acceptance_criteria>
    - "The black-box test consumes the generated A/U+034C/U+10300 expectations and checks all 74 ordered commands structurally."
    - "Every Q command checks both control and endpoint; every M/L coordinate and every Z close is checked; path length prevents extra or missing commands."
    - "The focused runner requires one exact DejaVu assertion pass on each target before constructing that target's evidence record, then still runs the complete package suite."
    - "Every target record retains the same independently computed supported-outline fingerprints and names the successful focused assertion in runner metadata."
    - "Four-target comparison removes only target/runner, remains equal, and public interface/dependencies/runtime behavior stay unchanged."
  </acceptance_criteria>
  <done>Every published supported DejaVu fingerprint is backed by a complete generated PathCommand assertion executed on js, wasm, wasm-gc, and native before evidence publication per D-08/D-09/D-10/D-12/D-14/D-17 and FONT-05.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Evidence spoofing and independent-oracle drift are high-severity and blocking.

## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable DejaVu bytes -> independent oracle | The closed offline reader must derive complete vectors and fingerprints without mb-font output. |
| independent oracle -> generated MoonBit | Deterministic generation must preserve every command field and reject schema/vector drift. |
| public Path2 -> target assertion | Returned untrusted-font geometry must match every expected variant and coordinate in order. |
| focused target test -> evidence record | Fingerprints may be published only after the exact complete-outline assertion passes on that target. |
| target records -> semantic comparison | Normalization must not erase any fixture, public fact, hostile outcome, dependency, or pass-state divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-06-01 | Tampering | oracle command vectors | high | mitigate | Persist complete vectors, recompute count/fingerprint from the same array, version the schema, and enforce generator plus policy drift checks. |
| T-100-06-02 | Spoofing | target outline fingerprint evidence | high | mitigate | Gate record construction on one exact filtered DejaVu assertion pass and record the test identity/command/pass state. |
| T-100-06-03 | Tampering | generated expectations | high | mitigate | Render structured expectations only from the freshly recomputed independent oracle and byte-compare generated source in `-Check`. |
| T-100-06-04 | Repudiation | partial Path2 coverage | medium | mitigate | Require exact path length and iterate every command index with variant-specific point/control/endpoint/close assertions. |
| T-100-06-05 | Elevation of privilege | runtime/public scope | high | mitigate | Keep all new symbols test-private, add no SHA/runtime dependency, and verify the exact 56-line public interface. |
| T-100-06-06 | Denial of service | focused qualification | low | accept | The fixed 74-command matrix and one filtered test per target are bounded by committed immutable fixtures and existing target timeouts. |
| T-100-06-SC | Tampering | dependency supply chain | low | accept | No package installation or new dependency occurs; generation uses the existing PowerShell/.NET and pinned MoonBit toolchain. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Reproducible complete public workflow with trustworthy identical target evidence | 100-06 Tasks 1-2 plus existing Plans 100-01 through 100-05 | COVERED |
| REQ | FONT-05 | Licensed immutable complete workflow qualification on js/wasm/wasm-gc/native without target behavior | 100-06 Tasks 1-2 | COVERED |
| RESEARCH | — | Independent closed oracle, deterministic generation, DejaVu fingerprints, focused four-target selector | 100-06 Tasks 1-2 | COVERED |
| RESEARCH | — | Original selected-coordinate-only recommendation | Superseded by the operator-approved verification closure in 100-06 because published complete fingerprints require complete target assertions | COVERED |
| CONTEXT | D-03,D-06 | Independent no-self-oracle facts and deterministic drift-sensitive generation | 100-06 Task 1 | COVERED |
| CONTEXT | D-08,D-09 | Stable command sequences for the compact/real public workflows | 100-06 Tasks 1-2 | COVERED |
| CONTEXT | D-10,D-12 | Complete same-schema evidence and identical no-skip behavior on all targets | 100-06 Task 2 | COVERED |
| CONTEXT | D-14,D-17 | Focused qualification gate with no public API/format expansion | 100-06 Task 2 and both-task prohibitions | COVERED |
| CONTEXT | D-01,D-02,D-04,D-05,D-07,D-11,D-13,D-15,D-16 | Immutable specimen, licensing, compact/hostile workflows, Required isolation, and capability exclusions | Existing Plans 100-01 through 100-05; unchanged by this closure | COVERED |
| VERIFICATION | human_needed | Align every published DejaVu fingerprint with target-executed assertions | 100-06 Tasks 1-2 | COVERED |

Deferred fonts, containers, shaping, hinting, rasterization, discovery, authoring, deeper composites, publication, and stability promotion remain excluded.
</source_audit>

<verification>
1. Recompute the independent `1.1.0` oracle and require complete 13/48/13 vectors with count and SHA-256 invariants.
2. Run generator `-Check` and policy to prove oracle/generated expectation drift fails closed.
3. Run the focused FontQualification selector, which executes one exact complete-outline test and the full package suite on each target before evidence creation.
4. Inspect all four records for exact focused-test provenance and oracle-equal fingerprints, then require unchanged target/runner-only normalization and equal semantics.
5. Regenerate the mb-font interface and require its exact 56-line public surface remains unchanged.
</verification>

<success_criteria>
- The independent oracle and generated test source contain all 74 commands behind the three supported DejaVu outline fingerprints.
- Every supported DejaVu Path2 command, control, endpoint, and close is structurally asserted on js, wasm, wasm-gc, and native.
- No target record is constructed until that target's exact focused complete-outline test and full package suite pass.
- Per-target fingerprints remain independent-oracle values and semantic comparison remains strict.
- No runtime/public SHA API, production behavior, public surface, dependency, target exception, or deferred capability is introduced.
</success_criteria>

<output>
Create `.planning/phases/100-portable-font-qualification/100-06-SUMMARY.md` when done, then rerun Phase 100 goal verification.
</output>
