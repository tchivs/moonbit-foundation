---
phase: 100-portable-font-qualification
plan: 02
type: tdd
wave: 2
depends_on:
  - 100-01
files_modified:
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/cmap.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
autonomous: true
requirements:
  - FONT-05
must_haves:
  truths:
    - "A valid Macintosh platform 1/encoding 0 cmap format-6 record may coexist with canonical format-4/12 records without blocking Font::open."
    - "Format 6 remains non-queryable and non-selectable; canonical 4/12 priority, validation, duplicate-key, and lookup behavior are unchanged."
    - "Malformed format-6 offsets or lengths still fail as Data/InvalidEncoding at the cmap envelope."
    - "The exact DejaVu fixture opens after the repair and the generated public semantic interface is byte-for-byte unchanged."
  artifacts:
    - "modules/mb-font/font/tables.mbt"
    - "modules/mb-font/font/cmap.mbt"
    - "modules/mb-font/font/font_test.mbt"
    - "modules/mb-font/font/font_wbtest.mbt"
  key_links:
    - "font_cmap_subtable_envelope bounds every record before format-specific validation."
    - "font_admit_cmap_lookup ignores only the exact recognized non-selected 1/0/6 record and still selects canonical 4/12."
    - "Compact coexistence and full DejaVu regressions exercise the same Font::open public path."
---

<objective>
Repair the single standards/conformance defect exposed by DejaVu without adding a public API or broadening supported mappings.

Purpose: The exact licensed specimen carries a valid non-selected Macintosh format-6 cmap beside canonical Unicode format 4/12 records; current admission incorrectly rejects the whole font.
Output: Failing-first regressions, a private bounded envelope classifier, the narrow coexistence repair, and unchanged-interface proof.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/100-portable-font-qualification/100-RESEARCH.md
@.planning/phases/100-portable-font-qualification/100-01-SUMMARY.md
@.planning/phases/98-unicode-mapping-and-kerning/98-01-SUMMARY.md
@.planning/phases/98-unicode-mapping-and-kerning/98-02-SUMMARY.md
@modules/mb-font/font/tables.mbt
@modules/mb-font/font/cmap.mbt
@modules/mb-font/font/font_test.mbt
@modules/mb-font/font/font_wbtest.mbt
@modules/mb-font/font/generated_font_qualification_test.mbt
@policy/foundation.json

<interfaces>
Existing private call chain:
- `font_cmap_declared_work` scans each encoding record and currently calls `font_cmap_subtable_facts`.
- `font_admit_cmap_lookup` scans the records again, calls `font_validate_cmap_subtable`, enforces domain/language rules and duplicate canonical ranks, then retains one `CmapLookupFacts`.
- `font_cmap_rank` recognizes only 0/4/12, 3/10/12, 0/3/4, and 3/1/4.
- `font_cmap_lookup_facts` can retain only Format12 or Format4.
- No format-6 public or retained lookup type exists or may be added.

DejaVu cmap inventory:
`(0,3)->4`, `(0,10)->12`, `(1,0)->6`, `(3,1)->4`, `(3,10)->12`; canonical public lookup selects format 12.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| New private symbol | `CmapSubtableEnvelope` in `tables.mbt` | Carries only checked format and in-table length before supported-format decoding. |
| New private symbol | `font_cmap_subtable_envelope` in `tables.mbt` | Bounds formats 4, 6, and 12; malformed/unknown envelopes remain Data. |
| New private symbol | `font_cmap_recognized_nonselected` in `cmap.mbt` | Returns true only for platform 1, encoding 0, format 6. |
| New test symbol | `font_test_cmap_format6` in `font_test.mbt` | Creates a compact valid or deliberately malformed format-6 envelope. |
| New regression | `format 6 coexists with canonical Unicode cmap without becoming selectable` | Compact public Font::open and lookup proof. |
| New regression | `DejaVu Sans 2.37 opens with its non-selected format 6 cmap` | Exact licensed-fixture public admission proof. |
| New run artifact | `artifacts/phase100-plan02/mb-font-interface-baseline.txt` | Pre-edit exact 56-line generated semantic interface, already matched to the current policy allowlist. |

No new file, `pub` symbol, `CmapLookupFacts` variant, canonical rank, supported format, dependency, or host capability is permitted.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the format-6 coexistence defect with compact and real-font RED regressions</name>
  <files>modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/font_test.mbt:394
    - modules/mb-font/font/font_test.mbt:3069
    - modules/mb-font/font/font_wbtest.mbt:459
    - modules/mb-font/font/generated_font_qualification_test.mbt
    - fixtures/font/dejavu-sans-2.37/oracle.json
    - modules/mb-font/font/cmap.mbt:20
  </read_first>
  <behavior>
    - "A checksum-correct compact font containing valid `(1,0)->format6` and canonical `(0,4)->format12` records opens and maps through format 12."
    - "The exact DejaVu Sans 2.37 generated bytes open with explicit comfortable limits and budgets."
    - "A format-6-only cmap does not become a supported public mapping."
    - "Truncated header, length below 10, odd length, and length beyond the cmap table remain Data/InvalidEncoding with `font-cmap-envelope`."
    - "Duplicate canonical format-4/12 keys still fail with `font-cmap-record-key`."
  </behavior>
  <action>
Before editing any cmap implementation or test file, run `moon -C modules/mb-font info --target all --frozen` to generate the Phase 99 interface from the untouched tree. Extract semantic lines by trimming line endings and excluding blanks/comments, extract the current `tchivs/mb-font/font` `semantic_interface` array from `policy/foundation.json`, require both sequences to contain exactly 56 lines and match in exact order, then write the generated sequence as UTF-8 without BOM to `artifacts/phase100-plan02/mb-font-interface-baseline.txt`. Record its SHA-256 in the task summary. Halt before any edit if generation, count, or policy comparison fails; a stale/missing `pkg.generated.mbti` is never accepted as a baseline.

After recording that authoritative baseline, add `font_test_cmap_format6` and compose it through existing checksum-correct record/table/SFNT builders; do not add format-6 decoding or a production helper in this RED task. Add a compact black-box test that places one valid 1/0/6 record beside canonical 0/4/12, opens via public `Font::open`, and proves canonical format-12 mapping wins. Add the exact DejaVu open regression using `font_qualification_dejavu_sans_237_bytes` plus explicit limits at or above the researched 757076 source bytes, 556004 table bytes, 6253 glyphs, 26 name records, 5 cmap records, 2727 kern pairs, 534 outline instruction bytes, 49512 post-name bytes, and sufficient max_work/budget.

Add focused malformed format-6 envelope cases and preserve the existing duplicate-canonical and unsupported-no-canonical assertions. Record the current expected RED result as `Data / InvalidEncoding / font-cmap-envelope` for both valid coexistence fonts. This is the concrete D-17 conformance defect; tests must not claim that format 6 is selected, queryable, or newly supported. White-box coverage may assert the exact private envelope classification and no work charge for a non-selected format-6 body, but all acceptance behavior must also pass through public `Font::open`.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$baselineDirectory = 'artifacts/phase100-plan02'
$baselinePath = Join-Path $baselineDirectory 'mb-font-interface-baseline.txt'
if (-not (Test-Path -LiteralPath $baselinePath -PathType Leaf)) { throw 'Pre-edit Phase 99 interface baseline was not recorded before test edits.' }
moon -C modules/mb-font info --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$generated = @(Get-Content ./modules/mb-font/font/pkg.generated.mbti | ForEach-Object { $_.TrimEnd() } | Where-Object { $_ -ne '' -and -not $_.TrimStart().StartsWith('//') })
$baseline = @(Get-Content $baselinePath)
$policy = Get-Content -Raw ./policy/foundation.json | ConvertFrom-Json
$font = @($policy.modules | Where-Object { $_.name -ceq 'tchivs/mb-font' })[0]
$package = @($font.public_packages | Where-Object { $_.path -ceq 'font' })[0]
$allowed = @($package.semantic_interface | ForEach-Object { [string]$_ })
if ($generated.Count -ne 56 -or $baseline.Count -ne 56 -or $allowed.Count -ne 56) { throw 'Phase 99 interface baseline must contain exactly 56 semantic lines.' }
if (($generated -join "`n") -cne ($baseline -join "`n") -or ($baseline -join "`n") -cne ($allowed -join "`n")) { throw 'Recorded pre-edit interface, regenerated interface, and current policy allowlist differ.' }
$output = & moon -C modules/mb-font test font --target native --frozen --target-dir target/phase100-plan02-red --no-parallelize 2>&1
$exit = $LASTEXITCODE
if ($exit -eq 0) { throw 'Format-6 coexistence regression unexpectedly passed before the production repair.' }
if (($output -join "`n") -cnotmatch 'format 6 coexists|DejaVu Sans 2[.]37') { throw 'RED run did not fail in a new coexistence regression.' }
    </automated>
  </verify>
  <acceptance_criteria>
    - "Both valid coexistence regressions fail before production changes for the known cmap-envelope reason."
    - "Malformed format-6 lengths and offsets are separately represented, not conflated with the valid specimen."
    - "The compact case proves canonical format-12 selection and the real case uses the exact Plan 01 bytes."
    - "Existing duplicate-key and no-canonical behavior remain asserted."
    - "Before any cmap/test edit, a freshly generated exact 56-line interface is matched in order to policy/foundation.json and recorded with its SHA-256."
    - "No production source or public API changes occur in the RED commit."
  </acceptance_criteria>
  <done>The exact compatibility defect and its non-broadening boundaries are reproducible before implementation per D-01/D-03/D-04/D-17.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound and ignore only recognized non-selected format 6, then prove GREEN</name>
  <files>modules/mb-font/font/tables.mbt, modules/mb-font/font/cmap.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/tables.mbt:672
    - modules/mb-font/font/tables.mbt:991
    - modules/mb-font/font/tables.mbt:1138
    - modules/mb-font/font/cmap.mbt:20
    - modules/mb-font/font/cmap.mbt:136
    - policy/foundation.json:2246
    - modules/mb-font/font/pkg.generated.mbti
  </read_first>
  <behavior>
    - "Formats 4 and 12 retain their complete structural, glyph-range, discovery-work, and selection validation."
    - "The exact `(platform=1, encoding=0, format=6)` record is envelope-checked and ignored for work/selection."
    - "Any malformed format-6 envelope, unsupported platform/encoding use, or unknown cmap format still fails closed."
    - "The selected lookup remains only Format4 or Format12 and public semantic interface lines are unchanged."
  </behavior>
  <action>
Introduce private `CmapSubtableEnvelope { format, length }` and `font_cmap_subtable_envelope`. It must use checked table-local arithmetic, require at least the common header, read format-specific length fields, and validate format 6 as even length at least 10 and at most the remaining cmap bytes. Retain all existing format-4/12 minimum, body-count, and table-bound checks. Unknown formats remain the existing `font-cmap-envelope` data failure; this is not general unknown-format tolerance.

Refactor `font_cmap_declared_work` so every record first obtains a bounded envelope, but only formats 4/12 call `font_cmap_subtable_facts`, contribute body records/search/mapping work, or receive supported-body charges. A valid format 6 contributes only the already charged record scan; malformed arithmetic or length remains Data.

In `font_admit_cmap_lookup`, read platform/encoding and the bounded envelope first. Add `font_cmap_recognized_nonselected` that is true only for 1/0/6; for that exact tuple, continue without calling supported-subtable validation, ranking, lookup-fact construction, or fallback. Every format-4/12 record still goes through full `font_validate_cmap_subtable`, domain/language validation, canonical duplicate detection, and selection. A format-6-only font must end at the existing `Capability / CapabilityUnavailable / font-cmap-unicode` result. Do not add a Format6 enum variant, lookup implementation, public API, new rank, FFI, or dependency (D-17).

Run all compact/malformed/full-DejaVu tests and the complete font suite on four targets. Regenerate the public interface after the repair with `moon info --target all --frozen`; compare its exact ordered semantic lines both to `artifacts/phase100-plan02/mb-font-interface-baseline.txt` and to the current exact 56-line `policy/foundation.json` allowlist. Record final and baseline hashes in the summary. Keep the independent `Assert-FontFoundationPolicy` gate. `pkg.generated.mbti` remains ignored tool output and must not be force-added.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$baselinePath = 'artifacts/phase100-plan02/mb-font-interface-baseline.txt'
if (-not (Test-Path -LiteralPath $baselinePath -PathType Leaf)) { throw 'Authoritative pre-edit interface baseline is missing.' }
$baseline = @(Get-Content $baselinePath)
foreach ($target in @('js','wasm','wasm-gc','native')) {
  moon -C modules/mb-font test font --target $target --frozen --target-dir "target/phase100-plan02-$target" --no-parallelize
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
moon -C modules/mb-font info --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$final = @(Get-Content ./modules/mb-font/font/pkg.generated.mbti | ForEach-Object { $_.TrimEnd() } | Where-Object { $_ -ne '' -and -not $_.TrimStart().StartsWith('//') })
$policy = Get-Content -Raw ./policy/foundation.json | ConvertFrom-Json
$font = @($policy.modules | Where-Object { $_.name -ceq 'tchivs/mb-font' })[0]
$package = @($font.public_packages | Where-Object { $_.path -ceq 'font' })[0]
$allowed = @($package.semantic_interface | ForEach-Object { [string]$_ })
if ($baseline.Count -ne 56 -or $final.Count -ne 56 -or $allowed.Count -ne 56) { throw 'Interface sequence must remain exactly 56 semantic lines.' }
if (($baseline -join "`n") -cne ($final -join "`n")) { throw 'Public mb-font semantic interface changed from the recorded pre-edit baseline.' }
if (($final -join "`n") -cne ($allowed -join "`n")) { throw 'Repaired interface differs from the current policy allowlist.' }
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
    </automated>
  </verify>
  <acceptance_criteria>
    - "Compact format-6 coexistence and the exact DejaVu open regression pass on all four targets."
    - "Malformed format-6 envelopes, wrong-domain format 6, unknown formats, and duplicate canonical keys still fail with stable public errors."
    - "Format 6 is never selected, queried, charged as a supported mapping body, or represented in retained lookup facts."
    - "All pre-existing font tests pass and the exact semantic public interface is unchanged."
    - "The post-repair regenerated interface matches both the recorded pre-edit baseline and the current ordered 56-line policy allowlist."
    - "Only the narrow private conformance repair and focused regressions change."
  </acceptance_criteria>
  <done>DejaVu's valid format-6 coexistence no longer blocks canonical Unicode mapping, while public scope and all format-4/12 contracts remain frozen per D-17.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Parser boundary changes are high-impact and must fail closed on malformed lengths.

## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted cmap bytes -> subtable envelope | Format and length arithmetic must be checked within the admitted cmap table. |
| recognized non-selected record -> canonical selection | Ignoring format 6 must not permit lookup, fallback, duplicate bypass, or unsupported-domain acceptance. |
| private repair -> published API | A conformance fix must not expand the semantic interface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-02-01 | Tampering | cmap format-6 length fields | high | mitigate | Validate minimum/even/in-table length with checked arithmetic before ignoring any record. |
| T-100-02-02 | Elevation of privilege | cmap selection | high | mitigate | Recognize only exact 1/0/6 coexistence and keep retained lookup/rank variants limited to 4/12. |
| T-100-02-03 | Denial of service | cmap admission work | medium | mitigate | Charge the record scan once and never traverse or charge a non-selected format-6 body. |
| T-100-02-04 | Repudiation | public scope | medium | mitigate | Compare the generated semantic interface and run the independent focused policy classifier. |
| T-100-02-SC | Tampering | dependency supply chain | low | accept | No dependency or package installation occurs; the repair uses existing MoonBit and mb-core primitives only. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Licensed real font completes the existing public workflow | 100-02 Tasks 1-2 | COVERED |
| REQ | FONT-05 | Same valid real-font admission on four targets | 100-02 Task 2 | COVERED |
| RESEARCH | — | Valid Macintosh format-6 coexistence defect and narrow repair | 100-02 Tasks 1-2 | COVERED |
| CONTEXT | D-01,D-03,D-04 | Exact independently inventoried DejaVu fixture exposes the defect | Task 1 | COVERED |
| CONTEXT | D-12 | Same regressions on all four targets | Task 2 | COVERED |
| CONTEXT | D-16,D-17 | No host capability, public API, or broadened format support | Task 2 | COVERED |

All deferred containers, outlines, text behavior, and additional cmap support remain excluded.
</source_audit>

<verification>
1. RED commits prove valid compact and exact DejaVu failures before production changes.
2. GREEN commits pass the focused valid/malformed matrix and full font suite on js, wasm, wasm-gc, and native.
3. Interface generation plus policy comparison proves no public signature or dependency drift.
4. A format-6-only font remains unsupported, demonstrating coexistence rather than support.
</verification>

<success_criteria>
- DejaVu Sans 2.37 opens through the existing public API.
- Valid 1/0/6 coexistence is bounded and ignored, never selected.
- Malformed or out-of-scope cmap records still fail closed.
- The exact mb-font public interface and sole mb-core dependency remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/100-portable-font-qualification/100-02-SUMMARY.md` when done.
</output>
