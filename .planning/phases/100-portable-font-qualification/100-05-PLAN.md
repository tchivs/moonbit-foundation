---
phase: 100-portable-font-qualification
plan: 05
type: execute
wave: 5
depends_on:
  - 100-04
files_modified:
  - modules/mb-font/README.mbt.md
  - modules/mb-font/CHANGELOG.md
  - README.md
  - .github/workflows/quality.yml
  - scripts/quality/Invoke-RequiredBounded.ps1
autonomous: true
requirements:
  - FONT-05
must_haves:
  truths:
    - "Documentation distinguishes the compact complete oracle from the licensed DejaVu interoperability oracle and records reproducible commands and provenance."
    - "CI runs focused font qualification and the repository Required lane as separate required evidence boundaries."
    - "The Required lane is bounded, captures output, kills the full process tree on timeout, and records timeout/nonzero exit as a real failure without modifying focused evidence."
    - "The passing font qualification artifact is success-only; any always-uploaded artifact is explicitly diagnostic."
  artifacts:
    - "modules/mb-font/README.mbt.md"
    - "modules/mb-font/CHANGELOG.md"
    - "README.md"
    - ".github/workflows/quality.yml"
    - "scripts/quality/Invoke-RequiredBounded.ps1"
    - "artifacts/release-qualification/required-phase100/required-invocation.json"
  key_links:
    - "scripts/quality/Invoke-RequiredBounded.ps1 launches scripts/quality.ps1 -Lane Required as a bounded child process, captures stdout/stderr, terminates the full tree on timeout, and writes a failure record."
    - "Invoke-RequiredBounded.ps1 receives a Required-only evidence directory and never deletes, overwrites, or relabels the validated focused Phase 100 evidence."
    - "The CI font-qualification-evidence upload step is success-only, while any if: always() upload has a distinct diagnostic name and path."
    - "README reproduction commands point to the focused selector from 100-04 and the bounded Required wrapper from 100-05."
---

<objective>
Publish the reproducible qualification contract, add bounded Required execution, and close CI with unambiguous evidence artifacts.

Purpose: Maintainers need truthful documentation and CI boundaries that preserve focused font evidence even when the separate workspace Required lane fails or times out.
Output: Public docs/changelog, bounded Required wrapper, separate CI jobs, success-only qualification upload, and final focused/Required closure.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/100-portable-font-qualification/100-RESEARCH.md
@.planning/phases/100-portable-font-qualification/100-04-SUMMARY.md
@modules/mb-font/README.mbt.md
@modules/mb-font/CHANGELOG.md
@README.md
@scripts/quality.ps1
@.github/workflows/quality.yml
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| New file | `scripts/quality/Invoke-RequiredBounded.ps1` | Captures Required output, enforces a timeout, kills the full process tree, records outcome, and returns failure on timeout/nonzero exit. |
| New PowerShell symbol | `Invoke-RequiredBounded` | Runs the existing Required lane as a bounded child without touching focused evidence. |
| New CI job | `font-qualification` in `.github/workflows/quality.yml` | Runs focused four-target qualification and uploads validated success evidence. |
| Updated CI job | `required` in `.github/workflows/quality.yml` | Runs Required through the bounded wrapper with a distinct evidence directory. |
| Updated public contract | `modules/mb-font/README.mbt.md` | Reproducible compact/DejaVu workflow, provenance, commands, and exclusions. |
| Updated release record | `modules/mb-font/CHANGELOG.md` | FONT-05 qualification and narrow format-6 coexistence repair. |
| Updated project status | `README.md` | English and Chinese Phase 100 completion claim backed by focused evidence. |

No policy inventory, focused evidence semantics, runtime module, public API, direct dependency, format support, or stability promotion is owned by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Publish the compact and DejaVu qualification contract</name>
  <files>modules/mb-font/README.mbt.md, modules/mb-font/CHANGELOG.md, README.md</files>
  <read_first>
    - modules/mb-font/README.mbt.md:14
    - modules/mb-font/README.mbt.md:306
    - modules/mb-font/CHANGELOG.md
    - README.md:20
    - README.md:130
    - .planning/phases/100-portable-font-qualification/100-04-SUMMARY.md
  </read_first>
  <action>
Replace the Phase 100 reservation in `modules/mb-font/README.mbt.md` with the finished qualification contract. Document `font-complete-public` as the exact complete-feature compact command oracle and DejaVu Sans 2.37 as the representative interoperability oracle; record immutable URL, TTF/notice digests, exact license expression, independent-oracle role, public mapping/metric/Path2/kerning facts, hostile categories, evidence schema, and the focused command. Component identities and raw table/cmap facts remain offline-oracle-only. Keep executable public examples on all four targets and restate exclusions: no runtime file/host discovery, FFI, GUI/canvas, shaping, hinting, CFF/CFF2, rasterization, additional formats, or deeper composite expansion (D-08/D-09/D-16/D-17).

Update the module changelog with FONT-05, exact fixture qualification, complete hostile/four-target evidence, and the narrow format-6 coexistence conformance repair explicitly described as non-selectable/non-queryable. Update the English and Chinese root status/table text consistently. Do not claim publication, stability promotion, new public APIs, or broader formats.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
foreach ($target in @('js','wasm','wasm-gc','native')) {
  moon -C modules/mb-font check README.mbt.md --target $target --frozen --target-dir "target/phase100-plan05-doc-$target" --serial
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
    </automated>
  </verify>
  <acceptance_criteria>
    - "Public documentation explains both workflow oracles, exact licensed provenance, hostile semantics, four-target evidence, and one-command reproduction."
    - "DejaVu assertions are limited to published mapping, metrics, Path2, and kerning facts; component identities remain offline."
    - "README and changelog do not claim new public APIs, format support, publication, or stability."
    - "English and Chinese project status agree and all four literate checks pass."
  </acceptance_criteria>
  <done>The reproducible FONT-05 contract is public, precise, and scope-faithful per D-08/D-09/D-16/D-17.</done>
</task>

<task type="auto">
  <name>Task 2: Bound Required execution and close CI artifact semantics</name>
  <files>scripts/quality/Invoke-RequiredBounded.ps1, .github/workflows/quality.yml</files>
  <read_first>
    - scripts/quality.ps1
    - .github/workflows/quality.yml
    - .planning/WINDOWS.md
    - scripts/quality/Invoke-FontQualification.ps1
  </read_first>
  <action>
Create `scripts/quality/Invoke-RequiredBounded.ps1` with mandatory evidence directory and finite timeout. Implement `Invoke-RequiredBounded` with `System.Diagnostics.Process`: launch `pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required -EvidenceDirectory <path>`, redirect stdout/stderr to distinct files, wait only for the configured duration, and on timeout call `Kill($true)`, wait for full process-tree termination, and write deterministic `required-invocation.json` containing command, timeout_seconds, timed_out, exit_code, stdout_path, stderr_path, termination status, and pass/failure status. Return nonzero for timeout, kill failure, or child nonzero exit. Never delete, overwrite, move, or relabel the validated focused evidence directory from 100-04 (D-15).

Add a separate required `font-qualification` CI job with the same pinned checkout/toolchain identity checks as Required. Run `./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory artifacts/release-qualification/ci-font`. Add a dedicated upload-artifact step whose mapping has exact `name: Upload passing font qualification evidence`, `if: ${{ success() }}`, `uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02`, and `with.name: font-qualification-evidence`; it uploads only `artifacts/release-qualification/ci-font`. A separate optional step may use `if: ${{ always() }}` only when its step name, artifact `with.name`, and path all contain `diagnostic` and exclude the passing evidence path.

Change the existing CI Required command to `./scripts/quality/Invoke-RequiredBounded.ps1 -EvidenceDirectory artifacts/release-qualification/ci-required -TimeoutSeconds 900`, retaining the GitHub job timeout as an outer bound. Run focused qualification locally first, then the bounded Required wrapper with `artifacts/release-qualification/required-phase100`. A Required failure/timeout remains a real failure with captured diagnostics and must leave focused Phase 100 evidence byte-unchanged.

Verify upload semantics by parsing `.github/workflows/quality.yml` into step mappings with a YAML parser already available in the repository/toolchain, or by an exact indentation-aware step-block parser when no YAML module exists. Select only the step whose `with.name` equals `font-qualification-evidence`; require exactly one, require its `uses` pin/path and success condition, and reject `always()` only within that selected step. Independently allow an always-running step only when its distinct artifact name and path are diagnostic. Do not use a cross-step regex that can associate another step's `if` with the passing upload.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$focusedPath = 'artifacts/release-qualification/font-final'
./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory $focusedPath
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$focusedBefore = Get-ChildItem -LiteralPath $focusedPath -File | Sort-Object Name | ForEach-Object { "$($_.Name):$((Get-FileHash $_.FullName -Algorithm SHA256).Hash)" }
./scripts/quality/Invoke-RequiredBounded.ps1 -EvidenceDirectory artifacts/release-qualification/required-phase100 -TimeoutSeconds 900
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$focusedAfter = Get-ChildItem -LiteralPath $focusedPath -File | Sort-Object Name | ForEach-Object { "$($_.Name):$((Get-FileHash $_.FullName -Algorithm SHA256).Hash)" }
if (($focusedBefore -join "`n") -cne ($focusedAfter -join "`n")) { throw 'Bounded Required execution modified focused evidence.' }
& ./scripts/quality/Invoke-RequiredBounded.ps1 -EvidenceDirectory artifacts/release-qualification/required-timeout-probe -TimeoutSeconds 1
if ($LASTEXITCODE -eq 0) { throw 'Bounded Required timeout probe unexpectedly passed.' }
$timeout = Get-Content -Raw ./artifacts/release-qualification/required-timeout-probe/required-invocation.json | ConvertFrom-Json
if ($timeout.timed_out -ne $true -or $timeout.status -cne 'failure' -or $timeout.process_tree_terminated -ne $true) { throw 'Timeout was not recorded as a terminated Required failure.' }
$lines = @(Get-Content ./.github/workflows/quality.yml)
$steps = @()
for ($index = 0; $index -lt $lines.Count; $index++) {
  if ($lines[$index] -match '^(?<indent>\s*)-\s+name:\s*(?<name>.+?)\s*$') {
    $indent = $Matches.indent.Length
    $stepName = $Matches.name
    $end = $index + 1
    while ($end -lt $lines.Count -and $lines[$end] -notmatch "^\s{$indent}-\s+name:") { $end++ }
    $steps += [pscustomobject]@{ Name = $stepName; Text = ($lines[$index..($end - 1)] -join "`n") }
    $index = $end - 1
  }
}
$passing = @($steps | Where-Object { $_.Text -match '(?m)^\s+name:\s*font-qualification-evidence\s*$' })
if ($passing.Count -ne 1) { throw 'Expected exactly one structurally scoped passing font evidence upload step.' }
$passingText = $passing[0].Text
if (
  -not $passingText.Contains('if: ${{ success() }}', [System.StringComparison]::Ordinal) -or
  -not $passingText.Contains('uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02', [System.StringComparison]::Ordinal) -or
  $passingText -notmatch '(?m)^\s+path:\s*artifacts/release-qualification/ci-font\s*$' -or
  $passingText.Contains('if: ${{ always() }}', [System.StringComparison]::Ordinal)
) { throw 'Passing font evidence upload step is not success-only and exact.' }
$always = @($steps | Where-Object { $_.Text.Contains('if: ${{ always() }}', [System.StringComparison]::Ordinal) })
foreach ($step in $always) {
  if ($step.Text -notmatch '(?im)diagnostic' -or $step.Text -match '(?m)^\s+name:\s*font-qualification-evidence\s*$' -or $step.Text -match '(?m)^\s+path:\s*artifacts/release-qualification/ci-font\s*$') {
    throw "Always-running upload step '$($step.Name)' is not isolated diagnostic output."
  }
}
    </automated>
  </verify>
  <acceptance_criteria>
    - "Required execution has a finite timeout, captures stdout/stderr, kills the full process tree, and writes a deterministic failure record on timeout/nonzero exit."
    - "Focused and Required jobs use distinct evidence directories, and bounded Required execution leaves focused evidence hashes unchanged."
    - "The exact `font-qualification-evidence` upload step is success-only and points only at validated focused evidence."
    - "Any `always()` upload has a separately parsed diagnostic artifact name/path and cannot be confused with passing evidence."
    - "Local or CI Required failures, including the Windows stall if reproduced, remain truthful failures rather than focused-font success."
  </acceptance_criteria>
  <done>CI and local closure preserve focused evidence while bounding and truthfully recording the separate Required lane per D-14/D-15.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Required process exhaustion and CI evidence spoofing are high-severity and blocking.

## Trust Boundaries

| Boundary | Description |
|---|---|
| focused selector -> passing artifact | Only validated four-target evidence may be labeled as passing qualification. |
| Required wrapper -> child process tree | A stalled workspace lane must be bounded and fully terminated. |
| Required diagnostics -> focused evidence | Failure capture must never mutate or relabel focused font records. |
| implementation evidence -> public docs | Claims must remain limited to exposed APIs and completed gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-05-01 | Denial of service | Required child process | high | mitigate | Enforce finite timeout, captured streams, full-tree Kill(true), wait-for-exit, and nonzero failure. |
| T-100-05-02 | Spoofing | passing CI artifact | high | mitigate | Parse the exact upload step and require success condition, pinned action, exact artifact name, and focused path. |
| T-100-05-03 | Tampering | focused evidence | high | mitigate | Hash focused records before/after Required and keep directories disjoint. |
| T-100-05-04 | Repudiation | Required outcome | medium | mitigate | Persist deterministic invocation metadata plus stdout/stderr and preserve timeout/nonzero status. |
| T-100-05-05 | Information disclosure | diagnostics upload | low | accept | Diagnostics contain only build/test output and use a distinct diagnostic artifact boundary. |
| T-100-05-SC | Tampering | CI action supply chain | medium | mitigate | Keep full commit SHA pins for checkout, setup, and upload actions; install no package-manager dependency. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Maintainer/CI reproduction and truthful final evidence | 100-05 Tasks 1-2 | COVERED |
| REQ | FONT-05 | Four-target host-independent qualification closure | 100-05 Tasks 1-2 plus 100-04 focused evidence | COVERED |
| RESEARCH | — | Docs, separate focused/Required CI, artifact upload, Windows stall honesty | 100-05 Tasks 1-2 | COVERED |
| CONTEXT | D-08,D-09 | Compact and DejaVu public workflow documentation | 100-05 Task 1 | COVERED |
| CONTEXT | D-14,D-15 | Dedicated selector plus separate bounded Required lane | 100-05 Task 2 | COVERED |
| CONTEXT | D-16,D-17 | Explicit exclusions and no scope/API expansion | 100-05 Task 1 | COVERED |
| CONTEXT | D-01-D-17 | Full phase implementation and enforcement | Plans 100-01 through 100-05 | COVERED |

All deferred families, containers, CFF, variations, color/bitmap fonts, deeper composites, shaping, hinting, discovery, rasterization, authoring, publication, and stability promotion remain excluded.
</source_audit>

<verification>
1. Run four target literate documentation checks and the focused policy selector.
2. Run focused qualification and hash its records.
3. Run bounded Required, verify its result record/streams, and prove focused hashes are unchanged.
4. Force a bounded timeout probe and require full-tree termination plus a real failure record.
5. Parse CI step mappings and verify passing versus diagnostic upload semantics independently.
</verification>

<success_criteria>
- Documentation accurately presents both public workflow oracles and exact provenance without private-component claims.
- Required execution is bounded, diagnostic, failure-preserving, and isolated from focused evidence.
- CI has separate required focused and Required jobs with unambiguous success/diagnostic artifacts.
- FONT-05 final closure remains reproducible on js, wasm, wasm-gc, and native without broadening scope.
</success_criteria>

<output>
Create `.planning/phases/100-portable-font-qualification/100-05-SUMMARY.md` when done, then hand Phase 100 to goal verification.
</output>
