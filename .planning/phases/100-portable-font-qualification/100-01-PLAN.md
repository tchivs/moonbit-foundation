---
phase: 100-portable-font-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/font/dejavu-sans-2.37/DejaVuSans.ttf
  - fixtures/font/dejavu-sans-2.37/LICENSE
  - fixtures/font/dejavu-sans-2.37/oracle.json
  - fixtures/font/qualification-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-FontQualification.ps1
  - modules/mb-font/font/generated_font_qualification_test.mbt
autonomous: true
requirements:
  - FONT-05
must_haves:
  truths:
    - "The repository contains the exact DejaVu Sans 2.37 font and notice selected by D-01/D-02, with immutable upstream identities and confirmed redistribution."
    - "A separately audited PowerShell oracle, never mb-font, owns the exact table inventory and semantic facts required by D-03/D-04."
    - "Portable tests receive exact generated MoonBit bytes bound to the canonical fixtures without runtime filesystem, network, or host-font access per D-05."
    - "One idempotent generator refuses archive, font, notice, schema, manifest-order, or generated-output drift per D-06/D-07."
  artifacts:
    - "fixtures/font/dejavu-sans-2.37/DejaVuSans.ttf"
    - "fixtures/font/dejavu-sans-2.37/LICENSE"
    - "fixtures/font/dejavu-sans-2.37/oracle.json"
    - "fixtures/font/qualification-cases.json"
    - "scripts/fixtures/Generate-FontQualification.ps1"
    - "modules/mb-font/font/generated_font_qualification_test.mbt"
  key_links:
    - "Generate-FontQualification.ps1 verifies the official ZIP before extracting the canonical TTF and notice."
    - "oracle.json is derived by the script's closed big-endian SFNT reader and is bound to the exact TTF SHA-256."
    - "generated_font_qualification_test.mbt reconstructs the exact TTF bytes and generated case data consumed by later public tests."
---

<objective>
Intake the licensed real font and establish the one-way canonical generation path for all Phase 100 test data.

Purpose: FONT-05 needs immutable licensed bytes, an independent oracle, and portable compile-time transport before parser or workflow claims can be qualified.
Output: Exact DejaVu bytes and notice, closed oracle and hostile-case JSON, deterministic generator/checker, manifest records, and generated MoonBit test data.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/100-portable-font-qualification/100-RESEARCH.md
@docs/policies/licensing-and-fixtures.md
@fixtures/manifest.json
@scripts/fixtures/Generate-ColorVectors.ps1
@scripts/fixtures/Generate-PngDecodeVectors.ps1
@scripts/quality/Test-FixturePolicy.ps1
@modules/mb-font/font/generated_fonts_wbtest.mbt

<interfaces>
The canonical external archive is:
`https://github.com/dejavu-fonts/dejavu-fonts/releases/download/version_2_37/dejavu-sans-ttf-2.37.zip`

Locked identities:
- ZIP: 417746 bytes, SHA-256 `5c6e497a2f36552cb5ffb112c413a6af39c0f3c47653662b90b4fa6499822fd7`
- TTF member `dejavu-sans-ttf-2.37/ttf/DejaVuSans.ttf`: 757076 bytes, SHA-256 `7da195a74c55bef988d0d48f9508bd5d849425c1770dba5d7bfc6ce9ed848954`
- notice member `dejavu-sans-ttf-2.37/LICENSE`: 8816 bytes, SHA-256 `7a083b136e64d064794c3419751e5c7dd10d2f64c108fe5ba161eae5e5958a93`
- license expression: `Bitstream-Vera AND LicenseRef-DejaVu-Arev`

`fixtures/manifest.json` requires the exact closed record fields `id`, `path`, `origin`, `source`, `author`, `retrieval_date`, `sha256`, `license`, `redistribution_status`, and `expected_use`; external records require `confirmed`.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| New file | `fixtures/font/dejavu-sans-2.37/DejaVuSans.ttf` | Exact upstream bytes; never rewritten by mb-font. |
| New file | `fixtures/font/dejavu-sans-2.37/LICENSE` | Exact upstream notice accompanying redistribution. |
| New file | `fixtures/font/dejavu-sans-2.37/oracle.json` | Closed, independently derived SFNT/table/cmap/metric/glyf/kern facts. |
| New file | `fixtures/font/qualification-cases.json` | Closed Apache-2.0 project-authored hostile matrix. |
| New file | `scripts/fixtures/Generate-FontQualification.ps1` | Sole intake/oracle/generation/check implementation. |
| New file | `modules/mb-font/font/generated_font_qualification_test.mbt` | Deterministic bounded MoonBit literal transport for canonical bytes and case records. |
| New PowerShell symbol | `Get-FontQualificationSha256` | Lower-case SHA-256 over exact bytes using .NET. |
| New PowerShell symbol | `Read-FontQualificationSfntOracle` | Small closed big-endian independent parser; must not invoke mb-font. |
| New PowerShell symbol | `Write-FontQualificationGeneratedSource` | Deterministic UTF-8-no-BOM renderer with round-trip byte verification. |
| New MoonBit symbol | `FontQualificationCase` | Test-private closed case record carrying id, stage, category, code, context, requested, limit, and expected publication. |
| New MoonBit symbol | `font_qualification_compact_bytes` | Test-private compact complete-workflow fixture accessor. |
| New MoonBit symbol | `font_qualification_dejavu_sans_237_bytes` | Test-private exact reconstructed DejaVu accessor. |
| New MoonBit symbol | `font_qualification_cases` | Test-private generated closed hostile-case accessor. |

No public `pub` symbol, production source file, runtime dependency, font format, or ambient capability is added by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Fail closed on DejaVu intake and derive the independent oracle</name>
  <files>scripts/fixtures/Generate-FontQualification.ps1, fixtures/font/dejavu-sans-2.37/DejaVuSans.ttf, fixtures/font/dejavu-sans-2.37/LICENSE, fixtures/font/dejavu-sans-2.37/oracle.json, fixtures/manifest.json</files>
  <read_first>
    - .planning/phases/100-portable-font-qualification/100-CONTEXT.md
    - .planning/phases/100-portable-font-qualification/100-RESEARCH.md
    - docs/policies/licensing-and-fixtures.md
    - fixtures/manifest.json
    - scripts/fixtures/Generate-ColorVectors.ps1
    - scripts/quality/Assert-Policy.ps1
  </read_first>
  <action>
Create `Generate-FontQualification.ps1` as the only Phase 100 intake and oracle tool. Its explicit intake mode must download the D-01 immutable URL to a temporary path, reject any ZIP length or SHA-256 mismatch before extraction, require the exact TTF and LICENSE member names, then reject extracted length/hash drift before writing repository files. Use .NET ZIP, SHA-256, and big-endian primitives already available in PowerShell; add no package. Do not commit the ZIP and do not consult installed fonts.

Implement `Read-FontQualificationSfntOracle` as a small closed offline parser, not a second runtime font engine and never a call into `mb-font` (D-03/D-17). Emit a stable schema/version plus oracle implementation/version, source identity, the exact 20-record sorted table inventory, and independently proven facts from research: SFNT signature, units-per-em, long loca, 6253 glyphs; global/hhea/OS2 metrics; U+0041->36, U+00E9->171, U+10300->5373; exact horizontal metrics and simple/composite classifications; independently derived outline command counts/fingerprints plus selected coordinates; A/V 36/57 kerning -131; cmap records including Macintosh 1/0 format 6; and maxp ceilings. Use the exact offsets, lengths, checksums, bounds, components, and counts from `100-RESEARCH.md`; reject unknown oracle keys and any mismatch rather than updating expectations from mb-font output.

Write the exact upstream TTF and notice under `fixtures/font/dejavu-sans-2.37/`. Append, without reordering existing records, two `external` manifest records for the TTF and notice using the immutable release URL, upstream authorship, retrieval date, exact digests, `Bitstream-Vera AND LicenseRef-DejaVu-Arev`, `confirmed`, and Phase 100 use (D-02). Record D-01 as `costly`: replacing this specimen would invalidate redistributed bytes, provenance, generated source, inventory, and frozen facts.
  </action>
  <reversibility rating="costly">Changing the D-01 specimen requires coordinated replacement of licensed bytes, manifest records, oracle, generated source, and every interoperability fact.</reversibility>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
./scripts/fixtures/Generate-FontQualification.ps1 -Check
$ttf = Get-Item -LiteralPath ./fixtures/font/dejavu-sans-2.37/DejaVuSans.ttf
$license = Get-Item -LiteralPath ./fixtures/font/dejavu-sans-2.37/LICENSE
if ($ttf.Length -ne 757076 -or (Get-FileHash $ttf.FullName -Algorithm SHA256).Hash.ToLowerInvariant() -cne '7da195a74c55bef988d0d48f9508bd5d849425c1770dba5d7bfc6ce9ed848954') { throw 'DejaVuSans.ttf identity drift' }
if ($license.Length -ne 8816 -or (Get-FileHash $license.FullName -Algorithm SHA256).Hash.ToLowerInvariant() -cne '7a083b136e64d064794c3419751e5c7dd10d2f64c108fe5ba161eae5e5958a93') { throw 'DejaVu LICENSE identity drift' }
$oracle = Get-Content -Raw ./fixtures/font/dejavu-sans-2.37/oracle.json | ConvertFrom-Json
if ($oracle.source.sha256 -cne '7da195a74c55bef988d0d48f9508bd5d849425c1770dba5d7bfc6ce9ed848954' -or @($oracle.tables).Count -ne 20) { throw 'Independent oracle identity/inventory drift' }
. ./scripts/quality/Assert-Policy.ps1
Assert-FixtureManifest -ManifestPath ./fixtures/manifest.json -RepositoryRoot (Resolve-Path .).Path
    </automated>
  </verify>
  <acceptance_criteria>
    - "A fresh intake verifies the official archive before repository mutation and verifies both extracted members before writing."
    - "The committed TTF and notice have the exact D-01 identities and remain under the precise upstream license/notice."
    - "The oracle names its independent implementation/version and contains the exact 20-table inventory and semantic facts from research."
    - "Two external manifest records are appended in canonical order with confirmed redistribution; existing record order is unchanged."
    - "No installed font, mb-font output, new package, public API, or runtime host capability participates in certification."
  </acceptance_criteria>
  <done>Exact licensed inputs and an independent fail-closed semantic oracle are canonical and reproducible per D-01 through D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Generate portable byte transport and the closed hostile matrix</name>
  <files>fixtures/font/qualification-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-FontQualification.ps1, modules/mb-font/font/generated_font_qualification_test.mbt</files>
  <read_first>
    - fixtures/font/dejavu-sans-2.37/oracle.json
    - modules/mb-font/font/generated_fonts_wbtest.mbt
    - modules/mb-font/font/font_test.mbt
    - fixtures/manifest.json
    - scripts/fixtures/Generate-PngDecodeVectors.ps1
    - scripts/quality/Test-FixturePolicy.ps1
  </read_first>
  <action>
Author `qualification-cases.json` as a closed schema of project-authored Apache-2.0 case descriptors for the exact D-11/D-13 IDs later consumed by public tests: malformed directory range, unsupported outline profile, retained-source mutation, checked range overflow, source-limit exact/one-short, open-budget exact/one-short, outline-budget exact/one-short, and recognized nested composite. Each descriptor must carry operation stage, public category/code, stable context, optional requested/limit, and whether a Font or Path2 may be published. Append its generated manifest record after the two external DejaVu records without disturbing pre-existing order. Do not label the TTF, notice, oracle byte source, or generated TTF literal as Apache-2.0 (D-07).

Extend the single generator so canonical TTF, oracle, and hostile JSON deterministically emit `generated_font_qualification_test.mbt`. Render 4096-byte bounded literal chunks initially, with one generator-owned chunk-size constant that may be adjusted only after the four-target compile probe. Reconstruct rendered bytes inside the generator and require exact length plus SHA-256 before write. The generated header must say it is generated/do-not-edit and bind the canonical TTF path, digest, and upstream license. Provide only the exact test-private symbols listed above. Build the compact complete-workflow fixture from established checksum-correct builders with BMP and supplementary cmap mappings, named metrics, exact simple/composite outlines, and a nonzero classic format-0 kern pair (D-04/D-08); do not copy production parser logic.

Make normal generation and `-Check` idempotent, preserve manifest ordering, use deterministic key/array order and UTF-8 without BOM, and refuse input/schema/license/digest drift. `-Check` must render in memory or a temporary file and byte-compare every generated artifact without mutating tracked files (D-05/D-06). Portable MoonBit code must never read fixture files or acquire filesystem, network, FFI, GUI, canvas, or host-font capability (D-05/D-16).
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$before = (Get-FileHash ./modules/mb-font/font/generated_font_qualification_test.mbt -Algorithm SHA256).Hash
./scripts/fixtures/Generate-FontQualification.ps1
$after = (Get-FileHash ./modules/mb-font/font/generated_font_qualification_test.mbt -Algorithm SHA256).Hash
if ($before -cne $after) { throw 'Generator is not idempotent' }
./scripts/fixtures/Generate-FontQualification.ps1 -Check
foreach ($target in @('js','wasm','wasm-gc','native')) {
  moon -C modules/mb-font check --target $target --frozen --target-dir "target/phase100-plan01-$target" --serial
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
    </automated>
  </verify>
  <acceptance_criteria>
    - "The hostile JSON has a closed ordered case set covering every D-11 category and every D-13 transactional exact/one-short fact."
    - "Generated MoonBit reconstructs exactly 757076 bytes with the locked TTF SHA-256 and carries upstream provenance in its header."
    - "The compact fixture contains the complete D-08 public workflow branches, including exact simple/composite commands and nonzero format-0 kerning."
    - "A second generation is byte-identical, `-Check` is read-only, and manifest ordering remains stable."
    - "The generated source compiles on js, wasm, wasm-gc, and native without target-specific source or ambient file access."
  </acceptance_criteria>
  <done>Canonical inputs deterministically produce portable test bytes and the complete closed hostile descriptor matrix per D-04 through D-07 and D-11/D-13.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. High-severity fixture substitution and self-oracle risks are blocking.

## Trust Boundaries

| Boundary | Description |
|---|---|
| official release -> intake script | Untrusted network bytes must match the locked archive identity before extraction. |
| extracted TTF/notice -> repository fixtures | Exact members, hashes, and license must be preserved without relabeling. |
| canonical fixtures -> generated MoonBit | Generated source must reproduce exact bytes without ambient runtime access. |
| external font -> semantic oracle | Independent tooling, never the library under test, must own expected facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-01-01 | Tampering | DejaVu archive intake | high | mitigate | Verify locked archive length/hash before extraction and locked member length/hash before any repository write. |
| T-100-01-02 | Spoofing | independent oracle | high | mitigate | Use a named/versioned closed PowerShell SFNT reader and reject mb-font-derived expectations or unknown schema keys. |
| T-100-01-03 | Tampering | generated MoonBit bytes | high | mitigate | Reconstruct rendered literals, verify exact length/SHA-256, and enforce byte-for-byte `-Check`. |
| T-100-01-04 | Repudiation | external licensing | medium | mitigate | Commit the exact notice and two complete external manifest records with immutable source and confirmed redistribution. |
| T-100-01-05 | Denial of service | generated source compilation | medium | mitigate | Use deterministic bounded chunks and compile-probe all four targets before freezing layout. |
| T-100-01-SC | Tampering | dependency supply chain | low | accept | No npm, pip, cargo, MoonBit, or other package is installed; only pinned repository tooling and .NET BCL are used. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Immutable licensed fixture and portable generated transport | 100-01 Tasks 1-2 | COVERED |
| REQ | FONT-05 | Licensed immutable fixtures without host capabilities | 100-01 Tasks 1-2 | COVERED |
| RESEARCH | — | Exact DejaVu identities, independent oracle, 4KiB chunks, closed hostile matrix | 100-01 Tasks 1-2 | COVERED |
| CONTEXT | D-01-D-04 | Exact specimen, notice, provenance, oracle, real/compact roles | 100-01 Task 1 and Task 2 | COVERED |
| CONTEXT | D-05-D-07 | Compile-time transport, one generator, license separation | 100-01 Task 2 | COVERED |
| CONTEXT | D-11,D-13 | Closed hostile descriptors and transactional pairs | 100-01 Task 2 | COVERED |
| CONTEXT | D-16,D-17 | No ambient capability or public-scope expansion | Both tasks | COVERED |

Deferred formats, shaping, hinting, rasterization, discovery, authoring, additional fonts, and publication promotion are excluded.
</source_audit>

<verification>
1. Re-fetch/intake proves archive and members before repository mutation.
2. `-Check` proves exact fixture, oracle, manifest, hostile JSON, and generated-source identity without writes.
3. Fixture policy validates the two external records and the project-authored hostile record.
4. The generated source compiles on all four supported targets with unique target directories.
</verification>

<success_criteria>
- Exact DejaVu Sans 2.37 bytes and notice are committed with fail-closed provenance.
- Independent oracle facts match the exact research inventory and cannot be updated from mb-font output.
- Generated compact and real-font bytes are deterministic, licensed correctly, and portable across all four targets.
- The closed hostile matrix is ready for public-entry-point execution without target-specific expectations.
</success_criteria>

<output>
Create `.planning/phases/100-portable-font-qualification/100-01-SUMMARY.md` when done.
</output>
