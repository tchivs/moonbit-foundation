---
phase: 73-explicit-packed-grayscale-png
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
autonomous: true
requirements:
  - GRAYPACK-01
  - GRAYPACK-02
must_haves:
  truths:
    - "A caller can explicitly select eager Gray1, Gray2, or Gray4 PNG output for a canonical opaque packed Gray/U8 image whose samples are exactly representable at that depth."
    - "The eager default for every new selector emits Stored DEFLATE, filter None, non-interlaced PNG colour type 0, with IHDR depth 1, 2, or 4."
    - "Packed scanlines place leftmost codes in high-order bits and deterministically clear unused final-byte lanes for odd widths."
    - "A nonrepresentable Gray/U8 sample fails before the eager writer advances or the caller budget changes."
    - "Existing generic, Gray8, and Gray16 routes preserve their prior wire behavior."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Explicit eager Gray1, Gray2, and Gray4 profile selectors fixed to Stored/None/non-interlaced output.
    - path: modules/mb-image/png/encode.mbt
      provides: Profile-aware exact-level admission, checked packed row layout, and one scalar MSB-first low-bit wire-byte provider.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Type-0 IHDR depth selection for the private low-bit profiles within the existing encoder machine.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Independent Stored-wire oracle and atomic eager rejection coverage for all three depths.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: PngEncoder profile reaches _png_encode_preflight_with_interlace_profile through PngEncodeMachine::new_with_profile.
      pattern: "PngEncodeProfile::Gray(1|2|4)"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: preflight facts carry the packed row bytes and profile into the one machine byte_at IHDR and IDAT traversal.
      pattern: "facts\.row_bytes|self\.profile"
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/encode.mbt
      via: public eager encodes are checked by a separate Stored-block parser against literal filtered-row bytes.
      pattern: "PngEncoder::new_gray(1|2|4)"
---

<objective>
Deliver D-01 through D-05 for GRAYPACK-01 and GRAYPACK-02: eager-only, lossless Type-0 grayscale PNG output at depths 1, 2, and 4 from the existing byte-per-pixel Gray/U8 source.

Purpose: allow callers to choose a legal packed PNG representation without widening the image model or creating another encoder transport.
Output: three explicit default selectors, profile-aware low-bit admission and serialization inside the established bounded machine, and independent black-box wire/atomicity evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-explicit-packed-grayscale-png/73-CONTEXT.md
@.planning/phases/73-explicit-packed-grayscale-png/73-RESEARCH.md
@.planning/phases/73-explicit-packed-grayscale-png/73-PATTERNS.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/model/descriptor.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship one complete Gray1 eager path through the existing bounded encoder</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/png.mbt:185-258 and 461-490 — private profile enum plus explicit Gray8/Gray16 selector shape.
    - modules/mb-image/png/encode.mbt:56-173, 431-476, and 1557-1866 — admission before the one charge, scalar wire-byte seam, checked layout, and eager traversal.
    - modules/mb-image/png/stream_encode.mbt:767-800 and 1288-1308 — one profile-aware machine constructor and IHDR byte source.
    - modules/mb-image/png/raster_decode.mbt:652-675 — authoritative inverse MSB-first low-bit lane computation.
    - modules/mb-image/png/encode_test.mbt:66-103, 594-646, and 1302-1352 — canonical Gray/U8 fixture, bounded Stored parser, and atomic eager test pattern.
  </read_first>
  <behavior>
    - RED: a canonical Gray/U8 row with width 9 and legal Gray1 values encodes through PngEncoder::new_gray1() to IHDR depth 1, colour type 0, and no interlace.
    - RED: the independent Stored block parser returns the literal None-filtered bytes for that row, including a final packed byte whose unused low seven lanes are zero.
    - RED: a Gray1 source containing a byte other than 0 or 255 returns a typed capability error while the memory writer remains at position 0 and every caller-budget remaining field equals its pre-call value.
    - GREEN: existing generic and Gray8/Gray16 tests remain unchanged and pass.
  </behavior>
  <action>Implement the first production-quality vertical slice per D-01 through D-04. Add the private low-bit profile family while exposing only PngEncoder::new_gray1() at this step; the public selector must construct the normal PngEncoder value with Stored compression, None filtering, and PngInterlaceStrategy::None. At the existing profile-aware admission seam, recognize low-bit Gray/U8 as canonical packed opaque Gray with the established metadata, top-left, and tightly packed source-row requirements. Add a reusable profile-to-depth/exact-code helper, scan every selected sample during admission, and return a capability error before any layout planning, limit validation, budget charge, machine construction, or writer byte when a value is not exactly representable (D-02). Preserve source tight-row validation as width bytes for Gray/U8, then derive wire row_bytes using checked ceiling arithmetic for width times selected bit depth divided by eight; use filtering byte stride 1 for packed wire bytes. Extend only the shared scalar provider: construct each packed output byte from zero, enumerate its in-range source pixels, reduce their validated codes, and place codes using start=x*depth and shift=8-depth-(start%8), so the first sample is MSB-first and tail lanes are zero (D-03). Keep the one preflight, existing Stored/None planner and replay, and eager one-byte acknowledge loop; introduce no allocated packed raster or alternate traversal. Extend the existing IHDR profile switch in the machine to emit type 0 plus depth 1 for Gray1 and retain interlace byte 0. Add a Gray1 black-box test and a newly named general bounded Stored-IDAT helper rather than coupling to the Gray16 helper; it must parse PNG chunks and Stored zlib framing independently, take the expected scanline length, and compare literal filtered bytes rather than calling decoder or encoder packing helpers.</action>
  <verify>
    <automated>moon test modules/mb-image/png</automated>
  </verify>
  <done>Gray1 has an explicit eager Stored/None selector that produces a legal non-interlaced Type-0/1 PNG with independently asserted MSB-first odd-width bytes, and invalid Gray1 levels are rejected atomically before output or caller-budget exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the same eager contract to Gray2 and Gray4 and complete all-depth atomic wire evidence</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/png.mbt — Task 1 selector/profile addition and the Gray8/Gray16 strategy-taking APIs that must remain the only such APIs.
    - modules/mb-image/png/encode.mbt — Task 1 depth helper, exact-level admission, packed row-byte arithmetic, and scalar provider.
    - modules/mb-image/png/stream_encode.mbt — Task 1 IHDR profile match.
    - modules/mb-image/png/encode_test.mbt — Task 1 bounded Stored helper and Gray1 atomic test, plus established Gray16 atomic budget-field assertions.
  </read_first>
  <behavior>
    - A width-5 legal Gray2 row serializes codes 0, 1, 2, 3, 0 MSB-first into literal packed bytes with the final six unused bits cleared, and IHDR is 2/type-0/non-interlaced.
    - A width-3 legal Gray4 row serializes codes 0, 15, 1 MSB-first into literal packed bytes with the final four unused bits cleared, and IHDR is 4/type-0/non-interlaced.
    - Gray2 rejects a sample outside {0,85,170,255}; Gray4 rejects a U8 sample not divisible by 17; each failure leaves writer position and all caller-budget remaining fields unchanged.
    - Existing default generic, Gray8, and Gray16 byte tests continue to pass byte-for-byte.
  </behavior>
  <action>Complete D-01 through D-05 by exposing PngEncoder::new_gray2() and PngEncoder::new_gray4(), each fixed exactly to Stored compression, None filter, and no interlace; do not add strategy-taking overloads, PngChunkEncoder factories, Adam7 selection, palette support, source-model APIs, or quantizing conversions. Generalize the Task 1 private profile/depth path so Gray2 admits exactly 0/85/170/255 and emits codes 0 through 3, and Gray4 admits only multiples of 17 and emits codes 0 through 15. Keep level admission strictly before output/limit/budget work, preserve the same checked packed row layout and filter byte stride, and route every byte through the single machine/provider added in Task 1 (D-02, D-04). Extend the existing IHDR match for all new profiles so Gray2 emits 0x02 and Gray4 emits 0x04 while colour type remains 0 and the final IHDR interlace byte remains 0. Add all-depth public black-box assertions using the independent Stored wire helper: use odd widths for every depth, literal expected filtered scanlines, IHDR offsets 24/25/28, and at least one zero-tail row per selector. For each new profile add an invalid canonical Gray/U8 fixture and assert the typed admission error, writer position 0, and bytes/allocations/work remaining equal before values; retain the Gray1 case from Task 1 so all three depths prove atomic rejection. Run the ordinary PNG package test command to guard the unchanged generic, Gray8, and Gray16 routes.</action>
  <verify>
    <automated>moon test modules/mb-image/png</automated>
  </verify>
  <done>All three explicit eager selectors produce independently verified Stored/None Type-0 grayscale wire data at the selected depth, their odd-width padding is zero, and every selected profile rejects nonrepresentable levels atomically without changing legacy output contracts.</done>
</task>

</tasks>

<source_audit>

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Explicit lossless Type-0/1, /2, /4 output from exactly representable canonical Gray/U8 | 73-01 | COVERED | Tasks 1-2 add public selectors, packed provider, IHDR, and black-box evidence.
REQ | GRAYPACK-01 | Explicit legal non-interlaced low-bit output with MSB-first samples and zero padding | 73-01 | COVERED | Task 1 proves Gray1 end to end; Task 2 covers Gray2/Gray4 and all-depth literals.
REQ | GRAYPACK-02 | Unsupported levels/descriptors/limits/budgets fail before eager output | 73-01 | COVERED | Tasks place level admission before preflight ledger charge and assert writer/budget atomicity.
RESEARCH | — | Defaults are Stored/None only; no strategy/chunk surface | 73-01 | COVERED | Both task actions freeze the eager constructor configuration.
RESEARCH | — | Profile-aware checked packed layout and one scalar provider/machine | 73-01 | COVERED | Tasks alter the existing preflight/provider/IHDR seams only.
RESEARCH | — | Independent Stored oracle for odd widths and zero tails | 73-01 | COVERED | Tasks require a new general bounded parser and literal rows for 1/2/4.
CONTEXT | D-01 | Explicit eager Gray1/Gray2/Gray4; generic and Gray8 frozen | 73-01 | COVERED | Task actions and behavior cite D-01.
CONTEXT | D-02 | Exact legal levels; rejection precedes output and budget | 73-01 | COVERED | Task actions and atomic tests cite D-02.
CONTEXT | D-03 | MSB-first packing and zero tail; Stored oracle | 73-01 | COVERED | Task actions and all-depth literal tests cite D-03.
CONTEXT | D-04 | One profile-aware bounded machine, no staging/model change | 73-01 | COVERED | Task actions require the shared preflight/provider/machine path.
CONTEXT | D-05 | Eager non-interlaced only; defer buffered/Adam7/strategy qualification | 73-01 | COVERED | Task actions restrict public surface and test scope.

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical ImageView → encoder preflight | Caller-controlled descriptor, geometry, and Gray/U8 samples enter the codec before resource accounting. |
| Preflight facts → bounded encoder machine | Packed row arithmetic and profile facts control all planner, checksum, and emitted-byte traversals. |
| Encoder → eager Writer | Fully admitted bytes cross into caller-owned output one acknowledged byte at a time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering | `_png_encode_source` exact-level admission | high | mitigate | Scan every selected Gray/U8 sample before layout planning, limits, budget charge, or machine creation; reject nonrepresentable inputs with the existing typed capability-error path. |
| T-73-02 | Denial of service | packed row-byte and scanline arithmetic | medium | mitigate | Use existing checked multiplication/addition for ceil(width*depth/8), scanline, output, and work calculations before the one budget charge. |
| T-73-03 | Tampering | scalar packed wire provider and IHDR emission | medium | mitigate | Derive every byte from a zero accumulator and in-range samples using the decoder-mirrored MSB-first shift; assert literal odd-width Stored rows and IHDR type/depth/interlace bytes independently. |
| T-73-04 | Repudiation | eager failure boundary | low | mitigate | Black-box tests assert typed invalid admission plus writer position and all caller-budget fields remain unchanged. |
| T-73-SC | Tampering | package installation | low | accept | This plan installs no package; no supply-chain mutation is in scope. |
</threat_model>

<verification>
Run `moon test modules/mb-image/png`. The suite must execute the new independent Stored/None wire oracle and invalid atomic-admission cases for depths 1, 2, and 4 alongside frozen generic/Gray8/Gray16 coverage.
</verification>

<success_criteria>
- `PngEncoder::new_gray1`, `new_gray2`, and `new_gray4` are the only new public selectors and each defaults to Stored/None/non-interlaced output.
- Legal canonical Gray/U8 sources emit Type-0 PNG with IHDR bytes `[24]` equal to the selected depth, `[25]` equal to 0, and `[28]` equal to 0.
- Independent Stored block parsing proves literal MSB-first rows and zero unused final lanes at odd widths for all three depths.
- Invalid selected-level samples fail before output and preserve caller-budget bytes, allocations, and work remaining.
- The sole bounded machine remains the source of preflight, compression/filter traversal, checksums, and eager emission; the original source model and legacy output tests remain intact.
</success_criteria>

<output>
Create `.planning/phases/73-explicit-packed-grayscale-png/73-01-SUMMARY.md` when done.
</output>
