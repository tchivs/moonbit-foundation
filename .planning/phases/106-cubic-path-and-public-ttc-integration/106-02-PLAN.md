---
phase: 106-cubic-path-and-public-ttc-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "106-01"
files_modified:
  - modules/mb-core/math/path_wbtest.mbt
  - modules/mb-font/font/cff_type2_path.mbt
  - modules/mb-font/font/cff_type2_path_wbtest.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/font_test.mbt
autonomous: true
requirements:
  - CFF-04
must_haves:
  truths:
    - "The Wave 1 production transaction continues to execute only the selected admitted GID with fresh VM, PRNG, geometry, and path state."
    - "Expansion evidence covers every exact/one-short caller and ancestor dimension over the complete Wave 1 VM/path transaction."
    - "Expansion evidence covers only authorized pre-commit mutation seams: before execution, during VM fetch, and after complete staging before the final source-revision guard."
    - "Every mutation at an authorized pre-commit seam returns State, leaves caller and ancestor budgets unchanged, and returns no partial Path2."
    - "Repeated and interleaved outline calls are deterministic, order-independent, and never substitute retained bounds for geometry."
    - "The only legal closing sequence is applicable pre-execution and in-VM probes, post-stage mutation probe, final source-revision guard, Budget::charge exactly once, then an infallible immediate Path2 return with no subsequent probe, guard, allocation, validation, callback, or other fallible work."
  artifacts:
    - "modules/mb-core/math/path_wbtest.mbt freezes zero, exact positive, negative, insertion-order, and length behavior for the format-neutral capacity constructor."
    - "modules/mb-font/font/cff_type2_path_wbtest.mbt expands direct conversion, command, exact/one-short, repetition, and mismatch evidence over the Wave 1 implementation."
    - "modules/mb-font/font/font_wbtest.mbt expands mutation injection and multi-fault precedence evidence without creating another transaction."
    - "modules/mb-font/font/font_test.mbt freezes invalid/cross-font GID and repeated public-query behavior."
  key_links:
    - "Retained AdmittedCff1 descriptor/count -> fresh selected VM -> Type2PathSink -> complete staged Path2."
    - "Wave 1 CffPathCharge transaction -> exact/one-short matrix for bytes, allocations, allocation size, work, caller, and ancestors."
    - "Wave 1 authorized pre-commit probes -> post-stage mutation probe -> final source-revision guard -> exactly one Budget::charge -> infallible immediate Path2 return."
  prohibitions:
    - statement: "No full-path or command-stream value is retained at admission."
      status: unverified
      verification: flagged
    - statement: "No query reparses CFF INDEX, DICT, charset, Encoding, FDSelect, Private DICT, or subroutine structure."
      status: unverified
      verification: flagged
    - statement: "No sink or VM helper commits Budget independently or publishes partial geometry."
      status: unverified
      verification: flagged
    - statement: "No retained bounds are used to reconstruct an outline."
      status: unverified
      verification: flagged
    - statement: "No existing glyf outline behavior or charge constant changes."
      status: unverified
      verification: flagged
---

<objective>
Expand hostile, repetition, multi-fault, resource, and mutation evidence around the complete selected-glyph transaction already delivered by Wave 1.

Purpose: Exhaustively freeze the Wave 1 D-08 through D-11 transaction and D-16/D-17 public compatibility without introducing its basic capacity, preflight, execution, guard, commit, or publication mechanics here.
Output: Direct capacity/conversion/path-sink goldens, full exact/one-short caller/ancestor matrices, mutation probes only at authorized pre-commit seams, deterministic multi-fault outcomes, and repeated/interleaved public-query evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-CONTEXT.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-RESEARCH.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-PATTERNS.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-01-SUMMARY.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-05-SUMMARY.md
@modules/mb-font/font/cff_type2_path.mbt
@modules/mb-font/font/font.mbt
@modules/mb-core/budget/budget.mbt
@modules/mb-font/font/outline.mbt

<interfaces>
Complete Wave 1 interfaces consumed without redesign:
- `Path2::with_capacity(Int) -> Path2`.
- Private `cff_decode_outline_atomic(AdmittedCff1, gid, Budget, ...) -> Result[Path2, CoreError]`.
- Private `CffPathCharge` computed from exact VM/path work, real allocation count/largest allocation, and retained capacity times 64 logical bytes.
- Private per-GID retained publishable command count aligned one-to-one with `AdmittedCff1.glyphs` and `bounds`.
- `Budget::preflight(ResourceCharge)` checks caller and every ancestor without mutation; `Budget::charge(ResourceCharge)` preflights then commits every window.
- `Font::outline_after_decode` performs the public initial GID/revision validation; after applicable pre-execution/in-VM probes and the post-stage mutation probe, the final source-revision guard is followed only by exactly one `Budget::charge` and the infallible immediate Path2 return.
</interfaces>
</context>

<edge_assumptions>
1. Null outline input is impossible in the typed API; zero-length path output is a successful value, not an absent result.
2. Exact equality of two repeated query inputs specifies identical ordered PathCommand values and identical charge deltas; state is fresh per call.
3. A one-short caller or ancestor limit is tested independently for bytes, allocations, allocation size, and work, with all other dimensions exact.
4. Mutation restoration still counts as drift because the retained ByteView revision changes.
</edge_assumptions>

## Artifacts this phase produces

New files:
- `modules/mb-font/font/cff_type2_path_wbtest.mbt` — direct conversion, native command, exact/one-short, mismatch, repetition, and atomic-failure expansion evidence.

New symbols:
- Private white-box query probes only before execution, during VM fetch where applicable, and after complete staging before the final source-revision guard.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand exact/one-short, conversion, repetition, and ancestor evidence</name>
  <files>modules/mb-core/math/path_wbtest.mbt, modules/mb-font/font/cff_type2_path.mbt, modules/mb-font/font/cff_type2_path_wbtest.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    @.planning/phases/106-cubic-path-and-public-ttc-integration/106-01-SUMMARY.md
    @modules/mb-core/math/path_wbtest.mbt
    @modules/mb-font/font/cff_type2_path.mbt
    @modules/mb-font/font/cff_type2_bounds_wbtest.mbt
    @modules/mb-font/font/font.mbt
    @modules/mb-font/font/font_wbtest.mbt
    @modules/mb-font/font/font_test.mbt
    @modules/mb-core/budget/budget.mbt
    @modules/mb-font/font/outline.mbt
  </read_first>
  <behavior>
    - Capacity zero creates an empty path; exact positive capacity preserves insertion order and length; negative capacity fails at the format-neutral constructor boundary.
    - The Wave 1 transaction succeeds at exact bytes, allocations, allocation size, and work for both direct caller and nested ancestor windows; every independent one-short case fails Resource with no path or charge.
    - Repeated and interleaved selected-GID calls reproduce identical ordered commands, fresh deterministic PRNG state, and identical charge deltas per D-08.
    - Direct path-sink goldens freeze the resolved quotient-plus-remainder conversion for positive, negative, integral, fractional, transformed cubic-control, and large-cancellation coordinates.
    - Retained/emitted command mismatch and `capacity * 64` overflow remain atomic failures over the Wave 1 D-09/D-10 transaction.
    - Per D-09, the closing transaction order remains applicable pre-execution/in-VM probes, post-stage mutation probe, final source-revision guard, exactly one `Budget::charge`, and an infallible immediate Path2 return; no fallible work follows the charge.
  </behavior>
  <action>
    Treat the Wave 1 `CffPathCharge`, `Path2::with_capacity(Int)`, selected-GID VM entry point, D-09 transaction ordering, and 64-byte logical authority unit as complete production contracts. Add the expansion-only direct `Path2::with_capacity(Int)` tests moved from Plan 106-01 for zero, exact positive, negative, insertion order, and length behavior. Add direct `cff_type2_path_wbtest.mbt` goldens for the resolved quotient-plus-remainder conversion and native line/cubic/flex command sequences. Expand the existing exact tracer pair into independent exact/one-short bytes, allocations, allocation-size, work, direct-caller, and nested-ancestor cases, plus checked `capacity * 64` overflow and retained/emitted count mismatch. Add repeated and interleaved public calls that compare every ordered PathCommand and every Budget delta. Preserve the sole closing sequence: applicable pre-execution/in-VM probes, post-stage mutation probe, final source-revision guard, exactly one `Budget::charge`, then the already-staged Path2 returns immediately through an infallible path.

    Reuse `outline.mbt` only as the typed PathCommand lowering and capacity-preflight analog. Do not alter its glyf-specific QuadTo behavior, `command_bound * 32` charge formula, or incremental branch. Production edits in this task are limited to the authorized pre-commit test probes or corrections exposed by the expanded evidence; do not create another selected-glyph transaction or any post-commit seam.
  </action>
  <acceptance_criteria>
    - The Wave 1 success changes every caller and ancestor counter by exactly the named CffPathCharge once.
    - Exact capacity succeeds; every independent one-short bytes, allocations, allocation-size, or work case fails Resource and changes no counter.
    - Direct conversion goldens freeze the resolved quotient-plus-remainder rule without early Double arithmetic.
    - Every call starts from the admitted seed and fresh state; repeated/interleaved calls have identical commands and charge deltas.
    - Retained/emitted command-count disagreement is a deterministic internal Data failure with no path and no charge.
    - Once `Budget::charge` succeeds, the already-staged Path2 is returned immediately without another probe, guard, allocation, validation, callback, conversion, or other fallible operation.
  </acceptance_criteria>
  <verify>
    <automated>moon test modules/mb-core/math --target native &amp;&amp; moon test modules/mb-font/font --target native --filter "*CFF outline exact path charge*"</automated>
  </verify>
  <done>The selected CFF outline path follows the sole D-09 sequence through one charge and an infallible immediate return under exact caller and ancestor authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze mutation, multi-fault, invalid-GID, and zero-publication behavior</name>
  <files>modules/mb-font/font/cff_type2_path_wbtest.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    @modules/mb-font/font/cff_type2_fixture_wbtest.mbt
    @modules/mb-font/font/cff_type2_path_wbtest.mbt
    @modules/mb-font/font/font.mbt
    @modules/mb-font/font/font_wbtest.mbt
    @modules/mb-font/font/font_test.mbt
    @modules/mb-core/budget/budget.mbt
    @modules/mb-font/font/outline.mbt
  </read_first>
  <behavior>
    - Mutation before selected execution, during VM fetch, or at the sole post-stage probe before the final source-revision guard yields State with no Path2 and no committed query charge.
    - An out-of-range or foreign-font GlyphId returns the existing `font-outline` invalid-range behavior before descriptor access or Budget mutation.
    - With stable revision, Resource wins recognized Capability and encountered Data; with revision drift, State wins every competing non-State failure.
    - Failed conversion, command mismatch, one-short authority, malformed execution, unsupported operator, and mutation all publish no partial command prefix.
    - Per D-09, after applicable pre-execution and in-VM probes the only closing sequence is post-stage mutation probe, final source-revision guard, `Budget::charge` exactly once, and infallible immediate Path2 return.
  </behavior>
  <action>
    Use private no-op-by-default probes only before the existing Wave 1 selected-query execution, during VM fetch where applicable, and at the single post-stage seam before the final source-revision guard. Do not add an immediate-return probe or any other seam after `Budget::charge`. Build twin mutation/no-mutation fixtures for truncated fetch, unsupported CFF2/seac operator, rational conversion failure, command-count mismatch, and one-short Budget. Assert that mutation at each authorized pre-commit seam returns State with unchanged caller plus ancestor counters and no Path2. Assert exact existing category/code/operation ordering for stable multi-fault cases. Add black-box invalid and cross-font GlyphId cases to freeze D-16 public behavior. Retain D-17 glyf typed lowering, QuadTo output, operation ordering, and charge fingerprints unchanged. The implementation must end with the post-stage probe, final source-revision guard, exactly one `Budget::charge`, and an infallible immediate return of the already-staged Path2, with no later probe, guard, allocation, validation, callback, conversion, or other fallible work.
  </action>
  <acceptance_criteria>
    - Every authorized pre-commit query mutation site returns State and leaves all Budget windows unchanged, including mutate-and-restore.
    - Stable multi-fault outcomes retain State -> Resource -> Capability -> Data precedence.
    - Invalid receiving-font GID behavior matches the existing glyph-range contract and performs no CFF work.
    - No failure exposes a partial path or changes the retained admitted Font.
    - Full standalone CFF mapping, metrics, kerning, and cubic-path black-box coverage remains green.
    - After the final source-revision guard, exactly one `Budget::charge` succeeds and the already-staged Path2 returns immediately through an infallible path.
  </acceptance_criteria>
  <verify>
    <automated>moon test modules/mb-font/font --target native --filter "*CFF outline atomic*"</automated>
  </verify>
  <done>CFF-04 outline queries have deterministic error precedence, mutation protection, and zero partial publication across all guarded seams.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Applicability and Threshold

This plan hardens untrusted retained font bytes and explicit caller/ancestor resource windows. Critical/high threats are mitigated and block completion; medium threats on the primary byte/query boundary are mitigated. No dependency installation occurs.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opaque GlyphId -> receiving CFF-backed Font | A numeric ID from another Font or outside cardinality must not select an admitted descriptor. |
| retained mutable ByteView -> selected VM execution | Revision drift is observed by authorized probes before execution, during VM fetch, and after complete staging before the final source-revision guard. |
| exact path transaction -> Budget caller/ancestors | All governed allocations and work must succeed together or leave every window unchanged. |
| private staged Path2 -> public return | No partial command prefix may cross on any failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-07 | Spoofing | receiving-font GlyphId validation | high | mitigate | Preserve initial revision and cardinality checks before descriptor lookup; cross-font and out-of-range tests prove no Budget mutation. |
| T-106-08 | Tampering | retained ByteView during outline execution | high | mitigate | Authorized probes run only before execution, during VM fetch, and post-stage before the final source-revision guard; any detected mutation returns State with every caller/ancestor Budget unchanged. |
| T-106-09 | Denial of Service | selected VM/path arrays and work | high | mitigate | Wave 1 stages exact CffPathCharge and Path2 under caller/ancestor preflight, then performs the post-stage probe, final source-revision guard, exactly one Budget::charge, and an infallible immediate return; independent one-short and nested-window tests prove zero charge on failure. |
| T-106-10 | Tampering | retained versus emitted command cardinality | medium | mitigate | Wave 1 rejects count disagreement atomically; this plan adds mismatch and checked capacity-times-64 overflow evidence. |
| T-106-11 | Repudiation | State/Resource/Capability/Data ordering | medium | mitigate | Twin multi-fault tests assert category, code, operation, context, result absence, and unchanged counters. |
| T-106-12 | Information Disclosure | partial path on error | low | accept | Path2 remains private through all authorized pre-commit probes and the final source-revision guard; after the sole charge it returns immediately through an infallible path, while every earlier failure exposes only `Err`. |
</threat_model>

<verification>
Run from the repository root:

1. `moon test modules/mb-core/math --target native`
2. `moon test modules/mb-font/font --target native --filter "*CFF outline exact path charge*"`
3. `moon test modules/mb-font/font --target native --filter "*CFF outline atomic*"`
4. `moon test modules/mb-font/font --target native --filter "*Phase 106 standalone CFF1*"`
5. `moon test modules/mb-font/font --target native --filter "Phase 105 preserves the public standalone static glyf fingerprint"`
6. `moon test --target native`
7. `moon check --target all`
</verification>

<success_criteria>
- The Wave 1 single exact caller-and-ancestor transaction remains the only selected CFF outline implementation.
- Fresh per-call state, exact capacity, count agreement, authorized pre-commit probes, the final source-revision guard, exactly one charge, and an infallible immediate return are executable facts.
- All failure and multi-fault cases return no Path2 and preserve every Budget window.
- CFF-04 format-neutral errors, glyph validation, mapping, metrics, kerning, and cubic paths remain stable.
- Existing glyf behavior and charges do not drift.
</success_criteria>

<output>
After completion, create `.planning/phases/106-cubic-path-and-public-ttc-integration/106-02-SUMMARY.md`.
</output>
