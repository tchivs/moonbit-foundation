---
phase: 25-portable-colour-conformance-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
  - modules/mb-image/png/png_test.mbt
  - scripts/quality/Invoke-MoonQuality.ps1
  - modules/mb-image/README.mbt.md
autonomous: true
requirements: [PNGCM-05]
must_haves:
  truths:
    - "Generated PNG vectors independently cover every recognised sRGB, gAMA, cHRM, and iCCP grammar, authoritative-precedence outcome, and bounded ICC resource rejection."
    - "Legal colour declarations produce equivalent metadata and pixels through the chosen split-IDAT schedules, while rejected declarations preserve the same typed failure and no-image result."
    - "The isolated PNG quality lane and explicit public PNG package tests establish those outcomes on js, wasm, wasm-gc, and native."
    - "Public mb-image documentation states that PNG declarations are retained or identified without transforming sample values, and explains why retained non-sRGB images cannot enter reference operations or canonical PNG encoding."
  artifacts:
    - path: fixtures/png/decode-cases.json
      provides: "Declarative independent positive, hostile, precedence, resource-ceiling, and split-IDAT colour corpus."
      contains: "colour_chunks"
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: "Independent assembly/oracle, required-ID completeness gate, and generated-source freshness proof for PNG colour conformance vectors."
      contains: "Generate-PngDecodeVectors"
    - path: modules/mb-image/png/png_test.mbt
      provides: "Public PngDecoder assertions for metadata identity, exact typed rejections, image invisibility, capability boundaries, and split equivalence."
      contains: "png_test_decode_vector"
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: "Isolated PNG quality trace that executes generated colour evidence, public all-target package tests, and the checked public documentation."
      contains: "Invoke-PngQualityLane"
    - path: modules/mb-image/README.mbt.md
      provides: "Checked public contract for PNG declaration preservation and unavailable colour-transform boundaries."
      contains: "PNG colour declarations"
  key_links:
    - from: fixtures/png/decode-cases.json
      to: modules/mb-image/png/generated_decode_vectors_test.mbt
      via: "Generate-PngDecodeVectors independently assembles authenticated PNG bytes and regenerates PngDecodeVector records; the manifest records the generated artifact digest."
      pattern: "colour_chunks|PngDecodeVector"
    - from: modules/mb-image/png/generated_decode_vectors_test.mbt
      to: modules/mb-image/png/png_test.mbt
      via: "The public PngDecoder harness executes each generated record under its declared limits and compares metadata/pixels or typed error outcome to its unsplit baseline."
      pattern: "_generated_png_decode_cases|comparison_group"
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: modules/mb-image/png/png_test.mbt
      via: "The isolated Png lane runs generator freshness plus `moon -C modules/mb-image test png --target all --frozen`."
      pattern: "PNG generated decode vectors|PNG four-target tests"
---

<objective>
Deliver independent, portable proof for the already shipped PNG colour-declaration
contracts and document their exact preservation boundary. The work adds evidence
and documentation only: it neither transforms colour samples nor expands the
PNG codec surface.

Purpose: Close PNGCM-05 with reproducible all-target conformance evidence that
cannot depend on parser-private helpers.
Output: A manifest-backed colour corpus, public decoder/capability assertions,
an explicit PNG quality-lane trace, and checked public documentation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-portable-colour-conformance-evidence/25-CONTEXT.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-VERIFICATION.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-01-SUMMARY.md
@fixtures/png/decode-cases.json
@fixtures/manifest.json
@scripts/fixtures/Generate-PngDecodeVectors.ps1
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/generated_decode_vectors_test.mbt
@scripts/quality/Invoke-MoonQuality.ps1
@modules/mb-image/README.mbt.md

<interfaces>
From `modules/mb-image/png/png_test.mbt`:
- `png_test_decode_vector(item : PngDecodeVector) -> Result[@codec.DecodeResult, @error.CoreError]` runs `PngDecoder` through the public `@codec.ImageDecoder::decode` contract with record-supplied output/work/budget ceilings.
- The generated-vector test already checks accepted dimensions, channels, pixels, sRGB intent, non-sRGB `LinearSrgb`/opaque identity, `icc_profile_length`, typed category/code/context, and same-group baseline equivalence.

From `scripts/fixtures/Generate-PngDecodeVectors.ps1`:
- `colour_chunks` accepts only `sRGB`, `gAMA`, `cHRM`, and `iCCP`; the generator independently assembles chunks and validates placement, duplicate rules, fixed-length grammar, ICC envelope grammar, CRCs, and declared split coverage.
- `comparison_group` plus an unsplit `baseline` produces partition-equivalence records; generated MoonBit is derived output and must be regenerated rather than edited manually.

From `scripts/quality/Invoke-MoonQuality.ps1`:
- `Invoke-PngQualityLane` is intentionally isolated and currently runs structural/decode generator freshness and `moon -C modules/mb-image test png --target all --frozen`; its trace assertion must be updated with any added stage.
</interfaces>
</context>

<source_audit>
SOURCE  | ID       | Feature / constraint | Plan | Status | Notes
------- | -------- | -------------------- | ---- | ------ | -----
GOAL    | —        | Independently verify colour-metadata behaviour and bounded failures on every portable target. | 01 | COVERED | Tasks 1-2 provide generated public evidence and the isolated all-target quality proof.
REQ     | PNGCM-05 | Recognised-colour positives, ordering/precedence failures, and bounded profile expansion failures run on js, wasm, wasm-gc, and native. | 01 | COVERED | Task 1 adds required corpus/harness coverage; Task 2 makes it canonical quality evidence.
RESEARCH| —        | No Phase 25 research artifact exists; Level 0 applies because established generator, public harness, documentation, and quality-lane patterns are extended without dependencies. | 01 | COVERED | No external library, service, API, or architecture selection is introduced.
CONTEXT | D-01     | Retain the declarative generator as source of truth; add only independently assembled grammar/precedence/resource/split fixtures. | 01 | COVERED | Task 1.
CONTEXT | D-02     | Assert public metadata, exact errors, no image visibility, and output/operation boundaries rather than private helpers. | 01 | COVERED | Task 1.
CONTEXT | D-03     | PNG quality lane plus explicit all-target public package tests are canonical proof; exclude CI/release automation. | 01 | COVERED | Task 2.
CONTEXT | D-04     | Add deterministic split-IDAT equivalence for legal colour declarations. | 01 | COVERED | Task 1.
CONTEXT | D-05     | Clearly state preservation/identification without sample transformation and non-sRGB ineligibility for reference operations/canonical encoding. | 01 | COVERED | Task 2.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand independently generated public colour conformance vectors</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - Every recognised grammar has compact accepted and rejected public cases: sRGB intent/length/duplicate/order, gAMA value/length/duplicate/order, cHRM length/duplicate/order, and iCCP name/method/envelope/zlib/header/profile-space rules.
    - Independent records prove the authoritative metadata result for compatible sRGB-plus-legacy declarations, retained legacy declarations, and iCCP precedence, plus the existing compressed, inflated, allocation, and work resource ceilings.
    - For each accepted legal colour-declaration representative, an unsplit baseline and deterministic split-IDAT schedules decode to identical descriptor metadata, opaque values/profile identity, dimensions, channels, and pixels; equivalent hostile partitions preserve category, code, context, and no decoded image.
    - Public assertions reject retained non-sRGB sources at the existing reference-operation and canonical-PNG-encoding capability boundary without any output becoming visible.
  </behavior>
  <action>Begin by adding failing public harness assertions and required corpus identifiers, then extend the declarative JSON and `Generate-PngDecodeVectors.ps1` per D-01 and D-02. Keep `decode-cases.json` as the only hand-authored corpus: derive PNG bytes, CRCs, ICC envelopes, all split records, generated MoonBit, and its manifest digest through the existing PowerShell oracle; do not edit the generated test directly. Add compact representatives for every recognised colour grammar and the existing precedence/resource-ceiling cases, using only enough split offsets to cover declaration-bearing legal inputs plus one representative failure schedule. Extend the generated record schema only where required to make public assertions compare metadata identity/opaque values, exact error data, image invisibility, and the established `supports_reference_operations`/encoder no-output capability boundary. Retain parser encapsulation: execute `PngDecoder`, descriptor metadata, public operation eligibility, and `PngEncoder` rather than structural helpers. Preserve all existing PNG cases and generator provenance checks; do not add transforms, FFI, public streaming, CI/release, or registry work.</action>
  <verify><automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>The generated corpus has independent, manifest-backed public vectors for every recognised colour grammar and precedence/resource boundary, and the public harness proves split-IDAT equivalence plus typed non-sRGB capability/no-output behavior on all four targets.</done>
</task>

<task type="auto">
  <name>Task 2: Make portable PNG proof and preservation limits public</name>
  <files>scripts/quality/Invoke-MoonQuality.ps1, modules/mb-image/README.mbt.md</files>
  <action>Update the isolated `Png` quality lane per D-03 so its trace contains a named colour-conformance stage after generated decode freshness and before/alongside the existing four-target public PNG test stage. That stage must exercise the public generated-vector harness on js, wasm, wasm-gc, and native and run the relevant literate README check on those same targets; update `Assert-PngLaneIsolation` to require the exact new trace. Do not invoke required/release lanes, change CI, or broaden package allowlists. Add a checked public PNG-colour section to `modules/mb-image/README.mbt.md` per D-05. State precisely that MNF validates, identifies, and retains supported PNG sRGB, legacy, and ICC declarations as metadata; decoded sample bytes are preserved and no colour conversion/profile transform is performed. State that only actual encoded-sRGB images are eligible for reference operations and canonical PNG encoding, while retained non-sRGB declarations receive the established typed capability outcome. Include a small compiling documentation assertion that observes metadata/eligibility through public contracts without claiming profile conversion or encoder metadata preservation.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated></verify>
  <done>The isolated PNG quality lane proves generator freshness, generated public colour vectors, package tests, and documentation across js/wasm/wasm-gc/native; the public documentation accurately describes declaration preservation and unavailable transforms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declarative fixture JSON → PowerShell generator | Fixture declarations are untrusted until the independent assembly/oracle validates grammar, ordering, CRCs, limits, and required coverage. |
| generated vectors → public decoder harness | Derived bytes and expectations must prove public behaviour without relying on parser-private helpers. |
| documentation → library consumer | The public contract must not imply an unavailable colour transformation or unsafe eligibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | `fixtures/png/decode-cases.json` and `Generate-PngDecodeVectors.ps1` | medium | mitigate | Task 1 keeps the declarative corpus as source of truth and makes `-Check` validate independent construction, required IDs, generated freshness, and manifest digest. |
| T-25-02 | Repudiation | generated public decoder evidence | medium | mitigate | Task 1 records exact public metadata/error expectations and split baselines in generated `PngDecodeVector` data executed through `PngDecoder` on every target. |
| T-25-03 | Information | `modules/mb-image/README.mbt.md` colour contract | low | mitigate | Task 2 states no sample conversion/profile transform occurs and documents the typed non-sRGB capability boundary; target-checked literate assertions keep wording tied to public APIs. |
| T-25-04 | Denial of service | iCCP compressed/inflated/allocation/work vectors | medium | mitigate | Task 1 retains hostile resource-ceiling records and verifies deterministic typed rejection before a decoded image is observable. |
</threat_model>

<verification>
1. `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
PNGCM-05 is demonstrably satisfied: generated independent vectors cover all
recognised PNG colour grammars, precedence and bounded ICC failures; legal and
hostile split-IDAT schedules preserve their public outcomes; the isolated
quality lane proves the same result across all four portable targets; and public
documentation distinguishes declaration preservation from unavailable colour
transformation.
</success_criteria>

<output>
Create `.planning/phases/25-portable-colour-conformance-evidence/25-01-SUMMARY.md` when done.
</output>
