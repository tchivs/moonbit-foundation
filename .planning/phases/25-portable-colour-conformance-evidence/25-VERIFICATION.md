---
phase: 25-portable-colour-conformance-evidence
verified: 2026-07-21T03:17:43Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 25: Portable Colour Conformance Evidence Verification Report

**Phase Goal:** Maintainers can independently verify colour-metadata behaviour and bounded failures across all portable targets.
**Verified:** 2026-07-21T03:17:43Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Generated fixtures cover precedence, every recognised chunk grammar, profile resource ceilings, and split-IDAT equivalence. | ✓ VERIFIED | `decode-cases.json` has 42 colour cases spanning `sRGB`, `gAMA`, `cHRM`, and `iCCP`, including ordering/duplicate/length failures, compatible sRGB+legacy and authoritative iCCP precedence, and compressed/inflated/allocation/work ICC ceilings. The generator requires all 42 case IDs plus five colour comparison groups, independently assembles/CRC-checks bytes, and generated 3,850 fresh records. Each required group produces one unsplit baseline plus 14 split records. |
| 2 | Legal declarations retain identical public metadata and pixels across split-IDAT schedules, while hostile schedules preserve the same typed error and no image. | ✓ VERIFIED | The public `PngDecoder` harness compares split vectors with their generated baseline: dimensions, channels, transfer/profile identity, opaque metadata bytes, reference eligibility, and every pixel for accepted records; category, code, and context for rejected records. The all-target package test passed 40/40 on wasm, wasm-gc, js, and native. |
| 3 | The isolated PNG quality lane proves the same outcomes on js, wasm, wasm-gc, and native. | ✓ VERIFIED | `Invoke-PngQualityLane` runs generator freshness, then its named `PNG colour conformance public evidence` stage loops through js/wasm/wasm-gc/native for both public `png` tests and the README check, before the existing all-target package test. A live lane run passed its exact isolation trace and all stages. |
| 4 | Public documentation distinguishes declaration preservation from unimplemented colour transforms and the resulting capability boundary. | ✓ VERIFIED | `README.mbt.md` states supported declarations are validated/identified/retained as metadata, sample bytes are not converted or profile-transformed, retained non-sRGB is ineligible for reference operations/canonical encoding, and canonical encoding does not claim declaration preservation. Its compiling example observes public metadata, eligibility, and the original sample byte; the quality lane checks it on all four targets. |

**Score:** 4/4 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `fixtures/png/decode-cases.json` | Independent positive, hostile, precedence, resource, and split corpus. | ✓ VERIFIED | Substantive declarative corpus with `colour_chunks` on 42 cases and five colour comparison groups. |
| `scripts/fixtures/Generate-PngDecodeVectors.ps1` | Independent assembly/oracle, completeness, and freshness proof. | ✓ VERIFIED | Accepts only the four recognised chunk kinds; validates assembly/CRC/placement and required IDs/groups; checks corpus SHA-256 manifest record and generated MoonBit freshness. |
| `modules/mb-image/png/generated_decode_vectors_test.mbt` | Generated public decoder records. | ✓ VERIFIED | Freshly regenerated by the checked generator; contains `PngDecodeVector` entries including baseline/split comparison fields. |
| `modules/mb-image/png/png_test.mbt` | Public decoder, metadata, error, no-output, and equivalence assertions. | ✓ VERIFIED | Drives every generated record through public `PngDecoder`; asserts descriptor/metadata/pixels or typed failure and image-invisibility boundary. |
| `scripts/quality/Invoke-MoonQuality.ps1` | Isolated all-target PNG proof trace. | ✓ VERIFIED | Wires the explicit colour-evidence stage and asserts the exact stage sequence in `Assert-PngLaneIsolation`. |
| `modules/mb-image/README.mbt.md` | Checked public preservation-boundary contract. | ✓ VERIFIED | Contains a `mbt check` example using public decoder/descriptor APIs; passed through the live quality lane. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `fixtures/png/decode-cases.json` | `generated_decode_vectors_test.mbt` | Independent PowerShell assembly, oracle, manifest digest, and freshness comparison. | ✓ WIRED | `-Check` passed, proving the generated artifact exactly matches the declarative corpus and manifest digest. |
| `generated_decode_vectors_test.mbt` | `png_test.mbt` | `_generated_png_decode_cases()` → `png_test_decode_vector()` → public `PngDecoder`. | ✓ WIRED | The live all-target test passed; split comparisons use generated baseline records rather than parser-private helpers. |
| `Invoke-MoonQuality.ps1` | public PNG tests and README | `PNG colour conformance public evidence` loops over all four targets. | ✓ WIRED | Live lane output showed the named stage, four successful target test runs, the all-target test stage, and isolation-proof success. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Generated decoder corpus | `PngDecodeVector` bytes, metadata/error expectations, `comparison_group`, `baseline` | JSON declarations → generator assembly/oracle → generated MoonBit records → public decoder test. | Yes — generator derives PNG chunks, CRCs, split positions, expectations, and source digest from the corpus; no static empty test response. | ✓ FLOWING |
| Split equivalence harness | decoded descriptor/image or typed `CoreError` | Public `PngDecoder` result for each generated vector. | Yes — the harness reads actual decoder metadata/pixels or error category/code/context and compares it to its unsplit baseline. | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Independent grammar/completeness/oracle/manifest/generated-source check | `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check` | `PNG decode vector generation/check passed (3850 executable cases).` | ✓ PASS |
| Public decoder metadata, typed rejection/no-image, capability/no-output, and split equivalence on every portable target | `moon -C modules/mb-image test png --target all --frozen` | 40/40 passed on each of wasm, wasm-gc, js, and native. | ✓ PASS |
| Isolated generator, per-target colour/README evidence, package tests, and stage-isolation trace | `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png` | Exit 0; named colour stage, all-target package stage, and `PNG lane isolation proof passed.` | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — this phase declares no probe and no conventional `scripts/**/tests/probe-*.sh` file exists.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| PNGCM-05 | `25-01-PLAN.md` | Recognised-colour positives, ordering/precedence failures, and bounded profile-expansion failures run on js, wasm, wasm-gc, and native. | ✓ SATISFIED | The generator's required-case gate includes all recognised grammar, precedence, and ICC resource-limit cases; `png --target all` and the isolated lane passed on all four targets. |

No Phase 25 requirement is orphaned from its plans. No later milestone phase exists to which a failed Phase 25 requirement could be deferred.

### Anti-Patterns Found

No `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder implementation, empty generated result, or hardcoded empty public data path was found in the seven Phase 25 files. `git diff --check` over the Phase 25 implementation range reported no whitespace errors. The quality lane reported 29 pre-existing compiler warnings and zero errors; none was introduced in the Phase 25 files.

### Disconfirmation Pass

I tested the likely false-positive paths instead of relying on fixture existence: the generator rejects a stale/incomplete colour group and validates generated freshness; the public harness invokes `PngDecoder` rather than a structural helper; and the hostile split group compares the actual typed failure with its unsplit baseline. The all-target lane also executes the literate documentation assertion, so the public no-transform wording is not documentation-only. No partial requirement, misleading passing test, or uncovered Phase 25 error path was found.

---

_Verified: 2026-07-21T03:17:43Z_
_Verifier: the agent (gsd-verifier)_
