---
phase: 29-pausable-png-encode-substrate
plan: "02"
type: tdd
wave: 2
depends_on: [29-01]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
requirements: [PNGE-01]
must_haves:
  truths:
    - "The unchanged public PngEncoder drives the private machine and preserves existing RGB8/straight-RGBA8 canonical bytes and typed preflight results, while a Writer failure returns the Writer's original typed error without claiming its failed byte as completed."
    - "The eager facade never writes before private construction succeeds, uses one fixed byte of transient output staging, calls the existing complete-write operation once per canonical byte, and acknowledges private progress only after that call succeeds."
    - "The new private source and test inventory is policy-registered, the generated public interface remains unchanged, and all PNG tests pass on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: Eager PngEncoder adapter that drains the private machine through a fixed one-byte staging slot and Writer complete-write calls while preserving byte order and typed Writer failures.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Black-box eager parity, preflight/no-output, Writer-progress, and bounded-adapter regression coverage.
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Canonical framing/length/checksum regression coverage through the public facade.
    - path: policy/foundation.json
      provides: PNG production-source inventory containing the private encode machine while retaining the same public semantic interface.
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: PngEncoder constructs the sole private canonical emitter, presents one byte into fixed staging, completes one Writer operation, then acknowledges that byte; a Writer error bypasses acknowledgement.
      pattern: PngEncodeMachine::new|@io.write_all|PngEncoder
    - from: policy/foundation.json
      to: modules/mb-image/png/pkg.generated.mbti
      via: Exact semantic-interface verification confirms Phase 29 adds only private implementation artifacts.
      pattern: PngEncoder|PngChunkDecoder
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: modules/mb-image/png | policy/foundation.json
      via: The isolated PNG lane checks exact source inventory, package policy, generated vectors, and four-target tests.
      pattern: stream_encode.mbt|PNG package contents
---

<objective>
Route the existing eager PNG encoder through the Phase 29 private machine, lock parity and atomicity with public tests, and register the new private artifacts in the repository quality gates.

Purpose: Prevent two PNG output implementations from drifting before Phase 30 wraps the same private machine in a public caller-buffered API.
Output: Eager facade parity, one-byte Writer drainage with acknowledge-after-success behavior, complete policy inventory, and four-target PNG qualification.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-pausable-png-encode-substrate/29-RESEARCH.md
@.planning/phases/29-pausable-png-encode-substrate/29-01-SUMMARY.md
@.planning/milestones/v0.8-phases/26-pausable-png-decode-substrate/26-01-SUMMARY.md
@.planning/milestones/v0.8-phases/27-public-png-chunk-decoder/27-03-VERIFICATION.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/qoi/stream_encode.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add black-box eager-machine parity and atomic Writer-adapter tests</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - The public `PngEncoder` produces the frozen RGB and straight-RGBA canonical bytes and `EncodeResult.bytes_written()` for the one-pixel oracles, a multi-row case, and a payload crossing a stored-DEFLATE block boundary.
    - Draining the private machine under byte and bounded-fragment schedules yields byte-for-byte equal public eager output and the exact machine total for every accepted case.
    - Current capability, dimension, output-limit, work-limit, and budget rejections leave a MemoryWriter at position zero and match the private constructor's CoreError shape and independent budget remainders.
    - A scripted Writer fails deterministically when asked to write canonical byte offset 43, after accepting exactly the preceding 43 bytes; the facade returns that Writer's original typed CoreError field-for-field, its accepted output is exactly the 43-byte canonical prefix, and neither the machine nor the error claims byte 43 as completed.
    - The public facade's temporary write owner has exactly one-byte capacity, calls the existing complete-write operation once per canonical byte, and no test requires or obtains a public stream encoder.
  </behavior>
  <action>Write RED-first additions to `encode_test.mbt` and `encode_wbtest.mbt`, using the present image/budget/limits helpers and the Phase 29 private presentation/acknowledgement helper. Compare full eager bytes against the machine for RGB8 and straight-RGBA8, including a generated-in-test tightly packed image whose scanlines cross 65,535 stored bytes; assert public framing, CRC, Adler, byte count, and deterministic repeat output remain frozen. Expand rejection coverage with paired independent budgets and writers so capability/dimension/output/work/budget errors compare full CoreError fields, preserve zero Writer position, and prove no budget work charge is retained on failed construction. Per D-01, add a scripted Writer that records every accepted byte and returns a distinctive typed CoreError on its attempt to write canonical byte offset 43. Assert a field-for-field match with that original error, exactly 43 successful complete-write calls, the canonical 43-byte prefix, and private emitted/completed state still at 43 rather than 44. Add white-box assertions that the eager adapter owns exactly one staging byte, calls `@io.write_all` once for each byte, and performs the private acknowledgement only after success. These tests establish byte order and prevent unobservable partial-write semantics: each complete-write call has one byte, so success commits that byte and failure leaves the same byte presented. Do not change public declarations, add stream-pull types, alter Reader semantics, modify generated fixtures, or add a Phase 30 schedule API; commit the RED tests before adapter or policy changes.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG encoder*'</automated>
  </verify>
  <done>The public eager regression suite makes shared-machine parity, preflight atomicity, Writer error accounting, canonical byte stability, and bounded adapter ownership executable before production changes are accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drain the private machine through PngEncoder and register private-source policy</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - All Task 1 tests pass; `PngEncoder::encode` obtains a private machine before touching Writer, presents each canonical byte through exactly one byte of transient owned staging, completes that Writer call, then acknowledges the byte.
    - Successful eager output preserves canonical byte counts; a Writer failure returns the unmodified typed error from its failed complete-write call, with the machine's emitted/completed count retaining only prior successes, while all preflight failures have zero bytes written and no partial work charge.
    - The PNG policy production order and exact directory/package allowlists include `stream_encode.mbt` and its two tests; generated MBTI remains exactly the Phase 28 public semantic interface.
    - Direct PNG policy/negative fixtures, generated vectors, complete PNG test suite, and the PNG quality lane pass on all four portable targets.
  </behavior>
  <action>Replace the eager whole-buffer assembly path in `encode.mbt` with an adapter that first calls the private `PngEncodeMachine::new` from Plan 29-01, then, per D-01, loops over a fixed one-byte owned staging slot: present the current private byte, place it in staging, invoke `@io.write_all` for that one-byte view, and acknowledge the same private byte only on success. Return the typed CoreError from a failed Writer call unchanged; do not transform it into a PNG-specific error, add a failed byte to any completed count, or advance the machine. One-byte complete writes make a successful call an observable all-or-nothing acceptance of exactly the presented byte, preserving canonical order and avoiding unobservable partial-write semantics. Preserve the eager public method signature, `EncodeResult` disposition, and deterministic source-reading behavior. The adapter must not recreate scanline/zlib/PNG arrays, retain Writer state, or allocate proportional to total output. Make any machine adjustment in `stream_encode.mbt` only when Task 1 proves the adapter needs the explicit private presentation/acknowledgement interface; it remains private and lease-free.

Update the exact PNG production-source sequence and directory inventories in `policy/foundation.json`, `Assert-Policy.ps1`, and `Invoke-MoonQuality.ps1` to register `stream_encode.mbt`, `stream_encode_test.mbt`, and `stream_encode_wbtest.mbt` in the same order/style as the existing private stream-decode artifacts. Preserve the exact public semantic-interface list (`PngChunkDecoder`, its established result types, `PngDecoder`, and `PngEncoder`) and update scoped negative fixtures so an extra prospective public PNG stream encoder remains rejected. Do not modify `png.mbt`, add public API, change supported targets/imports, add FFI/packages/release automation, introduce compression optimization/APNG/colour transformation, or alter Phase 30/31 consumer workflows. Run the full qualification commands after GREEN and retain all Task 1 regression tests.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check; pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The public eager encoder and future Phase 30 wrapper share one private canonical source, eager preflight remains atomic and byte-equivalent, policy recognizes every new private artifact, and no public interface change occurs in Phase 29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView/preflight → PngEncoder Writer adapter | Rejected images and resources must fail before the eager facade touches a host-provided Writer. |
| Private PngEncodeMachine → transient Writer fragment | Canonical private bytes are copied to temporary owned storage and handed to a potentially failing Writer. |
| Source/policy inventory → generated public interface | Private implementation changes must not silently publish a new PNG surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering | eager/private canonical parity | high | mitigate | Public RGB/RGBA and multi-block parity tests compare every emitted byte, framing checksum, and total with the machine's canonical sequence. |
| T-29-06 | Denial of service | eager Writer staging | high | mitigate | Drain exactly one fixed staging byte only after exact preflight; tests cross stored-block boundaries and prove no output-sized assembly. |
| T-29-07 | Tampering | Writer failure accounting | medium | mitigate | At deterministic byte offset 43, a scripted Writer returns its original typed error; the accepted prefix, one-byte call count, and private completed count prove that the failed byte was not acknowledged. |
| T-29-08 | Elevation of privilege | package policy/public API | medium | mitigate | Exact policy, generated-interface, and negative-fixture checks retain the current public PNG surface while registering only private source files. |
</threat_model>

<verification>
- `moon -C modules/mb-image test png --target native --frozen -f '*PNG encoder*'`
- `pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`
- `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `moon -C modules/mb-image test png --target all --frozen`
- `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- `PngEncoder` is an unchanged eager facade over the one private Phase 29 machine, with exact canonical bytes, typed preflight errors, and result/disposition behavior preserved; Writer failures pass through unchanged and never claim a failed byte.
- The facade writes one staged byte per complete-write call, acknowledges private progress only after success, and the private machine retains no caller output destination or full-output staging.
- Exact package policy and four-target PNG qualification acknowledge the private sources while the generated public interface remains unchanged.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Private resumable state prepares canonical output after eager-equivalent admission | 29-02 | COVERED | Tasks 1-2 route the existing facade through the single prepared machine and lock its observable parity. |
| REQ | PNGE-01 | All source/limit/budget rejection happens before any encoded byte is exposed | 29-02 | COVERED | Task 1 tests zero Writer output and paired error/budget results; Task 2 constructs before Writer access. |
| RESEARCH | — | Eager facade must share—not duplicate—the canonical emitter | 29-02 | COVERED | Task 2 removes the independent whole-buffer route and drains PngEncodeMachine. |
| RESEARCH | — | Preserve byte-stable stored-DEFLATE framing, CRC, Adler, and Writer failure semantics | 29-02 | COVERED | Task 1 asserts RGB/RGBA/multi-block parity plus the byte-43 Writer failure prefix/error/count; Task 2 uses one-byte complete-write acknowledgement with typed-error passthrough. |
| CONTEXT | D-01 | Eager adapter uses fixed one-byte staging, completes each byte before acknowledgement, and returns the original typed Writer error without counting the failed byte | 29-01, 29-02 | COVERED | Plan 29-01 supplies present/acknowledge machine state; Plan 29-02 locks the one-byte Writer loop and deterministic boundary regression. |
| RESEARCH | — | Private source/test inventory and unchanged public interface must pass exact policy gates | 29-02 | COVERED | Task 2 updates all three exact inventories and verifies generated MBTI/negative fixtures. |
| RESEARCH | — | No FFI, packages, public API, release work, optimization, APNG, or colour transform | 29-02 | COVERED | Task actions preserve the package imports/interface and confine work to private encoding and policy. |

<output>
Create `.planning/phases/29-pausable-png-encode-substrate/29-02-SUMMARY.md` when done.
</output>
