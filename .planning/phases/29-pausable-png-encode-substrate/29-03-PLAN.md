---
phase: 29-pausable-png-encode-substrate
plan: "03"
type: tdd
wave: 3
depends_on: [29-02]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
  - scripts/quality/Invoke-PngEncodeEvidence.ps1
autonomous: true
gap_closure: true
requirements: [PNGE-01]
must_haves:
  truths:
    - "On a one-byte Writer failure, PngEncoder returns that Writer's CoreError with category, code, operation, requested, completed, limit, and context intact, and the private machine retains the failed byte as unacknowledged."
    - "The eager/private canonical PNG and acknowledgement-after-success regressions execute successfully as four independently invoked, target-specific runtime checks for js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: Private one-byte Writer adapter that consumes WriteOutcome directly and preserves Writer-originated CoreError values.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Black-box field-for-field Writer-error and accepted-prefix regression coverage.
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Private-emitter/eager canonical-parity evidence selected by the isolated evidence test filter.
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: Private pending-byte acknowledgement evidence selected by the isolated evidence test filter.
    - path: scripts/quality/Invoke-PngEncodeEvidence.ps1
      provides: One-target-at-a-time four-target PNG encode evidence runner with isolated target build directories.
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-core/io/traits.mbt
      via: The eager adapter handles Writer.write's WriteOutcome directly instead of using the normalizing write_all helper.
      pattern: WriteOutcome|writer.write
    - from: scripts/quality/Invoke-PngEncodeEvidence.ps1
      to: modules/mb-image/png/encode_test.mbt
      via: Each explicit portable target runs the public Writer-error, private pending-byte acknowledgement, and eager/private parity tests that share the isolated evidence filter in its own target directory.
      pattern: PNG encoder isolated four-target evidence
---

<objective>
Close Phase 29's Writer-error identity defect and replace the aggregate four-target qualification command with repeatable target-isolated runtime evidence.

Purpose: Preserve the eager facade's promised Writer diagnostics and obtain executable proof of canonical private-emitter parity without a combined invocation exceeding the verifier timeout.
Output: A private direct-Writer adapter, RED/GREEN field-preservation tests, and a single-target evidence runner invoked once for each portable target.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-pausable-png-encode-substrate/29-VERIFICATION.md
@.planning/phases/29-pausable-png-encode-substrate/29-01-SUMMARY.md
@.planning/phases/29-pausable-png-encode-substrate/29-02-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt
@modules/mb-core/io/traits.mbt
@modules/mb-core/io/exact.mbt

<interfaces>
From `modules/mb-core/io/traits.mbt`:
- `pub(open) trait Writer { fn write(Self, @bytes.ByteView) -> WriteOutcome }`
- `WriteOutcome::Progress(UInt64)` reports an accepted count; `WriteOutcome::Failed(@error.CoreError, UInt64)` carries the provider's original error and per-call completed count.

From `modules/mb-image/png/stream_encode.mbt`:
- `PngEncodeMachine::present() -> Result[Byte?, CoreError]` holds the current byte in private `pending` state.
- `PngEncodeMachine::acknowledge(Byte)` is the only transition that clears `pending` and increments `completed()`.

From `modules/mb-core/io/exact.mbt`:
- `@io.write_all` rebuilds a failure through `propagate_stream_error`; it is not suitable where the Writer CoreError must remain field-for-field identical.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze direct Writer-error identity and isolated evidence behavior</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - The public Writer-error test named with the exact shared phrase `PNG encoder isolated four-target evidence` returns the exact CoreError it created: Host/HostOperationFailed, operation `png-scripted-write`, requested 91, completed 17, limit 23, and context `scripted`.
    - In that same named public Writer-error test, the Writer has accepted exactly 43 bytes and received 44 one-byte calls at the deterministic failure, and PngEncoder does not acknowledge or report the failed byte as completed.
    - The private pending-byte acknowledgement test in `stream_encode_wbtest.mbt`, also named with the exact shared phrase `PNG encoder isolated four-target evidence`, proves the pending byte remains visible and unacknowledged until the matching `acknowledge` call succeeds.
    - A direct one-byte Writer Progress(1) is the sole path that acknowledges the private byte; a zero or oversized progress outcome remains an adapter error with no further byte accepted.
    - The canonical-parity test in `encode_wbtest.mbt`, also named with the exact shared phrase `PNG encoder isolated four-target evidence`, proves the eager facade equals the private canonical sequence.
    - Filtering for the shared phrase selects all three named tests—the public Writer-error, private pending-byte acknowledgement, and canonical-parity tests—so the same evidence set can run independently on every portable target.
  </behavior>
  <action>Create RED coverage before changing `encode.mbt`. Rename or add the public scripted Writer-failure test in `encode_test.mbt` with the exact shared phrase `PNG encoder isolated four-target evidence`; assert each exposed CoreError field equals the Writer's deliberately distinctive original value rather than helper-derived write operation values, assert exactly 43 bytes were written before failure and 44 one-byte Writer calls occurred, and assert the failed byte remains unacknowledged/not completed. In `stream_encode_wbtest.mbt`, rename or add the private pending-byte acknowledgement regression with that same exact phrase; prove `present()` continues to expose the pending byte without completion until `acknowledge` succeeds, then prove only that acknowledgement clears it and advances completion. In `encode_wbtest.mbt`, rename or add the eager/private canonical-parity regression with the same exact phrase; drain `PngEncodeMachine` through present/acknowledge and compare its complete bytes/count to the eager RGB8 and straight-RGBA8 oracle. Add focused malformed-progress coverage only for direct one-byte calls so replacing the exact helper does not permit zero or greater-than-one progress to advance the machine. The three test names must be independently identifiable yet all contain the exact shared phrase, allowing one filter to execute all three. Keep the test set private-package only: do not introduce a public encoder type, pull API, FFI, package dependency, release automation, compression change, or image/colour feature.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG encoder isolated four-target evidence*' # output shows the named public Writer-error, private pending-byte acknowledgement, and canonical-parity tests all execute</automated>
  </verify>
  <done>Three focused tests sharing the exact evidence phrase fail against the current write_all-based adapter while precisely defining original CoreError field equality, exactly 43 written bytes, failed-byte unacknowledged ownership, valid direct-write progress, and target-selectable private/eager canonical parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use the supported direct Writer API and run one portable target per evidence invocation</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt, scripts/quality/Invoke-PngEncodeEvidence.ps1</files>
  <behavior>
    - The RED tests pass when PngEncoder directly handles Writer.write on its existing one-byte OwnedBytes staging view; a Failed outcome returns its embedded CoreError unchanged and does not call PngEncodeMachine::acknowledge.
    - A Progress(1) outcome acknowledges exactly the presented byte; other count-only outcomes are rejected by the adapter before another byte can become visible.
    - `Invoke-PngEncodeEvidence.ps1 -Target <js|wasm|wasm-gc|native>` runs the shared evidence filter for exactly one target, executes the named public Writer-error, private pending-byte acknowledgement, and canonical-parity tests under that filter, uses a target-specific build directory, returns that process's exit status, and prints the selected target and command result.
    - Four separately invoked runner commands pass, each executing all three named shared-filter tests, giving runtime evidence for js, wasm, wasm-gc, and native without using `--target all` or a combined multi-target shell command.
  </behavior>
  <action>Replace only the one-byte write_all call in `PngEncoder::encode` with a private direct-write adapter over `Writer.write`. Reuse the existing one-byte OwnedBytes staging and the current machine present/acknowledge order. Match `WriteOutcome::Progress(1UL)` as success and call `machine.acknowledge(byte)` only then. On `WriteOutcome::Failed(error, call_completed)`, return that embedded CoreError unchanged and leave the pending byte unacknowledged; the existing scripted one-byte failure must use a zero completed count. Reject count-only outcomes that cannot be a complete one-byte acceptance with the package's established adapter validation error shape, and do not transform a Writer-supplied error, retain the Writer, or alter public declarations. `Writer.write` is the supported existing API required to preserve a provider error; do not change `mb-core/io/exact.mbt` or broaden its deliberately normalizing write_all contract.

Create `scripts/quality/Invoke-PngEncodeEvidence.ps1` as a parameterized one-target runner. Accept exactly one `-Target` value from `js`, `wasm`, `wasm-gc`, and `native`; reject `all` and any omitted/unsupported target before invoking Moon. Resolve the repository root from the script location, invoke `moon -C modules/mb-image test png --target $Target --target-dir <repository _build/png-encode-evidence/$Target> --frozen -f '*PNG encoder isolated four-target evidence*'`, and propagate a nonzero process result. The shared phrase is present in exactly the three intended test names: public Writer-error identity, private pending-byte acknowledgement, and eager/private canonical parity; require each runner invocation to show all three named tests execute under its one filter before it reports success. Print a concise target/result record after success. Run the script as four distinct process invocations, one target at a time, rather than wrapping four targets in a single command; this keeps each qualification unit below the prior aggregate timeout boundary. Preserve the private/eager scope: do not modify `png.mbt`, public MBTI/policy/configuration, FFI, release work, compression behavior, or performance policy.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target native</automated>
  </verify>
  <done>PngEncoder returns the scripted Writer's original typed error field-for-field on its one-byte failure, leaves the failed byte unacknowledged after exactly 43 written bytes, and four independently repeatable target-specific runs each execute the public Writer-error, private pending-byte acknowledgement, and canonical-parity tests under the shared filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private PngEncodeMachine → host-provided Writer | A host Writer reports success, invalid progress, or a typed failure while the machine holds an unacknowledged canonical byte. |
| Isolated evidence runner → Moon target runtime | The supplied target selects the compiler/runtime and its isolated build output for the regression proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-09 | Tampering | eager Writer adapter | high | mitigate | Consume the one-byte WriteOutcome directly; only Progress(1) acknowledges, while Failed returns the embedded CoreError intact and retains the pending byte. |
| T-29-10 | Denial of service | four-target qualification | medium | mitigate | The runner accepts one portable target per process, uses a target-specific target directory and narrow evidence filter, and prohibits aggregate target selection. |
| T-29-11 | Repudiation | runtime evidence | medium | mitigate | The runner prints the selected target and propagates Moon's exit status; four explicit commands make each target result independently attributable. |
</threat_model>

<verification>
Run these as four separate process invocations; do not combine them into one shell command or use aggregate target selection:

- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target js`
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target wasm`
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target wasm-gc`
- `pwsh -NoProfile -File scripts/quality/Invoke-PngEncodeEvidence.ps1 -Target native`

Each command must exit zero, print its target/result record, and execute all three tests whose names contain the exact shared phrase: the public Writer-error identity test, the private pending-byte acknowledgement test, and the eager/private canonical-parity test. The public test must prove every original CoreError field is equal, exactly 43 bytes were written before failure, and the failed byte remains unacknowledged.
</verification>

<success_criteria>
- The shared-filtered public scripted one-byte Writer-failure test exposes exactly its source CoreError fields, proves exactly 43 bytes were written before failure, and proves the failed byte remains unacknowledged.
- PngEncoder advances private output only after a direct Writer progress result that accepts the one staged byte; a Writer failure never invokes acknowledgement.
- The shared-filtered private pending-byte acknowledgement and eager/private canonical-parity tests both execute alongside the public Writer-error test in every isolated js, wasm, wasm-gc, and native evidence command.
- The closure changes no public PNG API, public semantic interface, FFI, release/configuration surface, compression behavior, or performance policy.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Private resumable state remains reachable only through eager-equivalent admission | 29-01, 29-02, 29-03 | COVERED | Existing preflight remains intact; this closure proves the eager adapter's failure boundary without adding a public encoder. |
| REQ | PNGE-01 | No encoded byte is acknowledged after a failed Writer operation, with compatibility admission already atomic | 29-03 | COVERED | Tasks 1-2 lock direct one-byte acknowledgement and original Writer-error evidence. |
| RESEARCH | — | The eager facade shares the private canonical emitter and preserves its Writer failure semantics | 29-03 | COVERED | Task 2 uses Writer.write directly because write_all reconstructs errors. |
| RESEARCH | — | Canonical byte and acknowledgement behavior has portable runtime evidence | 29-03 | COVERED | The target-isolated runner executes the shared evidence set once per target. |
| CONTEXT | — | No Phase 29 CONTEXT.md decisions exist | 29-03 | COVERED | Parent scope is preserved: private/eager only, without public API, FFI, release, optimisation, or configuration work. |

<output>
Create `.planning/phases/29-pausable-png-encode-substrate/29-03-SUMMARY.md` when done.
</output>
