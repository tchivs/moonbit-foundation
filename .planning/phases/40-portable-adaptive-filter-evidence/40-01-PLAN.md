---
phase: 40-portable-adaptive-filter-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - scripts/quality/Invoke-PngEncodeEvidence.ps1
  - .planning/phases/40-portable-adaptive-filter-evidence/40-RESEARCH.md
autonomous: true
requirements:
  - PNGF-04
must_haves:
  truths:
    - "A bounded, documented candidate probe selects only RGB8 and straight-RGBA8 sources that show a FixedOrStored Adaptive strict size win over FixedOrStored None on every portable target before the final named evidence cases are codified."
    - "The named generated RGB8 and straight-RGBA8 public eager cases encode with FixedOrStored plus Adaptive to strictly fewer bytes than the same source with FixedOrStored plus None."
    - "For both generated sources, a public PngChunkEncoder driven through zero, one-byte, and ragged output capacities produces exactly the eager Adaptive bytes and preserves hostile-pull terminal behavior."
    - "Each named evidence case runs independently on js, wasm, wasm-gc, and native from a fresh temporary target root, and PngDecoder restores the exact source descriptor and components."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Bounded candidate-selection proof and named generated RGB8 and straight-RGBA8 eager strict-win plus public-decoder evidence."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Named generated hostile-capacity chunk/eager parity plus public-decoder evidence."
    - path: scripts/quality/Invoke-PngEncodeEvidence.ps1
      provides: "Fail-closed R1-R3/A1-A3 candidate selection and cleaned, selector-by-selector portable execution of final Phase 40 evidence cases."
    - path: .planning/phases/40-portable-adaptive-filter-evidence/40-RESEARCH.md
      provides: "Recorded four-target fixture-selection matrix and resolved-or-blocked Research A1 outcome."
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "PngEncoder::new_with_strategies(FixedOrStored, None/Adaptive) and PngDecoder public interfaces"
      pattern: "PngEncoder::new_with_strategies"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "PngChunkEncoder::new_with_strategies(FixedOrStored, Adaptive) drained under the fixed hostile schedule"
      pattern: "PngChunkEncoder::new_with_strategies"
    - from: scripts/quality/Invoke-PngEncodeEvidence.ps1
      to: modules/mb-image/png/encode_test.mbt
      via: "Candidate-selection and final-evidence modes invoke exact Moon selectors per portable target"
      pattern: "PNG adaptive filter evidence"
---

<objective>
Deliver PNGF-04 as reproducible public-API evidence: first prove which bounded generated RGB8 and straight-RGBA8 candidates win on every target, then codify only those selected sources to prove the intended Adaptive size win, hostile caller-buffer eager/chunk byte identity, and complete public decoding.

Purpose: Close the v0.12 evidence boundary without changing the Phase 39 filtering, compression, resource, factory, or legacy compatibility behavior.
Output: Four named public MoonBit tests and a focused PowerShell evidence runner that isolates and removes its temporary target root.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-portable-adaptive-filter-evidence/40-CONTEXT.md
@.planning/phases/40-portable-adaptive-filter-evidence/40-RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-VERIFICATION.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/png_test.mbt
@scripts/quality/Invoke-PngEncodeEvidence.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add named public eager RGB8/RGBA8 strict-win and decode evidence</name>
  <files>modules/mb-image/png/encode_test.mbt, scripts/quality/Invoke-PngEncodeEvidence.ps1, .planning/phases/40-portable-adaptive-filter-evidence/40-RESEARCH.md</files>
  <behavior>
    - A finite public-API probe measures three documented RGB8 candidates and three documented straight-RGBA8 candidates against the same FixedOrStored None baseline on js, wasm, wasm-gc, and native before final evidence sources are selected.
    - The RGB8 candidate set is: R1 32x1 with pixel x = (x, x+64, x+128); R2 64x1 with the same horizontal formula; R3 32x4 with pixel (x,y) = (x+y, x+y+64, x+y+128).
    - The straight-RGBA8 candidate set is: A1 16x8 with pixel (x,y) = (3x+y, 3x+y+64, 3x+y+128, 200); A2 32x4 with the same formula; A3 16x16 with pixel (x,y) = (x+y, x+y+64, x+y+128, 200).
    - Only one all-target strict winner from each profile is retained as the final named eager/chunk corpus; if no RGB8 or no straight-RGBA8 candidate wins on all four targets, evidence creation stops as blocked and Research A1 is updated with the observed matrix rather than claiming PNGF-04.
    - Each Adaptive eager byte sequence is decoded through PngDecoder with complete-input required; restored width, height, channel count, and every component match its generated source.
  </behavior>
  <action>Before locking any final corpus builder or selector, add six public candidate selectors in `encode_test.mbt`, one per R1-R3/A1-A3 candidate. Each selector must use only `PngEncoder::new_with_strategies(PngCompressionStrategy::FixedOrStored, ...)`, encode its specified generated source as None and Adaptive, and fail exactly when Adaptive is not strictly shorter. The finite set is RGB R1 32x1 `(x, x+64, x+128)`, R2 64x1 with that formula, and R3 32x4 `(x+y, x+y+64, x+y+128)`; straight RGBA A1 16x8 `(3x+y, 3x+y+64, 3x+y+128, 200)`, A2 32x4 with that formula, and A3 16x16 `(x+y, x+y+64, x+y+128, 200)`. Use these exact selector strings in the declared ordered `(id, selector)` array: `(R1, 'PNG adaptive filter evidence candidate RGB8 R1 strict win')`, `(R2, 'PNG adaptive filter evidence candidate RGB8 R2 strict win')`, `(R3, 'PNG adaptive filter evidence candidate RGB8 R3 strict win')`, `(A1, 'PNG adaptive filter evidence candidate straight-RGBA8 A1 strict win')`, `(A2, 'PNG adaptive filter evidence candidate straight-RGBA8 A2 strict win')`, `(A3, 'PNG adaptive filter evidence candidate straight-RGBA8 A3 strict win')`. Replace the current required single-target script parameter with a required `Mode` ValidateSet containing `CandidateSelection` and `FinalEvidence`; both modes own the explicit fixed target array `js`, `wasm`, `wasm-gc`, `native`. In CandidateSelection mode, hold that exact six-pair array in declared R1, R2, R3, A1, A2, A3 order; loop over every target; create a GUID-named `mnf-png-adaptive-evidence-` directory under the resolved OS temporary root for that target; invoke each candidate selector independently with `moon -C modules/mb-image test png --target $Target --target-dir $targetDirectory --frozen -f $selector`; capture every exit result without prematurely stopping the candidate loop; and remove only the exact resolved, contained, prefixed root in `finally`. Build and print a complete target-by-candidate boolean matrix, then select the first all-target passing RGB id in R1-R3 order and the first all-target passing RGBA id in A1-A3 order. Exit nonzero after printing the matrix if either selection is absent; never proceed to final evidence in that condition. After a successful script run, add the full matrix, selected ids, command, date, and `A1: RESOLVED` to `40-RESEARCH.md`; on failure add the full matrix and `A1: BLOCKED`, keep the task blocked, and do not add final evidence selectors. Only after the successful record, retain the chosen final source formulas in local builders named `png_adaptive_evidence_rgb8_horizontal_image` and `png_adaptive_evidence_straight_rgba8_vertical_image`; add exactly these public final selectors: `PNG adaptive filter evidence RGB8 horizontal eager strictly beats None and decodes completely` and `PNG adaptive filter evidence straight-RGBA8 vertical eager strictly beats None and decodes completely`. Per D-01 and D-03, keep sources generated in MoonBit and diagnostic; do not add binary fixtures, dependencies, benchmark loops, or absolute output-size snapshots. Per D-02, construct both final sides solely with the same explicit FixedOrStored strategy, use None for the baseline and Adaptive for the candidate, and assert the strict relation. Per D-04 and D-06, pass final Adaptive bytes to `png_filter_strategy_decode_matches_source` so the public decoder verifies descriptor and every sample. Per D-07, modify no production PNG file, public factory signature, strategy policy, accounting behavior, or legacy filter-None vector.</action>
  <verify>
    <automated>&amp; .\scripts\quality\Invoke-PngEncodeEvidence.ps1 -Mode CandidateSelection; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen -f 'PNG adaptive filter evidence RGB8 horizontal eager strictly beats None and decodes completely'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen -f 'PNG adaptive filter evidence straight-RGBA8 vertical eager strictly beats None and decodes completely'; exit $LASTEXITCODE</automated>
  </verify>
  <done>The CandidateSelection runner owns all four target loops and safe cleanup, prints and records the full R1-R3/A1-A3 matrix, deterministically records first all-target winners, and marks Research A1 resolved. Only those winners appear in the final eager selectors, which prove the same-strategy Adaptive strict win and completely round-trip through public PngDecoder. Missing either winner records A1 blocked and returns a nonzero runner result rather than a passing evidence claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hostile chunk parity cases and clean four-target selector runner</name>
  <files>modules/mb-image/png/stream_encode_test.mbt, scripts/quality/Invoke-PngEncodeEvidence.ps1</files>
  <behavior>
    - For the generated RGB8 and straight-RGBA8 formulas from Task 1, a chunk encoder configured as FixedOrStored plus Adaptive returns exactly the corresponding eager Adaptive bytes under [0, 8, 4, 1, 13, 2, 5, 3, 21].
    - The hostile drain preserves zero-capacity NeedOutput, exact per-pull/cumulative progress, and repeated Finished pulls without mutating their caller lease.
    - Both chunk outputs decode through PngDecoder to the exact selected generated source, and the PowerShell runner executes the already-resolved final eager/chunk selectors separately for each portable target from a fresh root that is removed in finally.
  </behavior>
  <action>Use only the two Task-1 all-target winners: in `stream_encode_test.mbt`, add local generated builders named `png_stream_adaptive_evidence_rgb8_horizontal_image` and `png_stream_adaptive_evidence_straight_rgba8_vertical_image` that duplicate the final retained formulas, not the candidate fallback set, so the public test file has no visibility dependency. Generalize the existing hostile-drain helper into `png_chunk_test_drain_hostile_with_strategies(image, strategy, filter_strategy, schedule)`: obtain the eager expectation through `png_stream_test_eager_with_strategies`, create the public `PngChunkEncoder::new_with_strategies`, and retain the existing zero-capacity, progress, exact-prefix, sticky-Finished, and lease-unchanged checks. Make the existing default hostile helper delegate to it so its legacy route coverage remains intact. Add exactly these selectors: `PNG adaptive filter evidence RGB8 horizontal chunk exactly matches eager and decodes completely` and `PNG adaptive filter evidence straight-RGBA8 vertical chunk exactly matches eager and decodes completely`. Per D-04 and D-05, each selector must use FixedOrStored plus Adaptive, the fixed `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]` schedule, byte-for-byte eager parity, and `png_stream_test_fixed_or_stored_corpus_decode_matches_source` for complete public decode equality. Complete `FinalEvidence` mode in `Invoke-PngEncodeEvidence.ps1` with the four final selector strings and the same explicit js/wasm/wasm-gc/native loop, per-target GUID-root creation, resolved-containment/prefix guard, and finally cleanup used by CandidateSelection. Invoke each selector separately, stop on the first final-evidence failure, and emit the target result only after all four pass. FinalEvidence is permitted only after CandidateSelection has succeeded and Research A1 is recorded resolved. Per D-06, this supplies isolated final evidence on all portable targets. Per D-07, keep this evidence-only: do not alter encoder implementation, compression selection, budgets, public API signatures, or frozen None bytes.</action>
  <verify>
    <automated>&amp; .\scripts\quality\Invoke-PngEncodeEvidence.ps1 -Mode FinalEvidence; exit $LASTEXITCODE</automated>
  </verify>
  <done>Both exact chunk selectors prove the required hostile-schedule Adaptive chunk/eager byte identity and public decode recovery; the focused script runs every eager and chunk evidence selector independently on js, wasm, wasm-gc, and native with a verified temporary-root cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated test source → public PNG encoders/decoder | Test-controlled pixels cross only existing public codec factory and decode contracts. |
| PowerShell runner → temporary filesystem | A script-owned target directory is created and recursively removed after target-specific evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | `Invoke-PngEncodeEvidence.ps1` target-root cleanup | high | mitigate | Create a GUID-prefixed child only beneath the resolved OS temp root; before recursive cleanup, resolve and verify containment plus the owned leaf prefix; always perform cleanup in `finally`. |
| T-40-02 | Tampering | portable evidence execution | medium | mitigate | Use a fresh target root per target invocation and run each exact selector in a separate Moon test process so cached output cannot certify a different evidence case. |
| T-40-03 | Repudiation | strict-win claim | low | mitigate | Assert only generated case-specific `Adaptive < None` relations under the same explicit FixedOrStored strategy, then run the exact named cases on all four targets. |
</threat_model>

<verification>
Run `& .\scripts\quality\Invoke-PngEncodeEvidence.ps1 -Mode FinalEvidence; exit $LASTEXITCODE` once; the script owns its exact selector set, all four targets, and temporary-root cleanup. Do not substitute an aggregate package suite or an absolute PNG-byte snapshot for these contracts.
</verification>

<success_criteria>
1. The two generated eager cases prove Adaptive is strictly smaller than None under the same explicit FixedOrStored route and public-decode exactly to source.
2. The two matching chunk cases preserve hostile caller-buffer semantics, emit exact eager Adaptive bytes, and public-decode exactly to source.
3. The focused runner executes all four selectors independently on all four targets with safe, deterministic temporary-root cleanup and no production PNG change.
</success_criteria>

<output>
Create `.planning/phases/40-portable-adaptive-filter-evidence/40-01-SUMMARY.md` when done.
</output>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Reproducible intended Adaptive wins, chunk interoperability, and portable public decode | 40-01 | COVERED |
| REQ | PNGF-04 | Generated RGB8/RGBA8 strict win, hostile eager/chunk identity, and four-target complete decode | 40-01 | COVERED |
| RESEARCH | — | Same explicit FixedOrStored comparison; generated predictor-friendly corpus; no absolute size claims | 40-01 Task 1 | COVERED |
| RESEARCH | A1 | Finite R1-R3/A1-A3 four-target matrix resolves candidate stability before final selectors are written | 40-01 Task 1 | COVERED |
| RESEARCH | — | Fixed zero/one/ragged hostile drain and public source-equality decoder oracle | 40-01 Task 2 | COVERED |
| RESEARCH | — | Selector-by-selector, temporary-root-isolated portable script with cleanup | 40-01 Task 2 | COVERED |
| CONTEXT | D-01 | Generated MoonBit RGB8/RGBA8 fixtures; no external fixture or dependency | 40-01 Task 1 | COVERED |
| CONTEXT | D-02 | Strict Adaptive win against None under the same explicit strategy | 40-01 Task 1 | COVERED |
| CONTEXT | D-03 | Small diagnostic horizontal/vertical patterns and relation assertions | 40-01 Task 1 | COVERED |
| CONTEXT | D-04 | Public eager/chunk factories and PngDecoder only | 40-01 Tasks 1-2 | COVERED |
| CONTEXT | D-05 | Fixed hostile zero/one/ragged schedule and exact bytes | 40-01 Task 2 | COVERED |
| CONTEXT | D-06 | Independent four-target named cases, clean root, exact public decode | 40-01 Tasks 1-2 | COVERED |
| CONTEXT | D-07 | Evidence-only; no production/API/legacy compatibility alteration | 40-01 Tasks 1-2 | COVERED |
