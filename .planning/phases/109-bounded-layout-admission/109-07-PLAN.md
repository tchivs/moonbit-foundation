---
phase: 109-bounded-layout-admission
plan: "07"
type: execute
wave: 5
depends_on:
  - 109-02
  - 109-04
  - 109-05
  - 109-06
files_modified:
  - modules/mb-font/font/layout_model.mbt
  - modules/mb-font/font/layout_admission.mbt
  - modules/mb-font/font/shape_transaction.mbt
  - modules/mb-font/font/layout_admission_wbtest.mbt
  - modules/mb-font/font/layout_admission_test.mbt
  - modules/mb-font/font/shape_transaction_test.mbt
  - modules/mb-font/font/shape_transaction_wbtest.mbt
autonomous: true
requirements:
  - LAY-01
  - LAY-02
must_haves:
  truths:
    - "D-29/D-38/D-40: one opaque owned profile is request-scoped to FontShapeScope, shares active/revision identity, exposes no Phase 109 facts, and becomes unusable when the callback closes."
    - "D-31/D-32/D-33/D-34: every semantic aggregate, retained byte, actual array allocation, maximum allocation, and parser-work event is checked and charged exactly; source table bytes are not recharged."
    - "D-35: exact-fit succeeds, one-short fails, admission failure changes no caller/ancestor budget, and the existing transaction remains the sole Budget::charge owner."
    - "D-36/D-37: all named revision guards and InvalidInput -> State -> Data -> Capability -> Resource precedence, including the declared-count preflight exception, are executable."
    - "D-39: internal operation/context diagnostics are stable, table-specific semantic tokens with no offsets, indices, selected tags, paths, or host prose."
  artifacts:
    - path: "modules/mb-font/font/layout_admission.mbt"
      provides: "Request-scoped GSUB -> GPOS -> conditional GDEF admission orchestration"
      exports: ["FontShapeScope::admit_layout"]
    - path: "modules/mb-font/font/layout_model.mbt"
      provides: "Complete opaque profile, exact charge, work, and deferred-capability ledgers"
    - path: "modules/mb-font/font/shape_transaction.mbt"
      provides: "Single-use admission staging under the existing sole-commit transaction"
    - path: "modules/mb-font/font/layout_admission_wbtest.mbt"
      provides: "Exact/one-short limits, charges, precedence, lifetime, and revision-guard evidence"
    - path: "modules/mb-font/font/layout_admission_test.mbt"
      provides: "Public abstract-surface, constructor, scope, and second-admission behavior"
  key_links:
    - from: "modules/mb-font/font/layout_admission.mbt"
      to: "modules/mb-font/font/layout_gsub.mbt"
      via: "before/after GSUB revision guards and selected lookup normalization"
      pattern: "gsub"
    - from: "modules/mb-font/font/layout_admission.mbt"
      to: "modules/mb-font/font/layout_gpos.mbt"
      via: "before/after GPOS revision guards and selected lookup normalization"
      pattern: "gpos"
    - from: "modules/mb-font/font/layout_admission.mbt"
      to: "modules/mb-font/font/layout_gdef.mbt"
      via: "conditional before/after GDEF binding when ignore flags require a classifier"
      pattern: "gdef"
    - from: "modules/mb-font/font/layout_admission.mbt"
      to: "modules/mb-font/font/shape_transaction.mbt"
      via: "complete-profile guard atomically stages exact font charge and single-use state"
      pattern: "font_charge|layout_admitted"
  prohibitions:
    - { statement: "Admission never calls Budget::charge, publishes a partial profile, or mutates staged scope state before complete profile validation and its guard.", status: unverified, flagged: true }
    - { statement: "No profile constructor, cache handle, raw fact accessor, commit method, executor, probe, or mutation API becomes public.", status: unverified, flagged: true }
---

<objective>
Integrate all normalized layout facts into one request-scoped, exactly charged, mutation-guarded admission profile.

Purpose: Complete LAY-01/LAY-02 behind the Phase 108 authority boundary while preserving atomicity and one final transaction commit.
Output: FontShapeScope admission, opaque profile lifetime, exact ledgers, stage guards, and complete hostile integration evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-VERIFICATION.md
@.planning/phases/109-bounded-layout-admission/109-02-SUMMARY.md
@.planning/phases/109-bounded-layout-admission/109-04-SUMMARY.md
@.planning/phases/109-bounded-layout-admission/109-05-SUMMARY.md
@.planning/phases/109-bounded-layout-admission/109-06-SUMMARY.md
@modules/mb-font/font/shape_transaction.mbt
@modules/mb-core/budget/budget.mbt

<interfaces>
- Preserve `Font::with_shape_transaction[T]` exactly; it remains the only path that preflights and charges a combined ResourceCharge.
- Add public-abstract `FontLayoutProfile` with no Phase 109 methods and `FontShapeScope::admit_layout(script: Bytes, language: Bytes?, liga: Bool, kern: Bool, limits: FontLayoutLimits)`.
- Consume all Plan 109-04/05/06 body normalizers and Plan 109-02 limits without widening module dependencies.
- Construct `LayoutAdmissionBuilder` only from Plan 109-01's private `LayoutAdmissionLimits`; do not re-read FontLayoutLimits fields, accept loose ceiling arguments, or duplicate the default table.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrate staged admission, exact ledgers, and opaque profile lifetime</name>
  <precondition>Phase 108 `modules/mb-font/font/shape_transaction.mbt` exists with the generic callback, active scope, revision authority, and sole final Budget::charge.</precondition>
  <reversibility rating="one-way">D-39 is an already-locked public diagnostic compatibility contract; operation/context drift would break consumers and fixtures.</reversibility>
  <files>modules/mb-font/font/layout_model.mbt, modules/mb-font/font/layout_admission.mbt, modules/mb-font/font/layout_admission_wbtest.mbt, modules/mb-font/font/layout_admission_test.mbt</files>
  <behavior>
    - "Admission validates direct semantic tag inputs, active scope, and revision before parsing."
    - "Stages run in GSUB, GPOS, conditional GDEF order with before/after guards and one after-complete-profile guard."
    - "Capability is deferred in stable stage/source order; State/Data and declared-count Resource behavior follow D-37."
    - "The complete profile owns only normalized facts plus shared scope/revision authority."
    - "Exact retained bytes follow the normative coefficients; allocations count every explicit scratch/retained array; allocation_size is a maximum; work covers every locked event."
    - "Equivalent normalized profiles in differently padded source tables have identical retained-byte charges."
  </behavior>
  <action>
Complete the private profile/model and `LayoutAdmissionBuilder` per D-29/D-31 through D-34. Its constructor accepts only Plan 109-01's sentinel-proved `LayoutAdmissionLimits` record, preserving the exact 27-field mapping; it must not re-read `FontLayoutLimits`, accept loose ceiling arguments, or restate defaults. Centralize semantic aggregates, record extents, retained coefficients, exact retained/scratch array allocations, maximum logical allocation payload, and exhaustive parser-work events. Use two-pass exact-size construction and move arrays into the profile; charge any added copy explicitly. Set ResourceCharge width/height/pixels to zero and exclude font-owned source table bytes.

Implement `FontShapeScope::admit_layout` orchestration per D-36 through D-40: validate direct four-byte semantic tags without echoing them, require active/revision state, discover optional table-local GSUB/GPOS windows, run global passes, selected GSUB, selected GPOS, and conditional GDEF, preserve deterministic deferred Capability, create the opaque profile with shared active/revision identity, run the complete-profile guard, and only then return the staged result. Internal errors use `font-layout-admit` and the sealed contexts from research. Add private summaries for counts/kinds/order hashes/charges only, plus generated matrices for selected-versus-unselected richness, Capability-versus-later-Data, declared-count Resource, exact/one-short every semantic/charge dimension, padded-source equality, all named guard mutations, and profile closure. Keep all summaries/probes package-private.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-font info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-29/D-31 through D-34/D-36 through D-40 are realized by one owned, bounded, diagnostic-stable profile that cannot escape usable scope authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage admission charge atomically in the Phase 108 transaction</name>
  <files>modules/mb-font/font/shape_transaction.mbt, modules/mb-font/font/layout_admission_wbtest.mbt, modules/mb-font/font/shape_transaction_test.mbt, modules/mb-font/font/shape_transaction_wbtest.mbt</files>
  <behavior>
    - "A scope accepts exactly one successful layout admission; the second returns State/InvalidRange with context layout-already-admitted."
    - "Every failed admission leaves font_charge and layout_admitted unchanged."
    - "After the complete-profile guard, admission checked-adds its charge to mutable staged font_charge and flips layout_admitted atomically."
    - "The transaction combines staged font/text charges, preflights caller/ancestors, performs the existing final revision guard, and calls Budget::charge exactly once."
    - "Exact budget succeeds; each one-short consumable/ceiling dimension fails with caller and ancestors unchanged."
  </behavior>
  <action>
Extend only the private scope state with mutable staged `font_charge` and `layout_admitted`, sharing the existing active/revision cells per D-35/D-38. Preserve the public generic transaction signature and tail. Make a successful admission checked-add its exact ResourceCharge after the complete-profile guard; make a second successful attempt return the sealed State error; keep all failure paths side-effect-free. Do not introduce a layout-side Budget argument or charge call.

Extend black-box and white-box transaction tests for successful exact composition, consumable addition, allocation-size maximum, overflow, every one-short dimension, ancestor preflight, failed admission, second admission, escaped scope/profile closure, each admission-stage mutation, post-staging mutation, and the existing final pre-commit guard. Assert exactly one `.charge(combined)` remains in the font/text transaction path and no fallible work follows it.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $hits = Select-String -Path 'modules/mb-font/font/shape_transaction.mbt' -Pattern '\.charge\(combined\)'; if ($hits.Count -ne 1) { throw ('expected one combined charge call, found ' + $hits.Count) }"</automated>
  </verify>
  <done>D-35/D-36/D-38 preserve one atomic authority debit, exact composition, single-use admission, complete lifetime invalidation, and zero failure-side budget mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted layout tables -> opaque profile | Complete selected structures become retained request-scoped authority |
| profile/charge -> FontShapeScope | Private staged state crosses the transaction boundary |
| FontShapeScope -> caller Budget hierarchy | One combined charge becomes externally observable authority mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-24 | Tampering | retained source revision | high | mitigate | Named entry, before/after table, complete-profile, and final transaction guards |
| T-109-25 | Denial of Service | aggregate ledgers | high | mitigate | Exhaustive semantic, retained, allocation, maximum, and work preflight with exact/one-short tests |
| T-109-26 | Elevation of Privilege | profile/scope lifetime | high | mitigate | Shared active/revision identity, no public profile operations, single-use admission |
| T-109-27 | Tampering | Budget hierarchy | high | mitigate | Immutable checked composition, full hierarchy preflight, one final charge only |
| T-109-28 | Information Disclosure | public errors/profile | medium | mitigate | Sealed semantic tokens and no raw source/index/tag fields or accessors |
| T-109-29 | Repudiation | stage winner | medium | mitigate | Generated simultaneous-fault and mutation matrix records exact category/code/operation/context |
</threat_model>

<verification>
- Four-target mb-font public and white-box suites pass.
- Exact/one-short matrices cover every semantic and ResourceCharge dimension.
- Interface inspection shows only abstract limits/profile and scoped admission, with no raw/debug/commit surface.
- Admission contains no Budget::charge; the existing transaction contains exactly one combined charge call.
</verification>

<success_criteria>
- A supported selected layout request produces one opaque, scope-bound normalized profile and staged exact charge.
- Every failure is deterministic, atomic, mutation-safe, and publicly non-revealing.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-07-SUMMARY.md` when done.
</output>
