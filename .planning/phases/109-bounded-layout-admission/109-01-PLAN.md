---
phase: 109-bounded-layout-admission
plan: "01"
type: execute
wave: 2
depends_on:
  - 109-02
files_modified:
  - modules/mb-font/font/layout_model.mbt
  - modules/mb-font/font/layout_common.mbt
  - modules/mb-font/font/layout_fixture_wbtest.mbt
  - modules/mb-font/font/layout_common_wbtest.mbt
autonomous: true
requirements:
  - LAY-01
  - LAY-02
must_haves:
  truths:
    - "D-01/D-02/D-03: every GSUB/GPOS/GDEF offset resolves with checked UInt64 arithmetic from its field-specific base inside a table-local window."
    - "D-04/D-05/D-06: global arrays and immediate targets are validated for selected and unselected records, while only selected bodies are deeply decoded and valid unselected rich bodies remain neutral."
    - "D-16/D-17/D-18/D-19: Coverage and ClassDef facts are owned, ordered, bounded, cardinality-correct, and contextually class-limited."
    - "Malformed fixed envelopes are Data; valid declared-count or product ceilings preflight before traversal as Resource per D-32/D-37."
    - "All 27 FontLayoutLimits fields cross one package-private ordered adapter into the builder-limit record exactly once; unique sentinels prove no field is omitted, duplicated, swapped, or defaulted."
  artifacts:
    - path: "modules/mb-font/font/layout_common.mbt"
      provides: "Checked table-local target resolution, common layout headers, Coverage, and ClassDef normalization"
    - path: "modules/mb-font/font/layout_model.mbt"
      provides: "Private owned layout fact/ledger types and the exact 27-field builder-limit record"
    - path: "modules/mb-font/font/layout_fixture_wbtest.mbt"
      provides: "Generated labelled-base hostile fixture builders"
    - path: "modules/mb-font/font/layout_common_wbtest.mbt"
      provides: "Exact-end, one-short, overflow, aliasing, envelope, Coverage, and ClassDef evidence"
  key_links:
    - from: "modules/mb-font/font/layout_common.mbt"
      to: "modules/mb-font/font/cursor.mbt"
      via: "checked UInt64 reads and table-local ByteView windows"
      pattern: "read_u(16|32)|CheckedRange|subview"
    - from: "modules/mb-font/font/layout_common.mbt"
      to: "modules/mb-font/font/layout_model.mbt"
      via: "normalizers return owned compact Coverage/ClassDef facts and checked ledgers"
      pattern: "Layout|Coverage|ClassDef"
    - from: "modules/mb-font/font/layout_limits.mbt"
      to: "modules/mb-font/font/layout_model.mbt"
      via: "one package-private ordered adapter maps all 27 values into LayoutAdmissionLimits consumed by LayoutAdmissionBuilder"
      pattern: "admission_values|LayoutAdmissionLimits"
  prohibitions:
    - { statement: "No raw ByteView, unchecked offset, mutable source state, or public record index is retained in normalized facts.", status: unverified, flagged: true }
    - { statement: "No GSUB/GPOS execution, metric lookup, legacy kerning, cache, UI, FFI, or external integration is introduced.", status: unverified, flagged: true }
---

<objective>
Create the bounded binary and normalized-fact foundation for LAY-01/LAY-02.

Purpose: Prove the highest-risk offset-base, window, count, and common-fact invariants before selection or body-specific admission depends on them.
Output: Private owned model types, one checked common parser boundary, and generated hostile structural evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/109-bounded-layout-admission/109-02-SUMMARY.md
@modules/mb-font/font/cursor.mbt
@modules/mb-font/font/directory.mbt
@modules/mb-font/font/cff_admission.mbt

<interfaces>
- Reuse `read_u16`, `read_u32`, checked range/subview mechanics, and `@checked.checked_add` / `checked_mul`; all offset and product values remain UInt64 until semantic bounds prove narrowing safe.
- Internal layout errors use operation `font-layout-admit`; only the sealed semantic context vocabulary from 109-RESEARCH.md is allowed.
- Normalized facts are package-private owned primitive arrays/descriptors. Source `ByteView` values remain traversal-only.
- Plan 109-02 provides a package-private 27-field `FontLayoutLimitValues` contract in constructor order. This plan owns the sole conversion to a same-named `LayoutAdmissionLimits` record; later builders consume that record without re-reading `FontLayoutLimits` or restating defaults.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build labelled hostile fixtures and checked table-local targets</name>
  <reversibility rating="costly">D-02 fixes offset bases that every normalized layout fact and hostile fixture will depend on.</reversibility>
  <files>modules/mb-font/font/layout_fixture_wbtest.mbt, modules/mb-font/font/layout_common.mbt, modules/mb-font/font/layout_common_wbtest.mbt</files>
  <behavior>
    - "Every ledger field accepts a target ending exactly at the owning table boundary and rejects a target one byte short of its minimum or complete count-derived extent."
    - "Checked-add overflow, illegal zero, sibling-relative confusion, noncontiguous siblings, and legal aliasing are distinguished deterministically."
    - "GSUB/GPOS 1.0 and 1.1 null FeatureVariations headers admit; non-null malformed FeatureVariations is Data and a complete top envelope records Capability."
    - "Unselected global Script/Feature/Lookup targets are envelope-validated, but valid unselected advanced bodies are not capability-checked."
  </behavior>
  <action>
Create generated MoonBit byte builders with named bases for layout, ScriptList, selected Script, FeatureList, selected Feature, LookupList, selected Lookup, subtable, PairSet, LigatureSet, GDEF, and extension wrapper. Add relative Offset16/Offset32 patch helpers plus exact truncation, noncontiguous, aliasing, and overflow constructors; use generated bytes only per D-45.

Implement a single table-local target resolver in `layout_common.mbt` that encodes each base explicitly, handles nullable zero at the field schema, checks `base + offset`, minimum width, and complete format/count extent using UInt64, and maps failures to stable Data contexts per D-01 through D-06. Validate 1.0/1.1 common headers, the full ScriptList/FeatureList/LookupList arrays, every global record target and immediate child envelope, and the FeatureVariations top record-array envelope without following its nested records. Add exact/one-short/overflow/selected-versus-unselected tests for every ledger field.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-01 through D-06 are executable through generated labelled-base fixtures, with no table escape or unselected-body over-validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize owned Coverage and ClassDef facts</name>
  <files>modules/mb-font/font/layout_model.mbt, modules/mb-font/font/layout_common.mbt, modules/mb-font/font/layout_common_wbtest.mbt</files>
  <behavior>
    - "Coverage 1 requires strictly increasing in-range glyphs."
    - "Coverage 2 requires ordered non-overlapping ranges and exact running startCoverageIndex, retaining compact ranges plus total cardinality."
    - "ClassDef 1 proves checked startGlyph + glyphCount; ClassDef 2 requires ordered non-overlapping in-range ranges."
    - "Explicit class zero remains valid, implicit misses remain class zero, and caller-supplied class maxima reject out-of-domain values."
    - "Every count, product, record extent, allocation payload, retained coefficient, and parser-work preflight has exact-fit success and one-short Resource failure after a readable fixed header."
    - "A FontLayoutLimits value built with 27 distinct nonzero sentinels reaches the same-named LayoutAdmissionLimits slots and builder probes in exact constructor order."
  </behavior>
  <action>
Define private owned Coverage1/Coverage2 and ClassDef1/ClassDef2 descriptors in `layout_model.mbt`; retain primitive arrays/ranges and scalar cardinalities only, never source windows or raw offsets per D-29. Define the private `LayoutAdmissionLimits` record with the exact same 27 names and order as Plan 109-02's `FontLayoutLimitValues`, and implement the sole package-private conversion used by `LayoutAdmissionBuilder`. The downstream builder constructor must accept only `LayoutAdmissionLimits`; it must not receive `FontLayoutLimits`, individual loose ceilings, or locally repeated defaults. Add a unique-sentinel white-box contract that exercises every mapped slot and rejects any missing, duplicate, swapped, or fallback value without adding public accessors.

Implement common format dispatch and complete normalization per D-16 through D-19. Apply checked count, cumulative-cardinality, range, product, record-extent, retained-byte, allocation, maximum-allocation, and parser-work ledgers per D-31/D-32/D-34/D-37, but do not create the final ResourceCharge or mutate a transaction in this plan. Cover format, glyph, order, overlap, cardinality, explicit-zero, contextual-class, exact-limit, and one-short cases in white-box tests.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-font info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-16 through D-19 and the common portions of D-29/D-31/D-32/D-34/D-37 are represented by owned facts and exact hostile boundaries; the complete 27-field private adapter is sentinel-proved without public leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| font bytes -> table-local parser | Untrusted offsets, counts, products, and aliases cross into checked parsing |
| parser -> normalized facts | Traversal-only source views must become owned semantic records |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-01 | Tampering | offset-base resolver | high | mitigate | Explicit base-kind call sites, checked UInt64 addition, minimum and complete table-local envelopes, hostile sibling/alias fixtures |
| T-109-02 | Denial of Service | count/product traversal | high | mitigate | Fixed-header validation followed by semantic/product/extent/work/allocation preflight before loops |
| T-109-03 | Information Disclosure | normalized model | medium | mitigate | Owned primitive facts only; no ByteView/raw offset/index fields and generated-interface inspection |
| T-109-04 | Repudiation | error classification | medium | mitigate | Stable Data versus Resource tests and sealed `font-layout-admit` contexts |
| T-109-40 | Tampering | 27-field limit adapter | high | mitigate | One ordered private adapter plus unique-sentinel mapping and downstream builder-constructor contract |
</threat_model>

<verification>
- Fail-fast four-target mb-font tests pass.
- `moon -C modules/mb-font info --target all --frozen` contains no Phase 109 public model, fixture, window, offset, or summary accessor.
- Generated fixtures cover every D-02 base with exact-end and one-short outcomes.
- The 27-field private adapter sentinel test proves exact field identity and builder consumption.
</verification>

<success_criteria>
- The parser cannot escape GSUB/GPOS/GDEF table-local windows or narrow before proof.
- Global envelope validation and selected-body depth are distinct.
- Coverage/ClassDef facts are owned, bounded, and exact.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-01-SUMMARY.md` when done.
</output>
