---
phase: 109-bounded-layout-admission
plan: "09"
type: execute
wave: 7
depends_on:
  - 109-08
files_modified:
  - modules/mb-font/README.mbt.md
  - modules/mb-font/CHANGELOG.md
  - modules/mb-text/README.mbt.md
  - modules/mb-text/CHANGELOG.md
  - docs/rfcs/0004-mb-font.md
  - docs/rfcs/0005-mb-text.md
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
autonomous: true
requirements:
  - LAY-01
  - LAY-02
must_haves:
  truths:
    - "LAY-01/LAY-02 public interfaces, source/test inventories, module DAG, diagnostics, and prohibition boundaries are exact and fail closed."
    - "D-09/D-12/D-28 divergences are explicitly documented and fixture-bound as MNF closed-profile policy, never misrepresented as broad OpenType requirements."
    - "D-39/D-40: exact generated interfaces expose only public-abstract limits/profile and scoped admission; no raw/debug/cache/commit/execute surface leaks."
    - "D-41 through D-46: documentation and policy make no layout-execution, successful public nonempty, licensed/oracle, or semantic four-target qualification claim."
    - "Generated hostile structural tests, public contract tests, all-target interfaces, and policy gates are reproducible without packages, services, UI, FFI, or external API/schema integration."
    - "The exact 27-field private default literal/order block and the sole ShapeLimits replacement assignment are source/policy sealed without public accessors."
    - "The complete limit proof is layered without cross-package private access: Plan 109-01 owns the 27-sentinel mb-font mapping test, Plan 109-08 owns 27 public exact/one-short outcome pairs, and this plan owns the sole-assignment source gate."
    - "Four-target mb-text --deny-warn diagnostics are fail-closed: only exact policy-fingerprinted inherited CFF diagnostics may explain a nonzero exit, and every new/modified Phase 109 file must be warning-free."
    - "Required is a separate child-process observation: success is accepted, while failure is accepted only when it terminates at WORK-04 mb-core and byte-matches a freshly captured live scoped formatter baseline."
  artifacts:
    - path: "policy/foundation.json"
      provides: "Exact Phase 109 inventories/interfaces/docs, 27 default literals/order, and inherited diagnostic fingerprints"
    - path: "scripts/quality/Assert-Policy.ps1"
      provides: "Fail-closed interface/default/diagnostic/scope/no-execution gates plus exact Required observation classifier"
    - path: "modules/mb-font/README.mbt.md"
      provides: "Compile-checked bounded admission, limits, lifetime, charge, and divergence documentation"
    - path: "modules/mb-text/README.mbt.md"
      provides: "Compile-checked default/custom limits and continued public nonempty closure"
    - path: "docs/rfcs/0004-mb-font.md"
      provides: "Governed font-owned request-scoped normalized layout authority"
    - path: "docs/rfcs/0005-mb-text.md"
      provides: "Governed semantic projection and execution ownership boundary"
  key_links:
    - from: "scripts/quality/Assert-Policy.ps1"
      to: "policy/foundation.json"
      via: "exact mb-font/mb-text source, test, docs, dependency, and generated-interface validation"
      pattern: "Assert-Font|Assert-Text|foundation"
    - from: "modules/mb-font/README.mbt.md"
      to: "modules/mb-font/font/layout_admission.mbt"
      via: "public-abstract scoped admission example and explicit no-execution boundary"
      pattern: "admit_layout|FontLayout"
    - from: "modules/mb-text/README.mbt.md"
      to: "modules/mb-text/text/shape.mbt"
      via: "default/custom limits and public layout-unavailable behavior"
      pattern: "ShapeLimits|layout-unavailable"
  prohibitions:
    - { statement: "No layout execution, public raw inspection, persistent cache, UI, FFI, external API/schema, or reverse dependency is added or claimed.", status: unverified, flagged: true }
    - { statement: "No licensed font, host oracle, FontQualification layout case, or Phase 113 semantic qualification claim is used as Phase 109 evidence.", status: unverified, flagged: true }
    - { statement: "No manual pkg.generated.mbti edit or premature policy hash refresh is allowed.", status: unverified, flagged: true }
    - { statement: "No warning suppression, stale warning count, broad path wildcard, or generic Required-lane failure exemption is allowed.", status: unverified, flagged: true }
---

<objective>
Seal Phase 109 as an exact policy-owned, documented, four-target structural-admission boundary.

Purpose: Make the additive interface, closed OpenType subset, diagnostic vocabulary, generated hostile evidence, and phase ownership mechanically auditable.
Output: Updated RFC/module documentation, exact policy/interface inventories, prohibition negatives, and reproducible structural qualification commands.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/109-bounded-layout-admission/109-08-SUMMARY.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-05-SUMMARY.md
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1

<interfaces>
- Preserve the exact Phase 108 `shape` and `with_shape_transaction` signatures.
- Seal `FontLayoutLimits`, its exact named constructor, abstract `FontLayoutProfile`, `FontShapeScope::admit_layout`, and `ShapeLimits::with_layout_limits` only after `moon info --target all --frozen` determines exact generated spelling.
- Never edit generated interface files directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document bounded admission and locked divergences in module guides</name>
  <reversibility rating="reversible">Candidate module documentation can be corrected without changing the public contract or repository history.</reversibility>
  <files>modules/mb-font/README.mbt.md, modules/mb-font/CHANGELOG.md, modules/mb-text/README.mbt.md, modules/mb-text/CHANGELOG.md</files>
  <action>
Document the public-abstract FontLayoutLimits/Profile and scoped admission method, exact default/custom limit behavior, table-local/owned-fact/charge/work/lifetime/error contracts, selected GSUB 1/4/7→1/4, GPOS 2/9→2, Coverage/ClassDef 1/2, and conditional GDEF 1.0 subset per D-01 through D-40. State that duplicate FeatureRecord rejection (D-09), selected FeatureParams Capability classification (D-12), and recursive extension Capability classification (D-28) are deliberate MNF closed-profile policy/diagnostic restrictions relative to broader official OpenType behavior.

Per D-41 through D-46, state explicitly that admission applies no layout operation, queries no final metrics/legacy kerning, publishes no nonempty run, uses generated hostile structural fixtures only, and makes no licensed/oracle/cross-target semantic qualification claim. Append candidate changelog entries without stable-publication claims. Keep docs free of UI, FFI, external service/API/schema, cache, and raw inspection examples.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font check README.mbt.md --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-text check README.mbt.md --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>Module guides and changelogs document all admission behaviors, three locked divergences, and D-41 through D-46 fences without execution or qualification overclaim.</done>
</task>

<task type="auto">
  <name>Task 2: Govern font/text ownership and later-phase execution boundaries</name>
  <reversibility rating="costly">RFC ownership decisions constrain the module contracts consumed by Phases 110-113.</reversibility>
  <files>docs/rfcs/0004-mb-font.md, docs/rfcs/0005-mb-text.md</files>
  <action>
Update RFC 0004 so parsing, normalized facts, the 27 semantic/resource limits, revision authority, and request-scoped opaque profiles remain mb-font-owned per D-29 through D-40. Update RFC 0005 so mb-text owns only semantic request projection and sanitized public failure, while substitution, positioning, final metrics, legacy kerning, integrated charging, and semantic qualification remain assigned to Phases 110 through 113 per D-41 through D-46. Record D-09/D-12/D-28 as deliberate closed-profile divergences rather than general OpenType requirements, and keep the module dependency direction acyclic.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & git diff --check -- docs/rfcs/0004-mb-font.md docs/rfcs/0005-mb-text.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $font = Get-Content docs/rfcs/0004-mb-font.md -Raw; $text = Get-Content docs/rfcs/0005-mb-text.md -Raw; foreach ($token in @('FontLayoutLimits','FontLayoutProfile','request-scoped','D-09','D-12','D-28')) { if (($font + $text) -notmatch [regex]::Escape($token)) { throw ('missing RFC ownership token: ' + $token) } }; foreach ($phase in @('Phase 110','Phase 111','Phase 112','Phase 113')) { if ($text -notmatch [regex]::Escape($phase)) { throw ('missing RFC phase fence: ' + $phase) } }"</automated>
  </verify>
  <done>RFCs assign bounded admission to mb-font, semantic projection to mb-text, and execution/qualification to Phases 110-113 with all three MNF divergences explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Seal exact interfaces, inventories, prohibitions, and structural qualification</name>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <action>
Regenerate interfaces through `moon info`, then update `policy/foundation.json` with the exact seven new mb-font production files, seven new mb-font test files, changed mb-text tests/sources, unchanged dependency DAG, compile-checked docs, exact public semantic lines, and final source/doc hashes. Update the existing Phase 108 font-scope policy assertion so `units_per_em` plus `admit_layout` are the exact approved operations; do not stack a contradictory allowlist.

Extend `Assert-Policy.ps1` to validate exact source/test/publication inventories, all-target generated interfaces, the D-39 context vocabulary, D-09/D-12/D-28 named divergence fixtures, the mb-font 27-sentinel mapping test owned by Plan 109-01, and all 27 public exact/one-short mb-text outcome pairs owned by Plan 109-08. Add a policy-owned ordered map of all 27 private ShapeLimits default names/literals and assert the constructor source contains each named argument exactly once in that order with no alternate default table. Assert source structure contains exactly one `with_layout_limits` assignment to the embedded layout bundle, preserves the scalar/output fields, and exposes no access path used by mb-text tests. These are source-literal/structure checks; do not add a public or test-only accessor.

Add `Assert-Phase109DenyWarnDiagnostics` and its exact policy data. For each of js, wasm, wasm-gc, and native, run the real `moon -C modules/mb-text check --target <target> --deny-warn --frozen` child and capture combined diagnostics. Normalize only ANSI and absolute repository/build-root prefixes, then fingerprint each diagnostic as relative source path plus severity/code/message (and stable location when emitted). A clean zero exit with no diagnostics passes. Otherwise require the exact per-target fingerprint set sealed from a verified final-state run, require every entry to be an inherited CFF path/fingerprint, reject any error, unknown fingerprint, broad wildcard, count-only comparison, or diagnostic from a new/modified Phase 109 file. The helper must execute the compiler; it cannot suppress warnings or accept a stale remembered count.

Add `Assert-Phase109RequiredObservation` for the broad Required lane. It must first enumerate the live `modules/mb-core` `.mbt`/`.mbt.md` files exactly as `Invoke-MoonQuality.ps1` does, run the scoped `moon fmt --check <files>` child, retain its exit and normalized combined output, and compute its SHA-256. Then run `quality.ps1 -Lane Required` in a separate PowerShell child with stdout/stderr logs and exit status under `artifacts/release-qualification/phase109-required`. A zero exit is clean success. A nonzero exit is accepted only when the live scoped baseline itself fails, every Required stage before WORK-04 completed, the terminal stage is exactly `WORK-04 independent format check for mb-core`, the embedded formatter diagnostics match the freshly captured live baseline byte-for-byte after the same normalization, no later module/stage started, and the outer error identifies the exact mb-core format command. Any earlier, different, additional, or Phase 109 diagnostic throws and blocks. Emit a JSON observation with child status, terminal classification, and both log/baseline hashes; do not turn the inherited classification into a clean Required claim.

Add negative fixtures/scans forbidding raw tables/windows/bytes/offsets/indices, public profile facts/summaries/probes/charges/commit/execute methods, caches, GSUB/GPOS execution, cmap/final metrics/legacy-kern selection, successful public nonempty shaping, UI/FFI/network/filesystem/external API/schema integration, foreign production dependencies, licensed/oracle Phase 109 evidence, semantic qualification language, warning suppression, and generic Required failure acceptance per D-40 through D-46. Refresh policy/default/diagnostic fingerprints only after documentation and source are final.

Run the Phase 109 hard-success gates first: fail-fast four-target font/text tests and interface generation, workspace test roster, exact policy/default mapping, four-target deny-warn diagnostic delta, and the external-API coverage precheck. Only after they pass, run the separate Required observation helper. Do not run or cite FontQualification as Phase 109 layout evidence.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-text test text --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-font info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-text info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon test --target all --frozen --outline; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; . ./scripts/quality/Assert-Policy.ps1; Assert-FoundationPolicy -PolicyPath policy/foundation.json; Assert-Phase109DefaultLimitMapping -PolicyPath policy/foundation.json; Assert-Phase109DenyWarnDiagnostics -PolicyPath policy/foundation.json; $gsdTools = Join-Path $env:USERPROFILE '.codex\gsd-core\bin\gsd-tools.cjs'; & node $gsdTools check api-coverage-verify-pre .planning/phases/109-bounded-layout-admission --raw; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Assert-Phase109RequiredObservation -PolicyPath policy/foundation.json -EvidenceDirectory artifacts/release-qualification/phase109-required"</automated>
  </verify>
  <done>LAY-01/LAY-02 hard-success tests, interfaces, exact private default mapping, diagnostic fingerprints, files, docs, prohibitions, and no-external/no-overclaim boundaries pass; Required either succeeds or is recorded only as the exact live inherited WORK-04 mb-core formatter terminal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| implementation -> generated public interface | Private layout facts can accidentally become public API |
| source/tests/docs -> policy evidence | Stale inventories or hashes can make drift invisible |
| structural evidence -> qualification claims | Generated admission parity must not become a licensed semantic shaping claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-35 | Elevation of Privilege | generated mb-font/mb-text interface | high | mitigate | Exact all-target allowlist plus negative raw/profile/cache/commit/execute fixtures |
| T-109-36 | Tampering | policy inventories and hashes | high | mitigate | Exact file arrays and final-state hash refresh after source/docs settle |
| T-109-37 | Repudiation | structural qualification record | medium | mitigate | Named commands, generated fixture IDs, exact target records, and honest inherited blocker status |
| T-109-38 | Information Disclosure | docs/errors | medium | mitigate | Sealed diagnostic tokens and no offsets/indices/tags/source paths |
| T-109-39 | Spoofing | semantic qualification claim | high | mitigate | Policy forbids licensed/oracle/FontQualification Phase 109 layout evidence and successful nonempty claims |
| T-109-42 | Tampering | private default table | high | mitigate | Exact ordered 27-name/literal source map and sole-replacement structural assertion |
| T-109-43 | Repudiation | inherited compiler diagnostics | high | mitigate | Live four-target child runs and exact relative-path/severity/message fingerprints; no count-only allowlist |
| T-109-44 | Elevation of Privilege | Required failure exemption | high | mitigate | Fresh scoped mb-core formatter baseline must byte-match the child terminal; any other stage/output blocks |
</threat_model>

<verification>
- All Phase 109 behavior-dependent suites execute on js, wasm, wasm-gc, and native.
- Exact font/text interfaces and policy pass.
- The 27 private default literals/order and sole replacement assignment pass source/policy checks.
- The three-layer limit proof is complete: mb-font sentinel mapping, 27 public mb-text exact/one-short outcome pairs, and one source-owned replacement assignment, with no cross-package private test access.
- Each four-target deny-warn child is clean or contains exactly the final-state inherited CFF fingerprint set and zero Phase 109-file diagnostics.
- The external API coverage gate confirms no integration/schema matrix is required.
- Required runs separately and is accepted only as clean success or the byte-matched live inherited WORK-04 mb-core formatter terminal; Phase 113 semantic qualification remains absent.
</verification>

<success_criteria>
- LAY-01/LAY-02 are documented, interface-sealed, generated-hostile-tested, and policy-owned.
- Phase 109 hard-success gates cannot be masked by the Required observation or inherited warning allowlist.
- Every locked decision D-01 through D-46 is implemented or mechanically fenced.
- No deferred capability or forbidden surface enters Phase 109.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Deterministic selected-path admission with distinct malformed/unreachable/unsupported outcomes behind an opaque profile | 01-09 | COVERED |
| REQ | LAY-01 | Exact Script/LangSys/Feature/Lookup selection and ordered de-duplicated plan | 03, 07-09 | COVERED |
| REQ | LAY-02 | Coverage/ClassDef/GDEF/flags/extensions bounded selected profile | 01, 04-09 | COVERED |
| RESEARCH | — | Two-pass global-envelope then selected-deep parsing | 01, 03, 07 | COVERED |
| RESEARCH | — | Public-abstract limits/profile and unchanged public shape/transaction signatures | 02, 07-09 | COVERED |
| RESEARCH | — | Official-spec divergences for duplicate FeatureRecords, FeatureParams, and recursive extensions | 03, 04, 09 | COVERED |
| RESEARCH | — | Normative owned-fact coefficients and exact charge/work/allocation ledgers | 01, 04-07 | COVERED |
| RESEARCH | — | Generated hostile exact-fit/one-short matrices and named revision guards | 01, 04-08 | COVERED |
| RESEARCH | — | Four-target tests, exact interfaces, policy, docs, and no semantic qualification overclaim | 09 | COVERED |
| CONTEXT | D-01 | Table-local windows and UInt64 arithmetic | 01 | COVERED |
| CONTEXT | D-02 | Field-specific offset bases | 01 | COVERED |
| CONTEXT | D-03 | Checked target resolution and nullable-zero schema | 01 | COVERED |
| CONTEXT | D-04 | Global envelope versus selected deep validation | 01, 03 | COVERED |
| CONTEXT | D-05 | Unselected invalid global offsets are Data; rich bodies neutral | 01, 03 | COVERED |
| CONTEXT | D-06 | GSUB/GPOS 1.0/1.1 and FeatureVariations classification | 01 | COVERED |
| CONTEXT | D-07 | Exact Script, absent-table neutrality, no DFLT fallback | 03 | COVERED |
| CONTEXT | D-08 | Default-only or exact-only LangSys selection | 03 | COVERED |
| CONTEXT | D-09 | Strict record order/uniqueness and LangSys index rules | 03, 09 | COVERED |
| CONTEXT | D-10 | Required/rlig/liga/kern feature selection | 03 | COVERED |
| CONTEXT | D-11 | Required wins and kern=false conflict | 03 | COVERED |
| CONTEXT | D-12 | Selected FeatureParams Data/Capability rule | 03, 09 | COVERED |
| CONTEXT | D-13 | Lookup reference union/de-duplication/ascending order | 03 | COVERED |
| CONTEXT | D-14 | Source-order subtable occurrences | 03-05 | COVERED |
| CONTEXT | D-15 | Every selected occurrence admitted; Capability cannot hide Data | 03-07 | COVERED |
| CONTEXT | D-16 | Coverage 1 strict GIDs | 01 | COVERED |
| CONTEXT | D-17 | Coverage 2 ranges/cardinality | 01 | COVERED |
| CONTEXT | D-18 | ClassDef 1/2 and class zero | 01 | COVERED |
| CONTEXT | D-19 | Contextual class/product bounds | 01, 05 | COVERED |
| CONTEXT | D-20 | GSUB 1/4 normalization only | 04 | COVERED |
| CONTEXT | D-21 | GPOS 2 normalization only | 05 | COVERED |
| CONTEXT | D-22 | Closed ValueFormat and rejected bits | 05 | COVERED |
| CONTEXT | D-23 | Conditional required GDEF 1.0 GlyphClassDef | 06 | COVERED |
| CONTEXT | D-24 | Required richer GDEF Capability; unused richer GDEF neutral | 06 | COVERED |
| CONTEXT | D-25 | Admitted flags and GDEF class semantics | 06 | COVERED |
| CONTEXT | D-26 | Unsupported flags after fixed-envelope validation | 06 | COVERED |
| CONTEXT | D-27 | One-hop GSUB 7 / GPOS 9 Offset32 dispatch | 04, 05 | COVERED |
| CONTEXT | D-28 | Recursive/other extension Data/Capability rule | 04, 05, 09 | COVERED |
| CONTEXT | D-29 | Owned compact opaque facts only | 01, 07 | COVERED |
| CONTEXT | D-30 | Compatible two-argument ShapeLimits and additive bundle | 02 | COVERED |
| CONTEXT | D-31 | Separate semantic/resource ceilings | 01, 02, 05, 07 | COVERED |
| CONTEXT | D-32 | Per-structure, aggregate, product, and extent ceilings | 01, 02, 05, 07 | COVERED |
| CONTEXT | D-33 | Exact retained/allocation/work charge and no source recharge | 07 | COVERED |
| CONTEXT | D-34 | Exhaustive parser-work equation | 01, 04-07 | COVERED |
| CONTEXT | D-35 | Exact-fit/one-short, atomic failure, later-stage composition | 07, 08 | COVERED |
| CONTEXT | D-36 | Named admission and final revision guards | 07, 08 | COVERED |
| CONTEXT | D-37 | Stable precedence and declared-count preflight exception | 01, 03, 07, 08 | COVERED |
| CONTEXT | D-38 | Request-scoped profile lifetime and no cache | 07, 08 | COVERED |
| CONTEXT | D-39 | Internal/public operations and sealed contexts | 07-09 | COVERED |
| CONTEXT | D-40 | Private summaries only; no public raw/commit/cache surface | 07, 09 | COVERED |
| CONTEXT | D-41 | Normalize without substitution/positioning/metrics/kern | 04, 05, 08, 09 | COVERED |
| CONTEXT | D-42 | GSUB execution/cmap/clusters/metrics remain Phase 110-owned | 04, 08, 09 | COVERED |
| CONTEXT | D-43 | PairPos execution/accumulation/kern authority remain Phase 111-owned | 05, 08, 09 | COVERED |
| CONTEXT | D-44 | Integrated execution charges/mutation/atomicity remain Phase 112-owned | 07-09 | COVERED |
| CONTEXT | D-45 | Generated hostile structural evidence only; Phase 113 owns semantic qualification | 01, 04-09 | COVERED |
| CONTEXT | D-46 | Public nonempty shaping remains capability-closed | 08, 09 | COVERED |

Deferred CONTEXT ideas and requirements assigned to Phases 110-113 are deliberate exclusions, not audit gaps.

<output>
Create `.planning/phases/109-bounded-layout-admission/109-09-SUMMARY.md` when done.
</output>
