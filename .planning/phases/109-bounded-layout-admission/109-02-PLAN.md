---
phase: 109-bounded-layout-admission
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-font/font/layout_limits.mbt
  - modules/mb-text/text/limits.mbt
  - modules/mb-text/text/contract_test.mbt
autonomous: true
requirements:
  - LAY-01
  - LAY-02
must_haves:
  truths:
    - "D-30: ShapeLimits::new remains exactly two-argument; the private default bundle and additive customization compile without exposing their values."
    - "D-31/D-32: FontLayoutLimits exposes one public-abstract constructor covering every named byte/count/product/retention/allocation/work ceiling and provides one package-private ordered 27-field value contract for Plan 109-01."
    - "Every zero constructor field fails in deterministic argument order; old and custom construction compile on all four targets and generated interfaces remain abstract."
  artifacts:
    - path: "modules/mb-font/font/layout_limits.mbt"
      provides: "Public-abstract FontLayoutLimits plus a package-private ordered 27-field adapter value"
      exports: ["FontLayoutLimits"]
    - path: "modules/mb-text/text/limits.mbt"
      provides: "Compatibility-preserving ShapeLimits defaults and additive customization"
      exports: ["ShapeLimits::new", "ShapeLimits::with_layout_limits"]
    - path: "modules/mb-text/text/contract_test.mbt"
      provides: "Old-constructor signature, zero-order, custom-construction compilation, and interface-abstractness tests"
  key_links:
    - from: "modules/mb-text/text/limits.mbt"
      to: "modules/mb-font/font/layout_limits.mbt"
      via: "private embedded FontLayoutLimits and additive replacement method"
      pattern: "FontLayoutLimits|with_layout_limits"
  prohibitions:
    - { statement: "No existing ShapeLimits constructor argument, scalar/output value, or public shape signature changes.", status: unverified, flagged: true }
    - { statement: "No public raw limit-field accessors, default constructor, layout profile accessor, or reverse mb-font-to-mb-text dependency is added.", status: unverified, flagged: true }
---

<objective>
Freeze the additive public-abstract layout-limit contract without breaking Phase 108 callers.

Purpose: Give every Phase 109 parser and ledger a single font-owned authority bundle while preserving the published two-argument ShapeLimits constructor.
Output: FontLayoutLimits, sealed default values, additive ShapeLimits customization, and four-target compatibility tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-04-SUMMARY.md
@modules/mb-font/font/limits.mbt
@modules/mb-text/text/limits.mbt

<interfaces>
- Preserve `ShapeLimits::new(max_input_scalars~ : UInt64, max_output_glyphs~ : UInt64) -> Result[ShapeLimits, CoreError]` exactly.
- Add only `ShapeLimits::with_layout_limits(self, @font.FontLayoutLimits) -> ShapeLimits` on the text side.
- `FontLayoutLimits` has private fields, one exact named-argument constructor, and package-private consumption helpers only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nonzero font-owned layout limits and preserve ShapeLimits compatibility</name>
  <reversibility rating="one-way">D-30 is an already-locked public construction and limit-semantics decision; changing it after downstream adoption requires a compatibility migration.</reversibility>
  <files>modules/mb-font/font/layout_limits.mbt, modules/mb-text/text/limits.mbt, modules/mb-text/text/contract_test.mbt</files>
  <behavior>
    - "FontLayoutLimits::new accepts the exact 27 named UInt64 ceilings from research and rejects the first zero in constructor order with operation font-layout-limits-new."
    - "ShapeLimits::new retains its exact two named arguments, scalar/output behavior, and error order."
    - "The old constructor, a custom FontLayoutLimits value, and with_layout_limits compile unchanged on all four targets."
    - "Generated interfaces expose no FontLayoutLimits fields/accessors, ShapeLimits layout accessor, or private adapter record."
    - "A package-private FontLayoutLimitValues record carries all 27 fields once in constructor order for Plan 109-01; this Wave 1 plan does not claim observable default-versus-custom admission behavior."
  </behavior>
  <action>
Implement `FontLayoutLimits` with the exact 27 private fields, constructor order, nonzero validation, `InvalidInput/InvalidRange`, requested `0`, limit `1`, operation `font-layout-limits-new`, and the kebab-case field context defined in research per D-30 through D-32. Define a package-private `FontLayoutLimitValues` record with the same 27 names/order and one package-private extraction method that copies each field exactly once; Plan 109-01 will own and sentinel-test its conversion into the builder record. Use the exact conservative defaults from the research table in the private ShapeLimits construction source: 1,048,576 GSUB/GPOS bytes; 262,144 GDEF bytes; the documented scripts, language systems, features, references, lookups, subtables, Coverage/ClassDef, substitution/ligature/component, PairSet/pair/class/cell, extent/product, retained byte, allocation, maximum allocation, and parser-work values.

Embed that default bundle inside the unchanged two-argument ShapeLimits constructor and add `with_layout_limits` as a value-returning additive replacement. Preserve the two scalar/output fields exactly. At Wave 1, add black-box tests only for all 27 zero fields in deterministic order, unchanged old-call behavior, successful custom construction/customization compilation, and generated interface abstractness. Do not attempt to inspect or infer private defaults or replacement behavior here: Plan 109-01 proves the internal 27-field mapping with mb-font white-box sentinels, Plan 109-08 proves all 27 custom fields through public exact/one-short admission outcomes, and Plan 109-09 source/policy gates seal the exact default block and sole replacement assignment. Do not add public field accessors or manually edit generated `.mbti` files.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-text test text --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-font info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-text info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-30/D-31/D-32 have one compatible, nonzero public constructor surface, a complete package-private 27-field adapter contract, four-target custom-call compilation, and no impossible Wave 1 observation of private defaults/replacement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller limits -> parser authority | Hostile zero/tiny/huge semantic ceilings configure untrusted binary work |
| mb-text -> mb-font | Public values cross the one-way module dependency without raw layout data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-05 | Denial of Service | FontLayoutLimits constructor | high | mitigate | Every dimension is mandatory/nonzero and later consumers independently check products/extents |
| T-109-06 | Tampering | ShapeLimits compatibility | medium | mitigate | Exact old constructor/scalar-output regressions now; live default-versus-custom admission in 109-08 and literal/order policy in 109-09 |
| T-109-07 | Information Disclosure | generated interfaces | medium | mitigate | Private fields and no raw accessors; all-target `moon info` inspection |
</threat_model>

<verification>
- Old and custom limit construction/compilation plus deterministic zero rejection pass on js, wasm, wasm-gc, and native.
- Generated interfaces expose the exact additive public lines only.
- No Wave 1 test claims to read private default values or prove bundle replacement behavior.
- No package, manifest, dependency, external API, FFI, UI, or cache change occurs.
</verification>

<success_criteria>
- Phase 108 ShapeLimits callers compile unchanged.
- All Phase 109 limit dimensions are explicit, nonzero, carried by one private 27-field contract, and ready for the sentinel mapping and live admission plans.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-02-SUMMARY.md` when done.
</output>
