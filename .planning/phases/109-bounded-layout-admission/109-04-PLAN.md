---
phase: 109-bounded-layout-admission
plan: "04"
type: execute
wave: 4
depends_on:
  - 109-03
files_modified:
  - modules/mb-font/font/layout_gsub.mbt
  - modules/mb-font/font/layout_gsub_wbtest.mbt
autonomous: true
requirements:
  - LAY-02
must_haves:
  truths:
    - "D-20: selected GSUB 1 formats 1/2 and GSUB 4 format 1 are completely normalized with exact cardinality, GID, component, base, and preference rules but never applied."
    - "D-27/D-28: GSUB 7 format 1 admits exactly one extension-relative Offset32 hop to type 1/4; zero/range/truncation is Data and other/recursive supported envelopes are Capability."
    - "Every selected occurrence is admitted in source order; a supported later occurrence cannot hide an unsupported earlier one."
  artifacts:
    - path: "modules/mb-font/font/layout_gsub.mbt"
      provides: "Owned SingleSubst, LigatureSubst, and one-hop ExtensionSubst normalization"
    - path: "modules/mb-font/font/layout_gsub_wbtest.mbt"
      provides: "Generated hostile GSUB format, cardinality, GID, preference, and Offset32 evidence"
  key_links:
    - from: "modules/mb-font/font/layout_gsub.mbt"
      to: "modules/mb-font/font/layout_common.mbt"
      via: "selected subtable windows, Coverage facts, ledgers, and deferred Capability"
      pattern: "Coverage|extension|capability"
  prohibitions:
    - { statement: "No substitution is applied and no cmap seed, cluster, final GID, metric, or output fact is produced.", status: unverified, flagged: true }
    - { statement: "No contextual, chained, reverse, recursive extension, alternate, multiple, or other selected GSUB type is silently skipped.", status: unverified, flagged: true }
---

<objective>
Normalize the complete selected GSUB admission subset without executing it.

Purpose: Give Phase 110 an owned, bounded, preference-preserving Single/Ligature plan while Phase 109 remains output-closed.
Output: GSUB 1/4 and 7→1/4 normalized facts plus generated hostile evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/109-bounded-layout-admission/109-03-SUMMARY.md
@modules/mb-font/font/layout_common.mbt
@modules/mb-font/font/layout_model.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize GSUB Single, Ligature, and one-hop extension bodies</name>
  <files>modules/mb-font/font/layout_gsub.mbt, modules/mb-font/font/layout_gsub_wbtest.mbt</files>
  <behavior>
    - "Single format 1 validates every modulo-65536 delta result against num_glyphs without expanding the mapping."
    - "Single format 2 requires substitute count equal Coverage cardinality and every substitute GID in range."
    - "Ligature format 1 requires set count equal Coverage cardinality, correct subtable-relative set bases, LigatureSet-relative Ligature bases, componentCount at least two, and all GIDs in range."
    - "Ligature sets, ligatures, components, duplicate offsets, and selected occurrences retain encoded preference order."
    - "Extension format 1 accepts a nonzero Offset32 above 0xFFFF and exactly one inner dispatch to GSUB 1 or 4."
    - "Mixed extension inner types are Data; recursive/other types and formats are Capability only after complete wrapper/target envelopes."
  </behavior>
  <action>
Implement selected GSUB dispatch and owned normalization per D-20. Use the common field-specific target resolver for Coverage, LigatureSet, and Ligature offsets; use two-pass exact-size arrays and the shared semantic/retained/allocation/work ledgers. Preserve every occurrence and every ligature preference in source order, including duplicate offsets. Validate delta results, Coverage/cardinality relations, output/component GIDs, component counts, record extents, and every exact/one-short semantic ceiling.

Implement GSUB 7 format-1 as one extension-relative nonzero Offset32 hop per D-27/D-28. Prove the eight-byte wrapper and complete inner subtable window inside the GSUB table, permit offsets above 0xFFFF, reject mixed declared inner types as Data, and defer recursive/other type or format Capability without hiding later selected Data. Add generated positive and hostile matrices for all formats, bases, counts, preference order, GIDs, extension range/truncation, mixed type, recursive wrapper, and unsupported selected bodies. Record recursive-extension classification as the locked MNF policy divergence.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-20/D-27/D-28 GSUB facts are fully admitted, owned, ordered, and hostile-tested without performing substitution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selected GSUB bytes -> normalized substitutions | Hostile cardinalities, nested offsets, GIDs, and preference order become retained facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-12 | Tampering | GSUB bases/cardinalities | high | mitigate | Field-specific bases, complete extents, equality checks, and labelled hostile fixtures |
| T-109-13 | Denial of Service | ligature/component arrays | high | mitigate | Per-structure and aggregate counts, record extents, exact allocations, and parser-work preflight |
| T-109-14 | Elevation of Privilege | extension dispatch | high | mitigate | Single-hop type allowlist and complete target validation; no recursive executor |
| T-109-15 | Repudiation | subtable preference | medium | mitigate | Private source-order summaries and duplicate-occurrence tests |
</threat_model>

<verification>
- All generated GSUB tests pass on four targets.
- No production call applies substitution or queries cmap/metrics.
- No public GSUB fact or debug accessor appears in `moon info`.
</verification>

<success_criteria>
- Selected GSUB 1/4/7 paths normalize completely and deterministically.
- Every unsupported selected branch fails closed after structural proof.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-04-SUMMARY.md` when done.
</output>
