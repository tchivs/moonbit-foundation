---
phase: 109-bounded-layout-admission
plan: "03"
type: execute
wave: 3
depends_on:
  - 109-01
files_modified:
  - modules/mb-font/font/layout_common.mbt
  - modules/mb-font/font/layout_common_wbtest.mbt
autonomous: true
requirements:
  - LAY-01
must_haves:
  truths:
    - "D-07/D-08: absent tables are neutral, but a present table requires the exact Script and exact Default-or-tag LangSys without DFLT or cross-choice fallback."
    - "D-09/D-10/D-11/D-12: tag/index/required-feature rules and selected FeatureParams classifications are deterministic, including required kern conflicting with kern=false."
    - "D-13/D-14/D-15: lookup references are validated and de-duplicated into ascending LookupList order while every selected subtable occurrence remains source-ordered and fully admitted."
    - "Duplicate FeatureRecord tags, selected FeatureParams, and recursive extension outcomes are named as MNF closed-profile policy divergences, not broad OpenType validity claims."
  artifacts:
    - path: "modules/mb-font/font/layout_common.mbt"
      provides: "Exact Script/LangSys/Feature/Lookup selection and deferred-capability machinery"
    - path: "modules/mb-font/font/layout_common_wbtest.mbt"
      provides: "Generated selection, ordering, duplicate, fallback, and divergence evidence"
  key_links:
    - from: "modules/mb-font/font/layout_common.mbt"
      to: "modules/mb-font/font/layout_model.mbt"
      via: "selected lookup bitmap becomes ascending owned lookup plan"
      pattern: "selected|lookup"
  prohibitions:
    - { statement: "No implicit DFLT/default/exact fallback, arbitrary feature selection, or lookup execution occurs.", status: unverified, flagged: true }
    - { statement: "Lookup indices are de-duplicated; subtable occurrences are never sorted or de-duplicated.", status: unverified, flagged: true }
---

<objective>
Implement the complete deterministic layout selection pipeline for LAY-01.

Purpose: Convert semantic script/language/features into one bounded ascending lookup plan without over-validating unrelated rich bodies.
Output: Selected Script/LangSys/Feature/Lookup normalization plus hostile selection and compatibility-divergence evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/109-bounded-layout-admission/109-01-SUMMARY.md
@modules/mb-font/font/layout_common.mbt
@modules/mb-font/font/layout_model.mbt

<interfaces>
- Consume Plan 109-01 table-local targets, common envelopes, owned models, and generated labelled-base builders.
- Selection receives semantic script bytes, `None` for Default language or `Some(tag)` for Exact, and liga/kern booleans; tag payloads never enter public diagnostics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select exact language features and normalize lookup order</name>
  <reversibility rating="costly">D-11 and D-13/D-14 determine later shaping output and feature-toggle compatibility across GSUB and GPOS consumers.</reversibility>
  <files>modules/mb-font/font/layout_common.mbt, modules/mb-font/font/layout_common_wbtest.mbt</files>
  <behavior>
    - "Present-table script absence and requested LangSys absence are Capability, with no DFLT/default/exact fallback."
    - "Script/LangSys/Feature records are strictly tag-sorted and unique; lookupOrderOffset is zero; feature indices are unique and in range."
    - "Required feature is always selected unless 0xFFFF; required selection wins de-duplication; required kern with kern=false is Capability."
    - "rlig is always selected, liga/kern follow toggles, and unrelated tags remain unselected."
    - "Selected non-null FeatureParams targets are structurally checked before Capability; malformed targets are Data."
    - "Duplicate lookup references collapse to one ascending lookup index, while duplicate subtable occurrences remain present and source-ordered."
    - "An earlier Capability never hides later selected Data; valid unselected advanced bodies remain neutral."
  </behavior>
  <action>
Extend the common parser with exact Script binary selection, Default-only or Exact-only LangSys selection, strict Script/LangSys/Feature tag ordering and uniqueness, LangSys envelope/index validation, required feature semantics, and closed `rlig`/`liga`/`kern` filtering per D-07 through D-12. Validate FeatureParams from the selected Feature base using the tag-specific envelopes in research; for undefined selected tags use the locked readable-two-byte rule before Capability. Test duplicate FeatureRecords as a deliberate MNF closed-profile restriction, not a specification requirement.

Validate all selected Feature lookup references, use an exact lookup bitmap, and emit selected lookup descriptors by ascending LookupList index per D-13. Retain every subtable offset occurrence in encoded order per D-14, including duplicates. Add a deterministic first-deferred-Capability accumulator that continues bounded selected structural validation so later Data wins per D-15/D-37. Cover absent tables, exact/no-fallback selection, required precedence, duplicate/out-of-range indices, toggle conflicts, lookup union/order, occurrence preservation, selected/unselected richness, and the D-09/D-12 divergence corpus.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>LAY-01 selection is deterministic and bounded, with explicit MNF divergence evidence and no lookup execution or unselected-body over-rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| semantic request -> font tags/indices | Caller choices select untrusted table records |
| selected references -> lookup plan | Duplicate/out-of-range references and unsupported bodies influence later execution order |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-08 | Tampering | Script/LangSys/Feature selection | high | mitigate | Strict record/index validation, exact-only selection, no fallback, hostile duplicate/order fixtures |
| T-109-09 | Denial of Service | selected reference traversal | high | mitigate | Count/work preflight and exact bitmap allocation before visits |
| T-109-10 | Repudiation | Capability versus Data winner | medium | mitigate | Deferred Capability only; later selected Data and named State guards remain dominant |
| T-109-11 | Information Disclosure | selection diagnostics | medium | mitigate | Stable relation tokens only; no selected tag bytes or record indices |
</threat_model>

<verification>
- Four-target mb-font selection tests pass.
- Lookup order and occurrence summaries are private white-box facts only.
- Divergence test names say `MNF closed-profile policy`.
</verification>

<success_criteria>
- Exact request selection yields one ascending de-duplicated lookup plan with stored source-order subtable preference.
- Unsupported selected behavior fails closed without hiding malformed selected data or rejecting unrelated valid rich data.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-03-SUMMARY.md` when done.
</output>
