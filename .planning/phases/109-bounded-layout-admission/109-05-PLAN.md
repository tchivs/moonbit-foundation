---
phase: 109-bounded-layout-admission
plan: "05"
type: execute
wave: 4
depends_on:
  - 109-03
files_modified:
  - modules/mb-font/font/layout_gpos.mbt
  - modules/mb-font/font/layout_gpos_wbtest.mbt
autonomous: true
requirements:
  - LAY-02
must_haves:
  truths:
    - "D-21/D-22: selected GPOS 2 formats 1/2 are fully normalized with exact PairSet/Coverage, second-GID, ClassDef, matrix, ValueRecord, and ValueFormat rules but never applied."
    - "D-19/D-32: class factors, class product, dense cells, matrix bytes, record extents, allocation size, and parser work are independently preflighted."
    - "D-27/D-28: GPOS 9 format 1 admits exactly one extension-relative Offset32 hop to type 2 with deterministic Data/Capability outcomes."
  artifacts:
    - path: "modules/mb-font/font/layout_gpos.mbt"
      provides: "Owned PairPos 1/2, ValueRecord, and one-hop ExtensionPos normalization"
    - path: "modules/mb-font/font/layout_gpos_wbtest.mbt"
      provides: "Generated hostile pair, class, value-bit, matrix, and Offset32 evidence"
  key_links:
    - from: "modules/mb-font/font/layout_gpos.mbt"
      to: "modules/mb-font/font/layout_common.mbt"
      via: "Coverage/ClassDef facts, checked target bases, limits, and deferred Capability"
      pattern: "Coverage|ClassDef|value"
  prohibitions:
    - { statement: "No positioning, ValueRecord accumulation, next-probe behavior, metrics, direction projection, or modern-versus-legacy kerning choice occurs.", status: unverified, flagged: true }
    - { statement: "No yAdvance, device/variation adjustment, reserved bit, or other selected GPOS body is silently ignored.", status: unverified, flagged: true }
---

<objective>
Normalize the complete selected GPOS admission subset without applying positioning.

Purpose: Give Phase 111 exact owned PairPos facts while proving class products, record windows, and unsupported value bits are bounded and fail closed.
Output: GPOS 2 and 9→2 normalized facts plus generated hostile evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/109-bounded-layout-admission/109-03-SUMMARY.md
@modules/mb-font/font/layout_common.mbt
@modules/mb-font/font/layout_model.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize PairPos formats and one-hop extension bodies</name>
  <files>modules/mb-font/font/layout_gpos.mbt, modules/mb-font/font/layout_gpos_wbtest.mbt</files>
  <behavior>
    - "PairPos 1 requires pairSetCount equal Coverage cardinality and strictly increasing in-range second GIDs within each complete PairSet."
    - "PairPos 2 accepts class zero, requires positive class counts, bounds ClassDefs by their declared domains, and retains row-major dense cells."
    - "ValueFormat zero is valid; x/y placement and x advance are normalized for both glyphs."
    - "yAdvance, non-null device/variation targets, and reserved bits become Capability only after every defined field and target envelope is validated."
    - "GPOS 9 accepts one nonzero extension-relative Offset32 hop to type 2; mixed inner types are Data and recursive/other types/formats are Capability."
  </behavior>
  <action>
Implement GPOS 2 format-1 normalization per D-21/D-22: compute complete ValueRecord widths, PairValueRecord stride, PairSet extents from the owning PairPos base, Coverage equality, strict second-GID order, GID bounds, and six owned signed value planes with omitted allowed values normalized to zero. Read forbidden defined fields and resolve every non-null device/variation target from the correct PairSet base before deferring Capability.

Implement format 2 with Coverage plus two contextual ClassDefs, explicit class-zero domains, checked positive factors, separate `max_classes`, `max_cross_product`, aggregate `max_class_cells`, matrix-byte, record-extent, allocation-size, retained-byte, and parser-work preflights per D-19/D-31/D-32/D-37. Retain row-major six-plane cells without applying them. Add GPOS 9 format-1 one-hop dispatch per D-27/D-28 using the extension-subtable base and complete inner target window. Cover every allowed/forbidden ValueFormat bit for both glyphs, zero format, truncated fields/targets, PairSet/order/cardinality, class factors/products/bounds, matrix exact/one-short, Offset32 above 0xFFFF, mixed/recursive/other inner types, and occurrence preservation.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-19/D-21/D-22/D-27/D-28 GPOS facts are fully admitted, owned, bounded, and hostile-tested without positioning execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selected GPOS bytes -> normalized pair facts | Hostile counts, products, formats, targets, and signed values become dense retained facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-16 | Denial of Service | PairPos class matrix | high | mitigate | Independent factor/product/cell/byte/extent/allocation/work preflights before matrix traversal |
| T-109-17 | Tampering | PairSet and ValueRecord windows | high | mitigate | Correct PairSet/PairPos bases, complete strides, strict second-GID order, all defined fields read |
| T-109-18 | Elevation of Privilege | ExtensionPos dispatch | high | mitigate | One-hop type-2 allowlist, complete target validation, no executor |
| T-109-19 | Repudiation | unsupported ValueFormat classification | medium | mitigate | Exhaustive per-bit generated Data/Capability matrix |
</threat_model>

<verification>
- All generated GPOS tests pass on four targets.
- PairPos factor/product exact-fit and one-short cases are independently exercised.
- No production positioning or legacy-kern call exists.
</verification>

<success_criteria>
- Selected GPOS 2/9 paths normalize completely and deterministically.
- Dense matrix and ValueRecord attack surfaces are bounded before traversal or allocation.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-05-SUMMARY.md` when done.
</output>
