---
phase: 109-bounded-layout-admission
plan: "08"
type: execute
wave: 6
depends_on:
  - 109-02
  - 109-07
files_modified:
  - modules/mb-text/text/shape.mbt
  - modules/mb-text/text/contract_test.mbt
  - modules/mb-text/text/contract_wbtest.mbt
autonomous: true
requirements:
  - LAY-01
  - LAY-02
must_haves:
  truths:
    - "D-38/D-39: mb-text projects only semantic script/language/toggle/limit values into scoped admission and sanitizes layout errors to text-shape without raw offsets, indices, tags, or host prose."
    - "D-41/D-42/D-43/D-44: Phase 109 applies no substitution/positioning, queries no cmap/final metrics/legacy kern, and does not claim integrated cross-stage execution hardening."
    - "D-45/D-46: generated structural fixtures prove admission on four targets, while public nonempty shaping still returns layout-unavailable with no run and no committed layout/text charge."
    - "Empty input preserves the exact Phase 108 guarded success and one-work charge without layout admission."
    - "D-30: the old ShapeLimits constructor's defaults admit generated structural profiles to layout-unavailable, while each of the 27 public custom ceilings has an exact-fit layout-unavailable case and a one-short named Resource case."
    - "D-40: mb-text tests prove customization only through public-abstract limit construction and observable shape outcomes; no accessor or cross-package internal limit record is introduced."
  artifacts:
    - path: "modules/mb-text/text/shape.mbt"
      provides: "Semantic scoped admission projection, sanitized error rebinding, and public nonempty closure"
    - path: "modules/mb-text/text/contract_test.mbt"
      provides: "Public empty/nonempty, source-compatible limits, error-redaction, and zero-commit evidence"
    - path: "modules/mb-text/text/contract_wbtest.mbt"
      provides: "Semantic projection, 27-field observable default/custom admission outcomes, stage precedence, profile path, mutation, and atomicity evidence"
  key_links:
    - from: "modules/mb-text/text/shape.mbt"
      to: "modules/mb-font/font/layout_admission.mbt"
      via: "FontShapeScope::admit_layout receives copied semantic tags, closed toggles, and FontLayoutLimits"
      pattern: "admit_layout"
    - from: "modules/mb-text/text/shape.mbt"
      to: "modules/mb-font/font/shape_transaction.mbt"
      via: "layout-unavailable exits the callback before the transaction can commit staged admission/text charge"
      pattern: "with_shape_transaction|layout-unavailable"
  prohibitions:
    - { statement: "No successful public nonempty ShapedRun, substitution, positioning, cmap seed, metric initialization, legacy-kern choice, or incomplete publication occurs.", status: unverified, flagged: true }
    - { statement: "No raw table/profile/source fact or admission charge is exposed through mb-text.", status: unverified, flagged: true }
---

<objective>
Wire private layout admission into the public text request while keeping Phase 109 nonempty shaping capability-closed.

Purpose: Prove the real semantic request reaches bounded font admission and that no incomplete run or charge becomes public before execution phases exist.
Output: Text-to-font admission projection, sanitized diagnostics, unchanged empty behavior, and four-target atomic fail-closed evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/108-public-contract-and-transaction-skeleton/108-VERIFICATION.md
@.planning/phases/109-bounded-layout-admission/109-02-SUMMARY.md
@.planning/phases/109-bounded-layout-admission/109-07-SUMMARY.md
@modules/mb-text/text/options.mbt
@modules/mb-text/text/tags.mbt
@modules/mb-text/text/shape.mbt

<interfaces>
- Public `shape(Font, Array[Int], ShapingOptions, ShapeLimits, Budget)` remains unchanged.
- `LanguageChoice::Default` maps to `None`; `Exact(tag)` maps to copied semantic bytes.
- A successful private `admit_layout` returns an abstract profile, but Phase 109 exposes and executes no profile method.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project semantic admission and preserve public nonempty closure</name>
  <precondition>Phase 108 public shape, options, immutable run, and transaction tests pass, and Plan 109-07 exposes scoped `admit_layout` without profile accessors.</precondition>
  <files>modules/mb-text/text/shape.mbt, modules/mb-text/text/contract_test.mbt, modules/mb-text/text/contract_wbtest.mbt</files>
  <behavior>
    - "Caller invalid input still wins before font state; closed-scope State wins before layout Data/Capability/Resource."
    - "Exact script bytes, None-or-exact language bytes, liga/kern booleans, and the abstract FontLayoutLimits value held by ShapeLimits are the only admission arguments."
    - "Layout errors preserve category/code and allowed requested/limit facts, rebind operation to text-shape, retain sealed context, and remove source offset/index/tag data."
    - "Valid empty input bypasses layout admission and preserves exact work=1 commit."
    - "Valid nonempty supported admission returns CapabilityUnavailable/layout-unavailable and leaves caller/ancestor budgets unchanged."
    - "Malformed, unsupported, over-limit, and mutated admission outcomes publish no run and leave budgets unchanged."
    - "For each of the 27 public FontLayoutLimits fields, a generated fixture with that field at its exact need reaches layout-unavailable and lowering only that field by one yields its stable Resource context; representative fixtures under the old constructor defaults also reach layout-unavailable."
  </behavior>
  <action>
Update the nonempty branch to call `scope.admit_layout` with copied semantic request values and the abstract `FontLayoutLimits` value held by `ShapeLimits` per D-38/D-39. Keep complete caller validation and scalar snapshot ahead of font authority. Reconstruct layout errors at the text boundary with operation `text-shape`, category/code and allowed semantic requested/limit fields, and the sealed context; omit source offsets, record ordinals, selected tags, indices, and host prose.

After a valid private profile is admitted, return the existing `Capability/CapabilityUnavailable`, operation `text-shape`, context `layout-unavailable` from the callback per D-41 through D-46. Do not inspect or execute the profile, call cmap/metrics/kerning, apply GSUB/GPOS, or create a ShapedRun. Preserve the empty path without admission.

Move the D-30 behavioral evidence here, after admission exists. Build a table-driven generated fixture family covering every one of the 27 public `FontLayoutLimits` fields. For each field, construct a custom bundle whose values equal the fixture's measured needs and prove public shaping reaches `layout-unavailable`; lower only the exercised field by one and prove its named Resource outcome occurs before `layout-unavailable`. Drive representative fixtures that fit the documented defaults through the unchanged two-argument `ShapeLimits::new` path and prove the same post-admission `layout-unavailable` outcome. These mb-text tests may use only public `FontLayoutLimits` construction, `ShapeLimits::with_layout_limits`, and observable shape results/errors; do not import cross-package internal records or add field/profile accessors.

Add the remaining public/white-box tests for semantic projection, exact/default language, toggles, error redaction, full InvalidInput/State/Data/Capability/Resource matrix, every named mutation seam, valid supported private admission followed by public closure, and zero caller/ancestor budget delta on every nonempty outcome.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-text test text --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & moon -C modules/mb-text info --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-30 and D-38 through D-46 are preserved at the public boundary: live admission distinguishes private defaults from exact/one-short custom limits, diagnostics are sanitized, empty succeeds unchanged, and every nonempty request publishes and commits nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| mb-text request -> mb-font admission | Semantic values cross the module boundary into untrusted binary parsing |
| font layout error -> public text error | Internal parser facts must be sanitized without changing precedence |
| private profile -> public result | Incomplete execution authority must not become a ShapedRun |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-30 | Tampering | semantic projection | high | mitigate | Closed option mapping and exact/default/tag/toggle tests |
| T-109-31 | Information Disclosure | error rebinding | high | mitigate | Reconstruct allowed fields only; redaction tests for offsets/indices/tags |
| T-109-32 | Elevation of Privilege | public nonempty route | high | mitigate | Unconditional layout-unavailable after private admission; no profile operations |
| T-109-33 | Denial of Service | caller/ancestor budgets | high | mitigate | Callback failure prevents final transaction commit; complete budget snapshots |
| T-109-34 | Repudiation | stage precedence | medium | mitigate | Combined-fault public/private matrix on all four targets |
| T-109-41 | Tampering | ShapeLimits custom bundle behavior | high | mitigate | Twenty-seven public exact/one-short outcome cases plus representative old-default outcomes, using only observable shaping results |
</threat_model>

<verification>
- mb-text and mb-font focused suites pass on all four targets.
- Public valid nonempty shaping remains CapabilityUnavailable with zero authority delta.
- Generated interface exposes no raw profile, table, index, source, charge, or commit facts.
- Old-default behavior and all 27 exact-custom/one-short-custom pairs are observed only through real public shaping outcomes; mb-text tests use no cross-package internal record or accessor.
</verification>

<success_criteria>
- The real text request reaches the bounded private profile seam.
- Every public layout-limit dimension affects an observable exact/one-short admission outcome without exposing font internals.
- Phase 109 cannot publish an incomplete run or make Phase 110-113 claims.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-08-SUMMARY.md` when done.
</output>
