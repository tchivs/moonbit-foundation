---
phase: 109-bounded-layout-admission
plan: "06"
type: execute
wave: 4
depends_on:
  - 109-03
files_modified:
  - modules/mb-font/font/layout_gdef.mbt
  - modules/mb-font/font/layout_gdef_wbtest.mbt
autonomous: true
requirements:
  - LAY-02
must_haves:
  truths:
    - "D-23/D-24: GDEF is untouched unless selected ignore-class flags require it; then absent/null/malformed 1.0 data is Data and a structurally complete non-1.0 dependency is Capability."
    - "D-25: RIGHT_TO_LEFT is retained as non-operative metadata; ignore-base/ligature/mark map to GDEF classes 1/2/3 while classes 0 and 4 remain visible."
    - "D-26: mark filtering, nonzero mark-attachment class, and reserved flags are Capability only after the complete Lookup envelope is valid."
  artifacts:
    - path: "modules/mb-font/font/layout_gdef.mbt"
      provides: "Conditional exact GDEF 1.0 GlyphClassDef binding and lookup-flag normalization"
    - path: "modules/mb-font/font/layout_gdef_wbtest.mbt"
      provides: "Generated dependency, version, class, flag, and visibility evidence"
  key_links:
    - from: "modules/mb-font/font/layout_gdef.mbt"
      to: "modules/mb-font/font/layout_common.mbt"
      via: "selected lookup flags request common ClassDef normalization from the GDEF table base"
      pattern: "ClassDef|ignore|gdef"
  prohibitions:
    - { statement: "No eager GDEF parsing, Unicode/cmap class inference, mark filtering set, mark attachment, or glyph deletion occurs.", status: unverified, flagged: true }
    - { statement: "RIGHT_TO_LEFT lookup metadata never changes caller run direction or admission order.", status: unverified, flagged: true }
---

<objective>
Bind only the GDEF glyph classifier required by admitted class-ignore flags.

Purpose: Make LAY-02 filtering dependencies explicit and bounded while leaving unrelated rich GDEF data neutral.
Output: Conditional GDEF 1.0/GlyphClassDef facts, normalized lookup flags, and hostile dependency evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/109-bounded-layout-admission/109-CONTEXT.md
@.planning/phases/109-bounded-layout-admission/109-RESEARCH.md
@.planning/phases/109-bounded-layout-admission/109-PATTERNS.md
@.planning/phases/109-bounded-layout-admission/109-03-SUMMARY.md
@modules/mb-font/font/layout_common.mbt
@modules/mb-font/font/layout_model.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize lookup flags and conditionally bind GDEF 1.0 classes</name>
  <files>modules/mb-font/font/layout_gdef.mbt, modules/mb-font/font/layout_gdef_wbtest.mbt</files>
  <behavior>
    - "No selected ignore bit means absent, malformed, or richer GDEF is not read and cannot change admission."
    - "Each ignore bit and combined masks require exact GDEF 1.0 with non-null valid GlyphClassDef."
    - "GDEF classes 0 through 4 admit; values above 4 are Data."
    - "Classes 1/2/3 map to the corresponding ignore masks; explicit/implicit class zero and component class 4 remain visible."
    - "RIGHT_TO_LEFT changes only retained metadata."
    - "USE_MARK_FILTERING_SET requires its extra uint16 to fit before Capability; reserved and mark-attachment flag bits likewise wait for the complete Lookup envelope."
  </behavior>
  <action>
Normalize LookupFlag metadata per D-23 through D-26. Admit RIGHT_TO_LEFT plus the three ignore-class bits and retain their semantic mask without applying filtering. Validate the optional MarkFilteringSet field when its bit is present, and classify that bit, nonzero mark-attachment class, and reserved flags as deferred Capability only after the complete Lookup fixed envelope.

When and only when an admitted selected lookup requires a class-ignore bit, open the GDEF table-local window, validate a complete known-version header, require exact 1.0 and non-null GlyphClassDef, resolve from GDEF start, and normalize with common ClassDef logic limited to classes 0..4. Missing table, null target, truncation, malformed ClassDef, or out-of-range class is Data; structurally complete non-1.0 is Capability. Add generated tests for no-flags neutrality, every mask/class, combined masks, classes 0..4 and above, absence/null/truncation/version outcomes, implicit/explicit zero, component visibility, and non-operative RTL metadata.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & moon -C modules/mb-font test font --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"</automated>
  </verify>
  <done>D-23 through D-26 conditional GDEF and flag behavior is exact, generated, bounded, and execution-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selected lookup flags -> GDEF dependency | Attacker-controlled flags decide whether another untrusted table becomes required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-109-20 | Tampering | GDEF dependency selection | high | mitigate | Bind only from admitted selected flags; exact 1.0 and non-null ClassDef requirements |
| T-109-21 | Denial of Service | optional rich GDEF | medium | mitigate | Do not parse when no selected classifier dependency exists |
| T-109-22 | Elevation of Privilege | unsupported flags | high | mitigate | Complete fixed envelope then fail closed as Capability; no silent weakening |
| T-109-23 | Repudiation | class visibility | medium | mitigate | Exhaustive class/mask private summaries and generated tests |
</threat_model>

<verification>
- All GDEF/flag tests pass on four targets.
- Rich unused GDEF neutrality and required dependency failures are directly tested.
- No filter execution or direction mutation is present.
</verification>

<success_criteria>
- GDEF is a precise selected dependency, not an eager font-wide lint.
- Every admitted and unsupported lookup flag has deterministic structural and capability behavior.
</success_criteria>

<output>
Create `.planning/phases/109-bounded-layout-admission/109-06-SUMMARY.md` when done.
</output>
