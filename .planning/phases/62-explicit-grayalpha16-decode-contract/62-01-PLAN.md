---
phase: 62-explicit-grayalpha16-decode-contract
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_decode.mbt
  - modules/mb-image/png/raster_decode.mbt
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
autonomous: true
requirements:
  - GRA16DEC-01
must_haves:
  truths:
    - "A caller can use PngDecoder::decode_graya16 to obtain an existing DecodeResult containing packed little-endian, straight-alpha, encoded-sRGB graya16 data from legal non-interlaced Type-4/16 PNG input."
    - "The reconstructed source lanes Ghi,Glo,Ahi,Alo are observable as Glo,Ghi,Alo,Ahi for every explicit-decoder pixel, including unequal high and low bytes."
    - "The ordinary PngDecoder generic route still exposes the same Type-4/16 source as RGBA8(Ghi,Ghi,Ghi,Ahi)."
    - "An incompatible explicit profile fails with the established typed capability diagnostic before a private image lifecycle or result is created."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Public PngDecoder::decode_graya16 eager selector returning @codec.DecodeResult.
      contains: "PngDecoder::decode_graya16"
    - path: modules/mb-image/png/stream_decode.mbt
      provides: Private GrayAlpha16 profile selection and first-IDAT admission before allocation.
      contains: "PngDecodeProfile::GrayAlpha16"
    - path: modules/mb-image/png/raster_decode.mbt
      provides: Profile-aware final Type-4/16 storage sink preserving both source bytes per component.
      contains: "set_component_byte"
    - path: modules/mb-image/png/png_test.mbt
      provides: Independent public Type-4/16 fidelity and generic-compatibility regression.
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: Private atomic-rejection evidence for the explicit profile.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: PngDecoder::decode_graya16 constructs the existing decoder machine with the private GrayAlpha16 profile.
      pattern: "decode_graya16.*new_with_profile"
    - from: modules/mb-image/png/stream_decode.mbt
      to: modules/mb-image/png/raster_decode.mbt
      via: First-IDAT preflight selects the graya16 descriptor and passes the same profile to the existing raster sink.
      pattern: "PngRasterSink::new"
    - from: modules/mb-image/png/raster_decode.mbt
      to: modules/mb-image/storage/views.mbt
      via: The completed reconstructed Type-4/16 row writes Gray and Alpha component bytes through the existing U16 component-byte API.
      pattern: "set_component_byte"
---

<objective>
Deliver the explicit eager GrayAlpha16 decode contract as one tested vertical slice.

Purpose: make exact Type-4/16 component preservation opt-in while retaining the established generic RGBA8 compatibility contract.
Output: the explicit eager selector, one private decode profile and sink route, an independent public byte-fidelity oracle, and atomic typed-admission coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-explicit-grayalpha16-decode-contract/62-CONTEXT.md
@.planning/phases/62-explicit-grayalpha16-decode-contract/62-RESEARCH.md
@.planning/research/v020-ARCHITECTURE.md
@.planning/research/v020-PITFALLS.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt
@modules/mb-image/png/encode_test.mbt
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Explicit legal Type-4/16 eager preservation into existing LE straight-alpha graya16 without generic drift | 62-01 | COVERED | Tracer proves the public selector, lane mapping, descriptor identity, and frozen generic output. |
| REQ | GRA16DEC-01 | Explicit decode through decode_graya16 returns existing packed graya16 with every source component byte preserved; generic remains high-byte RGBA8 | 62-01 | COVERED | Tasks 1 and 2 cover success and admissibility boundaries. |
| RESEARCH | — | Select one private profile before allocation and retain the existing framing, DEFLATE, byte-domain filtering, and raster lifecycle | 62-01 | COVERED | Task 1 carries a profile through the established machine and alters only final descriptor/store selection. |
| RESEARCH | — | Accept default or sRGB identity; reject incompatible type/depth/interlace/legacy-colour/ICC facts with typed pre-allocation failure | 62-01 | COVERED | Task 2 makes the profile gate and lifecycle/budget evidence executable. |
| RESEARCH | — | Use a hand-authored non-symmetric literal plus a generic high-byte compatibility anchor | 62-01 | COVERED | Task 1 owns both public oracles. |
| CONTEXT | D-01 | Expose only decode_graya16 and return existing DecodeResult/graya16 storage | 62-01 | COVERED | Task 1 adds the sole public eager operation. |
| CONTEXT | D-02 | Allow only encoded-sRGB Type-4/16 straight-alpha input and use typed diagnostics for incompatible descriptors | 62-01 | COVERED | Task 2 performs and proves the first-IDAT admission gate. |
| CONTEXT | D-03 | Reuse one profile-aware machine and final sink after byte-domain reconstruction | 62-01 | COVERED | Task 1 carries the private profile through the existing machine and changes the completed-row store. |
| CONTEXT | D-04 | Preserve the generic RGBA8 high-byte behavior and prove preservation with independent asymmetric input | 62-01 | COVERED | Task 1 asserts both routes from the same literal source. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Red-green explicit eager Type-4/16 preservation path</name>
  <reversibility rating="costly">The additive public selector and its precise existing-result contract become the compatibility boundary consumed by the next two phases.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - RED: a hand-authored complete non-interlaced Type-4/16 PNG literal with two non-symmetric pixels fails because PngDecoder::decode_graya16 does not exist.
    - GREEN: both the declaration-free literal and an independently fixed sRGB-declared variant return DecodeResult images whose descriptor is packed little-endian graya16, TopLeft, Straight, and built-in encoded-sRGB; each pixel exposes Glo,Ghi,Alo,Ahi through get_component_byte and the returned bytes_read equals the complete literal length.
    - GREEN: the same literal decoded through @codec.ImageDecoder::decode(PngDecoder::new(), ...) retains RGBA8(Ghi,Ghi,Ghi,Ahi), including the frozen first-pixel values 0x12,0x12,0x12,0xa7.
  </behavior>
  <action>Start with the public literal tests in png_test.mbt. Hand-author the complete PNG inputs, including their stored-DEFLATE payloads and CRCs, rather than obtaining their bytes from PngEncoder; use wire pixels 12,34,a7,c5 and be,0f,5a,76 so every source lane independently detects loss or byte-order drift. Per D-01, add only PngDecoder::decode_graya16 with the existing Reader/options/limits/budget/diagnostics shape and existing DecodeResult return. Per D-03, add a private PngDecodeProfile::GrayAlpha16 and a profile-taking machine constructor while PngDecodeMachine::new and the ImageDecoder trait implementation continue selecting the generic profile. At first-IDAT preflight, select the existing graya16 descriptor/identity and retain the existing four-byte result budget and two source-row accounting; pass the same profile to PngRasterSink. Keep the existing byte-domain Type-4/16 reconstruction distance, and after a completed reconstructed row write source Ghi,Glo,Ahi,Alo through the existing component-byte API as destination Glo,Ghi,Alo,Ahi. Keep the generic Type-4/16 writer on its established high-byte RGBA8 route per D-04. Run the targeted test once while red, then implement only the described vertical path until the package test command is green.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --frozen</automated>
  </verify>
  <done>PngDecoder::decode_graya16 is the sole new public eager selector; legal declaration-free and sRGB Type-4/16 literals return existing DecodeResult instances with exact little-endian graya16 component bytes, while the generic decoder returns the frozen RGBA8 high-byte result for the same wire data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green atomic typed admission for the explicit profile</name>
  <files>modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - RED: a GrayAlpha16-profile machine accepts an authenticated incompatible IHDR or colour declaration through pre-IDAT parsing and has no assertion that first-IDAT admission is a typed profile failure with zero lifecycle allocations.
    - GREEN: Type-4/8, Type-0/16, interlaced Type-4/16, Type-4/16 with legacy gAMA/cHRM facts, and Type-4/16 with iCCP facts each fail at first-IDAT admission with the established capability diagnostic for png-decode/graya16-profile.
    - GREEN: every rejected case retains zero private lifecycle allocations, no private outcome/result, and unchanged observable budget limits; valid declaration-free and sRGB Type-4/16 cases remain admitted by the Task 1 route.
  </behavior>
  <action>Begin by extending stream_decode_wbtest.mbt with direct profile-machine fixtures that CRC-authenticate IHDR and applicable colour chunks before presenting the first IDAT header. Per D-02, make PngDecodeMachine::preflight_first_idat validate the already-authenticated profile facts before descriptor, child-budget, OwnedImage, or PngRasterSink construction: require colour type 4, bit depth 16, non-interlaced input, no transparency-key path, and Default or Srgb declaration. Route each mismatch through the established @codec.capability_unavailable diagnostic using operation png-decode and stable context graya16-profile; assert its category, code, operation, and context in the white-box tests. Preserve the normal generic preflight branch unchanged, keep the rejection inside the single existing machine, and make the allocation/lifecycle and budget assertions prove the failure boundary instead of introducing a separate decoding route.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --frozen</automated>
  </verify>
  <done>All explicit-profile mismatches are typed and occur before private image allocation or result visibility; supported Type-4/16 default/sRGB inputs retain the Task 1 eager preservation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PNG bytes to decoder machine | Untrusted IHDR, ancillary colour declarations, compressed bytes, and filtered raster bytes select profile admission and write destination image storage. |
| Reconstructed PNG row to graya16 storage | Fully reconstructed MSB-first source component bytes cross into the existing little-endian component-byte representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Tampering | PngDecodeMachine::preflight_first_idat | high | mitigate | Admit the explicit profile only after CRC-authenticated Type-4/16, non-interlaced, Default/Srgb facts and return the existing typed capability error before descriptor or image allocation. |
| T-62-02 | Tampering | PngRasterSink Type-4/16 completed-row store | medium | mitigate | Retain byte-domain filtering with four source bytes per pixel and write all four reconstructed lanes through component-byte storage in the documented LE order. |
| T-62-03 | Denial of service | explicit first-IDAT allocation path | medium | mitigate | Reuse checked existing output/two-row budgets and prove rejected profiles create no lifecycle allocation or observable result. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --frozen`. The focused suite must cover the new independent public wire literal, the unchanged generic high-byte route, and first-IDAT profile rejection with lifecycle/budget observations.
</verification>

<success_criteria>
GRA16DEC-01 is complete when the explicit eager method returns a normal DecodeResult containing exact LE graya16 component bytes for legal non-interlaced default/sRGB Type-4/16 input, incompatible explicit profiles reject atomically with typed diagnostics, and the generic eager result remains its frozen high-byte RGBA8 representation.
</success_criteria>

<output>
Create `.planning/phases/62-explicit-grayalpha16-decode-contract/62-01-SUMMARY.md` when done.
</output>
