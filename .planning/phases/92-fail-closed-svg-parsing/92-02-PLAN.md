---
phase: 92-fail-closed-svg-parsing
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - modules/mb-svg/svg/color.mbt
  - modules/mb-svg/svg/transform.mbt
  - modules/mb-svg/svg/scene.mbt
  - modules/mb-svg/svg/scene_wbtest.mbt
  - modules/mb-svg/svg/transform_wbtest.mbt
  - modules/mb-svg/svg/svg_test.mbt
autonomous: true
requirements: [SVGPR-02]
must_haves:
  truths:
    - "Every explicit supported paint scalar, dash entry, and consumed RGB/HSL component returns a structured SVG numeric error instead of inheriting, defaulting, or becoming zero."
    - "Transform source parameters use exact SVG arity; unsafe radians, affine coefficients, or compositions fail before their group transform can enter a SceneNode."
    - "Finite scale(0) remains valid and existing group/element opacity lowering behavior is preserved."
    - "Scope assumption: the currently unconsumed fourth rgba()/hsla() alpha component remains outside SVGPR-02 ingress coverage; only the documented three consumed components are admitted."
  artifacts:
    - "Result-propagating color/paint and transform parser seams with policy-compliant CoreError identity."
    - "Focused white-box plus public black-box coverage for paint and transform rejection routes."
  key_links:
    - "build_paint and group/leaf builders propagate explicit scalar failure through parse_svg rather than selecting inherited paint."
    - "parse_transform admits source parameters and every constructed/composed affine coefficient before scene group construction."
---

<objective>
Extend the proven ingress contract to all supported paint scalars and transform construction/composition.

Purpose: Close the remaining explicit attribute fallbacks and prevent unsafe affine/trigonometric results from becoming a scene transform.
Output: Result-based paint/color/transform parsing and four-target route-specific regression evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-fail-closed-svg-parsing/92-CONTEXT.md
@.planning/phases/92-fail-closed-svg-parsing/92-RESEARCH.md
@.planning/phases/92-fail-closed-svg-parsing/92-PATTERNS.md
@.planning/phases/92-fail-closed-svg-parsing/92-01-SUMMARY.md
@docs/policies/svg-numeric-admission.md
@docs/rfcs/0008-mb-canvas-layer.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Propagate paint and color scalar failures through a parsed leaf</name>
  <read_first>Read modules/mb-svg/svg/color.mbt (parse_func_color, parse_component, parse_percent), scene.mbt (build_paint, inherit_double, group and leaf call sites), scene_wbtest.mbt, svg_test.mbt, and docs/policies/svg-numeric-admission.md rows SVG-NUM-PAINT-SCALAR through SVG-NUM-HSL-PERCENT.</read_first>
  <reversibility rating="costly">Result propagation through inherited paint makes explicit numeric rejection part of the observable parser behavior across every shape and group (D-01, D-03).</reversibility>
  <files>modules/mb-svg/svg/color.mbt, modules/mb-svg/svg/scene.mbt, modules/mb-svg/svg/scene_wbtest.mbt, modules/mb-svg/svg/svg_test.mbt</files>
  <behavior>
    - Red: malformed/non-finite/range-invalid explicit opacity, stroke scalar, dash list entry, and each consumed rgb/hsl numeric component fail with the corresponding typed numeric error.
    - Green: explicit scalar failure propagates from build_paint through group and every leaf builder to parse_svg; valid inherited and omitted paint attributes retain prior values.
    - Compatibility: lowercase comma-separated rgb/rgba/hsl/hsla keep their existing first-three-component consumption, and opacity layers remain represented for the unchanged lowerer.
  </behavior>
  <action>Per D-02 through D-04, convert parse_func_color, component/percent conversion, and the currently defaulting double parser to Result-returning admission at each consumed source scalar. Refactor build_paint, inherit_double, dash-list handling, group construction, and all leaf builders to propagate Result exactly as the completed root tracer does. Preserve defaults and inheritance only for attr_get == None; preserve nonnumeric color forms and the documented ignored rgba/hsla alpha boundary without expanding color syntax. Add source/nonfinite/range route tests for paint, dash, RGB numeric/percent, HSL hue/percent, plus controls for omitted/inherited values and existing nested PushLayer/PopLayer ordering per D-07.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>All explicit supported paint/color numeric failures return the policy fields through public parse_svg; absent/inherited controls and the established RFC 0008 opacity operation ordering pass unchanged on all four targets.</acceptance_criteria>
  <done>Paint no longer contains an explicit numeric fallback path, while normal SVG inheritance and all accepted nonnumeric color behavior remain deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make transforms exact-arity and derived-affine safe</name>
  <read_first>Read modules/mb-svg/svg/transform.mbt (parse_transform, build_transform, degrees_to_radians), transform_wbtest.mbt, scene.mbt build_group transform handling, modules/mb-core/math/affine.mbt coefficient/accessor patterns, and docs/policies/svg-numeric-admission.md rows SVG-NUM-TRANSFORM, SVG-NUM-TRIG, and SVG-NUM-AFFINE.</read_first>
  <files>modules/mb-svg/svg/transform.mbt, modules/mb-svg/svg/transform_wbtest.mbt, modules/mb-svg/svg/svg_test.mbt</files>
  <behavior>
    - Every matrix/translate/scale/rotate/skew form accepts only its SVG grammar arity; missing or extra numeric arguments reject before affine construction.
    - Source parameter, degree-to-radian value, all six constructed coefficients, and each composition coefficient satisfy the numeric contract or return svg-numeric-derived.
    - scale(0) and other finite determinant-zero matrices remain Ok; transform values that generate an out-of-envelope coefficient do not yield a group SceneNode.
  </behavior>
  <action>Per D-01, D-02, and D-05, retain parse_transform's early Err control flow while making build_transform enforce exact permitted arities for matrix, translate, scale, rotate, skewX, and skewY. Admit lexical arguments through the completed shared helper, then admit degree-to-radian output, all six Affine2 construction results, and each composition result before it can return. Do not use inverse availability or determinant as an admission condition. Add table-driven transform white-box cases for missing/extra args, source/nonfinite/range failures, trigonometric and composition-derived overflow, and finite scale(0); expose a compact public group-transform rejection fixture in svg_test.mbt.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Malformed or unsafe transform input has no successful parsed group, every accepted transform has admitted source and derived coefficients, and the finite singular-transform control still parses and lowers on js, wasm, wasm-gc, and native.</acceptance_criteria>
  <done>Transform parsing cannot carry an over-arity, unsafe trigonometric, or unsafe composed affine into the scene, without changing the scale(0) contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG presentation/transform attributes -> parser | Untrusted paint and transform strings resolve to inherited style state and Affine2 values. |
| parsed group/leaf -> validated SceneNode | Result propagation determines whether inherited/default state or an error reaches the public caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-04 | Tampering | color.mbt and scene paint inheritance | high | mitigate | Admit every consumed explicit scalar and propagate its Result through group and leaf builders; absence alone can inherit (D-02, D-04). |
| T-92-05 | Tampering | transform.mbt affine construction/composition | high | mitigate | Require exact arity and admit source, radians, constructed coefficients, and compositions before group creation (D-01, D-02). |
| T-92-06 | Denial of service | transform trigonometry | medium | mitigate | Fail derived out-of-envelope/non-finite results before a scene exists while retaining the existing parser budget regime. |
| T-92-07 | Tampering | opacity/layer seam | medium | mitigate | Leave lower_to_drawing_list and RFC 0008 PushLayer/PopLayer ownership unchanged; regression-test layer ordering (D-07). |
| T-92-13 | Tampering | ASVS L1 V5 input validation | high | mitigate | Apply ASVS L1 V5 by admitting explicit source and derived numeric values before parser-produced SceneNode publication, returning typed CoreError with no partial scene or DrawingList on rejection, and verifying public/internal controls on js, wasm, wasm-gc, and native. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target all --frozen`, ensuring public error fixtures, paint/inheritance controls, and transform derived-value tables execute across all four production targets.
</verification>

<success_criteria>
Supported paint and transform scalar routes fail closed with stable structured errors; their omission, inheritance, finite singular-transform, and layer compatibility controls remain valid.
</success_criteria>

<output>
Create `.planning/phases/92-fail-closed-svg-parsing/92-02-SUMMARY.md` when done.
</output>
