---
phase: 92-fail-closed-svg-parsing
plan: 03
type: execute
wave: 3
depends_on: [92-02]
files_modified:
  - modules/mb-svg/svg/path_data.mbt
  - modules/mb-svg/svg/scene.mbt
  - modules/mb-svg/svg/path_data_wbtest.mbt
  - modules/mb-svg/svg/lower_wbtest.mbt
  - modules/mb-svg/svg/numeric_contract_wbtest.mbt
  - modules/mb-svg/svg/svg_test.mbt
autonomous: true
requirements: [SVGPR-02]
must_haves:
  truths:
    - "Every direct, relative, arc, and normalized smooth path scalar either produces admitted path coordinates/controls or returns a structured SVG numeric error before parse_svg returns."
    - "S/s consumes four values and T/t consumes two; reflected controls use only the compatible immediately preceding curve state and every reflection/relative result is admitted."
    - "Unsafe viewBox, accumulated affine, transformed geometry, rounded-rect arithmetic, and circle/ellipse sampling fail during parser-side preflight, before a drawing list could be created."
    - "Finite scale(0), omitted attributes, and existing RFC 0008 opacity/layer behavior remain valid."
    - "Assumption boundary: SVGPR-02 governs parser-produced SceneNode values only; manually constructed public SceneNode values continue to use the total lower_to_drawing_list contract under D-07."
  artifacts:
    - "Result-returning path scanner with exact smooth-command grammar and checked source/derived coordinates."
    - "Parser-side validated-scene preflight that covers deterministic lowerer derivations without creating a DrawingList."
    - "Public and internal four-target evidence for no-scene/no-drawing-list rejection and valid compatibility controls."
  key_links:
    - "parse_path_data_with_budget -> Result scanner -> admitted CanvasPath -> scene builder -> parser preflight -> Ok(SceneNode) only after all required source and derived checks."
    - "parse_svg -> parser-side validation walk; lower_to_drawing_list remains a total consumer and is invoked only with a validated successful scene."
---

<objective>
Finish fail-closed SVG admission by normalizing path grammar and preflighting every parser-owned derived geometry route.

Purpose: Prevent a valid-looking source scalar from producing unsafe relative, reflected, affine, viewBox, or generated geometry after lexical parsing.
Output: Exact smooth-path parser behavior, validated-scene preflight, and complete public/internal all-target SVGPR-02 evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-fail-closed-svg-parsing/92-CONTEXT.md
@.planning/phases/92-fail-closed-svg-parsing/92-RESEARCH.md
@.planning/phases/92-fail-closed-svg-parsing/92-PATTERNS.md
@.planning/phases/92-fail-closed-svg-parsing/92-02-SUMMARY.md
@docs/policies/svg-numeric-admission.md
@docs/rfcs/0002-mb-svg.md
@docs/rfcs/0008-mb-canvas-layer.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Normalize and fail-close all path command numeric routes</name>
  <read_first>Read modules/mb-svg/svg/path_data.mbt, path_data_wbtest.mbt, svg_test.mbt, docs/policies/svg-numeric-admission.md rows SVG-NUM-PATH, SVG-NUM-RELATIVE, and SVG-NUM-SMOOTH-PENDING, plus 92-CONTEXT.md D-06.</read_first>
  <files>modules/mb-svg/svg/path_data.mbt, modules/mb-svg/svg/path_data_wbtest.mbt, modules/mb-svg/svg/svg_test.mbt</files>
  <behavior>
    - Direct M/L/H/V/C/Q/A path arguments reject malformed, non-finite, and range-invalid source tokens rather than becoming zero coordinates.
    - Direct relative m/l/h/v/c/q/a additions, arc endpoints, and smooth reflected controls reject an unsafe derived coordinate before CanvasPath is returned.
    - S/s consumes exactly cp2x, cp2y, x, y; T/t consumes exactly x, y; reflection is enabled only after the matching prior cubic or quadratic command family.
    - Missing or extra command-group values return a structured source error, while existing finite path controls retain their expected command shape.
  </behavior>
  <action>Per D-02, D-03, and D-06, change read_number from its fallback tuple into Result[(Double, Int), CoreError] and thread Err through every command branch before any CanvasPath can be returned. Admit every lexical token with the shared helper and admit each direct relative addition, curve control, arc endpoint, and smooth reflection result. Replace the current S/s six-read branch with its four-value grammar and the T/t branch with its two-value grammar; track command-family eligibility so a reflected control is used only after C/c/S/s or Q/q/T/t respectively, otherwise use the SVG fallback control at the current point. Preserve existing budget charges and CanvasPath operations. Add focused tables for source/nonfinite/range errors, relative overflow, arc overflow, malformed/missing/extra smooth data, correct S/s/T/t reflection and finite command output; add one public parse_svg path fixture proving error identity and no successful scene branch.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Every supported path command family either returns an admitted CanvasPath or a policy-defined Err; S/s/T/t follow exact grammar and compatible reflection, and no malformed path is converted into zero/partial geometry on all four targets.</acceptance_criteria>
  <done>Path parsing is Result-propagating and smooth semantics are normalized sufficiently to include their source and derived numeric routes in the Phase 92 guarantee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preflight all derived scene geometry before total lowering</name>
  <read_first>Read modules/mb-svg/svg/scene.mbt parse_svg_with_budget and SceneNode shape definitions; read-only anchors modules/mb-svg/svg/lower.mbt (lower_node, viewbox_transform, rect_path, circle_path, ellipse_path), modules/mb-core/math/path.mbt apply_to_path, lower_wbtest.mbt, numeric_contract_wbtest.mbt, and RFC 0008 §§5, 7, 10.</read_first>
  <reversibility rating="costly">The validation-walk coverage defines which parser-produced scene arithmetic is admissible before the public SceneNode boundary (D-01, D-02).</reversibility>
  <files>modules/mb-svg/svg/scene.mbt, modules/mb-svg/svg/lower_wbtest.mbt, modules/mb-svg/svg/numeric_contract_wbtest.mbt, modules/mb-svg/svg/svg_test.mbt</files>
  <behavior>
    - A source-valid document whose viewBox scale/translation, group composition, or transformed coordinate exceeds the envelope returns svg-numeric-derived from parse_svg.
    - Parser-side checks cover rounded-rect ratios/clamps and generated circle/ellipse sampling scalars before successful scene return.
    - Rejected documents have no SceneNode and are never passed to lower_to_drawing_list; successful inclusive boundary/scale(0)/opacity fixtures retain current drawing-list output.
  </behavior>
  <action>Per D-01, D-02, D-05, and D-07, add a private parser-side validated-scene walk invoked in parse_svg_with_budget immediately before its sole Ok(SceneNode) return. Mirror the deterministic root viewBox, accumulated root/group affine, transformed-coordinate/control, rounded-rect, and circle/ellipse sampling calculations from lower.mbt, admitting every intermediate/result with the shared derived helper. The walk must not allocate a DrawingList or alter lower_to_drawing_list's signature, lowering policy, PushLayer/PopLayer ordering, or the 16-layer canvas boundary. Add lower-facing tests that first require Ok(scene) before calling the unchanged total lowerer and reject fixtures with unsafe viewBox/affine/transformed/round-rect/ellipse derivations by matching Err. Retain public valid-boundary, omitted/default, scale(0), and nested-opacity controls. Document the parser-produced-SceneNode boundary directly in test naming or assertions without adding a new lowerer API.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>All route-matrix derived values are admitted before parse_svg exposes a scene; derived-invalid fixtures cannot reach lowering, while valid finite fixtures produce the same typed drawing-list compatibility controls on js, wasm, wasm-gc, and native.</acceptance_criteria>
  <done>The parser is the complete no-scene/no-drawing-list numeric boundary for explicit and derived SVG values, and lower/canvas remain total, behavior-preserving consumers of validated scenes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG path data -> CanvasPath | Untrusted command grammar and scalar tokens become mutable path coordinates and control points. |
| parsed scene -> parser-side validator | Source-admitted geometry is combined through viewBox, affine, and generated-shape arithmetic. |
| validated scene -> lower_to_drawing_list | Lowering consumes only a successful parser result; its canvas layer semantics remain outside numeric admission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-08 | Tampering | path_data.mbt scanner and smooth state | high | mitigate | Result-returning reads, exact arity, eligible reflection state, and source/derived admission prevent malformed coordinates becoming path commands (D-02, D-03, D-06). |
| T-92-09 | Denial of service | relative/affine/generated geometry | high | mitigate | Admit every arithmetic result before scene return while retaining Budget work/depth limits; no drawing list allocation occurs on Err (D-01, D-02). |
| T-92-10 | Tampering | scene preflight/lower boundary | high | mitigate | Validate viewBox, accumulated transforms, rounded rects, and sampling in scene.mbt; do not defer parser safety to total lower_to_drawing_list (D-07). |
| T-92-11 | Repudiation | SVG numeric errors | low | mitigate | Test typed CoreError category/code/operation/context on every public rejection family rather than diagnostic rendering (D-04). |
| T-92-14 | Tampering | ASVS L1 V5 input validation | high | mitigate | Apply ASVS L1 V5 by admitting explicit source and derived numeric values before parser-produced SceneNode publication, returning typed CoreError with no partial scene or DrawingList on rejection, and verifying public/internal controls on js, wasm, wasm-gc, and native. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target all --frozen`. The final package gate must exercise public parse-error cases, focused source/derived route tables, valid boundary controls, scale(0), omitted/default behavior, and nested opacity drawing-list ordering across js, wasm, wasm-gc, and native.
</verification>

<success_criteria>
SVGPR-02 is complete: every documented explicit source and derived numeric route is fail-closed before scene publication, while declared parser/default, singular-transform, lowerer, and canvas-layer compatibility boundaries remain intact.
</success_criteria>

<output>
Create `.planning/phases/92-fail-closed-svg-parsing/92-03-SUMMARY.md` when done.
</output>
