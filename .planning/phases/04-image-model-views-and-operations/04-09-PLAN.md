---
phase: 04-image-model-views-and-operations
plan: "09"
type: execute
wave: 9
depends_on: [04-08]
gap_closure: true
files_modified: [modules/mb-image/storage/owned_image.mbt, modules/mb-image/storage/views.mbt, modules/mb-image/storage/storage_test.mbt, modules/mb-image/storage/storage_wbtest.mbt, modules/mb-image/ops/copy_flip.mbt, modules/mb-image/ops/orientation.mbt, modules/mb-image/ops/resize.mbt, modules/mb-image/ops/convert.mbt, modules/mb-image/ops/copy_flip_test.mbt, modules/mb-image/ops/orientation_test.mbt, modules/mb-image/ops/resize_convert_test.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1]
autonomous: true
requirements: [IMAG-02, IMAG-03, IMAG-04, IMAG-05]
must_haves:
  truths:
    - "No public image-allocation interface accepts caller-forgeable width, height, pixels, or ResourceCharge."
    - "Operation allocation derives dimensions/pixels from the validated descriptor and charges them once with storage and operation work."
    - "OwnedImage::view() preserves its exact public ImageView return; planar full views expose descriptor/metadata only, while byte/pixel/crop access returns stable CapabilityUnavailable before backing access."
    - "Reordered-plane view failures preserve bytes, leases, metadata, and budgets exactly."
  artifacts:
    - path: modules/mb-image/storage/owned_image.mbt
      provides: "Descriptor-derived atomic operation allocation"
    - path: modules/mb-image/storage/views.mbt
      provides: "Fail-closed packed-U8 view eligibility gate"
    - path: modules/mb-image/storage/storage_wbtest.mbt
      provides: "Counter, byte, lease, and reordered-plane regressions"
  key_links:
    - from: modules/mb-image/ops/copy_flip.mbt
      to: modules/mb-image/storage/owned_image.mbt
      via: "descriptor plus work-only allocation seam"
    - from: modules/mb-image/storage/views.mbt
      to: modules/mb-image/model/descriptor.mbt
      via: "supported packed-U8 predicate before backing authority"
---

<objective>
Close both confirmed Phase 4 verification blockers without narrowing general descriptor representation.

Purpose: Remove caller-forgeable budget dimensions and prevent unsupported planar byte-pixel access/leases.
Output: Corrected storage/ops interfaces, regressions, exact negatives, and green four-target Required qualification.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-image-model-views-and-operations/04-VERIFICATION.md
@.planning/phases/04-image-model-views-and-operations/04-CONTEXT.md
@.planning/phases/04-image-model-views-and-operations/04-RESEARCH.md
@.planning/phases/04-image-model-views-and-operations/04-08-SUMMARY.md
@modules/mb-core/bytes/pkg.generated.mbti
@modules/mb-image/model/pkg.generated.mbti
@modules/mb-image/storage/pkg.generated.mbti
@modules/mb-image/ops/pkg.generated.mbti
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Derive atomic operation charges from validated descriptors</name>
  <files>modules/mb-image/storage/owned_image.mbt, modules/mb-image/storage/storage_test.mbt, modules/mb-image/storage/storage_wbtest.mbt, policy/foundation.json</files>
  <behavior>
    - Public interface contains no operation constructor accepting width, height, pixels, or ResourceCharge.
    - Internal `new_operation` derives width/height/pixels from the output descriptor and accepts only operation work before one atomic bytes allocation.
    - Success consumes descriptor-derived dimensions/pixels, work, bytes, allocation, and allocation-size exactly once.
    - Arithmetic, unsupported, allocator, and independently underfunded budget failures leave all counters unchanged.
  </behavior>
  <action>Make `OwnedImage::new_operation` non-public/internal. Remove public width/height/pixels/ResourceCharge parameters and derive dimensions only from the validated output descriptor; accept only operation work. If cross-package access is required, expose a safe descriptor-plus-work factory whose signature has no charge scalars. Preserve narrowing → allocator approval → one combined charge → allocation. Update exact interface/policy and black-box/white-box counter tests.</action>
  <verify><automated>moon -C modules/mb-image test storage --target all --frozen</automated></verify>
  <done>No scalar-forging seam exists and descriptor-derived charging commits once or not at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve full views while rejecting planar byte and mutable authority</name>
  <files>modules/mb-image/storage/views.mbt, modules/mb-image/storage/storage_test.mbt, modules/mb-image/storage/storage_wbtest.mbt, policy/foundation.json</files>
  <behavior>
    - General planar and reordered disjoint-plane descriptors remain constructible/inspectable.
    - Exact public `OwnedImage::view() -> ImageView` remains unchanged; a planar full view safely exposes descriptor and metadata without touching backing bytes.
    - `ImageView::get_byte` and every pixel/crop accessor that could touch backing return stable CapabilityUnavailable before ByteView access; existing planar crop rejection remains intact.
    - `OwnedImage::with_mut_view` rejects unsupported planar layout before `OwnedBytes::with_mut`; no lease is acquired, and `MutImageView` accessors retain a defensive layout gate.
    - Failures leave backing bytes, metadata, budgets, and lease availability unchanged; immediate supported/reacquisition succeeds.
  </behavior>
  <action>Preserve the exact public `OwnedImage::view() -> ImageView` interface. A full planar view may expose only descriptor/metadata safely. Invoke one shared descriptor eligibility gate inside `ImageView::get_byte` and every byte/pixel/crop accessor before any ByteView access; keep the existing planar crop rejection. Gate `OwnedImage::with_mut_view` before `OwnedBytes::with_mut` so unsupported planar layouts acquire no lease, and retain the same defensive gate in `MutImageView` accessors. Keep planar validation intact and never fabricate row-major offsets for reordered planes. Add ordinary/reordered planar full-view regressions with sentinels, budget snapshots, metadata equality, and post-rejection lease reacquisition. Do not introduce a Result-returning view or migrate callers for view construction.</action>
  <verify><automated>moon -C modules/mb-image test storage --target all --frozen</automated></verify>
  <done>Unsupported planar views fail before authority with no observable change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate operation allocation calls and close exact regression gates</name>
  <files>modules/mb-image/ops/copy_flip.mbt, modules/mb-image/ops/orientation.mbt, modules/mb-image/ops/resize.mbt, modules/mb-image/ops/convert.mbt, modules/mb-image/ops/copy_flip_test.mbt, modules/mb-image/ops/orientation_test.mbt, modules/mb-image/ops/resize_convert_test.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - Copy/flips/orientation/resize/conversions call only descriptor-plus-work allocation and retain exact outputs/metadata.
    - Unsupported planar inputs may obtain descriptor/metadata-only full views but fail before byte/pixel access, mutable lease, operation allocation, or charge; arithmetic/allocator/budget failures preserve counters.
    - Exact interface/negative gates preserve `OwnedImage::view() -> ImageView`, reject scalar-forging seams, and reject planar byte access/lease before CapabilityUnavailable.
  </behavior>
  <action>Update only operation allocation calls to pass validated descriptor plus operation work; remove scalar dimensions/pixels and duplicate charge derivation. Do not migrate view construction or introduce Result-view handling. Keep operation format checks first, rely on the preserved ImageView accessors' defensive gate, and preserve generated cases. Extend policy/quality classifiers and synthetic negatives for both gaps, including an exact assertion that `OwnedImage::view() -> ImageView` remains public and unchanged.</action>
  <verify><automated>moon -C modules/mb-image test ops --target all --frozen; pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>All callers use the safe seam and both regressions fail closed on every target.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-G1 | Tampering | allocation charge inputs | high | mitigate | Descriptor-derived dimensions/pixels remove forgeable scalars. |
| T-04-G2 | Information Disclosure | planar view | high | mitigate | Eligibility gate returns before backing access. |
| T-04-G3 | Elevation of Privilege | mutable planar lease | high | mitigate | Gate returns before lease acquisition. |
| T-04-G4 | Denial of Service | operation allocation | high | mitigate | One atomic charge precedes allocation/loop. |
</threat_model>

<verification>Run storage/ops tests on js, wasm, wasm-gc, native, exact interface negatives, and complete Required with tracked-read-only proof.</verification>
<success_criteria>Both blockers in 04-VERIFICATION.md have public-interface, behavioral, counter, byte, lease, policy, and all-target closure evidence.</success_criteria>
<output>Create `.planning/phases/04-image-model-views-and-operations/04-09-SUMMARY.md`.</output>
