---
phase: 04-image-model-views-and-operations
plan: "04"
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - modules/mb-image/ops/moon.pkg
  - modules/mb-image/ops/copy_flip.mbt
  - modules/mb-image/ops/copy_flip_test.mbt
  - modules/mb-image/ops/copy_flip_wbtest.mbt
  - policy/foundation.json
autonomous: true
requirements: [IMAG-05, IMAG-06]
must_haves:
  truths:
    - "Copy and horizontal/vertical flips produce fresh deterministic images for exactly the three supported packed U8 formats."
    - "Logical row bytes, not padding, determine output; source aliasing cannot affect traversal."
    - "Metadata and stored-coordinate orientation are preserved with an explicit disposition."
  artifacts:
    - path: modules/mb-image/ops/copy_flip.mbt
      provides: "Fresh-output copy and exact flip permutations"
  key_links:
    - from: modules/mb-image/ops/copy_flip.mbt
      to: modules/mb-image/storage/views.mbt
      via: "ImageView source and fresh OwnedImage result"
    - from: modules/mb-image/ops/copy_flip.mbt
      to: modules/mb-image/metadata/metadata.mbt
      via: "preserve-all MetadataDisposition"
---

<objective>
Create the operation spine with alias-safe copy and exact stored-coordinate flips.

Purpose: Implement the first deterministic D-14/D-15 operations and establish shared format gating, output allocation, budget, and metadata-disposition patterns for later algorithms.
Output: A policy-registered `mb-image/ops` package with four-target copy/flip evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-image-model-views-and-operations/04-CONTEXT.md
@.planning/phases/04-image-model-views-and-operations/04-RESEARCH.md
@.planning/phases/04-image-model-views-and-operations/04-03-SUMMARY.md
@modules/mb-core/budget/pkg.generated.mbti
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fresh copy and flip permutations</name>
  <files>modules/mb-image/ops/moon.pkg, modules/mb-image/ops/copy_flip.mbt, modules/mb-image/ops/copy_flip_test.mbt, modules/mb-image/ops/copy_flip_wbtest.mbt</files>
  <behavior>
    - Copy creates tightly packed independent output from padded Rgb8, straight Rgba8, and premultiplied Rgba8 sources and never copies padding.
    - Horizontal flip maps `(x,y)` to `(width-1-x,y)` and vertical flip maps `(x,y)` to `(x,height-1-y)` exactly for 1xN, Nx1, and multi-row images.
    - Planar/U16/F32/unknown operation inputs fail with operation-specific `CapabilityUnavailable` context before allocation or work charge.
    - Copy/flips preserve metadata/profile/color/orientation and report preserve-all disposition.
  </behavior>
  <action>Add the closed U8 dispatcher and fresh copy/flips per D-03/D-14/D-15/D-19. Validate support/geometry, compute explicit UInt64 width, height, pixels, and work, then call Plan 03's exact scalar factory `(length, budget, allocator, width, height, pixels, work)` so mb-core constructs one charge before allocation/loop. Never construct/pass `ResourceCharge` or separately charge. Failure counters remain unchanged; success consumes once. Copy logical row bytes only and keep orientation.</action>
  <verify><automated>moon -C modules/mb-image test ops --target all --frozen</automated></verify>
  <done>Copy/flips have exact pixel, padding, unsupported-format, fresh-backing, budget, and metadata disposition tests.</done>
</task>

<task type="auto">
  <name>Task 2: Register the initial operations interface</name>
  <files>policy/foundation.json</files>
  <action>Add `ops` after storage with exact files, interface, targets, and inward imports only. Its public signatures accept `ImageView`, explicit `Budget`, and return fresh `OwnedImage` plus machine-readable metadata disposition; no host, codec, filesystem, or root facade imports are permitted.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>The initial operations package passes all-target focused tests and exact Required qualification.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-10 | Tampering | aliased copy traversal | high | mitigate | Every operation allocates a distinct result before writes; no in-place seam exists. |
| T-04-11 | Information Disclosure | padded rows | medium | mitigate | Loops copy only descriptor logical row bytes into tight output. |
| T-04-12 | Denial of Service | operation work | high | mitigate | Support/geometry validation and work preflight precede allocation/iteration. |
</threat_model>

<verification>Run copy/flip tests on all targets and Required; verify unsupported layouts fail with bounded operation tokens and unchanged budgets.</verification>
<success_criteria>Deterministic fresh copy and exact flips satisfy their IMAG-05 slice and establish metadata disposition behavior required by IMAG-06.</success_criteria>
<output>Create `.planning/phases/04-image-model-views-and-operations/04-04-SUMMARY.md` when done.</output>
