---
phase: 96-svg-boundary-parity-qualification
plan: 02
type: execute
wave: 2
depends_on: [96-01]
files_modified:
  - modules/mb-svg/svg/lower_wbtest.mbt
  - modules/mb-svg/svg/portable_qualification_wbtest.mbt
autonomous: true
requirements: [SVGUNI-03]
must_haves:
  truths:
    - "Manually constructed invalid SceneNode values keep the total lowerer's deterministic identity or empty-path recovery and never masquerade as a parser success."
    - "RFC 0008 group and element opacity preserve exact DrawOp layer order, semantic raster behavior, and the bounded layer-depth contract."
    - "The parity and compatibility suite succeeds through the frozen wasm, wasm-gc, js, and native package command."
  artifacts:
    - path: modules/mb-svg/svg/lower_wbtest.mbt
      provides: Deterministic manual-invalid lowerer fallback and operation-order qualification.
    - path: modules/mb-svg/svg/portable_qualification_wbtest.mbt
      provides: Target-neutral opacity/layer semantic raster and depth-bound controls.
  key_links:
    - from: modules/mb-svg/svg/lower_wbtest.mbt
      to: modules/mb-svg/svg/lower.mbt
      via: lower_to_drawing_list observes private identity/empty-path adapters on manual invalid values.
      pattern: "lower_to_drawing_list"
    - from: modules/mb-svg/svg/portable_qualification_wbtest.mbt
      to: modules/mb-svg/svg/lower.mbt and mb-canvas rasterizer
      via: parsed SVG lowers to DrawOps and renders to semantic pixel assertions.
      pattern: "(PushLayer|PopLayer|render)"
---

<objective>
Qualify total manual-scene fallback and RFC 0008 compatibility with a frozen four-target semantic gate.

Purpose: Preserve the deliberate distinction between fail-closed parser rejection and total lowerer recovery while proving target-neutral layer/opacity semantics, per D-02, D-04, and D-05.
Output: Deterministic manual-invalid DrawOp controls and portable semantic raster controls; no production or public-API changes.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-svg-boundary-parity-qualification/96-CONTEXT.md
@.planning/phases/96-svg-boundary-parity-qualification/96-RESEARCH.md
@.planning/phases/96-svg-boundary-parity-qualification/96-PATTERNS.md
@.planning/phases/95-shared-svg-geometry-boundary/95-02-SUMMARY.md
@.planning/phases/95-shared-svg-geometry-boundary/95-VERIFICATION.md
@modules/mb-svg/svg/lower_wbtest.mbt
@modules/mb-svg/svg/portable_qualification_wbtest.mbt
@modules/mb-svg/svg/lower.mbt
@docs/rfcs/0008-mb-canvas-layer.md

<interfaces>
From `lower.mbt`: `lower_to_drawing_list(node : SceneNode) -> @canvas.DrawingList` remains total. Private adapters turn invalid manual root transform facts into identity and invalid manual paths into `@math.Path2::new()`.

From RFC 0008 and existing qualification tests: group layers order around children, element layers wrap both Fill and Stroke, and a 16-deep layer stack succeeds while the next depth is rejected by the existing structured behavior.

From `modules/mb-svg/moon.mod.json`: `supported-targets` declares js, wasm, wasm-gc, and native; use one frozen package command to exercise all four.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Distinguish manual-invalid total recovery from parser-boundary rejection</name>
  <files>modules/mb-svg/svg/lower_wbtest.mbt</files>
  <behavior>
    - A valid parsed fixture supplies paint, then explicitly constructed invalid SvgRoot, Rect, or CanvasPath values lower twice to identical observable DrawingList output.
    - Invalid paths are empty and invalid roots use the established identity behavior where applicable; preserved PushLayer, Fill/Stroke, and PopLayer order remains observable.
    - This test never treats a parser `Err` as an empty drawing-list result.
  </behavior>
  <action>Extend `lower_wbtest.mbt` with manual-value-only controls, per D-02. Start from a valid parsed fixture to obtain existing paint, then explicitly construct invalid root and primitive/path SceneNode values outside parser provenance. Lower each invalid scene twice and assert deterministic identity/empty-path sentinels plus exact DrawOp sequencing, including layer wrapping where the fixture carries opacity. Keep this behavior visibly separate from Plan 96-01's `parse_svg(...) -> Err(CoreError)` controls: the total lowerer has no new error path or public API. Retain valid viewBox/default and finite scale(0) DrawOp forms as compatibility guards instead of adding a new fallback policy.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target native --frozen</automated>
  </verify>
  <done>Manual invalid values deterministically lower through the existing identity or empty-path adapters, retain required DrawOp/layer ordering, and remain distinct from fail-closed parser errors.</done>
  <reversibility rating="reversible">Qualification assertions are test-only and preserve an existing total-lowerer contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve RFC 0008 semantic opacity behavior through the frozen all-target gate</name>
  <files>modules/mb-svg/svg/portable_qualification_wbtest.mbt</files>
  <behavior>
    - Group and element opacity produce the established PushTransform, PushLayer, Fill/Stroke, PopLayer, and PopTransform sequence with the expected opacity values.
    - Opaque and transparent raster targets retain deterministic semantic pixel assertions for the RFC 0008 fixture.
    - Sixteen nested layers retain the existing success behavior and the next depth retains structured failure on every supported target.
  </behavior>
  <action>Extend the portable qualification test using the existing operation-sequence and pixel-inspection patterns, per D-04 and D-05. Preserve group and element opacity as isolated layer semantics by asserting the exact DrawOp order and opacity values, then retain semantic pixels on opaque and transparent destinations. Keep the existing bounded 16-layer success and next-depth structured-failure controls. Run the complete `mb-svg/svg` package with `--target all --frozen`; do not introduce target-specific fixtures, snapshots, timing comparisons, new scripts, or package changes.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <done>Operation, pixel, and layer-depth controls preserve RFC 0008 behavior and the frozen package suite passes on wasm, wasm-gc, js, and native.</done>
  <reversibility rating="reversible">All changes are deterministic test controls over established portable behavior.</reversibility>
</task>

</tasks>

<fallback>
For manually constructed invalid values only, assert the existing total-lowerer identity or empty-path recovery through DrawingList observations. Never route parser failures into this fallback and never add a public recovery/error API.
</fallback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public manually constructed SceneNode → total lowerer | Values can bypass parser admission and must recover deterministically without panic or publication reinterpretation. |
| DrawingList → rasterizer | Layer operations allocate bounded offscreen state and must preserve semantic compositing across targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-96-03 | Tampering | manual SceneNode total lowering | medium | mitigate | Task 1 proves deterministic identity/empty-path observations and exact layer order without conflating them with parser success. |
| T-96-04 | Denial of Service | RFC 0008 layer stack | medium | mitigate | Task 2 retains the 16-depth success and next-depth structured-failure semantic controls in the portable suite. |
| T-96-05 | Tampering | cross-target SVG qualification | medium | mitigate | Task 2 runs the frozen all-target command and asserts operations and pixels rather than timing or target internals. |
| T-96-SC | Tampering | package installation | low | accept | No package-manager installation is planned; the frozen MoonBit test command reuses locked dependencies. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target native --frozen` after Task 1 and `moon test modules/mb-svg/svg --target all --frozen` after Task 2. The latter is the Phase 96 acceptance command.
</verification>

<success_criteria>
- Manually invalid SceneNode data has deterministic DrawingList fallback evidence without changing `lower_to_drawing_list` or public errors.
- Parser errors remain a separate fail-closed behavior covered by the preceding tracer plan.
- RFC 0008 DrawOp order, semantic raster pixels, and depth bounds are explicit portable regressions.
- The frozen all-target package command passes for wasm, wasm-gc, js, and native.
</success_criteria>

<output>
Create `.planning/phases/96-svg-boundary-parity-qualification/96-02-SUMMARY.md` when done.
</output>
