---
phase: 96-svg-boundary-parity-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-svg/svg/svg_test.mbt
  - modules/mb-svg/svg/geometry_wbtest.mbt
autonomous: true
requirements: [SVGUNI-02]
must_haves:
  truths:
    - "Unsafe explicit and derived SVG geometry is observed as the established structured parser error before a parser-produced SceneNode can be lowered or rasterized."
    - "The private checked geometry seam and the public parser agree on derived-boundary failure semantics without a duplicate arithmetic oracle."
    - "Finite scale(0), compact viewBox mapping, and omitted-default valid SVG routes remain accepted."
  artifacts:
    - path: modules/mb-svg/svg/svg_test.mbt
      provides: Public fail-closed parser regression fixtures with structured-error assertions.
    - path: modules/mb-svg/svg/geometry_wbtest.mbt
      provides: Direct Result-based private checked-seam parity matrix.
  key_links:
    - from: modules/mb-svg/svg/svg_test.mbt
      to: modules/mb-svg/svg/scene.mbt
      via: exported parse_svg returns Err(CoreError) before SceneNode publication.
      pattern: "assert_public_numeric_error\\(parse_svg"
    - from: modules/mb-svg/svg/geometry_wbtest.mbt
      to: modules/mb-svg/svg/geometry.mbt
      via: direct checked_* Result observations for the same numeric boundary.
      pattern: "checked_(viewbox_transform|compose_affine|transform_point|rect_path|canvas_path)"
---

<objective>
Establish the public-parser and private-geometry-seam tracer for SVG numeric-boundary parity.

Purpose: Make unsafe source or derived geometry fail at the published parser boundary while detecting a future divergence from the shared checked seam, per D-01 and D-03.
Output: Deterministic public parser and package-private Result controls; no production or public-API changes.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-svg-boundary-parity-qualification/96-CONTEXT.md
@.planning/phases/96-svg-boundary-parity-qualification/96-RESEARCH.md
@.planning/phases/96-svg-boundary-parity-qualification/96-PATTERNS.md
@.planning/phases/95-shared-svg-geometry-boundary/95-VERIFICATION.md
@modules/mb-svg/svg/svg_test.mbt
@modules/mb-svg/svg/geometry_wbtest.mbt
@modules/mb-svg/svg/geometry.mbt
@modules/mb-svg/svg/scene.mbt

<interfaces>
From `modules/mb-svg/svg/svg.mbt` and `scene.mbt`: `parse_svg(source : String) -> Result[SceneNode, @error.CoreError]` is the public publication boundary; `Err` proves no parser-produced SceneNode is available to lower.

From `geometry.mbt`: package-private `checked_viewbox_transform`, `checked_compose_affine`, `checked_transform_point`, primitive/path checked builders, `checked_canvas_path`, and `checked_path_points` return Result facts. Their existing derived-boundary error context is `svg-numeric-derived`.

From `svg_test.mbt`: `assert_public_numeric_error` asserts Data category, `InvalidRange`/other established code, operation `svg`, and exact context through public `CoreError` accessors.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one unsafe derived route from exported parser rejection to its checked-seam counterpart</name>
  <files>modules/mb-svg/svg/svg_test.mbt, modules/mb-svg/svg/geometry_wbtest.mbt</files>
  <behavior>
    - A source-valid viewBox translation or transformed rectangle that crosses the derived envelope returns the established `InvalidRange` error with the derived numeric context from `parse_svg`.
    - The paired direct checked geometry fact returns the same observable derived-error context.
    - A finite scale(0) control still returns an accepted collapsed point and valid public SVG can be lowered only inside an Ok(scene) branch.
  </behavior>
  <action>Add one table-oriented end-to-end parity tracer using the existing public error helper in `svg_test.mbt` and the direct Result style in `geometry_wbtest.mbt`. Pair a derived-overflow source fixture with the checked viewBox, affine, or transformed-point fact that crosses the same boundary; assert structured facts at the public parser boundary per D-01, then assert the Result error context through the private seam per D-03. Keep parser failure and lowering mutually exclusive: the negative fixture stops at `Err`, while the positive finite `scale(0)` fixture lowers only after `Ok(scene)`. Reuse existing compact viewBox/default fixtures rather than adding a new parser, numeric policy, public error field, test harness, or arithmetic reimplementation.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target native --frozen</automated>
  </verify>
  <done>A derived-overflow SVG is proven to fail with the established public structured error before publication, its checked fact exposes the matching derived outcome, and finite scale(0) remains a successful public route.</done>
  <reversibility rating="reversible">Test-only semantic fixtures can be refined locally without changing a public contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the parity matrix across every shared geometry family</name>
  <files>modules/mb-svg/svg/svg_test.mbt, modules/mb-svg/svg/geometry_wbtest.mbt</files>
  <behavior>
    - Public derived-error rows cover root mapping, accumulated group affine plus child coordinate, rounded rectangle, ellipse extrema, and path or point-list coordinates.
    - Direct seam rows cover checked root mapping, affine composition, transformed points, primitive paths, point-list builders, CanvasPath traversal, and post-construction path-point admission.
    - Valid viewBox/default and scale(0) sentinels remain explicit positive controls.
  </behavior>
  <action>Extend the tracer into a deterministic row-per-family semantic matrix, per D-03 and D-05. In `svg_test.mbt`, retain source-facing numeric ingress rows and add or consolidate derived fixtures for viewBox translation, group-affine child coordinates, rounded-rect edges, circle/ellipse extrema, and path/point-list coordinates; every such row must assert the established structured parser error. In `geometry_wbtest.mbt`, call the corresponding `checked_*` facts directly and inspect Result semantics and supported command forms. Do not compare source text, wall-clock behavior, snapshots, or duplicated formulas; the shared checked function is the oracle. Keep explicit valid `scale(0)`, compact viewBox mapping, and omitted-default assertions so qualification cannot convert compatibility cases into rejection.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target native --frozen</automated>
  </verify>
  <done>Every supported transform, viewBox, primitive, point-list, and path geometry family has deterministic public and/or direct-seam boundary evidence, with explicit valid singular-transform and default/viewBox regressions.</done>
  <reversibility rating="reversible">The test matrix adds no production behavior and can be extended or narrowed with local fixture edits.</reversibility>
</task>

</tasks>

<fallback>
Parser-boundary failures have no scene, DrawingList, or raster fallback: assert `Err(CoreError)` only. Total recovery belongs exclusively to manually constructed invalid SceneNode values and is covered by Plan 96-02 per D-02.
</fallback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG source → `parse_svg` | Untrusted numeric text becomes a public `SceneNode` only after checked admission. |
| Test → package-private checked seam | Qualification observes the same Result facts without exposing new public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-96-01 | Tampering / Denial of Service | `parse_svg` derived geometry admission | high | mitigate | Tasks 1-2 assert the established structured error before parser publication for each adversarial geometry family. |
| T-96-02 | Tampering | parser / checked-seam semantic boundary | medium | mitigate | Tasks 1-2 pair public error observations with direct Result semantics, without duplicate arithmetic or timing controls. |
| T-96-SC | Tampering | package installation | low | accept | No package-manager installation is planned; the frozen MoonBit test command reuses locked dependencies. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target native --frozen` during each task. Plan 96-02 runs the frozen all-target package gate after its expansion controls are added.
</verification>

<success_criteria>
- Unsafe explicit and derived SVG fixtures return the established structured parser error before public scene publication.
- Direct checked-seam tests observe matching semantic boundary results for every shared geometry family.
- Finite scale(0), compact viewBox mapping, and omitted defaults remain explicit successful controls.
- No production source, public signature, numeric envelope, or error schema is modified.
</success_criteria>

<output>
Create `.planning/phases/96-svg-boundary-parity-qualification/96-01-SUMMARY.md` when done.
</output>
