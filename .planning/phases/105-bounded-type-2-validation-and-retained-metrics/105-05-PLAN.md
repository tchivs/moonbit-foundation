---
phase: 105-bounded-type-2-validation-and-retained-metrics
plan: "05"
type: execute
wave: 5
depends_on:
  - "105-04"
files_modified:
  - modules/mb-font/font/cff_type2_fixed.mbt
  - modules/mb-font/font/cff_type2_fixed_wbtest.mbt
  - modules/mb-font/font/cff_type2_bounds.mbt
  - modules/mb-font/font/cff_type2_bounds_wbtest.mbt
  - modules/mb-font/font/cff_type2.mbt
  - modules/mb-font/font/cff_type2_fixture_wbtest.mbt
autonomous: true
gap_closure: true
requirements:
  - T2-01
  - T2-02
  - CFF-03
must_haves:
  truths:
    - "The project-owned xorshift32 random operator emits locked Q16.16 raw high16(state)+1 values and its golden vectors no longer mask a low-bit implementation."
    - "Every non-empty contour contributes its start point to the conservative hull, while moveto-only and endchar-only glyphs remain empty bounds."
    - "Every VM non-State error, including number/escape/fetch/EOF decode failures, passes through one final revision-preference seam so concurrent mutation always wins as State."
  artifacts:
    - "cff_type2_fixed.mbt and its white-box tests implement and freeze the high16 PRNG mapping."
    - "cff_type2_bounds.mbt tracks first segment per contour and its tests cover a later contour-start extreme."
    - "cff_type2.mbt centralizes guarded error return and its fixture tests mutate after the loop guard but before a failing fetch/decode."
  key_links:
    - "Private DICT seed → Type2Prng::next → random operator stack result uses exactly the context D-04 mapping."
    - "moveto/start → first line or cubic in that contour → transformed hull includes the start before retained outward rounding."
    - "all decode/fetch/operator failure paths → type2_error_with_revision → State/Resource/Capability/Data result."
---

<objective>
Close all three Phase 105 verifier blockers without widening the phase boundary.

Purpose: Restore the locked deterministic random contract, truthful multi-contour bounds, and universal mutation precedence required by T2-01, T2-02, and CFF-03.
Output: Three focused RED/GREEN fixes, complete native regression, all-target check, and a gap-closure summary.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-CONTEXT.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-RESEARCH.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-VERIFICATION.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-04-SUMMARY.md
@modules/mb-font/font/cff_type2_fixed.mbt
@modules/mb-font/font/cff_type2_bounds.mbt
@modules/mb-font/font/cff_type2.mbt
</context>

<edge_assumptions>
1. A-01: xorshift32 state transition is already correct; only output-bit selection and frozen golden data change.
2. A-02: A contour start enters the hull only when that contour emits its first segment, preserving None for moveto-only glyphs.
3. A-03: The start point must be transformed through the same exact matrix before hull comparison.
4. A-04: Mutation precedence is enforced on every non-State return, not only operator dispatch or GID boundaries.
5. A-05: Deterministic test probes may inject mutation between a loop guard and byte fetch but must not ship as a public API.
</edge_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct and refreeze deterministic random output</name>
  <files>modules/mb-font/font/cff_type2_fixed.mbt, modules/mb-font/font/cff_type2_fixed_wbtest.mbt</files>
  <read_first>
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-VERIFICATION.md
    @modules/mb-font/font/cff_type2_fixed.mbt
    @modules/mb-font/font/cff_type2_fixed_wbtest.mbt
  </read_first>
  <behavior>
    - Seed 0x12345678 transitions first to 0x87985AA5 and emits raw 0x8798+1 = 34713.
    - Subsequent golden outputs use high16(state)+1, remain in 1..65536, and reset/order-independence tests pass.
    - Zero seed normalization and state transition remain unchanged.
  </behavior>
  <action>
    Change the existing golden test first so the current low16 implementation fails for the exact locked sequence. Then change `Type2Prng::next` to select the unsigned high 16 state bits before adding one and returning Q16.16 raw. Do not change the xorshift transition, seed normalization, reset boundary, or random operator plumbing.
  </action>
  <acceptance_criteria>
    - The verifier's seed/state example emits 34713.
    - Golden tests fail before and pass after the one semantic fix.
    - Existing PRNG isolation/range tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>moon test modules/mb-font/font --target native -j 2</automated>
  </verify>
  <done>T2-01 random behavior matches context D-04 exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Include every non-empty contour start in conservative bounds</name>
  <files>modules/mb-font/font/cff_type2_bounds.mbt, modules/mb-font/font/cff_type2_bounds_wbtest.mbt</files>
  <read_first>
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-VERIFICATION.md
    @modules/mb-font/font/cff_type2_bounds.mbt
    @modules/mb-font/font/cff_type2_bounds_wbtest.mbt
  </read_first>
  <behavior>
    - A second contour whose moveto is outside all later endpoints/control points retains that moveto extreme in the final bound.
    - Both line-first and cubic-first later contours include their transformed start.
    - Empty, endchar-only, and moveto-only glyphs still finalize to None.
  </behavior>
  <action>
    Add failing multi-contour line and cubic tests whose second contour returns inward, asserting exact outward bounds rather than only counters. Track whether the current contour has emitted a segment. Immediately before the first line/cubic hull update for each contour, include the saved contour start through the same transformed fixed-point path; do not include it at moveto time.
  </action>
  <acceptance_criteria>
    - Later contour-start extrema cannot be omitted.
    - No moveto-only glyph becomes non-empty.
    - Existing geometry counts, transforms, and outward rounding remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>moon test modules/mb-font/font --target native -j 2</automated>
  </verify>
  <done>CFF-03 retained bounds are conservative for every contour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route all VM errors through final revision precedence</name>
  <files>modules/mb-font/font/cff_type2.mbt, modules/mb-font/font/cff_type2_fixture_wbtest.mbt</files>
  <read_first>
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-VERIFICATION.md
    @modules/mb-font/font/cff_type2.mbt
    @modules/mb-font/font/cff_type2_fixture_wbtest.mbt
    @modules/mb-font/font/cff_admission_wbtest.mbt
  </read_first>
  <behavior>
    - Mutation injected after a loop-entry guard but before a truncated number or escaped-operator fetch returns State, not Data.
    - Without mutation, the same truncated programs retain their stable Data contexts.
    - Resource still wins Capability/Data when revision is unchanged; State wins every simultaneous non-State failure.
  </behavior>
  <action>
    Add a private deterministic VM read probe usable only by white-box fixtures and inject a content-preserving revision change between the loop guard and a failing number/escape fetch. Replace all direct non-State returns in the execute loop with one guarded error helper or a `Result` value flowing through the common exit. Audit root/subr EOF, number decode, escape decode, raw-byte fetch, mask fetch, and operator dispatch. Preserve existing encounter order and error contexts.
  </action>
  <acceptance_criteria>
    - No non-State VM error path bypasses `type2_error_with_revision`.
    - Mid-fetch mutation fixtures return State while no-mutation twins return Data.
    - Full native tests and all-target check pass.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native -j 2 &amp;&amp; moon check --target all</automated>
  </verify>
  <done>T2-02 mutation precedence holds for every VM error exit.</done>
</task>

</tasks>

<verification>
- `moon test --target native -j 2`
- `moon check --target all`
</verification>

<success_criteria>
- All three blockers in 105-VERIFICATION.md have direct failing-before/passing-after regressions.
- T2-01, T2-02, and CFF-03 are no longer blocked.
- The existing atomic admission transaction and public/static-glyf regressions remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-05-SUMMARY.md`.
</output>

