---
phase: 105-bounded-type-2-validation-and-retained-metrics
plan: "03"
type: execute
wave: 3
depends_on:
  - "105-02"
files_modified:
  - modules/mb-font/font/cff_type2.mbt
  - modules/mb-font/font/cff_type2_bounds.mbt
  - modules/mb-font/font/cff_type2_wbtest.mbt
  - modules/mb-font/font/cff_type2_bounds_wbtest.mbt
  - modules/mb-font/font/cff_type2_fixture_wbtest.mbt
autonomous: true
requirements:
  - T2-01
  - T2-02
  - CFF-03
must_haves:
  truths:
    - "Every supported Type 2 line, curve, and flex form emits equivalent checked cubic geometry through the same VM."
    - "Contours close deterministically and geometry ceilings fail before partial retained state escapes."
    - "Every GID produces one truthful compact optional bound after the exact effective matrix."
  artifacts:
    - "cff_type2_bounds.mbt implements a fixed-point conservative geometry sink and compact bound finalization."
    - "cff_type2.mbt dispatches all path/flex operators to the sink."
    - "cff_type2_bounds_wbtest.mbt freezes operator lowering, transforms, closure, and outward rounding."
  key_links:
    - "The VM's sole operator switch drives the bounds sink now and the Phase 106 path sink later."
    - "Plan 01 matrix facts transform endpoints/control points before compact bound rounding."
    - "All-glyph execution returns staged bounds and charge without publishing AdmittedCff1."
---

<objective>
Complete Type 2 geometry semantics and retain truthful conservative bounds for every GID.

Purpose: Convert fully validated programs into compact admission-time geometry facts without retaining Path2 or doing hidden query-time work.
Output: Fixed bounds sink, all line/curve/flex lowering, contour lifecycle, and staged all-glyph results.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-CONTEXT.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-RESEARCH.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-02-SUMMARY.md
@modules/mb-font/font/cff_type2.mbt
@modules/mb-font/font/cff_type2_fixed.mbt
@modules/mb-font/font/metrics.mbt
@modules/mb-core/math/path.mbt
</context>

<assumption_delta_decision>
Primary noun: Type 2 geometry sink.
Decision: promote a sink protocol with a Phase 105 conservative-bounds implementation.
Variants: validation/bounds now and cubic Path2 publication in Phase 106.
Rationale: one VM must validate and later render identical geometry while retaining only compact facts during admission.
</assumption_delta_decision>

<edge_assumptions>
1. A-01: All coordinates are relative checked Q16.16 until transformed; current point begins at zero.
2. A-02: A new moveto closes any open contour; legal endchar closes the final contour.
3. A-03: Curves contribute endpoints and both control points to the conservative hull; exact extrema are unnecessary.
4. A-04: Flex lowers to exactly two cubic curves and six geometry points.
5. A-05: Bounds are transformed before outward rounding; empty glyphs retain None.
6. A-06: Every glyph starts with fresh VM/geometry state and is visited in ascending GID order.
7. A-07: Points, contours, commands, allocations, and work are staged and bounded before any retained array escapes.
</edge_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement checked contour, line, and cubic lowering</name>
  <files>modules/mb-font/font/cff_type2.mbt, modules/mb-font/font/cff_type2_bounds.mbt, modules/mb-font/font/cff_type2_wbtest.mbt, modules/mb-font/font/cff_type2_bounds_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/cff_type2.mbt
    @modules/mb-font/font/outline.mbt
    @modules/mb-core/math/path.mbt
  </read_first>
  <behavior>
    - r/h/vmoveto, rlineto, hlineto, vlineto, rrcurveto, rcurlineto, rlinecurve, vv/hh/vh/hvcurveto obey exact arity, alternation, and relative current-point semantics.
    - Geometry before moveto, invalid residual arguments, coordinate overflow, or point/command/contour exhaustion fails without publishing a partial bound.
    - New moveto/endchar closure produces the locked command/point/contour charge.
  </behavior>
  <action>
    Add a private sink protocol and fixed bounds sink with staged accumulator state. Implement every line/cubic path operator in the sole VM switch, using checked current-point updates and per-operator preflight. Count Move/Line/Curve/Close and points according to context. Write table-driven hand-derived fixtures first, including alternating h/v forms, optional final arguments, multi-segment operators, no-current-contour failures, and exact/one-short geometry limits.
  </action>
  <acceptance_criteria>
    - Every non-flex Type 2 path operator has golden endpoint/control-point and invalid-arity coverage.
    - Contour closure and geometry charges are deterministic.
    - No geometry failure leaks a completed bound or mutable sink state.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>All ordinary Type 2 path construction is represented as checked cubic geometry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lower flex forms and finalize transformed conservative bounds</name>
  <files>modules/mb-font/font/cff_type2.mbt, modules/mb-font/font/cff_type2_bounds.mbt, modules/mb-font/font/cff_type2_bounds_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/cff_type2_fixed.mbt
    @modules/mb-font/font/metrics.mbt
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-RESEARCH.md
  </read_first>
  <behavior>
    - flex, hflex, hflex1, and flex1 lower to exactly two cubics with correct inferred axis and final point.
    - Name Top-only, CID FD+Top, and CID omitted-FD transforms match hand-derived font-unit coordinates.
    - Minima floor and maxima ceil after transformation; empty/no-segment glyphs return None.
  </behavior>
  <action>
    Implement the four flex forms through the same cubic sink calls and exact six-point charge. Transform every line endpoint and cubic endpoint/control point with Plan 01 exact matrix helpers, accumulate a control hull, and finalize checked `GlyphBoundsFacts`. Add positive/negative fractional, translation, scale, explicit/omitted FD, large coordinate, empty glyph, moveto-only, and overflow fixtures. Do not allocate or retain Path2/commands.
  </action>
  <acceptance_criteria>
    - Flex golden vectors match two explicit rrcurveto equivalents.
    - Bounds always enclose all transformed endpoints/control points and never round inward.
    - Retained output is exactly one optional compact bound per glyph.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>The bounds sink produces truthful compact transformed extents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stage ascending all-GID execution and combined VM facts</name>
  <files>modules/mb-font/font/cff_type2.mbt, modules/mb-font/font/cff_type2_bounds.mbt, modules/mb-font/font/cff_type2_fixture_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/cff_keying.mbt
    @modules/mb-font/font/cff_admission.mbt
    @modules/mb-font/font/cff_type2_fixture_wbtest.mbt
  </read_first>
  <behavior>
    - Every descriptor executes in ascending GID order with fresh stack/transient/frame/PRNG/geometry state.
    - The smallest failing GID is the stable failure site and discards the entire staged bounds array.
    - Successful traversal returns exact aggregate VM/bounds charge plus one bound slot per GID, but commits no budget.
  </behavior>
  <action>
    Add an all-glyph staging entry point over Phase 104 descriptors. Allocate the compact bounds array only after checked authority, aggregate named execution/geometry facts with checked addition, and route all errors through the revision guard. Add multi-GID fixtures proving fresh state, PRNG reset/order independence, CID-local environment selection, first-failing GID, zero partial output, exact aggregate charge, and no caller budget mutation.
  </action>
  <acceptance_criteria>
    - The same VM handles every GID and returns complete staged results only on total success.
    - Bounds cardinality equals CharStrings/maxp glyph count.
    - Failure publishes no array and changes no budget.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>All-glyph validation and retained metrics are ready to join the admission transaction.</done>
</task>

</tasks>

<verification>
- `moon test --target native`
- `moon check --target native`
</verification>

<success_criteria>
- T2-01 line/curve/flex semantics are complete.
- T2-02 geometry/resource ceilings are deterministic and atomic.
- CFF-03 has truthful compact staged bounds for every GID.
</success_criteria>

<output>
After completion, create `.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-03-SUMMARY.md`.
</output>

