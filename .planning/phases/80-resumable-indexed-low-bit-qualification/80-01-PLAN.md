---
phase: 80-resumable-indexed-low-bit-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [INDEXLOW-04, INDEXLOW-05]
must_haves:
  truths:
    - "A caller can select Indexed Type-3 depth One, Two, or Four and receive the same PNG bytes through caller-owned leases as eager encoding (D-01, D-02)."
    - "For every selected depth, hostile zero-capacity, one-byte, and ragged leases commit only accepted bytes, preserve unused caller storage, and replay completed and failed terminals without later-lease mutation (D-03)."
    - "Rejected selected-depth construction has no observable budget charge; Indexed8 behavior and independent Type-3 wire/decode evidence remain part of the portable qualification (D-04, D-05)."
  artifacts:
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "PngChunkEncoder::new_indexed selected-depth caller-buffered factory over the shared profile-aware machine"
      exports: ["PngChunkEncoder::new_indexed"]
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "All-depth hostile lease, sticky terminal, atomic admission, and eager-parity qualification"
  key_links:
    - from: "PngChunkEncoder::new_indexed"
      to: "PngEncodeMachine::new_with_indexed_profile"
      via: "PngIndexedBitDepth One/Two/Four mapping and existing Active machine initialization"
      pattern: "new_with_indexed_profile"
    - from: "png_stream_indexed_low_bit_hostile_drain"
      to: "PngEncoder::encode_indexed"
      via: "test-local eager oracle compares collected lease bytes at each selected depth"
      pattern: "encode_indexed"
---

<objective>
Expose Phase 79's low-bit Indexed PNG machine through the existing caller-owned chunk encoder and qualify its lifecycle without creating a second output path.

Purpose: Complete resumable Type-3/1, /2, and /4 encoding with the same atomic admission, acknowledged byte progression, and portable evidence as the eager route.
Output: One additive public factory plus targeted stream qualification in the existing PNG package.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-resumable-indexed-low-bit-qualification/80-CONTEXT.md
@.planning/phases/80-resumable-indexed-low-bit-qualification/80-RESEARCH.md
@.planning/phases/79-indexed-low-bit-eager-packing/79-01-SUMMARY.md
@.planning/phases/79-indexed-low-bit-eager-packing/79-VERIFICATION.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end Indexed2 caller lease through the shared low-bit machine</name>
  <reversibility rating="costly">Per D-01, this additive public lease factory becomes a consumer-facing lifecycle contract, so changing its signature later would coordinate downstream callers.</reversibility>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A `PngChunkEncoder::new_indexed` request for `PngIndexedBitDepth::Two` returns an active encoder whose collected caller-lease bytes equal a test-local `PngEncoder::encode_indexed` oracle.
    - An initial zero-capacity `Z`-filled lease reports `NeedOutput` with zero progress and unchanged storage; subsequent 1/3/2/5-byte leases reach `Finished` with accepted-only totals and untouched tails.
  </behavior>
  <action>Add public `PngChunkEncoder::new_indexed(source : PngIndexedImage, bit_depth : PngIndexedBitDepth, limits : @codec.CodecLimits, budget : @budget.Budget, diagnostics : @error.Diagnostics)` adjacent to `new_indexed8`. Per D-01 and the closest eager spelling, map `One`, `Two`, and `Four` to `PngIndexedWireProfile::{One,Two,Four}`, call `PngEncodeMachine::new_with_indexed_profile`, propagate its error unchanged, and initialize `PngChunkEncoderState::Active(machine)` with `total_written: 0UL`. Per D-02, retain `PngChunkEncoder::pull`, `PngEncodeMachine`, and the fixed-Eight `new_indexed8` route as their existing shared implementation. In `stream_encode_test.mbt`, add `png_stream_indexed_low_bit_eager(source, bit_depth)` using public eager encoding and add `test "PNG Indexed2 chunk tracer retains eager bytes"`; create the selected-depth chunk encoder, exercise a zero-capacity sentinel lease then the 1/3/2/5 schedule, collect exactly `written()` bytes, compare them to the eager oracle, and inspect every lease tail.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>`PngChunkEncoder::new_indexed` exposes the selected low-bit API, and a Type-3/2 PNG completes through caller-owned leases with eager-identical bytes and acknowledged ownership behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Qualify all selected depths, admission, compatibility, and portable independent evidence</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Expand the tracer helpers into `png_stream_indexed_low_bit_source`, `png_stream_indexed_low_bit_hostile_drain`, and `png_stream_indexed_low_bit_released_failure` (or the closest existing test-local naming) so each of `PngIndexedBitDepth::{One,Two,Four}` is tested with schedules `[0UL, 1UL]`, `[1UL]`, and `[0UL, 1UL, 3UL, 2UL, 5UL]`. Per D-03, assert zero-capacity progress, accepted-only `total_written`, `Z` sentinel tails, eager byte equality, and a later fresh sentinel lease that receives a zero-write `Finished`; release the first one-byte lease for every depth and require the same cached failure, unchanged totals, and untouched later storage. Per D-04, add selected-depth output, pixel, and work-budget rejection cases using remaining-budget snapshots before and after construction; each rejected `new_indexed` result must be `Err` before any lease can be pulled. Preserve and execute the existing `new_indexed8` tracer, hostile-drain, released-lease, and atomic-admission tests as the fixed-Eight compatibility proof. Per D-05, keep `encode_test.mbt`'s literal Type-3 IHDR/Stored/CRC vectors and public RGB8/RGBA8 decoder assertions as the independent oracle; stream tests may compare only against the public eager result and must not use production packing helpers to manufacture expected bytes. Use the ordinary frozen PNG package command as the sole portability gate across wasm, wasm-gc, js, and native.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>All One/Two/Four chunk paths pass hostile lifecycle, sticky-terminal, and atomic-admission proof; Indexed8 compatibility and literal independent Type-3/decode evidence remain exercised by the ordinary four-target package run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned `MutByteLease` to `PngChunkEncoder::pull` | A caller controls capacity and lease validity while encoder state must advance only after an accepted write. |
| public selected bit depth to profile-aware machine | A bounded external selection determines Type-3 frame facts, palette admission, and resource accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-01 | Tampering / Denial of service | `PngChunkEncoder::new_indexed` | high | mitigate | Map the finite selector directly to `PngEncodeMachine::new_with_indexed_profile`, preserving checked selected-depth preflight and its single budget charge (D-01, D-02, D-04). |
| T-80-02 | Tampering | `PngChunkEncoder::pull` with caller leases | high | mitigate | Reuse the acknowledged machine; all-depth sentinel, accepted-progress, released-lease, and sticky-terminal tests prove state and later buffers are protected (D-03). |
| T-80-03 | Tampering | Type-3 output qualification | medium | mitigate | Retain literal wire/CRC plus public decode evidence independently of production packing and run it under every supported target (D-05). |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target all --frozen`. The command must pass for wasm, wasm-gc, js, and native while exercising the additive selected-depth chunk tests, preserved Indexed8 routes, and retained independent eager vectors.
</verification>

<success_criteria>
- `PngChunkEncoder::new_indexed` is the only added selected-depth caller-buffered factory and delegates to the Phase 79 profile-aware machine.
- Every low-bit depth emits eager-identical Type-3 bytes under zero, one-byte, and ragged caller leases, with acceptance and terminal guarantees observable in tests.
- Rejected selected-depth construction preserves budget state, and fixed Indexed8 and legacy evidence pass unchanged in the complete portable PNG package gate.
</success_criteria>

## Source Audit

| Source | ID | Requirement / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Caller-owned low-bit Indexed output has eager-identical bytes, sticky terminals, and portable evidence | 80-01 | COVERED |
| REQ | INDEXLOW-04 | Shared bounded machine, hostile capacities, lease ownership, sticky terminals | 80-01 Task 1–2 | COVERED |
| REQ | INDEXLOW-05 | Independent wire/decode proof, Indexed8 compatibility, four targets | 80-01 Task 2 | COVERED |
| RESEARCH | — | Selector maps to profile-aware constructor; no duplicate traversal, CRC, or admission path | 80-01 Task 1 | COVERED |
| RESEARCH | — | All-depth schedules, released-lease replay, atomic selected-depth admission | 80-01 Task 2 | COVERED |
| CONTEXT | D-01 | Additive selected-depth chunk factory | 80-01 Task 1 | COVERED |
| CONTEXT | D-02 | One profile-aware machine and preflight facts | 80-01 Task 1 | COVERED |
| CONTEXT | D-03 | All-depth hostile lifecycle and terminal proof | 80-01 Task 2 | COVERED |
| CONTEXT | D-04 | Atomic selected-depth preflight and frozen Indexed8 behavior | 80-01 Task 2 | COVERED |
| CONTEXT | D-05 | Independent evidence and ordinary four-target gate | 80-01 Task 2 | COVERED |

<output>
Create `.planning/phases/80-resumable-indexed-low-bit-qualification/80-01-SUMMARY.md` when done.
</output>
