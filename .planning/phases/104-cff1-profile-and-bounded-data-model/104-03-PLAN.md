---
phase: 104-cff1-profile-and-bounded-data-model
plan: "03"
type: execute
wave: 3
depends_on:
  - 104-01
  - 104-02
files_modified:
  - modules/mb-font/font/limits.mbt
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/directory.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/cff_admission.mbt
  - modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
  - modules/mb-font/font/collection_wbtest.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/collection_test.mbt
  - modules/mb-font/font/font_test.mbt
autonomous: true
requirements:
  - CFF-01
  - CFF-02
must_haves:
  truths:
    - "CFF-01: structural ceilings, exact work/allocation accounting, caller authority, error precedence, mutation guards, and one final commit bound the complete CFF1 structural transaction."
    - "CFF-02: the entire per-GID descriptor set is either retained atomically or absent, including exact and one-short hostile outcomes at every structural stage."
    - "Standalone and selected collection admission use the same CFF table-local transaction while collection table records remain root-relative."
    - "Only a complete admitted outline source can cross the private promotion invariant, and existing static-glyf behavior remains frozen."
  artifacts:
    - "modules/mb-font/font/limits.mbt derives private CFF structural ceilings from the compatible non-zero FontLimits contract."
    - "modules/mb-font/font/cff_admission.mbt owns exact structural charge calculation, final revision validation, and one private-fact commit."
    - "modules/mb-font/font/cff_hostile_fixture_wbtest.mbt contains the exact/one-short structural, work, allocation, authority, mutation, and precedence matrix with private atomicity assertions."
    - "modules/mb-font/font/collection_wbtest.mbt proves private non-zero-root and shared-CFF collection admission authority."
    - "modules/mb-font/font/font_wbtest.mbt proves only complete admitted CFF facts can cross the private outline-source promotion invariant."
    - "modules/mb-font/font/collection_test.mbt and modules/mb-font/font/font_test.mbt freeze only existing public glyf collection and font behavior."
  key_links:
    - "tables.mbt stages common facts and CFF structural facts under one ledger; cff_admission.mbt commits only after the final source-revision check."
    - "directory.mbt preserves root-relative TTC records and passes one checked table-local CFF window to the same admission transaction used by standalone data."
    - "font.mbt accepts only a complete closed private outline-source value; no partially parsed CFF state can be promoted or observed."
---

<objective>
Close Phase 104 with bounded atomic admission, hostile exact/one-short evidence, collection offset authority, a closed outline-source promotion invariant, and frozen static-glyf compatibility.

Purpose: Make the private CFF1 structural/keying result safe and deterministic under hostile bytes, limits, budgets, mutation, and TTC/OTC rebasing without exposing a public CFF font or path.
Output: Exact admission accounting, stable precedence, atomic private publication, collection-authority fixtures, and glyf compatibility proofs.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/104-cff1-profile-and-bounded-data-model/104-CONTEXT.md
@.planning/phases/104-cff1-profile-and-bounded-data-model/104-01-SUMMARY.md
@.planning/phases/104-cff1-profile-and-bounded-data-model/104-02-SUMMARY.md
@modules/mb-font/font/limits.mbt
@modules/mb-font/font/tables.mbt
@modules/mb-font/font/directory.mbt
@modules/mb-font/font/font.mbt
</context>

## Artifacts this phase produces

- `modules/mb-font/font/limits.mbt`: private `CffStructuralLimits` derivation from existing `FontLimits`.
- `modules/mb-font/font/tables.mbt`: integration of common-table and CFF structural facts under the existing staged admission ledger.
- `modules/mb-font/font/directory.mbt`: standalone/collection routing that preserves root-relative SFNT records and creates one checked table-local CFF window.
- `modules/mb-font/font/cff_admission.mbt`: `CffStructuralCharge`, exact preflight, final mutation guard, and atomic private-fact commit.
- `modules/mb-font/font/cff_hostile_fixture_wbtest.mbt`: white-box `cff_structural_exact_and_one_short_cases`, private retained-fact/charge atomicity assertions, and multi-fault precedence fixtures.
- `modules/mb-font/font/collection_wbtest.mbt`: white-box non-zero-directory, root-relative-record, table-local-CFF, shared-CFF-range, and private collection admission fixtures.
- `modules/mb-font/font/font.mbt`: closed private outline-source promotion constructor/invariant with CFF1 facts remaining non-public in Phase 104.
- `modules/mb-font/font/font_wbtest.mbt`: white-box complete-CFF promotion and partial/mixed outline-source rejection assertions.
- `modules/mb-font/font/collection_test.mbt`: frozen existing public static-`glyf` collection compatibility fingerprints only.
- `modules/mb-font/font/font_test.mbt`: frozen existing public static-`glyf` font compatibility fingerprints only.

<edge_assumptions_closed>
- A-01 is closed by stable capability classification before semantic CFF traversal.
- A-02 is closed by standalone/collection equivalence with root-relative SFNT records and table-relative CFF internals.
- A-03 and A-04 are closed by exact/one-short Header and every INDEX family boundary.
- A-05 and A-06 are closed by exact/one-short DICT forms, typed-schema failures, and duplicate-singleton precedence.
- A-07 is closed by exact and mismatched `maxp`/CharStrings/charset/FDSelect cardinality fixtures.
- A-08 is closed by exact-versus-one-short work, allocation, caller-budget, and mutation cases proving one commit or no publication/charge.
</edge_assumptions_closed>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace selected collection CFF1 facts through one bounded atomic admission transaction</name>
  <files>modules/mb-font/font/limits.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/directory.mbt, modules/mb-font/font/cff_admission.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    @.planning/phases/104-cff1-profile-and-bounded-data-model/104-CONTEXT.md
    @.planning/phases/104-cff1-profile-and-bounded-data-model/104-02-SUMMARY.md
    @modules/mb-font/font/limits.mbt
    @modules/mb-font/font/tables.mbt
    @modules/mb-font/font/directory.mbt
    @modules/mb-font/font/collection_wbtest.mbt
  </read_first>
  <behavior>
    - A selected face at a non-zero collection directory offset resolves root-relative table records into one checked shared CFF table window and the same table-local admission transaction used by standalone data.
    - Exact structural limits and caller budget admit and commit once; one-less work, allocation, or caller-budget authority fails without retained CFF facts or committed charge.
    - A source revision change before final commit returns the state error and publishes/charges nothing.
  </behavior>
  <action>
    In `collection_wbtest.mbt`, write a failing selected-collection tracer covering a non-zero directory offset, shared CFF range, private admitted facts, and committed charge; do not place these private assertions in `collection_test.mbt` or `font_test.mbt`. Derive private non-zero CFF structural ceilings from existing `FontLimits` without changing established public semantics. Stage common-table and complete CFF structural/keying facts under the existing ledger, charging Header bytes, every INDEX offset/object, every DICT token/operator, charset/Encoding entry, FD range, SID/CID lookup, and retained compact allocation. Use checked arithmetic and exact preflight before traversal, narrowing, or allocation. Preserve root-relative collection table records, create exactly one checked table-local CFF window, run the same admission function as standalone, perform the final source-revision guard, commit one charge, and only then retain private facts.
  </action>
  <acceptance_criteria>
    - Standalone and selected collection routes converge on one `admit_cff1_structure` transaction.
    - Exact authority succeeds; one-less work, allocation, or caller budget returns the stable resource outcome with zero committed charge.
    - Mutation before final commit wins as a state failure and leaves no admitted facts.
    - Private selected-collection admission, retained facts, and committed-charge assertions live in `collection_wbtest.mbt`; public black-box collection tests contain only existing glyf compatibility behavior.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>The collection tracer proves offset authority, exact accounting, mutation safety, and one-commit publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the complete structural exact/one-short and multi-fault hostile matrix</name>
  <files>modules/mb-font/font/limits.mbt, modules/mb-font/font/cff_admission.mbt, modules/mb-font/font/cff_hostile_fixture_wbtest.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    @.planning/phases/104-cff1-profile-and-bounded-data-model/104-CONTEXT.md
    @modules/mb-font/font/limits.mbt
    @modules/mb-font/font/cff_admission.mbt
    @modules/mb-font/font/cff_index.mbt
    @modules/mb-font/font/cff_dict.mbt
    @modules/mb-font/font/cff_keying.mbt
    @modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
    @modules/mb-font/font/collection_wbtest.mbt
  </read_first>
  <behavior>
    - Exact and one-byte-short fixtures cover Header, Name/Top/String/Global-Subrs/CharStrings/FDArray/local-Subrs INDEX regions, DICT operands/operators, charset, Encoding/supplements, ROS, FDSelect 0/3, Private DICT, and subroutine data.
    - Exact and one-less resource fixtures cover every derived structural ceiling, work unit, retained allocation, and caller-budget boundary.
    - Multi-fault precedence is state/revision and invalid caller authority, then resource limits, then recognized capability boundaries, then malformed supported-profile data.
    - Every failure retains no descriptor set and commits no charge.
  </behavior>
  <action>
    In `cff_hostile_fixture_wbtest.mbt`, build one generated hostile fixture matrix, reusing production parsers as subjects but never as expected-value oracles. For every named structural region, pair the exact valid extent with a one-byte-short variant and assert the precise error stage. Pair every semantic ceiling and accounting total with exact and one-less values, including cumulative work, retained allocation, and caller budget. Add multi-fault fixtures that lock the context-specified precedence order across state/revision, caller authority, resource exhaustion, recognized unsupported profile, and malformed supported CFF data. Assert white-box atomic absence of private facts and zero committed charge for all failures, including failures in the final glyph or FD; keep collection-specific private admission coverage in `collection_wbtest.mbt`, never in public `collection_test.mbt` or `font_test.mbt`.
  </action>
  <acceptance_criteria>
    - The hostile matrix contains an explicit exact and one-short pair for every requested structural family.
    - The resource matrix contains an explicit exact and one-less pair for each structural limit and charge dimension.
    - Multi-fault outcomes follow the frozen precedence and never expose partial descriptors or charges.
    - All retained-fact and charge atomicity assertions are white-box coverage in `cff_hostile_fixture_wbtest.mbt` or `collection_wbtest.mbt`.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>CFF-01/CFF-02 hostile structural, resource, mutation, and precedence behavior is reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce closed outline-source promotion and freeze static-glyf compatibility</name>
  <files>modules/mb-font/font/font.mbt, modules/mb-font/font/directory.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    @.planning/phases/104-cff1-profile-and-bounded-data-model/104-CONTEXT.md
    @modules/mb-font/font/font.mbt
    @modules/mb-font/font/directory.mbt
    @modules/mb-font/font/tables.mbt
    @modules/mb-font/font/font_wbtest.mbt
    @modules/mb-font/font/collection_test.mbt
    @modules/mb-font/font/font_test.mbt
  </read_first>
  <behavior>
    - The closed private outline-source model can represent `Glyf` or a complete admitted `Cff1` fact set; no constructor accepts partial profile, parser, or per-GID state.
    - Phase 104 CFF1 structural success remains private and cannot be returned through the public `Font::open`, outline, bounds, or path workflows.
    - Existing standalone and collection static-glyf acceptance/rejection, errors, charges, metrics, mappings, kerning, and paths retain their frozen results.
    - Mixed outline profiles never promote to either outline-source case.
  </behavior>
  <action>
    Refine the private outline-source representation into a closed `Glyf | Cff1` model whose CFF1 constructor requires the complete atomically admitted structural/keying fact set. Keep the public CFF promotion and path routes outside this phase: directory recognition may produce private CFF facts for white-box admission tests, but public opening must not expose a CFF-backed `Font`. In `font_wbtest.mbt`, assert complete-CFF promotion and prove mixed or partial CFF states cannot cross the private invariant. In public `collection_test.mbt` and `font_test.mbt`, preserve and freeze only the existing Glyf constructors, charge formulas, errors, metrics, mappings, kerning, outlines, and collection behavior; do not inspect private CFF facts or private outline-source construction from those black-box tests.
  </action>
  <acceptance_criteria>
    - There is one closed private outline-source type and no independently constructible partial CFF1 source.
    - Public CFF-backed `Font`, bounds, and path behavior is absent while private Phase 104 facts remain testable.
    - Frozen glyf tests pass with unchanged semantic records and resource charges.
    - Complete/partial/mixed private outline-source assertions live in `font_wbtest.mbt`, while `collection_test.mbt` and `font_test.mbt` exercise only the existing public glyf surface.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>The private promotion boundary is closed and static-glyf compatibility is unchanged.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Applicability and Threshold

This is a pure bounded-binary utility with no external API surface. ASVS L1 resource validation, state integrity, and atomic-result controls apply. All high and critical threats are mitigated in this plan; lower-severity acceptance requires explicit deterministic evidence. No package installation occurs.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected TTC/OTC face → CFF table window | Root-relative records and shared ranges cross into table-local CFF authority. |
| staged facts → retained outline source | Complete validation, revision state, and budget authority gate private publication. |
| new CFF profile → existing glyf workflows | Closed dispatch must not alter already-qualified static-glyf semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-09 | Tampering | TTC/OTC offset rebasing | high | mitigate | Preserve root-relative records, create one table-local window, and test private non-zero-directory/shared-CFF admission in `collection_wbtest.mbt`. |
| T-104-10 | Denial of Service | Structural work and allocation | high | mitigate | Derive ceilings, charge every named unit, preflight exact totals, and test exact/one-less authority. |
| T-104-11 | Tampering | Source mutation and partial publication | high | mitigate | Final revision guard precedes one commit and closed-source promotion; white-box hostile and collection failures assert zero retained facts and zero charge. |
| T-104-12 | Elevation of Privilege | Partial CFF outline-source construction | high | mitigate | Keep constructors private, require the complete admitted fact aggregate, and assert rejection only in `font_wbtest.mbt`. |
| T-104-13 | Repudiation | Error precedence | medium | accept | Generated multi-fault fixtures lock deterministic category/stage outcomes. |
| T-104-14 | Tampering | Glyf compatibility drift | high | mitigate | Public-only frozen standalone/collection semantic and charge fingerprints run in `font_test.mbt` and `collection_test.mbt` with the CFF changes. |
</threat_model>

<verification>
- `moon test --target native` passes after each task.
- Exact/one-short hostile pairs cover Header, every INDEX family, every typed DICT family, String, CharStrings, charset, Encoding, ROS, FDArray, FDSelect, Private DICT, and global/local Subrs.
- Exact/one-less resource pairs cover structural limits, cumulative work, retained allocation, and caller budget.
- White-box `collection_wbtest.mbt` coverage proves non-zero-root/shared-CFF root/table coordinate authority and private admission atomicity.
- White-box `font_wbtest.mbt` coverage proves only complete admitted CFF facts cross the private outline-source invariant.
- Public `collection_test.mbt` and `font_test.mbt` glyf tests prove unchanged collection and standalone results without inspecting private CFF state.
</verification>

<success_criteria>
- CFF-01 exact profile and bounded structural admission completes with stable errors, limits, work, mutation, and budget semantics.
- CFF-02 publishes one complete private descriptor per GID or publishes nothing.
- All eight edge assumptions from the phase context have exact, one-short, or invariant evidence.
- The closed private outline-source boundary accepts only complete facts and does not expose public CFF font/path behavior.
- Existing static-glyf behavior is frozen and passing.
</success_criteria>

<output>
Create `.planning/phases/104-cff1-profile-and-bounded-data-model/104-03-SUMMARY.md` when done.
</output>
