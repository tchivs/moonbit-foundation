---
phase: 75-packed-grayscale-png-qualification
plan: 01
type: execute
wave: 1
depends_on: ["74-01"]
files_modified:
  - modules/mb-image/png/png_test.mbt
autonomous: true
requirements: [GRAYPACK-04]
must_haves:
  truths:
    - "Three externally authored, CRC-valid Type-0 PNGs at depths 1, 2, and 4 decode through the public generic facade into their stated canonical opaque RGBA8 pixels."
    - "Each fixed public Gray1, Gray2, and Gray4 eager encoder emits byte-for-byte the complete matching hand-authored PNG, including framing and CRCs."
    - "The retained packed chunk lifecycle and frozen legacy eager-vector tests, together with the new vectors, pass through the ordinary wasm, wasm-gc, js, and native package command."
  artifacts:
    - path: modules/mb-image/png/png_test.mbt
      provides: "Hand-authored 1/2/4-bit Type-0 PNG fixtures plus public canonical-decode and eager-full-wire qualification cases."
  key_links:
    - from: "static Type-0 PNG fixture"
      to: "@codec.ImageDecoder::decode(PngDecoder::new(), ...)"
      via: "public generic decode assertions for dimensions, complete source consumption, RGBA8 samples, and opaque alpha"
    - from: "PngEncoder::new_gray1/new_gray2/new_gray4"
      to: "the matching static fixture"
      via: "complete byte equality at the public eager writer boundary"
---

<objective>
Close the independent qualification gap for packed grayscale PNG output without changing the PNG implementation or public API.

Purpose: Prove fixed 1-, 2-, and 4-bit Type-0 wire contracts and generic RGBA8 decode semantics using inputs that do not originate from the encoder under test, while preserving Phase 73/74 lifecycle and compatibility evidence.
Output: `modules/mb-image/png/png_test.mbt` contains the complete static vector corpus and public-boundary checks; the ordinary four-target PNG suite is the phase gate.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/75-packed-grayscale-png-qualification/75-CONTEXT.md
@.planning/phases/75-packed-grayscale-png-qualification/75-RESEARCH.md
@.planning/phases/73-explicit-packed-grayscale-png/73-01-SUMMARY.md
@.planning/phases/74-resumable-packed-grayscale-png/74-01-SUMMARY.md
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add independent packed Type-0 vectors and public canonical decode coverage</name>
  <files>modules/mb-image/png/png_test.mbt</files>
  <action>Per D-01 and D-04, add one qualification fixture table or equivalent static helpers in the established `png_test_*_literal` style. Hand-author three complete non-interlaced Type-0 PNG byte literals with valid IHDR, Stored-DEFLATE IDAT, CRCs, and IEND: depth 1 / width 9 with filtered row `00 55 80` and canonical samples `00 ff 00 ff 00 ff 00 ff ff`; depth 2 / width 5 with `00 1b 00` and samples `00 55 aa ff 00`; depth 4 / width 3 with `00 0f 10` and samples `00 ff 11`. Keep expected RGBA8 channel values separately spelled out from the literals and do not create any fixture through PngEncoder, packing helpers, or decoder internals. Decode each fixture via `@codec.ImageDecoder::decode(PngDecoder::new(), ...)` with bounded owned input, complete-input options, limits, and budget matching the existing qualification patterns. Assert successful width/height, full source consumption, canonical RGBA8 descriptor/view, every R=G=B value, and A=255. This tracer is test-only: do not alter production sources, decoder behavior, or public APIs.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>All three complete external Type-0 fixtures are accepted through the public generic decoder and each independently stated pixel sequence is observed as opaque canonical RGBA8.</done>
</task>

<task type="auto">
  <name>Task 2: Compare public eager output to the fixed corpus and preserve qualification gates</name>
  <files>modules/mb-image/png/png_test.mbt</files>
  <action>Per D-01 and D-02, extend the same qualification case so matching canonical Gray/U8 sources pass through `PngEncoder::new_gray1`, `PngEncoder::new_gray2`, and `PngEncoder::new_gray4` at the established public eager writer boundary; assert each complete emitted byte sequence equals its corresponding static PNG fixture, not merely its packed row. Reuse existing package-visible test helpers when appropriate, but never make an encoder-produced result the decode oracle. Per D-02 and D-04, leave `png_stream_packed_hostile_drain`, packed admission/released-lease checks, and frozen Gray8/Gray16/GrayAlpha/RGB/RGBA compatibility literals in `stream_encode_test.mbt` and `encode_test.mbt` unchanged; they remain the lifecycle and legacy evidence. Per D-03, add no target wrapper, copied tree, release script, dependency, production API, Adam7 low-bit route, palette route, quantizer, strategy matrix, packed image model, or FFI.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The three fixed eager selectors equal their full external vectors; the existing hostile lifecycle and frozen legacy tests remain part of the successful ordinary package suite on wasm, wasm-gc, js, and native.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Exact, bounded, portable low-bit grayscale PNG output | 75-01 | COVERED | Static public decode/eager wire checks plus the ordinary four-target gate prove the stated outcome.
REQ | GRAYPACK-04 | Independent packed-wire/decode vectors, lifecycle evidence, frozen legacy compatibility, and full package pass | 75-01 | COVERED | Task 1 supplies independent vectors; Task 2 binds eager equality, retains existing evidence, and runs the package gate.
RESEARCH | — | Three hand-authored CRC-valid Type-0 vectors and canonical RGBA8 assertions | 75-01 | COVERED | Task 1 defines all required depths, wire rows, and public decode assertions.
RESEARCH | — | Complete eager-wire equality against the same external corpus | 75-01 | COVERED | Task 2 asserts full public output equality, including PNG framing and CRCs.
RESEARCH | — | Retain Phase 74 hostile schedules, atomic rejection, and sticky terminals | 75-01 | COVERED | Task 2 preserves the current stream suite and exercises it in the package gate.
RESEARCH | — | Retain frozen legacy eager vectors | 75-01 | COVERED | Task 2 preserves the established encode suite and exercises it in the package gate.
RESEARCH | — | Use the ordinary all-target command without wrappers | 75-01 | COVERED | Task 2 invokes the frozen package command directly.
CONTEXT | D-01 | Independent black-box packed wire and canonical decode proof | 75-01 | COVERED | Tasks use external literal PNGs and separate expected pixels, then compare public boundaries.
CONTEXT | D-02 | Eager/chunk identity, hostile capacities, atomic rejection, sticky terminals | 75-01 | COVERED | Task 2 adds eager full-wire equality and retains the Phase 74 chunk evidence unchanged.
CONTEXT | D-03 | Final portability gate covers all supported targets | 75-01 | COVERED | Task 2 executes the ordinary `--target all --frozen` command.
CONTEXT | D-04 | Qualification-only scope; retain prior evidence rather than rebuilding behavior | 75-01 | COVERED | Both tasks modify only the qualification test file and preserve Phase 73/74 evidence.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External PNG bytes → public generic decoder | The qualification vectors model caller-controlled compressed input crossing PNG framing, CRC, DEFLATE, and packed-sample expansion. |
| Canonical Gray/U8 source → public eager encoder | Caller-controlled source samples cross exact-level admission and whole-file serialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-01 | Tampering | Packed Type-0 decode expansion | medium | mitigate | Hand-authored CRC-valid vectors traverse the public decoder and assert each RGBA8 channel and opaque alpha independently of production packing. |
| T-75-02 | Tampering | Fixed low-bit eager serialization | medium | mitigate | Compare complete eager byte streams, including PNG chunk framing and CRCs, to the external literals for all three depths. |
| T-75-03 | Denial of service / Tampering | Caller-buffered chunk admission and terminal state | medium | mitigate | Preserve and execute the existing Phase 74 hostile schedules, admission snapshots, and released-lease replay tests in the ordinary package suite. |
| T-75-04 | Tampering | Existing compatibility baselines | low | mitigate | Keep frozen Gray8/16, GrayAlpha8/16, RGB8, and RGBA8 eager literals unchanged and include them in the same package execution. |
| T-75-SC | Tampering | Package installation | low | accept | This plan installs no package or external dependency. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target native --frozen` after adding the independent decoder tracer, then run `moon -C modules/mb-image test png --target all --frozen` as the phase gate. The latter must execute the unchanged Phase 74 hostile transport tests and existing legacy vector cases together with the new qualification corpus on wasm, wasm-gc, js, and native.
</verification>

<success_criteria>
- Public generic decoding of the three complete external Type-0 vectors yields precisely the declared opaque canonical RGBA8 samples at depths 1, 2, and 4.
- Public eager Gray1, Gray2, and Gray4 encoders each reproduce the matching complete static PNG byte sequence.
- No production API or implementation file changes are introduced, and the Phase 73/74 packed lifecycle plus legacy compatibility evidence remains in place.
- `moon -C modules/mb-image test png --target all --frozen` succeeds across all four supported targets.
</success_criteria>

<output>
Create `.planning/phases/75-packed-grayscale-png-qualification/75-01-SUMMARY.md` when execution is complete.
</output>
