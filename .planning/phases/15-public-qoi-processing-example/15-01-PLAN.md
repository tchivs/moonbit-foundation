---
phase: 15-public-qoi-processing-example
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - moon.work
  - examples/qoi-portable/moon.mod.json
  - examples/qoi-portable/main/moon.pkg
  - examples/qoi-portable/main/main.mbt
  - modules/mb-image/README.mbt.md
autonomous: true
requirements: [QOI-06]
must_haves:
  truths:
    - "A user can run one independent portable MoonBit consumer that decodes fixed QOI bytes, flips the image horizontally, and encodes canonical QOI bytes."
    - "The consumer proves dimensions, flipped pixel positions, decode/encode byte progress, empty diagnostics, exact output bytes, and deterministic digest before its only status line."
    - "The documented consumer produces the same required evidence on js, wasm, wasm-gc, and native without GUI state, FFI, host files, a registry, or a platform-specific codec."
  artifacts:
    - path: "examples/qoi-portable/moon.mod.json"
      provides: "Independent portable QOI example module declaration."
    - path: "examples/qoi-portable/main/moon.pkg"
      provides: "Executable package importing only public mb-core and mb-image packages."
    - path: "examples/qoi-portable/main/main.mbt"
      provides: "Deterministic in-memory QOI decode, flip_horizontal, and encode proof."
    - path: "modules/mb-image/README.mbt.md"
      provides: "Public QOI portable-example instructions and frozen four-target commands."
  key_links:
    - from: "examples/qoi-portable/main/main.mbt"
      to: "modules/mb-image/qoi/qoi.mbt"
      via: "public QoiDecoder and QoiEncoder values passed through ImageDecoder::decode and ImageEncoder::encode"
      pattern: "Qoi(Decoder|Encoder)::new"
    - from: "examples/qoi-portable/main/main.mbt"
      to: "modules/mb-image/ops/copy_flip.mbt"
      via: "public ops.flip_horizontal applied to the decoded ImageView"
      pattern: "flip_horizontal"
    - from: "modules/mb-image/README.mbt.md"
      to: "examples/qoi-portable/main/main.mbt"
      via: "four frozen moon run commands for the independent example"
      pattern: "examples/qoi-portable"
---

<objective>
Deliver the single documented public QOI decode-process-encode proof required by QOI-06.

Purpose: Give library users a small, reproducible all-target consumer of the verified portable QOI codec and an existing public image operation.
Output: An independent `examples/qoi-portable` executable and a focused public README entry.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-public-qoi-processing-example/15-CONTEXT.md
@.planning/phases/14-canonical-qoi-encode-and-four-target-vectors/14-01-SUMMARY.md
@examples/ppm-portable/moon.mod.json
@examples/ppm-portable/main/moon.pkg
@examples/ppm-portable/main/main.mbt
@modules/mb-image/qoi/qoi.mbt
@modules/mb-image/codec/contracts.mbt
@modules/mb-image/ops/copy_flip.mbt
@modules/mb-image/README.mbt.md
@fixtures/qoi/cases.json

<interfaces>
Use these public contracts directly; do not import package-private QOI helpers.

- `@qoi.QoiDecoder::new()` and `@qoi.QoiEncoder::new()` are the public codec values.
- Decode through `@codec.ImageDecoder::decode(decoder, reader, DecodeOptions, CodecLimits, Budget, Diagnostics)`; consume `DecodeResult::image()` and `DecodeResult::bytes_read()`.
- Transform through `@ops.flip_horizontal(view, Budget)`; consume its fresh owned image via `OperationResult::image()`.
- Encode through `@codec.ImageEncoder::encode(encoder, view, writer, EncodeOptions, CodecLimits, Budget, Diagnostics)`; consume `EncodeResult::bytes_written()`.
- `@io.MemoryReader`, `@io.MemoryWriter`, `@bytes.OwnedBytes`, `@budget.Budget`, and `@error.Diagnostics` are the portable, caller-owned boundaries demonstrated by `examples/ppm-portable/main/main.mbt`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the independent deterministic QOI portable workflow</name>
  <files>moon.work, examples/qoi-portable/moon.mod.json, examples/qoi-portable/main/moon.pkg, examples/qoi-portable/main/main.mbt</files>
  <action>Update the root `moon.work` to add the exact workspace member `./examples/qoi-portable`, then create `examples/qoi-portable` as a separate executable module per D-01. Its `moon.mod.json` must retain the public registry dependencies `tchivs/mb-core: 0.1.0` and `tchivs/mb-image: 0.1.0`, mirror the portable target declaration and public dependency style in `examples/ppm-portable`, and import only the needed public mb-core packages plus `mb-image/codec`, `mb-image/ops`, and `mb-image/qoi`. In `main.mbt`, implement small local budget, codec-limit, byte-comparison, and rolling-digest helpers following the PPM example; every codec and operation boundary must receive newly constructed explicit `Budget`, `CodecLimits`, `MemoryReader` or `MemoryWriter`, options, and `Diagnostics` per D-03. Use the repository-owned `fixtures/qoi/cases.json` `valid_cases.diff-byte-wrap` source stream as the fixed in-memory source: 27 bytes `716f696600000002000000010300feffffff7a0000000000000001`, representing the 2x1 RGB pixels `(255,255,255)` then `(0,255,255)`. Decode it with `QoiDecoder`, call only `@ops.flip_horizontal`, then encode the returned image with `QoiEncoder`. Before printing, assert width `2`, height `1`, transformed stored pixels `(0,255,255)` then `(255,255,255)`, decoder progress `27`, encoder progress and writer position `24`, and zero diagnostics at decode and encode. Byte-compare the writer output against the canonical 24-byte result `716f696600000002000000010300655a0000000000000001`, then assert the existing PPM-style rolling digest `750514177`; use the checked output identity `f6767a56873b631989d02e2cfef3baef2900ef40dc74e7bcf236b51d27095138` in the evidence. The sole normal-output line must be emitted only after all assertions pass and include stable markers `example=qoi-portable`, `bytes_read=27`, `bytes_written=24`, `width=2`, `height=1`, `flip_horizontal`, `digest=750514177`, and that SHA-256 identity, per D-02 and D-04. Do not add tests, fixtures, image APIs, registry behavior, FFI, filesystem/CLI input, streaming, benchmark, release, or configuration work; failures should abort without a second status line.</action>
  <verify>
    <automated>moon -C examples/qoi-portable run main --target js --frozen; moon -C examples/qoi-portable run main --target wasm --frozen; moon -C examples/qoi-portable run main --target wasm-gc --frozen; moon -C examples/qoi-portable run main --target native --frozen</automated>
  </verify>
  <done>The new standalone consumer uses only public portable contracts, completes decode → flip_horizontal → encode in memory, and each required target emits its one identical checked status line with the prescribed progress, dimensions, operation, digest, and SHA-256 markers.</done>
</task>

<task type="auto">
  <name>Task 2: Document the public QOI portable consumer and frozen target commands</name>
  <files>modules/mb-image/README.mbt.md</files>
  <action>Update the README’s existing standalone-consumer section per D-05: add a concise QOI portable-example bullet that links to `examples/qoi-portable/main/main.mbt`, describes the fixed in-memory QOI decode → `flip_horizontal` → canonical QOI encode workflow, and states that it asserts the 27-byte input, 24-byte canonical output, dimensions and pixel order, empty diagnostics, rolling digest `750514177`, and SHA-256 `f6767a56873b631989d02e2cfef3baef2900ef40dc74e7bcf236b51d27095138` before printing success. Add these exact frozen commands in the same four-target convention as the PPM example: `moon -C examples/qoi-portable run main --target js --frozen`, `moon -C examples/qoi-portable run main --target wasm --frozen`, `moon -C examples/qoi-portable run main --target wasm-gc --frozen`, and `moon -C examples/qoi-portable run main --target native --frozen`. Keep the PPM and native CLI examples intact. Do not document unsupported QOI streaming, host files, GUI, FFI, registry selection, release workflow, or performance claims.</action>
  <verify>
    <automated>moon -C examples/qoi-portable run main --target js --frozen; moon -C examples/qoi-portable run main --target wasm --frozen; moon -C examples/qoi-portable run main --target wasm-gc --frozen; moon -C examples/qoi-portable run main --target native --frozen; moon -C modules/mb-image test --target all --frozen</automated>
  </verify>
  <done>The public README links the independent QOI consumer, preserves existing examples, contains all four exact runnable target commands, and describes only the portable QOI workflow and its deterministic evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixed example bytes → QOI decoder | Untrusted-format parsing seam is exercised with an in-memory repository-owned valid vector. |
| Decoded ImageView → image operation/encoder | Public image data crosses explicit budgets, limits, options, diagnostics, and forward-only memory I/O boundaries. |
| Example source → target runtimes | Deterministic public API behavior must agree on js, wasm, wasm-gc, and native. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | `examples/qoi-portable/main/main.mbt` fixed source/output evidence | medium | mitigate | Compare the complete canonical 24-byte output and rolling digest before the sole status line; all four target runs exercise the check. |
| T-15-02 | Denial of service | Public decoder/operation/encoder calls | medium | mitigate | Pass fresh bounded `CodecLimits` and `Budget` values plus explicit options at every boundary, exercising the existing codec and operation resource contracts. |
| T-15-03 | Repudiation | Target-specific example behavior | low | mitigate | Require the same stable evidence markers from direct frozen runs on all four portable targets and record them in the README. |
| T-15-04 | Elevation of privilege | Example execution | low | accept | The example has no external input, filesystem access, FFI, registry, GUI, network, or privileged host integration; it operates solely on fixed in-memory bytes. |
</threat_model>

<verification>
Run the four explicit frozen `moon -C examples/qoi-portable run main` commands and confirm each emits the same one checked evidence line. Run `moon -C modules/mb-image test --target all --frozen` after the README change to retain the public documentation and package regression coverage.
</verification>

<success_criteria>
- QOI-06 is satisfied by a separately runnable public consumer, not an extension of the PPM example.
- The full public `QoiDecoder` → `flip_horizontal` → `QoiEncoder` path stays in memory and relies only on portable public contracts.
- Its fixed source, canonical output, dimensions, transformed pixels, byte progress, diagnostics, digest, and status-line identity are deterministic on all four supported targets.
- The public README makes the example discoverable and supplies the exact four commands needed to reproduce it.
</success_criteria>

<output>
Create `.planning/phases/15-public-qoi-processing-example/15-01-SUMMARY.md` when done.
</output>
