---
phase: 22-canonical-png-encode-and-portable-evidence
plan: "01"
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - moon.work
  - examples/png-portable/moon.mod.json
  - examples/png-portable/main/moon.pkg
  - examples/png-portable/main/main.mbt
autonomous: true
requirements: [PNG-06, PNG-07]
must_haves:
  truths:
    - "Compatible RGB8 and straight-RGBA8 image views encode to one deterministic PNG byte sequence only after all preflight checks pass."
    - "Every incompatible source, limit, budget, and setup failure occurs before the Writer receives bytes."
    - "A public portable program decodes PNG, applies an existing image operation, re-encodes PNG, and verifies deterministic evidence on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: "Pure-MoonBit stored-DEFLATE PngEncoder implementation."
    - path: examples/png-portable/main/main.mbt
      provides: "Public decode-operation-encode PNG evidence executable."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: "PngEncoder implements the existing codec ImageEncoder contract."
    - from: examples/png-portable/main/main.mbt
      to: modules/mb-image/png/png.mbt
      via: "The example invokes PngDecoder, an existing image operation, and PngEncoder through public contracts."
---

<objective>
Deliver canonical pure-MoonBit PNG encode plus portable public interoperability evidence.

Purpose: Complete PNG-06 and PNG-07 without FFI, public streaming APIs, release work, or PNG extensions.
Output: `PngEncoder`, exact-byte/zero-write tests, and one all-target public PNG workflow.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/qoi/encode.mbt
@examples/qoi-portable/main/main.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical stored-DEFLATE PngEncoder with zero-write preflight</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - Compatible packed-U8 RGB8 and straight-RGBA8, TopLeft, built-in encoded-sRGB views encode through the existing `@codec.ImageEncoder` seam and decode back to identical pixels.
    - The exact output is signature, one IHDR, one deterministic IDAT containing a zlib stored-DEFLATE stream of filter-None scanlines, then IEND; IHDR/IDAT/IEND CRC-32 and zlib Adler-32 are valid.
    - Source-semantic, dimension, encoded-byte, output-limit, work-limit, and budget failures leave a scripted Writer at zero completed bytes.
    - Short successful writes and later Writer failure retain exact requested/completed progress.
  </behavior>
  <action>Add public `PngEncoder` and `PngEncoder::new()` beside the unchanged `PngDecoder`, then implement the existing `@codec.ImageEncoder` contract in private package code. Reuse the PNG package's checked arithmetic, CRC-32, Adler-32, descriptor semantics, typed errors, and forward-Writer progress conventions. Preflight the complete image before any Writer call: require non-empty packed U8 RGB8 without alpha or RGBA8 with straight alpha, TopLeft built-in encoded-sRGB metadata, no opaque metadata, addressable tightly packed logical rows, supported dimensions, every `CodecLimits` ceiling, exact output length, and one authoritative budget/work charge. Emit filter byte zero before each logical row; wrap those scanlines in deterministic zlib stored DEFLATE blocks of at most 65,535 bytes with canonical zlib header, lengths/complements, and Adler trailer; write one canonical IHDR, IDAT, and IEND sequence with big-endian fields and CRCs. Start with public RED tests for exact RGB/RGBA bytes, decode-after-encode pixels, preflight zero-write behavior, and Writer progress, then implement and refactor. Do not alter decoder signatures or add FFI, compression dependencies, semantic PNG chunks, or public streaming types.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>`PngEncoder` deterministically writes the documented stored-DEFLATE representation for compatible views, and all preflight failures are observed before any output byte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove canonical bytes and publish the four-target PNG workflow</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, moon.work, examples/png-portable/moon.mod.json, examples/png-portable/main/moon.pkg, examples/png-portable/main/main.mbt</files>
  <behavior>
    - White-box tests independently parse emitted chunk boundaries, stored-block length/complement fields, CRCs, Adler-32, filter bytes, and exact canonical bytes for RGB and RGBA vectors.
    - The public executable uses only public portable contracts to decode a fixed supported PNG, apply an existing operation, encode it, and print one deterministic evidence line including dimensions, byte count, and digest.
    - The exact executable output and its expected encoded bytes pass unchanged on js, wasm, wasm-gc, and native.
  </behavior>
  <action>Add `encode_wbtest.mbt` with compact literal vectors that verify byte-level PNG framing, stored-DEFLATE structure, checksums, canonical stability across repeated encodes, and public PngDecoder round trips without treating a successful decode alone as canonical proof. Create `examples/png-portable` as a workspace executable modeled on the existing portable QOI consumer: own fixed PNG input/output buffers and budgets, call `PngDecoder` through `@codec.ImageDecoder`, apply `@ops.flip_horizontal`, call `PngEncoder` through `@codec.ImageEncoder`, compare the expected canonical output bytes and a target-neutral digest, then print exactly one deterministic workflow evidence line. Add the workspace member and only the portable public package imports needed by the example. Make the all-target MoonBit test command execute both encoder evidence and the public example; preserve caller-owned I/O and avoid GUI state, host adapters, FFI, registry, CI, or release changes.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen; moon -C examples/png-portable run main --target all --frozen</automated></verify>
  <done>Canonical PNG bytes have independent structural/checksum proof, and the public decode→flip-horizontal→encode program produces the same verified evidence on all four supported targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller ImageView → encoder preflight | Caller-controlled layout, metadata, limits, and budget determine whether encoding is permitted. |
| preflight → Writer | A forward Writer may make short, stalled, or failed progress after validated output begins. |
| public example input → decoder → operation → encoder | Fixed portable evidence must retain strict PNG and image-operation semantics across targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Denial of service | encode preflight | high | mitigate | Validate representation and checked byte/work/output limits, then charge the exact budget before Writer use. |
| T-22-02 | Tampering | PNG/zlib byte construction | high | mitigate | Fix chunk order, scanline filter, stored-block segmentation, big-endian fields, CRC-32, Adler-32, and verify exact bytes. |
| T-22-03 | Repudiation | Writer progress | medium | mitigate | Test stalled, short, and failing writers with exact completed-byte accounting. |
| T-22-04 | Tampering | public portable evidence | medium | mitigate | Assert fixed input, operation output, encoded bytes, digest, and one output line on every required target. |
| T-22-SC | Tampering | package installation | low | accept | No package-manager installation occurs. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target all --frozen` and `moon -C examples/png-portable run main --target all --frozen`; confirm the public output is identical for js, wasm, wasm-gc, and native.
</verification>

<success_criteria>
PNG-06 and PNG-07 are met: compatible RGB8/RGBA8 views encode after zero-write preflight to one stored-DEFLATE PNG representation, while a public four-target decode→operation→encode workflow verifies deterministic output.
</success_criteria>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Canonical PNG output and portable interoperability evidence | 22-01 | COVERED | Tasks 1 and 2 implement output plus public proof. |
| REQ | PNG-06 | Deterministic RGB8/straight-RGBA8 encode after zero-write preflight | 22-01 | COVERED | Task 1 owns the encoder and failure contract. |
| REQ | PNG-07 | Public PNG decode→operation→encode and four-target hostile/fixture evidence | 22-01 | COVERED | Task 2 adds exact-byte structure tests and portable public workflow. |
| RESEARCH | — | No Phase 22 research artifact exists | 22-01 | COVERED | Phase 21 implementation and QOI encoder establish local patterns. |
| CONTEXT | — | No Phase 22 CONTEXT decisions exist | 22-01 | COVERED | Roadmap and requirement scope are retained. |

<output>
Create `.planning/phases/22-canonical-png-encode-and-portable-evidence/22-01-SUMMARY.md` when done.
</output>
