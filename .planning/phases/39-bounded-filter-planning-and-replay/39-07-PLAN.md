---
phase: 39-bounded-filter-planning-and-replay
plan: "07"
type: tdd
wave: 7
depends_on: [39-06]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
gap_closure: true
requirements: [PNGF-03]
must_haves:
  truths:
    - "An Adaptive DynamicOrFixedOrStored preflight charges the exact real frequency and, when reached, bit-count traversal facts even when Dynamic declines and Fixed or Stored is selected."
    - "The exact selected work/budget boundary for a Dynamic-decline route admits construction, while one unit less rejects atomically before an eager byte or caller lease exists."
    - "The repair preserves Dynamic strict-win selection, output bytes, public factories, no-full-image staging, and the acknowledgement-safe owned cursor contract."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: A Dynamic planning outcome that transports real traversal facts on both selected and declined candidate paths into the atomic preflight ledger.
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: White-box Dynamic-decline facts and exact/one-less accounting coverage.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public eager/chunk Adaptive fallback regression across the exact admission boundary.
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/encode_wbtest.mbt
      via: The returned Dynamic outcome exposes the frequency and bit facts actually accumulated before candidate decline.
      pattern: _png_dynamic_plan|frequency_facts|bit_facts|adaptive_planning_facts
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: The shared preflight charges Dynamic-decline work before both eager and caller-buffered Adaptive constructors can expose output.
      pattern: new_with_strategies|DynamicOrFixedOrStored|Adaptive
---

<objective>
Close the verified PNGF-03 Dynamic-decline accounting gap without changing the bounded Adaptive cursor, public surface, or compression-selection policy.

Purpose: A Dynamic candidate can perform real frequency and bit-count cursor work before it is rejected. That work must be returned to preflight and admitted before either encoder adapter exposes output.
Output: A fact-carrying Dynamic planning result, deterministic declined-candidate accounting tests, and targeted four-portable-target public regression evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-VERIFICATION.md
@.planning/phases/39-bounded-filter-planning-and-replay/RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-06-PLAN.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-06-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- `_png_dynamic_plan(...)` currently invokes `_png_dynamic_frequencies(...)`, then has normal `Ok(None)` exits while its real `frequency_facts` and potentially real `bit_facts` are local-only. Its `Ok(Some(PngDynamicPlan))` path already carries both facts.
- `_png_encode_preflight_with_filter(...)` already adds Stored and Fixed actual traversal facts, then adds Dynamic facts only in its current successful-candidate branch. It is the sole shared atomic admission path for eager and caller-buffered factories.
- `PngFilteredMatchCursor` from Plan 39-06 owns the bounded 262-byte producer/window contract. This gap changes fact transport and ledger composition only; it must not replace the cursor, add a second traversal, or stage source/filter/output data.
- Existing policy is fixed: Fixed wins an equality with Stored, Dynamic replaces the baseline only for a strictly shorter complete PNG, legacy None remains byte-identical, and `new_with_strategies` public signatures remain unchanged.
</interfaces>
</context>

<feature>
  <name>Dynamic-decline Adaptive traversal accounting</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - PNGF-03: Every `_png_dynamic_plan` result carries the exact real traversal facts accumulated before it returns, including a frequency-only decline and a post-bit-count decline.
    - PNGF-03: `DynamicOrFixedOrStored` preflight adds those facts before retaining its existing Fixed-or-Stored fallback, then predicts one selected replay traversal and performs the existing one atomic limit/budget admission.
    - PNGF-03: A deterministic Adaptive input that evaluates and declines Dynamic reports the returned facts, accepts exactly the resulting selected-work/budget amount, and rejects one unit below it with unchanged resource counters.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED Dynamic-decline fact and atomic-boundary regressions</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: A compact deterministic RGB8 Adaptive fixture invokes private Dynamic planning and proves that Dynamic was evaluated but declined, while the returned outcome has nonzero frequency traversal facts and preserves any bit-count facts actually reached before the decline.
    - Test 2: The white-box preflight ledger for that fixture equals the checked sum of its real Stored walk, Fixed walk, declined Dynamic facts, and exactly one selected Fixed-or-Stored replay walk; it does not infer work from a sweep formula.
    - Test 3: The returned selected-work amount succeeds and consumes exactly that work; one-less work and one-less budget reject with the existing work context and leave the rejected budget unchanged.
    - Test 4: Public eager and caller-buffered `new_with_strategies(DynamicOrFixedOrStored, Adaptive)` use the same deterministic fallback fixture, retain eager/chunk bytes and complete public decode, and observe the exact/one-less admission boundary without exposing an eager byte or changing an offered chunk lease on rejection.
  </behavior>
  <action>Start RED by adding a package-private fact assertion in `encode_wbtest.mbt` and a matching black-box regression in `stream_encode_test.mbt`. Use the existing small strict-Dynamic-fallback corpus if it reaches a normal `_png_dynamic_plan` decline; otherwise add the smallest hand-authored RGB8 fixture that does. The test must call the new fact-carrying Dynamic outcome directly and assert the decline variant rather than treating a non-selected successful Dynamic candidate as this case. Derive expected ledger totals by adding the individual real facts returned from the Stored, Fixed, Dynamic-frequency, Dynamic-bit (when executed), and selected replay walks. Assert that every Dynamic traversal fact is present once even though the plan remains Fixed or Stored.

For the exact boundary, take `selected_work` from the successful private preflight, not a copied numeric constant. Prove the exact work/budget case succeeds; prove one unit less returns the established work failure and preserves all rejected budget counters. In the public test, use the same source through both eager and chunk combined factories, verify their accepted bytes are identical and decode to the source, then exercise the established atomic writer/lease helpers with the precise boundary derived by the white-box fixture contract. Keep the test corpus deterministic and small. Do not modify production code, QOI files, public declarations, policies, fixtures, quality scripts, compression comparison rules, legacy None vectors, or cursor/ring state in this RED task. Run the named native selectors and confirm they fail because a declined Dynamic result cannot yet expose its facts, then commit `test(39-07): cover declined Dynamic accounting`.</action>
  <verify>
    <automated>$targetRoot = Join-Path (Resolve-Path '_build') 'phase39-07-red'; if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null; try { moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f 'PNG adaptive Dynamic decline facts are admitted'; if ($LASTEXITCODE -eq 0) { throw 'RED Dynamic-decline fact selector unexpectedly passed' }; moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f 'PNG adaptive Dynamic decline public admission is atomic'; if ($LASTEXITCODE -eq 0) { throw 'RED Dynamic-decline public selector unexpectedly passed' } } finally { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } }</automated>
  </verify>
  <done>Focused tests fail for the missing fact transport, distinguish a true Dynamic decline from a merely non-winning Dynamic candidate, and define exact accounting plus public atomic behavior without changing existing output contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return declined Dynamic facts and charge them before selection</name>
  <files>modules/mb-image/png/encode.mbt</files>
  <behavior>
    - Test 1: The Dynamic planner returns a single private outcome shape that distinguishes selected from declined candidates while carrying real `frequency_facts` and `bit_facts` in both cases.
    - Test 2: Every normal decline after frequency collection retains those facts; a decline after the exact-bit walk also retains its bit facts. Error exits remain errors and do not turn into candidate declines.
    - Test 3: Preflight adds returned Dynamic facts before applying the unchanged strict Dynamic-versus-baseline comparison, then validates limits and charges the existing budget once before either adapter can return output state.
    - Test 4: Existing selected Dynamic plans, Fixed-or-Stored fallback selection, bytes, fingerprints, bounded cursor behavior, and public factory signatures remain unchanged.
  </behavior>
  <action>Implement the narrow fact-transport repair in `encode.mbt`. Replace the Dynamic planner's option-only success/decline result with a private outcome that carries the actual `frequency_facts` and `bit_facts` for every normal result. Preserve the selected `PngDynamicPlan` as the selected payload; initialise bit facts to the zero traversal record only until an exact-bit traversal has actually started, and return the completed cursor facts when it has. Convert each current normal Dynamic decline after frequency collection or later calculation into the declined outcome with the facts accumulated at that point. Retain `Err` propagation for checked arithmetic and encoder errors.

In `_png_encode_preflight_with_filter`, add the Dynamic outcome's facts to `adaptive_planning_facts` before branching on selected versus declined. A selected Dynamic continues to replace the Fixed-or-Stored baseline only when its complete PNG is strictly shorter. A declined Dynamic continues to select the already computed baseline, but its real facts remain included in `adaptive_filter_work`, `selected_work`, work-limit validation, and the single pre-output budget charge. Do not add a substitute cursor pass, recompute facts merely for accounting, alter `PngFilteredMatchCursor`, stage an image/filter/output/token buffer, change public APIs, modify QOI code, or alter Fixed equality/legacy None/Dynamic strict-win semantics. Run RED to GREEN, format only `encode.mbt`, and commit `feat(39-07): charge declined Dynamic traversal facts`.</action>
  <verify>
    <automated>$targetRoot = Join-Path (Resolve-Path '_build') 'phase39-07-green'; if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null; try { $filters = @('PNG adaptive Dynamic decline facts are admitted','PNG adaptive Dynamic decline public admission is atomic','*adaptive match cursor*','PNG adaptive compression selection retains Fixed equality and Dynamic strict winner','PNG adaptive combined eager routes','PNG adaptive combined chunk routes','PNG adaptive combined admission is atomic','PNG filter strategy eager frozen compatibility vectors','PNG filter strategy chunk frozen compatibility vectors'); foreach ($target in @('js','wasm','wasm-gc','native')) { $targetDir = Join-Path $targetRoot $target; foreach ($filter in $filters) { moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen -f $filter; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } }; moon -C modules/mb-image check png --target all --target-dir (Join-Path $targetRoot 'check') --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } }</automated>
  </verify>
  <done>A real Dynamic candidate decline no longer drops cursor work: its exact recorded facts are admitted before selection and before output/lease visibility; focused white-box and public Adaptive fallback regressions pass on js, wasm, wasm-gc, and native, and all temporary target roots are removed.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Adaptive filtering is integrated before compression planners while eager/chunk admission stays atomic | 39-07 | COVERED | Repairs the verified Dynamic-decline omission without changing the completed filtering/replay architecture.
REQ | PNGF-02 | Deterministic bounded candidate selection and stable winner | 39-07 | COVERED | Preserved by explicitly retaining the existing owned cursor, selection rules, and targeted regressions; no PNGF-02 behavior changes.
REQ | PNGF-03 | No-staging Stored/Fixed/Dynamic planning with atomic capability/resource admission | 39-07 | COVERED | Returned decline facts are summed in preflight; exact/one-less white-box and public eager/chunk tests prove the repair.
RESEARCH | — | Preserve legacy None bytes, Fixed equality, Dynamic strict win, and bounded acknowledgement-safe replay | 39-07 | COVERED | Task 2 constrains the repair to Dynamic outcome/ledger transport and reruns focused four-target compatibility/selection tests.
RESEARCH | — | Account for actual plan/replay traversal rather than synthetic work | 39-07 | COVERED | Task 1 asserts facts from the executed Dynamic decline; Task 2 carries and sums those facts without another traversal.
CONTEXT | — | No Phase 39 CONTEXT.md or D-NN locked decisions exist | 39-07 | COVERED | No deferred ideas or discretionary scope additions are introduced.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image → Dynamic candidate cursor walks | Caller-controlled Adaptive bytes can cause a candidate to perform bounded planning work and then decline. |
| Dynamic outcome → atomic preflight ledger | Returned facts must reach the sole work-limit and budget calculation without being discarded on fallback. |
| Preflight → eager writer/caller lease | Failure must occur before either adapter exposes mutable output ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-07-01 | Denial of service | Declined Dynamic candidate | high | mitigate | Task 2 returns actual frequency and bit facts for every normal decline and includes them in checked preflight work before the existing budget charge. |
| T-39-07-02 | Tampering | Compression-plan selection | high | mitigate | Task 2 keeps Fixed equality preference and strict Dynamic complete-PNG replacement; Task 1 locks fallback selection on the deterministic declined fixture. |
| T-39-07-03 | Denial of service | Planner/replay storage | high | mitigate | Task 2 changes only facts/outcome transport and explicitly retains the owned bounded cursor with no image, scanline, token, or output staging. |
| T-39-07-04 | Tampering | Eager/chunk admission boundary | high | mitigate | Task 1 proves exact and one-less eager/chunk behavior with unchanged rejected budgets and untouched offered leases; Task 2 retains the sole shared preflight charge. |
| T-39-07-05 | Tampering | Legacy and accepted Adaptive bytes | medium | mitigate | Task 2 does not alter emitter/cursor/public APIs; Task 2 reruns frozen None, Adaptive combined, and selection regressions on all portable targets. |
</threat_model>

<verification>
- RED: named native white-box and public Dynamic-decline selectors fail because `Ok(None)` cannot yet transport real traversal facts.
- GREEN: on each of js, wasm, wasm-gc, and native, run the new white-box fact/boundary selector and public Adaptive fallback selector, then the existing bounded-cursor, selection, combined-route, atomic-admission, and frozen-None selectors.
- Compilation: check the PNG package for all targets using an isolated build root.
- Cleanup: every `phase39-07-*` target root is removed in `finally`, including failed target runs.
</verification>

<success_criteria>
- A normal Dynamic decline always returns the real facts from work already executed, including frequency-only and post-bit-count declines.
- Preflight charges those facts before retaining the unchanged Fixed-or-Stored fallback, so exact work/budget succeeds and one unit less fails atomically.
- Deterministic public Adaptive fallback bytes, selection rules, legacy None bytes, bounded cursor ownership, and public APIs are unchanged across all four portable targets.
</success_criteria>

<output>
Create `.planning/phases/39-bounded-filter-planning-and-replay/39-07-SUMMARY.md` when done.
</output>
