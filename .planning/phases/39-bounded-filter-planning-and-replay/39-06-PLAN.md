---
phase: 39-bounded-filter-planning-and-replay
plan: "06"
type: tdd
wave: 6
depends_on: [39-05]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
gap_closure: true
requirements: [PNGF-02, PNGF-03]
must_haves:
  truths:
    - "Every Adaptive Stored, FixedOrStored, and DynamicOrFixedOrStored planning and replay byte comes from a bounded forward cursor that selects each entered row once and never uses the stateless random selector."
    - "Adaptive preflight charges the checked sum of facts returned by the real Stored, Fixed, Dynamic-frequency, Dynamic-bit-count, and selected-replay walks before either eager output or a caller lease exists."
    - "A Fixed or Dynamic pending preview owns a deep successor window and advances logical consumption only when acknowledge accepts its byte; repeated present calls leave the committed cursor and ring unchanged."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: "Measured bounded match-cursor planning walks and a preflight ledger assembled from their returned facts."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Acknowledgement-safe Stored/Fixed/Dynamic Adaptive replay using deep-owned match-cursor successors."
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: "RED/GREEN per-walk accounting tests for Fixed=4 and Dynamic=8 planning row facts plus exact atomic admission."
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: "Cursor ownership, retained-window, and repeated-present acknowledgement regression tests."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Public Adaptive Fixed/Dynamic mutation-sticky and compression-selection-rule regression evidence."
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Returned traversal facts from real candidate walks determine the pre-output ledger, while the selected plan supplies an equivalent owned replay cursor."
      pattern: "PngFilteredMatchCursor|adaptive_planning_facts|adaptive_replay_facts"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "Fixed and Dynamic preview/emission use the same forward producer and retained absolute-byte window as the planning walkers."
      pattern: "ensure|retained_from|produced_exclusive|acknowledge"
---

<objective>
Close the final Phase 39 gap by replacing the split synthetic/stateless Adaptive sources with one bounded, acknowledgement-safe match cursor used by the real Stored, Fixed, and Dynamic planning/replay paths. PNGF-02, PNGF-03.

Purpose: Restore a truthful pre-output resource bound without changing PNG bytes, selection ties, public APIs, or atomic semantics.
Output: Traversal-derived planning facts, a deep-owned 262-byte Adaptive match cursor, and focused four-target regression evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-bounded-filter-planning-and-replay/RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-VERIFICATION.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-04-PLAN.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-04-SUMMARY.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-05-PLAN.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-05-SUMMARY.md
@.planning/debug/phase39-dynamic-ring-crash.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- The proven defect is architectural, not a ring-size issue: `PngFilteredCursor` facts are collected by synthetic traversals while `_png_fixed_plan`, `_png_dynamic_frequencies`, `_png_dynamic_plan`, and active Stored/Fixed/Dynamic replay still obtain Adaptive bytes through `_png_filtered_scanline_byte` or its matcher caller.
- Keep `PngFilteredCursor` as the forward source-row selector. A new private `PngFilteredMatchCursor` must separately own producer/source state, logical consumed position, produced-exclusive position, retained-start position, and a deep-owned 262-byte circular window.
- `ensure(exclusive_position)` may advance only the producer until the requested bytes exist. `read(absolute_position)` may return only a retained byte. Only consuming an emitted literal/match advances logical consumption; matcher probes do not.
- Fixed planning, Dynamic frequency/fingerprint collection, and Dynamic exact-bit counting each return their own measured `PngFilteredTraversalFacts`; `_png_encode_preflight_with_filter` composes those returned facts instead of appending independent formula or substitute traversals.
- `PngEncodeMachine::present()` is preview-only. `pending_fixed` and `pending_dynamic` must contain an independently deep-owned successor cursor/window; `acknowledge()` alone commits it with existing bit, Adler, CRC, matcher-work, and fingerprint state.
- Preserve the existing legacy None provider, byte streams, Fixed-over-Stored equality preference, Dynamic strict complete-PNG win rule, source-mutation sticky failures, public factories, and no-full-image-staging contract.
</interfaces>
</context>

<feature>
  <name>Measured acknowledgement-safe Adaptive match cursor</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - PNGF-02: A fresh Adaptive walk uses the existing None/Sub/Up/Average/Paeth order and strict earlier-wins tie rule, selecting a row once when its forward producer enters that row.
    - PNGF-03: The match cursor retains at most 262 filtered bytes: the 258-byte maximum probe length and four-byte maximum DEFLATE distance. Its checked invariants are `retained_start <= requested < produced_exclusive` and `produced_exclusive - retained_start <= 262`.
    - PNGF-03: Fixed planning returns its own actual facts; Dynamic returns separate frequency/fingerprint and exact-bit facts; preflight sums those real facts with the real Stored candidate and one selected replay prediction, with no parallel synthetic accounting passes.
    - PNGF-03: Before acknowledgement, a preview may fill only its successor producer/window. The committed logical position, produced-exclusive position, retained start, facts, and ring bytes remain unchanged.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-test real match-cursor facts and preview ownership</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: RGB8 and straight-RGBA8 multi-row periodic sources force 258-byte look-ahead across scanline tags and payloads; a FixedOrStored preflight reports four planning resolved rows (Stored walk 2 + Fixed walk 2), and a DynamicOrFixedOrStored preflight reports eight (Stored 2 + Fixed 2 + frequency/fingerprint 2 + exact-bit 2).
    - Test 2: Tests inspect the returned per-walk facts, not only aggregate totals: every two-row walk has two resolved rows, `2 * 5 * row_bytes` candidate residuals, and exactly its produced selected residual bytes; aggregate planning facts equal the named returned walks and replay facts equal the selected plan's one two-row replay walk.
    - Test 3: Exact selected work drains successfully, while one-less work and one-less budget reject before eager bytes or chunk leases and preserve remaining budget for Stored, FixedOrStored, and DynamicOrFixedOrStored.
    - Test 4: A private 262-byte cursor test proves producer position can lead logical consumption during probes, confirms retained absolute reads for all four history distances through wraparound, and rejects reads outside `[retained_start, produced_exclusive)`.
    - Test 5: Fixed and Dynamic machines call `present()` twice before one `acknowledge()`: both presents return the same byte and leave committed logical position, producer position, retained start, facts, and all ring slots unchanged; acknowledgement commits exactly the deep-owned successor state once.
    - Test 6: Public caller-buffered Adaptive FixedOrStored and DynamicOrFixedOrStored encoders constructed from mutable RGB8 and straight-RGBA8 sources may accept the valid PNG signature/IHDR/IDAT prefix after a source mutation, but the first terminal pull writes zero bytes and leaves that offered lease unchanged; later pulls return the identical sticky error with zero writes and unchanged leases while the recorded accepted-prefix total remains stable.
    - Test 7: Public Adaptive selection regression proves Fixed still wins a complete-PNG equality against Stored and Dynamic still replaces its baseline only on a strict complete-PNG win, falling back to the same Fixed-or-Stored baseline when no strict Dynamic win exists.
  </behavior>
  <action>Start RED by extending the committed `*adaptive match cursor*` white-box suite rather than weakening its Fixed=4 and Dynamic=8 reproduction. Use two-row RGB8 and straight-RGBA8 periodic fixtures with rows short enough that maximum matcher look-ahead crosses multiple row boundaries. Add package-private test accessors only for the individual facts returned from the real Stored, Fixed, Dynamic frequency/fingerprint, Dynamic bit-count, and selected replay walks, plus match-cursor logical/producer/retained positions and ring content. Do not calculate expected planner counts from a copied formula: prove the four and eight aggregate facts by summing each returned two-row walk, and prove each walk's candidate and selected-residual facts independently.

Add a ring-boundary test that intentionally fills, probes, and wraps the fixed window while checking the producer may move ahead without changing logical consumption. Add Fixed and Dynamic pending-state tests that snapshot every retained slot and all cursor positions before the first `present()`, compare after a repeated `present()`, then compare after one successful acknowledgement.

In `stream_encode_test.mbt`, add public caller-buffered tests named `PNG adaptive FixedOrStored public replay mutation is sticky` and `PNG adaptive DynamicOrFixedOrStored public replay mutation is sticky`. For each mutable RGB8 and straight-RGBA8 source, construct the named Adaptive combined encoder, mutate the source before its first pull, and record every successfully acknowledged prefix byte/lease. Permit the already valid signature/IHDR/IDAT prefix to be accepted. On the first terminal pull, assert the offered lease has zero written bytes and unchanged contents, capture the established replay-drift error, and record the accepted-prefix total. On every later pull, assert the same error, zero written bytes, unchanged offered lease, and the same accepted-prefix total. Add `PNG adaptive compression selection retains Fixed equality and Dynamic strict winner` using existing complete-PNG plan observability plus public drain/decode evidence: assert Fixed is selected on equality with Stored, Dynamic is selected only when strictly shorter, and the non-winning Dynamic case preserves its Fixed-or-Stored baseline. Keep all public declarations, policy, QOI, fixtures, and quality work out of scope. Run the focused native selectors and confirm the new cursor/accounting assertions are RED against the restored Plan 04 baseline.</action>
  <verify>
    <automated>$targetRoot = Join-Path (Resolve-Path '_build') 'phase39-06-red'; if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null; try { moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f '*adaptive match cursor*'; if ($LASTEXITCODE -eq 0) { throw 'RED cursor selector unexpectedly passed before the real matcher cursor exists' }; moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f 'PNG adaptive FixedOrStored public replay mutation is sticky'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f 'PNG adaptive DynamicOrFixedOrStored public replay mutation is sticky'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f 'PNG adaptive compression selection retains Fixed equality and Dynamic strict winner'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } }</automated>
  </verify>
  <done>The failing regression distinguishes actual planning/replay walks from synthetic facts, preserves Fixed=4 and Dynamic=8 evidence, proves the required deep-successor acknowledgement boundary, and locks public mutation-sticky and selection-rule contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one owned cursor source for planning and replay</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <behavior>
    - Test 1: Every Task 1 per-walk fact, Fixed=4, Dynamic=8, retained-window, and repeated-present assertion passes on js, wasm, wasm-gc, and native.
    - Test 2: Stored, Fixed, and Dynamic Adaptive routes get all planning and replay bytes from the forward cursor, while None retains `_png_fixed_scanline_byte` and all frozen compatibility vectors remain byte-identical.
    - Test 3: Fixed/Dynamic candidate selection, Dynamic fingerprint replay protection, eager/chunk atomic failures, and accepted-prefix semantics are unchanged.
  </behavior>
  <action>Implement a private `PngFilteredMatchCursor` (or equivalent) around the existing forward `PngFilteredCursor`. Store five independently meaningful values: the producer/source cursor, logical consumed absolute position, produced-exclusive absolute position, retained-start absolute position, and a deep-owned 262-byte circular window. Its `ensure` operation may only advance the producer and append bytes while preserving the retained-window invariant; its absolute `read` operation may only read an already retained byte; its consume operation advances logical position only after the current literal or match is actually emitted. Do not treat matcher look-ahead as consumption, and do not alias mutable window storage between current and successor state. Do not allocate a selected-filter table, full image, scanline image, token stream, or encoded-output buffer.

Refactor the real Adaptive candidate/replay operations, not merely their accounting. Make Stored candidate traversal and replay own forward cursor state; make `_png_fixed_plan` return the facts accumulated by its actual match cursor; make `_png_dynamic_frequencies` return its own frequency/fingerprint facts; and make `_png_dynamic_plan` return its exact-bit walk facts. Replace direct Adaptive arbitrary-byte calls in these walks and in Fixed/Dynamic emission with cursor `ensure`/`read`/consume operations. In preflight, remove the independent substitute traversals and compose only returned candidate facts plus one bounded selected-replay prediction before existing checked output/work validation and the sole budget charge. The ledger must still cover all requested strategy passes even when Dynamic loses and replay uses the existing Fixed-or-Stored winner.

Carry an independently deep-owned cursor successor in `PngFixedState` and `PngDynamicState`; make the Stored path use the corresponding forward source. `present()` may construct/fill its private successor but must leave committed state untouched. Commit the successor only in `acknowledge()` together with the existing pending byte, DEFLATE, Adler, CRC, matcher-work, and Dynamic fingerprint state. Preserve existing bytes, tie comparisons, public names/signatures, atomic failure categories, source-mutation sticky behavior, and all None branches. Run RED to GREEN, format only these two MoonBit implementation files, and leave all target roots created by verification absent.</action>
  <verify>
    <automated>$targetRoots = @((Join-Path (Resolve-Path '_build') 'phase39-06-green'), (Join-Path (Resolve-Path '_build') 'phase39-06-check')); foreach ($targetRoot in $targetRoots) { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null }; try { $filters = @('*adaptive match cursor*','*adaptive cursor work ledger*','PNG adaptive combined eager routes','PNG adaptive combined chunk routes','PNG adaptive combined admission is atomic','PNG adaptive FixedOrStored public replay mutation is sticky','PNG adaptive DynamicOrFixedOrStored public replay mutation is sticky','PNG adaptive compression selection retains Fixed equality and Dynamic strict winner','PNG filter strategy eager frozen compatibility vectors','PNG filter strategy chunk frozen compatibility vectors','PNG compression strategy legacy eager constructors retain frozen stored bytes','PNG compression strategy legacy chunk constructors retain frozen stored bytes','PNG fixed-or-stored public admission is atomic','PNG dynamic public exact and one-less admission is atomic'); foreach ($target in @('js','wasm','wasm-gc','native')) { $targetDir = Join-Path $targetRoots[0] $target; foreach ($filter in $filters) { moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen -f $filter; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } }; moon -C modules/mb-image check png --target all --target-dir $targetRoots[1] --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { foreach ($targetRoot in $targetRoots) { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } } }</automated>
  </verify>
  <done>All Adaptive Stored/Fixed/Dynamic planning and selected replay facts originate from bounded real cursors; the complete targeted public Adaptive/chunk/atomic/frozen-None matrix passes on all four targets, and every verification target root is removed.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Deterministic bounded Adaptive filtering before compression planning with atomic eager/chunk behavior | 39-06 | COVERED | Replaces the proven synthetic/stateless split with one owned source for real candidate/replay walks.
REQ | PNGF-02 | Deterministic five-candidate row selection and stable winner | 39-06 | COVERED | The forward producer retains the existing candidate order/tie rule and tests one selection per entered row.
REQ | PNGF-03 | Bounded Stored/Fixed/Dynamic integration and atomic admission with no image staging | 39-06 | COVERED | Returned real facts, deep 262-byte window ownership, exact/one-less admission, and all-target public atomic evidence close the gap.
RESEARCH | — | Preserve None bytes and Fixed/Dynamic selection ties | 39-06 | COVERED | Task 2 retains the legacy provider/tie comparisons and runs frozen vector coverage on each target.
RESEARCH | — | Forward-only fixed-state replay and acknowledgement safety | 39-06 | COVERED | Task 1 snapshots committed ring/cursor state; Task 2 commits only the deep successor in acknowledge.
RESEARCH | — | Retain Dynamic replay-drift and compression-selection behavior | 39-06 | COVERED | Task 1 adds public mutable-source sticky regressions and Fixed-equality/Dynamic-strict-win assertions.
RESEARCH | — | Avoid filtered-image or row-table staging | 39-06 | COVERED | The only retained byte storage is the fixed 262-slot window with explicit absolute bounds.
CONTEXT | — | No Phase 39 CONTEXT.md or locked D-NN decisions exist | — | COVERED | No deferred idea or scope expansion is introduced.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image → forward Adaptive producer | Caller-controlled pixels determine filter winners and residual bytes. |
| Real cursor walks → preflight ledger | Every candidate/replay source traversal must be charged before output visibility. |
| Pending successor → writer/caller acknowledgement | Preview may read ahead but cannot mutate the committed producer, consumer, facts, or retained window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06-01 | Denial of service | Adaptive match producer/window | high | mitigate | Task 2 fixes capacity at 262 and validates retained absolute-byte bounds; Task 1 forces look-ahead/history wrap. |
| T-39-06-02 | Denial of service | Preflight work admission | high | mitigate | Task 2 composes checked facts returned by every real candidate and selected replay walk before the single budget charge; Task 1 proves exact/one-less rejection. |
| T-39-06-03 | Tampering | Fixed/Dynamic byte source and plan facts | high | mitigate | Task 2 removes the stateless Adaptive matcher source and returns actual Fixed/frequency/bit facts; Task 1 preserves Fixed=4 and Dynamic=8 regressions. |
| T-39-06-04 | Tampering | Pending cursor ownership | high | mitigate | Task 2 deep-copies successor window state and commits only in acknowledge; Task 1 compares all committed ring slots across repeated present calls. |
| T-39-06-05 | Tampering | Legacy bytes and compression selection | high | mitigate | Task 1 adds public Fixed-equality/Dynamic-strict-win evidence, and Task 2 preserves None provider/tie rules; the four-target public frozen/vector matrix verifies them. |
| T-39-06-06 | Tampering | Caller-buffered Adaptive replay after source mutation | high | mitigate | Task 1 adds public Fixed and Dynamic mutation-before-first-lease sticky-error tests; Task 2 preserves the existing fingerprint and pending-state boundary. |
</threat_model>

<verification>
- RED/GREEN: preserve the committed native failure shape, then pass the real per-walk facts: Fixed planning aggregate 4 rows and Dynamic planning aggregate 8 rows, with each named walk returning its own facts.
- Ownership: prove producer look-ahead can populate a successor window without consuming or mutating committed state; acceptance is the sole state-commit boundary.
- Atomicity: exact work/budget drains; one-less rejects before eager output or a caller lease for Stored, FixedOrStored, and DynamicOrFixedOrStored.
- Portable public matrix: run private cursor/ledger, eager/chunk Adaptive, atomic, public Fixed/Dynamic mutation-sticky, public selection-rule, legacy/frozen None, Fixed admission, and Dynamic admission selectors independently on js, wasm, wasm-gc, and native; then check the PNG package.
- Cleanup: `finally` removes every `phase39-06-red`, `phase39-06-green`, and `phase39-06-check` target root whether a target passes or fails.
</verification>

<success_criteria>
- No real Adaptive Fixed/Dynamic matcher or replay operation can reach the stateless row selector; every requested byte is produced/reused through an owned bounded cursor.
- Preflight's filter work is a checked sum of actual returned Stored, Fixed, Dynamic-frequency, Dynamic-bit, and selected-replay facts, preserving pre-output atomic failure.
- The 262-byte window has independent producer, consumer, produced-exclusive, and retained-start state; preview cannot mutate it, while acknowledge commits one deep successor.
- Existing bytes, compression ties, public API, public mutation-sticky behavior, and all targeted four-portable-target Adaptive/chunk/atomic/frozen-None evidence remain intact, with no persistent target directory.
</success_criteria>

<output>
Create `.planning/phases/39-bounded-filter-planning-and-replay/39-06-SUMMARY.md` when done.
</output>
