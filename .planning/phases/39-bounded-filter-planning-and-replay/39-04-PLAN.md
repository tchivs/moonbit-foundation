---
phase: 39-bounded-filter-planning-and-replay
plan: "04"
type: tdd
wave: 4
depends_on: [39-01, 39-02, 39-03]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
gap_closure: true
requirements: [PNGF-02, PNGF-03]
must_haves:
  truths:
    - "For Adaptive, a forward traversal resolves each PNG row winner exactly once when that traversal first enters the row, then serves its filter tag and residual bytes from bounded cursor state."
    - "Preflight charges the actual selector and selected-residual work of every requested compression-planning traversal and of the selected acknowledgement-safe replay traversal before either encoder exposes output."
    - "Stored, FixedOrStored, and DynamicOrFixedOrStored retain their existing output-selection ties, public surface, atomic failures, and filter-None bytes without full-image staging."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: "Filter-cursor traversal facts and an exact, strategy-specific adaptive preflight work ledger."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Acknowledgement-safe bounded forward adaptive cursor state shared by Stored, Fixed, and Dynamic replay."
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: "White-box admission tests that compare charged filter work with actual planning/replay traversal facts for every compression strategy."
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: "White-box cursor tests for once-per-row selection, bounded look-back/look-ahead replay, and acknowledgement stability."
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Preflight receives real per-pass PngFilteredTraversalFacts and the machine carries an equivalent bounded cursor into selected replay."
      pattern: "PngFilteredCursor|PngFilteredTraversalFacts|adaptive_filter_work"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "Stored, fixed, and dynamic emission obtain filtered bytes through the cursor instead of the stateless adaptive byte supplier."
      pattern: "filtered.*cursor|matcher_work|replay"
    - from: modules/mb-image/png/encode_wbtest.mbt
      to: modules/mb-image/png/stream_encode_wbtest.mbt
      via: "The admission fact and fully acknowledged replay fact are compared across all three requested compression strategies."
      pattern: "adaptive cursor work ledger|traversal"
---

<objective>
Close the verified adaptive-filter accounting gap by replacing stateless per-byte row selection with bounded forward traversals whose measured selector/residual work is exactly admitted before output visibility. PNGF-02, PNGF-03.

Purpose: Preserve deterministic Adaptive PNG bytes and atomic resource semantics while making the preflight ledger describe the work actually performed by Stored, FixedOrStored, and DynamicOrFixedOrStored.
Output: A fixed-memory filter cursor, exact traversal-derived work accounting, and four-target white-box regression evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-VERIFICATION.md
@.planning/phases/39-bounded-filter-planning-and-replay/RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-01-SUMMARY.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-02-SUMMARY.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-03-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt

<interfaces>
- `_png_filtered_scanline_byte(...)` currently resolves `_png_filter_image_row_winner(...)` for every adaptive byte request; this is the verified undercharge source and must no longer be the adaptive replay entry point.
- `_png_fixed_plan`, `_png_dynamic_frequencies`, `_png_dynamic_plan`, and `_png_filtered_match_at` currently use random-position filtered-byte reads. Fixed and Dynamic preview state recomputes match reads until acknowledgement.
- `PngEncodeMachine::present()` holds `pending_fixed` or `pending_dynamic` until `acknowledge()` accepts the byte. Any cursor state used for a preview must follow that same clone-then-commit lifecycle.
- The Phase 39 compatibility contracts are locked by existing behavior: None remains on `_png_fixed_scanline_byte`; Fixed wins equal Stored output length; Dynamic replaces its baseline only on a strict complete-PNG win; public signatures are unchanged.
</interfaces>
</context>

<feature>
  <name>Exact bounded Adaptive filter traversal accounting</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - PNGF-02: An Adaptive traversal selects a row's existing stable None/Sub/Up/Average/Paeth winner only on its first entry to that row, then emits that tag and selected residual bytes without rescoring the row.
    - PNGF-03: The ledger admits each actual preflight cursor pass plus the selected Stored, Fixed, or Dynamic replay cursor pass; its selector and selected-residual units equal the traversal facts produced by those cursors.
    - PNGF-03: Cursor memory is limited to scalar row/filter state and a bounded DEFLATE look-ahead/history window sufficient for the existing maximum match length 258 and maximum distance 4; no selected-filter table, filtered-image buffer, token stream, or output staging is retained.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-test actual Adaptive cursor traversal and work admission</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - Test 1: A multi-row RGB8 fixture and a multi-row straight-RGBA8 fixture exercise row boundaries, selected tags, and short rows that force the matcher's 258-byte preview to cross more than one scanline.
    - Test 2: For each requested strategy Stored, FixedOrStored, and DynamicOrFixedOrStored, the tests inspect private traversal facts: resolved-row count is exactly `height` for each forward cursor pass, candidate residual work is exactly `height * 5 * row_bytes` for that pass, and selected-residual work follows the actual bytes traversed rather than the number of byte lookups.
    - Test 3: The preflight fact records every planning pass actually made by the selected strategy, including the necessary Fixed and Dynamic candidate walks, plus exactly one future replay pass. An exact work limit/budget succeeds; one unit below the returned selected work rejects before construction and leaves remaining budget unchanged.
    - Test 4: Draining a machine compares the committed replay cursor's accumulated facts with the replay portion admitted in preflight; repeated `present()` calls before acknowledgement leave cursor facts and row-selection count unchanged, while acknowledgement advances them once.
  </behavior>
  <action>Begin RED by replacing the current formula-only test with named internal tests `PNG adaptive cursor work ledger covers Stored planning and replay`, `PNG adaptive cursor work ledger covers FixedOrStored planning and replay`, and `PNG adaptive cursor work ledger covers DynamicOrFixedOrStored planning and replay`. Add small RGB8 and straight-RGBA8 sources whose scanlines are intentionally shorter than the bounded match preview so test coverage catches row transitions inside matcher look-ahead as well as ordinary tag/payload reads. Define the expected work from the cursor's private traversal facts, not a copied constant: each fresh Adaptive pass must report its actual row-resolution count, five-candidate residual total, and selected-residual total; match lookups that revisit cached positions must not create another row resolution or residual evaluation.

For every requested compression strategy, first construct with a generous budget to obtain the private selected plan and its complete planning/replay filter facts. Then construct with exactly that returned selected-work amount, drain the machine through `present()`/`acknowledge()`, and assert all charged preflight filter facts equal the sum of the actual completed planning and replay facts. Repeat with one unit less, assert the existing work rejection and unchanged budget, and assert no machine/lease has been produced. In the stream white-box test, observe a pending byte twice before acknowledgement and then once after acknowledgement to prove preview state does not resolve a row twice. Retain existing source-mutation sticky replay tests and add no public API, policy, QOI, fixture, or quality-lane work. Run the focused native selector and confirm it is RED because cursor facts and traversal-derived accounting do not yet exist.</action>
  <verify>
    <automated>$targetDir = Join-Path (Resolve-Path '_build') 'phase39-04-red'; if (Test-Path -LiteralPath $targetDir) { Remove-Item -LiteralPath $targetDir -Recurse -Force }; New-Item -ItemType Directory -Path $targetDir | Out-Null; try { moon -C modules/mb-image test png --target native --target-dir $targetDir --frozen -f '*adaptive cursor work ledger*'; if ($LASTEXITCODE -eq 0) { throw 'RED selector unexpectedly passed before cursor implementation' } } finally { if (Test-Path -LiteralPath $targetDir) { Remove-Item -LiteralPath $targetDir -Recurse -Force } }</automated>
  </verify>
  <done>The regression suite fails against the stateless supplier and expresses exact, runtime-observable cursor accounting for Stored, FixedOrStored, and DynamicOrFixedOrStored without relying on a hand-copied two-pass formula.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded forward cursor and traversal-derived ledger</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <behavior>
    - Test 1: The RED traversal-fact assertions pass for both channel counts and every requested compression strategy on all four portable targets.
    - Test 2: Stored, Fixed, and Dynamic output paths consume the bounded cursor; fixed/dynamic replay retains its planned matcher work and Dynamic fingerprint checks, and filter-None continues through its old provider.
    - Test 3: A pending output byte has a private next cursor state; failed/no-progress acknowledgement cannot commit it, and a successful acknowledgement cannot cause a row to be selected again.
  </behavior>
  <action>Implement the gap closure in the two encoder implementation files. Replace adaptive use of the stateless `_png_filtered_scanline_byte` with a private `PngFilteredCursor` (or equivalently named private state) that advances only forward. On first entering a row it calls the existing strict row-winner helper once, retains that winner while serving the tag and residual payload, and records private traversal facts for row selections, five-candidate score residuals, and selected residuals. Give matcher consumers a fixed ring/preview buffer that is bounded by the existing match look-ahead (at most 258) plus distance (at most 4), so Fixed/Dynamic match comparisons can revisit buffered bytes without re-entering a row scorer. Do not allocate by image dimensions, number of rows, scanlines, output length, or token count.

Refactor `_png_fixed_plan`, Dynamic frequency/bit calculation, Stored preflight traversal, and their replay paths to consume fresh forward cursor passes and return/retain their actual facts. Perform a real planning cursor traversal even for Stored so its admitted planning filter work corresponds to executed selection/residual operations. Dynamic may require more than one distinct planning pass because its Fixed baseline, frequency/fingerprint collection, and post-tree bit calculation have different data dependencies; preserve those passes and sum their measured facts rather than pretending they are one pass. After plan selection, add exactly the bounded cursor replay facts for the selected Stored, Fixed, or Dynamic emitter. Feed the resulting strategy-specific total into the existing checked limit validation and the single pre-output budget charge; never use the former fixed two-sweep formula as a substitute for the actual passes.

Carry cloneable cursor state through the machine's Stored route and through `PngFixedState`/`PngDynamicState` previews. `present()` must compute a private successor cursor and `acknowledge()` must be the sole commit point, alongside existing bit/adler/matcher state. Keep the existing Fixed-over-Stored and strict Dynamic selection rules, error categories, source-mutation replay detection, public factories, and all None code paths unchanged. Remove or make unreachable the adaptive branch that independently invokes the row winner for arbitrary byte positions. Run RED to GREEN and format only the two edited MoonBit files.</action>
  <verify>
    <automated>$targetRoot = Join-Path (Resolve-Path '_build') 'phase39-04-filter-cursor'; if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null; try { $filters = @('*adaptive cursor work ledger*','*adaptive planner cursor*','PNG adaptive combined eager routes','PNG adaptive combined chunk routes','PNG adaptive combined admission is atomic','PNG filter strategy eager frozen compatibility vectors','PNG filter strategy chunk frozen compatibility vectors'); foreach ($target in @('js','wasm','wasm-gc','native')) { $targetDir = Join-Path $targetRoot $target; foreach ($filter in $filters) { moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen -f $filter; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } }; moon -C modules/mb-image check png --target all --target-dir (Join-Path $targetRoot 'check') --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } }</automated>
  </verify>
  <done>Adaptive selector/residual work is measured and fully admitted for the real Stored, FixedOrStored, and DynamicOrFixedOrStored planning/replay passes; bounded acknowledgement-safe replay consumes the same cursor state, and no full-image staging or legacy None behavior is introduced.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Deterministic bounded adaptive filtering before compression planners with atomic eager/chunk behavior | 39-04 | COVERED | This closure repairs the only failed goal truth: selector work is both bounded and actually admitted before output.
REQ | PNGF-02 | Stable method-0 candidate winner and deterministic Adaptive rows | 39-04 | COVERED | Cursor reuses the existing five-candidate strict winner once per row and tests RGB8/RGBA8 row transitions.
REQ | PNGF-03 | Stored/Fixed/Dynamic integration, no image staging, atomic resource failures | 39-04 | COVERED | Strategy-specific real cursor passes, one checked preflight charge, bounded replay state, and exact/one-less tests close the verified gap.
RESEARCH | — | Preserve None bytes and Fixed/Dynamic strict tie behavior | 39-04 | COVERED | None keeps its provider; plan selection rules are explicit non-regression constraints.
RESEARCH | — | Forward-only fixed-state replay and acknowledgement safety | 39-04 | COVERED | Cursor preview state is cloned and commits only on acknowledgement.
RESEARCH | — | Adaptive work must represent actual plan/replay traversal | 39-04 | COVERED | Facts are accumulated from actual cursor passes, including Dynamic's necessary distinct planning passes.
CONTEXT | — | No Phase 39 CONTEXT.md or locked D-NN decisions exist | — | COVERED | No deferred idea is introduced.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image → Adaptive cursor | Caller-controlled pixels influence winner selection and residuals. |
| Cursor traversal → preflight resource charge | Cursor facts must describe every selector/residual operation that construction and replay can perform. |
| Preview cursor → Writer/caller lease acknowledgement | A pending byte must not make cursor progress visible before accepted output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04-01 | Denial of service | Adaptive filter selector and preflight | high | mitigate | Task 2 uses checked facts from every real planning and selected replay traversal, then validates limits and charges budget before machine creation. |
| T-39-04-02 | Tampering | Cursor/matcher byte source | high | mitigate | Task 2 gives matchers a bounded cursor buffer and retains existing matcher-work/fingerprint replay checks; Task 1 asserts facts after drain. |
| T-39-04-03 | Tampering | Acknowledgement-safe cursor state | high | mitigate | Task 2 carries private successor cursor state in pending replay states; Task 1 proves repeated presentation does not resolve another row. |
| T-39-04-04 | Denial of service | Filter-selection storage | high | mitigate | Task 2 bounds storage to scalar state plus 258-byte look-ahead and 4-byte distance; it forbids row tables and image/output/token buffers. |
| T-39-04-05 | Tampering | Legacy filter-None and compression ties | high | mitigate | Task 2 preserves the None provider, Fixed equality preference, and Dynamic strict-win rule; focused package checks run on all four targets. |
</threat_model>

<verification>
- RED/GREEN: add runtime-observable cursor-work facts first; prove the stateless implementation fails those tests, then make both RGB8/RGBA8 cases pass for each requested compression strategy.
- Atomicity: exact work admission drains to completion; one-less work rejects before a machine or caller lease exists and leaves the budget unchanged.
- Portable scope: run the two named cursor/ledger groups plus `PNG adaptive combined eager routes`, `PNG adaptive combined chunk routes`, `PNG adaptive combined admission is atomic`, and both frozen filter-strategy compatibility-vector tests independently on js, wasm, wasm-gc, and native, then check the PNG package. Use the one `_build/phase39-04-filter-cursor` target root and remove it in `finally` whether a target passes or fails.
</verification>

<success_criteria>
- No Adaptive path invokes full row selection once per filtered byte; every forward cursor traversal selects each entered row once and reuses bounded state for tag/residual reads and matcher revisits.
- The admitted adaptive ledger is the checked sum of actual selector and residual facts for all necessary requested-strategy planning passes and exactly one selected replay pass.
- Stored, FixedOrStored, and DynamicOrFixedOrStored complete with their existing output-selection semantics, atomic failures, and bounded memory; filter-None output remains unchanged.
- The focused RED/GREEN regression and scoped four-target evidence pass, leaving no persistent temporary target directory.
</success_criteria>

<output>
Create `.planning/phases/39-bounded-filter-planning-and-replay/39-04-SUMMARY.md` when done.
</output>
