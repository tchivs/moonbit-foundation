---
phase: 65-packed-rgba16-decode-model
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/model/descriptor.mbt
  - modules/mb-image/model/model_test.mbt
  - modules/mb-image/storage/storage_test.mbt
autonomous: true
requirements:
  - RGBA16DEC-01
must_haves:
  truths:
    - "A checked descriptor can represent one packed little-endian U16 straight-alpha RGBA pixel with an eight-byte row and encoded builtin-sRGB top-left identity."
    - "All four RGBA16 lanes are inspectable as Rlo,Rhi,Glo,Ghi,Blo,Bhi,Alo,Ahi through existing component-byte views."
    - "RGBA8, GrayAlpha16, and U8-only access behavior remain unchanged; invalid RGBA16 identity facts fail before an image is created."
  artifacts:
    - path: modules/mb-image/model/descriptor.mbt
      provides: ImageFormat::rgba16 and constrained packed RGBA16 identity validation.
      contains: "rgba16"
    - path: modules/mb-image/model/model_test.mbt
      provides: Public descriptor and invalid-identity evidence.
      contains: "rgba16"
    - path: modules/mb-image/storage/storage_test.mbt
      provides: Checked all-lane component-byte evidence and U8 accessor rejection.
      contains: "rgba16"
  key_links:
    - from: modules/mb-image/model/descriptor.mbt
      to: modules/mb-image/storage/views.mbt
      via: ImageDescriptor format/channel/component facts drive existing packed U16 component-byte offsets.
      pattern: "rgba16|component_byte"
---

<objective>
Add the narrow, checked packed RGBA16 result model required by later Type-6/16 PNG decoding without changing any existing U8 image contract.

Purpose: establish a public byte-order and metadata identity that a decoder can safely target.
Output: `ImageFormat::rgba16`, strict descriptor admission, and portable model/storage regressions.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/65-packed-rgba16-decode-model/65-CONTEXT.md
@.planning/phases/65-packed-rgba16-decode-model/65-RESEARCH.md
@modules/mb-image/model/descriptor.mbt
@modules/mb-image/model/model_test.mbt
@modules/mb-image/storage/views.mbt
@modules/mb-image/storage/storage_test.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the public packed RGBA16 descriptor identity</name>
  <files>modules/mb-image/model/descriptor.mbt, modules/mb-image/model/model_test.mbt</files>
  <behavior>
    - RED: model tests name the absent `ImageFormat::rgba16` and require an 8-byte packed U16 RGBA plane with straight-alpha encoded builtin-sRGB top-left metadata.
    - GREEN: the public format reports U16/RGBA/Packed/Little; a narrow validator rejects missing or premultiplied alpha, linear/non-builtin colour identity, non-top-left orientation, non-packed layout, and big-endian alternatives.
    - GREEN: existing `rgba8` and `graya16` valid descriptors remain accepted unchanged.
  </behavior>
  <action>Add focused failing public model tests by copying the nearest `graya16` descriptor fixtures and changing only channel count, 8-byte plane shape, and metadata facts. Implement `ImageFormat::rgba16()` in descriptor.mbt. Add a dedicated RGBA16 identity validator (not a broad change to existing RGBA alpha validation), invoke it from `ImageDescriptor::new`, and preserve all existing validation ordering and error conventions. Do not add PNG code, conversions, or storage types.</action>
  <verify><automated>moon -C modules/mb-image test model --target js --frozen --filter '*rgba16*'</automated></verify>
  <done>One packed straight-alpha U16 RGBA descriptor is publicly constructible only under the documented identity, with all neighbouring formats unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove all RGBA16 component lanes through existing storage views</name>
  <files>modules/mb-image/storage/storage_test.mbt</files>
  <behavior>
    - RED: a checked rgba16 image lacks an all-eight-lane storage-order proof.
    - GREEN: the existing mutable/view component-byte APIs write and read distinct Rlo,Rhi,Glo,Ghi,Blo,Bhi,Alo,Ahi values; invalid channel and component indexes fail; `get_byte` stays rejected for U16.
    - GREEN: model and storage packages pass on wasm, wasm-gc, js, and native.
  </behavior>
  <action>Add a local rgba16 descriptor fixture in storage_test.mbt following the graya16 fixture pattern. Allocate exactly eight bytes for one pixel under the existing budget, write all four U16 lane pairs through `set_component_byte`, and assert readback and bounds through `get_component_byte`. Assert the U8 accessor error, without changing views.mbt. Run the focused JS tests, then run the ordinary model and storage package tests on all targets serially.</action>
  <verify><automated>moon -C modules/mb-image test model --target all --frozen; moon -C modules/mb-image test storage --target all --frozen</automated></verify>
  <done>RGBA16 has concrete checked storage semantics on every target, and no U8-only path silently narrows it.</done>
</task>

</tasks>

<verification>
Run focused JS RED/GREEN checks during development, then exactly these ordinary target-wide package commands serially:

`moon -C modules/mb-image test model --target all --frozen`

`moon -C modules/mb-image test storage --target all --frozen`
</verification>

<success_criteria>
RGBA16DEC-01 is complete when the public descriptor exposes one exact packed little-endian U16 RGBA contract, every storage byte is observable without narrowing, invalid identity facts fail closed, and model/storage tests pass on all four targets.
</success_criteria>
