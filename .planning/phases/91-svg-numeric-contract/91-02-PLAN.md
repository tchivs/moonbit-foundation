---
phase: 91-svg-numeric-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 91-01
files_modified:
  - modules/mb-svg/svg/parse_wbtest.mbt
  - modules/mb-svg/svg/scene_wbtest.mbt
  - modules/mb-svg/svg/path_data_wbtest.mbt
  - modules/mb-svg/svg/transform_wbtest.mbt
  - modules/mb-svg/svg/lower_wbtest.mbt
autonomous: true
requirements:
  - SVGPR-01
must_haves:
  truths:
    - "Every documented scalar-ingress family has a focused valid finite boundary control in the existing mb-svg test organization, including dash-list ingress and each currently supported functional RGB/HSL component path (D-01, D-02)."
    - "Every documented derived route has an executable valid control for relative geometry, viewBox mapping, affine construction/composition, trigonometric transforms, rounded-rect ratio calculations, and circle/ellipse sampling (D-02)."
    - "Absent-attribute controls, finite singular-transform controls, and valid opacity draw-operation controls preserve their existing behavior (D-03, D-05, D-06)."
  artifacts:
    - "Focused numeric policy controls in parse_wbtest.mbt, scene_wbtest.mbt, path_data_wbtest.mbt, transform_wbtest.mbt, and lower_wbtest.mbt"
  key_links:
    - "Each focused test names a route identifier from docs/policies/svg-numeric-admission.md."
    - "The valid controls execute on js, wasm, wasm-gc, and native through the mb-svg package test command."
  assumptions:
    - "SVGPR-01 adjacency/equality remains outside scalar admission; no geometric touching behavior is introduced."
    - "SVGPR-01 empty/null remains outside scalar admission except that absent SVG attributes retain their documented default branch (D-03)."
    - "SVGPR-01 ordering remains outside scalar admission; no equal-item ordering promise is added."
---

<objective>
Expand the policy tracer into focused, executable route controls without changing production SVG parsing.

Purpose: Make the published source and derived route matrix reviewable in the existing test files and preserve valid SVG behavior before the Phase 92 parser migration.
Output: Five focused white-box test files tied to public route identifiers.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-svg-numeric-contract/91-CONTEXT.md
@.planning/phases/91-svg-numeric-contract/91-RESEARCH.md
@.planning/phases/91-svg-numeric-contract/91-PATTERNS.md
@docs/policies/svg-numeric-admission.md
@docs/rfcs/0008-mb-canvas-layer.md
</context>

## Artifacts this phase produces

- `modules/mb-svg/svg/parse_wbtest.mbt` — `test "numeric contract: lexical finite boundaries"`.
- `modules/mb-svg/svg/scene_wbtest.mbt` — `test "numeric contract: scene ingress and absent controls"`.
- `modules/mb-svg/svg/path_data_wbtest.mbt` — `test "numeric contract: path source and relative controls"`.
- `modules/mb-svg/svg/transform_wbtest.mbt` — `test "numeric contract: affine and trigonometric controls"`.
- `modules/mb-svg/svg/lower_wbtest.mbt` — `test "numeric contract: viewBox and opacity lower controls"`.

## Source coverage audit

SOURCE | ID | Feature or constraint | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Target-neutral route evidence for supported ingress and derived paths | 02 | COVERED | Focused existing-file controls execute on all targets.
REQ | SVGPR-01 | Route-matrix tests for every supported scalar ingress and derivation | 02 | COVERED | Test groups mirror the policy route identifiers.
RESEARCH | — | Source lexical, scene, path, transform, trig, viewBox, lower, and paint routes | 02 | COVERED | Each owns an explicit control in one of five test files, including dash-list ingress, supported functional RGB/HSL components, rounded-rect ratio, and circle/ellipse sampling.
RESEARCH | — | Stable error fields and invalid-present contract | 01 | COVERED | Plan 01 is the policy authority; this plan does not alter parser behavior.
CONTEXT | D-01 | Resource-derived envelope | 02 | COVERED | Boundary controls use the published inclusive limit.
CONTEXT | D-02 | Source plus derived route coverage | 02 | COVERED | Route identifiers are exercised in dedicated tests.
CONTEXT | D-03 | Absent/default distinction | 02 | COVERED | Scene controls retain absent root, points, and inheritance behavior.
CONTEXT | D-04 | Stable error fields | 01 | COVERED | Field contract is already published in Plan 01.
CONTEXT | D-05 | Finite singular transforms | 02 | COVERED | `scale(0)` remains a successful transform control.
CONTEXT | D-06 | Pre-scene ownership and unchanged opacity/layer behavior | 02 | COVERED | Lower test validates existing valid scene output only; no canvas file changes.

<tasks>

<task type="auto">
  <name>Task 1: Add lexical and scene-family numeric route controls</name>
  <read_first>
    docs/policies/svg-numeric-admission.md — route identifiers and envelope;
    modules/mb-svg/svg/parse_wbtest.mbt — parse_length, parse_viewbox, and parse_number_list patterns;
    modules/mb-svg/svg/scene_wbtest.mbt — parse_svg and absent/inheritance controls;
    modules/mb-svg/svg/length.mbt — length and list scalar ingress;
    modules/mb-svg/svg/scene.mbt — root, geometry, points, and paint scalar seams;
    .planning/phases/91-svg-numeric-contract/91-CONTEXT.md — D-01, D-02, D-03.
  </read_first>
  <files>modules/mb-svg/svg/parse_wbtest.mbt, modules/mb-svg/svg/scene_wbtest.mbt</files>
  <action>Add `test "numeric contract: lexical finite boundaries"` to exercise `parse_length`, `parse_number_list`, and exact-four-value viewBox input at the documented finite endpoints, using explicit `Result` matches and showable value assertions. Add `test "numeric contract: scene ingress and absent controls"` at the `parse_svg` boundary: cover finite boundary geometry for rect, circle, ellipse, line, polygon/polyline points, root width/height/viewBox, and presentational style/colour numeric paths; add the individual `paint-dash-list-ingress` row with a finite `stroke-dasharray` list and assert its resolved dash values. Add individual rows for each currently accepted functional component path: numeric RGB, percent RGB, numeric HSL hue, HSL saturation percent, and HSL lightness percent, using lowercase comma-separated `rgb()`/`rgba()` or `hsl()`/`hsla()` spellings and asserting the resulting resolved paint channels. Do not add controls for CSS Color 4 space/slash forms, `hwb()`, `lab()`, `lch()`, `color()`, or the ignored fourth alpha component because they are unsupported/non-ingress in the current parser. Pair the supported-source rows with absent root, absent points, and inherited paint controls that preserve the existing D-03 default branch. Give each row the route identifier from the policy document. Retain the files' existing CoreError discipline by asserting success values and booleans rather than inspecting rendered errors or aborting. These are Phase 91 valid finite/boundary controls; do not add behavior-changing rejection assertions before Phase 92. Do not modify production source files.
  </action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Lexical, root/viewBox, shape, points, dash-list, and paint ingress routes named by the policy have focused valid controls. The five individual supported functional-colour routes—numeric RGB, percent RGB, numeric HSL hue, HSL saturation percent, and HSL lightness percent—assert resolved paint values; unsupported colour forms have no ingress controls. Absent-attribute behavior is exercised separately from present finite values, and the package suite passes on all production targets.</acceptance_criteria>
  <done>The first half of the source route matrix is represented by focused, all-target executable controls in the existing parser and scene test files.</done>
</task>

<task type="auto">
  <name>Task 2: Add path, affine, derivation, and valid-lowering route controls</name>
  <read_first>
    docs/policies/svg-numeric-admission.md — derived route identifiers;
    modules/mb-svg/svg/path_data_wbtest.mbt — command-family test form;
    modules/mb-svg/svg/transform_wbtest.mbt — parse_transform and Affine2 assertion form;
    modules/mb-svg/svg/lower_wbtest.mbt — DrawOp sequence and opacity controls;
    modules/mb-svg/svg/{path_data,transform,lower}.mbt — relative, affine, trigonometric, viewBox, and lowering seams;
    docs/rfcs/0008-mb-canvas-layer.md — layer ownership and opacity boundary;
    .planning/phases/91-svg-numeric-contract/91-CONTEXT.md — D-02, D-05, D-06.
  </read_first>
  <files>modules/mb-svg/svg/path_data_wbtest.mbt, modules/mb-svg/svg/transform_wbtest.mbt, modules/mb-svg/svg/lower_wbtest.mbt</files>
  <action>Add `test "numeric contract: path source and relative controls"` using finite numeric examples for `M`, `L`, `H`, `V`, `C`, `Q`, `S`, `T`, and `A`, including a relative-coordinate result at the documented boundary. Add `test "numeric contract: affine and trigonometric controls"` for matrix, translate, scale, rotate, skewX, skewY, and ordered transform composition; retain `scale(0)` as a successful finite control per D-05 and use the existing tolerance form for trigonometric outputs. Add `test "numeric contract: viewBox and opacity lower controls"` that parses and lowers a valid finite viewBox mapping, confirms the expected drawing-operation path, and preserves existing group/element opacity `PushLayer`/`PopLayer` ordering per D-06. In that lower test, add an individual `lower-rounded-rect-ratio` control with finite dimensions and radii that exercise rounded-rect clamp/ratio arithmetic, then assert a closed finite path with its expected command count. Add an individual `lower-circle-sampling` control at a documented finite boundary that lowers a circle or ellipse through the 32-segment sampler, then assert the expected closed sample-command count and finite sampled coordinates. Associate every case with its public source or derived route identifier, use typed scene/lower results only, and leave `mb-canvas` and all production SVG sources untouched. These remain Phase 91 valid finite/boundary and lowering-preservation controls; do not add rejection assertions reserved for Phase 92.
  </action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Path command families, relative arithmetic, affine construction/composition, trigonometric transforms, viewBox lowering, valid rounded-rect ratio output, finite circle/ellipse sampling output, and valid opacity-layer sequencing all have individually route-identified finite controls. The all-target test run retains the finite singular-transform and RFC 0008 opacity invariants.</acceptance_criteria>
  <done>The entire published route matrix has focused all-target source or derived coverage, while parsing and canvas implementation ownership remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SVG scalar text -> parser and scene test controls | Untrusted numeric routes need complete policy coverage before runtime enforcement changes. |
| Valid scene -> lower -> canvas draw operations | Valid controls must catch an accidental ownership or opacity-layer semantic change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-91-04 | DoS | untested scalar source and arithmetic routes | high | mitigate | Add all-target controls for each documented lexical, scene, path, affine, trigonometric, and lower derivation route. |
| T-91-05 | Tampering | valid SVG compatibility and opacity semantics | medium | mitigate | Assert valid finite lowering and DrawOp layer order in the existing focused test files without changing mb-canvas. |
| T-91-06 | Repudiation | policy-to-test traceability | medium | mitigate | Label each control with the public route identifier so review can map coverage to the documented contract. |
| T-91-NA | Spoofing / Information disclosure / Elevation | identity, secrets, authorization | low | accept | Not applicable: these tests and policy controls process no identity, secret, transport, or privilege data. |
| T-91-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this phase. |

## ASVS L1 V5 Input-Validation Mapping

| ASVS control | Boundary and ownership | Phase 91 evidence |
|---|---|---|
| V5 Input Validation | Untrusted SVG source scalars and derived arithmetic values are owned by `mb-svg` before `SceneNode`; explicit supplied values and absent/default branches remain distinct per D-03, and lower/canvas stay total valid-scene consumers per D-06. | Route-identified, valid finite/boundary controls cover every supported ingress and derivation on js, wasm, wasm-gc, and native; the policy-to-test mapping makes Phase 92 rejection enforcement auditable without changing behavior in Phase 91. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target all --frozen`. Review the five test files against the policy route identifiers and confirm the finite singular-transform and opacity-layer controls remain successful.
</verification>

<success_criteria>
All supported scalar source and derivation categories have visible, focused, all-target route controls, and valid parse-to-lower behavior stays stable without a production parser or canvas change.
</success_criteria>

<output>
Create `.planning/phases/91-svg-numeric-contract/91-02-SUMMARY.md` when done.
</output>
