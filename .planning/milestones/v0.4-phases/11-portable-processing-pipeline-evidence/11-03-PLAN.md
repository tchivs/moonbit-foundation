---
phase: 11-portable-processing-pipeline-evidence
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/ppm/ppm_bench.mbt
  - benchmarks/ppm/phase-11-resize-composite-baseline.md
autonomous: true
requirements: [INTEG-03]
must_haves:
  truths:
    - "A native local benchmark named ppm/pipeline/resize-composite/256x256 measures the declared public decode, resize, conversion, source-over, conversion, and encode sequence after correctness validation."
    - "A Phase-11-local record declares the exact workload, command, native target/toolchain, one warmup, seven captures, output/correctness digests, and observed capture data without performance claims."
  artifacts:
    - "benchmarks/ppm/ppm_bench.mbt"
    - "benchmarks/ppm/phase-11-resize-composite-baseline.md"
  key_links:
    - "The record’s command invokes only moon -C benchmarks bench --release --target native --frozen ppm and the benchmark test uses public codec and ops APIs."
---

<objective>
Create isolated local native timing evidence for the resize-and-composite public pipeline, per D-05 and D-06, without changing release qualification.

Purpose: let maintainers reproduce one declared workload and compare it with honest recorded captures while keeping the release harness, schema, and baseline independent.
Output: one native benchmark case and a Phase-11-local record populated by an untimed warmup plus seven actual captures.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-CONTEXT.md
@.planning/phases/11-portable-processing-pipeline-evidence/11-RESEARCH.md
@benchmarks/ppm/ppm_bench.mbt
@benchmarks/ppm/moon.pkg
@benchmarks/ppm/moon.mod.json
@scripts/benchmarks/Invoke-PpmBenchmarks.ps1
@release/qualification/ppm-native-release-baseline.json

<interfaces>
Existing benchmark conventions in ppm_bench.mbt:
  fn ppm_bytes(width, height, flipped) -> Bytes
  fn decode_public(source, width, height) -> @codec.DecodeResult
  fn encode_public(image, width, height) -> (@codec.EncodeResult, @io.MemoryWriter)
  fn require_benchmark(condition, label) -> Unit
  it.bench(fn() { it.keep(value) }, count=1)

New workload must remain a public consumer: strict PPM decode; @ops.resize_nearest; @ops.rgb8_to_straight_rgba8; @ops.composite_source_over; @ops.straight_rgba8_to_rgb8; strict PPM encode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the isolated native resize-composite benchmark workload</name>
  <files>benchmarks/ppm/ppm_bench.mbt</files>
  <action>Add one test named ppm/pipeline/resize-composite/256x256 per D-05 and D-06. Construct deterministic opaque strict PPM sources outside the measured closure: a 128x128 foreground and 256x256 destination. Before timing, execute the exact public pipeline once—decode both inputs, nearest-resize foreground to 256x256, convert each RGB8 image to straight RGBA8, composite foreground over destination, convert the opaque result to RGB8, and encode PPM—and validate output extent plus a deterministic correctness digest. Benchmark that same pipeline with the existing @bench.T and count=1. Keep all budgets/codec/parser limits explicit and fresh. Do not edit the existing decode/encode/flip benchmark cases, release harness, release schema/baseline, or add external dependencies/algorithms.</action>
  <verify>
    <automated>$output = moon -C benchmarks bench --release --target native --frozen ppm 2>&1; if ($LASTEXITCODE -ne 0) { throw 'native benchmark failed' }; if ((($output | Out-String) -notmatch [regex]::Escape('ppm/pipeline/resize-composite/256x256'))) { throw 'named resize-composite benchmark did not execute' }</automated>
  </verify>
  <done>The native benchmark suite exposes a single separately named resize-and-composite public pipeline workload with correctness proven before timing and no release-qualification code path involved.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and document the local Phase-11 benchmark baseline</name>
  <files>benchmarks/ppm/phase-11-resize-composite-baseline.md</files>
  <action>Create the Phase-11-local Markdown record after Task 1 works, per D-05 and D-06. Declare the workload name, 128x128 foreground to 256x256 resize, 256x256 destination, operation sequence, opaque PPM inputs, native release target, exact Moon command, installed moon/moonc/moonrun identities, source/correctness digests, and environment facts needed to interpret the run. Run one untimed warmup, then run exactly seven separate captures of the direct frozen native benchmark command; record per-capture timestamps and the named workload’s parsed timing summary or complete retained console output/digest, plus a transparent aggregate derived from those seven observations. State only that this is local reproducibility/regression evidence and omit thresholds, performance promises, registry/credential/GUI/hosted-workflow dependencies, and any invocation of scripts/benchmarks/Invoke-PpmBenchmarks.ps1 or release/qualification artifacts.</action>
  <verify>
    <automated>$record = 'benchmarks/ppm/phase-11-resize-composite-baseline.md'; if (-not (Test-Path -LiteralPath $record -PathType Leaf)) { throw 'baseline record is not a leaf file' }; $text = Get-Content -Raw -LiteralPath $record; foreach ($field in @('ppm/pipeline/resize-composite/256x256','128x128','256x256','moon -C benchmarks bench --release --target native --frozen ppm','moon','moonc','moonrun','source digest','correctness digest','warmup')) { if ($text -notmatch [regex]::Escape($field)) { throw "baseline missing provenance field: $field" } }; $captures = (Select-String -Path $record -Pattern '^\| [1-7] \| .*\d{4}-\d{2}-\d{2}').Count; if ($captures -ne 7) { throw "expected seven timestamped captures, found $captures" }; $releasePath = 'release' + '/qualification'; $releaseHarness = 'Invoke' + '-PpmBenchmarks.ps1'; if ($text -match [regex]::Escape($releasePath) -or $text -match [regex]::Escape($releaseHarness)) { throw 'baseline must not reference the release harness' }</automated>
  </verify>
  <done>The tracked Phase-11-local record contains one warmup and seven actual direct-command captures with declared workload, command, target/toolchain, correctness/source identity, and transparent non-marketing interpretation; no release artifact is changed or required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deterministic benchmark corpus → public codec/ops route | Generated PPM input crosses strict decode and checked operation boundaries. |
| local timing console output → tracked evidence record | Host-dependent measurements must retain their provenance and not become release authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Tampering | timing evidence | medium | mitigate | Record command, workload/source and correctness digests, toolchain identity, one warmup, and seven individually identifiable captures in a Phase-11-local record. |
| T-11-08 | Denial of service | benchmark pipeline allocations | medium | mitigate | Reuse bounded strict codec limits and explicit fresh budgets; run correctness validation before the measured closure. |
| T-11-09 | Elevation of privilege | benchmark execution | low | accept | The command is a local frozen Moon benchmark with no credentials, registry access, GUI, hosted workflow, or release-script invocation. |
</threat_model>

<verification>
Run the native benchmark command successfully, then verify the Phase-11 record has one warmup, exactly seven capture rows, the required direct command/toolchain/workload fields, and no dependency on the release benchmark harness.
</verification>

<success_criteria>
INTEG-03 is met by a reproducible, local native resize-and-composite benchmark workload and an honest seven-capture Phase-11 record isolated from release automation.
</success_criteria>

<output>
Create `.planning/phases/11-portable-processing-pipeline-evidence/11-03-SUMMARY.md` when done.
</output>
