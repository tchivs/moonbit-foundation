# v0.26 Indexed8 Adam7 — Cross-Phase Integration Audit

**Audited:** 2026-07-24  
**Scope:** Phase 81 → Phase 82; `INDEXADAM7-01` through `INDEXADAM7-06`  
**Verdict:** **PASS**

## Integration Check Complete

Phase 81's additive Indexed8 Adam7 public seams both reach the one acknowledged `PngEncodeMachine`; Phase 82 drives that actual chunk seam through caller-owned leases and consumes the accepted output as independent PNG/decode evidence. No cross-phase connection is absent.

### Wiring Summary

**Connected:** 6 verified end-to-end links  
**Orphaned:** 0 phase exports  
**Missing:** 0 expected connections

| From | To | Verification | Status |
|---|---|---|---|
| Phase 81 eager API | Shared machine | `encode_indexed8_with_interlace_strategy` constructs `PngEncodeMachine::new_with_indexed_profile(source, Eight, interlace_strategy, ...)`. | WIRED |
| Phase 81 chunk API | Shared machine | `new_indexed8_with_interlace_strategy` constructs that same profile-aware machine. | WIRED |
| Indexed8 Adam7 admission | Emitted raster | Preflight sums `_png_adam7_passes(width, height, 1UL, 8)` rows before frame/limit/budget facts; scalar emission uses the same geometry. | WIRED |
| Adam7 pass coordinates | Canonical source | Scalar pass helper maps `pass.x/y/dx/dy` and calls `PngIndexedImage::index_at`, without staging or another encoder. | WIRED |
| Phase 82 caller leases | Phase 81 eager contract | Hostile drain uses the explicit chunk selector, appends only `written` prefixes, and compares completed bytes with a fresh eager Adam7 result. | WIRED |
| Accepted chunk bytes | Independent evidence | `Bytes::from_array(output)` is parsed for chunks/CRC/raster and decoded with public `PngDecoder` for all 25 RGBA pixels. | WIRED |

### API Coverage

There are no HTTP/API routes in this MoonBit library milestone. Both public library selector families are consumed by executable tests: the eager selector is the parity oracle and the caller-buffered selector is the real producer under hostile leases.

**Consumed:** 2 public Adam7 selector families  
**Orphaned:** 0

### Auth Protection

Not applicable: v0.26 is a deterministic local PNG encoding library with no user/session-protected routes.

### E2E Flows

**Complete:** 2  
**Broken:** 0

| Flow | Trace | Result |
|---|---|---|
| Explicit eager Indexed8 Adam7 encode | `PngIndexedImage` → eager selector → shared machine/preflight → Adam7 scalar bytes → `IHDR → PLTE → tRNS? → IDAT → IEND` → public decoder | COMPLETE. The tests assert 5×5 Type-3/8 framing, canonical tRNS, CRCs, literal 36-byte seven-pass raster, RGB8/RGBA8 decode, and exact/one-less atomic admission. |
| Caller-buffered Indexed8 Adam7 encode | `PngIndexedImage` → Phase 81 chunk selector → same machine → `pull(MutByteLease)` under `[0,1]`, `[1]`, and ragged schedules → accepted prefixes → Phase 82 parser/decoder | COMPLETE. Tests prove byte parity, accepted-only totals, untouched tails, and sticky Failed/Finished outcomes. |

### Scope and Compatibility Boundary

**PASS — no low-bit Adam7 or scope creep found.**

- The only additive Adam7 public selectors are `encode_indexed8_with_interlace_strategy` and `new_indexed8_with_interlace_strategy`.
- Legacy `encode_indexed8` and `new_indexed8` explicitly forward `PngInterlaceStrategy::None`.
- Indexed1/2/4 eager and chunk constructors explicitly pass `None`; indexed preflight permits Adam7 only for `PngIndexedWireProfile::Eight` and rejects every other indexed profile with `indexed-adam7-depth`.
- Phase 82's implementation range (`4bf8664..226192f`) changes only `modules/mb-image/png/stream_encode_test.mbt`; no production transport, selector, model, FFI, staging, filter/compression, or wrapper was added.
- The full Phase 81→82 range changes the planned five PNG files plus planning artifacts. `git diff --check` reports only existing Markdown trailing whitespace in planning artifacts, not a source defect.

### Detailed Findings

#### Orphaned Exports

None.

#### Missing Connections

None.

#### Broken Flows

None.

#### Unprotected Routes

Not applicable.

#### Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| INDEXADAM7-01 | Phase 81 eager/chunk selectors → `new_with_indexed_profile(... Eight, requested interlace, ...)`; legacy wrappers → `None` | WIRED | — |
| INDEXADAM7-02 | Phase 81 preflight → `_png_adam7_passes(..., 1, 8)` → Stored/frame/budget facts; same geometry → scalar machine emitter → `index_at` | WIRED | — |
| INDEXADAM7-03 | Eager machine bytes → literal framing/CRC/pass-raster oracle → public `PngDecoder` RGB8/RGBA8 assertions | WIRED | — |
| INDEXADAM7-04 | Selected-layout facts → limits and one budget charge before construction/output; one-less checks → unchanged writer/budget | WIRED | — |
| INDEXADAM7-05 | Phase 82 hostile leases → Phase 81 explicit chunk selector → shared machine `pull` → prefix/tail/terminal assertions → eager parity | WIRED | — |
| INDEXADAM7-06 | Phase 82 chunk-origin bytes → chunk/CRC checks → literal Stored raster → public RGBA8 decode; frozen vectors → four-target gate | WIRED | — |

**Requirements with no cross-phase wiring:** None. `INDEXADAM7-01`–`04` establish the Phase 81 machine contract, and `INDEXADAM7-05`–`06` directly consume and qualify it in Phase 82.

### Test Evidence

| Command / evidence | Result |
|---|---|
| `moon -C modules/mb-image test png --target all --frozen` (this audit) | **291/291 passed** on each of wasm, wasm-gc, js, and native; exit code 0. |
| Phase 81 code review | Native 289 passed; all-target 289 passed per target; no findings. |
| Phase 82 code review | Focused native `png/stream_encode_test.mbt`: 88/88 passed; no findings. |
| Phase 82 verification | Recorded all-target gate: 291/291 passed on each target. |

## Final Verdict

**PASS.** All six `INDEXADAM7` requirements have a verified integration path, Phase 82 qualifies the actual Phase 81 machine rather than a parallel test path, and both eager and chunk E2E flows complete without a low-bit Adam7 expansion.
