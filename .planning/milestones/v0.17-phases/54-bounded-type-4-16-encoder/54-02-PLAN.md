---
phase: 54-bounded-type-4-16-encoder
plan: "02"
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16-03]
must_haves:
  truths:
    - "Every GrayAlpha16 strategy/filter construction rejection preserves zero eager output, unchanged resource accounting, and untouched caller-owned lease bytes."
    - "A post-admission GrayAlpha16 mutation invalidates selected Fixed and Dynamic replay before a next lease write, preserving accepted totals and returning a sticky terminal error."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "GrayAlpha16 all-pair atomic-admission and U16 replay-sticky regression helpers."
  key_links:
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Public graya16 combined constructors reach PngEncodeMachine before a PngChunkEncoder can expose a lease."
      pattern: "new_graya16_with_strategies"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/encode.mbt
      via: "Mutating a checked U16 GrayAlpha component after planning exercises source revision validation before destination.set."
      pattern: "set_component_byte"
---

<objective>
Prove that the new GrayAlpha16 profile retains the shared encoder's atomic resource admission and acknowledgement-safe caller-buffered replay contract.

Purpose: demonstrate that valid PNG framing is not the only property inherited by the profile; failures cannot expose output, alter budgets, or mutate caller leases.
Output: focused strategy-wide atomicity and U16 mutation-replay regressions in the existing PNG stream test package.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-bounded-type-4-16-encoder/54-CONTEXT.md
@.planning/phases/54-bounded-type-4-16-encoder/54-RESEARCH.md
@.planning/phases/54-bounded-type-4-16-encoder/54-PATTERNS.md
@.planning/phases/54-bounded-type-4-16-encoder/54-01-SUMMARY.md
@.planning/milestones/v0.15-phases/48-bounded-gray16-encoder-path/48-VERIFICATION.md
@.planning/milestones/v0.16-phases/51-bounded-gray-alpha-png-encoding/51-02-SUMMARY.md
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert pre-exposure atomic GrayAlpha16 admission for all strategy pairs</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - An incompatible source and each valid-source geometry, output, work, or budget envelope return matching typed eager and chunk errors for every compression/filter pair.
    - Each rejected eager route leaves the memory writer at position zero and preserves the preflight budget balance.
    - Each rejected chunk constructor leaves its budget balance and every byte of an already allocated sentinel lease unchanged.
  </behavior>
  <action>Per D-03 and D-06, add a `png_graya16_combined_public_reject` helper beside the Gray16 and GrayAlpha8 helpers. Cross Stored, FixedOrStored, and DynamicOrFixedOrStored with None and Adaptive. For eager construction, capture the resource budget before calling the public combined factory and assert the returned typed error arrives while the real memory writer remains empty. For the paired caller-buffered construction, allocate the existing sentinel owner before `new_graya16_with_strategies`, compare the structured error and unchanged budget with eager, then inspect every sentinel byte after the constructor returns `Err`. Invoke the helper with an incompatible descriptor plus valid GrayAlpha16 sources whose width, output, work, and budget envelopes each fail. Use the real public constructors and the existing limits/budget/error comparison helpers so the regression observes the one shared preflight transaction rather than a synthetic path.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter '*GrayAlpha16*'</automated>
  </verify>
  <done>All six GrayAlpha16 strategy/filter selections reject incompatible and resource-limited construction before writer output, budget debit, or caller-lease mutation, with matching typed eager and chunk outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove GrayAlpha16 Fixed and Dynamic replay errors are pre-write and sticky</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A FixedOrStored GrayAlpha16 encoder accepts its framing prefix, observes a checked gray or alpha U16 component mutation, then fails a later pull with zero written bytes and unchanged accepted total.
    - A DynamicOrFixedOrStored GrayAlpha16 encoder with an appropriate periodic source selects the dynamic route and retains the same post-mutation failure properties under Adaptive filtering.
    - The next lease after either failure receives the same terminal error, zero bytes, the same total, and no sentinel-byte mutation.
  </behavior>
  <action>Per D-04, mirror `png_gray16_replay_mutation_is_sticky` for the new four-byte profile. Construct public GrayAlpha16 encoders, accept only the framing prefix through real one-byte leases, assert the expected Fixed or Dynamic DEFLATE selection where the existing helper does so, and mutate a checked gray or alpha component with `set_component_byte` after planning. Pull into a sentinel lease and require a failure before any destination byte is set, with total progress equal to the accepted prefix. Pull again into a fresh sentinel lease and require the same structured terminal error and unchanged total. Exercise FixedOrStored with None and DynamicOrFixedOrStored with Adaptive so the regression covers both planned replay modes and the four-byte filter cursor. Retain the ordinary Stored route in the preceding six-pair parity and atomicity grids; do not add another replay implementation for it.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter '*GrayAlpha16*'</automated>
  </verify>
  <done>Fixed and Dynamic GrayAlpha16 replay validates source revision before mutating a caller lease; each post-mutation error is zero-write, preserves accepted progress, and remains sticky on a later lease.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller resource limits to preflight | Caller-controlled capability, dimension, output, work, and budget inputs decide whether the shared transaction may construct an encoder. |
| Planned source to caller-owned lease | A caller may mutate source backing after plan selection; replay must detect drift before the lease receives a byte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-05 | Denial of Service | GrayAlpha16 checked limits and budget ledger | high | mitigate | Task 1 crosses all resource rejection modes through each public strategy pair and asserts no resource-accounting change. |
| T-54-06 | Tampering | source revision validation before `pull` writes | high | mitigate | Task 2 mutates checked U16 gray/alpha bytes after plan selection and requires a zero-write sticky terminal before lease exposure. |
| T-54-07 | Tampering | caller-owned output lease | high | mitigate | Tasks 1-2 inspect every sentinel byte after constructor and replay failures. |
| T-54-08 | Repudiation | eager/chunk error parity | medium | mitigate | Task 1 compares the existing structured CoreError values and resource state instead of accepting generic failure. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target native --frozen --filter '*GrayAlpha16*'` after each task, then `moon -C modules/mb-image test png --target native --frozen` before completing the plan. The first command gives fast ownership/regression feedback; the second protects the full native PNG contract.
</verification>

<success_criteria>
- GRAYA16-03's atomicity contract is observable on all public GrayAlpha16 compression/filter constructions through writer state, budget state, and caller-lease sentinels.
- Selected Fixed and Dynamic GrayAlpha16 replay detects source drift before a caller-visible write and preserves sticky terminal semantics.
- The additions remain focused local tests in the existing PNG package.
</success_criteria>

<output>
Create `.planning/phases/54-bounded-type-4-16-encoder/54-02-SUMMARY.md` when done.
</output>
