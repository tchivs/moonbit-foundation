---
phase: 54-bounded-type-4-16-encoder
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16-02, GRAYA16-03]
must_haves:
  truths:
    - "A compatible packed U16 GrayAlpha image has explicit eager and caller-buffered graya16 factory families whose output is PNG type 4, depth 16, and non-interlaced."
    - "The shared encoder reads each GrayAlpha16 pixel as four PNG wire bytes in gray high, gray low, alpha high, alpha low order from either declared source byte order."
    - "Stored, FixedOrStored, and DynamicOrFixedOrStored each work with None and Adaptive filters through the same PngEncodeMachine, and ordinary chunk draining equals eager output."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Private GrayAlpha16 encode profile and explicit eager graya16 factory family."
    - path: modules/mb-image/png/encode.mbt
      provides: "Strict GrayAlpha16 admission and four-byte component-aware PNG wire reader."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Explicit caller-buffered graya16 factory family and type-4/depth-16 IHDR mapping through PngEncodeMachine."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Focused eager GrayAlpha16 framing, wire-order, endian-equivalence, and strategy coverage."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Focused ordinary GrayAlpha16 caller-buffered strategy parity coverage."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Both explicit graya16 factory families select the private GrayAlpha16 profile with PngInterlaceStrategy::None."
      pattern: "GrayAlpha16"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngEncodeMachine::new_with_profile consumes profile-aware preflight, filtered cursors, compression plans, and replay."
      pattern: "new_with_profile"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/encode_test.mbt
      via: "The component-aware scalar reader produces the asserted four-byte non-symmetric scanline sequence."
      pattern: "Ghi"
---

<objective>
Connect the Phase 53 packed U16 GrayAlpha source contract to the one bounded PNG encoder as explicit eager and caller-buffered non-interlaced Type-4/16 factories.

Purpose: deliver the production encoder surface and prove its wire, filtering, and compression seams use the same bounded machine as established profiles.
Output: private GrayAlpha16 profile, explicit graya16 factory families, strict admission, four-byte scalar wire traversal, and focused native strategy regressions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-bounded-type-4-16-encoder/54-CONTEXT.md
@.planning/phases/54-bounded-type-4-16-encoder/54-RESEARCH.md
@.planning/phases/54-bounded-type-4-16-encoder/54-PATTERNS.md
@.planning/phases/53-grayalpha16-model-and-checked-storage/53-01-SUMMARY.md
@.planning/phases/53-grayalpha16-model-and-checked-storage/53-VERIFICATION.md
@.planning/milestones/v0.15-phases/48-bounded-gray16-encoder-path/48-01-SUMMARY.md
@.planning/milestones/v0.16-phases/51-bounded-gray-alpha-png-encoding/51-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deliver one end-to-end GrayAlpha16 eager and chunk PNG path</name>
  <reversibility rating="one-way">D-01 fixes public graya16 factory spellings; changing exported names after consumers adopt them breaks the package API.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A packed little-endian U16 GrayAlpha fixture holding distinct gray and alpha byte pairs encodes through default eager and chunk factories to the same complete PNG.
    - The default Stored/None PNG has IHDR depth 16, colour type 4, methods 0, interlace 0, and its decoded Stored scanline is filter byte plus the two pixels in four-byte wire order.
    - U16 GrayAlpha inputs with incorrect channel identity, alpha association, component width, metadata, layout, or row shape return the existing typed capability errors before source traversal or resource charge.
  </behavior>
  <action>Start RED with focused eager and caller-buffered tests named for GrayAlpha16. Per D-01, add the default, compression-only, filter-only, and combined `new_graya16*` families to both `PngEncoder` and `PngChunkEncoder`, mirroring the complete Gray16 and GrayAlpha8 signatures; each combined form binds the one private profile and `PngInterlaceStrategy::None`. Per D-02, add the exhaustive private `GrayAlpha16` profile arm and map it to IHDR depth 16, colour type 4, compression method 0, filter method 0, and interlace method 0. Per D-03, admit only `ChannelOrder::GrayAlpha`, U16, straight alpha, packed rows, encoded builtin sRGB, and TopLeft metadata in `_png_encode_source`; return a four-byte filter stride and retain the current typed capability/error and preflight order. Reject any non-None interlace at the profile guard before the shared preflight can traverse pixels or charge a budget. Per D-04, extend `_png_wire_byte` so a raster position resolves pixel, gray-or-alpha component lane, and byte-within-U16 component, then calls checked component-byte access with each component independently converted to PNG big-endian order; the four produced bytes are gray high, gray low, alpha high, alpha low. Route the caller-buffered combined factory directly through `PngEncodeMachine::new_with_profile`, preserving the existing source revision, filter, plan, checksum, acknowledgement, and terminal ownership path. Add non-symmetric two-pixel fixtures and real eager/chunk tests that assert signature, IHDR fields, the Stored/None scanline bytes, and byte equality between the two public routes.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter '*GrayAlpha16*'</automated>
  </verify>
  <done>Default graya16 eager and caller-buffered constructors create one real bounded PNG path with type-4/depth-16/non-interlaced framing and exact non-symmetric four-byte raster order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the shared GrayAlpha16 path across every supported strategy pair</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Compression-only and filter-only graya16 constructors bind the expected companion strategy, and all six compression/filter pairs produce valid type-4/depth-16/non-interlaced eager output.
    - Equivalent little-endian and big-endian U16 GrayAlpha backing produce equal PNG bytes, so each component is normalized independently rather than as an entire pixel.
    - An Adaptive fixture with repeated four-byte pixels makes the second pixel's Sub residual operate at a four-byte distance, and each ordinary chunk drain equals its eager peer.
  </behavior>
  <action>Per D-04 and D-05, build focused GrayAlpha16 strategy regressions from the Gray16 U16 and GrayAlpha8 type-4 matrices. Invoke all eager and chunk convenience forms once, then cross Stored, FixedOrStored, and DynamicOrFixedOrStored with None and Adaptive using fresh chunk encoders and the existing drain helper. Compare each complete caller-buffered result to the eager oracle and inspect its IHDR fields. Add paired little- and big-endian packed U16 sources with the same logical gray/alpha values; require byte-identical PNG output and assert the non-symmetric Stored scanline sequence. Add an Adaptive row whose repeated four-byte pixels make a wrong two-byte left-predictor distance observable. Per D-06, keep the changes additive to the new profile/factory family and run the existing PNG package suite so the established Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 coverage remains exercised.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter '*GrayAlpha16*'</automated>
  </verify>
  <done>Each public graya16 factory shape and all six bounded strategy/filter selections are exercised; both declared source byte orders produce the same four-byte wire stream, Adaptive uses stride four, and ordinary chunk output equals eager output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller ImageView to profile admission | Caller-controlled descriptor, metadata, dimensions, and storage are accepted only if they satisfy the packed U16 straight-alpha GrayAlpha contract. |
| Scalar source read to PNG raster | Checked component bytes cross an endian-sensitive boundary into the PNG wire order used by filters and compression. |
| PngEncodeMachine to output consumer | Bytes become visible through a writer or caller-owned lease only after profile admission and atomic preflight finish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-01 | Tampering | `_png_encode_source` GrayAlpha16 arm | high | mitigate | Task 1 implements D-03's closed U16, GrayAlpha, straight-alpha, metadata, layout, and row-shape predicates before traversal or budget charge. |
| T-54-02 | Tampering | `_png_wire_byte` component mapping | high | mitigate | Task 1 implements D-04's per-component big-endian mapping; Tasks 1-2 use non-symmetric pairs and both source byte orders to expose lane swaps. |
| T-54-03 | Denial of Service | profile-aware preflight and compression planning | high | mitigate | Task 1 sends the four-byte stride through the existing checked limits, one-charge budget ledger, filters, and plan selection without a raster staging allocation. |
| T-54-04 | Tampering | profile-specific filtering and replay cursors | high | mitigate | Tasks 1-2 bind one `PngEncodeMachine` path and exercise all None/Adaptive and Stored/Fixed/Dynamic selections, including stride-sensitive Adaptive data. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target native --frozen --filter '*GrayAlpha16*'` during each task, then run `moon -C modules/mb-image test png --target native --frozen` before completing the plan. The focused checks prove the new profile; the full native package check protects existing PNG routes.
</verification>

<success_criteria>
- GRAYA16-02 is met by explicit eager and caller-buffered graya16 factory families that produce non-interlaced Type-4/16 PNG bytes in gray-high, gray-low, alpha-high, alpha-low order.
- GRAYA16-03 is met on the production route: the profile uses the shared checked preflight, None/Adaptive filtering, Stored/Fixed/Dynamic planning, and caller-buffered replay machine.
- The implementation stays confined to the existing portable PNG package and reaches no separate production encoder path.
</success_criteria>

## Source Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Compatible packed U16 GrayAlpha sources encode through explicit bounded eager and caller-buffered Type-4/16 factories. | 01 | COVERED |
| REQ | GRAYA16-02 | Explicit factory families, non-interlaced type 4/depth 16 framing, and `Ghi,Glo,Ahi,Alo` serialization. | 01 | COVERED |
| REQ | GRAYA16-03 | Shared preflight, six strategy pairs, and acknowledgement-safe caller-buffered route. | 01, 02 | COVERED |
| RESEARCH | profile/factory composition | One private profile and mirrored factory families on the existing machine. | 01 | COVERED |
| RESEARCH | four-byte scalar wire producer | Per-component endian mapping reaches filters, plans, checksums, and replay without a conversion buffer. | 01 | COVERED |
| RESEARCH | stride and planner coverage | All compression/filter pairs use four bytes per pixel. | 01 | COVERED |
| CONTEXT | D-01 | Explicit `graya16` eager and caller-buffered family spellings. | 01 Task 1 | COVERED |
| CONTEXT | D-02 | Private Type-4/16 non-interlaced profile. | 01 Task 1 | COVERED |
| CONTEXT | D-03 | Strict admission before source reads, output, budget charge, or lease exposure. | 01 Task 1; 02 Task 1 | COVERED |
| CONTEXT | D-04 | Four-byte profile stride, filter cursor, limits, planner, and replay in one machine. | 01 Tasks 1-2; 02 Task 2 | COVERED |
| CONTEXT | D-05 | None/Adaptive and Stored/Fixed/Dynamic without an alternate route. | 01 Task 2 | COVERED |
| CONTEXT | D-06 | Existing profile behavior and atomic semantics remain covered. | 01 Task 2; 02 Task 1 | COVERED |

<output>
Create `.planning/phases/54-bounded-type-4-16-encoder/54-01-SUMMARY.md` when done.
</output>
