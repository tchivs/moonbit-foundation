---
phase: 55-portable-public-evidence
plan: "01"
type: execute
wave: 1
depends_on: [54-01, 54-02]
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16-04]
must_haves:
  truths:
    - "Public legal GrayAlpha16 samples `(1234,A7C5)/(BE0F,5A76)` prove the literal Type-4/16 `Ghi,Glo,Ahi,Alo` raster order and public straight-RGBA8 high-byte canonicalization."
    - "Every compression/filter pair has independent zero-capacity, one-byte, and ragged caller-buffered evidence with eager identity, accepted-only totals, untouched lease tails, and sticky success terminals."
    - "Literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 compatibility PNG vectors remain frozen in eager and caller-buffered tests, and the same package suite passes on all four portable targets."
    - "The existing strict descriptor-boundary rejection of Big-endian GrayAlpha16 remains covered; public evidence creates only legal little-endian GrayAlpha16 sources."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: Public GrayAlpha16 Stored/None wire vector, public decoder canonicalization, six-pair semantic matrix, and literal eager compatibility vectors.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: GrayAlpha16 hostile public drain/matrix, accepted-prefix lease ownership and sticky-terminal checks, and literal chunk compatibility vectors.
  key_links:
    - from: PngEncoder::new_graya16_with_strategies
      to: PngDecoder::new
      via: Generated Stored/None public bytes are checked at the Type-4/16 raster boundary, then decoded through ImageDecoder::decode.
      pattern: new_graya16_with_strategies
    - from: PngChunkEncoder::new_graya16_with_strategies
      to: PngEncoder::new_graya16_with_strategies
      via: Each fresh zero, one-byte, and ragged chunk run must equal a fresh eager oracle for the same strategy/filter pair.
      pattern: new_graya16_with_strategies
---

<objective>
Close GRAYA16-04 with portable, public regression evidence only: prove legal Type-4/16 GrayAlpha16 wire/decode fidelity, caller-buffered lease ownership, frozen legacy bytes, strict endian-boundary continuity, and the identical all-target suite.

Purpose: turn the Phase 54 bounded encoder behavior into a consumer-visible compatibility contract without changing production PNG, model, target, or release behavior.
Output: focused public evidence in the two established PNG package test files.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-portable-public-evidence/55-CONTEXT.md
@.planning/phases/55-portable-public-evidence/55-RESEARCH.md
@.planning/phases/55-portable-public-evidence/55-PATTERNS.md
@.planning/phases/54-bounded-type-4-16-encoder/54-01-SUMMARY.md
@.planning/phases/54-bounded-type-4-16-encoder/54-02-SUMMARY.md
@.planning/phases/54-bounded-type-4-16-encoder/54-VERIFICATION.md
@.planning/milestones/v0.15-phases/49-portable-gray16-public-evidence/49-01-PLAN.md
@.planning/milestones/v0.15-phases/49-portable-gray16-public-evidence/49-01-SUMMARY.md
@.planning/milestones/v0.15-phases/49-portable-gray16-public-evidence/49-VERIFICATION.md
@.planning/milestones/v0.16-phases/52-portable-gray-alpha-public-evidence/52-01-PLAN.md
@.planning/milestones/v0.16-phases/52-portable-gray-alpha-public-evidence/52-01-SUMMARY.md
@.planning/milestones/v0.16-phases/52-portable-gray-alpha-public-evidence/52-VERIFICATION.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- `PngEncoder::new_graya16_with_strategies(strategy, filter_strategy)` is the public eager Type-4/16 construction seam.
- `PngChunkEncoder::new_graya16_with_strategies(source, strategy, filter_strategy, limits, budget, diagnostics)` is the public caller-buffered construction seam; `pull` returns the current accepted bytes, cumulative total, and terminal outcome.
- `@codec.ImageDecoder::decode(PngDecoder::new(), ...)` is the public decoding seam; the current Type-4/16 result is straight U8 RGBA, with `R=G=B=Ghi` and `A=Ahi`.
- `png_encode_graya16_image`, `png_stream_graya16_image`, `png_stream_gray16_public_drain`, and `png_stream_graya8_public_drain` are the closest established fixture and lease-ownership patterns.
</interfaces>
</context>

<source_audit>
| Source | ID | Required item | Plan coverage |
|---|---|---|---|
| GOAL | Phase 55 | Public wire/decode proof, caller safety, legacy stability, and portable execution | Tasks 1-2 |
| REQ | GRAYA16-04 | Literal U16 wire fidelity, RGBA8 high-byte decode, hostile caller semantics, frozen compatibility, and all-target suite | Tasks 1-2 |
| RESEARCH | R-01 | Legal `(1234,A7C5)/(BE0F,5A76)` Stored/None scanline `00 12 34 A7 C5 BE 0F 5A 76` with type-4/depth-16/non-interlaced framing | Task 1 |
| RESEARCH | R-02 | Reuse current Gray16/GrayAlpha8 fresh-encoder drains for all six strategy/filter pairs | Task 2 |
| RESEARCH | R-03 | Modify the two existing PNG test files only and use `moon -C modules/mb-image test png --target all --frozen` | Tasks 1-2 |
| CONTEXT | D-01 | Compact public legal U16 vector with literal wire bytes and public RGBA8 canonicalization | Task 1 |
| CONTEXT | D-02 | Evidence stays at public eager, chunk, and decoder seams | Tasks 1-2 |
| CONTEXT | D-03 | Zero, one-byte, and deterministic ragged schedules for every pair, with progress/tail/terminal proof | Task 2 |
| CONTEXT | D-04 | Literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 eager/chunk baselines | Tasks 1-2 |
| CONTEXT | D-05 | One portable all-target test invocation and no platform-specific paths | Task 2 |
| CONTEXT | D-06 | Legal little-endian fixtures plus retained descriptor-boundary rejection coverage | Tasks 1-2 |
</source_audit>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one public GrayAlpha16 wire-to-decode path and freeze eager compatibility vectors</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/encode_test.mbt`: reuse `png_encode_graya16_image`, the existing GrayAlpha16 framing/rejection tests, `png_encode_gray16_public_decode_is_canonical`, `png_encode_graya8_decode_matches_source`, and the eager frozen-vector test.
    - `55-PATTERNS.md`: combine the Gray16 U16 public oracle with the GrayAlpha8 public RGBA helper and retain literal-vector policy.
    - `54-VERIFICATION.md`: preserve the legal little-endian source boundary and public factory behavior already proven by Phase 54.
  </read_first>
  <action>D-01 and D-02: add a compact public GrayAlpha16 evidence helper/test around the existing legal `png_encode_graya16_image` data `(1234,A7C5)/(BE0F,5A76)`. Encode through `PngEncoder::new_graya16_with_strategies(Stored, None)` and assert signature plus IHDR depth/type/interlace and the literal filtered raster `00 12 34 A7 C5 BE 0F 5A 76`; the filter byte and every `Ghi,Glo,Ahi,Alo` lane must be explicit expected data, not output from another current encoder. Decode exactly those bytes through `ImageDecoder::decode(PngDecoder::new(), ...)`, require straight `Rgba`, and assert `(12,12,12,A7)` and `(BE,BE,BE,5A)`, keeping lower bytes as wire-only proof. Extend the existing three-compression-by-two-filter loop to construct every route through the same public eager factory and validate type-4/16 framing plus that public decoder canonicalization. D-04: extend the established literal eager compatibility test with the missing GrayAlpha8 Stored/None complete PNG literal while preserving the existing literal Gray8, Gray16, RGB8, and straight-RGBA8 values. D-06: retain the existing Big-endian GrayAlpha16 descriptor-rejection test unchanged and use no Big-endian source in the new public corpus. Do not modify PNG production code, public API, test target configuration, fixtures, scripts, or decoder semantics. Commit this completed test-only tracer atomically as `test(55-01): add public graya16 wire evidence`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter "GrayAlpha16"</automated>
  </verify>
  <done>Public legal GrayAlpha16 output proves the exact Type-4/16 Stored/None lane order and public straight-RGBA8 high-byte canonicalization; all six eager strategy/filter routes remain semantically valid, the strict descriptor rejection stays covered, and five literal eager format baselines are compared without production changes.</done>
  <reversibility rating="costly">Per D-01, D-04, and D-06, the literal public vectors and legal endian boundary are externally observable compatibility commitments; changing them requires an explicit contract decision and replacement evidence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove all-six-pair GrayAlpha16 hostile chunk schedules and all-target portability</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: reuse `png_stream_graya16_image`, `png_stream_graya16_eager_with_strategies`, the Gray16 and GrayAlpha8 public drain helpers/matrices, the existing GrayAlpha16 atomic/replay tests, and the frozen chunk-vector test.
    - `55-PATTERNS.md`: copy the accepted-prefix, tail-sentinel, and sticky-success terminal structure without building another stream driver.
    - `54-02-SUMMARY.md`: distinguish the existing sticky failure replay checks from this task's sticky success-terminal evidence.
  </read_first>
  <action>D-02 and D-03: add `png_stream_graya16_public_drain` by specializing the established Gray16/GrayAlpha8 public-drain contract to `PngChunkEncoder::new_graya16_with_strategies`. For each fresh schedule encoder, fill caller leases with `Z`, append only indices below `written`, require `total_written == accepted_before + written`, keep every unwritten tail byte as `Z`, compare completed output to a fresh `png_stream_graya16_eager_with_strategies` oracle, then prove a later seven-byte `Z` lease returns zero bytes, unchanged total, `Finished`, and an untouched tail. Add the focused public evidence matrix for Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive: directly pull an empty lease for each pair and require zero progress, `NeedOutput`, and its untouched sentinel, then independently execute `[0UL, 1UL]`, `[1UL]`, and `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]`. D-04: extend the existing literal chunk compatibility test with a GrayAlpha8 Stored/None complete PNG literal, retaining literal Gray8, Gray16, RGB8, and straight-RGBA8 comparisons. D-05: keep test code portable MoonBit and run `moon -C modules/mb-image test png --target all --frozen` as the required identical four-target suite. D-06: retain the current strict Big-endian descriptor-boundary coverage and construct all new GrayAlpha16 schedule images through the legal little-endian fixture only. Do not add private profiles, test-only factories, production behavior, native branches, FFI, release work, source copies, retry code, or fixtures. Commit this expansion atomically as `test(55-01): prove portable graya16 stream evidence`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Each of the six public GrayAlpha16 strategy/filter pairs is eager-byte-identical under independent zero/one/ragged schedules, reports only accepted output, preserves lease ownership, and returns a sticky successful terminal; five literal chunk format baselines stay frozen and the one public suite passes on js, wasm, wasm-gc, and native.</done>
  <reversibility rating="costly">Per D-03 through D-05, the schedule matrix, caller-lease ownership assertions, literal vectors, and all-target gate protect public compatibility; weakening them removes the regression boundary.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legal public image to PNG bytes | Non-symmetric packed U16 gray/alpha components cross the public eager factory into standards-visible Type-4/16 raster lanes. |
| Encoder state to caller-owned mutable lease | Caller-controlled capacities meet `PngChunkEncoder::pull`; the encoder reports accepted bytes and terminal state while the caller owns tails. |
| Public PNG bytes to decoded image | Type-4/16 samples pass through the public decoder's documented straight-RGBA8 canonicalization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Tampering | GrayAlpha16 raster order and public decoder evidence | high | mitigate | Task 1 implements D-01/D-02 with four distinct lanes per legal sample, literal Stored/None bytes, type-4/16 framing, and public RGBA8 high-byte assertions. |
| T-55-02 | Tampering | caller-buffered progress, tail ownership, and success terminals | high | mitigate | Task 2 implements D-03 across every strategy/filter pair with direct empty pulls, fresh schedule encoders, accepted-prefix accounting, sentinels, eager identity, and later terminal pulls. |
| T-55-03 | Tampering | frozen PNG compatibility baselines | high | mitigate | Tasks 1-2 implement D-04 using complete literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 PNG values rather than an encoder-derived oracle. |
| T-55-04 | Denial of Service | portable qualification path | medium | mitigate | Task 2 implements D-05 through one bounded public PNG package suite on `--target all`, exercising every supported portable target without platform forks. |
| T-55-05 | Tampering | GrayAlpha16 endian contract | high | mitigate | Tasks 1-2 implement D-06 by retaining strict descriptor-boundary rejection coverage and limiting all new evidence fixtures to legal little-endian sources. |
</threat_model>

<verification>
1. Run `moon -C modules/mb-image test png --target all --frozen`; record the package result for wasm, wasm-gc, js, and native from the one invocation.
2. Run `git diff --check` and confirm implementation changes are confined to `modules/mb-image/png/encode_test.mbt` and `modules/mb-image/png/stream_encode_test.mbt`.
3. Confirm test construction uses only `PngEncoder::new_graya16_with_strategies`, `PngChunkEncoder::new_graya16_with_strategies`, and `ImageDecoder::decode(PngDecoder::new(), ...)`; exact wire and frozen compatibility values are literals.
4. Confirm the named Big-endian descriptor-rejection coverage remains in the focused GrayAlpha16 test set and no new source fixture attempts unsupported backing parity.
</verification>

<success_criteria>
- [ ] GRAYA16-04: Public legal `(1234,A7C5)/(BE0F,5A76)` GrayAlpha16 evidence proves `00 12 34 A7 C5 BE 0F 5A 76` at the Type-4/16 Stored/None raster boundary and public `(12,12,12,A7)/(BE,BE,BE,5A)` decode canonicalization.
- [ ] All six compression/filter pairs have independent direct zero-capacity, one-byte, and ragged caller-buffered coverage with eager identity, accepted-only progress, untouched tails, and sticky `Finished` evidence.
- [ ] Literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 eager and chunk vectors remain byte-identical compatibility baselines.
- [ ] `moon -C modules/mb-image test png --target all --frozen` passes as the same portable MoonBit suite on wasm, wasm-gc, js, and native.
- [ ] The strict Big-endian GrayAlpha16 descriptor rejection stays covered, while all public Phase 55 fixtures are legal little-endian sources.
- [ ] No production PNG/model code, API, build/release artifact, target-specific test branch, FFI, fixture directory, source copy, buffer, retry path, or deferred PNG capability is added.
</success_criteria>

<output>
Create `.planning/phases/55-portable-public-evidence/55-01-SUMMARY.md` when done.
</output>
