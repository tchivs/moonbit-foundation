---
milestone: v0.16
name: Grayscale Alpha PNG
audited: 2026-07-23
status: passed
scores:
  requirements: 5/5
  phases: 3/3
  integration: 4/4
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - scope: planning metadata
    severity: non_blocking
    item: "PROJECT.md still lists the v0.16 goals as Active and unchecked; STATE.md includes stale 'Plan: Not started' and 0/3 metrics despite its completed header. ROADMAP.md has completed phase checkboxes but still labels the milestone Planned."
---

# v0.16 Grayscale Alpha PNG — Milestone Audit

## Verdict: Passed

The milestone goal is achieved. All five requirements have a completed plan summary, a passed phase verification, a completed traceability entry, and live-code evidence. The Gray+Alpha path is wired from the public model through the shared bounded encoder to the public decoder and caller-buffered evidence; it is not an orphaned API or test-only implementation.

The only observation is non-blocking planning-metadata freshness drift recorded in the frontmatter. It does not affect the implementation, requirement coverage, or test results.

## Scope and Phase Evidence

| Phase | Goal | Verification | Review / security | Audit result |
| --- | --- | --- | --- | --- |
| 50 — Gray+Alpha Image Model | Establish packed U8, straight-alpha two-component model | `passed`, 4/4 must-haves | Review clean; security verified, 0 open threats | Passed |
| 51 — Bounded Gray+Alpha PNG Encoding | Add bounded eager and caller-buffered GrayAlpha8 routes | `passed`, 4/4 must-haves | Review clean; security verified, 0 open threats | Passed |
| 52 — Portable Gray+Alpha Public Evidence | Prove fidelity, hostile scheduling, compatibility, portability | `passed`, 4/4 must-haves | Review clean; security verified, 0 open threats | Passed |

Nyquist validation is disabled in `.planning/config.json`; no validation-file compliance gap applies.

## Requirements Coverage

The status below cross-checks `.planning/REQUIREMENTS.md`, each relevant summary's `requirements-completed` frontmatter, the relevant phase verification, and live code/tests.

| Requirement | Phase | Traceability / summary / verification | Live end-to-end evidence | Status |
| --- | --- | --- | --- | --- |
| GRAYA-01 | 50 | Complete / listed in `50-01-SUMMARY.md` / satisfied | `ImageFormat::graya8()` produces packed U8 `GrayAlpha`; descriptor validation requires straight alpha and two channels; `OwnedImage` views retain distinct components. | Satisfied |
| GRAYA-02 | 51 | Complete / listed in both 51 summaries / satisfied | Public eager and chunk `new_graya8*` factories pass `GrayAlpha8` into the shared machine; IHDR emits depth 8, type 4, no interlace; real decode preserves pairs. | Satisfied |
| GRAYA-03 | 51 | Complete / listed in both 51 summaries / satisfied | The same profile-aware preflight/replay path serves eager and chunk routes for all 3 compression × 2 filter pairs; rejection tests retain zero eager output, unchanged budget, and untouched leases. | Satisfied |
| GRAYA-04 | 52 | Complete / listed in `52-01-SUMMARY.md` / satisfied | Public Stored/None test freezes `00 13 A7 D2 4C` and decodes it as straight RGBA8 `(13,13,13,A7)` / `(D2,D2,D2,4C)`. | Satisfied |
| GRAYA-05 | 52 | Complete / listed in `52-01-SUMMARY.md` / satisfied | Each strategy/filter pair passes zero, one-byte, and ragged chunk schedules; it checks eager identity, accepted-only progress, untouched tails, sticky finish, literal legacy vectors, and all four targets. | Satisfied |

No v0.16 requirement is orphaned, partially verified, or lacks cross-phase wiring.

## Integration Wiring

| Connection | Trace | Result |
| --- | --- | --- |
| Phase 50 model → storage/view | `ChannelOrder::GrayAlpha` has `channel_count() == 2`; `ImageFormat::graya8()` creates packed U8; descriptor validation locks straight-alpha encoded sRGB; existing views use the channel count for bounds and offsets. | Wired |
| Phase 50 model → Phase 51 eager encoder | `PngEncoder::new_graya8_with_strategies` selects private `GrayAlpha8`; `ImageEncoder::encode` calls `PngEncodeMachine::new_with_profile`; shared preflight invokes `_png_encode_source`, which admits only compatible GrayAlpha U8 straight-alpha sources. | Wired |
| Phase 50 model → Phase 51 caller-buffered encoder | `PngChunkEncoder::new_graya8_with_strategies` passes the same `GrayAlpha8` profile and `None` interlace to `new_with_profile` before an encoder state is constructed. | Wired |
| Phase 51 encoder → Phase 52 public evidence | The eager route emits IHDR `08 04 00 00 00` and component order from the two-channel scalar reader; Phase 52's public tests decode its actual bytes and compare fresh chunk output to a fresh eager oracle under hostile schedules. | Wired |

There are no HTTP APIs in this library milestone, so API-consumer coverage is not applicable.

## End-to-End Flow Results

| Flow | Trace | Result |
| --- | --- | --- |
| Create and inspect a GrayAlpha image | `ImageFormat::graya8()` → `ImageDescriptor::new` validation → `OwnedImage::new` → checked `MutImageView` writes / `ImageView` reads. The third component is rejected. | Complete |
| Eager PNG fidelity | Non-symmetric `(13,A7)/(D2,4C)` image → `PngEncoder::new_graya8_with_strategies(Stored, None)` → shared preflight/filter/planner/replay → type-4 PNG → public `PngDecoder` → straight RGBA8 canonicalization. | Complete |
| Caller-buffered bounded output | Same image → `PngChunkEncoder::new_graya8_with_strategies` → zero/one/ragged leases across six compression/filter pairs → accepted prefixes only → byte equality with eager output → sticky `Finished`. | Complete |

## Current Test Evidence

Executed during this audit against the current checkout:

| Command | Result |
| --- | --- |
| `moon -C modules/mb-image test png --target all --frozen` | 196 passed, 0 failed on wasm, wasm-gc, js, and native |
| `moon test --target all modules/mb-image/model modules/mb-image/storage modules/mb-image/ops` | 79 passed, 0 failed on wasm, wasm-gc, js, and native |

The live traces inspected in addition to those runs include `descriptor.mbt`, `png.mbt`, `encode.mbt`, `stream_encode.mbt`, and the public package tests. They confirm that the Phase 50 export is consumed by Phase 51 production paths and that Phase 52 tests exercise those public paths rather than a private substitute.

## Requirements with No Cross-Phase Wiring

None. GRAYA-01 supplies the model/data contract consumed by GRAYA-02/03; GRAYA-04/05 consume the Phase 51 public eager and caller-buffered interfaces.
