---
phase: 33-fixed-or-stored-png-planning-and-emission
plan: "02"
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
gap_closure: true
requirements: [PNGC-02, PNGC-03]
must_haves:
  truths:
    - "FixedOrStored eager and chunk construction reject capability, geometry, selected-output, selected-work, and budget failures before output, without changing any remaining budget field; the exact selected-work boundary admits and charges exactly once."
    - "The configured fixed route proves A2 selected-work boundaries and fails closed on a replay-work mismatch without committing an unacknowledged output byte."
    - "FixedOrStored chunk output replays terminal Finished and the original first failure without changing a later caller lease on js, wasm, wasm-gc, and native."
    - "The focused four-target selector fails closed unless every named configured admission, A2, and terminal test is present in the package outline before the filter runs."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: Public configured eager admission and exact-budget-charge tests.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public configured chunk admission and sticky-terminal tests, all named for the focused selector.
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Private FixedOrStored selected-work boundary and one-charge assertions.
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: Private A2 replay mismatch and present/acknowledgement-boundary tests.
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/encode.mbt
      via: PngEncoder::new_with_compression_strategy(FixedOrStored) reaches _png_encode_preflight before its Writer receives a byte.
      pattern: "new_with_compression_strategy.*FixedOrStored|_png_encode_preflight"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: PngChunkEncoder::new_with_compression_strategy(FixedOrStored) and pull exercise Active-to-Finished and Active-to-Failed replay states.
      pattern: "PngChunkEncoder::new_with_compression_strategy|PngChunkEncoder::pull"
    - from: modules/mb-image/png/stream_encode_wbtest.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: PngEncodeMachine::present previews pending fixed output and acknowledge alone commits PngFixedState.
      pattern: "fixed_preview_byte|present|acknowledge|png-encode-fixed-replay-work"
    - from: moon test PNG outline
      to: modules/mb-image/png/*_test.mbt and *_wbtest.mbt
      via: Per-target outline checks every exact required test name before `-f '*fixed-or-stored*'` executes.
      pattern: "PNG fixed-or-stored public admission is atomic|PNG fixed-or-stored white-box replay mismatch is sticky"
---

<objective>
Close Phase 33's behavioral-evidence gaps by testing the existing FixedOrStored route directly through its public eager/chunk factories and private fixed-plan state.

Purpose: Convert the verifier's present-but-unverified admission, A2 replay, and sticky-terminal behaviors into deterministic four-target evidence.
Output: Focused public and white-box FixedOrStored tests with stable exact names, selected only after a fail-closed per-target outline guard; no production encoder, policy, corpus, or benchmark changes.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-fixed-or-stored-png-planning-and-emission/33-VERIFICATION.md
@.planning/phases/33-fixed-or-stored-png-planning-and-emission/33-01-SUMMARY.md
@modules/mb-core/budget/budget.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt
</context>

<source_audit>
SOURCE  | ID      | Required closure evidence | Plan | Status | Notes
------- | ------- | ------------------------- | ---- | ------ | -----
GOAL    | —       | Optimized output is admitted before eager or caller-buffered bytes and retains hostile-output terminal behavior. | 33-02 | COVERED | Tasks 1-2 exercise the existing configured route only.
REQ     | PNGC-02 | Exact capability, geometry, output, work, and budget admission before exposure. | 33-02 | COVERED | Public rejection and white-box exact-charge boundaries.
REQ     | PNGC-03 | Exact configured chunk progress plus sticky completion/failure on four targets. | 33-02 | COVERED | Public hostile-capacity and terminal replay assertions run via the corrected selector.
RESEARCH| A2      | Selected work includes plan/replay matcher work and replay drift fails closed. | 33-02 | COVERED | Task 2 injects and observes the private replay mismatch branch.
RESEARCH| Scope   | Corpus fixtures, measured compression claims, and benchmark reporting belong to Phase 34. | 33-02 | COVERED | Both tasks retain this exclusion.
CONTEXT | —       | No CONTEXT.md or locked D-XX decisions exist for this phase. | 33-02 | COVERED | No additional decision implementation is required.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover configured public admission boundaries and sticky chunk terminals</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: `PNG fixed-or-stored public admission is atomic` proves both PngEncoder::new_with_compression_strategy(FixedOrStored) and PngChunkEncoder::new_with_compression_strategy(FixedOrStored) reject incompatible capability, width/height/pixel geometry, selected output, selected work, and exhausted-budget cases before a Writer or output lease receives a byte.
    - Test 2: `PNG fixed-or-stored public admission is atomic` proves every rejected configured attempt leaves all ResourceLimits fields unchanged: bytes, allocations, allocation_size, width, height, pixels, depth, and work.
    - Test 3: `PNG fixed-or-stored public selected work charges once` proves a successful configured construction debits exact selected work once; a fresh budget set to that debit admits and reaches zero remaining work, while one less rejects without a debit.
    - Test 4: `PNG fixed-or-stored configured sticky terminals preserve leases` proves a configured chunk encoder drained with zero, one-byte, and ragged leases returns a zero-write Finished replay without changing a later filled lease; after a released lease fails, a fresh filled lease receives zero bytes and the same complete CoreError fields.
    - Test 5: `PNG fixed-or-stored public replay mismatch is sticky` constructs a configured encoder from the exact mutable 5×1 RGB8 replay fixture, mutates pixel (0,0) channel 0 after Fixed preflight, drains fourteen fixed-DEFLATE preview bytes, and reaches `png-encode-fixed-replay-work` on the next one-byte pull with total_written equal to 57; repeated pull returns the first complete Failed(error) and a fresh filled lease is unchanged.
  </behavior>
  <action>Add public black-box tests alongside the existing PNG helpers, using a compact repetitive fixture that selects Fixed and the two existing configured factories rather than Stored constructors. Create exactly these stable names: `PNG fixed-or-stored public admission is atomic`, `PNG fixed-or-stored public selected work charges once`, `PNG fixed-or-stored configured sticky terminals preserve leases`, and `PNG fixed-or-stored public replay mismatch is sticky`; retain the shared lowercase `fixed-or-stored` token in each so the package filter selects it. Add a reusable test helper that compares all eight ResourceLimits accessors before and after each rejected attempt. For capability, geometry, selected-output, selected-work, and budget rejection, exercise both public routes: assert the eager MemoryWriter remains empty; assert the chunk factory returns Err before any lease can be supplied; and compare the typed errors where the existing helpers support parity. Derive the selected-work debit from a successful FixedOrStored construction on a generous fresh budget, then prove exact-limit success and one-less atomic failure on fresh budgets; assert the successful debit equals exactly one selected-work charge, not a second charge from output draining. Convert the existing Stored-only completion and released-lease cases into configured FixedOrStored cases (or add configured siblings if retaining the legacy regressions), preserving their assertions for zero written bytes, stable total_written, every CoreError field, and an unchanged later lease. Include zero/one/ragged pulls in the configured terminal path. For the executable public replay stimulus, build a mutable 5×1 RGB8 image with pixel triples `(0x0a,0x0c,0x0c)`, `(0x0a,0x0a,0x0c)`, `(0x0b,0x0c,0x0a)`, `(0x0c,0x0a,0x0b)`, `(0x0a,0x0c,0x0a)`. Construct PngChunkEncoder::new_with_compression_strategy with FixedOrStored while this backing view is unchanged, then drain exactly 43 one-byte pulls (the 41-byte PNG frame prefix and two zlib-header bytes). The next presented byte is the first fixed-DEFLATE byte at PNG offset 43; assert `(byte & 0x07) == 0x03` to prove the configured preflight selected a final fixed block. Only then mutate backing pixel `(0,0,0)` from `0x0a` to `0x00`, before the remaining replay-sensitive fixed bytes are presented. The source algorithm gives this fixture a planned matcher_work of 189 and a 126-bit/16-byte fixed plan, while replay has matcher_work 183 with the same bit length: acknowledge the first fixed byte plus thirteen more fixed-DEFLATE bytes, then the next one-byte pull must fail before EOB at total_written 57 with State/InvalidRange context `png-encode-fixed-replay-work`. Make a second pull with a fresh sentinel-filled lease, compare category, code, operation, context, requested, completed, and limit to the first failure, and assert zero written bytes, total_written 57, and no sentinel mutation. Keep the same mutation fixture in Task 2's white-box proof so the public and private paths prove the same branch. Before each focused run, execute the shared outline guard in `<verification>`; do not accept a filtered run unless it contains every exact required name and no no-match warning. Do not alter production sources, public APIs, policy inventories, Phase 34 corpus fixtures, benchmark reports, or compression-win claims.</action>
  <verify>
    <automated>$required = @('PNG fixed-or-stored public admission is atomic','PNG fixed-or-stored public selected work charges once','PNG fixed-or-stored configured sticky terminals preserve leases','PNG fixed-or-stored public replay mismatch is sticky','PNG fixed-or-stored white-box selected work boundary charges once','PNG fixed-or-stored white-box failed output awaits acknowledgement','PNG fixed-or-stored white-box replay mismatch is sticky'); $outline = @(moon -C modules/mb-image test png --target native --frozen --outline 2&gt;&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw 'focused PNG outline has no test entry' }; foreach ($name in $required) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing required PNG test: $name" } }; moon -C modules/mb-image test png --target native --frozen -f '*fixed-or-stored*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Configured public tests prove complete pre-output atomicity and one selected-work budget debit for both factories, plus sticky configured completion and the explicit post-preflight replay-work failure without output mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise A2 selected-work, failed-output acknowledgement, and replay-mismatch paths</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - Test 1: `PNG fixed-or-stored white-box selected work boundary charges once` proves the private FixedOrStored preflight exposes the selected Fixed plan's exact selected-work value; that value is charged once, its exact limit succeeds, and one less returns a pre-output error with the full budget snapshot intact.
    - Test 2: `PNG fixed-or-stored white-box failed output awaits acknowledgement` proves a presented fixed byte that is not acknowledged models a failed downstream handoff: repeated present returns that same byte and leaves committed emitted count, fixed cursor, Adler, CRC, and matcher-work state unchanged; acknowledging it once advances those fields once.
    - Test 3: `PNG fixed-or-stored white-box replay mismatch is sticky` uses the same post-preflight mutable 5×1 RGB8 fixture, reaches the replay-work error at fixed-DEFLATE preview 15 after fourteen accepted previews, and proves every later present returns the same full CoreError fields with committed state unchanged.
  </behavior>
  <action>Add white-box tests using PngEncodeMachine::new_with_compression_strategy and _png_encode_preflight with PngCompressionStrategy::FixedOrStored. Create exactly these stable names: `PNG fixed-or-stored white-box selected work boundary charges once`, `PNG fixed-or-stored white-box failed output awaits acknowledgement`, and `PNG fixed-or-stored white-box replay mismatch is sticky`. Keep the fixture compact and deterministic so the selected plan is Fixed. Inspect PngDeflatePlan::Fixed matcher_work and the planned selected-work arithmetic, then use fresh exact and one-less CodecLimits/Budget values to prove the A2 boundary and one final ResourceCharge; reuse the full eight-field remaining-budget comparison for rejection. For the acknowledgement boundary, drive the machine through the fixed header/token path, call present for a fixed byte, deliberately omit acknowledgement to represent a failed output byte, call present again, and assert byte equality plus unchanged committed state; then acknowledge the original byte and assert exactly one committed transition. For the replay mismatch, use the exact mutable 5×1 RGB8 pixel triples specified in Task 1. Assert preflight selects PngDeflatePlan::Fixed with matcher_work 189, deflate_bits 126, and deflate_bytes 16; construct the machine first, mutate pixel `(0,0,0)` from `0x0a` to `0x00`, and do not mutate it again. Acknowledge exactly the 43 non-deflate PNG/zlib-prefix bytes and fourteen fixed-DEFLATE previews; the fifteenth fixed-DEFLATE preview must return State/InvalidRange with context `png-encode-fixed-replay-work` before EOB, leaving emitted at 57 and no pending continuation byte. Capture every error field, then call present repeatedly and require the same complete error fields with unchanged emitted count, pending byte, fixed cursor, Adler, CRC, and matcher-work state. Task 1 must use this identical post-preflight stimulus through the public PngChunkEncoder so its first failing pull latches Failed(error), its repeated pull returns the same complete error, and a fresh filled lease is not mutated. Before each focused run, execute the shared outline guard in `<verification>`; do not rely on a successful empty filter. Do not weaken the A1 matcher contract or change encoder implementation, package policy, Phase 34 corpus ownership, or benchmark scope.</action>
  <verify>
    <automated>$required = @('PNG fixed-or-stored public admission is atomic','PNG fixed-or-stored public selected work charges once','PNG fixed-or-stored configured sticky terminals preserve leases','PNG fixed-or-stored public replay mismatch is sticky','PNG fixed-or-stored white-box selected work boundary charges once','PNG fixed-or-stored white-box failed output awaits acknowledgement','PNG fixed-or-stored white-box replay mismatch is sticky'); $outline = @(moon -C modules/mb-image test png --target native --frozen --outline 2&gt;&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw 'focused PNG outline has no test entry' }; foreach ($name in $required) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing required PNG test: $name" } }; moon -C modules/mb-image test png --target native --frozen -f '*fixed-or-stored*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>Private tests certify A2's exact selected-work admission, acknowledgement-only commit boundary including an unacknowledged output byte, and the replay-drift failure path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image, limits, and budget → FixedOrStored factories | Untrusted capability, geometry, output, work, and budget inputs must be rejected before output is observable. |
| Preflight plan → replaying machine | Planned selected work must agree with fixed replay before further output is accepted. |
| Machine → Writer or caller lease | Previewed bytes become state only after acknowledged acceptance; terminal replay must not touch a later lease. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-02-01 | Denial of service | FixedOrStored preflight and Budget | high | mitigate | Tasks 1-2 test every configured admission boundary, all unchanged budget fields on rejection, and the exact one-charge work boundary. |
| T-33-02-02 | Tampering | PngEncodeMachine present/acknowledge | high | mitigate | Task 2 proves an unacknowledged fixed byte cannot alter committed scalar/checksum state and has one acknowledgement effect. |
| T-33-02-03 | Tampering | fixed replay and PngChunkEncoder terminal state | high | mitigate | Task 2 reaches replay mismatch; Task 1 proves Finished and the first lease error replay without changing another lease. |
</threat_model>

<verification>
- The focused selector is `-f '*fixed-or-stored*'`; the former literal phrase selector is not a MoonBit glob match and must not be used.
- Before each filtered run, the outline must contain every exact required name and must not report a no-test warning. Run this fail-closed command from PowerShell:
  `$required = @('PNG fixed-or-stored public admission is atomic','PNG fixed-or-stored public selected work charges once','PNG fixed-or-stored configured sticky terminals preserve leases','PNG fixed-or-stored public replay mismatch is sticky','PNG fixed-or-stored white-box selected work boundary charges once','PNG fixed-or-stored white-box failed output awaits acknowledgement','PNG fixed-or-stored white-box replay mismatch is sticky'); foreach ($target in @('js','wasm','wasm-gc','native')) { $outline = @(moon -C modules/mb-image test png --target $target --frozen --outline 2>&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "focused PNG outline has no test entry on $target" }; foreach ($name in $required) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing required PNG test on ${target}: $name" } }; moon -C modules/mb-image test png --target $target --frozen -f '*fixed-or-stored*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }`
- Run the complete PNG regression suite after focused evidence: `moon -C modules/mb-image test png --target all --frozen`.
- Run the scoped policy and four-target quality lane: `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
</verification>

<success_criteria>
- The focused glob executes the named configured FixedOrStored public and white-box tests on js, wasm, wasm-gc, and native only after each target's outline contains every exact required name and contains no no-test warning.
- Both public FixedOrStored factories prove capability, geometry, selected-output, selected-work, and budget rejection before output and preserve all remaining budget fields; the exact selected-work boundary debits once.
- A2 work, failed-output acknowledgement, replay mismatch, and configured sticky Finished/first-failure semantics have direct assertions, with no output mutation after a terminal replay.
- Phase 34 corpus, decoder-round-trip corpus evidence, measured compression claims, and benchmark reporting remain excluded from this closure.
</success_criteria>

<output>
Create `.planning/phases/33-fixed-or-stored-png-planning-and-emission/33-02-SUMMARY.md` when done.
</output>
