---
phase: 29-pausable-png-encode-substrate
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
requirements: [PNGE-01]
must_haves:
  truths:
    - "A compatible RGB8 or straight-RGBA8 ImageView creates a private PNG encode machine only after the eager encoder's capability, dimension, output-limit, work-limit, and budget checks succeed."
    - "An incompatible source or exhausted limit/budget produces the existing typed error before a machine or output byte can exist, and failed construction does not consume the work budget."
    - "The private machine can present each byte of the frozen signature, IHDR, single stored-DEFLATE IDAT, and IEND representation from owned scalar state and its immutable source, and commits its emitted/completed count only when its private consumer acknowledges that byte, without a complete PNG, IDAT, scanline buffer, or caller destination lease."
  artifacts:
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Private PngEncodeMachine state, uncommitted-byte presentation plus accepted-byte acknowledgement, canonical PNG framing, stored-DEFLATE cursors, and checksum state.
    - path: modules/mb-image/png/encode.mbt
      provides: Shared source validation, exact-length arithmetic, ordered limits, atomic disposition/budget preflight, and eager facade seam.
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: White-box preflight, byte-boundary, checksum, length, and ownership regression evidence for the private machine.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Private-package canonical byte and eager-equivalence regression coverage without a public streaming declaration.
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: One authoritative constructor passes validated source geometry, exact canonical length, disposition, and charged budget into the private machine.
      pattern: PngEncodeMachine|png_encode_preflight
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/structural.mbt
      via: Incremental IDAT emission initializes and updates the established PNG CRC helpers while writing type and every zlib byte.
      pattern: _png_crc_for_type|_png_crc_step
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/deflate_inflate.mbt
      via: Stored-DEFLATE emission reuses the established Adler-32 step for filter and sample bytes only.
      pattern: _png_adler_step
---

<objective>
Create the private, portable PNG encode state machine and its single atomic preflight path for PNGE-01.

Purpose: Establish a byte-resumable canonical source of the existing eager PNG representation before any public caller-buffered encoder is introduced.
Output: A private `PngEncodeMachine`, shared eager-equivalent admission logic, and executable preflight/byte-generation tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-pausable-png-encode-substrate/29-RESEARCH.md
@.planning/milestones/v0.8-phases/26-pausable-png-decode-substrate/26-01-SUMMARY.md
@.planning/milestones/v0.8-phases/27-public-png-chunk-decoder/27-03-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/qoi/stream_encode.mbt
@modules/mb-image/qoi/stream_encode_wbtest.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze private PNG encode admission and incremental-byte behavior</name>
  <files>modules/mb-image/png/stream_encode_test.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - A 1x1 RGB8 source emits the existing 72-byte PNG oracle and a straight-RGBA8 source changes only the established IHDR colour type and corresponding canonical bytes.
    - The test-only machine driver presents then acknowledges every byte, pauses at each accepted-byte boundary and at split points inside signature, IHDR length/type/payload/CRC, zlib header, stored-block header, filter byte, sample byte, Adler-32, IDAT CRC, and IEND; concatenation remains exactly equal to the eager oracle.
    - The driver proves exact total length for one-block and multi-block scanlines using `scanline_bytes + 6 + 5 * blocks + 57`, with the final stored-block flag occurring only in the final block.
    - The IDAT CRC covers its type and complete zlib stream, while Adler-32 covers exactly filter and sample bytes, including scanlines spanning stored-block boundaries.
    - Constructor attempts for non-U8, non-packed, non-sRGB, non-top-left, opaque-metadata, non-RGB/straight-RGBA, dimension, output, work, and budget failures match current eager error category/code/context and leave a fresh budget's work remainder unchanged; no test obtains a destination before those results.
    - The machine state contains no MutByteLease, ByteView destination, whole-PNG, whole-IDAT, or complete scanline field; its bounded pending fragment is exercised across a fragment split.
  </behavior>
  <action>Before implementation, create `stream_encode_test.mbt` and `stream_encode_wbtest.mbt` as private-package RED coverage. Put canonical eager-machine byte equality and constructor error/budget parity in the ordinary test file; put byte-boundary state, checksums, exact-length arithmetic, and ownership-shape assertions in the white-box file. Build small RGB8/RGBA8 image helpers and a test-only drain helper that presents the current canonical byte and acknowledges it separately, so the tests can prove presentation alone leaves the machine byte and emitted/completed count unchanged; do not introduce any public type. Derive expected bytes from the existing eager encoder helper, not a second PNG builder. Include a width large enough to cross the 65,535-byte stored-block threshold and inspect real block headers, CRC, Adler, totals, and final-block selection. Pair each failed constructor case with an independent eager encode invocation so the tests freeze the existing typed error shape and verify the constructor has not charged work. Keep the tests private: do not declare `PngChunkEncoder`, pull outcomes, a generic stream abstraction, or any API in `png.mbt`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG encode machine*'</automated>
  </verify>
  <done>The focused tests fail before the private encode machine and shared admission path exist, while precisely stating canonical bytes, boundary state, preflight error parity, budget atomicity, and output-ownership limits required by PNGE-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared preflight and private canonical emitter</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - The RED tests in Task 1 pass on native and preserve the existing canonical RGB/RGBA byte sequences and typed preflight diagnostics.
    - `PngEncodeMachine::new` performs source capability checks, checked pixels/row/scanline/block/total arithmetic, current ordered limit checks, empty-disposition construction, and exactly one work charge before returning a usable machine.
    - A later fallible field construction cannot leave a charged budget when no machine is returned.
    - Presenting then acknowledging private-emitter bytes produces signature, IHDR, one IDAT header/type, zlib header, stored blocks, Adler-32, IDAT CRC, and IEND without materializing any output-sized buffer; a presented byte cannot advance cursors or emitted/completed count until acknowledged.
  </behavior>
  <action>Extract `_png_encode_source` and the current output-size/limit/budget logic into one private preflight helper in `encode.mbt`; retain the eager error operation, capability contexts, checked arithmetic, and existing limit ordering, while calculating scanline, stored-block, IDAT, and total lengths before source pixel reads. Construct `_png_empty_disposition()` before charging `ResourceCharge`, then return the disposition and validated scalar facts only after every fallible constructor step is complete. Per D-01, add `stream_encode.mbt` with private `PngEncodeMachine` state for immutable ImageView, dimensions/channels, row/scanline/block positions, IDAT CRC, Adler value, exact emitted/total counts, one presented-but-unacknowledged byte, and terminal phase. Expose only private operations that present the current byte without mutation and acknowledge that exact byte after a consumer accepts it; on acknowledgement update CRC/Adler, cursors, and emitted/completed count in the same transition. This preserves canonical byte order because a failed consumer can neither skip the presented byte nor cause the following byte to become visible. Reuse `_png_crc_for_type`, `_png_crc_step`, and `_png_adler_step`; do not create alternate checksum code. Do not retain a caller output buffer or lease, build full scanlines/zlib/IDAT/PNG bytes, modify `png.mbt`, alter the public semantic interface, add FFI/external packages, optimize compression, add APNG, or add colour transforms. Make only the test-driven changes needed to pass Task 1, then run the full PNG package suite across all portable targets.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>A private MoonBit `PngEncodeMachine` provides the existing canonical PNG byte sequence from resumable owned state, and every rejected source/limit/budget case fails atomically before any output sink exists, satisfying PNGE-01 without a public encoder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView and caller limits/budget → PngEncodeMachine::new | Source metadata, dimensions, and resource ceilings determine whether private encoding state may be created. |
| PngEncodeMachine → later eager/public sink adapters | Canonical bytes and checksum state cross from owned private state to a destination that this phase must not retain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of service | preflight geometry and length arithmetic | high | mitigate | Checked pixel, row, scanline, block, IDAT, and total-length arithmetic plus the current width, height, pixels, output, work, and budget checks run before machine creation. |
| T-29-02 | Tampering | canonical emitter cursors and checksums | high | mitigate | Persist stored-block, scanline, CRC, Adler, pending-fragment, and byte-count state; byte-boundary tests compare the complete stream with the eager oracle. |
| T-29-03 | Tampering | rejected construction and budget accounting | high | mitigate | Build disposition and all fallible fields before the one ResourceCharge; paired error/budget tests prove a failed constructor exposes no machine or output path. |
| T-29-04 | Information disclosure | output destination ownership | medium | mitigate | Private machine state excludes caller lease/view and output-sized buffers; white-box state and split-fragment tests make this boundary executable. |
</threat_model>

<verification>
- `moon -C modules/mb-image test png --target native --frozen -f '*PNG encode machine*'`
- `moon -C modules/mb-image test png --target all --frozen`
</verification>

<success_criteria>
- All compatible RGB8 and straight-RGBA8 sources use one private preflight route that preserves eager capability, dimension, limit, budget, and typed-error semantics before output is observable.
- The machine calculates the exact existing canonical PNG length and incrementally emits signature, IHDR, one stored-DEFLATE IDAT, and IEND with current CRC/Adler semantics.
- No public encoder declarations, output lease retention, output-sized staging buffer, FFI, alternate compression, APNG, or colour transform is introduced.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Compatible RGB8/straight-RGBA8 images enter private resumable encoding only after eager-equivalent admission succeeds | 29-01 | COVERED | Tasks 1-2 freeze and implement the one authoritative constructor. |
| REQ | PNGE-01 | Capability, dimensions, limits, and budget reject before encoded bytes are exposed | 29-01 | COVERED | Task 1 pairs errors/budget remainders with eager; Task 2 centralizes preflight before any sink. |
| RESEARCH | — | Preserve signature/IHDR/one stored-DEFLATE IDAT/IEND canonical bytes | 29-01 | COVERED | Tasks 1-2 prove byte parity, stored-block transitions, CRC, and Adler. |
| RESEARCH | — | Retain no caller output buffer or full output/scanline/IDAT staging | 29-01 | COVERED | Task 1 asserts private ownership shape; Task 2 restricts retained state to bounded fragments and scalar cursors. |
| RESEARCH | — | Pure MoonBit and four portable targets; no public API, FFI, packages, release work, optimization, APNG, or colour work | 29-01 | COVERED | Task actions and all-target test gate preserve the package boundary. |
| CONTEXT | D-01 | Private machine must retain a presented byte until its consumer acknowledges successful completion | 29-01 | COVERED | Task 1 tests present/acknowledge boundaries; Task 2 implements the private no-advance-before-acknowledgement state. |

<output>
Create `.planning/phases/29-pausable-png-encode-substrate/29-01-SUMMARY.md` when done.
</output>
