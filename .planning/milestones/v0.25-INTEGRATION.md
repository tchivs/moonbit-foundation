# v0.25 Integration Check

**Audited:** 2026-07-24  
**Status:** PASS — no cross-phase BLOCKER or WARNING found.

## Wiring Summary

**Connected:** 3 required Phase 79 → Phase 80 links, verified in live code and tests.  
**Orphaned:** 0.  
**Missing:** 0.

| Expected connection | Live trace | Status |
|---|---|---|
| `PngIndexedBitDepth::{One,Two,Four}` (79) → selected eager API and selected chunk API (80) | `encode.mbt:2285` and `stream_encode.mbt:41` each exhaustively map the same public selector to the private profile. | WIRED |
| Phase 79 selected-depth machine → Phase 80 caller-buffered adapter | `PngChunkEncoder::new_indexed` calls `PngEncodeMachine::new_with_indexed_profile` directly at `stream_encode.mbt:53`; that constructor performs Phase 79 profile-aware preflight and initializes the one acknowledged machine at `stream_encode.mbt:953`. | WIRED |
| Phase 79 eager output → Phase 80 byte-parity oracle | `png_stream_indexed_low_bit_eager` calls public `PngEncoder::encode_indexed` at `stream_encode_test.mbt:4924`; all One/Two/Four hostile-drain schedules compare accepted lease bytes to that result at `5080` and `5167`. | WIRED |

The selected profile drives depth caps (2/4/16), checked row sizing, IHDR depth/type, and scalar MSB-first packing. The fixed-Eight wrappers remain separate compatibility consumers, so the low-bit adapter neither bypasses admission nor creates a second transport.

## API Coverage

Not applicable: this is a MoonBit library milestone with no HTTP/API routes. Both public library entry points have live consumers in package tests.

## Auth Protection

Not applicable: no authentication or sensitive routes are in scope.

## E2E Flows

**Complete:** 2.  
**Broken:** 0.

1. **Eager Type-3/1, /2, /4:** `PngIndexedImage` → selector → selected preflight (palette caps and atomic budget charge) → shared machine packing/framing → literal Stored wire vectors → public RGB8/RGBA8 decode. Independent vectors assert `00 55 80`, `00 1B 00`, and `00 01 20` in `encode_test.mbt:957` and `999`.
2. **Caller-buffered Type-3/1, /2, /4:** `PngIndexedImage` → `PngChunkEncoder::new_indexed` → the same profile-aware machine → acknowledged `pull` leases → byte equality with the eager flow. Zero, one-byte, and ragged leases check accepted progress, untouched tails, sticky completion, released-lease failure replay, and atomic output/pixel/work rejection in `stream_encode_test.mbt:5078-5232`.

The ordinary portable package gate was rerun during this audit: `moon -C modules/mb-image test png --target all --frozen` passed **286/286** on wasm, wasm-gc, js, and native.

## Detailed Findings

### Orphaned Exports

None.

### Missing Connections

None.

### Broken Flows

None.

### Unprotected Routes

Not applicable.

### Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| INDEXLOW-01 | `PngIndexedImage` → `PngIndexedBitDepth` → `PngEncoder::encode_indexed` → shared machine | WIRED | — |
| INDEXLOW-02 | selected profile cap/facts → preflight → one budget charge → eager/chunk construction | WIRED | — |
| INDEXLOW-03 | machine scalar packing/PLTE/tRNS → literal wire vectors → public decoder | WIRED | — |
| INDEXLOW-04 | Phase 79 profile-aware machine → Phase 80 `new_indexed` → acknowledged `pull` → eager parity/lifecycle tests | WIRED | — |
| INDEXLOW-05 | retained eager wire/decode vectors + Indexed8 lifecycle tests + all-target package gate | WIRED | — |

**Requirements with no cross-phase wiring:** None. INDEXLOW-01–03 provide the selected-depth profile/machine and independent eager oracle consumed by INDEXLOW-04; INDEXLOW-05 qualifies the combined path on all targets.

## Scope Check

Phase 79 modified only its five planned PNG model/encoder/machine/test files. Phase 80 modified only `stream_encode.mbt` and `stream_encode_test.mbt`; its production diff is the thin `new_indexed` adapter. No generic model widening, second encoder/transport, Adam7, strategy expansion, staging, FFI, wrappers, copied source trees, or release automation was introduced.

_Note: the Phase 80 summary still records the original all-target run as `unknown`; its later verification claims a pass, and this audit independently reproduced the current four-target pass above._
