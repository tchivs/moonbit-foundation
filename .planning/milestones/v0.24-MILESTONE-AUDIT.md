---
milestone: v0.24
audited: 2026-07-24T20:20:00+08:00
status: passed
scores:
  requirements: 5/5
  phases: 3/3
  integration: 5/5
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 77-indexed-png-transparency
    items:
      - "PngIndexedImage::new gained required palette alpha during the unreleased v0.24 milestone; document the experimental/breaking API boundary before a stable package release."
---

# v0.24 Indexed PNG Encode — Milestone Audit

**Audit verdict: PASS — all five indexed-PNG requirements, cross-phase integrations, public-library flows, and the independent four-target package gate are evidenced.**

## Scope and method

The audit covers Phase 76 (owning Indexed8 source/eager PLTE), Phase 77 (canonical transparency), and Phase 78 (caller-buffered parity and qualification). It cross-referenced the milestone traceability table, each phase SUMMARY frontmatter, each passed phase VERIFICATION report, the integration review, and fresh main-branch runtime evidence.

| Check | Result | Evidence |
|---|---|---|
| Phase verification artifacts | PASS | `76-VERIFICATION.md`, `77-VERIFICATION.md`, and `78-VERIFICATION.md` are passed reports. |
| Summary requirement claims | PASS | Phase 76 lists INDEX-01/02, Phase 77 lists INDEX-03, and Phase 78 lists INDEX-04/05. |
| Traceability records | PASS | `REQUIREMENTS.md` marks INDEX-01 through INDEX-05 validated and maps each exactly once. |
| Cross-phase integration | PASS | Independent integration audit found five wired handoffs and no orphaned exports or broken flows. |
| Fresh main-branch package gate | PASS | `moon -C modules/mb-image test png --target all --frozen --target-dir D:\source\moonbit-foundation-v019\.moon-phase78-main` exited 0 in 187.3s: wasm, wasm-gc, js, native each reported 279/279; the temporary directory was removed. |

## Requirement coverage

| Requirement | Phase | Verification and integration evidence | Verdict |
|---|---:|---|---|
| INDEX-01 | 76 | `PngIndexedImage` owns validated canonical unpacked indices, RGB palette, and later alpha segments without widening generic image models; source validation and atomicity are covered by Phase 76 tests and verification. | PASS |
| INDEX-02 | 76 | `PngEncoder::encode_indexed8` uses the bounded shared machine to emit Type-3/8 `IHDR → PLTE → IDAT → IEND`; independent wire/CRC/public RGB8 decode and atomic admission are verified. | PASS |
| INDEX-03 | 77 | Last-non-opaque alpha determines canonical optional `tRNS`; the same machine emits/acknowledges its CRC, and public decode proves opaque RGB8 and transparent RGBA8 semantics. | PASS |
| INDEX-04 | 78 | `PngChunkEncoder::new_indexed8` is a thin adapter to `PngEncodeMachine::new_with_indexed`; zero/one/ragged, split/released leases, accepted-only progress, sticky terminals, and eager byte identity are verified. | PASS |
| INDEX-05 | 78 | Test-local wire/CRC oracles, frozen 89-byte opaque vector, public decoder paths, hostile lifecycle cases, and the ordinary main-branch four-target package gate all pass. | PASS |

## Cross-phase integration and public-library flows

| Flow | Result |
|---|---|
| Opaque eager Indexed8 → PLTE → RGB8 public decode | PASS |
| Transparent eager Indexed8 → canonical `tRNS`/CRC → RGBA8 public decode | PASS |
| Opaque caller-buffered Indexed8 → hostile leases → byte-identical 89-byte output | PASS |
| Transparent caller-buffered Indexed8 → shared machine → byte-identical `tRNS` output and RGBA8 decode | PASS |
| Invalid admission or invalid/released/split leases → no unauthorized write and sticky terminal | PASS |

## Non-blocking versioning note

Phase 77 added required palette alpha to the new `PngIndexedImage::new` constructor. The source and PNG bytes are consistent across v0.24 and there is no repository consumer or released stable API baseline to migrate, so this does not block the milestone. Before declaring the package API stable, document this experimental/breaking constructor evolution or add an explicit opaque compatibility constructor.

## Conclusion

v0.24 ships a dedicated, MoonBit-owned Indexed8 PNG path with exact PLTE and canonical optional transparency, plus caller-buffered parity through the existing acknowledged machine. No requirement, integration link, or portable qualification gate is unresolved. The milestone is ready to complete and archive.
