---
phase: 103-hostile-licensed-and-four-target-qualification
plan: 03
type: execute
wave: 3
depends_on:
  - 103-02
files_modified:
  - scripts/quality/Invoke-FontQualification.ps1
  - scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
  - scripts/quality/Assert-Policy.ps1
  - policy/foundation.json
  - modules/mb-font/moon.mod.json
  - .github/workflows/quality.yml
  - docs/policies/licensing-and-fixtures.md
  - modules/mb-font/README.mbt.md
  - modules/mb-font/CHANGELOG.md
autonomous: true
requirements:
  - TTC-04
  - TTC-05
must_haves:
  decisions:
    - "D-01/D-02: final qualification consumes the settled runtime/test contract and preserves the exact 85-line interface."
    - "D-09: version 0.32 standalone facts, 11 hostile outcomes, named focused assertions, exact Font subsequence, and per-target full package remain explicit baseline evidence without a brittle total constant."
    - "D-10: a fresh managed evidence schema version 2 emits exactly four ordered target records and compares every semantic field after removing only target and runner."
    - "D-11: the existing FontQualification runner and CI job own independent js, wasm, wasm-gc, and native qualification; no second lane/job is introduced."
    - "D-12: WOFF absence, inspect-only CFF/CFF2/variable profiles, DSIG unverified status, pure MoonBit, mb-core-only dependency, and exact API/source boundaries are executable facts."
    - "D-13/D-14: policy, foundation description, CI, license policy, README, and changelog match the shipped collection contract without publication/release-policy changes."
    - "D-15: this final slice consumes Plans 103-01/103-02 identities verbatim and completes TTC-04/TTC-05 qualification."
  truths:
    - "The managed marker is mnf-font-qualification-evidence/v2, workflow is font-complete-public-v2, and fresh local/CI directories never reuse, migrate, or delete version 1 evidence."
    - "Exactly four records appear in js, wasm, wasm-gc, native order with closed nested schemas covering toolchain, fixtures, standalone baseline, generated/licensed facts, hostile/mutation facts, boundaries, dependencies, focused assertions, runner, and pass."
    - "Semantic projection removes exactly top-level target and runner; every other canonical payload byte is equal and comparison.json records per-file and semantic SHA-256."
    - "Each target independently passes module check, all named standalone/generated/licensed/hostile/public/private assertions, and the complete font package; exact focused and final gate counts are discovered from actual output/artifacts and agree across targets."
    - "Negative evidence rejects schema/order/key/target/pass/digest/sharing/error/budget/boundary/dependency/assertion divergence and unsafe evidence-directory ownership."
    - "Policy independently preserves the production set, exact 85-line surface, five mb-core imports, sole mb-core module edge, four targets, WOFF/CFF/variable/source/FFI/I/O boundaries, and unchanged publication/release policy."
    - "README/changelog document TTC/OTC inspection and static-glyf selection, licensed derivative identity, qualification command/report, and retained exclusions; the complete final lane passes."
  prohibitions:
    - "Do not edit Plan 103-01 fixtures/generated artifacts or Plan 103-02 focused tests/identities."
    - "Do not add a second quality selector, runner, CI job, runtime decoder/profile capability, public API line, dependency, FFI, ambient I/O, publication file, publication policy, or release promotion."
    - "Do not normalize toolchain, fixtures, pass, nested fields, or anything except top-level target and runner."
    - "Do not reuse or recursively clean the version 1 evidence directory, accept caller-owned roots, follow links/reparse points, or upload failing evidence."
    - "Do not increase the 20-minute timeout unless the measured complete final lane demonstrates inadequate headroom; any increase must be minimal and recorded."
  artifacts:
    - path: scripts/quality/Invoke-FontQualification.ps1
      provides: "Closed managed four-target evidence version 2 runner, canonical comparison, and negative probes."
    - path: scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
      provides: "Managed-root/marker/link/cleanup/version-boundary negative matrix."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Independent fixture/evidence/interface/source/dependency/CI/documentation classifiers and negative fixtures."
    - path: policy/foundation.json
      provides: "Collection-aware module description with unchanged 85-line interface, dependency, targets, and publication inventories."
    - path: modules/mb-font/moon.mod.json
      provides: "Description synchronized with foundation policy and otherwise unchanged manifest."
    - path: .github/workflows/quality.yml
      provides: "Existing FontQualification job wired to passing version 2 evidence."
    - path: docs/policies/licensing-and-fixtures.md
      provides: "Explicit external-derivative provenance and notice rule."
    - path: modules/mb-font/README.mbt.md
      provides: "Executable collection workflow, licensed provenance, qualification command/report, and retained boundaries."
    - path: modules/mb-font/CHANGELOG.md
      provides: "Unpublished candidate history corrected for shipped collection inspection/selection and qualification."
  key_links:
    - from: scripts/quality/Invoke-FontQualification.ps1
      to: modules/mb-font/font/font_qualification_test.mbt
      via: "Exact Plan 103-02 public focused names gate record construction."
      pattern: "font-complete-public"
    - from: scripts/quality/Invoke-FontQualification.ps1
      to: artifacts/release-qualification/font-v2/comparison.json
      via: "Read-back-validated records and explicit target/runner-only semantic projection."
      pattern: "normalization_removed"
    - from: scripts/quality/Assert-Policy.ps1
      to: policy/foundation.json
      via: "Independent exact interface, source, dependency, target, fixture, runner, CI, and documentation assertions."
      pattern: "Assert-FontFoundationPolicy"
    - from: .github/workflows/quality.yml
      to: scripts/quality/Invoke-FontQualification.ps1
      via: "Existing FontQualification selector invokes the version 2 directory and uploads only success."
      pattern: "ci-font-v2"
---

<objective>
Produce and enforce the managed four-target evidence version 2 record, negative boundary matrix, policy/CI integration, documentation, and complete final qualification.

Purpose: Close TTC-04/TTC-05 with one canonical release-evidence boundary whose four independent target records are semantically identical and whose deferred/runtime/publication boundaries fail closed.
Output: Versioned runner/evidence policy, existing CI job wiring, collection-aware public documentation, and a passing complete qualification report.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-hostile-licensed-and-four-target-qualification/103-CONTEXT.md
@.planning/phases/103-hostile-licensed-and-four-target-qualification/103-RESEARCH.md
@.planning/phases/103-hostile-licensed-and-four-target-qualification/103-PATTERNS.md
@.planning/phases/103-hostile-licensed-and-four-target-qualification/103-01-SUMMARY.md
@.planning/phases/103-hostile-licensed-and-four-target-qualification/103-02-SUMMARY.md
@.planning/milestones/v0.32-phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/102-root-relative-selected-face-admission/102-VERIFICATION.md
@scripts/quality/Invoke-FontQualification.ps1
@scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
@scripts/quality/Assert-Policy.ps1
@policy/foundation.json
@.github/workflows/quality.yml
@modules/mb-font/moon.mod.json
@docs/policies/licensing-and-fixtures.md
@modules/mb-font/README.mbt.md
@modules/mb-font/CHANGELOG.md

<interfaces>
Frozen managed evidence identities:
- marker schema: `mnf-font-qualification-evidence/v2`
- workflow: `font-complete-public-v2`
- local directory: `artifacts/release-qualification/font-v2`
- CI directory: `artifacts/release-qualification/ci-font-v2`
- target order: `js`, `wasm`, `wasm-gc`, `native`
- target directories: `target/phase103-font-qualification-{target}`
- comparison normalization: exactly `target`, `runner`

Frozen public/dependency boundary:
- exactly 85 ordered semantic lines, including one `FontCollection::open_face` line.
- production sources remain the existing 13-file set.
- module dependency remains exactly `tchivs/mb-core`; package imports remain the existing five mb-core packages.
- supported targets remain `+js+wasm+wasm-gc+native`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Advance the managed runner and evidence-boundary matrix to version 2</name>
  <files>scripts/quality/Invoke-FontQualification.ps1, scripts/quality/Test-FontQualificationEvidenceBoundary.ps1</files>
  <read_first>
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-CONTEXT.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-RESEARCH.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-PATTERNS.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-01-SUMMARY.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-02-SUMMARY.md
    - scripts/quality/Invoke-FontQualification.ps1
    - scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
  </read_first>
  <behavior>
    - "Boundary tests fail against version 1 defaults and then pass only for a fresh version 2 child directory with the exact ownership marker/workflow."
    - "A version 1 marker cannot authorize version 2 cleanup; caller root, managed root, unowned/non-empty/corrupt/link/reparse paths fail without modifying unrelated bytes."
    - "Every target record and nested section rejects missing/extra/reordered keys, IDs, targets, assertions, false pass, or semantic divergence."
    - "Semantic comparison removes only target and runner and detects changes in every other section."
    - "Per-target focused identities/counts, full-package discovered pass totals, written record count, target count, negative-probe count, and final comparison gate count are derived from executed commands/artifacts and closed lists rather than guessed package totals."
  </behavior>
  <action>
Write failing boundary and schema negatives first. Advance the existing runner in place to marker `mnf-font-qualification-evidence/v2`, workflow `font-complete-public-v2`, default `artifacts/release-qualification/font-v2`, target directories `target/phase103-font-qualification-{target}`, and exactly four ordered targets. Preserve strict-child containment, link/reparse rejection, known-file-only cleanup, unrelated-file preservation, and read-back validation. Reject a valid version 1 marker in the version 2 path without migrating or deleting it.

Define the exact top-level target-record order `schema_version, workflow_id, target, toolchain, fixtures, standalone_baseline, generated_collection_facts, licensed_derivative_facts, collection_hostile_outcomes, mutation_atomicity_facts, boundary_facts, dependency_facts, focused_assertions, runner, pass`. Close every nested key and enum. Bind Plan 103-01 fixture/digest/oracle facts and Plan 103-02 exact test names/result shapes verbatim. Preserve compact/format-4/DejaVu/11-hostile/unsupported/Font-subsequence facts under `standalone_baseline`.

Per target run module check; each exact standalone compact, format-4, DejaVu, generated collection, licensed derivative, collection hostile/limit/budget, public mutation, private collection transition, and inherited query mutation assertion; then the complete font package. Each focused filter must execute exactly one named test. Parse and store the actual full-package passing total and command result, require equality across targets, and never compare it to a source constant. Derive/report the exact number of focused gates from the closed identity array, the target/record totals from the four written records, and final negative/comparison gate totals from the probes actually executed.

Construct one explicit ordered semantic projection containing every field except top-level `target` and `runner`; serialize compact LF UTF-8-no-BOM, compute SHA-256, and require equality. Write each record only after its target gates pass, read it back and revalidate, then write `comparison.json` with exact keys `schema_version, workflow_id, normalization_removed, targets, record_sha256, semantic_sha256, equal`.

Retain existing missing-record and divergence probes and add duplicate/reordered/unknown target, schema/workflow/pass, top-level/nested keys, derivative digest, shared coordinate, hostile error, one budget-after field, WOFF/dependency boundary, assertion identity/order, and version 1 marker divergences.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
./scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
./scripts/fixtures/Generate-FontQualification.ps1 -Check
pwsh -NoProfile -Command "& { . ./scripts/quality/Invoke-FontQualification.ps1; if (-not (Get-Command Invoke-FontQualification -ErrorAction SilentlyContinue)) { throw 'Import-only runner seam missing.' } }"
    </automated>
  </verify>
  <acceptance_criteria>
    - "Version 2 owns only its fresh link-free child directory and cannot mutate version 1 or unrelated files."
    - "Target/comparison records are fully closed, read-back validated, and target/runner-only normalized."
    - "Every mandatory semantic section and negative divergence is executable."
    - "Exact gate/result counts are discoverable from closed identities and actual run output, not a brittle remembered package total."
  </acceptance_criteria>
  <done>D-09/D-10/D-11 managed evidence identity, runner semantics, canonical comparison, and negative ownership/schema boundary are complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close policy, CI, documentation, and measured final qualification</name>
  <reversibility rating="costly">D-13 synchronizes exact policy inventories and CI evidence ownership; drift requires coordinated runner, policy, and documentation updates.</reversibility>
  <files>scripts/quality/Assert-Policy.ps1, policy/foundation.json, modules/mb-font/moon.mod.json, .github/workflows/quality.yml, docs/policies/licensing-and-fixtures.md, modules/mb-font/README.mbt.md, modules/mb-font/CHANGELOG.md</files>
  <read_first>
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-CONTEXT.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-RESEARCH.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-PATTERNS.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-01-SUMMARY.md
    - .planning/phases/103-hostile-licensed-and-four-target-qualification/103-02-SUMMARY.md
    - scripts/quality/Assert-Policy.ps1
    - policy/foundation.json
    - modules/mb-font/moon.mod.json
    - .github/workflows/quality.yml
    - docs/policies/licensing-and-fixtures.md
    - modules/mb-font/README.mbt.md
    - modules/mb-font/CHANGELOG.md
  </read_first>
  <behavior>
    - "Policy accepts only the exact new fixture/oracle/corpus identities and rejects digest, order, license, notice, redistribution, schema, and generated-symbol drift."
    - "The independent classifier still requires exactly 85 ordered semantic lines and rejects missing/duplicate/reordered/altered/private/deferred API alternatives."
    - "Executable-source policy rejects WOFF1/WOFF2 admission, CFF/CFF2 outline execution, variable instantiation, FFI, ambient I/O, extra sources/imports/dependencies, and target drift while ignoring comments/literal-only documentation correctly."
    - "The existing CI FontQualification job uses the version 2 directory, success-only upload, pinned action, and measured timeout policy; no second lane/job exists."
    - "Foundation and module descriptions mention bounded standalone TrueType plus TTC/OTC version 1/version 2 inspection and selected static-glyf admission while publication/release fields remain unchanged."
    - "README examples execute on all four targets and document the public collection workflow, licensed derivative, version 2 evidence command/report, and retained exclusions."
    - "The complete lane is measured under the existing 20-minute timeout; that value is retained when sufficient, otherwise the workflow receives only the smallest integer-minute increase strictly above the measured duration and the policy plus complete lane rerun."
  </behavior>
  <action>
Add failing independent policy negatives before updating accepted facts. Extend `Assert-FontFoundationPolicy` with exact Phase 103 manifest records, collection corpus/oracle schemas and ordered IDs, derivative length/digest/sharing/profile facts, generated symbols, runner records/normalization, focused names, managed-directory identities, and CI run/upload mapping. Add negative fixtures for relabeled licensed content, missing notice/digest, fixture/oracle drift, schema/key/order/ID drift, runner directory/normalization/target/pass drift, upload condition, and unmeasured timeout change.

Keep the independent interface classifier byte-identical at 85 lines, changing only its phase label if needed. Add fail-closed forbidden declarations for WOFF opening, collection extraction/materialization, CFF outline queries, and variable instantiation. Extend the existing comment/string/interpolation-aware portable-source lexer with executable WOFF admission/decode and variable/CFF execution patterns; retain direct negatives for FFI, ambient filesystem/network/host-font access, extra production/test/publication sources, extra dependency/import, and target drift. Prove public WOFF/WOFF2/OTTO Capability outcomes and collection CFF/CFF2/variable inspectable-but-not-selectable facts enter canonical evidence.

Update only the synchronized `tchivs/mb-font` descriptions in `foundation.json` and `moon.mod.json` to accurately name bounded standalone TrueType and TTC/OTC versions 1/2 inspection plus selected static-glyf admission. Preserve candidate stability, version, readme, repository, four targets, direct dependency, publication files, 13 production sources, test inventory, five allowed imports, and all 85 interface lines. Do not alter publication or release policy.

Correct the public README contract and executable examples per D-14. Add a public-only `mbt check` collection example that constructs caller-owned bytes, explicit `FontCollectionLimits`/`FontLimits`/budgets, opens a collection, inspects count/profiles/DSIG status, selects a static-glyf face, and calls existing `Font` observations. Document TTC/OTC versions 1/2 inspection, selected static-glyf admission, the two-face DejaVu derivative lineage/length/SHA-256 and both-face equivalence, the `FontQualification` command, fresh version 2 report directory, exactly four target records, and target/runner-only normalization.

Remove only stale statements that exclude collection inspection/selection. Retain explicit exclusions for WOFF1/WOFF2 admission, CFF/CFF2 selection/execution, variable instantiation, DSIG cryptographic trust, ambient I/O/FFI, discovery/shaping/hinting/rasterization, authoring/materialization, registry publication, and stability promotion. Preserve the Phase 100 standalone oracle narrative.

Update the existing unpublished `0.1.0 candidate` changelog section with TTC/OTC inspection and selected admission delivered by Phases 101/102, Phase 103 hostile/mutation/atomicity qualification, licensed derivative/oracle, and four-target evidence version 2. Replace only the stale collection exclusion and retain all other runtime, publication, release, and stability boundaries.

Keep the existing `font-qualification` CI job and selector. Change its evidence path and success-only artifact name/path to the version 2 directory while retaining the pinned installer/action and initial 20-minute timeout. Extend the licensing policy with the rule that deterministic external derivatives retain upstream origin/license/source/author, confirmed redistribution, generator/date, parent/derivative digests, notice, and expected use.

After all policy, CI, licensing, README, and changelog edits are present, run the complete CI-equivalent lane once with elapsed-time measurement. The runner must independently execute all four targets, exact focused assertions, complete per-target package tests, four README checks, policy/generator/boundary prerequisites, record read-back, negative probes, and canonical comparison. Record the discovered focused/full/final gate counts, elapsed duration, and semantic digest in the summary. If elapsed time is below 20 minutes, keep `timeout-minutes: 20` unchanged. If elapsed time is 20 minutes or greater, change only that timeout to the smallest whole minute strictly greater than the measured duration (`floor(elapsed_minutes) + 1`), record the measured justification, rerun `Assert-FontFoundationPolicy`, then rerun the complete four-target lane and require it to pass. No other CI or release-policy field changes are authorized by the measurement.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$elapsed = Measure-Command {
  ./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory artifacts/release-qualification/font-v2
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
Write-Host ("FontQualification elapsed: {0}" -f $elapsed)
$workflow = Get-Content -Raw '.github/workflows/quality.yml'
$timeoutMatch = [regex]::Match($workflow, '(?m)^\s*timeout-minutes:\s*(\d+)\s*$')
if (-not $timeoutMatch.Success) { throw 'FontQualification timeout-minutes is missing.' }
$configuredTimeout = [int]$timeoutMatch.Groups[1].Value
if ($configuredTimeout -lt 20) { throw "FontQualification timeout regressed to $configuredTimeout." }
if ($elapsed.TotalMinutes -ge $configuredTimeout) {
  throw "Complete lane took $($elapsed.TotalMinutes) minutes, exceeding configured timeout $configuredTimeout."
}
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
./scripts/fixtures/Generate-FontQualification.ps1 -Check
./scripts/quality/Test-FixturePolicy.ps1
./scripts/quality/Test-FontQualificationEvidenceBoundary.ps1
moon -C modules/mb-font check --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git diff --check
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "Policy independently closes fixture, license, oracle, corpus, generated, runner, evidence, CI, source, API, dependency, target, and documentation identities."
    - "The generated interface remains exactly 85 ordered lines and production/import/dependency/publication inventories do not expand."
    - "Deferred runtime formats are executable negative gates, not documentation-only statements."
    - "README examples pass on js, wasm, wasm-gc, and native through the final runner and accurately describe the public collection contract."
    - "README/changelog state the derivative identity, reproducible qualification route, and all retained format/trust/I/O/FFI/publication boundaries."
    - "Exactly four closed target records and comparison.json pass read-back, negative probes, semantic equality, and all discovered gate counts."
    - "The existing CI job owns passing version 2 evidence with no release-policy mutation; timeout stays 20 when measured sufficient or changes only to floor(elapsed_minutes)+1 followed by a passing policy and complete-lane rerun."
    - "The complete final lane, policy, generator, fixture/evidence boundaries, target-all check, and whitespace pass."
  </acceptance_criteria>
  <done>D-01/D-05/D-10 through D-14 and TTC-04/TTC-05 are completely policy-closed, documented, measured, and reproducible through the existing FontQualification ownership boundary under one exact CI/docs/measurement owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| target command output -> canonical evidence record | Only passing, attributable, closed facts may be serialized. |
| target records -> semantic comparison | Only approved target-specific fields may be excluded from equality. |
| caller evidence path -> managed cleanup | Cleanup must not escape ownership, follow links, or mutate older evidence. |
| fixture/runtime contract -> policy/CI | Independent policy must detect coordinated schema, API, dependency, or capability drift. |
| documentation -> maintainer workflow | Commands, provenance, and exclusions must remain executable and honest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-103-03-01 | Spoofing | target/fixture evidence identity | high | mitigate | Task 1 closes target order, toolchain, fixture hashes, test identities, pass state, and per-file hashes. |
| T-103-03-02 | Tampering | semantic comparison | high | mitigate | Task 1 explicitly projects every field except target/runner and executes section-specific divergence probes. |
| T-103-03-03 | Repudiation | qualification commands/results | high | mitigate | Tasks 1-2 record commands, discovered counts, target order, elapsed time, record hashes, and semantic digest. |
| T-103-03-04 | Information disclosure | evidence paths/runner records | medium | mitigate | Task 1 stores repository-relative paths and rejects caller roots, links, and reparse traversal. |
| T-103-03-05 | Denial of service | four-target lane/hostile inputs | high | mitigate | Tasks 1-2 use independent serial target dirs, bounded runtime tests, and one measured timeout owner that reruns policy and the full lane after any minimal increase. |
| T-103-03-06 | Elevation of privilege | evidence cleanup/upload | high | mitigate | Task 1 enforces child-only ownership/known-file cleanup; Task 2 keeps success-only CI upload. |
| T-103-03-07 | Tampering | API/source/dependency boundaries | high | mitigate | Task 2 independently locks 85 lines, 13 production sources, five imports, one dependency, four targets, and executable negatives. |
| T-103-03-08 | Spoofing | license/document claims | medium | mitigate | Task 2 binds exact derivative/source/notice digests and keeps unsupported/trust/publication claims explicit. |
| T-103-03-SC | Tampering | package supply chain | low | accept | No dependency install/change occurs; CI actions remain pinned and mb-font retains mb-core as its only runtime dependency. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Reproduce complete collection-to-Font workflow and fail-closed boundaries with generated/licensed four-target evidence | 103-01 through 103-03 | COVERED |
| REQ | TTC-04 | Structured hostile, mutation, limit, arithmetic, and budget failures are atomic | 103-01 corpus; 103-02 behavior; 103-03 reports | COVERED |
| REQ | TTC-05 | Generated/licensed/standalone/public facts are reproducible and equal on four targets | 103-01 fixtures; 103-02 tests; 103-03 runner | COVERED |
| RESEARCH | — | 757428-byte exact-sharing derivative and closed independent TTC oracle | 103-01 | COVERED |
| RESEARCH | — | No-duplicate generated mirror and immutable Phase 100 identities | 103-01 | COVERED |
| RESEARCH | — | Closed hostile/mutation/limit/budget schemas and exact public facts | 103-02 | COVERED |
| RESEARCH | — | Fresh managed evidence schema with target/runner-only normalization | 103-03 Task 1 | COVERED |
| RESEARCH | — | WOFF/CFF/variable/source/dependency/API boundaries and unchanged production surface | 103-03 Task 2 | COVERED |
| CONTEXT | D-01 | Qualification-only; exact 85-line interface and runtime semantics | 103-01 through 103-03 | COVERED |
| CONTEXT | D-02 | Promote existing behavior into canonical qualification artifacts | 103-01 through 103-03 | COVERED |
| CONTEXT | D-03 | Separate collection corpus/mirror; Phase 100 corpus unchanged | 103-01 | COVERED |
| CONTEXT | D-04 | Deterministic two-face exact-sharing DejaVu derivative | 103-01; 103-02 | COVERED |
| CONTEXT | D-05 | External derivative licensing and complete lineage | 103-01; 103-03 Task 2 | COVERED |
| CONTEXT | D-06 | Independent closed TTC oracle bound to SFNT oracle | 103-01; 103-02 | COVERED |
| CONTEXT | D-07 | Closed hostile/error/publication/budget matrix | 103-01; 103-02; 103-03 Task 1 | COVERED |
| CONTEXT | D-08 | Public mutation plus narrow deterministic private hooks | 103-02; 103-03 Task 1 | COVERED |
| CONTEXT | D-09 | Named v0.32 standalone semantic baseline without aggregate count lock | 103-02; 103-03 Task 1 | COVERED |
| CONTEXT | D-10 | Closed evidence version 2 and four exact ordered records | 103-03 Task 1 | COVERED |
| CONTEXT | D-11 | Extend existing FontQualification runner/CI lane | 103-03 Tasks 1-2 | COVERED |
| CONTEXT | D-12 | Inspect-only CFF/variable, absent WOFF, pure MoonBit/dependency/API boundary | 103-02; 103-03 Tasks 1-2 | COVERED |
| CONTEXT | D-13 | Fixture/evidence policy, foundation, source negatives, description, measured timeout | 103-03 Task 2 | COVERED |
| CONTEXT | D-14 | README/changelog TTC workflow/provenance/qualification and exclusions | 103-03 Task 2 | COVERED |
| CONTEXT | D-15 | Three strict dependent, conflict-safe slices | 103-01 -> 103-02 -> 103-03 | COVERED |
</source_audit>

<verification>
- Evidence boundary tests reject unsafe paths/markers and all schema/semantic divergences.
- Policy closes fixtures, oracle, corpus, generated source, 85-line API, production/test/publication inventory, targets, dependencies, source capabilities, runner, CI, and docs.
- Final FontQualification emits exactly four ordered records plus comparison.json and reports semantic equality after target/runner-only normalization.
- Per-target focused/full results and final gate counts are derived from actual commands/artifacts and agree without a hard-coded aggregate package count.
- Generator, fixture policy, boundary tests, target-all check, four README checks, existing CI-equivalent lane, and whitespace all pass.
</verification>

<success_criteria>
- TTC-04 atomic hostile/mutation/resource outcomes are preserved in canonical four-target records.
- TTC-05 generated, licensed, standalone, and complete public workflow facts are identical on js, wasm, wasm-gc, and native.
- Pure MoonBit, mb-core-only dependency, WOFF/CFF/variable/DSIG/API/source/publication boundaries fail closed independently.
- Maintainers have accurate executable documentation and a fresh managed evidence version 2 command/report without changing release policy.
</success_criteria>

<output>
Create `.planning/phases/103-hostile-licensed-and-four-target-qualification/103-03-SUMMARY.md` when done.
</output>
