---
phase: 101-collection-contract-and-bounded-envelope
plan: 03
type: execute
wave: 3
depends_on:
  - 101-02
files_modified:
  - modules/mb-font/font/collection_parser.mbt
  - modules/mb-font/font/collection.mbt
  - modules/mb-font/font/collection_test.mbt
  - modules/mb-font/font/collection_wbtest.mbt
autonomous: true
requirements:
  - TTC-01
must_haves:
  decisions:
    - "D-12: all failures publish nothing and leave the collection transaction uncommitted."
    - "D-14: final publication and every inspection are guarded by the same root revision."
    - "D-15: the TTCHeader 2.0 all-zero DSIG tuple is absent; every partial-zero tuple is malformed."
    - "D-16: a present DSIG is one bounded version-1/format-1 structural envelope ending at collection EOF, with opaque payload and PresentUnverified status."
    - "D-17: malformed DSIG structure is Data, complete unsupported version/format is Capability, and no cryptographic trust is inferred."
    - "D-18: limit/source/header/tuple authority, declaration-work preflight, bounded counts, structural-work preflight, face/protected/alias, DSIG semantics/body, retained/exact-work/full-budget, and revision failures follow one stable precedence."
    - "D-19: the public error taxonomy remains InvalidInput, Data, Capability, Resource, and State by condition."
  truths:
    - "Empty/DSIG predicate per D-15: TTCHeader 1.0 and a TTCHeader 2.0 all-zero `(tag,length,offset)` tuple report Absent; each partially zero tuple is Data; one valid present envelope reports PresentUnverified."
    - "Boundary predicate per D-16/D-17: a present DSIG uses tag DSIG, a checked non-empty aligned range ending exactly at collection EOF, version 1, at least one bounded format-1 record, contained non-overlapping blocks, zero reserved fields, exact inner/outer lengths, and non-empty opaque payload; exact boundaries pass and malformed or unsupported facts map deterministically."
    - "Precision predicate per D-09/D-10/D-12/D-13: DSIG record/byte counts and `C2(N)` work remain checked UInt64, exact max_dsig_records/max_dsig_bytes/max_work values pass, and every exact one-over or caller-budget one-short failure is Resource and atomic."
    - "D-12/D-14/D-18: validation and normalization finish before the final revision guard, one Budget charge, and FontCollection publication; deterministic mid-open drift, mutation, mutate-restoration, and all other errors leave every Budget dimension unchanged."
    - "D-18/D-19: multi-fault vectors prove the exact staged-authority-first, traversal-stable precedence at declaration_work-1, structural_work-1, and exact_work-1 under both collection and caller authority, plus complete category/code/context/requested/limit facts."
    - "TTC-01/D-01/D-03/D-06: the complete phase passes on js, wasm, wasm-gc, and native with additive count/profile/status APIs and byte-identical pre-existing Font declarations."
  prohibitions:
    - "Font::open must not auto-detect or admit TTC/OTC bytes."
    - "No public collection type or method may expose a raw offset, table record, table tag, checksum, checked range, ByteView, retained revision, DSIG record, DSIG payload, or parser fact."
    - "No DSIG status may be named or documented as valid, verified, authenticated, or trusted."
    - "No Phase 101 method may parse PKCS#7, validate certificates, consult a trust store, select a face into Font, checksum sibling payloads, decode WOFF, execute CFF/CFF2 or variation semantics, call FFI, or perform ambient I/O."
  artifacts:
    - path: modules/mb-font/font/collection_parser.mbt
      provides: "Bounded TTCHeader 2.0 DSIG tuple/envelope traversal plus complete parse/charge facts."
    - path: modules/mb-font/font/collection.mbt
      provides: "Final atomic publication sequence and explicitly unverified public DSIG status."
    - path: modules/mb-font/font/collection_test.mbt
      provides: "Public DSIG, precedence, budget atomicity, revision, interface, compatibility, and four-target evidence."
    - path: modules/mb-font/font/collection_wbtest.mbt
      provides: "Private DSIG tuple/record/block/work and deterministic mid-open revision matrix."
  key_links:
    - from: modules/mb-font/font/collection_parser.mbt
      to: modules/mb-font/font/collection.mbt
      via: "Private DSIG facts collapse to Absent or PresentUnverified before crossing the public facade."
      pattern: "FontCollectionDsigStatus"
    - from: modules/mb-font/font/collection_parser.mbt
      to: modules/mb-core/budget/budget.mbt
      via: "DSIG record/block work is included in the same exact ResourceCharge committed once."
      pattern: "font_collection_exact_work"
    - from: scripts/quality/Assert-Policy.ps1
      to: policy/foundation.json
      via: "The tracked Phase 101 classifier and ordered golden exact-compare ephemeral pkg.generated.mbti and reject trust wording, payload access, parser/storage facts, and selected-face methods."
      pattern: "Assert-FontFoundationPolicy"
---

<objective>
Complete TTCHeader 2.0 DSIG structural inspection, freeze full atomic/error precedence, and qualify the complete TTC-01 contract on every supported target.

Purpose: Distinguish absence from bounded structural presence without converting signature bytes into a trust claim or weakening the collection's resource/revision transaction.
Output: DSIG tuple/record/block validation, final public/white-box hostile matrices, unchanged standalone compatibility, and four-target phase evidence for D-12 and D-14 through D-19.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-CONTEXT.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-RESEARCH.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-PATTERNS.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-01-SUMMARY.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-02-SUMMARY.md
@.planning/milestones/v0.32-phases/100-portable-font-qualification/100-03-SUMMARY.md
@modules/mb-font/font/collection_limits.mbt
@modules/mb-font/font/collection_parser.mbt
@modules/mb-font/font/collection.mbt
@modules/mb-font/font/collection_test.mbt
@modules/mb-font/font/collection_wbtest.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
@modules/mb-core/budget/budget.mbt
@modules/mb-core/bytes/views.mbt

<interfaces>
Keep the public surface unchanged from Plan 101-01:
- `FontCollectionDsigStatus::{Absent, PresentUnverified}`
- `FontCollection::dsig_status() -> Result[FontCollectionDsigStatus, CoreError]`

Add only private DSIG facts and helpers. A supported present structure is TTCHeader 2.0 tuple tag `0x44534947`, nonzero length/aligned offset, EOF-terminated checked range, 8-byte version/count/flags header, 12-byte records, and version 1 / format 1 blocks whose payload bytes remain opaque.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| Modified file | `modules/mb-font/font/collection_parser.mbt` | TTCHeader 2.0 DSIG tuple, envelope, record, block, work, and precedence implementation. |
| Modified file | `modules/mb-font/font/collection.mbt` | Final atomic collection publication and closed DSIG status mapping. |
| Modified file | `modules/mb-font/font/collection_test.mbt` | Black-box DSIG/error/atomicity/revision/interface/compatibility matrix. |
| Modified file | `modules/mb-font/font/collection_wbtest.mbt` | Private DSIG/formula/traversal/mid-open matrix. |
| New private symbols | `CollectionDsigRecord`, `CollectionDsigFacts`, `font_collection_parse_dsig_tuple`, `font_collection_parse_dsig_envelope` | Bounded DSIG declaration and structural facts. |
| New private symbols | `font_collection_dsig_range`, `font_collection_dsig_block_range`, `font_collection_validate_dsig_blocks` | Checked root-relative containment and non-overlap helpers. |
| New private symbol | `FontCollection::open_after_normalize` | Deterministic white-box revision hook immediately before final publication guard. |
| New test-private symbols | `font_collection_test_dsig`, `font_collection_wb_dsig_case`, `font_collection_wb_assert_budget_unchanged` | Generated DSIG and atomicity matrices. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse bounded DSIG structure without making a trust claim</name>
  <files>modules/mb-font/font/collection_parser.mbt, modules/mb-font/font/collection.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-CONTEXT.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-RESEARCH.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-PATTERNS.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-02-SUMMARY.md
    - modules/mb-font/font/collection_parser.mbt
    - modules/mb-font/font/collection.mbt
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-font/font/cursor.mbt
    - modules/mb-core/checked/range.mbt
  </read_first>
  <behavior>
    - "TTCHeader 1.0 reports Absent; TTCHeader 2.0 all-zero tuple reports Absent; every partial-zero combination reports Data/InvalidEncoding."
    - "A present tuple requires DSIG tag, nonzero bounded bytes, aligned offset, exact EOF end, and no protected/table intersection."
    - "A supported envelope requires version 1, nonzero bounded record count, permitted flags, complete record array, format 1, contained non-overlapping blocks, zero reserved fields, exact inner/outer lengths, and nonempty payload."
    - "Malformed/truncated/inconsistent structure is Data; a structurally complete unsupported version or signature format is Capability."
    - "A supported envelope exposes only PresentUnverified and never reads, copies, hashes, parses, or authenticates payload bytes."
  </behavior>
  <action>
Write the complete tuple/header/record/block mutation matrix before implementation. For TTCHeader 2.0, treat all tuple fields equal to zero as Absent and every partial-zero combination as Data per D-15. For a present tuple, require the DSIG tag, nonzero length, four-byte-aligned checked root range ending exactly at source EOF, semantic byte authority, and disjointness from the header, every directory, and every table per D-08/D-16.

Parse the bounded 8-byte header (`version`, `numSignatures`, `flags`) and checked `8 + 12*N` record array in wire order. Require version 1; classify a complete other version as Capability. Require nonzero count before `max_dsig_records`, permit only the defined flag bit, and for each record require format 1 after its complete envelope is available. Derive every block relative to DSIG start, require record length at least 8, containment outside the header/record array, pairwise block non-overlap, two zero reserved UInt16 fields, exact `8 + signatureLength == record.length`, and nonzero signatureLength. Complete unsupported formats are Capability; truncation, escape, overlap, reserved, and length inconsistency are Data per D-17/D-19.

Include `3 + 6*N + C2(N)` in exact DSIG work, bound bytes/records before loops, and retain only the closed status/facts needed by the facade. Do not inspect payload content. The public result for every supported structure is exactly PresentUnverified.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase101-plan03-dsig-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font/collection_test.mbt --target native --frozen --target-dir target/phase101-plan03-dsig-public --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "Absent, every partial-zero tuple, valid present, malformed present, unsupported version, and unsupported format cases have exact expected outcomes."
    - "Exact DSIG byte/record ceilings pass and one-over declarations fail Resource before dependent traversal."
    - "All protected/table/block containment, touching, overlap, reserved, and exact-length boundaries are covered."
    - "Present DSIG work matches the exact formula and overflows/one-short work fail atomically."
    - "Supported public DSIG cases return PresentUnverified and payload bytes remain opaque in all black-box observations."
  </acceptance_criteria>
  <done>D-15 through D-17 and the DSIG empty/boundary/precision predicates are complete through the closed TTC-01 public status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close publication precedence, standalone compatibility, and four-target evidence</name>
  <files>modules/mb-font/font/collection_parser.mbt, modules/mb-font/font/collection.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-CONTEXT.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-RESEARCH.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-01-SUMMARY.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-02-SUMMARY.md
    - modules/mb-font/font/collection_parser.mbt
    - modules/mb-font/font/collection.mbt
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/font_test.mbt
    - policy/foundation.json
    - scripts/quality/Assert-Policy.ps1
    - modules/mb-core/budget/budget.mbt
  </read_first>
  <behavior>
    - "Multi-fault inputs fail in the exact D-18 sequence and preserve full CoreError fields for all five D-19 categories."
    - "Every malformed, unsupported, semantic-limit, caller-budget, or deterministic mid-open revision failure publishes no FontCollection and leaves Budget::remaining byte-identical."
    - "A successful open preflights exact charge, normalizes compact facts, final-guards the root revision, charges once, then publishes."
    - "Mutation and mutate-restoration after open invalidate count, profile, and DSIG queries; independent collections retain independent revision identities."
    - "The complete public/private suite and generated interface pass independently on js, wasm, wasm-gc, and native while existing standalone vectors remain unchanged."
  </behavior>
  <action>
Add table-driven public and white-box precedence cases for the complete D-18 order: limit constructor; source ceiling; tag/version/header/face-count and DSIG-tuple authority; exact declaration-work `max_work` and caller-work preflight before the per-face declaration scan; face table-count and cumulative-record ceilings; bounded DSIG count discovery; exact structural-work `max_work` and caller-work preflight before face/protected/alias/DSIG-body traversal; face search/tag/range/profile facts in wire order; protected/alias relations; DSIG version/count-zero/flags and record/block facts; retained bytes; exact total work; full Budget preflight; normalization; final revision; single charge/publication. Keep DSIG count discovery separate from semantic declaration validation so tuple/count authority is bounded early while face/protected/alias errors precede DSIG version/count-zero/flags. Add exact `declaration_work-1`, `structural_work-1`, and `exact_work-1` cases for both authority sources and combined malformed-face versus DSIG tuple/version/count/flags cases. Assert exact category, code, operation, context, source_offset, requested, and limit whenever defined per D-19.

Expose a private test hook immediately after normalization and before the final revision check. Use it to mutate and mutate-restore the shared root, proving State failure with no budget commit and no returned collection. Cover every Data/Capability/Resource/State open failure with complete Budget::remaining equality; cover successful exact charges and independent collection/budget/revision isolation. Re-run all public inspection mutation cases after successful DSIG-absent and PresentUnverified opens.

Treat `modules/mb-font/font/pkg.generated.mbti` as repository-ignored ephemeral compiler output: assert that policy with `git check-ignore`, regenerate it with `moon info`, and do not hand-edit, force-add, or commit it. Without changing the Plan 01 allowlist in this plan, invoke `Assert-FontFoundationPolicy` so its case-sensitive exact-sequence comparison to `policy/foundation.json` and independent Phase 101 classifier fail closed on any added, removed, reordered, parser/storage, trust, payload, or selected-face declaration. Run the complete `font` package independently on js, wasm, wasm-gc, and native with isolated target directories. The gate must exercise black-box collection tests, white-box formulas/ranges/DSIG/precedence, existing standalone checksum/error vectors, and package compilation on every declared target. Preserve D-01 and D-06: do not route collection bytes through `Font::open`, perform selected-face admission, or add payload checksums.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
foreach ($target in @('js','wasm','wasm-gc','native')) {
  moon -C modules/mb-font test font --target $target --frozen --target-dir "target/phase101-plan03-$target" --no-parallelize
  if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
}
moon -C modules/mb-font info --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
git check-ignore -q -- modules/mb-font/font/pkg.generated.mbti
if ($LASTEXITCODE -ne 0) { throw 'pkg.generated.mbti must remain ignored compiler output.' }
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
moon -C modules/mb-font check --target all --frozen
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "The complete staged D-18 precedence sequence and D-19 taxonomy are executable with exact declaration, structural, and total one-short boundaries plus DSIG authority-versus-semantics conflicts."
    - "Every rejected open leaves all Budget dimensions unchanged; success applies one exact compact charge and returns one collection."
    - "Mid-open mutation, post-open mutation, and mutate-restoration fail State without stale semantic publication."
    - "All four targets pass the complete package with identical public collection semantics and no target-specific branch or narrowing."
    - "The generated interface is exactly additive; standalone Font::open tests and public signatures are unchanged."
  </acceptance_criteria>
  <done>TTC-01 and D-01 through D-19 are qualified end-to-end with atomic, revision-safe, four-target evidence and no Phase 102 capability.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS L1 applies to untrusted DSIG bytes, revision races, and transactional publication. The configured block threshold is high; every high-severity threat is mitigated and has explicit test ownership.

## Trust Boundaries

| Boundary | Description |
|---|---|
| TTCHeader DSIG tuple -> DSIG parser | Untrusted offsets/counts/lengths must not escape collection or resource authority. |
| structural DSIG -> public status | Structural presence must not be misrepresented as authenticity or trust. |
| normalized facts -> Budget/publication | Revision and complete validation must precede the single commit. |
| target runtime -> phase evidence | Host-width or target-specific results must not diverge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-03-01 | Denial of service | DSIG count/range/block traversal | high | mitigate | Task 1 owns byte/record ceilings, checked ranges/work, and exact/one-short DSIG matrices. |
| T-101-03-02 | Spoofing | public DSIG status | high | mitigate | Task 1 owns the closed PresentUnverified contract and payload/trust interface assertions. |
| T-101-03-03 | Tampering | DSIG/protected/table overlap | high | mitigate | Task 1 owns root-coordinate EOF, protected, record-array, and block non-overlap tests. |
| T-101-03-04 | Tampering | source revision race | high | mitigate | Task 2 owns the deterministic post-normalization hook, mutate-restoration, and every-query revision gates. |
| T-101-03-05 | Repudiation | partial budget/publication | high | mitigate | Task 2 owns complete failure atomicity, exact success charge, and no-returned-collection assertions. |
| T-101-03-06 | Information disclosure | error/parser surface | medium | mitigate | Task 2 owns exact bounded CoreError fields and generated-interface opacity checks. |
| T-101-03-07 | Tampering | target-specific narrowing | high | mitigate | Task 2 owns separate full-suite runs on js, wasm, wasm-gc, and native plus target-all interface/check gates. |
| T-101-03-SC | Tampering | dependency supply chain | low | accept | No package installation or new dependency occurs; all code is pure MoonBit over the existing mb-core workspace dependency. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Complete bounded collection inspection and absent/present-unverified DSIG distinction | Tasks 1-2 | COVERED |
| REQ | TTC-01 | Raw TTC/OTC 1.0/2.0 open, exact profiles, closed DSIG status, opaque internals | Tasks 1-2 plus Plans 101-01/02 | COVERED |
| RESEARCH | — | Structural DSIG policy, exact DSIG work, final revision/charge sequence, full adversarial/four-target matrix, and exact interface-leak rejection | Tasks 1-2 | COVERED |
| CONTEXT | D-01-D-04 | Separate opaque facade and closed semantic facts | Preserved and gated by Task 2 | COVERED |
| CONTEXT | D-05-D-09 | Complete structure, root offsets, protected/alias/work rules | Preserved from 101-02 and extended by Task 1 | COVERED |
| CONTEXT | D-10-D-13 | All ceilings, no-copy charge, atomic preflight/commit, exact work | Tasks 1-2 plus 101-02 | COVERED |
| CONTEXT | D-14 | Open/query revision protocol | Task 2 | COVERED |
| CONTEXT | D-15-D-17 | DSIG absence, bounded structural presence, no trust, Data/Capability split | Task 1 | COVERED |
| CONTEXT | D-18-D-19 | Complete precedence and structured taxonomy | Task 2 | COVERED |

All GOAL/REQ/RESEARCH/CONTEXT items are covered across the three plans. Selected-face admission remains Phase 102; licensed end-to-end qualification remains Phase 103. WOFF, CFF/CFF2/variation execution, DSIG cryptography, FFI, ambient I/O, shaping, rendering, and authoring are explicit exclusions rather than planning gaps.
</source_audit>

<verification>
1. Run focused DSIG and precedence suites on native during task development.
2. Run the complete font package independently on js, wasm, wasm-gc, and native.
3. Run target-all package check, regenerate the ignored interface output, and exact-compare it to the tracked policy and independent classifier.
4. Inspect exact budget deltas, failure atomicity, revision hooks, public opacity, and standalone compatibility.
</verification>

<success_criteria>
- Absent and structurally present-unverified DSIG states are exact, bounded, and never imply trust.
- Malformed, unsupported, resource, and revision failures follow one stable precedence and publish/charge nothing.
- All Phase 101 boundary, adjacency, empty, ordering, and precision predicates are executable.
- The complete additive TTC-01 contract passes on all four targets without selected-face admission or standalone regressions.
</success_criteria>

<output>
Create `.planning/phases/101-collection-contract-and-bounded-envelope/101-03-SUMMARY.md` when done.
</output>
