---
phase: 101-collection-contract-and-bounded-envelope
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - modules/mb-font/font/collection_parser.mbt
  - modules/mb-font/font/collection.mbt
  - modules/mb-font/font/collection_test.mbt
  - modules/mb-font/font/collection_wbtest.mbt
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
autonomous: true
requirements:
  - TTC-01
must_haves:
  decisions:
    - "D-05: opening validates exact collection/header/offset/directory/range/profile/protected facts for every face."
    - "D-06: collection inspection does not checksum table payloads or perform selected-face semantic admission."
    - "D-07: face-directory fields use absolute directory bases while table-record offsets remain relative to collection byte zero."
    - "D-08: exact cross-face range sharing requires matching tag, length, and stored checksum; every other overlap is rejected."
    - "D-09: cumulative declarations and exact deterministic formulas bound all allocation and pair work."
    - "D-11: source length is semantic authority, not copied-allocation bytes."
    - "D-12: exact retained/work charge is preflighted before normalization and committed once on successful publication."
    - "D-13: work includes declaration, structure, profile, protected/alias, and normalization operations but excludes payload checksum and selected-face work."
  truths:
    - "Boundary predicate per D-05/D-09/D-10: complete offset arrays and directories accept exact source/face/per-face/cumulative ceilings; truncation or malformed ranges are Data, and each exact one-over ceiling is Resource before its dependent scan."
    - "Adjacency predicate per D-08: exact cross-face range plus tag/length/checksum metadata sharing is valid; touching half-open endpoints are valid; same-face equality/overlap, partial cross-face overlap, conflicting metadata, and any table intersection with header, offset array, any directory, or present DSIG are Data."
    - "Ordering predicate per D-03/D-04/D-05: public zero-based face indices preserve TTC offset-array order, directory tags are strictly ordered and unique, and equal face profiles remain distinct entries at their original indices."
    - "Precision predicate per D-07/D-09/D-12/D-13: every range/formula stays checked UInt64 until authority permits narrowing, and exact-one-short max_work, retained-bookkeeping, Budget bytes, allocations, allocation_size, and work evidence fails atomically."
    - "D-04/D-06: structurally valid StaticGlyf, Cff, Cff2, Variable, and OtherUnsupported siblings are all inspectable through their closed semantic profiles in offset-array order."
    - "D-11/D-12: a successful open charges `96 + 40*F + 24*(1+F+S)` retained-bookkeeping bytes, two allocations, `max(40*F,24*(1+F+S))` allocation size, and exact declared work."
  prohibitions:
    - "Font::open must not auto-detect or admit TTC/OTC bytes."
    - "No public collection type or method may expose a raw offset, table record, table tag, checksum, checked range, ByteView, retained revision, or parser fact."
    - "No collection structural pass may checksum table payloads, enforce the standalone required-table set, decode sibling semantics, or construct a selected Font."
    - "ResourceCharge bytes must not include caller-owned source length as copied allocation."
    - "No Phase 101 method may interpret DSIG payload, claim signature trust, decode WOFF, execute CFF/CFF2 or variation semantics, call FFI, or perform ambient I/O."
  artifacts:
    - path: modules/mb-font/font/collection_parser.mbt
      provides: "Three-pass all-face structural parser, protected/alias validators, compact facts, and exact resource formulas."
    - path: modules/mb-font/font/collection.mbt
      provides: "Atomic facade integration for fully normalized non-DSIG and DSIG-absent collection facts."
    - path: modules/mb-font/font/collection_test.mbt
      provides: "Black-box mixed-profile/order/sharing/limit/budget and standalone-compatibility evidence."
    - path: modules/mb-font/font/collection_wbtest.mbt
      provides: "White-box root-range, profile, protected-set, alias, formula, and traversal-order evidence."
    - path: policy/foundation.json
      provides: "Exact mb-font source/test/publication inventories including the new collection white-box suite."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Executable exact directory/source/publication selectors aligned with the Phase 101 white-box artifact."
  key_links:
    - from: modules/mb-font/font/collection_parser.mbt
      to: modules/mb-font/font/directory.mbt
      via: "Each face reuses the pure search-fact helper while retaining a collection-specific root-coordinate parser."
      pattern: "font_directory_search_facts"
    - from: modules/mb-font/font/collection_parser.mbt
      to: modules/mb-core/checked/range.mbt
      via: "Header, directories, tables, and protected structures use checked half-open root ranges."
      pattern: "CheckedRange"
    - from: modules/mb-font/font/collection.mbt
      to: modules/mb-core/budget/budget.mbt
      via: "The exact compact ResourceCharge is preflighted and committed once after validation/revision guards."
      pattern: "ResourceCharge::new"
    - from: modules/mb-font/font/collection_test.mbt
      to: modules/mb-font/font/font.mbt
      via: "Standalone regression vectors prove the separate Font::open path has not changed."
      pattern: "Font::open"
---

<objective>
Expand the proven collection tracer into a complete bounded all-face structural envelope with root-relative ranges, stable profile ordering, protected-range/alias rules, and exact retained/work accounting.

Purpose: Make attacker-controlled TTC/OTC topology inspectable without allowing counts, offsets, overlaps, or host-width conversion to become allocation/work authority.
Output: Complete non-cryptographic collection structure validation and adversarial black-box/white-box evidence for D-05 through D-13 and TTC-01.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-CONTEXT.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-RESEARCH.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-PATTERNS.md
@.planning/phases/101-collection-contract-and-bounded-envelope/101-01-SUMMARY.md
@modules/mb-font/font/collection_limits.mbt
@modules/mb-font/font/collection_parser.mbt
@modules/mb-font/font/collection.mbt
@modules/mb-font/font/directory.mbt
@modules/mb-font/font/cursor.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
@modules/mb-core/checked/checked.mbt
@modules/mb-core/checked/range.mbt
@modules/mb-core/budget/budget.mbt

<interfaces>
Preserve the Plan 101-01 public collection interface unchanged. Internally introduce:
- private `CollectionTableRecord`, `CollectionFaceFacts`, `CollectionProtectedRange`, and aggregate `CollectionParseFacts`
- a declaration pass that establishes count/limit/formula authority without allocation
- a structural pass in face-offset and record order
- a normalization pass that allocates only compact face/protected arrays
- exact `font_collection_retained_charge(F,S)` and `font_collection_exact_work(F,R,P,V,S,N)` helpers using checked UInt64

Use the research constants exactly: base 96 bytes, 40 bytes per face fact, 24 bytes per protected fact, two retained allocations, and `P = 1 + F + S`.
</interfaces>
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| Modified file | `modules/mb-font/font/collection_parser.mbt` | Complete count-first all-face parser and accounting engine. |
| Modified file | `modules/mb-font/font/collection.mbt` | Exact preflight/normalization/revision/charge publication sequence. |
| Modified file | `modules/mb-font/font/collection_test.mbt` | Public mixed-profile, order, sharing, authority, budget, and compatibility matrix. |
| New file | `modules/mb-font/font/collection_wbtest.mbt` | Private range/alias/formula/precedence matrix. |
| Modified file | `policy/foundation.json` | Add the collection white-box suite to the exact mb-font test-source and publication inventories. |
| Modified file | `scripts/quality/Assert-Policy.ps1` | Keep executable exact font source/test/publication selectors synchronized with the new white-box file. |
| New private symbols | `CollectionTableRecord`, `CollectionFaceFacts`, `CollectionProtectedRange`, `CollectionParseFacts` | Compact parser and retained topology facts. |
| New private symbols | `font_collection_declaration_facts`, `font_collection_structural_facts`, `font_collection_normalize_facts` | Three-pass count-first admission sequence. |
| New private symbols | `font_collection_directory_range`, `font_collection_table_range`, `font_collection_classify_profile` | Root-relative range and closed profile helpers. |
| New private symbols | `font_collection_validate_protected_ranges`, `font_collection_validate_aliases` | Protected/table and exact cross-face sharing enforcement. |
| New private symbols | `font_collection_choose2`, `font_collection_retained_charge`, `font_collection_exact_work` | Checked target-neutral accounting formulas. |
| New test-private symbols | `font_collection_test_multi_ttc`, `font_collection_test_shared_ttc`, `font_collection_wb_assert_range`, `font_collection_wb_assert_charge` | Deterministic adversarial builders and exact assertions. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate every face directory and preserve semantic order</name>
  <reversibility rating="costly">D-07 fixes the coordinate seam inherited by selected-face admission; changing it later would alter retained windows, identity, and downstream checksum behavior.</reversibility>
  <files>modules/mb-font/font/collection_parser.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/collection_wbtest.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <read_first>
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-RESEARCH.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-PATTERNS.md
    - .planning/phases/101-collection-contract-and-bounded-envelope/101-01-SUMMARY.md
    - modules/mb-font/font/collection_parser.mbt
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-font/font/directory.mbt
    - modules/mb-font/font/cursor.mbt
    - policy/foundation.json
    - scripts/quality/Assert-Policy.ps1
    - modules/mb-core/checked/range.mbt
  </read_first>
  <behavior>
    - "TTCHeader 1.0 and 2.0 base structures accept a complete non-zero face offset array and reject every truncation boundary in wire order."
    - "Each non-zero directory offset validates the SFNT signature, non-zero table count, searchRange/entrySelector/rangeShift, ordered unique tags, and checked root-relative table ranges."
    - "A deliberately in-bounds wrong-rebase fixture succeeds only when record offsets are resolved from collection byte zero."
    - "Face profiles follow offset-array order and classify static glyf, CFF, CFF2, variable, and contradictory/other unsupported combinations without merging equal profiles."
    - "Collection inspection does not read table payload checksums or enforce the standalone required-table set."
  </behavior>
  <action>
Write failing white-box and black-box matrices first, then implement the declaration and complete structural passes per D-05/D-07/D-09. In the declaration pass, check source authority, base header, non-zero/max face count, checked complete offset array, then each face's non-zero table count, per-face ceiling, and cumulative table-record ceiling before allocation or dependent pair work. In the structural pass, replay fields in stable offset-array/record order, compute each directory as `directory_offset + 12 + 16*numTables`, validate all three stored search facts with the existing pure helper, require ordered unique tags, and derive every table range from the collection-root record offset with checked UInt64 operations.

Classify profiles after each ordered tag scan using the D-04 priority: any variation marker is Variable; otherwise coherent OTTO+CFF2 is Cff2, coherent OTTO+CFF is Cff, coherent 0x00010000+glyf+loca without excluded markers is StaticGlyf, and every other structurally valid combination is OtherUnsupported. Keep profiles in original face index order. Treat unsupported profiles as information, not open failures, and perform no payload checksum, required-table, cmap/metric/kern/glyf, or selected-face work per D-06/D-13. Remap hostile checked-range failures to Data/InvalidEncoding; limit failures remain Resource/BudgetExceeded per D-19.

In the same change that creates `collection_wbtest.mbt`, add that exact filename to the tracked font `test_sources` and publication inventory in `policy/foundation.json`, and to the matching exact arrays in `scripts/quality/Assert-Policy.ps1`. Preserve the Phase 101 semantic-interface golden and independent classifier established by Plan 101-01. This keeps the repository's exact package-directory gate satisfiable at the wave where the new file first exists.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase101-plan02-structure-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font/collection_test.mbt --target native --frozen --target-dir target/phase101-plan02-structure-public --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
    </automated>
  </verify>
  <acceptance_criteria>
    - "Empty/truncated headers, offset arrays, directory headers, and final records fail at their first wire-order fault."
    - "Exact source, face, per-face-table, and cumulative-record ceilings pass; one-over values fail before dependent work."
    - "The wrong-rebase-in-bounds fixture and before/after-directory ranges prove collection-root record offsets."
    - "Mixed and equal-profile collections preserve exact face count, index order, and all five closed profile outcomes."
    - "Unsupported siblings open structurally and no payload checksum or selected-face semantic branch executes."
  </acceptance_criteria>
  <done>D-04 through D-07, D-09, and the boundary/ordering predicates are fully executable for every collection face.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact sharing and preflight deterministic retention/work</name>
  <files>modules/mb-font/font/collection_parser.mbt, modules/mb-font/font/collection.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/collection_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/collection_parser.mbt
    - modules/mb-font/font/collection.mbt
    - modules/mb-font/font/collection_test.mbt
    - modules/mb-font/font/collection_wbtest.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-core/budget/budget.mbt
    - modules/mb-core/checked/checked.mbt
    - modules/mb-core/checked/range.mbt
  </read_first>
  <behavior>
    - "Header/offset-array, every directory, and present-DSIG coordinates form a pairwise-disjoint protected set; every non-empty table is disjoint from it."
    - "Same-face table overlap always fails; different-face exact range sharing succeeds only with matching tag, length, and stored checksum."
    - "Different-face partial overlap or exact range with conflicting metadata fails; same-size/equal-content distinct ranges remain distinct and valid."
    - "Retained bytes and exact work use the research 96/40/24 model and formula, with every product/sum checked before narrowing or looping."
    - "Exact semantic and caller budgets succeed; each one-short retained/work/bytes/allocations/allocation_size failure leaves all budget dimensions unchanged."
  </behavior>
  <action>
Build the complete protected set in header, face-directory, then optional-DSIG order and validate protected pairs, every table against every protected range, and every unordered table pair per D-08. Empty ranges and exact touching endpoints do not overlap. For table pairs, reject every same-face overlap; permit cross-face exact equality only when tag, length, and stored checksum metadata all match; reject partial intersections and metadata conflicts. Do not compare payload bytes or hashes to infer identity.

Implement `C2(x)` with checked subtraction before multiplication and one checked exact-work helper containing declaration reads, structural reads, profile work, `C2(P)`, `R*P`, `C2(R)`, DSIG terms, and normalization exactly as specified in research. Implement retained bytes as `96 + 40*F + 24*P`, allocations=2, allocation_size=`max(40*F,24*P)`, width/height/pixels=0, and work=exact work. Source length is checked against `max_source_bytes` but excluded from ResourceCharge bytes per D-11. Check all semantic ceilings, then caller `Budget::preflight`, normalize compact arrays, run the final revision guard, call `Budget::charge` exactly once, and publish per D-09/D-12/D-13/D-18.

Add exact-fit and one-short tests for max_retained_bookkeeping_bytes, max_work, and every nonzero caller charge dimension, including formula overflow and multi-fault precedence. Assert full Budget::remaining equality on each rejection and exact deltas on success.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
moon -C modules/mb-font test font/collection_wbtest.mbt --target native --frozen --target-dir target/phase101-plan02-accounting-wb --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font/collection_test.mbt --target native --frozen --target-dir target/phase101-plan02-accounting-public --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font test font --target native --frozen --target-dir target/phase101-plan02-regression --no-parallelize
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
    </automated>
  </verify>
  <acceptance_criteria>
    - "The complete adjacency matrix distinguishes valid touching/exact sharing from every forbidden overlap or metadata conflict."
    - "Header, all directories, and DSIG coordinates protect one global root-coordinate space."
    - "The exact retained/work formulas match white-box term assertions and overflow maps to Resource/BudgetExceeded."
    - "Exact semantic/budget values pass; every one-short failure is atomic and success charges compact bookkeeping rather than source length."
    - "The complete native font suite remains green and standalone Font::open error/checksum vectors are unchanged."
  </acceptance_criteria>
  <done>D-08 through D-13 and the adjacency/precision predicates are enforced with exact, atomic resource evidence.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS L1 applies to hostile multi-face counts, ranges, and pair work. The configured block threshold is high; all high-severity topology/resource threats are mitigated and verified in this plan.

## Trust Boundaries

| Boundary | Description |
|---|---|
| wire counts -> loops and arrays | Face/table declarations cannot authorize allocation or pair work by themselves. |
| wire offsets -> root checked ranges | Face-local field positions and collection-root table offsets use different, explicit coordinate rules. |
| table/protected ranges -> sharing decision | Exact aliases must be distinguished from collision and metadata confusion. |
| exact parser facts -> caller Budget | The complete charge must be preflighted and committed atomically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-02-01 | Denial of service | face/table declarations | high | mitigate | Task 1 owns per-face/cumulative ceilings before loops/allocation and exact boundary tests. |
| T-101-02-02 | Tampering | directory/table coordinate origin | high | mitigate | Task 1 owns root-relative checked ranges and the in-bounds wrong-rebase fixture. |
| T-101-02-03 | Tampering | overlap and shared-table identity | high | mitigate | Task 2 owns full protected/table pair matrices and exact range+metadata sharing tests. |
| T-101-02-04 | Denial of service | pair comparisons and normalization | high | mitigate | Task 2 owns checked exact-work/retention formulas, semantic ceilings, and overflow/one-short tests. |
| T-101-02-05 | Repudiation | partial budget publication | high | mitigate | Task 2 owns complete preflight, final revision guard, single charge, and Budget::remaining equality assertions. |
| T-101-02-06 | Information disclosure | raw normalized facts | medium | mitigate | Both tasks retain parser facts privately and rerun the black-box/interface boundary established by Plan 01. |
| T-101-02-SC | Tampering | dependency supply chain | low | accept | No dependency or package installation occurs; checked arithmetic, ranges, bytes, and budget come from the existing pinned mb-core workspace module. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Bounded all-face semantic facts without parser internals | Tasks 1-2 | COVERED |
| REQ | TTC-01 | Exact count/profile over raw TTC/OTC 1.0/2.0 under explicit limits | Tasks 1-2 | COVERED |
| RESEARCH | — | Three-pass authority, root ranges, compact profiles, exact sharing, 96/40/24 retention, exact work formula | Tasks 1-2 | COVERED |
| CONTEXT | D-03-D-07 | Closed ordered profiles and complete root-relative all-face structure | Task 1 | COVERED |
| CONTEXT | D-08 | Protected ranges and exact cross-face sharing | Task 2 | COVERED |
| CONTEXT | D-09-D-10 | Cumulative limits before allocation/pair work | Tasks 1-2 | COVERED |
| CONTEXT | D-11-D-13 | No-copy charge, exact transaction, declared work scope | Task 2 | COVERED |
| CONTEXT | D-18-D-19 | Stable traversal/ceiling/budget precedence and errors | Tasks 1-2 | COVERED |

DSIG body semantics remain assigned to Plan 101-03. Selected-face admission and checksum policy remain assigned to Phase 102. WOFF, outline execution, FFI, ambient I/O, and cryptographic trust remain outside this phase.
</source_audit>

<verification>
1. Run white-box directory/range/profile/protected/alias/formula tests on native.
2. Run black-box mixed-profile/order/sharing/authority/budget tests on native.
3. Assert exact-fit and one-short values for every semantic and caller resource dimension.
4. Run the complete native font package and compare standalone `Font::open` vectors.
</verification>

<success_criteria>
- Every face is validated in wire order and remains observable at its original zero-based index.
- Root-relative table ranges, protected structures, and exact cross-face aliases obey one checked global coordinate model.
- Counts, retained bytes, and pair work are bounded by exact checked formulas before allocation or traversal.
- Resource failures are atomic and a successful open charges only compact retained bookkeeping.
</success_criteria>

<output>
Create `.planning/phases/101-collection-contract-and-bounded-envelope/101-02-SUMMARY.md` when done.
</output>
