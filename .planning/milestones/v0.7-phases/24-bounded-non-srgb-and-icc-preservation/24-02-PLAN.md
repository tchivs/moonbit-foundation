---
phase: 24-bounded-non-srgb-and-icc-preservation
plan: "02"
type: execute
wave: 2
depends_on: ["24-01"]
gap_closure: true
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
autonomous: true
requirements: [PNGCM-03]
must_haves:
  truths:
    - "Every temporary iCCP compressed/input/history/output allocation and every DEFLATE work unit is reserved from the caller's budget and max_work before image construction."
    - "Malformed ICC header/signature/declared size, incompatible profile space, compressed/inflated cap, allocation, and work-limit cases return typed errors with no image or budget overrun."
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: "Checked iCCP temporary resource budget and DEFLATE work enforcement."
      contains: "_png_inflate_iccp"
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: "Independent iCCP hostile resource/header vectors."
      contains: "icc"
---

<objective>
Close the sole Phase 24 verifier gap: make iCCP temporary storage and DEFLATE
work obey the same checked caller budget and codec limits as the rest of PNG
decode, with executable all-target evidence for each resource/header boundary.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-CONTEXT.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-01-SUMMARY.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-VERIFICATION.md
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/png_test.mbt
@scripts/fixtures/Generate-PngDecodeVectors.ps1
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Charge iCCP temporary allocations and DEFLATE work before expansion</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt</files>
  <behavior>
    - The forward reader reserves/charges bounded compressed payload collection, inflation output, history/window, and scratch allocations from a caller-derived budget before allocating each temporary buffer.
    - `_png_inflate_iccp` receives the caller budget and codec limits, preflights every declared/derived allocation, and consumes checked work for decompression rather than relying only on a private 64 KiB ceiling.
    - Allocation, work, compressed-byte, and inflated-byte failures use deterministic typed errors, leave no visible `OwnedImage`, and do not let a later metadata budget retroactively hide prior side effects.
  </behavior>
  <action>Write failing budget/work tests first. Thread the decode caller's `Budget` and `CodecLimits` into `_png_read_colour_chunk` and `_png_inflate_iccp`; derive a dedicated temporary child/reservation before collecting iCCP data, allocating output/history, or entering DEFLATE loops. Reuse existing checked budget and work-accounting helpers so every byte/allocation/work unit is either preflighted or charged in the same convention as IDAT decode. Preserve the 64 KiB hard safety ceiling as a secondary cap, but make caller limits authoritative when smaller. Ensure resource rejection happens before transport metadata/raster/OwnedImage construction, and retain the existing valid RGB iCCP retention path. Add white-box/public tests for all four resource dimensions and unchanged/no-result error behavior.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>No iCCP parser buffer, inflater history/output buffer, or DEFLATE CPU work bypasses the caller's checked budget/max_work envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand independent ICC hostile evidence</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - The independent corpus contains legal compatible ICC input plus malformed header, signature, declared-size, incompatible-space, compressed-limit, inflated-limit, allocation-limit, and work-limit cases.
    - Generated all-target assertions distinguish the expected typed ICC/resource errors from generic EOF and prove no successful image result; focused tests assert budget remains within the denied reservation boundary.
  </behavior>
  <action>Extend the declarative colour fixture schema and PowerShell oracle so it can construct deterministic minimal ICC envelopes for each header/signature/size/space/resource condition without hand-copied profile blobs. Add compact iCCP records for incompatible profile space and malformed header fields alongside compressed/inflated limits, then route allocation/work-limit cases through decoder options/limits in the existing public test harness. Make the generator reject invalid schema combinations, update its required-ID/completeness checks, regenerate the MoonBit corpus, and update only the associated manifest digest. Keep valid metadata and Phase 24 capability tests intact.</action>
  <verify><automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated></verify>
  <done>All ICC grammar, profile-space, byte/allocation/work bounds are independently encoded and exercised through PngDecoder on every supported target.</done>
</task>
</tasks>

<verification>
1. `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `moon -C modules/mb-image test model --target all --frozen`
4. `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<output>
Create `.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-02-SUMMARY.md` when done.
</output>
