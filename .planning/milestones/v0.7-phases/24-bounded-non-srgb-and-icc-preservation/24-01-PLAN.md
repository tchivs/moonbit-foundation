---
phase: 24-bounded-non-srgb-and-icc-preservation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - modules/mb-image/png/generated_decode_vectors_test.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
autonomous: true
requirements: [PNGCM-03, PNGCM-04]
must_haves:
  truths:
    - "Legal gAMA/cHRM declarations and bounded legal iCCP profiles decode as explicit, bounded non-sRGB metadata without transforming or relabelling pixels."
    - "Invalid ICC envelope, compression, profile-space, and resource-expansion paths fail before image visibility."
    - "Reference operations and canonical PNG encoding return a typed capability error instead of losing retained non-sRGB semantics."
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: "Bounded colour-declaration transport, ICC validation, precedence, and error path."
      contains: "PngColourFacts"
    - path: modules/mb-image/png/png.mbt
      provides: "Non-sRGB ImageMetadata/opaque metadata construction."
      contains: "PngDecoder"
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: "Independent legal and hostile legacy/ICC decoder vectors."
      contains: "Generate-PngDecodeVectors"
  key_links:
    - from: modules/mb-image/png/structural.mbt
      to: modules/mb-image/png/png.mbt
      via: "CRC-authenticated authoritative colour facts travel in PngStreamTransport into bounded ImageMetadata."
      pattern: "PngColourFacts|PngStreamTransport"
    - from: fixtures/png/decode-cases.json
      to: modules/mb-image/png/generated_decode_vectors_test.mbt
      via: "Generator emits legal/hostile colour records executed by the public PngDecoder on all targets."
      pattern: "colour_chunks|PngDecodeVector"
---

<objective>
Replace Phase 23's non-sRGB decode rejection with bounded, explicit metadata
preservation. Raster samples remain untransformed; invalid declarations fail
before image visibility and lossy operations remain typed capability boundaries.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-CONTEXT.md
@.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-VERIFICATION.md
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/metadata/metadata.mbt
@modules/mb-color/profile/profile.mbt
</context>

<source_audit>
SOURCE  | ID       | Feature / constraint | Coverage | Status
REQ     | PNGCM-03 | Retain legal legacy/ICC declarations as bounded non-sRGB metadata. | Tasks 1-2 | COVERED
REQ     | PNGCM-04 | Reject operations that would transform or discard retained semantics. | Task 3 | COVERED
CONTEXT | D-01/D-02 | Canonical opaque metadata; do not relabel non-sRGB pixels. | Task 1 | COVERED
CONTEXT | D-03/D-04 | Bounded pure-MoonBit ICC envelope/profile path and deterministic failure. | Task 2 | COVERED
CONTEXT | D-05/D-06 | Typed operation boundary; preserve sRGB behavior and deterministic precedence. | Tasks 1/3 | COVERED
CONTEXT | D-07 | Independent all-target legal/hostile vectors. | Task 3 | COVERED
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve authenticated legacy declarations as non-sRGB metadata</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png.mbt, modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt</files>
  <behavior>
    - Legal gAMA/cHRM payloads are retained as deterministic bounded opaque metadata and return a successfully decoded image whose colour/transfer identity is not encoded sRGB.
    - `sRGB` remains authoritative over gAMA/cHRM only when those legacy values are compatible, preserving Phase 23's conflict errors and existing sRGB metadata.
    - A legal iCCP declaration is authoritative over sRGB, gAMA, and cHRM: its validated profile metadata is the authoritative declaration, lower-priority legacy metadata is not exposed as a competing interpretation, and illegal conflicts retain typed rejection rather than encounter-order behavior.
    - Metadata allocation failure occurs before OwnedImage creation and no image is exposed.
  </behavior>
  <action>Write failing parser/public tests first. Extend `PngColourFacts` and transport with only CRC-authenticated, fixed-size legacy bytes needed for canonical opaque metadata. In `png.mbt`, build a bounded non-sRGB ImageMetadata/opaque-metadata result for authoritative legal gAMA/cHRM declarations while preserving Phase 23's sRGB result exactly. Implement a single explicit precedence function: a validated iCCP profile is authoritative; otherwise validated sRGB is authoritative and only compatible gAMA/cHRM may coexist; otherwise the present legacy declarations are authoritative non-sRGB metadata. Do not label a retained legacy result as `EncodedSrgb`; keep reference operations ineligible. Use `OpaqueMetadata::from_entries` and existing checked budget/metadata limits before storage allocation. Add focused white-box and public tests for bytes/keys, no relabelling, sRGB+compatible/contradictory legacy values, and iCCP+sRGB/legacy precedence fixtures. No transform, FFI, release work, or public streaming API.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>Legal non-ICC legacy declarations produce bounded explicit non-sRGB image metadata; precedence and sRGB compatibility are deterministic and image construction stays atomic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate and retain bounded iCCP profile data</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png.mbt, modules/mb-image/png/png_test.mbt, modules/mb-image/png/structural_wbtest.mbt</files>
  <behavior>
    - A legal iCCP name, method, zlib stream, and compatible RGB/gray ICC header decode to bounded opaque profile/name metadata without a colour transform.
    - Invalid names/methods, malformed/truncated zlib, bad ICC header/signature/declared length, incompatible profile colour space, and compressed/inflated/allocation/work overages return deterministic typed errors before an image is visible.
    - Profile input and inflated bytes are retained only after explicit bounds and budget preflight; no attacker-controlled unbounded collection is introduced.
  </behavior>
  <action>Start with hostile tests. Reuse existing pure-MoonBit zlib/DEFLATE internals through a private adapter, not an FFI or generic new public compression API. Buffer compressed iCCP data only behind a declared/codec-limit cap, then inflate through checked byte/allocation/work limits and retain an opaque profile only after validating the minimum ICC header and declared profile size. Accept only the profile colour spaces compatible with the decoded PNG sample family (RGB/gray as supported by the phase); reject other spaces deterministically. Preserve the validated profile name and canonical opaque data in the Task 1 transport/metadata path. Keep iCCP authoritative according to Task 1 and do not implement profile transforms. Add tests proving each cap/error is reached before `OwnedImage` creation and budget/storage side effects remain atomic.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test model --target all --frozen</automated></verify>
  <done>Valid bounded compatible iCCP profiles survive as explicit non-sRGB metadata, while all malformed/incompatible/over-limit paths fail safely before image visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove portable metadata and lossy-operation boundaries</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/png_test.mbt, fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt</files>
  <behavior>
    - Independent compact vectors cover legal gAMA, cHRM, compatible iCCP, authoritative iCCP combinations, and malformed/over-bound ICC paths through PngDecoder on js, wasm, wasm-gc, and native.
    - Generated assertions inspect metadata identities/keys/values and exact typed contexts, not only a successful decode result.
    - Canonical PNG encoding and reference operations reject every retained non-sRGB image with the established typed capability category/code before producing output; existing encoded-sRGB behavior remains valid.
  </behavior>
  <action>Extend the declarative fixture schema/generator and manifest with minimal independently assembled legacy/ICC chunks, zlib payloads, malformed profiles, and declared resource-limit records; do not hand-edit generated MoonBit output. Make the generator validate ordering, CRC, iCCP grammar, expected authoritative metadata, and exact errors independently. Gate `PngEncoder` before writing output whenever source metadata is retained non-sRGB or otherwise lacks actual encoded-sRGB eligibility, returning the established typed capability result. Reuse the existing reference-operation eligibility predicate and add public assertions for operation/encoder no-output behavior. Regenerate the vector corpus and preserve provenance/digest checks.</action>
  <verify><automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test model --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated></verify>
  <done>PNGCM-03/04 have manifest-backed, four-target evidence for retained declarations, explicit precedence, bounded failures, and typed no-loss operation/encoding boundaries.</done>
</task>

</tasks>

<verification>
1. `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `moon -C modules/mb-image test model --target all --frozen`
4. `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
PNGCM-03 and PNGCM-04 are met with bounded pure-MoonBit metadata/profile handling, deterministic iCCP precedence, and portable proof that no operation silently transforms or drops non-sRGB semantics.
</success_criteria>

<output>
Create `.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-01-SUMMARY.md` when done.
</output>
