---
milestone: v0.26
name: Indexed8 Adam7 PNG Encode
audited: 2026-07-24T00:50:20Z
head: bb708ce13ab808c64a2ef168a91851381218acd2
status: passed
verdict: PASS
scores:
  requirements: 6/6
  phases: 2/2
  integration: 6/6
  flows: 2/2
gaps: []
tech_debt: []
---

# v0.26 Indexed8 Adam7 PNG Encode — Milestone Audit

**Original intent:** Add an opt-in, bounded Type-3/8 Adam7 encoder for canonical `PngIndexedImage`, preserve every legacy non-interlaced Indexed8/low-bit route, and prove wire, lifecycle, compatibility, public decode, and all-four-target portability.

**Audited HEAD:** `bb708ce13ab808c64a2ef168a91851381218acd2` (`docs(v0.26): verify Indexed8 Adam7 integration`)

## Verdict: PASS — ready to archive

The product implementation, two required end-to-end flows, and the four-target qualification are all evidenced. The retained raw output in `.planning/milestones/v0.26-FOUR-TARGET-TEST.txt` records the clean current-HEAD command `moon -C modules/mb-image test png --target all --frozen`, exit code 0, and 291/291 passing tests on wasm, wasm-gc, js, and native.

## Coverage Against Original Intent

| Requirement | Requirement / original outcome | Requirements traceability | Phase verification | Summary claim | Independent code/audit evidence | Final |
|---|---|---|---|---|---|---|
| INDEXADAM7-01 | Additive eager and chunk Type-3/8 Adam7 selectors; legacy Indexed8 signatures and bytes remain non-interlaced. | `[x]`, Phase 81 | `passed` | Listed by 81-01 | Eager selector in `encode.mbt:2286`; chunk selector in `stream_encode.mbt:36`; both construct `PngEncodeMachine::new_with_indexed_profile(... Eight, requested strategy, ...)`. Legacy Indexed8 and indexed 1/2/4 paths pass `None`. | PASS |
| INDEXADAM7-02 | Checked shared Adam7 geometry and direct canonical index traversal; no second encoder or staging. | `[x]`, Phase 81 | `passed` | Listed by 81-01 | Indexed preflight uses `_png_adam7_passes(width, height, 1UL, 8)` at `encode.mbt:2128`; scalar emission uses the same geometry and `PngIndexedImage::index_at` at `stream_encode.mbt:1018`. One `PngEncodeMachine` path remains. | PASS |
| INDEXADAM7-03 | Correct Type-3/8 frame, palette/transparency, CRC, seven-pass raster, and public RGB/RGBA decode. | `[x]`, Phase 81 | `passed` | Listed by 81-01 | Frame emission is in the shared machine; `encode_test.mbt:1103` checks literal frame/raster/CRCs and public RGB8/RGBA8 decode. The expected 36-byte raster is a hand-authored test literal (`encode_test.mbt:1097`), not production geometry. | PASS |
| INDEXADAM7-04 | Exact atomic scanline/frame/work/output/budget admission. | `[x]`, Phase 81 | `passed` | Listed by 81-01 | Layout-specific preflight occurs before construction/output; `encode_wbtest.mbt:1142` covers exact and one-less admission. A failed chunk constructor cannot expose a caller lease. | PASS |
| INDEXADAM7-05 | Eager-identical caller leases with accepted-only progress, untouched tails, and sticky terminals. | `[x]`, Phase 82 | `passed` | Listed by 82-01 | Real `PngChunkEncoder::pull` path is driven at `stream_encode_test.mbt:5174`; schedules include zero/one/ragged capacities, prefix-only collection, sentinel tails, released-lease failure, and repeated `Finished` checks (`:5242`). | PASS |
| INDEXADAM7-06 | Independent wire/decode, frozen compatibility, and ordinary PNG gate on wasm, wasm-gc, js, native. | `[x]`, Phase 82 | `passed` | Listed by 82-01 | `stream_encode_test.mbt:5117` independently qualifies drained bytes. `moon.mod.json` declares all four targets. The retained raw output records `moon -C modules/mb-image test png --target all --frozen`, exit 0, and 291/291 for wasm, wasm-gc, js, and native. | PASS |

**Three-source coverage:** Every INDEXADAM7 item is mapped in `REQUIREMENTS.md`, listed in its phase SUMMARY frontmatter, and marked satisfied in its phase verification. INDEXADAM7-06 additionally has retained raw four-target execution output, so its runtime qualification no longer depends on documentation claims.

## Phase and Cross-Phase Integration

| Phase | Goal outcome | Verification | Code audit | Result |
|---|---|---|---|---|
| 81 | Bounded Adam7 Indexed8 machine and eager wire contract, with frozen legacy routes. | `5/5`, `passed` | Selectors, preflight, pass mapping, scalar source reads, frame construction, and literal independent eager test are all wired. | PASS |
| 82 | Caller-owned lease qualification and portability proof over the same machine. | `5/5`, `passed` | Tests exercise Phase 81's public chunk selector rather than a test-owned transport; lifecycle and independent drained-byte qualification are substantive. Retained raw output proves the all-target package gate at 291/291 each. | PASS |

| Cross-phase handoff | Actual path | Result |
|---|---|---|
| Eager selector → shared machine | `PngEncoder::encode_indexed8_with_interlace_strategy` → `PngEncodeMachine::new_with_indexed_profile` | WIRED |
| Chunk selector → same machine | `PngChunkEncoder::new_indexed8_with_interlace_strategy` → the same constructor | WIRED |
| Adam7 facts → scalar bytes | Shared `_png_adam7_passes(..., 1, 8)` → mapped `index_at(x, y)` | WIRED |
| Phase 82 leases → Phase 81 selector | Hostile drain calls the public Adam7 chunk factory and `pull` | WIRED |
| Accepted bytes → independent qualification | Accepted prefixes → `Bytes::from_array` → parser/CRC/Stored raster/public decoder | WIRED |
| Compatibility tests → package gate | Frozen Indexed8/1/2/4 vectors remain in the same PNG test package | WIRED |

**Integration score:** 6/6 expected links wired; no orphaned selector or parallel encoder path was found.

## End-to-End Flows

| Flow | Trace | Result |
|---|---|---|
| Explicit eager Indexed8 Adam7 encode/decode | `PngIndexedImage` → eager explicit selector → shared preflight/machine → mapped Adam7 scalar bytes → `IHDR → PLTE → tRNS? → IDAT → IEND` → public decoder | PASS: the eager test asserts literal 5×5 seven-pass raster, CRCs, canonical transparency, and all RGB/RGBA palette pixels. |
| Caller-buffered Indexed8 Adam7 encode/decode | `PngIndexedImage` → public chunk selector → same machine → `pull` under zero/one/ragged leases → accepted prefixes → independent parser/raster/public decoder | PASS: the Phase 82 tests check parity, accepted-only totals, untouched sentinel tails, sticky Failed and Finished outcomes, then independently consume drained bytes. |

## Scope, Compatibility, and Debt

- **PASS — scope stayed bounded.** `git diff --name-status 38a16dc..226192f -- modules/mb-image/png` changes only `encode.mbt`, `stream_encode.mbt`, `encode_test.mbt`, `encode_wbtest.mbt`, and `stream_encode_test.mbt`; Phase 82 changes only `stream_encode_test.mbt`.
- **PASS — no production scope creep.** No alternate encoder, image/pass/output staging, low-bit Adam7 selector, model widening, filters/compression expansion, FFI, wrapper, copied tree, or decoder change was found.
- **PASS — compatibility boundary remains explicit.** Legacy `encode_indexed8`/`new_indexed8` and indexed 1/2/4 constructors select `PngInterlaceStrategy::None`; Adam7 preflight accepts only `PngIndexedWireProfile::Eight`.
- `git diff --check` is clean for the five product files. The one `Phase 36 RED` comment at `encode_wbtest.mbt:10` predates this milestone (`e845e5ec`) and is not v0.26 debt. Planning-Markdown trailing whitespace is non-product hygiene only.
- The pre-existing untracked Phase 66 research input was not used as evidence and was left unchanged.

## Four-Target 291 Evidence Gate

**Status: PASS.**

The retained raw output in `.planning/milestones/v0.26-FOUR-TARGET-TEST.txt` is from the clean current-HEAD execution of:

```powershell
moon -C modules/mb-image test png --target all --frozen
```

It records exit code 0 and the following result for every declared production target:

| Target | Result |
|---|---|
| wasm | 291 total, 291 passed, 0 failed |
| wasm-gc | 291 total, 291 passed, 0 failed |
| js | 291 total, 291 passed, 0 failed |
| native | 291 total, 291 passed, 0 failed |

`modules/mb-image/moon.mod.json` declares `+js+wasm+wasm-gc+native`, and no PNG source/test change follows the Phase 82 test fix (`git diff --name-status 226192f..HEAD -- modules/mb-image/png` is empty).

## Archive Recommendation

**ARCHIVE APPROVED.** All six requirements are covered by requirements traceability, phase verification, SUMMARY declaration, and code/retained runtime evidence. Cross-phase integration is complete, both E2E flows pass, no product scope creep was found, and the ordinary PNG package gate now has auditable four-target proof.

---

_Auditor: gsd-audit-milestone (goal-backward, adversarial evidence review)_
