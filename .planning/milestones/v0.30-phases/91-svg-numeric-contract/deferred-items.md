# Deferred Items

- Review the production `skewX(45)` / `skewY(45)` interpretation separately from this test-only contract plan: current `Affine2::skew` controls map a unit side to approximately `0.7853981633974483`, and this plan preserves that established finite behavior without changing parser semantics.
