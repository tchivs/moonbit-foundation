---
phase: 91-svg-numeric-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/policies/svg-numeric-admission.md
  - modules/mb-svg/svg/numeric_contract_wbtest.mbt
autonomous: true
requirements:
  - SVGPR-01
must_haves:
  truths:
    - "Library users can consult one policy that defines the inclusive, target-neutral SVG scalar envelope [-65536.0, 65536.0] and applies it to source and derived values (D-01, D-02)."
    - "A finite SVG at the documented boundary parses and lowers on js, wasm, wasm-gc, and native, and a finite singular transform remains an accepted control (D-05, D-06)."
    - "The policy states that explicit unsafe values use stable CoreError category/code/operation/context fields while omitted attributes retain their existing default or inheritance branch (D-03, D-04)."
  artifacts:
    - "docs/policies/svg-numeric-admission.md"
    - "modules/mb-svg/svg/numeric_contract_wbtest.mbt"
  key_links:
    - "The policy's scalar envelope is evidenced by the all-target parse_svg -> lower_to_drawing_list tracer test."
    - "The policy's route identifiers map source and derived numeric routes to the existing mb-svg seams without assigning canvas ownership to SVG."
  assumptions:
    - "SVGPR-01 adjacency/equality: scalar admission has no merge, collision, or separation behavior; no such behavior is specified or changed."
    - "SVGPR-01 empty/null: collection emptiness is not a scalar-admission rule; existing SVG defaults remain documented only where an attribute is absent (D-03)."
    - "SVGPR-01 ordering: equal-item ordering is not a scalar-admission rule; this phase specifies no ordering behavior."
---

<objective>
Publish and prove the first end-to-end path of the SVG numeric admission contract.

Purpose: Give library users one portable policy derived from existing resource limits, with an executable valid-boundary path before Phase 92 changes parser behavior.
Output: A public numeric-admission policy and an all-target white-box tracer test.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-svg-numeric-contract/91-CONTEXT.md
@.planning/phases/91-svg-numeric-contract/91-RESEARCH.md
@.planning/phases/91-svg-numeric-contract/91-PATTERNS.md
@docs/rfcs/0002-mb-svg.md
@docs/rfcs/0008-mb-canvas-layer.md
</context>

## Artifacts this phase produces

- `docs/policies/svg-numeric-admission.md` — public numeric envelope, error-field contract, route matrix, ownership boundary, and explicit edge-coverage assumptions.
- `modules/mb-svg/svg/numeric_contract_wbtest.mbt` — `test "numeric contract tracer: inclusive boundary parses and lowers"` and the route-matrix control data it consumes.

## Source coverage audit

SOURCE | ID | Feature or constraint | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Target-neutral documented admission contract for every supported scalar ingress and derivation | 01, 02 | COVERED | Policy plus focused route controls.
REQ | SVGPR-01 | Documented contract with route-matrix tests | 01, 02 | COVERED | Requirement is present in both plan frontmatters.
RESEARCH | — | Inclusive 65536 envelope derived from resource limits; source and derived routes; four-target evidence | 01, 02 | COVERED | No new dependency or production parser migration.
RESEARCH | — | Preserve finite singular transforms and RFC 0008 layer ownership | 01, 02 | COVERED | Valid controls guard both boundaries.
CONTEXT | D-01 | Derive the envelope from established bounds | 01 | COVERED | Policy names the resource-limit basis.
CONTEXT | D-02 | Cover source and every derived path | 01, 02 | COVERED | Route identifiers and focused control files cover both sets.
CONTEXT | D-03 | Explicit unsafe versus absent/default distinction | 01, 02 | COVERED | Policy gives the error/default contract; scene controls retain absent behavior.
CONTEXT | D-04 | Stable error fields, not message prose | 01 | COVERED | Policy fixes field-level assertions.
CONTEXT | D-05 | Retain finite singular transforms | 01, 02 | COVERED | `scale(0)` is an all-target valid control.
CONTEXT | D-06 | Validate before SceneNode; leave lower/canvas total and opacity unchanged | 01, 02 | COVERED | Tracer and lower controls preserve ownership.

<tasks>

<task type="tracer">
  <name>Task 1: Publish and run the inclusive-boundary SVG contract tracer</name>
  <reversibility rating="costly">D-01 publishes a shared numeric envelope that later SVG parser routes and compatibility fixtures must honor.</reversibility>
  <read_first>
    docs/policies/targets.md — policy document layout;
    modules/mb-svg/svg/conformance_wbtest.mbt — table-driven parse-to-lower test form;
    modules/mb-svg/svg/scene.mbt — parse_svg SceneNode boundary;
    modules/mb-svg/svg/lower.mbt — lower_to_drawing_list total consumer;
    .planning/phases/91-svg-numeric-contract/91-CONTEXT.md — D-01, D-05, D-06.
  </read_first>
  <files>docs/policies/svg-numeric-admission.md, modules/mb-svg/svg/numeric_contract_wbtest.mbt</files>
  <action>Create `docs/policies/svg-numeric-admission.md` following the targets-policy structure. State `SVG_NUMERIC_LIMIT = 65536.0`, inclusive `[-65536.0, 65536.0]` admission after `Double.is_nan()`/`Double.is_inf()` checks, and its derivation from the existing SVG resource width/height limit per D-01. State that both source scalars and derived scalar or affine components are subject to the same envelope per D-02; finite determinant-zero affine values remain admitted per D-05; validation belongs before `SceneNode`, with `lower_to_drawing_list` and RFC 0008 canvas layers retaining their current ownership per D-06. Create `numeric_contract_wbtest.mbt` with `test "numeric contract tracer: inclusive boundary parses and lowers"`: parse an SVG whose finite geometry uses the documented boundary, lower its successful scene, and assert a non-empty drawing list. Add a `scale(0)` successful parse control in the same tracer data so the test exercises the declared non-invertibility rule. Keep the test package-local and use `Result` matching rather than panic or diagnostic text.
  </action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>The new policy identifies the 65536.0 resource-limit basis, the inclusive finite predicate, source-plus-derived coverage, finite singular-transform admission, and canvas/lower ownership. The tracer test completes parse_svg to lower_to_drawing_list for a finite boundary scene on all four targets.</acceptance_criteria>
  <done>The policy and tracer test are present, and the all-target package test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the public route matrix and stable error-field contract</name>
  <reversibility rating="costly">D-03 and D-04 define a published error and defaulting contract that downstream parser work and consumer fixtures must share.</reversibility>
  <read_first>
    docs/policies/svg-numeric-admission.md — tracer policy text from Task 1;
    .planning/phases/91-svg-numeric-contract/91-RESEARCH.md — Numeric Admission Contract and Route matrix to lock in Phase 91;
    modules/mb-svg/svg/{length,path_data,transform,scene,lower,color}.mbt — named source and derivation seams;
    modules/mb-core/error/core_error.mbt — category, code, operation, and context accessors;
    .planning/phases/91-svg-numeric-contract/91-CONTEXT.md — D-02, D-03, D-04.
  </read_first>
  <files>docs/policies/svg-numeric-admission.md</files>
  <action>Extend the policy with a route matrix that assigns a stable route identifier, existing seam, scalar kind, and required admission outcome to root width/height/viewBox, geometry, points, path numbers, transform lists, trigonometric transform results, relative geometry, viewBox mapping, affine construction/composition, rounded-rect clamp/ratio derivation, circle/ellipse sampling derivation, and paint numerics including dash-list ingress. Per the resolved research question, give individual paint rows to the current supported lowercase comma-separated functional routes: numeric and percent RGB components in `rgb()`/`rgba()`, plus numeric hue and percent saturation/lightness components in `hsl()`/`hsla()`; record fourth alpha components and CSS Color 4 space/slash forms, `hwb()`, `lab()`, `lch()`, and `color()` as unsupported/non-ingress rather than supported routes. Record D-03 exactly: a present malformed, non-finite, out-of-envelope, or unsafe-derived value has the structured SVG outcome, whereas only an absent attribute may select the existing default or inheritance branch. Define the D-04 field-level outcomes as `CoreError` category `Data`, code `InvalidEncoding` or `InvalidRange`, operation `svg`, and the route contexts `svg-numeric-source`, `svg-numeric-nonfinite`, `svg-numeric-range`, and `svg-numeric-derived`; state that rendered diagnostic prose is outside the compatibility contract. Add an explicit scope/ownership section that preserves RFC 0008 opacity and layer behavior, and an assumptions section recording the three SVGPR-01 adjacency, empty/null, and ordering probes as inapplicable to scalar admission without defining merge, empty-collection, or ordering semantics. State that Phase 91 controls are valid finite/boundary and lowering-preservation evidence only, while the planned rejection assertions are Phase 92 behavior changes.
  </action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Every source and derived route named in D-02 has one matrix row with an admission outcome, including rounded-rect ratio, circle/ellipse sampling, dash-list ingress, and the supported functional RGB/HSL component paths. The document marks unsupported colour forms as non-ingress, gives stable CoreError fields rather than message text, keeps Phase 91 controls passing, and assigns behavior-changing rejection assertions to Phase 92. The three unresolved spec-less probes are visible as non-applicable scalar-contract assumptions, and the all-target tracer remains green.</acceptance_criteria>
  <done>The public policy is complete enough for a consumer to determine the numeric outcome and stable error context for every supported scalar route without assigning rendering policy to mb-svg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SVG text and numeric attributes -> `mb-svg` contract | Untrusted scalar spellings and arithmetic expectations enter the documented parser boundary. |
| Validated `SceneNode` -> lower/canvas | The contract must not move SVG validation into total lowering or change RFC 0008 layer semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-91-01 | DoS | numeric ingress and derived arithmetic route inventory | high | mitigate | Document the finite 65536 envelope and complete route matrix; all-target controls make missing path categories visible before Phase 92 enforces the boundary. |
| T-91-02 | Tampering | numeric policy and white-box evidence | medium | mitigate | Couple the published predicate and route identifiers to a portable parse-to-lower tracer under the package test command. |
| T-91-03 | Repudiation | structured SVG diagnostics | medium | mitigate | Specify stable CoreError category, code, operation, and context fields rather than mutable rendered messages. |
| T-91-NA | Spoofing / Information disclosure / Elevation | identity, secrets, authorization | low | accept | Not applicable: this phase adds no identity, credentials, external transport, or privilege boundary. |
| T-91-SC | Tampering | package supply chain | low | accept | No package-manager install occurs in this phase. |

## ASVS L1 V5 Input-Validation Mapping

| ASVS control | Boundary and ownership | Phase 91 evidence |
|---|---|---|
| V5 Input Validation | Untrusted SVG scalar text and each derived scalar or affine component must be admitted before `SceneNode`; explicit supplied values are distinct from absent/default branches per D-03, while lower/canvas remain total consumers per D-06. | The policy fixes the source/derived route matrix and stable error-field contract, and the parse-to-lower finite-boundary tracer runs on js, wasm, wasm-gc, and native. Phase 92 activates the corresponding rejection behavior. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target all --frozen`. Confirm the policy route table has rows for every source and derived path in the research matrix and that its stated valid boundary is exercised by `parse_svg` followed by `lower_to_drawing_list`.
</verification>

<success_criteria>
Library users have a single public finite bounded numeric policy, a stable structured-error contract, and an all-target valid-boundary tracer that reaches lowering without changing canvas semantics.
</success_criteria>

<output>
Create `.planning/phases/91-svg-numeric-contract/91-01-SUMMARY.md` when done.
</output>
