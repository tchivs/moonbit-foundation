---
phase: 94-svg-benchmark-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-svg/svg/moon.pkg
  - modules/mb-svg/svg/svg_bench.mbt
autonomous: true
requirements: [SVGPR-04]
must_haves:
  truths:
    - "The three fixed workloads use the exact names path-parse/1000-line-to, transform-composition/50-segment, and parse-to-lower/50-rect, and each rejects an invalid semantic result before timing."
    - "moon bench modules/mb-svg/svg --target all --frozen builds and runs all three benchmark gates on wasm, wasm-gc, js, and native as runnable qualification."
  artifacts:
    - path: modules/mb-svg/svg/svg_bench.mbt
      provides: "Three deterministic correctness-gated SVG benchmarks"
      contains: "path-parse/1000-line-to"
    - path: modules/mb-svg/svg/moon.pkg
      provides: "Explicit MoonBit core benchmark import"
      contains: "moonbitlang/core/bench"
  key_links:
    - from: modules/mb-svg/svg/svg_bench.mbt
      to: modules/mb-svg/svg/path_data.mbt
      via: "pre-timing parse_path_data assertion and timed parse_path_data closure"
      pattern: "parse_path_data"
    - from: modules/mb-svg/svg/svg_bench.mbt
      to: modules/mb-svg/svg/transform.mbt
      via: "pre-timing parse_transform assertion and timed parse_transform closure"
      pattern: "parse_transform"
    - from: modules/mb-svg/svg/svg_bench.mbt
      to: modules/mb-svg/svg/lower.mbt
      via: "pre-timing parse_svg/lower_to_drawing_list operation assertion and timed parse-to-lower closure"
      pattern: "lower_to_drawing_list"
---

<objective>
Turn the existing SVG benchmark tests into the fixed, correctness-gated portable workload suite required by SVGPR-04.

Purpose: Make a timing result evidence of the intended SVG operation rather than an accidentally successful error path.
Output: Explicit benchmark-package import and three named MoonBit benchmark tests that pass semantic gates before their measured closures run.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-svg-benchmark-evidence/94-CONTEXT.md
@.planning/phases/94-svg-benchmark-evidence/94-RESEARCH.md
@.planning/phases/93-svg-compatibility-portable-qualification/93-VERIFICATION.md
@docs/rfcs/0002-mb-svg.md
@docs/policies/toolchain.md
@policy/foundation.json
@modules/mb-svg/svg/svg_bench.mbt
@modules/mb-svg/svg/moon.pkg
@modules/mb-svg/svg/path_data_wbtest.mbt
@modules/mb-svg/svg/transform_wbtest.mbt
@modules/mb-svg/svg/portable_qualification_wbtest.mbt
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the path-parse workload end to end before measuring it</name>
  <reversibility rating="costly">Per D-01, this workload name and correctness contract become a comparison identity recorded in the durable native baseline.</reversibility>
  <files>modules/mb-svg/svg/moon.pkg, modules/mb-svg/svg/svg_bench.mbt</files>
  <action>Per D-01: establish the fixed path-workload correctness contract. Per D-02: add the explicit `moonbitlang/core/bench` import aliased as `@bench` to the package manifest, then replace the local no-op sink with the package benchmark API. Add one small aborting benchmark-guard helper and a deterministic path corpus builder. Rename the path test exactly to `bench path-parse/1000-line-to`. Before calling `b.bench`, parse that same corpus and require `Ok`, exactly 1,001 expanded `PathCommand` values, command zero `MoveTo(0,0)`, and command 1,000 `LineTo(99,99)`; use the existing `@math.PathCommand` patterns. The measured closure must do only `b.keep(parse_path_data(corpus))`, so corpus construction and semantic validation stay outside measured work. Preserve the two other workload tests until Task 2 expands them.</action>
  <verify>
    <automated>moon bench modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <done>The named path-parse benchmark aborts before timing if its deterministic corpus does not parse to the required first/last command sequence, and the all-target command completes successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the fixed transform and parse-to-lower workload contracts</name>
  <reversibility rating="costly">Per D-01, the remaining workload names, corpus shapes, and correctness facts are part of the native baseline comparison contract.</reversibility>
  <files>modules/mb-svg/svg/svg_bench.mbt</files>
  <action>Per D-01: finish the suite with exactly `bench transform-composition/50-segment` and `bench parse-to-lower/50-rect`. Per D-02: retain deterministic test-local builders and no external benchmark package beyond the explicit core import. Define the transform corpus as exactly ten concatenated repeats of `translate(1,1) scale(2) rotate(15) skewX(5) skewY(3)`; it has 50 functions and must not include a matrix function. Before timing, parse it once, require all six public affine components (`a`, `b`, `c`, `d`, `tx`, `ty`) finite and within `SVG_NUMERIC_LIMIT`, and apply that affine to the fixed `@math.Point2(1.25, -2.5)` probe. Compare with absolute tolerance `1.0e-9` against the frozen literal affine `(-764.5825470346006, 981.6717123516748, -550.8736348781798, -664.151879387284, -1060.1606213143448, 997.9648720635827)` and frozen output `(-638.704717912146, 3885.434210971386)`; the expected vector must remain source literals rather than a second runtime parser call. Preserve the canonical transform correctness text `v1|ok|a=-764.5825470346006|b=981.6717123516748|c=-550.8736348781798|d=-664.151879387284|tx=-1060.1606213143448|ty=997.9648720635827|probe_x=1.25|probe_y=-2.5|out_x=-638.704717912146|out_y=3885.434210971386|tolerance=1e-9` and its frozen lower-case SHA-256 `ec32349185e19b24757e391c72ac5fa8709f889847a0035b7257fc3e0ba483ff` for Plan 94-02's evidence identity. For the 50-rect SVG, require `parse_svg` succeeds, lower the scene before timing, require exactly 50 operations, and require every indexed operation is `Fill`; the guard must reject any transform, layer, stroke, or other operation. Preserve the fixed corpus/correctness digest pairs: path `e97e1c8a8e29fdb3e84c309e421de34d41cbab7583cf1e88cf94a67af51eb259` / `0c7d3af32d324a136215c1158c4aab127d11e160f4b9239991114a0303762f22`, transform `c0ed3307e143d7cb20fd90e531e6208a14bbe2e42ce2816a0579d04cbd320840` / `ec32349185e19b24757e391c72ac5fa8709f889847a0035b7257fc3e0ba483ff`, and parse-to-lower `db053c95e904e016041f8b2f4a5e6471ed4bf1b144cfd0fc99c44d7d670cdddc` / `e76479b6744a5f062c21d7e5502971a45388346767e9d91aea0119c4340c18e5`; each digest is SHA-256 over the stated no-BOM, no-trailing-newline UTF-8 literal. The transform closure sinks only `parse_transform(corpus)`; the parse-to-lower closure parses and lowers the fixed document then sinks the resulting list. Per D-03: run the all-target benchmark strictly as runnable qualification and keep the emitted timing lines out of retained evidence.</action>
  <verify>
    <automated>moon bench modules/mb-svg/svg --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <done>All four targets run all three exact workload names after their command, affine, or drawing-list gate succeeds; ordinary SVG regressions also pass on all four targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deterministic benchmark corpus to public SVG API | Source-controlled strings cross into parsers and lowering before a timing runner consumes their result. |
| MoonBit runner output to later evidence | The runner emits target-specific timing text that must not be promoted to portable performance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-01 | Tampering | `svg_bench.mbt` corpus/result path | high | mitigate | D-01 pre-timing assertions validate the exact path commands, affine result, and drawing-list shape through public SVG APIs before `b.bench` executes. |
| T-94-02 | Repudiation | workload identity | medium | mitigate | D-01 fixes three stable names and deterministic builders; Plan 02 binds their source, corpus, and correctness digests to captured native evidence. |
| T-94-03 | Integrity | four-target runner output | medium | mitigate | D-03 treats `moon bench ... --target all --frozen` as a pass/fail runnable qualification; only Plan 02 retains native release captures. |
</threat_model>

<verification>
Run `moon bench modules/mb-svg/svg --target all --frozen` and confirm wasm, wasm-gc, js, and native each execute the three benchmark tests. Then run `moon test modules/mb-svg/svg --target all --frozen` to preserve the Phase 93 portable semantics baseline.
</verification>

<success_criteria>
- The only workload families are the three D-01 names, with fixed sizes in their names.
- Every workload has an independently executed semantic gate before its timing closure.
- The all-target benchmark command is usable as an executable qualification without creating a target-timing comparison artifact.
</success_criteria>

<source_audit>

SOURCE | ID | Required item | Plan | Status
--- | --- | --- | --- | ---
GOAL | — | Correctness-gated reproducible SVG measurements | 94-01, 94-02 | COVERED
REQ | SVGPR-04 | Three named gates and responsible native baseline comparison | 94-01, 94-02 | COVERED
RESEARCH | — | Explicit `@bench` import, pre-timing gates, all-target runnable qualification | 94-01 | COVERED
RESEARCH | — | Frozen 50-function transform corpus, affine/probe oracle, canonical text, and digest values | 94-01 | COVERED
RESEARCH | — | Windows PowerShell 5.1-safe capture generator and Markdown baseline without PPM threshold harness | 94-02 | COVERED
RESEARCH | — | Read-only Markdown audit of all input/output digests, summaries, and seven-sample aggregates | 94-02 | COVERED
RESEARCH | — | Clean-worktree preflight as native baseline comparison identity | 94-02 | COVERED
CONTEXT | D-01 | Fixed workload names and semantic gates | 94-01 | COVERED
CONTEXT | D-02 | Built-in MoonBit benchmark convention and local builders | 94-01 | COVERED
CONTEXT | D-03 | Four-target runnable qualification without cross-target claims | 94-01, 94-02 | COVERED
CONTEXT | D-04 | Exact native evidence and comparison identity | 94-02 | COVERED

</source_audit>

<output>
Create `.planning/phases/94-svg-benchmark-evidence/94-01-SUMMARY.md` when done.
</output>
