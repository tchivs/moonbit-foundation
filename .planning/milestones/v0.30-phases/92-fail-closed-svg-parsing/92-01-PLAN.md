---
phase: 92-fail-closed-svg-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-svg/svg/svg.mbt
  - modules/mb-svg/svg/length.mbt
  - modules/mb-svg/svg/scene.mbt
  - modules/mb-svg/svg/parse_wbtest.mbt
  - modules/mb-svg/svg/numeric_contract_wbtest.mbt
  - modules/mb-svg/svg/svg_test.mbt
autonomous: true
requirements: [SVGPR-02]
must_haves:
  truths:
    - "An explicit malformed, non-finite, or out-of-envelope root, viewBox, or basic geometry scalar returns the policy-defined CoreError before parse_svg returns a SceneNode."
    - "Absent root and geometry attributes retain their established SVG defaults; an explicitly present invalid value cannot choose that branch."
    - "The public parse API exposes stable Data/category, InvalidEncoding-or-InvalidRange code, svg operation, and numeric-route context on all four targets."
  artifacts:
    - "modules/mb-svg/svg/svg.mbt exposes private package-local numeric admission/error helpers."
    - "modules/mb-svg/svg/svg_test.mbt black-boxes the public parse-error contract."
  key_links:
    - "parse_svg -> build_svg_root -> Result-propagating root/leaf builders -> Err(CoreError), with no SceneNode available on the rejection branch."
    - "parse_length and parse_number_list classify lexical input through the shared SVG admission helper before scene construction."
---

<objective>
Establish the fail-closed numeric error seam and prove one public parse rejection reaches it end-to-end.

Purpose: Make the first public SVG scalar family reject unsafe explicit input at parser ingress instead of fabricating a usable scene.
Output: Shared numeric-error/admission helpers, Result-propagating root/basic-geometry ingress, focused internal tests, and public error-contract tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-fail-closed-svg-parsing/92-CONTEXT.md
@.planning/phases/92-fail-closed-svg-parsing/92-RESEARCH.md
@.planning/phases/92-fail-closed-svg-parsing/92-PATTERNS.md
@.planning/phases/91-svg-numeric-contract/91-01-SUMMARY.md
@docs/policies/svg-numeric-admission.md
@docs/rfcs/0002-mb-svg.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove fail-closed root and geometry admission end to end</name>
  <read_first>Read modules/mb-svg/svg/svg.mbt, length.mbt, scene.mbt (parse_svg_with_budget, build_svg_root, build_svg_root_attrs, build_element, attr_double), parse_wbtest.mbt, and docs/policies/svg-numeric-admission.md.</read_first>
  <reversibility rating="costly">The shared parser admission semantics become the package-wide public error contract relied on by all remaining scalar routes (D-01, D-03).</reversibility>
  <files>modules/mb-svg/svg/svg.mbt, modules/mb-svg/svg/length.mbt, modules/mb-svg/svg/scene.mbt, modules/mb-svg/svg/parse_wbtest.mbt</files>
  <behavior>
    - Red: explicit malformed root width, a non-finite lexical scalar, an out-of-range viewBox/geometry scalar, and invalid exact-viewBox arity return a typed SVG numeric error.
    - Green: each error has Data category, svg operation, InvalidEncoding for source/non-finite or InvalidRange for range, and the matching svg-numeric-source/nonfinite/range context.
    - Compatibility: omitted width, height, viewBox, and shape attributes keep their existing None/default behavior; inclusive +/-65536.0 remains accepted.
  </behavior>
  <action>Per D-01 and D-03, add one private numeric-error constructor and source/derived admission helpers beside svg_error. Preserve the existing CoreError representation but emit the policy's exact category, code, operation, and four context tokens. Refactor parse_length and parse_token to translate lexical conversion failure into the source context, then reject NaN/infinity and finite values outside inclusive [-65536.0, 65536.0]. Require parse_viewbox to receive exactly four admitted values. Refactor build_svg_root_attrs, attr_double, root construction, and each basic leaf dispatch/builder only as needed to propagate Result upward: attr_get == None alone preserves its documented None/default value, while Some(text) failure exits parse_svg_with_budget. Keep markup/budget behavior unchanged. Add route-local white-box assertions for ROOT, GEOMETRY, and POINTS/list ingress that inspect CoreError accessors rather than rendered prose.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Every listed explicit root/viewBox/basic-geometry failure reaches Err with policy-stable fields; the parser has no Ok(SceneNode) branch for those inputs, while all finite boundary and omitted-attribute controls pass on js, wasm, wasm-gc, and native.</acceptance_criteria>
  <done>Shared admission is used by the length/list/root/basic-geometry tracer path, its error identity is deterministic, and the original absent-value controls remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the public no-scene/no-drawing-list contract</name>
  <read_first>Read modules/mb-svg/svg/svg_test.mbt conventions from modules/mb-core/error/error_test.mbt, numeric_contract_wbtest.mbt, scene.mbt public parse entry points, lower_wbtest.mbt, and docs/policies/svg-numeric-admission.md §Explicit values and stable errors.</read_first>
  <files>modules/mb-svg/svg/svg_test.mbt, modules/mb-svg/svg/numeric_contract_wbtest.mbt</files>
  <behavior>
    - A public parse_svg fixture for each source/nonfinite/range context matches typed CoreError fields and cannot supply a scene to lower_to_drawing_list.
    - A valid inclusive-boundary document and finite scale(0) still parse and lower to a non-empty DrawingList.
    - The black-box suite does not depend on rendered diagnostic wording or private helpers.
  </behavior>
  <action>Create the public svg_test.mbt black-box suite per D-03 and D-04. Add a reusable CoreError accessor assertion and fixtures that exercise the tracer route's malformed, non-finite, and out-of-range explicit values; pattern-match Err as the evidence that no SceneNode exists and do not invoke lowering on those branches. Extend the existing numeric-contract tracer only with the successful control needed to keep the public parse-to-lower relationship explicit. Keep lower_to_drawing_list and all RFC 0008 layer behavior untouched per D-07.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>Public tests prove rejected explicit input exposes neither SceneNode nor DrawingList and valid finite boundary/scale(0) input still reaches a non-empty drawing list on every declared target.</acceptance_criteria>
  <done>The public parse contract is regression-tested independently of white-box route tests, with no lowerer API or canvas behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG source text -> mb-svg scalar parser | Untrusted lexical tokens become Doubles and scene attributes. |
| parser Result -> caller | A typed error or validated SceneNode crosses the public API boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-01 | Tampering | length.mbt and scene root/basic geometry | high | mitigate | Classify lexical, non-finite, and range failures with the shared SVG CoreError helper before a node is constructed (D-01, D-03). |
| T-92-02 | Denial of service | parse_svg_with_budget ingress | medium | mitigate | Preserve existing token/work/depth budget charging while adding constant-work numeric checks; do not allocate a drawing list on error. |
| T-92-03 | Repudiation | public parse diagnostics | low | mitigate | Black-box tests assert category, code, operation, and context rather than mutable rendered prose (D-04). |
| T-92-12 | Tampering | ASVS L1 V5 input validation | high | mitigate | Apply ASVS L1 V5 by admitting explicit source and derived numeric values before parser-produced SceneNode publication, returning typed CoreError with no partial scene or DrawingList on rejection, and verifying public/internal controls on js, wasm, wasm-gc, and native. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target all --frozen`. Confirm the public suite and focused ingress controls execute for js, wasm, wasm-gc, and native.
</verification>

<success_criteria>
The first end-to-end rejection path returns the policy-defined structured error before a SceneNode can exist, with absent/default and finite-boundary compatibility preserved.
</success_criteria>

<output>
Create `.planning/phases/92-fail-closed-svg-parsing/92-01-SUMMARY.md` when done.
</output>
