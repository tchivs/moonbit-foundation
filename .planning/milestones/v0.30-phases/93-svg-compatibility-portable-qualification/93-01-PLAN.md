---
phase: 93-svg-compatibility-portable-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/svg/cases.json
  - fixtures/manifest.json
  - modules/mb-svg/svg/moon.pkg
  - modules/mb-svg/svg/portable_qualification_wbtest.mbt
  - modules/mb-svg/svg/lower.mbt
  - modules/mb-canvas/canvas/rasterize.mbt
autonomous: true
requirements:
  - SVGPR-03
must_haves:
  truths:
    - "D-01: The same compact valid-SVG matrix produces deterministic draw-operation structure and semantic RGBA pixels on wasm, wasm-gc, js, and native."
    - "D-03, D-04: Group and element opacity use nested isolated canvas layers, while fill-opacity and stroke-opacity remain paint alpha on the same four targets."
    - "D-05: Sixteen SVG-originated nested opacity layers render successfully, while the seventeenth returns the established canvas resource error without modifying the primary raster."
    - "D-02: Malformed explicit SVG numeric input still returns a structured parser error before any scene, drawing list, or raster can exist on every supported target."
    - "A target regression exposed by the qualification is corrected only in its responsible existing lowering or raster seam, then the complete SVG and canvas all-target suites requalify without changing Phase 91 numeric admission, RFC 0008 opacity/layer allocation, or SVG surface behavior."
  artifacts:
    - path: fixtures/svg/cases.json
      provides: "Normative portable SVG qualification fixture schedule with valid opacity and capacity cases."
    - path: fixtures/manifest.json
      provides: "Updated provenance, SHA-256, and expected-use record for the SVG fixture authority."
    - path: modules/mb-svg/svg/portable_qualification_wbtest.mbt
      provides: "All-target parse-to-lower-to-raster operation, pixel, capacity, and retained rejection controls."
    - path: modules/mb-svg/svg/moon.pkg
      provides: "Test-only direct model and storage imports required to own and inspect tiny RGBA8 qualification targets."
    - path: modules/mb-svg/svg/lower.mbt
      provides: "The only SVG-production remediation seam when all-target qualification proves that lowering, rather than rasterization, regressed."
    - path: modules/mb-canvas/canvas/rasterize.mbt
      provides: "The only canvas-production remediation seam when the identical SVG-generated DrawingList proves a target-specific raster or layer-boundary regression."
  key_links:
    - from: modules/mb-svg/svg/portable_qualification_wbtest.mbt
      to: modules/mb-svg/svg/lower.mbt
      via: "parse_svg(source) succeeds before lower_to_drawing_list(scene) supplies the tested DrawingList."
      pattern: "parse_svg.*lower_to_drawing_list"
    - from: modules/mb-svg/svg/portable_qualification_wbtest.mbt
      to: modules/mb-canvas/canvas/rasterize.mbt
      via: "@canvas.render(list, owned_rgba8_target, budget) executes the SVG-generated operation stream."
      pattern: "@canvas\\.render"
    - from: modules/mb-svg/svg/portable_qualification_wbtest.mbt
      to: modules/mb-canvas/canvas/rasterize.mbt
      via: "The generated seventeenth SVG opacity layer is checked against canvas-render Resource/BudgetExceeded and its depth context."
      pattern: "canvas-layer-depth"
---

<objective>
Qualify the existing SVG parse → lower → canvas raster path as deterministic portable behavior, including RFC 0008 opacity composition and the shared layer-depth boundary.

Purpose: Deliver executable SVGPR-03 evidence without changing SVG numeric policy, opacity policy, canvas allocation policy, supported SVG surface, snapshots, or timing methodology.
Output: Provenance-backed finite fixture matrix and one focused all-target internal qualification suite.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-svg-compatibility-portable-qualification/93-CONTEXT.md
@.planning/phases/93-svg-compatibility-portable-qualification/93-RESEARCH.md
@.planning/phases/92-fail-closed-svg-parsing/92-03-SUMMARY.md
@.planning/phases/92-fail-closed-svg-parsing/92-VERIFICATION.md
@docs/rfcs/0002-mb-svg.md
@docs/rfcs/0008-mb-canvas-layer.md
@modules/mb-svg/svg/lower.mbt
@modules/mb-svg/svg/lower_wbtest.mbt
@modules/mb-svg/svg/svg_test.mbt
@modules/mb-canvas/canvas/rasterize.mbt
@modules/mb-canvas/canvas/render_wbtest.mbt

<interfaces>
From modules/mb-svg/svg/lower.mbt: `pub fn lower_to_drawing_list(root : SceneNode) -> @canvas.DrawingList`; group opacity emits PushTransform, PushLayer, child operations, PopLayer, PopTransform, and element opacity wraps its Fill and optional Stroke in PushLayer/PopLayer.

From modules/mb-canvas/canvas/rasterize.mbt: `pub fn render(list : DrawingList, target : @storage.OwnedImage, budget : @budget.Budget) -> Result[Unit, @error.CoreError]`; depth sixteen is enforced at PushLayer and failure uses Resource / BudgetExceeded with operation canvas-render and layer-depth context.

From modules/mb-canvas/canvas/render_wbtest.mbt: construct a tiny RGBA8 OwnedImage, seed a transparent or opaque backdrop through with_mut_view, and inspect semantic pixels through image.view().get_byte.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one valid SVG source reaches a portable semantic raster</name>
  <reversibility rating="costly">The fixture authority becomes the durable SVGPR-03 compatibility record across the four declared targets (D-01).</reversibility>
  <files>fixtures/svg/cases.json, fixtures/manifest.json, modules/mb-svg/svg/moon.pkg, modules/mb-svg/svg/portable_qualification_wbtest.mbt</files>
  <action>Create the focused internal `portable_qualification_wbtest.mbt` seam and add only the direct workspace imports it needs in `moon.pkg`: `tchivs/mb-image/model` as `@imgmodel`, `tchivs/mb-image/storage` as `@storage`, `tchivs/mb-image/metadata` for `@metadata`, and `tchivs/mb-color/profile` for `@profile`; direct OwnedImage construction requires all four imports used by the established `render_wbtest.mbt` helper pattern. Do not add an external dependency or production behavior. Define test-local helpers following `render_wbtest.mbt` for a bounded 8×8 RGBA8 OwnedImage, deterministic transparent/opaque backdrop initialization, selected RGBA byte reads, a render budget with enough allocation/depth headroom, and parse → lower → render execution. Add `portable_qualification_cases` to `fixtures/svg/cases.json` with named finite literals for opaque-rect, viewbox-blue-rect, fill-stroke-opacity, isolated-group-overlap, nested-group-element-opacity, layer-depth-16, and layer-depth-17; mirror only literal SVG and expected semantic facts in the wbtest because tests do not load fixture JSON. Recalculate the manifest SHA-256 and extend its expected-use/provenance text for this schedule. Per D-01: implement the tracer with opaque-rect on a transparent target, asserting its exact one-Fill/no-layer operation shape, center `(4,4)` RGBA `(255,0,0,255)`, and untouched corner `(0,0)` RGBA `(0,0,0,0)` after `parse_svg` → `lower_to_drawing_list` → `@canvas.render`.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <done>The new fixture authority and test-local raster seam are provenance-consistent, and opaque-rect succeeds through parser, lowerer, and rasterizer with exact operation and selected-pixel assertions on wasm, wasm-gc, js, and native.</done>
</task>

<task type="auto">
  <name>Task 2: Expand valid SVG opacity and retained no-partial qualification</name>
  <reversibility rating="costly">RFC 0008 isolated layer ordering and per-paint alpha distinguish public rendering semantics; changing either affects SVG consumers (D-03, D-04).</reversibility>
  <files>modules/mb-svg/svg/portable_qualification_wbtest.mbt</files>
  <action>Expand the same all-target suite with the remaining finite fixture cases from the matrix. For viewbox-blue-rect, assert the root transform plus fill ordering and an interior opaque-blue pixel. For fill-stroke-opacity, assert fill and stroke retain their respective alpha fields without element layer operations, then sample one full-coverage fill interior and one stroke-only interior point against an opaque backdrop. For isolated-group-overlap, assert one balanced group layer pair around both paint operations and sample the opaque-backdrop overlap pixel that differs from per-child group-alpha application. For nested-group-element-opacity, assert nested PushLayer values and reverse PopLayer order around mixed fill/stroke, then sample stable interior pixels on both opaque and transparent backdrops so source-over composition and retained alpha are independently demonstrated. Per D-03: use balanced isolated group/element layers and retain fill/stroke alpha as paint fields. Per D-04: use full-coverage interior/exterior points and semantic channel tuples, not binary image snapshots or edge-pixel tolerances. Per D-02: retain a structured public `parse_svg` rejection control for an explicit unsafe numeric/opacity source; require the established Data error fields, confirm the result is Err before any SceneNode is available, and keep the test branch free of lower/render calls. Do not modify parser admission rules, lowerer opacity policy, or canvas compositor behavior.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <done>Valid finite viewBox, fill/stroke alpha, isolated group overlap, and nested group/element opacity have exact diagnostic operation assertions plus selected semantic RGBA results on all four targets; the retained malformed-input control still yields the typed parser error before raster work.</done>
</task>

<task type="auto">
  <name>Task 3: Qualify SVG-originated layer capacity and remediate only a demonstrated portability regression</name>
  <reversibility rating="costly">The sixteen-layer raster capability is a shared RFC 0008 resource contract; altering its outcome would affect canvas and SVG callers (D-05).</reversibility>
  <files>modules/mb-svg/svg/portable_qualification_wbtest.mbt, modules/mb-svg/svg/lower.mbt, modules/mb-canvas/canvas/rasterize.mbt</files>
  <action>Add a test-local SVG generator that nests a full-coverage finite rect in exactly N `<g opacity="0.5">` groups, then parses and lowers the generated source rather than constructing canvas operations directly. For N=16, assert the SVG-generated list contains sixteen balanced layer pairs in nesting order, `@canvas.render` succeeds under the explicit qualification budget, and the expected center is nontransparent while a selected exterior/backdrop sentinel retains its semantic value. For N=17, start from a nontransparent sentinel primary image, assert the SVG-generated stream has seventeen layer pairs, require Resource / BudgetExceeded with operation `canvas-render` and context `canvas-layer-depth: layer nesting depth exceeded`, then assert the same primary pixel bytes are unchanged after the failed render. Per D-05: preserve the existing capacity result exactly and do not add layer clamping, flattening, recovery, or a separate SVG depth policy. After the suite first exposes a target regression, apply a post-qualification remediation only when its failure is reproducibly localized to one existing responsible seam: modify `lower.mbt` only when the same valid SVG lowers to incorrect operations, or modify `rasterize.mbt` only when the identical SVG-generated DrawingList lowers correctly but rasterization or depth enforcement differs by target. Change only that single responsible seam and add the regression assertion to the qualification suite. Preserve the Phase 91 numeric admission envelope and errors, RFC 0008 isolated opacity ordering and sixteen-layer allocation/error policy, and the existing SVG surface; do not change parser admission, opacity values, layer counts, clamping, flattening, recovery, allocation strategy, or introduce a target-specific branch. If evidence cannot localize the failure to either seam, stop and report the failing target and evidence rather than broadening the repair. When no regression is exposed, leave both production files unchanged. In either case, rerun both exact all-target commands after qualification or any permitted repair.</action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen; moon test modules/mb-canvas/canvas --target all --frozen</automated>
  </verify>
  <done>All four targets prove sixteen SVG-originated isolated layers raster successfully, while the seventeenth returns the exact established canvas capacity error and leaves the primary raster unchanged. If qualification exposed a target regression, exactly one responsible existing seam is repaired without changing the Phase 91 numeric admission contract, RFC 0008 opacity/layer allocation policy, or SVG surface, and both exact all-target commands pass afterward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG source → `parse_svg` | Untrusted document text is admitted or rejected before scene publication. |
| `DrawingList` → canvas renderer | SVG lowering sends opacity operations and resource demand to the shared rasterizer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-01 | Tampering | SVG opacity lowering and canvas composition | medium | mitigate | Tasks 1-2 assert operation order and discriminating opaque/transparent-backdrop pixels for per-paint versus isolated group/element opacity. |
| T-93-02 | Denial of service | Nested SVG opacity groups at `@canvas.render` | high | mitigate | Task 3 proves SVG-generated sixteen-layer success and seventeenth-layer `canvas-render` resource rejection with unchanged primary output. |
| T-93-03 | Tampering | Explicit unsafe SVG scalar admission | medium | mitigate | Task 2 retains typed parser-error/no-lower/no-raster controls in the four-target qualification command. |
| T-93-SC | Tampering | Package supply chain | low | accept | No package-manager install or external dependency is in scope; only existing workspace package imports are declared for test helpers. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target all --frozen` after Tasks 1-2. For Task 3 and after any permitted remediation, run `moon test modules/mb-svg/svg --target all --frozen; moon test modules/mb-canvas/canvas --target all --frozen` as the exact all-target re-verification. Confirm the output passes on wasm, wasm-gc, js, and native. Review the fixture manifest digest after its update with `Get-FileHash fixtures/svg/cases.json -Algorithm SHA256` and ensure it equals the manifest entry.
</verification>

<success_criteria>
1. A compact provenance-backed finite matrix exercises valid lowering and semantic raster pixels through the portable parser/lowerer/rasterizer path.
2. Operation order and stable RGBA checks distinguish group/element isolation from fill/stroke alpha on opaque and transparent backdrops.
3. SVG-generated depth sixteen succeeds; depth seventeen returns Resource/BudgetExceeded at canvas-render with the established layer-depth context and no primary-raster mutation.
4. The all-target test command retains structured numeric rejection before a scene, drawing list, or raster result can exist.
5. A demonstrated target regression is either absent or repaired only in the single responsible existing `lower.mbt` or `rasterize.mbt` seam, followed by the exact SVG and canvas all-target re-verification without changing Phase 91 numeric admission, RFC 0008 opacity/layer allocation, or SVG surface policy.
</success_criteria>

## Source Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Deterministic valid lowering/raster, isolated opacity, layer capacity on four targets | 93-01 | COVERED | Tasks 1-3 and final all-target gate. |
| REQ | SVGPR-03 | Deterministic lowering/raster, opacity semantics, sixteen-layer boundary on js/wasm/wasm-gc/native | 93-01 | COVERED | Frontmatter requirement plus every task. |
| RESEARCH | — | SVG-owned parse → lower → raster wbtest with semantic pixel rather than snapshot oracle | 93-01 | COVERED | Tasks 1-2. |
| RESEARCH | — | Normative fixture metadata mirror and manifest digest | 93-01 | COVERED | Task 1. |
| RESEARCH | — | SVG-originated 16/17 boundary with exact error and unchanged primary target | 93-01 | COVERED | Task 3. |
| RESEARCH | — | Regression-only repair in the responsible existing lowering or raster seam | 93-01 | COVERED | Task 3 permits one evidence-localized repair and otherwise leaves production files unchanged. |
| RESEARCH | — | Retain Phase 92 parser rejection controls in four-target suite | 93-01 | COVERED | Task 2. |
| CONTEXT | D-01 | Curated matrix, deterministic operations and pixels, no target snapshots/timing | 93-01 | COVERED | Task 1. |
| CONTEXT | D-02 | Retain parser rejection/no-partial controls without numeric-policy redesign | 93-01 | COVERED | Task 2. |
| CONTEXT | D-03 | Balanced isolated group/element layers; fill/stroke remain per-paint alpha | 93-01 | COVERED | Task 2. |
| CONTEXT | D-04 | Operation order plus selected semantic pixels on opaque/transparent backdrops | 93-01 | COVERED | Task 2. |
| CONTEXT | D-05 | Exact sixteen-success/seventeen-failure shared layer contract | 93-01 | COVERED | Task 3. |

<output>
Create `.planning/phases/93-svg-compatibility-portable-qualification/93-01-SUMMARY.md` when done.
</output>
