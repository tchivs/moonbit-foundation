---
phase: 45-bounded-gray8-encoder-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYPNG-02
must_haves:
  truths:
    - "Gray8 eager and caller-buffered factories select Stored, FixedOrStored, or DynamicOrFixedOrStored with None or Adaptive filtering through the existing bounded PNG encoder path."
    - "Gray8 capability, geometry, output, work, and budget failures occur before eager output or a usable caller-buffered encoder lease is observable."
    - "Gray8 caller-buffered pulls report only accepted bytes and retain acknowledgement-safe replay semantics under the focused native schedules for this phase."
  artifacts:
    - "modules/mb-image/png/png.mbt exposes explicit Gray8 compression-only, filter-only, and combined-strategy eager factories."
    - "modules/mb-image/png/stream_encode.mbt exposes matching caller-buffered Gray8 factories and routes them to PngEncodeMachine::new_with_profile."
    - "modules/mb-image/png/encode.mbt admits one-channel Gray8 into the shared bounded filter and preflight planners while still rejecting Gray8 Adam7."
    - "modules/mb-image/png/encode_test.mbt and modules/mb-image/png/stream_encode_test.mbt provide focused native strategy, atomic-admission, progress, and replay regressions."
  key_links:
    - "Gray8 public factory selection -> PngEncodeMachine::new_with_profile -> _png_encode_preflight_with_interlace_profile preserves one preflight, output/work/budget ledger, and strict compression winner policy."
    - "PngChunkEncoder Gray8 construction -> shared machine -> pull acknowledgement preserves cumulative progress as accepted output only."
---

<objective>
Complete the bounded Gray8 encoder path required by GRAYPNG-02: explicit eager and caller-buffered Gray8 strategy factories use the same preflight, filtering, compression planning, and acknowledgement-safe replay machinery already used by legacy RGB8/RGBA8 routes.

Purpose: deliver Gray8 compression and filter selection without changing frozen legacy public bytes or weakening atomic resource admission.
Output: Gray8 strategy factories, one-channel shared-path support, and focused native regressions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-gray8-factory-compatibility/44-VERIFICATION.md
@.planning/phases/45-bounded-gray8-encoder-path/45-CONTEXT.md

<interfaces>
From modules/mb-image/png/png.mbt:
- `PngEncoder::new_gray8() -> PngEncoder` is the Phase 44 Gray8 Stored/None/non-interlaced entry point.
- `PngEncoder::new_with_compression_strategy`, `new_with_filter_strategy`, and `new_with_strategies` establish the legacy naming/delegation pattern.
- `PngCompressionStrategy` has `Stored`, `FixedOrStored`, and `DynamicOrFixedOrStored`; `PngFilterStrategy` has `None` and `Adaptive`.

From modules/mb-image/png/stream_encode.mbt:
- `PngChunkEncoder::new_gray8(source, limits, budget, diagnostics)` constructs the Phase 44 caller-buffered path with `PngEncodeMachine::new_with_profile`.
- `PngChunkEncoder::new_with_compression_strategy`, `new_with_filter_strategy`, and `new_with_strategies` establish the caller-buffered factory pattern.
- `PngChunkEncoder::pull` reports per-call `written`, cumulative `total_written`, and `NeedOutput`, `Finished`, or sticky `Failed` outcome.

From modules/mb-image/png/encode.mbt:
- `_png_filter_candidate_byte` is the one-channel filter admission seam.
- `_png_encode_preflight_with_interlace_profile` is the sole profile-aware atomic admission seam.
- `PngEncodeMachine::new_with_profile` is the common eager/chunk construction seam.
</interfaces>
</context>

## Source Coverage Audit

| Source | ID | Required item | Plan task(s) | Status |
|---|---|---|---|---|
| GOAL | — | Gray8 uses the bounded pipeline before eager bytes or caller leases are exposed. | 1, 2 | COVERED |
| REQ | GRAYPNG-02 | Shared bounded preflight, Stored/FixedOrStored/DynamicOrFixedOrStored planning, admission, and output rules. | 1, 2 | COVERED |
| RESEARCH | — | Research intentionally skipped because the phase scope and code seams are explicit in CONTEXT.md. | 1, 2 | COVERED |
| CONTEXT | D-01 | Symmetric Gray8 compression-only, filter-only, and combined factories on eager and chunk surfaces. | 2 | COVERED |
| CONTEXT | D-02 | Gray8 uses all listed compression/filter strategies, strict winner rules, and rejects Adam7 before output. | 1, 2 | COVERED |
| CONTEXT | D-03 | Generalize one-channel shared filtering/preflight without a profile-specific planner, retained full-image encoder buffer, or replay state. | 1, 2 | COVERED |
| CONTEXT | D-04 | Preserve capability/geometry/output/work/budget atomic admission for eager and chunk construction. | 1, 2 | COVERED |
| CONTEXT | D-05 | Focus native tests on selections, eager/chunk equality, atomic failures, and accepted-progress replay. | 1, 2 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire Gray8 FixedOrStored/None through the existing bounded eager and chunk pipeline</name>
  <reversibility rating="costly">The additive public Gray8 strategy factory names become a shared API surface, so later renaming would require coordinated consumer changes.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - .planning/phases/45-bounded-gray8-encoder-path/45-CONTEXT.md (D-02, D-03, D-04, D-05 and Phase 46 deferrals)
    - .planning/phases/44-gray8-factory-compatibility/44-VERIFICATION.md (the accepted Stored baseline and legacy compatibility invariants)
    - modules/mb-image/png/png.mbt (existing `new_gray8` and legacy eager factory delegation)
    - modules/mb-image/png/encode.mbt (`_png_encode_preflight_with_interlace_profile`, fixed planning, and machine construction)
    - modules/mb-image/png/stream_encode.mbt (Gray8 caller construction and pull ownership)
    - modules/mb-image/png/encode_test.mbt and modules/mb-image/png/stream_encode_test.mbt (FixedOrStored eager/chunk fixtures, atomic-admission helpers, and replay assertions)
  </read_first>
  <behavior>
    - Test 1: A packed U8 Gray8 image encoded by explicit eager FixedOrStored/None emits a valid non-interlaced type-0 PNG and uses the existing fixed-or-stored winner selection.
    - Test 2: The matching Gray8 chunk factory drained through ordinary positive capacities produces exactly the eager byte sequence and reports each cumulative total as prior accepted bytes plus this pull's accepted bytes.
    - Test 3: A Gray8 FixedOrStored capability, output, work, or budget admission rejection leaves eager writer position and resource limits unchanged; chunk construction fails without mutating the sentinel owner.
  </behavior>
  <action>Per D-02 and D-03, first write the focused native eager and chunk regressions, then add matching explicit Gray8 compression-strategy factories following the existing `new_gray8` and `new_with_compression_strategy` naming/delegation conventions. Route both factories to `PngEncodeMachine::new_with_profile` with `PngEncodeProfile::Gray8`, `FixedOrStored`, `None`, and `PngInterlaceStrategy::None`. In `_png_encode_preflight_with_interlace_profile`, remove the Gray8 restriction that excludes the bounded compression planners while preserving its Adam7 capability rejection. Retain the existing scalar `PngDeflatePlan` and filtered traversal facts as the only planner/replay state; both eager and chunk construction must still pass the same profile, source, geometry, output, work, and budget checks before output access. Keep all existing legacy factory delegation and byte fixtures unchanged, and do not add Phase 46 generated fidelity, zero/one/ragged capacity, or four-target qualification coverage.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <acceptance_criteria>Explicit Gray8 FixedOrStored/None eager and caller-buffered paths share the existing machine, produce identical complete bytes under the focused positive-capacity drain, and preserve no-output/no-usable-lease atomic admission failures.</acceptance_criteria>
  <done>One production-quality Gray8 FixedOrStored/None path is exercised end-to-end from explicit eager and chunk factories through the shared preflight/planner/machine, with native regression evidence for exact bytes, cumulative accepted progress, and atomic admission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the shared Gray8 path to every compression and filter selection with atomic replay coverage</name>
  <reversibility rating="costly">The complete additive Gray8 factory family fixes an API naming and delegation contract used by both eager and caller-buffered consumers.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/png.mbt (Task 1 Gray8 factory naming and unchanged legacy factory surface)
    - modules/mb-image/png/encode.mbt (`_png_filter_candidate_byte`, adaptive traversal facts, strict Fixed/Dynamic winner selection, and Gray8 Adam7 gate)
    - modules/mb-image/png/stream_encode.mbt (`pull` acknowledgement and sticky-terminal behavior)
    - modules/mb-image/png/encode_test.mbt (adaptive and dynamic strict-winner regression patterns)
    - modules/mb-image/png/stream_encode_test.mbt (hostile positive-capacity drain, lease sentinel, admission parity, and replay-mutation patterns)
  </read_first>
  <behavior>
    - Test 1: Gray8 compression-only, filter-only, and combined eager/chunk factories cover Stored, FixedOrStored, DynamicOrFixedOrStored, None, and Adaptive; every chunk result equals its eager counterpart for the focused schedule.
    - Test 2: Gray8 Adaptive evaluates the existing method-0 candidate traversal with one channel; Fixed/Dynamic selection continues to retain the established strict complete-PNG winner rule.
    - Test 3: Gray8 capability, geometry, output, work, and budget failures are atomic for every selected strategy surface, including an unchanged caller-owned sentinel and unchanged resource limits.
    - Test 4: A Gray8 replay-drift or terminal replay failure accepts no new bytes, keeps `total_written` at the accepted prefix, repeats the same terminal error, and leaves a later lease untouched.
    - Test 5: Gray8 Adam7 remains rejected at the profile-aware preflight before writer bytes, budget charge, or a usable caller encoder is exposed.
  </behavior>
  <action>Per D-01, add explicit Gray8 filter-only and combined-strategy factories plus the remaining eager/chunk compression delegation, using the existing `new_with_filter_strategy` and `new_with_strategies` conventions while fixing `PngInterlaceStrategy::None` for every public Gray8 factory. Per D-02 and D-03, generalize `_png_filter_candidate_byte` and every closely coupled one-channel validation so the existing None/Adaptive filter traversal, Stored/Fixed/Dynamic planning, scalar work ledger, strict winner selection, and acknowledgement-safe machine replay operate on Gray8 without a profile-specific branch or retained full-raster encoder buffer. Keep the profile-aware Adam7 rejection and every RGB/RGBA public route unchanged. Add focused native tests in both suites for all factory selections, eager/chunk equality, strict winner behavior, atomic capability/geometry/output/work/budget rejections, accepted-only progress, sticky replay, and Gray8 Adam7 admission; deliberately leave generated public decode fidelity, zero/one/ragged schedules, and independent js/wasm/wasm-gc/native execution to Phase 46 per D-05.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <acceptance_criteria>All explicit Gray8 compression/filter factory selections are backed by the shared bounded planner and replay machine; their focused eager/chunk tests prove byte identity, strict selection, admission atomicity, accepted-only progress, and unchanged Gray8 Adam7 rejection.</acceptance_criteria>
  <done>GRAYPNG-02 is complete: the public eager and caller-buffered Gray8 strategy surface reaches one bounded preflight/filter/compression/replay implementation, all required focused native safety regressions pass, and legacy RGB8/RGBA8 fixtures remain in the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView and caller-selected strategy -> profile-aware preflight | Potentially unsupported geometry, metadata, resource envelope, or Gray8 Adam7 request reaches encoder admission. |
| Shared machine -> eager Writer / caller MutableByteView | Encoded bytes and cumulative progress cross into caller-owned output only after admission and explicit pull acknowledgement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Denial of service | `_png_encode_preflight_with_interlace_profile` and one-channel filter/planner traversal | high | mitigate | Preserve checked geometry/output/work arithmetic and the one atomic admission order before any writer access or active chunk encoder; native tests assert unchanged budgets and untouched output on each rejection class. |
| T-45-02 | Tampering | `PngEncodeMachine` filtered replay and `PngChunkEncoder::pull` | medium | mitigate | Reuse the existing fingerprint/replay-drift terminal handling and explicit present/acknowledge ownership; regressions assert accepted-prefix totals, sticky error identity, and untouched later caller leases. |
| T-45-03 | Spoofing | Gray8 public factory selection | low | accept | Factories are local typed constructors with a private profile and no identity or remote boundary; explicit names and profile-aware source validation make the selected format deterministic. |
| T-45-SC | Tampering | package-manager installs | low | accept | This plan adds no npm, pip, or cargo dependency and performs no package-manager installation. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target native --frozen`. The focused Gray8 tests must run with the established RGB/RGBA regression fixtures so frozen legacy bytes and behavior are guarded in the same package suite.
</verification>

<success_criteria>
- Explicit Gray8 eager and caller-buffered compression-only, filter-only, and combined factories cover Stored, FixedOrStored, DynamicOrFixedOrStored, None, and Adaptive without selecting Adam7.
- One-channel Gray8 uses the existing bounded preflight, filter, strict compression winner, scalar planning facts, and acknowledgement-safe replay path; no retained full-image encoder staging is introduced.
- Eager failures expose no writer bytes, chunk construction exposes no usable encoder lease, and caller pulls count only bytes accepted into their supplied lease.
- Native strategy, eager/chunk identity, atomic-failure, and replay tests pass while Phase 46 evidence remains outside this plan.
</success_criteria>

<output>
Create `.planning/phases/45-bounded-gray8-encoder-path/45-01-SUMMARY.md` when done.
</output>
