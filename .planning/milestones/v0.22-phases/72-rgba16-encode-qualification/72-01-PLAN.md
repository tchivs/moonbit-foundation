---
phase: 72-rgba16-encode-qualification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode_test.mbt
autonomous: true
requirements: [RGBA16ENC-04]
must_haves:
  truths:
    - "A non-symmetric two-pixel RGBA16 normal PNG proves the complete independently authored 17-byte Stored/None filtered raster and all packed little-endian source lanes after public decode."
    - "The existing non-symmetric 5x5 RGBA16 Adam7 path retains its complete independent 211-byte seven-pass raster parser oracle and every decoded source lane."
    - "Public eager and caller-buffered RGBA16 routes retain the established three-compression-by-two-filter matrix, hostile admission/lease/replay behavior, and literal legacy compatibility anchors."
    - "The ordinary frozen PNG package command passes once across wasm, wasm-gc, js, and native after the focused qualification checks."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Independent normal Stored/None 17-byte Type-6/16 raster assertion alongside the retained Adam7 211-byte public parser/decode oracle."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Existing public RGBA16 eager/chunk matrix, hostile admission, lease-tail, released-lease, and sticky replay evidence exercised without rebaselining."
  key_links:
    - from: PngEncoder::new_rgba16_with_strategies
      to: png_encode_gray16_public_stored_scanlines
      via: "Public Stored/None output is bounded-parsed and compared with a test-authored normal Type-6/16 raster, rather than checked only at PNG offsets."
      pattern: "PNG RGBA16 public eager wire and explicit decode fidelity"
    - from: PngEncoder::new_rgba16_with_all_strategies
      to: png_encode_rgba16_adam7_expected_passes
      via: "The public Adam7 stream remains checked against its coordinate-derived 211-byte seven-pass raster and explicit public decode lane loop."
      pattern: "PNG RGBA16 Adam7 eager wire and explicit decode fidelity"
    - from: PngChunkEncoder::pull
      to: "caller-owned lease tails and sticky terminals"
      via: "The retained public chunk tests exercise accepted-prefix accounting, Z tails, capability/resource admission, mutation replay, and released-lease terminals."
      pattern: "PNG RGBA16 chunk public evidence|PNG RGBA16 Adam7 chunk parity and hostile schedules"
---

<objective>
Qualify the completed RGBA16 encoder using independent public normal and Adam7 fidelity evidence, already-established hostile caller-buffered and legacy compatibility coverage, and one final ordinary portable package run.

Purpose: close RGBA16ENC-04 without altering encoder behavior, build configuration, or release tooling.
Output: one strengthened public normal-wire regression in the existing PNG test package and recorded focused/four-target qualification evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-rgba16-encode-qualification/72-CONTEXT.md
@.planning/phases/72-rgba16-encode-qualification/72-RESEARCH.md
@.planning/phases/72-rgba16-encode-qualification/72-PATTERNS.md
@.planning/phases/69-explicit-rgba16-png-encoding/69-VERIFICATION.md
@.planning/phases/70-resumable-rgba16-png-encoding/70-VERIFICATION.md
@.planning/phases/71-rgba16-adam7-png-encoding/71-VERIFICATION.md
@.planning/milestones/v0.17-phases/55-portable-public-evidence/55-01-PLAN.md
@.planning/milestones/v0.18-phases/58-portable-adam7-public-evidence/58-01-PLAN.md
@.planning/milestones/v0.18-phases/58-portable-adam7-public-evidence/58-02-PLAN.md
@.planning/milestones/v0.18-phases/58-portable-adam7-public-evidence/58-03-PLAN.md
@.planning/milestones/v0.19-phases/61-portable-grayalpha8-adam7-public-evidence/61-01-PLAN.md
@.planning/milestones/v0.19-phases/61-portable-grayalpha8-adam7-public-evidence/61-02-PLAN.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- `PngEncoder::new_rgba16_with_strategies(Stored, None)` is the normal public eager seam.
- `png_encode_gray16_public_stored_scanlines(bytes, expected_scanline_length)` is the intentionally bounded public PNG/Stored-DEFLATE parser; it must remain a known-structure parser, not a general inflater.
- `PngDecoder::decode_rgba16(...)` observes the packed little-endian storage result after public decoding.
- `png_encode_rgba16_adam7_expected_passes()` is the independent seven-pass coordinate oracle; its 211-byte result is already the Adam7 fidelity anchor.
- `PNG RGBA16 chunk public evidence`, `PNG RGBA16 Adam7 chunk parity and hostile schedules`, `PNG RGBA16 strategy admission is atomic`, `PNG RGBA16 Fixed and Dynamic replay mutations are sticky`, and `PNG RGBA16 chunk replays released lease failure` are the retained public hostile/lifecycle seams.
</interfaces>
</context>

<source_audit>
| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 72 | Normal/Adam7 fidelity, bounded hostile behavior, compatibility, and portability | Tasks 1-2 | COVERED |
| REQ | RGBA16ENC-04 | Independent normal/Adam7 vectors, hostile capability/resource/lease failures, frozen legacy behavior, four targets | Tasks 1-2 | COVERED |
| RESEARCH | Normal Stored/None gap | Replace ordinary fixed-offset lane-only proof with independent complete 17-byte filtered-raster comparison | Task 1 | COVERED |
| RESEARCH | Adam7/public drains/compatibility | Retain the 211-byte Adam7 oracle, existing six-pair public chunk matrices, hostile terminals, literal anchors, and ordinary all-target command | Tasks 1-2 | COVERED |
| CONTEXT | D-01 | Non-symmetric normal and Adam7 independent parser/decode fidelity | Task 1 | COVERED |
| CONTEXT | D-02 | Eager/chunk selectors and legal three-by-two strategy/filter coverage | Tasks 1-2 | COVERED |
| CONTEXT | D-03 | Atomic hostile admission/lifecycle with acknowledged-only totals, tails, and sticky terminals | Task 2 | COVERED |
| CONTEXT | D-04 | Frozen RGB8/RGBA8 and Gray/GrayAlpha normal/Adam7 compatibility evidence | Task 2 | COVERED |
| CONTEXT | D-05 | Existing source-package-only all-target frozen gate | Task 2 | COVERED |
| CONTEXT | D-06 | Tests-only qualification; narrow defect protocol if evidence finds a contract failure | Tasks 1-2 | COVERED |
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Strengthen one normal public RGBA16 wire-to-decode path with a complete raster oracle</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/encode_test.mbt`: read `png_encode_gray16_public_stored_scanlines`, `PNG RGBA16 public eager wire and explicit decode fidelity`, `png_encode_rgba16_adam7_expected_passes`, and `PNG RGBA16 Adam7 eager wire and explicit decode fidelity` as one local evidence family.
    - `72-PATTERNS.md`: preserve the narrow Stored parser and distinguish PNG big-endian wire lanes from packed little-endian source/decode lanes.
    - `71-VERIFICATION.md`: retain the all-seven-pass 211-byte Adam7 oracle and explicit 25-by-4-by-2 lane restoration coverage.
  </read_first>
  <behavior>
    - RED: the normal Stored/None test fails until the bounded parser result equals an independently authored 17-byte filtered row for the existing two non-symmetric RGBA16 pixels.
    - The normal expected bytes are filter `00`, then `12 34 A7 C5 BE 0F 5A 76`, then `DE 89 43 21 87 65 CD AB`, in PNG R/G/B/A high-byte/low-byte order.
    - The existing public `PngDecoder::decode_rgba16` check still proves every source storage lane in packed little-endian order, and the Adam7 test continues to require its independent 211-byte parser comparison and all lanes.
  </behavior>
  <action>Implement D-01 and D-06 in `PNG RGBA16 public eager wire and explicit decode fidelity`. Keep its existing legal two-pixel `png_encode_rgba16_image()` source, public `PngEncoder::new_rgba16_with_strategies(Stored, None)` call, IHDR assertions, and full `PngDecoder::decode_rgba16` component/lane loop. Add a test-local `Bytes` expectation for the complete 17-byte normal filtered raster (`00 12 34 A7 C5 BE 0F 5A 76 DE 89 43 21 87 65 CD AB`) and require `png_encode_gray16_public_stored_scanlines(bytes, 17)` to equal it. Remove the normal route's fixed absolute-IDAT lane-offset assertions once the parser comparison owns that wire proof; retain PNG signature/IHDR checks because they establish the Type-6/16 non-interlaced envelope. Do not alter `png_encode_gray16_public_stored_scanlines`, the 5x5 Adam7 fixture, `png_encode_rgba16_adam7_expected_passes`, the Adam7 211-byte assertion, production files, APIs, build configuration, fixtures, scripts, target wrappers, copied sources, staging, FFI, or encoder strategy logic. If this stronger public assertion exposes an encoder defect, halt after capturing the failing test and route the narrow production fix through the execution deviation protocol before making any implementation change.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG RGBA16 public eager wire and explicit decode fidelity' && moon -C modules/mb-image test png --target native --frozen -f 'PNG RGBA16 Adam7 eager wire and explicit decode fidelity' && moon -C modules/mb-image test png --target native --frozen -f 'PNG RGBA16 Adam7 eager all strategy framing'</automated>
  </verify>
  <done>The ordinary Type-6/16 Stored/None proof parses and matches all 17 independently authored filtered bytes, public decode still restores all sixteen packed source lanes, and the retained Adam7 public proof parses the independent 211-byte seven-pass raster and restores every 5x5 source lane.</done>
  <reversibility rating="reversible">This is test-only evidence over completed public factories; it makes no API, persisted-data, or production behavior decision.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Execute retained hostile and compatibility evidence, then run the frozen four-target qualification last</name>
  <files>None — read-only qualification execution after Task 1; record results in 72-01-SUMMARY.md</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: inspect the named normal/Adam7 chunk evidence, atomic admission, mutation replay, released-lease, and literal frozen compatibility tests before executing them.
    - `72-RESEARCH.md` and `72-PATTERNS.md`: retain the existing six-pair fresh-encoder schedules, Z-tail checks, and literal compatibility oracles unchanged.
    - `58-03-PLAN.md`: follow the serial ordinary-package gate pattern; the all-target command is not replaced by a wrapper or subset.
  </read_first>
  <action>Implement D-02, D-03, D-04, D-05, and D-06 without rebaselining established evidence. First run the focused native tests for `PNG RGBA16 eager factories retain atomic profile and resource admission`, `PNG RGBA16 chunk factory strategies match eager`, `PNG RGBA16 chunk public evidence`, `PNG RGBA16 Adam7 chunk parity and hostile schedules`, `PNG RGBA16 Adam7 chunk admission and terminal lifecycle`, `PNG RGBA16 Adam7 chunk mutation terminal is sticky`, `PNG RGBA16 strategy admission is atomic`, `PNG RGBA16 Fixed and Dynamic replay mutations are sticky`, and `PNG RGBA16 chunk replays released lease failure`; these retain the public eager/caller-buffered legal selector matrix, incompatible descriptor/capability/output/work/budget rejection, accepted-prefix totals, Z tails, source-revision replay, and released-lease sticky terminals. Then run both literal frozen compatibility-vector tests to retain the smallest existing RGB8/RGBA8 and Gray/GrayAlpha normal/Adam7 anchors. After every focused check succeeds, run exactly `moon -C modules/mb-image test png --target all --frozen` as the last test command of this task and record its wasm, wasm-gc, js, and native result in the summary. Finish with `git diff --check` and inspect the phase change surface: only `modules/mb-image/png/encode_test.mbt` may have a functional modification, while the established stream/legacy tests are exercised unchanged. If any public assertion finds a real contract defect, preserve the failure evidence and stop for the deviation protocol; do not weaken the oracle or broaden scope with a production rewrite.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG RGBA16' && moon -C modules/mb-image test png --target native --frozen -f 'PNG filter strategy eager frozen compatibility vectors' && moon -C modules/mb-image test png --target native --frozen -f 'PNG filter strategy chunk frozen compatibility vectors' && moon -C modules/mb-image test png --target all --frozen && git diff --check</automated>
  </verify>
  <done>The retained public selector, hostile admission, caller-tail, replay, released-lease, and literal compatibility tests pass unchanged; the ordinary frozen PNG package passes on wasm, wasm-gc, js, and native from its final unwrapped invocation; and the functional phase diff is limited to the normal eager test evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checked RGBA16 source to public PNG bytes | Distinct packed little-endian U16 lanes cross public eager factories into PNG Type-6/16 big-endian normal or Adam7 raster data. |
| Public PNG bytes to decoded RGBA16 storage | The test observes exported PNG output via a bounded public parser and `PngDecoder::decode_rgba16`. |
| Encoder state to caller-owned lease | Chunk pulls may write only their acknowledged prefix; caller-owned tails and later terminal leases must remain intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-72-01 | Tampering | Normal RGBA16 Type-6/16 wire order | high | mitigate | Task 1 implements D-01 with a non-symmetric complete 17-byte Stored/None parser oracle and public decode-lane loop. |
| T-72-02 | Tampering | Adam7 pass scatter and lane restoration | high | mitigate | Task 1 retains D-01's coordinate-derived complete 211-byte seven-pass oracle plus every decoded source lane. |
| T-72-03 | Tampering | Caller-buffered admission, tail ownership, and terminals | high | mitigate | Task 2 executes D-02/D-03's public six-pair matrices, accepted-only totals, Z tails, atomic failures, replay, and released-lease checks. |
| T-72-04 | Tampering | Legacy normal and Adam7 PNG output | medium | mitigate | Task 2 executes D-04's existing literal eager/chunk compatibility vectors without deriving new expectations from current encoder output. |
| T-72-05 | Denial of Service | Portable qualification evidence | medium | mitigate | Task 2 implements D-05 through one ordinary frozen `png` package command across all supported targets. |
| T-72-SC | Tampering | Package supply chain | low | accept | This plan installs no npm, pip, or cargo package and uses the pinned local MoonBit toolchain with existing package tests. |
</threat_model>

<verification>
1. Run the three Task 1 normal/Adam7 eager commands on native.
2. Run the Task 2 focused RGBA16 public-selector, hostile-lifecycle, and literal compatibility checks on native.
3. Run `moon -C modules/mb-image test png --target all --frozen` once, after all focused checks, as the final test command.
4. Run `git diff --check`; inspect that only `modules/mb-image/png/encode_test.mbt` has functional changes and no production/build/release/target artifact was introduced.
</verification>

<success_criteria>
- [ ] RGBA16ENC-04: a public non-symmetric normal Type-6/16 Stored/None PNG matches an independently authored 17-byte raster and restores every packed U16 source lane through the public decoder.
- [ ] The public Adam7 evidence still matches the independent 211-byte all-seven-pass raster and restores every 5x5 RGBA16 lane.
- [ ] Existing eager/chunk legal selectors, hostile capability/resource/lease/replay terminals, and literal legacy RGB8/RGBA8/Gray/GrayAlpha compatibility vectors pass without rebaselining.
- [ ] The unwrapped `moon -C modules/mb-image test png --target all --frozen` command passes as the final package test across wasm, wasm-gc, js, and native.
- [ ] No production encoder/model change, target wrapper, release script, copied source tree, fixture directory, staging path, FFI, generic admission widening, color conversion, or alternate encoder/pass planner is introduced.
</success_criteria>

<output>
Create `.planning/phases/72-rgba16-encode-qualification/72-01-SUMMARY.md` when done.
</output>
