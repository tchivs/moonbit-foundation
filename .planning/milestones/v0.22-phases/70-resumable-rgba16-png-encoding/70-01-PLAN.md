---
phase: 70-resumable-rgba16-png-encoding
plan: 01
type: tdd
wave: 1
depends_on: [69-01]
files_modified:
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [RGBA16ENC-02]
requirements_addressed: [RGBA16ENC-02]
must_haves:
  truths:
    - "D-01: Four explicit PngChunkEncoder::new_rgba16* factories select the existing non-interlaced Rgba16 profile."
    - "D-02 and D-03: A fresh eager RGBA16 encoder and the caller-buffered encoder produce identical Type-6/16 bytes through zero-capacity, one-byte, and ragged leases, with progress only for acknowledged bytes."
    - "D-04: Admission rejection, source-replay mutation, and destination failure expose no caller lease bytes and retain the original typed terminal on later pulls."
    - "D-05: The legacy generic chunk constructor stays RGB8/RGBA8-only and this phase adds no RGBA16 Adam7 selection or another encoding transport."
  artifacts:
    - "modules/mb-image/png/stream_encode.mbt exposes exactly the four non-interlaced RGBA16 caller-buffered factory shapes."
    - "modules/mb-image/png/stream_encode_test.mbt contains RGBA16 eager-parity, admission, mutation, and terminal lifecycle evidence."
  key_links:
    - "PngChunkEncoder::new_rgba16_with_strategies -> PngEncodeMachine::new_with_profile(Rgba16, ..., None)"
    - "PngChunkEncoder::pull -> existing acknowledgement, revision, destination-failure, and cached-terminal lifecycle"
  prohibitions:
    - statement: "No generic constructor widening, RGBA16 Adam7 selector, output staging, distinct encoder machine, FFI, copied source tree, release automation, or target wrapper is introduced."
      status: required
      verification: focused source review plus ordinary PNG package suites
---

# Phase 70 Plan 01: Resumable RGBA16 PNG Encoding

## Objective

Expose the completed explicit Type-6/16 RGBA16 profile through the existing caller-owned chunk façade, then prove byte identity and the established atomic, acknowledged-progress, and sticky-terminal lifecycle without altering the shared transport.

Purpose: Fulfil `RGBA16ENC-02` with the same bounded encoder machine used by the eager Phase 69 path.

Output: Four public non-interlaced RGBA16 chunk factories and focused public contract tests in the existing PNG package.

## Source Audit

| SOURCE | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 70 | Caller-owned chunks emit eager-identical Type-6/16 output with atomic admission and sticky terminals. | 01 | COVERED |
| REQ | RGBA16ENC-02 | Explicit bounded RGBA16 chunk route, hostile-capacity parity, accepted-only progress, and no partial output after rejected admission. | 01 | COVERED |
| RESEARCH | façade seam | Reuse `PngEncodeMachine::new_with_profile` and existing `pull`; use a separate eager encoder as the oracle. | 01 | COVERED |
| RESEARCH | hostile lifecycle | Cover lease-tail isolation, resource admission, replay mutation, destination failure, and terminal replay. | 01 | COVERED |
| CONTEXT | D-01 | Four RGBA16 factory shapes select `Rgba16` with non-interlaced output. | 01 / Task 1 | COVERED |
| CONTEXT | D-02 | Use the existing machine and lifecycle; eager remains the identity oracle. | 01 / Task 1 | COVERED |
| CONTEXT | D-03 | Cover zero, one-byte, and ragged leases with acknowledged progress and a single-lease boundary. | 01 / Task 1 | COVERED |
| CONTEXT | D-04 | Preserve atomic rejection and typed sticky terminals for invalid source/resources, mutation, and destination failure. | 01 / Task 2 | COVERED |
| CONTEXT | D-05 | Preserve generic admission and exclude Adam7, staging, alternate transport, FFI, copied trees, and delivery automation. | 01 / Tasks 1-2 | COVERED |

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-resumable-rgba16-png-encoding/70-CONTEXT.md
@.planning/phases/70-resumable-rgba16-png-encoding/70-RESEARCH.md
@.planning/phases/70-resumable-rgba16-png-encoding/70-PATTERNS.md
@.planning/phases/69-explicit-rgba16-png-encoding/69-01-SUMMARY.md
@.planning/phases/69-explicit-rgba16-png-encoding/69-VERIFICATION.md
</context>

<tasks>
<task type="tdd" tdd="true">
<name>Task 1: Red-green the four non-interlaced RGBA16 chunk selectors and hostile-capacity eager parity</name>
<files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
<read_first>
- modules/mb-image/png/stream_encode.mbt: `PngChunkEncoder::new_graya16*` (lines 201-261), `PngChunkEncoder::pull` (lines 440-529), and `PngEncodeMachine::new_with_profile` (lines 665-679)
- modules/mb-image/png/png.mbt: `PngEncoder::new_rgba16*` (lines 393-431)
- modules/mb-image/png/stream_encode_test.mbt: GrayAlpha16 U16 source/oracle/drain/factory patterns (lines 278-312, 848-1017, 1354-1383, and 1621-1658)
- .planning/phases/70-resumable-rgba16-png-encoding/70-CONTEXT.md
- .planning/phases/69-explicit-rgba16-png-encoding/69-01-SUMMARY.md
</read_first>
<behavior>
- A checked non-symmetric `rgba16` image has a fresh eager `PngEncoder::new_rgba16_with_strategies` byte oracle independent of the chunk encoder.
- Each of `new_rgba16`, `new_rgba16_with_compression_strategy`, `new_rgba16_with_filter_strategy`, and `new_rgba16_with_strategies` produces the same applicable non-interlaced Type-6/16 bytes as the eager route.
- For Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive filtering, zero-capacity, one-byte, and ragged leases preserve untouched tails, append only accepted bytes, and converge exactly to the eager oracle.
- A zero-capacity pull accepts no byte and reports unchanged total progress; a later successful terminal pull accepts no byte and leaves its sentinel lease unchanged.
</behavior>
<action>
Begin RED by adding an RGBA16 source helper with visibly distinct U16 lanes, a separate eager-byte helper, and caller-buffer drain evidence patterned after the GrayAlpha16 public schedule harness. **D-01:** Add exactly `PngChunkEncoder::new_rgba16`, `new_rgba16_with_compression_strategy`, `new_rgba16_with_filter_strategy`, and `new_rgba16_with_strategies` in `stream_encode.mbt`. The first three delegate to the fourth exactly as the GrayAlpha16 family does; the final factory invokes `PngEncodeMachine::new_with_profile` with `PngEncodeProfile::Rgba16` and literal `PngInterlaceStrategy::None`.

Implement only those façade declarations after the tests fail. **D-02:** Keep `PngChunkEncoder::pull` and machine construction/preflight code unchanged; every expected byte sequence must come from a separately constructed fresh eager RGBA16 encoder. **D-03:** Exercise schedules `[0UL, 1UL]`, `[1UL]`, and `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]` over the established compression/filter matrix. Assert each pull writes no more than its leased capacity, increments total progress by exactly its accepted byte count, does not modify the sentinel tail, and keeps one lease boundary per pull. Add direct coverage for the default, compression-only, and filter-only factory forms; use the all-strategies form for the matrix. **D-05:** Do not alter generic `PngChunkEncoder::new*`, introduce RGBA16 interlace-selection APIs, or modify the shared `pull` lifecycle.
</action>
<acceptance_criteria>
- `stream_encode.mbt` contains exactly the four requested `PngChunkEncoder::new_rgba16*` public shapes; their final construction route selects `Rgba16` and `None` interlace.
- Public RGBA16 chunk bytes equal fresh eager bytes for every required schedule and all six compression/filter combinations, while zero/one/ragged lease progress and sentinel tails meet the shared contract.
- Default, compression-only, and filter-only RGBA16 factories are independently exercised.
- `moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'` passes.
</acceptance_criteria>
<verify>
<automated>moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'</automated>
</verify>
<done>The caller can select any of the four explicit non-interlaced RGBA16 chunk factories and drain eager-identical PNG bytes through hostile caller-owned capacities without changing common transport behavior.</done>
</task>

<task type="tdd" tdd="true">
<name>Task 2: Red-green RGBA16 atomic-admission and sticky failed-terminal lifecycle evidence</name>
<files>modules/mb-image/png/stream_encode_test.mbt</files>
<read_first>
- modules/mb-image/png/stream_encode.mbt: `PngChunkEncoder::pull` (lines 440-529) and `PngEncodeMachine::validate_replay_revision` (lines 844-856)
- modules/mb-image/png/stream_encode_test.mbt: released-lease terminal helper (lines 2617-2646), GrayAlpha16 atomic-admission helper/cases (lines 2914-2961 and 3114-3135), and replay-mutation helper/cases (lines 3329-3388 and 3478-3490)
- modules/mb-image/png/stream_encode_wbtest.mbt: profile-agnostic accepted-accounting evidence (lines 519-539)
- .planning/phases/70-resumable-rgba16-png-encoding/70-CONTEXT.md
- .planning/phases/70-resumable-rgba16-png-encoding/70-RESEARCH.md
</read_first>
<behavior>
- An incompatible source/profile and width, output, work, or budget admission limits return the same typed error as a fresh eager RGBA16 attempt, reserve no chunk budget, and leave a sentinel caller lease unchanged.
- After acknowledged framing bytes, a replay-source mutation makes the next Fixed and Dynamic RGBA16 pulls accept zero bytes, preserve prior total progress, return a typed failure, and replay that same error without changing a later sentinel lease.
- A released destination lease on an RGBA16 encoder returns a zero-write typed failure with unchanged total progress; a later pull returns the same failure and does not modify its lease.
- The frozen generic chunk constructor continues to reject `rgba16` rather than selecting this explicit profile.
</behavior>
<action>
Begin RED by adapting the non-interlaced GrayAlpha16 public admission and replay helpers to an RGBA16 image and fresh eager RGBA16 factory. **D-04:** Across all established compression/filter pairs, compare incompatible-source/profile, width, output, work, and budget failures with the independent eager error; assert no eager bytes, no chunk-budget reservation, and no caller-sentinel change before a machine is admitted. Add RGBA16 Fixed and Dynamic replay-mutation cases that prove first and later failed pulls have zero accepted bytes, retain the pre-mutation total, replay the same typed error, and leave both later leases untouched. Add a released-destination-lease RGBA16 case with the equivalent sticky, zero-write lifecycle.

Keep this task test-only: shared preflight, revision validation, destination handling, and `pull` already own the contract and must not be forked. **D-05:** Add a focused frozen-generic regression for an RGBA16 source, retain non-interlaced-only selection, and avoid all changes outside `stream_encode_test.mbt`; no added RGBA16 Adam7 API, staged output, transport wrapper, FFI, copied tree, or delivery automation belongs to this phase.
</action>
<acceptance_criteria>
- RGBA16 rejected profile/capability/resource requests have eager-error parity, expose no eager or caller-buffered bytes, and preserve the caller budget.
- RGBA16 mutation and released-destination schedules prove zero accepted bytes after failure, unchanged totals, unchanged sentinel leases, and identical typed failures on later pulls.
- A generic `PngChunkEncoder::new` attempt remains incompatible with RGBA16.
- `moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'` passes.
</acceptance_criteria>
<verify>
<automated>moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'</automated>
</verify>
<done>Every RGBA16 rejected-admission, replay-mutation, and destination-failure path is publicly proven atomic and sticky, while the generic route remains frozen.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller image view → shared encoder machine | Caller-owned source descriptor, limits, budget, and mutation revision enter profile admission and replay validation. |
| Shared encoder machine → caller-owned mutable lease | Canonical output bytes are copied into caller memory only after destination acceptance and acknowledgement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-70-01 | Tampering | source revision before a later pull | medium | mitigate | Task 2 proves the existing revision guard fails before writing a lease and replays the cached typed terminal. |
| T-70-02 | Tampering | released or invalid destination lease | medium | mitigate | Task 2 proves the shared destination failure path records a zero-write sticky terminal and leaves future leases untouched. |
| T-70-03 | Denial of service | output/work/budget admission | medium | mitigate | Task 2 compares resource rejection with eager admission and asserts no reservation or byte exposure. |
| T-70-04 | Integrity | RGBA16 profile selection | low | mitigate | Task 1 fixes profile selection to the shared `Rgba16` non-interlaced machine and verifies fresh eager byte parity. |
</threat_model>

## Verification

Run the focused RGBA16 JS test family while implementing and as the phase verification gate:

```text
moon -C modules/mb-image test png --target js --frozen --filter '*RGBA16*'
```

Review the diff to confirm only `stream_encode.mbt` and `stream_encode_test.mbt` changed; the generic constructor, `pull`, white-box machine tests, eager API file, release paths, and source-tree layout remain untouched.

## Success Criteria

1. All four explicit non-interlaced RGBA16 chunk factory shapes are public and use the existing `Rgba16` bounded machine path.
2. Fresh eager and chunk output are byte-identical through zero-capacity, one-byte, and ragged schedules for the required compression/filter matrix, with acknowledged-only progress and lease isolation.
3. Invalid admission, source mutation, and destination failure expose no partial caller bytes and preserve a typed sticky terminal on later pulls.

<output>
Create `.planning/phases/70-resumable-rgba16-png-encoding/70-01-SUMMARY.md` when complete.
</output>
