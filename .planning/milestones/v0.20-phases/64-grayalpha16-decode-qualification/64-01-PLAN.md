---
phase: 64-grayalpha16-decode-qualification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/stream_decode_test.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
  - modules/mb-image/png/stream_decode.mbt
  - modules/mb-image/png/raster_decode.mbt
autonomous: true
requirements:
  - GRA16DEC-03
must_haves:
  truths:
    - "Independent Type-4/16 literals covering PNG filters 0 through 4 and all seven nonempty Adam7 passes preserve every Gray and Alpha U16 component byte as Glo,Ghi,Alo,Ahi through the explicit eager and chunk decoders."
    - "The explicit profile rejects incompatible type/depth and legacy-colour or ICC declarations before a result becomes visible, and exact versus one-less resource limits retain bounded no-result behavior."
    - "The unchanged generic eager and chunk facades decode the same independent Type-4/16 fixtures as frozen RGBA8(Ghi,Ghi,Ghi,Ahi), with their established progress, diagnostics, and terminal behavior."
    - "The ordinary PNG package test command passes on wasm, wasm-gc, js, and native without a target wrapper, copied build tree, target-specific fixture, or generated expected output."
  artifacts:
    - path: modules/mb-image/png/png_test.mbt
      provides: Fixed independent Type-4/16 filter and Adam7 public literals with complete component-byte, generic-compatibility, and resource-boundary assertions.
      contains: "decode_graya16"
    - path: modules/mb-image/png/stream_decode_test.mbt
      provides: Explicit and generic chunk schedule, hostile metadata, resource-terminal, and sticky replay evidence for the independent fixtures.
      contains: "PngChunkDecoder::new_graya16"
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: Profile-admission assertions that accept legal Type-4/16 Adam7 while retaining invalid type/depth and non-sRGB metadata rejection.
      contains: "graya16-profile"
    - path: modules/mb-image/png/stream_decode.mbt
      provides: GrayAlpha16 first-IDAT admission that permits legal Adam7 Type-4/16 input before the existing lifecycle allocation.
      contains: "PngDecodeProfile::GrayAlpha16"
    - path: modules/mb-image/png/raster_decode.mbt
      provides: Profile-aware Adam7 scatter that selects the existing GrayAlpha16 U16 component-byte store after shared byte-domain reconstruction.
      contains: "_png_write_adam7_transport_row"
  key_links:
    - from: modules/mb-image/png/stream_decode.mbt
      to: modules/mb-image/png/raster_decode.mbt
      via: First-IDAT preflight carries PngDecodeProfile::GrayAlpha16 into PngRasterSink, whose Adam7 completed-row writer selects the existing U16 lane store rather than the generic high-byte expansion.
      pattern: "new_with_profile|new_with_profile"
    - from: modules/mb-image/png/png_test.mbt
      to: modules/mb-image/png/png.mbt
      via: Independent public Type-4/16 literals enter decode_graya16 and the frozen generic decoder, so their output assertions observe the profile boundary rather than an encoder-derived oracle.
      pattern: "decode_graya16"
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: Fresh caller-owned ByteViews drive PngChunkDecoder::new_graya16 and PngChunkDecoder::new through empty, one-byte, ragged, and hostile schedules before finish transfers or replays a result.
      pattern: "PngChunkDecoder::new_graya16"
---

<objective>
Qualify and complete the explicit GrayAlpha16 Type-4/16 decoder across filters, Adam7, hostile boundaries, frozen generic compatibility, and all portable targets.

Purpose: make the existing opt-in preservation contract reliable for all legal Type-4/16 PNG traversal shapes while retaining its single bounded decoder machine and the historical generic result.
Output: independent public filter/Adam7 fixtures and adversarial evidence, a narrow Adam7 profile-admission/scatter correction exposed by those fixtures, and a direct four-target package qualification.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-grayalpha16-decode-qualification/64-CONTEXT.md
@.planning/phases/64-grayalpha16-decode-qualification/64-RESEARCH.md
@.planning/phases/62-explicit-grayalpha16-decode-contract/62-01-SUMMARY.md
@.planning/phases/63-resumable-grayalpha16-decode/63-01-SUMMARY.md
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/stream_decode_test.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/raster_decode.mbt

<interfaces>
From modules/mb-image/png/stream_decode.mbt:
- The first-IDAT profile gate currently checks Type-4/16, colour declaration, transparency, and an interlace byte before it creates the private lifecycle and PngRasterSink.
- PngDecodeMachine passes its selected PngDecodeProfile to PngRasterSink::new_with_profile; PngChunkDecoder retains the same machine for public push/finish accounting and terminal replay.

From modules/mb-image/png/raster_decode.mbt:
- The non-interlaced Type-4/16 two-component branch already calls _png_write_16bit_grayscale_alpha_row_graya16 for PngDecodeProfile::GrayAlpha16 after PngPackedRows reconstructs byte-domain filter data.
- The Adam7 completed-row call currently uses _png_write_adam7_transport_row without the profile; its Type-4/16 two-component branch is therefore the missing counterpart and must retain the shared pass-local reconstruction/scatter path.

From modules/mb-image/png/*_test.mbt:
- png_test_graya16_literal and png_test_graya16_srgb_literal are prior small independent Type-4/16 public fixtures; png_graya16_chunk_schedule_matches_eager and png_graya16_chunk_assert_sticky provide the public shape for explicit caller schedules and terminal replay.
- stream_decode_wbtest owns private first-IDAT admission assertions, including the current interlaced expectation that Phase 64 must correct only for legal Type-4/16 GrayAlpha16 input.
</interfaces>
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Exact Type-4/16 preservation through filters, Adam7, hostile boundaries, generic compatibility, and portable targets | 64-01 | COVERED | The tracer proves legal Adam7 end-to-end; expansion completes filter, hostile, compatibility, and target evidence. |
| REQ | GRA16DEC-03 | Independent vectors, filters/Adam7, adversarial rejection, frozen RGBA8, and four-target package pass | 64-01 | COVERED | Tasks 1 and 2 cover every named proof with direct public or private regression evidence. |
| RESEARCH | — | Remove only the explicit interlace disqualifier and choose the existing GrayAlpha16 lane store in Adam7 scatter | 64-01 | COVERED | Task 1 begins red with a seven-pass literal, then performs the minimal demonstrated correction. |
| RESEARCH | — | Use five-filter and seven-pass independent literals, coordinate-byte assertions, exact/one-less bounds, and public chunk schedules | 64-01 | COVERED | Task 2 expands the vector and hostile matrix without an encoder oracle or a duplicate decoder. |
| RESEARCH | — | Retain profile metadata gates, one-machine lifecycle, and direct ordinary full-package qualification | 64-01 | COVERED | Tasks 1 and 2 preserve existing gates and run the specified direct final command serially. |
| CONTEXT | D-01 | Independent non-symmetric Type-4/16 vectors prove all filters and Adam7 component lanes | 64-01 | COVERED | Both tasks require fixed literals and every-byte coordinate checks. |
| CONTEXT | D-02 | Eager/chunk malformed metadata, resource, split-input, and terminal failures stay atomic while generic decoding is frozen | 64-01 | COVERED | Task 2 drives the existing public eager/chunk helper patterns and retains private allocation proof. |
| CONTEXT | D-03 | Ordinary full PNG package runs on all four portable targets | 64-01 | COVERED | Task 2 uses exactly the direct all-target package command as its final automated gate. |

<feature>
  <name>GrayAlpha16 Adam7 admission, exact preservation, and portable qualification</name>
  <files>modules/mb-image/png/png_test.mbt, modules/mb-image/png/stream_decode_test.mbt, modules/mb-image/png/stream_decode_wbtest.mbt, modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/raster_decode.mbt</files>
  <behavior>
    - A hand-authored 5x5 Type-4/16 Adam7 PNG with all seven nonempty passes fails before the correction, then explicit eager and chunk decoding preserve each coordinate's non-symmetric Gray and Alpha lane bytes as Glo,Ghi,Alo,Ahi under one-byte and ragged schedules.
    - Generic eager and chunk decoding of that same literal remains RGBA8(Ghi,Ghi,Ghi,Ahi); neither facade gains a new result type, traversal machine, staging buffer, or conversion behavior.
    - A 2x5 fixed Type-4/16 literal covering filters None, Sub, Up, Average, and Paeth proves every output lane; exact resource requests succeed and one-less requests expose the established no-result terminal for eager and chunk callers.
    - Incompatible type/depth, legacy-colour, and ICC declarations remain rejected before lifecycle/result visibility; split hostile input and later caller views retain accepted-only progress and identical sticky terminal errors.
    - The unfiltered final command runs the entire PNG package through wasm, wasm-gc, js, and native.
  </behavior>
  <implementation>Use the existing PngDecodeProfile::GrayAlpha16, PngPackedRows reconstruction, U16 component-byte store, public schedule helpers, and white-box admission anchors. Do not add a decoder, selector, storage format, fixture generator, or target-specific execution route.</implementation>
</feature>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Red-green legal Type-4/16 Adam7 preservation through the existing profile</name>
  <reversibility rating="costly">Changing the explicit decoder's documented Adam7 admission affects a public fidelity contract, but it remains additive behind the existing explicit selector and does not alter generic wire behavior.</reversibility>
  <files>modules/mb-image/png/png_test.mbt, modules/mb-image/png/stream_decode_test.mbt, modules/mb-image/png/stream_decode_wbtest.mbt, modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/raster_decode.mbt</files>
  <behavior>
    - RED: a fixed CRC-valid 5x5 Type-4/16 Adam7 literal whose seven passes are all nonempty reaches the explicit profile and fails because the current admission rejects interlace.
    - GREEN: PngDecoder::decode_graya16 preserves every coordinate's unequal Gray and Alpha bytes from the literal as Glo,Ghi,Alo,Ahi, and PngChunkDecoder::new_graya16 yields the same result for one-byte and existing ragged schedules only after finish.
    - GREEN: PngDecoder::new and PngChunkDecoder::new retain RGBA8(Ghi,Ghi,Ghi,Ahi) for the same wire bytes and do not change their accepted-progress or terminal behavior.
    - GREEN: the white-box profile case accepts legal Type-4/16 Adam7 while retaining wrong type/depth, transparency, legacy-colour, and ICC rejection coverage.
  </behavior>
  <action>Start RED by adding, per D-01: a hand-authored fixed Type-4/16 Adam7 literal and an expected-byte-by-coordinate oracle in png_test.mbt; make high and low bytes differ for every component and arrange 5x5 dimensions so every Adam7 pass is nonempty. Feed the literal through decode_graya16, the existing explicit chunk schedule/eager-peer helper, and the unchanged generic eager/chunk facades; assert every explicit component byte rather than comparing only image metadata or high bytes. Update the established white-box profile-admission test to make the legal Adam7 expectation observable while retaining its incompatible Type-4/16 profile cases. After the failing focused test identifies the current gate, remove only the GrayAlpha16 interlace-byte condition in stream_decode.mbt; retain Type-4/16, transparency, declaration, limits, and pre-allocation ordering. Thread self.profile to the existing Adam7 transport-row writer in raster_decode.mbt, and for Type-4/16 two-component data select _png_write_16bit_grayscale_alpha_row_graya16 only when the profile is GrayAlpha16; all other branches keep their generic high-byte writers. This implements D-01: shared byte-domain filtering, pass-local rows, and coordinate scatter; it preserves the D-02: generic route without a profile-specific traversal or buffer. Run focused tests red, then green; production edits beyond those two demonstrated seams require a failing contract test that identifies a Phase 62/63 defect.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target js --frozen --filter '*graya16*'</automated>
  </verify>
  <done>A legal seven-pass Type-4/16 Adam7 fixture reaches the one existing explicit decoder profile and preserves every U16 lane through eager and chunk routes, while the generic eager/chunk facades and all non-Adam7 profile gates retain their established outputs and errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand filter, hostile-boundary, compatibility, and direct all-target evidence</name>
  <precondition>The installed MoonBit toolchain can run `moon -C modules/mb-image test png --target all --frozen`, and no process owned by another workflow holds the package build lock; wait for its owner instead of deleting the lock.</precondition>
  <files>modules/mb-image/png/png_test.mbt, modules/mb-image/png/stream_decode_test.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - RED: a fixed non-interlaced 2x5 Type-4/16 literal tagged with filters 0 through 4 lacks complete per-lane output, generic compatibility, and exact/one-less resource assertions.
    - GREEN: every literal lane is observed as Glo,Ghi,Alo,Ahi through explicit eager decoding and as RGBA8(Ghi,Ghi,Ghi,Ahi) through the generic eager/chunk facades; no expected output is derived from PngEncoder.
    - GREEN: exact output, allocation, and work budgets for a filter fixture and the 5x5 Adam7 fixture succeed, while one-less limits return the established resource terminal without a public result; chunk schedules retain the same atomic terminal and budget/diagnostic shape.
    - GREEN: split gAMA and CRC-valid iCCP metadata input, incompatible type/depth, early EOF, and later caller views retain the explicit profile's pre-allocation/replay contract while generic selection remains unchanged.
    - GREEN: the ordinary package command succeeds for wasm, wasm-gc, js, and native with the complete suite.
  </behavior>
  <action>Add the remaining D-01: qualification matrix with a second fixed, non-symmetric 2x5 non-interlaced Type-4/16 literal containing one row each for filters 0, 1, 2, 3, and 4. Reuse the smallest existing public component-byte and chunk schedule helpers, generalizing them only where a coordinate expectation or fresh budget needs to serve both filter and Adam7 literals. Assert all Gray and Alpha U16 bytes for explicit eager and chunk output, then assert the unchanged generic eager and chunk RGBA8 high-byte result against the same independent fixtures. Per D-02: use fresh public limits/budgets for exact and one-less output, allocation, and work cases across both a filter literal and the 5x5 Adam7 literal; assert eager and chunk failure produces no DecodeResult, preserves the established typed terminal, accepted-only input accounting, remaining-budget/diagnostic contract, and zero-consumption sticky replay. Split legal and hostile metadata across caller views and retain white-box proof that wrong type/depth plus legacy gAMA and iCCP facts remain rejected before lifecycle allocation. Keep the only accepted declarations as absent or sRGB; do not implement a colour transform, conversion API, generic result widening, additional storage, FFI, generated fixture path, wrapper, or copied-source workflow. After the focused JS suite passes, perform D-03: final gate serially and unmodified: `moon -C modules/mb-image test png --target all --frozen`. Record its normal test output in the plan summary; if a real resource failure occurs, recover normal executor resources and retry the identical direct command rather than replacing the gate.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Independent five-filter and seven-pass Type-4/16 evidence proves exact explicit U16 lanes, bounded hostile failure, and frozen generic RGBA8 behavior, and the unmodified PNG package succeeds on all four supported targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PNG IHDR and colour metadata to explicit profile admission | Attacker-controlled type, depth, interlace, transparency, and declaration bytes determine whether the high-precision result may allocate and become visible. |
| Compressed IDAT bytes to byte-domain filter and Adam7 scatter | Untrusted reconstructed source bytes cross from the bounded inflater/row state to coordinate-addressed image storage. |
| Caller-owned ByteView to chunk terminal lifecycle | Hostile split input and later caller views cross into accepted-byte accounting, result transfer, and sticky error replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | GrayAlpha16 profile admission in stream_decode.mbt | high | mitigate | Task 1 admits only legal Type-4/16 Adam7 and keeps wrong type/depth, transparency, legacy-colour, and ICC rejection before lifecycle allocation; Task 2 proves hostile metadata terminals. |
| T-64-02 | Tampering | Adam7 Type-4/16 scatter in raster_decode.mbt | high | mitigate | Task 1 routes the already selected profile to the existing U16 component-byte store after shared reconstruction and proves all pass coordinates with unequal byte lanes. |
| T-64-03 | Denial of service | PNG limits and chunk terminal state | high | mitigate | Task 2 covers exact/one-less resource limits, no-result failure, accepted-only progress, and zero-consumption sticky replay through existing bounded APIs. |
| T-64-04 | Tampering | generic PNG facade | medium | mitigate | Both tasks assert the generic eager and chunk facades retain their frozen RGBA8 high-byte output for the independent vectors. |
| T-64-05 | Information disclosure | caller ByteView/result transfer | low | accept | The phase adds no retained view or new ownership path; Task 2 retains the established finish-only transfer and terminal replay evidence. |
</threat_model>

<verification>
First run the focused portable development command: `moon -C modules/mb-image test png --target js --frozen --filter '*graya16*'`. Then run the required final qualification serially and without a wrapper, filter, copied build tree, or generated expectation: `moon -C modules/mb-image test png --target all --frozen`.
</verification>

<success_criteria>
GRA16DEC-03 is complete when two independent Type-4/16 fixtures prove all five filters and all seven Adam7 passes preserve every explicit GrayAlpha16 byte, hostile resource/metadata/chunk boundaries remain atomic, generic RGBA8 behavior remains frozen, and the ordinary all-target PNG package command passes.
</success_criteria>

<output>
Create `.planning/phases/64-grayalpha16-decode-qualification/64-01-SUMMARY.md` when done.
</output>
