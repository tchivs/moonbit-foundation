---
phase: 85-indexed-compression-api-and-fixed-wire-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
requirements:
  - INDEXCOMP-01
  - INDEXCOMP-02
must_haves:
  truths:
    - "Callers have exactly four additive non-interlaced indexed compression selectors: eager and chunk forms for Indexed8 and selected Indexed1/2/4, each accepting PngCompressionStrategy (D-01)."
    - "Legacy non-interlaced indexed APIs are literal Stored plus filter-None forwards, and indexed Adam7 remains the existing Stored/None route (D-02)."
    - "DynamicOrFixedOrStored returns the stable indexed capability error before indexed preflight, machine construction, writer progress, chunk lease exposure, or budget mutation (D-03)."
    - "FixedOrStored chooses Fixed when, and only when, its complete palette-aware Type-3 frame is no larger than Stored; equality selects Fixed (D-04)."
    - "The selected non-interlaced indexed route uses one immutable bounded filter-None raw-byte/match producer with the existing matcher, Fixed emitter, and acknowledged PngEncodeMachine (D-05)."
  artifacts:
    - "modules/mb-image/png/encode.mbt provides the eager selectors, strategy validation, indexed candidate facts, and shared indexed producer seam."
    - "modules/mb-image/png/stream_encode.mbt provides the chunk selectors and wires the selected indexed plan through the sole existing acknowledged machine."
    - "Public and white-box PNG tests prove facade compatibility, capability rejection, frame-aware selection, and replay-state invariants."
  key_links:
    - "PngEncoder indexed compression selectors and PngChunkEncoder indexed compression selectors converge at the same indexed machine/preflight seam."
    - "The indexed producer feeds Stored traversal, Fixed planning, and Fixed replay rather than duplicating matching or encoding logic."
    - "Both candidate lengths are framed by _png_frame_facts using palette length and canonical shortest tRNS length before the <= comparison."
  prohibitions:
    - "Do not add indexed Dynamic DEFLATE, adaptive filters, indexed Adam7 compression selection, a combined compression/filter/interlace selector, generic source-model changes, image/pass/output/token staging, a second encoder, matcher widening, FFI, copied source trees, or release automation."
---

<objective>
Deliver the explicit, deterministic Fixed-or-Stored Type-3 compression contract for the existing immutable indexed source model.

Purpose: Make non-interlaced Indexed8 and Indexed1/2/4 callers opt into bounded compression selection while retaining every established indexed Stored/filter-None byte stream.
Output: Four additive public selector methods, one shared bounded indexed raw-byte/match producer, palette-aware Fixed-or-Stored selection routed through the existing machine, and direct regression tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-CONTEXT.md
@.planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-RESEARCH.md
@.planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-PATTERNS.md
@.planning/research/v028-INDEXED-PNG-COMPRESSION.md
@.planning/milestones/v0.27-phases/84-low-bit-indexed-adam7-streaming-qualification/84-01-SUMMARY.md
</context>

## Artifacts this phase produces

- The exact four `*_with_compression_strategy` indexed public APIs, with legacy forwards frozen to `Stored` and filter `None`.
- A shared immutable indexed filter-None raw-byte/match producer consumed by Stored accounting, Fixed planning, and acknowledged Fixed replay.
- Type-3 frame-aware candidate selection based on `PngFrameFacts`, including PLTE and the canonical shortest tRNS.
- Direct public and white-box regression coverage for API shape, compatibility forwarding, Dynamic rejection, Fixed-or-Stored choice, and replay consistency.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove Indexed8 through PngMatchProducer/PngIndexedRawCursor end to end</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/encode.mbt: existing `PngEncoder::encode_indexed8`, `_png_encode_indexed_preflight_with_profile`, `PngFilteredMatchCursor`, `_png_fixed_plan_with_interlace`, and `_png_frame_facts`; use the selected `PngMatchProducer` plus `PngIndexedRawCursor` representation under `PngFilteredMatchCursor`.
    - modules/mb-image/png/stream_encode.mbt: `PngChunkEncoder::new_indexed8`, `PngEncodeMachine::new_with_indexed_profile`, Stored cursor delivery, and Fixed `present`/`acknowledge` handling; initialize both indexed cursors with `PngFilteredMatchCursor::new_indexed`.
    - modules/mb-image/png/encode_test.mbt: generic Fixed-or-Stored output assertions and existing Indexed8 helper/compatibility fixtures.
    - .planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-CONTEXT.md: D-01 through D-06.
  </read_first>
  <behavior>
    - An Indexed8 caller using `Stored` receives the same bytes as the legacy Indexed8 eager API.
    - An Indexed8 caller using `FixedOrStored` emits a Fixed block for a deterministic compressible palette/index fixture when the full Type-3 frame is not larger than Stored.
    - An Indexed8 caller using `DynamicOrFixedOrStored` receives `indexed-dynamic-compression-unavailable` with unchanged writer and budget observations.
    - A chunk constructor follows the same strategy decision through the existing acknowledged machine, with no additional encoder path.
  </behavior>
  <action>First add focused failing public Indexed8 tests, then implement the Indexed8 vertical slice. Add `PngEncoder::encode_indexed8_with_compression_strategy` and `PngChunkEncoder::new_indexed8_with_compression_strategy` using the existing `PngCompressionStrategy` vocabulary, per D-01. Convert the legacy non-interlaced Indexed8 eager/chunk methods into literal calls to these selectors with `PngCompressionStrategy::Stored` and the established filter-None route, per D-02. At the common indexed construction seam, reject `DynamicOrFixedOrStored` with `_png_encode_capability("indexed-dynamic-compression-unavailable")` before `_png_encode_indexed_preflight_with_profile`, budget charging, `PngEncodeMachine` construction, writer observation, or `PngChunkEncoderState::Active`, per D-03. Change only `PngFilteredMatchCursor`'s concrete producer field to the private tagged `PngMatchProducer::{Filtered(PngFilteredCursor), IndexedNone(PngIndexedRawCursor)}` and add `PngIndexedRawCursor::{new,next}` plus `PngFilteredMatchCursor::new_indexed`; its scalar source/profile/row-byte/index state emits the established filter-None Indexed8 bytes. Keep the 262-byte retained window and leave `_png_filtered_match_at` unchanged so its distance-1..4 matcher/emitter serves Stored traversal, Fixed planning, and existing `PngFixedState` replay, per D-05. Build Stored and Fixed candidate frame facts with `_png_frame_facts(source.palette_length(), canonical_trns_length, candidate_idat_length)` and select Fixed exactly on `fixed_frame.total_length <= stored_frame.total_length`, per D-04. In the strategy-taking indexed machine sibling, initialize `stored_cursor` and Fixed replay with fresh identical `PngFilteredMatchCursor::new_indexed` values so `zlib_byte`, `fixed_preview_byte`, and `acknowledge` retain their existing ownership branches. Keep the result in the sole existing machine and do not introduce a second encoder or a Dynamic path.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <acceptance_criteria>Indexed8 has the two additive compression-aware entry points; legacy and explicit Stored bytes agree; Dynamic returns the exact unavailable context before observable work; and a deterministic Fixed-or-Stored Indexed8 case uses the selected plan through the acknowledged machine.</acceptance_criteria>
  <done>The production-quality Indexed8 tracer exercises an explicit strategy from public eager and chunk facades to deterministic encoded output, with the Fixed decision based on actual Type-3 frame facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the PngMatchProducer/PngIndexedRawCursor contract to Indexed1, Indexed2, and Indexed4</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/encode.mbt: `PngEncoder::encode_indexed`, selected-depth profile resolution, existing low-bit non-interlaced and Adam7 forwards, and the Task 1 `PngMatchProducer`/`PngIndexedRawCursor`/`PngFilteredMatchCursor::new_indexed` seam.
    - modules/mb-image/png/stream_encode.mbt: `PngChunkEncoder::new_indexed`, selected-depth machine construction, and the Task 1 strategy-taking indexed machine sibling with its fresh indexed Stored/replay cursors.
    - modules/mb-image/png/encode_test.mbt and stream_encode_test.mbt: existing deterministic Indexed1/2/4 sources, packed-tail assertions, façade parity helpers, and the new Indexed8 test helpers.
    - .planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-PATTERNS.md: selected-depth API, parity, and literal-forward patterns.
  </read_first>
  <behavior>
    - `PngEncoder::encode_indexed_with_compression_strategy` and `PngChunkEncoder::new_indexed_with_compression_strategy` work for `PngIndexedBitDepth::{One, Two, Four}`.
    - At every selected low-bit depth, legacy non-interlaced output equals explicit Stored output, while Adam7 remains on the existing Stored/None selector route.
    - A compact repetitive low-bit source can select Fixed and a literal-heavy selected-depth source selects Stored, with Fixed winning on equal frame lengths.
    - Both selected-depth facades return the same early Dynamic capability result and do not expose a chunk encoder on failure.
  </behavior>
  <action>Add the remaining two additive selected-depth APIs, `PngEncoder::encode_indexed_with_compression_strategy` and `PngChunkEncoder::new_indexed_with_compression_strategy`, per D-01, and route Indexed1/2/4 through the Task 1 `PngMatchProducer::IndexedNone(PngIndexedRawCursor)` and `PngFilteredMatchCursor::new_indexed` seam with the selected plan. Convert only the legacy non-interlaced selected-depth eager/chunk methods into literal `PngCompressionStrategy::Stored` forwards while retaining their existing filter-None semantics; preserve all indexed Adam7 constructors/selectors as their current Stored/None compatibility route, per D-02. Ensure `PngIndexedRawCursor::next` produces packed low-bit filter-None rows, odd widths, and zero-tail behavior from the same immutable bounded cursor abstraction, not a prebuilt raster or a duplicate encoder, per D-05. Add `test "PNG indexed compression selectors preserve Stored and select the matrix plan"` in `encode_test.mbt` and `test "PNG indexed compression chunk selectors match eager matrix bytes"` in `stream_encode_test.mbt`: for every depth compare legacy eager bytes with explicit Stored; require `stored_fallback` FixedOrStored bytes to equal explicit Stored and `fixed_winner` selected eager bytes to be shorter than Stored; collect one sufficiently large chunk lease and compare it with fresh eager bytes for the same explicit strategy. Extend direct public eager and normal chunk-parity regressions for every selected depth with equality choosing Fixed and exact early Dynamic error identity, per D-03 and D-04. Keep complete frame comparisons palette-aware through `_png_frame_facts`; do not use the generic non-palette frame total.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <acceptance_criteria>All four additive APIs exist exactly once; non-interlaced Indexed1/2/4 legacy vectors match explicit Stored vectors; selected low-bit Fixed-or-Stored behavior is deterministic and frame-aware; and no selected-depth route creates Dynamic output or changes indexed Adam7 behavior.</acceptance_criteria>
  <done>Every non-interlaced Type-3 depth can opt into the same bounded Fixed-or-Stored path while existing public routes stay byte-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the all-depth fixed_winner/stored_fallback matrix, accounting, and acknowledgement replay</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <read_first>
    - modules/mb-image/png/encode_wbtest.mbt: generic Fixed plan, matcher-work, and preflight/frame-fact test seams.
    - modules/mb-image/png/stream_encode_wbtest.mbt: pending Fixed preview, acknowledgement-only state mutation, and replay-drift failure tests.
    - modules/mb-image/png/encode.mbt: the completed indexed preflight/plan facts and shared producer interfaces from Tasks 1-2.
    - modules/mb-image/png/stream_encode.mbt: the completed Indexed Fixed replay path from Tasks 1-2.
    - .planning/research/v028-INDEXED-PNG-COMPRESSION.md: Type-3 accounting and bounded-producer requirements.
  </read_first>
  <action>Add the test-local `png_indexed_compression_matrix_source(bit_depth, case)` helper and `test "PNG indexed compression matrix selects Fixed and Stored at every depth"` at the indexed preflight seam. For `One`, `Two`, `Four`, and `Eight`, create one row at widths `512`, `256`, `128`, and `64` so every case has 64 packed raster bytes, a full deterministic palette, and a canonical one-byte tRNS. The `fixed_winner` case is 64 `0x00` packed bytes and must select `PngDeflatePlan::Fixed`; the `stored_fallback` case is the byte sequence `0xc0` through `0xff` and must select `PngDeflatePlan::Stored` because its 65-byte raw stream has no distance-1..4 three-byte match, yielding Fixed IDAT length 81 versus Stored IDAT length 76. Assert `fixed_frame <= stored_frame` for the winner, `fixed_frame > stored_frame` for the fallback, and the one-byte tRNS frame fact, per D-04. Inspect the selected `PngMatchProducer`/`PngIndexedRawCursor` facts, bounded matcher work, and raw fingerprint so planning and Fixed replay consume the same `PngFilteredMatchCursor` contract, per D-05. Exercise `present` twice before acknowledgement and then acknowledge once to prove the Indexed Fixed replay state commits only after the accepted byte; retain the existing typed replay-drift behavior for a deliberately mismatched work/fingerprint seam. Keep tests focused on internal contract facts and direct regression behavior; Phase 86 owns the complete exact-limit/atomic-admission matrix, and Phase 87 owns hostile leases, independent parsing, and four-target qualification, per D-06.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <acceptance_criteria>The all-depth matrix proves 64 zero packed bytes select Fixed and `0xc0` through `0xff` select Stored with IDAT lengths 81 and 76 respectively; white-box evidence also proves palette-aware complete-frame selection, shared bounded producer facts, and acknowledgement-safe Indexed Fixed replay without a parallel matcher, producer, or output lifecycle.</acceptance_criteria>
  <done>The new fixed-wire contract has deterministic private evidence for the decision inputs and replay ownership that later admission and hostile-qualification phases can rely on.</done>
</task>

</tasks>

<verification>
- `moon -C modules/mb-image test png --target native --frozen` passes after each task and at plan completion.
- Public regressions cover the four API surface, literal Stored compatibility forwards, Dynamic capability identity, direct Fixed-or-Stored selection, and eager/chunk agreement under ordinary schedules.
- White-box regressions cover `PngFrameFacts` candidate totals, Fixed-on-tie selection, bounded raw/matcher facts, and acknowledgement-only Fixed replay transitions.
</verification>

<success_criteria>
1. INDEXCOMP-01 is satisfied by exactly four additive non-interlaced selectors and byte-identical Stored compatibility forwards, with early stable Dynamic rejection.
2. INDEXCOMP-02 is satisfied by one immutable bounded filter-None indexed producer reused across Stored traversal, Fixed planning, and acknowledged Fixed replay, using the existing matcher/emitter and machine.
3. The Fixed selection rule is evaluated from the complete Type-3 frame facts—IHDR, PLTE, canonical shortest tRNS, IDAT, and IEND—with Fixed chosen on equality.
4. The implementation remains within the Phase 85 scope fence and leaves full atomic admission and hostile four-target qualification to their assigned phases.
</success_criteria>

<output>
Create `.planning/phases/85-indexed-compression-api-and-fixed-wire-contract/85-01-SUMMARY.md` when done.
</output>
