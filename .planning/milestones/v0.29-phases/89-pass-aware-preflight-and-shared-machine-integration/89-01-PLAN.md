---
phase: 89
plan: "01"
type: execute
wave: 1
depends_on: [88-01]
files_modified:
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
requirements: [ADAM7COMP-02, ADAM7COMP-03]
must_haves:
  truths:
    - "D-01: All four indexed Adam7 wire profiles retain a selected Stored or Fixed plan whose frame facts equal the candidate actually emitted by the shared machine."
    - "D-02: Exact output/work limits and exact budget work admit once; one-less values fail atomically without consuming budget."
    - "D-03: Eager and chunked constructors remain wired to the one acknowledged indexed machine and no alternate encoder appears."
  artifacts:
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Adam7 candidate/frame/atomic-admission proof for Indexed1/2/4/8
      contains: Adam7 FixedOrStored
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: shared-machine constructor and acknowledge-path regression proof
      contains: indexed Adam7
  key_links:
    - from: encode_wbtest.mbt
      to: _png_encode_indexed_preflight_with_profile_and_strategy
      via: exact candidate and budget assertions
      pattern: _png_encode_indexed_preflight_with_profile_and_strategy
    - from: stream_encode.mbt
      to: PngEncodeMachine::new_with_indexed_profile_and_strategy
      via: public eager/chunk constructors
      pattern: new_with_indexed_profile_and_strategy
---

<objective>
Prove the Phase 89 pass-aware preflight and shared-machine contract for all four indexed Adam7 profiles with exact limit and budget accounting.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add all-profile Adam7 candidate and atomic-admission tests</name>
  <files>modules/mb-image/png/encode_wbtest.mbt</files>
  <action>Use an independent odd 5x5 indexed fixture for Indexed1/2/4/8. Assert the retained plan is Stored or Fixed as selected, the retained frame equals the candidate frame including PLTE/tRNS, and exact/one-less output/work limits plus budgets behave atomically.</action>
  <verify><automated>moon test modules/mb-image/png --target native --filter '*Adam7*preflight*'</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Verify shared eager/chunk machine wiring and regression surface</name>
  <files>modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <action>Exercise the additive Adam7 FixedOrStored chunk constructor through the acknowledged machine, compare its first accepted bytes to the eager route, and retain existing replay/legacy tests. Only modify production code if a focused test exposes a real mismatch.</action>
  <verify><automated>moon test modules/mb-image/png --target all</automated></verify>
</task>

</tasks>

<verification>
Run focused white-box tests, the full PNG package on native/wasm/wasm-gc/js, `git diff --check`, and a scope inspection for Dynamic/staging/second-encoder additions.
</verification>

<success_criteria>
ADAM7COMP-02 and ADAM7COMP-03 are evidenced by all-profile exact candidate facts, one-charge admission, atomic rejection, and shared-machine regression tests.
</success_criteria>
