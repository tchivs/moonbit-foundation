---
phase: 90
plan: "01"
type: execute
wave: 1
depends_on: [89-01]
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [ADAM7COMP-04, ADAM7COMP-05]
must_haves:
  truths:
    - "D-01: Fixed Adam7 chunk output survives zero/one/ragged leases and matches eager bytes without sentinel corruption."
    - "D-02: An independent test-local parser proves chunk order/CRC, Fixed DEFLATE, Adler, pass raster length/tails, and canonical PLTE/tRNS."
    - "D-03: Public decode proves RGB8 for opaque palettes and RGBA8 for transparent palettes across Indexed1/2/4/8."
    - "D-04: Existing Stored/replay compatibility and all-target package tests remain green."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: independent Fixed Adam7 wire/decode qualification
      contains: png_phase90_parse_fixed_adam7
  key_links:
    - from: independent parser
      to: chunk-origin bytes
      via: local CRC/DEFLATE/Adler checks
      pattern: png_phase90_parse_fixed_adam7
---

<objective>
Prove the final hostile streaming and independent qualification contract around the Phase 89 machine.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add independent Fixed Adam7 wire and public decode qualification</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Collect all four depth routes under zero/one/ragged leases, compare eager/chunk parity, parse PNG/CRC/Fixed-DEFLATE/Adler and explicit pass raw lengths, then decode transparent and opaque fixtures through public RGB8/RGBA8 APIs.</action>
  <verify><automated>moon test modules/mb-image/png --target native --filter '*independent wire and decode qualification*'</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Run the final four-target package gate and scope audit</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Run the full package on native, wasm, wasm-gc, and js, inspect the diff for prohibited architecture, and record the verification artifact.</action>
  <verify><automated>moon test modules/mb-image/png --target all</automated></verify>
</task>

</tasks>

<verification>
Run focused qualification, full all-target package tests, `git diff --check`, and search for prohibited Dynamic/staging/second-encoder changes.
</verification>

<success_criteria>
ADAM7COMP-04 and ADAM7COMP-05 are evidenced by hostile accepted-byte behavior, independent Fixed Adam7 parsing/checksums, RGB8/RGBA8 decode, compatibility vectors, and a 4-target package gate.
</success_criteria>
