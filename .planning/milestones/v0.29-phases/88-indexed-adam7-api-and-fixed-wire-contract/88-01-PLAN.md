---
phase: 88
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
requirements: [ADAM7COMP-01]
must_haves:
  truths:
    - "D-01: Indexed1, Indexed2, Indexed4, and Indexed8 expose paired additive eager and chunked Adam7 selectors named with the existing `...with_interlace_and_compression_strategy` vocabulary; existing interlace-only methods remain literal Stored forwards."
    - "D-02: The selectors reuse `PngCompressionStrategy::Stored` and `FixedOrStored`; no indexed-specific enum, Dynamic strategy, or widened source model is introduced."
    - "D-03: All four indexed wire depths (1, 2, 4, and 8) share the same public contract and machine seam."
    - "D-04: Every non-empty Adam7 pass is locally framed with filter byte 0, low-bit indices packed MSB-first, and zero unused tail bits using `_png_adam7_passes(width, height, 1, depth)`; the emitted IHDR has interlace method 1 and complete PLTE/tRNS facts."
    - "D-05: FixedOrStored selection is bounded to complete Type-3 frame facts, including PLTE/tRNS and IDAT framing, chooses Fixed on a win or tie, and never introduces a second indexed encoder or unbounded staging buffer."
    - "D-06: Legacy/default output is byte-frozen, new routes are opt-in, and unsupported combinations fail before any output, lease acknowledgement, or budget charge."
  artifacts:
    - path: "modules/mb-image/png/encode.mbt"
      provides: "Additive eager indexed Adam7 compression selector overloads and shared-machine forwarding"
      contains: "encode_indexed_with_interlace_and_compression_strategy"
    - path: "modules/mb-image/png/stream_encode.mbt"
      provides: "Additive chunked indexed Adam7 compression selector overloads"
      contains: "new_indexed_with_interlace_and_compression_strategy"
    - path: "modules/mb-image/png/stream_encode_test.mbt"
      provides: "Public API and wire contract regression coverage for all indexed depths"
      contains: "FixedOrStored"
  key_links:
    - from: "modules/mb-image/png/encode.mbt"
      to: "PngEncodeMachine::new_with_indexed_profile_and_strategy"
      via: "new overloads forward one explicit interlace and compression selection"
      pattern: "new_with_indexed_profile_and_strategy"
    - from: "modules/mb-image/png/stream_encode.mbt"
      to: "_png_indexed_adam7_scanline_byte"
      via: "Adam7 indexed wire cursor uses shared pass geometry and packed tails"
      pattern: "_png_indexed_adam7_scanline_byte"
---

<objective>
Expose the next indexed PNG capability as a small additive API: callers can explicitly request Adam7 plus Stored or FixedOrStored for all four indexed wire depths, while the existing Stored routes remain byte-for-byte compatible.

Purpose: complete the public contract needed by the next machine-integration phase without duplicating encoder ownership or widening scope into Dynamic/adaptive compression.
Output: tested eager/chunked selectors, deterministic pass-local packed wire assertions, and compatibility coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/88-indexed-adam7-api-and-fixed-wire-contract/88-CONTEXT.md
@.planning/phases/88-indexed-adam7-api-and-fixed-wire-contract/88-RESEARCH.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock additive API and pass-local wire behavior with failing tests</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - Public eager and chunked calls compile for Indexed1/2/4/8 with Adam7 + Stored and Adam7 + FixedOrStored.
    - Existing interlace-only calls produce the same bytes as explicit Adam7 + Stored.
    - Independent assertions validate IHDR interlace=1, complete palette/alpha chunks, seven-pass ordering, filter byte 0, MSB-first packed indices, and zero tail bits for widths that do not fill a byte.
    - Dynamic remains rejected before writer activity; tiny, zero/one-row, and ragged lease cases preserve sentinel/progress behavior until Phase 90 extends hostile coverage.
  </behavior>
  <action>Add compile-level public API tests for both eager and caller-buffered encoders across all four indexed depths. Add deterministic fixtures that decode or independently parse the emitted IDAT stream, compare explicit Stored output with legacy Adam7 wrappers, and assert the packed pass boundaries and tail bits. Keep tests black-box in `*_test.mbt` and representation-focused checks in `*_wbtest.mbt`; do not require a second encoder implementation or opaque binary snapshots alone.</action>
  <verify>
    <automated>moon test modules/mb-image/png --target native</automated>
  </verify>
  <done>The new selector names and all wire invariants are expressed as red tests, while legacy vectors and public RGB8/RGBA8 behavior remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement additive selectors through the shared indexed machine</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <action>Add paired eager/chunked methods named `encode_indexed8_with_interlace_and_compression_strategy`, `encode_indexed_with_interlace_and_compression_strategy`, `new_indexed8_with_interlace_and_compression_strategy`, and `new_indexed_with_interlace_and_compression_strategy` (or the repository's exact overload convention). Preserve current interlace-only methods as explicit Stored forwards. Route every new method through `PngEncodeMachine::new_with_indexed_profile_and_strategy`; do not create a second indexed encoder. Keep Dynamic unavailable and keep Phase 89's pass-aware preflight/matcher integration as the next boundary. Reuse `_png_adam7_passes` and `_png_indexed_adam7_scanline_byte` for local row/bit packing, ensure IHDR/PLTE/tRNS facts are complete before writer/lease/budget admission, and make FixedOrStored fall back to Stored when the bounded fixed plan cannot win.</action>
  <verify>
    <automated>moon test modules/mb-image/png --target native; moon check modules/mb-image/png --target all</automated>
  </verify>
  <done>All four depths expose the additive selectors, explicit Stored remains byte-identical to legacy routes, unsupported strategies fail before side effects, and the implementation shares the existing machine and bounded pass geometry.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused regression and scope guard checks</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <action>Expand the tests from Task 1 only where implementation exposes a mismatch: verify every accepted call makes progress with one-byte leases and sentinel tails, release/replay is sticky after finish, and legacy non-interlaced compression profiles remain untouched. Add explicit assertions that Dynamic, adaptive indexed filtering, wider dictionaries, and any out-of-phase capability are not silently enabled.</action>
  <verify>
    <automated>moon test modules/mb-image/png --target native; moon test modules/mb-image/png --target wasm; moon test modules/mb-image/png --target wasm-gc; moon test modules/mb-image/png --target js</automated>
  </verify>
  <done>Phase 88 has focused all-depth API/wire coverage and a clean scope boundary; hostile lifecycle and full independent qualification remain tracked for Phases 89–90.</done>
</task>

</tasks>

<verification>
Run the focused package tests on native and all portable targets. Confirm the explicit Adam7 + Stored bytes equal the corresponding legacy wrapper bytes for every indexed depth, decode the FixedOrStored output through the independent decoder/reference parser, and inspect the diff to ensure no Dynamic/adaptive/wider matcher or second encoder was introduced.
</verification>

<success_criteria>
- ADAM7COMP-01 is satisfied for Indexed1/2/4/8 in eager and chunked APIs.
- Adam7 pass-local Type-3 framing, packed tails, palette facts, and interlace flag are deterministic and tested.
- Existing wrappers and non-interlaced/v0.28 behavior remain unchanged.
- All focused tests pass on native, wasm, wasm-gc, and js.
</success_criteria>

</output>
