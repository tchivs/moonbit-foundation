---
phase: 39-bounded-filter-planning-and-replay
plan: "02"
type: tdd
wave: 2
depends_on: [39-01]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
requirements: [PNGF-02, PNGF-03]
must_haves:
  truths:
    - "Adaptive scanlines are selected once per row by the fixed candidate scorer and are the logical input to Stored, FixedOrStored, and Dynamic compression planning."
    - "Planning and acknowledgement-safe replay consume the same adaptive byte stream without retaining image-sized filter or output state."
    - "Adaptive output and work admission are complete before a shared encode machine can expose an eager byte or caller lease."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: Filter-aware preflight and exact adaptive planning/replay work ledger.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Fixed-state adaptive filtered-byte cursor wired to Stored, Fixed, and Dynamic emission replay.
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: Internal adaptive plan and exact work-ledger tests.
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: Internal cursor/replay identity and failure-state tests.
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: The same filter strategy and filtered-byte cursor contract supplies preflight plans and replay bytes.
      pattern: PngFilterStrategy|_png_encode_preflight|PngEncodeMachine
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: Fixed and Dynamic replay verify the preflight matcher work and Dynamic fingerprint over the selected adaptive stream.
      pattern: matcher_work|fingerprint|replay
---

<objective>
Connect the tested adaptive filter arithmetic to one bounded byte-source contract used by compressor planning and acknowledgement-safe replay. PNGF-02, PNGF-03.

Purpose: Ensure every compression strategy plans, admits, and emits the same filtered bytes without image-sized staging or post-output resource work.
Output: A fixed-state adaptive cursor, filter-aware Stored/Fixed/Dynamic planning, and exact preflight work accounting.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-adaptive-filter-compatibility/38-VERIFICATION.md
@.planning/phases/39-bounded-filter-planning-and-replay/RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-01-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt

<interfaces>
- `_png_encode_preflight(source, strategy, limits, budget)` is the sole atomic admission point today and selects Stored, FixedOrStored, or strict Dynamic complete-PNG winners.
- `PngEncodeMachine::new_with_strategies(source, strategy, filter_strategy, limits, budget, diagnostics)` currently normalizes Adaptive to None before calling preflight; this plan removes only that compatibility normalization.
- Fixed/Dynamic planning uses `_png_fixed_match_at` plus exact `matcher_work`; Dynamic also stores an Adler-style `fingerprint`, and replay turns either drift into a sticky state failure.
- The existing present/acknowledge lifecycle must keep preview state private until the Writer or caller lease accepts the byte.
</interfaces>
</context>

<feature>
  <name>Bounded adaptive preflight and replay byte source</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - PNGF-02: Adaptive uses the exact five-candidate row winner from Plan 39-01 and emits the chosen filter type byte followed by its transformed row bytes.
    - PNGF-03: Stored, FixedOrStored, and DynamicOrFixedOrStored obtain their candidates, exact output sizes, checksums, and DEFLATE facts from the same selected adaptive stream.
    - PNGF-03: The adaptive scorer's planning and replay traversals are charged through one checked preflight ledger before construction returns; no image-sized filter cache, token stream, compressed output, or retained caller lease is introduced.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED internal tests for adaptive planning, replay, and admission facts</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - Test 1: Small RGB8 and straight-RGBA8 multi-row inputs show that an Adaptive cursor returns the row winner's type byte and residual payload, while a None cursor remains the established provider.
    - Test 2: Stored, FixedOrStored, and DynamicOrFixedOrStored preflight plans consume adaptive bytes; a fixed/dynamic replay reaches the same expected matcher-work facts and Dynamic fingerprint as planning.
    - Test 3: An exact adaptive work limit/budget succeeds and one-less work limit or budget rejects during construction with unchanged budget state; the returned fact is exactly `2 * height * (5 * row_bytes)` checked adaptive score units plus the pre-existing selected-output and, for Fixed/Dynamic, two matcher-walk units.
    - Test 4: A source mutation between successful adaptive preflight and fixed/dynamic replay reaches the existing sticky replay-error route rather than emitting a stream inconsistent with planned facts.
  </behavior>
  <action>Begin with RED after Plan 39-01. In the two existing white-box test files, add fixtures that construct the private combined machine with `PngFilterStrategy::Adaptive` and inspect its internal filtered-byte, selected-plan, replay, and resource facts. Use a multi-row RGB8 fixture and a multi-row straight-RGBA8 fixture whose expected row winners are already justified by Task 39-01 vectors. Exercise all three compression strategies; require Fixed/Dynamic replay to equal the plan’s matcher work and require Dynamic replay to equal its fingerprint before finalization. Assert the resolved checked work formula: with `row_bytes = checked(width * channels)`, adaptive score work is `checked(2 * height * (5 * row_bytes))`, one five-candidate residual-and-signed-magnitude traversal in preflight plus one in replay. Assert final selected work is Stored `total_length + adaptive_filter_work` and Fixed/Dynamic `total_length + matcher_work + matcher_work + adaptive_filter_work`. Derive exact and one-less limits from these facts without magic totals. For rejected cases, assert the existing matching error and unchanged budget; for source mutation, retain the package’s sticky-terminal convention. Do not write public factory tests here, cache row selections, create an image-proportional allocation, weaken existing strict compression winner rules, change filter-None compatibility paths, or amend policy. Run the focused native internal selector and confirm it is RED because the filter-aware preflight/cursor contract is absent, then commit as `test(39-02): add adaptive planner replay tests`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --target-dir _build/phase39-02-red --frozen -f '*adaptive planner*'  # expected to fail before filter-aware planning and replay exist</automated>
  </verify>
  <done>Internal tests define adaptive filtered-byte identity, all compression-plan integration, exact filter work admission, and sticky replay drift before production integration exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one bounded adaptive cursor for planning and replay</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <behavior>
    - Test 1: The selected filter type and transformed bytes from Task 1 are consumed identically by preflight and each replay path.
    - Test 2: Fixed and Dynamic match finding remains bounded to its existing length and distance limits while reading through the filtered source contract.
    - Test 3: Preflight has calculated the selected complete plan, output limit, work limit, and one budget charge before a machine can be returned.
    - Test 4: None continues to use the existing byte provider and preserves its previous Stored/Fixed/Dynamic selection and output behavior.
  </behavior>
  <action>Implement PNGF-02 and PNGF-03 by replacing the Adaptive-to-None normalization with a shared filtered-byte resolver contract. Keep the None branch on `_png_fixed_scanline_byte` so Phase 38 legacy vectors retain their exact wire representation. For Adaptive, implement a forward-only cursor with scalar row/byte positions, one selected row filter, bounded preview state, and only the existing constant DEFLATE matcher look-ahead/history limits; select each row via Plan 39-01’s fixed candidate scorer and expose the type byte plus residual bytes. Do not retain selected filters by height, raw/filtered image buffers, a DEFLATE token list, compressed output, or caller leases.

Make `_png_encode_preflight` accept the filter strategy/resolver facts and route every Stored size calculation, Fixed matcher/frequency walk, Dynamic frequency/fingerprint walk, and replay byte access through the same logical selected stream. Preserve the established Fixed-over-Stored tie and Dynamic strict-complete-PNG-win rules. Use the resolved checked filter ledger exactly: `row_bytes = checked(width * channels)`, `candidate_score_work_per_row = checked(5 * row_bytes)`, and `adaptive_filter_work = checked(checked(height * candidate_score_work_per_row) + checked(height * candidate_score_work_per_row))`. Set selected work to Stored `checked(total_length + adaptive_filter_work)` and Fixed/Dynamic `checked(total_length + matcher_work + matcher_work + adaptive_filter_work)`; the selected filter tag adds no separate score unit because `total_length` already accounts for emitted bytes. Enforce output/work limits and make the one budget charge before `PngEncodeMachine::new_with_strategies` returns. Preserve preview/acknowledge semantics: cursor/matcher state calculated while presenting a byte must not commit until that byte is accepted. Preserve existing replay-drift detection and include adaptive stream facts wherever required so mutation cannot make the planned and emitted streams silently diverge. Do not add public factory surface or policy changes in this plan. Run RED to GREEN, format changed MoonBit files, and commit as `feat(39-02): integrate bounded adaptive PNG replay`.</action>
  <verify>
    <automated>$targets = @('js','wasm','wasm-gc','native'); foreach ($target in $targets) { moon -C modules/mb-image test png --target $target --target-dir "_build/phase39-02-$target" --frozen -f '*adaptive planner*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; moon -C modules/mb-image test png --target all --target-dir _build/phase39-02-regression --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>Adaptive has one fixed-memory source contract shared by all preflight and replay consumers; all selected plans and resource charges are final before output visibility, while None routes retain their prior byte provider and compression rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source image → adaptive cursor | Caller-controlled pixels determine candidate scores and filtered bytes. |
| Adaptive cursor → compressor preflight | Selected bytes determine complete output length, work, checksum, and compression winner. |
| Preflight facts → acknowledgement-safe replay | Replay must reproduce the planned adaptive stream despite output pauses and source mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-02-01 | Denial of service | Adaptive candidate scoring | high | mitigate | Task 2 uses a checked deterministic planning-plus-replay work ledger, max-work validation, and a single pre-output budget charge. |
| T-39-02-02 | Tampering | Planner/replay filtered byte stream | high | mitigate | Tasks 1-2 use one resolver contract for all plans and replay, retain matcher/fingerprint drift checks, and test source mutation terminals. |
| T-39-02-03 | Tampering | Legacy filter-None stream | high | mitigate | Task 2 keeps None on `_png_fixed_scanline_byte` and does not alter Stored/Fixed/Dynamic winner rules. |
| T-39-02-04 | Denial of service | Row-selection storage | high | mitigate | Task 2 forbids height- or image-proportional filter/token/output storage and limits cursor state to scalar/bounded matcher state. |
| T-39-02-05 | Tampering | Preview acknowledgement state | medium | mitigate | Task 2 keeps calculated cursor/matcher state private until Writer or lease acknowledgement; Task 1 exercises the replay state route. |
</threat_model>

<verification>
- RED/GREEN: run named adaptive-planner internal tests first on native, then independently on js, wasm, wasm-gc, and native.
- Regression: run the full PNG package suite across all targets and the existing scoped PNG quality lane.
</verification>

<success_criteria>
- Adaptive row filtering is the actual byte source for every compressor plan and replay path, not a post-processing branch.
- Adaptive work, output, and budget admission is atomic and test-proven without image staging.
- Existing None bytes and compression winner rules remain protected by their existing full-package regressions; public API publication is deferred to Plan 39-03.
</success_criteria>

<output>
Create `.planning/phases/39-bounded-filter-planning-and-replay/39-02-SUMMARY.md` when done.
</output>
