---
phase: 39-bounded-filter-planning-and-replay
plan: "05"
type: tdd
wave: 5
depends_on: [39-04]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_wbtest.mbt
autonomous: true
gap_closure: true
requirements: [PNGF-02, PNGF-03]
must_haves:
  truths:
    - "Adaptive FixedOrStored and DynamicOrFixedOrStored matcher probes obtain random comparison bytes from an acknowledgement-safe bounded forward cursor, so entering a row never reselects its filter within a traversal."
    - "Before a machine or caller lease is exposed, the checked Adaptive ledger admits the traversal facts from every Stored, Fixed, and Dynamic planning walk actually selected by the requested route and from exactly one selected replay walk."
    - "All public Adaptive combined routes retain deterministic output, Fixed-over-Stored ties, Dynamic strict complete-PNG wins, atomic rejection, and all filter-None public bytes on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: "A fixed-capacity Adaptive matcher byte source that feeds Fixed and Dynamic planning instead of stateless random filter selection, returning measured traversal facts with each pass."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Stored, Fixed, and Dynamic replay state whose preview cursor and match window commit only after a byte acknowledgement."
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: "RED/GREEN accounting tests that expose Fixed and Dynamic probe undercharge and assert every admitted Adaptive planning/replay traversal."
    - path: modules/mb-image/png/stream_encode_wbtest.mbt
      provides: "White-box look-ahead/history wrap and repeated-present acknowledgement tests for the bounded Adaptive matcher cursor."
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Preflight plans with fresh measured match cursors; the selected emitter owns an equivalent persistent cursor whose state advances only through acknowledge()."
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/png.mbt
      via: "The existing PngEncoder::new_with_strategies and PngChunkEncoder::new_with_strategies routes continue to use the single shared machine without a new public surface."
---

<objective>
Close the remaining Phase 39 verification gap: Fixed and Dynamic DEFLATE matcher probing must use the bounded forward Adaptive cursor rather than the stateless random byte supplier.

Purpose: Make the exact preflight work ledger truthful for every Adaptive planning and replay traversal while preserving deterministic PNG bytes and atomic eager/caller-buffered behavior.
Output: A fixed-memory matcher cursor with a 258-byte look-ahead plus four-byte history ring, acknowledgement-safe replay integration, and four-target regression evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-VERIFICATION.md
@.planning/phases/39-bounded-filter-planning-and-replay/RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-04-PLAN.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-04-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_wbtest.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- `PngFilteredCursor` and `PngFilteredTraversalFacts` from Plan 04 already select an Adaptive row once on sequential entry; preserve their candidate order and strict earlier-wins tie rule.
- `_png_filtered_match_at` is the defective seam: Fixed and Dynamic planning call it for arbitrary positions, and it currently obtains both operands through the stateless Adaptive byte supplier.
- `_png_fixed_plan`, `_png_dynamic_frequencies`, and `_png_dynamic_plan` are all planning traversals; their returned plans/facts must reveal the actual match-cursor facts rather than adding a formula-only cursor pass beside the work they perform.
- `PngEncodeMachine::present()` retains `pending_stored`, `pending_fixed`, or `pending_dynamic` until `acknowledge()` accepts a byte. A preview may calculate a successor, but only acknowledgement may commit its filtered/match cursor state.
- Public affected routes are `PngEncoder::new_with_strategies` and `PngChunkEncoder::new_with_strategies` for Stored, FixedOrStored, and DynamicOrFixedOrStored. `new()`, compression-only factories, and filter-only routes remain the frozen None/Stored contracts.
</interfaces>
</context>

<feature>
  <name>Bounded acknowledgement-safe Adaptive matcher cursor</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - PNGF-02: Each fresh Adaptive match-cursor traversal resolves a row exactly once when its sequential producer first enters that row, retains the selected tag/residual bytes, and preserves None, Sub, Up, Average, Paeth order with strict-lower replacement.
    - PNGF-03: Match probing can compare a maximum 258-byte candidate against distances one through four by using only the current forward cursor plus a fixed 262-byte ring: 258 bytes of look-ahead and the four-byte maximum history. Repeated reads of buffered positions do not invoke row selection again.
    - PNGF-03: The final checked preflight work is the sum of facts from every cursor traversal actually executed for the requested strategy: Stored candidate planning; Fixed candidate planning when requested; Dynamic frequency/fingerprint and Dynamic bit-counting passes when requested; and exactly one selected Stored, Fixed, or Dynamic replay pass.
    - PNGF-03: Pending eager writes and pending chunk leases retain an uncommitted successor match cursor. Failed/no-progress acknowledgement leaves its selection count, ring contents, and logical position unchanged; one accepted acknowledgement commits them once.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-test Fixed and Dynamic Adaptive matcher probing and exact traversal admission</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_wbtest.mbt</files>
  <behavior>
    - Test 1: New `PNG adaptive match cursor covers FixedOrStored probe traversals` and `PNG adaptive match cursor covers DynamicOrFixedOrStored probe traversals` tests use RGB8 and straight-RGBA8 multi-row periodic fixtures whose row widths are shorter than 258 logical bytes, forcing a long matcher probe through several tags and row payloads.
    - Test 2: Each named strategy exposes the facts for every executed planning walk and the selected replay walk. For every such fresh Adaptive traversal, `resolved_rows == height`, candidate residuals equal `height * 5 * row_bytes`, and selected residuals equal the logical bytes actually produced once; probe revisits may increase matcher work but cannot increase row-resolution or residual facts.
    - Test 3: Exact selected work/budget admits construction and a complete drain; one unit less rejects before a machine or chunk lease is produced and leaves the caller budget unchanged for Stored, FixedOrStored, and DynamicOrFixedOrStored.
    - Test 4: A private matcher cursor test fills and wraps the maximum 258-byte look-ahead/history window, verifies the four prior-byte distance operands remain available, and verifies row selection remains once-per-row without image-sized retention.
    - Test 5: Fixed and Dynamic pending-state tests call `present()` twice before acknowledgement, then acknowledge once. The first two observations have the same byte, facts, ring state, and logical position; only the accepted acknowledgement advances them.
  </behavior>
  <action>Start RED by adding the two named white-box plan tests and a focused stream cursor test. Build the fixtures so Fixed and Dynamic execute actual match comparisons across several Adaptive row boundaries, not only literal emission or a preflight-only sequential cursor walk. Make the assertions derive expected work from private per-pass traversal facts and the selected plan rather than a copied sweep formula. The tests must enumerate every actual route traversal: Stored candidate walk; Fixed candidate matcher walk for FixedOrStored and DynamicOrFixedOrStored; Dynamic frequency/fingerprint walk and Dynamic bit-counting walk for DynamicOrFixedOrStored; then one replay walk for the plan that wins. A route that declines Dynamic still records the walks it executed and uses the Fixed-or-Stored winner for replay.

Expose only package-private test observability needed to distinguish one source-byte production from arbitrary matcher lookups: row resolutions, candidate residuals, selected residuals, logical cursor position, and bounded ring occupancy/capacity. Assert a 258-byte look-ahead plus four-byte distance history limit, rather than any allocation derived from image width, height, scanline count, filtered length, or token count. Add exact/one-less atomic checks for work and budget on all three strategies, and pending Fixed/Dynamic replay checks that prove no state commits before acknowledgement. Do not alter public declarations, policy, QOI, fixtures, quality lanes, compression tie rules, source-mutation sticky tests, or filter-None tests. Run the named native selector and confirm it fails against the current stateless matcher seam because Fixed/Dynamic probes bypass the cursor and their undercharged traversal facts cannot be supplied.</action>
  <verify>
    <automated>$targetRoot = Join-Path (Resolve-Path '_build') 'phase39-05-red'; if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null; try { moon -C modules/mb-image test png --target native --target-dir $targetRoot --frozen -f '*adaptive match cursor*'; if ($LASTEXITCODE -eq 0) { throw 'RED matcher-cursor selector unexpectedly passed before the bounded matcher implementation' } } finally { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } }</automated>
  </verify>
  <done>The regression suite fails on the Plan 04 implementation and proves the concrete failure: Fixed/Dynamic random probes cannot report one-selection-per-row traversal facts or exact admission through the stateless byte source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every Adaptive matcher pass and replay preview through the fixed-memory ring</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt</files>
  <behavior>
    - Test 1: The RED Fixed/Dynamic probe and exact-accounting tests pass for both RGB8 and straight-RGBA8 fixtures on all four portable targets.
    - Test 2: FixedOrStored still selects Fixed on an equal complete-PNG size, DynamicOrFixedOrStored replaces its baseline only on a strict complete-PNG win, and all None providers continue to supply the existing frozen bytes.
    - Test 3: Stored, Fixed, and Dynamic emission carry a persistent successor cursor through preview/acknowledgement; source-mutation fingerprint and sticky-failure behavior remain intact.
  </behavior>
  <action>Implement one private bounded Adaptive match-byte source shared by the Fixed and Dynamic planning walkers and their selected replay states. It may be named `PngFilteredMatchCursor` and may wrap `PngFilteredCursor`, but it must advance its producer only forward. Retain a constant 262-byte circular window consisting of the matcher's maximum 258-byte forward look-ahead and maximum four-byte distance history; record absolute logical positions so every current/prior operand requested by the existing distance-one-through-four, length-at-most-258 matcher is served from the window. If a requested byte has not yet been produced, advance the wrapped cursor until it is; if it is retained, read it without calling the row winner again. Do not retain selected-filter rows, pixels, filtered scanlines, DEFLATE tokens, output bytes, or any image-dimension-sized allocation.

Replace the Adaptive use of `_png_filtered_match_at` and direct arbitrary filtered-byte lookup in `_png_fixed_plan`, `_png_dynamic_frequencies`, and `_png_dynamic_plan` with fresh measured match-cursor traversals. Keep the legacy None scanline supplier on its existing route. Make each plan return the actual filter traversal facts generated while performing its own matcher work, including Dynamic's frequency/fingerprint and bit-counting passes. In `_png_encode_preflight_with_filter`, build the checked ledger by summing only those returned, executed facts: the Stored candidate pass, the requested Fixed and Dynamic candidate passes, and exactly one new selected replay pass. Validate output/work limits and charge the existing budget once before returning a machine; remove the parallel formula-only Adaptive passes that double-count or omit real matcher traversals.

Carry a cloneable match cursor through `PngFixedState` and `PngDynamicState` as part of the existing pending preview successor. Stored retains its forward cursor equivalent. `present()` must leave the committed cursor/ring untouched, and `acknowledge()` must commit the successor exactly with the byte, DEFLATE, Adler, CRC, matcher, and fingerprint state already guarded by that boundary. Preserve deterministic output, Dynamic replay-drift fingerprint protection, sticky terminal outcomes, public factory signatures, Fixed equality preference, Dynamic strict-win comparison, and all filter-None execution paths. Run the RED selector to GREEN, then run the full scoped portable matrix; format only these two edited MoonBit implementation files.</action>
  <verify>
    <automated>$targetRoot = Join-Path (Resolve-Path '_build') 'phase39-05-match-ring'; if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force }; New-Item -ItemType Directory -Path $targetRoot | Out-Null; try { $filters = @('*adaptive match cursor*','*adaptive cursor work ledger*','PNG adaptive combined eager routes','PNG adaptive combined chunk routes','PNG adaptive combined admission is atomic','PNG filter strategy eager frozen compatibility vectors','PNG filter strategy chunk frozen compatibility vectors','PNG compression strategy legacy eager constructors retain frozen stored bytes','PNG compression strategy legacy chunk constructors retain frozen stored bytes','PNG dynamic strategy eager factory freezes compatibility routes','PNG dynamic strategy chunk factory freezes compatibility routes'); foreach ($target in @('js','wasm','wasm-gc','native')) { $targetDir = Join-Path $targetRoot $target; foreach ($filter in $filters) { moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen -f $filter; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } }; $checkDir = Join-Path $targetRoot 'check'; moon -C modules/mb-image check png --target all --target-dir $checkDir --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } } finally { if (Test-Path -LiteralPath $targetRoot) { Remove-Item -LiteralPath $targetRoot -Recurse -Force } }</automated>
  </verify>
  <done>Every real Adaptive Fixed/Dynamic probe and replay byte comes from the bounded acknowledgement-safe cursor; the preflight ledger admits all and only the executed cursor traversals before output visibility, and the requested public Adaptive routes plus frozen None evidence pass independently on js, wasm, wasm-gc, and native.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Deterministic bounded Adaptive rows before Stored, FixedOrStored, and Dynamic planning with atomic eager/chunk behavior | 39-05 | COVERED | Replaces the remaining stateless Fixed/Dynamic matcher bypass with bounded per-traversal state and exact admission.
REQ | PNGF-02 | Deterministic five-candidate Adaptive row selection with a stable winner | 39-05 | COVERED | The matcher cursor consumes Plan 04's strict cursor one row at a time and tests prove probes cannot reseat a row winner.
REQ | PNGF-03 | No image staging; exact atomic planning/replay for Stored, FixedOrStored, and DynamicOrFixedOrStored | 39-05 | COVERED | All actual candidate/replay passes produce measured facts; exact/one-less work and budget tests prove admission before machine or lease visibility.
RESEARCH | — | None remains on the legacy provider and Fixed/Dynamic ties remain unchanged | 39-05 | COVERED | Task 2 preserves those providers/rules; four-target frozen None and public strategy tests are mandatory verification.
RESEARCH | — | Maximum match length 258 and distance 4 require fixed look-ahead/history state | 39-05 | COVERED | Task 2 establishes a fixed 258-plus-four-byte ring and Task 1 verifies its bound and wrap behavior.
RESEARCH | — | Preview state cannot commit before writer or lease acknowledgement | 39-05 | COVERED | Pending Fixed/Dynamic successor-cursor tests and implementation instructions retain the existing acknowledgement boundary.
CONTEXT | — | No Phase 39 CONTEXT.md or locked D-NN decisions exist | — | COVERED | No deferred idea or new public scope is added.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image → Adaptive match cursor | Caller-controlled rows determine residuals, selections, and matcher operands. |
| Match cursor traversal → preflight ledger | Every executed candidate and selected replay traversal must be charged before output is observable. |
| Pending cursor/ring → writer or caller lease acknowledgement | A previewed byte must not advance Adaptive selection or retained match history until accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-05-01 | Denial of service | Fixed/Dynamic Adaptive matcher probes | high | mitigate | Task 2 bounds each probe source to 258 bytes of look-ahead plus four history bytes and removes image-sized/random stateless rescoring. |
| T-39-05-02 | Denial of service | Adaptive work admission | high | mitigate | Task 2 composes checked facts from all real requested-strategy planning and selected replay traversals before the one existing budget charge; Task 1 proves exact/one-less behavior. |
| T-39-05-03 | Tampering | Filtered-byte/matcher consistency | high | mitigate | Task 2 routes Fixed/Dynamic planning and replay through the same deterministic cursor contract and preserves fingerprint replay checks. |
| T-39-05-04 | Tampering | Pending Fixed/Dynamic cursor state | high | mitigate | Task 2 commits successor cursor/ring state only in acknowledge(); Task 1 verifies repeated present calls do not resolve rows or mutate the ring. |
| T-39-05-05 | Tampering | Frozen public None routes and compression ties | high | mitigate | Task 2 leaves legacy providers and selection comparisons unchanged; Task 2 runs named eager/chunk frozen compatibility evidence on four targets. |
</threat_model>

<verification>
- RED/GREEN: the new native matcher-cursor selector fails before implementation because Fixed/Dynamic probing uses the stateless byte source, then passes after all Adaptive match walks use the bounded cursor.
- Accounting: for Stored, FixedOrStored, and DynamicOrFixedOrStored, assert the selected plan ledger equals each actual planning traversal plus exactly one replay traversal; exact work/budget succeeds and one-less rejects before any eager byte or chunk lease.
- Acknowledgement: prove Fixed/Dynamic repeated present is observational only, while successful acknowledgement commits one successor state and preserves existing sticky replay-drift behavior.
- Public routes: independently run the affected eager and chunk `new_with_strategies` Adaptive tests for all three compression choices, the atomic route, and the frozen None eager/chunk/default/compression/dynamic vectors on js, wasm, wasm-gc, and native.
- Cleanup: both verification commands remove their named `_build` target roots in `finally`, including every target-specific and check directory beneath them regardless of failure.
</verification>

<success_criteria>
- Fixed/Dynamic matcher probes never invoke stateless Adaptive row selection; every row is selected once per forward cursor traversal and all random probe reads are satisfied by the bounded ring.
- The Adaptive preflight ledger is a checked sum of every traversal actually performed by the requested strategy and exactly one selected replay traversal, retaining pre-output atomicity.
- The implementation retains only fixed scalar/cursor/ring state, preserves output determinism and existing compression selection rules, and leaves legacy filter-None bytes unchanged.
- Focused private and public evidence passes on js, wasm, wasm-gc, and native, with no persistent Phase 39 test target directories.
</success_criteria>

<output>
Create `.planning/phases/39-bounded-filter-planning-and-replay/39-05-SUMMARY.md` when done.
</output>
