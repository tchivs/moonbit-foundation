---
phase: 39-bounded-filter-planning-and-replay
plan: "03"
type: tdd
wave: 3
depends_on: [39-02]
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - policy/foundation.json
autonomous: true
requirements: [PNGF-02, PNGF-03]
must_haves:
  truths:
    - "Library users can combine Adaptive filtering with Stored, FixedOrStored, or DynamicOrFixedOrStored through additive eager and caller-buffered factories documented with the fixed candidate order and stable scorer rule."
    - "Filter-only Adaptive remains Stored plus Adaptive, while all legacy/default and existing compression factories remain explicit None routes with frozen output."
    - "Public eager and caller-buffered Adaptive failures for capability, geometry, output, work, and budget occur before a writer byte or caller lease is exposed."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Documented additive eager combined compression/filter factory, including adaptive candidate order and stable-score behavior.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Documented additive caller-buffered combined compression/filter factory entering the shared machine with matching adaptive-selection semantics.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Public eager combined-route decoding and legacy-vector tests.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public hostile-capacity combined-route and atomic-failure tests.
    - path: policy/foundation.json
      provides: PNG semantic-interface declarations for the two additive public combined factories.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: Eager and chunk combined factories retain both strategy values and create the same PngEncodeMachine.
      pattern: new_with_strategies|PngFilterStrategy|PngCompressionStrategy
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: Hostile drain and rejected-construction tests prove no caller lease observes adaptive output before admission succeeds.
      pattern: new_with_strategies|pull|remaining
    - from: modules/mb-image/png/png.mbt
      to: policy/foundation.json
      via: Generated semantic-interface policy records the eager and caller-buffered public combined-factory declarations.
      pattern: new_with_strategies|semantic_interface
---

<objective>
Publish and prove the additive public combined compression/filter route over the bounded adaptive implementation, retaining Phase 38 factory semantics and atomic caller-buffered safety. PNGF-02, PNGF-03.

Purpose: Make the completed adaptive planner accessible for every existing compression strategy without changing a legacy filter-None route.
Output: Documented eager/chunk combined factories, semantic-interface registration, public black-box behavior, and scoped quality evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-adaptive-filter-compatibility/38-01-SUMMARY.md
@.planning/phases/39-bounded-filter-planning-and-replay/RESEARCH.md
@.planning/phases/39-bounded-filter-planning-and-replay/39-02-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@policy/foundation.json

<interfaces>
- Phase 38 factories are source-compatible contracts: default and compression-only factories specify `PngFilterStrategy::None`; `new_with_filter_strategy(Adaptive)` specifies Stored plus Adaptive.
- Plan 39-02 supplies the one bounded Adaptive planning/replay contract beneath `PngEncodeMachine::new_with_strategies`.
- Existing public test helpers provide complete-input decode, independent frozen full-PNG vectors, hostile `[0UL, 1UL, 3UL, 2UL, 5UL]` drains, exact progress, and unchanged-budget error assertions.
- The PNG quality lane validates public-interface policy, package contents, generated vectors, portable workflow, colour evidence, and all-target PNG tests without release or deployment actions.
</interfaces>
</context>

<feature>
  <name>Public combined adaptive-filter compression route</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt, policy/foundation.json</files>
  <behavior>
    - PNGF-02: Eager and caller-buffered users can request each `PngCompressionStrategy` with `PngFilterStrategy::Adaptive` through documented additive `new_with_strategies` factories; public `png.mbt` documentation states None, Sub, Up, Average, Paeth candidate order, signed-absolute residual scoring, and strict-lower earlier-candidate tie retention.
    - PNGF-03: Every combined route reaches the same filter-aware atomic preflight and acknowledgement-safe machine; public decode confirms RGB8 and straight-RGBA8 source fidelity.
    - PNGF-03: Capability, geometry, output, work, and budget rejection has the same eager/chunk error with zero eager writer bytes, no chunk pull/lease exposure, and unchanged rejected budget.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED public tests for combined Adaptive routes and atomic rejection</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: Eager and chunk `new_with_strategies` calls compile for Stored, FixedOrStored, and DynamicOrFixedOrStored with Adaptive and round-trip compatible RGB8 and straight-RGBA8 images through public complete-input decode.
    - Test 2: Hostile chunk drains copy only accepted prefixes and equal eager output for every combined route, including zero-capacity turns and sticky terminal handling.
    - Test 3: `new_with_filter_strategy(Adaptive)` continues to mean Stored plus Adaptive; defaults and compression-only routes retain independent full-PNG None vectors for Stored, FixedOrStored, and strict Dynamic.
    - Test 4: Adaptive combined eager and chunk construction reject each capability, geometry, output, work, and budget failure atomically, with matching errors and no changed rejected budget.
  </behavior>
  <action>Start with public black-box RED tests after Plan 39-02. Name the new tests `PNG adaptive combined eager routes`, `PNG adaptive combined chunk routes`, and `PNG adaptive combined admission is atomic` so the all-target verification can select them deterministically. In `encode_test.mbt`, add compact RGB8 and straight-RGBA8 multi-row fixtures that select meaningful adaptive filters, call the planned eager `PngEncoder::new_with_strategies`, and verify public complete-input decoding. In `stream_encode_test.mbt`, use the established hostile drain helper and `[0UL, 1UL, 3UL, 2UL, 5UL]` schedule for the matching chunk factory; compare accepted-prefix output to eager bytes for Stored, FixedOrStored, and DynamicOrFixedOrStored. Preserve the independent Phase 38 complete PNG literals for legacy/default and compression-only None routes rather than deriving their expected bytes from new output. Add focused public rejected-construction checks for incompatible capability, geometry, output, exact/one-less filter-aware work, and budget exhaustion: eager must leave the writer at zero and chunk construction must fail before any `pull` receives a lease, with the rejected budget unchanged. Do not assert a particular compression size win, corpus outcome, or four-target portable evidence reserved for Phase 40. Do not modify public source, policy, scripts, configuration, release workflow, QOI code, or fixtures in this RED task. Run the named native public selector and confirm failure because public combined factories are absent, then commit as `test(39-03): add public adaptive compression coverage`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --target-dir _build/phase39-03-red --frozen -f '*adaptive combined*'  # expected to fail before public combined factories exist</automated>
  </verify>
  <done>Public tests define every combined compression/filter call, hostile eager/chunk parity, retained filter-only and legacy semantics, and atomic resource rejection without relying on private encoder state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish combined factories and register the public interface</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_encode.mbt, policy/foundation.json</files>
  <behavior>
    - Test 1: The public eager and caller-buffered combined factories expose the same three compression values and two filter values, then enter the shared filter-aware machine.
    - Test 2: Filter-only Adaptive remains Stored plus Adaptive; every old factory remains an explicit None route.
    - Test 3: The generated semantic interface has exactly the additive eager and chunk combined declarations and the PNG quality lane accepts the policy.
  </behavior>
  <action>Implement PNGF-02 and PNGF-03 public access with additive documented `PngEncoder::new_with_strategies(compression_strategy, filter_strategy)` and `PngChunkEncoder::new_with_strategies(source, compression_strategy, filter_strategy, limits, budget, diagnostics)` factories. Use the repository’s established public naming convention from Phase 38: the existing private chunk constructor may be renamed internally if MoonBit visibility requires it, but there must be one public factory pair with identical strategy terminology. Update the public `png.mbt` documentation for `PngFilterStrategy::Adaptive` and both additive factory routes to state the exact method-0 selection policy: candidates are evaluated in None, Sub, Up, Average, Paeth order; each candidate is scored by the sum of signed-absolute residual magnitudes; only a strictly lower score replaces the winner, so an equal score retains the earlier candidate. Retain `new()`, both compression-only factories, and both filter-only factories unchanged in meaning: legacy/compression-only calls explicitly pass None, while filter-only Adaptive explicitly remains Stored plus Adaptive. Route both public combined calls directly through the shared filter-aware machine from Plan 39-02; do not add a second emitter, staging store, or new resource-admission branch.

Regenerate the semantic interface through the established repository mechanism, then make the smallest scoped `policy/foundation.json` update that records only the two combined factory declarations in the PNG interface. Preserve policy structure, source inventories, target declarations, allowlists, and unrelated semantic declarations. Do not introduce external packages, FFI, CI/release/deploy work, benchmarks, generated corpus artifacts, QOI changes, or public claims of a strict compression win. Run RED to GREEN, format changed MoonBit source, and commit as `feat(39-03): publish adaptive PNG strategy factories`.</action>
  <verify>
    <automated>$required = @('PNG adaptive combined eager routes','PNG adaptive combined chunk routes','PNG adaptive combined admission is atomic'); foreach ($target in @('js','wasm','wasm-gc','native')) { $targetDir = "_build/phase39-03-$target"; $outline = @(moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen --outline 2>&1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "focused PNG outline has no test entry on $target" }; foreach ($name in $required) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing required PNG test on ${target}: $name" } }; moon -C modules/mb-image test png --target $target --target-dir $targetDir --frozen -f '*adaptive combined*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; moon -C modules/mb-image test png --target all --target-dir _build/phase39-03-all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The documented public combined factories make Adaptive available with Stored, FixedOrStored, and DynamicOrFixedOrStored; `png.mbt` publicly documents None/Sub/Up/Average/Paeth order, signed-absolute residual scoring, and strict-lower earlier-candidate tie retention; legacy None and filter-only Stored semantics remain intact; all-target public tests and the PNG quality lane pass.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Deterministic bounded adaptive filtering before existing compression planners while retaining atomic eager/chunk behavior | 39-01, 39-02, 39-03 | COVERED | Arithmetic, cursor/preflight/replay, then public adapters and atomic black-box proof.
REQ | PNGF-02 | Standard None/Sub/Up/Average/Paeth selection with stable winner | 39-01, 39-02, 39-03 | COVERED | Formula/tie vectors, bounded integration, public configuration.
REQ | PNGF-03 | Stored/Fixed/Dynamic integration, no staging, atomic capability/geometry/output/work/budget failures | 39-02, 39-03 | COVERED | Shared cursor and preflight ledger followed by public eager/chunk rejection proof.
RESEARCH | — | Legacy None bytes remain frozen | 39-01, 39-02, 39-03 | COVERED | None provider remains separate and existing public immutable vectors are retained.
RESEARCH | — | Fixed candidate order, signed score, strict lower winner | 39-01 | COVERED | White-box formula, score, and tie tests.
RESEARCH | — | Forward-only fixed-state replay without image staging | 39-02 | COVERED | Cursor contract, bounded matcher state, and explicit staging prohibition.
RESEARCH | — | Checked adaptive ledger: `2 * height * (5 * row_bytes)` scoring units plus existing output/matcher work | 39-02, 39-03 | COVERED | Internal exact/one-less facts and public atomic budget/work rejection cover the resolved formula.
RESEARCH | — | Public combined route while filter-only Adaptive remains Stored | 39-03 | COVERED | Additive combined factories and explicit existing-factory semantics.
RESEARCH | — | All-target regression and scoped quality lane | 39-02, 39-03 | COVERED | Each plan’s GREEN verification runs portable tests; final public plan reruns the PNG quality lane.
CONTEXT | — | No Phase 39 CONTEXT.md or D-NN locked decisions supplied | — | COVERED | User pre-authorized the documented best-option choices; no deferred ideas are introduced.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library caller → combined factory | Caller chooses compression/filter settings and supplies untrusted image capability/geometry. |
| Combined factory → atomic shared machine | Public configuration must reach one preflight path before a writer byte or lease can be observed. |
| Shared machine → caller-owned output lease | Hostile capacities and terminal errors must retain accepted-prefix ownership and exact progress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-03-01 | Tampering | Legacy/default and configured None byte representations | high | mitigate | Task 1 retains independent Phase 38 complete vectors; Task 2 makes existing factories explicit None routes. |
| T-39-03-02 | Denial of service | Adaptive public construction | high | mitigate | Task 1 proves capability, geometry, output, work, and budget failure before output; Task 2 uses the single Plan 39-02 preflight path. |
| T-39-03-03 | Tampering | Eager/chunk adaptive parity | high | mitigate | Task 1 compares public decoded eager output and hostile accepted-prefix chunk output for all compression strategies. |
| T-39-03-04 | Tampering | Public semantic interface | medium | mitigate | Task 2 regenerates and narrowly updates PNG interface declarations; final PNG quality lane verifies policy and package contents. |
| T-39-03-05 | Spoofing | Factory documentation | low | mitigate | Task 2 documents combined semantics and retains filter-only Stored behavior instead of implying a changed default. |
</threat_model>

<verification>
- RED/GREEN: prove absent combined API first, then execute named public eager/chunk/admission tests independently on js, wasm, wasm-gc, and native.
- Regression: execute all PNG tests on all targets after public tests pass.
- Quality: run `Invoke-MoonQuality.ps1 -Lane Png`; it is the scoped policy and portable workflow lane, not release or deployment work.
</verification>

<success_criteria>
- All three existing compression strategies can be deliberately combined with Adaptive on eager and caller-buffered APIs, and the public `png.mbt` contract states its candidate order, signed-absolute scoring, and earlier-wins tie rule.
- Adaptive selection is deterministic, bounded, and used by planning plus replay; filter-only and legacy defaults retain their defined Phase 38 behavior.
- Every new public resource rejection is observed atomically before eager bytes or a caller lease, with unchanged rejected budget accounting.
- Public black-box tests, full portable PNG regression, and the scoped PNG quality lane pass without release, registry, CI, or deployment changes.
</success_criteria>

<output>
Create `.planning/phases/39-bounded-filter-planning-and-replay/39-03-SUMMARY.md` when done.
</output>
