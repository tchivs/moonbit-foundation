---
phase: 60-bounded-adam7-streaming-semantics
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYA8A7-02
must_haves:
  truths:
    - "Each legal GrayAlpha8 Adam7 None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored selection remains on the shared bounded traversal and replay route."
    - "A checked GrayAlpha8 source mutation after acknowledged framing makes the next nonzero caller lease report a plan-specific failure with zero accepted bytes, unchanged total progress, and an untouched tail."
    - "The same terminal diagnostic is returned by a later pull, with no new lease writes."
    - "GrayAlpha8 Adam7 descriptor, capability, geometry, output, work, and budget rejection remains atomic for eager and caller-buffered construction."
  artifacts:
    - path: "modules/mb-image/png/stream_encode.mbt"
      provides: "Profile-neutral source revision validation before the active chunk pull can write a caller lease."
      contains: "validate_replay_revision"
    - path: "modules/mb-image/png/stream_encode_test.mbt"
      provides: "Phase-local GrayAlpha8 Adam7 replay-drift matrix and atomic-admission regression coverage."
      contains: "PNG GrayAlpha8 Adam7 replay mutations are sticky for every strategy pair"
  key_links:
    - from: "modules/mb-image/png/stream_encode.mbt:PngChunkEncoder::pull"
      to: "modules/mb-image/png/stream_encode.mbt:PngEncodeMachine::validate_replay_revision"
      via: "The Active-machine branch validates its admitted source revision before the loop can invoke destination.set."
      pattern: "machine\\.validate_replay_revision\\(\\)"
    - from: "modules/mb-image/png/stream_encode_test.mbt:png_graya8_replay_mutation_is_sticky"
      to: "modules/mb-image/png/stream_encode.mbt:PngChunkEncoder::new_graya8_with_all_strategies"
      via: "Every GrayAlpha8 Adam7 compression/filter pair is constructed through the public all-strategy selector and pulled into caller-owned sentinel leases."
      pattern: "new_graya8_with_all_strategies"
---

<objective>
Make GrayAlpha8 Adam7 replay integrity use the existing profile-neutral bounded machine, then prove the pre-lease and atomic-admission contracts through the public selector.

Purpose: Fulfill GRAYA8A7-02 without adding an encoder path, staging, broad public evidence, or target-specific work.
Output: One shared replay-revision seam and phase-local GrayAlpha8 Adam7 mutation/admission regressions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-bounded-adam7-streaming-semantics/60-CONTEXT.md
@.planning/phases/60-bounded-adam7-streaming-semantics/60-RESEARCH.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-VERIFICATION.md
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/encode.mbt

<interfaces>
From modules/mb-image/png/stream_encode.mbt:
- `pub fn PngChunkEncoder::pull(self, destination : @bytes.MutByteLease) -> PngChunkPullResult` is the only caller-lease emission seam; its Active branch must reject replay drift before the first `destination.set`.
- `fn PngEncodeMachine::new_with_profile(...) -> Result[PngEncodeMachine, @error.CoreError]` performs shared profile-aware atomic preflight and stores `source_revision: source.mutation_revision()`.
- `pub fn PngChunkEncoder::new_graya8_with_all_strategies(source, strategy, filter_strategy, interlace_strategy, limits, budget, diagnostics)` is the public GrayAlpha8 Adam7 constructor to exercise.

From modules/mb-image/png/encode.mbt:
- `_png_encode_preflight_with_interlace_profile(...)` owns descriptor/capability validation, Adam7 pass geometry, filter-aware planning, output/work checks, and one budget charge before machine construction.
- `PngFilteredCursor` resolves Adam7 `(pass, row, in_row)` and preserves Adaptive filter history per pass; leave that traversal unchanged.

From modules/mb-image/png/stream_encode_test.mbt:
- `png_graya16_replay_mutation_is_sticky` is the direct assertion pattern for one-byte acknowledged framing, selected BTYPE, source mutation, sentinel lease preservation, accepted-only totals, and repeated terminal comparison.
- `png_graya8_combined_public_reject` is the existing GrayAlpha8 all-pair eager/chunk admission helper; extend it to accept the selected interlace route rather than duplicating its transaction assertions.
- The measured Phase 60 corpus is fixed: `stored-ramp-5x5` selects `0x01`, `fixed-flat-5x5` selects `0x03`, and `dynamic-periodic-128x5` selects `0x05` under both None and Adaptive; each generator exercises all seven Adam7 passes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TDD tracer — reject one GrayAlpha8 Adam7 replay before the next caller-lease write</name>
  <reversibility rating="reversible">D-01 changes one private validation method and its call site behind the existing public chunk contract, so it can be locally reverted without a migration.</reversibility>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - RED: A legal GrayAlpha8 Adam7 Stored/None source is pulled one byte at a time until 44 bytes have been acknowledged, then its alpha component is mutated.
    - RED: The following pull into a nonzero sentinel lease returns the selected Stored replay-drift diagnostic with `written() == 0UL`, the saved accepted total, and every requested lease byte still equal to the sentinel.
    - GREEN: The same test passes after the shared Active-machine pull seam validates every profile's stored source revision before presenting or copying a byte.
    - Regression: Existing GrayAlpha16 replay-drift tests still report their established Fixed, Dynamic, and Stored diagnostics.
  </behavior>
  <action>D-01: replace the U16-only revision check at the common pre-lease seam while retaining the selected Stored/Fixed/Dynamic diagnostics. D-03: retain the profile-aware traversal, filter contexts, preflight ledger, and replay plans. First add `png_stream_graya8_adam7_stored_ramp_5x5` and its GrayAlpha8 Adam7 Stored/None mutation regression beside `png_graya16_replay_mutation_is_sticky`; the generator must use `s = 5*y + x`, `G = 0x20 + s`, and `A = 0xa0 + s`, and the public all-strategy constructor must assert BTYPE `0x01` after the one-byte acknowledged prefix. Use the existing prefix/pull and sentinel-owner helpers, and make the RED command fail because U8 revision drift can still reach lease emission. Commit the RED state as `test(60-01): expose GrayAlpha8 Adam7 replay drift`. Then rename `PngEncodeMachine::validate_u16_replay_revision` to `validate_replay_revision`, remove only its U16-profile bypass, keep the Stored/Fixed/Dynamic error selection exactly intact, and call the renamed method from `PngChunkEncoder::pull` in the same pre-loop location before `destination.set` is reachable. Do not alter `PngEncodeMachine::new_with_profile`, cursor/traversal/filter code, factory selectors, compression plans, or the acknowledgement protocol. Run the focused test and U16 regressions, then commit GREEN as `feat(60-01): guard all PNG replay revisions`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 replay mutations are sticky for every strategy pair' &amp;&amp; moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 replay mutations are sticky for every strategy pair'</automated>
  </verify>
  <done>The common Active pull seam validates the captured/current revision for GrayAlpha8 and existing profiles before lease mutation; the Stored/None GrayAlpha8 Adam7 tracer has an acknowledged prefix followed by zero-write, accepted-only, sentinel-preserving, sticky plan-specific failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the shared GrayAlpha8 Adam7 contract across all strategy pairs and atomic admission</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Every None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored Adam7 pairing reaches its asserted Stored, Fixed, or Dynamic BTYPE before mutation and then preserves the first and later sentinel leases with zero writes and accepted-only totals.
    - Both failure pulls expose the identical selected-plan terminal diagnostic after source mutation.
    - GrayAlpha8 Adam7 capability, geometry, output, work, and budget rejections expose neither eager bytes nor a caller-buffered lease and retain the caller budget.
    - Existing non-interlaced GrayAlpha8 admission coverage continues to use explicit `PngInterlaceStrategy::None`.
  </behavior>
  <action>D-02: cover the legal GrayAlpha8 Adam7 image in every compression/filter pair, mutating only after accepted framing/progress and proving first/later zero-write sticky failures. D-03: preserve the existing shared traversal, pass-local filter contexts, preflight ledger, and replay plans. D-04: prove GrayAlpha8 Adam7 admission is atomic for descriptor, capability, geometry, output, work, and budget rejection while retained None coverage remains frozen. Evolve the tracer helper into the phase-local `png_graya8_replay_mutation_is_sticky` matrix and add two exact generators: `png_stream_graya8_adam7_fixed_flat_5x5` must emit 5×5 pixels `(G, A) = (0x34, 0xa7)` for FixedOrStored, and `png_stream_graya8_adam7_dynamic_periodic_128x5` must emit 128×5 pixels with `i = 2*(128*y + x)`, `G = i mod 5`, and `A = (i + 1) mod 5` for DynamicOrFixedOrStored. In the named six-case test, pass `png_stream_graya8_adam7_stored_ramp_5x5` to Stored and assert `0x01` for None and Adaptive; pass `png_stream_graya8_adam7_fixed_flat_5x5` to FixedOrStored and assert `0x03` for None and Adaptive; pass `png_stream_graya8_adam7_dynamic_periodic_128x5` to DynamicOrFixedOrStored and assert `0x05` for None and Adaptive. Preserve each `prefix[43] & 0x07` assertion so fallback routes cannot satisfy the regression. For every pair, mutate only after the 44-byte acknowledged prefix, inspect every byte of each nonzero first and later sentinel lease, require `written() == 0UL`, retain the accepted total, and compare the two terminal errors with `png_chunk_test_same_error`. Adapt the existing GrayAlpha8 rejection helper to take `PngInterlaceStrategy`, make its eager and chunk constructors use their all-strategy GrayAlpha8 selectors, update its current callers to pass `None`, and add a named Adam7 test that passes `Adam7` through each existing incompatible descriptor, capability, geometry, output, work, and budget case. This task adds only phase-local evidence and must not add public all-target schedules, literal decode/wire proof, staging, alternate encoders, release automation, wrappers, or copied-source work. Commit the completed expansion as `test(60-01): cover GrayAlpha8 Adam7 bounded replay`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 replay mutations are sticky for every strategy pair' &amp;&amp; moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 strategy admission is atomic' &amp;&amp; moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 chunk all strategy parity' &amp;&amp; moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 strategy admission is atomic'</automated>
  </verify>
  <done>All six legal GrayAlpha8 Adam7 compression/filter selections prove their chosen replay route and pre-lease mutation behavior; the Adam7 and retained None admission matrices prove the same atomic shared preflight transaction without new encoder machinery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutable `ImageView` → admitted `PngEncodeMachine` | Caller-owned source data can change after preflight recorded its revision. |
| active encoder → caller-owned `MutByteLease` | A failed replay must not mutate memory owned by the caller. |
| descriptor/limits/budget → eager or chunk construction | Untrusted source facts and caller limits must be rejected before output or resource charge becomes observable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-01 | Tampering | `PngChunkEncoder::pull` | high | mitigate | Validate the captured source revision at the common Active pull seam before any `destination.set`, then store the existing selected-plan terminal error as sticky. |
| T-60-02 | Tampering | GrayAlpha8 Adam7 replay | medium | mitigate | Exercise all six public all-strategy selector pairs with post-prefix mutation, full sentinel-tail checks, accepted-only totals, and repeated-error equality. |
| T-60-03 | Denial of Service | profile-aware preflight | medium | mitigate | Retain and execute GrayAlpha8 Adam7 descriptor/capability/geometry/output/work/budget rejection assertions through the existing single preflight ledger. |
| T-60-SC | Tampering | dependency supply chain | low | accept | No package manager operation or third-party dependency is introduced in this code-and-test-only phase. |
</threat_model>

<verification>
- Run the four named native PNG regressions in Task 2 after the TDD RED/GREEN cycle.
- Run `moon -C modules/mb-image test png --target native --frozen` before writing the phase summary to detect regressions outside the focused selectors.
- Confirm the RED and GREEN commits exist with `git log --oneline --grep="^test(60-01)"` and `git log --oneline --grep="^feat(60-01)"`.
</verification>

<success_criteria>
- GRAYA8A7-02 is covered by one profile-neutral pre-lease revision check and six GrayAlpha8 Adam7 mutation cases.
- Stored, Fixed, and Dynamic selected-plan diagnostics remain the same on the first and later terminal pulls.
- Existing public GrayAlpha8 chunk/eager parity and non-interlaced admission coverage pass alongside the new Adam7 matrix.
- No source outside `stream_encode.mbt` and `stream_encode_test.mbt` changes during this phase plan.
</success_criteria>

<output>
Create `.planning/phases/60-bounded-adam7-streaming-semantics/60-01-SUMMARY.md` when done.
</output>
