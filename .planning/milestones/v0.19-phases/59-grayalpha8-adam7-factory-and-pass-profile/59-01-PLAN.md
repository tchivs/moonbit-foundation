---
phase: 59-grayalpha8-adam7-factory-and-pass-profile
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA8A7-01]
requirements_addressed: [GRAYA8A7-01]
must_haves:
  truths:
    - "A caller can explicitly select eager or caller-buffered Adam7 encoding for a legal packed straight-alpha U8 GrayAlpha image."
    - "The selected route emits PNG IHDR depth 8, colour type 4, methods 0, and Adam7 interlace method 1."
    - "All seven nonempty 5x5 Adam7 passes serialize each source pixel in PNG G,A component order through the single bounded encoder machine."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Additive public eager GrayAlpha8 Adam7 selector factories."
    - path: modules/mb-image/png/encode.mbt
      provides: "GrayAlpha8 Adam7 admission through the existing profile-aware traversal and scalar wire reads."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Additive public caller-buffered GrayAlpha8 Adam7 selector factories using the existing machine."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Focused eager Type-4/8 seven-pass G,A wire oracle and frozen interlace-0 regression."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Focused ordinary caller-buffered GrayAlpha8 Adam7 eager-parity regression."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: "new_graya8_with_all_strategies retains GrayAlpha8 and the selected PngInterlaceStrategy until profile-aware preflight."
      pattern: "new_graya8_with_all_strategies"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "new_graya8_with_all_strategies constructs PngEncodeMachine::new_with_profile with GrayAlpha8."
      pattern: "PngEncodeMachine::new_with_profile"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/encode_test.mbt
      via: "The Adam7 cursor reaches profile-aware scalar reads which must expose the test-owned G,A pass raster."
      pattern: "_png_adam7_candidate_byte"
---

<objective>
Deliver the first production-quality end-to-end GrayAlpha8 Adam7 path: explicit eager and caller-buffered factory selection, the existing bounded profile/machine, and an independent Type-4/8 pass-wire proof.

Purpose: satisfy GRAYA8A7-01 and locked decisions D-01 through D-04 without altering legacy non-interlaced selection or creating a second encoder route.
Output: additive `graya8` Adam7 factory pairs, a narrowly opened GrayAlpha8 profile gate, and one focused eager/chunk Stored/None tracer regression.

Locked decision coverage: D-01 adds only the mirrored eager and chunk `graya8` Adam7 factory families. D-02 routes GrayAlpha8 through the existing profile-aware machine after removing only its profile-specific interlace exclusion. D-03 proves the existing seven-pass geometry emits Type-4/8 samples in G,A order. D-04 retains legal packed straight-alpha sRGB/top-left admission and frozen interlace-method-0 legacy outputs.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-CONTEXT.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-RESEARCH.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-PATTERNS.md
@.planning/milestones/v0.18-phases/56-grayalpha16-adam7-factory-and-pass-profile/56-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and deliver one legal GrayAlpha8 Adam7 eager-to-chunk path</name>
  <reversibility rating="costly">The four exported selector spellings are additive public package contracts and should match the established GrayAlpha16 family exactly.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A legal 5x5 `ImageFormat::graya8()` fixture has distinct gray and alpha bytes at every coordinate, making all seven Adam7 passes nonempty and channel/order mistakes observable.
    - `PngEncoder::new_graya8_with_interlace_strategy(Adam7)` and `PngEncoder::new_graya8_with_all_strategies(Stored, None, Adam7)` produce equal Type-4/depth-8/Adam7 PNGs through the existing encoder record and machine.
    - A test-owned seven-pass coordinate enumeration produces Stored scanlines containing filter byte zero followed by G,A pairs for every selected source pixel, and equals the inflated IDAT raster.
    - Corresponding `PngChunkEncoder` selectors drain on the ordinary existing schedule to their fresh eager selector output and expose the same Type-4/depth-8/Adam7 IHDR identity.
    - Existing `new_graya8` and `new_graya8_with_strategies` paths remain Type-4/depth-8/interlace-0 and retain their frozen bytes.
  </behavior>
  <action>Start RED by adding focused eager and chunk regressions around a shared legal 5x5 `ImageFormat::graya8()` fixture. Give each coordinate distinct `G` and `A` bytes. In the eager test, construct an expected Stored raster independently by enumerating the seven Adam7 `(start_x, start_y, step_x, step_y)` tuples, appending filter byte zero per pass row, then the fixture's source G,A bytes for every selected pixel. Exercise the narrow and all-strategy eager selectors with Stored/None/Adam7; assert complete-byte parity, IHDR depth `0x08`, colour type `0x04`, methods `0`, interlace `0x01`, and equality between the expected raster and the bounded test unpacking of the stored IDAT payload. In the chunk test, construct fresh sources and use the established ordinary drain helper to compare both new chunk selectors to their corresponding fresh eager bytes and IHDR fields. After the tests fail, implement D-01 by adding the eager `PngEncoder::new_graya8_with_interlace_strategy` and `PngEncoder::new_graya8_with_all_strategies` pair immediately beside the existing GrayAlpha8 family, and identically shaped `PngChunkEncoder` factories. The narrow variants forward Stored/None; all-strategy variants preserve the caller's compression, filter, and interlace choice. Per D-04 retain every existing `new_graya8*` factory route with explicit `PngInterlaceStrategy::None` and its byte oracle. Per D-02 remove only the `GrayAlpha8` Adam7 restriction in the profile-aware preflight match; retain the Gray8 and Gray16 Adam7 restrictions, all current descriptor/capability/geometry/limit checks, and the single ledger. Route chunk construction only through `PngEncodeMachine::new_with_profile(... GrayAlpha8 ...)`; do not add a new machine, pass cursor, staging storage, decoder change, or target branch. Per D-03 leave `_png_adam7_passes` and the existing scalar `_png_wire_byte` path authoritative so ordinary U8 GrayAlpha8 coordinates are read as G then A before filter, compression, checksum, and replay.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 eager pass profile'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 chunk parity'; exit $LASTEXITCODE</automated>
  </verify>
  <done>Both additive eager and chunk GrayAlpha8 selector pairs use the established profile-aware machine; focused Stored/None output proves Type-4/depth-8/interlace-1 framing, all seven pass rows, and G,A sample order while legacy Type-4/8 interlace-0 bytes remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller ImageView to GrayAlpha8 profile admission | Caller-controlled descriptor and metadata must remain within the packed straight-alpha U8 sRGB/top-left contract before state creation. |
| Adam7 pass coordinate to PNG wire byte | Checked source samples cross from packed storage into the mandated Type-4/8 G,A PNG representation. |
| Public factory selection to PngEncodeMachine | Eager and caller-buffered selectors must feed one bounded, profile-aware construction path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-01 | Tampering | GrayAlpha8 profile gate | high | mitigate | Task 1 opens Adam7 only for the existing GrayAlpha8 profile and retains all established preflight checks. |
| T-59-02 | Tampering | Adam7 source-to-wire traversal | high | mitigate | Task 1 asserts a non-symmetric independent seven-pass G,A raster rather than encoder self-consistency. |
| T-59-03 | Tampering | legacy GrayAlpha8 constructors | high | mitigate | Task 1 preserves explicit None forwarding and frozen Type-4/8 interlace-0 byte assertions. |
| T-59-04 | Denial of Service | pass traversal | medium | mitigate | Task 1 retains scalar `_png_adam7_passes` traversal and the existing bounded PngEncodeMachine without pass/image staging. |
| T-59-05 | Denial of Service | caller-controlled source and limits | medium | mitigate | Task 1 routes both new public entry points through the current descriptor, geometry, output, work, and budget preflight ledger. |
</threat_model>

<verification>
Run the two named native tracer tests after Task 1. This plan proves the factory/profile/pass-wire seam; Phase 60 owns all-strategy atomic/replay semantics and Phase 61 owns hostile schedules, decode proof, and all-target qualification.
</verification>

<success_criteria>
- GRAYA8A7-01 exposes explicit eager and caller-buffered GrayAlpha8 Adam7 selectors for the existing legal packed straight-alpha U8 source contract.
- Stored/None Type-4/8 output proves Type-4/depth-8/interlace-1 framing and all seven G,A pass scanlines through the single machine.
- Existing non-interlaced GrayAlpha8 selection and frozen method-0 bytes remain intact.
</success_criteria>

## Source Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | Phase 59 | Explicit eager/chunk Type-4/8 Adam7 selection preserves frozen non-interlaced behavior. | 01 | COVERED |
| REQ | GRAYA8A7-01 | Explicit public factories for legal GrayAlpha8 with unchanged legacy bytes. | 01 Task 1 | COVERED |
| RESEARCH | Existing selector/profile/machine seams | Use GrayAlpha16 as exact selector analogue and remove only the GrayAlpha8 gate. | 01 Task 1 | COVERED |
| RESEARCH | Independent 5x5 wire oracle | Enumerate all seven Adam7 passes and assert literal G,A output. | 01 Task 1 | COVERED |
| RESEARCH | Avoid staging and separate cursors | Preserve `_png_adam7_passes`, scalar cursor, and PngEncodeMachine. | 01 Task 1 | COVERED |
| CONTEXT | D-01 | Mirrored additive eager/chunk selector names and strategy shape. | 01 Task 1 | COVERED |
| CONTEXT | D-02 | One profile-aware machine and only the GrayAlpha8 interlace restriction removed. | 01 Task 1 | COVERED |
| CONTEXT | D-03 | Type-4/8 G,A order through existing seven-pass geometry. | 01 Task 1 | COVERED |
| CONTEXT | D-04 | Legal packed straight-alpha admission and frozen interlace-0 output. | 01 Task 1 | COVERED |

<output>
Create `.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-01-SUMMARY.md` when done.
</output>
