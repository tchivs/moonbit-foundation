---
phase: 59-grayalpha8-adam7-factory-and-pass-profile
plan: "02"
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA8A7-01]
requirements_addressed: [GRAYA8A7-01]
must_haves:
  truths:
    - "Every public GrayAlpha8 Adam7 eager selector shape remains visibly Type-4/depth-8/interlace-1 across six legal compression/filter choices."
    - "Each caller-buffered selector shape has direct ordinary eager-byte parity without claiming the hostile caller or replay guarantees of later phases."
    - "The frozen non-interlaced GrayAlpha8 public vectors remain isolated from the new opt-in surface."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Six-pair GrayAlpha8 Adam7 eager framing coverage and legacy interlace-0 regression."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Selector-specific ordinary chunk/eager parity coverage."
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "Public all-strategy GrayAlpha8 Adam7 eager factory calls are checked for Type-4/depth-8/interlace-1 framing."
      pattern: "new_graya8_with_all_strategies"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Public narrow and all-strategy chunk factories drain to corresponding fresh eager selector output."
      pattern: "new_graya8_with_interlace_strategy"
---

<objective>
Harden the Phase 59 public factory/profile boundary with compact six-pair framing coverage and selector-specific ordinary eager/chunk parity, while leaving replay, atomic-admission matrices, hostile schedules, decode proof, and portability evidence in their assigned phases.

Purpose: make GRAYA8A7-01 independently understandable and regressible after the tracer delivers the production seam.
Output: named eager and caller-buffered public regressions that preserve the frozen GrayAlpha8 non-interlaced boundary.

Locked decision coverage: D-01 validates both public eager/chunk GrayAlpha8 selector shapes. D-02 confirms they continue to use the profile-aware shared construction. D-03 confirms each legal factory framing is Type-4/8 Adam7 and retains the tracer's G,A oracle. D-04 retains existing legal-source and frozen interlace-0 coverage.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-CONTEXT.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-RESEARCH.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-PATTERNS.md
@.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-01-SUMMARY.md
@.planning/milestones/v0.18-phases/56-grayalpha16-adam7-factory-and-pass-profile/56-02-SUMMARY.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover every legal GrayAlpha8 Adam7 eager factory framing pair</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - The all-strategy eager GrayAlpha8 Adam7 factory accepts Stored, FixedOrStored, and DynamicOrFixedOrStored with both None and Adaptive filters for a legal 5x5 source.
    - Every resulting PNG declares depth 8, colour type 4, and interlace method 1; the Stored/None path retains the tracer's exact independent G,A pass raster assertion.
    - Existing default and configured non-interlaced GrayAlpha8 eager factories still emit their frozen Type-4/8 method-0 bytes.
  </behavior>
  <action>Write and run a focused RED regression which reuses the Phase 59 5x5 fixture and known public factory APIs. Iterate the three existing `PngCompressionStrategy` choices and both `PngFilterStrategy` choices through `PngEncoder::new_graya8_with_all_strategies(..., Adam7)`, encode each through the ordinary eager public route, and assert IHDR depth `0x08`, colour type `0x04`, methods `0`, and interlace `0x01`. Keep the Stored/None expected-raster assertion from Plan 01 as the independent proof of D-03 rather than duplicating compression internals. Confirm the narrow selector remains byte-identical to the all-strategy Stored/None selector. Per D-04 retain the existing literal non-interlaced GrayAlpha8 byte vector and method-0 assertions, without altering old factory signatures or tests. This is factory framing coverage only: do not add source-mutation, budget-admission, replay, or decoder checks.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 eager'; exit $LASTEXITCODE</automated>
  </verify>
  <done>All six legal eager GrayAlpha8 Adam7 selector combinations have Type-4/depth-8/interlace-1 framing evidence; the independent Stored/None G,A oracle and frozen non-interlaced vector remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make both GrayAlpha8 Adam7 chunk selector routes explicitly comparable</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Fresh narrow and all-strategy GrayAlpha8 Adam7 chunk encoders drain through the ordinary established schedule to their matching fresh eager selector bytes.
    - The narrow/all-strategy Stored/None chunk outputs agree and have Type-4/depth-8/interlace-1 framing.
    - The regression does not use zero-capacity, one-byte, ragged schedules, lease-tail ownership assertions, source mutation, or sticky replay behavior.
  </behavior>
  <action>Write the named chunk selector parity test first and execute it RED if the exact public pairs are not already covered. Build fresh legal 5x5 sources for the narrow `PngChunkEncoder::new_graya8_with_interlace_strategy(Adam7, ...)` and all-strategy `new_graya8_with_all_strategies(Stored, None, Adam7, ...)` routes. Drain each only with the ordinary established helper, compare to its corresponding fresh eager factory output, compare narrow/all-strategy Stored/None bytes, and assert their IHDR Type-4/depth-8/interlace-1 fields. Per D-01 and D-02, use public selectors and the existing machine path exclusively; do not change `PngChunkEncoder`, `PngEncodeMachine`, output accounting, or profile admission. Per D-04 preserve current method-0 GrayAlpha8 chunk vector checks. Keep caller-buffer ownership schedules, all-strategy chunk semantics, atomic admission, and mutation/replay coverage for Phases 60-61.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha8 Adam7 chunk parity'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen; exit $LASTEXITCODE</automated>
  </verify>
  <done>Both public GrayAlpha8 Adam7 chunk selector shapes have ordinary eager-byte parity and Type-4/depth-8/interlace-1 framing evidence; the native PNG suite still protects existing routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legal GrayAlpha8 view to public selector | Test coverage must distinguish the opt-in public Adam7 surface from frozen default/non-interlaced factories. |
| Public all-strategy factory to emitted PNG | Caller-supplied compression/filter values must retain Type-4/8 Adam7 identity on the existing shared route. |
| Caller-buffered selector to complete output | Chunk construction must preserve profile/interlace choice through ordinary drain parity with an independently built eager peer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-06 | Tampering | eager all-strategy selector | high | mitigate | Task 1 covers the six legal compression/filter combinations and checks exact Type-4/8/Adam7 framing. |
| T-59-07 | Tampering | chunk selector construction | high | mitigate | Task 2 compares both public chunk selector forms with fresh matching eager peers. |
| T-59-08 | Tampering | G,A pass representation | high | mitigate | Task 1 preserves the Plan 01 independent seven-pass Stored/None raster assertion. |
| T-59-09 | Tampering | frozen method-0 route | high | mitigate | Tasks 1-2 retain existing GrayAlpha8 non-interlaced byte and framing assertions. |
</threat_model>

<verification>
Run the focused eager family tests, focused chunk parity test, and complete native PNG package suite. Phase 60 owns bounded preflight/replay proof; Phase 61 owns public hostile schedules, decoding, frozen cross-format vectors, and all-target qualification.
</verification>

<success_criteria>
- GRAYA8A7-01 has explicit public eager/chunk selector proof at the narrow and all-strategy Stored/None surface.
- Every legal all-strategy eager selection retains Type-4/depth-8/interlace-1 framing on the shared profile route.
- Existing GrayAlpha8 non-interlaced factories and byte vectors remain observable method-0 compatibility baselines.
</success_criteria>

## Source Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | Phase 59 | Explicit eager/chunk Adam7 factory behavior must remain testable without changing old routes. | 01-02 | COVERED |
| REQ | GRAYA8A7-01 | Legal public Type-4/8 Adam7 factories and unchanged existing bytes. | 01-02 Tasks 1-2 | COVERED |
| RESEARCH | Factory selector parity | Exercise narrow and all-strategy public surfaces with fresh eager/chunk peers. | 02 Tasks 1-2 | COVERED |
| RESEARCH | Six legal framing choices | Check all compression/filter pairs without moving replay/admission work forward. | 02 Task 1 | COVERED |
| RESEARCH | Legacy method-0 compatibility oracle | Retain literal GrayAlpha8 byte/framing coverage. | 02 Tasks 1-2 | COVERED |
| CONTEXT | D-01 | Mirrored eager/chunk GrayAlpha8 selector evidence. | 02 Tasks 1-2 | COVERED |
| CONTEXT | D-02 | Existing profile-aware shared construction is exercised rather than a second route. | 02 Tasks 1-2 | COVERED |
| CONTEXT | D-03 | Retain Type-4/8 Adam7 framing and Plan 01's G,A oracle. | 02 Task 1 | COVERED |
| CONTEXT | D-04 | Frozen legal admission and method-0 compatibility remain visible. | 02 Tasks 1-2 | COVERED |

<output>
Create `.planning/phases/59-grayalpha8-adam7-factory-and-pass-profile/59-02-SUMMARY.md` when done.
</output>
