---
phase: 48-bounded-gray16-encoder-path
plan: "01"
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [GRAY16-02]
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
---

<objective>
Route explicit Gray16 PNG encoding through the existing bounded filter, Stored/Fixed/Dynamic planning, preflight, and acknowledgement-safe replay machinery, while preserving PNG type-0/16-bit big-endian wire bytes and every legacy/Gray8 contract.

Purpose: replace the Phase 47 transitional Gray16 Stored/None bypass with one profile-aware scalar wire-byte stream that every planner and replay cursor consumes.
Output: explicit Gray16 strategy factory families, a bounded profile-aware producer, and native regressions for the six supported strategy pairs plus atomic/sticky behavior.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-bounded-gray16-encoder-path/48-CONTEXT.md
@.planning/phases/48-bounded-gray16-encoder-path/48-RESEARCH.md
@.planning/phases/47-gray16-factory-compatibility/47-CONTEXT.md
@.planning/phases/47-gray16-factory-compatibility/47-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/storage/views.mbt
</context>

<source_audit>
SOURCE | ID | Feature / constraint | Coverage | Status
ROADMAP | GRAY16-02 | One bounded None/Adaptive and Stored/FixedOrStored/DynamicOrFixedOrStored Gray16 path, atomic admission, accepted-byte-only caller replay. | Tasks 1-2 | COVERED
REQ | GRAY16-02 | Existing bounded preflight, all strategy pairs, no observable output before rejection, and atomic unsupported Gray16 Adam7/noncanonical input. | Tasks 1-2 | COVERED
CONTEXT | D-01 | Mirror all explicit Gray8 eager/chunk compression-only, filter-only, and combined factories with Gray16 names; legacy constructors and Gray16 Adam7 remain unchanged/unavailable. | Task 1 | COVERED
CONTEXT | D-02 | Every filter, match, checksum, plan, and replay byte is profile-aware; U16 source storage becomes high-byte/low-byte PNG wire order with scalar cursors only. | Task 1 | COVERED
CONTEXT | D-03 | Reuse the shared ledger with a two-byte Gray16 filter stride; capability, geometry, output, work, budget, and Adam7 failures are atomic. | Tasks 1-2 | COVERED
CONTEXT | D-04 | Native evidence covers all six pairs, type-0/16-bit wire order, eager/chunk identity, atomic rejection, and sticky replay; hostile schedules/four targets defer to Phase 49. | Task 2 | COVERED
RESEARCH | wire producer | Remove `gray16-stored-none-required` and `gray16_stored_none`; pass profile through filtered/match/planning/replay traversal. | Task 1 | COVERED
</source_audit>

<artifacts_this_phase_produces>
- `PngEncoder::new_gray16_with_compression_strategy`, `PngEncoder::new_gray16_with_filter_strategy`, and `PngEncoder::new_gray16_with_strategies`.
- `PngChunkEncoder::new_gray16_with_compression_strategy`, `PngChunkEncoder::new_gray16_with_filter_strategy`, and `PngChunkEncoder::new_gray16_with_strategies`.
- A private profile-aware raw/wire-byte seam carried by `PngFilteredCursor`, `PngFilteredMatchCursor`, every Fixed/Dynamic planner traversal, and `PngEncodeMachine` replay cursors.
- Native Gray16 bounded-strategy, atomic-admission, and sticky-replay regressions.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD tracer for the profile-aware Gray16 bounded encoder path</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/png.mbt — copy the complete Gray8 eager factory family and preserve legacy constructors.
    - modules/mb-image/png/stream_encode.mbt — copy the Gray8 caller-buffered factory family, inspect `PngEncodeMachine::new_with_profile`, `scanline_byte`, and Fixed/Dynamic cursor initialization.
    - modules/mb-image/png/encode.mbt — inspect `_png_encode_preflight_with_interlace_profile`, `gray16_stored_none`, `_png_fixed_scanline_byte`, `_png_filter_image_*`, `PngFilteredCursor`, `PngFilteredMatchCursor`, and every Fixed/Dynamic planner traversal.
    - modules/mb-image/png/encode_test.mbt and modules/mb-image/png/stream_encode_test.mbt — reuse the existing Gray8 factory, adaptive-filter, admission, and sticky-replay helpers.
    - modules/mb-image/storage/views.mbt — retain `ImageView::get_component_byte` as the checked U16 source accessor; do not change storage APIs.
  </read_first>
  <behavior>
    - A packed, top-left, tightly packed `ChannelOrder::Gray` / `ComponentType::U16` image can use the new eager and chunk Gray16 combined factory with `Stored` plus `Adaptive` and produces the same complete PNG bytes through both APIs.
    - The tracer PNG keeps IHDR bit depth `0x10`, colour type `0x00`, interlace `0x00`, and emits a non-symmetric U16 sample in PNG high-byte/low-byte order independently of source endianness.
    - For a repeated U16 sample row, the adaptive stored stream treats `2UL` as the filter byte stride: Sub compares a byte only with the matching byte of the preceding U16 sample, not with the other byte of the same sample.
  </behavior>
  <action>
    Implement **D-01:** by mirroring all three explicit Gray8 eager and caller-buffered strategy-factory families with `gray16` names while keeping legacy constructors unchanged and Gray16 Adam7 unavailable. Implement **D-02:** by making the filtered-byte producer profile-aware from packed U16 component storage through planning and replay, with high-byte/low-byte PNG order, scalar cursors, and no converted rows or image-sized staging. Implement **D-03:** by retaining the shared preflight ledger and using `2UL` as the Gray16 filter byte stride so capability, geometry, output, work, budget, and Adam7 failures remain atomic. Task 2 supplies **D-04** native contract evidence.
  </action>
  <implementation>
    1. RED: add focused native tests for the six new public Gray16 factory names, then make the tracer call `PngEncoder::new_gray16_with_strategies(Stored, Adaptive)` and `PngChunkEncoder::new_gray16_with_strategies(...)`. Use a little-endian and a big-endian U16 Gray fixture with non-symmetric values. Assert eager/chunk equality, IHDR bytes `[24]=0x10`, `[25]=0x00`, `[28]=0x00`, and Stored-filtered payload bytes in the order `filter-tag`, high byte, low byte for each sample. The test must fail before implementation because the factory names and profile-aware adaptive path do not exist.
    2. GREEN: add the three Gray16 eager factories and the three Gray16 caller-buffered factories beside their Gray8 counterparts. Compression-only chooses filter None, filter-only chooses Stored, and combined takes both strategies; all bind `PngEncodeProfile::Gray16` and `PngInterlaceStrategy::None`. Do not add a Gray16 interlace factory and do not alter `new()` or any legacy/Gray8 factory.
    3. Replace the Gray16 special Stored/None emission bypass with one private profile-aware wire-byte helper. For Gray16, map `ImageView::get_component_byte(pixel, row, 0UL, storage_byte)` to PNG big-endian order (`Little` reads storage component bytes 1 then 0; `Big` reads 0 then 1). Legacy and Gray8 continue to use their existing `get_byte` semantics.
    4. Carry `PngEncodeProfile` into `PngFilteredCursor` and `PngFilteredMatchCursor`, their constructors, `_png_fixed_scanline_byte`, `_png_filtered_scanline_byte`, `_png_filter_image_raw_byte`, candidate/winner helpers, and every call that creates a planner or replay cursor. Pass `facts.profile` from `PngEncodeMachine::new_with_profile` into stored, Fixed, and Dynamic cursor state so planning, filter scoring, DEFLATE matching, Adler/fingerprint computation, preview, and acknowledgement replay all read the same wire bytes.
    5. Remove only the transitional Gray16 `gray16-stored-none-required` rejection and `gray16_stored_none` planning/replay exceptions. Retain `gray16-noninterlaced-required`, `_png_encode_source` profile validation, one preflight charge point, the fixed 262-byte match window, and no retained converted rows or image-sized staging.
  </implementation>
  <acceptance_criteria>
    - `PngEncoder::new_gray16_with_compression_strategy`, `PngEncoder::new_gray16_with_filter_strategy`, `PngEncoder::new_gray16_with_strategies`, and their three `PngChunkEncoder` counterparts exist and each uses `PngEncodeProfile::Gray16` plus `PngInterlaceStrategy::None`.
    - `modules/mb-image/png/encode.mbt` no longer contains `gray16-stored-none-required` or `gray16_stored_none`, while it retains `gray16-noninterlaced-required`.
    - Every `PngFilteredCursor` / `PngFilteredMatchCursor` construction in Gray16 planning and replay receives the profile-aware wire-byte source; no branch can return to `_png_gray16_scanline_byte` only for Stored output.
    - The tracer tests demonstrate byte-identical eager/chunk output, exact Gray16 IHDR fields, high-byte/low-byte output for both source endiannesses, and an Adaptive Sub residual calculated with byte stride `2UL`.
    - `moon -C modules/mb-image test png --target native --frozen` passes after GREEN.
  </acceptance_criteria>
  <verify>
    `moon -C modules/mb-image test png --target native --frozen`
  </verify>
  <done>Gray16's public strategy surfaces and one Stored/Adaptive end-to-end tracer use the same bounded profile-aware producer from source U16 storage through eager/chunk output, with no legacy behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the native Gray16 six-pair, atomicity, and sticky-replay contract</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/encode_test.mbt — extend the existing `png_encode_gray16_image` fixture and Gray8 factory-strategy test shape without adding public decoder scope owned by Phase 49.
    - modules/mb-image/png/stream_encode_test.mbt — reuse `png_gray8_combined_public_reject`, one-byte pull helpers, sentinel leases, and Gray8 Fixed/Adaptive/Dynamic sticky replay patterns.
    - modules/mb-image/png/png.mbt and modules/mb-image/png/stream_encode.mbt — verify the exact eager/chunk Gray16 factory signatures established by Task 1.
    - modules/mb-image/png/encode.mbt — verify the common preflight continues to reject Gray16 Adam7 and invalid source facts before output/budget mutation.
  </read_first>
  <action>
    Complete **D-04:** with native regression helpers and tests that iterate the complete Cartesian product of `Stored`, `FixedOrStored`, and `DynamicOrFixedOrStored` with `None` and `Adaptive` through both `PngEncoder::new_gray16_with_strategies` and `PngChunkEncoder::new_gray16_with_strategies`. Use U16 Gray fixtures containing non-symmetric component bytes and a repeated-row corpus that makes the two-byte filter stride observable. Assert complete eager/chunk identity and type-0/16-bit/non-interlaced IHDR on every pair; assert the Stored/None and Stored/Adaptive payloads preserve high-byte/low-byte order, and use existing fixed/dynamic corpus conventions to prove each selected compression replay consumes the same wire stream.

    Mirror `png_gray8_combined_public_reject` as a Gray16 helper over all six pairs. Cover invalid profile/capability input, width/geometry limit, output limit, work limit, and caller budget exhaustion; additionally invoke the private Gray16 Adam7 construction/preflight seam so `gray16-noninterlaced-required` is rejected before eager writer position, chunk encoder result, caller lease bytes, or budget remaining changes.

    Mirror the existing Gray8 fixed/adaptive replay-mutation tests with `set_component_byte` on a U16 Gray source after framing bytes have been accepted. For FixedOrStored and DynamicOrFixedOrStored (including an Adaptive case), require the first drift failure to report `written()==0UL`, keep `total_written()` at accepted progress, leave the current and later sentinel leases unchanged, and return an identical terminal error on the next pull. Do not add zero/one/ragged schedule matrices or js/wasm/wasm-gc runs; Phase 49 owns them.
  </action>
  <acceptance_criteria>
    - Native tests exercise exactly six Gray16 compression/filter pairs: three compression strategies times `None` and `Adaptive`.
    - Each pair proves eager/chunk complete-byte identity and IHDR bit depth `0x10`, colour type `0x00`, and interlace `0x00`; Stored payload assertions prove high-byte/low-byte U16 wire order and a two-byte adaptive stride.
    - Rejection tests prove profile/capability, geometry, output, work, budget, and private Gray16 Adam7 failures leave eager writer output at zero, return no usable chunk encoder, leave lease sentinels untouched, and preserve the caller budget ledger.
    - FixedOrStored and DynamicOrFixedOrStored Gray16 replay-drift tests prove accepted-byte-only progress, zero-byte sticky failures, identical repeat errors, and untouched later leases.
    - No test introduces Phase 49's zero/one/ragged capacity schedules, public decode-canonicalization claims, or non-native target runs.
    - `moon -C modules/mb-image test png --target native --frozen` passes.
  </acceptance_criteria>
  <verify>
    `moon -C modules/mb-image test png --target native --frozen`
  </verify>
  <done>Native evidence covers every Phase 48 bounded Gray16 strategy pair and proves atomic admission plus acknowledgement-safe sticky replay without expanding Phase 49's portable public-evidence scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| packed U16 source storage -> PNG wire stream | Caller-controlled storage order must become standards-required high-byte/low-byte wire bytes without a divergent planner or replay view. |
| constructor admission -> writer/caller lease | Invalid profile, interlace, geometry, output, work, and budget requests must fail before observable output or mutable caller state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-48-01 | Tampering | profile-aware filtered/match producer | medium | mitigate | Task 1 routes every filter, planner, checksum, and replay byte through one Gray16-aware high/low wire source; Task 2 asserts exact Stored wire bytes and cross-route identity. |
| T-48-02 | Denial of service | Gray16 preflight and filter/compression planning | medium | mitigate | Task 1 retains the single shared admission ledger, scalar cursors, and fixed matcher window; Task 2 proves output/work/budget failures are atomic for all pairs. |
| T-48-03 | Repudiation | caller-buffered replay state | low | mitigate | Task 2 verifies accepted-progress accounting, zero-byte failure, repeated terminal error, and untouched later leases after U16 source mutation. |
</threat_model>

<must_haves>
<truths>
  - "D-01: Explicit Gray16 eager and caller-buffered compression-only, filter-only, and combined factories mirror Gray8 names while legacy constructors stay unchanged and Gray16 Adam7 stays unavailable."
  - "D-02: Every Gray16 filter, match, Fixed/Dynamic plan, checksum, and replay traversal consumes the same profile-aware high-byte/low-byte PNG wire stream using scalar cursors and no converted-row or image-sized staging."
  - "D-03: All six Gray16 strategy pairs use the shared preflight ledger with byte stride 2UL; capability, geometry, output, work, budget, and Gray16 Adam7 failures are atomic before writer bytes or a usable chunk lease."
  - "D-04: Native tests cover all six Gray16 strategy pairs, type-0/16-bit wire order, eager/chunk equality, atomic rejection, and sticky replay; hostile capacity schedules and four-target execution remain Phase 49 work."
  - "A valid non-interlaced Gray16 image supports None/Adaptive filtering and Stored/FixedOrStored/DynamicOrFixedOrStored selection through one bounded path."
  - "Caller pulls report only accepted bytes and retain terminal sticky replay semantics for Gray16 Fixed/Dynamic drift failures."
</truths>
<prohibitions>
  - statement: "Do not add Gray16 Adam7, Gray+alpha, RGB/RGBA16, palette/low-bit output, transparency conversion, or implicit Gray16 legacy selection."
    status: resolved
    verification: "Public-factory source assertions plus native PNG tests"
  - statement: "Do not retain converted rows, image-sized staging, a second encoder, or a separate Gray16 stream driver."
    status: resolved
    verification: "Profile-aware cursor/source review and bounded native tests"
  - statement: "Do not add zero/one/ragged capacity matrices, public decode-canonicalization claims, or js/wasm/wasm-gc evidence in Phase 48."
    status: resolved
    verification: "Scoped test and command review"
</prohibitions>
</must_haves>

<verification>
1. Run `moon -C modules/mb-image test png --target native --frozen` after the RED/GREEN tracer and after the completion regressions.
2. Confirm `rg -n "gray16-stored-none-required|gray16_stored_none" modules/mb-image/png` returns no matches, while `gray16-noninterlaced-required` remains in preflight.
3. Inspect the scoped diff: only `png.mbt`, `encode.mbt`, `stream_encode.mbt`, `encode_test.mbt`, and `stream_encode_test.mbt` change; no storage API, roadmap, requirements, or Phase 49 evidence changes are included.
4. Inspect the final test names/assertions for the exact six strategy pairs, `2UL` filter stride, both-endian high/low wire ordering, atomic rejection, accepted-progress accounting, sticky error equality, and untouched lease sentinels.
</verification>

<success_criteria>
- Gray16 has explicit eager and caller-buffered compression-only, filter-only, and combined strategy factory families that remain non-interlaced.
- All allowed Gray16 pairs use one profile-aware bounded wire producer and shared preflight/replay machine, with high-byte/low-byte PNG sample ordering and a two-byte filter stride.
- Invalid Gray16 construction remains atomic, and fixed/dynamic replay preserves accepted-byte-only progress plus sticky terminals.
- Native PNG tests pass without adding Phase 49 hostile-capacity, public-decode, or four-target evidence.
</success_criteria>
