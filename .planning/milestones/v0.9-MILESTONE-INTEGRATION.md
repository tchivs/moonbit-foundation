# v0.9 Resumable PNG Encode — Milestone Integration Audit

**Audited:** 2026-07-21
**Scope:** Phases 29–31 and requirements `PNGE-01` through `PNGE-05`
**Status:** PASS — **5/5 requirements wired end-to-end; 0 blockers; 0 warnings**

## Integration Check Complete

### Wiring Summary

**Connected:** 4 required phase-boundary links verified in source and exercised by runtime evidence.
**Orphaned:** 0 phase-owned exports.
**Missing:** 0 expected connections.

| From | To | Verified connection | Status |
|---|---|---|---|
| Phase 29 private substrate | Phase 30 public API | `PngChunkEncoder::new` in `stream_encode.mbt` constructs exactly one `PngEncodeMachine`; the machine calls the sole `_png_encode_preflight` admission path. | WIRED |
| Phase 29 canonical source | Eager compatibility facade | `PngEncoder::encode` also constructs and drains `PngEncodeMachine`, so the public pull API and eager oracle share preflight and byte generation rather than parallel encoders. | WIRED |
| Phase 30 public encoder | Phase 31 hostile evidence | The selected public test constructs `PngChunkEncoder`, supplies callback-scoped leases to `pull`, and is selected by `Invoke-PngEncodeEvidence.ps1` for each target. | WIRED |
| Phase 30 public encoder | Phase 31 portable consumer and lane | `png-portable` calls `PngChunkEncoder::new` and `pull`; the PNG quality lane executes it once per required target and exact-matches its sole evidence line. | WIRED |

### API Coverage

There are no HTTP/API routes in this library milestone. The relevant public API is `PngChunkEncoder`.

| Public contract | Consumer(s) | Status |
|---|---|---|
| `PngChunkEncoder::new` / `pull` / pull result | Public black-box encoder evidence; `examples/png-portable/main/main.mbt` | CONSUMED |
| `PngChunkDecoder::new` / `push` / `finish` | `examples/png-portable/main/main.mbt` before the encode leg | CONSUMED |
| `resize_bilinear` | `examples/png-portable/main/main.mbt` between decoded image and chunk encoder | CONSUMED |

### Auth Protection

Not applicable: this is a portable MoonBit library with no authentication, routes, or user/session boundary.

### E2E Flows

**Complete:** 2
**Broken:** 0

1. **Public caller-buffered PNG encode** — `PngChunkEncoder::new` delegates to Phase 29 preflight before any lease exists; `pull` performs `present → destination.set → acknowledge`, updates exact totals only after acknowledgement, and persists sticky `Finished`/original `Failed`. The Phase 31 hostile schedules proved eager parity, preflight parity, terminal non-mutation, and sticky behavior on `js`, `wasm`, `wasm-gc`, and `native`.
2. **Portable public decode → resize → chunk-encode** — the executable feeds 16 public decoder chunks, calls `finish`, runs public bilinear resize, drains the public encoder through reusable caller-owned output, then checks 78 exact bytes and digest `626208771`. Direct runs on all four targets printed the one required evidence line.

### Runtime Evidence Re-run

The following audit-time scoped checks passed:

- `Invoke-PngEncodeEvidence.ps1 -Target js|wasm|wasm-gc|native`: **4/4 tests passed per target**.
- `moon -C examples/png-portable run main --target js|wasm|wasm-gc|native --frozen`: every target emitted the identical 78-byte, 14-pull, digest-`626208771` line.

The existing Phase 31 verification also records a passing scoped PNG lane with all four PNG suites. The lane was not rerun in this audit because its recorded execution takes several minutes; the direct target traces above re-exercise each cross-phase runtime handoff.

### Detailed Findings

#### Export/Import and Data-Flow Trace

| Chain | End-to-end trace | Status |
|---|---|---|
| Private preflight to public construction | `PngChunkEncoder::new` → `PngEncodeMachine::new` → `_png_encode_preflight` → source/capability/dimension/limit/budget admission | WIRED |
| Canonical bytes to caller buffer | private `present()` → current `MutByteLease.set()` → matching `acknowledge()` → `machine.completed()` → `PngChunkPullResult` | WIRED |
| Eager parity | `PngEncoder::encode` drains the same `PngEncodeMachine` through its Writer adapter; Phase 31 compares public chunk aggregates with this eager oracle | WIRED |
| Public workflow to quality gate | `PngChunkDecoder.push/finish` → `resize_bilinear` → `PngChunkEncoder.pull` → exact output line → `Invoke-PngQualityLane` comparison for four targets | WIRED |

#### Orphaned Exports

None. The Phase 29 substrate is consumed by both eager and chunked facades; the Phase 30 public contract is consumed by Phase 31 tests and the sole portable executable.

#### Missing Connections

None.

#### Broken Flows

None.

#### Unprotected Routes

Not applicable.

#### Tech Debt / Informational Items

- `examples/png-portable/main/moon.pkg` still lists `tchivs/mb-core/io`, although Phase 31 removed the example's eager `MemoryWriter` path. MoonBit reports it as unused. This does not affect the public flow, evidence, or requirements; remove it in a scoped manifest-cleanup task.

### Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| PNGE-01 | Phase 29 `_png_encode_preflight` → `PngEncodeMachine::new` → Phase 30 `PngChunkEncoder::new` (also Phase 29 eager facade) | WIRED | — |
| PNGE-02 | Phase 29 machine `present/acknowledge` → Phase 30 `pull` current caller lease → Phase 31 hostile schedule collector/eager parity | WIRED | — |
| PNGE-03 | Phase 29 canonical emitter/eager oracle → Phase 30 persistent `Finished`/`Failed` lifecycle → Phase 31 sentinel and replay evidence | WIRED | — |
| PNGE-04 | Phase 30 public contract → Phase 31 selected black-box test → target-isolated evidence runner on js/wasm/wasm-gc/native | WIRED | — |
| PNGE-05 | Public `PngChunkDecoder` → `finish` → `resize_bilinear` → public `PngChunkEncoder` → four-target `png-portable` output → PNG lane exact match | WIRED | — |

**Requirements with no cross-phase wiring:** None. Every v0.9 requirement crosses at least one completed phase boundary and has executable consumer evidence.

### Audit Verdict

The phases are not silos. Phase 29 is the sole admission/canonical-byte substrate, Phase 30 calls it through the published caller-buffered lifecycle, and Phase 31 invokes that public lifecycle both under hostile schedules and in the four-target decode-process-encode consumer. No integration blocker was found.
