---
phase: 95-shared-svg-geometry-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - modules/mb-svg/svg/lower_wbtest.mbt
  - modules/mb-svg/svg/svg_test.mbt
autonomous: true
requirements:
  - SVGUNI-01
must_haves:
  truths:
    - "Library users retain valid parse-to-drawing-list behavior for transforms, viewBox mapping, shapes, and paths."
    - "Finite scale(0), omitted defaults, structured parser errors, opacity ordering, and the 16-layer boundary retain their established behavior."
    - "The total lowerer has deterministic behavior for manually constructed invalid SceneNode values without introducing a public error API."
  artifacts:
    - path: "modules/mb-svg/svg/lower_wbtest.mbt"
      provides: "Semantic drawing-list and total-lowerer compatibility controls."
    - path: "modules/mb-svg/svg/svg_test.mbt"
      provides: "Public valid-SVG parser-to-lowerer compatibility test."
  key_links:
    - from: "modules/mb-svg/svg/svg_test.mbt"
      to: "modules/mb-svg/svg/scene.mbt"
      via: "public parse_svg success is lowered with lower_to_drawing_list"
      pattern: "parse_svg.*lower_to_drawing_list"
    - from: "modules/mb-svg/svg/lower_wbtest.mbt"
      to: "modules/mb-svg/svg/lower.mbt"
      via: "direct drawing-list operation assertions exercise total adapters and unchanged layer order"
      pattern: "lower_to_drawing_list"
---

<objective>
Lock the completed shared seam behind semantic white-box and public compatibility controls, then prove the package still behaves identically on every supported target.

Purpose: Ensure the mechanical consolidation cannot change valid SVG output, parser error timing, total-lowerer behavior, or RFC 0008 opacity/layer semantics.
Output: Focused lowerer compatibility tests, public valid-fixture regression coverage, and a passing four-target package suite.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-shared-svg-geometry-boundary/95-CONTEXT.md
@.planning/phases/95-shared-svg-geometry-boundary/95-RESEARCH.md
@.planning/phases/95-shared-svg-geometry-boundary/95-PATTERNS.md
@.planning/phases/95-shared-svg-geometry-boundary/95-01-SUMMARY.md
@docs/rfcs/0008-mb-canvas-layer.md
</context>

<source_audit>
| Source | Item | Coverage in this plan |
|---|---|---|
| GOAL | Retain established valid SVG scene/drawing-list behavior. | Public and operation-level fixtures assert parse-to-lower behavior rather than implementation text. |
| REQ | SVGUNI-01. | Verifies valid transform/viewBox/shape/path compatibility after both consumers use the seam. |
| RESEARCH | Preserve public errors, finite singular transforms, opacity/layers, and four-target behavior. | Tests and final portable command retain each sentinel. |
| CONTEXT | D-02 through D-05. | Actions cite public totality, numeric compatibility, rendering boundary, and semantic regression style. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert deterministic total lowering and unchanged semantic operation forms</name>
  <files>modules/mb-svg/svg/lower_wbtest.mbt</files>
  <read_first>
    - modules/mb-svg/svg/lower.mbt: `lower_to_drawing_list`, `lower_node`, `emit_shape`, and the private total adapters from Plan 95-01
    - modules/mb-svg/svg/lower_wbtest.mbt: existing viewBox, rounded-rectangle, ellipse, path, opacity, and layer operation assertions
    - modules/mb-svg/svg/geometry.mbt: checked fact function names and their identity/empty fallback call sites
    - modules/mb-svg/svg/portable_qualification_wbtest.mbt: existing nested opacity and 16-success/17-atomic-failure sentinels
  </read_first>
  <behavior>
    - Valid meet/slice/none root mapping, rounded rectangle, 32-sample ellipse, point-list, and path fixtures retain selected affine components and Path2 command forms.
    - A manually constructed invalid `SceneNode` has a stable non-panicking drawing-list result across repeated lowerings: failed root facts use identity and failed primitive/path facts use an empty Path2 while paint/layer sequencing stays owned by `emit_shape`.
    - Group and element opacity retain PushTransform/PushLayer/fill-or-stroke/PopLayer/PopTransform ordering and do not change the renderer's 16-layer boundary.
  </behavior>
  <action>
    Add focused `lower_wbtest.mbt` cases that lower valid compact fixtures covering the shared root map and each supported geometry representation, asserting concrete affine components, Path2 command counts/forms, and DrawOp ordering. Reuse the existing operation inspection style; do not use snapshots or source-text duplication checks, per D-05.

    Construct representative invalid public `SceneNode` values directly and lower each twice. Assert that the DrawingList is returned both times with equal observable operation structure, that an invalid root uses the documented identity-map recovery, and that invalid primitive/path geometry supplies an empty Path2 without moving `emit_shape` or layer branches. This verifies the deterministic total-lowerer policy required by D-02 without adding a public Result. Keep group/element opacity and the 16-layer capability behavior unchanged, per D-04.
  </action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target native --frozen -f '*lower*'</automated>
  </verify>
  <acceptance_criteria>Focused lowerer controls prove valid semantic operations remain stable and directly prove deterministic non-panicking lowering for invalid manually constructed scenes; RFC 0008 layer operation order remains intact.</acceptance_criteria>
  <done>The lowerer compatibility suite passes with explicit coverage of the total adapters.</done>
  <reversibility rating="costly">The total-lowerer fallback is a compatibility policy for public SceneNode construction under D-02.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public valid-SVG compatibility coverage and run the portable regression gate</name>
  <files>modules/mb-svg/svg/svg_test.mbt</files>
  <read_first>
    - modules/mb-svg/svg/svg_test.mbt: public parser error helpers and existing finite-boundary/scale(0) tests
    - modules/mb-svg/svg/conformance_wbtest.mbt: compact supported transform/viewBox/shape/path fixtures
    - modules/mb-svg/svg/numeric_contract_wbtest.mbt: omitted defaults and finite singular-transform lowerability sentinel
    - modules/mb-svg/svg/portable_qualification_wbtest.mbt: opacity raster semantics and layer-depth controls
    - modules/mb-svg/svg/geometry_wbtest.mbt: private seam fact coverage added by Plan 95-01
  </read_first>
  <behavior>
    - A valid public SVG fixture combining viewBox mapping, transforms, rect/circle/ellipse/line/polyline/polygon/path inputs, and omitted attributes returns a SceneNode and a usable drawing list.
    - The public finite `scale(0)` case remains successful while source and derived failures retain the existing structured error facts.
    - The portable suite passes on wasm, wasm-gc, js, and native without a new SVG API, FFI, feature, numeric limit, or error schema.
  </behavior>
  <action>
    Add one compact exported-API-only test in `svg_test.mbt` that uses `parse_svg` and `lower_to_drawing_list` for a valid fixture exercising root viewBox mapping, a finite singular transform, each supported shape/path family, and omitted-default behavior. Assert public success and usable DrawingList semantics without accessing private geometry helpers. Keep the existing structured-error assertions intact so `parse_svg` remains the fail-closed boundary, preserve the inclusive `[-65536.0, 65536.0]` contract and `scale(0)` acceptance, and do not add public API or change public error facts, per D-02 and D-03.

    Run the focused tests plus the ordinary frozen all-target package suite. Treat `numeric_contract_wbtest.mbt`, `conformance_wbtest.mbt`, and `portable_qualification_wbtest.mbt` as unchanged sentinels for omitted defaults, valid geometry, RFC 0008 opacity ordering, and the 16-layer capability boundary, per D-04 and D-05. Do not add SVG features, native FFI, target-specific snapshots, timing comparisons, or renderer policy changes.
  </action>
  <verify>
    <automated>moon test modules/mb-svg/svg --target all --frozen</automated>
  </verify>
  <acceptance_criteria>The public valid-SVG fixture remains parseable and lowerable; established numeric/error and scale(0) contracts remain green; the frozen SVG suite passes on all supported targets.</acceptance_criteria>
  <done>Public compatibility evidence and the complete portable package gate pass.</done>
  <reversibility rating="costly">Public parser timing, numeric diagnostics, and valid rendering output are compatibility constraints in D-02 and D-03.</reversibility>
</task>

</tasks>

<spec_less_probe_fallback>
SVGUNI-01 produced unresolved generic probes; they are carried here explicitly rather than treated as unplanned scope.

| Probe | Status | Planning treatment |
|---|---|---|
| Adjacency — "When two things are exactly equal or just touch, do they merge, collide, or separate?" | NOT APPLICABLE | Compatibility checks preserve existing independent SVG paths; the phase creates no collision or merge behavior. |
| Empty — "What is the result for empty, single-element, or null input?" | ASSUMPTION | Existing empty Path2/DrawingList semantics are retained. Manual invalid-scene tests cover the only new explicit empty-path recovery; MoonBit values have no null input. |
| Ordering — "When elements compare equal, is output order specified and stable?" | ASSUMPTION | The geometry seam does not sort. Tests assert the established source-to-DrawOp/layer ordering for valid fixtures. |
</spec_less_probe_fallback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public SVG API to parser/lowerer | Compatibility tests must distinguish valid public behavior from package-private implementation details. |
| DrawingList to canvas renderer | SVG operation ordering controls canvas opacity and layer-capacity semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-04 | Tampering | valid parser-to-lowerer compatibility | high | mitigate | Exported-API test covers a compact valid fixture; the all-target package gate catches target divergence. |
| T-95-05 | Denial of Service | total lowerer | medium | mitigate | Direct invalid SceneNode tests require deterministic return of a DrawingList rather than a panic or public error channel. |
| T-95-06 | Tampering | opacity/layer DrawOp ordering | medium | mitigate | Retain lower-level operation assertions and existing portable 16/17-layer and opacity sentinels. |
</threat_model>

<verification>
Run `moon test modules/mb-svg/svg --target native --frozen -f '*lower*'` after the lowerer controls, then run `moon test modules/mb-svg/svg --target all --frozen` as the phase gate. The final command is required evidence for wasm, wasm-gc, js, and native.
</verification>

<success_criteria>
- Valid public SVG scenes and drawing lists remain compatible for all supported geometry families.
- `lower_to_drawing_list` remains total with tested deterministic behavior for invalid manually constructed public scenes.
- Numeric admission, finite scale(0), omitted defaults, RFC 0008 opacity ordering, and 16-layer behavior retain their existing controls across all targets.
</success_criteria>

## Artifacts this phase produces

- `modules/mb-svg/svg/lower_wbtest.mbt` — deterministic total-lowerer and semantic DrawOp compatibility evidence.
- `modules/mb-svg/svg/svg_test.mbt` — exported-API valid SVG parse-to-lower compatibility evidence.

<output>
Create `.planning/phases/95-shared-svg-geometry-boundary/95-02-SUMMARY.md` when done.
</output>
