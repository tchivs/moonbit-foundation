---
phase: 83-low-bit-indexed-adam7-machine-and-eager-contract
plan: 01
type: execute
wave: 1
depends_on:
  - 82-01
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - INDEXLOWADAM7-01
  - INDEXLOWADAM7-02
  - INDEXLOWADAM7-03
  - INDEXLOWADAM7-04
must_haves:
  truths:
    - "Callers can explicitly request Type-3/1, /2, or /4 Adam7 from a canonical unpacked PngIndexedImage through additive eager and thin chunk selectors while existing encode_indexed/new_indexed paths remain explicit None forwards and all legacy bytes stay frozen."
    - "Every nonempty selected-depth Adam7 pass row has its own checked packed width, filter-None tag, MSB-first local-coordinate codes, and deterministic zero tail bits, emitted by the existing PngEncodeMachine without staging or a second encoder."
    - "Each selected depth keeps actual-entry PLTE caps, shortest canonical tRNS, Stored Type-3 framing/CRCs, public palette-exact decode, and atomic checked pass/frame/work/output admission."
    - "Phase 83 proves only eager wire/preflight plus a sufficient-lease selector smoke; hostile lease lifecycle and collected-stream qualification remain Phase 84 work."
  artifacts:
    - "modules/mb-image/png/encode.mbt exposes selected low-bit interlace selection and selected-depth Adam7 indexed preflight."
    - "modules/mb-image/png/stream_encode.mbt exposes the thin selected low-bit interlace selector and pass-local packed Adam7 byte provider in the sole machine."
    - "modules/mb-image/png/encode_test.mbt and encode_wbtest.mbt contain literal 5×5 depth-1/2/4 raster/framing/decode/admission/freeze evidence."
    - "modules/mb-image/png/stream_encode_test.mbt has only a sufficient-capacity selected-depth Adam7 IHDR wiring smoke."
  key_links:
    - "Both low-bit interlace selectors map PngIndexedBitDepth once to PngIndexedWireProfile and call PngEncodeMachine::new_with_indexed_profile(source, profile, interlace_strategy, ...)."
    - "Selected-depth Adam7 preflight and byte replay call _png_adam7_passes(width, height, 1UL, profile.depth()), ensuring packed pass totals and local-row bytes use one geometry authority."
    - "Existing encode_indexed/new_indexed pass PngInterlaceStrategy::None, preserving non-interlaced 1/2/4 vectors; Indexed8 Adam7 vectors remain unchanged."
---

<objective>
Extend the existing acknowledged PNG machine with bounded Type-3/1, /2, and /4 Adam7 eager output and its thin chunk selector seam, backed by independent packed-pass wire/preflight evidence.

Purpose: Reuse the canonical unpacked indexed source and one machine to make selected-depth Adam7 output available without treating a non-interlaced packed row as a pass row.

Output: Additive selected-depth interlace forwards, depth-aware checked pass preflight and scalar local packing, independent eager facts/wire/decode evidence, frozen compatibility tests, and a minimal chunk selector smoke.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-low-bit-indexed-adam7-machine-and-eager-contract/83-CONTEXT.md
@.planning/phases/83-low-bit-indexed-adam7-machine-and-eager-contract/83-RESEARCH.md
@.planning/research/v027-LOWBIT-ADAM7.md
@.planning/phases/81-indexed8-adam7-machine-and-eager-wire-contract/81-VERIFICATION.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/stream_encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add independent failing 5×5 low-bit Adam7 wire, framing, preflight, and freeze tracers</name>
  <precondition>Phase 82 shipped Indexed8 Adam7 through the one machine, while selected Indexed1/2/4 APIs still explicitly select non-interlaced output and the indexed preflight/emitter only admits Eight for Adam7.</precondition>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - RED: add intended eager calls to `PngEncoder::encode_indexed_with_interlace_strategy(..., bit_depth, PngInterlaceStrategy::Adam7, ...)` for One, Two, and Four plus one sufficient-capacity `PngChunkEncoder::new_indexed_with_interlace_strategy(..., Adam7, ...)` smoke per depth. The focused package run initially fails because these selectors and selected-depth Adam7 admission/packing do not exist.
    - Use three independent non-symmetric 5×5 canonical unpacked sources with actual palette length at their depth cap and alpha through the final palette entry. State the source codes and raw oracle in test-local literals only: depth 1 rows `01011/10110/11001/01100/10011` with seven-pass groups `P1 00 00 | P2 00 80 | P3 00 C0 | P4 00 00 00 00 | P5 00 A0 | P6 00 C0 00 80 00 40 | P7 00 B0 00 60` (22 bytes); depth 2 rows `01230/23012/01230/23012/01230` with `P1 00 00 | P2 00 00 | P3 00 00 | P4 00 80 00 80 | P5 00 20 | P6 00 70 00 70 00 70 | P7 00 B4 80 00 B4 80` (24 bytes); depth 4 rows `01234/56789/ABCDE/F0123/45678` with `P1 00 00 | P2 00 40 | P3 00 48 | P4 00 20 00 60 | P5 00 AC E0 | P6 00 13 00 BD 00 57 | P7 00 56 78 90 00 F0 12 30` (27 bytes). Each group is filter tag then locally packed MSB-first payload; unused low bits are explicitly zero.
    - Independently parse every eager result: IHDR must be `depth, 03, 00, 00, 01`; assert `IHDR → PLTE → tRNS → IDAT → IEND`, actual full-cap PLTE, literal shortest tRNS, one Stored IDAT, and all CRCs. Publicly decode each transparent stream as exact RGBA8 palette pixels and opaque peers as RGB8 pixels; neither expected raw stream may call production pass geometry, preflight, or packer.
    - Add white-box exact/atomic facts for all depths: raw scanlines/one Stored block/IDAT/frame total/work are `1: 22/1/33/122/122`, `2: 24/1/35/132/132`, and `4: 27/1/38/183/183`. At exact output/work limits construction succeeds and consumes one selected-work charge; one-less output/work, selected-cap-plus-one palette, and checked pass-arithmetic overflow fail before eager writer bytes, chunk encoder/lease exposure, or any budget-resource mutation.
    - Retain and run the existing Type-3/1, /2, and /4 non-interlaced literal vectors (including zero-tail evidence) and the Type-3/8 Adam7 vector unchanged. The only chunk test in this phase drains one ordinary sufficient-capacity lease per selected depth and asserts IHDR depth/type/interlace; it deliberately excludes zero/one/ragged schedules, lease tails, released leases, accepted-only progress, and sticky terminal checks.
  </behavior>
  <action>Write these tests before production edits. Keep fixture codes, palette/alpha bytes, pass groups, frame facts, and tRNS data test-local/literal; compute no expected bytes with `_png_adam7_passes`, `_png_profile_wire_row_bytes`, `scanline_byte`, or a production packer. Reuse existing test-only PNG chunk/CRC/Stored extraction and public decoder helpers. Choose explicit full-cap palettes whose final alpha is non-opaque (depth 1: 2 entries/tRNS 2; depth 2: 4/tRNS 4; depth 4: 16/tRNS 16), so the stated 122/132/183 totals remain self-consistent. Do not add hostile lease qualification or collected chunk-origin parser tests: Phase 84 owns them.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The native package run is red because selected low-bit Adam7 selectors/preflight/packing are absent, while the tests independently pin every depth's all-seven-pass local packing, framing, atomic facts, and frozen compatibility boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize selected-depth Adam7 preflight and local packing in the sole machine</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Implement only the Task 1 tracer contract. In encode.mbt, add `PngEncoder::encode_indexed_with_interlace_strategy(source, bit_depth, interlace_strategy, writer, limits, budget, diagnostics)` and make the existing `encode_indexed` explicitly forward `PngInterlaceStrategy::None`. Map `PngIndexedBitDepth::{One,Two,Four}` once to the existing private wire profile; preserve Indexed8 selectors and all legacy signatures. Thread interlace selection through indexed profile preflight and permit Adam7 for the selected low-bit profiles. For Adam7, derive every nonempty pass from `_png_adam7_passes(width, height, 1UL, wire_profile.depth())`, checked-sum `(pass.row_bytes + 1) * pass.height`, then feed that total into existing Stored IDAT, PngFrameFacts, output/work limits, and the single budget charge. Retain the normal non-interlaced low-bit arithmetic exactly for None and preserve palette-cap/tRNS logic.

In stream_encode.mbt, add `PngChunkEncoder::new_indexed_with_interlace_strategy(source, bit_depth, interlace_strategy, limits, budget, diagnostics)` as a thin profile mapping forward; make existing `new_indexed` explicitly forward None. Generalize the Indexed8 Adam7 scalar-byte helper (or add a tightly scoped selected-depth sibling) so it locates a selected-depth pass with the same `_png_adam7_passes(..., depth)` authority, emits `00` at every pass-row start, and otherwise packs only local pass columns into a zero-initialized byte at MSB-first shifts. Never slice a non-interlaced packed row; map each visible local slot back through `PngIndexedImage::index_at(pass.x + column*pass.dx, pass.y + row*pass.dy)`. Keep `present → write → acknowledge`, Stored/filter-None framing, CRC/Adler behavior, and all caller lease lifecycle logic unchanged. Do not add a second encoder, packed source model, pass/image/output staging, filter/compression strategies, decoder changes, FFI, wrappers, or release automation.

Make Task 1's eager and sufficient-lease smoke tests pass without weakening their literal oracles. Confirm `encode_indexed`/`new_indexed` and all prior Indexed8 routes still explicitly choose None and retain frozen vectors. Leave hostile schedules, tail ownership, released leases, terminal replay, and collected-chunk independent parser/decode evidence to Phase 84.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Every selected depth passes its independent 5×5 eager raster/frame/decode/atomicity tracer and minimal selector smoke through the same machine; non-interlaced low-bit and Indexed8 Adam7 output remains frozen; no hostile lifecycle or second encoder/staging path is introduced.</done>
</task>

</tasks>

<verification>

1. After Task 1, run `moon -C modules/mb-image test png --target native --frozen` and record the expected red missing-selector/admission failure. Re-run it after Task 2 for the focused green result.
2. Inspect the two selector call chains: both selected low-bit Adam7 variants must reach `PngEncodeMachine::new_with_indexed_profile` using one profile mapper and requested interlace; existing low-bit wrappers explicitly pass None.
3. Check test oracle independence: raw bytes and framing facts are literal/test-local, and no expected pass data is generated from production Adam7 or packing helpers.
4. Run `moon -C modules/mb-image test png --target all --frozen`. This ordinary package gate is the authoritative four-target proof for the Phase 83 implementation surface; Phase 84 still owns its hostile stream qualification matrix.

</verification>

<success_criteria>

- **INDEXLOWADAM7-01:** Additive eager and thin chunk selected-depth interlace selectors accept Adam7 at depths 1/2/4; established low-bit wrappers remain None forwards and all legacy Indexed1/2/4 and Indexed8 vectors remain unchanged.
- **INDEXLOWADAM7-02:** The sole machine uses selected-depth shared seven-pass geometry and local-coordinate MSB-first repacking with filter tags and deterministic zero tails, without a packed public source, second encoder, or staging.
- **INDEXLOWADAM7-03:** Literal 5×5 eager evidence proves each Type-3 selected-depth Adam7 frame, actual-cap PLTE/canonical tRNS, CRCs, packed pass raster, and palette-exact public RGB8/RGBA8 decode.
- **INDEXLOWADAM7-04:** Checked selected-depth pass/frame/work/output facts are admitted before one budget charge; exact limits pass, and one-less limit/work, palette overflow, and arithmetic failures expose no writer bytes or chunk lease and leave budget state unchanged.
- **Boundary:** Phase 83 contains eager/preflight evidence and sufficient-lease selector smoke only. Phase 84 owns zero/one/ragged leases, tails, released/sticky terminals, collected-stream parser qualification, and final portability qualification.

</success_criteria>

## Source Audit

| Source | ID | Plan coverage | Status |
|---|---|---|---|
| Requirements | INDEXLOWADAM7-01 | Task 2 adds selected-depth Adam7 eager/thin chunk forwards and retains None wrappers. | COVERED |
| Requirements | INDEXLOWADAM7-02 | Task 2 implements selected-depth geometry and local zero-tail packing in one machine. | COVERED |
| Requirements | INDEXLOWADAM7-03 | Task 1 has literal packed raster/framing/decode evidence for depths 1/2/4. | COVERED |
| Requirements | INDEXLOWADAM7-04 | Task 1 exact/one-less/cap/overflow tests; Task 2 checked preflight. | COVERED |
| Context | D-01, D-04 | Additive selectors with legacy None forwards; hostile lifecycle deferred to Phase 84. | COVERED |
| Context | D-02, D-03 | Selected-depth local pass packing and one checked preflight/machine. | COVERED |
| Context | D-05 | PLTE/tRNS, CRC, public decode, and frozen compatibility evidence. | COVERED |

<output>
Create `.planning/phases/83-low-bit-indexed-adam7-machine-and-eager-contract/83-01-SUMMARY.md` when execution completes.
</output>
