---
phase: 66-explicit-rgba16-png-preservation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_decode.mbt
  - modules/mb-image/png/raster_decode.mbt
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
autonomous: true
requirements:
  - RGBA16DEC-02
must_haves:
  truths:
    - "PngDecoder::decode_rgba16 accepts only legal default/sRGB Type-6/16 input and returns a packed little-endian straight-alpha rgba16 image with every source byte preserved."
    - "Normal rows and Adam7 scatter both store Rlo,Rhi,Glo,Ghi,Blo,Bhi,Alo,Ahi without a second raster buffer."
    - "Generic Type-6/16 decoding remains RGBA8(Rhi,Ghi,Bhi,Ahi); incompatible explicit profile facts fail before image allocation or result exposure."
    - "Explicit RGBA16 allocation/preflight charges eight storage bytes per output pixel without regressing established GrayAlpha16 resource behavior."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Public eager PngDecoder::decode_rgba16 selector.
      contains: "decode_rgba16"
    - path: modules/mb-image/png/stream_decode.mbt
      provides: Private Rgba16 profile, strict first-IDAT admission, and profile-aware output layout charging.
      contains: "PngDecodeProfile::Rgba16"
    - path: modules/mb-image/png/raster_decode.mbt
      provides: Exact Type-6/16 normal and Adam7 final packed stores.
      contains: "set_component_byte"
    - path: modules/mb-image/png/png_test.mbt
      provides: Public eager exact-lane and frozen-generic compatibility regressions.
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: Preallocation admission and eight-byte layout boundary evidence.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: decode_rgba16 creates the existing byte-fed machine with only the new Rgba16 profile.
      pattern: "decode_rgba16.*new_with_profile"
    - from: modules/mb-image/png/stream_decode.mbt
      to: modules/mb-image/png/raster_decode.mbt
      via: first-IDAT preflight selects rgba16 descriptor/layout and passes Rgba16 to the existing sink and Adam7 traversal.
      pattern: "PngRasterSink::new"
    - from: modules/mb-image/png/raster_decode.mbt
      to: modules/mb-image/storage/views.mbt
      via: completed Type-6/16 pixels use existing U16 component-byte views for the documented LE lane order.
      pattern: "set_component_byte"
---

<objective>
Deliver the explicit eager Type-6/16 PNG preservation path as one minimal, tested vertical slice.

Purpose: allow callers to request exact packed RGBA16 bytes without widening the generic decoder.
Output: `decode_rgba16`, a strict private profile, correct 8-byte layout charging, exact normal/Adam7 stores, and focused admission/compatibility regressions.

Decision coverage: D-01 adds only the eager selector and defers chunks; D-02 freezes generic RGBA8 high-byte decoding; D-03 admits only Type-6/16 default/sRGB facts before allocation; D-04 reuses byte-domain normal and Adam7 traversal for the exact final store; D-05 charges eight output storage bytes while retaining existing GrayAlpha16 boundaries.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/65-packed-rgba16-decode-model/65-CONTEXT.md
@.planning/phases/65-packed-rgba16-decode-model/65-01-SUMMARY.md
@.planning/phases/66-explicit-rgba16-png-preservation/66-CONTEXT.md
@.planning/phases/66-explicit-rgba16-png-preservation/66-RESEARCH.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Red-green explicit eager RGBA16 profile and exact normal/Adam7 stores</name>
  <reversibility rating="costly">The additive public selector fixes a high-precision result contract that the chunk API and qualification phases consume.</reversibility>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - RED: independent complete Type-6/16 literals fail because `PngDecoder::decode_rgba16` and its exact result path do not exist.
    - GREEN: legal declaration-free and sRGB Type-6/16 inputs produce Phase-65 packed `rgba16`, with all distinct source lanes observable as `Rlo,Rhi,Glo,Ghi,Blo,Bhi,Alo,Ahi`; include both ordinary and Adam7 traversal.
    - GREEN: decoding the same literals through the generic façade remains `RGBA8(Rhi,Ghi,Bhi,Ahi)` and cannot observe low bytes.
  </behavior>
  <action>Begin in png_test.mbt with hand-authored, asymmetric Type-6/16 wire literals rather than encoder-derived expected bytes. Add only `PngDecoder::decode_rgba16`, reusing the existing reader/options/limits/budget/diagnostics shape and a private `PngDecodeProfile::Rgba16`; generic construction and trait decoding must still select GenericRgba8. At first-IDAT preflight construct the checked Phase-65 rgba16 descriptor and pass Rgba16 through the existing sink. Retain byte-domain reconstruction and its eight-byte source pixel stride. Under Rgba16 only, write each completed normal row and Adam7 final coordinate from source MSB-first wire order to the documented little-endian component-byte order. Do not add a chunk selector, alternate decoder, conversion, or staging buffer.</action>
  <verify><automated>moon -C modules/mb-image test png --target js --frozen --filter '*rgba16*'</automated></verify>
  <done>Only the eager explicit selector is new; legal default/sRGB Type-6/16 input returns exact packed rgba16 across normal and Adam7 paths while generic decoding remains frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green strict preallocation admission and 8-byte output-layout accounting</name>
  <files>modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - RED: the Rgba16 profile has no proof that incompatible IHDR/colour facts reject before lifecycle allocation or that output charging uses eight bytes per pixel.
    - GREEN: wrong Type/depth, tRNS, legacy-colour, and ICC facts fail with the established explicit-profile typed diagnostic before a lifecycle/result exists; default/sRGB Type-6/16 is admitted.
    - GREEN: exact and one-less profile layout boundaries show the explicit output allocation uses eight bytes per pixel, while existing GrayAlpha16 preflight/resource cases retain their old result.
  </behavior>
  <action>Extend direct machine white-box fixtures so all profile facts are CRC-authenticated before the first IDAT header. Make Rgba16 admission require exactly Type-6/16 and Default or Srgb declaration, reject transparency/legacy/ICC facts before descriptor, child budget, OwnedImage, sink, or outcome. Reuse existing capability diagnostic conventions and assert lifecycle/result absence. Refactor the shared preflight helper only as needed to distinguish output storage bytes-per-pixel (Rgba16=8, GrayAlpha16=4, existing generic layouts unchanged) from encoded filtered-row bytes; keep checked arithmetic and existing error precedence. Add exact/one-less resource observations plus GrayAlpha16 regression evidence. Do not expose any public chunk API.</action>
  <verify><automated>moon -C modules/mb-image test png --target js --frozen --filter '*rgba16*'; moon -C modules/mb-image test png --target js --frozen --filter '*graya16*'</automated></verify>
  <done>All explicit profile mismatches are atomic and typed, Rgba16 output is charged as eight bytes per pixel before inflate, and shared GrayAlpha16 resource behaviour is retained.</done>
</task>

</tasks>

<verification>
During development run focused JS PNG checks. Before closing the plan run the ordinary package command, serially across every supported target:

`moon -C modules/mb-image test png --target all --frozen`
</verification>

<success_criteria>
RGBA16DEC-02 is complete when `decode_rgba16` preserves every legal Type-6/16 lane into Phase-65 packed rgba16 for normal and Adam7 input, admission fails before allocation for incompatible colour/profile facts, eight-byte output limits are correctly charged, generic high-byte RGBA8 stays unchanged, and the PNG package passes on all four targets.
</success_criteria>

<output>
Create `.planning/phases/66-explicit-rgba16-png-preservation/66-01-SUMMARY.md` when done.
</output>
