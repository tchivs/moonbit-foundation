---
phase: 68-rgba16-decode-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/stream_decode_test.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
autonomous: true
requirements:
  - RGBA16DEC-04
must_haves:
  truths:
    - "Independent Type-6/16 fixtures covering filter tags 0 through 4 and all seven nonempty Adam7 passes retain every Rlo,Rhi,Glo,Ghi,Blo,Bhi,Alo,Ahi byte through the explicit eager and chunk selectors."
    - "Exact and one-less normal and Adam7 output, image, and work envelopes prove the eight-byte result charge happens at first-IDAT preflight, before raster decompression can advance, and a rejected request exposes no DecodeResult."
    - "Hostile metadata, malformed/truncated data, and one-less resources retain strict typed failure with no public result through both eager and chunk selectors; chunk additionally retains accepted-only progress, private lifecycle, and sticky terminals; generic eager and chunk Type-6/16 decoding stays the RGBA8 high-byte projection."
    - "The ordinary PNG package suite passes serially, unchanged, on wasm, wasm-gc, js, and native."
  artifacts:
    - path: modules/mb-image/png/png_test.mbt
      provides: Hand-authored Type-6/16 filter and Adam7 literals plus per-coordinate explicit-lane and generic-eager compatibility assertions.
      contains: "decode_rgba16"
    - path: modules/mb-image/png/stream_decode_test.mbt
      provides: Public RGBA16 chunk schedules, hostile terminal replay, and generic-chunk compatibility coverage over the independent fixtures.
      contains: "PngChunkDecoder::new_rgba16"
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: First-IDAT resource/admission evidence for exact and one-less normal and Adam7 RGBA16 requests.
      contains: "PngDecodeProfile::Rgba16"
  key_links:
    - from: modules/mb-image/png/png_test.mbt
      to: modules/mb-image/png/png.mbt
      via: Hand-authored Type-6/16 wire literals enter decode_rgba16 and the unchanged generic decoder; assertions observe explicit little-endian component lanes and generic high-byte projection from the same input.
      pattern: "decode_rgba16|ImageDecoder::decode"
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: Empty, one-byte, ragged, malformed, and replay schedules drive new_rgba16 and the generic constructor through push and finish.
      pattern: "PngChunkDecoder::new_rgba16"
    - from: modules/mb-image/png/stream_decode_wbtest.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: Private first-IDAT fixtures observe Rgba16 preflight charge, lifecycle/outcome state, and typed admission failures before compressed raster input advances.
      pattern: "PngDecodeProfile::Rgba16"
---

<objective>
Qualify the shipped explicit RGBA16 PNG decode contract with independent filter and Adam7 wire evidence, exact hostile-resource boundaries, frozen generic behavior, and ordinary four-target package execution.

Purpose: demonstrate that the shared byte-fed decoder preserves all Type-6/16 component lanes only through the explicit profile while retaining its bounded preflight and established generic compatibility behavior.
Output: focused public and white-box PNG regression coverage only; decoder source changes are permitted solely when these regressions demonstrate a shared-store defect.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/68-rgba16-decode-qualification/68-CONTEXT.md
@.planning/phases/68-rgba16-decode-qualification/68-RESEARCH.md
@.planning/phases/66-explicit-rgba16-png-preservation/66-VERIFICATION.md
@.planning/phases/67-resumable-rgba16-png-preservation/67-VERIFICATION.md
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/stream_decode_test.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt

<interfaces>
From modules/mb-image/png/png_test.mbt:
- `png_test_rgba16_literal()` is the existing declaration-free Type-6/16 normal vector; `png_test_rgba16_adam7_literal()` is the existing sRGB 5x5 vector, and `png_test_graya16_adam7_limits()` supplies a usable 5x5 envelope.
- `PngDecoder::decode_rgba16(...)` returns the explicit profile result; `@codec.ImageDecoder::decode(PngDecoder::new(), ...)` must retain the generic RGBA8 view for identical Type-6/16 bytes.

From modules/mb-image/png/stream_decode_test.mbt:
- `png_rgba16_chunk_schedule(...)` begins with an empty push, asserts per-call accepted-byte progress under caller-selected packet sizes, and transfers the result only through `finish()`.
- `png_rgba16_chunk_eager(...)`, `png_graya16_chunk_result_matches(...)`, and `png_graya16_chunk_assert_sticky(...)` are the established eager-parity, result-byte, and terminal-replay helpers; extend their use rather than creating another decoder harness.

From modules/mb-image/png/stream_decode_wbtest.mbt:
- `_png_rgba16_machine_for_test`, `_png_rgba16_machine_with_bytes_for_test`, `_png_rgba16_accept_signature_and_ihdr`, `_png_rgba16_first_idat_error`, and `_png_assert_rgba16_profile_error` expose the first-IDAT profile boundary without a public result.
- Existing assertions establish that the Rgba16 profile reserves eight result bytes per pixel plus byte-domain row storage and that the private lifecycle/outcome is absent after a failed preflight.
</interfaces>
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Exact Type-6/16 fidelity through filters and Adam7, bounded hostile behavior, frozen compatibility, and portability | 68-01 | COVERED | Tasks 1–3 prove the independent fixture, streaming/terminal, boundary, and target portions. |
| REQ | RGBA16DEC-04 | Independent vectors, filters/Adam7, metadata/resource rejection, generic RGBA8, and ordinary four-target package pass | 68-01 | COVERED | Every requirement clause has a direct public or white-box regression plus the final serial suite gate. |
| RESEARCH | — | Extend test seams only; use hand-authored all-filter and seven-pass wire vectors | 68-01 | COVERED | Tasks 1 and 2 add fixed literals and coordinate assertions without an encoder-derived expectation. |
| RESEARCH | — | Reuse eager/chunk schedule and first-IDAT helper seams for resources and hostile terminals | 68-01 | COVERED | Tasks 2 and 3 extend the named public and white-box helpers. |
| RESEARCH | — | Run `moon -C modules/mb-image test png --target <target> --frozen` serially for the four portable targets | 68-01 | COVERED | Task 3 runs each ordinary target command independently. |
| CONTEXT | D-01 | Fixed hand-authored Type-6/16 all-filter and all-seven-pass Adam7 literals with distinct asserted lane bytes | 68-01 | COVERED | Tasks 1 and 2 cite D-01 and require coordinate/lane assertions. |
| CONTEXT | D-02 | Eager/chunk qualification, hostile schedules, terminal replay, and frozen generic high-byte behavior | 68-01 | COVERED | Task 2 cites D-02 for public selector, schedule, terminal, and generic assertions. |
| CONTEXT | D-03 | Exact and one-less normal/Adam7 output, image, and work limits charged before decompression with no exposed result | 68-01 | COVERED | Task 3 cites D-03 and tests first-IDAT preflight state plus public resource terminals. |
| CONTEXT | D-04 | Strict Default/sRGB admission, typed preallocation failure, private lifecycle, and sticky terminals under hostile metadata/data | 68-01 | COVERED | Tasks 2 and 3 cite D-04 at public and white-box boundaries. |
| CONTEXT | D-05 | Ordinary serial wasm, wasm-gc, js, and native package validation without target-specific execution behavior | 68-01 | COVERED | Task 3 cites D-05 and names the four direct package commands. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove an independent all-filter eager RGBA16 path</name>
  <files>modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - A hand-authored, CRC-valid 2x5 Type-6/16 literal uses filter tags 0, 1, 2, 3, and 4 once each, with distinct high and low bytes for every channel at every coordinate.
    - `PngDecoder::decode_rgba16` reconstructs each source lane as `Rlo,Rhi,Glo,Ghi,Blo,Bhi,Alo,Ahi` at the asserted coordinate.
    - The generic eager decoder consumes the identical literal and remains `RGBA8(Rhi,Ghi,Bhi,Ahi)`.
  </behavior>
  <action>Begin RED with D-01 by adding a fixed, hand-authored Type-6/16 2x5 PNG literal in `png_test.mbt`; encode the five PNG filter tag values in the scanlines, make every high and low byte unequal, and write the expected coordinate/lane bytes as an independent test oracle. Add a focused eager regression that invokes `PngDecoder::decode_rgba16`, reads every component through `get_component_byte`, and checks the packed little-endian lane order. In the same regression route the identical source through `@codec.ImageDecoder::decode(PngDecoder::new(), ...)` and check the four historical high bytes at each coordinate, per D-02. The literal and expected bytes must be authored directly in the test and must not be produced by `PngEncoder`. Keep this task confined to test evidence; if it demonstrates a shared-store defect, add the narrowest targeted regression before changing the demonstrated source seam.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target js --frozen --filter '*rgba16*'</automated>
  </verify>
  <done>The explicit eager selector retains every lane from an independent all-five-filter Type-6/16 literal, while the generic eager selector returns only the corresponding RGBA8 high-byte projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend independent Adam7, chunk-schedule, and hostile-terminal evidence</name>
  <files>modules/mb-image/png/png_test.mbt, modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - A separate sRGB Type-6/16 5x5 literal has all seven Adam7 passes nonempty and supplies distinct lane bytes for every output coordinate.
    - Explicit eager and `new_rgba16` decoding preserve every lane from both independent fixtures under empty-then-one-byte and ragged caller schedules, matching a fresh eager peer only after `finish()`.
    - Generic eager and generic chunk decoding of both fixtures remain RGBA8 high-byte results.
    - Public `decode_rgba16` and `new_rgba16` both reject truncated, malformed, CRC-authenticated profile-invalid, and one-less-resource input with established typed errors and no returned DecodeResult; chunk calls additionally retain accepted-only progress and replay their first terminal with zero later consumption.
  </behavior>
  <action>Extend the public qualification per D-01 by adding a separately hand-authored, CRC-valid 5x5 sRGB Type-6/16 Adam7 literal and its complete per-coordinate RGBA16 byte oracle in `png_test.mbt`. Ensure every Adam7 pass is nonempty and test every component byte through the explicit eager selector; retain a direct generic-eager high-byte assertion over that exact source. Per D-02/D-04, add public `PngDecoder::decode_rgba16` regressions for truncated input, malformed signature, CRC-authenticated legacy-colour metadata, and one-less output/resource limits; assert the existing typed diagnostic and that no DecodeResult is returned. In `stream_decode_test.mbt`, feed both the all-filter and Adam7 literals through `png_rgba16_chunk_schedule` using the established empty-first, one-byte, and ragged schedules, then compare each returned explicit result to a freshly decoded eager result and check every lane. Extend generic chunk assertions to both fixtures and expand the hostile matrix to the same incomplete, malformed, profile-invalid, and resource-limited cases; preserve accepted-byte count, finish-only transfer, and sticky replay helper checks. Keep absent and sRGB declarations as the accepted facts. Make changes only in these tests unless a failing lane or terminal test proves a shared-store defect; any repair must be in the existing shared-store path with its new regression kept in this task.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target js --frozen --filter '*rgba16*'</automated>
  </verify>
  <done>Independent all-filter and all-seven-pass literals exercise exact explicit eager/chunk preservation, hostile caller terminals, and frozen generic eager/chunk high-byte compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Qualify first-IDAT resource boundaries and serial portability</name>
  <files>modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - For normal and Adam7 Type-6/16 geometry, exact output-byte, image-size, and work envelopes create one private lifecycle with the expected eight-byte-per-pixel result reservation before compressed data is accepted.
    - One-less envelopes fail at the first-IDAT preflight with the established typed resource diagnostic, unchanged relevant remaining budget/limit state, no private lifecycle or outcome, and no public DecodeResult.
    - Default and sRGB facts admit; wrong type/depth, transparency, legacy-colour, and ICC facts retain typed preallocation rejection.
    - The complete ordinary PNG package succeeds on wasm, wasm-gc, js, and native through four direct serial commands.
  </behavior>
  <action>In `stream_decode_wbtest.mbt`, add D-03 exact-versus-one-less matrices for both the 2x5 all-filter geometry and the 5x5 Adam7 geometry. Drive each matrix only through the existing Rgba16 first-IDAT helper boundary: test `max_output_bytes`, image dimensions/pixels and the corresponding resource envelope, and `max_work`/work budget at their exact thresholds and one unit below. For every rejected case assert the established typed resource context, retained relevant remaining state, absent private lifecycle, absent private outcome, and first-IDAT failure before any compressed raster payload is accepted; for exact cases assert the single lifecycle and expected eight-byte result reservation. Per D-04, retain direct white-box Default/sRGB admission and wrong type/depth, transparency, legacy-colour, and ICC rejection assertions in the same boundary suite. Keep the implementation test-only unless these checks prove a shared-store error, in which case change only that shared-store seam and retain the failing case as its regression. After focused JS tests pass, satisfy D-05 by running the ordinary unfiltered PNG package command serially and unchanged for `wasm`, `wasm-gc`, `js`, and `native`; record the four normal results in the phase summary without a wrapper, copied tree, generated oracle, FFI, or target-specific expectation.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target wasm --frozen</automated>
    <automated>moon -C modules/mb-image test png --target wasm-gc --frozen</automated>
    <automated>moon -C modules/mb-image test png --target js --frozen</automated>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>Normal and Adam7 RGBA16 preflight proves exact and one-less output, image, and work limits at first IDAT, strict profile admission remains atomic, and the ordinary PNG package passes on every supported target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type-6/16 IHDR and colour facts to Rgba16 first-IDAT admission | Attacker-controlled type, depth, interlace, transparency, and colour declaration facts determine whether a high-precision result can reserve resources. |
| IDAT/filter bytes to normal and Adam7 component stores | Untrusted decompressed rows cross into byte-domain reconstruction and final packed U16 coordinate storage. |
| Caller-owned ByteView to public chunk lifecycle | Arbitrary packet boundaries and later caller views cross into accepted-byte accounting, finish-only transfer, and terminal replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-01 | Tampering | Type-6/16 normal filter and Adam7 final stores | high | mitigate | Tasks 1 and 2 use independent unequal component bytes at every asserted coordinate, covering filter tags 0–4 and all seven passes. |
| T-68-02 | Denial of service | Rgba16 first-IDAT resource preflight | high | mitigate | Task 3 tests exact and one-less output, image, and work envelopes before compressed-row advancement and checks lifecycle/outcome state. |
| T-68-03 | Tampering | Rgba16 profile admission | high | mitigate | Tasks 2 and 3 retain Default/sRGB acceptance and typed preallocation rejection for incompatible type/depth, transparency, legacy-colour, and ICC facts. |
| T-68-04 | Tampering | PngChunkDecoder progress and terminal state | medium | mitigate | Task 2 drives empty, one-byte, ragged, malformed, and replay schedules with accepted-only and sticky-terminal assertions. |
| T-68-05 | Tampering | Generic Type-6/16 compatibility facade | medium | mitigate | Tasks 1 and 2 assert generic eager and chunk RGBA8 high-byte projection against the same independent bytes. |
</threat_model>

<verification>
Run the focused JS RGBA16 tests while developing. Final qualification executes the ordinary unfiltered PNG package separately for wasm, wasm-gc, js, and native: `moon -C modules/mb-image test png --target &lt;target&gt; --frozen`.
</verification>

<success_criteria>
RGBA16DEC-04 is complete when independent all-five-filter and all-seven-pass Type-6/16 wire vectors retain every explicit packed lane, normal and Adam7 resource boundaries fail atomically at first IDAT, hostile eager/chunk behavior and generic RGBA8 compatibility remain frozen, and the ordinary PNG package passes on all four targets.
</success_criteria>

<output>
Create `.planning/phases/68-rgba16-decode-qualification/68-01-SUMMARY.md` when done.
</output>
