---
phase: 99-simple-and-composite-outlines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-font/font/pkg.generated.mbti
  - modules/mb-font/font/moon.pkg
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/outline.mbt
  - modules/mb-font/font/limits.mbt
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/generated_fonts_wbtest.mbt
autonomous: true
requirements:
  - FONT-03
must_haves:
  decision_coverage:
    - "D-01: direct receiving-font-validated Budget-taking shared Path2 query."
    - "D-02: retained FontLimits and independent caller-authorized query Budget."
    - "D-03: pre-read and post-decode revision guards, including empty paths."
    - "D-04: exact endpoints, instructions, repeated flags, deltas, and trailing-byte validation."
    - "D-05: preserved contour/point order and winding with geometry-neutral overlap metadata."
    - "D-06: standard quadratic start, line, control, implied-midpoint, and Close lowering."
    - "D-07: deterministic empty/degenerate results with instructions skipped, never executed."
    - "D-10: checked private Int64 Q15 until final Point2 construction."
    - "D-16: lazy outline-body decode owned by Font::outline."
    - "D-17: four explicit non-zero outline ceilings plus max_work and caller Budget."
    - "D-18: preflight/charge discovery and allocation work before traversal/allocation."
    - "D-19: private temporary geometry and one complete publication."
    - "D-20: stable InvalidInput/Data/Capability/Resource/State taxonomy."
    - "D-21: checksum-correct generated simple semantic fixtures."
    - "D-22: hostile simple, arithmetic, resource, and mutation fixtures."
    - "D-23: public/private tests plus repaired interface-baseline gate."
  truths:
    - "Before any Phase 99 public-source edit, `moon info` regenerates the stale Phase 98 interface baseline and the executor records that its semantic delta contains only the already-shipped glyph_for_scalar, kerning, kern-limit, and expanded-constructor surface; Phase 99 drift is assessed only from that repaired baseline (D-23)."
    - "Font::outline revalidates the receiving Font's opaque GlyphId, uses a fresh caller Budget for each call, guards source revision before glyph reads and after all private work, and publishes exactly one complete shared Path2, including for empty results (D-01, D-02, D-03, D-19)."
    - "Simple glyph decoding validates exact endpoints, bounded instructions, packed/repeated flags, signed deltas, strict trailing padding, and every malformed boundary before lowering (D-04, D-07, D-16)."
    - "Simple contours preserve encoded contour/point order and winding and lower the TrueType on/off-curve rules, exact implied midpoints, degenerate contours, and one Close per contour without overlap processing or instruction execution (D-05, D-06, D-07)."
    - "All internal coordinates and implied midpoints use checked Int64 Q15 and convert to Double only while creating final Point2 commands (D-10)."
    - "Actual counts are checked first against admitted maxp claims and then against non-zero retained outline limits; max_work and the caller Budget are both authoritative, discovery/allocation work is charged before traversal/allocation, and failed queries expose no geometry (D-17, D-18, D-19, D-20)."
    - "Generated black-box and white-box tests freeze exact simple PathCommand sequences, cursor/count/Q15 facts, structured error categories, exact-fit/one-short resource behavior, and deterministic post-decode mutation failure (D-21, D-22, D-23)."
  flagged_assumptions:
    - "The specless FONT-03 probe was unclassified. This plan resolves its simple-outline portion by treating the ROADMAP success criteria and D-01 through D-07 plus D-10 and D-16 through D-23 as binding executable behavior; it does not invent null, string, shaping, raster, or host-font semantics."
  prohibitions:
    - "No failed outline query may expose a Path2 containing any command."
    - "No simple contour may be reversed, normalized, flattened, unioned, overlap-processed, or silently dropped."
    - "No TrueType instruction byte may be executed."
  artifacts:
    - path: modules/mb-font/font/outline.mbt
      provides: "Private checked-Q15 simple glyph decoder, contour lowerer, resource staging, and transactional Path2 construction."
    - path: modules/mb-font/font/font.mbt
      provides: "Guarded public Font::outline facade over retained FontLimits and MetricIndexFacts."
    - path: modules/mb-font/font/limits.mbt
      provides: "Four non-zero outline ceilings and public accessors."
    - path: modules/mb-font/font/tables.mbt
      provides: "Retained version-1 maxp outline maxima used as font-consistency claims."
    - path: modules/mb-font/font/pkg.generated.mbti
      provides: "Repaired Phase 98 generated-interface baseline before Phase 99 interface assessment."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Public simple outline, empty/degenerate, error, budget, and mutation evidence."
    - path: modules/mb-font/font/font_wbtest.mbt
      provides: "Private decoder cursor, Q15, count, charging, and error-taxonomy evidence."
  key_links:
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/outline.mbt
      via: "Font::outline performs pre-guard, receiving-font glyph validation, private charged decode/lower, post-guard, then one Path2 return."
    - from: modules/mb-font/font/outline.mbt
      to: modules/mb-core/math/path.mbt
      via: "Private Q15 facts become Point2 and PathCommand values only during final local Path2 assembly."
    - from: modules/mb-font/font/outline.mbt
      to: modules/mb-font/font/tables.mbt
      via: "Actual point, contour, and instruction facts are checked against retained maxp claims before FontLimits and Budget."
---

## Artifacts this phase produces

| File | Created or changed symbols |
|---|---|
| `modules/mb-font/font/font.mbt` | Add private retained field `Font.limits : FontLimits`; add `pub fn Font::outline(self : Font, glyph : GlyphId, budget : @budget.Budget) -> Result[@math.Path2, @error.CoreError]`; add the private after-decode test hook used before the second revision guard (its identifier remains implementation discretion, but its signature accepts the completed private outline result and returns no public geometry). |
| `modules/mb-font/font/outline.mbt` | Create private `OutlinePoint` facts with `x_q15 : Int64`, `y_q15 : Int64`, and `on_curve : Bool`; create `font_decode_outline(index : MetricIndexFacts, maxp : MaxpFacts, limits : FontLimits, glyph_index : UInt64, budget : @budget.Budget) -> Result[@math.Path2, @error.CoreError]`; create `midpoint_q15(a : OutlinePoint, b : OutlinePoint) -> Result[OutlinePoint, @error.CoreError]` and `contour_start(first : OutlinePoint, last : OutlinePoint) -> Result[OutlinePoint, @error.CoreError]`; add private simple-body decode/lower and checked signed-Q15 helpers whose concrete identifiers follow local conventions but whose inputs/outputs remain private decoded points, contour endpoints, and one complete `Path2`. |
| `modules/mb-font/font/limits.mbt` | Extend private `FontLimits` storage with non-zero `max_outline_points`, `max_outline_contours`, `max_outline_components`, and `max_outline_instruction_bytes` UInt64 values; add the four matching public `FontLimits` accessors; expand `pub fn FontLimits::new(...) -> Result[FontLimits, @error.CoreError]` with those four required named UInt64 parameters while retaining all prior parameters. |
| `modules/mb-font/font/tables.mbt` | Extend private `MaxpFacts` with `max_points`, `max_contours`, `max_composite_points`, `max_composite_contours`, `max_size_of_instructions`, `max_component_elements`, and `max_component_depth`; update the existing private `font_decode_maxp(...) -> Result[MaxpFacts, @error.CoreError]` to populate them from the single admitted version-1 table. |
| `modules/mb-font/font/moon.pkg` | Add the sole new public production import `tchivs/mb-core/math`, enabling the shared `Path2`, `PathCommand`, and `Point2` return boundary. |
| `modules/mb-font/font/pkg.generated.mbti` | Regenerate the repaired Phase 98 baseline before source changes; after implementation, this plan causes the generated `@math` import, `Font::outline` signature, four `FontLimits` accessors, and expanded `FontLimits::new` signature that Plan 99-03 later freezes. |
| `modules/mb-font/font/generated_fonts_wbtest.mbt` | Extend `generated_maxp_table(...)` with the seven outline-maxima inputs and add checksum-correct simple/empty/degenerate `glyf` plus normalized `loca` fixture builders; builder helper identifiers may follow the established `generated_*` convention, while their produced bytes and maxima are the frozen artifact. |
| `modules/mb-font/font/font_test.mbt`, `modules/mb-font/font/font_wbtest.mbt` | Add public `test` cases for exact simple `PathCommand` and structured error/resource/mutation outcomes; add private `test` cases for decoded cursor, expanded flags, contour endpoints, Q15 midpoint/coordinate facts, maxp/limit ordering, charging, and no-publication invariants; migrate every local `FontLimits::new` call to the four new parameters. |

<objective>
Repair the inherited generated-interface baseline, then deliver complete transactional simple TrueType outline extraction.

Purpose: FONT-03 needs one proven public path before composite placement expands the hostile-input state machine, and Phase 99 interface review must not misattribute stale Phase 98 signatures.
Output: Repaired Phase 98 `.mbti` baseline, guarded `Font::outline`, exact-Q15 simple decoding/lowering, retained outline limits/maxp facts, and executable public/private simple-glyph evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-simple-and-composite-outlines/99-CONTEXT.md
@.planning/phases/99-simple-and-composite-outlines/99-RESEARCH.md
@.planning/phases/99-simple-and-composite-outlines/99-PATTERNS.md
@.planning/phases/98-unicode-mapping-and-kerning/98-03-SUMMARY.md

<interfaces>
Preserve these existing seams:
- `Font::require_revision(String) -> Result[Unit, @error.CoreError]`
- `Font::horizontal_metrics(GlyphId) -> Result[GlyphHorizontalMetrics, @error.CoreError]`
- `GlyphId::value() -> UInt64`
- private `MetricIndexFacts` with normalized `loca_offsets`, retained table-local `glyf`, and `num_glyphs`
- `Budget::preflight(ResourceCharge)` and `Budget::charge(ResourceCharge)`
- `Path2::new`, `Path2::push`, `PathCommand::MoveTo/LineTo/QuadTo/Close`, and `Point2::new`

Create the locked public seam:
- `Font::outline(Self, GlyphId, @budget.Budget) -> Result[@math.Path2, @error.CoreError]`

Expand `FontLimits::new` with four named UInt64 ceilings for total expanded outline points, total contours, composite components/inspected descriptors, and per-glyph instruction bytes. Exact private names may follow local conventions, but the public accessors and constructor arguments must be singular, explicit, and non-zero.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair the Phase 98 interface baseline and trace one simple contour end to end</name>
  <reversibility rating="costly">D-01 publishes a direct Budget-taking Font method returning shared Path2; changing that seam later requires coordinated consumer, policy, and documentation migration.</reversibility>
  <files>modules/mb-font/font/pkg.generated.mbti, modules/mb-font/font/moon.pkg, modules/mb-font/font/font.mbt, modules/mb-font/font/outline.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    - modules/mb-font/font/pkg.generated.mbti
    - modules/mb-font/font/moon.pkg
    - modules/mb-font/font/font.mbt:1
    - modules/mb-font/font/font.mbt:67
    - modules/mb-font/font/font.mbt:250
    - modules/mb-font/font/font.mbt:308
    - modules/mb-font/font/metrics.mbt:1
    - modules/mb-font/font/metrics.mbt:361
    - modules/mb-font/font/font_test.mbt
    - modules/mb-core/math/path.mbt:1
    - modules/mb-core/math/affine.mbt:19
  </read_first>
  <behavior>
    - "The first operation regenerates the unchanged-source Phase 98 interface; its semantic diff contains the two Phase 98 queries, two kern accessors, and Phase 98 constructor expansion, with no outline/math line."
    - "One checksum-correct font carrying one simple contour opens, produces the exact MoveTo/LineTo/QuadTo/Close sequence through Font::outline, and uses the caller's independent Budget."
    - "A loca-empty glyph and a validated zero-contour glyph return empty Path2 values only after both revision guards."
    - "A foreign or out-of-range GlyphId fails before a glyf read, and deterministic mutation after private decoding fails before Path2 publication."
  </behavior>
  <action>
Before editing `font.mbt`, `moon.pkg`, or any other Phase 99 public source, run `moon -C modules/mb-font info --target all --frozen --target-dir target/phase99-phase98-baseline`. Inspect `git diff -- modules/mb-font/font/pkg.generated.mbti` and record in the task execution notes the repaired Phase 98 semantic lines and baseline hash. Require the delta to contain only the already-implemented `glyph_for_scalar`, `kerning`, `max_kern_subtables`, `max_kern_pairs`, and corresponding Phase 98 constructor parameters; stop if an outline/math/private line appears. Keep the generated result and never hand-edit it (D-23).

Then write the failing public tracer. Add only `tchivs/mb-core/math` to the existing package imports and create private `outline.mbt`. Implement the production public coordinator shaped exactly as D-01 through D-03: retain opening `FontLimits` in `Font`, revalidate the opaque glyph against `MetricIndexFacts.num_glyphs`, use the caller's supplied Budget without caching it, obtain the admitted table-local glyph interval, decode/lower into private state, invoke a private after-decode test seam, run the post-read revision guard, and return one complete shared Path2. Apply this ordering to loca-empty and zero-contour results too.

For the tracer's one simple contour, implement checked signed coordinate accumulation and Q15 storage from the outset, preserve encoded order/winding, apply the TrueType start-point and implied-midpoint rules, and append Close. Convert Q15 to Double only when building the local Path2. This is a permanent supported path, not a throwaway decoder; later work expands its validation and cases without replacing the public/query architecture. Do not add a font-specific public outline DTO, retained query Budget, eager open-time body decoding, hint execution, rasterization, host discovery, FFI, or composite lowering.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font info --target all --frozen --target-dir target/phase99-phase98-baseline; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font test font --target native --frozen --target-dir target/phase99-simple-tracer --no-parallelize -f '*outline*'; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - "The Phase 98 baseline is regenerated and separately identified before any Phase 99 public semantic line is introduced."
    - "A real simple contour is extracted through Font::outline into exact shared PathCommand values under an independent caller Budget."
    - "Empty and zero-contour results, foreign glyphs, and post-decode mutation obey the same guarded transactional publication rule."
    - "The package still imports only mb-core packages and the focused isolated native tracer passes."
  </acceptance_criteria>
  <done>A library author can extract one production simple Path2 through the locked guarded public method, and the inherited interface baseline is no longer stale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete simple decoding, explicit limits, maxp consistency, and hostile boundaries</name>
  <reversibility rating="costly">D-17 adds four explicit hostile-input ceilings to every FontLimits constructor, so removal or reshaping later changes the published resource contract and all explicit callers.</reversibility>
  <files>modules/mb-font/font/limits.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/outline.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/generated_fonts_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/limits.mbt
    - modules/mb-font/font/tables.mbt:12
    - modules/mb-font/font/tables.mbt:507
    - modules/mb-font/font/outline.mbt
    - modules/mb-font/font/kern.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-font/font/generated_fonts_wbtest.mbt
    - modules/mb-font/font/cursor.mbt
    - modules/mb-core/budget/budget.mbt:279
    - modules/mb-core/checked/checked.mbt
  </read_first>
  <behavior>
    - "Repeated flags expand by exactly one plus repeat count and end at the declared point count; bit 7, overrun, underrun, truncation, invalid endpoints, and invalid trailing payload are Data errors."
    - "Every short/same/positive/negative x/y delta encoding and cumulative coordinate uses checked signed math and reaches exact Q15 facts."
    - "First/last/consecutive off-curve cases, repeated encoded points, original winding/order, zero-contour instruction envelopes, and declared degenerate contours produce deterministic exact commands."
    - "Actual simple point/contour/instruction counts exceeding maxp are Data; counts within maxp but over FontLimits or max_work/Budget are Resource; signed overflow is Data."
    - "Declared discovery work and scratch/output allocations are preflighted and charged before their loops/allocations; exact-fit succeeds, one-short fails, and later malformed failure retains only already-authorized consumption."
  </behavior>
  <action>
Write RED public and white-box matrices before expanding the decoder. Complete D-04 through D-07 with contained reads over the admitted `loca`/`glyf` facts: classify empty intervals and signed contour count, require strictly increasing endpoints, derive point count with checked arithmetic, validate and skip the bounded instruction envelope, expand stored flags to exactly the logical point count, decode the complete x stream then y stream, and accept only exact exhaustion or one zero word-alignment byte. Treat `OVERLAP_SIMPLE` as geometry-neutral metadata on the first stored flag and reject reserved flags or any non-padding residue as Data. Follow `kern.mbt` as the primary binary-decoder analog: use its checked record-envelope arithmetic and strict logical-body cursor consumption, and use its staged limit check -> aggregate work calculation -> Budget preflight -> charge ordering before every attacker-declared contour, flag/point, instruction, scratch, and output traversal or allocation (D-04, D-18).

Implement the full D-06 lowerer over exact private Q15: choose the on-curve first point or the last-on-curve/exact first-last midpoint start, emit lines between on-curve points, use each off-curve point as a quadratic control, insert exact midpoints between consecutive off-curve points, and close every declared contour. Preserve encoded contour order, point order, winding, overlaps, and degenerate contours. Validate/skips instructions without execution (D-05, D-07, D-10).

Expand the already-exact version-1 `MaxpFacts` decoder with `maxPoints`, `maxContours`, `maxCompositePoints`, `maxCompositeContours`, `maxSizeOfInstructions`, `maxComponentElements`, and `maxComponentDepth`; do not add a second maxp read. Extend FontLimits with explicit non-zero outline points, contours, components/descriptors, and instruction-byte ceilings, accessors, and constructor validation. Atomically migrate every explicit test constructor in `font_test.mbt`, `font_wbtest.mbt`, and `generated_fonts_wbtest.mbt` to pass all four required outline ceilings in this task, so the Plan 99-01 verification never compiles a stale `FontLimits::new` call before dependent Plan 99-02 reuses the fixture helper. For every discovered count, compare actual against maxp first, then retained limits, then max_work and the caller Budget. Preflight and charge declared loop work before traversal and conservative scratch/output allocation before allocation; map private signed overflow to outline Data rather than propagating generic InvalidInput (D-17, D-18, D-20).

Freeze D-21 through D-23 public command sequences and structured category/code/operation/context/requested/limit fields for repeated flags, every delta form, off-curve starts/midpoints, order/winding, empty/degenerate contours, truncation, endpoint/flag/instruction/padding errors, arithmetic edges, exact-fit/one-short limits/work/budget, failure-path consumption, and mid-query mutation. White-box tests must assert cursor positions, expanded counts, Q15 integer facts, command bounds, and no private temporary escapes.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font check --target native --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font test font --target native --frozen --target-dir target/phase99-simple-full --no-parallelize -f '*outline*'; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - "All simple-glyph format rules, implied-point lowering cases, winding/order, empty/degenerate behavior, and strict padding are executable and deterministic."
    - "The seven maxp outline maxima and four non-zero public outline ceilings are integrated without eager whole-font body decoding."
    - "Every explicit FontLimits::new call in font_test.mbt, font_wbtest.mbt, and generated_fonts_wbtest.mbt passes the four required outline ceilings, so Plan 99-01 compiles independently before Plan 99-02."
    - "The decoder follows kern.mbt's checked record envelopes, strict logical-body consumption, and staged limit/preflight/charge ordering for declared work and allocations."
    - "InvalidInput, Data, Capability, Resource, and State remain distinguishable where applicable; no checked arithmetic error leaks the wrong category."
    - "Exact-fit/one-short and already-consumed failure-work tests prove authoritative limits/Budget with no partial geometry."
    - "Isolated native check and focused outline tests pass without invoking the known unscoped workspace lane."
  </acceptance_criteria>
  <done>Simple FONT-03 extraction is complete on native, including exact geometry, malformed input, resource accounting, mutation, and transactional failure.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Critical and high threats are blocking and must be mitigated; the phase's configured block threshold is high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted glyf bytes -> private decoder | Packed counts, flags, coordinates, instructions, and offsets drive reads, loops, arithmetic, and allocation. |
| caller GlyphId/Budget -> Font::outline | Untrusted identity and resource authority cross into retained font state. |
| mutable retained ByteView -> public Path2 | Backing bytes can change between validation and result publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verification |
|---|---|---|---|---|---|---|
| T-99-01-01 | Tampering | `outline.mbt` simple decoder | high | mitigate | Use admitted table-local windows, exact endpoint/flag/delta/instruction/trailing validation, and checked signed Q15 arithmetic. | Public malformed-stream tests plus private cursor/Q15 tests. |
| T-99-01-02 | Denial of Service | repeated flags, contours, instructions, allocations | high | mitigate | Enforce maxp, non-zero FontLimits, max_work, and caller Budget in that order; charge before loops/allocations. | Exact-fit/one-short and malformed-retry budget-delta tests. |
| T-99-01-03 | Tampering | retained source mutation | high | mitigate | Guard source revision before glyph reads and after private lowering, including empty paths. | Deterministic after-decode mutation tests. |
| T-99-01-04 | Spoofing | opaque GlyphId reuse | high | mitigate | Revalidate every GlyphId against the receiving Font before reading glyf. | Foreign/out-of-range glyph black-box tests. |
| T-99-01-05 | Tampering | partial geometry publication | high | mitigate | Keep points, contours, and commands private until all checks and the post-read guard pass. | Failure matrix asserts Err only and no returned Path2. |
| T-99-01-06 | Repudiation | error taxonomy ambiguity | medium | mitigate | Map caller, data, capability, resource, and state conditions to stable structured CoreError facts. | Black-box category/code/context assertions. |
| T-99-01-SC | Tampering | dependency/interface baseline | low | accept | No package install occurs; frozen toolchain and exact Phase 98 baseline comparison constrain publication drift. | `moon info --frozen`, semantic diff review, and unchanged module dependency. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Complete unhinted Path2-compatible simple outlines with no partial failure | Tasks 1-2 | COVERED |
| REQ | FONT-03 | Valid simple outline extraction with checked arithmetic and transactional failure | Tasks 1-2 | COVERED |
| RESEARCH | — | Lazy guarded query over admitted MetricIndexFacts; exact Q15; maxp/limits/Budget ordering; strict padding | Tasks 1-2 | COVERED |
| RESEARCH | — | Repair stale Phase 98 generated interface before Phase 99 drift assessment | Task 1 | COVERED |
| CONTEXT | D-01,D-02,D-03 | Direct Path2 query, independent Budget, two revision guards | Task 1 | COVERED |
| CONTEXT | D-04,D-05,D-06,D-07 | Packed simple decode, preserved order/winding, quadratic lowering, empty/degenerate/instruction behavior | Tasks 1-2 | COVERED |
| CONTEXT | D-10 | Checked Int64 Q15 until final Point2 construction | Tasks 1-2 | COVERED |
| CONTEXT | D-16,D-17,D-18 | Lazy body validation, explicit ceilings, preflight/charge ordering | Task 2 | COVERED |
| CONTEXT | D-19,D-20 | Private transactional state and stable five-way error taxonomy | Tasks 1-2 | COVERED |
| CONTEXT | D-21,D-22,D-23 | Generated simple/hostile fixtures and public/private executable evidence | Task 2 | COVERED |

Composite-only D-08,D-09,D-11 through D-15 cases are owned by Plan 99-02, while final all-target interface/policy/docs closure is owned by Plan 99-03. Deferred nested lowering, phantom attachment, hinting, rasterization, variations/CFF/color, shaping, discovery, authoring, and Phase 100 licensed-real-font qualification are exclusions rather than gaps.
</source_audit>

<verification>
1. Regenerate and inspect the unchanged-source Phase 98 `.mbti` baseline before any Phase 99 public edit.
2. Run isolated native package check and focused outline tests with unique target directories and `--no-parallelize`.
3. Confirm exact simple PathCommand sequences, private Q15/cursor facts, five-way structured outcomes, resource deltas, and post-decode mutation failure.
4. Do not use the known-stalling unscoped Windows workspace test lane as a Phase 99 authority.
</verification>

<success_criteria>
- The repaired generated interface cleanly separates inherited Phase 98 signatures from later Phase 99 drift.
- A public caller can extract exact empty, degenerate, and full simple Path2 outlines through one guarded independent-budget query.
- All simple packed streams, Q15 arithmetic, limits, budgets, mutation, and error boundaries are deterministic and transactional.
- Focused isolated native gates pass while preserving pure MoonBit, four-target package metadata, and the sole mb-font -> mb-core dependency.
</success_criteria>

<output>
Create `.planning/phases/99-simple-and-composite-outlines/99-01-SUMMARY.md` when done.
</output>
