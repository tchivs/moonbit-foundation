---
phase: 99-simple-and-composite-outlines
plan: 03
type: execute
wave: 3
depends_on:
  - 99-01
  - 99-02
files_modified:
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/generated_fonts_wbtest.mbt
  - modules/mb-font/font/pkg.generated.mbti
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
  - modules/mb-font/README.mbt.md
  - modules/mb-font/CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - FONT-03
must_haves:
  decision_coverage:
    - "D-01: final direct shared Path2 interface."
    - "D-02: retained limits and independent query Budget."
    - "D-03: two revision guards on every result."
    - "D-04: exact simple stream validation."
    - "D-05: preserved simple order/winding and overlap metadata."
    - "D-06: exact quadratic contour lowering and closure."
    - "D-07: empty/degenerate behavior with no instruction execution."
    - "D-08: bounded one-level graph classification."
    - "D-09: XY and real-point attachment semantics."
    - "D-10: portable exact Int64 Q15 evaluation."
    - "D-11: all supported transforms and matrix order."
    - "D-12: component-offset and grid-rounding outcomes."
    - "D-13: component order and strict flag behavior."
    - "D-14: real versus phantom versus invalid point references."
    - "D-15: unique geometry-neutral USE_MY_METRICS."
    - "D-16: lazy outline query ownership."
    - "D-17: four outline ceilings plus max_work/Budget."
    - "D-18: staged preflight and charging."
    - "D-19: no partial geometry publication."
    - "D-20: stable five-way error taxonomy."
    - "D-21: complete generated semantic micro-font matrix."
    - "D-22: complete hostile/resource/mutation fixture matrix."
    - "D-23: public/private, interface, policy, bilingual docs, isolated package, and four-target gates."
  truths:
    - "One permanent generated public workflow opens a font, extracts an exact simple outline, extracts an exact one-level composite outline, and returns identical PathCommand/structured-error facts on js, wasm, wasm-gc, and native through the direct guarded Budget-taking Path2 query (D-01, D-02, D-03, D-21, D-23)."
    - "The portable matrix freezes packed flags/deltas, implied points, contour order/winding/closure, empty/degenerate behavior, no hint execution, and strict malformed-body outcomes (D-04, D-05, D-06, D-07, D-21, D-22)."
    - "The portable matrix freezes one-level graph classification, XY/point attachment, all F2DOT14 transforms, Q15 exactness, offset scaling/defaults, component order, phantom classification, and USE_MY_METRICS neutrality (D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-21, D-22)."
    - "Every target proves lazy query-owned work, the four explicit limits, max_work/caller-Budget intersection, precharged loops/allocations, private transactional publication, five-way errors, exact-fit/one-short behavior, and deterministic mutation failure (D-16, D-17, D-18, D-19, D-20, D-22, D-23)."
    - "After Phase 99 code is green, `moon info` regenerates the final interface from a fresh target directory and the Phase 99-only delta from the repaired baseline is exactly the @math import, Font::outline, four outline-limit accessors, and expanded constructor; no private parser/descriptor/Q15 fact is public (D-01, D-17, D-23)."
    - "Policy registers outline.mbt, the math import, exact semantic interface, four targets, and unchanged mb-font -> mb-core dependency while independently rejecting a second outline DTO, nested/hint/raster/host APIs, and private cursor/table/descriptor/Q15 facts (D-01, D-08, D-10, D-23)."
    - "Literate documentation, candidate changelog, and synchronized English/Chinese root text describe complete simple and bounded one-level composite Path2 behavior, budgets/limits/errors, and exact exclusions without claiming Phase 100 licensed-real-font qualification (D-01 through D-23)."
  flagged_assumptions:
    - "The specless FONT-03 probe was unclassified. Final closure treats all four ROADMAP success criteria and D-01 through D-23 as the exhaustive phase contract, proves them through public/private generated tests, and preserves every CONTEXT deferred item as an exclusion."
  prohibitions:
    - "No generated interface or policy entry may expose raw glyf/loca/maxp, descriptor graph, real/implied/phantom point bookkeeping, or private Q15 facts."
    - "No policy or documentation may claim nested composite lowering, phantom placement, hinting, rasterization, host discovery, shaping, or licensed-real-font qualification."
    - "No target-specific branch, FFI, GUI state, filesystem font discovery, or dependency beyond mb-core may enter mb-font."
    - "No failed public workflow may be represented as a successful empty or partial Path2."
  artifacts:
    - path: modules/mb-font/font/font_test.mbt
      provides: "Permanent public simple-plus-composite tracer and complete all-target outcome/resource/mutation matrix."
    - path: modules/mb-font/font/font_wbtest.mbt
      provides: "All-target private decoder, Q15, graph, numbering, and taxonomy matrix."
    - path: modules/mb-font/font/pkg.generated.mbti
      provides: "Final generated Phase 99 public interface derived from the repaired Phase 98 baseline."
    - path: policy/foundation.json
      provides: "Exact Phase 99 source/import/target/dependency/interface publication contract."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Independent fail-closed Phase 99 public-surface and private-leak classifier."
    - path: modules/mb-font/README.mbt.md
      provides: "Executable public outline contract, limits, outcomes, and exclusions."
    - path: modules/mb-font/CHANGELOG.md
      provides: "Candidate release record for simple and bounded composite outlines."
    - path: README.md
      provides: "Equivalent English and Chinese mb-font Phase 99 capability descriptions."
  key_links:
    - from: modules/mb-font/font/font_test.mbt
      to: modules/mb-font/font/font.mbt
      via: "Black-box tests call only Font::open, glyph APIs, Font::outline, metrics, and Budget on every target."
    - from: modules/mb-font/font/pkg.generated.mbti
      to: policy/foundation.json
      via: "Final non-comment generated semantic lines are copied verbatim only after comparison with the repaired Phase 98 baseline."
    - from: policy/foundation.json
      to: scripts/quality/Assert-Policy.ps1
      via: "The independent selector mirrors exact sources/imports/targets but separately rejects private/deferred surface."
    - from: modules/mb-font/README.mbt.md
      to: modules/mb-font/font/font_test.mbt
      via: "Documented examples and outcomes use the same public constructor, independent Budget, GlyphId, and Path2 contract exercised by tests."
---

## Artifacts this phase produces

| File | Created or changed symbols / records |
|---|---|
| `modules/mb-font/font/generated_fonts_wbtest.mbt` | Add the permanent checksum-correct portable outline fixture record containing empty, repeated-flag/implied-midpoint simple, and transformed plus real-point-attached one-level composite glyphs; its private builder has the established generated-fixture shape `() -> Bytes` and is shared by the four-target public tracer. |
| `modules/mb-font/font/font_test.mbt` | Add permanent public `test` definitions that exercise `Font::open`, opaque `GlyphId` lookup, fresh `Budget`, `Font::outline`, and unchanged `Font::horizontal_metrics` facts for the same fixture; freeze complete `PathCommand` sequences and full structured error fields across the D-21/D-22 matrix. |
| `modules/mb-font/font/font_wbtest.mbt` | Complete private `test` definitions for cursor positions, stored/expanded flags, maxp comparisons, signed-Q15 matrix/midpoint/translation facts, encoded-real point numbering, tri-color graph state, command/allocation bounds, charge stages, and taxonomy mapping. |
| `modules/mb-font/font/pkg.generated.mbti` | Regenerate the authoritative public symbols: import `tchivs/mb-core/math`; `pub fn Font::outline(Self, GlyphId, @budget.Budget) -> Result[@math.Path2, @error.CoreError]`; four UInt64 `FontLimits` outline accessors; and the expanded `FontLimits::new` signature. Exact accessor and parameter spellings come verbatim from Plan 99-01 source generation. |
| `policy/foundation.json` | Change the mb-font policy record fields `publication_files`, `production_sources`, `allowed_imports`, module `description`, and `semantic_interface`: inventory `font/outline.mbt`, admit only `tchivs/mb-core/math`, preserve `+js+wasm+wasm-gc+native` and the sole mb-font-to-mb-core dependency, and copy the generated public signatures verbatim. |
| `scripts/quality/Assert-Policy.ps1` | Change `Assert-FontFoundationPolicy -PolicyPath <path>` and its private exact-set/interface classifiers to accept the Phase 99 source/import/target/signature records while continuing to reject a second outline DTO/method, nested/hint/raster/host surface, and public parser/descriptor/Q15 symbols. |
| `modules/mb-font/README.mbt.md` | Change frontmatter imports and the executable `FontLimits::new` call; add a checked public example invoking `Font::outline(glyph, budget)` and inspecting the shared path through `Path2::length` and `Path2::get`; add delivered simple/composite, limits/budget/taxonomy, Q15 boundary, and explicit-exclusion sections. |
| `modules/mb-font/CHANGELOG.md` | Extend the existing `0.1.0 candidate` Added record with the direct outline query, four outline ceilings, simple/composite semantics, exact Q15 placement, and transactional hostile-input behavior; replace the old blanket outline exclusion with the remaining post-Phase-99 exclusions. |
| `README.md` | Change the English and Chinese mb-font responsibility/status records in lockstep to state complete unhinted simple and bounded one-level composite `Path2` extraction while withholding Phase 100 licensed-real-font qualification claims. |

<objective>
Qualify and publish the complete Phase 99 outline contract across all four portable targets.

Purpose: FONT-03 is complete only when simple/composite semantics, hostile-input behavior, generated interface, exact policy, and public documentation agree without target-specific or deferred capabilities.
Output: Permanent four-target public tracer, full portable public/private matrix, final regenerated `.mbti`, exact independent policy, executable literate docs, candidate changelog, and bilingual discovery text.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-simple-and-composite-outlines/99-CONTEXT.md
@.planning/phases/99-simple-and-composite-outlines/99-RESEARCH.md
@.planning/phases/99-simple-and-composite-outlines/99-PATTERNS.md
@.planning/phases/99-simple-and-composite-outlines/99-01-SUMMARY.md
@.planning/phases/99-simple-and-composite-outlines/99-02-SUMMARY.md
@.planning/phases/98-unicode-mapping-and-kerning/98-03-SUMMARY.md

<interfaces>
Expected final public Phase 99 delta from the repaired Phase 98 baseline:
- import `tchivs/mb-core/math`
- `pub fn Font::outline(Self, GlyphId, @budget.Budget) -> Result[@math.Path2, @error.CoreError]`
- four UInt64 FontLimits accessors for outline points, contours, components/descriptors, and per-glyph instruction bytes
- `FontLimits::new` retains every prior named parameter and adds those four outline-limit parameters

`moon info` is the only writer of `modules/mb-font/font/pkg.generated.mbti`; never hand-edit it. The exact accessor/parameter spellings generated from Plan 99-01 source are authoritative and must be copied verbatim into policy rather than reconstructed from this plan.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace simple and composite public outlines on each target, then close the portable matrix</name>
  <files>modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/generated_fonts_wbtest.mbt</files>
  <read_first>
    - .planning/phases/99-simple-and-composite-outlines/99-01-SUMMARY.md
    - .planning/phases/99-simple-and-composite-outlines/99-02-SUMMARY.md
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-font/font/generated_fonts_wbtest.mbt
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/outline.mbt
    - modules/mb-font/font/limits.mbt
  </read_first>
  <behavior>
    - "One checksum-correct font contains an exact simple glyph and a one-level transformed/attached composite; both extract through public Font::outline with independent Budgets on native, js, wasm, and wasm-gc."
    - "Every target returns identical PathCommand kind/order/coordinates and identical structured invalid-input/data/capability/resource/state facts."
    - "The D-21 semantic fixture matrix and D-22 hostile/resource/mutation matrix have no untested item."
    - "The public matrix observes only shared Path2 and structured errors; private tests alone observe Q15, cursors, graph colors, descriptor/point numbering, and charge stages."
  </behavior>
  <action>
Add one permanent production public tracer before broad qualification. Build one checksum-correct generated font containing an empty glyph, a nontrivial simple glyph with repeated flags and implied midpoint, and a bounded one-level composite using a transform and a later real-point attachment. On each supported target, open the same bytes, obtain opaque glyph IDs, call the same `Font::outline` method with fresh caller Budgets, and assert exact complete PathCommand sequences and unchanged horizontal metric facts.

Audit the existing tests against every D-21 and D-22 item and add only missing or weak oracles. Public tests must freeze command kind/order/exact coordinates plus full CoreError category/code/operation/context/requested/limit fields for simple packing/lowering, composite placement/transforms/order, empty/degenerate glyphs, malformed streams/flags/references, cycles versus deeper capability, phantom/grid capability, USE_MY_METRICS neutrality, arithmetic, every semantic ceiling, max_work/Budget exact-fit/one-short, failure-path consumption, foreign IDs, and deterministic mid-query mutation. Private tests must freeze cursor positions, stored/expanded flags, maxp comparisons, exact signed Q15 matrix/midpoint/translation facts, encoded-real point numbering, tri-color transitions, command/allocation bounds, and taxonomy mapping.

Run native first, then run the complete same package selector separately on js, wasm, and wasm-gc with unique target directories and `--no-parallelize`. Do not use `moon test --target all` or the unscoped workspace Required lane on Windows; isolated `modules/mb-font` package results are the authoritative Phase 99 execution evidence. Do not add COVERAGE.md, licensed real-font evidence, target branches, or host adapters.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font test font --target native --frozen --target-dir target/phase99-final-native --no-parallelize; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font test font --target js --frozen --target-dir target/phase99-final-js --no-parallelize; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font test font --target wasm --frozen --target-dir target/phase99-final-wasm --no-parallelize; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font test font --target wasm-gc --frozen --target-dir target/phase99-final-wasm-gc --no-parallelize; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - "The permanent public tracer proves complete simple and composite Path2 extraction through identical APIs and facts on all four targets."
    - "Every D-21 semantic and D-22 hostile/resource/mutation item has an exact public or private oracle at its correct boundary."
    - "No target-specific implementation, external dependency/API, FFI, GUI state, host discovery, or partial-success convention appears."
    - "All four isolated package test commands pass with unique target directories."
  </acceptance_criteria>
  <done>The complete FONT-03 semantic, hostile, resource, mutation, and determinism matrix passes independently on native, js, wasm, and wasm-gc.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the final interface and freeze exact publication policy</name>
  <reversibility rating="costly">The generated public query and four limit fields become a compatibility promise consumed by downstream modules and policy automation.</reversibility>
  <files>modules/mb-font/font/pkg.generated.mbti, policy/foundation.json, scripts/quality/Assert-Policy.ps1</files>
  <read_first>
    - .planning/phases/99-simple-and-composite-outlines/99-01-SUMMARY.md
    - modules/mb-font/font/pkg.generated.mbti
    - modules/mb-font/font/moon.pkg
    - modules/mb-font/font/font.mbt
    - modules/mb-font/font/limits.mbt
    - policy/foundation.json:2181
    - scripts/quality/Assert-Policy.ps1:981
    - scripts/quality/Assert-Policy.ps1:1054
  </read_first>
  <action>
After Task 1 is green, run `moon -C modules/mb-font info --target all --frozen --target-dir target/phase99-final-interface` and keep the generated update. Compare its non-comment semantic lines against the repaired Phase 98 baseline recorded by Plan 99-01. The Phase 99-only delta must be exactly the generated math import, direct `Font::outline` signature, four outline-limit accessors, and expanded constructor parameters. Stop on any public raw table/cursor/maxp/loca/glyf, descriptor, graph, real/implied/phantom point, Q15, nested, hint, raster, or host symbol.

Update the existing mb-font policy entry in one scoped change: add `font/outline.mbt` to publication and production-source inventories in actual accepted order, add only `tchivs/mb-core/math` to allowed imports, update the module description, preserve `+js+wasm+wasm-gc+native` and the sole mb-font -> mb-core runtime dependency, and copy final generated semantic lines verbatim into `semantic_interface`.

Advance the independent PowerShell classifier from the Phase 98 boundary to the exact Phase 99 contract. Mirror publication/source/import sets, approve only the generated direct Path2 query and four numeric outline ceilings, and keep fail-closed rejection for a second outline DTO/method family, nested composites, hint/grid/raster/host/file/FFI APIs, and public parser/descriptor/Q15 internals. Preserve generated-interface exact-sequence comparison; do not make the classifier trust policy JSON for semantic approval.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font info --target all --frozen --target-dir target/phase99-final-interface; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json"</automated>
  </verify>
  <acceptance_criteria>
    - "The final generated interface is derived after Phase 99 code, compared to the repaired baseline, and contains exactly the intended additive public surface."
    - "Policy inventories outline.mbt and math while preserving all four targets and mb-core-only dependency."
    - "The independent selector accepts the exact direct Path2/limits contract and rejects private or deferred capabilities."
    - "Generated interface and policy classifier pass without hand-editing pkg.generated.mbti."
  </acceptance_criteria>
  <done>The Phase 99 public surface is generated, baseline-diffed, and frozen by exact independent publication policy.</done>
</task>

<task type="auto">
  <name>Task 3: Publish executable literate, changelog, and bilingual outline documentation</name>
  <files>modules/mb-font/README.mbt.md, modules/mb-font/CHANGELOG.md, README.md</files>
  <read_first>
    - modules/mb-font/README.mbt.md
    - modules/mb-font/CHANGELOG.md
    - README.md:26
    - README.md:129
    - modules/mb-font/font/pkg.generated.mbti
    - policy/foundation.json:2181
    - docs/rfcs/0004-mb-font.md:1
  </read_first>
  <action>
Update the literate package README frontmatter with the math package import and expand its executable FontLimits constructor with the four generated outline parameter names. Add a public example that opens immutable generated-style bytes, obtains a GlyphId, creates a fresh query Budget, calls `Font::outline`, and inspects shared Path2 commands. Document exact empty/simple/bounded-one-level outcomes, Q15-to-Double boundary, contour/component order, supported XY/point placement and transforms, offset defaults, unique geometry-neutral USE_MY_METRICS, lazy query charging, two revision guards, and InvalidInput/Data/Capability/Resource/State distinctions.

Narrow the deliberate exclusions exactly: deeper composite geometry, phantom attachment, grid rounding/hinting, rasterization, variations, CFF/CFF2, color/bitmap glyphs, shaping/layout/discovery, writing/editing, and Phase 100 licensed-real-font qualification remain outside the delivered contract. Extend the existing candidate changelog entry with the direct query, four limits, simple/composite semantics, exact Q15, and transactional hostile-input behavior.

Update the root README's English and Chinese mb-font responsibility/status text equivalently. Both languages must say complete unhinted simple and bounded one-level composite Path2 outlines and must not imply full real-font/four-target workflow qualification beyond this phase's generated package evidence. Run literate checks separately for native, js, wasm, and wasm-gc with unique target directories, then run the exact font policy selector. Do not run the known-stalling unscoped workspace lane.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font check README.mbt.md --target native --frozen --target-dir target/phase99-doc-native --serial; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font check README.mbt.md --target js --frozen --target-dir target/phase99-doc-js --serial; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font check README.mbt.md --target wasm --frozen --target-dir target/phase99-doc-wasm --serial; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font check README.mbt.md --target wasm-gc --frozen --target-dir target/phase99-doc-wasm-gc --serial; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json"</automated>
  </verify>
  <acceptance_criteria>
    - "The literate example compiles on all four targets using the exact generated query and limit names."
    - "Package README and changelog accurately describe all delivered D-01 through D-23 behavior and the five-way taxonomy."
    - "English and Chinese root text are semantically equivalent and do not claim Phase 100 evidence or deferred capabilities."
    - "All four isolated doc checks and exact font policy selector pass."
  </acceptance_criteria>
  <done>Phase 99 is executable, discoverable, and accurately documented under the exact portable publication contract.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Critical and high threats are blocking and must be mitigated; the phase's configured block threshold is high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| hostile generated fonts -> four target runtimes | Identical malicious structures must not diverge by backend or expose partial geometry. |
| private implementation -> generated public interface | A private decoder, graph, Q15, or deferred capability could accidentally become a compatibility promise. |
| generated interface -> policy/docs | Manual transcription or stale baselines can misclassify inherited or new public surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verification |
|---|---|---|---|---|---|---|
| T-99-03-01 | Tampering | all-target hostile font execution | high | mitigate | Run the same exact public/private malformed, arithmetic, mutation, and no-partial matrices independently on four targets. | Four isolated package test commands with identical facts. |
| T-99-03-02 | Denial of Service | all-target limits/work/Budget | high | mitigate | Freeze exact-fit/one-short and consumed-failure-work behavior for points, contours, components, instructions, work, and allocations. | Public resource matrix on each target. |
| T-99-03-03 | Tampering | generated interface drift | high | mitigate | Regenerate from a fresh target dir, compare to repaired Phase 98 baseline, and permit only the exact Phase 99 delta. | `moon info` plus semantic diff review. |
| T-99-03-04 | Elevation of Privilege | private/deferred API publication | high | mitigate | Independently reject parser, descriptor, graph, Q15, nested, hint, raster, host, file, and FFI public symbols. | Fail-closed PowerShell classifier and synthetic rejects. |
| T-99-03-05 | Spoofing | documentation overclaim | medium | mitigate | Keep executable literate API names and synchronized bilingual delivered/excluded scope. | Four-target doc checks and manual exact-scope comparison. |
| T-99-03-06 | Repudiation | structured failure collapse | medium | mitigate | Document and test InvalidInput/Data/Capability/Resource/State facts; never describe error as empty success. | Public error-field assertions and docs taxonomy. |
| T-99-03-SC | Tampering | package/dependency supply chain | low | mitigate | No install occurs; frozen commands and exact policy preserve the sole mb-core dependency and four targets. | Policy import/dependency/target checks. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Complete simple and bounded one-level composite Path2 extraction without partial failure | 99-01, 99-02, 99-03 | COVERED |
| REQ | FONT-03 | Valid simple/composite outlines, checked arithmetic, transactional failure | 99-01, 99-02, 99-03 | COVERED |
| RESEARCH | — | Lazy guarded query, admitted windows/maxp, exact Q15, iterative graph, bounded charging | 99-01, 99-02 | COVERED |
| RESEARCH | — | Two-baseline interface workflow, independent policy, docs, isolated four-target gates | 99-01, 99-03 | COVERED |
| CONTEXT | D-01,D-02,D-03 | Direct shared Path2 query, independent Budget, receiving-font validation, two guards | 99-01, 99-03 | COVERED |
| CONTEXT | D-04,D-05,D-06,D-07 | Exact simple stream, preserved geometry, quadratic lowering, empty/degenerate/no-hint | 99-01, 99-03 | COVERED |
| CONTEXT | D-08,D-09 | One-level graph classification and XY/real-point placement | 99-02, 99-03 | COVERED |
| CONTEXT | D-10,D-11,D-12 | Checked Q15, all transforms/matrix order, exact offset/grid semantics | 99-01, 99-02, 99-03 | COVERED |
| CONTEXT | D-13,D-14,D-15 | Component order/flags, phantom taxonomy, geometry-neutral USE_MY_METRICS | 99-02, 99-03 | COVERED |
| CONTEXT | D-16,D-17,D-18 | Lazy decode, four explicit limits, max_work/Budget and precharging | 99-01, 99-02, 99-03 | COVERED |
| CONTEXT | D-19,D-20 | Private transactional state and stable five-way taxonomy | 99-01, 99-02, 99-03 | COVERED |
| CONTEXT | D-21,D-22,D-23 | Generated semantic/hostile fixtures, public/private tests, interface/policy/docs/four targets | 99-01, 99-02, 99-03 | COVERED |

The unclassified specless probe is explicitly resolved through the complete ROADMAP plus D-01-through-D-23 executable matrix. No item is silently dismissed. CONTEXT deferred items and REQUIREMENTS future/out-of-scope capabilities remain exclusions, and no COVERAGE.md is required.
</source_audit>

<verification>
1. Run one permanent public simple-plus-composite tracer independently on native, js, wasm, and wasm-gc.
2. Run the complete package tests separately per target with unique target dirs and `--no-parallelize`; treat these isolated font gates as authoritative on Windows.
3. Regenerate the final interface after code/tests and compare it with the repaired Phase 98 baseline before updating exact policy.
4. Run four separate literate doc checks and the independent font policy selector.
5. Do not prescribe the known-stalling unscoped mb-image/png workspace lane and do not create COVERAGE.md.
</verification>

<success_criteria>
- FONT-03's four ROADMAP criteria and every D-01 through D-23 decision have executable all-target or exact publication evidence.
- The final interface contains only the direct Path2 query and four outline ceilings beyond the repaired Phase 98 baseline.
- Exact policy preserves pure MoonBit, four targets, mb-core-only dependency, and rejects private/deferred capabilities.
- Literate, changelog, English, and Chinese documentation accurately describe delivered and excluded behavior.
- All isolated package, interface, policy, and documentation gates pass without relying on the known-stalling full workspace lane.
</success_criteria>

<output>
Create `.planning/phases/99-simple-and-composite-outlines/99-03-SUMMARY.md` when done.
</output>
