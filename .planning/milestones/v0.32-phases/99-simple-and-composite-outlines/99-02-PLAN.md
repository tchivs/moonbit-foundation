---
phase: 99-simple-and-composite-outlines
plan: 02
type: execute
wave: 2
depends_on:
  - 99-01
files_modified:
  - modules/mb-font/font/outline.mbt
  - modules/mb-font/font/metrics.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/generated_fonts_wbtest.mbt
autonomous: true
requirements:
  - FONT-03
must_haves:
  decision_coverage:
    - "D-01: preserve the direct shared Path2 query while adding composites."
    - "D-02: preserve independent caller Budget authorization."
    - "D-03: preserve two revision guards and atomic publication."
    - "D-08: one-level simple/empty children with cycle-before-deeper classification."
    - "D-09: first-component XY and later XY/real-point attachment after child transform."
    - "D-10: exact private checked Int64 Q15 placement."
    - "D-11: uniform, independent, and 2x2 transforms with exact matrix order."
    - "D-12: scaled/unscaled/default offsets and grid-rounding Capability."
    - "D-13: preserved component order and strict metadata/flag behavior."
    - "D-14: real-point attachment with explicit phantom/out-of-range taxonomy."
    - "D-15: at most one geometry-neutral USE_MY_METRICS component."
    - "D-17: retained points/contours/components/instructions/max_work ceilings."
    - "D-18: precharged graph, descriptor, scratch, and output work."
    - "D-19: private graph/placement/commands and one complete Path2."
    - "D-20: stable five-way composite error taxonomy."
    - "D-21: generated placement, transform, offset, and Q15 fixtures."
    - "D-22: hostile references, cycles, nesting, phantom, arithmetic, resource, and mutation fixtures."
    - "D-23: exact public commands and private graph/Q15/numbering/charging tests."
  truths:
    - "A one-level composite parent accepts only simple/empty direct children; bounded iterative descriptor traversal classifies any reachable cycle as Data before classifying a structurally valid deeper acyclic composite as Capability, and it never recursively lowers deeper geometry (D-08)."
    - "The first component uses signed XY placement; later components use signed XY or real-parent-point to real-child-contour-point attachment after transforming the child, with implied points excluded from numbering (D-09, D-14)."
    - "Uniform, independent x/y, and general 2x2 transforms use mutually exclusive flags and exact OpenType matrix ordering over checked Int64 Q15; all points, offsets, and attachment translations convert to Double only during final Path2 assembly (D-10, D-11)."
    - "Scaled component offsets are transformed, unscaled/default offsets remain parent design units, both offset flags are Data, and ROUND_XY_TO_GRID is Capability (D-12)."
    - "Component/contour order and winding are preserved; OVERLAP_COMPOUND is geometry-neutral metadata and reserved/contradictory flags are Data (D-13)."
    - "Real point attachment is supported, any of four phantom references is Capability, larger indices are Data, and at most one structurally valid USE_MY_METRICS marker is accepted without changing Path2 or horizontal_metrics (D-14, D-15)."
    - "Component/graph discovery, simple-child decoding, transforms, attachment, scratch/output allocation, and final publication obey retained limits, max_work, caller Budget, stable taxonomy, and no-partial-geometry semantics (D-17, D-18, D-19, D-20)."
    - "Generated public/private tests freeze every supported placement/transform, Q15 fact, graph classification, phantom/USE_MY_METRICS outcome, hostile flag/reference/cycle/arithmetic/budget edge, and post-decode mutation result (D-21, D-22, D-23)."
  flagged_assumptions:
    - "The specless FONT-03 probe was unclassified. This plan resolves its composite portion through exact D-08 through D-15 semantics plus the ROADMAP bounded-one-level criterion; nested lowering and phantom placement remain explicitly unsupported rather than silently approximated."
  prohibitions:
    - "No composite implementation may recurse into nested geometry."
    - "No implied midpoint may enter the TrueType component point-number space."
    - "No USE_MY_METRICS flag may rewrite Path2 coordinates or admitted horizontal metrics."
    - "No cycle or malformed descriptor may be downgraded to an unsupported-capability result."
  artifacts:
    - path: modules/mb-font/font/outline.mbt
      provides: "Iterative descriptor-graph classifier, exact Q15 transforms, XY/point placement, phantom/metrics handling, and composite lowering."
    - path: modules/mb-font/font/metrics.mbt
      provides: "One reusable table-local glyph-window helper shared by metric bounds and outline decoding."
    - path: modules/mb-font/font/generated_fonts_wbtest.mbt
      provides: "Checksum-correct semantic glyf/loca/maxp builders for simple and composite graphs."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Public exact PathCommand and structured composite outcome/resource/mutation matrix."
    - path: modules/mb-font/font/font_wbtest.mbt
      provides: "Private matrix-order, Q15, real-point numbering, graph-state, and failure-charging evidence."
  key_links:
    - from: modules/mb-font/font/outline.mbt
      to: modules/mb-font/font/generated_fonts_wbtest.mbt
      via: "Generated descriptors independently exercise flags, arguments, transforms, references, nesting, cycles, and maxp facts."
    - from: modules/mb-font/font/outline.mbt
      to: modules/mb-font/font/limits.mbt
      via: "Every inspected descriptor and accumulated point/contour is bounded by retained component/outline ceilings and caller Budget."
    - from: modules/mb-font/font/outline.mbt
      to: modules/mb-core/math/path.mbt
      via: "Placed real Q15 child points lower in component/contour order into one private complete Path2."
---

## Artifacts this phase produces

| File | Created or changed symbols |
|---|---|
| `modules/mb-font/font/metrics.mbt` | Add private `font_glyph_window(index : MetricIndexFacts, glyph_index : UInt64) -> Result[Option[ByteView], @error.CoreError]`; change the existing `font_read_glyph_bounds(...)` path to consume this same admitted table-local view, with `None` representing equal adjacent `loca` offsets. |
| `modules/mb-font/font/outline.mbt` | Extend the Plan 99-01 private outline state with composite descriptor facts, a tri-color graph state (`Unvisited`, `Visiting`, `Done`), exact raw-F2DOT14 matrix facts, and component placement facts for signed XY or real-point attachment. These private type identifiers remain discretionary, but their fields concretely include component flags, referenced glyph, signed arguments, `xscale_raw`, `scale01_raw`, `scale10_raw`, `yscale_raw`, placement kind, transformed real child points, and accumulated parent real-point indices. |
| `modules/mb-font/font/outline.mbt` | Add `transform_q15(point : OutlinePoint, matrix : F2Dot14Matrix) -> Result[OutlinePoint, @error.CoreError]`; add a private bounded graph-classification operation `(root glyph, MetricIndexFacts, MaxpFacts, FontLimits, Budget) -> Result[deeper_acyclic : Bool, @error.CoreError]`; add a private placement operation `(descriptor, transformed child real points, accumulated parent real points) -> Result[placed child real points, @error.CoreError]`; extend `font_decode_outline(...)` to classify the entire reachable graph before lowering only direct simple/empty children into one complete `Path2`. The latter two identifiers may follow local conventions, but these signatures and ownership boundaries are required. |
| `modules/mb-font/font/generated_fonts_wbtest.mbt` | Add checksum-correct composite descriptor/graph fixture builders covering uniform, independent-axis, and 2x2 transforms; scaled/unscaled/default offsets; XY and real-point attachment; component order; phantom/out-of-range indices; unique/duplicate `USE_MY_METRICS`; self/multi-cycle and deeper-acyclic graphs. |
| `modules/mb-font/font/font_test.mbt` | Add public `test` cases that call the unchanged `Font::outline` signature and freeze exact composite `PathCommand` order/coordinates plus Data, Capability, Resource, and State outcomes for the complete D-08-through-D-15 profile. |
| `modules/mb-font/font/font_wbtest.mbt` | Add private `test` cases for raw F2DOT14 coefficients, OpenType cross-term ordering, exact Q15 transforms/translations, encoded-real point numbering, transform-before-attachment, tri-color transitions, descriptor/work charges, and late-failure no-publication. |

<objective>
Extend the proven simple-outline transaction into the complete bounded one-level composite profile.

Purpose: FONT-03 requires interoperable component placement and exact failure classification without allowing nested recursion, phantom invention, or partial geometry.
Output: Iterative composite graph classification, exact Q15 transforms and attachments, generated semantic/hostile composite fixtures, and complete native public/private evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-simple-and-composite-outlines/99-CONTEXT.md
@.planning/phases/99-simple-and-composite-outlines/99-RESEARCH.md
@.planning/phases/99-simple-and-composite-outlines/99-PATTERNS.md
@.planning/phases/99-simple-and-composite-outlines/99-01-SUMMARY.md

<interfaces>
Consume the Plan 99-01 contracts without changing their public shape:
- `Font::outline(Self, GlyphId, @budget.Budget) -> Result[@math.Path2, @error.CoreError]`
- private exact-Q15 simple point/contour representation and lowerer
- retained seven-field version-1 `MaxpFacts` outline maxima
- four explicit FontLimits outline ceilings
- guarded pre-read/decode/post-read publication coordinator

Composite descriptors remain private. Preserve the OpenType flag encodings and matrix order from `99-RESEARCH.md`; do not publish graph, descriptor, Q15, phantom, maxp, loca, or glyf facts.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one XY-placed uniformly scaled simple component through Font::outline</name>
  <files>modules/mb-font/font/outline.mbt, modules/mb-font/font/metrics.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/generated_fonts_wbtest.mbt</files>
  <read_first>
    - .planning/phases/99-simple-and-composite-outlines/99-01-SUMMARY.md
    - modules/mb-font/font/outline.mbt
    - modules/mb-font/font/metrics.mbt:1
    - modules/mb-font/font/metrics.mbt:361
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/generated_fonts_wbtest.mbt
    - modules/mb-font/font/tables.mbt:12
    - modules/mb-font/font/limits.mbt
    - modules/mb-core/math/path.mbt:1
  </read_first>
  <behavior>
    - "One checksum-correct one-level parent references one admitted simple child, applies signed XY placement plus uniform F2DOT14 scale, and returns exact transformed PathCommands through the unchanged public method."
    - "The child is transformed in Int64 Q15 before translation, component/contour order is preserved, and only final Point2 construction uses Double."
    - "The first component without XY arguments, a missing/out-of-range child glyph, or a malformed transform flag combination returns Data with no Path2."
    - "A composite result remains protected by the same independent Budget and pre/post source guards as a simple result."
  </behavior>
  <action>
First factor the existing normalized `loca` pair and retained table-local `glyf` subview logic from `font_read_glyph_bounds` into one private `font_glyph_window` helper in `metrics.mbt`. Return None for equal adjacent offsets and a contained ByteView otherwise; keep common-header validation in the bounds reader. Make both metrics and outline decoding consume this helper so composite child lookup cannot grow a second root-offset path.

Then extend the generated checksum-correct fixture seam with semantic constructors for a simple glyph body, composite component record, normalized loca offsets, glyph count, and the seven maxp outline maxima. Keep fixtures deterministic, pure MoonBit, table-aligned, and independent from production parser helpers.

Write a failing black-box tracer for one composite parent whose first record uses signed XY arguments and uniform F2DOT14 scale. Extend the private decoder to classify a composite header, parse the bounded direct descriptor, require XY placement for the first component, resolve an admitted simple/empty child, transform its encoded real Q15 points with exact checked matrix arithmetic, apply the translation, and lower the placed contours through the existing simple lowerer. Preserve the unchanged D-01 through D-03 guarded public transaction and D-17 through D-20 resource/error behavior.

This tracer is production geometry: implement contained child lookup, exact signed arguments/coefficient parsing, mutual transform-flag exclusion, checked matrix numerators/division, maxp/component ceiling checks, and private accumulation. Do not add point attachment, deeper geometry, phantom synthesis, grid rounding, floating intermediate transforms, hinting, or another public method in this task.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font test font --target native --frozen --target-dir target/phase99-composite-tracer --no-parallelize -f '*outline*'; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - "The permanent generated parent-to-child tracer returns the exact uniformly scaled and translated PathCommand sequence."
    - "First-component XY, child range, transform flags, Q15 conversion, limits/Budget, and source guards are real production checks."
    - "Malformed tracer variants fail atomically with stable structured Data/Resource/State outcomes."
    - "Focused isolated native outline tests pass."
  </acceptance_criteria>
  <done>A library author can extract one exact, guarded, resource-bounded one-level composite Path2 through the same public query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete graph classification, placements, transforms, phantom/metrics rules, and hostile composites</name>
  <files>modules/mb-font/font/outline.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt, modules/mb-font/font/generated_fonts_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/outline.mbt
    - modules/mb-font/font/font_test.mbt
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-font/font/generated_fonts_wbtest.mbt
    - modules/mb-font/font/tables.mbt:507
    - modules/mb-font/font/limits.mbt
    - modules/mb-core/budget/budget.mbt:320
  </read_first>
  <behavior>
    - "Iterative tri-color traversal detects self and multi-glyph cycles as Data, validates every reachable descriptor, and returns Capability only after proving a deeper graph acyclic."
    - "Later components support signed XY or real-point attachment; attachment uses transformed child encoded points, excludes implied points, and preserves accumulated component order."
    - "Uniform, independent x/y, and 2x2 rotation/shear facts follow exact OpenType matrix ordering; scaled/unscaled/default offsets have the locked distinct results."
    - "ROUND_XY_TO_GRID and real-or-child phantom indices are Capability; indices beyond real plus four phantom points are Data."
    - "At most one USE_MY_METRICS marker is accepted as geometry-neutral metadata; duplicates/contradictions are Data and horizontal_metrics remains unchanged."
    - "Reserved flags, invalid references, arithmetic edges, component/instruction/maxp/limit/work/Budget edges, and post-decode mutation fail without partial commands."
  </behavior>
  <action>
Write RED public and white-box matrices for every D-08 through D-15 branch. Implement a dense tri-color glyph state plus explicit descriptor-frame stack, precharging its allocation and every descriptor scan. Traverse the reachable descriptor graph far enough to validate flags, references, instruction envelopes, and maxp depth/elements; a Visiting edge is Data, while a complete acyclic graph containing composite grandchildren is Capability. Never recursively lower geometry or let early nesting classification hide a cycle/malformed record.

Complete component placement in record order. Decode every direct simple/empty child into encoded real Q15 points and contour endpoints, apply uniform/independent/2x2 matrices using serialized `xscale, scale01, scale10, yscale` ordering, then compute placement. For XY arguments, transform the offset only when SCALED_COMPONENT_OFFSET is set; leave it in parent design units for UNSCALED_COMPONENT_OFFSET or neither; reject both and classify ROUND_XY_TO_GRID as Capability. For point arguments, reject the first component, address only previously accumulated parent real points and current child real points, transform child first, and translate by parent minus child. Implied midpoint commands never enter either point index space.

Implement D-13 through D-15 exactly: append components/contours without reordering or overlap processing; accept OVERLAP_COMPOUND as metadata; reject reserved/contradictory flags; classify an index in the four-phantom interval as Capability and a larger index as Data; accept and structurally validate no more than one USE_MY_METRICS marker without changing Path2 or horizontal_metrics.

Expand generated fixtures and assertions to all D-21/D-22 composite cases: empty/simple children, component order, signed XY, point attachment, all transform forms and matrix cross-terms, offset scaling/default, overlap metadata, reserved flags, bad glyph/point indices, self/multi cycles, valid deeper acyclic nesting, phantom parent/child attachment, duplicate metrics flags, arithmetic boundaries, component/instruction/point/contour/maxp claims, exact-fit/one-short limits/work/Budget, consumed failure work, and mid-query mutation. Private tests freeze raw F2DOT14, exact Q15 products/sums/midpoints/translations, real-point numbering, tri-color transitions, and Data/Capability precedence; public tests freeze complete PathCommand sequences and CoreError facts (D-17 through D-23).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; moon -C modules/mb-font check --target native --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-font test font --target native --frozen --target-dir target/phase99-composite-full --no-parallelize -f '*outline*'; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - "Every supported XY/point placement, matrix, offset policy, order/winding, and metadata flag has an exact executable oracle."
    - "Cycles and malformed reachable graphs are Data; only fully validated deeper acyclic graphs, phantom attachment, and grid rounding are Capability."
    - "Point numbering uses encoded real points only, transform precedes attachment, and Q15 facts are exact before Double."
    - "USE_MY_METRICS is unique and geometry/metric neutral; phantom references are never synthesized."
    - "All hostile/resource/mutation cases fail atomically and isolated native composite tests pass."
  </acceptance_criteria>
  <done>The complete bounded one-level composite profile is implemented and proven on native without nested lowering, phantom invention, or partial geometry.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. Critical and high threats are blocking and must be mitigated; the phase's configured block threshold is high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted composite records -> graph/placement engine | Flags, references, arguments, matrices, nesting, and instructions control traversal and arithmetic. |
| referenced glyph graph -> one-level geometry | Malicious cycles or deeper graphs must be classified without recursion or partial lowering. |
| private Q15 points -> public Path2 | Incorrect numbering/order/rounding can silently corrupt public geometry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verification |
|---|---|---|---|---|---|---|
| T-99-02-01 | Denial of Service | descriptor graph traversal | high | mitigate | Use precharged iterative tri-color traversal, explicit stack, component/depth/work ceilings, and no recursive geometry. | Self/multi-cycle, deep acyclic, exact-fit/one-short tests. |
| T-99-02-02 | Tampering | F2DOT14 transform/attachment math | high | mitigate | Use checked signed Int64 Q15 with exact matrix ordering and transform-before-translation/attachment. | Private cross-term, overflow, offset, and attachment Q15 tests. |
| T-99-02-03 | Tampering | component point references | high | mitigate | Keep encoded real-point arrays distinct from implied commands and classify real/phantom/out-of-range indices explicitly. | Public parent/child/phantom/bad-index matrix. |
| T-99-02-04 | Tampering | descriptor flags and USE_MY_METRICS | high | mitigate | Validate reserved/contradictory flags, exclusive transforms/offsets, and unique geometry-neutral metrics marker. | Generated flag-combination and horizontal-metrics stability tests. |
| T-99-02-05 | Tampering | partial composite publication | high | mitigate | Accumulate graph, placements, points, and commands privately; publish only after all checks and post-read guard. | Late-failure and mutation tests assert no Path2 return. |
| T-99-02-06 | Repudiation | cycle versus unsupported nesting ambiguity | medium | mitigate | Complete reachable structural traversal so cycle/Data takes precedence over deeper-acyclic/Capability. | White-box graph transition and black-box category tests. |
| T-99-02-SC | Tampering | generated fixture oracle | low | accept | Pure MoonBit checksum-correct builders are independent from production decoding and add no dependency. | Builder checksum/open tests and frozen generated bytes. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Bounded one-level composite Path2 extraction without partial failure | Tasks 1-2 | COVERED |
| REQ | FONT-03 | Valid composite outlines, checked arithmetic, transactional failure | Tasks 1-2 | COVERED |
| RESEARCH | — | Iterative descriptor graph, exact Q15 transform, real-point numbering, transform-before-attachment | Tasks 1-2 | COVERED |
| RESEARCH | — | One reusable admitted table-local glyph-window helper for metric and outline reads | Task 1 | COVERED |
| RESEARCH | — | maxp/FontLimits/Budget ordering and generated independent micro-font oracles | Tasks 1-2 | COVERED |
| CONTEXT | D-08 | One-level simple/empty children; cycle Data versus deeper-acyclic Capability | Task 2 | COVERED |
| CONTEXT | D-09 | First XY; later XY or transformed-child real-point attachment | Tasks 1-2 | COVERED |
| CONTEXT | D-10,D-11,D-12 | Exact Q15, all matrix forms/order, scaled/unscaled/default offsets, grid Capability | Tasks 1-2 | COVERED |
| CONTEXT | D-13,D-14,D-15 | Component order/metadata flags, phantom taxonomy, unique geometry-neutral USE_MY_METRICS | Task 2 | COVERED |
| CONTEXT | D-17,D-18,D-19,D-20 | Limits/work/Budget, precharging, private publication, error taxonomy | Tasks 1-2 | COVERED |
| CONTEXT | D-21,D-22,D-23 | Complete generated composite/hostile/Q15/public/private evidence | Tasks 1-2 | COVERED |

Plan 99-01 owns the direct query and complete simple decoder. Plan 99-03 owns final four-target/interface/policy/documentation closure. Nested geometry, phantom placement, grid fitting/hinting, rasterization, and Phase 100 real-font qualification remain explicit exclusions.
</source_audit>

<verification>
1. Run the permanent one-component XY/scale tracer first through the unchanged public query.
2. Run isolated native check and focused outline tests with unique target directories and `--no-parallelize`.
3. Require exact public commands and private Q15/graph/point-numbering/charging facts for the full matrix.
4. Confirm no recursive lowering, public descriptor/Q15 facts, instruction execution, FFI, host discovery, or extra dependency.
</verification>

<success_criteria>
- Library authors can extract exact complete one-level composite Path2 outlines using all supported placements and transforms.
- Cycle, deeper nesting, phantom, grid rounding, malformed bytes/references/flags, arithmetic, resource, and mutation outcomes match the locked taxonomy.
- All composite work is bounded and transactional; component/contour order and horizontal metrics remain stable.
- Isolated native public/private gates pass without using the known-stalling unscoped Windows workspace lane.
</success_criteria>

<output>
Create `.planning/phases/99-simple-and-composite-outlines/99-02-SUMMARY.md` when done.
</output>
