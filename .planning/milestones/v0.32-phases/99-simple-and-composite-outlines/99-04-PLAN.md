---
phase: 99-simple-and-composite-outlines
plan: 04
type: execute
wave: 4
depends_on:
  - 99-03
files_modified:
  - modules/mb-font/moon.mod.json
  - scripts/quality/Assert-Policy.ps1
  - modules/mb-font/README.mbt.md
autonomous: true
gap_closure: true
requirements:
  - FONT-03
must_haves:
  decision_coverage:
    - "D-20: maxp underclaims remain Data while retained FontLimits, max_work, and caller Budget exhaustion remain Resource."
    - "D-23: the focused font selector, repository-wide policy gate, and executable README agree on the exact Phase 99 publication contract."
  truths:
    - "The mb-font manifest description is byte-for-byte identical to the approved Phase 99 description in policy/foundation.json, and both focused and repository-wide policy gates pass (D-23)."
    - "Assert-FontFoundationPolicy independently rejects a future mb-font manifest-description drift instead of relying on callers to remember the repository-wide selector (D-23)."
    - "The public README classifies an actual glyph exceeding a maxp claim as Data and reserves Resource for retained FontLimits, cumulative max_work, or caller Budget exhaustion (D-20)."
    - "The focused font selector fails if the README moves maxp underclaims back into Resource or omits the retained-limit/work/Budget resource contract (D-20, D-23)."
  artifacts:
    - path: modules/mb-font/moon.mod.json
      provides: "The exact approved Phase 99 module description."
    - path: scripts/quality/Assert-Policy.ps1
      provides: "Focused manifest-description and README error-taxonomy drift detection."
    - path: modules/mb-font/README.mbt.md
      provides: "Accurate five-way public outline error taxonomy."
  key_links:
    - from: modules/mb-font/moon.mod.json
      to: policy/foundation.json
      via: "Assert-FontFoundationPolicy performs a case-sensitive manifest-description equality check."
    - from: modules/mb-font/README.mbt.md
      to: modules/mb-font/font/font_test.mbt
      via: "The documented maxp/Data and FontLimits/max_work/Budget/Resource split matches the executable structured-error assertions."
---

## Artifacts this gap plan produces

| File | Created or changed symbols / records |
|---|---|
| `modules/mb-font/moon.mod.json` | Replace only the stale Phase 98 `description` value with the exact `tchivs/mb-font` Phase 99 description already approved in `policy/foundation.json`; preserve name, version, targets, dependency, license, repository, and readme metadata. |
| `scripts/quality/Assert-Policy.ps1` | Extend `Assert-FontFoundationPolicy -PolicyPath <path>` with a case-sensitive comparison between the selected font policy description and `modules/mb-font/moon.mod.json`, plus focused README taxonomy assertions that bind maxp underclaims to Data and retained `FontLimits`/`max_work`/caller `Budget` exhaustion to Resource. |
| `modules/mb-font/README.mbt.md` | Correct only the outline failure taxonomy: actual glyph facts exceeding `maxp` claims are malformed Data; retained `FontLimits`, cumulative `max_work`, and caller `Budget` exhaustion are Resource. |

<objective>
Close the two Phase 99 publication gaps without changing the already-verified FONT-03 implementation or public interface.

Purpose: The outline runtime passes on all targets, but Phase 99 cannot close while its manifest disagrees with policy and its public error taxonomy contradicts executable behavior.
Output: Synchronized mb-font metadata, focused drift detection, accurate README taxonomy, and fresh focused/repository-wide closure evidence for D-20 and D-23.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-simple-and-composite-outlines/99-CONTEXT.md
@.planning/phases/99-simple-and-composite-outlines/99-RESEARCH.md
@.planning/phases/99-simple-and-composite-outlines/99-PATTERNS.md
@.planning/phases/99-simple-and-composite-outlines/99-03-PLAN.md
@.planning/phases/99-simple-and-composite-outlines/99-03-SUMMARY.md
@.planning/phases/99-simple-and-composite-outlines/99-VERIFICATION.md

<interfaces>
Approved module description source of truth:
- `policy/foundation.json` module `tchivs/mb-font`, field `description`
- exact current value: `Portable bounded standalone TrueType admission with named metrics, deterministic Unicode mapping, basic legacy horizontal kerning, and transactional simple plus one-level composite Path2 outlines for MoonBit Native Foundation.`

Existing policy entry points:
- `Assert-FontFoundationPolicy -PolicyPath <path>` at `scripts/quality/Assert-Policy.ps1`
- `Assert-FoundationPolicy -PolicyPath <path>` at `scripts/quality/Assert-Policy.ps1`

Executable taxonomy oracle:
- `modules/mb-font/font/font_test.mbt` test `outline maxp claims bound actual simple facts`
- expected maxp point-underclaim result: `ErrorCategory::Data`, `ErrorCode::InvalidEncoding`, operation `font-outline`, context `font-outline-maxp-points`
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the focused font gate expose manifest drift, then synchronize the approved description</name>
  <files>scripts/quality/Assert-Policy.ps1, modules/mb-font/moon.mod.json</files>
  <read_first>
    - .planning/phases/99-simple-and-composite-outlines/99-VERIFICATION.md
    - policy/foundation.json:2180
    - modules/mb-font/moon.mod.json
    - scripts/quality/Assert-Policy.ps1:697
    - scripts/quality/Assert-Policy.ps1:1061
  </read_first>
  <behavior>
    - "With the current stale manifest, the focused selector fails with `Manifest description drift in modules/mb-font`."
    - "After synchronization, the manifest and selected policy description compare case-sensitively equal."
    - "A temporary policy fixture whose font description differs from the manifest is rejected by the focused selector with the same drift diagnostic."
    - "The real focused font selector and repository-wide foundation selector both pass."
  </behavior>
  <action>
Add the missing focused assertion before changing the manifest. In `Assert-FontFoundationPolicy`, resolve `modules/mb-font/moon.mod.json` from the existing repository root, parse it with `Read-QualityJson`, and compare its `description` case-sensitively with `$fontModule.description`. Use the repository-wide gate's established diagnostic `Manifest description drift in modules/mb-font.` so both selectors report the same contract failure. Do not duplicate or weaken the existing name/version/target/dependency checks owned by `Assert-FoundationPolicy`.

Before editing `moon.mod.json`, run these statements directly in the current PowerShell session: set `$ErrorActionPreference = 'Stop'`, dot-source `./scripts/quality/Assert-Policy.ps1`, then call `Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json`. This is the failing-first policy check: require a terminating error whose exception contains `Manifest description drift in modules/mb-font`; stop if it passes or fails for another reason. Do not wrap these statements in a nested string-valued `-Command` invocation.

Then replace only `modules/mb-font/moon.mod.json`'s description with the exact approved string from the selected `tchivs/mb-font` record in `policy/foundation.json`. Do not edit policy JSON, package targets, direct dependencies, the generated interface, implementation, tests, or prior plans. After the real data is green, exercise the new focused assertion with an in-memory/temporary copied policy whose font description is deliberately changed; require that negative fixture to fail on the manifest-description diagnostic, remove the temporary file, and run both real policy entry points.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
. ./scripts/quality/Assert-Policy.ps1
$policy = Get-Content -Raw -LiteralPath ./policy/foundation.json | ConvertFrom-Json
$font = @($policy.modules | Where-Object { $_.name -ceq 'tchivs/mb-font' })[0]
$manifest = Get-Content -Raw -LiteralPath ./modules/mb-font/moon.mod.json | ConvertFrom-Json
if ($manifest.description -cne $font.description) {
  throw 'Manifest description drift in modules/mb-font.'
}
$temp = New-TemporaryFile
try {
  $font.description = 'intentional manifest-description drift fixture'
  $policy | ConvertTo-Json -Depth 100 | Set-Content -LiteralPath $temp -Encoding utf8
  $failure = $null
  try {
    Assert-FontFoundationPolicy -PolicyPath $temp
  } catch {
    $failure = $_.Exception.Message
  }
  if ($null -eq $failure -or $failure -cnotmatch 'Manifest description drift in modules/mb-font') {
    throw 'Focused font policy accepted manifest-description drift.'
  }
} finally {
  Remove-Item -LiteralPath $temp -Force -ErrorAction SilentlyContinue
}
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
Assert-FoundationPolicy -PolicyPath ./policy/foundation.json
    </automated>
  </verify>
  <acceptance_criteria>
    - "The focused gate demonstrably fails against the pre-fix manifest and against an isolated drift fixture."
    - "The real manifest description exactly equals the approved policy description."
    - "No policy JSON, public interface, target, dependency, or implementation surface changes."
    - "Both focused and repository-wide policy gates pass on the synchronized repository."
  </acceptance_criteria>
  <done>The Phase 99 module metadata is internally consistent, and either closure gate now detects a future description drift per D-23.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the documented maxp/Data and resource-exhaustion taxonomy, then correct the README</name>
  <files>scripts/quality/Assert-Policy.ps1, modules/mb-font/README.mbt.md</files>
  <read_first>
    - .planning/phases/99-simple-and-composite-outlines/99-VERIFICATION.md
    - modules/mb-font/README.mbt.md:232
    - modules/mb-font/font/font_test.mbt:2280
    - modules/mb-font/font/outline.mbt:502
    - scripts/quality/Assert-Policy.ps1:1061
  </read_first>
  <behavior>
    - "The focused selector rejects the current README because its Resource bullet includes maxp."
    - "The Data taxonomy names actual glyph facts exceeding maxp claims as malformed font data."
    - "The Resource taxonomy names retained FontLimits, cumulative max_work, and caller Budget exhaustion and does not include maxp."
    - "The corrected prose remains executable on native, js, wasm, and wasm-gc and both policy selectors pass."
  </behavior>
  <action>
Add the documentation oracle before correcting the prose. In `Assert-FontFoundationPolicy`, use the existing `$readmeText` and isolate the `Data` and `Resource` bullets from the five-way outline taxonomy rather than searching the whole file. Assert that the Data bullet contains `maxp` and an underclaim/exceeds-claim meaning. Assert that the Resource bullet contains `FontLimits`, `max_work`, and `Budget`, and rejects `maxp`. Give each failure a focused diagnostic that identifies the incorrect Data or Resource taxonomy.

Run the focused selector immediately after adding those assertions and before editing the README. Require it to fail specifically because the current Resource bullet contains maxp; this is the failing-first documentation check. Then correct only the relevant bullets in `modules/mb-font/README.mbt.md`: state that actual glyph point/contour/component/instruction facts exceeding admitted `maxp` claims are malformed `Data`, while exhaustion of retained `FontLimits`, cumulative `max_work`, or the caller's query `Budget` is `Resource` (D-20). Preserve InvalidInput, Capability, State, executable examples, Q15 wording, exclusions, and the Phase 100 evidence boundary.

Run the focused selector, all four isolated literate README checks with unique target directories, and the repository-wide selector. Do not change `outline.mbt`, `font_test.mbt`, public signatures, generated fixtures, or `99-VERIFICATION.md`; those files are read-only behavioral evidence for this closure.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
$readme = Get-Content -Raw -LiteralPath ./modules/mb-font/README.mbt.md
$data = [regex]::Match($readme, '(?ms)^- `Data`.*?(?=^- `Capability`)').Value
$resource = [regex]::Match($readme, '(?ms)^- `Resource`.*?(?=^- `State`)').Value
if (
  [string]::IsNullOrWhiteSpace($data) -or
  $data -cnotmatch 'maxp' -or
  $data -cnotmatch '(underclaim|exceed)'
) {
  throw 'Font README Data taxonomy does not classify maxp underclaims.'
}
if (
  [string]::IsNullOrWhiteSpace($resource) -or
  $resource -cnotmatch 'FontLimits' -or
  $resource -cnotmatch 'max_work' -or
  $resource -cnotmatch 'Budget' -or
  $resource -cmatch 'maxp'
) {
  throw 'Font README Resource taxonomy must contain only retained FontLimits, max_work, and caller Budget exhaustion.'
}
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
moon -C modules/mb-font check README.mbt.md --target native --frozen --target-dir target/phase99-gap04-doc-native --serial
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font check README.mbt.md --target js --frozen --target-dir target/phase99-gap04-doc-js --serial
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font check README.mbt.md --target wasm --frozen --target-dir target/phase99-gap04-doc-wasm --serial
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
moon -C modules/mb-font check README.mbt.md --target wasm-gc --frozen --target-dir target/phase99-gap04-doc-wasm-gc --serial
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Assert-FoundationPolicy -PolicyPath ./policy/foundation.json
    </automated>
  </verify>
  <acceptance_criteria>
    - "The focused gate fails before the README correction and passes after it."
    - "README Data text agrees with the executable maxp-underclaim Data assertion."
    - "README Resource text is limited to retained FontLimits, max_work, and caller Budget exhaustion."
    - "All four literate checks plus focused and repository-wide policy gates pass."
  </acceptance_criteria>
  <done>The public five-way outline taxonomy is accurate and protected against recurrence per D-20 and D-23.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. This gap plan changes no parser or runtime behavior; it closes integrity and repudiation risks in the publication boundary.

## Trust Boundaries

| Boundary | Description |
|---|---|
| `policy/foundation.json` -> `modules/mb-font/moon.mod.json` | Approved publication metadata must reach the actual module manifest without drift. |
| executable outline errors -> public README | Hostile-font and resource failures must be described with the same categories callers observe. |
| focused selector -> phase closure | A narrow green gate must not omit metadata or documentation invariants needed for publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Verification |
|---|---|---|---|---|---|---|
| T-99-04-01 | Tampering | mb-font publication metadata | medium | mitigate | Compare policy and manifest descriptions case-sensitively inside the focused selector and retain the repository-wide comparison. | Temporary drift fixture, real focused gate, and real repository-wide gate. |
| T-99-04-02 | Repudiation | README maxp/resource taxonomy | medium | mitigate | Bind Data to maxp underclaims and Resource to retained FontLimits/max_work/Budget with focused bullet-level assertions. | Pre-fix red result, final direct taxonomy probe, and focused selector. |
| T-99-04-03 | Spoofing | Phase 99 closure evidence | medium | mitigate | Require both policy entry points and four executable README checks before re-verification. | Exact Task 2 command and fresh verifier handoff. |
| T-99-04-SC | Tampering | dependency supply chain | low | accept | No package installation, dependency, generated-interface, or runtime source change occurs. | `files_modified` is limited to manifest, policy script, and README. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Complete simple and bounded one-level composite Path2 extraction without partial failure | 99-01, 99-02, 99-03; publication closure in 99-04 | COVERED |
| REQ | FONT-03 | Checked, transactional valid simple/composite outline extraction | 99-01 through 99-03; metadata/docs consistency in 99-04 | COVERED |
| RESEARCH | — | maxp underclaims are Data before retained caller ceilings become Resource | 99-04 Task 2 | COVERED |
| RESEARCH | — | Exact independent policy and documentation gates close publication | 99-04 Tasks 1-2 | COVERED |
| CONTEXT | D-01-D-19 | Public query, geometry, limits, graph, arithmetic, and transactional behavior | Existing Plans 99-01 through 99-03; verification truths 1-12 remain green | COVERED |
| CONTEXT | D-20 | Stable InvalidInput/Data/Capability/Resource/State taxonomy | 99-04 Task 2 | COVERED |
| CONTEXT | D-21,D-22 | Generated semantic and hostile/runtime evidence | Existing Plans 99-01 through 99-03; no new runtime gap | COVERED |
| CONTEXT | D-23 | Interface, policy, docs, isolated package, and four-target gates | 99-04 Tasks 1-2 | COVERED |

CONTEXT deferred ideas and Phase 100 licensed-real-font/full-workflow qualification remain excluded. No deferred capability, implementation change, or new requirement is introduced.
</source_audit>

<verification>
1. Task 1 first proves the new focused description check red against the stale manifest, then green against the exact approved description and red again against a temporary drift fixture.
2. Task 2 first proves the focused taxonomy check red against the stale README, then green after the Data/Resource correction.
3. Run `Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json` and `Assert-FoundationPolicy -PolicyPath ./policy/foundation.json` on the final tree.
4. Run `moon -C modules/mb-font check README.mbt.md` independently on native, js, wasm, and wasm-gc with the exact unique target directories in Task 2.
5. After `99-04-SUMMARY.md` exists, hand Phase 99 back to the verifier. Re-run goal verification against the existing `99-VERIFICATION.md` gaps and require both failed truths to become verified; the executor does not edit the verification report.
</verification>

<success_criteria>
- The exact policy and manifest descriptions agree and both policy selectors pass.
- The focused selector independently rejects manifest-description drift.
- The README and focused selector agree that maxp underclaims are Data and only FontLimits/max_work/Budget exhaustion is Resource.
- Four-target literate checks remain green.
- Phase 99 re-verification can report 14/14 observable truths with no behavior-unverified item.
</success_criteria>

<output>
Create `.planning/phases/99-simple-and-composite-outlines/99-04-SUMMARY.md` when done, then hand off to Phase 99 goal verification to refresh `99-VERIFICATION.md` from fresh evidence.
</output>
