---
phase: 98-unicode-mapping-and-kerning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-font/font/cmap.mbt
  - modules/mb-font/font/tables.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/font_wbtest.mbt
autonomous: true
requirements:
  - FONT-02
must_haves:
  truths:
    - "One signed Unicode scalar maps through Font::glyph_for_scalar to one opaque, in-range GlyphId; BMP and supplementary scalars use this same singular method (D-01, D-02; assumption delta: no-change)."
    - "Negative values, surrogates, and values above U+10FFFF return InvalidInput/InvalidRange, while any other valid unmapped scalar returns glyph zero (D-01)."
    - "Font opening validates every supported cmap record, selects exactly one canonical mapping using the frozen format-12-before-format-4 rank, treats same-offset aliases as one mapping, rejects a duplicate same-priority record key, and never consults lower ranks after selection or a selected-table miss (D-04, D-05, D-06)."
    - "A font with no eligible Unicode record fails opening as a capability error, while a supplementary scalar over a selected format-4 table is a valid glyph-zero miss (D-07a)."
    - "Every mapping is structurally and cardinality-proven during atomic admission; queries are allocation-free binary searches protected by pre/post retained-source revision checks (D-03, D-07, D-12, D-14)."
  flagged_assumptions:
    - "FONT-02 empty/encoding probe: the public input is exactly one signed Int scalar, not nullable text; empty input, byte length, grapheme length, normalization, and string equality are outside this API."
    - "Supplementary-plane support changes only the accepted scalar range: it does not change the one-scalar-to-one-opaque-GlyphId identity model."
  prohibitions:
    - statement: "MUST NOT silently reinterpret malformed or unsupported cmap data as a successful glyph-zero result."
      status: resolved
      verification: test
  artifacts:
    - path: modules/mb-font/font/cmap.mbt
      provides: "Private canonical cmap rank, retained format-4/12 lookup facts, same-priority record-key uniqueness, and allocation-free lookup."
    - path: modules/mb-font/font/font.mbt
      provides: "Guarded Font::glyph_for_scalar public facade returning only opaque GlyphId."
    - path: modules/mb-font/font/tables.mbt
      provides: "Atomic selected-cmap integration in RequiredTableFacts with charged semantic validation."
    - path: modules/mb-font/font/font_test.mbt
      provides: "Public Unicode mapping, invalid-input, selection, miss, mutation, and atomic-opening evidence."
    - path: modules/mb-font/font/font_wbtest.mbt
      provides: "Private rank, offset, candidate-equivalence, and binary-search boundary evidence."
  key_links:
    - from: modules/mb-font/font/font.mbt
      to: modules/mb-font/font/cmap.mbt
      via: "Font::glyph_for_scalar guards revision, validates the signed scalar, performs one selected lookup, guards again, then constructs GlyphId."
    - from: modules/mb-font/font/tables.mbt
      to: modules/mb-font/font/cmap.mbt
      via: "font_admit_required_tables selects and validates compact cmap facts before its sole RequiredTableFacts publication."
---

<objective>
Deliver the production Unicode tracer and expand it into the complete deterministic format-12/format-4 mapping contract.

Purpose: Library authors need one portable scalar query whose mapping, miss, error, mutation, and resource behavior is fixed before kerning builds on admitted glyph identities.
Output: `cmap.mbt`, selected cmap facts in atomic admission, `Font::glyph_for_scalar`, and public/private generated evidence for FONT-02.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/98-unicode-mapping-and-kerning/98-CONTEXT.md
@.planning/phases/98-unicode-mapping-and-kerning/98-RESEARCH.md
@.planning/phases/98-unicode-mapping-and-kerning/98-PATTERNS.md
@.planning/phases/97-font-admission-and-metrics/97-02-SUMMARY.md
@.planning/phases/97-font-admission-and-metrics/97-03-SUMMARY.md

<interfaces>
Preserve the existing private seams `CmapEnvelope`, `CmapSubtableFacts`, `RequiredTableFacts`, `font_cmap_subtable_facts`, `font_validate_cmap_subtable`, `font_cmap_declared_work`, `font_preflight_admission_work`, and `font_admit_required_tables`. Extend or replace the envelope with selected lookup facts; do not create a parallel admission transaction.

Preserve the public/private query seam:
- `Font::require_revision(String) -> Result[Unit, @error.CoreError]`
- `Font::glyph_id(UInt64) -> Result[GlyphId, @error.CoreError]`
- `GlyphId::value() -> UInt64`
- new `Font::glyph_for_scalar(Int) -> Result[GlyphId, @error.CoreError]`

New private symbols owned here:
- `CmapLookupFacts`, `CmapFormat4Facts`, `CmapFormat12Facts`
- `font_cmap_rank`, `font_admit_cmap_lookup`, `font_lookup_cmap`
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one admitted format-12 map through the public scalar query</name>
  <reversibility rating="costly">The scalar method and opaque result become the public seam used by later font consumers, so renaming or reshaping it requires coordinated interface, policy, documentation, and call-site changes.</reversibility>
  <files>modules/mb-font/font/cmap.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    - modules/mb-font/font/tables.mbt:32
    - modules/mb-font/font/tables.mbt:558
    - modules/mb-font/font/tables.mbt:1157
    - modules/mb-font/font/tables.mbt:1763
    - modules/mb-font/font/font.mbt:39
    - modules/mb-font/font/font.mbt:67
    - modules/mb-font/font/font.mbt:234
    - modules/mb-font/font/font_test.mbt:252
    - modules/mb-font/font/font_test.mbt:2086
    - modules/mb-font/font/moon.pkg
  </read_first>
  <behavior>
    - "One generated eligible format-12 subtable maps BMP U+0041 and supplementary U+1F600 through the same Font::glyph_for_scalar method to two opaque in-range GlyphIds."
    - "A valid gap in the selected subtable returns GlyphId zero; a negative, surrogate, or 0x110000 input returns stable invalid-input facts."
    - "Mutation before or during lookup returns the established revision-drift error and publishes no GlyphId."
  </behavior>
  <action>
Write the failing public test first, using the existing in-language format-12 builder and one checksum-correct generated font containing a BMP group and a supplementary group. Exercise both values through the same new `Font::glyph_for_scalar(Int)` method, then cover one valid miss and the signed scalar boundaries required by D-01. This is the no-change assumption-delta invariant: supplementary-plane input widens the scalar domain but does not introduce strings, sequences, or plural glyph results.

Create `cmap.mbt` with private retained format-12 facts and allocation-free lower-bound lookup over admitted groups. Reuse Phase 97 checked table-local windows and structural/cardinality proof rather than reparsing root bytes. Extend `RequiredTableFacts` and the existing `font_admit_required_tables` coordinator so the eligible format-12 descriptor is retained before the sole Font publication (D-02, D-07, D-12). The public method must check `require_revision("font-query")`, validate the signed scalar before conversion, dispatch through exactly one admitted mapping, check revision again, and construct the existing opaque `GlyphId` only after the admitted lookup returns (D-01, D-02, D-03, D-14).

Keep the tracer production-quality: all attacker-derived offset/count arithmetic is checked, format-12 group search derives bounds from the admitted count, glyph-zero is a real miss, and no raw record/table/offset type becomes public. Do not add kerning, strings, normalization, shaping, or host-font behavior.
  </action>
  <verify>
    <automated>moon -C modules/mb-font test --target native --frozen</automated>
  </verify>
  <acceptance_criteria>
    - "The same public method maps one BMP and one supplementary scalar to opaque in-range GlyphIds, proving the singular identity invariant."
    - "Valid miss, invalid signed scalar, and retained-source mutation return the exact locked semantic classes without a partial result."
    - "The format-12 lookup reads only admitted table-local facts and native tests pass."
  </acceptance_criteria>
  <done>A library author can open the generated font and map BMP/supplementary scalars end-to-end through one guarded public method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete canonical cmap selection, format-4 lookup, and hostile boundaries</name>
  <files>modules/mb-font/font/cmap.mbt, modules/mb-font/font/tables.mbt, modules/mb-font/font/font_test.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    - modules/mb-font/font/cmap.mbt
    - modules/mb-font/font/tables.mbt:736
    - modules/mb-font/font/tables.mbt:988
    - modules/mb-font/font/tables.mbt:1138
    - modules/mb-font/font/font_test.mbt:252
    - modules/mb-font/font/font_test.mbt:1124
    - modules/mb-font/font/font_wbtest.mbt
    - modules/mb-core/checked/checked.mbt
  </read_first>
  <behavior>
    - "The exact canonical rank is platform 0/encoding 4/format 12, then 3/10/12, then 0/3/4, then 3/1/4; record order cannot change selection."
    - "Same-offset aliases are one admitted mapping; a second distinct record with the same winning-priority record key rejects under directory uniqueness before selection."
    - "Lower-ranked records are structurally validated but never semantically compared, consulted, or used as per-scalar fallback after the winning priority is selected."
    - "Format 4 maps direct-delta and glyph-array entries, preserves a raw array zero as glyph zero, and returns zero for supplementary input without fallback."
    - "Format 12 handles first/last group hits and gaps; format 4 handles first/middle/last segment hits and holes using count-derived binary-search bounds."
    - "No eligible Unicode record is capability failure; malformed selected mapping, glyph-range failure, or checked arithmetic failure is data/resource failure with no Font publication."
  </behavior>
  <action>
Add RED public and white-box matrices before expanding the implementation. Encode the D-05 rank literally in `font_cmap_rank`; every other platform/encoding/format tuple is noncanonical. Scan and structurally validate every supported format-4/12 record under the existing Phase 97 rules, then select format 12 before format 4 independent of record order (D-04, D-05, D-06, D-07). Treat records that alias the same checked subtable offset as one mapping. Enforce the existing directory record-key uniqueness rule so a second distinct record with the same winning `(platform, encoding)` priority rejects before selection. Do not perform cross-map semantic equality: lower-ranked records are never compared with the winner, consulted by queries, or used after a per-scalar miss. This is the exact D-06 consistency scope and keeps attacker-driven work bounded without an additional equality scan.

Implement format-4 lookup using the admitted segment count and checked array bases. Binary-search by `endCode`, validate containment against `startCode`, apply direct `idDelta` modulo 65536 when `idRangeOffset` is zero, and otherwise base the glyph-array read on the address of the current range-offset word. Return glyph zero immediately for a raw array zero and apply delta only to nonzero raw glyphs. Supplementary input over a selected format-4 descriptor returns glyph zero without consulting any other record (D-06, D-07a).

Extend public tests for all D-15 mapping cases: rank/order permutations, precedence, same-offset aliases, duplicate same-priority record-key rejection, lower-ranked disagreement ignored after winner selection, direct and array format-4 paths, raw-zero, valid holes/noncharacters, legacy-only/no-eligible capability failure, glyph-cardinality failures, exact work/budget fit, one-short atomic rejection, and mutation drift. Add D-16 white-box coverage for record-key uniqueness, rank rejection, checked format-4 bases, format-12/4 first-middle-last-miss search edges, and stable invalid-input/data/capability/state/resource taxonomy.
  </action>
  <verify>
    <automated>moon -C modules/mb-font test --target native --frozen &amp;&amp; moon -C modules/mb-font info --target native --frozen</automated>
  </verify>
  <acceptance_criteria>
    - "All four canonical ranks, same-offset aliasing, duplicate same-priority rejection, lower-rank non-comparison, format-12 precedence, and no-fallback semantics are executable and deterministic."
    - "Format-4 direct/array/raw-zero and format-12 boundary searches return admitted glyphs or glyph zero without allocation."
    - "No eligible mapping, malformed mapping, invalid scalar, mutation, and resource exhaustion remain distinguishable and atomic."
    - "Native tests pass and generated interface inspection exposes only the new Font method, not private cmap facts."
  </acceptance_criteria>
  <done>FONT-02 is complete on native with deterministic canonical selection and fully admitted format-4/12 lookup semantics.</done>
</task>

</tasks>

## Artifacts this phase produces

| New symbol/file owned by this plan | Contract |
|---|---|
| `modules/mb-font/font/cmap.mbt` | New private canonical selection and format-4/12 lookup implementation |
| `CmapLookupFacts`, `CmapFormat4Facts`, `CmapFormat12Facts` | Compact admitted table-local lookup state |
| `font_cmap_rank`, `font_admit_cmap_lookup`, `font_lookup_cmap` | Exact rank, atomic selection, allocation-free lookup |
| `Font::glyph_for_scalar(Int)` | One signed scalar to one opaque `GlyphId` result |
| `RequiredTableFacts.cmap` selected descriptor | Single admitted mapping carried into `Font` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller font bytes -> cmap admission | Attacker-controlled record counts, offsets, groups, segments, and glyph values drive validation and reads. |
| caller scalar -> public query | Signed untrusted input crosses into table-local lookup and opaque result construction. |
| retained ByteView -> published result | The caller can mutate admitted storage between validation and publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-98-01-01 | Tampering | malicious cmap bytes in `cmap.mbt` | high | mitigate | Use checked offsets/lengths, exact contained envelopes, sorted ranges, glyph-cardinality proof, and raw-zero format-4 semantics before publication. |
| T-98-01-02 | Denial of Service | record/group/segment/candidate loops | high | mitigate | Discover counts, intersect semantic ceilings/max_work/budget, and preflight each attacker-declared loop before iteration and one aggregate charge. |
| T-98-01-03 | Tampering | retained source mutation | high | mitigate | Run pre-read and post-read revision guards in `Font::glyph_for_scalar`; extend all-query mutation and mutate-back tests. |
| T-98-01-04 | Tampering | glyph range and format-4 address math | high | mitigate | Prove mapped glyphs below `numGlyphs`, derive range-offset addresses with checked arithmetic, and construct only opaque GlyphId. |
| T-98-01-05 | Spoofing | error-taxonomy confusion | medium | mitigate | Assert invalid scalar, malformed bytes, no eligible capability, mutation, and resource exhaustion as distinct structured CoreError facts; never map them to glyph zero. |
| T-98-01-06 | Tampering | partial Font publication | high | mitigate | Select/validate cmap inside the existing atomic admission transaction and preserve one final Font construction after all checks and final revision guard. |
| T-98-SC | Tampering | dependency/publication surface | low | accept | No package install occurs; frozen commands and the unchanged `mb-font -> mb-core` package boundary limit supply-chain change. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Resolve Unicode scalars deterministically over admitted fonts | Tasks 1-2 | COVERED |
| REQ | FONT-02 | Any valid scalar, format-12/4 selection, zero miss, structured invalid/malformed errors | Tasks 1-2 | COVERED |
| RESEARCH | — | Retained facts, exact rank, same-priority uniqueness, lower-rank non-comparison, checked lookup, preflight work | Tasks 1-2 | COVERED |
| CONTEXT | D-01,D-02,D-03 | Signed scalar, opaque in-range result, two revision guards | Task 1 | COVERED |
| CONTEXT | D-04,D-05,D-06 | Canonical precedence/rank, aliases/consistency, no fallback | Task 2 | COVERED |
| CONTEXT | D-07,D-07a | Preserve structural proof; capability/no-eligible and format-4 supplementary miss | Task 2 | COVERED |
| CONTEXT | D-12,D-14,D-15,D-16 | Atomic compact admission, allocation-free lookup, generated public/private evidence | Tasks 1-2 | COVERED |

Empty/null/string normalization questions are explicit non-input assumptions, not omitted behavior; outlines, shaping, GPOS, host discovery, and real-font qualification remain deferred by CONTEXT.
</source_audit>

<verification>
1. Run `moon -C modules/mb-font test --target native --frozen` after each RED/GREEN cycle.
2. Run `moon -C modules/mb-font info --target native --frozen` and confirm only `Font::glyph_for_scalar` is newly public.
3. Inspect the public matrix for BMP/supplementary same-method identity, exact ranks, format-12 precedence, valid misses, invalid scalars, conflicts, mutation, and atomic resource failures.
</verification>

<success_criteria>
- FONT-02 passes native public and white-box tests through one signed-scalar method returning opaque GlyphId.
- Canonical selection is independent of record order and selected-table misses never fall through.
- Malformed bytes, unsupported/no-eligible capability, invalid scalar, mutation, and resource exhaustion remain distinct and publish no partial value.
- No dependency, FFI, UI, schema, string normalization, shaping, outline, or real-font scope is added.
</success_criteria>

<output>
Create `.planning/phases/98-unicode-mapping-and-kerning/98-01-SUMMARY.md` when done.
</output>
