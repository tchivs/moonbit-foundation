---
phase: 100-portable-font-qualification
plan: 04
type: execute
wave: 4
depends_on:
  - 100-03
files_modified:
  - scripts/quality/Invoke-FontQualification.ps1
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Test-FixturePolicy.ps1
  - scripts/quality.ps1
  - policy/foundation.json
autonomous: true
requirements:
  - FONT-05
must_haves:
  truths:
    - "One dedicated quality selector proves fixture identity/generation, exact interface, four-target semantics, and the sole mb-font -> mb-core dependency edge."
    - "Policy rejects FFI/native stubs, runtime filesystem or host-font discovery, GUI/canvas/image/color dependencies, shaping, hinting, CFF/CFF2, and rasterization."
    - "Focused qualification requires all four targets, the complete hostile outcome set, and byte-identical normalized semantic evidence."
  artifacts:
    - "scripts/quality/Invoke-FontQualification.ps1"
    - "scripts/quality/Assert-Policy.ps1"
    - "scripts/quality/Test-FixturePolicy.ps1"
    - "scripts/quality.ps1"
    - "policy/foundation.json"
    - "artifacts/release-qualification/font/{js,wasm,wasm-gc,native,comparison}.json"
  key_links:
    - "quality.ps1 FontQualification dispatches Invoke-FontQualification.ps1 without invoking or replacing Required."
    - "Assert-FontQualificationArtifacts freezes fixtures, generated/test inventories, public interface, dependency edges, and forbidden capabilities."
    - "Invoke-FontQualification.ps1 writes passing target records only after isolated tests and refuses missing targets or semantic divergence."
---

<objective>
Freeze the focused Phase 100 selector, policy inventories, negative capability boundary, and four-target evidence comparison.

Purpose: FONT-05 needs one fail-closed focused command before documentation and CI can present it as the canonical qualification boundary.
Output: Focused selector integration, exact fixture/test/interface/dependency policy, negative tests, and validated target evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-portable-font-qualification/100-CONTEXT.md
@.planning/phases/100-portable-font-qualification/100-RESEARCH.md
@.planning/phases/100-portable-font-qualification/100-03-SUMMARY.md
@docs/rfcs/0004-mb-font.md
@docs/policies/licensing-and-fixtures.md
@fixtures/manifest.json
@policy/foundation.json
@scripts/quality/Invoke-FontQualification.ps1
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Test-FixturePolicy.ps1
@scripts/quality.ps1
@modules/mb-font/font/moon.pkg
@modules/mb-font/moon.mod.json
</context>

## Artifacts this phase produces

| Kind | Exact artifact | Contract |
|---|---|---|
| New selector value | `FontQualification` in `scripts/quality.ps1` | Dispatches the focused runner and remains separate from Required. |
| New PowerShell symbol | `Assert-FontQualificationArtifacts` in `Assert-Policy.ps1` | Freezes exact fixtures, oracle schema, generated/test inventory, dependencies, interface, and forbidden capabilities. |
| New policy records | Three Phase 100 test-source entries in `policy/foundation.json` | Exact ordered inventory for generated, workflow, and hostile tests. |
| New evidence set | `artifacts/release-qualification/font/{js,wasm,wasm-gc,native,comparison}.json` | Four passing closed-schema records plus strict semantic comparison. |

No documentation, CI workflow, Required wrapper, runtime module, public API, direct dependency, format support, or stability promotion is owned by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Freeze the focused selector, exact inventories, and negative capability boundary</name>
  <files>scripts/quality/Invoke-FontQualification.ps1, scripts/quality/Assert-Policy.ps1, scripts/quality/Test-FixturePolicy.ps1, scripts/quality.ps1, policy/foundation.json</files>
  <read_first>
    - scripts/quality/Invoke-FontQualification.ps1
    - scripts/quality/Assert-Policy.ps1:1061
    - scripts/quality/Test-FixturePolicy.ps1
    - scripts/quality.ps1
    - policy/foundation.json:2181
    - modules/mb-font/font/moon.pkg
    - modules/mb-font/moon.mod.json
    - fixtures/manifest.json
  </read_first>
  <action>
Extend `Assert-FontFoundationPolicy` through a focused `Assert-FontQualificationArtifacts` helper. Update the exact publication/package test-source inventories for `generated_font_qualification_test.mbt`, `font_qualification_test.mbt`, and `font_qualification_hostile_test.mbt`; keep production sources, supported targets, direct dependency set, allowed edge, candidate stability, and the current 56-line semantic interface unchanged (D-14/D-17). Require the exact DejaVu TTF/notice external manifest records, hashes, license expression, confirmed redistribution, closed oracle/case schemas, generator `-Check`, generated do-not-edit/provenance header, and case-ID sequence. Add negative fixture-policy tests for font/notice digest, license, redistribution, path containment, and manifest-order drift (D-02/D-06/D-07).

Enforce D-16 with source-aware checks: production and portable test code may not import/call FFI/native stubs, filesystem/path/file-open or host-font discovery, GUI/canvas/image/color, shaping, hinting execution, CFF/CFF2, or rasterization. Allow the generated provenance comment to name its canonical repository fixture path, but do not exempt executable code or search the whole file with self-invalidating prose gates. Keep exact module/package import checks proving only mb-core budget/bytes/checked/error/math and the sole module edge `tchivs/mb-font -> tchivs/mb-core`.

Finish `Invoke-FontQualification.ps1` ordering as: generator `-Check`; manifest/license/hash/oracle/policy/interface/dependency/source-boundary checks; isolated `moon check` and complete `moon test font` on js/wasm/wasm-gc/native with unique target directories; four evidence writes; normalized comparison; four literate README checks. No target skip, native allowance, component identities in public facts, semantic normalization expansion, or Required-lane invocation belongs inside the focused selector (D-10/D-12/D-14).

Add `FontQualification` to `scripts/quality.ps1` as a dedicated lane that forwards `-EvidenceDirectory` to the focused script. Retain `Required` as the existing repository-wide dispatch. Add temporary negative probes for an extra public interface line, an extra dependency, a forbidden executable source call, a missing target evidence record, and semantic evidence divergence.
  </action>
  <verify>
    <automated>
$ErrorActionPreference = 'Stop'
./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory artifacts/release-qualification/font-plan04
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
. ./scripts/quality/Assert-Policy.ps1
Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json
./scripts/quality/Test-FixturePolicy.ps1
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$manifest = Get-Content -Raw ./modules/mb-font/moon.mod.json | ConvertFrom-Json
$deps = @($manifest.deps.PSObject.Properties.Name)
if ($deps.Count -ne 1 -or $deps[0] -cne 'tchivs/mb-core') { throw 'mb-font dependency drift' }
$comparison = Get-Content -Raw ./artifacts/release-qualification/font-plan04/comparison.json | ConvertFrom-Json
if ($comparison.equal -ne $true -or @($comparison.targets).Count -ne 4) { throw 'Focused four-target semantic evidence is incomplete or divergent.' }
    </automated>
  </verify>
  <acceptance_criteria>
    - "The dedicated selector runs generation, provenance, policy, interface, dependency, four-target test, evidence-comparison, and README gates in a fixed order."
    - "Exact policy inventories include all three new test files while production sources and the 56-line semantic interface remain unchanged."
    - "Negative probes reject fixture/license drift, forbidden capabilities, extra public API/dependencies, missing targets, and semantic divergence."
    - "The only public dependency edge remains mb-font to mb-core and all four targets remain mandatory."
    - "The selector is offline after fixture intake and neither invokes nor weakens the repository Required lane."
  </acceptance_criteria>
  <done>One fail-closed focused command enforces D-02/D-06/D-07/D-10/D-12/D-14/D-16/D-17 and the exact FONT-05 dependency boundary.</done>
</task>

</tasks>

<threat_model>
## Security Profile

OWASP ASVS level 1 applies. High-severity scope, dependency, fixture, and evidence bypasses are blocking.

## Trust Boundaries

| Boundary | Description |
|---|---|
| package files -> publication policy | New tests must be inventoried without changing production/public surfaces. |
| source code -> capability boundary | Portable behavior must not acquire ambient host or deferred font capabilities. |
| focused selector -> evidence JSON | A green record must correspond to a complete real target run. |
| four records -> comparison | Normalization must not hide semantic divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-04-01 | Elevation of privilege | font capability boundary | high | mitigate | Source-aware negative checks reject host/FFI/GUI/shaping/hinting/CFF/rasterization execution and extra dependencies. |
| T-100-04-02 | Tampering | public interface and inventory | high | mitigate | Freeze exact semantic interface, production/test files, imports, targets, and dependency edges with negative probes. |
| T-100-04-03 | Spoofing | focused target evidence | high | mitigate | Require four target records, complete hostile IDs, successful semantic comparison, and pinned toolchain identity. |
| T-100-04-04 | Tampering | fixture provenance | high | mitigate | Verify exact hashes/license/redistribution, generator drift, manifest order, and independent oracle schema. |
| T-100-04-SC | Tampering | dependency supply chain | low | accept | No package-manager installation or new runtime dependency occurs. |
</threat_model>

<source_audit>

| Source | ID | Feature / constraint | Plan owner | Status |
|---|---|---|---|---|
| GOAL | — | Focused reproducible four-target qualification | 100-04 Task 1 | COVERED |
| REQ | FONT-05 | Host-independent fixture/public/hostile qualification | 100-04 Task 1 | COVERED |
| RESEARCH | — | Focused selector, exact inventory, evidence comparison, dependency/source gates | 100-04 Task 1 | COVERED |
| CONTEXT | D-02,D-06,D-07 | Fixture/license/generation enforcement | 100-04 Task 1 | COVERED |
| CONTEXT | D-10,D-12 | Complete equal four-target evidence with no skips | 100-04 Task 1 | COVERED |
| CONTEXT | D-14,D-16,D-17 | Focused selector, negative boundary, frozen public scope | 100-04 Task 1 | COVERED |

Documentation, CI upload semantics, bounded Required execution, and final closure are owned by 100-05. Deferred font and text capabilities remain excluded.
</source_audit>

<verification>
1. Run the focused selector and require all four target records plus equal comparison.
2. Run focused policy, fixture negatives, interface, dependency, and source-boundary probes.
3. Confirm the selector does not invoke Required or emit target-specific expectations.
</verification>

<success_criteria>
- One focused command reproduces exact licensed public and hostile font qualification.
- Policy proves no API, dependency, target, production inventory, or forbidden capability drift.
- Four target records contain the complete public/hostile semantic payload and compare equal.
- Plan 100-05 can consume a validated focused evidence directory without modifying it.
</success_criteria>

<output>
Create `.planning/phases/100-portable-font-qualification/100-04-SUMMARY.md` when done.
</output>
