---
milestone: v0.4
audited: 2026-07-20
status: tech_debt
scores:
  requirements: 6/6
  phases: 8/8
  integration: 6/6
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: inherited
    items:
      - "The broad foundation-policy check retains an unrelated mb-image/ops import-policy mismatch (expected 10, actual 12)."
      - "Historical registry/release artifacts remain outside the v0.4 QOI scope."
---

# v0.4 Portable Image Interchange — Milestone Audit

## Verdict

**TECH DEBT — code milestone complete.** All six v0.4 requirements, all eight
milestone phases, and the public QOI end-to-end flow are verified. The remaining
items are inherited release/governance drift outside the QOI implementation and
are not on the required code path.

## Requirement coverage

| Requirement | Integrated path | Status | Evidence |
| --- | --- | --- | --- |
| QOI-01 | `QoiDecoder` → public `ImageDecoder::probe` → prefix outcome | SATISFIED | Phase 13 verification passed. |
| QOI-02 | decode → bounded preflight → `OwnedImage` → pixels/metadata | SATISFIED | Phase 13 verification passed. |
| QOI-03 | `QoiEncoder` → public `ImageEncoder::encode` → decode round trip | SATISFIED | Phase 14 verification passed. |
| QOI-04 | malformed/truncated/limit/I/O paths → deterministic errors | SATISFIED | Phase 13 verification passed. |
| QOI-05 | QOI fixtures → generated vectors → four-target codec tests | SATISFIED | Phase 14 and Phase 16 verification passed. |
| QOI-06 | public decode → `flip_horizontal` → encode example | SATISFIED | Phase 15 and Phase 16 verification passed. |

All Phase 09–16 `VERIFICATION.md` reports are passed. The requirements
traceability table marks QOI-01 through QOI-06 complete, and the associated
Phase 13–15 summaries/verifications supply matching implementation evidence.

## Cross-phase and end-to-end evidence

`examples/qoi-portable/main/main.mbt` uses the public contracts in the intended
order:

`QoiDecoder::new` → `ImageDecoder::decode` → `ops.flip_horizontal` →
`QoiEncoder::new` → `ImageEncoder::encode`.

It checks transformed pixels, 27 bytes read, 24 bytes written, diagnostics,
canonical bytes, digest, and SHA-256 before printing its one status line.

Fresh focused audit evidence:

| Check | Result |
| --- | --- |
| `scripts/quality/Invoke-MoonQuality.ps1 -Lane Qoi` | Passed. |
| `Assert-QoiLaneIsolation` | Passed; qualification, registry, report, release, publication, and credential routes were trap-tested and not reached. |
| `moon -C modules/mb-image test --target all --frozen` | 235/235 passed on wasm, wasm-gc, js, and native. |
| `qoi-portable` public example | Exact evidence passed on js, wasm, wasm-gc, and native. |

The scoped QOI policy helper and nine artificial drift cases fail closed for
package, imports, targets, interface, source ordering, and QOI production
inventory. The QOI quality lane is deliberately bounded to those checks and
the public example; it does not invoke qualification, source isolation,
registry resolution, reporting, release, publication, or credential functions.

## Deferred, non-blocking debt

| Item | Disposition |
| --- | --- |
| Broad `Assert-FoundationPolicy` rejects `mb-image/ops` imports (policy 10 vs actual 12). | Inherited non-QOI drift. The independent QOI assertion passes; do not change this within the QOI code milestone. |
| Historical `release/registry` artifacts and prior release-governance removals. | Outside the v0.4 QOI runtime and quality lane; retain for a separately scoped governance review. |
| Phase 16 ROADMAP prose says `Goal: [To be planned]` / `TBD`. | Administrative documentation drift only; Phase 16 plan, summary, verification, and completion state are authoritative. |

## Conclusion

v0.4 meets its code and test definition of done. Archive it with the above
governance items explicitly deferred; do not add release automation or registry
work to this milestone.

_Audited: 2026-07-20_
