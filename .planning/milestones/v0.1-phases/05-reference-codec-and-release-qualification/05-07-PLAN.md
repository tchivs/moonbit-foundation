---
phase: 05-reference-codec-and-release-qualification
plan: 07
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - policy/release-qualification.json
  - qualification/consumers/mb-core/moon.mod.json
  - qualification/consumers/mb-core/main/moon.pkg
  - qualification/consumers/mb-core/main/main.mbt
  - qualification/consumers/downstream-public/main.mbt
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - release/qualification/package-schema.json
autonomous: true
requirements: [WORK-06, QUAL-06]
must_haves:
  truths:
    - "Each module produces the same ordered package list, ZIP bytes, and SHA-256 in two clean full-repository copies that may use the checked moon.work only for building artifacts."
    - "An external mb-core consumer compiles from the exact extracted package artifact with moon.work disabled."
    - "mb-color and mb-image honestly report registry_resolution=blocked_unpublished_namespace while clean copied-source workspace consumers report source_isolation=pass and exact manifests still pass."
    - "The report states the post-publication order contract mb-core then mb-color then mb-image."
  artifacts:
    - path: policy/release-qualification.json
      provides: Closed module order, package allowlists, metadata, dependency, and blocked-state policy
    - path: scripts/quality/Invoke-ReleaseQualification.ps1
      provides: Clean temporary packaging and external-consumer qualification
    - path: qualification/consumers/mb-core/main/main.mbt
      provides: Exact core artifact public API consumer
  key_links:
    - from: scripts/quality/Invoke-ReleaseQualification.ps1
      to: policy/release-qualification.json
      via: fail-closed ordered policy comparison
    - from: qualification/consumers/mb-core/main/main.mbt
      to: _build/publish/mb-core-0.1.0.zip
      via: generated isolated root populated only from exact extracted artifact bytes
---

<objective>
Prove deterministic packages and the strongest honest clean-consumer result supported by the pinned toolchain.

Purpose: Implement D-19 through D-23 and WORK-06/QUAL-06 without workspace substitution, path-manifest rewrites, credentials, or publication.
Output: Closed release policy, deterministic ZIP/list/hash qualification, exact core artifact consumer, downstream source-isolation proof, and explicit blocked registry resolution.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@policy/foundation.json
@modules/mb-core/moon.mod.json
@modules/mb-color/moon.mod.json
@modules/mb-image/moon.mod.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock exact release and post-publication policies</name>
  <files>policy/release-qualification.json, release/qualification/package-schema.json, scripts/quality/ReleaseQualification.Common.ps1</files>
  <behavior>
    - Policy orders modules exactly mb-core, mb-color, mb-image and closes package contents, versions, candidate status, targets, licenses, repository metadata, fixture provenance, and named dependency constraints.
    - Path/local substitutions, unexpected archive entries, missing metadata, or dependency on a higher layer fail closed.
    - mb-core expects pass; mb-color and mb-image expect blocked_unpublished_namespace until their dependencies are published.
  </behavior>
  <action>Define one canonical policy/schema per D-19, D-20, and D-22. Include the explicit post-publish contract: publish and independently resolve mb-core, then mb-color against published core, then mb-image against both published dependencies; the blocked state may change to pass only in that order with exact 0.1.0 constraints. Common helpers must canonicalize paths/JSON, SHA-256, ZIP entries, and tracked-diff snapshots without executing data-derived commands.</action>
  <verify><automated>pwsh -NoProfile -Command "$p=Get-Content policy/release-qualification.json -Raw | ConvertFrom-Json; if (($p.module_order -join ',') -ne 'mb-core,mb-color,mb-image') { exit 1 }; if ($p.modules.'mb-color'.registry_resolution -ne 'blocked_unpublished_namespace' -or $p.modules.'mb-image'.registry_resolution -ne 'blocked_unpublished_namespace') { exit 1 }"</automated></verify>
  <done>The closed policy names every exact positive/blocked outcome and cannot imply successful downstream registry resolution before publication.</done>
</task>

<task type="auto">
  <name>Task 2: Package twice and run exact isolated consumers</name>
  <files>qualification/consumers/mb-core/moon.mod.json, qualification/consumers/mb-core/main/moon.pkg, qualification/consumers/mb-core/main/main.mbt, qualification/consumers/downstream-public/main.mbt, scripts/quality/Invoke-ReleaseQualification.ps1</files>
  <action>Create two clean full-repository copies at the same committed HEAD; permit their unchanged checked `moon.work` only while running `moon package --frozen --list` for deterministic module ZIP production, then compare ordered lists, ZIP bytes, and SHA-256 and inspect exact manifest/archive contents. For the only exact no-workspace artifact-consumer pass, extract the exact mb-core ZIP into an isolated root with no `moon.work`, copy only the checked mb-core consumer, and run its public checks against those artifact bytes. For mb-color and mb-image, copy unchanged sources into a clean workspace and require `source_isolation: pass`; separately remove workspace resolution and probe unchanged named dependencies, requiring `registry_resolution: blocked_unpublished_namespace`. Never claim a downstream artifact-consumer pass, rewrite manifests, invoke publish, read credentials, or hide the blocker per D-21 through D-23. Snapshot tracked diff before/after and preserve the post-publish core then color then image order contract.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Invoke-ReleaseQualification.ps1 -Check -OutputDirectory artifacts/release-qualification/plan-07</automated></verify>
  <done>Two clean-copy workspace builds produce deterministic list/ZIP/hash evidence; only the exact core artifact consumer passes outside workspace resolution; downstream copied-source workspaces pass and both named registry probes remain explicit expected blocked results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workspace -> package artifact -> external root | Workspace substitution and untracked files could falsify consumer evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-19 | Tampering | package ZIP | high | mitigate | Two fresh builds compare closed entry lists, bytes, and SHA-256. |
| T-05-20 | Spoofing | external consumer | high | mitigate | Packaging and consumer modes are separated: clean-copy workspace builds artifacts, while only the core artifact consumer runs with no moon.work. |
| T-05-21 | Repudiation | downstream resolution | medium | mitigate | Machine-readable blocked_unpublished_namespace plus post-publish order records the limitation. |
</threat_model>

<verification>
Focused `-Check` completes with no tracked diff and reports pass for exact core artifact consumption and expected blocked results for both downstream registry resolutions.
</verification>

<success_criteria>
- Packaging is byte/list/hash deterministic from clean temporary roots.
- No path substitution, ambient workspace, credentials, or real publication participates.
- WORK-06 independence and QUAL-06 packaging evidence distinguish proven facts from the unpublished-namespace blocker.
</success_criteria>

<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-07-SUMMARY.md` when done.</output>
