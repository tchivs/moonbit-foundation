---
phase: 05-reference-codec-and-release-qualification
plan: 08
type: execute
wave: 8
depends_on: [05-07]
files_modified:
  - qualification/negative/path-dependency/mb-color.moon.mod.json
  - qualification/negative/higher-layer-dependency/mb-core.moon.pkg
  - qualification/negative/unexpected-package/package-list.txt
  - qualification/negative/false-registry-pass/report.json
  - scripts/quality/Test-ReleaseQualification.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
  - scripts/quality.ps1
  - .github/workflows/quality.yml
  - release/qualification/v0.1-requirements.json
autonomous: true
requirements: [WORK-06, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
must_haves:
  truths:
    - "A static committed ledger maps every Phase 5 requirement to executable focused and Required selectors before final qualification begins."
    - "Release policy fails closed for path substitution, higher-layer dependency, extra package content, and fabricated downstream registry success."
    - "Two complete Required runs from the same clean committed baseline pass and have identical deterministic evidence digests."
    - "Each module qualifies independently; downstream registry resolution remains blocked without failing the honest candidate qualification contract."
  artifacts:
    - path: scripts/quality/Test-ReleaseQualificationNegative.ps1
      provides: Exact fail-closed release-policy mutation suite
    - path: release/qualification/v0.1-requirements.json
      provides: Static WORK-06 and QUAL-01..06 selector/artifact contract with no run IDs, timestamps, results, or digests
    - path: scripts/quality.ps1
      provides: Required-lane integration of examples, docs, benchmarks, packaging, consumers, and read-only checks
  key_links:
    - from: scripts/quality.ps1
      to: scripts/quality/Invoke-ReleaseQualification.ps1
      via: Required lane after generation/check tests
    - from: release/qualification/v0.1-requirements.json
      to: artifacts/release-qualification/required-run-1
      via: Test-ReleaseQualification consumes the static ledger plus both untracked run reports
---

<objective>
Close Phase 5 with fail-closed policy evidence, independent module qualification, and two clean deterministic Required runs.

Purpose: Fulfill D-23 and D-24 while proving WORK-06 and QUAL-01 through QUAL-06 without overstating registry publication or codec scope.
Output: Exact negative fixtures, Required-lane wiring, independent requirement map, and two-run clean-baseline evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reference-codec-and-release-qualification/05-CONTEXT.md
@.planning/phases/05-reference-codec-and-release-qualification/05-RESEARCH.md
@policy/foundation.json
@policy/release-qualification.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove exact release-policy negatives fail closed</name>
  <files>qualification/negative/path-dependency/mb-color.moon.mod.json, qualification/negative/higher-layer-dependency/mb-core.moon.pkg, qualification/negative/unexpected-package/package-list.txt, qualification/negative/false-registry-pass/report.json, scripts/quality/Test-ReleaseQualificationNegative.ps1</files>
  <behavior>
    - A local/path dependency in mb-color is rejected as workspace substitution.
    - An mb-core import of mb-color or mb-image is rejected as reverse dependency.
    - One unexpected package/archive entry is rejected against the closed ordered allowlist.
    - A downstream report that claims registry pass while namespace/dependencies are unpublished is rejected.
    - PPM policy mutations for missing/extra import, wrong target, missing/extra interface, wrong publication order, and unregistered content are rejected.
  </behavior>
  <action>Create the four named immutable negative fixtures and a table-driven test that copies each to a temporary root, requires the exact expected rule ID, and fails if a mutation passes or fails for another reason. Keep production manifests untouched. Also invoke the Plan 03 PPM policy classifiers against temporary missing/extra import, wrong target, missing/extra semantic interface, wrong publication position, and unregistered content mutations. Test missing version/status/license/provenance/checksum, reordered publication, changed artifact after hashing, and tracked-source mutation through temporary derived cases per D-19 through D-23.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-ReleaseQualificationNegative.ps1</automated></verify>
  <done>Every exact policy boundary has a positive owner and a negative fixture proving fail-closed classification.</done>
</task>

<task type="auto">
  <name>Task 2: Wire independent module and requirement qualification into Required</name>
  <files>scripts/quality/Test-ReleaseQualification.ps1, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1, scripts/quality.ps1, .github/workflows/quality.yml, release/qualification/v0.1-requirements.json</files>
  <action>Add ordered independent selectors for mb-core, mb-color, and mb-image: format, check/test on +js+wasm+wasm-gc+native, runnable docs/examples, exact policy semantic interfaces/imports/publication contents and order, fixture generation/provenance, benchmark correctness/baseline check, separated consumer outcomes, and tracked read-only assertion. Write `v0.1-requirements.json` as a static closed mapping of WORK-06 and QUAL-01..06 to selector IDs, commands, expected artifact schemas, and policy rule IDs only; it must contain no run ID, timestamp, commit, result, environment, or digest. This task's commit freezes the static ledger and all Required wiring before Task 3. Required accepts only the policy-declared `blocked_unpublished_namespace` for downstream no-workspace named resolution, requires `source_isolation: pass` for copied-source workspaces, and reserves no-workspace artifact-consumer pass for mb-core. CI runs the same Required entrypoint with read-only permissions and no credentials/network publication per D-23/D-24.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-ReleaseQualification.ps1 -Focused</automated></verify>
  <done>Focused qualification proves all selectors and policies, and the committed static ledger is complete, schema-valid, and free of dynamic run evidence before final runs.</done>
</task>

<task type="auto">
  <name>Task 3: Produce two clean-baseline Required run proofs</name>
  <files>artifacts/release-qualification/required-run-1/report.json, artifacts/release-qualification/required-run-2/report.json</files>
  <action>After Tasks 1 and 2 and their static ledger are committed, require no tracked or staged diff and capture the final HEAD. Run Required into the first untracked evidence directory, assert the same HEAD and no tracked/staged diff, then run Required into the second untracked directory and assert the same conditions again. Dynamic run IDs, timestamps, environment, results, raw evidence, and digests live only beneath those untracked run directories. Do not edit `v0.1-requirements.json` or any tracked file after the final HEAD is captured. The independent verifier must consume the already committed static ledger plus both reports, require every selector/artifact contract, require both reports name the unchanged final HEAD, and compare canonical deterministic evidence digests while excluding only schema-declared run-local fields. This is the only phase-close route per D-24.</action>
  <verify><automated>pwsh -NoProfile -Command "$head=git rev-parse HEAD; if (git status --porcelain --untracked-files=no) { exit 1 }; & ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/required-run-1; if ($LASTEXITCODE -or (git rev-parse HEAD) -ne $head -or (git status --porcelain --untracked-files=no)) { exit 1 }; & ./scripts/quality.ps1 -Lane Required -EvidenceDirectory artifacts/release-qualification/required-run-2; if ($LASTEXITCODE -or (git rev-parse HEAD) -ne $head -or (git status --porcelain --untracked-files=no)) { exit 1 }; & ./scripts/quality/Test-ReleaseQualification.ps1 -StaticLedger release/qualification/v0.1-requirements.json -VerifyTwoRuns artifacts/release-qualification/required-run-1/report.json,artifacts/release-qualification/required-run-2/report.json"</automated></verify>
  <done>Two full Required reports pass at one unchanged final HEAD, no tracked file changes after that HEAD, and the verifier proves both reports against the precommitted static ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Required runner -> phase-close ledger | Generated evidence can authorize release-candidate completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-22 | Elevation of Privilege | blocked-state classifier | high | mitigate | Only exact policy-declared downstream namespace misses map to blocked; mutation tests reject fabricated pass. |
| T-05-23 | Tampering | Required evidence | high | mitigate | Clean HEAD checks, two runs, canonical digest comparison, and read-only tracked-source assertion. |
| T-05-24 | Repudiation | requirement ledger and reports | medium | mitigate | Static selectors are committed first; commit/toolchain/run identities remain in the two untracked signed-by-digest reports consumed by the verifier. |
</threat_model>

<verification>
Focused negative and qualification suites pass, then two Required runs pass from the same clean commit with identical deterministic digests and no tracked changes.
</verification>

<success_criteria>
- Exact positive policies and negative fixtures cover module order, dependencies, contents, metadata, provenance, hashes, and blocked resolution.
- WORK-06 and QUAL-01..06 each have executable independent evidence.
- A static ledger is committed before the unchanged final HEAD; two untracked Required reports, not a tracked post-run edit, close the phase.
</success_criteria>

<output>Create `.planning/phases/05-reference-codec-and-release-qualification/05-08-SUMMARY.md` when done.</output>
