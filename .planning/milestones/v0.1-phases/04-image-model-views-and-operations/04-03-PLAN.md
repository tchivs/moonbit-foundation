---
phase: 04-image-model-views-and-operations
plan: "03"
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - modules/mb-core/bytes/owned_bytes.mbt
  - modules/mb-core/bytes/bytes_test.mbt
  - modules/mb-core/bytes/bytes_wbtest.mbt
  - modules/mb-image/storage/moon.pkg
  - modules/mb-image/storage/owned_image.mbt
  - modules/mb-image/storage/views.mbt
  - modules/mb-image/storage/storage_test.mbt
  - modules/mb-image/storage/storage_wbtest.mbt
  - policy/foundation.json
autonomous: true
requirements: [IMAG-02, IMAG-03, IMAG-04]
must_haves:
  truths:
    - "Owned images retain validated descriptor, metadata, and independently owned bytes with explicit caller budgets."
    - "Immutable views retain backing; representable crops are zero-copy; the canonical empty immutable crop touches no backing."
    - "Mutable image access is callback-scoped, stale after exit, rejects empty/overlapping views, and permits only proved-disjoint splits."
  artifacts:
    - path: modules/mb-image/storage/owned_image.mbt
      provides: "Budgeted owned-image construction and retained state"
    - path: modules/mb-image/storage/views.mbt
      provides: "Immutable and callback-scoped mutable image views/crops"
  key_links:
    - from: modules/mb-image/storage/owned_image.mbt
      to: moonbit-foundation/mb-core/bytes
      via: "one authoritative OwnedBytes allocation"
    - from: modules/mb-image/storage/views.mbt
      to: moonbit-foundation/mb-core/bytes
      via: "retained ByteView and one enclosing MutByteLease"
---

<objective>
Attach validated descriptors and metadata to safe owned storage and lifetime-checked views.

Purpose: Deliver D-04 and D-06 through D-12 without raw mutable backing, budget double charge, or unproved aliasing.
Output: A tested `mb-image/storage` package implementing IMAG-03 and IMAG-04.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-image-model-views-and-operations/04-RESEARCH.md
@.planning/phases/04-image-model-views-and-operations/04-01-SUMMARY.md
@.planning/phases/04-image-model-views-and-operations/04-02-SUMMARY.md
@modules/mb-core/bytes/pkg.generated.mbti
@modules/mb-core/budget/pkg.generated.mbti
@.planning/phases/02-bounded-core-primitives/02-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add atomic storage-plus-operation allocation</name>
  <files>modules/mb-core/bytes/owned_bytes.mbt, modules/mb-core/bytes/bytes_test.mbt, modules/mb-core/bytes/bytes_wbtest.mbt, policy/foundation.json</files>
  <behavior>
    - New constructor orders checked narrowing, allocator approval, one combined hierarchical charge for storage plus caller pixels/work, then allocation.
    - Arithmetic, allocator, and budget failures leave every counter unchanged; success consumes every dimension exactly once.
    - Existing constructors delegate with no extra charge and never double-charge.
  </behavior>
  <action>Add `OwnedBytes::new_with_allocator_and_charge(length, budget, allocator, width, height, pixels, work)` with explicit UInt64 scalars, never a caller-supplied `ResourceCharge`. Inside mb-core/bytes construct exactly one existing public `ResourceCharge::new(bytes=length, allocations=1, allocation_size=length, width, height, pixels, work)`, then execute checked narrowing → allocator approval → one `Budget::charge` → allocation. Add exact source/tests/interface/policy evidence for each scalar. Charge precedes allocation and any loop; add no inspection of caller charges, refund, double charge, or new errors.</action>
  <verify><automated>moon -C modules/mb-core test bytes --target all --frozen</automated></verify>
  <done>The combined transaction commits once or leaves all counters unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic owned-image construction and retained immutable views</name>
  <files>modules/mb-image/storage/moon.pkg, modules/mb-image/storage/owned_image.mbt, modules/mb-image/storage/views.mbt, modules/mb-image/storage/storage_test.mbt</files>
  <behavior>
    - Positive validated descriptors allocate exactly their required storage once and retain descriptor/metadata; invalid descriptors, unsupported layout requests, and injected allocator rejection do not double-charge or partially consume budgets.
    - `view` retains backing; nonempty half-open packed padded-row crops preserve parent stride and checked origin without copying; unrepresentable planar crops return `CapabilityUnavailable` with a bounded stable token.
    - A zero-area immutable crop returns one canonical non-owning empty view, performs no byte access, and remains inspectable; owned images and mutable crops reject zero dimensions.
  </behavior>
  <action>Implement `OwnedImage`, `ImageView`, and canonical empty-view semantics per D-04, D-06, D-08, D-09, D-11, and D-12. Validate pure and unsupported conditions first, then route every allocation through the new combined-charge constructor. Expose a fresh-operation storage factory accepting explicit UInt64 `width`, `height`, `pixels`, and `work` scalars and forwarding them once with length/budget/allocator; it never accepts `ResourceCharge` and never precharges. Arithmetic/unsupported/allocator/budget failures preserve counters; success charges storage and extras once before allocation/loop. Retain metadata/views and expose no raw backing.</action>
  <verify><automated>moon -C modules/mb-image test storage --target all --frozen</automated></verify>
  <done>Owned construction and immutable crops have explicit storage, lifetime, range, and atomic budget evidence including empty and padded-row cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce callback-scoped mutable image authority</name>
  <files>modules/mb-image/storage/views.mbt, modules/mb-image/storage/storage_wbtest.mbt, policy/foundation.json</files>
  <behavior>
    - `with_mut_view` acquires one enclosing backing lease and every derived handle rejects access after normal or error callback exit.
    - Empty and overlapping mutable crop/split requests reject before mutation; checked disjoint packed ranges can split and mutate without affecting sentinels.
    - Retained descendants cannot poison owner reacquisition; cleanup restores owner availability exactly once.
  </behavior>
  <action>Build `MutImageView` as a runtime-validated logical mapping over one enclosing `OwnedBytes::with_mut` lease per D-10. Derived disjoint views must share a callback scope, prove rectangle and byte-range disjointness before creation, and fail closed after scope exit. Reject mutable empty crops. Do not acquire per-plane independent leases or claim static/thread-safe borrowing. Register storage's exact interface/imports/files/targets in policy only after all lifecycle tests pass.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Immutable and mutable image views enforce backing lifetime and alias rules across all targets, with the exact package surface qualified.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07 | Elevation of Privilege | mutable view authority | high | mitigate | Opaque callback scope plus active-handle checks invalidate all descendants at exit. |
| T-04-08 | Tampering | disjoint split/crop | high | mitigate | Checked logical rectangles and backing byte ranges are proven disjoint before mutation. |
| T-04-09 | Denial of Service | construction budgets | high | mitigate | Pure preflight precedes the single authoritative storage allocation and work loop. |
</threat_model>

<verification>Focused storage tests cover normal/error cleanup, padded rows, planar rejection, sentinels, and budgets; Required proves exact four-target topology.</verification>
<success_criteria>IMAG-02 through IMAG-04 are usable without raw backing, escaped authority, copying representable crops, or partial invalid-path charge.</success_criteria>
<output>Create `.planning/phases/04-image-model-views-and-operations/04-03-SUMMARY.md` when done.</output>
