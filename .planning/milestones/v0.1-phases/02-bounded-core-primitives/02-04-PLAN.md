---
phase: 02-bounded-core-primitives
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [modules/mb-core/bytes/moon.pkg, modules/mb-core/bytes/owned_bytes.mbt, modules/mb-core/bytes/views.mbt, modules/mb-core/bytes/bytes_test.mbt, modules/mb-core/bytes/bytes_wbtest.mbt, policy/foundation.json]
autonomous: true
requirements: [CORE-02]
must_haves:
  truths: ["D-04/D-05/D-06: opaque retained byte windows and runtime-validated exclusive mutable leases cannot escape allocation or overlap.", "D-07: range, budget, injected allocator failure, and unrecoverable built-in physical OOM are represented honestly and distinctly.", "Bytes topology and classifiers leave root Required green."]
  artifacts:
    - { path: "modules/mb-core/bytes/owned_bytes.mbt", provides: "OwnedBytes, Allocator seam, budgeted constructors" }
    - { path: "modules/mb-core/bytes/views.mbt", provides: "ByteView, MutByteLease, subview, scoped acquisition, split_mut" }
  key_links:
    - { from: "modules/mb-core/bytes/owned_bytes.mbt", to: "modules/mb-core/budget", via: "precharge before allocation", pattern: "ResourceCharge" }
  assumptions:
    - { id: EDGE-CORE-02-IDEMPOTENCY, statement: "Immutable reads/subviews repeat stably and lease cleanup is idempotent without reactivation.", verification: automated-test, flagged: true }
    - { id: EDGE-CORE-02-CONCURRENCY, statement: "Reentrant/interleaved overlap is rejected; checked split yields disjoint children; no unproven thread guarantee.", verification: adversarial-test, flagged: true }
  prohibitions:
    - { id: PROH-CORE-02-CATCHABLE-OOM, requirement_id: CORE-02, category: correctness, statement: "MUST NOT claim built-in physical OOM is a recoverable portable result; only checked/budget and injected allocator rejection are structured.", status: unverified, verification: "Required docs/interface checks and allocator-double tests", flagged: true }
---
<!-- phase-source-audit: CORE-02,D-04,D-05,D-06,D-07,EDGE-CORE-02-IDEMPOTENCY,EDGE-CORE-02-CONCURRENCY,PROH-CORE-02-CATCHABLE-OOM -->
<objective>Deliver the fourth locked package: owned bytes, validated immutable views, and compiler-proven runtime mutable leases.
Purpose: portable byte access remains bounded and alias-safe before I/O consumes it.
Output: public `bytes`, early lease spike evolved into final behavior, exact policy entry, green Required.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-core-primitives/02-CONTEXT.md
@.planning/phases/02-bounded-core-primitives/02-RESEARCH.md
@.planning/phases/02-bounded-core-primitives/02-PATTERNS.md
@.planning/phases/02-bounded-core-primitives/02-03-SUMMARY.md</context>
## Artifacts this phase produces

- `bytes/moon.pkg`; `owned_bytes.mbt` (`OwnedBytes`, open `Allocator`, budgeted constructors); `views.mbt` (`ByteView`, `MutByteLease`, `subview`, scoped lease, `split_mut`); public/internal tests; updated policy.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Compile-spike and freeze runtime lease ergonomics</name>
  <files>modules/mb-core/bytes/moon.pkg, modules/mb-core/bytes/views.mbt, modules/mb-core/bytes/bytes_wbtest.mbt</files>
  <read_first>resolved research Open Questions; D-04..D-06; pinned FixedArray/MutArrayView; checked/budget APIs</read_first>
  <behavior>Probe/tests cover acquire, mutation, invalidation, second overlap rejection, parent-consuming disjoint split, child use, stale use, and idempotent cleanup on all targets.</behavior>
  <action>Run the resolved early compile/test spike for callback-scoped `MutByteLease`. Ship shared active-state validation, deterministic invalidation, and checked `split_mut`; if compiler syntax forces an equivalent scoped method surface, record evidence but preserve all D-06 runtime behavior. Never expose raw mutable backing.</action>
  <acceptance_criteria>The chosen surface compiles on all targets and proves exclusive acquisition, disjoint split, stale rejection, and idempotent cleanup.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test bytes --target all --frozen</automated></verify>
  <done>The research question is executable and the locked lease behavior ships.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Complete owned storage and validated views</name>
  <files>modules/mb-core/bytes/owned_bytes.mbt, modules/mb-core/bytes/views.mbt, modules/mb-core/bytes/bytes_test.mbt, modules/mb-core/bytes/bytes_wbtest.mbt</files>
  <read_first>D-04/D-05/D-07; research Pattern 2/Pitfalls 2/5; Task 1 lease API</read_first>
  <behavior>Tests first cover retained backing, relative empty/adjacent subviews, range-before-access, budget-before-allocation, copied external bytes, and distinct range/limit/injected AllocationFailed results.</behavior>
  <action>Implement opaque `OwnedBytes` and zero-copy retained `ByteView`; validate every relative window. Use built-in storage behind budgeted constructors and an injected `Allocator` double seam. Document built-in physical OOM as unrecoverable while testing preflight and explicit allocator rejection per D-07.</action>
  <acceptance_criteria>No backing/raw mutable view escapes, allocation effects follow checks/charges, and tests never fabricate catchable physical OOM.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test bytes --target all --frozen</automated></verify>
  <done>CORE-02 ownership, bounds, aliasing, and failure distinctions are complete.</done>
</task>
<task type="auto">
  <name>Task 3: Register bytes and prove the green exit</name>
  <files>policy/foundation.json</files>
  <read_first>bytes interface/package output; current policy</read_first>
  <action>Add exact bytes identity, targets, earlier imports, interface, and contents; retain prior packages; run Required.</action>
  <acceptance_criteria>Policy/classifiers exactly include bytes and root Required passes.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Plan 02-04 exits green atomically.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-06 | Tampering / DoS | mutable leases | high | mitigate | opaque backing, active identity, checked split/stale validation |
| T-02-07 | Information Disclosure | byte views | high | mitigate | retained validated windows before access |
</threat_model>
<verification>All-target bytes tests plus root Required.</verification>
<success_criteria>CORE-02, both edge probes, and the OOM prohibition are explicit and tested/backstopped.</success_criteria>
<output>Create `.planning/phases/02-bounded-core-primitives/02-04-SUMMARY.md` when done.</output>
