---
phase: 02-bounded-core-primitives
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1, modules/mb-core/error/moon.pkg, modules/mb-core/error/core_error.mbt, modules/mb-core/error/diagnostics.mbt, modules/mb-core/error/error_test.mbt, modules/mb-core/error/error_wbtest.mbt]
autonomous: true
requirements: [CORE-06]
must_haves:
  truths:
    - "D-12/D-13/D-14: stable category/code pairs, typed bounded context, fixed rendering/escaping, and ordered diagnostics expose no ambient/backend prose."
    - "Adding the first subpackage and updating policy/classifiers is one atomic plan exit whose root Required lane passes."
  artifacts:
    - { path: "modules/mb-core/error/core_error.mbt", provides: "ErrorCategory, ErrorCode, opaque CoreError, render_error" }
    - { path: "modules/mb-core/error/diagnostics.mbt", provides: "DiagnosticSeverity, Diagnostic, Diagnostics, render_diagnostic" }
    - { path: "scripts/quality/Invoke-MoonQuality.ps1", provides: "Policy-driven exact multi-package interface/package classification" }
  key_links:
    - { from: "policy/foundation.json", to: "modules/mb-core/error/moon.pkg", via: "exact package identity/targets/imports", pattern: "mb-core/error" }
  assumptions:
    - { id: EDGE-CORE-06-UNCLASSIFIED, statement: "Review stable codes, typed context, order, escaping, and host-leak behavior for an omitted diagnostic edge.", verification: phase-verification-review, flagged: true }
  prohibitions:
    - { id: PROH-CORE-06-PROSE-SEMANTICS, requirement_id: CORE-06, category: determinism, statement: "MUST NOT require prose parsing or expose ambient paths, clocks, foreign exception names, or platform codes as portable semantics.", status: unverified, verification: "Required interface/source checks plus deterministic public tests", flagged: true }
---

<!-- phase-source-audit: CORE-06,D-12,D-13,D-14,EDGE-CORE-06-UNCLASSIFIED,PROH-CORE-06-PROSE-SEMANTICS -->

<objective>Establish the error/diagnostic vocabulary and convert the root quality machinery to policy-driven exact package topology without any broken plan exit.
Purpose: every later package needs structured deterministic failures, and every topology increment must leave Required green.
Output: public `error`, generalized fail-closed classifiers, policy updated for root-plus-error, and a passing root Required lane.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-core-primitives/02-CONTEXT.md
@.planning/phases/02-bounded-core-primitives/02-RESEARCH.md
@.planning/phases/02-bounded-core-primitives/02-PATTERNS.md
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1</context>

## Artifacts this phase produces

- `error/moon.pkg`, `core_error.mbt` (`ErrorCategory`, `ErrorCode`, `CoreError`, `render_error`), `diagnostics.mbt` (`DiagnosticSeverity`, `Diagnostic`, `Diagnostics`, `render_diagnostic`), public/internal tests.
- Updated `foundation.json`, `Assert-Policy.ps1`, and `Invoke-MoonQuality.ps1` supporting exact incremental package inventories while the Phase 1 root package remains until Plan 02-07.

<tasks>
<task type="auto">
  <name>Task 1: Generalize exact policy and Required classifiers</name>
  <files>policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <read_first>02-PATTERNS.md policy/quality assignments; current root-package classifiers; foundation mb-core record</read_first>
  <action>Generalize validation to iterate the exact policy-owned package list, comparing canonical paths/names, targets, allowed imports, semantic interface allowlists, and package contents. Preserve fixed commands, JSON-as-data, formatter compatibility, per-module docs/info, four targets, and read-only proof. Initially retain the declared Phase 1 root package so the plan can atomically add `error`; reject undeclared extras and reverse/cyclic imports.</action>
  <acceptance_criteria>Classifiers are exact and policy-driven rather than broad discovery, and the unchanged baseline Required lane still passes before adding `error`.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Quality automation can accept only explicitly declared incremental package topology.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add stable errors and deterministic diagnostics atomically</name>
  <files>modules/mb-core/error/moon.pkg, modules/mb-core/error/core_error.mbt, modules/mb-core/error/diagnostics.mbt, modules/mb-core/error/error_test.mbt</files>
  <read_first>02-CONTEXT.md D-12..D-14; 02-RESEARCH.md Pattern 5/Pitfall 6; Task 1 policy schema</read_first>
  <behavior>Tests first cover category/code equality, typed requested/completed/range/source-offset fields, bounded adapter context, fixed order/escaping, locale-independent numerics, encounter order, and excluded host text.</behavior>
  <action>Write the public black-box tests before implementation, then create `error` with exact four-target metadata and implement the listed public symbols using explicit canonical rendering rather than Debug. Map host failures at the boundary per D-12/D-13/D-14.</action>
  <acceptance_criteria>Structured and rendered behavior passes on all targets and no foreign/platform prose is public semantics.</acceptance_criteria>
  <verify><automated>moon -C modules/mb-core test error --target all --frozen</automated></verify>
  <done>The lowest package exists with deterministic public behavior and exact policy ownership.</done>
</task>
<task type="auto">
  <name>Task 3: Register error invariants and prove the atomic green exit</name>
  <files>modules/mb-core/error/error_wbtest.mbt, policy/foundation.json, scripts/quality/Invoke-MoonQuality.ps1</files>
  <read_first>completed error interface/package list; updated classifier schema; Required stage order</read_first>
  <action>Add internal escaping/order/bounded-context invariants. Add the exact error identity, targets, imports, semantic interface, and publication contents to policy/classifiers without weakening root-package checks. Run the sole root lane and correct only classifier defects exposed by real outputs.</action>
  <acceptance_criteria>Root Required passes with root-plus-error topology, four-target tests, docs/interfaces/packages/policy, and no tracked mutation.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>Plan 02-01 exits green with topology and classifiers synchronized.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Information Disclosure | renderer | high | mitigate | bounded portable context and explicit canonical rendering |
| T-02-02 | Tampering | package policy | high | mitigate | exact policy-owned identities, interfaces, contents, and DAG |
</threat_model>
<verification>Package tests run on all targets and the plan ends with the existing root Required lane green.</verification>
<success_criteria>CORE-06 is public and deterministic; topology/policy/classifiers change atomically; the flagged edge/prohibition remains visible to phase verification.</success_criteria>
<output>Create `.planning/phases/02-bounded-core-primitives/02-01-SUMMARY.md` when done.</output>
