---
phase: 03-reference-color-semantics
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [scripts/fixtures/Generate-ColorVectors.ps1, fixtures/color/srgb-reference-vectors.json, fixtures/color/derived-edge-vectors.json, fixtures/manifest.json]
autonomous: true
requirements: [COLR-04]
must_haves:
  truths:
    - "D-13/D-14: primary-formula-derived reference data and locally derived adversarial cases are separate, deterministic, and registered with honest provenance and actual digests."
    - "One selective generator can emit fixtures or exactly one package-local MoonBit vector table for transfer, quantize, alpha, or profile; portable tests never read files at runtime."
    - "Repeated generation is byte-identical and fixture-policy failures remain fail closed."
  artifacts:
    - { path: "scripts/fixtures/Generate-ColorVectors.ps1", provides: "selective deterministic fixtures/transfer/quantize/alpha/profile artifact generator" }
    - { path: "fixtures/color/srgb-reference-vectors.json", provides: "standards-formula-derived conversion vectors" }
    - { path: "fixtures/color/derived-edge-vectors.json", provides: "project-derived quantize, alpha, profile, and adversarial vectors" }
    - { path: "fixtures/manifest.json", provides: "origin/source/author/date/digest/license/redistribution/use records" }
  key_links:
    - { from: "scripts/fixtures/Generate-ColorVectors.ps1", to: "fixtures/color/*.json", via: "invariant-culture stable JSON and SHA-256 refresh", pattern: "Artifacts" }
---
<!-- phase-source-audit: GOAL,COLR-04,D-13,D-14 -->
<objective>Establish reproducible Phase 3 evidence independently from package implementation.
Purpose: keep standards provenance honest while letting every portable package consume generated MoonBit constants without filesystem or host capabilities.
Output: selective generator, two registered fixture classes, and a green fixture-policy matrix.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-reference-color-semantics/03-CONTEXT.md
@.planning/phases/03-reference-color-semantics/03-RESEARCH.md
@.planning/phases/03-reference-color-semantics/03-01-SUMMARY.md
@docs/policies/licensing-and-fixtures.md
@fixtures/manifest.json
@scripts/quality/Test-FixturePolicy.ps1</context>
<tasks>
<task type="auto">
  <name>Task 1: Generate and register honest reference evidence</name>
  <files>scripts/fixtures/Generate-ColorVectors.ps1, fixtures/color/srgb-reference-vectors.json, fixtures/color/derived-edge-vectors.json, fixtures/manifest.json</files>
  <read_first>D-13/D-14; research Fixture Provenance, numerical formulas, and adversarial classes; fixture policy and validator</read_first>
  <action>Create a deterministic PowerShell generator with `-Artifacts fixtures|transfer|quantize|alpha|profile|all` and `-Check`. `fixtures` emits only the two JSON artifacts plus refreshed manifest digests; each package selector emits only its corresponding `modules/mb-color/{package}/reference_vectors_wbtest.mbt` from the same canonical in-memory dataset, and `-Check` compares expected bytes without writing. Use invariant culture, stable key/order/newline formatting, published sRGB formulas, and explicit derived cases. Register the JSON artifacts with unique IDs, contained paths, `origin=generated`, primary formula URLs or repository-derived source as appropriate, project generator authorship, retrieval date `2026-07-17`, actual SHA-256, Apache-2.0, `redistribution_status=not-applicable`, and precise expected use. Do not copy third-party bytes or call derived values official.</action>
  <acceptance_criteria>`fixtures` touches no package directory; each later package selector touches exactly one declared test table; two runs are byte-identical.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/fixtures/Generate-ColorVectors.ps1 -Artifacts fixtures -Check</automated></verify>
  <done>The complete evidence dataset and selective code-generation seam are deterministic and scoped.</done>
</task>
<task type="auto">
  <name>Task 2: Prove fixture provenance remains fail closed</name>
  <files>fixtures/manifest.json</files>
  <read_first>generated files and digests; fixture containment/digest/redistribution negative matrix</read_first>
  <action>Run fixture policy validation against the new records and confirm missing file, digest mismatch, traversal, malformed date, and unconfirmed external redistribution cases still reject for the intended reason. Run the full Required lane once to prove the new tracked fixtures are stable inputs.</action>
  <acceptance_criteria>Positive records pass and every existing negative provenance/containment case still fails closed.</acceptance_criteria>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-FixturePolicy.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality.ps1 -Lane Required</automated></verify>
  <done>COLR-04 provenance evidence is registered without weakening repository fixture policy.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04 | Repudiation | reference provenance | high | mitigate | primary source, derivation label, date/license and actual digest records |
| T-03-05 | Tampering | generated artifacts | high | mitigate | selective byte-for-byte check mode and stable serialization |
| T-03-06 | Elevation of Privilege | portable tests | medium | mitigate | generated constants only; no filesystem/host capability at test runtime |
</threat_model>
<verification>Generator check, fixture negative matrix, and root Required.</verification>
<success_criteria>D-13/D-14 and the evidence foundation of COLR-04 are complete with no implementation coupling or ambient file access.</success_criteria>
<output>Create `.planning/phases/03-reference-color-semantics/03-02-SUMMARY.md` when done.</output>
