---
phase: 58-portable-adam7-public-evidence
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode_test.mbt
autonomous: true
requirements: [GRAYA16A7-03]
must_haves:
  truths:
    - "A public Stored/None GrayAlpha16 Adam7 PNG from the legal non-symmetric 5x5 source exposes the complete seven-pass Type-4/16 Ghi,Glo,Ahi,Alo raster without private encoder helpers."
    - "The same public PNG decodes as a 5x5 straight RGBA8 image whose RGB lanes equal each source gray high byte and whose alpha equals each source alpha high byte."
    - "Every legal public GrayAlpha16 Adam7 strategy pair retains Type-4/16 Adam7 framing and the documented RGBA8 high-byte decode behavior, while literal non-interlaced compatibility vectors remain unchanged."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Public GrayAlpha16 Adam7 wire, decode, selector, and frozen eager compatibility evidence."
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/encode.mbt
      via: "PngEncoder public Adam7 factories emit bytes inspected by a bounded PNG parser and PngDecoder decodes those emitted bytes."
      pattern: "PngEncoder::new_graya16_with_all_strategies|PngDecoder::new"
---

<objective>
Create independent public eager evidence for the legal GrayAlpha16 Adam7 contract: literal Type-4/16 pass data and documented straight-RGBA8 high-byte decode behavior.

Purpose: Prove the consumer-visible wire and decode boundary without duplicating private traversal or changing the already-verified encoder.
Output: Focused public eager PNG regressions in `encode_test.mbt`.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-portable-adam7-public-evidence/58-CONTEXT.md
@.planning/phases/58-portable-adam7-public-evidence/58-RESEARCH.md
@.planning/phases/58-portable-adam7-public-evidence/58-PATTERNS.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-VERIFICATION.md
@modules/mb-image/png/encode_test.mbt
</context>

<feature>
  <name>Public GrayAlpha16 Adam7 wire and high-byte decode evidence</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - A legal 5x5 little-endian GrayAlpha16 source reaches all seven Adam7 passes through `PngEncoder` and emits IHDR depth 16, colour type 4, and interlace method 1.
    - Stored/None emitted bytes parse through the existing bounded public Stored-IDAT parser to exactly the independently derived 111-byte pass stream, including every Ghi,Glo,Ahi,Alo lane.
    - The public `PngDecoder` result is U8/Rgba, 5x5, and for every source position yields `(Ghi,Ghi,Ghi,Ahi)`.
  </behavior>
  <implementation>Begin RED by adding a named public-evidence test around `png_encode_graya16_adam7_image`, its independent pass tuple oracle, a public `PngEncoder::new_graya16_with_all_strategies(Stored, None, Adam7)` factory, and `ImageDecoder::decode(PngDecoder::new(), ...)`. Per D-01 and D-02, generalize only `png_encode_gray16_public_stored_scanlines` enough to validate the known 111-byte Adam7 stored payload, retaining it as a bounded emitted-byte parser rather than a general inflater. Add a dedicated 5x5 public decode helper that derives expected Ghi/Ahi from the fixture formula and checks every pixel; keep the older 2x1 non-interlaced decoder helper untouched. GREEN uses only public emitted bytes, public decoder APIs, and the existing independently authored Adam7 pass geometry. Do not read a cursor/profile helper, add production code, widen decode models, admit Big-endian descriptors, or create a staging route.</implementation>
</feature>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one full public Stored/None Adam7 wire-to-decode path</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - Stored/None Adam7 framing is type 4, depth 16, interlace 1.
    - The independently expected 111-byte seven-pass raster equals the bounded parser result.
    - Public decoding validates all 25 high-byte grayscale and alpha values.
  </behavior>
  <action>Implement D-01 and D-02 with a RED-to-GREEN public regression. Reuse the deliberately non-symmetric legal little-endian `png_encode_graya16_adam7_image` and `png_encode_graya16_adam7_expected_passes`; do not replace either with symmetric data or a production-derived oracle. Construct output through the all-strategy public eager factory with Stored, None, and Adam7, validate IHDR, extract only the known Stored IDAT payload through the bounded public PNG parser, and compare the complete pass raster. Add an Adam7-specific public decode helper driven by the fixture's x/y sample formula, require U8/Rgba dimensions 5x5, and check RGB high-byte replication plus alpha high byte at every position. Keep the legacy 2x1 helper as its own non-interlaced proof and make no production/API/fixture/script changes.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 public wire and decode'</automated>
  </verify>
  <done>A named public test proves a legal non-symmetric Adam7 Type-4/16 PNG's complete literal pass raster and every public RGBA8 high-byte decoded pixel.</done>
  <reversibility rating="costly">Per D-01, the literal wire vector and high-byte decoder assertions define a public compatibility boundary; changing either requires an explicit contract decision.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand eager public evidence across legal selectors and frozen None routes</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <behavior>
    - Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive all retain Type-4/16 Adam7 framing and the 5x5 public decode oracle.
    - Existing literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 non-interlaced vectors remain equal to their fixed bytes and method 0.
  </behavior>
  <action>Implement D-01, D-04, and D-05 by extending the existing six-selector Adam7 eager matrix to call the new public 5x5 decode oracle after checking Type-4/16/Adam7 framing. Preserve Stored/None as the sole literal pass-stream parser case; Adaptive and compressed fallbacks need framing plus public decoder evidence, not a second DEFLATE parser. Keep the existing frozen eager compatibility test as literal-byte evidence for Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8, and add focused method-0 assertions only where the retained test lacks an explicit interlace check. All new sources remain legal little-endian GrayAlpha16. Do not alter expected byte literals, non-interlaced factories, descriptor admission, production sources, target branches, release tooling, or decoder semantics.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 public eager evidence'</automated>
  </verify>
  <done>All six legal public eager selector pairs prove framing and high-byte decode behavior, while the five literal eager compatibility families retain their non-interlaced bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legal U16 image to public PNG bytes | Distinct Gray/Alpha high and low bytes cross the public eager factory into standards-visible pass lanes. |
| Public PNG bytes to decoded image | Type-4/16 data crosses the decoder's documented U16-to-straight-RGBA8 canonicalization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-58-01 | Tampering | Adam7 public wire proof | high | mitigate | Task 1 implements D-01/D-02 with non-symmetric samples, independent seven-pass geometry, literal Stored/None parsing, and full lane comparison. |
| T-58-02 | Tampering | public Type-4/16 decode boundary | high | mitigate | Tasks 1-2 decode emitted bytes only through PngDecoder and validate every RGBA8 high-byte result. |
| T-58-03 | Tampering | frozen eager PNG compatibility | high | mitigate | Task 2 retains literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 method-0 vectors under D-04. |
</threat_model>

<verification>
Run both named eager selectors on native during the RED/GREEN cycle. Wave 2 will serialize the complete public four-target PNG package command after this disjoint test-file plan and the chunk plan both finish.
</verification>

<success_criteria>
- [ ] D-01/D-02: public Stored/None output proves the full literal Type-4/16 Adam7 seven-pass Ghi,Glo,Ahi,Alo raster and public 5x5 straight-RGBA8 high-byte canonicalization.
- [ ] D-01: every legal eager compression/filter pair has public Adam7 framing and decoder evidence.
- [ ] D-04: literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 eager baselines remain non-interlaced compatibility artifacts.
- [ ] No production encoder, private traversal proof, Big-endian source route, staging buffer, decoder model change, target fork, or release artifact is introduced.
</success_criteria>

<output>
Create `.planning/phases/58-portable-adam7-public-evidence/58-01-SUMMARY.md` when done.
</output>
