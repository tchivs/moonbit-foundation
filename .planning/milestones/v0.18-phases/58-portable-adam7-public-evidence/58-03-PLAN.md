---
phase: 58-portable-adam7-public-evidence
plan: "03"
type: execute
wave: 2
depends_on: [58-01, 58-02]
files_modified: []
autonomous: true
requirements: [GRAYA16A7-03]
must_haves:
  truths:
    - "The complete public PNG package passes unchanged test behavior on js, wasm, wasm-gc, and native from one serialized command."
    - "Phase 58 implementation changes are confined to the two public PNG test files and do not introduce production, API, script, fixture, or target-specific work."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Public eager Adam7 and frozen compatibility evidence from Plan 58-01."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Public hostile-schedule and frozen chunk evidence from Plan 58-02."
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: "One public package test command compiles and executes both evidence surfaces on all supported targets."
      pattern: "moon -C modules/mb-image test png --target all --frozen"
---

<objective>
Qualify the completed public evidence through the repository's ordinary four-target PNG package test and audit its implementation scope.

Purpose: Make D-05 a reproducible portable gate without adding a release runner or another test harness.
Output: Recorded all-target evidence and a scoped-diff result in the Phase 58 summary.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-portable-adam7-public-evidence/58-CONTEXT.md
@.planning/phases/58-portable-adam7-public-evidence/58-RESEARCH.md
@.planning/phases/58-portable-adam7-public-evidence/58-PATTERNS.md
@.planning/phases/58-portable-adam7-public-evidence/58-01-PLAN.md
@.planning/phases/58-portable-adam7-public-evidence/58-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the serialized four-target public PNG evidence gate</name>
  <files>None — read-only test execution; record results in 58-03-SUMMARY.md</files>
  <action>Implement D-05 by running exactly `moon -C modules/mb-image test png --target all --frozen` after Plans 58-01 and 58-02 are complete. Serialize this command after the focused work so MoonBit build state is not contended. Record the command, overall result, and the evidence that it exercised wasm, wasm-gc, js, and native in the plan summary. Do not add a script, retry wrapper, target directory, environment branch, or release automation.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The ordinary public PNG package suite passes on all four supported targets from one command.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the final Phase 58 change surface</name>
  <files>None — read-only repository audit; record results in 58-03-SUMMARY.md</files>
  <action>Check `git diff --check` and inspect the phase-scoped change list. Confirm functional changes are limited to `modules/mb-image/png/encode_test.mbt` and `modules/mb-image/png/stream_encode_test.mbt`, with planning summaries as the only additional artifacts. Confirm the tests use public `PngEncoder`, `PngChunkEncoder`, and `PngDecoder` seams; their expected wire and legacy bytes remain independent compatibility data. If a real public contract failure is exposed, stop and route that defect through the GSD debugging workflow rather than changing scope or weakening an assertion.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <done>The portable evidence has a clean diff and remains a two-public-test-file phase with no unexpected implementation surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Portable package runner to target builds | A single MoonBit command compiles and executes public tests for every supported target. |
| Planned test scope to repository change set | The phase must retain its public-evidence-only boundary despite discovering an implementation defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-58-07 | Tampering | portable qualification evidence | high | mitigate | Task 1 executes D-05's ordinary all-target public PNG command after both test plans complete. |
| T-58-08 | Tampering | phase implementation surface | medium | mitigate | Task 2 inspects the scoped change set and preserves the two-test-file public boundary from D-02 through D-04. |
</threat_model>

<verification>
1. `moon -C modules/mb-image test png --target all --frozen`
2. `git diff --check`
3. Inspect the phase-scoped change list and confirm only public test code plus planning/summary artifacts were added.
</verification>

<success_criteria>
- [ ] D-05: one standard MoonBit public PNG package run passes on js, wasm, wasm-gc, and native.
- [ ] GRAYA16A7-03 remains demonstrated only by the public eager/chunk test surfaces and fixed compatibility data from Plans 58-01 and 58-02.
- [ ] The phase introduces no release script, target fork, source copy, production encoder route, FFI, or new fixture artifact.
</success_criteria>

<output>
Create `.planning/phases/58-portable-adam7-public-evidence/58-03-SUMMARY.md` when done.
</output>
