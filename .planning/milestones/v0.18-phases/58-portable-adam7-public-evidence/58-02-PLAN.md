---
phase: 58-portable-adam7-public-evidence
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16A7-03]
must_haves:
  truths:
    - "Each legal GrayAlpha16 Adam7 compression/filter pair drains from a fresh public chunk encoder under direct zero-capacity, one-byte, and ragged schedules to byte-identical fresh eager output."
    - "Every caller lease reports accepted-only cumulative progress, leaves its unwritten sentinel tail intact, and retains a sticky successful terminal after completion."
    - "Literal non-interlaced Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 chunk baselines remain unchanged."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Public hostile-schedule and frozen chunk compatibility evidence for GrayAlpha16 Adam7."
  key_links:
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Fresh public PngChunkEncoder Adam7 factories are pulled through caller-owned leases and compared with matching public eager bytes."
      pattern: "PngChunkEncoder::new_graya16_with_all_strategies|PngChunkPullOutcome::Finished"
---

<objective>
Create public caller-buffer evidence that every legal GrayAlpha16 Adam7 selector preserves byte identity and caller ownership under hostile output capacities.

Purpose: Prove the public `PngChunkEncoder::pull` contract rather than re-testing the private replay machine already verified in Phase 57.
Output: Focused hostile-schedule and frozen chunk compatibility regressions in `stream_encode_test.mbt`.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-portable-adam7-public-evidence/58-CONTEXT.md
@.planning/phases/58-portable-adam7-public-evidence/58-RESEARCH.md
@.planning/phases/58-portable-adam7-public-evidence/58-PATTERNS.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-VERIFICATION.md
@modules/mb-image/png/stream_encode_test.mbt
</context>

<feature>
  <name>Public GrayAlpha16 Adam7 hostile caller-buffer evidence</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - For all six legal strategy/filter pairs, a one-byte Z owner lent as a zero-length lease returns NeedOutput with zero current and cumulative output and an unchanged Z byte.
    - Fresh `[0UL, 1UL]`, `[1UL]`, and `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]` drains append only accepted bytes and finish exactly equal to a fresh eager result.
    - Each nonzero lease keeps unwritten Z bytes unchanged, and a later seven-byte Z lease reports zero writes, unchanged total, Finished, and an intact owner.
  </behavior>
  <implementation>Start RED with a named all-six-pair public-evidence test using only `PngChunkEncoder::new_graya16_with_all_strategies(..., Adam7, ...)` and `png_stream_graya16_eager_with_all_strategies`. Per D-03, adapt the established local Adam7 drain helper to construct one fresh chunk encoder for exactly one schedule and use the one-byte Z owner direct empty-lease probe before normal pulls. GREEN keeps accepted-prefix accounting, tail checks, eager equality, and later terminal checks in that same public helper. Keep U16 replay-mutation coverage in Phase 57 tests and avoid private machine inspection, alternate encoders, staging, or production changes.</implementation>
</feature>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one public Adam7 hostile schedule end to end</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Adaptive plus FixedOrStored performs the direct zero-capacity probe, a fresh one-byte drain, and a fresh ragged drain.
    - Each drain is byte-identical to a fresh public eager encoding and preserves accepted-prefix, tail, and sticky-Finished invariants.
  </behavior>
  <action>Implement D-03 as a RED-to-GREEN tracer by tightening `png_graya16_adam7_chunk_drain` around the established public lease pattern. Replace its zero-length owner with a one-byte Z owner lent as a zero-length lease and assert the owner remains Z. Keep the helper's per-call accepted-only total calculation, only append the reported written prefix, require all unwritten tail bytes to remain Z, compare completion to a fresh eager byte oracle, and verify a later seven-byte Z lease is unchanged while reporting sticky Finished. Add a focused public tracer for Adaptive plus FixedOrStored using `[1UL]` and the established ragged schedule. Construct exclusively through the legal little-endian Adam7 all-strategy public chunk factory; do not inspect private replay state or touch production code.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 public hostile tracer'</automated>
  </verify>
  <done>A single public legal Adam7 selector has end-to-end direct empty-lease, hostile-capacity, eager-identity, caller-tail, and sticky-terminal evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand hostile schedules to all selectors and retain literal chunk baselines</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive each use fresh encoders for zero/one/ragged schedules.
    - The literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 chunk vectors remain exact method-0 compatibility checks.
  </behavior>
  <action>Implement D-03 and D-04 by adding one named public evidence matrix that invokes the strengthened helper separately for each legal compression/filter pair and each required fresh schedule: `[0UL, 1UL]`, `[1UL]`, and `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]`. Ensure every invocation constructs a new GrayAlpha16 Adam7 chunk encoder and obtains a new eager oracle for its matching pair. Keep the literal frozen chunk-vector test for Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 as its own compatibility oracle, adding explicit method-0 assertions only where needed. Do not derive a legacy expected value from current output, alter literal bytes, widen descriptor admission, add a target condition, or introduce release tooling, fixture files, source copies, or production behavior.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 public hostile schedules'</automated>
  </verify>
  <done>All six legal public Adam7 pairs independently satisfy zero/one/ragged caller-buffer evidence, and the five literal chunk compatibility families remain frozen and non-interlaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Encoder state to caller-owned lease | `PngChunkEncoder::pull` is allowed to write only its reported accepted prefix into a caller-provided lease. |
| Public eager result to public chunk result | Two independently constructed public encoders must converge on one complete PNG byte sequence for each legal selector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-58-04 | Tampering | caller lease progress and ownership | high | mitigate | Tasks 1-2 apply D-03 direct empty-lease probes, accepted-only totals, Z-tail checks, and fresh zero/one/ragged drains. |
| T-58-05 | Tampering | post-completion stream behavior | high | mitigate | Task 1 verifies a later seven-byte Z lease retains bytes and total while reporting Finished. |
| T-58-06 | Tampering | frozen chunk PNG compatibility | high | mitigate | Task 2 retains D-04 literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 method-0 vectors. |
</threat_model>

<verification>
Run both named hostile-schedule selectors on native during the RED/GREEN cycle. Wave 2 will run the complete public four-target PNG suite only after this plan and the disjoint eager plan have landed.
</verification>

<success_criteria>
- [ ] D-03: every legal None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored pair uses fresh zero, one-byte, and ragged public drains with eager byte identity, accepted-only totals, tails, and sticky terminal outcome.
- [ ] D-04: literal Gray8, Gray16, GrayAlpha8, RGB8, and straight-RGBA8 chunk vectors remain method-0 compatibility evidence.
- [ ] No private state assertion, source staging, alternative encoder, Big-endian route, production code, fixture download, target branch, or release artifact is introduced.
</success_criteria>

<output>
Create `.planning/phases/58-portable-adam7-public-evidence/58-02-SUMMARY.md` when done.
</output>
