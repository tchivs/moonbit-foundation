---
phase: 57-bounded-adam7-streaming-semantics
plan: "02"
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16A7-02]
must_haves:
  truths:
    - "Incompatible capability, geometry, output, work, and budget requests for every legal GrayAlpha16 Adam7 strategy pair fail before eager output or caller lease exposure."
    - "After a checked U16 mutation, every None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored GrayAlpha16 Adam7 replay fails before its next lease write, retains accepted progress, and returns its same sticky terminal outcome on later pulls."
    - "The public bounded failure and replay checks use the existing single profile-aware PngEncodeMachine rather than a separate Adam7 failure path."
  artifacts:
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "U16 replay-revision guard that rejects any changed admitted U16 source for Stored, Fixed, and Dynamic plans before a caller lease write."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "GrayAlpha16 Adam7 all-pair pre-exposure atomicity and table-driven U16 replay-drift sticky-terminal regressions."
  key_links:
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Public eager and chunk factories must agree before PngChunkEncoder can expose an active lease."
      pattern: "new_graya16_with_all_strategies"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "The profile-aware preflight completes before PngEncodeMachine construction and its single budget charge."
      pattern: "_png_encode_preflight_with_interlace_profile"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "A checked U16 component mutation reaches validate_u16_replay_revision before destination.set in pull for every supported Adam7 strategy pair."
      pattern: "set_component_byte"
---

<objective>
Complete the GrayAlpha16 Adam7 bounded contract with public atomic-admission and source-mutation replay evidence on the already-proven shared machine.

Purpose: Deliver GRAYA16A7-02's failure and sticky-replay guarantees after Plan 57-01 has established normal all-strategy traversal. Locked-decision coverage for the plan set: D-01: one profile-aware bounded route with pass-local history; D-02: atomic preflight before output or lease; D-03: pre-write U16 replay validation with sticky terminals; D-04: preserve little-endian-only admission and frozen non-interlaced routes.
Output: A shared Stored/Fixed/Dynamic U16 replay-revision correction plus focused public stream regressions for all-pair atomic construction and pre-write sticky mutation failure.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-CONTEXT.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-PATTERNS.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-01-SUMMARY.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-VERIFICATION.md
@.planning/milestones/v0.13-phases/42-bounded-adam7-pass-encoding/42-02-SUMMARY.md
@.planning/milestones/v0.17-phases/54-bounded-type-4-16-encoder/54-02-PLAN.md
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
From modules/mb-image/png/stream_encode.mbt:
- `PngChunkEncoder::new_graya16_with_all_strategies(...)` returns an error before constructing an active encoder when profile-aware preflight rejects the request.
- `PngChunkEncoder::pull(lease)` validates `PngEncodeMachine::validate_u16_replay_revision()` before copying into the caller lease; failed state replays its same terminal `CoreError`.
- `PngEncodeMachine::validate_u16_replay_revision()` compares the retained source revision for U16 wire profiles before `pull` can call `destination.set`; its Stored, Fixed, and Dynamic branches must all return plan-specific drift errors after a revision change.

From modules/mb-image/png/stream_encode_test.mbt:
- `png_graya16_combined_public_reject` and `png_graya16_replay_mutation_is_sticky` are the Phase-54 analogues to extend with an explicit Adam7 selector rather than duplicate their writer, budget, error, sentinel-owner, and one-byte pull assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert all-pair GrayAlpha16 Adam7 admission is atomic before output or lease exposure</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - For each None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored choice, an incompatible source and legal-source geometry, output, work, or budget failure return equivalent eager and chunk errors.
    - Each eager rejection leaves the real MemoryWriter at position zero and leaves every budget field unchanged.
    - Each chunk rejection leaves no usable encoder, leaves the same budget unchanged, and preserves every byte in a preallocated sentinel owner.
  </behavior>
  <action>Per D-02: write RED cases by parameterizing the existing Phase-54 GrayAlpha16 public-rejection helper with `PngInterlaceStrategy::Adam7`; keep its real MemoryWriter, copied resource snapshot, typed-error comparator, and caller-owned sentinel inspection. Cross all six legal strategy/filter pairs, invoke the helper for incompatible capability plus width, output, work, and budget envelopes, and require the eager/chunk error and accounting results to agree. Route every case through `PngEncoder::new_graya16_with_all_strategies(..., Adam7)` and `PngChunkEncoder::new_graya16_with_all_strategies(..., Adam7, ...)`, so it observes the one profile-aware preflight transaction rather than a synthetic limit test. If RED reveals an actual shared preflight or constructor defect, change the smallest existing `encode.mbt` or `stream_encode.mbt` seam and rerun the same matrix; otherwise retain a test-only diff.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 strategy admission is atomic'</automated>
  </verify>
  <done>Every legal GrayAlpha16 Adam7 filter/compression pair rejects incompatible and resource-limited construction with zero writer output, no caller lease mutation, matching typed eager/chunk errors, and unchanged resource accounting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject U16 replay drift before lease writes for every Adam7 strategy pair</name>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Each table row for None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored creates a fresh GrayAlpha16 Adam7 encoder, accepts only its framing prefix, then a checked alpha-lane mutation makes the next pull fail with zero writes and the retained accepted total.
    - A periodic DynamicOrFixedOrStored row continues to prove dynamic DEFLATE selection, while FixedOrStored rows retain the existing selected-route assertion and Stored rows exercise the Stored replay branch under both filters.
    - For every table row, a fresh later sentinel lease receives no byte, the identical structured terminal error, and the same total as that row's first failed pull.
  </behavior>
  <action>Per D-03, start RED by changing `PngEncodeMachine::validate_u16_replay_revision()` in `stream_encode.mbt` so its `PngDeflatePlan::Stored(_)` branch returns a plan-specific machine-state drift error when an admitted U16 source revision differs; keep the existing Fixed and Dynamic branches and the guard's position at the top of active `PngChunkEncoder::pull`, before any `destination.set`. Then replace the Fixed/Dynamic-only Phase-54 replay test with one table-driven public helper that receives the compression strategy, filter strategy, source shape, and expected selected DEFLATE route. Cross all six None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored Adam7 pairs using fresh legal GrayAlpha16 sources; use the existing periodic source and route assertions to keep Dynamic and Fixed selections observable, and include Stored with both filters specifically to exercise the corrected Stored guard. For each row, accept a one-byte framing prefix through the public pull helper, mutate a checked U16 alpha component with `set_component_byte`, and inspect every byte of first and later sentinel leases. The first and later post-mutation pulls must each report zero writes, retain the accepted total, leave their leases unchanged, and expose the same structured terminal error for that encoder. Do not create a format-specific machine, bypass the public pull path, or move Phase 58's wider portability and hostile-schedule matrix into this task. Per D-04, retain descriptor-level Big-endian rejection and all non-interlaced GrayAlpha16 factory behavior without extending either into a new Adam7 branch.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 replay mutations are sticky for every strategy pair'</automated>
  </verify>
  <done>Every legal GrayAlpha16 Adam7 None/Adaptive and Stored/FixedOrStored/DynamicOrFixedOrStored choice validates U16 source revision before a caller-visible write, keeps accepted totals stable at failure, and repeats its same zero-write terminal result on later leases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller capability, limits, and budget to encoder construction | Untrusted resource inputs decide whether the shared preflight may create observable eager or chunk state. |
| Checked mutable U16 source to caller-owned lease | A source mutation after planning must be detected before `pull` writes into caller memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-05 | Denial of Service | GrayAlpha16 Adam7 shared preflight ledger | high | mitigate | Task 1 crosses capability, geometry, output, work, and budget envelopes through every public strategy pair and checks unchanged resource state. |
| T-57-06 | Tampering | U16 Stored/Fixed/Dynamic replay revision guard | high | mitigate | Task 2 corrects the Stored guard branch, then mutates a checked alpha lane after accepted framing for every Adam7 pair and requires failure before any lease byte is written. |
| T-57-07 | Tampering | caller-owned output lease | high | mitigate | Tasks 1-2 inspect every sentinel byte after rejected construction and both terminal replay pulls. |
| T-57-08 | Repudiation | eager/chunk terminal equivalence | medium | mitigate | Task 1 compares structured eager/chunk errors; Task 2 compares the original and sticky replay errors with accepted totals. |
</threat_model>

<verification>
- Run each named TDD regression after RED and GREEN.
- Run `moon -C modules/mb-image test png --target native --frozen` and `git diff --check` after Task 2.
- Do not substitute Phase 58's portable public evidence matrix for these package-level bounded-machine checks.
</verification>

<success_criteria>
- GRAYA16A7-02's resource failures are atomic across all legal GrayAlpha16 Adam7 strategy pairs, before writer bytes or caller-buffered lease visibility.
- Every legal Stored/FixedOrStored/DynamicOrFixedOrStored and None/Adaptive replay detects checked U16 drift before a lease write and preserves sticky terminal accounting.
- The phase remains inside the shared PNG machine; strict little-endian admission and frozen non-interlaced routes remain intact.
</success_criteria>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Atomic bounded construction and acknowledgement-safe caller-buffered replay | 57-02 | COVERED | Task 1 proves constructor atomicity; Task 2 proves replay failure before lease writes.
REQ | GRAYA16A7-02 | Atomic incompatible/resource failures and acknowledgement-safe replay path | 57-02 | COVERED | Both tasks invoke the public factories and common machine path.
RESEARCH | — | No Phase 57 RESEARCH.md: discovery intentionally skipped because 57-PATTERNS.md maps exact Phase 42/54 composition seams | 57-01, 57-02 | COVERED | Plan uses the cited atomicity and replay helpers rather than choosing new technology.
CONTEXT | D-02 | Preflight failure before eager output or caller lease exposure | 57-02 | COVERED | Task 1 cites D-02 and asserts writer, budget, and sentinel state.
CONTEXT | D-03 | Source validation before post-mutation lease write, accepted-only progress, sticky terminals for every supported Adam7 strategy choice | 57-02 | COVERED | Task 2 cites D-03, corrects the Stored guard, and table-tests first/later lease and terminal state across all six pairs.
CONTEXT | D-04 | Preserve little-endian-only admission and non-interlaced compatibility | 57-01, 57-02 | COVERED | Task 2 retains the locked boundary while Phase 57 adds no alternate route.

<output>
Create `.planning/phases/57-bounded-adam7-streaming-semantics/57-02-SUMMARY.md` when done.
</output>
