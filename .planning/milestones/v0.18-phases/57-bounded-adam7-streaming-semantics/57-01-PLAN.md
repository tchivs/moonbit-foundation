---
phase: 57-bounded-adam7-streaming-semantics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [GRAYA16A7-02]
must_haves:
  truths:
    - "A legal little-endian GrayAlpha16 Adam7 image uses None or Adaptive filtering with Stored, FixedOrStored, and DynamicOrFixedOrStored through the existing single bounded profile-aware machine."
    - "Adam7 traversal keeps predictor history local to each nonempty pass, and the six legal selector pairs produce caller-buffered bytes equal to their matching eager output."
    - "Strict Big-endian descriptor rejection and explicit non-interlaced GrayAlpha16 behavior remain unchanged."
  artifacts:
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: "GrayAlpha16 Adam7 pass-local predictor and exact-work composition regressions."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Eager all-strategy GrayAlpha16 Adam7 bounded-route regression coverage."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Focused all-strategy eager/chunk parity and accepted-only progress checks."
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/structural.mbt
      via: "The profile-aware Adam7 cursor obtains pass geometry solely from _png_adam7_passes and reads each U16 lane through _png_wire_byte."
      pattern: "_png_adam7_passes"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "PngEncodeMachine preflights and replays each strategy through the same PngFilteredMatchCursor selected by profile, filter, and interlace."
      pattern: "PngFilteredMatchCursor::new_with_interlace"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Public GrayAlpha16 Adam7 factories drain through PngChunkEncoder pull progress and compare their accumulated bytes with eager output."
      pattern: "new_graya16_with_all_strategies"
---

<objective>
Prove the first complete legal GrayAlpha16 Adam7 bounded path, then expand it to every required filter/compression selection without creating a format-specific machine.

Purpose: Deliver the shared traversal, filtering, compression, and eager/chunk parity portion of GRAYA16A7-02 before the failure and replay matrix in Plan 57-02. Locked-decision coverage for the plan set: D-01: one profile-aware bounded route with pass-local history; D-02: atomic preflight before output or lease; D-03: pre-write U16 replay validation with sticky terminals; D-04: preserve little-endian-only admission and frozen non-interlaced routes.
Output: Focused red-green regressions over the existing profile-aware machine; production sources change only if those regressions expose a real composition defect.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-CONTEXT.md
@.planning/phases/57-bounded-adam7-streaming-semantics/57-PATTERNS.md
@.planning/phases/56-grayalpha16-adam7-factory-and-pass-profile/56-VERIFICATION.md
@.planning/milestones/v0.13-phases/42-bounded-adam7-pass-encoding/42-02-SUMMARY.md
@.planning/milestones/v0.17-phases/54-bounded-type-4-16-encoder/54-02-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_wbtest.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
From modules/mb-image/png/encode.mbt:
- `_png_encode_preflight_with_interlace_profile(source, profile, strategy, filter_strategy, interlace_strategy, limits, budget)` is the private, one-charge construction gate.
- `PngFilteredCursor::new_with_interlace(source, row_bytes, channels, filter_strategy, interlace_strategy, profile=...)` is the scalar logical source used by `PngFilteredMatchCursor`; its Adam7 row is local to one `PngAdam7Pass`.
- `_png_adam7_passes(...)` is the sole pass-geometry authority; `_png_wire_byte(...)` preserves legal GrayAlpha16 `Ghi,Glo,Ahi,Alo` lane order.

From modules/mb-image/png/stream_encode.mbt:
- `PngEncoder::new_graya16_with_all_strategies(strategy, filter, Adam7)` and `PngChunkEncoder::new_graya16_with_all_strategies(source, strategy, filter, Adam7, limits, budget, diagnostics)` select the public eager and chunk paths.
- `PngEncodeMachine::present` retains a preview and `acknowledge` is the only state-commit point; `PngChunkEncoder::pull` reports per-call and cumulative accepted bytes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one Adaptive Fixed GrayAlpha16 Adam7 eager-to-chunk bounded route</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - A legal packed little-endian 5x5 GrayAlpha16 source encoded with Adaptive plus FixedOrStored plus Adam7 has a matching eager oracle and caller-buffered output.
    - A zero-capacity pull reports zero accepted bytes and NeedOutput; subsequent one-byte and `[1, 3, 2, 5]` pulls advance only by the accepted prefix and finish at the eager byte total.
    - The focused route uses the current `PngEncodeMachine` and profile-aware Adam7 cursor rather than a second construction, traversal, or output path.
  </behavior>
  <action>Per D-01: begin RED by adding the smallest public tracer beside `PNG GrayAlpha16 Adam7 chunk parity`: construct fresh legal 5x5 sources through the all-strategy GrayAlpha16 Adam7 eager and chunk factories with `FixedOrStored` and `Adaptive`, then compare the complete caller-buffered drain with the matching eager bytes. Use one empty lease followed by one-byte and the established short ragged capacities, assert each pull's cumulative total equals the prior total plus its reported writes, and assert a post-finish sentinel lease remains untouched. Exercise real `PngChunkEncoder::pull` ownership rather than any helper that bypasses it. If the red test identifies a genuine shared composition defect, amend only the smallest existing seam in `encode.mbt` or `stream_encode.mbt` so the existing profile-aware preflight, scalar `PngFilteredMatchCursor`, and present/acknowledge protocol carry this selection; retain `_png_adam7_passes` and the bounded matcher as the only traversal state. If the tracer is green without production changes, keep the task test-only.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 bounded tracer'</automated>
  </verify>
  <done>The Adaptive Fixed GrayAlpha16 Adam7 tracer is red-green tested end-to-end, drains identically to eager output with accepted-only progress, and uses the shared bounded machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the shared GrayAlpha16 Adam7 strategy and pass-history matrix</name>
  <files>modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Each of Stored, FixedOrStored, and DynamicOrFixedOrStored accepts legal GrayAlpha16 Adam7 inputs with both None and Adaptive filtering through the same profile-aware preflight and machine route.
    - For every nonempty pass, Adaptive resolves filters from pass-local rows; its first row never has a predecessor from the prior Adam7 pass, and exact selected work admits while one-less work leaves the budget untouched.
    - Fresh eager and caller-buffered instances for all six selections produce identical complete bytes under the compact zero/one/ragged drain pattern.
    - The existing Big-endian descriptor rejection and non-interlaced GrayAlpha16 factory regressions still pass unchanged.
  </behavior>
  <action>Per D-01: add the smallest GrayAlpha16 composition cases to the Phase-42 white-box cursor/preflight tests. Build one legal packed little-endian fixture with distinct U16 gray and alpha lanes; create the profile-aware Adam7 cursor and preflight through `PngEncodeProfile::GrayAlpha16`. Cross the three compression strategies with None and Adaptive, assert the selected exact-work budget succeeds while one less fails atomically, and inspect each nonempty pass boundary so Adaptive cannot carry an Up, Average, or Paeth predecessor from a previous pass. Extend the Phase-56 eager and chunk helpers rather than duplicating encoders: run fresh sources for all six public selections, compare complete chunk bytes to the selected eager oracle, and retain the compact empty/one/ragged ownership checks from the tracer. Per D-04: preserve the locked descriptor and factory boundary by running the existing Big-endian rejection and non-interlaced GrayAlpha16 tests as compatibility checks; do not create a Big-endian admission variant or alter an existing None factory. Keep public portability-wide vectors and broader hostile capacity schedules assigned to Phase 58.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>All legal None/Adaptive × Stored/FixedOrStored/DynamicOrFixedOrStored GrayAlpha16 Adam7 choices have shared-route, pass-local-history, exact-work, eager/chunk-parity, and retained-compatibility regression evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image and strategy selection to profile-aware preflight | Caller-controlled packed storage, filter, compression, interlace, limits, and budget become bounded traversal and plan facts. |
| Encoder machine to caller-owned output lease | A previewed PNG byte becomes caller-visible only after the caller lease accepts it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01 | Denial of Service | Adam7 profile-aware preflight and planner | high | mitigate | Tasks 1-2 exercise all six selections through one checked preflight and scalar cursor, including exact/one-less work evidence. |
| T-57-02 | Tampering | Adam7 Adaptive predictor state | high | mitigate | Task 2 asserts pass-local first-row behavior over the four-byte GrayAlpha16 wire profile. |
| T-57-03 | Tampering | PngChunkEncoder accepted-byte accounting | high | mitigate | Tasks 1-2 use zero, one-byte, and ragged caller leases, then compare accumulated bytes and post-terminal lease contents. |
| T-57-04 | Elevation of Privilege | GrayAlpha16 descriptor/factory boundary | medium | mitigate | Task 2 retains descriptor-level Big-endian rejection and non-interlaced factory compatibility checks per D-04. |
</threat_model>

<verification>
- Run the named tracer with `moon -C modules/mb-image test png --target native --frozen -f 'PNG GrayAlpha16 Adam7 bounded tracer'` during RED and GREEN.
- Run `moon -C modules/mb-image test png --target native --frozen` after Task 2 and `git diff --check` before completing the plan.
- Keep broad public four-target and hostile-schedule qualification for Phase 58; this plan's native package suite is regression verification for the bounded composition seam.
</verification>

<success_criteria>
- GRAYA16A7-02's filter/compression portion is observable across every legal strategy pair without a format-specific encoder route or staged raster output.
- Adam7 Adaptive pass history is local to each canonical pass, and each tested caller-buffered selection equals its eager peer with accepted-only accounting.
- Strict little-endian admission and frozen non-interlaced GrayAlpha16 behavior remain regression-protected.
</success_criteria>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Shared bounded filtering, compression, and eager/chunk semantics for GrayAlpha16 Adam7 | 57-01 | COVERED | Tracer proves one end-to-end machine path; Task 2 expands it to all six legal selections.
REQ | GRAYA16A7-02 | Reuse the shared pass filtering and Stored/Fixed/Dynamic planning path | 57-01 | COVERED | Tasks 1-2 exercise the profile-aware cursor and preflight without a parallel machine.
RESEARCH | — | No Phase 57 RESEARCH.md: discovery intentionally skipped because 57-PATTERNS.md maps exact Phase 42 and 54 composition seams | 57-01 | COVERED | Pattern-map anchors are embedded in context and task actions.
CONTEXT | D-01 | One profile-aware bounded route with pass-local predictor history | 57-01 | COVERED | Tasks 1-2 cite and assert D-01.
CONTEXT | D-04 | Little-endian-only admission and frozen non-interlaced compatibility | 57-01 | COVERED | Task 2 cites D-04 and runs the retained boundary regressions.

<output>
Create `.planning/phases/57-bounded-adam7-streaming-semantics/57-01-SUMMARY.md` when done.
</output>
