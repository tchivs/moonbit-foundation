---
name: extend-portable-png-bilinear-workflow
quick_id: 260721-j94
created: 2026-07-21
phase: quick-260721-j94
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
autonomous: true
requirements: [PNG-07]
files_modified:
  - examples/png-portable/main/main.mbt
  - scripts/quality/Invoke-MoonQuality.ps1
must_haves:
  truths:
    - The portable public PNG program decodes its fixed valid 2×1 RGB input, applies alpha-correct `@ops.resize_bilinear` to 3×1, encodes the result, and proves exact canonical output bytes plus a target-neutral digest.
    - The program prints the same one-line 75-byte-read, 78-byte-written, 3×1 bilinear evidence on js, wasm, wasm-gc, and native.
    - The isolated PNG quality lane executes and verifies that public workflow on all four portable targets without invoking QOI, release, or host-dependent work.
  artifacts:
    - path: examples/png-portable/main/main.mbt
      provides: Public portable PNG decode → bilinear resize → encode example with exact 78-byte output assertions.
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: PNG-lane four-target public-workflow evidence with a frozen expected semantic line.
  key_links:
    - from: examples/png-portable/main/main.mbt
      to: modules/mb-image/ops/resize.mbt
      via: The public example calls `@ops.resize_bilinear` after `PngDecoder` and before `PngEncoder`.
      pattern: resize_bilinear
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: examples/png-portable/main/main.mbt
      via: The PNG quality lane runs the executable independently on each required target and checks its evidence line.
      pattern: PNG portable bilinear public workflow
---

<objective>
Extend the portable public PNG workflow from flip-only processing to deterministic, alpha-correct bilinear resize evidence.

Purpose: keep the Phase 22 public decode→operation→encode proof current with the new portable operation while preserving the fixed valid input and canonical PNG contract.
Output: exact resize output assertions and isolated four-target quality evidence.
</objective>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-canonical-png-encode-and-portable-evidence/22-01-SUMMARY.md
@.planning/quick/260721-i66-add-deterministic-alpha-correct-bilinear/260721-i66-SUMMARY.md
@examples/png-portable/main/main.mbt
@examples/png-portable/main/moon.pkg
@examples/png-portable/moon.mod.json
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<source_audit>
SOURCE  | ID       | Feature / constraint | Coverage | Status
GOAL    | Phase 22 | Public portable PNG decode → operation → encode evidence remains deterministic across all four targets. | Tasks 1-2 | COVERED
REQ     | PNG-07   | A public PNG workflow uses portable contracts and has four-target deterministic evidence. | Tasks 1-2 | COVERED
RESEARCH| i66      | `resize_bilinear` accepts builtin encoded-sRGB RGB8, uses linear-light interpolation, and requires four-tap output work. | Task 1 | COVERED
CONTEXT | j94      | Preserve the valid source and portable imports; use bilinear in the real workflow; exact bytes/digest, bounded limits, and quality evidence; exclude codec/PNG implementation, QOI, release, and user-QOI work. | Tasks 1-2 | COVERED
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the public PNG workflow prove a 3×1 bilinear resize</name>
  <files>examples/png-portable/main/main.mbt</files>
  <behavior>
    - The existing valid 75-byte, 2×1 RGB PNG source remains the decoder input and is completely consumed.
    - The decoded image is resized through public `@ops.resize_bilinear` to 3×1; its RGB pixels are exactly `[0x12,0x34,0x56]`, `[0x8f,0xad,0xcc]`, and `[0xab,0xcd,0xef]`, where the middle pixel proves linear-light interpolation rather than encoded-byte blending.
    - Canonical encoding produces exactly 78 bytes: `89504e470d0a1a0a0000000d4948445200000003000000010802000000948283e300000015494441547801010a00f5ff001234568fadccabcdef131b050c656426a80000000049454e44ae426082`; the rolling-257 digest is `626208771`.
    - The expected canonical-output `OwnedBytes::from_bytes` uses a distinct exact 78-byte budget with one allocation and allocation-size 78, while the preserved source buffer retains its exact 75-byte allocation budget.
    - The only semantic output line is `example=png-portable bytes_read=75 bytes_written=78 width=3 height=1 resize_bilinear digest=626208771` on each portable target.
  </behavior>
  <action>Replace the example’s flip-only operation with the actual public decode → `@ops.resize_bilinear` → `PngEncoder` path. Keep the source PNG literal, `MemoryReader`/`MemoryWriter`, package imports, and all portable target declarations unchanged. Keep the source `OwnedBytes::from_bytes` allocation budget at exactly 75 bytes, then construct the stated 78-byte expected PNG in a separate `OwnedBytes::from_bytes` call with `bytes=78UL`, `allocations=1UL`, and `allocation_size=78UL`; do not reuse the source-buffer budget for expected output. Use target geometry `3UL × 1UL`; update the shared codec limits only as needed to permit width/pixels 3 while retaining `max_input_bytes=75UL`, a finite output allowance, and the existing one-row bound. Give resize exactly the tight 9-byte, one-allocation, width-3, height-1, pixel-3, work-12 budget; retain bounded writer capacity and give encoding exact 3×1/78-byte output limits. Replace the expected literal with the stated 78-byte canonical PNG, compare only the completed output length, assert the three resized RGB pixels before encoding, assert `bytes_read=75UL` and `bytes_written=78UL`, then assert exact bytes and digest `626208771`. Emit exactly the stated evidence line. Do not retain a flip merely as a second transform, alter the valid source bytes, add imports, change `PngDecoder`/`PngEncoder` implementation, or touch QOI/release files.</action>
  <verify>
    <automated>$targets = @('js','wasm','wasm-gc','native'); foreach ($target in $targets) { $out = @(moon -C examples/png-portable run main --target $target --frozen 2>&1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if (@($out | Where-Object { $_ -eq 'example=png-portable bytes_read=75 bytes_written=78 width=3 height=1 resize_bilinear digest=626208771' }).Count -ne 1) { throw "PNG public evidence mismatch on $target" } }</automated>
  </verify>
  <done>The public example uses only portable public packages, keeps its fixed valid source, proves the exact nontrivial linear-light 3×1 bilinear result and its canonical 78-byte encoding, and emits the frozen evidence line on all four targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the frozen public workflow to the isolated PNG quality lane</name>
  <files>scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png` runs the public PNG example separately on js, wasm, wasm-gc, and native.
    - For each target, quality accepts exactly one line equal to `example=png-portable bytes_read=75 bytes_written=78 width=3 height=1 resize_bilinear digest=626208771` and fails on a missing, duplicate, or different evidence line.
    - The PNG lane trace contains the named portable-workflow stage while preserving its existing PNG-only stages and isolation guarantees.
  </behavior>
  <action>Add a `PNG portable bilinear public workflow` stage to `Invoke-PngQualityLane` after the existing generated-vector freshness stages and before the package PNG test stage. For each of `js`, `wasm`, `wasm-gc`, and `native`, invoke `moon -C examples/png-portable run main --target <target> --frozen` through the existing combined-output helper, select only lines beginning with the example evidence prefix so compiler warnings cannot mask a result, and require exactly one equal to the frozen Task 1 line. Update `Assert-PngLaneIsolation`’s expected trace to include this stage in the same position. Keep every existing quality stage and exact PNG package allowlist intact; do not call Required/Qoi/release lanes, alter QOI expected output, or add host, registry, CI, or release behavior.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The isolated PNG quality command independently runs and verifies the same frozen public bilinear workflow output on js, wasm, wasm-gc, and native, with the stage trace proving it remains in the PNG-only lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixed PNG bytes → public decoder | The example must continue to use a complete supported input within its declared codec limits. |
| decoded image → bilinear operation | Geometry, metadata, and resource budget cross into an allocating four-tap operation. |
| encoded bytes → quality assertion | Cross-target runtime output is accepted only when its exact deterministic evidence line is present once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260721-j94-01 | Denial of service | Example resize and codec limits | medium | mitigate | Keep source/input bounds fixed; set finite 3×1 geometry, 9-byte allocation, 12-work resize, and 78-byte encoded-output limits. |
| T-260721-j94-02 | Tampering | Public pixel and canonical-output evidence | high | mitigate | Assert all resized pixels, exact completed PNG bytes, and rolling digest in the executable before it prints evidence. |
| T-260721-j94-03 | Information integrity | Four-target quality result | medium | mitigate | Run each backend independently and require exactly one frozen semantic line per backend; retain PNG-lane isolation trace checks. |
</threat_model>

<verification>
1. Run `moon -C examples/png-portable run main --target js --frozen`.
2. Run `moon -C examples/png-portable run main --target wasm --frozen`.
3. Run `moon -C examples/png-portable run main --target wasm-gc --frozen`.
4. Run `moon -C examples/png-portable run main --target native --frozen`.
5. Run `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
</verification>

<success_criteria>
- The true public PNG workflow decodes the preserved 2×1 input, performs public alpha-correct bilinear resize to 3×1, and encodes the pinned 78-byte result with its exact digest.
- All required portable targets produce the same frozen evidence, and the PNG quality lane makes that evidence a blocking assertion.
- No PNG codec implementation, QOI/user-QOI file, release script, dependency, host capability, or source input validity changes.
</success_criteria>

<output>
Create `.planning/quick/260721-j94-extend-the-portable-png-public-workflow-/260721-j94-SUMMARY.md` when done.
</output>
