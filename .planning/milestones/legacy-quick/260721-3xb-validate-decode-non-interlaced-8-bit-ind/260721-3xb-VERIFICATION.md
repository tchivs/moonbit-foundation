---
quick_id: 260721-3xb
verified: 2026-07-21T00:00:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
commit_verified: 9c8bbe6c6450a89d30f296ffd2b39e72ac0c3bb7
---

# Quick Task 260721-3xb: Indexed PNG Decode Verification Report

**Objective:** Decode the bounded non-interlaced, 8-bit indexed PNG subset through `PngDecoder` as RGB8, with one valid pre-IDAT PLTE, index-lane filtering, bounded resource use, and unchanged existing PNG semantics.

**Verified:** 2026-07-21 UTC
**Status:** passed
**Mode:** initial verification (no prior `VERIFICATION.md`)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A non-interlaced 8-bit indexed PNG with one valid PLTE and no tRNS decodes as RGB8 by mapping reconstructed indexes through the palette. | VERIFIED | `_png_read_stream_transport` accepts type 3 as one source channel / three output channels, captures a CRC-valid PLTE, and returns its bytes and entry count in `PngStreamTransport`. The public generated corpus includes the 26 nonempty two-IDAT variants of `indexed-filters-every-idat-boundary`; the decoder test byte-compares every RGB output on wasm, wasm-gc, js, and native. |
| 2 | Indexed filters use one-byte index neighbours rather than RGB output bytes, across every nonempty contiguous two-IDAT split. | VERIFIED | `PngIndexRows::reconstruct` reads only `current`/`previous` index rows; `_png_inflate_zlib_to_raster` calls it before `_png_write_indexed_pixel`. The generator expands split offsets 1 through 25 for the 26-byte zlib stream, confirms its independent bpp-1 oracle, and emitted vectors all pass on four targets. |
| 3 | Missing, duplicate, misplaced, malformed, CRC-invalid PLTE chunks and indexes outside the actual palette reject deterministically without a completed result. | VERIFIED | `structural.mbt` requires PLTE before IDAT, enforces `3 <= length <= 768` and divisibility by 3, and maps duplicate/post-IDAT PLTE to `png-plte-order`; shared payload reading validates CRC. `_png_write_indexed_pixel` rejects `entry >= palette_entries` before any output write. The generated public corpus exercises missing PLTE, all six invalid lengths, duplicate/post-IDAT PLTE, bad PLTE CRC, one-past-last index, and type-3 tRNS. |
| 4 | Type 0, type 2, and type 6 decoded pixels and semantic policy remain unchanged; tRNS and colour-management chunks remain rejected. | VERIFIED | The non-indexed inflater branch remains the existing grayscale/RGB/RGBA path. `_png_is_known_semantic` still includes `tRNS`, `cHRM`, `gAMA`, `iCCP`, `sRGB`, `cICP`, `mDCv`, `cLLi`, and animation semantic chunks; the structural corpus retains their rejection vectors. Existing grayscale, RGB, and RGBA generated bytes pass in the same four-target run. |
| 5 | Indexed preflight charges RGB storage plus two index rows through one budget child before image exposure, and the PNG suite works on all four targets. | VERIFIED | `_png_indexed_decode_budget` creates a non-expanding child with `image_bytes + 2 * width`, three allocations, and image-size cap. `png.mbt` consumes that same child for both index-row allocations and `OwnedImage::new_operation`; `Budget::child` validates all limits before allocation and `Budget::charge` preflights all windows before committing. Four-target tests and the isolated PNG quality lane passed. |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified).

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `modules/mb-image/png/structural.mbt` | Type-3 framing, PLTE validation, transport and budget child | VERIFIED | Substantive streaming transport with type-3 predicate, PLTE storage, CRC checks, ordering checks, and index-row-inclusive child budget. |
| `modules/mb-image/png/deflate_inflate.mbt` | Streaming index reconstruction and palette mapping | VERIFIED | Its indexed branch obtains `PngIndexRows`, reconstructs bpp-1 values, bounds-checks palette indexes through the write helper, then writes RGB8. |
| `fixtures/png/decode-cases.json` | Indexed acceptance and rejection corpus | VERIFIED | Contains all required indexed IDs, including exhaustive two-IDAT split expansion source and named negative cases. |
| `scripts/fixtures/Generate-PngDecodeVectors.ps1` | Independent indexed oracle and vector generation | VERIFIED | Inflates fixture zlib through .NET, reconstructs bpp-1 scanlines independently, maps PLTE triples, verifies expected RGB, and checks generated output plus manifest digest. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `_png_read_stream_transport` | `_png_inflate_zlib_to_raster` | `PngStreamTransport.palette` and `palette_entries` | WIRED | `png.mbt` forwards the whole stream transport; inflater passes both fields into `_png_write_indexed_pixel`. |
| Indexed budget child | index rows and RGB image | `stream.budget` | WIRED | The sole indexed child is returned in transport, consumed twice by `_png_new_index_rows`, then by `OwnedImage::new_operation`. |
| Decode JSON | generated MoonBit vectors and public test | generator `-Check` output | WIRED | The generator check validates the checked-in table and digest; `png_test.mbt` executes each generated vector through the public decoder. |

### Data-Flow Trace

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Stream transport | `palette`, `palette_entries` | CRC-authenticated pre-IDAT PLTE payload | Exact PLTE bytes and `length / 3` entries | FLOWING |
| Streaming inflater | reconstructed `index` | DEFLATE bytes plus two retained index rows | One-byte filtered index values | FLOWING |
| RGB output | palette triple at `index * 3` | Bounds-checked PLTE lookup | Byte-compared expected RGB vectors | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Decode corpus/oracle and manifest integrity | `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check` | 68 executable cases | PASS |
| Structural corpus and manifest integrity | `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check` | 89 P+W cases | PASS |
| Public decoder, filters, PLTE negatives, and target portability | `moon -C modules/mb-image test png --target all --frozen` | 22/22 on wasm, wasm-gc, js, and native | PASS |
| PNG scoped quality contract | `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png` | Policy, negatives, generators, four-target tests, and isolation proof passed | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- |
| PNGX-01 | `260721-3xb-PLAN.md` | Decode palette, grayscale, transparency, and 16-bit PNG profiles with explicit image-model mapping. | PARTIAL — intentional scoped slice | This task verifies only opaque non-interlaced 8-bit indexed PNG to RGB8. Palette alpha/tRNS mapping, low bit depths, 16-bit, Adam7, and colour management remain explicitly outside its scope, so PNGX-01 is not marked complete. |

### Anti-Patterns and Scope

| Check | Status | Evidence |
| --- | --- | --- |
| Stubs / debt markers | PASS | No TODO, FIXME, XXX, placeholder, empty implementation, or console-only pattern in changed task files. |
| Scope boundary | PASS | The five supplied commits are all ancestors of `9c8bbe6`; their changed paths are limited to PNG sources/tests, fixtures, generated vectors, and generator script. No QOI, release, registry, or public-contract path appears. |
| Generated artifacts | PASS | Both generators accept the current artifacts unchanged and validate the manifest SHA-256 records. |

## Conclusion

The bounded indexed-PNG quick-task objective is achieved. The PLTE-to-RGB8 data path is real and public-test exercised, filters reconstruct index lanes before palette lookup, malformed/semantic inputs reject deterministically, the shared child budget covers the image and both private row allocations, and checked corpus/quality evidence passes across all four supported targets.

---

_Verified: 2026-07-21 UTC_
_Verifier: gsd-verifier_
