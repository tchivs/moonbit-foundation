---
name: add-deterministic-alpha-correct-bilinear
quick_id: 260721-i66
created: 2026-07-21
phase: quick-260721-i66
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
autonomous: true
requirements: [PNGCM-04]
files_modified:
  - modules/mb-image/ops/resize.mbt
  - modules/mb-image/ops/processing.mbt
  - modules/mb-image/ops/resize_convert_test.mbt
  - modules/mb-image/ops/resize_convert_wbtest.mbt
  - modules/mb-image/README.mbt.md
must_haves:
  truths:
    - `@ops.resize_bilinear` produces deterministic, linear-light output for packed U8 encoded-sRGB RGB8 and straight-RGBA8 images, while `resize_nearest` retains its existing output and failure contract; byte identity is retained for RGB8 and opaque straight RGBA8, whereas transparent/zero-alpha RGBA canonicalizes stored RGB to black after filtering.
    - A transparent saturated source sample cannot create a coloured fringe because straight RGBA interpolation occurs in linear premultiplied form and is unpremultiplied only at output.
    - Every rejected bilinear request, including an empty source, incompatible metadata, checked arithmetic overflow, and a resource limit, leaves the caller's complete Budget remaining snapshot unchanged.
    - Exact public and white-box vectors pass unchanged on js, wasm, wasm-gc, and native.
  artifacts:
    - path: modules/mb-image/ops/resize.mbt
      provides: Public checked `resize_bilinear` with nearest-compatible coordinate mapping and one authoritative operation allocation.
    - path: modules/mb-image/ops/processing.mbt
      provides: Package-visible, shared linear-premultiplied pixel helpers plus an opaque RGB8 adapter that use the established transfer and quantization rules.
    - path: modules/mb-image/ops/resize_convert_test.mbt
      provides: Black-box exact RGB8 and straight-RGBA8 bilinear vectors through the exported ops package.
    - path: modules/mb-image/ops/resize_convert_wbtest.mbt
      provides: White-box coordinate, capability, overflow, resource, and whole-Budget-atomicity evidence.
    - path: modules/mb-image/README.mbt.md
      provides: Executable public contract documentation for supported formats, coordinate semantics, alpha correctness, bounds, and portable verification.
  key_links:
    - from: modules/mb-image/ops/resize.mbt
      to: modules/mb-image/ops/processing.mbt
      via: Bilinear taps load and store through the one shared linear-premultiplied conversion/quantization path rather than interpolating encoded bytes.
      pattern: resize_bilinear|load_linear_premultiplied|store_linear_premultiplied
    - from: modules/mb-image/ops/resize.mbt
      to: modules/mb-image/storage/owned_image.mbt
      via: Fully checked output descriptor and four-tap work reach `OwnedImage::new_operation` once, so the authoritative budget charge is atomic.
      pattern: resized_descriptor|checked_mul|new_operation
    - from: modules/mb-image/ops/resize_convert_wbtest.mbt
      to: modules/mb-image/ops/resize.mbt
      via: Exact coordinate/tap and rejection vectors exercise the public operation and preserve the independent nearest baseline.
      pattern: resize_bilinear|resize_nearest|remaining_is_unchanged
---

<objective>
Add deterministic, alpha-correct bilinear resize for portable RGB8 and straight-RGBA8 images without altering the established nearest-neighbour API or contract.

Purpose: enable quality resizing that retains truthful sRGB/alpha semantics, checked resource limits, and deterministic four-target behavior.
Output: one public `resize_bilinear` operation, shared conversion helpers, executable exact-vector coverage, and literate public documentation.

Scope exclusions: no FFI, codec/PNG/QOI changes, release scripts, or user QOI edits.
</objective>

## Scope coverage audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Deterministic alpha-correct bilinear resize for portable RGB8 and straight-RGBA8 | Task 1 |
| GOAL | Preserve `resize_nearest`; carry checked dimensions, limits, atomic budgets, and all-four-target evidence | Task 1 |
| REQ | PNGCM-04: fail closed when an operation would make unsupported/non-sRGB colour semantics unreliable | Task 1 |
| RESEARCH | Nearest-compatible integer coordinate convention, checked tap-index products, output work of `pixels * 4`, one `new_operation` allocation | Task 1 |
| RESEARCH | Linear-premultiplied load/interpolate/unpremultiply/store and exact black/white plus transparent-red vectors | Task 1 |
| CONTEXT | Public API, RGB8 + straight-RGBA8-only support, checked limits/budgets, deterministic vectors, four targets, and stated exclusions | Tasks 1-2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the checked alpha-correct bilinear operation with exact vectors</name>
  <files>modules/mb-image/ops/resize.mbt, modules/mb-image/ops/processing.mbt, modules/mb-image/ops/resize_convert_test.mbt, modules/mb-image/ops/resize_convert_wbtest.mbt</files>
  <behavior>
    - Public RGB8 input `[0,0,0, 255,255,255]` resized from `2×1` to `3×1` has exact channel bytes `[0, 213, 255]`; the middle result proves linear-light interpolation rather than encoded-byte averaging.
    - Public straight-RGBA8 input `[255,0,0,0, 0,0,0,255]` resized from `2×1` to `3×1` has exact middle pixel `[0,0,0,170]`, proving transparent red does not create a halo; a `1×1` transparent-red source expanded to `2×3` has the exact six-pixel output `[0,0,0,0]` at every position.
    - RGB8 produces RGB8 output with no alpha metadata; straight-RGBA8 produces straight-RGBA8 output; an opaque RGBA input has the same RGB values as equivalent RGB8 input; and only RGB8 or fully opaque straight-RGBA8 `1×1` input remains byte-identical at every destination extent.
    - The coordinate helper keeps nearest's base convention: `2×1 → 1×1` reads source x=0, `2×1 → 3×1` uses base/remainder pairs `(0,0)`, `(0,2)`, and `(1,1)` over denominator 3, with the upper edge clamped to the last source sample.
    - An empty source returns `InvalidDimensions` with operation `image-resize-bilinear` and context `empty-operation-source` before descriptor construction, allocation, or charge; zero destination axes, U16/gray/premultiplied-RGBA layouts, non-encoded-sRGB or non-builtin-sRGB metadata, below-limit output bytes/work, and checked geometry/tap products likewise preserve every `Budget.remaining()` field.
  </behavior>
  <action>Write the named public and white-box assertions before implementing the operation. In `resize.mbt`, add `pub fn resize_bilinear(source, width, height, budget)` beside `resize_nearest`; retain `resize_nearest`, `nearest_source_index`, and its diagnostics unchanged. Before format/metadata gates, descriptor construction, dimension/tap arithmetic, allocation, or any budget charge, reject `source.is_empty()` with `InvalidDimensions`, operation `image-resize-bilinear`, and context `empty-operation-source`. Add one private bilinear coordinate helper that validates nonzero axes, uses checked destination-index × source-extent products, returns low/high indices plus the checked remainder over the destination extent, and clamps only the high neighbour. Gate bilinear before descriptor creation to packed U8 encoded-sRGB RGB8 with no alpha or packed U8 encoded-sRGB straight-RGBA8, and additionally require `source.metadata().profile().is_builtin_srgb()`; use `image-resize-bilinear-format` for format/capability rejection and `image-resize-bilinear-metadata` for non-builtin/non-sRGB metadata rejection. Reuse `resized_descriptor` after making its error operation argument explicit so nearest retains `image-resize-nearest` and bilinear receives `image-resize-bilinear`; reject zero axes with the bilinear operation and `zero-destination-axis` context. Check `(width - 1) * source.width()` and `(height - 1) * source.height()` with `@checked` before allocation, calculate work as checked output pixels × 4, and allocate once with `OwnedImage::new_operation`; do not pre-charge or mutate the Budget elsewhere.

  In `processing.mbt`, intentionally expose package-visible shared helpers instead of copying transfer/quantization logic. Preserve the established straight-RGBA8 load/store sequence: dequantize encoded sRGB and alpha, decode sRGB, premultiply; after filtering, unpremultiply, encode sRGB, and use the current ties-to-even quantizers. Treat zero-alpha straight-RGBA output as canonical transparent black, because unpremultiplication has no defined retained straight RGB at alpha zero; this means transparent source RGB is not byte-identical after output storage. Add the RGB8 opaque counterpart that decodes/stores only RGB channels with alpha 1.0. In the resize loop, load the four source samples into linear premultiplied RGBA, interpolate each premultiplied RGB component and alpha with the two checked-axis fractions, then select the RGB or straight-RGBA store helper. Do not use floating-point for geometry/indexing, interpolate encoded channel bytes, interpolate straight RGB before premultiplication, route through conversion APIs, add fallback conversion, or accept premultiplied RGBA.

  Extend the existing public tests with the black-to-white and transparent-red exact byte vectors, metadata/format checks, opaque RGB/RGBA parity, RGB8/opaque-RGBA-only identity, the exact transparent-red `1×1 → 2×3` six-pixel canonical-transparent-black vector, and an unchanged `resize_nearest` regression. Extend white-box tests with the exact asymmetric coordinate/remainder cases, edge clamp, typed empty-source/zero-axis/format/metadata errors, U16/gray/premultiplied/non-sRGB/custom-profile failures, checked overflow, and each bytes/allocations/allocation-size/width/height/pixels/depth/work failure boundary. Reuse the existing full remaining-snapshot helper for every failure rather than testing a subset of budget fields.</action>
  <verify>
    <automated>$targets = @('js', 'wasm', 'wasm-gc', 'native'); foreach ($target in $targets) { moon test --target $target modules/mb-image/ops; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }</automated>
  </verify>
  <done>`resize_bilinear` is public, deterministic, and limited to truthful packed RGB8/straight-RGBA8 encoded-sRGB inputs; it bilinearly filters linear-premultiplied samples with nearest-compatible checked coordinates, stores zero-alpha RGBA as canonical transparent black, rejects empty sources before charge, charges four taps per output pixel through one allocation, preserves metadata/disposition consistently, and is proved by exact public/white-box vectors plus atomic failure coverage on every portable target.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the executable bilinear contract and target matrix</name>
  <files>modules/mb-image/README.mbt.md</files>
  <action>Update the existing operations documentation with a checked public `resize_bilinear` example using a fresh explicit Budget and the exported ops API. State precisely that it supports only packed U8 encoded-sRGB RGB8 without alpha and straight-RGBA8 with the builtin sRGB profile; it preserves the nearest integer base-coordinate convention, uses linear-light premultiplied-alpha interpolation, unpremultiplies only when storing straight RGBA8, stores zero-alpha RGBA as transparent black, and charges four source taps per output pixel. State that byte identity applies to RGB8 and opaque straight-RGBA8 only, then include executable observations for the `2×1 → 3×1` black-to-white middle byte 213, the transparent-red-to-opaque-black middle `[0,0,0,170]`, and the exact `1×1 → 2×3` transparent-red output of six `[0,0,0,0]` pixels. Say that empty source with `empty-operation-source`, zero axes, incompatible representations, non-sRGB metadata, checked overflow, and exhausted resources are typed, atomic failures. Keep nearest resize documented as a separate unchanged operation. Add the four exact portable commands for the ops suite: `moon test --target js modules/mb-image/ops`, `moon test --target wasm modules/mb-image/ops`, `moon test --target wasm-gc modules/mb-image/ops`, and `moon test --target native modules/mb-image/ops`. Do not document or add FFI, codec/PNG/QOI functionality, release workflows, performance claims, or user QOI changes.</action>
  <verify>
    <automated>$targets = @('js', 'wasm', 'wasm-gc', 'native'); foreach ($target in $targets) { moon -C modules/mb-image check README.mbt.md --target $target; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }</automated>
  </verify>
  <done>The literate public documentation executes on each portable target, names the exact alpha-correct bilinear semantics including zero-alpha canonical transparent black and limited identity, supported representations and empty-source/error boundary, deterministic proof vectors, four-tap work policy, and the separate unchanged nearest-neighbour contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller ImageView and metadata → bilinear operation | Caller controls pixel layout, declared colour/alpha semantics, dimensions, and a mutable resource budget. |
| destination geometry → output allocation | Checked dimensions and four-tap work determine the single charged output allocation. |
| source samples → linear interpolation → encoded output | Conversion and alpha representation cross from untrusted byte samples to a deterministic public image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260721-i66-01 | Denial of service | Bilinear geometry, tap products, and output allocation | high | mitigate | Check all descriptor and `(destination - 1) × source` products before the one authoritative allocation; charge checked `pixels × 4` work and test every resource boundary atomically. |
| T-260721-i66-02 | Tampering | Encoded-sRGB/alpha interpretation | high | mitigate | Fail closed for every unsupported format, alpha mode, transfer, space, or non-builtin profile before budget charge; admit only RGB8 and straight-RGBA8 with truthful builtin-sRGB metadata. |
| T-260721-i66-03 | Information integrity | Pixel interpolation and quantization | high | mitigate | Share existing linear-premultiplied load/store helpers, interpolate premultiplied linear values, and lock exact black/white, opaque parity, identity, edge, and transparent-red vectors. |
| T-260721-i66-04 | Tampering | Public documentation and cross-target evidence | medium | mitigate | Make the README literate and run both operations tests and README checks on js, wasm, wasm-gc, and native. |
</threat_model>

<verification>
1. `moon test --target js modules/mb-image/ops`
2. `moon test --target wasm modules/mb-image/ops`
3. `moon test --target wasm-gc modules/mb-image/ops`
4. `moon test --target native modules/mb-image/ops`
5. `moon -C modules/mb-image check README.mbt.md --target js`
6. `moon -C modules/mb-image check README.mbt.md --target wasm`
7. `moon -C modules/mb-image check README.mbt.md --target wasm-gc`
8. `moon -C modules/mb-image check README.mbt.md --target native`
</verification>

<success_criteria>
- The new public bilinear API is deterministic and alpha correct for exactly RGB8 and straight-RGBA8 builtin encoded-sRGB inputs, with exact fixed pixel vectors on all four targets; RGB8 and opaque RGBA retain identity while zero-alpha RGBA stores canonical black RGB.
- Existing nearest-neighbour behavior and the documented integer-base coordinate convention remain intact; bilinear adds only a checked high-neighbour/remainder interpolation layer.
- Empty source, incompatible capability/metadata, invalid destination dimensions, overflow, and every allocation/work boundary fail before a charge or partial output, retaining all budget fields.
- No FFI, codec/PNG/QOI behavior, release script, or user QOI file is changed.
</success_criteria>

<output>
Create `.planning/quick/260721-i66-add-deterministic-alpha-correct-bilinear/260721-i66-SUMMARY.md` when done.
</output>
