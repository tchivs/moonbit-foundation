---
name: implement-bounded-adam7-interlaced-png-decode
quick_id: 260721-8nz
created: 2026-07-21
phase: quick-260721-8nz
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
autonomous: true
requirements: [PNGX-01]
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/deflate_inflate.mbt
  - modules/mb-image/png/raster_decode.mbt
  - modules/mb-image/png/structural_wbtest.mbt
  - modules/mb-image/png/raster_decode_wbtest.mbt
  - modules/mb-image/png/png_test.mbt
  - fixtures/png/decode-cases.json
  - fixtures/manifest.json
  - scripts/fixtures/Generate-PngDecodeVectors.ps1
  - modules/mb-image/png/generated_decode_vectors_test.mbt
must_haves:
  truths:
    - A valid bounded Adam7 PNG decodes through PngDecoder to the same exact pixels, RGB8 or straight-RGBA8 descriptor, and output bytes as the equivalent supported non-interlaced PNG for opaque and valid-tRNS grayscale depths 1/2/4/8/16, opaque indexed depths 1/2/4/8 with PLTE and indexed tRNS variants, opaque and valid-tRNS truecolour depths 8/16, grayscale-alpha depths 8/16, and RGBA depths 8/16.
    - The decoder follows all seven Adam7 passes with their PNG offsets and strides, skips zero-width or zero-height passes without reading a filter byte, reconstructs each nonempty pass using pass-local rows, and scatters every reconstructed source pixel to its full-image coordinate.
    - Every pass filters encoded bytes rather than projected RGB8/RGBA8 pixels: low-bit grayscale and indexed data use packed pass rows with bpp 1, while 8-bit and 16-bit profiles use their declared encoded source-byte bpp, including 2/6/4/8 for depth-16 types 0/2/4/6.
    - Adam7 preflight, zlib consumption, and reusable row allocations are checked and bounded: work uses the exact sum of filtered nonempty pass rows; allocation uses the completed image plus no more than two reusable maximum-pass source rows; malformed, short, surplus, invalid-filter, or over-limit input exposes no DecodeResult image.
    - PLTE/tRNS semantics remain complete raw PNG semantics under interlace: indexed rows validate every reconstructed index against the full palette and table alpha, and grayscale/truecolour depth-16 tRNS compares all raw sample bytes before RGB8/RGBA8 high-byte projection.
    - An independent PowerShell Adam7 oracle, regenerated manifest-backed vectors, unsplit and every nonempty two-IDAT split baseline comparisons, hostile pass-layout cases, and js/wasm/wasm-gc/native tests prove the implementation without changing non-interlaced behavior.
  artifacts:
    - path: modules/mb-image/png/structural.mbt
      provides: IHDR interlace validation, Adam7 pass geometry, checked per-pass filtered-size/work accounting, and a transport mode that preserves existing profile and transparency facts.
    - path: modules/mb-image/png/raster_decode.mbt
      provides: reusable pass-local encoded-row reconstruction and profile writers that scatter decoded pass pixels into the final RGB8/RGBA8 image.
    - path: modules/mb-image/png/deflate_inflate.mbt
      provides: streaming zlib emission consumption across seven passes, exact pass completion checks, and arbitrary-IDAT-boundary equivalence.
    - path: scripts/fixtures/Generate-PngDecodeVectors.ps1
      provides: independent Adam7 PNG construction, bytewise pass reconstruction, full profile/tRNS/PLTE oracle checks, and generated split-vector evidence.
    - path: fixtures/png/decode-cases.json
      provides: accepted and hostile Adam7 corpus cases covering all supported profiles, pass geometry, limits, and segmentation.
    - path: modules/mb-image/png/generated_decode_vectors_test.mbt
      provides: regenerated public PngDecoder vectors for all four targets.
  key_links:
    - from: modules/mb-image/png/structural.mbt
      to: modules/mb-image/png/deflate_inflate.mbt
      via: PngStreamTransport carries the validated interlace mode plus source layout into checked Adam7 pass sequencing.
      pattern: PngStreamTransport|_png_adam7
    - from: modules/mb-image/png/deflate_inflate.mbt
      to: modules/mb-image/png/raster_decode.mbt
      via: Each pass supplies pass-local reconstructed encoded rows and its x/y origin and stride to profile-specific scatter writers before the next pass resets predictor state.
      pattern: PngPackedRows|_png_write.*pass|_png_inflate_zlib_to_raster
    - from: fixtures/png/decode-cases.json
      to: modules/mb-image/png/generated_decode_vectors_test.mbt
      via: The independent PowerShell generator validates Adam7 bytes and emits unsplit/two-IDAT-split baselines while checking the png-decode-vectors manifest digest.
      pattern: Generate-PngDecodeVectors
---

<objective>
Implement strict, bounded Adam7 interlaced PNG decoding across every colour profile the eager PngDecoder currently supports, while retaining its existing RGB8/RGBA8 mapping, full raw transparency semantics, authenticated streaming transport, and non-interlaced behavior.

Purpose: complete the decoder's raster-layout support without weakening bounds, atomic result visibility, or four-target conformance.
Output: private seven-pass structural/raster/streaming support with independently generated accepted and hostile conformance evidence. Excludes colour-management, encoding, public streaming, QOI, release, and configuration.
</objective>

## Scope coverage audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Bounded Adam7 decode for every currently supported PNG profile with unchanged RGB8/RGBA8 semantics | Tasks 1-2 |
| REQ | PNGX-01 strict eager RGB/RGBA PNG interchange with explicit image-model mapping | Tasks 1-2 |
| CONTEXT | Seven-pass geometry and empty passes; pass-local packed/filter bpp; bounded work/allocation; scatter/no partial result; raw PLTE/tRNS; arbitrary IDAT boundaries; independent hostile oracle; four targets; exclusions | Tasks 1-2 |
| RESEARCH | Level 0: the completed 661, 7d5, and 81r quick tasks establish encoded-source filtering, all-profile mapping, raw tRNS, generated split baselines, manifest validation, and portable PNG test patterns; no new dependency or external integration is required | Tasks 1-2 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode checked Adam7 pass rows into the existing RGB8/RGBA8 model</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png.mbt, modules/mb-image/png/deflate_inflate.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/structural_wbtest.mbt, modules/mb-image/png/raster_decode_wbtest.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - IHDR accepts interlace method 0 and 1 only. Method 0 retains the existing byte-for-byte non-interlaced path; any other method has the established typed IHDR-method failure.
    - Interlace method 1 follows the seven PNG Adam7 tuples in order: (x=0,y=0,dx=8,dy=8), (4,0,8,8), (0,4,4,8), (2,0,4,4), (0,2,2,4), (1,0,2,2), and (0,1,1,2). A pass whose derived width or height is zero contributes neither a row nor a filter byte.
    - Each nonempty pass applies filters only against that pass's prior row and left pixel. Packed grayscale/indexed data use bpp 1; depth-8 source rows use source channels; depth-16 type 0/2/4/6 source rows use bpp 2/6/4/8; predictor state resets before every pass.
    - Reconstructed pass pixels scatter to x = pass_x + column * pass_dx and y = pass_y + row * pass_dy, preserving the existing grayscale expansion, palette lookup/tRNS alpha, full raw 16-bit grayscale/truecolour tRNS equality, and straight native-alpha high-byte behavior.
    - Checked preflight and decode use the exact aggregate of (pass_row_bytes + 1) * pass_height for nonempty passes, allocate the final image plus exactly two reusable rows sized to the largest nonempty pass row, and reject truncated, surplus, invalid-filter, or below-limit data without returning an image.
  </behavior>
  <action>Write focused white-box and public tests first, then add Adam7 as a private layout branch. Replace the current IHDR interlace-method-zero-only gate with validation of methods 0 and 1, carry the mode in both private transports, and introduce one checked private Adam7 geometry helper that returns the seven fixed origin/stride tuples, ceil-divided pass dimensions, pass encoded row width, and aggregate filtered work. Do not duplicate the geometry in structural, streaming, and raster code. For method 1, preflight all checked pass arithmetic before image allocation; reserve the exposed RGB8/RGBA8 image plus exactly two reusable rows at the maximum pass encoded width, and charge exact aggregate filtered output/work. Reuse/reset the two row buffers at each pass boundary so the first pass row has no above predictor and no allocations scale with pass count or image height. Update the live inflater to consume one filter byte and exactly pass-row bytes only for nonempty passes, use the profile's encoded filter bpp, finish/scatter a pass row at its full-image coordinates, and require zlib output to end exactly after pass seven. Do not use the final image as Adam7 predictor storage, because neighbouring output pixels need not share a pass row. Factor or extend private row writers so each existing profile writer can consume reconstructed source bytes and scatter through an explicit pass mapping: opaque and tRNS grayscale low/8/16, opaque indexed low/8 with required PLTE plus indexed-alpha variants, opaque and tRNS RGB 8/16, grayscale-alpha 8/16, and RGBA 8/16. Keep depth-16 full raw tRNS comparisons before high-byte projection, retain all PLTE/tRNS order/length/cardinality/index checks, and retain the method-0 routes unchanged. Update the public capability documentation in `modules/mb-image/png/png.mbt` from a non-interlaced-only claim to the current supported scope: strict eager Adam7 and non-interlaced RGB8/RGBA8 decoding, with compression and scanline state private; do not expose new public APIs. Cover all seven geometry tuples, representative empty-pass dimensions, all filter tags with nonzero left/above/upper-left values within one pass, packed bpp-1 reconstruction, depth-16 bpp boundaries, scatter coordinates, exact and one-unit-short image/output/work/row budgets, truncated and surplus interlaced raster data, invalid filter tags, invalid interlace methods, and no DecodeResult image on failure. Do not add public APIs, colour management, PNG encoding, resumable streaming, QOI, release, or configuration work.</action>
  <verify><automated>moon -C modules/mb-image test png --target all --frozen</automated></verify>
  <done>Interlace method 1 streams and scatters all seven pass layouts through the same bounded profile semantics as method 0, including opaque and tRNS grayscale/truecolour variants and opaque indexed PLTE cases, with pass-local filtering, exact resource accounting, raw PLTE/tRNS behavior, atomic decoder failures, and public documentation that accurately states Adam7 support.</done>
</task>

<task type="auto">
  <name>Task 2: Generate an independent Adam7 profile and hostile-boundary corpus</name>
  <files>fixtures/png/decode-cases.json, fixtures/manifest.json, scripts/fixtures/Generate-PngDecodeVectors.ps1, modules/mb-image/png/generated_decode_vectors_test.mbt, modules/mb-image/png/png_test.mbt</files>
  <action>Extend the decode-case schema and PNG builder with an explicit Adam7 interlace field that writes IHDR method 1, preserving method-0 defaults and every existing case. Add accepted interlaced records for both opaque and tRNS grayscale at depths 1/2/4/8/16, including a valid transparency sample for each depth; opaque indexed depths 1/2/4/8 with depth-valid PLTE, plus separate indexed tRNS cases with partial/full alpha tables; both opaque and tRNS truecolour at depths 8/16, including a valid transparency sample for each depth; grayscale-alpha depths 8/16; and RGBA depths 8/16. Use dimensions that make every Adam7 pass nonempty, distribute filter tags 0 through 4 with nonzero pass-local predictor inputs, include a compact set of dimensions that leaves specified passes empty, and declare exact full-image RGB8/RGBA8 descriptor semantics and exact output pixels/bytes for every accepted case. Include same-high-byte/different-low-byte depth-16 samples to prove raw tRNS matching remains distinct from projected output. Add hostile interlaced records for unsupported IHDR interlace values, missing or extra pass raster bytes, truncation in each early and late pass, invalid pass filter bytes, malformed compression, PLTE/index and tRNS order/length/CRC failures, and exact/below image/output/work limits. Assign comparison groups to every accepted and hostile Adam7 payload so the generator emits an unsplit baseline and every nonempty contiguous two-IDAT split; generated public tests must assert each accepted split's RGB8-versus-RGBA8 descriptor/output semantics and exact pixels against its baseline, and compare each hostile split category/code/context with its baseline while asserting no successful result for hostile cases. Extend Assert-Oracle independently of MoonBit decoder helpers: calculate the seven pass geometries, skip empty passes, derive each profile's packed or encoded pass stride and bpp, reconstruct filters with pass-local current/previous rows, expand or map source samples using the existing documented RGB8/RGBA8 semantics, perform complete raw tRNS comparisons, validate PLTE/index/table alpha, and scatter into a full-image oracle buffer before comparing declared descriptor semantics and exact pixels. Update required IDs and schema version, regenerate the MoonBit table instead of hand-editing it, and update only the png-decode-vectors SHA-256 record in fixtures/manifest.json. Preserve corpus provenance, all existing non-interlaced vectors, unrelated manifest records, and the public generated-vector runner.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image check --target all --deny-warn --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png; exit $LASTEXITCODE</automated></verify>
  <done>The independent Adam7 oracle, corpus, generated vectors, manifest integrity check, exhaustive IDAT-boundary comparisons, hostile cases, non-interlaced regressions, and all four target runs reproducibly demonstrate opaque and tRNS grayscale/truecolour coverage at every required depth, opaque indexed PLTE coverage, RGB8-versus-RGBA8 descriptor/output semantics, and exact accepted pixels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted IHDR to private pass transport | Interlace mode, dimensions, colour layout, source byte width, and per-pass arithmetic determine allocations and image coordinates. |
| compressed IDAT to Adam7 pass rows | Attacker-controlled zlib bytes and filter tags reconstruct pass-local encoded samples before they are scattered into the image. |
| PLTE/tRNS to profile writers | Palette cardinality, indexed alpha, and complete raw transparency keys determine RGB8/RGBA8 pixel and alpha semantics. |
| fixture JSON/generator to generated tests | Corpus declarations and an independent oracle become portable conformance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260721-8nz-01 | Denial of service | Adam7 preflight, row reuse, and zlib output accounting | high | mitigate | Use checked seven-pass geometry and exact aggregate filtered work; reserve only the image plus two maximum-pass rows and prove exact/below limit behavior. |
| T-260721-8nz-02 | Tampering | pass-local inverse filtering and scatter mapping | high | mitigate | Reset predictor rows at each pass, filter encoded bytes with declared bpp, validate exact output length, and test all pass coordinates, empty passes, malformed rows, and invalid filters. |
| T-260721-8nz-03 | Information integrity | PLTE/tRNS and depth-16 projection | high | mitigate | Retain existing full palette/index/table semantics and compare complete raw depth-16 keys before high-byte output mapping across scattered passes. |
| T-260721-8nz-04 | Tampering | arbitrary IDAT segmentation | high | mitigate | Stream IDAT through the existing authenticated source and compare every nonempty two-IDAT split to its unsplit accepted or hostile baseline. |
| T-260721-8nz-05 | Tampering | generated conformance evidence | medium | mitigate | Independently construct and reconstruct Adam7 scanlines in PowerShell, validate the generated table and manifest digest, and run portable tests on all four targets. |
</threat_model>

<verification>
1. `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
2. `moon -C modules/mb-image test png --target all --frozen`
3. `moon -C modules/mb-image check --target all --deny-warn --frozen`
4. `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- Adam7 method-1 inputs for every currently supported colour/depth/transparency profile decode to the same RGB8 or straight-RGBA8 semantics as their method-0 counterparts, and method 0 remains unchanged.
- Seven-pass geometry, empty-pass treatment, packed and encoded bpp filters, pass-local reconstruction, scatter coordinates, raw transparency/palette semantics, zlib completion, bounds, and no-image-on-error behavior are all checked.
- Independently generated fixtures, manifest evidence, exhaustive arbitrary two-IDAT boundaries, hostile pass/raster/semantic/resource cases, and js/wasm/wasm-gc/native tests make the contract reproducible.
- No colour-management, encoder, public-streaming, QOI, release, or configuration change is planned.
</success_criteria>

<output>
Create `.planning/quick/260721-8nz-implement-bounded-adam7-interlaced-png-d/260721-8nz-SUMMARY.md` when done
</output>
