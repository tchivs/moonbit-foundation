---
quick_id: 260721-6k0
title: Bounded non-interlaced 8-bit grayscale-alpha PNG decode
status: complete
completed: 2026-07-21
commits:
  - 6154f21
  - 59fba97
  - 289a4e6
---

# Quick Task 260721-6k0 Summary

Implemented strict, bounded decoding of non-interlaced 8-bit PNG colour type 4 into straight RGBA8, preserving the encoded alpha byte and expanding every grayscale byte into RGB.

## Delivered

- Validated type 4 only at bit depth 8; type-4 `tRNS` is rejected before decode success.
- Added checked type-4 source-row, filtered-output, image, work, and two-row allocation accounting.
- Reconstructed grayscale-alpha filter predictors with source bpp 2 in both the eager helper and live IDAT inflater.
- Added independent PowerShell oracle/vector generation for all five filters, native alpha, type-4 semantic/CRC/filter/depth failures, malformed compression, and resource limits.
- Generated unsplit baselines and every nonempty two-IDAT split for every type-4 success and hostile comparison group; public tests compare accepted descriptors/pixels and hostile category/code/context to their unsplit baselines without accessing an image on failure.

## Verification

- Passed: `pwsh -NoProfile -File ./scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check` (527 executable vectors).
- Passed: `moon -C modules/mb-image test png --target all --frozen` (28 tests on wasm, wasm-gc, js, and native).
- Passed: `pwsh -NoProfile -File ./scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
- Scoped limitation: `moon -C modules/mb-image check --target all --deny-warn --frozen` fails on 26 pre-existing unused-field diagnostics in `generated_vectors.mbt` and the legacy unused `PngTransport.idat` / `consumed` fields. The PNG quality lane, all-target tests, and generator verification pass; no unrelated warning cleanup was made.

## Commits

- `6154f21` — failing type-4 IHDR/budget and bpp-2 raster tests.
- `59fba97` — type-4 transport, budgets, and eager/streamed raster implementation.
- `289a4e6` — independent fixture oracle, manifest, generated split table, and baseline-comparison tests.

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 1 - Test data] Corrected the expected alpha byte in the Paeth-focused white-box vector after independently calculating the predictor result.

## Known Stubs

None.

## Self-Check: PASSED

- All three commits exist in git history.
- Generated fixture table, corpus, manifest record, source changes, and this summary exist on disk.
