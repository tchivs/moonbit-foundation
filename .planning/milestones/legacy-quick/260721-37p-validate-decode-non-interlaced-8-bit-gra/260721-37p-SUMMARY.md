---
quick_id: 260721-37p
subsystem: png-decoder
tags: [moonbit, png, grayscale, deflate, fixtures]
requires:
  - bounded PNG RGB8/RGBA8 decoder
provides:
  - non-interlaced opaque 8-bit grayscale PNG decoded as RGB8
  - exhaustive two-IDAT-boundary grayscale fixture coverage
affects: [PNGX-01, png-fixtures]
tech-stack:
  added: []
  patterns:
    - source scanline channels are tracked separately from public output channels
key-files:
  modified:
    - modules/mb-image/png/structural.mbt
    - modules/mb-image/png/deflate_inflate.mbt
    - scripts/fixtures/Generate-PngDecodeVectors.ps1
key-decisions:
  - "Colour type 0 remains limited to 8-bit, non-interlaced opaque input and expands each source sample into RGB8."
  - "Fixture generation creates a distinct PNG for every nonempty two-IDAT split of the grayscale zlib payload."
requirements-completed: []
requirements-progress:
  - PNGX-01 opaque 8-bit grayscale-to-RGB8 subset
coverage:
  - id: D1
    description: Opaque non-interlaced 8-bit grayscale PNGs decode as RGB8 with all five PNG filters.
    requirement: PNGX-01
    verification:
      - kind: unit
        ref: moon -C modules/mb-image test png --target all --frozen
        status: pass
    human_judgment: false
  - id: D2
    description: Grayscale decode vectors cover every contiguous nonempty two-IDAT boundary and retain fixture integrity.
    requirement: PNGX-01
    verification:
      - kind: integration
        ref: Generate-PngDecodeVectors.ps1 -Check; Generate-PngStructuralVectors.ps1 -Check
        status: pass
    human_judgment: false
duration: 31min
completed: 2026-07-21
status: complete
---

# Quick Task 260721-37p: Grayscale PNG Decode Summary

**Bounded PNG colour type 0 now reconstructs one-byte filtered scanlines into opaque RGB8 pixels while retaining RGB/RGBA behavior and semantic-chunk rejection.**

## Accomplishments

- Separated source scanline channels from RGB8/RGBA8 descriptor and allocation channels; grayscale filtered work uses one byte per pixel while output accounting uses RGB8.
- Reconstructed grayscale filters 0–4 with grayscale neighbours, then fanned each value into R, G, and B in both streaming and white-box raster paths.
- Added an opaque grayscale corpus whose generator emits all 22 possible nonempty two-IDAT zlib split positions, regenerated vectors, and refreshed both manifest digests.
- Added grayscale filter and exact/below resource-envelope tests.

## Scope Completion

This quick task completes only the opaque, non-interlaced 8-bit grayscale
slice of PNGX-01. Palette, grayscale-alpha, transparency, and 16-bit profiles
remain future work.

## Task Commit

1. **Tasks 1–3: Decoder, tests, and deterministic fixture evidence** — `51dc49e` (feat)

## Verification

- PASS: `Generate-PngDecodeVectors.ps1 -Check` (31 executable cases).
- PASS: `Generate-PngStructuralVectors.ps1 -Check` (89 structural cases).
- PASS: `moon -C modules/mb-image test png --target all --frozen` (20/20 on wasm, wasm-gc, js, and native).
- BLOCKED outside task scope: `moon -C modules/mb-image check --target all --deny-warn --frozen` promotes pre-existing unused generated-vector fields to errors.
- BLOCKED outside task scope: the documented `scripts/quality.ps1 -Lane Required` wrapper drops its mandatory `Lane` when dot-sourcing; its underlying runner reaches foundation-policy validation then fails because `moon.work` has 7 members while policy expects 6.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test fixture arithmetic] Corrected the grayscale Up-filter source bytes.**
- **Found during:** Task 2 verification.
- **Issue:** The initial vector used values relative to row zero instead of the preceding reconstructed row.
- **Fix:** Recomputed the canonical zlib payload and filtered bytes while preserving the intended expanded RGB8 pixels.
- **Files modified:** `fixtures/png/decode-cases.json`, generated decode vectors.
- **Verification:** Four-target PNG test suite passed.
- **Committed in:** `51dc49e`.

**Total deviations:** 1 auto-fixed.

## Known Stubs

None.

## Self-Check: PASSED

- Commit `51dc49e` exists and contains only the planned PNG decoder, tests, fixtures, generators, manifest, and generated-vector files.
- All generated PNG vector artifacts exist and both generator `-Check` commands pass.

## Next Readiness

The grayscale subset is ready for subsequent PNGX-01 work. Palette, grayscale-alpha, 16-bit, Adam7, transparency mapping, and colour-management support remain intentionally unsupported.
