---
phase: 41-adam7-opt-in-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - scripts/quality/Invoke-PngAdam7Compatibility.ps1
autonomous: true
requirements:
  - PNGI-01
must_haves:
  truths:
    - "A PNG user can select an explicit Adam7 strategy on eager and caller-buffered RGB8 or straight-RGBA8 encoder routes."
    - "Every pre-existing eager and caller-buffered factory still emits its frozen non-interlaced bytes, and the additive explicit-None routes emit those same bytes."
    - "An Adam7 request returns the typed png-encode capability error with the stable pending context before an eager writer observes bytes or a caller-buffered encoder is returned."
    - "The focused compatibility selectors pass independently on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: "Public PngInterlaceStrategy and additive eager factory selections."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Additive caller-buffered factories and one interlace-aware private construction seam."
    - path: modules/mb-image/png/encode.mbt
      provides: "Atomic Adam7-pending preflight rejection through the existing typed PNG capability path."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Immutable public eager legacy-byte and Adam7 pre-output rejection evidence."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Immutable public caller-buffered legacy-byte and Adam7 pre-construction rejection evidence."
    - path: scripts/quality/Invoke-PngAdam7Compatibility.ps1
      provides: "Target-isolated runner for the two Phase 41 public compatibility selectors."
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngEncoder and PngChunkEncoder additive factories forward PngInterlaceStrategy through the shared machine constructor."
      pattern: "PngInterlaceStrategy"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/encode.mbt
      via: "PngEncodeMachine construction calls the interlace-aware atomic preflight before a machine is returned."
      pattern: "adam7-pending"
    - from: scripts/quality/Invoke-PngAdam7Compatibility.ps1
      to: modules/mb-image/png/encode_test.mbt
      via: "Exact Moon test selector is run once per isolated portable target."
      pattern: "PNG Adam7 opt-in eager compatibility"
---

<objective>
Publish the explicit Adam7 configuration seam while preserving every legacy non-interlaced encoder byte route and enforcing the deterministic pre-implementation boundary.

Purpose: Satisfy PNGI-01 and D-01 through D-07 without introducing seven-pass traversal, filtering, compression planning, replay changes, or any unrelated image subsystem work reserved for Phase 42.
Output: Public interlace selection types/factories, one atomic typed pending rejection path, immutable eager/chunk compatibility tests, and a focused four-target selector runner.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-adam7-opt-in-compatibility/41-CONTEXT.md
@.planning/milestones/v0.12-phases/40-portable-adaptive-filter-evidence/40-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@scripts/quality/Invoke-PngEncodeEvidence.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing public compatibility and pending-boundary tests</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt, scripts/quality/Invoke-PngAdam7Compatibility.ps1</files>
  <behavior>
    - Legacy eager factories—default, compression-only, filter-only, and the existing compression/filter combined factory—produce their current immutable complete PNG vectors for both RGB8 and straight-RGBA8 sources; the new explicit None routes produce the same literal vectors.
    - Legacy caller-buffered factories cover the same factory families, drain the existing zero/one/ragged schedule, and produce the same immutable complete PNG vectors for both supported source profiles; the new explicit None routes match each literal vector.
    - An eager RGB8 or straight-RGBA8 request configured for Adam7 fails with the existing typed PNG capability category/code and context `png-adam7-pending`; the writer position remains zero and the caller budget remains unchanged.
    - A caller-buffered RGB8 or straight-RGBA8 Adam7 construction returns that same typed error before an encoder is returned, with the caller budget unchanged. No capacity test is fabricated after this constructor rejection because no caller lease can exist.
    - A PowerShell selector runner executes the named eager and chunk compatibility tests separately for js, wasm, wasm-gc, and native from an owned temporary target root, and removes only that verified root in finally.
  </behavior>
  <action>Create the RED tests first, per D-06 and D-07. Extend the existing public vector tests instead of introducing generated fixtures or a second live encoder as an oracle: preserve and reuse literal complete-PNG byte baselines for 1x1 RGB8/straight-RGBA8 Stored cases and the existing flat 32-pixel RGB8/straight-RGBA8 FixedOrStored cases. Cover every legacy eager factory family (`new`, compression-only for all three compression strategies, filter-only for None and Adaptive, and every existing compression/filter combined pair) against the literal baseline applicable to its source. Add matching assertions for the additive explicit-None factories, naming them `new_with_interlace_strategy(PngInterlaceStrategy::None)` and `new_with_all_strategies(compression, filter, PngInterlaceStrategy::None)` to match the established factory vocabulary. Mirror the same complete-vector coverage for `PngChunkEncoder`, draining through the existing hostile schedule and comparing only to immutable byte literals. Add two named public tests: `PNG Adam7 opt-in eager compatibility` and `PNG Adam7 opt-in chunk compatibility`. In each, cover RGB8 and straight-RGBA8, use Adam7 through both the narrow interlace-only factory and the all-strategy factory, assert the existing capability-unavailable category/code plus the stable pending context, writer/constructor atomicity, and unchanged budget. Create `scripts/quality/Invoke-PngAdam7Compatibility.ps1` with only those two exact selectors and the explicit target array js, wasm, wasm-gc, native. Follow the Phase 40 runner's containment pattern: create a GUID-prefixed child of the resolved OS temporary root per target, pass it as Moon's target directory, verify containment and ownership prefix before cleanup, and remove it in finally. The runner must not create repository build, debug, recover, or probe directories. Run the new native selectors and record their nonzero RED result before production edits; do not alter QOI, colour, operations, Adam7 geometry, filter planning, DEFLATE selection, or replay tests.</action>
  <verify>
    <automated>$ErrorActionPreference = 'Continue'; moon -C modules/mb-image test png --target native --frozen -f 'PNG Adam7 opt-in eager compatibility'; if ($LASTEXITCODE -eq 0) { exit 1 }; moon -C modules/mb-image test png --target native --frozen -f 'PNG Adam7 opt-in chunk compatibility'; if ($LASTEXITCODE -eq 0) { exit 1 }; exit 0</automated>
  </verify>
  <done>The two named public compatibility tests and their all-target runner exist, and the native RED command confirms they cannot pass until the Adam7 type/factory/preflight seam is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the additive interlace seam and make the compatibility suite pass</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode.mbt</files>
  <behavior>
    - PngInterlaceStrategy is a separately selectable, equality-comparable public enum with None and Adam7; it does not alter PngCompressionStrategy or PngFilterStrategy.
    - Existing eager and chunk signatures preserve source compatibility and explicitly forward None at every legacy default, compression-only, filter-only, combined, and private-machine factory seam.
    - Eager and chunk encoders expose one interlace-only factory plus one additive all-strategy factory. The former fixes Stored and filter None; the latter accepts compression, filter, and interlace independently.
    - Adam7 reaches the shared atomic construction/preflight boundary and returns capability_unavailable for png-encode with context png-adam7-pending before source traversal, budget charging, eager output, a returned chunk encoder, or a caller lease.
  </behavior>
  <action>Implement the contract exactly as locked by D-01 through D-05. In `png.mbt`, add `pub(all) enum PngInterlaceStrategy { None; Adam7 } derive(Eq)`, add an interlace field to `PngEncoder`, and retain every existing public signature unchanged. Make `new`, compression-only, filter-only, and the existing `new_with_strategies` explicitly construct `PngInterlaceStrategy::None` per D-02; add `PngEncoder::new_with_interlace_strategy` and `PngEncoder::new_with_all_strategies` as the D-03 additive seams. In `stream_encode.mbt`, give `PngChunkEncoder` the corresponding interlace-only and all-strategy constructors; make every existing constructor and the private `PngEncodeMachine::new`, compression-only, and existing combined constructor forward explicit None. Add the interlace intent to the machine and route all paths through a single private all-strategy construction method, preserving existing byte emission state and legacy method signatures. In `encode.mbt`, add an interlace-aware wrapper around the existing filter-aware preflight and make it the sole machine admission call. Its Adam7 branch must return `_png_encode_capability` with the stable pending context before `_png_encode_source`, candidate/filter/compression work, budget reservation, or output state creation. Its None branch must delegate unchanged to the current preflight so all legacy byte and error behavior is preserved. Do not modify `_png_adam7_passes` or any seven-pass geometry in `structural.mbt`; do not add FFI, staging, a new image representation, provisional output, new compression/filter policies, or a decoder change. After the RED tests compile and pass, run the focused all-target runner and the existing PNG package suite to detect compatibility drift.</action>
  <verify>
    <automated>&amp; .\scripts\quality\Invoke-PngAdam7Compatibility.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; moon -C modules/mb-image test png --target native --frozen; exit $LASTEXITCODE</automated>
  </verify>
  <done>PngInterlaceStrategy and both additive eager/chunk selection seams are public; every legacy route forwards None and retains its immutable vectors; Adam7 deterministically returns the typed pending rejection before observable eager or caller-buffered output; the two focused selectors pass independently on all four portable targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller selection → PNG construction | Public interlace, compression, and filter inputs determine whether a machine can be admitted. |
| eager writer / caller-buffered lease → PNG output | Output must remain unavailable when the selected interlace implementation is pending. |
| PowerShell evidence runner → temporary filesystem | The target-isolated test runner creates and deletes a temporary compiler-output root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | legacy strategy routing | high | mitigate | Explicitly forward None at every legacy factory and prove complete literal eager/chunk vectors for RGB8 and straight-RGBA8. |
| T-41-02 | Information Disclosure | eager writer and caller lease | medium | mitigate | Reject Adam7 at atomic machine preflight before source traversal, budget charging, output-state creation, writer calls, or a returned chunk encoder. |
| T-41-03 | Tampering | Invoke-PngAdam7Compatibility.ps1 cleanup | high | mitigate | Create only a GUID-prefixed child of the resolved OS temporary root; verify containment and prefix immediately before recursive cleanup in finally. |
| T-41-04 | Repudiation | portable compatibility claim | low | mitigate | Run the two exact public selectors independently on js, wasm, wasm-gc, and native with target-specific temporary roots. |
</threat_model>

<verification>
Run `& .\scripts\quality\Invoke-PngAdam7Compatibility.ps1`; it owns the exact eager/chunk selectors, four target invocations, and safe temporary-root cleanup. Then run `moon -C modules/mb-image test png --target native --frozen` to verify existing public PNG behavior remains intact.
</verification>

<success_criteria>
1. Public callers can name `PngInterlaceStrategy::None` or `PngInterlaceStrategy::Adam7` through additive eager and caller-buffered factories without changing existing signatures.
2. Immutable complete PNG byte assertions cover every legacy and explicit-None eager/chunk factory family for RGB8 and straight-RGBA8.
3. Adam7 configuration fails with the typed `png-adam7-pending` capability error before eager bytes or a caller-buffered encoder/lease is observable.
4. The focused public compatibility selectors pass separately on js, wasm, wasm-gc, and native.
</success_criteria>

<output>
Create `.planning/phases/41-adam7-opt-in-compatibility/41-01-SUMMARY.md` when done.
</output>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Explicit Adam7 eager/chunk selection while preserving non-interlaced bytes | 41-01 | COVERED | Public factories, None forwarding, immutable vectors, and pending rejection are one vertical compatibility boundary. |
| REQ | PNGI-01 | Adam7 selection for RGB8/straight-RGBA8 with existing constructor/compression-only byte compatibility | 41-01 | COVERED | Tasks 1-2 cover both public encoder forms and all legacy factory families. |
| CONTEXT | D-01 | Separate public None/Adam7 strategy | 41-01 Task 2 | COVERED | New PngInterlaceStrategy only. |
| CONTEXT | D-02 | Existing signatures explicitly default None | 41-01 Task 2 | COVERED | Every legacy eager, chunk, and private construction route forwards None. |
| CONTEXT | D-03 | Add interlace-only and all-strategy eager/chunk factories | 41-01 Task 2 | COVERED | Narrow and additive all-strategy factory seams are specified by name. |
| CONTEXT | D-04 | Deterministic pending rejection before eager output or caller lease | 41-01 Tasks 1-2 | COVERED | Atomic eager writer and chunk-construction evidence with preflight implementation. |
| CONTEXT | D-05 | Typed capability path, stable pending context, no backend expansion | 41-01 Task 2 | COVERED | Existing capability helper is used; Phase 42 work is excluded. |
| CONTEXT | D-06 | Immutable legacy eager/chunk vectors plus explicit None equality | 41-01 Task 1 | COVERED | Tests compare complete outputs to literal baselines, not live encoders. |
| CONTEXT | D-07 | RGB8/RGBA8 Adam7 rejection on eager/chunk boundaries | 41-01 Task 1 | COVERED | Both profiles and both factory shapes are covered with atomicity assertions. |
