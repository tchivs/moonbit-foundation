---
phase: 43-portable-adam7-public-evidence
plan: 01
type: execute
wave: 1
depends_on: [42-01, 42-02]
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - scripts/quality/Invoke-PngAdam7Compatibility.ps1
autonomous: true
requirements: [PNGI-04]
must_haves:
  truths:
    - "Generated RGB8 and straight-RGBA8 sources that exercise all seven Adam7 passes round-trip through the public eager PNG API with exact dimensions, channel count, and pixels."
    - "For every supported compression strategy, public caller-buffered Adam7 output drained with zero, one-byte, and ragged capacities is byte-identical to eager output and decodes to the original pixels."
    - "Frozen legacy and explicit-None non-interlaced byte vectors remain unchanged while every Adam7 public evidence PNG declares IHDR interlace method 1."
    - "The focused public Adam7 evidence selectors run independently on js, wasm, wasm-gc, and native, each with a validated GUID-owned temporary target directory that is removed on success or failure."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Public eager Adam7 generated RGB8/RGBA8 decode-fidelity and frozen-None compatibility selector."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Public Adam7 hostile-capacity byte-identity, decode-fidelity, and frozen-None chunk compatibility selector."
    - path: scripts/quality/Invoke-PngAdam7Compatibility.ps1
      provides: "Independent four-target runner for the Phase 43 public evidence selectors with owned temporary-directory cleanup."
  key_links:
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "PngEncoder and PngDecoder public factories encode generated RGB8/RGBA8 images and decode their emitted PNG bytes."
      pattern: "PngEncoder::new_with_all_strategies|PngDecoder::new"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngChunkEncoder public factory is drained through caller-owned capacities and its complete accepted bytes are compared with public eager bytes."
      pattern: "PngChunkEncoder::new_with_all_strategies|png_chunk_test_drain_encoder"
    - from: scripts/quality/Invoke-PngAdam7Compatibility.ps1
      to: modules/mb-image/png/encode_test.mbt
      via: "One exact MoonBit test selector is invoked per target in an isolated target directory."
      pattern: "moon -C modules/mb-image test png --target"
---

<objective>
Establish portable, black-box public evidence that Phase 42 Adam7 output is faithful and stream-safe without changing the encoder implementation.

Purpose: Satisfy PNGI-04 with generated RGB8 and straight-RGBA8 round trips, immutable non-interlaced compatibility baselines, and independently reproducible execution on every supported target.
Output: Public eager and chunk PNG evidence selectors plus an independent four-target quality runner.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-portable-adam7-public-evidence/43-CONTEXT.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-01-SUMMARY.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-02-SUMMARY.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-VERIFICATION.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@scripts/quality/Invoke-PngAdam7Compatibility.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generated public eager Adam7 fidelity and immutable None evidence</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <action>Extend the public eager test helpers and compatibility selector per D-01, D-02, and D-04. Generate deterministic bounded 5x5 RGB8 and straight-RGBA8 OwnedImage sources whose component formula varies by x, y, and channel so all seven Adam7 passes contribute distinct pixels. For each profile and Stored, FixedOrStored, and DynamicOrFixedOrStored, construct only the public PngEncoder Adam7 factory, collect the eager PNG bytes, assert the IHDR interlace byte is method 1, then decode only through the public PngDecoder/ImageDecoder path and compare width, height, channel count, and every component against the source. Keep the existing complete legacy and explicit-None byte-vector assertions in the focused selector and verify their IHDR method remains 0; do not alter encoder production code, private cursor assertions, fixture files, or codec algorithms. Name the resulting focused test selector for the Phase 43 eager public evidence so the quality runner can invoke it exactly.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f "PNG Adam7 public eager fidelity and frozen None compatibility"</automated>
  </verify>
  <done>Both generated profiles exercise all Adam7 passes, every public eager compression route decodes exactly to its source, Adam7 uses IHDR method 1, and the retained legacy plus explicit-None vectors remain byte-for-byte and method-0 compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Prove public hostile-capacity Adam7 stream fidelity and byte identity</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Extend the public chunk compatibility test surface per D-01, D-02, D-03, and D-04. Reuse or extract the smallest deterministic generated RGB8 and straight-RGBA8 source helper needed to cover all seven Adam7 passes. For each profile and all three compression strategies, create only PngChunkEncoder through its public Adam7 factory; drain it with zero-capacity, one-byte, and ragged schedules, compare the complete accepted-byte result exactly with the matching public eager encoding, and decode the collected chunk bytes through public PngDecoder to compare every source pixel. Preserve the existing immutable legacy and explicit-None chunk byte vectors under the same hostile schedules and assert their IHDR method is 0 while Adam7 output is method 1. Keep acknowledgement/private-machine assertions in their existing white-box tests; this task must not reproduce encoder traversal or change production streaming behavior. Name the focused public selector so it is independently executable by the quality runner.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f "PNG Adam7 public chunk fidelity, hostile identity, and frozen None compatibility"</automated>
  </verify>
  <done>For RGB8 and straight-RGBA8, zero, one-byte, and ragged drains produce exactly the eager Adam7 bytes for Stored, FixedOrStored, and DynamicOrFixedOrStored; those bytes decode to every original pixel, and legacy/explicit-None hostile-drain vectors stay frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Run the public evidence selectors independently on all portable targets</name>
  <files>scripts/quality/Invoke-PngAdam7Compatibility.ps1</files>
  <action>Update the focused selector list in the existing quality runner per D-05 to invoke the Phase 43 eager and chunk public-evidence tests instead of the superseded opt-in compatibility-only selectors. Retain the explicit js, wasm, wasm-gc, and native target list; execute each selector separately with moon, --frozen, and the target's own GUID-named target directory rather than a combined target run. Preserve the validated OS-temp-root child-path checks and try/finally removal so cleanup occurs after a passing selector and after a failed selector, without accepting an external directory as a cleanup target. Do not add release, publication, package-install, or encoder-algorithm work.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/quality/Invoke-PngAdam7Compatibility.ps1</automated>
  </verify>
  <done>The runner reports a pass for each of js, wasm, wasm-gc, and native only after both public Phase 43 selectors pass independently, and every target directory it removes is a validated GUID-owned child of the system temporary root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated public source → PNG encoder/decoder | Deterministic source pixels cross the public codec boundary; a false-positive test could conceal interlace or reconstruction corruption. |
| Quality runner → filesystem and MoonBit target cache | The runner creates and recursively removes temporary build directories while invoking target-specific tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | `encode_test.mbt`, `stream_encode_test.mbt` | medium | mitigate | Compare every decoded public component and descriptor field to deterministic RGB8/RGBA8 sources, assert Adam7 IHDR method 1, and retain frozen method-0 legacy/None vectors. |
| T-43-02 | Tampering/Elevation | `Invoke-PngAdam7Compatibility.ps1` cleanup | high | mitigate | Keep GUID-prefix, direct-child, and system-temp-root validation before creation and recursive removal; cleanup remains inside `finally` for each target. |
</threat_model>

<verification>
Run both focused public selectors on native during implementation, then execute `pwsh -NoProfile -File scripts/quality/Invoke-PngAdam7Compatibility.ps1` to prove each selector independently on js, wasm, wasm-gc, and native. Finally run `moon -C modules/mb-image test png --target all --frozen` to ensure the public PNG package remains portable as a whole.
</verification>

<success_criteria>
- PNGI-04 is demonstrated through public generated RGB8 and straight-RGBA8 Adam7 encode/decode tests with exact pixel equality.
- Every compression strategy has eager/chunk byte identity under zero, one-byte, and ragged caller capacities, while legacy and explicit-None byte vectors remain frozen.
- The dedicated quality runner proves both selectors independently on each of the four supported targets and leaves no owned target directory behind.
- No encoder algorithm, private traversal assertion, image-sized fixture, publication flow, or release script is introduced by this phase.
</success_criteria>

<output>
Create `.planning/phases/43-portable-adam7-public-evidence/43-01-SUMMARY.md` when done.
</output>
