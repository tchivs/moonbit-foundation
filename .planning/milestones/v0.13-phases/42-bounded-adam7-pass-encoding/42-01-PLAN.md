---
phase: 42-bounded-adam7-pass-encoding
plan: 01
type: execute
wave: 1
depends_on: [41-01]
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [PNGI-02, PNGI-03]
must_haves:
  truths:
    - "An RGB8 or straight-RGBA8 Adam7 request emits the seven canonical pass scanline streams in deterministic order, with pass-local filter history and no image-sized or pass-sized staging."
    - "Stored, FixedOrStored, and DynamicOrFixedOrStored admit Adam7 only after exact checked geometry, output, work, and budget facts are known; rejected eager and chunk construction exposes no bytes or lease and leaves the budget unchanged."
    - "Adam7 caller-buffered output makes exact progress for zero and tiny capacities, commits replay state only for acknowledged bytes, reaches a stable terminal state, and equals eager bytes."
    - "Real Adam7 IHDR uses interlace method 1 while every legacy and explicit-None route retains method 0 and its frozen byte vectors."
  artifacts:
    - path: modules/mb-image/png/encode.mbt
      provides: "Pass-aware, bounded logical filtered-byte traversal and its shared atomic planner/preflight ledger."
    - path: modules/mb-image/png/stream_encode.mbt
      provides: "Adam7-aware replay initialization and IHDR framing with acknowledgement-gated successor commits."
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: "Private pass-order, pass-filter-reset, exact-ledger, and no-staging regression coverage."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Eager public Adam7 framing success and legacy-None compatibility coverage."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Public hostile-capacity, atomic-admission, and eager/chunk parity coverage for Adam7."
  key_links:
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/structural.mbt
      via: "The Adam7 byte producer obtains all checked pass dimensions from _png_adam7_passes; no encoder-local pass formula exists."
      pattern: "_png_adam7_passes"
    - from: modules/mb-image/png/encode.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Preflight and replay instantiate fresh instances of the same bounded pass-aware filtered source for the selected DEFLATE plan."
      pattern: "PngFilteredMatchCursor"
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: "PngChunkEncoder pulls from present/acknowledge only after caller-owned lease bytes are accepted."
      pattern: "acknowledge"
---

<objective>
Replace the Phase 41 Adam7-pending capability boundary with real bounded seven-pass emission for compatible RGB8 and straight-RGBA8 sources, without changing any public factory or legacy non-interlaced byte route.

Purpose: Deliver PNGI-02 and PNGI-03 by making Adam7 a different bounded logical filtered-byte source for the existing Stored, FixedOrStored, and DynamicOrFixedOrStored planners and acknowledgement-safe replay machine.
Output: A pass-aware private encoder path, Adam7-aware streaming framing/replay, and targeted white-box and black-box regression tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-CONTEXT.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-RESEARCH.md
@.planning/phases/41-adam7-opt-in-compatibility/41-01-SUMMARY.md

<interfaces>
From modules/mb-image/png/structural.mbt:
- `_png_adam7_passes(width, height, source_channels, bit_depth)` is the sole checked authority for seven pass records, each carrying start, stride, pass dimensions, and `row_bytes`.

From modules/mb-image/png/encode.mbt:
- `PngFilteredCursor` and `PngFilteredMatchCursor` produce persistent logical filtered bytes and retain the existing fixed 262-byte match window.
- `_png_filtered_match_traverse`, `_png_fixed_plan`, and `_png_dynamic_plan_with_idat_limit` currently consume the same non-interlaced logical source during planning; `_png_encode_preflight_with_interlace` is the one atomic admission seam.

From modules/mb-image/png/stream_encode.mbt:
- `PngEncodeMachine::new_with_all_strategies` invokes atomic preflight before machine construction.
- `present` exposes an uncommitted byte and `acknowledge` alone commits `pending_stored`, `pending_fixed`, or `pending_dynamic`; `byte_at` constructs IHDR and all PNG framing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the single bounded Adam7 logical filtered-byte source and atomic planning ledger</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/encode_wbtest.mbt</files>
  <read_first>
    - modules/mb-image/png/structural.mbt: `PngAdam7Pass` and `_png_adam7_passes`
    - modules/mb-image/png/encode.mbt: `PngFilteredCursor`, `PngFilteredMatchCursor`, all three planning walks, and `_png_encode_preflight_with_interlace`
    - modules/mb-image/png/encode_wbtest.mbt: existing traversal-fact and exact/one-less-work assertions
    - .planning/phases/42-bounded-adam7-pass-encoding/42-CONTEXT.md
  </read_first>
  <behavior>
    - A nontrivial RGB8 and straight-RGBA8 image produces the exact filter-tag-plus-sample sequence for all nonempty canonical Adam7 passes, including skipped empty passes.
    - Every new pass begins with no prior-pass predictor history; Adaptive selection never uses an Up/Average/Paeth predecessor from an earlier pass.
    - Stored, FixedOrStored, and DynamicOrFixedOrStored calculate the same selected logical stream for planning and replay, admit at exact work, and reject at one less work without changing the caller budget.
  </behavior>
  <action>
    Replace the Adam7 branch of `_png_encode_preflight_with_interlace` with the same checked source capability, dimension, output, work, and one-final-budget-charge ledger used by the None route, but parameterize that ledger over a private raster-source mode. Per the Phase 42 locked decision, obtain pass dimensions exclusively through `_png_adam7_passes(width, height, channels, 8)`; do not reproduce its starts, strides, or pass-dimension arithmetic in `encode.mbt`.

    Extend the filtered-byte and match-cursor construction APIs so a fresh cursor can traverse either the existing full-image layout or Adam7 from retained scalar source/dimension/index state. The Adam7 traversal must emit each nonempty pass row as its filter tag followed by channel bytes at `pass.x + column * pass.dx` and `pass.y + row * pass.dy`; reset the filter winner and predictor row relationship at every pass boundary. Feed this exact source into Stored traversal, fixed planning, dynamic frequency/bit planning, and the selected replay traversal. Use checked arithmetic for every aggregate scanline/block/output/work value, retain only existing bounded matcher storage plus scalar cursor state, and do not retain a pass buffer, selected-row cache, token stream, staged PNG output, or copied pass-geometry collection.

    First add focused `encode_wbtest.mbt` coverage for exact 5x5-style pass order, 1x1 empty-pass omission, pass-local Adaptive reset, and exact/one-less selected-work and budget atomicity across all three compression strategies. Keep these tests package-private and deterministic; do not introduce Phase 43 generated public corpus cases or target-runner evidence.
  </action>
  <acceptance_criteria>
    - Adam7 geometry is read only from `_png_adam7_passes`, and the encoder contains no second Adam7 formula or retained copy of pass geometry.
    - One pass-aware logical source serves every Stored, FixedOrStored, and DynamicOrFixedOrStored planner/replay traversal with checked totals.
    - The implementation keeps the current fixed-size matcher window and scalar cursor state only; it has no image-sized/pass-sized cache or staging allocation.
    - White-box tests prove pass ordering, empty-pass behavior, Adaptive filter isolation, and exact atomic work/budget admission for RGB8 and straight-RGBA8.
  </acceptance_criteria>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>PNGI-02 has a canonical, bounded private Adam7 byte producer and planner ledger with executable invariant coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route Adam7 replay and framing through acknowledgement-safe chunk output</name>
  <files>modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/stream_encode.mbt: `PngEncodeMachine::new_with_all_strategies`, stored/fixed/dynamic cursor setup, `byte_at`, `present`, and `acknowledge`
    - modules/mb-image/png/stream_encode_test.mbt: Adam7 opt-in compatibility test, hostile-drain helpers, and atomic-admission helpers
    - modules/mb-image/png/encode.mbt: new raster-source/cursor constructor contract from Task 1
    - .planning/phases/42-bounded-adam7-pass-encoding/42-CONTEXT.md
  </read_first>
  <behavior>
    - Adam7 output uses IHDR interlace method 1, while None remains method 0.
    - Stored, FixedOrStored, and DynamicOrFixedOrStored Adam7 encoders drain through zero, one-byte, and ragged capacities with exact per-pull/cumulative progress and eager-byte identity.
    - Repeated preview before acceptance does not advance the pass/match/Adler/CRC successor state; terminal pulls remain idempotent and do not mutate caller memory.
    - Capability, geometry, output, work, and budget rejection remains atomic for eager and chunk Adam7 construction across all three compression strategies.
  </behavior>
  <action>
    Update `PngEncodeMachine::new_with_all_strategies` and all Stored/Fixed/Dynamic state initializers to select Task 1's pass-aware match cursor whenever `interlace_strategy` is Adam7, including filter-None. Preserve the direct non-interlaced filter-None scanline path exactly for `PngInterlaceStrategy::None`, so frozen legacy bytes and constructors remain untouched. Carry only the selected interlace mode and the bounded cursor state; do not cache pass geometry, filtered rows, DEFLATE tokens, or output bytes.

    Make IHDR payload byte 12 reflect the retained interlace strategy: Adam7 emits PNG method 1 and None emits method 0. Leave CRC accumulation and the `present`/`acknowledge` ownership discipline intact: preview may set only private pending successors, and `acknowledge` must remain the sole commit point after the lease accepts that exact byte.

    Replace Phase 41's compatible-Adam7 pending expectations in `stream_encode_test.mbt` with real output tests for RGB8 and straight-RGBA8. Extend the existing hostile drain/admission helpers with an interlace argument rather than duplicating drains. Exercise Stored, FixedOrStored, and DynamicOrFixedOrStored with `[0, 1, 3, 2, 5]` and one-byte schedules, assert byte identity against eager output, exact progress, sticky finished behavior, and no progress before acknowledgement. Add below-limit capability/geometry/output/work/budget cases that prove eager writer position stays zero, chunk construction yields no lease, and every budget field is unchanged on failure.
  </action>
  <acceptance_criteria>
    - All three compression strategies replay Adam7 through the common bounded source and acknowledgement-gated successor state.
    - Adam7 chunk output matches eager output under hostile capacities and has exact, non-mutating terminal behavior.
    - IHDR interlace method is correct for Adam7 and None, without changing legacy direct replay or frozen byte routes.
    - All specified preflight rejections are atomic before output or lease visibility and preserve the input budget.
  </acceptance_criteria>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>PNGI-03 connects bounded Adam7 planning to real caller-buffered replay without weakening atomic admission or acknowledgement safety.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the eager Adam7 boundary while retaining frozen None compatibility</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/encode_test.mbt: `PNG Adam7 opt-in eager compatibility`, frozen legacy vector assertions, and eager writer helpers
    - modules/mb-image/png/stream_encode_test.mbt: completed Adam7 hostile-parity helper coverage from Task 2
    - .planning/phases/41-adam7-opt-in-compatibility/41-VERIFICATION.md
    - .planning/phases/42-bounded-adam7-pass-encoding/42-CONTEXT.md
  </read_first>
  <action>
    Convert the compatible RGB8 and straight-RGBA8 Adam7 eager cases from typed pending-boundary assertions to successful output assertions. Use small deterministic inputs and verify complete framing plus IHDR interlace method 1 for interlace-only and all-strategy eager factories, including Stored, FixedOrStored, and DynamicOrFixedOrStored where the public factory exposes them. Retain the existing immutable complete-PNG vectors for every legacy and explicit-None eager constructor unchanged, and preserve unsupported-image capability coverage as an atomic failure boundary.

    Keep this task focused on the Phase 42 eager boundary. Do not add Phase 43's generated fidelity corpus, broad legacy compatibility corpus, or separate js/wasm/wasm-gc/native public-evidence runner; the task may reuse the native package suite as regression verification only.
  </action>
  <acceptance_criteria>
    - Compatible eager Adam7 factories now emit complete PNGs with IHDR method 1 instead of the Phase 41 pending response.
    - Frozen legacy and explicit-None eager vectors remain byte-identical and keep method 0.
    - The eager public tests cover both supported source profiles and the available compression strategy surface without expanding into Phase 43 evidence scope.
  </acceptance_criteria>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The public eager boundary observes real Adam7 output while all legacy None compatibility evidence remains intact.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- A private Adam7 logical filtered-byte producer shared by all three existing compression planner/replay routes.
- An interlace-aware `PngEncodeMachine` path that writes method-1 IHDR and commits replay only after acknowledgement.
- Package-private pass/filter/ledger tests and focused eager/chunk black-box regressions; no generated corpus, target runner, cache, pass-geometry copy, or staging artifact.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageView to Adam7 preflight | Caller-controlled image format, dimensions, limits, and budget become checked pass geometry and compression/work facts. |
| Encode machine to caller lease/Writer | Generated PNG bytes cross into caller-owned output; retries can occur before a byte is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Denial of service | Adam7 pass total and preflight ledger | high | mitigate | Use `_png_adam7_passes` plus checked additions/multiplications for every nonempty-pass scanline, block, output, and work aggregate; execute limits and the single budget charge before construction. |
| T-42-02 | Denial of service | Filtered-byte producer and DEFLATE planners | high | mitigate | Regenerate pass bytes from scalar cursor state through the existing bounded 262-byte matcher window; prohibit pass/image buffers, selected-row caches, token streams, and staged output. |
| T-42-03 | Tampering / denial of service | `PngEncodeMachine::present` and `acknowledge` | high | mitigate | Keep Adam7 cursor successors private during preview and swap them only after the exact pending byte is acknowledged; test zero/tiny lease schedules and terminal idempotence. |
| T-42-04 | Tampering | IHDR framing | medium | mitigate | Select IHDR method from `PngInterlaceStrategy` in `byte_at`, retain acknowledgement-driven CRC calculation, and assert method 1 for Adam7 and method 0 for None. |
</threat_model>

<verification>
- `moon -C modules/mb-image test png --target native --frozen`
- `git diff --check`
- `git status --short` confirms only expected Phase 42 implementation/test changes during execution.
</verification>

<success_criteria>
- PNGI-02: compatible Adam7 RGB8/RGBA8 traversal uses canonical checked geometry, pass-local filtering, exact bounded planner input, and no pass/image staging or cached pass geometry.
- PNGI-03: Stored, FixedOrStored, and DynamicOrFixedOrStored preserve one atomic preflight charge and acknowledgement-only replay advancement for eager and chunk output.
- Phase 41 None routes preserve frozen bytes; Phase 43 generated fidelity and independent portable public evidence are not introduced here.
</success_criteria>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Deterministic bounded Adam7 passes with atomic acknowledgement-safe replay | 42-01 | COVERED | Tasks 1–3 create the source, replay/framing, and public boundary tests.
REQ | PNGI-02 | Seven deterministic bounded pass geometry, filtering, and compression input without image-sized staging | 42-01 | COVERED | Task 1 uses only `_png_adam7_passes`, one logical source, scalar cursor state, and white-box tests.
REQ | PNGI-03 | Atomic capability/geometry/output/work/budget admission and accepted-byte-only replay for all strategies | 42-01 | COVERED | Tasks 1–2 retain the ledger and acknowledgement discipline with eager/chunk tests.
RESEARCH | R-01 | Canonical checked geometry authority | 42-01 | COVERED | Task 1 uses `_png_adam7_passes` exclusively.
RESEARCH | R-02 | Pass-local method-0 predictor history | 42-01 | COVERED | Task 1 resets row/filter state at every pass boundary.
RESEARCH | R-03 | Shared Stored/Fixed/Dynamic planner and replay source | 42-01 | COVERED | Tasks 1–2 route all strategies through fresh bounded cursors.
RESEARCH | R-04 | Existing exact atomic ledger and one budget charge | 42-01 | COVERED | Task 1 carries actual pass traversals into preflight; Task 2 asserts public atomicity.
RESEARCH | R-05 | Method-1 IHDR and acknowledgement-safe CRC/replay | 42-01 | COVERED | Task 2 changes framing and tests only accepted-byte commits.
CONTEXT | C-01 | `_png_adam7_passes` is the only checked geometry authority | 42-01 | COVERED | Task 1.
CONTEXT | C-02 | One deterministic source with pass-local filter history | 42-01 | COVERED | Task 1.
CONTEXT | C-03 | All planners/replay consume it with no pass/image cache or staging | 42-01 | COVERED | Tasks 1–2.
CONTEXT | C-04 | Exact pass traversal extends atomic preflight ledger | 42-01 | COVERED | Task 1.
CONTEXT | C-05 | IHDR uses method 1 only for actual Adam7; legacy None remains method 0 | 42-01 | COVERED | Tasks 2–3.
CONTEXT | C-06 | Public factories unchanged; compatible Adam7 becomes emission | 42-01 | COVERED | Tasks 2–3.

<output>
Create `.planning/phases/42-bounded-adam7-pass-encoding/42-01-SUMMARY.md` when done.
</output>
