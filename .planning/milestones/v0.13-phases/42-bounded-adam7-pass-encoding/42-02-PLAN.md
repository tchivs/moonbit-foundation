---
phase: 42-bounded-adam7-pass-encoding
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
gap_closure: true
requirements: [PNGI-02, PNGI-03]
must_haves:
  truths:
    - "A 5x5 RGB8 and straight-RGBA8 Adam7 cursor test observes every canonical nonempty pass in actual filter-tag-plus-sample order, while a 1x1 source emits only its sole nonempty pass."
    - "Stored, FixedOrStored, and DynamicOrFixedOrStored Adam7 eager and caller-buffered constructors reject capability, geometry, output, work, and budget failures atomically, without eager output, a chunk encoder, lease mutation, or budget mutation."
    - "A multi-pass Adam7 caller-buffered stream does not commit a preview before acknowledgement, makes progress only for accepted bytes under zero and tiny capacities, and has a non-mutating sticky terminal result."
  artifacts:
    - path: modules/mb-image/png/encode_wbtest.mbt
      provides: "Literal RGB8/RGBA8 5x5 Adam7 filtered-source order and 1x1 empty-pass omission regressions."
    - path: modules/mb-image/png/encode_test.mbt
      provides: "Eager Adam7 atomic-admission regressions across all compression strategies."
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Caller-buffered Adam7 atomic-admission, preview, hostile-capacity, accepted-progress, and terminal regressions."
  key_links:
    - from: modules/mb-image/png/encode_wbtest.mbt
      to: modules/mb-image/png/encode.mbt
      via: "A real PngFilteredCursor built with Adam7 emits source bytes derived from the implementation's sole checked pass geometry authority."
      pattern: "PngFilteredCursor::new_with_interlace"
    - from: modules/mb-image/png/encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "Eager all-strategy Adam7 factories must finish preflight before their Writer is observable."
      pattern: "new_with_all_strategies"
    - from: modules/mb-image/png/stream_encode_test.mbt
      to: modules/mb-image/png/stream_encode.mbt
      via: "PngChunkEncoder pull results reflect acknowledgement-gated machine successor commits."
      pattern: "PngChunkEncoder::new_with_all_strategies"
---

<objective>
Close the Phase 42 verification gaps by proving the already-implemented bounded Adam7 pass source, atomic admission boundary, and acknowledgement-safe replay with targeted native tests.

Purpose: Make PNGI-02 and PNGI-03 behaviorally verified without changing implementation, public factories, generated public fidelity corpus, or portable evidence runner.
Output: Exact private pass-stream assertions plus eager and caller-buffered Adam7 admission/replay regressions.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-CONTEXT.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-RESEARCH.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-01-SUMMARY.md
@.planning/phases/42-bounded-adam7-pass-encoding/42-VERIFICATION.md

<interfaces>
From modules/mb-image/png/encode.mbt:
- `PngFilteredCursor::new_with_interlace` produces the private logical filtered source; `PngInterlaceStrategy::Adam7` obtains pass facts through `_png_adam7_passes`.
- `_png_encode_preflight_with_interlace` resolves all strategies and charges the caller budget only after admission succeeds.

From modules/mb-image/png/stream_encode.mbt:
- `PngEncodeMachine::present` previews the next byte and `acknowledge` is the only successor-state commit.
- `PngChunkEncoder::new_with_all_strategies` constructs only after preflight; `pull` reports `written`, `total_written`, and a sticky terminal outcome.

Existing test helpers:
- `png_wb_same_remaining` and `png_fixed_or_stored_same_remaining` compare every `ResourceLimits` field.
- `png_chunk_test_owner`, `png_chunk_test_pull`, and hostile-drain helpers permit byte-by-byte lease and terminal assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert real multi-pass filtered bytes and empty-pass omission</name>
  <files>modules/mb-image/png/encode_wbtest.mbt</files>
  <read_first>
    - modules/mb-image/png/encode_wbtest.mbt: current Adam7 cursor and work-bound tests
    - modules/mb-image/png/encode.mbt: `PngFilteredCursor::new_with_interlace` and `next`
    - modules/mb-image/png/structural.mbt: `_png_adam7_passes`
  </read_first>
  <action>
    Replace the structural-only `PNG Adam7 filtered cursor emits canonical pass order and omits empties` assertion with actual cursor consumption per C-01 and C-02: construct deterministic 5x5 RGB8 and straight-RGBA8 images whose byte at `(x, y, channel)` is `y * channels * 5 + x * channels + channel`, use `PngFilterStrategy::None` with `PngInterlaceStrategy::Adam7`, consume the cursor through completion, and compare the complete result with a literal expected byte array.

    The RGB8 expected pass rows are tag 0 followed by: pass 1 `[0,1,2]`; pass 2 `[12,13,14]`; pass 3 `[60,61,62,72,73,74]`; pass 4 rows `[6,7,8]`, `[66,67,68]`; pass 5 `[30,31,32,36,37,38,42,43,44]`; pass 6 rows `[3,4,5,9,10,11]`, `[33,34,35,39,40,41]`, `[63,64,65,69,70,71]`; and pass 7 rows `[15..29]`, `[45..59]`. The RGBA8 expected pass rows use the corresponding four-channel ranges: pass 1 `[0..3]`; pass 2 `[16..19]`; pass 3 `[80..87]`, `[96..99]`; pass 4 `[8..11]`, `[88..91]`; pass 5 `[40..43,48..51,56..59]`; pass 6 `[4..7,12..15]`, `[44..47,52..55]`, `[84..87,92..95]`; and pass 7 `[20..39]`, `[60..79]`. Include one filter tag before each listed row in the literal array.

    Add a separate 1x1 RGB8/RGBA8 cursor drain that proves the result contains exactly one filter tag plus the source pixel bytes and then completes; this proves empty canonical passes produce neither tag nor sample bytes. Retain the existing exact/one-less work test as complementary ledger coverage; do not alter encoder implementation or duplicate Adam7 geometry formulas.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter "PNG Adam7" --no-parallelize</automated>
  </verify>
  <done>Targeted white-box tests observe the literal seven-pass 5x5 source stream for both supported profiles and prove 1x1 omission of empty passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Adam7 admission and acknowledgement-safe caller-buffered replay</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - modules/mb-image/png/encode_test.mbt: eager encoder options, writer, and existing Adam7 boundary test
    - modules/mb-image/png/stream_encode_test.mbt: Adam7 chunk test, public admission helpers, machine-preview tests, and hostile drain helpers
    - modules/mb-image/png/stream_encode.mbt: all-strategy factories, `present`, `acknowledge`, and `pull`
  </read_first>
  <action>
    Add a shared deterministic 5x5 RGB8/RGBA8 Adam7 image helper in the appropriate test files, reusing existing package-local image/budget/limit helpers and the existing complete `ResourceLimits` equality helpers. For each of Stored, FixedOrStored, and DynamicOrFixedOrStored, exercise the all-strategy Adam7 eager and chunk constructors against five isolated failures: incompatible capability (opaque metadata), geometry below the 5x5 image, output below the complete PNG, codec work below required work, and caller budget work below required work. For eager construction, assert the Writer position is zero and every budget field equals its pre-call snapshot. For chunk construction, assert factory failure returns no encoder, a preallocated sentinel lease remains unchanged because no pull can occur, and every budget field equals its pre-call snapshot. Compare eager and chunk errors using the existing error comparison helper. Keep tests limited to native package regressions; do not add generated corpus cases, public decoding assertions, a cross-target command, or runner changes.

    Replace the 1x1-only Adam7 chunk parity coverage with a 5x5 Adam7 replay helper for both RGB8 and straight-RGBA8 and all three compression strategies. Create a private `PngEncodeMachine` with the all-strategy Adam7 selection, advance acknowledged framing bytes until DEFLATE payload production, call `present` repeatedly before an acknowledgement, and assert each preview is the same byte. Then acknowledge that byte once and compare the subsequent fully acknowledged machine output with an otherwise identical immediate-acknowledgement run, so the test detects any premature cursor, CRC, or Adler successor commit.

    Drain `PngChunkEncoder` for the same multi-pass inputs through an empty lease followed by one-byte and `[0, 1, 3, 2, 5]` capacities. At every pull, assert `written` does not exceed capacity and `total_written` increases only by `written`; collect only accepted lease bytes and require equality with the eager Adam7 bytes. After `Finished`, pull twice with sentinel-filled leases and assert zero written bytes, unchanged total, `Finished`, and unchanged sentinel bytes.
  </action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --no-parallelize</automated>
  </verify>
  <done>All strategy-specific Adam7 constructor failures are observably atomic, and multi-pass chunk replay proves preview stability, accepted-byte-only progress, eager identity, and sticky terminals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image and limits to encoder preflight | Caller-controlled capability, geometry, output, work, and budget facts must fail before output state exists. |
| Encode machine to caller lease | Previewed bytes cross a retry boundary and must commit only after acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-05 | Denial of service | Adam7 preflight ledger | high | mitigate | Exercise every strategy against capability, geometry, output, work, and budget failures while asserting zero eager output and unchanged complete budget snapshots. |
| T-42-06 | Tampering / denial of service | Adam7 filtered cursor | high | mitigate | Assert literal 5x5 RGB8/RGBA8 pass-filter byte order and 1x1 empty-pass omission against real cursor output. |
| T-42-07 | Tampering / denial of service | `PngEncodeMachine` preview and chunk pull | high | mitigate | Repeat pre-acknowledgement previews inside DEFLATE output, then prove accepted-byte-only totals, eager identity, and sentinel-preserving sticky terminals. |
</threat_model>

<verification>
- `moon -C modules/mb-image test png --target native --frozen --filter "PNG Adam7" --no-parallelize`
- `moon -C modules/mb-image test png --target native --frozen --no-parallelize`
- `git diff --check`
</verification>

<success_criteria>
- PNGI-02 is supported by executable literal pass-stream evidence for 5x5 RGB8/RGBA8 and exact empty-pass omission evidence for 1x1 sources.
- PNGI-03 is supported by an all-strategy eager/chunk failure matrix with zero output, no encoder/lease exposure, and unchanged budgets.
- PNGI-03 is supported by multi-pass pre-acknowledgement, hostile-capacity, accepted-progress, eager-identity, and sticky-terminal evidence.
- Phase 43 generated public fidelity, legacy corpus, independent portable execution, and runner scope remain absent.
</success_criteria>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Bounded deterministic Adam7 passes with atomic acknowledgement-safe replay | 42-01, 42-02 | COVERED | 42-01 implements the path; Tasks 1-2 close the three behavior-test gaps.
REQ | PNGI-02 | Deterministic bounded pass geometry, bytes, filtering, and compression input without staging | 42-01, 42-02 | COVERED | Task 1 observes literal pass-filter bytes through the existing cursor.
REQ | PNGI-03 | Atomic all-strategy admission and accepted-byte-only replay | 42-01, 42-02 | COVERED | Task 2 proves the public atomic matrix and replay state boundary.
RESEARCH | R-01 | `_png_adam7_passes` remains the geometry authority | 42-01, 42-02 | COVERED | Task 1 consumes its production cursor result without duplicating geometry.
RESEARCH | R-02 | Pass-local filter history | 42-01, 42-02 | COVERED | Task 1 asserts all pass rows and tags in actual order.
RESEARCH | R-03 | Shared Stored/Fixed/Dynamic source and replay | 42-01, 42-02 | COVERED | Task 2 covers all three strategies.
RESEARCH | R-04 | Exact atomic ledger and final budget charge | 42-01, 42-02 | COVERED | Task 2 snapshots every budget field around each rejected case.
RESEARCH | R-05 | Method-1 framing and acknowledgement-safe replay | 42-01, 42-02 | COVERED | Task 2 uses real Adam7 all-strategy factories and preview/acknowledgement evidence.
CONTEXT | C-01 | Sole checked pass geometry authority | 42-01, 42-02 | COVERED | Task 1 validates output through the authoritative production path.
CONTEXT | C-02 | One deterministic source with pass-local history | 42-01, 42-02 | COVERED | Task 1.
CONTEXT | C-03 | Shared planners/replay without staging | 42-01, 42-02 | COVERED | Task 2's all-strategy streams use the existing path.
CONTEXT | C-04 | Exact atomic preflight ledger | 42-01, 42-02 | COVERED | Task 2.
CONTEXT | C-05 | Adam7 IHDR method 1 and None method 0 | 42-01 | COVERED | Verified report confirms this behavior; no gap action is required.
CONTEXT | C-06 | Factories unchanged and compatible Adam7 emits | 42-01, 42-02 | COVERED | Task 2 exercises the retained all-strategy factories.

<output>
Create `.planning/phases/42-bounded-adam7-pass-encoding/42-02-SUMMARY.md` when done.
</output>
