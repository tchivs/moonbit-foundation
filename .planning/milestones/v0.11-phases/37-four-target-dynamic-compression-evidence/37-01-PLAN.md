---
phase: 37-four-target-dynamic-compression-evidence
plan: "01"
type: execute
wave: 1
depends_on: [36-01, 36-02]
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements: [PNGD-04]

must_haves:
  truths:
    - "A generated in-memory periodic five-symbol corpus supplies both RGB8 and straight-RGBA8 sources that are literal-heavy for the retained distance-1-through-4 matcher."
    - "For each declared corpus source, DynamicOrFixedOrStored emits a final Dynamic DEFLATE block, is never larger than unchanged FixedOrStored, and is strictly smaller than FixedOrStored."
    - "Repeated eager Dynamic encodes and a hostile caller-buffered Dynamic drain have byte-identical output for each corpus source."
    - "Every Dynamic eager and chunk corpus output complete-input decodes through PngDecoder with matching dimensions, channel count, and every source component on js, wasm, wasm-gc, and native."
  artifacts:
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: "Named public four-target Dynamic compression corpus, relative-size, BTYPE, eager/chunk determinism, and complete-decode evidence."
  key_links:
    - from: "png_stream_test_dynamic_corpus_case"
      to: "PngEncoder::new_with_compression_strategy and PngChunkEncoder::new_with_compression_strategy"
      via: "Public FixedOrStored and DynamicOrFixedOrStored factories encode the same generated image before eager/chunk byte comparisons."
      pattern: "DynamicOrFixedOrStored|FixedOrStored|new_with_compression_strategy"
    - from: "Dynamic eager and chunk Bytes"
      to: "PngDecoder::new"
      via: "Existing complete-input decoder/source oracle compares descriptor fields and every component."
      pattern: "require_complete_input=true|PngDecoder::new"
---

<objective>
Add reproducible, test-only four-target evidence for the existing strict Dynamic PNG route.

Purpose: Meet PNGD-04 with public API evidence that Dynamic is deterministic, wins the declared literal-heavy RGB8/RGBA8 corpus, and faithfully decodes without changing compression behavior.
Output: One named package test and its test-only helpers in the existing PNG stream-encode test file.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/v0.11-png-dynamic-huffman.md
@.planning/phases/36-bounded-dynamic-planning-and-replay/36-01-SUMMARY.md
@.planning/phases/36-bounded-dynamic-planning-and-replay/36-02-SUMMARY.md
@.planning/phases/36-bounded-dynamic-planning-and-replay/36-VERIFICATION.md
@.planning/milestones/v0.10-phases/34-portable-png-compression-corpus-evidence/34-01-PLAN.md
@.planning/milestones/v0.10-phases/34-portable-png-compression-corpus-evidence/34-01-SUMMARY.md
@modules/mb-image/png/stream_encode_test.mbt
</context>

<interfaces>
- `PngCompressionStrategy::{Stored, FixedOrStored, DynamicOrFixedOrStored}` is public. Only `DynamicOrFixedOrStored` may select a Dynamic representation; `FixedOrStored` is the unchanged comparison baseline.
- `png_stream_test_eager_with_strategy`, `PngChunkEncoder::new_with_compression_strategy`, and `png_chunk_test_drain_encoder` already provide public eager and caller-buffered evidence. The drain helper accepts hostile schedules including zero-capacity pulls.
- `png_stream_test_fixed_or_stored_corpus_decode_matches_source` already runs complete-input `PngDecoder` and compares descriptor dimensions, channel count, and every source component; retain it as the decode oracle rather than duplicating decoder policy.
- The first DEFLATE byte is the byte at complete PNG offset 43. A final Dynamic block has its low three bits set to the LSB-first `101` encoding.
</interfaces>

<source_audit>
SOURCE   | ID      | Feature / constraint | Plan | Status | Coverage
---------|---------|----------------------|------|--------|---------
GOAL     | —       | Reproducible portable Dynamic determinism, intended strict wins, and public decode fidelity. | 37-01 | COVERED | The sole test constructs two generated records, proves strict public selection, output parity, and component-level decode on every target.
REQ      | PNGD-04 | Generated literal-heavy RGB8/RGBA8 corpus proves BTYPE=10, strict Dynamic win over FixedOrStored, eager/chunk determinism, and complete decode. | 37-01 | COVERED | The named test invokes the public corpus case for both formats and the target-isolated verification executes it on all four targets.
RESEARCH | Phase 37 | Periodic five-symbol in-memory sources, explicit Stored/Fixed/Dynamic comparisons, hostile schedule, BTYPE, outline-before-filter, complete PNG suite. | 37-01 | COVERED | The task specifies the exact corpus, public comparisons, schedule, and execution protocol.
RESEARCH | Scope fences | Retain filter-None and matcher distances 1–4; exclude benchmarks, scripts, adaptive filtering, wider dictionaries, production/API/policy changes, and external dependencies. | 37-01 | COVERED | Only the existing MoonBit package test is modified; commands are verification only.
CONTEXT  | —       | No Phase 37 CONTEXT.md or locked D-XX decisions exist. | 37-01 | COVERED | ROADMAP.md, REQUIREMENTS.md, v0.11 research, and completed Phase 36 evidence govern the task.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add the public five-symbol Dynamic corpus and four-target evidence</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: A 128x1 generated periodic five-symbol source is available for RGB8 and straight-RGBA8; its component stream uses `((x * channels + channel) % 5)` so no repeated source component has a match distance from 1 through 4.
    - Test 2: For each source, public DynamicOrFixedOrStored eager output has final Dynamic block bits, is no larger than FixedOrStored, and is strictly smaller than FixedOrStored; explicit Stored output is also constructed as the third public comparison route.
    - Test 3: A second Dynamic eager encode and Dynamic PngChunkEncoder drained under `[0UL, 1UL, 3UL, 2UL, 5UL]` equal the first Dynamic eager bytes.
    - Test 4: The first eager Dynamic bytes, repeated eager Dynamic bytes, and chunk Dynamic bytes each complete-input decode with the existing descriptor-and-component oracle.
  </behavior>
  <action>Add test-only helpers beside the Phase 34 FixedOrStored corpus helpers. Create `png_stream_test_dynamic_corpus_image(channels : UInt64, width? : UInt64 = 128UL)` from `png_stream_test_image`; fill its one row with the periodic component formula described in the behavior so its flattened RGB8/RGBA8 stream cycles through exactly five byte values and remains literal-heavy for the existing matcher. Create `png_stream_test_dynamic_corpus_case(source)` that constructs explicit Stored, unchanged FixedOrStored, and two DynamicOrFixedOrStored eager outputs through `png_stream_test_eager_with_strategy`; construct Dynamic PngChunkEncoder output through `png_chunk_test_drain_encoder` under `[0UL, 1UL, 3UL, 2UL, 5UL]`. Assert Dynamic eager length is at most FixedOrStored length and strictly smaller, assert its byte at offset 43 has low bits equal to the final-Dynamic block value, and assert both repeated eager and chunk bytes exactly equal the first Dynamic eager bytes. Apply `png_stream_test_fixed_or_stored_corpus_decode_matches_source` to all three Dynamic products, retaining complete-input public decode and every-component comparisons. Add exactly one named test, `PNG dynamic corpus evidence is deterministic, strictly wins periodic RGB8/RGBA8, and decodes completely`, that calls the case helper once with `png_stream_test_dynamic_corpus_image(3UL)` and once with `png_stream_test_dynamic_corpus_image(4UL)`. Keep all existing FixedOrStored corpus helpers and existing compact RGB Dynamic tests unchanged. Per PNGD-04, edit no production source, public API, fixture, script, benchmark, quality lane, policy, CI, adaptive-filter, matcher, or dictionary code.</action>
  <verify>
    <automated>$filter = '*PNG dynamic corpus evidence*'; $testName = 'PNG dynamic corpus evidence is deterministic, strictly wins periodic RGB8/RGBA8, and decodes completely'; foreach ($target in @('js','wasm','wasm-gc','native')) { $outline = @(moon -C modules/mb-image test png --target $target --frozen --outline -f $filter 2>&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "PNG outline has no entries on $target" }; if (@($outline | Select-String -SimpleMatch $testName).Count -ne 1) { throw "outline did not name exactly one Dynamic corpus test for $target" }; moon -C modules/mb-image test png --target $target --target-dir "_build/png-dynamic-corpus/$target" --frozen -f $filter; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The one named public test covers generated 128x1 periodic five-symbol RGB8 and straight-RGBA8 records; both select a final Dynamic block that strictly beats FixedOrStored, repeat deterministically through eager/chunk adapters, and completely decode to every source component on every supported target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated in-memory image → public compression factories | The deterministic test corpus crosses Stored, FixedOrStored, and opt-in Dynamic encoder selection without accessing private plans. |
| Dynamic PNG bytes → public decoder | Dynamic eager and caller-buffered output is accepted as complete PNG input only after PngDecoder validates it. |
| Named selector → portable test runner | A target-specific filter decides whether the declared evidence executes on each supported target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01-01 | Tampering | Dynamic selection evidence | medium | mitigate | Compare complete public Dynamic and unchanged FixedOrStored PNG lengths with both non-larger and strict-win predicates, and require final Dynamic block bits. |
| T-37-01-02 | Tampering | Deterministic output evidence | medium | mitigate | Compare the first and second public eager Dynamic outputs with a zero/ragged caller-buffered Dynamic drain for each generated source. |
| T-37-01-03 | Tampering | Decode fidelity evidence | medium | mitigate | Complete-input decode every Dynamic eager/chunk result and compare dimensions, channel count, and each source component. |
| T-37-01-04 | Tampering | Four-target execution | medium | mitigate | Require the exact test name once in each target outline before a filtered run in a dedicated target directory, then run the full PNG suite across all targets. |
| T-37-01-SC | Tampering | Package/dependency supply chain | low | accept | The plan adds no package, dependency, download, generated file, or external service; it modifies only MoonBit tests. |
</threat_model>

<verification>
- The target-isolated outline-and-filter command in Task 1 is required evidence; an empty selector is a failure, not a passing run.
- Run `moon -C modules/mb-image test png --target all --frozen` after the named test passes to preserve the existing PNG regression suite on every supported target.
- Do not run or add a benchmark, quality-lane stage, fixture generator, or standalone runner for this phase.
</verification>

<success_criteria>
1. The existing package test source contains one generated 128x1 periodic five-symbol RGB8 record and one straight-RGBA8 record; both are in-memory and keep the established matcher stimulus literal-heavy.
2. For both records, DynamicOrFixedOrStored uses the final Dynamic block encoding, is no larger than FixedOrStored, and strictly smaller than unchanged FixedOrStored.
3. Repeated eager and hostile `[0, 1, 3, 2, 5]` caller-buffered Dynamic outputs are byte-identical, and every eager/chunk product complete-input decodes to the full generated image.
4. The named test appears exactly once in target outlines and passes from isolated directories on js, wasm, wasm-gc, and native; the full PNG package suite passes across all four targets.
</success_criteria>

<output>
Create `.planning/phases/37-four-target-dynamic-compression-evidence/37-01-SUMMARY.md` when done.
</output>
