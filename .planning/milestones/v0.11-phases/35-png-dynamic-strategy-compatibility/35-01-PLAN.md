---
phase: 35-png-dynamic-strategy-compatibility
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
  - policy/foundation.json
autonomous: true
requirements: [PNGD-01]
must_haves:
  truths:
    - "A library user can pass PngCompressionStrategy::DynamicOrFixedOrStored to the documented eager and caller-buffered PNG factories."
    - "PngEncoder::new() and PngChunkEncoder::new(...) retain their independently frozen complete Stored RGB8 and straight-RGBA8 PNG bytes."
    - "Explicit FixedOrStored eager and caller-buffered factories retain their frozen complete byte sequences, and only DynamicOrFixedOrStored is eligible to select a Dynamic DEFLATE representation."
    - "Public documentation defines DynamicOrFixedOrStored as strict-complete-PNG-size-win selection over unchanged FixedOrStored output and names the compression scope exclusions."
  artifacts:
    - path: modules/mb-image/png/png.mbt
      provides: Additive public DynamicOrFixedOrStored strategy case and exact eager factory contract documentation.
    - path: modules/mb-image/png/encode.mbt
      provides: Exhaustive strategy admission that preserves the Stored and FixedOrStored plan-selection paths.
    - path: modules/mb-image/png/stream_encode.mbt
      provides: Caller-buffered factory documentation and shared strategy-aware machine construction.
    - path: modules/mb-image/png/encode_test.mbt
      provides: Public eager compatibility vectors for Stored, FixedOrStored, and DynamicOrFixedOrStored.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Caller-buffered compatibility vectors under a hostile-capacity schedule.
    - path: policy/foundation.json
      provides: Updated exact PNG semantic-interface registration for the additive enum case.
  key_links:
    - from: modules/mb-image/png/png.mbt
      to: modules/mb-image/png/encode.mbt
      via: PngEncoder retains the caller-selected PngCompressionStrategy and ImageEncoder::encode supplies it to _png_encode_preflight.
      pattern: PngCompressionStrategy|new_with_compression_strategy|_png_encode_preflight
    - from: modules/mb-image/png/stream_encode.mbt
      to: modules/mb-image/png/stream_encode_test.mbt
      via: PngChunkEncoder::new_with_compression_strategy passes the selection into the shared PngEncodeMachine and tests drain accepted caller-lease prefixes only.
      pattern: PngChunkEncoder::new_with_compression_strategy|PngEncodeMachine::new_with_compression_strategy|png_chunk_test_drain_encoder
    - from: modules/mb-image/png/png.mbt
      to: policy/foundation.json
      via: The additive public enum variant is regenerated and exact-compared by the PNG semantic-interface policy gate on every declared target.
      pattern: PngCompressionStrategy|semantic_interface
---

<objective>
Add the public DynamicOrFixedOrStored compatibility contract without changing the frozen Stored or FixedOrStored eager and caller-buffered PNG representations. PNGD-01.

Purpose: Reserve a clear opt-in route for bounded Dynamic selection while preserving every established constructor and configured FixedOrStored result as a byte-level compatibility baseline.
Output: A documented additive enum case, exhaustive shared construction routing, frozen eager/chunk vectors, and exact public-interface policy registration.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/v0.11-png-dynamic-huffman.md
@.planning/milestones/v0.10-phases/32-png-compression-strategy-and-compatibility/32-01-SUMMARY.md
@.planning/milestones/v0.10-phases/33-fixed-or-stored-png-planning-and-emission/33-02-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt
@policy/foundation.json

<interfaces>
From modules/mb-image/png/png.mbt:
- `pub(all) enum PngCompressionStrategy { Stored; FixedOrStored } derive(Eq)` is the additive public selection type.
- `PngEncoder::new_with_compression_strategy(strategy)` retains the selection for `ImageEncoder::encode`.
- `PngChunkEncoder::new_with_compression_strategy(source, strategy, limits, budget, diagnostics)` is the public caller-buffered configured route.

From modules/mb-image/png/encode.mbt and stream_encode.mbt:
- `_png_encode_preflight(source, strategy, limits, budget)` is the single atomic admission point and returns a `PngDeflatePlan::{Stored, Fixed}`.
- `PngEncodeMachine::new_with_compression_strategy(...)` consumes that preflight; its `present`/`acknowledge` lifecycle owns exact output progress and sticky terminals.
</interfaces>
</context>

<feature>
  <name>Additive dynamic-strategy public contract with frozen compatibility routes</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt, policy/foundation.json</files>
  <behavior>
    - `PngCompressionStrategy::DynamicOrFixedOrStored` is public and equality-comparable; both public configured factories accept it for compatible RGB8 and straight-RGBA8 images.
    - `PngEncoder::new()` and `PngChunkEncoder::new(...)` independently retain their existing complete stored-DEFLATE byte vectors.
    - `PngEncoder::new_with_compression_strategy(FixedOrStored)` and `PngChunkEncoder::new_with_compression_strategy(..., FixedOrStored, ...)` independently retain exact pre-Phase-35 byte vectors for RGB8 and straight-RGBA8 fixtures.
    - The DynamicOrFixedOrStored selection shares the existing exact FixedOrStored candidate route in this compatibility phase; no Dynamic plan variant or block emitter is introduced here.
    - Documentation says a Dynamic representation is permitted only when a complete PNG is strictly smaller than unchanged FixedOrStored output; a tie retains FixedOrStored. It also excludes adaptive filtering, matching beyond distance four, 32 KiB history, image-sized staging, length-limited optimization, FFI, host streaming, APNG, colour, and metadata expansion.
  </behavior>
  <implementation>Preserve the existing Stored branch byte-for-byte. Retain the current FixedOrStored planning branch as its own branch and route DynamicOrFixedOrStored to that same selected Fixed-or-Stored plan so the new public route is valid through both factories without widening the private emitter. Do not introduce PngDeflatePlan::Dynamic, dynamic tree construction, dynamic DEFLATE emission, broader matching, altered zlib/PNG framing, changed CRC/Adler accounting, new preflight/budget paths, or changed present/acknowledge semantics. The Phase 36 dynamic implementation is constrained by this contract: it may change the DynamicOrFixedOrStored branch only, and only on the documented strict complete-PNG win.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing eager and caller-buffered compatibility-vector tests</name>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - Test 1: `PNG dynamic strategy eager factory freezes compatibility routes` constructs RGB8 and straight-RGBA8 images with Stored, FixedOrStored, and the missing DynamicOrFixedOrStored case; legacy Stored and explicit FixedOrStored output each equal their own exact pre-change byte literals, and DynamicOrFixedOrStored equals the frozen FixedOrStored bytes for this phase's compatibility route.
    - Test 2: `PNG dynamic strategy chunk factory freezes compatibility routes` drains the same three strategies through `[0, 1, 3, 2, 5]`; legacy Stored and explicit FixedOrStored each equal their own exact pre-change byte literals, and DynamicOrFixedOrStored equals the frozen FixedOrStored bytes.
    - Test 3: The explicit FixedOrStored vectors retain their present Fixed DEFLATE block selection for the existing repetitive fixture, proving that the compatibility oracle is not a Stored-only assertion.
  </behavior>
  <action>Start with RED in the existing black-box PNG test files. Before production edits, capture the complete current FixedOrStored byte sequences for the existing compact repetitive RGB8 and straight-RGBA8 fixtures and encode them as immutable `Bytes` literals in the tests; use those literals as the FixedOrStored oracle, never a result computed by another strategy at assertion time. Retain the existing exact legacy Stored RGB8/RGBA8 literals as the independent Stored oracle. Add exactly the two named tests above. In `encode_test.mbt`, use only `PngEncoder`, `PngEncoder::new_with_compression_strategy`, and `ImageEncoder::encode`; assert every baseline and decode the DynamicOrFixedOrStored result through public `PngDecoder` with complete-input required. In `stream_encode_test.mbt`, use `PngChunkEncoder::new_with_compression_strategy` and `png_chunk_test_drain_encoder` with `[0UL, 1UL, 3UL, 2UL, 5UL]`, copying only `[0, written)` lease bytes; assert the two old baselines and DynamicOrFixedOrStored equality to the frozen FixedOrStored vector. Require the repetitive FixedOrStored vector's first DEFLATE byte to retain the existing final fixed-block bits. Do not change existing vector meanings, change a legacy constructor to delegate through a configurable default, add corpus win measurements, inspect private machine state, or assert a Dynamic DEFLATE block in this phase. Run the focused native selector and confirm RED fails because DynamicOrFixedOrStored is absent; commit the test stage as `test(35-01): add failing dynamic strategy compatibility tests`.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*dynamic strategy*'  # expected to fail in RED before DynamicOrFixedOrStored exists</automated>
  </verify>
  <done>Two public tests fail solely because the new enum case is missing, while they already lock independent complete byte vectors for legacy Stored and configured FixedOrStored eager/chunk output on RGB8 and straight-RGBA8 sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the additive strategy case and regenerate its public contract</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, policy/foundation.json</files>
  <behavior>
    - Test 1: The two configured public factories compile and finish successfully with DynamicOrFixedOrStored for the supported RGB8 and straight-RGBA8 test images.
    - Test 2: Stored constructors and FixedOrStored factories satisfy the exact independent eager/chunk compatibility vectors from Task 1.
    - Test 3: DynamicOrFixedOrStored currently selects the unchanged FixedOrStored plan and output path, leaving preflight, output acknowledgement, exact progress, and sticky terminal behavior unchanged.
    - Test 4: The generated PNG interface exposes the new enum case exactly and continues to match its semantic-interface policy on all four targets.
  </behavior>
  <action>Add exactly `DynamicOrFixedOrStored` to `PngCompressionStrategy` in `png.mbt` and retain `derive(Eq)`. Revise the enum, eager configured-factory, caller-buffered configured-factory, and generic PngEncoder documentation: specify that Stored and FixedOrStored are frozen compatibility routes; specify that DynamicOrFixedOrStored may select Dynamic only for a strict complete-PNG size win over unchanged FixedOrStored output and retains FixedOrStored on equal size; state all phase scope exclusions from the feature contract. Make `_png_encode_preflight` exhaustive by routing DynamicOrFixedOrStored through the present FixedOrStored candidate-selection logic, retaining the Fixed-versus-Stored tie rule and all existing admission, charge, framing, checksum, progress, and terminal paths. Keep the legacy constructors as direct Stored selections and do not alter the existing configured factory signatures. Regenerate the public PNG interface with the repository workflow, then update only the PNG `semantic_interface` registration in `policy/foundation.json` so it contains the additive enum case and preserves every other declaration, source inventory, import allowlist, target declaration, and policy entry. Make the RED selector pass before the complete verification sequence; commit the implementation as `feat(35-01): add dynamic PNG strategy compatibility contract`.</action>
  <verify>
    <automated>$required = @('PNG dynamic strategy eager factory freezes compatibility routes','PNG dynamic strategy chunk factory freezes compatibility routes'); foreach ($target in @('js','wasm','wasm-gc','native')) { $outline = @(moon -C modules/mb-image test png --target $target --frozen --outline 2&gt;&amp;1); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($outline -match 'Warning: no test entry found') { throw "focused PNG outline has no test entry on $target" }; foreach ($name in $required) { if (-not ($outline -match [regex]::Escape($name))) { throw "missing required PNG test on ${target}: $name" } }; moon -C modules/mb-image test png --target $target --frozen -f '*dynamic strategy*'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; moon -C modules/mb-image test png --target all --frozen; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>DynamicOrFixedOrStored is an additive public configured choice on eager and caller-buffered encoders; every legacy Stored and FixedOrStored compatibility literal remains unchanged; the exact semantic interface passes the PNG policy gate and named tests run on js, wasm, wasm-gc, and native.</done>
</task>

</tasks>

<scope_fences>
- This phase owns only public strategy compatibility, contract documentation, exhaustive shared construction, compatibility vectors, and semantic-interface registration.
- Keep filter-None scanlines, the distance-1-through-4 matcher, one IDAT, zlib framing, CRC-32, Adler-32, preflight admission, caller-lease ownership, acknowledgement-only commits, exact progress, and sticky terminals unchanged.
- Exclude Dynamic planning/tree construction/header RLE/replay emission, adaptive filtering, broader matching, a 32 KiB dictionary, image-sized token/output/history retention, length-limited optimization, FFI, host adapters, dependencies, CI/release/registry work, APNG, colour work, metadata expansion, corpus win claims, and benchmark reporting.
</scope_fences>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library caller → configured eager/chunk factories | Caller-controlled strategy must enter the existing bounded admission path without changing legacy constructor selection. |
| Strategy selection → exact preflight plan | A new enum case must not bypass capability, geometry, output, work, or budget validation, nor alter an old plan's bytes. |
| Public API source → semantic-interface policy | The additive public enum case must be accurately represented in the cross-target compatibility contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Tampering | Stored eager and caller-buffered constructors | high | mitigate | Task 1 retains independent complete RGB8/RGBA8 byte literals for each legacy route; Task 2 keeps both constructors as direct Stored selections. |
| T-35-02 | Tampering | FixedOrStored configured eager and caller-buffered routes | high | mitigate | Task 1 freezes complete pre-change FixedOrStored vectors under eager and hostile-capacity chunk schedules; Task 2 preserves its candidate-selection branch. |
| T-35-03 | Spoofing | DynamicOrFixedOrStored public documentation and policy interface | medium | mitigate | Task 2 specifies strict complete-PNG winner/tie semantics and exclusions, regenerates the interface, and runs the PNG semantic-interface gate. |
| T-35-04 | Denial of service | DynamicOrFixedOrStored preflight entry | high | mitigate | Task 2 routes the new strategy through the existing authoritative FixedOrStored admission and the all-target package suite retains resource and terminal regressions. |
</threat_model>

<verification>
- RED: `moon -C modules/mb-image test png --target native --frozen -f '*dynamic strategy*'` fails for the missing public enum case before implementation.
- Focused GREEN: the automated Task 2 PowerShell gate proves both exact named tests are present in the outline and executes them separately on js, wasm, wasm-gc, and native.
- Regression GREEN: `moon -C modules/mb-image test png --target all --frozen` and `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png` pass after focused target evidence.
- TDD history: `test(35-01): ...` precedes `feat(35-01): ...`; add a refactor commit only for behavior-preserving cleanup.
</verification>

<success_criteria>
- The sole additive public compression surface is `PngCompressionStrategy::DynamicOrFixedOrStored`; existing factory signatures and legacy direct Stored selections are unchanged.
- Stored and FixedOrStored each retain independent complete frozen RGB8/RGBA8 vectors through eager and caller-buffered APIs; DynamicOrFixedOrStored is the only route eligible for a Dynamic block.
- Public documentation states strict complete-PNG winning selection, retains FixedOrStored on ties, and declares all phase compression and ecosystem fences.
- The focused compatibility tests have fail-closed outline evidence and pass on js, wasm, wasm-gc, and native; the complete package and PNG quality lane remain green.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Explicit dynamic route while Stored defaults and FixedOrStored byte sequences remain frozen | 35-01 | COVERED | Tasks 1-2 add only the enum case and lock both existing routes with independent eager/chunk byte vectors. |
| REQ | PNGD-01 | DynamicOrFixedOrStored is opt-in; legacy Stored and FixedOrStored retain frozen bytes; only the new route can select Dynamic | 35-01 | COVERED | Feature behavior and both tasks cover public construction, exhaustive routing, exact vectors, and route ownership. |
| RESEARCH | — | Add enum case and use existing configured eager/chunk factory seam | 35-01 | COVERED | Task 2 changes the enum and shared preflight branch without adding a second formatter or construction path. |
| RESEARCH | — | Strict complete-PNG win/tie policy and named dynamic-scope exclusions in public documentation | 35-01 | COVERED | Task 2 requires each relevant public docstring to state the policy and exclusions. |
| RESEARCH | — | Preserve atomic preflight, acknowledgement-safe machine, one IDAT, zlib/CRC/Adler, and portable test pattern | 35-01 | COVERED | Task 2 retains existing routing; verification uses outline-guarded isolated target runs plus full package and quality gates. |
| RESEARCH | — | Dynamic plan/tree/header/emission and corpus proof are later phase responsibilities | 35-01 | COVERED | Feature behavior and scope fences exclude them without weakening the public contract. |
| CONTEXT | — | No Phase 35 CONTEXT.md or D-NN locked decisions exist | 35-01 | COVERED | ROADMAP, REQUIREMENTS, archived v0.10 results, and v0.11 research control the plan. |

<output>
Create `.planning/phases/35-png-dynamic-strategy-compatibility/35-01-SUMMARY.md` when done.
</output>
