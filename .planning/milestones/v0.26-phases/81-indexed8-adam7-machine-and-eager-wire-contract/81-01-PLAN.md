---
phase: 81-indexed8-adam7-machine-and-eager-wire-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode.mbt
  - modules/mb-image/png/stream_encode.mbt
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/encode_wbtest.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - INDEXADAM7-01
  - INDEXADAM7-02
  - INDEXADAM7-03
  - INDEXADAM7-04
must_haves:
  truths:
    - "Users can select Type-3/8 Adam7 through additive eager and thin caller-buffered Indexed8 APIs while encode_indexed8 and new_indexed8 retain their non-interlaced signatures and frozen bytes."
    - "One acknowledged PngEncodeMachine derives Indexed8 Adam7 pass rows from _png_adam7_passes(width, height, 1UL, 8), emits a filter-None byte per pass row, and scalar-reads PngIndexedImage indices without staging."
    - "Indexed8 Adam7 preserves IHDR → PLTE → optional shortest canonical tRNS → IDAT → IEND, valid CRCs, the independent seven-pass raw raster, and public RGB8/RGBA8 palette decode."
    - "Selected Adam7 frame/work/output facts are checked and charged once before output; exact limits pass and one-less failures leave eager writer and budget state unchanged."
    - "Indexed1/2/4 remain non-interlaced and the existing opaque/transparent Indexed8 literal vectors remain unchanged."
  artifacts:
    - "modules/mb-image/png/encode.mbt contains additive Indexed8 interlace selection and interlace-aware indexed preflight."
    - "modules/mb-image/png/stream_encode.mbt contains the thin Indexed8 interlace selector plus scalar Adam7 scanline emission in PngEncodeMachine."
    - "modules/mb-image/png/encode_test.mbt owns the independent 5×5 wire/frame/decode/legacy tracer, while stream_encode_test.mbt proves the new chunk selector actually emits Adam7 IHDR wiring."
    - "modules/mb-image/png/encode_wbtest.mbt owns exact indexed Adam7 preflight and atomic accounting facts."
  key_links:
    - "Both public Indexed8 Adam7 selectors construct PngEncodeMachine::new_with_indexed_profile(source, Eight, interlace_strategy, ...); no alternate eager or chunk encoder is introduced."
    - "Indexed Adam7 preflight and scalar emission both consume _png_adam7_passes(..., 1UL, 8), so Stored-IDAT/frame facts and emitted pass coordinates cannot use separate geometry."
    - "The legacy fixed-Eight wrappers explicitly forward PngInterlaceStrategy::None, preserving Type-3/8 compatibility while Indexed1/2/4 stay on their current non-interlaced path."
---

<objective>
Deliver the narrow Type-3/8 Indexed8 Adam7 capability in the existing acknowledged PNG machine, with independent eager wire/resource evidence and frozen non-interlaced compatibility.

Purpose: Let callers opt into Adam7 for an existing immutable `PngIndexedImage` without widening the image model, introducing a second encoder, or weakening bounded atomic admission.

Output: Additive eager and thin chunk selector seams; one interlace-aware indexed preflight/traversal path; independent wire/decode/atomicity tests; retained Indexed8 and low-bit compatibility evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-indexed8-adam7-machine-and-eager-wire-contract/81-CONTEXT.md
@.planning/phases/81-indexed8-adam7-machine-and-eager-wire-contract/81-RESEARCH.md
@.planning/research/v026-ADAM7-SUMMARY.md
@modules/mb-image/png/encode.mbt
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/encode_wbtest.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the independent failing Indexed8 Adam7 wire, resource, and legacy tracer</name>
  <precondition>The PNG package already has immutable PngIndexedImage, stored Type-3/8 framing, _png_adam7_passes, public PngDecoder, and frozen Indexed8/low-bit tests; it has no Indexed8 Adam7 selector or indexed pass traversal.</precondition>
  <files>modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt, modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - RED: add a non-symmetric transparent 5×5 PngIndexedImage and call the proposed PngEncoder::encode_indexed8_with_interlace_strategy(..., PngInterlaceStrategy::Adam7, ...). The test must initially fail because the selector and traversal do not exist.
    - Define the fixture completely in test code: indices by rows are `00 01 02 03 00 / 02 03 00 01 02 / 00 01 02 03 00 / 02 03 00 01 02 / 00 01 02 03 00` (the hand-authored function `(x + 2*y) mod 4`, not production traversal); four RGB palette entries are `12 34 56`, `A0 B0 C0`, `D0 E0 F0`, `21 43 65`; alpha bytes are `00 80 7F FF`. Therefore PLTE is exactly 12 bytes and canonical tRNS is exactly the first three bytes `00 80 7F`.
    - State the complete pass coordinate/index oracle literally, without _png_adam7_passes, a production cursor, or scanline_byte: P1 `(0,0):00`; P2 `(4,0):00`; P3 `(0,4),(4,4):00 00`; P4 `(2,0),(2,4):02 / 02`; P5 `(0,2),(2,2),(4,2):00 02 00`; P6 rows `(1,0),(3,0):01 03`, `(1,2),(3,2):01 03`, `(1,4),(3,4):01 03`; P7 rows `(0..4,1):02 03 00 01 02`, `(0..4,3):02 03 00 01 02`. The exact 36-byte inflated Stored/None sequence, grouped by pass so its count is executable, is `P1: 00 00 | P2: 00 00 | P3: 00 00 00 | P4: 00 02 00 02 | P5: 00 00 02 00 | P6: 00 01 03 00 01 03 00 01 03 | P7: 00 02 03 00 01 02 00 02 03 00 01 02`; every row begins with its literal `00` filter tag.
    - Parse the eager chunks with existing test-local helpers and assert IHDR `05 05 08 03 00 00 01`, PLTE length 12, tRNS length 3/bytes `00 80 7F`, one 47-byte IDAT, IEND, total PNG length 143, chunk order, and each CRC.
    - Decode the generated transparent bytes through public PngDecoder and assert all 25 RGBA8 pixels; add an opaque peer that omits tRNS and decodes every pixel as RGB8. Retain/re-run the existing 89-byte opaque and 112-byte transparent non-interlaced Indexed8 literals and Indexed1/2/4 literals unchanged.
    - Add white-box expectations for the same fixture: Indexed8/Adam7 scanlines 36, one Stored block, IDAT length 47, total frame length 143, selected work equal to the planned frame length, exact work admission success, and one-less work/output failure with no budget mutation or eager writer bytes.
    - Add the minimal non-hostile chunk selector proof: construct `PngChunkEncoder::new_indexed8_with_interlace_strategy(..., Adam7, ...)`, drain it through one ordinary sufficient-capacity lease, and parse its IHDR to assert interlace byte `01` (and Type-3/8 fields). Do not add zero/one/ragged schedules, tail sentinels, released leases, accepted-only progress, or sticky-terminal coverage here; those remain Phase 82.
  </behavior>
  <action>Write the tests before production edits. Keep the stated palette, alpha table, coordinate table, and raw pass bytes literal/test-local; do not derive any expected data from production pass helpers. Reuse the existing Indexed test-local chunk parser, CRC, Stored-IDAT inflater, public decoder assertions, MemoryWriter position checks, complete budget snapshot helpers, and ordinary chunk drain shape. The chunk smoke test proves public selector-to-machine IHDR wiring only; Phase 82 exclusively owns hostile lease/tail/progress/sticky-terminal qualification. Do not add low-bit Adam7 cases, filter/compression strategy cases, generic image-model tests, staging tests, or release automation.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The focused package run is red for the missing Indexed8 Adam7 entry point/facts, while the test independently specifies a self-consistent four-entry/three-byte-tRNS seven-pass wire contract, public decode, atomic resource behavior, minimal chunk-selector Adam7 wiring, and frozen legacy output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route Indexed8 Adam7 through the sole bounded machine and satisfy the tracer</name>
  <files>modules/mb-image/png/encode.mbt, modules/mb-image/png/stream_encode.mbt, modules/mb-image/png/encode_test.mbt, modules/mb-image/png/encode_wbtest.mbt</files>
  <action>Implement only the tracer contract. In encode.mbt, add PngEncoder::encode_indexed8_with_interlace_strategy(source, interlace_strategy, writer, limits, budget, diagnostics); make encode_indexed8 a compatibility wrapper that explicitly selects PngInterlaceStrategy::None. Thread interlace strategy through _png_encode_indexed_preflight_with_profile and PngEncodeMachine::new_with_indexed_profile while retaining fixed-Eight wrapper entry points for legacy callers and retaining the selected low-bit One/Two/Four route at None. For Indexed8 Adam7, checked-sum each nonempty `_png_adam7_passes(width, height, 1UL, 8)` `(pass.row_bytes + 1) * pass.height` before Stored block/IDAT/frame/output/work calculations, all limits, and the one successful budget charge; retain normal `(row_bytes + 1) * height` facts exactly for None. Reuse PngFrameFacts and the existing canonical tRNS/CRC framing path.

In stream_encode.mbt, add PngChunkEncoder::new_indexed8_with_interlace_strategy as a thin constructor over the same indexed machine; make new_indexed8 explicitly forward None. Extend PngEncodeMachine::scanline_byte only for Indexed8 plus Adam7: obtain location from `_png_adam7_passes(width, height, 1UL, 8)` (factor a geometry-only helper shared with the existing ImageView cursor only when that avoids duplicate arithmetic), return `00` at each pass-row start, and otherwise read `indexed_source.index_at(pass.x + column * pass.dx, pass.y + pass_row * pass.dy)` using the actual PngAdam7Pass fields. Leave Indexed1/2/4 existing normal/packed emission, stored-only policy, present/acknowledge, CRC/Adler accounting, and all lease lifecycle mechanics intact. Do not allocate a pass/image/output buffer, create a second encoder, expose Adaptive/Fixed/Dynamic indexed variants, widen PngIndexedBitDepth, or alter decoder/source-model code.

Make the Task 1 tests pass without weakening their oracle. Confirm both legacy Indexed8 wrappers preserve their old signatures and exact bytes, and confirm atomic rejection occurs before writer progress or any caller budget-resource delta.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The independent eager tracer passes: Adam7 output has exact Type-3/8 framing, seven-pass raw raster, palette decode, and atomic selected-layout facts; legacy Indexed8 and selected low-bit paths remain non-interlaced and unchanged; eager and thin chunk selectors share one PngEncodeMachine construction seam.</done>
</task>

</tasks>

<verification>

1. Run `moon -C modules/mb-image test png --target native --frozen` after Task 1 to record the expected red missing-selector/fact failure, then again after Task 2 for the focused green loop.
2. Run `moon -C modules/mb-image test png --target all --frozen` after Task 2. This is the authoritative ordinary PNG package gate across native, js, wasm, and wasm-gc; Phase 82 still owns the hostile lifecycle and final qualification matrix.
3. Inspect the implementation seam: both public Indexed8 selector families must reach `PngEncodeMachine::new_with_indexed_profile(..., Eight, interlace_strategy, ...)`, while legacy `encode_indexed8` and `new_indexed8` explicitly supply `None`.
4. Confirm the test oracle is literal/test-local and does not call production `_png_adam7_passes`, location helpers, or `scanline_byte` to construct expected pass bytes.

</verification>

<success_criteria>

- **INDEXADAM7-01:** Additive eager and thin caller-buffered Indexed8 selectors accept Adam7; fixed legacy Indexed8 wrappers preserve their signatures and interlace-0 bytes.
- **INDEXADAM7-02:** Indexed8 Adam7 preflight and raster emission use checked shared seven-pass geometry and scalar immutable index reads only, with no second encoder or image/pass-sized staging.
- **INDEXADAM7-03:** Independent eager evidence proves Type-3/8 Adam7 IHDR, PLTE/canonical optional tRNS/chunk/CRC order, exact seven-pass payload, and public RGB8/RGBA8 decode.
- **INDEXADAM7-04:** Exact selected output/work admission succeeds; one-less output/work/budget failures are atomic before writer output and leave caller budget state unchanged.
- **Compatibility and boundary:** Existing Indexed8 opaque/transparent and Indexed1/2/4 non-interlaced vectors remain green. No low-bit Adam7, strategy expansion, generic model widening, staging, FFI, wrappers, copied trees, or release automation is added.

</success_criteria>

## Source Audit

| Source | ID | Plan coverage | Status |
|---|---|---|---|
| Requirements | INDEXADAM7-01 | Task 2 adds both additive selector seams and freezes legacy wrappers. | COVERED |
| Requirements | INDEXADAM7-02 | Task 2 uses shared pass geometry and scalar index reads without staging. | COVERED |
| Requirements | INDEXADAM7-03 | Task 1's independent wire/decode tracer; Task 2 makes it pass. | COVERED |
| Requirements | INDEXADAM7-04 | Task 1 exact/one-less resource tracer; Task 2 interlace-aware preflight. | COVERED |
| Context | D-01, D-02 | Additive Indexed8-only selection; low-bit Adam7 remains deferred. | COVERED |
| Context | D-03, D-04, D-05 | One machine, shared geometry, scalar pass rows, checked facts, and one charge. | COVERED |
| Context | D-06, D-07, D-08 | Stored/None framing, independent 5×5 oracle, and literal compatibility freezes. | COVERED |

<output>
Create `.planning/phases/81-indexed8-adam7-machine-and-eager-wire-contract/81-01-SUMMARY.md` when execution completes.
</output>
