---
phase: 82-indexed8-adam7-streaming-and-qualification
plan: 01
type: execute
wave: 1
depends_on:
  - 81-01
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - INDEXADAM7-05
  - INDEXADAM7-06
must_haves:
  truths:
    - "A fresh Indexed8 Adam7 chunk encoder produces exactly the bytes of a fresh eager Adam7 encoder under zero-capacity, one-byte, and ragged caller lease schedules; totals count accepted bytes only and unaccepted tails remain caller-owned."
    - "Finished and released-lease Failed outcomes are sticky zero-write outcomes that leave later sentinel-filled leases untouched."
    - "The successful drained chunk bytes—not just the eager parity oracle—independently prove Type-3/8 Adam7 IHDR, PLTE, canonical three-byte tRNS, CRCs, literal 36-byte seven-pass Stored raster, and public RGBA8 palette decode."
    - "The existing opaque/transparent Indexed8 and Indexed1/2/4 literal vectors remain unchanged, and the ordinary PNG package passes on wasm, wasm-gc, js, and native."
  artifacts:
    - "modules/mb-image/png/stream_encode_test.mbt contains the Indexed8 Adam7 hostile-drain, released-lease, chunk-origin parser, and public-decode qualification tests."
  key_links:
    - "Every Phase 82 encoder is constructed with PngChunkEncoder::new_indexed8_with_interlace_strategy(..., PngInterlaceStrategy::Adam7, ...), then exercised through the existing pull/lease lifecycle rather than a new transport."
    - "Each successful drain compares accepted bytes to a fresh eager Adam7 oracle and then parses/decompresses/decodes the collected chunk-origin bytes against Phase 81's literal fixture/oracle."
    - "No production file changes: Phase 81's PngEncodeMachine, preflight, selector, and traversal are the fixed subject under qualification."
---

<objective>
Qualify the completed Indexed8 Adam7 machine through real caller-owned leases and independent stream-origin evidence, without changing production code.

Purpose: Prove the thin caller-buffered facade preserves the acknowledged machine's bytes, ownership, progress, terminal behavior, framing, decode fidelity, frozen compatibility, and portability.

Output: Test-only hostile lifecycle and chunk-origin qualification in `stream_encode_test.mbt`, followed by the ordinary four-target PNG package gate.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-indexed8-adam7-streaming-and-qualification/82-CONTEXT.md
@.planning/phases/82-indexed8-adam7-streaming-and-qualification/82-RESEARCH.md
@.planning/phases/81-indexed8-adam7-machine-and-eager-wire-contract/81-CONTEXT.md
@.planning/phases/81-indexed8-adam7-machine-and-eager-wire-contract/81-VERIFICATION.md
@modules/mb-image/png/stream_encode.mbt
@modules/mb-image/png/stream_encode_test.mbt
@modules/mb-image/png/encode_test.mbt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the failing Indexed8 Adam7 hostile lifecycle and chunk-origin tracer</name>
  <precondition>Phase 81 has already exposed `PngChunkEncoder::new_indexed8_with_interlace_strategy(..., Adam7, ...)`, its literal 5×5 Indexed8 Adam7 fixture/raw oracle, and a sufficient-lease IHDR smoke test. No hostile lifecycle or drained-byte qualification exists yet.</precondition>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - RED: add a named test that calls a not-yet-defined test helper for the transparent Phase 81 5×5 fixture with schedules `[0UL, 1UL]`, `[1UL]`, and `[0UL, 1UL, 3UL, 2UL, 5UL]`; the first focused native run fails because the hostile/chunk-origin helper is absent.
    - The intended helper contract creates a fresh eager Adam7 oracle and a fresh `PngChunkEncoder::new_indexed8_with_interlace_strategy(..., PngInterlaceStrategy::Adam7, ...)` for each schedule. For every pull, it requires `written <= capacity`, `total_written == accepted_before + written`, only appends the accepted prefix, and confirms every suffix byte remains `b'Z'`. A zero-capacity first lease must be zero-write `NeedOutput` with total zero and an untouched sentinel.
    - On successful completion, it compares all collected bytes to the fresh eager Adam7 bytes, then pulls into a fresh sentinel-filled lease and requires zero-write `Finished`, unchanged total, and no destination mutation.
    - Add a second red test path for a released one-byte first lease: its first and later fresh-lease pulls must return the same sticky `Failed(error)`, with zero writes, zero total, and unchanged `b'Z'` destinations.
    - Specify the independent post-drain qualification in the test contract: parse the collected *chunk-origin* bytes for total 143, IHDR data `00 00 00 05 00 00 00 05 08 03 00 00 01`, 12-byte PLTE, tRNS `00 80 7F`, 47-byte IDAT, IEND, chunk order, and every CRC; test-only Stored extraction must equal `png_indexed8_adam7_expected_raw()` (36 literal bytes); public PngDecoder must assert all 25 transparent RGBA palette pixels.
  </behavior>
  <action>Add test calls and assertions first, deliberately referring to an undefined narrow Adam7 hostile-drain/parser helper so the tracer is observably red without modifying production code. Reuse Phase 81's test-only `png_indexed8_adam7_source(true)`, `png_indexed8_adam7_expected_raw()`, PNG chunk/CRC helpers, Stored scanline extractor, and public decode expectations where package visibility permits; do not recreate production geometry or use eager bytes as the only wire oracle. Do not alter frozen vectors in encode_test.mbt, and do not add split-parent behavior, low-bit Adam7, filters/compression variants, source/model changes, FFI, wrappers, staging, or release automation.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>The focused run is red solely because the new test-only hostile/parser helper does not yet exist; the tracer states concrete schedule, ownership, terminal, chunk-origin framing/raster, and decode requirements for INDEXADAM7-05/06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement only the test helper and close Indexed8 Adam7 streaming qualification</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <action>Implement the missing test-only helper/sibling by adapting the existing Indexed8 or selected low-bit hostile-drain and released-lease patterns; do not edit `stream_encode.mbt`, `encode.mbt`, or any other production file. For every schedule, create fresh eager and chunk instances with the Adam7 selector, drive real caller-owned `MutByteLease` owners, verify accepted-only accounting and `Z` suffix isolation after every pull, and require the collected chunk bytes to equal the fresh eager oracle at `Finished`. Add the released-lease replay test and completed-terminal replay exactly as Task 1 specifies.

After a successful ragged (or separately fresh) drain, independently inspect the collected chunk bytes: use existing test-only u32/slice/CRC helpers to validate IHDR, PLTE/tRNS lengths and bytes, IDAT/IEND order, CRCs, and the 143-byte frame; use the existing test-only Stored extractor to compare with Phase 81's literal 36-byte pass raster; public-decode those drained bytes and assert all 25 RGBA palette values. This parser/decode assertion must consume chunk-origin bytes after the drain, not the eager oracle. Leave opaque/transparent non-interlaced Indexed8 and Indexed1/2/4 literal test implementations untouched; their continued execution in the package gate is the compatibility proof. Keep all Phase 82 changes test-only: no selectors, transport state, buffers, strategy families, source model, or production diagnostics code may be added.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>All three hostile schedules preserve eager identity, accepted-only totals, and lease tails; released/finished terminal replays are zero-write and sticky; independently parsed/decompressed/public-decoded chunk-origin bytes meet the literal Type-3/8 Adam7 contract; the all-target ordinary PNG package passes without any production diff.</done>
</task>

</tasks>

<verification>

1. Run `moon -C modules/mb-image test png --target native --frozen` after Task 1 and record the expected red undefined-helper failure. Run it again after Task 2 for focused green behavior.
2. Confirm `git diff --name-only` for the phase contains only `modules/mb-image/png/stream_encode_test.mbt` (plus the phase summary artifact). Any production-file diff is a scope failure.
3. Run `moon -C modules/mb-image test png --target all --frozen`. This ordinary package gate is the required proof on wasm, wasm-gc, js, and native; do not replace it with target-specific wrappers or filtered-only output.
4. Check that the independent parser/decode path reads collected chunk bytes, uses Phase 81's literal raw oracle, and is executed in addition to—not instead of—fresh eager/chunk equality.

</verification>

<success_criteria>

- **INDEXADAM7-05:** Fresh Adam7 chunk encoders match fresh eager output under `[0,1]`, `[1]`, and `[0,1,3,2,5]`; progress reflects accepted bytes only, lease suffixes remain untouched, and Finished/Failed replays are sticky zero-write outcomes.
- **INDEXADAM7-06:** Drained chunk-origin output independently proves 5×5 Type-3/8 Adam7 framing, canonical PLTE/tRNS, CRCs, literal seven-pass Stored raster, and all-pixel public RGBA8 decode; frozen Indexed8 and low-bit literal vectors remain green.
- **Portability:** `moon -C modules/mb-image test png --target all --frozen` passes unchanged tests plus Phase 82 tests across wasm, wasm-gc, js, and native.
- **Boundary:** No production code is modified and no low-bit Adam7, strategy expansion, second encoder/transport, staging, FFI, wrappers, copied trees, or release automation is introduced.

</success_criteria>

## Source Audit

| Source | ID | Plan coverage | Status |
|---|---|---|---|
| Requirements | INDEXADAM7-05 | Tasks 1–2 exercise the real Adam7 chunk selector under zero/one/ragged leases and sticky terminal replay. | COVERED |
| Requirements | INDEXADAM7-06 | Tasks 1–2 parse/decompress/decode chunk-origin bytes, retain frozen vectors, and run the all-target gate. | COVERED |
| Context | D-01 | Every test uses Phase 81's existing thin Adam7 chunk facade; no production transport is added. | COVERED |
| Context | D-02, D-03 | Tests assert accepted-only progression, sentinel tails, released failure, and completed terminal replay. | COVERED |
| Context | D-04, D-05 | Chunk-origin independent framing/raster/decode proof and frozen compatibility/all-target execution. | COVERED |
| Phase 81 verification | Existing machine and fixture | Treats selector/preflight/traversal as fixed; adds qualification only. | COVERED |

<output>
Create `.planning/phases/82-indexed8-adam7-streaming-and-qualification/82-01-SUMMARY.md` when execution completes.
</output>
