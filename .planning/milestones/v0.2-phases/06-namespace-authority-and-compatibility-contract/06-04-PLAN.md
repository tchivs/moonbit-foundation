---
phase: 06-namespace-authority-and-compatibility-contract
plan: "04"
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - docs/support.md
  - SECURITY.md
  - scripts/quality/Test-CandidateDocumentation.ps1
autonomous: true
requirements: []
must_haves:
  truths:
    - "One bounded collective validator consumes compatibility policy, shared support/security routes, and module source documents without claiming post-publication Mooncakes rendering."
    - "Actual registry-render proof remains exclusively PROV-05 in Phase 8 after publication and read-only observation."
  artifacts:
    - path: "docs/support.md"
      provides: "Stable public support route"
    - path: "SECURITY.md"
      provides: "Stable security-reporting route"
    - path: "scripts/quality/Test-CandidateDocumentation.ps1"
      provides: "Policy-owned collective source-document validator and negatives"
  key_links:
    - from: "scripts/quality/Test-CandidateDocumentation.ps1"
      to: "policy/compatibility.json"
      via: "policy-owned class, version, migration, and RFC consequences"
      pattern: "compatibility[.]json"
---

<!-- phase-source-audit: GOAL-06,D-10,D-11,D-12,RESEARCH-DOCUMENTATION-CONTRACT,RESEARCH-REGISTRY-RENDER-UNKNOWN,CONTEXT-PUBLICATION-DOCUMENTATION -->

<objective>
Establish the shared publication-document routes and bounded collective validator.

Purpose: Prepare one policy-owned validation seam before module documents are changed, while keeping Phase 8 render proof separate.
Output: Two shared route documents and one focused validator.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@policy/release-qualification.json
@policy/compatibility.json
@scripts/quality/Test-CandidateDocumentation.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish stable support and security routes</name>
  <read_first>README.md, docs/governance/rfc-process.md, policy/release-qualification.json</read_first>
  <files>docs/support.md, SECURITY.md</files>
  <action>Create stable public support and security-reporting routes with candidate scope, response boundaries, and links suitable for identical reuse by all three module READMEs. Do not introduce team approvals, credentials, registry operations, or 1.0 commitments.</action>
  <verify><automated>pwsh -NoProfile -Command "$a=Get-Content -Raw docs/support.md; $b=Get-Content -Raw SECURITY.md; if([string]::IsNullOrWhiteSpace($a)-or[string]::IsNullOrWhiteSpace($b)){throw 'shared route missing'}"</automated></verify>
  <acceptance_criteria>Both routes are stable, public, sole-maintainer compatible, and contain no secret or registry-mutation workflow.</acceptance_criteria>
  <done>Shared routes are ready for module references.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the collective source-document validator</name>
  <read_first>scripts/quality/Test-CandidateDocumentation.ps1, scripts/quality/Test-ReleaseQualificationNegative.ps1, policy/compatibility.json, docs/support.md, SECURITY.md</read_first>
  <files>scripts/quality/Test-CandidateDocumentation.ps1</files>
  <action>Extend the existing module loop and isolated-copy negative analog to validate exact source install/import, candidate/toolchain/targets, class, shared routes, changelog, conditional migration/RFC, and intended registry metadata source. Parse policy facts instead of duplicating consequences. Explicitly reject fabricated live-render pass and identify PROV-05/Phase 8 as the only actual Mooncakes-render proof. Add exact negatives for missing/incorrect command, route drift, class or dependency-floor disagreement, missing migration/RFC, unsupported manifest field, empty ambiguity, and duplicate/reordered records.</action>
  <verify><automated>pwsh -NoProfile -Command "$s=Get-Content -Raw scripts/quality/Test-CandidateDocumentation.ps1; @('docs/support.md','SECURITY.md','PROV-05')|ForEach-Object{if(-not $s.Contains($_)){throw ('missing '+$_)}}"</automated></verify>
  <acceptance_criteria>The validator is policy-owned, source-only, table-driven, and cannot promote an unknown or guessed registry render to pass.</acceptance_criteria>
  <done>The bounded validation seam is ready for module document plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Policy and shared routes -> validator | Duplicated or fabricated facts could bypass source-document checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-08 | Spoofing | registry-render claim | high | mitigate | Source-only checks and explicit PROV-05 deferral reject fabricated render pass. |
| T-06-09 | Tampering | support/security/class facts | high | mitigate | Exact policy-owned routes and isolated negative copies. |

## OWASP ASVS L1 Control Mapping

| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Limited | Validator cannot promote unobserved registry facts. | Task 2 |
| V5 Validation, Sanitization and Encoding | Applicable | Exact routes, policy facts, and negative fixtures. | Tasks 1-2 |
</threat_model>

## Artifacts this phase produces

- `docs/support.md` and `SECURITY.md` — shared routes.
- `scripts/quality/Test-CandidateDocumentation.ps1` — collective source validator.

<verification>Run validator static checks and confirm PROV-05 remains the only actual registry-render proof.</verification>

<success_criteria>
- Shared routes and validator are bounded to three files.
- D-10 through D-12 remain policy-owned.
- No Phase 6 live-render claim is introduced.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-04-SUMMARY.md` when done.</output>

