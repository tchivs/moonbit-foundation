---
phase: 06-namespace-authority-and-compatibility-contract
plan: "14"
type: execute
wave: 12
depends_on: [06-10]
files_modified:
  - docs/support.md
  - SECURITY.md
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Test-CandidateDocumentation.ps1
  - scripts/quality/Compare-PublicInterfaceBaseline.ps1
  - scripts/quality/Test-PublicCompatibility.ps1
  - scripts/quality/Test-RegistryAuthority.ps1
autonomous: true
requirements: [REG-01, REG-02, REG-03, COMP-02, COMP-03, COMP-04, PROV-03]
must_haves:
  truths:
    - "D-12/D-14: collective validators accept exact tchivs/* source truth while archived/completed evidence remains historical."
    - "D-16: future owner changes are forward-only; rename, transfer, overwrite, unpublish, or yank are never assumed."
    - "D-17: intended GitHub, support, and security routes remain explicitly unverified until a read-only existence proof passes."
    - "D-18: blocked registry authority remains blocked and no credentialed collector is invoked."
  artifacts:
    - path: "docs/support.md"
      provides: "Usable support contract without fabricated external liveness"
    - path: "SECURITY.md"
      provides: "Usable security reporting contract without fabricated external liveness"
    - path: "scripts/quality/Test-CandidateDocumentation.ps1"
      provides: "Collective three-module source-document contract"
    - path: "scripts/quality/Test-RegistryAuthority.ps1"
      provides: "Blocked-state identity and repository-liveness enforcement"
  key_links:
    - from: "docs/support.md"
      to: "policy/registry-authority.json"
      via: "intended versus verified-live repository state"
      pattern: "unverified"
    - from: "scripts/quality/Test-CandidateDocumentation.ps1"
      to: "modules/mb-image/README.mbt.md"
      via: "exact collective source contract"
      pattern: "tchivs/mb-image"
---

<!-- phase-source-audit: GOAL-06,REG-01,REG-02,REG-03,COMP-02,COMP-03,COMP-04,PROV-03,D-01,D-02,D-04,D-08,D-10,D-11,D-12,D-13,D-14,D-16,D-17,D-18,RESEARCH-SHARED-ROUTE-VALIDATION -->

<objective>
Reconcile shared support/security routes and the five remaining collective validators as one seven-file boundary.

Purpose: Make current source truth machine-enforced without mixing route liveness, project prose, or source-graph ownership.
Output: Honest shared routes plus identity, compatibility, documentation, and authority validators on the canonical graph.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-10-SUMMARY.md
@policy/registry-authority.json
@policy/release-qualification.json
@policy/compatibility.json
@release/registry/authority-observation.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the two shared routes</name>
  <read_first>docs/support.md, SECURITY.md, policy/registry-authority.json, README.md</read_first>
  <files>docs/support.md, SECURITY.md</files>
  <action>Update canonical module references to `tchivs/*` while retaining a locally usable support and security contract. State that the intended GitHub repository and any issue/security routes are unverified and not operational until read-only existence proof succeeds. Do not create a repository, push, authenticate, invent an email, or redirect users to an absent route as live.</action>
  <verify><automated>pwsh -NoProfile -Command "$x=(Get-Content -Raw docs/support.md)+(Get-Content -Raw SECURITY.md); if($x -match 'moonbit-foundation/(mb-core|mb-color|mb-image)'){throw 'route identity drift'}; if($x -notmatch 'unverified'){throw 'missing route liveness boundary'}"</automated></verify>
  <acceptance_criteria>Both shared routes use canonical identities and explicitly distinguish intended metadata from verified-live channels.</acceptance_criteria>
  <done>Shared support/security truth is honest.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile source, compatibility, and policy validators</name>
  <read_first>scripts/quality/Assert-Policy.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-CandidateDocumentation.ps1, scripts/quality/Compare-PublicInterfaceBaseline.ps1, scripts/quality/Test-PublicCompatibility.ps1, policy/foundation.json, policy/release-qualification.json, policy/compatibility.json</read_first>
  <files>scripts/quality/Assert-Policy.ps1, scripts/quality/Test-CandidateDocumentation.ps1, scripts/quality/Compare-PublicInterfaceBaseline.ps1, scripts/quality/Test-PublicCompatibility.ps1</files>
  <action>Keep the shared qualification helper's canonical positive constants from completed plan 06-25 unchanged. Update the other positive identity expectations, the production candidate dependency-floor parser, and stable rule-owned negatives to consume the exact `tchivs/*` policy/source/docs graph. Reject old positive owner, mixed owner, extra identity, branding replacement, invented migration/SemVer requirements, and assumed destructive recovery. Preserve all 17-package, four-target, four-class, source-contract, semantic-overclaim, path-dependency, and exact failure-ownership checks.</action>
  <verify><automated>pwsh -NoProfile -Command "& ./scripts/quality/Assert-Policy.ps1; if($LASTEXITCODE){exit $LASTEXITCODE}; & ./scripts/quality/Invoke-ReleaseQualification.ps1 -Check -StaticOnly; if($LASTEXITCODE){exit $LASTEXITCODE}; & ./scripts/quality/Test-CandidateDocumentation.ps1 -Module all; if($LASTEXITCODE){exit $LASTEXITCODE}; & ./scripts/quality/Test-PublicCompatibility.ps1"</automated></verify>
  <acceptance_criteria>The production comparator and collective validators pass canonical positive truth and retain one exact owner for every compatibility/documentation negative.</acceptance_criteria>
  <done>Source and compatibility validators are reconciled.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce authority and repository-liveness negatives</name>
  <read_first>scripts/quality/Test-RegistryAuthority.ps1, policy/registry-authority.json, release/registry/authority-observation.json, docs/support.md, SECURITY.md</read_first>
  <files>scripts/quality/Test-RegistryAuthority.ps1</files>
  <action>Extend the blocked-state authority validator with exact negatives for GitHub identity substituted for Mooncakes authority, fabricated account/namespace/module proof, fabricated repository/support/security liveness, rename/transfer/destructive semantics, and a fresh status inferred from the tracked seed. Preserve stable digest projection, the `tchivs` blocked seed, credential prohibition, and external OAuth boundary. The test must not invoke `Invoke-RegistryObservation.ps1`.</action>
  <verify><automated>pwsh -NoProfile -Command "& ./scripts/quality/Test-RegistryAuthority.ps1; if($LASTEXITCODE){exit $LASTEXITCODE}; $s=Get-Content -Raw scripts/quality/Test-RegistryAuthority.ps1; if($s -match 'Invoke-RegistryObservation'){throw 'validator invokes operator collector'}"</automated></verify>
  <acceptance_criteria>Blocked-state authority passes only with exact unknown facts; every liveness/authority/destructive overclaim fails under one stable rule.</acceptance_criteria>
  <done>Repository and registry claims fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Intended metadata -> shared routes | An absent external route could be presented as operational. |
| Active policy/docs -> validators | Stale expectations could permit mixed identity truth. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-17 | Spoofing | support/security metadata | high | mitigate | Explicit unverified state and stable liveness negatives. |
| T-06-28 | Tampering | collective validators | high | mitigate | Exact policy projection and positive/negative suite reconciliation. |

## OWASP ASVS L1 Control Mapping
| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Applicable | Unobserved external state cannot become authority. | Tasks 1-3 |
| V5 Validation | Applicable | Exact source, compatibility, and authority rules. | Tasks 2-3 |
</threat_model>

## Artifacts this phase produces
- Two honest shared routes and five reconciled collective validators, consuming the canonical shared helper completed by 06-25.

<verification>Run all three commands and confirm only the seven declared implementation files can change.</verification>

<success_criteria>
- Shared support/security routes do not claim absent infrastructure as live.
- Canonical documentation, compatibility, policy, and blocked authority tests pass.
- OAuth, repository creation, publication, and destructive operations remain outside this plan.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-14-SUMMARY.md` when done.</output>
