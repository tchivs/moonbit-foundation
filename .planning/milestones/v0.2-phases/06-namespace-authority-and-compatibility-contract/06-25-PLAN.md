---
phase: 06-namespace-authority-and-compatibility-contract
plan: "25"
type: execute
wave: 9
depends_on: [06-09]
files_modified:
  - modules/mb-core/moon.mod.json
  - modules/mb-color/moon.mod.json
  - modules/mb-image/moon.mod.json
  - scripts/quality/ReleaseQualification.Common.ps1
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, PROV-03]
must_haves:
  truths:
    - "D-13/D-14/D-17: the three canonical module roots and release policy agree on the exact intended, still-unverified tchivs repository metadata."
    - "D-02/D-07/D-08: the shared qualification helper consumes the canonical tchivs module, package, dependency, publication-order, and repository truth before 06-13 executes the real positive path."
    - "D-18: this prerequisite repair is credential-free and does not create a repository, authenticate, publish, or claim external liveness."
  artifacts:
    - path: "modules/mb-core/moon.mod.json"
      provides: "Canonical leaf manifest repository metadata"
      contains: "https://github.com/tchivs/moonbit-foundation"
    - path: "modules/mb-color/moon.mod.json"
      provides: "Canonical middle manifest repository metadata"
      contains: "https://github.com/tchivs/moonbit-foundation"
    - path: "modules/mb-image/moon.mod.json"
      provides: "Canonical top manifest repository metadata"
      contains: "https://github.com/tchivs/moonbit-foundation"
    - path: "scripts/quality/ReleaseQualification.Common.ps1"
      provides: "Canonical shared positive qualification constants"
  key_links:
    - from: "policy/release-qualification.json"
      to: "modules/mb-core/moon.mod.json"
      via: "exact repository and manifest projection"
      pattern: "https://github.com/tchivs/moonbit-foundation"
    - from: "scripts/quality/ReleaseQualification.Common.ps1"
      to: "policy/release-qualification.json"
      via: "exact canonical module, package, dependency, order, and repository assertions"
      pattern: "tchivs/mb-core"
---

<!-- phase-source-audit: GOAL-06,COMP-01,COMP-02,COMP-03,COMP-04,PROV-03,D-02,D-07,D-08,D-13,D-14,D-17,D-18,DEBUG-PHASE6-QUALIFICATION-ORDER -->

<objective>
Repair the missing canonical-metadata prerequisite before the partial 06-13 qualification plan resumes.

Purpose: Close the four-file ordering gap without rewriting completed plan evidence or widening 06-13's consumer boundary.
Output: Three repository-correct module manifests and one canonical shared qualification helper ready for 06-13 Task 2.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/resolved/phase6-qualification-order.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-09-SUMMARY.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-12-SUMMARY.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-13-DEFERRED.md
@policy/foundation.json
@policy/release-qualification.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the three manifest repository fields</name>
  <read_first>modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json, policy/foundation.json, policy/release-qualification.json</read_first>
  <files>modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json</files>
  <action>Change only the repository field in each module manifest from the obsolete organization URL to the exact intended and unverified `https://github.com/tchivs/moonbit-foundation` value already frozen by both active policies. Preserve names, version 0.1.0, descriptions, license, readme, preferred/supported targets, and exact tchivs dependency floors byte-for-byte apart from unavoidable JSON line-ending normalization. Do not claim the repository is live and do not create or push it.</action>
  <verify><automated>pwsh -NoProfile -Command "$p=Get-Content -Raw policy/release-qualification.json|ConvertFrom-Json -Depth 100; foreach($m in @('mb-core','mb-color','mb-image')){$x=Get-Content -Raw ('modules/'+$m+'/moon.mod.json')|ConvertFrom-Json -Depth 100; if($x.repository -cne 'https://github.com/tchivs/moonbit-foundation' -or $x.repository -cne $p.modules.$m.manifest.repository){throw ('repository projection drift: '+$m)}; if($x.name -cne ('tchivs/'+$m) -or $x.version -cne '0.1.0'){throw ('manifest identity drift: '+$m)}}"</automated></verify>
  <acceptance_criteria>All three manifests agree exactly with active policy repository metadata while every other canonical identity and compatibility field remains intact.</acceptance_criteria>
  <done>The manifest metadata prerequisite is canonical.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile shared positive qualification constants</name>
  <read_first>scripts/quality/ReleaseQualification.Common.ps1, policy/foundation.json, policy/release-qualification.json, modules/mb-core/moon.mod.json, modules/mb-color/moon.mod.json, modules/mb-image/moon.mod.json</read_first>
  <files>scripts/quality/ReleaseQualification.Common.ps1</files>
  <action>Replace every stale positive module/package identity, dependency key, post-publication identity, higher-layer canonical matcher, PPM package/import expectation, and repository expectation in the shared helper with the exact `tchivs/*` and intended repository values from active policy. Preserve rule IDs, exact sequence/set semantics, publication-blocked outcomes, credential-free behavior, historical artifact handling, and all non-identity validation. The helper must contain no old positive module or old repository literal after this task.</action>
  <verify><automated>pwsh -NoProfile -Command "$s=Get-Content -Raw scripts/quality/ReleaseQualification.Common.ps1; if($s -match 'moonbit-foundation/(mb-core|mb-color|mb-image)' -or $s -match 'github\.com/moonbit-foundation/moonbit-foundation'){throw 'stale positive identity in shared helper'}; & ./scripts/quality/Invoke-ReleaseQualification.ps1 -Check -StaticOnly; if($LASTEXITCODE){exit $LASTEXITCODE}"</automated></verify>
  <acceptance_criteria>The shared helper accepts the exact active canonical graph in static qualification while retaining all existing fail-closed and blocked-publication semantics.</acceptance_criteria>
  <done>The positive qualification prerequisite is canonical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Active policy -> shared validator | Stale frozen constants can reject canonical policy or authorize mixed-owner truth. |
| Intended repository metadata -> external liveness | Correct metadata must not be mistaken for proof that the repository exists. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-29 | Tampering | qualification ordering | high | mitigate | Exact four-file prerequisite plan and policy projection checks. |
| T-06-17 | Spoofing | repository metadata | high | mitigate | Preserve intended/unverified semantics and prohibit external mutation. |

## OWASP ASVS L1 Control Mapping
| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V4 Access Control | Applicable | Metadata cannot confer registry or repository authority. | Tasks 1-2 |
| V5 Validation | Applicable | Exact policy projection and stale-positive scan. | Tasks 1-2 |
</threat_model>

## Artifacts this phase produces
- Four canonical prerequisites required by the resumed 06-13 real qualification path.

<verification>Run both task commands, run `git diff --check`, and confirm only the four declared source files changed; preserve partial commit `9f05754` and the uncommitted 06-13 Task 2 scripts without staging them.</verification>

<success_criteria>
- Three module repository fields exactly match active policy.
- The shared qualification helper contains only canonical positive identity truth.
- Static qualification passes without credentials or external mutation.
- 06-13 can resume at Task 2 without changing its declared seven-file boundary.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-25-SUMMARY.md` when done.</output>
