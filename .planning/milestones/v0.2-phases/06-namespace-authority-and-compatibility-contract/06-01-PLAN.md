---
phase: 06-namespace-authority-and-compatibility-contract
plan: "01"
type: execute
wave: 24
depends_on: [06-11]
files_modified:
  - policy/registry-authority.json
  - release/registry/authority-observation-schema.json
  - release/registry/capability-matrix-schema.json
  - release/registry/authority-observation.json
  - release/registry/capability-matrix.json
  - scripts/quality/Invoke-RegistryObservation.ps1
  - scripts/quality/Test-RegistryAuthority.ps1
  - scripts/quality/Test-RegistryAuthorityNegative.ps1
autonomous: true
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "D-01/D-02: one closed machine-readable policy owns the exact personal namespace, `tchivs/*` module identities, pinned toolchain, required-current facts, and dispositions."
    - "D-03/D-04: evidence contains only allowlisted sanitized fields and classifies every capability as documented, safely_observed, or unknown with source and disposition."
    - "D-05/D-06: observation is read-only and operator-only; no production module version is published, duplicated, overwritten, deleted, unpublished, or yanked."
    - "D-17: repository metadata remains intended/unverified until a separate read-only GitHub existence check passes; the plan never creates or pushes a repository."
    - "D-18/REG-01: sanitized evidence records the observed `tchivs` account, documented canonical identities, and exact `0.1.0` absence without inferring namespace or current-token authority."
    - "REG-03: Phase 6 succeeds when `-AssertPublishReady` rejects the unknown namespace-authority and authenticated-publish-seam facts with `REG03-REQUIRED-FACT-UNKNOWN`; it does not claim publish readiness."
  artifacts:
    - path: "policy/registry-authority.json"
      provides: "Canonical personal namespace, exact identities, freshness, required facts, and dispositions"
      contains: "tchivs/mb-core"
    - path: "release/registry/authority-observation.json"
      provides: "Credential-redacted repository-bound authority evidence"
      contains: "credentials_read"
    - path: "release/registry/capability-matrix.json"
      provides: "Closed documented/safely_observed/unknown capability matrix"
      contains: "destructive_recovery"
    - path: "scripts/quality/Test-RegistryAuthority.ps1"
      provides: "Credential-free fail-closed readiness validator"
  key_links:
    - from: "scripts/quality/Test-RegistryAuthority.ps1"
      to: "policy/registry-authority.json"
      via: "exact identity, freshness, repository-state, required-fact, and disposition validation"
      pattern: "registry-authority[.]json"
    - from: "scripts/quality/Invoke-RegistryObservation.ps1"
      to: "release/registry/authority-observation-schema.json"
      via: "allowlisted scalar projection before persistence"
      pattern: "authority-observation-schema[.]json"
  assumptions:
    - { id: EDGE-REG-01-BOUNDARY, statement: "[FLAGGED-UNVERIFIED] Review authority-evidence freshness at the exact maximum age and immediately beyond it.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-ADJACENCY, statement: "[FLAGGED-UNVERIFIED] Review whether account identity equal to `tchivs` is insufficient without explicit namespace authorization.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-EMPTY, statement: "[FLAGGED-UNVERIFIED] Review missing, null, or single-module authority evidence and require a precise blocked result.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-ENCODING, statement: "[FLAGGED-UNVERIFIED] Review canonical identity equality as ordinal UTF-8 text with no normalization or case folding.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-ORDERING, statement: "[FLAGGED-UNVERIFIED] Review stable ordering of the three module authority records and capability rows.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-01-PRECISION, statement: "[FLAGGED-UNVERIFIED] Review UTC timestamp parsing and freshness arithmetic for truncation, overflow, and clock-boundary ambiguity.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-02-UNCLASSIFIED, statement: "[FLAGGED-UNVERIFIED] Manually review the capability matrix for an omitted registry semantic.", verification: manual-review, flagged: true }
    - { id: EDGE-REG-03-UNCLASSIFIED, statement: "[FLAGGED-UNVERIFIED] Manually review publication-readiness facts and unknown dispositions for a fail-open omission.", verification: manual-review, flagged: true }
  prohibitions:
    - { id: PROH-REG-CREDENTIALS, requirement_id: REG-01, category: confidentiality, statement: "MUST NOT read, record, hash, copy, or derive evidence from tokens, cookies, authorization headers, credential files, credential paths, or raw authentication output.", status: unverified, flagged: true }
    - { id: PROH-REG-MUTATION, requirement_id: REG-02, category: safety, statement: "MUST NOT publish, overwrite, delete, unpublish, yank, rename, transfer, or otherwise mutate the three production modules while proving authority.", status: unverified, flagged: true }
---

<!-- phase-source-audit: GOAL-06,REG-01,REG-02,REG-03,D-01,D-02,D-03,D-04,D-05,D-06,D-17,D-18,RESEARCH-SANITIZED-OBSERVATION,RESEARCH-FAIL-CLOSED-HANDOFF,RESEARCH-REPOSITORY-AUTHORIZATION-BOUNDARY -->

<objective>
Close the Phase 6 registry-authority contract with truthful sanitized evidence and a demonstrated fail-closed publication gate.

Purpose: Satisfy REG-01 through REG-03 by recording what is actually known, proving missing namespace/current-token authority blocks publication, and transferring the unresolved authenticated publish seam to Phase 7's isolated publisher preflight.
Output: Personal-owner policy, refreshed capability dispositions, sanitized evidence, positive/negative contract validation, and an explicit blocked-by-design Phase 7 handoff.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-CONTEXT.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/06-RESEARCH.md
@.planning/phases/06-namespace-authority-and-compatibility-contract/COVERAGE.md
@policy/registry-authority.json
@release/registry/authority-observation.json
@release/registry/capability-matrix.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze truthful sanitized authority and capability evidence</name>
  <read_first>policy/registry-authority.json, release/registry/authority-observation-schema.json, release/registry/capability-matrix-schema.json, release/registry/authority-observation.json, release/registry/capability-matrix.json, .planning/phases/06-namespace-authority-and-compatibility-contract/COVERAGE.md</read_first>
  <files>policy/registry-authority.json, release/registry/authority-observation-schema.json, release/registry/capability-matrix-schema.json, release/registry/authority-observation.json, release/registry/capability-matrix.json, scripts/quality/Test-RegistryAuthority.ps1, scripts/quality/Test-RegistryAuthorityNegative.ps1</files>
  <action>Enforce D-01 through D-06 and D-17/D-18 against the evidence recorded through commit `b03157a`. Keep the authenticated account as safely observed, the three canonical identities as documented, and exact `0.1.0` absence as safely observed. Keep namespace authority and the authenticated publish seam explicitly unknown with `block_publication`; neither the public account record, local `moon whoami`, username-prefix documentation, nor version absence may be promoted into current-token authorization. Refresh the capability matrix only from the same allowlisted documented/read-only facts, preserve explicit fail-closed or forward-only dispositions, and retain exact negatives for identity conflation, stale or malformed evidence, fabricated repository liveness, unsafe value shapes, and missing dispositions.</action>
  <verify><automated>pwsh -NoProfile -Command "& ./scripts/quality/Test-RegistryAuthority.ps1; if($LASTEXITCODE){exit $LASTEXITCODE}; & ./scripts/quality/Test-RegistryAuthorityNegative.ps1"</automated></verify>
  <acceptance_criteria>The credential-free contract passes in an honest blocked state; every known fact has exact provenance and every unknown fact has one explicit disposition.</acceptance_criteria>
  <done>REG-01 and REG-02 evidence is truthful, sanitized, current where observable, and incapable of authorizing publication by inference.</done>
</task>

<task type="auto">
  <name>Task 2: Prove fail-closed readiness and transfer the publish seam</name>
  <read_first>scripts/quality/Invoke-RegistryObservation.ps1, scripts/quality/Test-RegistryAuthority.ps1, scripts/quality/Test-RegistryAuthorityNegative.ps1, policy/registry-authority.json, release/registry/authority-observation-schema.json</read_first>
  <files>scripts/quality/Invoke-RegistryObservation.ps1, release/registry/authority-observation.json, release/registry/capability-matrix.json, scripts/quality/Test-RegistryAuthority.ps1, scripts/quality/Test-RegistryAuthorityNegative.ps1</files>
  <action>Run the normal validator and negative matrix, then invoke `-AssertPublishReady` as an expected rejection. Require nonzero exit and the exact owning rule `REG03-REQUIRED-FACT-UNKNOWN`; any success is a fail-open defect. Statically prove the collector and Required boundary do not authenticate, publish, create/push a repository, read credentials, or mutate registry state. Record in the plan summary that Phase 7's isolated credentialed publisher must obtain and validate the actual current-token publish seam before any production publication; Phase 6 supplies the immutable blocked evidence and must not fabricate that proof.</action>
  <verify><automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; & ./scripts/quality/Test-RegistryAuthority.ps1; & ./scripts/quality/Test-RegistryAuthorityNegative.ps1; $rejected=$false; try { & ./scripts/quality/Test-RegistryAuthority.ps1 -AssertPublishReady } catch { $rejected=$true; if($_.Exception.Message -cnotmatch 'REG03-REQUIRED-FACT-UNKNOWN'){throw 'REG-03 rejected under the wrong rule'} }; if(-not $rejected){throw 'REG-03 failed open'}; $source=(Get-Content -Raw scripts/quality/Invoke-RegistryObservation.ps1); if($source -match 'moon\s+(register|login|publish)|gh\s+repo\s+create|git\s+push'){throw 'operator seam crossed authentication/publication boundary'}"</automated></verify>
  <acceptance_criteria>Normal and negative validation pass; publish-ready assertion fails only under `REG03-REQUIRED-FACT-UNKNOWN`; no credential, repository, or registry mutation path is introduced.</acceptance_criteria>
  <done>REG-03 is proven fail-closed, and the unresolved authenticated publish seam is explicitly handed to Phase 7 before production publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human OAuth session -> read-only collector | Authentication must not expose secret material to the repository. |
| External text -> tracked observation | Untrusted output must be sanitized before persistence. |
| Tracked evidence -> publication readiness | Stale or conflated identity facts could authorize the wrong owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Spoofing/Elevation of Privilege | account and namespace facts | high | mitigate | Require separate exact Mooncakes account, namespace, and module facts; GitHub identity equality alone never passes. |
| T-06-02 | Information Disclosure | observation collector | high | mitigate | Allowlist scalar projection, reject secret/header/cookie/path shapes, and persist no raw output. |
| T-06-03 | Tampering/Repudiation | observation/capability artifacts | high | mitigate | Closed schemas, commit/toolchain/time binding, stable SHA-256, freshness, and exact negatives. |
| T-06-18 | Elevation of Privilege | GitHub repository mutation | high | mitigate | Read-only existence check only; creation and push require separate explicit authorization. |

## OWASP ASVS L1 Control Mapping

| ASVS Category | Applicability | Plan Control | Task Mapping |
|---|---|---|---|
| V2 Authentication | Applicable | Human official OAuth plus sanitized post-auth observation; automation never handles credentials. | Tasks 2-3 |
| V3 Session Management | Limited | Expiry/revocation/non-interactive semantics remain explicit unknowns unless safely observed. | Tasks 1, 3 |
| V4 Access Control | Applicable | Exact personal namespace authority is distinct from GitHub/local authentication and gates readiness. | Tasks 1, 3 |
| V5 Validation, Sanitization and Encoding | Applicable | Closed schemas, ordinal identities, allowlisted fields, pre-persistence sanitization, and exact negatives. | Tasks 1-3 |
| V6 Stored Cryptography | Limited | Platform SHA-256 binds evidence integrity only. | Tasks 1, 3 |
</threat_model>

## Artifacts this phase produces

- `policy/registry-authority.json` — exact `tchivs` namespace/freshness/disposition contract.
- `release/registry/authority-observation.json` — sanitized repository-bound authority proof.
- `release/registry/capability-matrix.json` — complete explicit capability dispositions.
- Registry observation and authority positive/negative scripts with a single human OAuth checkpoint.

<verification>Run both tasks and treat the exact `REG03-REQUIRED-FACT-UNKNOWN` rejection as the required Phase 6 safety result; confirm no collector path authenticates, creates a repository, publishes, or persists raw output.</verification>

<success_criteria>
- REG-01 and REG-02 retain truthful sanitized personal-owner observations and explicit unknown dispositions.
- REG-03 rejects publication because namespace authority and the current-token publish seam remain unknown; Phase 6 does not claim publish readiness.
- D-01 through D-06, D-17, D-18, all eight registry edge items, and both registry prohibitions remain explicit.
- Credentials and production registry mutation remain outside repository automation.
- Phase 7 receives an explicit pre-publication obligation to validate the authenticated publish seam inside the isolated publisher boundary.
</success_criteria>

<output>Create `.planning/phases/06-namespace-authority-and-compatibility-contract/06-01-SUMMARY.md` when done.</output>
