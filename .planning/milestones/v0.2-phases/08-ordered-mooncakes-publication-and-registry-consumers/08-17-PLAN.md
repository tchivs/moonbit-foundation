---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "17"
type: execute
wave: 17
depends_on: ["08-16"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "r7 is the sole current initial forward retry with module 0.1.0, intent kind initial, sequence 0, root=current, and no predecessor (D-01, D-07, D-15)."
    - "Attempt-zero and r1 through r6 are seven distinct immutable terminal-negative histories with individual digests and one canonical ordered-set digest."
    - "r6 binds source c05cacbc3cfc583205c612f4bf293a4e251ec079, annotated tag object cdff825cc870a50c0393d5347f21351011092149, run 29671691604/1, failed prepare job 88151792308, and P08-PREPARED-INTENT-BINDING caused by Windows/Linux EOL-dependent ZIP bytes."
    - "Every r6 downstream dry-run, exact-existing, hosted-preflight, publisher, observation, cold-consumer, packet, receipt, handoff, PublishOne, mutation, and successor effect is zero."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r7 current profile and seven exact histories"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r7 seven-history AuthorityUnion handoff"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r7 root/genesis/prepared/index/store identity"
  key_links:
    - from: "attempt-zero plus r1 through r6 histories"
      to: "r7 prepared, authority, receipt, and handoff artifacts"
      via: "seven individual SHA-256 digests plus canonical LF-ordered-set SHA-256"
      pattern: "historical_r6_sha256"
---

<objective>Define and test the r7 initial retry and seven immutable terminal-negative histories.

Purpose: Recover forward from r6's cross-platform prepared-intent binding failure without moving r6, reusing its state, or entering the correction lane.
Output: Closed r7 policy, intent, prepared, authority, receipt, handoff, and qualification contracts.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-prepared-zip.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-16-SUMMARY.md
@policy/release-control.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the initial attempt family to r7 and preserve seven histories</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r7 is current initial; version 0.1.0, root=current, sequence zero, and no-predecessor invariants remain.
    - Exact history order is attempt_zero,r1,r2,r3,r4,r5,r6 and each member has distinct terminal facts and digest.
    - r6 binds its exact source/tag/run/job/error and all skipped or nonexistent downstream effects as zero.
  </behavior>
  <action>Per D-01, D-07, D-14, and D-15, advance only the current initial profile from r6 to r7. Preserve attempt-zero and r1 through r5 byte-for-byte, then add r6 as a terminal prepare-job failure: source `c05cacbc3cfc583205c612f4bf293a4e251ec079`, annotated tag object `cdff825cc870a50c0393d5347f21351011092149`, workflow dispatch run `29671691604/1`, prepare job `88151792308`, failure code `P08-PREPARED-INTENT-BINDING`, and the Windows/Linux EOL-dependent ZIP-byte mismatch recorded by the debug artifact. Require all downstream jobs/effects to be skipped or zero and forbid an uploaded prepared artifact, packet, receipt, handoff, PublishOne, mutation, or successor. Define seven individual digests and one canonical LF-ordered-set digest; reject r6-current, replacement, duplication, reorder, set drift, predecessor/sequence drift, state reuse, and initial-to-correction substitution.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r7 alone is current and the seven immutable histories include the exact r6 hosted prepare failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind seven histories into authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Authority, receipt, and handoff require seven individual history digests and their matching ordered set.
    - Mutation authority requires packet plus same-turn literal receipt; exact-existing authority forbids both; stop is ineligible.
    - r6-current, fake successful r6 preparation, history substitution/reorder/set drift, stale UTC, and cross-attempt path reuse fail closed.
  </behavior>
  <action>Per D-04, D-07, D-14, and D-15, advance the closed authority, receipt, and handoff schemas to r7 and seven histories. Bind r6's exact failed prepare job without allowing a fabricated downstream run, artifact, or authority. Make the receipt bind packet, r7 ref/boundary/source, literal `authorize-core`, canonical UTC evidence, every history and the set, and its reciprocal digest. Fix the production handoff at `%TEMP%/mnf-phase08-r7-handoff.json`, bind every path/file/digest, and admit exactly one AuthorityUnion variant. Add exact-existing, absent-receipt, stop, fake-r6-success, stale-history, reordered-set, old-root, and old-fixed-handoff adversarial fixtures.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$p=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($f in $p){$null=Get-Content -LiteralPath $f -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All live schemas require r7 plus seven histories and preserve receipt-or-exact exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fresh r7 prepared, index, store, and qualification identity</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r7 root/genesis/prepared/index/store bind seven histories and reject every r6 path, locator, active attempt, store, tag, or handoff.
    - r6's failed cross-platform preparation is historical evidence only and never supplies a reusable prepared artifact.
    - Equal empty snapshots, nonempty drift rejection, UTC, LF, no-tags, and duplicate workflow-key protections remain deterministic.
  </behavior>
  <action>Per D-01, D-07, D-14, D-15, and D-18, extend canonical qualification and prepared generation to r7 with all seven histories. Require a fresh initial root/genesis/prepared/index/store and prevent any r6 StateRoot, locator, index, store, active-attempt, prepared artifact, or handoff reuse. Carry the exact r6 run/job/error and downstream-zero facts as terminal evidence only. Add positive r7 fixtures and adversarial r6-current, old-root/index/store, fake prepared upload/successor, missing/reordered/substituted history, set mismatch, correction-lane, UTC, LF, no-tags, equal-empty, and nonempty-drift cases.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r7 eligibility is seven-history-bound and cannot inherit r6's mismatched prepared bytes or state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Seven prior attempts to r7 | Histories are immutable evidence only and cannot grant current authority. |
| Hosted r6 failure to history | A failed prepare job must not be upgraded into prepared, dry-run, or mutation authority. |
| r6 state to r7 preparation | No locator, store, tag, handoff, or StateRoot reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-17-01 | Tampering | attempt family | critical | mitigate | Exact seven facts/digests/set and substitution/reorder tests. |
| T-08-17-02 | Spoofing | r6 downstream success | critical | mitigate | Closed failed-job shape and fabricated-success rejection. |
| T-08-17-03 | Elevation of Privilege | receipt/handoff | critical | mitigate | Exclusive r7 union bound to seven histories. |
| T-08-17-04 | Tampering | r6 state or prepared reuse | high | mitigate | Fresh r7 root/store and old-path/artifact rejection. |
| T-08-17-05 | Repudiation | r6 failure | high | mitigate | Exact source/tag/run/job/error/downstream-zero facts are digest-bound. |
</threat_model>

<verification>Run intent, prepared, and qualification suites; validate r7, seven histories, exact r6 failure and zero downstream effects, fresh state, UTC/LF/no-tags/snapshot protections, and zero live effects.</verification>
<success_criteria>r7 and seven terminal histories are closed tested contracts ready for hosted seam integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-17-SUMMARY.md` when done.</output>
