---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "37"
type: execute
wave: 37
depends_on: ["08-36"]
files_modified:
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/Invoke-Phase08R13Boundary.ps1
  - scripts/quality/Invoke-Phase08R13PreLive.ps1
  - scripts/quality/Test-Phase08R13Boundary.ps1
  - scripts/quality/Test-Phase08R13PreLive.ps1
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
autonomous: false   # has a blocking checkpoint gate
requirements: [DIST-01, DIST-04]   # boundary establishment is prerequisite evidence for these; does NOT complete them
must_haves:
  truths:
    - "r13 source/tag object/peel/remote-clone HEAD/clone policy/boundary bind one tested SHA, and PrepareAttempt succeeds BEFORE the tag is created (the ordering invariant r12 violated)."
    - "The exact eight-path user-dirty baseline stays unchanged; every other worktree drift fails closed."
    - "Non-publishing r13 ends absent with one packet and no receipt/handoff, zero mutation."
    - "Only absent displays same-turn authorize-core; stop leaves no receipt/handoff."
  artifacts:
    - path: "refs/tags/modules-v0.1.0-r13"
      provides: "unique r13 immutable boundary tag"
    - path: "%TEMP%/mnf-phase08-r13-user-dirty-baseline.json"
      provides: "closed eight-path dirty baseline (current HEAD)"
    - path: "%TEMP%/mnf-phase08-r13-preauthorization.json"
      provides: "closed r13 absent-branch projection"
---
<objective>Establish the canonical non-publishing r13 immutable boundary with the ordering-invariant fix (script, policy, and PrepareAttempt verified to agree BEFORE the tag is created), then stop at the absent-only authorize-core decision.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-36-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-35-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
@scripts/quality/Invoke-Phase08R13Boundary.ps1
@scripts/quality/Invoke-Phase08R13PreLive.ps1
@scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1</context>
<tasks>
<task type="auto" non_publishing="true">
  <name>Task 1: Advance qualification script and r13 boundary wrappers</name>
  <files>scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Invoke-Phase08R13Boundary.ps1 (created), scripts/quality/Invoke-Phase08R13PreLive.ps1 (created), scripts/quality/Test-Phase08R13Boundary.ps1 (created), scripts/quality/Test-Phase08R13PreLive.ps1 (created), .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md (annotated to note 08-37 authorized the r13 boundary now that 08-36 has passed)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-PLAN.md (structural template for the boundary+checkpoint pattern)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-35-SUMMARY.md (the documented forward constraint this task resolves)
    - .planning/STATE.md (the ordering-invariant decision r12 violated)
    - policy/release-control.json (already declares current_attempt=r13, release_ref=refs/tags/modules-v0.1.0-r13, thirteen terminal_negative_history entries, history_set_sha256=961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5)
    - scripts/quality/Invoke-ReleaseQualification.ps1 lines 300-360 (the three r12 hardcodes to update)
    - scripts/quality/Invoke-Phase08R12Boundary.ps1 (the wrapper to mirror as r13)
    - scripts/quality/Invoke-Phase08R12PreLive.ps1 (the selector to mirror as r13/thirteen histories)
    - scripts/quality/Test-Phase08R12Boundary.ps1, scripts/quality/Test-Phase08R12PreLive.ps1 (tests to mirror)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 (the eight-path guard)
  </action>
    The goal of this task is the **script side of the r13 ordering invariant**: bring `Invoke-ReleaseQualification.ps1` and the new r13 boundary wrappers into agreement with the already-r13 policy (`policy/release-control.json` line 350 `current_attempt=r13`, line 363 `release_ref=refs/tags/modules-v0.1.0-r13`). This task MUST NOT create, push, or fetch any r13 tag — Task 3 does that, only after Task 2 proves agreement in a disposable clone.

    **Step 1 — Baseline capture (current HEAD).** Before any edit, run:
    ```
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Capture `
      -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" `
      -TaskOwnedPath "scripts/quality/Invoke-ReleaseQualification.ps1,scripts/quality/Invoke-Phase08R13Boundary.ps1,scripts/quality/Invoke-Phase08R13PreLive.ps1,scripts/quality/Test-Phase08R13Boundary.ps1,scripts/quality/Test-Phase08R13PreLive.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md"
    ```
    The captured head MUST equal `git -C $repoRoot rev-parse HEAD`. The baseline file schema MUST be `mnf-phase08-r13-dirty-baseline/1` with exactly the eight protected paths recorded by `Assert-Phase08R13DirtyBaseline.ps1` lines 19-28.

    **Step 2 — Update `scripts/quality/Invoke-ReleaseQualification.ps1` r12 → r13 (exactly three lines).**
    - Line 303: change `-ReleaseRef 'refs/tags/modules-v0.1.0-r12'` → `-ReleaseRef 'refs/tags/modules-v0.1.0-r13'`.
    - Line 314: change `release_ref = 'refs/tags/modules-v0.1.0-r12'` → `release_ref = 'refs/tags/modules-v0.1.0-r13'`.
    - Line 354: change `$releaseRef = 'refs/tags/modules-v0.1.0-r12'` → `$releaseRef = 'refs/tags/modules-v0.1.0-r13'`.
    These are the three lines documented as the forward constraint in `08-35-SUMMARY.md` line 100. No other line in this file is touched; in particular, do NOT touch the `REL01-REF` throws, the `New-ReleaseIntent.ps1` invocation arguments other than `-ReleaseRef`, the `$policy` read, or the `New-CleanReleaseClone` calls.

    **Step 3 — Create `scripts/quality/Invoke-Phase08R13Boundary.ps1`** by mirroring `scripts/quality/Invoke-Phase08R12Boundary.ps1` line-for-line with these substitutions:
    - Replace every identifier prefix `P08-R12-` with `P08-R13-` and every function name `Invoke-P08R12BoundaryGit` / `Throw-P08R12Boundary` with the `R13` equivalent.
    - The wrapper parameters, the `Set-StrictMode -Version Latest`, the clone-toplevel check, the policy path resolution at `Join-Path $clone 'policy/release-control.json'`, the canonical-release-ref regex `^refs/tags/modules-v0[.]1[.]0-r[1-9][0-9]*$`, the tag/peel/HEAD equality assertion, the `Invoke-Phase08HostedRun.ps1` discovery, the `InitializeBoundary` then `PrepareAttempt` invocation sequence, and the returned closed invocation record (`release_ref`, `tag_object`, `peeled_source_sha`, `head`, `boundary_sha`, `mutation_count=0`) MUST be byte-faithful to the r12 wrapper except for the identifier prefix.
    - The wrapper MUST NOT expose a caller-controlled `-ReleaseRef`; the canonical ref is derived exclusively from clone-local `policy/release-control.json`. (This is the property `Test-Phase08R13Boundary.ps1` enforces by static source scan, mirroring `Test-Phase08R12Boundary.ps1` lines 22-25.)

    **Step 4 — Create `scripts/quality/Invoke-Phase08R13PreLive.ps1`** by mirroring `scripts/quality/Invoke-Phase08R12PreLive.ps1` line-for-line with these substitutions:
    - Identifier prefix `P08-R12-` → `P08-R13-` everywhere; function names `Throw-P08R12`, `Get-P08R12Sha` → `R13` equivalents.
    - `$expectedAttempts` (r12 line 25) becomes the **thirteen** ordered attempt names: `'attempt_zero,r1,r2,r3,r4,r5,r6,r7,r8,r9,r10,r11,r12'`.
    - `$expectedDigests` (r12 line 26) becomes the **thirteen** `record_sha256` values from `policy/release-control.json` `terminal_negative_history[0..12]`, in canonical attempt order. Source the values from the committed policy file; the thirteenth entry MUST be the r12 record `92397fbdfc679f154382928ee6f94c57e46b40b7c5f7e8d65759b0165d6c96a8` (policy line 344).
    - `$historical_r12_sha256=$expectedDigests[12]` (the r13 selector's last-history digest slot, mirroring r12's `$historical_r11_sha256=$expectedDigests[11]`).
    - The policy-history assertion (r12 line 29) requires `current_attempt -ceq 'r13'`, `release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `history.Count -eq 13`, the joined attempts equal to the thirteen-name list, and the joined `record_sha256` equal to the thirteen-digest list.
    - The history_set_sha256 check (r12 line 30) recomputes the LF-joined SHA-256 of the thirteen digests and requires it to equal `policy.initial_attempt_family.history_set_sha256` (`961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5`).
    - The `$r12=$history[12]` terminal record assertion (r12 line 33 selected `$r11=$history[11]`) MUST bind r12 as immutable terminal REL01-REF evidence: `attempt -ceq 'r12'`, `release_ref -ceq 'refs/tags/modules-v0.1.0-r12'`, `source_sha -ceq '5e7b19cdc74ec11d5c524ff34a36c266b15bba39'`, `tag_object_sha -ceq '57b76c9f9044d3190acc1e4c3fb7ada516f4dece'`, `tag_peeled_source_sha -ceq '5e7b19cdc74ec11d5c524ff34a36c266b15bba39'`, `failure_code -ceq 'REL01-REF'`, and every `zeroFields` counter (`boundary_locator_count`, `active_attempt_count`, `prepared_bundle_count`, `publisher_dry_run_count`, `observation_count`, `authorization_packet_count`, `authorization_receipt_count`, `handoff_count`, `publish_one_count`, `registry_operation_count`, `mutation_count`, `successor_count`) equals zero. Throw `P08-R13-R12-TERMINAL` on any drift.
    - The remote-tag absence gate (r12 line 37) MUST require exactly one remote row binding r12's tag object and peel AND zero rows matching `refs/tags/modules-v0[.]1[.]0-r13(?:\^\{\})?$`. Throw `P08-R13-REMOTE-TAG` on any violation.
    - The fixed handoff absence gate (r12 line 38-39) targets `%TEMP%/mnf-phase08-r13-handoff.json` and throws `P08-R13-HANDOFF` if it exists.
    - The summary-committed-clean requirements (r12 line 41) MUST be the two summaries that bound the r13 static route: `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-35-SUMMARY.md` and `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-36-SUMMARY.md`. The local-tag absence check (r12 line 42-43) MUST `git show-ref --verify --quiet refs/tags/modules-v0.1.0-r13` and throw `P08-R13-LOCAL-TAG` if r13 exists locally.
    - The emitted JSON MUST be schema `mnf-phase08-r13-prelive/1` with `release_ref='refs/tags/modules-v0.1.0-r13'`, `historical_r12_sha256`, `history_set_sha256`, all authority/mutation fields `$null`, `mutation_count=0`, `output_write_count=0`, `eligible=$true`.
    - The required-wrapper list (r12 line 22) MUST enumerate `Invoke-Phase08R13Boundary.ps1` and `Test-Phase08R13Boundary.ps1`.

    **Step 5 — Create `scripts/quality/Test-Phase08R13Boundary.ps1`** mirroring `scripts/quality/Test-Phase08R12Boundary.ps1`: identifier prefix `P08-R13-`; wrapper path `Invoke-Phase08R13Boundary.ps1`; required-source tokens including `policy/release-control.json`, `InitializeBoundary`, `PrepareAttempt`, `tag_object`, `peeled_source_sha`, `boundary_sha`, `mutation_count=0`; the static "no caller-controlled ReleaseRef" assertion; the disposable `git clone --quiet --no-local --no-tags $repoRoot $clone`; the clone-local `git tag -a modules-v0.1.0-r13 -m 'r13 wrapper fixture boundary' HEAD`; the probe scriptblock that throws `P08-R13-PROVIDER` when the canonical policy ref reaches the provider; the positive test confirming `probe.calls -eq 1`, `probe.release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `probe.execution_root` and `probe.control_policy_path` resolved under the clone; the negative test passing `-ExpectedTagObject ('0'*40)` to force `P08-R13-TAG` before any state or provider work; and the historical args `-HistoricalReleaseRef refs/tags/modules-v0.1.0-r12 -HistoricalSourceSha 5e7b19cdc74ec11d5c524ff34a36c266b15bba39`.

    **Step 6 — Create `scripts/quality/Test-Phase08R13PreLive.ps1`** mirroring `scripts/quality/Test-Phase08R12PreLive.ps1`: identifier prefix `P08-R13-`; selector path `Invoke-Phase08R13PreLive.ps1`; static-source required tokens `refs/tags/modules-v0.1.0-r13`, `attempt_zero,r1,r2,r3,r4,r5,r6,r7,r8,r9,r10,r11,r12`, `historical_r12_sha256`, `57b76c9f9044d3190acc1e4c3fb7ada516f4dece`, `5e7b19cdc74ec11d5c524ff34a36c266b15bba39`, `Invoke-Phase08R13Boundary.ps1`, `Test-Phase08R13Boundary.ps1`, `mnf-phase08-r13-handoff.json`, `output_write_count=0`; forbidden live side-effect tokens `PublishOne`, `Invoke-MooncakesLiveMutation`, `MOONCAKES_TOKEN`, `gh workflow run`, `git tag `, `git push `; the fixture policy cloned from `policy/release-control.json`; the `$r12=$policy.initial_attempt_family.terminal_negative_history[12]` row used to build `$remote` rows binding r12 object and peel; the positive `LibraryOnly` call asserting `release_ref='refs/tags/modules-v0.1.0-r13'`, `historical_r12_sha256=$r12.record_sha256`, `remote_tag_query_count=1`, `output_write_count=0`, `mutation_count=0`, `eligible=$true`, and every authority/mutation path `$null`; the negative `P08-R13-R12-TERMINAL` test mutating `$bad.initial_attempt_family.terminal_negative_history[12].mutation_count=1`; the two `P08-R13-REMOTE-TAG` negatives (empty rows, and a rows list that includes a phantom r13 entry).

    **Step 7 — Annotate `08-R13-RECOVERY-CONTEXT.md`.** Append a short dated note under the "Active Route" section: "Plan 08-36 has passed; Plan 08-37 is now authorized to create and verify the immutable r13 boundary as the non-publishing prerequisite for any future authorize-core/publication plan. No r13 authorization, registry observation, or publication is asserted by 08-37." Do not edit any other line of the file.

    **Step 8 — Commit.** Stage exactly the six files in `<files>`. Commit message: `feat(08-37): advance qualification and r13 boundary wrappers`. Run `Assert-Phase08R13DirtyBaseline -Mode Verify` after the commit; the eight protected paths MUST be unchanged from the Step 1 capture. Run the new r13 tests and the existing static gates; they must all pass.

    **Task 1 MUST NOT** create any r13 tag, push any tag, fetch any r13 ref into the working repository, dispatch any workflow, read any credential, perform any registry observation, or emit any pre-authorization/handoff/packet/receipt. Task 2 proves agreement in a disposable clone; Task 3 creates the tag.
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-ReleaseQualification.ps1,scripts/quality/Invoke-Phase08R13Boundary.ps1,scripts/quality/Invoke-Phase08R13PreLive.ps1,scripts/quality/Test-Phase08R13Boundary.ps1,scripts/quality/Test-Phase08R13PreLive.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08R13Boundary.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08R13PreLive.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08R13DirtyBaseline.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "if ((Select-String -Path ./scripts/quality/Invoke-ReleaseQualification.ps1 -Pattern 'modules-v0[.]1[.]0-r12').Count -ne 0) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "if ((Select-String -Path ./scripts/quality/Invoke-ReleaseQualification.ps1 -Pattern 'modules-v0[.]1[.]0-r13').Count -lt 3) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "if ((& git -C $PWD show-ref --verify --quiet refs/tags/modules-v0.1.0-r13; \$LASTEXITCODE) -eq 0) { Write-Error 'r13 tag already exists locally before Task 2'; exit 1 }"
  </automated></verify>
  <done>The qualification script and r13 boundary/prelive wrappers are committed at one SHA, all r13 static tests pass, the qualification script contains zero r12 hardcodes and exactly three r13 hardcodes on lines 303/314/354, the eight-path baseline is unchanged, and no r13 tag exists locally or remotely.</done>
</task>
<task type="auto" non_publishing="true">
  <name>Task 2: Prove script/policy/tag-name agreement in a disposable clone BEFORE any tag is created</name>
  <files>None (disposable clone, GUID-owned state)</files>
  <read_first>
    - .planning/STATE.md (ordering-invariant decision, lines 159: "(1) Invoke-ReleaseQualification.ps1 references refs/tags/modules-v0.1.0-rN on every -ReleaseRef, (2) policy/release-control.json declares current_attempt=rN and release_ref=refs/tags/modules-v0.1.0-rN, (3) the boundary wrapper completes PrepareAttempt (not just InitializeBoundary) in a disposable clone.")
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-SUMMARY.md (the r12 REL01-REF terminal history this task prevents from recurring)
    - scripts/quality/Invoke-Phase08R13Boundary.ps1 (created in Task 1)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 (the InitializeBoundary and PrepareAttempt modes)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
  </action>
    This task is the **ordering-invariant gate r12 skipped** (STATE.md lines 158-159). r12 was tagged 26 minutes before its script fix landed, so its own `Invoke-ReleaseQualification.ps1` still hardcoded `r9` at the tagged commit and `Assert-ReleaseInitialCloneBinding` (`ReleaseQualification.Common.ps1` line 300-301) threw `REL01-REF` deterministically; the tag became immutable publish-blocked evidence. This task PROVES the candidate HEAD agrees on r13 across (a) the qualification script's three `-ReleaseRef` sites, (b) `policy/release-control.json`, and (c) `Invoke-Phase08R13Boundary.ps1`'s PrepareAttempt — all in a disposable clone where no r13 tag has been fetched, then SYNTHESIZES a clone-local r13 fixture tag and re-runs PrepareAttempt to prove it succeeds against the same script+policy. **If either step fails, STOP — do not create the r13 tag.**

    **Step 1 — Baseline Verify (before).** Run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the same six-path task-owned list as Task 1. The eight protected paths MUST be unchanged.

    **Step 2 — Capture the candidate SHA.** Record `$head = (& git -C $repoRoot rev-parse HEAD).Trim()` (a 40-hex SHA). This is the SHA Task 3 will tag if and only if Step 5 passes. Record `$commitMessage = & git -C $repoRoot log -1 --format=%s HEAD`.

    **Step 3 — Prove (a): the committed qualification script references r13 on every ReleaseRef.** Read `scripts/quality/Invoke-ReleaseQualification.ps1` from the working tree (which equals committed HEAD because the baseline guard passed). Run `Select-String -Path Invoke-ReleaseQualification.ps1 -Pattern 'modules-v0[.]1[.]0-r12'`; the match count MUST be `0`. Run `Select-String -Path Invoke-ReleaseQualification.ps1 -Pattern 'modules-v0[.]1[.]0-r13'`; the match count MUST be exactly `3` and the matched line numbers MUST include 303, 314, and 354. Any other count is a Task 1 regression — STOP.

    **Step 4 — Prove (b): the committed policy declares r13.** Read `policy/release-control.json` from the working tree. Require `$policy.initial_attempt_family.current_attempt -ceq 'r13'`, `$policy.initial_profile.release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `terminal_negative_history.Count -eq 13`, the joined attempt list equals `attempt_zero,r1,r2,r3,r4,r5,r6,r7,r8,r9,r10,r11,r12`, and `$policy.initial_attempt_family.history_set_sha256 -ceq '961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5'`. Any mismatch — STOP.

    **Step 5 — Prove (c): PrepareAttempt succeeds in a disposable clone that has NOT fetched r13.** This is the proof r12 never had. Create:
    ```
    $root = Join-Path ([IO.Path]::GetTempPath()) ('mnf-phase08-r13-agreement-' + [Guid]::NewGuid().ToString('N'))
    $clone = Join-Path $root 'clone'
    $null = New-Item -ItemType Directory -Path $root
    & git clone --quiet --no-local --no-tags $repoRoot $clone
    & git -C $clone config user.name 'MNF r13 agreement fixture'
    & git -C $clone config user.email 'r13-agreement-fixture@moonbit-foundation.invalid'
    & git -C $clone checkout --quiet $head
    ```
    Confirm clone HEAD equals `$head` via `& git -C $clone rev-parse HEAD`. Confirm `refs/tags/modules-v0.1.0-r13` is ABSENT in the clone (`& git -C $clone show-ref --verify --quiet refs/tags/modules-v0.1.0-r13` MUST return non-zero) — this is the pre-fetch state. Now SYNTHESIZE the clone-local r13 fixture tag exactly as `Test-Phase08R13Boundary.ps1` does:
    ```
    & git -C $clone tag -a modules-v0.1.0-r13 -m 'r13 agreement fixture boundary' $head
    $tagObject = (& git -C $clone rev-parse refs/tags/modules-v0.1.0-r13).Trim()
    $peeled = (& git -C $clone rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim()
    ```
    Require `$peeled -ceq $head`. Invoke the r13 boundary wrapper:
    ```
    $state = Join-Path $root 'canonical-state'
    $probe = [pscustomobject]@{calls=0;release_ref='';execution_root='';control_policy_path='';prepared=$false}
    $provider = {
      param($Context)
      $probe.calls++
      $probe.release_ref = [string]$Context.release_ref
      $probe.execution_root = [string]$Context.execution_root
      $probe.control_policy_path = [string]$Context.control_policy_path
      $probe.prepared = $true
      # agreement probe: do NOT mutate; return a no-op prepared marker so PrepareAttempt can finish its closed shape
    }.GetNewClosure()
    $result = & ./scripts/quality/Invoke-Phase08R13Boundary.ps1 `
      -CloneRoot $clone -StateRoot $state `
      -ExpectedTagObject $tagObject -ExpectedPeeledSourceSha $peeled `
      -HistoricalReleaseRef refs/tags/modules-v0.1.0-r12 `
      -HistoricalSourceSha 5e7b19cdc74ec11d5c524ff34a36c266b15bba39 `
      -PrepareProvider $provider
    ```
    The call MUST return exit 0 with a closed invocation record whose `release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `tag_object -ceq $tagObject`, `peeled_source_sha -ceq $peeled`, `head -ceq $head`, `boundary_sha -ceq $head`, `mutation_count -eq 0`. The probe MUST have been invoked exactly once with `release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `execution_root` resolved under `$clone`, and `control_policy_path` resolved to `<clone>/policy/release-control.json`. Any throw (especially `P08-PREPARE-REF` or `REL01-REF`) means the script, policy, and tag name do NOT agree — STOP, do not create the r13 tag, do not retry, write a SUMMARY recording the disagreement.

    **Step 6 — Negative control.** Re-run the boundary wrapper with `-ExpectedTagObject ('0'*40)`; it MUST throw `P08-R13-TAG` before invoking the provider, and `$probe.calls` MUST be unchanged.

    **Step 7 — Teardown and Verify (after).** Remove `$root`. Run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the six-path task-owned list. The eight protected paths MUST still be unchanged.

    This task MUST NOT push, fetch into the working repo, dispatch a workflow, read a credential, observe the registry, or create any r13 tag in the working repository or on the remote. The synthesized clone-local r13 tag is a fixture inside the disposable clone only and is destroyed with it.
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-ReleaseQualification.ps1,scripts/quality/Invoke-Phase08R13Boundary.ps1,scripts/quality/Invoke-Phase08R13PreLive.ps1,scripts/quality/Test-Phase08R13Boundary.ps1,scripts/quality/Test-Phase08R13PreLive.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "& { $head=(& git rev-parse HEAD).Trim(); $root=Join-Path ([IO.Path]::GetTempPath()) ('mnf-phase08-r13-agreement-'+[Guid]::NewGuid().ToString('N')); $clone=Join-Path $root 'clone'; $null=New-Item -ItemType Directory -Path $root; & git clone --quiet --no-local --no-tags $PWD $clone; & git -C $clone config user.name 'fixture'; & git -C $clone config user.email 'fixture@moonbit-foundation.invalid'; & git -C $clone checkout --quiet $head; & git -C $clone tag -a modules-v0.1.0-r13 -m 'agreement' $head; $tagObject=(& git -C $clone rev-parse refs/tags/modules-v0.1.0-r13).Trim(); $peeled=(& git -C $clone rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim(); if ($peeled -cne $head) { throw 'peel drift' }; $state=Join-Path $root 'state'; $probe=[pscustomobject]@{calls=0}; $provider={ param($c); $probe.calls++; $probe.release_ref=[string]$c.release_ref; $probe.execution_root=[string]$c.execution_root; $probe.control_policy_path=[string]$c.control_policy_path }.GetNewClosure(); $r=& ./scripts/quality/Invoke-Phase08R13Boundary.ps1 -CloneRoot $clone -StateRoot $state -ExpectedTagObject $tagObject -ExpectedPeeledSourceSha $peeled -HistoricalReleaseRef refs/tags/modules-v0.1.0-r12 -HistoricalSourceSha 5e7b19cdc74ec11d5c524ff34a36c266b15bba39 -PrepareProvider $provider; if ($r.release_ref -cne 'refs/tags/modules-v0.1.0-r13' -or $r.tag_object -cne $tagObject -or $r.peeled_source_sha -cne $peeled -or $r.head -cne $head -or $r.boundary_sha -cne $head -or [int]$r.mutation_count -ne 0) { throw 'agreement proof failed' }; if ($probe.calls -ne 1 -or $probe.release_ref -cne 'refs/tags/modules-v0.1.0-r13') { throw 'provider probe drift' }; Remove-Item -LiteralPath $root -Recurse -Force; Write-Output ('PASS: r13 agreement proven at ' + $head) }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-ReleaseQualification.ps1,scripts/quality/Invoke-Phase08R13Boundary.ps1,scripts/quality/Invoke-Phase08R13PreLive.ps1,scripts/quality/Test-Phase08R13Boundary.ps1,scripts/quality/Test-Phase08R13PreLive.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md"
  </automated></verify>
  <done>The Task 1 candidate HEAD has been independently proven in a disposable clone to make the r13 boundary wrapper's PrepareAttempt succeed (release_ref, tag object, peel, HEAD, boundary SHA, and probe all agree on r13) — the ordering invariant r12 violated is satisfied BEFORE any r13 tag is created in the working repository or pushed to the remote.</done>
</task>
<task type="auto">
  <name>Task 3: Create and push the r13 immutable boundary tag, then emit the absent pre-authorization</name>
  <files>None (Git refs, disposable remote clone, GUID-owned state, sanitized temporary artifacts)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-PLAN.md (the boundary task pattern to mirror)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-SUMMARY.md (the REL01-REF recurrence this task avoids)
    - scripts/quality/Invoke-Phase08R13PreLive.ps1 (the pre-tag absence selector created in Task 1; the pre-tag form is run here only to confirm absence immediately before creation)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 (modes: ValidatePreAuthorization, HostedPreflight, PublisherDryRun)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
  </action>
    This task creates and pushes the r13 immutable boundary tag ONLY because Task 2 has proven agreement. It mirrors 08-32 Task 1 exactly, but at the tested committed HEAD proven in Task 2, and adds the explicit pre-tag absence re-check 08-32 lacked.

    **Step 1 — Baseline Verify (before).** `Assert-Phase08R13DirtyBaseline -Mode Verify` with the six-path task-owned list from Task 1. The eight protected paths MUST be unchanged.

    **Step 2 — Pre-tag absence re-check (immediately before creation).** Run the r13 pre-live selector ONCE in its credential-free read-only form to confirm the remote still has zero r13 rows AND the local r13 ref still does not exist:
    ```
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08R13PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin
    ```
    The selector performs one credential-free `git ls-remote --tags origin`, requires r12 tag/peel to be present, requires zero r13 rows, requires the local `refs/tags/modules-v0.1.0-r13` to be absent, requires `%TEMP%/mnf-phase08-r13-handoff.json` to be absent, and emits the closed `mnf-phase08-r13-prelive/1` projection. If this throws `P08-R13-REMOTE-TAG` or `P08-R13-LOCAL-TAG`, a sibling process has already created the r13 tag — STOP and write a SUMMARY recording the conflict. This step MUST NOT be re-run after the tag is created; it is a pre-tag absence gate by design.

    **Step 3 — Resolve and record the tested HEAD.** `$head = (& git -C $repoRoot rev-parse HEAD).Trim()`. This MUST equal the SHA Task 2 proved. Record the commit subject: `& git -C $repoRoot log -1 --format=%s $head`.

    **Step 4 — Create and push ONE non-force annotated r13 tag.** Create the annotated tag at the tested HEAD only:
    ```
    & git -C $repoRoot tag -a modules-v0.1.0-r13 -m 'MNF Phase 08 r13 non-publishing boundary' $head
    & git -C $repoRoot push origin refs/tags/modules-v0.1.0-r13
    ```
    The push MUST be a plain `push origin refs/tags/modules-v0.1.0-r13`; no `--force`, no `--tags`, no `--delete`, no annotated-tag rewrite. If `git push` reports the remote ref already exists (non-fast-forward), STOP without forcing; a sibling process created r13 first.

    **Step 5 — Verify remote annotated tag object and peel.** Require local and remote identity to agree byte-for-byte:
    ```
    $localObject  = (& git -C $repoRoot rev-parse refs/tags/modules-v0.1.0-r13).Trim()
    $localPeel    = (& git -C $repoRoot rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim()
    $remoteRows   = @(& git ls-remote --tags origin modules-v0.1.0-r13)
    ```
    Assert `$localObject -cmatch '^[0-9a-f]{40}$'`, `$localPeel -ceq $head`, and `$remoteRows` contains exactly two rows: `<localObject>\trefs/tags/modules-v0.1.0-r13` and `<localPeel>\trefs/tags/modules-v0.1.0-r13^{}`. Assert `& git -C $repoRoot merge-base --is-ancestor $localPeel HEAD` succeeds (the tag points at genuine committed history).

    **Step 6 — Disposable remote-clone canonical boundary invocation.** Mirror 08-32 Task 1:
    ```
    $root = Join-Path ([IO.Path]::GetTempPath()) ('mnf-phase08-r13-remote-clone-' + [Guid]::NewGuid().ToString('N'))
    $clone = Join-Path $root 'clone'
    $null = New-Item -ItemType Directory -Path $root
    & git clone --quiet --no-local --no-tags $repoRoot $clone
    & git -C $clone config user.name 'MNF r13 boundary fixture'
    & git -C $clone config user.email 'r13-boundary-fixture@moonbit-foundation.invalid'
    & git -C $clone checkout --quiet $head
    & git -C $clone fetch --no-tags origin refs/tags/modules-v0.1.0-r13:refs/tags/modules-v0.1.0-r13
    ```
    Require clone-local `refs/tags/modules-v0.1.0-r13` object and peel equal `$localObject` and `$localPeel`, clone HEAD equals `$head`, and clone-local `policy/release-control.json` still declares `release_ref=refs/tags/modules-v0.1.0-r13` and `current_attempt=r13`. Invoke:
    ```
    $state = Join-Path $root 'canonical-state'
    $result = & ./scripts/quality/Invoke-Phase08R13Boundary.ps1 `
      -CloneRoot $clone -StateRoot $state `
      -ExpectedTagObject $localObject -ExpectedPeeledSourceSha $localPeel `
      -HistoricalReleaseRef refs/tags/modules-v0.1.0-r12 `
      -HistoricalSourceSha 5e7b19cdc74ec11d5c524ff34a36c266b15bba39
    ```
    (No `-PrepareProvider` is supplied so PrepareAttempt uses its default provider, exactly as in 08-32 Task 1.) Require the closed invocation record: `release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `tag_object -ceq $localObject`, `peeled_source_sha -ceq $localPeel`, `head -ceq $head`, `boundary_sha -ceq $head`, `mutation_count -eq 0`. Require all thirteen history files were written under `$state`.

    **Step 7 — Hosted preflight and publisher dry-run with publisher-only temporary Moon home and teardown.** Run one credential-free `Invoke-Phase08HostedRun.ps1 -Mode HostedPreflight` and one `Invoke-Phase08HostedRun.ps1 -Mode PublisherDryRun` with an isolated, GUID-owned temporary `MOON_HOME` that is removed in a `finally` block; no production Moon home, no real Mooncakes credential, no environment that could mutate the registry. Require both to finish without dispatching a real workflow, accessing a credential, mutating the registry, or producing any packet/receipt/handoff.

    **Step 8 — Fresh credential-free core observation.** Run one credential-free read-only registry observation bounded by the existing observation policy (20 attempts at 15-second cadence per STATE.md). Record the sanitized projection only. Do NOT read, store, or persist any credential, secret, token, or cookie-shaped value.

    **Step 9 — Emit the absent pre-authorization.** Produce `%TEMP%/mnf-phase08-r13-preauthorization.json` with schema `mnf-phase08-pre-authorization/1`, `release_ref='refs/tags/modules-v0.1.0-r13'`, `authority_variant='confirmed_absent'`, exactly one mutation-authorization packet path bound to the r13 packet digest, every exact-existing/receipt/handoff/observation path either bound to a digest-valid artifact or `$null` per the closed contract, `mutation_count=0`, and `output_write_count=0`. The fixed `%TEMP%/mnf-phase08-r13-handoff.json` MUST remain absent (no handoff is produced by a non-publishing boundary plan). No `PublishOne`, no mutation dispatch, no Mooncakes request, no `gh workflow run`, no `gh run create`.

    **Step 10 — Baseline Verify (after).** `Assert-Phase08R13DirtyBaseline -Mode Verify` with the six-path task-owned list. The eight protected paths MUST be unchanged.
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-ReleaseQualification.ps1,scripts/quality/Invoke-Phase08R13Boundary.ps1,scripts/quality/Invoke-Phase08R13PreLive.ps1,scripts/quality/Test-Phase08R13Boundary.ps1,scripts/quality/Test-Phase08R13PreLive.ps1,.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$lo=(& git rev-parse refs/tags/modules-v0.1.0-r13).Trim(); $lp=(& git rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim(); $rows=@(& git ls-remote --tags origin modules-v0.1.0-r13); if ($rows.Count -ne 2) { throw 'remote r13 row count' }; if ($rows -notcontains ($lo + '`trefs/tags/modules-v0.1.0-r13')) { throw 'remote r13 object' }; if ($rows -notcontains ($lp + '`trefs/tags/modules-v0.1.0-r13^{}')) { throw 'remote r13 peel' }; & git merge-base --is-ancestor $lp HEAD; if ($LASTEXITCODE) { throw 'r13 not ancestor of HEAD' }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath "$env:TEMP/mnf-phase08-r13-preauthorization.json" -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json"
  </automated></verify>
  <done>r13 (refs/tags/modules-v0.1.0-r13) exists as one immutable annotated tag whose object and peel are byte-identical locally and remotely, is an ancestor of main HEAD, was created at the exact SHA Task 2 proved, the canonical disposable remote-clone boundary invocation returned one zero-mutation closed record, one hosted preflight and one publisher dry-run completed with publisher-only temporary Moon home and teardown, and `%TEMP%/mnf-phase08-r13-preauthorization.json` exists with the absent variant and zero mutation while `%TEMP%/mnf-phase08-r13-handoff.json` remains absent.</done>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Dynamic absent-only authorize-core checkpoint</name>
  <files>None (only an authorized absent branch may emit receipt/handoff; this plan stops at absent and does not authorize)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-PLAN.md Task 2 (the checkpoint pattern to mirror exactly)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md (only a distinct still-later r13-specific plan may seek explicit authorization and attempt one ordered module operation)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 (modes: ValidatePreAuthorization, ValidateHandoff)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
  </action>
    This task SURFACES the authorize-core decision to the operator; it does NOT resolve it. The actual authorize-core → publish lives in a SEPARATE later plan, not this one. The non-publishing r13 boundary ends absent with one packet and no receipt/handoff; either resume-signal that this plan accepts is recorded into a follow-up hand-off plan, never into this plan's own artifacts.

    **Step 1 — Recompute baseline and identity/authority facts.** Re-run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the six-path task-owned list. Re-display: the canonical clone-policy ref `refs/tags/modules-v0.1.0-r13`; the r13 tag object and peeled source SHA (local and remote); the r12 terminal facts (source `5e7b19cdc74ec11d5c524ff34a36c266b15bba39`, tag object `57b76c9f9044d3190acc1e4c3fb7ada516f4dece`, `failure_code=REL01-REF`, `authority_acquired=false`); the thirteen-history LF aggregate `961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5`; the r13 boundary wrapper proof from Task 2 (agreement proven before tagging) and Task 3 (remote-clone canonical invocation); the prepared/preflight/dry/actor/teardown records; the observation/packet/projection digests; zero mutation; and the unchanged eight-path baseline. Exact-existing has no packet/receipt/handoff and ends without this prompt; only the confirmed-absent branch reaches this checkpoint.

    **Step 2 — Accept only the same-turn literal resume-signal.** Accept exactly `authorize-core` or `stop`. Any drift, stale observation, mismatch, malformed response, or non-literal input stops without mutation. After the branch, re-compare the baseline; any drift fails closed.

    **Step 3 — `stop` branch (default for this plan).** No receipt, no handoff, no Mooncakes mutation, no `PublishOne`, no workflow dispatch. `%TEMP%/mnf-phase08-r13-handoff.json` remains absent. Validate the pre-authorization zero-mutation projection. The authorize-core → publish path is deferred to a separate later plan that re-validates everything from a fresh boundary invocation.

    **Step 4 — `authorize-core` branch (surfaces intent only; this plan does not execute it).** Record that operator intent is to authorize; a separate later plan is required to consume that intent. This plan does NOT create a receipt, does NOT create a handoff, does NOT dispatch `PublishOne`. If the operator demands immediate mutation, STOP and re-route to the separate publication plan — do not expand this plan's scope.
  <options>
    <option id="authorize-core">
      <name>authorize-core</name>
      <pros>Surfaces operator intent to enable a future single-core mutation in a separate later plan after revalidation.</pros>
      <cons>This plan does not execute it; the next plan can make one irreversible operation after its own fresh boundary passes.</cons>
    </option>
    <option id="stop">
      <name>stop</name>
      <pros>Preserves r13 zero-mutation boundary evidence and the unchanged eight-path baseline.</pros>
      <cons>Publication remains blocked until a separate later r13 publication plan is run.</cons>
    </option>
  </options>
  <resume-signal>Type exactly `authorize-core` or `stop`.</resume-signal>
  <verify><automated>pwsh -NoProfile -Command "$h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r13-handoff.json'; $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r13-preauthorization.json'; if(Test-Path $h){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $h; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $p -HandoffPath $h; exit $LASTEXITCODE"</automated></verify>
  <done>Only a same-turn absent authorization surfaces intent for a separate later publication plan; this plan ends with zero mutation, no receipt, no handoff, and the eight-path baseline unchanged. The actual authorize-core → publish is left to a separate later r13-specific plan.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| caller to r13 boundary wrapper | Canonical policy ref cannot be overridden; the wrapper derives `refs/tags/modules-v0.1.0-r13` from clone-local `policy/release-control.json` only. |
| user dirties to artifacts | The exact eight-path baseline preserves user state and excludes it from release identity; every other worktree drift fails closed. |
| pre-tag state to tag creation | The Task 2 disposable-clone PrepareAttempt proof MUST succeed before any r13 tag is created (the ordering invariant r12 violated). |
| pre-authorization to decision | Fresh absent evidence is required for any same-turn authorize-core surfacing; stop retains no receipt/handoff. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-37-01 | Spoofing | r13 caller ref | critical | mitigate | The r13 wrapper accepts no caller-controlled `-ReleaseRef`; the canonical ref is read from clone-local policy. Static-source test in `Test-Phase08R13Boundary.ps1` enforces this. |
| T-08-37-02 | Tampering | eight-path dirty baseline | high | mitigate | `Assert-Phase08R13DirtyBaseline -Mode Verify` runs before and after every task; the eight protected paths are never staged, edited, reset, cleaned, stashed, or included in any artifact. |
| T-08-37-03 | Information Disclosure | publisher dry-run credential | critical | mitigate | Publisher-only temporary GUID-owned `MOON_HOME` removed in a `finally` block; no production Moon home, no real Mooncakes credential is materialized. |
| T-08-37-04 | Elevation of Privilege | core receipt / handoff | critical | mitigate | Same-turn absent-only revalidation; this plan stops at the absent branch and does not produce a receipt or handoff; the actual authorize-core → publish lives in a separate later plan. |
| T-08-37-05 | Tampering | tag-before-script-fix recurrence (the r12 REL01-REF bug) | critical | mitigate | Task 2 proves in a disposable clone that the committed `Invoke-ReleaseQualification.ps1` (three r13 hardcodes on lines 303/314/354), the committed `policy/release-control.json` (`current_attempt=r13`, `release_ref=refs/tags/modules-v0.1.0-r13`), and `Invoke-Phase08R13Boundary.ps1` PrepareAttempt all agree on r13 BEFORE Task 3 creates the immutable tag. Any disagreement throws `P08-PREPARE-REF` / `REL01-REF` and stops the plan without tagging. |
| T-08-37-06 | Repudiation | r12 terminal evidence | high | mitigate | The r13 pre-live selector and the boundary wrapper carry r12 as immutable REL01-REF terminal history (source `5e7b19cd`, tag object `57b76c9f`, `failure_code=REL01-REF`, `authority_acquired=false`, all twelve counters zero); r12 is never reused as current authority. |
| T-08-37-07 | Denial of Service | r13 remote re-creation conflict | medium | mitigate | Task 3 Step 2 pre-tag absence re-check and Step 4 non-force push both fail closed if a sibling process has already created `refs/tags/modules-v0.1.0-r13`; no force, delete, or rewrite is permitted. |
</threat_model>
<verification>Re-run the r13 boundary, pre-live, dirty-baseline, qualification-fixture, and prepare-history-schema gates; prove the Task 2 disposable-clone agreement proof passes before tagging; prove the remote annotated tag object and peel are byte-identical locally and remotely and the peel is an ancestor of HEAD; prove the disposable remote-clone canonical boundary invocation returns one zero-mutation closed record; prove the pre-authorization projection is the absent variant with one packet and zero mutation while the handoff path is absent; prove the eight-path baseline is unchanged after every task. No registry mutation, no Mooncakes request, no credential materialization in production state, no `PublishOne`, no workflow dispatch.</verification>
<success_criteria>Only a canonical r13 clone boundary whose script, policy, and wrapper all agree on r13 — proven by PrepareAttempt in a disposable clone BEFORE the tag is created — can reach a future human authorization; no ref override, dirty-state drift, or tag-before-script-fix recurrence can enter it. The non-publishing r13 boundary ends absent with one packet and no receipt/handoff; the actual authorize-core → publish is deferred to a separate later plan.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-SUMMARY.md` when done.</output>
