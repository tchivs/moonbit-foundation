---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "08"
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher, workflow, mutation adapter, hosted controller, and observation selectors accept r2 as the sole current initial retry and bind attempt-zero/r1 terminal-negative history."
    - "Every boundary locator timestamp is normalized to canonical UTC before hashing; offset-equivalent values and JSON reload retain one digest while changed/tampered/stale values fail."
    - "The hosted seam can atomically persist a closed authorization receipt and fixed-path r2 handoff, update active-attempt digests, and validate exclusive mutation-receipt versus exact-existing variants."
    - "Only LibraryOnly test mode may inject HandoffPath or TempRoot; every fixture owns an independent GUID root and production always uses the non-overridable fixed handoff path."
    - "All tests are credential-free and perform no tag, push, dispatch, secret access, or publication."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r2 preparation, UTC locator, receipt, active attempt, and fixed handoff implementation"
    - path: ".github/workflows/publish-modules.yml"
      provides: "One-module r2 hosted publisher contract"
    - path: "scripts/quality/Test-Phase08LiveSeam.ps1"
      provides: "Adversarial r2/UTC/receipt/handoff live-seam tests"
  key_links:
    - from: "r2 prepared identity"
      to: "publisher and workflow requests"
      via: "initial/root/no-predecessor and terminal-history digests"
      pattern: "modules-v0.1.0-r2"
    - from: "literal receipt"
      to: "active attempt and handoff"
      via: "atomic path plus SHA-256 update and revalidation"
      pattern: "authorization_receipt_path"
    - from: "LibraryOnly test seam"
      to: "isolated fixture handoff"
      via: "explicit HandoffPath or TempRoot accepted only outside production"
      pattern: "LibraryOnly"
---

<objective>
Wire the tested r2 attempt, UTC locator, authorization receipt, and deterministic handoff contracts through the hosted publisher seam.

Purpose: Ensure the later live plan can execute only clone-rooted r2 code and can persist human authorization without interpretation.
Output: r2-capable publisher/workflow/controller plus complete credential-free adversarial tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-07-SUMMARY.md
@policy/release-control.json
@release/qualification/phase-08-authorization-receipt-schema.json
@release/qualification/phase-08-handoff-schema.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce r2 in publisher, one-step adapter, and hosted workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - r2 initial requests bind root=current, sequence zero, null predecessor, prepared identity, and both terminal-negative history digests.
    - r1 current requests, correction semantics, history drift, or more than one module mutation fail before credentials.
    - The workflow continues to isolate the credential to one mutation step and sanitizes actor evidence.
  </behavior>
  <action>Per D-01 through D-07 and D-13 through D-15, replace current-r1 publisher/workflow validation with the Task 07 r2 profile while keeping r1 exclusively historical. Require both history digests in request/prepared validation. Preserve one-module maximum, isolated MOON_HOME, pinned MOON_TOOLCHAIN_ROOT, exact actor proof, no raw output, and closed outcome handling. Extend negative tests for r1 substitution, history drift, second mutation, correction-lane confusion, and credential exposure. Commit this four-file publisher unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>The publisher/workflow accepts only exact r2 current authority and remains one-step, isolated, and fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UTC-stable r2 hosted state, receipt, and fixed handoff</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1</files>
  <behavior>
    - PrepareAttempt accepts only r2 and creates fresh root/genesis/prepared/index state bound to both terminal negatives.
    - Every locator/receipt/handoff timestamp hashes as canonical UTC Z; offset-equivalent, DateTime, DateTimeOffset, and JSON-reloaded inputs agree.
    - PersistAuthorizationReceipt accepts only exact literal authorize-core and atomically binds packet/ref/boundary/UTC/digest into active-attempt.
    - WriteHandoff atomically creates `%TEMP%/mnf-phase08-r2-handoff.json` only when absent and valid; mutation requires receipt, exact forbids it, and stop cannot write an eligible handoff.
    - LibraryOnly/tests accept an explicit HandoffPath or TempRoot, but production rejects caller override and always resolves the fixed path internally.
    - Mutation, exact-existing, and collision fixtures each use a different GUID temp root, clean up only their owned root in finally, and assert the real production fixed path is absent both before and after the suite.
  </behavior>
  <action>Per D-03, D-04, D-07, and D-13 through D-19, implement clone-rooted r2 modes for preparation, receipt persistence, active-attempt update, AuthorityUnion, and fixed discoverable handoff. Production computes `%TEMP%/mnf-phase08-r2-handoff.json` internally and rejects any caller-supplied HandoffPath/TempRoot override. The fixed handoff is a pointer/projection only; StateRoot and ExecutionRoot remain independently GUID-unique and may never be reused. Refuse to overwrite any existing handoff or non-empty StateRoot. Canonicalize all locator, receipt, and handoff DateTime/DateTimeOffset values to UTC Z before hashing.

Add a LibraryOnly test contract that accepts an explicit HandoffPath or TempRoot solely when no production mode is executing. Test-Phase08LiveSeam and Test-Phase08Qualification must assert the real fixed production path does not exist before setup and again after cleanup. Create separate GUID-owned temp roots for the positive mutation fixture, positive exact-existing fixture, and collision fixture; never share a fixture root or fixed-path state. Wrap every fixture in try/finally and remove only the exact root that fixture created after verifying its full path stays beneath the test-owned parent. The collision fixture creates its collision only at its injected path. Independently execute all three fixtures so order cannot make them pass. Also test nonliteral response, stop, missing/tampered receipt, stale/offset timestamps, JSON reload, changed instant, old root, r1 current, relative/out-of-root paths, and raw/secret-shaped evidence. Run all adjacent suites and commit atomically. Do not perform live network actions or touch the real fixed handoff.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>The hosted seam produces UTC-stable fresh r2 state, closed receipt/union/handoff artifacts, and rejects all cross-attempt or ambiguous inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Prepared r2 to credentialed workflow | Only exact r2 bindings may reach the isolated step. |
| Human response to persisted receipt | Free-form continuation text is untrusted until exact literal validation. |
| Unique live state to fixed handoff | The fixed file is digest-bound projection, never reusable StateRoot. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-08-01 | Elevation of Privilege | publisher request | critical | mitigate | r2/history/one-module validation before credential materialization. |
| T-08-08-02 | Tampering | locator/receipt/handoff digest | critical | mitigate | UTC canonicalization, closed schemas, atomic writes, reload verification, adversarial tests. |
| T-08-08-03 | Spoofing | old attempt state | critical | mitigate | Unique roots, non-empty/fixed-handoff refusal, r1-current rejection, rooted paths. |
| T-08-08-04 | Information Disclosure | workflow evidence | high | mitigate | Isolated credential state, raw-output exclusion, secret-shape rejection. |
| T-08-08-05 | Tampering | test fixture versus production handoff | high | mitigate | Production path is non-overridable; LibraryOnly fixtures use independent GUID roots, owned-path finally cleanup, and before/after fixed-path absence assertions. |
</threat_model>

<verification>
Run publisher, live-seam, Phase 8 qualification, and observation suites; confirm UTC stability, receipt/handoff exclusivity, and absence of live effects.
</verification>

<success_criteria>
The exact hosted implementation can safely create and hand off r2 authority while remaining fully credential-free under static tests.
</success_criteria>

<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-08-SUMMARY.md` when done.</output>
