---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "28"
type: execute
wave: 28
depends_on: ["08-27"]
files_modified:
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Invoke-Phase08R11PreLive.ps1
  - scripts/quality/Test-Phase08PrepareHistorySchema.ps1
  - scripts/quality/Test-Phase08R11PreLive.ps1
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "Hosted controller, publisher, adapter, and workflow accept only r11 with all eleven history fields and aggregate (D-01, D-02, D-07)."
    - "The r11 zero-write selector treats r10 only as immutable REL01-REF terminal evidence, verifies remote tag/peel, and rejects r10 state or handoff reuse."
    - "Pre-authorization remains read-only and mutually exclusive: exact-existing has no packet/receipt/handoff; confirmed-absent has one packet and no receipt/handoff, both zero mutation (D-04, D-14)."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R11PreLive.ps1"
      provides: "credential-free r11 eligibility selector"
    - path: ".github/workflows/publish-modules.yml"
      provides: "r11 eleven-history publisher propagation and isolated credential boundary"
  key_links:
    - from: "r11 pre-live result"
      to: "r11 boundary plan"
      via: "read-only tag/history/summary/handoff eligibility"
      pattern: "historical_r10_sha256"
---
<objective>Advance hosted, publisher, and zero-write seams to r11 after the pre-tag qualification fix is proven.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-26-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-27-SUMMARY.md
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-Phase08R10PreLive.ps1
@scripts/quality/Invoke-ReleasePublisher.ps1
@.github/workflows/publish-modules.yml
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Advance r11 hosted preparation and zero-write pre-live selection</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Invoke-Phase08R11PreLive.ps1, scripts/quality/Test-Phase08PrepareHistorySchema.ps1, scripts/quality/Test-Phase08R11PreLive.ps1</files>
  <behavior>
    - An r11 fixture with attempt-zero+r1..r10 produces a fresh active attempt after the clone-local tag fixture passes.
    - r10 is accepted only as its exact tag/peel/REL01-REF terminal history; any r10 active-root/locator/archive/index/store/handoff input is rejected.
    - The selector is closed and zero-write, requiring r11 absence, r10 remote tag/peel evidence, clean prerequisite summaries, and no r11 handoff.
    - Pre-authorization validates exact-existing versus absent packet states with no receipt/handoff and zero mutation.
  </behavior>
  <action>Write RED tests then advance the HostedRun history family to eleven entries and add `historical_r10_sha256` plus aggregate binding everywhere the r10 field was formerly terminal. Preserve the strict r8 reader and r9/r10 terminal record validation without treating historical fields as generally optional. Implement the r11 selector by adapting the r10 read-only pattern: require r10’s exact remote tag/peel and terminal disposition, r11 tag/handoff absence, and the completed 08-26/27 summaries; it may make read-only checks only and must never schedule r10 or r11 preparation. Retain the closed pre-authorization projection/validator and add fixtures for all mixed authority states. Run the actual clean-clone PrepareAttempt fixture as part of this suite before any r11 tag exists.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08R11PreLive.ps1</automated></verify>
  <done>Only r11 with eleven exact histories can reach hosted preparation; r10 remains terminal-only and the pre-live/pre-authorization seams write nothing.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Carry r11 authority through the isolated one-module publisher workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1, scripts/quality/Test-Phase08LiveSeam.ps1</files>
  <behavior>
    - Publisher/workflow validation rejects stale r10 current requests, missing r10 history, altered aggregate, or reused r10 receipt/handoff.
    - Only r11 can reach a journal-selected single-module adapter with all eleven history values.
    - The token remains materialized solely inside the publisher job’s temporary Moon home; no prepare/observer/consumer path receives it.
  </behavior>
  <action>Write failing seam tests before updating publisher, live adapter, and workflow r10 fields to r11/eleven histories. Per D-02, D-03, D-05, D-06, D-07, and D-12, preserve one journal-selected `moon publish --frozen` adapter invocation per run and do not introduce a module loop. Keep default permissions read-only, actions pinned, `MOON_TOOLCHAIN_ROOT` pinned, and temporary publisher `MOON_HOME` cleanup mandatory. Tests are fixture-only: no hosted dispatch, secret, tag, or registry call is permitted in this plan.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1</automated></verify>
  <done>Only complete r11 authority can reach the isolated single-module publisher seam; stale r10 authority and credential leaks fail closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r10 terminal to r11 controller | Historical evidence cannot become an active root. |
| zero-write eligibility to publisher | Credential-free output cannot authorize a mutation. |
| workflow to adapter | Only publisher job accesses the credential and one module scope. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-28-01 | Tampering | eleven-history binding | critical | mitigate | Closed individual fields/aggregate in controller and publisher suites. |
| T-08-28-02 | Spoofing | r10 retry state | critical | mitigate | Remote tag/peel terminal checks and active-state rejection. |
| T-08-28-03 | Information Disclosure | publisher token | critical | mitigate | Publisher-only temporary Moon home with teardown. |
| T-08-28-04 | Elevation of Privilege | adapter | high | mitigate | One journal-selected module and no stale receipt/handoff. |
</threat_model>
<verification>Run r11 selector, strict history, publisher-negative, live-seam, parser, and diff checks; confirm no external action.</verification>
<success_criteria>r11 is fully wired through hosted and publisher seams while r10 remains immutable terminal evidence and no credential/mutation boundary is crossed.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-28-SUMMARY.md` when done.</output>
