---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "05"
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-ReleasePublisherNegative.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "The fresh immutable initial-attempt ref refs/tags/modules-v0.1.0-r1 creates a new initial intent/root at unchanged 0.1.0 module versions, while the protected base tag and failed run remain terminal historical evidence (D-01, D-15)."
    - "Prepared, publisher, and live-mutation validation accept only one exact r1 ref/source/root/current/prepared/actor binding and reject old, mixed, predecessor-bearing, or correction-lane inputs before credential materialization (D-02, D-03)."
    - "Publisher actor policy requires a closed sanitized exact-tchivs classification and makes actor mismatch, ambiguous stdout, stderr, nonzero exit, raw-output persistence, or teardown failure ineligible for authorization (D-03, D-04, D-18)."
    - "All work is local and credential-free: this plan creates no ref, performs no push or workflow dispatch, reads no secret, and invokes no publish command (D-04)."
  artifacts:
    - path: "policy/release-control.json"
      provides: "Closed base/r1/correction attempt-family and actor policy"
    - path: "release/intent/schema.json"
      provides: "Fresh r1 initial-intent schema contract"
    - path: "release/prepared/schema.json"
      provides: "Exact r1 prepared and actor-evidence bindings"
    - path: "scripts/quality/Invoke-ReleasePublisher.ps1"
      provides: "Fail-closed r1 publisher request guard"
    - path: "scripts/quality/Invoke-MooncakesLiveMutation.ps1"
      provides: "One-module live guard aligned to r1 and exact actor evidence"
  key_links:
    - from: "policy/release-control.json"
      to: "scripts/quality/New-ReleaseIntent.ps1"
      via: "attempt-family selection and monotonic r1 validation"
      pattern: "modules-v0.1.0-r1"
    - from: "release/intent/schema.json"
      to: "scripts/quality/Invoke-ReleasePublisher.ps1"
      via: "identical ref/source/root/current intent fields"
      pattern: "release_ref"
    - from: "scripts/quality/Invoke-ReleasePublisher.ps1"
      to: "scripts/quality/Invoke-MooncakesLiveMutation.ps1"
      via: "exact actor and prepared evidence before one-step adapter entry"
      pattern: "actor_match"
---

<objective>
Define the complete static r1 intent, prepared, publisher, and actor-policy boundary without touching any hosted service.

Purpose: Separate immutable release authority contracts from hosted orchestration so the later live attempt can run entirely from reviewed, committed source.
Output: Ten bounded policy/schema/runtime/test files implementing D-01 through D-04, D-15, and D-18.

Scope note: The ten-file warning is accepted without another plan because the work remains two independently executable contract slices: Task 1 owns five intent/actor-policy files and Task 2 owns five prepared/publisher-guard files, with no file overlap and separate focused verification commands. Neither task exceeds five modified files.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/hosted-toolchain-setup-failure.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-RESEARCH.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-04-SUMMARY.md
@policy/release-control.json
@release/intent/schema.json
@release/prepared/schema.json
@scripts/quality/Invoke-ReleasePublisher.ps1
@scripts/quality/Invoke-MooncakesLiveMutation.ps1

<interfaces>
Initial attempt zero is immutable at refs/tags/modules-v0.1.0 and SHA 198436a45b7403a3c28c98d5fa0d5ed6a958455f. Initial attempt one is refs/tags/modules-v0.1.0-r1, keeps all module versions at 0.1.0, has intent_kind initial, correction_sequence 0, no predecessor, and root_intent_sha256 equal to its newly generated intent_sha256. The modules-correction-N family remains exclusively for published-content mismatch and requires forward versions.
Actor evidence is a closed sanitized record with expected_actor=tchivs, observed_actor=tchivs, actor_check_classification=moon_whoami_exact, actor_exit_code=0, actor_stdout_line_count=1, actor_stderr_empty=true, actor_match=true, actor_raw_output_persisted=false, credential_state_removed=true, mutation_performed=false, and dry-run-only command classification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define fresh r1 intent and actor policy</name>
  <files>policy/release-control.json, release/intent/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <read_first>policy/release-control.json, release/intent/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-ReleaseIntent.ps1, .planning/debug/hosted-toolchain-setup-failure.md</read_first>
  <behavior>
    - Attempt zero and r1 are distinct immutable initial attempts; r1 generates a fresh deterministic intent/root while module identities, versions, and dependency floors remain the locked 0.1.0 values.
    - Old-source, old-intent, reused, non-monotonic, leading-zero, branch, mixed-ref/source, predecessor-bearing, and correction-family-as-retry inputs fail closed.
    - Exact actor evidence accepts only one normalized tchivs stdout line, empty stderr, zero exit, closed fields, teardown true, raw persistence false, and dry-only classification.
  </behavior>
  <action>Per D-01, D-03, D-04, D-15, and D-18, write the failing contract fixtures first, then extend the closed release-control policy, intent schema, generator, and shared validation helpers. Keep the protected attempt-zero ref/run/intent available only as historical negative evidence. Add the r1 initial-attempt family without changing module versions or conflating it with published-content correction. Define the exact actor projection in policy so hosted code has one authority source; reject parse ambiguity, mismatch, stderr, secret-shaped values, raw-output persistence, or failed credential teardown. Do not create a ref or contact GitHub/Mooncakes.</action>
  <verify><automated>pwsh -NoProfile -File scripts/quality/Test-ReleaseIntent.ps1 -ContractOnly</automated></verify>
  <done>The selector proves fresh r1 intent/root determinism, preserves attempt-zero terminal history, and rejects every ref/source/root/actor ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate the exact static boundary into qualification and publisher guards</name>
  <files>release/prepared/schema.json, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <read_first>release/prepared/schema.json, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, scripts/quality/Test-ReleasePublisherNegative.ps1, release/journal/record-schema.json, policy/phase-08-distribution.json</read_first>
  <behavior>
    - Qualification, prepared request, publisher, and one-module adapter require identical r1 ref/source/root/current/prepared/actor fields before command execution.
    - Attempt-zero current input, mixed digests, correction-as-retry, wrong module order/version, predecessor-bearing initial input, actor drift, and incomplete dry-run evidence stop before credentials or mutation.
    - Exact-existing resume authority is representable as a verified no-republish checkpoint bound to packet/ref/intent/observation/cold-proof digests; it never enters the mutation adapter.
  </behavior>
  <action>Per D-02 through D-07, D-14, D-15, and D-18, propagate the static r1 and actor contract through prepared schema, release qualification, publisher validation, and live-mutation eligibility. Preserve the single `moon publish --frozen` adapter seam, but make the outcome contract distinguish published-now from exact-existing checkpoint authority. Exact-existing must be eligible only after exact public observation plus cold proof and must return zero mutation count; mismatch requires terminal forward-correction evidence; unknown, disagreement, and timeout stop. Add adversarial tests for each closed branch and for packet/ref/intent drift. This task is static only and must not invoke the live adapter with a real command.</action>
  <verify><automated>pwsh -NoProfile -Command "&amp; { ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; ./scripts/quality/Test-ReleaseIntent.ps1 -ContractOnly }"</automated></verify>
  <done>Static qualification and publisher guards accept only exact r1 authority, expose a zero-mutation exact-existing checkpoint branch, and fail before credentials for every negative fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical attempt to r1 policy | Old ref/run/intent can enter only the immutable terminal-negative projection. |
| Intent/prepared data to publisher | Untrusted cross-stage values decide whether credential materialization is reachable. |
| Actor output to authorization policy | Untrusted CLI output is reduced to one closed sanitized identity classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Tampering | r1 ref/source/root bindings | critical | mitigate | Closed schemas, canonical digests, exact attempt-family fixtures, and reciprocal validation in all guards. |
| T-08-05-02 | Spoofing | publisher actor evidence | critical | mitigate | Exact tchivs projection with stdout/stderr cardinality, teardown, raw-output, and secret-shape checks. |
| T-08-05-03 | Elevation of Privilege | live adapter eligibility | critical | mitigate | All r1/prepared/actor checks precede credential or command reachability; exact-existing has zero mutations. |
| T-08-05-04 | Repudiation | attempt-zero history | high | mitigate | Preserve exact old ref/SHA/run identity as terminal evidence and prohibit reuse as current authority. |
</threat_model>

<verification>
Run both focused static suites, full credential-free Required, and git diff checking; inspect git status only for the ten plan-owned files and planning tracking paths.
</verification>

<success_criteria>
The ten-file static authority boundary is complete, deterministic, adversarially tested, and incapable of external action; both bounded five-file tasks pass independently.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-05-SUMMARY.md`. Commit implementation and normal GSD SUMMARY/tracking updates locally, but do not push; 08-07 performs the first push only after both 08-05 and 08-06 are complete.
</output>
