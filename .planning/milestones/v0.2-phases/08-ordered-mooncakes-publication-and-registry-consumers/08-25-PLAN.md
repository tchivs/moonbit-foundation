---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "25"
type: execute
wave: 25
depends_on: ["08-24"]
files_modified:
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Invoke-Phase08R10PreLive.ps1
  - scripts/quality/Test-Phase08PrepareHistorySchema.ps1
  - scripts/quality/Test-Phase08R10PreLive.ps1
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "The hosted controller, publisher seam, and workflow accept only r10 with attempt-zero+r1..r9 and reject all r9 active-attempt or handoff reuse (D-01, D-02, D-07)."
    - "The r10 pre-live selector is credential-free, does no write, and treats r9 as a required immutable partial terminal record rather than a candidate run."
    - "An r10 PreparedAttempt reaches fresh active-locator creation after the narrowly verified legacy-r8 history reader, while any altered r8/r9 history fails closed before side effects."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08R10PreLive.ps1"
      provides: "credential-free r10 eligibility selector"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r10 hosted preparation and authority seam"
    - path: ".github/workflows/publish-modules.yml"
      provides: "r10 exact history propagation and isolated secret boundary"
  key_links:
    - from: "r10 pre-live selector"
      to: "future r10 boundary plan"
      via: "read-only remote tag/commit and ten-history eligibility result"
      pattern: "historical_r9_sha256"
---

<objective>
Advance the hosted, publisher, and zero-write eligibility seams to the r10 forward-retry contract.

Purpose: Establish complete non-publishing r10 qualification before a future source push, boundary tag, authorization checkpoint, or Mooncakes mutation.
Output: r10 controller/workflow tests and pre-live selector only; no credential read, remote write, tag, dispatch, handoff, PublishOne, or registry mutation.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-24-SUMMARY.md
@.planning/debug/phase08-r9-history-schema-debug.md
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-Phase08R9PreLive.ps1
@scripts/quality/Invoke-ReleasePublisher.ps1
@scripts/quality/Invoke-MooncakesLiveMutation.ps1
@.github/workflows/publish-modules.yml

<interfaces>
From `Invoke-Phase08HostedRun.ps1`: `InitializeBoundary` precedes `PrepareAttempt`; all later modes consume the durable locator and exact history binding. r10 must add one r9 history pair without accepting historical state roots as active input.

From `Invoke-ReleasePublisher.ps1`: the live path validates a prepared request then calls a single injected one-module adapter. The plan must preserve this one-module boundary and must not add a module loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make r10 hosted preparation and pre-live selection history-complete</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Invoke-Phase08R10PreLive.ps1, scripts/quality/Test-Phase08PrepareHistorySchema.ps1, scripts/quality/Test-Phase08R10PreLive.ps1</files>
  <behavior>
    - An r10 fixture with ten ordered immutable histories, including r9’s existing tag/peel and pre-locator no-run disposition, produces a fresh r10 prepared attempt and active locator.
    - Any r9 active-root, locator, archive, index, store, tag, or handoff reuse is rejected before a new r10 locator is written.
    - The r10 pre-live selector returns a closed zero-write eligibility record only when r9 terminal facts, expected remote refs, clean owned summaries, and r10 tag/handoff absence agree.
    - A read-only pre-authorization validator accepts exactly one of two zero-mutation states: exact-existing authority with no packet, receipt, or handoff; or confirmed-absent authority with one valid packet and no receipt or handoff.
    - Altered legacy-r8 inventory/digest, altered r9 evidence, stale remote tag state, or a present r10 handoff fail closed.
  </behavior>
  <action>Begin with RED fixtures, then update the hosted controller from r9/nine histories to r10/ten histories. Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, preserve `3a761ae`’s exact r8 legacy reader and add a distinct, fully populated immutable r9 terminal record; do not treat either record as generally optional. Have the non-publishing flow emit one closed sanitized pre-authorization projection containing the active-attempt, authority, packet, receipt, fixed handoff, observation, and zero-mutation paths/digests. Add a read-only `ValidatePreAuthorization` hosted mode that consumes that projection and its fixed handoff path and accepts only the mutually exclusive exact-existing or confirmed-absent zero-mutation states: exact requires exact authority with packet/receipt/handoff absent; absent requires exactly one digest-valid packet with receipt/handoff absent. It must reject nonzero mutation counts and every mixed, missing, or premature authority artifact without writing. Implement `Invoke-Phase08R10PreLive.ps1` by adapting the established credential-free selector pattern: it may inspect committed policy/summaries and injected or real read-only remote facts, emits a closed sanitized result, and performs no filesystem output beyond its disposable test roots. It must require r10 absence and r9 remote tag/peel evidence, never schedule r9 preparation, and never create a boundary, locator, handoff, or active attempt. Retain strict source-path isolation and test the precise `InitializeBoundary` to `PrepareAttempt` order with a sentinel before any Git/provider/credential behavior.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08R10PreLive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1</automated>
  </verify>
  <done>Hosted preparation proves r10 can cross the historical reader only with the exact ten-history set; pre-authorization states are closed and receipt/handoff-free; the new selector treats r9 solely as terminal evidence with zero writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry r10 authority through the isolated publisher workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1, scripts/quality/Test-Phase08LiveSeam.ps1</files>
  <behavior>
    - Publisher and workflow validation require r10, all ten individual history values, and their canonical aggregate before a one-module adapter is reachable.
    - A stale r9 request, r9 authority receipt/handoff, missing r9 history field, altered aggregate, or non-isolated credential route is rejected.
    - The only credential materialization remains inside the publisher job’s new temporary `MOON_HOME`; prepare, observation, and consumer jobs receive none.
  </behavior>
  <action>Write failing seam and negative tests before changing the publisher, live adapter, and workflow. Per D-02, D-03, D-05, D-06, D-07, and D-12, advance request/receipt/handoff validation to r10 and thread the tenth history field through exact14-style hosted dispatch. Retain one journal-selected `moon publish --frozen` adapter invocation per workflow run; do not loop over modules or expose the secret to a prepare, observation, or consumer job. Ensure the workflow maintains read-only defaults, pinned actions, the pinned `MOON_TOOLCHAIN_ROOT`, and cleanup of the ephemeral publisher-only Moon home. These tests must only use fixtures/stubs and must not invoke a hosted run, dispatch, secret, tag, or registry call.</action>
  <verify>
    <automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08R10PreLive.ps1</automated>
  </verify>
  <done>Only r10 with complete ten-history binding can reach the isolated single-module publisher seam; all stale r9 authority and credential-boundary variants fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| r9 terminal evidence to r10 controller | Historical facts must not turn into a runnable root or active state. |
| Credential-free selector to hosted publisher | Eligibility output cannot materialize credentials or authorize a mutation. |
| Workflow to publisher adapter | Only the isolated publisher job may access the secret and invoke one module-scoped adapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-25-01 | Tampering | ten-history hosted binding | critical | mitigate | Closed field order, individual hash checks, and aggregate validation in controller/publisher tests. |
| T-08-25-02 | Spoofing | r9 retry identity | critical | mitigate | Require r9 tag/peel as terminal evidence while rejecting every r9 active-state input. |
| T-08-25-03 | Information Disclosure | workflow credential route | critical | mitigate | Keep credential materialization and cleanup inside the publisher-only temporary Moon home. |
| T-08-25-04 | Elevation of Privilege | live adapter | high | mitigate | Preserve one journal-selected module invocation and reject stale receipts/handoffs before adapter reachability. |
</threat_model>

<verification>
Run the r10 pre-live, strict history-schema, publisher-negative, and live-seam suites; parse every changed PowerShell script and validate workflow YAML with its existing local validation. Confirm the commit is credential-free and contains no network, tag, dispatch, handoff, or registry output.
</verification>

<success_criteria>
The repository has deterministic r10 hosted and publisher contracts, r9 cannot be replayed, and an executor can proceed to a separate non-publishing r10 boundary plan only after all focused checks pass.
</success_criteria>

<output>
Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-25-SUMMARY.md` when done.
</output>
