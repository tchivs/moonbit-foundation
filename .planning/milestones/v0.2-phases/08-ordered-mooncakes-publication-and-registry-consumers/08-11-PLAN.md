---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "11"
type: execute
wave: 11
depends_on: ["08-10"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "r4 is the sole current initial forward-retry with module 0.1.0, intent_kind initial, sequence 0, root=current, and null predecessor (D-01, D-07, D-15)."
    - "Attempt-zero, r1, r2, and r3 at 67b1fbc9dd62288d19018c46a44c1e3293212b76 are four distinct immutable terminal-negative histories."
    - "r3 binds PrepareAttempt and confirmed_absent followed by exact 17-versus-14 workflow/controller parity failure before gh run creation, with no hosted run, mutation, or authority."
    - "All eligibility schemas, prepared/index/store records, packet, receipt, authority, and handoff bind four individual history digests plus their canonical ordered-set digest and reject replacement/mixing/reorder."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r4 current profile and four exact terminal histories"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r4 handoff with four-history receipt-or-exact union"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r4 prepared/store identity"
  key_links:
    - from: "four terminal histories"
      to: "r4 eligibility artifacts"
      via: "four SHA-256 values plus ordered-set SHA-256"
      pattern: "historical_r3_sha256"
    - from: "r4 current profile"
      to: "intent/prepared schemas"
      via: "initial root equality and no predecessor"
      pattern: "modules-v0.1.0-r4"
---

<objective>Define and test the r4 initial retry and four immutable terminal-history bindings.

Purpose: Recover from r3's pre-run workflow input parity failure without rewriting history or using correction-N.
Output: Closed r4 policy/schemas/prepared contracts and adversarial tests.</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-workflow-receipt-input.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-10-SUMMARY.md
@policy/release-control.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the initial attempt family to r4</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r4 validates as current initial retry with unchanged 0.1.0 modules and no correction/predecessor.
    - Four histories have exact distinct facts/digests in canonical attempt-zero,r1,r2,r3 order.
    - r3 records PrepareAttempt, confirmed_absent, 17-vs-14 parity/missing-receipt failure before gh run creation, and no run/mutation/authority.
  </behavior>
  <action>Per D-01, D-07, and D-15, advance only the current initial profile from r3 to r4. Preserve prior history and add exact r3 facts/SHA from context. Define four individual digests and their canonical ordered-set digest; reject old-current selection, history collapse/reorder/mix, predecessor, sequence drift, and correction semantics. Keep correction-N exclusively for published-content mismatch with advanced versions. Commit this five-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r4 alone is current and all four prior attempts are exact immutable terminal histories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind four histories into authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Authority/receipt/handoff require four individual history digests and matching ordered set.
    - Mutation requires packet plus literal receipt; exact forbids both; stop is ineligible.
    - r3-current, replacement, duplicate, reorder, aggregate drift, stale UTC, or unknown fields fail.
  </behavior>
  <action>Extend closed schemas and fixtures to r4/four histories. Receipt binds packet digest, r4 ref, boundary/source SHA, literal authorize-core, UTC creation evidence, all histories/set, and its own digest. Handoff uses `%TEMP%/mnf-phase08-r4-handoff.json`, binds every file/path/digest, and admits one union branch. Add all substitution/mix/aggregate/branch negatives and commit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$p=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($f in $p){$null=Get-Content -LiteralPath $f -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All live schemas require four histories and preserve receipt-or-exact exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fresh r4 prepared, index, and store identity</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r4 root/genesis/prepared/index/store bind four histories/set and cannot reuse r3 state.
    - r4 remains initial/root-equal/sequence-zero/predecessor-null.
    - Missing, reordered, substituted, or cross-attempt records fail qualification.
  </behavior>
  <action>Extend canonical qualification and prepared generation from r3/three histories to r4/four histories. Every eligibility locator/index/store record carries all individual digests and recomputed ordered set. Add positive r4 and adversarial r3-current, old-root, reused-index, missing/reordered/mixed history, aggregate, and correction cases. Preserve UTC and no-tags isolation. Commit this five-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r4 prepared/store identity is fully four-history-bound and statically qualified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Four terminal attempts to r4 | Histories are evidence only. |
| Individual histories to set digest | Membership/order must be deterministic. |
| r3 state to r4 preparation | No locator/root/index reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-11-01 | Tampering | attempt family | critical | mitigate | Exact four facts/digests/set and replacement/reorder tests. |
| T-08-11-02 | Spoofing | r3 state as r4 | critical | mitigate | r4-only schemas and old-root/index rejection. |
| T-08-11-03 | Elevation of Privilege | receipt/handoff | critical | mitigate | Closed receipt-or-exact union bound to four histories. |
| T-08-11-04 | Repudiation | r3 termination | high | mitigate | Exact parity failure/no-run/no-mutation facts are digest-bound. |
</threat_model>

<verification>Run intent, prepared, and qualification suites; validate r4 invariants, four-history membership/order/digests, UTC, and no live effects.</verification>
<success_criteria>r4 and all four prior attempts are closed tested contracts ready for hosted integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-11-SUMMARY.md` when done.</output>
