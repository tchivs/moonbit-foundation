---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "16"
type: execute
wave: 16
depends_on: ["08-15"]
files_modified:
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-MooncakesObservation.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
  - scripts/quality/Invoke-Phase08R6PreLive.ps1
  - scripts/quality/Test-Phase08R6PreLive.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Publisher, workflow, and hosted controller accept only r6 and bind all six histories plus their ordered set (D-02, D-05, D-07)."
    - "Every workflow env mapping has unique keys and the exact 14-field controller/workflow/propagation receipt parity remains mechanical."
    - "Clean snapshot equal-empty passes; nonempty drift fails; UTC, LF, no-tags, and LibraryOnly isolation remain fail-closed."
    - "A read-only zero-write pre-live selector mechanically validates all six historical refs, artifacts, digests, ordered set, r5 no-run terminal facts, committed-clean 08-15/16 ownership, and r6 absence before any push, tag, root creation, or dispatch."
    - "No autonomous seam task pushes, tags, dispatches, accesses a secret, creates the fixed r6 handoff, or calls PublishOne."
  artifacts:
    - path: ".github/workflows/publish-modules.yml"
      provides: "r6 exact14 receipt-aware workflow with unique env keys"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r6 six-history hosted controller seam"
    - path: "scripts/quality/Test-Phase08LiveSeam.ps1"
      provides: "duplicate-key, exact14, LF/no-tags, and fixed-handoff isolation regressions"
    - path: "scripts/quality/Invoke-Phase08R6PreLive.ps1"
      provides: "Executable read-only r6 pre-live history and boundary gate"
  key_links:
    - from: "controller 14 fields"
      to: "workflow declarations and job propagation"
      via: "exact name, order, and count equality"
      pattern: "authorization_receipt_sha256"
    - from: "six individually validated histories"
      to: "historical_attempts_sha256"
      via: "canonical ordered-set recomputation"
      pattern: "historical_r5_sha256"
---

<objective>Wire r6 through the publisher, workflow, and hosted controller while retaining duplicate-key, exact14, snapshot, UTC, LF, no-tags, and isolation protections.

Purpose: Make the corrected r6 HostedPreflight seam structurally dispatchable without performing any external action.
Output: r6 hosted implementation and credential-free adversarial tests.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-workflow-duplicate-env.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-15-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce r6 and six histories in publisher and workflow</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1</files>
  <behavior>
    - r6 request binds initial root/current/prepared state and all six histories/set.
    - r5-current, fake r5 run, history drift, correction substitution, and second mutation fail before credential materialization.
    - Workflow env mappings have no duplicate keys and exact14 packet/receipt propagation remains closed.
  </behavior>
  <action>Per D-02, D-03, D-05, D-07, and D-14, advance the publisher, one-module adapter, and workflow from r5 to r6 and from five to six histories. Preserve one-module maximum, isolated ephemeral MOON_HOME, pinned MOON_TOOLCHAIN_ROOT, actor parsing, raw-output rejection, secret scoping, and exact14 controller/workflow packet-plus-receipt parity. Add a structural uniqueness assertion over every workflow env mapping, specifically protecting the historical r5 digest field from duplication or placement in the wrong job. Reject old-current requests, fake r5 run evidence, missing/reordered histories, set mismatch, correction substitution, second mutation, and field-parity drift before credentials.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Publisher and workflow accept only r6, carry six histories, and expose unique exact14 mappings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve snapshot, UTC, LF, no-tags, exact14, and fixed-handoff isolation</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-MooncakesObservation.ps1, scripts/quality/Test-ReleaseQualificationNegative.ps1</files>
  <behavior>
    - Equal empty Before/After snapshots pass and unequal nonempty ordinal snapshots fail tracked-mutation validation.
    - Controller/workflow retain exactly 14 names in the same order for start-empty and resume-valid propagation.
    - History-set identity derives from six individually validated terminal records, including r5's no-run form.
    - UTC/LF/--no-tags and LibraryOnly GUID fixtures never touch `%TEMP%/mnf-phase08-r6-handoff.json`.
  </behavior>
  <action>Per D-03, D-04, D-07, D-14, and D-18, extend hosted state to r6 and six histories while preserving the exact r5 pre-run failure form. Retain exact14 count/order/names/propagation and the existing cardinality, missing-receipt, ungrouped, collapse, and duplicate-key negatives. Preserve UTC offset/reload/tamper/stale detection, core.autocrlf=false LF bytes, `--no-tags` cloning, clean snapshot equality/drift, and LibraryOnly GUID roots. Production `%TEMP%/mnf-phase08-r6-handoff.json` is non-overridable; all static tests assert it is absent before and after and clean only paths they own. Run focused and adjacent suites without push, tag, dispatch, network, secret, StateRoot, registry, or publication effects.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseQualificationNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -HostedFieldsOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1 -WorkflowOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-MooncakesObservation.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r6 passes duplicate-key, exact14, six-history, snapshot, UTC, LF, no-tags, and isolation regressions with zero live effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the zero-write r6 pre-live history selector</name>
  <files>scripts/quality/Invoke-Phase08R6PreLive.ps1, scripts/quality/Test-Phase08R6PreLive.ps1</files>
  <behavior>
    - The selector exits nonzero before live work unless attempt-zero and r1 through r5 refs, peels, individual digests, and canonical ordered-set digest all match immutable recorded evidence.
    - r5 must bind exact source/tag, successful PrepareAttempt, fresh `confirmed_absent`, duplicate-env pre-run rejection, zero publish runs, and zero dry-run/packet/receipt/handoff/PublishOne/mutation/successor effects.
    - Every historical ExecutionRoot, StateRoot, locator, index, and store path exists, is read-only to the selector, and matches its recorded immutable digest.
    - The complete 08-15/16 owned file sets and summaries are committed-clean at HEAD, both summary commits are ancestors of HEAD, and r6 tag/fixed handoff are absent.
    - Success emits one closed sanitized JSON object to stdout and performs no filesystem, Git, GitHub, registry, or temporary-state mutation.
  </behavior>
  <action>Per D-01, D-04, D-07, D-14, D-15, D-18, and D-19, add `Invoke-Phase08R6PreLive.ps1 -Check -Repository tchivs/moonbit-foundation -Remote origin` as the mandatory first live selector. Compose existing canonical hashing, intent, hosted-run, observation, and history validators without creating a StateRoot or accepting override paths. Mechanically validate: all six configured history records; every remote annotated tag object and peel; every individual history digest and the recomputed LF-ordered set; r5 source `df105f06205298f1f82ac2f2cdca214d69d42e15`; r5 tag object `4a11582cf9aeae15802cf4f6d7394b013ece63ac`; successful PrepareAttempt; the bound fresh credential-free core observation with terminal `confirmed_absent`; the duplicate environment-key rejection before run creation; zero r5 publish workflow runs; and zero PublisherDryRun, packet, receipt, handoff, PublishOne, mutation, and successor artifacts. Walk every historical record's ExecutionRoot, StateRoot, boundary/active locator, index, and store path; require existence, path containment, immutable file SHA-256, record cross-bindings, and no digest drift. Resolve the 08-15 and 08-16 plan-owned file lists, require both worktree and index clean for those exact paths, require both summary files committed at HEAD, and require their last-touch commits to be ancestors of HEAD. Finally require remote/local r6 tag absence and `%TEMP%/mnf-phase08-r6-handoff.json` absence. Emit only a closed sanitized result containing the six digests/set, r5 terminal disposition, zero-effect counts, summary commit ids, HEAD, and absence booleans. Add isolated bare-remote and GUID-root fixtures for missing/moved refs, bad peel, digest/set drift, fabricated r5 run, non-absent observation, nonzero downstream count, missing/mutated historical path, dirty owned file, missing/non-ancestor summary, existing r6 tag/handoff, attempted output write, and the complete positive case.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08R6PreLive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>The executable selector proves the entire immutable pre-live boundary read-only and fails before any live side effect on every historical, filesystem, commit, or absence drift.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Controller to workflow parser | Duplicate YAML/environment keys must not shadow a digest binding. |
| Controller to workflow inputs | Exact14 declaration and propagation parity is mandatory. |
| Static tests to production state | Fixtures cannot touch fixed r6 handoff, tags, or external state. |
| Historical filesystem and remote refs to live execution | The pre-live selector must prove immutable existence and identity without writing. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-16-01 | Tampering | workflow env mappings | critical | mitigate | Mechanical unique-key validation and duplicate-key regression. |
| T-08-16-02 | Tampering | exact14 parity | critical | mitigate | Exact declaration/propagation equality in focused tests. |
| T-08-16-03 | Spoofing | history set | critical | mitigate | Recompute from six validated records and reject a fake r5 run. |
| T-08-16-04 | Elevation of Privilege | packet/receipt | critical | mitigate | Paired digest validation and stale alias rejection. |
| T-08-16-05 | Information Disclosure | hosted evidence | high | mitigate | Credential isolation and raw-output rejection. |
| T-08-16-06 | Tampering | pre-live historical boundary | critical | mitigate | Zero-write selector validates refs, peels, digests, stores, commits, and r6 absence before live work. |
</threat_model>

<verification>Run publisher-negative, qualification-negative, HostedFieldsOnly, WorkflowOnly, full live-seam, qualification, observation, and r6 pre-live selector suites; confirm unique env keys, exact14, six histories, immutable old roots/index/store/locators, committed-clean summaries, snapshot/UTC/LF/no-tags/isolation, r6 absence, and no external effects.</verification>
<success_criteria>The r6 static hosted seam is ready for one clean non-publishing live preflight and later explicitly authorized resume.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-16-SUMMARY.md` when done.</output>
