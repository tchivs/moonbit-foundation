---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "13"
type: execute
wave: 13
depends_on: ["08-12"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "r5 is the sole current initial forward-retry with module 0.1.0, intent_kind initial, sequence 0, root=current, and no predecessor (D-01, D-07, D-15)."
    - "Attempt-zero, r1, r2, r3, and r4 at ee4a8eb9b8dca5d69b404c9a4a1cd81608a5462a are five distinct immutable terminal-negative histories."
    - "r4 binds unique failed HostedPreflight run 29667231047/1, clean-snapshot empty Before binding failure during credential-free qualification, and zero dry-run/packet/receipt/handoff/mutation."
    - "All eligibility artifacts bind five individual history digests plus canonical ordered-set digest and reject replacement, mixing, duplication, or reorder."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r5 current profile and five histories"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r5 five-history union handoff"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r5 prepared/store identity"
  key_links:
    - from: "five histories"
      to: "r5 prepared/authority artifacts"
      via: "five SHA-256 plus ordered-set SHA-256"
      pattern: "historical_r4_sha256"
---

<objective>Define and test r5 initial retry and five immutable terminal histories.

Purpose: Recover from r4's clean-checkout snapshot binding failure without correction-N or history rewrite.
Output: Closed r5 policy/schemas/prepared contracts.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/resolved/phase08-clean-tracked-snapshot.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-12-SUMMARY.md
@policy/release-control.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend initial attempt family to r5</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r5 is current initial; 0.1.0/root/sequence/predecessor invariants remain.
    - Five histories have exact distinct facts/digests in attempt-zero,r1,r2,r3,r4 order.
    - r4 records run 29667231047/1 clean-snapshot empty binding failure and zero downstream artifacts/mutation.
  </behavior>
  <action>Advance only current profile r4→r5. Preserve four histories and add exact r4 ref/SHA/run/stage/outcome facts. Define five individual digests and ordered-set digest. Reject old-current selection, collapse/reorder/mix, predecessor, sequence/correction drift. Keep correction-N for published mismatch only. Commit five-file unit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r5 alone is current and five histories are immutable/distinct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind five histories into authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Authority/receipt/handoff require five individual digests and matching ordered set.
    - Mutation requires packet+literal receipt; exact forbids both; stop ineligible.
    - r4-current, replacement/duplicate/reorder/set drift/stale UTC fails.
  </behavior>
  <action>Extend closed schemas and fixtures to r5/five histories. Receipt binds packet, r5 ref/boundary/source, literal authorize-core, UTC evidence, five histories/set, own digest. Handoff uses `%TEMP%/mnf-phase08-r5-handoff.json`, binds every path/file/digest, admits one union. Add branch/history adversarial cases. Commit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$p=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($f in $p){$null=Get-Content -LiteralPath $f -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All live schemas require five histories and receipt-or-exact exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fresh r5 prepared, index, store, and qualification identity</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r5 root/genesis/prepared/index/store bind five histories/set and reject r4 state.
    - Clean tracked snapshots allow equal empty Before/After while unequal nonempty snapshots fail.
    - Missing/reordered/substituted history or cross-attempt records fail qualification.
  </behavior>
  <action>Extend canonical qualification/prepared generation to r5/five histories. Preserve the clean-snapshot fix: empty equals empty is a valid clean baseline, ordinal nonempty drift remains REL14 failure. Every eligibility locator/index/store carries five digests/set. Add positive r5 and adversarial r4-current/old-root/reused-index/history/set/correction cases. Preserve UTC/no-tags. Commit atomically.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r5 identity is five-history-bound and clean-snapshot-safe.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Five prior attempts to r5 | Histories are evidence only. |
| Clean snapshot to mutation detector | Empty equality is valid; drift must fail. |
| r4 state to r5 preparation | No state reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-13-01 | Tampering | attempt family | critical | mitigate | Exact five facts/digests/set and substitution tests. |
| T-08-13-02 | Spoofing | r4 state as r5 | critical | mitigate | r5-only schemas and old-state rejection. |
| T-08-13-03 | Elevation of Privilege | receipt/handoff | critical | mitigate | Closed union bound to five histories. |
| T-08-13-04 | Tampering | tracked snapshot | high | mitigate | Equal-empty accepted; unequal nonempty ordinal drift rejected. |
| T-08-13-05 | Repudiation | r4 failure | high | mitigate | Exact run/stage/no-downstream/no-mutation facts digest-bound. |
</threat_model>

<verification>Run intent/prepared/qualification suites; validate r5, five histories, clean snapshot equality/drift, UTC, and no live effects.</verification>
<success_criteria>r5 and five histories are closed tested contracts ready for hosted integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-13-SUMMARY.md` when done.</output>
