---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "36"
type: execute
wave: 36
depends_on: ["08-35"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Test-Phase08PrepareHistorySchema.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "The r13 HostedRun dispatch boundary accepts exactly thirteen ordered history digests, including r12, and recomputes their canonical LF aggregate before it accepts any dispatch-shaped request."
    - "Stale twelve-history r11 vectors, missing r12, reordered inputs, and altered aggregate values fail before dispatch/provider/credential effects."
    - "The static end-to-end publisher/hosted seam passes fixture-only with the Plan 34 task-owned dirty-baseline guard before and after each task."
  artifacts:
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r13 thirteen-history HostedDispatch contract"
    - path: "scripts/quality/Test-Phase08LiveSeam.ps1"
      provides: "end-to-end static r13 HostedDispatch regression"
  key_links:
    - from: "Invoke-ReleasePublisher r13 request"
      to: "Invoke-P08HostedDispatch"
      via: "thirteen ordered individual history digests and canonical aggregate"
      pattern: "historical_r12_sha256"
---
<objective>Repair the r13 thirteen-history HostedRun dispatch integration exposed by the completed 08-35 Task 1 and in-progress Task 2 seams.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-35-PLAN.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
@scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Test-Phase08LiveSeam.ps1
@scripts/quality/Test-ReleasePublisherNegative.ps1</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock the failing r13 HostedDispatch integration contract</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md, scripts/quality/Test-Phase08LiveSeam.ps1, scripts/quality/Test-ReleasePublisherNegative.ps1, scripts/quality/Test-Phase08PrepareHistorySchema.ps1</files>
  <behavior>
    - The full static seam supplies attempt-zero then r1 through r12 and requires `historical_r12_sha256` plus the recomputed thirteen-item LF aggregate at HostedDispatch.
    - A legacy twelve-history request, missing/altered r12, reordered history, or aggregate mismatch fails with the HostedRun history rule before any dispatch/provider/credential action.
    - Fixtures make no tag, workflow dispatch, credential materialization, authorization artifact, Mooncakes request, registry mutation, or publication.
  </behavior>
  <action>First update or verify routing documents: ROADMAP must list Wave 36 after Wave 35, and STATE plus r13 recovery context must name 08-36 as the required static integration before any later r13 boundary or authorization plan. Verify the Plan 34 baseline before and after this task with exactly these routing and test task-owned files. Add RED end-to-end static seam coverage for the observed `P08-HOSTED-HISTORY` failure and direct adversarial checks at the HostedDispatch boundary. Update only fixture builders/assertions that must carry the r13 thirteenth history; do not modify publisher, adapter, workflow, schemas, policy, pre-live selection, or any r12 artifact. This is a follow-up integration fix to 08-35, never a rerun or reuse of r12.</action>
  <verify><automated>$taskPaths=@('.planning/ROADMAP.md','.planning/STATE.md','.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md','scripts/quality/Test-Phase08LiveSeam.ps1','scripts/quality/Test-ReleasePublisherNegative.ps1','scripts/quality/Test-Phase08PrepareHistorySchema.ps1'); pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $road=Get-Content -Raw ./.planning/ROADMAP.md; $state=Get-Content -Raw ./.planning/STATE.md; $route=Get-Content -Raw ./.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md; if($road -notmatch '\*\*Wave 36\*\*' -or $road.IndexOf('**Wave 36**') -lt $road.IndexOf('**Wave 35**')){throw 'P08-R13-ROADMAP-36'}; if($state -notmatch '(?m)^status: executing — required next static integration is 08-36 after 08-35 and before any r13 boundary or authorization; obsolete r12 authority is quarantined$'){throw 'P08-R13-STATE-STATUS'}; if($state -notmatch '(?m)^Plan: 36 of 36 \(08-33 quarantined; 08-36 is the mandatory static integration after 08-35\)$'){throw 'P08-R13-STATE-POSITION'}; if($state -notmatch '(?m)^- Phase 8 plans completed: 33/36 \(08-33 quarantined; 08-34 complete, 08-35 precedes mandatory 08-36 static integration\)$'){throw 'P08-R13-STATE-PROGRESS'}; if($state -notmatch '(?m)^\*\*Resume file:\*\* 08-36-PLAN.md$'){throw 'P08-R13-STATE-RESUME-FILE'}; if($state -notmatch '(?m)^Resume with: `/gsd-execute-phase 8` to complete mandatory 08-36 static HostedDispatch integration after 08-35; do not create an r13 boundary or seek authorization until 08-36 passes\.$'){throw 'P08-R13-STATE-NEXT'}; if($route -notmatch '08-36'){throw 'P08-R13-CONTEXT-36'}; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE -eq 0) { throw 'P08-R13-HOSTED-RED: expected pre-fix HostedDispatch failure.' }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths</automated></verify>
  <done>Focused tests reproduce the twelve-history HostedDispatch rejection and define only the r13 thirteen-history success/failure contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Advance HostedRun dispatch to the r13 thirteen-history boundary</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1</files>
  <behavior>
    - `Invoke-P08HostedDispatch` accepts exactly thirteen ordered individual digests, including r12, and verifies their canonical LF aggregate before dispatch-shaped work.
    - No twelve-history compatibility path remains reachable; malformed vectors stop with `P08-HOSTED-HISTORY` before provider, dispatch, credential, registry, or mutation effects.
  </behavior>
  <action>Verify the Plan 34 baseline before and after this task with exactly `scripts/quality/Invoke-Phase08HostedRun.ps1` as the task-owned path. Implement the smallest direct HostedRun repair: update the closed field set, expected r13 sequence, individual-digest validation, and aggregate recomputation at `Invoke-P08HostedDispatch` and its immediate static serialization checks. Do not modify the already-owned publisher/adapter/workflow producers unless the tests prove an immediate HostedRun interface mismatch that cannot be fixed in HostedRun; if that occurs, stop and create a new narrow follow-up plan. Run the complete static seam after GREEN. No tags, workflow dispatches, credentials, authorization receipts/handoffs, Mooncakes/registry calls, or publications are authorized.</action>
  <verify><automated>$taskPaths=@('scripts/quality/Invoke-Phase08HostedRun.ps1'); pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths</automated></verify>
  <done>The full static r13 seam no longer raises `P08-HOSTED-HISTORY`; only thirteen-history requests reach the HostedRun dispatch boundary, and baseline equality remains proven.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r13 publisher request to HostedRun | Every individual history and aggregate must agree before dispatch-shaped processing. |
| task-owned edits to user dirties | Baseline guard permits only the active task's declared files outside protected user paths. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-36-01 | Tampering | HostedDispatch history vector | critical | mitigate | Closed thirteen-field ordering and aggregate adversarial tests. |
| T-08-36-02 | Elevation of Privilege | stale r12/r11 dispatch path | critical | mitigate | Reject all twelve-history and malformed vectors before provider or dispatch effects. |
| T-08-36-03 | Tampering | worktree drift | high | mitigate | Baseline Verify before/after each task with exact task-owned paths. |
</threat_model>
<verification>Run the focused RED/GREEN and full static seam suites with the baseline guard. Stop on any unexpected producer mismatch; do not compensate by touching non-owned files.</verification>
<success_criteria>The r13 static integration accepts thirteen histories through HostedDispatch; no r12 route is reactivated and no live release state changes.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-36-SUMMARY.md` when done.</output>
