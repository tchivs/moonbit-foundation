---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "30"
type: execute
wave: 30
depends_on: ["08-29"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "r12 is the sole current initial retry and binds attempt-zero+r1..r11 as twelve immutable terminal histories."
    - "r11 is bound as exact tag/peel terminal evidence; its canonical remote-clone provider path and noncanonical caller mismatch remain distinct and neither is reused."
    - "Prepared, authority, receipt, and handoff contracts require twelve individual digests and their LF aggregate before publisher access."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r12 current policy and r11 terminal history"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "closed twelve-history r12 authority contract"
---
<objective>Advance credential-free release contracts to r12 with complete twelve-history binding.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-29-SUMMARY.md
@.planning/debug/phase08-r11-real-ref-mismatch.md
@policy/release-control.json
@scripts/quality/ReleaseQualification.Common.ps1</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Record immutable r11 evidence and close r12 schemas</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Valid r12 artifacts contain exactly attempt-zero then r1 through r11, individual digests, and the canonical LF aggregate.
    - r11 binds tag object 735ad679, peel/source 30479a, and only attested invocation facts; missing/reordered/mutated r11 evidence fails closed.
    - r11 cannot become current or provide state, locator, archive, packet, receipt, or handoff input.
  </behavior>
  <action>Write RED tests then update policy and closed initial/prepared/authority/receipt/handoff schemas to `modules-v0.1.0-r12`. Per D-01, D-07, D-14, D-15, and D-18, record r11 from its checkpoint and debug evidence without collapsing canonical remote-clone success into the failed noncanonical external invocation or inventing unobserved effects. Require twelve individual values plus aggregate in every artifact; preserve all earlier records exactly and reject optional-field compatibility paths.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1</automated></verify>
  <done>r12 alone is current and all static contracts bind r11 as immutable twelfth history evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind fresh r12 preparation to contracts and canonical boundary wrapper</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r12 preparation needs twelve histories and a new root; r11 state/path reuse fails before provider work.
    - FixtureOnly invokes the r12 canonical boundary wrapper and the real remote-clone gate before any r12 tag exists.
  </behavior>
  <action>After RED, advance shared history serialization and bundle validation to r12 while consuming the canonical boundary wrapper from 08-29. Require a new r12 root and canonical bytes, carry r11 only as digest evidence, and retain the real remote clone ref test as a pre-tag gate. This plan remains local/credential-free: no push, r12 tag, dispatch, secret, PublishOne, or registry action.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08RemoteCloneRef.ps1</automated></verify>
  <done>Only fresh r12 preparation with twelve histories and canonical clone-ref invocation passes before an r12 tag exists.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| r11 checkpoint to r12 policy | Only attested r11 identity/disposition facts enter history. |
| canonical wrapper to prepared bundle | Fresh preparation must use clone-policy ref without state reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-30-01 | Tampering | r11 record | critical | mitigate | Object/peel/fact/digest binding and twelve-history tests. |
| T-08-30-02 | Spoofing | r12 ref | critical | mitigate | Wrapper-backed remote-clone gate before tag. |
| T-08-30-03 | Elevation of Privilege | prepared artifact | high | mitigate | Fresh roots, closed schemas, no publisher boundary. |
</threat_model>
<verification>Run intent, prepared, FixtureOnly, remote-clone, parser, and diff checks; no external mutation.</verification>
<success_criteria>r12 static contracts cannot reuse r11 and cannot qualify a noncanonical caller ref.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-30-SUMMARY.md` when done.</output>
