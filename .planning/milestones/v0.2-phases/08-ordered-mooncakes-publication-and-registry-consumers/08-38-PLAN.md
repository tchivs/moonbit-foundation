---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "38"
type: execute
wave: 38
depends_on: ["08-37"]
files_modified:
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08PreAuthorization.ps1   # if it exists; otherwise create
  - scripts/quality/Test-Phase08HostedBinding.ps1      # if it exists; otherwise create
autonomous: false   # Task 3 is a blocking checkpoint
requirements: [DIST-01, DIST-04]   # publication-path prerequisite; does NOT complete publication
must_haves:
  truths:
    - "The 3 active-authority r12 hardcodes in Invoke-Phase08HostedRun.ps1 (lines 208, 229, 1042) are upgraded to r13 while the 2 terminal-history r12 references (lines 662, 667) remain r12 as immutable REL01-REF evidence."
    - "HostedPreflight and PublisherDryRun run through the r13 boundary wrapper with publisher-only temp Moon home + teardown and zero mutation."
    - "%TEMP%/mnf-phase08-r13-preauthorization.json exists with authority_variant=confirmed_absent, one packet, zero mutation, no receipt, no handoff."
    - "The eight-path user-dirty baseline stays unchanged; every other drift fails closed."
    - "Only absent displays same-turn authorize-core; stop leaves no receipt/handoff. No Mooncakes mutation."
  artifacts:
    - path: "%TEMP%/mnf-phase08-r13-user-dirty-baseline.json"
      provides: "recaptured current-HEAD eight-path baseline"
    - path: "%TEMP%/mnf-phase08-r13-preauthorization.json"
      provides: "closed r13 absent-branch pre-authorization"
---
<objective>Complete the r13 publication path that 08-37 deferred: upgrade the three active-authority r12 hardcodes in Invoke-Phase08HostedRun.ps1 (lines 208, 229, 1042) to r13 via RED-then-GREEN TDD while leaving the two terminal-history r12 references (lines 662, 667) untouched as immutable REL01-REF evidence, then run HostedPreflight + PublisherDryRun through the r13 boundary wrapper with a publisher-only temporary Moon home and emit the absent-variant preauthorization.json, then stop at the absent-only authorize-core checkpoint.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-PLAN.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-PLAN.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
@scripts/quality/Invoke-Phase08HostedRun.ps1
@scripts/quality/Invoke-Phase08R13Boundary.ps1
@scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
@policy/release-control.json</context>
<tasks>
<task type="auto" non_publishing="true">
  <name>Task 1: TDD-upgrade the three active-authority r12 hardcodes in Invoke-Phase08HostedRun.ps1 to r13</name>
  <files>scripts/quality/Invoke-Phase08HostedRun.ps1 (lines 208, 229, 1042-1044), scripts/quality/Test-Phase08PreAuthorization.ps1 (created), scripts/quality/Test-Phase08HostedBinding.ps1 (created)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-SUMMARY.md lines 76-90 (the deferral note and the exact five-site list this task addresses; only the three active-authority sites change, the two terminal-history sites stay r12)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-PLAN.md Task 1 Step 1 (the baseline-capture template) and `<done>` lines (TDD discipline)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 lines 205-232 (Assert-P08PreAuthorization — the two active-authority sites at 208 and 229, plus the PREAUTH-CLOSED projection check and the PREAUTH-ABSENT message text)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 lines 655-685 (PrepareAttempt — the terminal-history sites at 661/662 and 666-684 that MUST stay r12; the `current_attempt -cne 'r13'` at 661 is already r13 from 08-35)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 lines 1030-1046 (the later-mode common path — the active-authority binding at 1042-1044 with rule id P08-HOSTED-R12-BINDING and message 'Only the exact r12 release binding is accepted.')
    - scripts/quality/Test-Phase08R13Boundary.ps1 (the existing r13 test idiom: `Set-StrictMode -Version Latest`, `Throw-P08R13*` helper, `Confirm-P08R13Failure` negative helper, GUID-owned temp root with try/finally teardown)
    - scripts/quality/Test-Phase08TagBoundHostedStore.ps1 lines 34-60 (the disposable-clone + fixture-tag + HostedRun-candidate staging pattern the new binding test will mirror)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 (the eight-path guard and its task-owned path normalization)
    - policy/release-control.json lines 316-360 (the immutable r12 terminal record and `current_attempt=r13`, `initial_profile.release_ref=refs/tags/modules-v0.1.0-r13`)
  <action>
    The goal of this task is the **publication-path side of the r13 upgrade**: bring `Invoke-Phase08HostedRun.ps1`'s active-authority checks into agreement with the already-r13 policy and the already-r13 boundary tag (object `611fb286`, peel `003b604`, established by 08-37). This unblocks HostedPreflight, PublisherDryRun, AssembleAuthorizationPacket, and ValidatePreAuthorization for r13. The two terminal-history r12 references at lines 662 and 667 MUST remain r12 — they are immutable REL01-REF evidence, not active authority. This task MUST NOT create, push, fetch, or verify any r13 tag (the tag already exists from 08-37), MUST NOT run any hosted mode, MUST NOT observe the registry, MUST NOT read any credential, and MUST NOT emit any packet/receipt/handoff/preauthorization.

    **Step 1 — Baseline capture (current HEAD).** The Task 2 baseline from 08-37 was consumed (see 08-37-SUMMARY.md line 90). Before any edit, recapture against current HEAD:
    ```
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Capture `
      -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" `
      -TaskOwnedPath "scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1"
    ```
    The captured head MUST equal `git -C $repoRoot rev-parse HEAD`. The baseline file schema MUST be `mnf-phase08-r13-dirty-baseline/1` with exactly the eight protected paths recorded by `Assert-Phase08R13DirtyBaseline.ps1` lines 19-28. Both new test files are listed as task-owned so they may be created without tripping `P08-R13-UNEXPECTED-DRIFT`; `Invoke-Phase08HostedRun.ps1` is task-owned because Task 1 edits it.

    **Step 2 — Write RED tests first (TDD).** Create two test files mirroring the `Test-Phase08R13Boundary.ps1` idiom (`Set-StrictMode -Version Latest`, `$ErrorActionPreference='Stop'`, GUID-owned temp root under `[IO.Path]::GetTempPath()`, `try { ... } finally { Remove-Item -LiteralPath $root -Recurse -Force }`).

    **Step 2a — Create `scripts/quality/Test-Phase08PreAuthorization.ps1`.** This test exercises `Assert-P08PreAuthorization` (lines 205-232) directly by dot-sourcing `Invoke-Phase08HostedRun.ps1` after defining a stub `Throw-P08HostedRule` that re-throws `"$Id`: $Message"`, a stub `Get-P08Sha256` returning a fixed 64-hex digest, and a stub `Get-P08SelfExcludingDigest` returning a fixed 64-hex digest. The test MUST cover:
      - **RED case A (PREAUTH-CLOSED active release_ref):** build a projection whose `schema_version='mnf-phase08-pre-authorization/1'`, `release_ref='refs/tags/modules-v0.1.0-r13'`, `authority_variant='confirmed_absent'`, `fixed_handoff_path=<temp absent path>`, `fixed_handoff_sha256=$null`, `mutation_count=0`, `output_write_count=0`, `active_attempt_path=$null`, `authorization_receipt_path=$null`, `authorization_receipt_sha256=$null`, `exact_existing_authority_path=$null`, `exact_existing_authority_sha256=$null`, plus a real `mutation_authorization_packet_path` + matching `mutation_authorization_packet_sha256` (written from a packet fixture with `release_ref='refs/tags/modules-v0.1.0-r13'` and a placeholder `packet_sha256`), plus `observation_path`/`observation_sha256` either both null or both bound. BEFORE the line-208 edit, this projection throws `P08-PREAUTH-CLOSED` because line 208 still requires `r12`. AFTER the edit, this projection passes and returns the projection object. Assert the RED run throws `P08-PREAUTH-CLOSED` first (capture the failure message), then the GREEN run (after Step 4) returns without throwing.
      - **RED case B (PREAUTH-ABSENT packet release_ref):** build the same projection but with the packet fixture carrying `release_ref='refs/tags/modules-v0.1.0-r12'`. BEFORE the line-229 edit, this case is masked by case A (line 208 fails first); AFTER the line-208 edit but BEFORE the line-229 edit, this case throws `P08-PREAUTH-ABSENT` with the message containing `r12 packet only`. AFTER the line-229 edit, the same case (now constructed with `release_ref='refs/tags/modules-v0.1.0-r13'`) passes. Assert both phases explicitly.
      - **Adversarial case C (r12 terminal-history regression — mirror of T-08-38-02):** dot-source `Invoke-Phase08HostedRun.ps1` and call its `PrepareAttempt` mode through a disposable clone fixture (mirror `Test-Phase08TagBoundHostedStore.ps1` lines 34-58: `git clone --quiet --no-local --no-tags`, configure identity, copy the post-edit `Invoke-Phase08HostedRun.ps1` into the clone and commit it, synthesize a clone-local r13 tag at HEAD, detach at the peel, invoke `-Mode PrepareAttempt` with the r13 locator). Then construct a *mutated* policy fixture where `terminal_negative_history[12].mutation_count=1` and re-run `PrepareAttempt`; it MUST throw `P08-PREPARE-HISTORICAL-BINDING`. Also assert that the unmutated r12 terminal record (`attempt=r12`, `release_ref=refs/tags/modules-v0.1.0-r12`, `source_sha=5e7b19cdc74ec11d5c524ff34a36c266b15bba39`, `tag_object_sha=57b76c9f9044d3190acc1e4c3fb7ada516f4dece`, `record_sha256=92397fbdfc679f154382928ee6f94c57e46b40b7c5f7e8d65759b0165d6c96a8`, `failure_code=REL01-REF`) is still accepted by the historical-binding check at lines 666-684. This proves the line-662/667 r12 references were NOT corrupted by Task 1.

    **Step 2b — Create `scripts/quality/Test-Phase08HostedBinding.ps1`.** This test exercises the later-mode common path (lines 1030-1046) by staging a disposable clone of the post-edit `Invoke-Phase08HostedRun.ps1` (mirror `Test-Phase08TagBoundHostedStore.ps1`), synthesizing a clone-local r13 tag at HEAD, and invoking `-Mode HostedPreflight -LibraryOnly` (which returns at line 1038 after passing the binding check at 1042). The test MUST:
      - **RED case D:** BEFORE the line-1042 edit, invoke with `$ReleaseRef='refs/tags/modules-v0.1.0-r13'` and assert the call throws `P08-HOSTED-R12-BINDING` with message containing `r12 release binding`. (This is the failure 08-37 Task 3 Step 7 hit.)
      - **GREEN case D:** AFTER the line-1042/1044 edit, invoke with `$ReleaseRef='refs/tags/modules-v0.1.0-r13'` and assert it returns past the binding check (reaching the `LibraryOnly` early return at 1038) with no throw.
      - **Negative case E:** AFTER the edit, invoke with `$ReleaseRef='refs/tags/modules-v0.1.0-r12'` and assert the call throws `P08-HOSTED-R13-BINDING` with message containing `r13 release binding` — proving the new binding rejects stale r12.
      - Static-source scan: read the post-edit `Invoke-Phase08HostedRun.ps1` source and assert the literal `'P08-HOSTED-R13-BINDING'` is present and `'P08-HOSTED-R12-BINDING'` is absent; assert the literal `'Only the exact r13 release binding is accepted.'` is present and `'Only the exact r12 release binding is accepted.'` is absent.

    **Step 3 — Confirm RED.** Run both new tests against the unmodified `Invoke-Phase08HostedRun.ps1`. Cases A and D MUST throw their respective RED rule ids; case C's unmutated sub-case passes (the r12 terminal record is already accepted), and its mutated sub-case throws `P08-PREPARE-HISTORICAL-BINDING`. If any RED case does not throw as expected, STOP — the test or the source has drifted from this plan's assumptions.

    **Step 4 — Change EXACTLY three active-authority sites (and the one message text they own).** Apply four edits to `scripts/quality/Invoke-Phase08HostedRun.ps1` and NO others:

      - **Line 208 (P08-PREAUTH-CLOSED active release_ref):** change `$Projection.release_ref -cne 'refs/tags/modules-v0.1.0-r12'` → `$Projection.release_ref -cne 'refs/tags/modules-v0.1.0-r13'`. The remainder of line 208 (the `schema_version` check and the `PSObject.Properties.Name` join) is unchanged; the rule id `P08-PREAUTH-CLOSED` and its message `'Pre-authorization projection shape drifted.'` are unchanged.
      - **Line 229 (P08-PREAUTH-ABSENT packet release_ref + message text):** change `$packet.release_ref -cne 'refs/tags/modules-v0.1.0-r12'` → `$packet.release_ref -cne 'refs/tags/modules-v0.1.0-r13'` AND change the message `'Confirmed-absent authority requires one digest-valid r12 packet only.'` → `'Confirmed-absent authority requires one digest-valid r13 packet only.'`. The rule id `P08-PREAUTH-ABSENT` and the other three conditions on line 229 (`authority_variant -cne 'confirmed_absent'`, `$null -ne $Projection.exact_existing_authority_sha256`, `$packet.packet_sha256 -cne (Get-P08SelfExcludingDigest $packet 'packet_sha256')`) are unchanged.
      - **Lines 1042-1044 (P08-HOSTED-R12-BINDING → P08-HOSTED-R13-BINDING):** change `$ReleaseRef -cne 'refs/tags/modules-v0.1.0-r12'` → `$ReleaseRef -cne 'refs/tags/modules-v0.1.0-r13'` (line 1042); change the rule id `'P08-HOSTED-R12-BINDING'` → `'P08-HOSTED-R13-BINDING'` (line 1044); change the message `'Only the exact r12 release binding is accepted.'` → `'Only the exact r13 release binding is accepted.'` (line 1044). The other four conditions on line 1042 (`$SourceSha -cnotmatch '^[0-9a-f]{40}$'`, `$RootIntentSha256 -cnotmatch '^[0-9a-f]{64}$'`, `$IntentSha256 -cnotmatch '^[0-9a-f]{64}$'`, `$PreparedManifestSha256 -cnotmatch '^[0-9a-f]{64}$'`) and the `$store=Open-P08BoundaryStore ...` line 1046 that follows are unchanged.

    **Step 5 — DO NOT CHANGE the terminal-history r12 references.** The following lines MUST remain byte-identical to their pre-Task-1 state; they are immutable REL01-REF evidence, not active authority:
      - **Line 662:** `($history.release_ref -join ',') -cne 'refs/tags/modules-v0.1.0,refs/tags/modules-v0.1.0-r1,refs/tags/modules-v0.1.0-r2,refs/tags/modules-v0.1.0-r3,refs/tags/modules-v0.1.0-r4,refs/tags/modules-v0.1.0-r5,refs/tags/modules-v0.1.0-r6,refs/tags/modules-v0.1.0-r7,refs/tags/modules-v0.1.0-r8,refs/tags/modules-v0.1.0-r9,refs/tags/modules-v0.1.0-r10,refs/tags/modules-v0.1.0-r11,refs/tags/modules-v0.1.0-r12'` — the joined historical release_ref list terminates at r12 because r12 is the last immutable terminal entry; r13 is the *current* attempt, not a terminal-history entry.
      - **Lines 666-684:** the `$historicalPolicy=$history[12]` block binding r12 as terminal evidence — `attempt -cne 'r12'`, `release_ref -cne 'refs/tags/modules-v0.1.0-r12'`, `source_sha -cne '5e7b19cdc74ec11d5c524ff34a36c266b15bba39'`, `tag_object_sha -cne '57b76c9f9044d3190acc1e4c3fb7ada516f4dece'`, `tag_peeled_source_sha -cne '5e7b19cdc74ec11d5c524ff34a36c266b15bba39'`, `failure_code -cne 'REL01-REF'`, `record_sha256 -cne '92397fbdfc679f154382928ee6f94c57e46b40b7c5f7e8d65759b0165d6c96a8'`, and the `Throw-P08HostedRule 'P08-PREPARE-HISTORICAL-BINDING'` calls. Line 661 (`current_attempt -cne 'r13'` and the `history.Count -ne 13` and thirteen-name attempt list) is ALREADY r13 from 08-35 — leave it.

    **Step 6 — Confirm GREEN.** Re-run `Test-Phase08PreAuthorization.ps1` and `Test-Phase08HostedBinding.ps1`. Cases A, B, D must now pass; case C's unmutated sub-case still passes and its mutated sub-case still throws `P08-PREPARE-HISTORICAL-BINDING`; case E throws `P08-HOSTED-R13-BINDING`.

    **Step 7 — Static-source audit.** Run `Select-String` against the modified `Invoke-Phase08HostedRun.ps1`:
      - `Select-String -Pattern 'modules-v0[.]1[.]0-r12'` MUST return exactly two matches, on lines 662 and 667 (within the r12 terminal block 666-684; the 667 match is the `$historicalPolicy.release_ref -cne 'refs/tags/modules-v0.1.0-r12'` literal). Any other r12 match is a regression.
      - `Select-String -Pattern 'modules-v0[.]1[.]0-r13'` MUST return exactly three matches on active-authority sites: line 208, line 229, and line 1042. (Line 661's `current_attempt -cne 'r13'` does not match the `modules-v0[.]1[.]0-r13` pattern and is not counted; the r13 tag name in policy-fetched contexts is not hardcoded in this file other than at these three sites.)
      - `Select-String -Pattern 'P08-HOSTED-R12-BINDING'` MUST return zero matches; `Select-String -Pattern 'P08-HOSTED-R13-BINDING'` MUST return exactly one match on line 1044.
      - `Select-String -Pattern 'r12 packet only'` MUST return zero matches; `Select-String -Pattern 'r13 packet only'` MUST return exactly one match on line 229.
      - `Select-String -Pattern 'r12 release binding'` MUST return zero matches; `Select-String -Pattern 'r13 release binding'` MUST return exactly one match on line 1044.

    **Step 8 — Baseline Verify (after).** Run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the same three-path task-owned list as Step 1. The eight protected paths MUST be unchanged from the Step 1 capture. (The baseline guard's `Assert-NoUnexpectedDirty` permits the three task-owned paths to appear as dirty because they are listed in `-TaskOwnedPath`; the eight protected paths themselves must show no status or sha256 drift.)

    **Step 9 — Commit.** Stage exactly the three files in `<files>`. Commit message: `feat(08-38): upgrade HostedRun publication path to r13`. Do not push, do not tag, do not run any hosted mode in this task.

    **Task 1 MUST NOT** create or push any tag, fetch any r13 ref into the working repository, dispatch any workflow, run any `-Mode HostedPreflight`/`PublisherDryRun`/`AssembleAuthorizationPacket`/`PublishOne` (those are Task 2's job), read any credential, perform any registry observation, or emit any pre-authorization/handoff/packet/receipt. Task 1 is purely a script-and-test change.
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08PreAuthorization.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Test-Phase08HostedBinding.ps1;
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$src=Get-Content -LiteralPath ./scripts/quality/Invoke-Phase08HostedRun.ps1; $r12=@($src | Select-String -Pattern 'modules-v0[.]1[.]0-r12'); if ($r12.Count -ne 2 -or ($r12[0].LineNumber) -ne 662 -or ($r12[1].LineNumber) -ne 667) { Write-Error ('r12 active-authority leak: ' + ($r12.LineNumber -join ',')); exit 1 }; $r13=@($src | Select-String -Pattern 'modules-v0[.]1[.]0-r13'); if ($r13.Count -ne 3 -or (@($r13.LineNumber) -join ',') -cne '208,229,1042') { Write-Error ('r13 active-authority mismatch: ' + (@($r13.LineNumber) -join ',')); exit 1 }; if (@($src | Select-String -Pattern 'P08-HOSTED-R12-BINDING').Count -ne 0) { exit 1 }; if (@($src | Select-String -Pattern 'P08-HOSTED-R13-BINDING').Count -ne 1) { exit 1 }; if (@($src | Select-String -Pattern 'r12 packet only').Count -ne 0) { exit 1 }; if (@($src | Select-String -Pattern 'r13 packet only').Count -ne 1) { exit 1 }; if (@($src | Select-String -Pattern 'r12 release binding').Count -ne 0) { exit 1 }; if (@($src | Select-String -Pattern 'r13 release binding').Count -ne 1) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1"
  </automated></verify>
  <acceptance_criteria>
    - `Test-Phase08PreAuthorization.ps1` and `Test-Phase08HostedBinding.ps1` both exit 0 (RED→GREEN complete; case C adversarial r12-terminal regression also passes).
    - `Select-String -Pattern 'modules-v0[.]1[.]0-r12'` on `Invoke-Phase08HostedRun.ps1` returns exactly two matches on lines 662 and 667.
    - `Select-String -Pattern 'modules-v0[.]1[.]0-r13'` on `Invoke-Phase08HostedRun.ps1` returns exactly three matches on lines 208, 229, and 1042.
    - `P08-HOSTED-R12-BINDING` is absent; `P08-HOSTED-R13-BINDING` appears exactly once on line 1044.
    - The message literals `'...r13 packet only.'` (line 229) and `'Only the exact r13 release binding is accepted.'` (line 1044) are present; their r12 counterparts are absent.
    - The eight protected paths are unchanged from the Step 1 baseline capture.
  </acceptance_criteria>
</task>
<task type="auto" non_publishing="true">
  <name>Task 2: Recapture baseline, run HostedPreflight + PublisherDryRun via the r13 boundary wrapper, emit absent-variant preauthorization.json</name>
  <files>None (disposable clone, GUID-owned state, publisher-only temporary Moon home, sanitized temporary artifacts)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-PLAN.md Task 3 Steps 6-9 (the deferred publication-path sequence this task completes — disposable remote-clone boundary invocation, HostedPreflight, PublisherDryRun, fresh credential-free observation, absent preauthorization emission)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-SUMMARY.md lines 74-90 (the deferral scope and the recapture-baseline requirement)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-PLAN.md Task 1 (the publication-path pattern — preflight + dry-run + observation + preauthorization)
    - scripts/quality/Invoke-Phase08R13Boundary.ps1 (the boundary wrapper that returns the closed invocation record; its returned `boundary_sha` and the locator it produces seed the HostedPreflight and PublisherDryRun bindings)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 modes: `HostedPreflight`, `PublisherDryRun`, `MaterializePublicSurface`, `ObserveOnly`, `AssembleAuthorizationPacket`, `ValidatePreAuthorization` (lines 4-7 enumerate all modes; the binding requirement at line 1042 now accepts r13 after Task 1)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
    - policy/release-control.json (r13 identity: `current_attempt=r13`, `initial_profile.release_ref=refs/tags/modules-v0.1.0-r13`, thirteen histories, `history_set_sha256=961780e0...`)
  <action>
    This task completes the publication-path sequence 08-37 Task 3 deferred (08-37-SUMMARY.md lines 74-78). Task 1 unblocked the modes; this task runs them through the r13 boundary wrapper with a publisher-only temporary Moon home and emits the absent-variant preauthorization. **Zero mutation is permitted.** No `PublishOne`, no `Invoke-MooncakesLiveMutation`, no `gh workflow run`, no `gh run create`, no credential use for mutation. The publisher dry-run uses a publisher-only temporary GUID-owned `MOON_HOME` removed in a `finally` block; no production Moon home is touched.

    **Step 1 — Baseline Verify (before).** Run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the same three-path task-owned list as Task 1 (`scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1`). The eight protected paths MUST be unchanged.

    **Step 2 — Confirm the r13 boundary tag identity.** Record the immutable r13 boundary established by 08-37:
    ```
    $localObject  = (& git -C $repoRoot rev-parse refs/tags/modules-v0.1.0-r13).Trim()   # 611fb2862533ffd959ed830a222da663df80af49
    $localPeel    = (& git -C $repoRoot rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim() # 003b604978a06c87858fe2cdc0a1969fd20159f7
    $remoteRows   = @(& git ls-remote --tags origin modules-v0.1.0-r13)
    ```
    Assert `$localObject -cmatch '^[0-9a-f]{40}$'` and equals `611fb2862533ffd959ed830a222da663df80af49`; assert `$localPeel -ceq '003b604978a06c87858fe2cdc0a1969fd20159f7'`; assert `& git -C $repoRoot merge-base --is-ancestor $localPeel HEAD` succeeds; assert `$remoteRows` contains exactly two rows `<localObject>\trefs/tags/modules-v0.1.0-r13` and `<localPeel>\trefs/tags/modules-v0.1.0-r13^{}`. If any of these fail, the r13 boundary established by 08-37 has drifted — STOP and write a SUMMARY.

    **Step 3 — Disposable remote-clone canonical boundary invocation.** Mirror 08-37 Task 3 Step 6 exactly:
    ```
    $root = Join-Path ([IO.Path]::GetTempPath()) ('mnf-phase08-r13-pubpath-' + [Guid]::NewGuid().ToString('N'))
    $clone = Join-Path $root 'clone'
    $null = New-Item -ItemType Directory -Path $root
    & git clone --quiet --no-local --no-tags $repoRoot $clone
    & git -C $clone config user.name 'MNF r13 publication-path fixture'
    & git -C $clone config user.email 'r13-pubpath-fixture@moonbit-foundation.invalid'
    & git -C $clone checkout --quiet $localPeel
    & git -C $clone fetch --no-tags origin refs/tags/modules-v0.1.0-r13:refs/tags/modules-v0.1.0-r13
    ```
    Require clone HEAD equals `$localPeel`, clone-local r13 tag object equals `$localObject`, clone-local r13 peel equals `$localPeel`, and clone-local `policy/release-control.json` still declares `release_ref=refs/tags/modules-v0.1.0-r13` and `current_attempt=r13`. Invoke the r13 boundary wrapper to establish the boundary and PrepareAttempt:
    ```
    $state = Join-Path $root 'canonical-state'
    $boundary = & ./scripts/quality/Invoke-Phase08R13Boundary.ps1 `
      -CloneRoot $clone -StateRoot $state `
      -ExpectedTagObject $localObject -ExpectedPeeledSourceSha $localPeel `
      -HistoricalReleaseRef refs/tags/modules-v0.1.0-r12 `
      -HistoricalSourceSha 5e7b19cdc74ec11d5c524ff34a36c266b15bba39
    ```
    Require the closed invocation record: `release_ref -ceq 'refs/tags/modules-v0.1.0-r13'`, `tag_object -ceq $localObject`, `peeled_source_sha -ceq $localPeel`, `head -ceq $localPeel`, `boundary_sha -ceq $localPeel`, `mutation_count -eq 0`. The boundary locator and prepared manifest produced under `$state` are the inputs to the hosted modes below.

    **Step 4 — HostedPreflight through the r13 boundary (publisher-only temp Moon home + teardown).** Create an isolated GUID-owned `MOON_HOME` and invoke one credential-free `HostedPreflight`:
    ```
    $moonHome = Join-Path $root ('moon-home-' + [Guid]::NewGuid().ToString('N'))
    $null = New-Item -ItemType Directory -Path $moonHome
    $env:MOON_HOME = $moonHome
    try {
      $preflight = & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode HostedPreflight `
        -BoundaryLocatorPath <locator from $state> -ReleaseRef 'refs/tags/modules-v0.1.0-r13' `
        -SourceSha $localPeel -BoundarySha $localPeel -Repository tchivs/moonbit-foundation `
        -Workflow publish-modules.yml -PreparedManifestSha256 <prepared manifest digest> `
        -RootIntentSha256 <root intent digest> -IntentSha256 <intent digest>
    } finally {
      Remove-Item -LiteralPath $moonHome -Recurse -Force
      Remove-Item -LiteralPath Env:\MOON_HOME
    }
    ```
    The exact SHA-256 inputs (`PreparedManifestSha256`, `RootIntentSha256`, `IntentSha256`) are read from the PrepareAttempt locator and prepared manifest produced in Step 3; do NOT synthesize them. Require `$preflight` returns one closed `HostedPreflight` evidence record binding `release_ref=refs/tags/modules-v0.1.0-r13`, `boundary_sha=$localPeel`, the bounded run inputs, fresh timestamps within the 6-hour/5-minute window enforced by `Assert-P08HostedRunEvidence`, `raw_output_persisted=false`, and `mutation_count=0`. Require NO workflow dispatch (`gh workflow run` / `gh run create`), NO credential read, NO Mooncakes request, NO `PublishOne`. The publisher-only temporary Moon home is removed in the `finally` block before any subsequent step.

    **Step 5 — PublisherDryRun through the r13 boundary (publisher-only temp Moon home + teardown).** Same publisher-only temporary GUID-owned `MOON_HOME` pattern in a fresh `try { ... } finally { ... }` block. Invoke one `PublisherDryRun`:
    ```
    $moonHome2 = Join-Path $root ('moon-home-dry-' + [Guid]::NewGuid().ToString('N'))
    $null = New-Item -ItemType Directory -Path $moonHome2
    $env:MOON_HOME = $moonHome2
    try {
      $dryrun = & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode PublisherDryRun `
        -BoundaryLocatorPath <locator> -ReleaseRef 'refs/tags/modules-v0.1.0-r13' `
        -SourceSha $localPeel -BoundarySha $localPeel -Repository tchivs/moonbit-foundation `
        -Workflow publish-modules.yml -PreparedManifestSha256 <prepared manifest digest> `
        -RootIntentSha256 <root intent digest> -IntentSha256 <intent digest> `
        -TargetModule mb-core
    } finally {
      Remove-Item -LiteralPath $moonHome2 -Recurse -Force
      Remove-Item -LiteralPath Env:\MOON_HOME
    }
    ```
    Require `$dryrun` returns one closed `PublisherDryRun` evidence record binding the same r13 identity and inputs, with `module_manifest_sha256` and `archive_sha256` matching the canonical 64-hex pattern, `mutation_performed=false`, and `mutation_count=0`. The publisher dry-run MUST NOT write to the production Moon home, MUST NOT read a real Mooncakes credential, MUST NOT dispatch `PublishOne`, and MUST NOT make any registry-mutating request.

    **Step 6 — Fresh credential-free core observation.** Run one credential-free read-only registry observation bounded by the existing observation policy (20 attempts at 15-second cadence per STATE.md). Record the sanitized projection only. Do NOT read, store, or persist any credential, secret, token, or cookie-shaped value. The observation feeds `AssembleAuthorizationPacket`'s `observation_sha256` binding.

    **Step 7 — Assemble the absent-branch mutation-authorization packet.** Invoke `-Mode AssembleAuthorizationPacket` against the boundary store populated by Steps 4-6 (the store index must contain exactly one `HostedPreflight`, one `PublisherDryRun`, and one `Observation` record — see line 1075). Require the packet's `release_ref='refs/tags/modules-v0.1.0-r13'`, `boundary_sha=$localPeel`, `target_module='mb-core'`, `mutation_count=0`, and `packet_sha256` equals `Get-P08SelfExcludingDigest $packet 'packet_sha256'` (the digest-valid check Assert-P08PreAuthorization line 229 enforces after Task 1).

    **Step 8 — Emit the absent-variant preauthorization.json.** Produce `%TEMP%/mnf-phase08-r13-preauthorization.json` with schema `mnf-phase08-pre-authorization/1`, the exact sixteen property names enumerated at `Assert-P08PreAuthorization` line 207, `release_ref='refs/tags/modules-v0.1.0-r13'`, `authority_variant='confirmed_absent'`, `active_attempt_path=$null`, `exact_existing_authority_path=$null`, `exact_existing_authority_sha256=$null`, `mutation_authorization_packet_path` bound to the Step 7 packet path and `mutation_authorization_packet_sha256` to its digest, `authorization_receipt_path=$null`, `authorization_receipt_sha256=$null`, `fixed_handoff_path='...\mnf-phase08-r13-handoff.json'`, `fixed_handoff_sha256=$null`, `observation_path` and `observation_sha256` bound to the Step 6 sanitized observation, `mutation_count=0`, and `output_write_count=0`. The fixed `%TEMP%/mnf-phase08-r13-handoff.json` MUST remain absent (no handoff is produced by a non-publishing boundary plan).

    **Step 9 — Validate the preauthorization.** Run:
    ```
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization `
      -PreAuthorizationPath "$env:TEMP/mnf-phase08-r13-preauthorization.json" `
      -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json"
    ```
    This MUST exit 0 — it exercises `Assert-P08PreAuthorization` (lines 205-232) against the just-emitted projection. After Task 1, line 208 accepts `release_ref='refs/tags/modules-v0.1.0-r13'`, line 229 accepts the r13 packet and the new `'...r13 packet only.'` message, and the handoff absence gate at line 211 passes because the handoff path is absent.

    **Step 10 — Teardown and Baseline Verify (after).** Remove `$root` (the disposable clone and all GUID-owned state under it, including the publisher-only Moon homes). Run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the same three-path task-owned list. The eight protected paths MUST be unchanged.

    This task MUST NOT push, fetch into the working repo, dispatch a workflow, read a credential for mutation, mutate the registry, create or push any tag (the r13 tag already exists), or produce any receipt/handoff.
  <verify><automated>
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$lo=(& git rev-parse refs/tags/modules-v0.1.0-r13).Trim(); $lp=(& git rev-parse 'refs/tags/modules-v0.1.0-r13^{}').Trim(); if ($lo -cne '611fb2862533ffd959ed830a222da663df80af49') { exit 1 }; if ($lp -cne '003b604978a06c87858fe2cdc0a1969fd20159f7') { exit 1 }; & git merge-base --is-ancestor $lp HEAD; if ($LASTEXITCODE) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath "$env:TEMP/mnf-phase08-r13-preauthorization.json" -HandoffPath "$env:TEMP/mnf-phase08-r13-handoff.json";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -Command "$h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r13-handoff.json'; if (Test-Path $h) { Write-Error 'handoff must be absent'; exit 1 }; $p=Get-Content -LiteralPath (Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r13-preauthorization.json') -Raw | ConvertFrom-Json -Depth 50; if ($p.authority_variant -cne 'confirmed_absent') { exit 1 }; if ($p.release_ref -cne 'refs/tags/modules-v0.1.0-r13') { exit 1 }; if ([int]$p.mutation_count -ne 0 -or [int]$p.output_write_count -ne 0) { exit 1 }; if ($null -ne $p.authorization_receipt_path -or $null -ne $p.active_attempt_path) { exit 1 }";
    if ($LASTEXITCODE) { exit $LASTEXITCODE };
    pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath "scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1"
  </automated></verify>
  <done>The Task 1 upgraded HostedRun publication path has been exercised end-to-end through the r13 boundary wrapper: one HostedPreflight and one PublisherDryRun completed with publisher-only temporary GUID-owned Moon homes removed in finally blocks, one credential-free observation recorded, one absent-variant mutation-authorization packet assembled, `%TEMP%/mnf-phase08-r13-preauthorization.json` exists with `authority_variant=confirmed_absent`, one packet, zero mutation, no receipt, no handoff, and `ValidatePreAuthorization` exits 0 against it; the eight protected paths are unchanged before and after.</done>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Dynamic absent-only authorize-core checkpoint</name>
  <files>None (only an authorized absent branch may emit receipt/handoff; this plan stops at absent and does not authorize)</files>
  <read_first>
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-37-PLAN.md Task 4 (the checkpoint pattern to mirror EXACTLY — surface only, no resolution)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-32-PLAN.md Task 2 (the original authorize-core checkpoint pattern)
    - .planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md (only a distinct still-later r13-specific plan may seek explicit authorization and attempt one ordered module operation)
    - scripts/quality/Invoke-Phase08HostedRun.ps1 (modes: ValidatePreAuthorization, ValidateHandoff)
    - scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
  <action>
    This task SURFACES the authorize-core decision to the operator; it does NOT resolve it. Mirror 08-37 Task 4 EXACTLY. The actual authorize-core → publish (mb-core → mb-color → mb-image + cold consumers) lives in a SEPARATE later plan (08-39), not this one. The non-publishing r13 boundary ends absent with one packet and no receipt/handoff; either resume-signal this plan accepts is recorded into a follow-up hand-off plan, never into this plan's own artifacts.

    **Step 1 — Recompute baseline and identity/authority facts.** Re-run `Assert-Phase08R13DirtyBaseline -Mode Verify` with the three-path task-owned list from Task 1 (`scripts/quality/Invoke-Phase08HostedRun.ps1,scripts/quality/Test-Phase08PreAuthorization.ps1,scripts/quality/Test-Phase08HostedBinding.ps1`). Re-display: the canonical clone-policy ref `refs/tags/modules-v0.1.0-r13`; the r13 tag object `611fb2862533ffd959ed830a222da663df80af49` and peeled source SHA `003b604978a06c87858fe2cdc0a1969fd20159f7` (local and remote, ancestor of HEAD); the r12 terminal facts (source `5e7b19cdc74ec11d5c524ff34a36c266b15bba39`, tag object `57b76c9f9044d3190acc1e4c3fb7ada516f4dece`, `record_sha256=92397fbdfc679f154382928ee6f94c57e46b40b7c5f7e8d65759b0165d6c96a8`, `failure_code=REL01-REF`, `authority_acquired=false`); the thirteen-history LF aggregate `961780e02672005c9dfb19299fc81a0531fb58081de3ee2a78274dda88f630e5`; the Task 1 proof (3 active-authority r13 matches on lines 208/229/1042, 2 terminal-history r12 matches on lines 662/667, P08-HOSTED-R13-BINDING, the `'...r13 packet only.'` and `'Only the exact r13 release binding is accepted.'` messages, the adversarial r12-terminal regression test passing); the Task 2 records (disposable remote-clone canonical boundary returning a zero-mutation closed record, one HostedPreflight and one PublisherDryRun with publisher-only temp Moon home + teardown, fresh credential-free observation, the absent-variant packet digest, zero mutation); the unchanged eight-path baseline. Only the confirmed-absent branch reaches this checkpoint.

    **Step 2 — Accept only the same-turn literal resume-signal.** Accept exactly `authorize-core` or `stop`. Any drift, stale observation, mismatch, malformed response, or non-literal input stops without mutation. After the branch, re-compare the baseline; any drift fails closed.

    **Step 3 — `stop` branch (default for this plan).** No receipt, no handoff, no Mooncakes mutation, no `PublishOne`, no workflow dispatch. `%TEMP%/mnf-phase08-r13-handoff.json` remains absent. Validate the pre-authorization zero-mutation projection. The authorize-core → publish path (mb-core → mb-color → mb-image + cold consumers per DIST-01..04 and PROV-05) is deferred to plan 08-39, which re-validates everything from a fresh boundary invocation.

    **Step 4 — `authorize-core` branch (surfaces intent only; this plan does not execute it).** Record that operator intent is to authorize; plan 08-39 is required to consume that intent. This plan does NOT create a receipt, does NOT create a handoff, does NOT dispatch `PublishOne`, does NOT mutate the registry. If the operator demands immediate mutation, STOP and re-route to plan 08-39 — do not expand this plan's scope.
  <options>
    <option id="authorize-core">
      <name>authorize-core</name>
      <pros>Surfaces operator intent to enable plan 08-39's ordered module publication (mb-core → mb-color → mb-image + cold consumers) after its own fresh boundary revalidation.</pros>
      <cons>This plan does not execute it; plan 08-39 can make irreversible registry mutations only after its own fresh boundary passes and authorization is re-acquired same-turn.</cons>
    </option>
    <option id="stop">
      <name>stop</name>
      <pros>Preserves r13 zero-mutation boundary evidence, the absent-variant preauthorization, and the unchanged eight-path baseline.</pros>
      <cons>Publication remains blocked until plan 08-39 is run.</cons>
    </option>
  </options>
  <resume-signal>Type exactly `authorize-core` or `stop`.</resume-signal>
  <verify><automated>pwsh -NoProfile -Command "$h=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r13-handoff.json'; $p=Join-Path ([IO.Path]::GetTempPath()) 'mnf-phase08-r13-preauthorization.json'; if(Test-Path $h){ & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidateHandoff -HandoffPath $h; exit $LASTEXITCODE }; & ./scripts/quality/Invoke-Phase08HostedRun.ps1 -Mode ValidatePreAuthorization -PreAuthorizationPath $p -HandoffPath $h; exit $LASTEXITCODE"</automated></verify>
  <done>Only a same-turn absent authorization surfaces intent for plan 08-39; this plan ends with zero mutation, no receipt, no handoff, and the eight-path baseline unchanged. The actual authorize-core → publish is left to plan 08-39.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| active-authority checks to r13 policy | The three upgraded sites (lines 208, 229, 1042) now bind r13 in agreement with `policy/release-control.json` and the r13 boundary tag; stale r12 authority is rejected at line 1044 with `P08-HOSTED-R13-BINDING`. |
| terminal-history evidence to r12 record | The two unchanged sites (lines 662, 667) keep r12 as immutable REL01-REF terminal evidence; the active-authority upgrade MUST NOT touch them. |
| user dirties to artifacts | The exact eight-path baseline preserves user state and excludes it from release identity; every other worktree drift fails closed. |
| publisher dry-run to credentials | Publisher-only temporary GUID-owned `MOON_HOME` removed in a `finally` block; no production Moon home, no real Mooncakes credential is materialized. |
| pre-authorization to decision | Fresh absent evidence is required for any same-turn authorize-core surfacing; stop retains no receipt/handoff. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-38-01 | Tampering | r12 terminal-history corruption | critical | mitigate | The two terminal-history r12 references (line 662 joined release_ref list terminating at r12; lines 666-684 `$historicalPolicy=$history[12]` binding r12's `release_ref=refs/tags/modules-v0.1.0-r12`, `source_sha=5e7b19cd`, `tag_object_sha=57b76c9f`, `record_sha256=92397fbdfc...`, `failure_code=REL01-REF`) MUST remain byte-identical to their pre-Task-1 state. Task 1 Step 5 forbids touching them; Task 1 Step 7's `Select-String -Pattern 'modules-v0[.]1[.]0-r12'` audit requires exactly two matches on lines 662 and 667; Test-Phase08PreAuthorization.ps1 case C is an adversarial test that mutates `terminal_negative_history[12].mutation_count=1` and asserts `PrepareAttempt` still throws `P08-PREPARE-HISTORICAL-BINDING`, proving the r12 terminal record still rejects mutation after the active-authority upgrade. |
| T-08-38-02 | Spoofing | stale r12 active authority | critical | mitigate | After Task 1, `P08-HOSTED-R12-BINDING` is absent and `P08-HOSTED-R13-BINDING` rejects any `$ReleaseRef -cne 'refs/tags/modules-v0.1.0-r13'` (line 1042); Test-Phase08HostedBinding.ps1 case E asserts that `$ReleaseRef='refs/tags/modules-v0.1.0-r12'` throws `P08-HOSTED-R13-BINDING`. Line 208's `Assert-P08PreAuthorization` rejects any projection whose `release_ref` is not r13; line 229 rejects any absent-branch packet whose `release_ref` is not r13. |
| T-08-38-03 | Tampering | eight-path dirty baseline | high | mitigate | `Assert-Phase08R13DirtyBaseline -Mode Verify` runs before and after every task (Task 1 Steps 1 and 8, Task 2 Steps 1 and 10, Task 3 Step 1); the eight protected paths are never staged, edited, reset, cleaned, stashed, or included in any artifact. |
| T-08-38-04 | Information Disclosure | publisher dry-run credential | critical | mitigate | Publisher-only temporary GUID-owned `MOON_HOME` created under the disposable clone root, set in `try`, removed in `finally`; no production Moon home, no real Mooncakes credential is materialized. Task 2 Steps 4 and 5 each use a fresh GUID-owned Moon home with its own `try`/`finally`. |
| T-08-38-05 | Elevation of Privilege | core receipt / handoff | critical | mitigate | Same-turn absent-only revalidation; this plan stops at the absent branch and does not produce a receipt or handoff; the actual authorize-core → publish lives in plan 08-39. `%TEMP%/mnf-phase08-r13-handoff.json` remains absent by Task 2 Step 8 design and Task 3 Step 3 default. |
| T-08-38-06 | Repudiation | r12 terminal evidence | high | mitigate | The r12 terminal record carried at lines 662 and 666-684 (source `5e7b19cd`, tag object `57b76c9f`, `record_sha256=92397fbdfc...`, `failure_code=REL01-REF`, `authority_acquired=false`, all twelve counters zero) is preserved verbatim; r12 is never reused as current authority and never silently edited to r13. |
</threat_model>
<verification>Re-run the eight-path baseline guard before and after every task; prove Task 1's RED tests failed against unmodified source and its GREEN tests pass after the three-line upgrade; prove `Select-String` shows exactly three r13 active-authority matches on lines 208/229/1042 AND exactly two r12 terminal-history matches on lines 662/667 remain, with `P08-HOSTED-R13-BINDING` present and `P08-HOSTED-R12-BINDING` absent; prove the adversarial r12-terminal regression test still throws `P08-PREPARE-HISTORICAL-BINDING` on a mutated record; prove Task 2 ran one HostedPreflight and one PublisherDryRun through the r13 boundary wrapper with publisher-only temp Moon homes removed in finally, one credential-free observation, one absent-variant packet, zero mutation; prove `%TEMP%/mnf-phase08-r13-preauthorization.json` exists with `authority_variant=confirmed_absent`, one packet, zero mutation, no receipt, no handoff, and `ValidatePreAuthorization` exits 0; prove the handoff path is absent. No registry mutation, no Mooncakes request, no credential materialization in production state, no `PublishOne`, no workflow dispatch.</verification>
<success_criteria>Only a HostedRun publication path whose three active-authority sites agree with the r13 policy and the r13 boundary tag (object `611fb286`, peel `003b604`) — while the two terminal-history sites keep r12 as immutable REL01-REF evidence — can run HostedPreflight and PublisherDryRun for r13 and emit a digest-valid absent-variant preauthorization. No stale r12 active authority, no r12 terminal-history corruption, no dirty-state drift, no credential materialization, and no Mooncakes mutation can enter the publication path. The non-publishing r13 boundary ends absent with one packet and no receipt/handoff; the actual authorize-core → publish is deferred to plan 08-39.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-38-SUMMARY.md` when done.</output>
