---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "35"
type: execute
wave: 35
depends_on: ["08-34"]
files_modified:
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - scripts/quality/Test-Phase08PrepareHistorySchema.ps1
  - scripts/quality/Invoke-ReleasePublisher.ps1
  - scripts/quality/Invoke-MooncakesLiveMutation.ps1
  - .github/workflows/publish-modules.yml
  - scripts/quality/Test-ReleasePublisherNegative.ps1
  - scripts/quality/Test-Phase08LiveSeam.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "Hosted r13 preparation checks out SHA, fetches and verifies the exact policy tag, and then may create local preparation state; every disagreement fails before state/provider work."
    - "Publisher-facing seams require r13 and all thirteen histories but remain static-only and cannot authorize, tag, dispatch, or publish."
    - "The Plan 34 eight-path guard proves equality before and after every task."
  artifacts:
    - path: "scripts/quality/ReleaseQualification.Common.ps1"
      provides: "post-checkout exact-tag fetch/verification gate"
    - path: "scripts/quality/Invoke-Phase08HostedRun.ps1"
      provides: "r13 hosted preparation binding"
---
<objective>Wire the r13 static hosted and publisher seams after the r13 policy and baseline guard exist.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-34-SUMMARY.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-R13-RECOVERY-CONTEXT.md
@scripts/quality/Assert-Phase08R13DirtyBaseline.ps1
@scripts/quality/ReleaseQualification.Common.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Gate hosted preparation on post-checkout exact r13 policy-tag verification</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Invoke-Phase08HostedRun.ps1, scripts/quality/Test-Phase08Qualification.ps1, scripts/quality/Test-Phase08PrepareHistorySchema.ps1</files>
  <behavior>
    - A disposable fixture checks out bound SHA, explicitly fetches clone-local policy's r13 tag, then verifies annotated object and peel against policy and SHA before locator/provider work.
    - Missing tag, ref substitution, object/peel/SHA mismatch, stale r12 reference, or incomplete thirteen histories stops with zero locator, bundle, active-attempt, handoff, dispatch, credential, and mutation effects.
    - The directly invoked r13 intent serializer emits attempt-zero then r1 through r12 in exact order, includes every individual digest, and recomputes the canonical LF aggregate before HostedRun or prepared-bundle consumers accept it.
  </behavior>
  <action>Use the Plan 34 baseline guard before and after work. Write ordering-sensitive RED tests, then advance the directly invoked `New-ReleaseIntent.ps1` serializer, prepared validation, shared qualification, HostedRun, and fixtures to r13/thirteen histories. Require the serializer to emit attempt-zero then r1 through r12 in that exact order, carry every individual digest, and recompute the canonical LF aggregate rather than accepting caller order or a stale r12 aggregate. `New-ReleaseIntent.ps1` directly consumes only the already task-owned shared qualification helper and Plan 34 policy/schema contracts; do not broaden this task to indirect consumers. A locally pre-existing tag, a caller ref, a generic fetch, or verification before checkout is insufficient. Use disposable fixtures only. Do not create the real r13 tag; that irreversible boundary belongs to a later plan and must not be folded into this static plan.</action>
  <verify><automated>$taskPaths=@('scripts/quality/New-PreparedReleaseBundle.ps1','scripts/quality/New-ReleaseIntent.ps1','scripts/quality/ReleaseQualification.Common.ps1','scripts/quality/Test-PreparedReleaseBundle.ps1','scripts/quality/Invoke-Phase08HostedRun.ps1','scripts/quality/Test-Phase08Qualification.ps1','scripts/quality/Test-Phase08PrepareHistorySchema.ps1'); pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1 -FixtureOnly; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08PrepareHistorySchema.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths</automated></verify>
  <done>Hosted preparation proves post-checkout fetched exact-tag identity and exact r13 thirteen-history intent ordering before it can create state, with baseline equality retained.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Carry r13 history through publisher seams without activating release authority</name>
  <files>scripts/quality/Invoke-ReleasePublisher.ps1, scripts/quality/Invoke-MooncakesLiveMutation.ps1, .github/workflows/publish-modules.yml, scripts/quality/Test-ReleasePublisherNegative.ps1, scripts/quality/Test-Phase08LiveSeam.ps1</files>
  <behavior>
    - Publisher, adapter, and workflow reject stale r12, missing r12 digest, altered aggregate, non-r13 ref, or more-than-one-module request before adapter execution.
    - Static fixtures remain credential-free and do not create tags, dispatch workflows, produce authorization artifacts, invoke Mooncakes, or publish.
  </behavior>
  <action>Use the Plan 34 baseline guard before and after work. Add RED negative/seam tests, then update all request fields, aggregate checks, fixed paths, and workflow propagation to r13/thirteen histories while preserving the one journal-selected module limit and isolated publisher-only temporary Moon home. Do not add an r13 pre-live selector, receipt/handoff, actual workflow dispatch, or publication. Later plans must separately establish r13's immutable tag and then obtain explicit authorization; 08-33 remains quarantined.</action>
  <verify><automated>$taskPaths=@('scripts/quality/Invoke-ReleasePublisher.ps1','scripts/quality/Invoke-MooncakesLiveMutation.ps1','.github/workflows/publish-modules.yml','scripts/quality/Test-ReleasePublisherNegative.ps1','scripts/quality/Test-Phase08LiveSeam.ps1'); pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-ReleasePublisherNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08LiveSeam.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Assert-Phase08R13DirtyBaseline.ps1 -Mode Verify -BaselinePath "$env:TEMP/mnf-phase08-r13-user-dirty-baseline.json" -TaskOwnedPath $taskPaths</automated></verify>
  <done>Static publisher seams accept only complete r13 authority while no release operation or baseline drift occurs.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| clone policy/tag to HostedRun | Exact tag must be fetched after checkout and verified before preparation state. |
| r13 request to publisher seam | Incomplete/stale authority must not reach adapter execution. |
| dirty baseline to all tasks | Plan 34 guard blocks changed allowed paths and all other drift. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-35-01 | Spoofing | policy ref/tag | critical | mitigate | Post-checkout explicit fetch and object/peel/SHA verification tests. |
| T-08-35-02 | Elevation of Privilege | publisher request | critical | mitigate | Closed r13 thirteen-history checks before adapter command. |
| T-08-35-03 | Tampering | user dirties | high | mitigate | Execute baseline verify before/after each task and fail closed. |
</threat_model>
<verification>Run focused static suites, baseline guard, parser checks, and diff checks. Stop on any tag, dispatch, credential, registry, or baseline anomaly.</verification>
<success_criteria>r13 hosted and publisher contracts are static-tested, but r13 has not been tagged, authorized, dispatched, or published.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-35-SUMMARY.md` when done.</output>
