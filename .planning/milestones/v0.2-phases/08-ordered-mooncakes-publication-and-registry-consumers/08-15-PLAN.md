---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "15"
type: execute
wave: 15
depends_on: ["08-14"]
files_modified:
  - policy/release-control.json
  - release/intent/schema.json
  - release/prepared/schema.json
  - release/qualification/phase-08-authority-schema.json
  - release/qualification/phase-08-authorization-receipt-schema.json
  - release/qualification/phase-08-handoff-schema.json
  - scripts/quality/New-ReleaseIntent.ps1
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseIntent.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "r6 is the sole current initial forward retry with module 0.1.0, intent kind initial, sequence 0, root=current, and no predecessor (D-01, D-07, D-15)."
    - "Attempt-zero and r1 through r5 are six distinct immutable terminal-negative histories; r5 is bound to source df105f06205298f1f82ac2f2cdca214d69d42e15 and annotated tag object 4a11582cf9aeae15802cf4f6d7394b013ece63ac."
    - "r5 records successful PrepareAttempt, fresh credential-free core absence, then HostedPreflight dispatch rejection before run creation because the immutable workflow had a duplicate environment key; hosted run and every downstream effect remain zero."
    - "Every r6 eligibility artifact binds six individual history digests plus their canonical ordered-set digest and rejects replacement, mixing, duplication, collapse, or reorder."
  artifacts:
    - path: "policy/release-control.json"
      provides: "r6 current profile and six exact histories"
    - path: "release/qualification/phase-08-handoff-schema.json"
      provides: "r6 six-history AuthorityUnion handoff"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "Fresh r6 prepared/index/store identity"
  key_links:
    - from: "attempt-zero plus r1 through r5 histories"
      to: "r6 prepared and authority artifacts"
      via: "six SHA-256 digests plus canonical ordered-set SHA-256"
      pattern: "historical_r5_sha256"
---

<objective>Define and test the r6 initial retry and six immutable terminal-negative histories.

Purpose: Recover forward from r5's pre-run workflow validation failure without moving r5, inventing a run, or entering the correction lane.
Output: Closed r6 policy, intent, prepared, authority, receipt, handoff, and qualification contracts.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-workflow-duplicate-env.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-14-SUMMARY.md
@policy/release-control.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the initial attempt family to r6 and preserve six histories</name>
  <files>policy/release-control.json, release/intent/schema.json, release/prepared/schema.json, scripts/quality/New-ReleaseIntent.ps1, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Only r6 is current initial; version 0.1.0, root=current, sequence zero, and no-predecessor invariants remain.
    - The exact history order is attempt_zero,r1,r2,r3,r4,r5 and every member has distinct terminal facts and digest.
    - r5 binds source df105f06205298f1f82ac2f2cdca214d69d42e15, tag object 4a11582cf9aeae15802cf4f6d7394b013ece63ac, completed PrepareAttempt, fresh absent observation, duplicate-key pre-run rejection, no hosted run, and zero downstream effects.
  </behavior>
  <action>Per D-01, D-07, and D-15, advance only the current initial profile from r5 to r6. Preserve the first five histories byte-for-byte and add the exact r5 source/tag/preparation/absence/pre-run rejection facts. Model the terminal stage as dispatch validation before run creation: require no run id or attempt and require PublisherDryRun, packet, receipt, handoff, PublishOne, and mutation counts to remain zero. Define six individual digests and one canonical LF-ordered set digest. Reject r5-current selection, historical source reuse, replacement, duplication, reorder, set drift, predecessor/sequence drift, and initial-to-correction substitution. Keep correction-N available only for an observed published mismatch.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r6 alone is current and six immutable histories include the exact no-run r5 failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind six histories into authority, receipt, and handoff schemas</name>
  <files>release/qualification/phase-08-authority-schema.json, release/qualification/phase-08-authorization-receipt-schema.json, release/qualification/phase-08-handoff-schema.json, scripts/quality/Test-ReleaseIntent.ps1</files>
  <behavior>
    - Authority, receipt, and handoff require six individual history digests and their matching ordered set.
    - Mutation authority requires packet plus literal receipt; exact-existing authority forbids both; stop is ineligible.
    - r5-current, fake r5 run, history replacement/duplicate/reorder/set drift, stale UTC, and cross-attempt path reuse fail closed.
  </behavior>
  <action>Per D-04, D-07, D-14, and D-15, advance the closed authority, receipt, and handoff schemas to r6 and six histories. Bind the r5 no-run terminal facts without allowing a synthetic run identity. Make the receipt bind the packet, r6 ref/boundary/source, literal authorize-core, UTC evidence, all six histories and set, and its own digest. Fix the production handoff at `%TEMP%/mnf-phase08-r6-handoff.json`, bind every path/file/digest, and admit exactly one AuthorityUnion variant. Add exact-existing, absent-receipt, stop, fake-run, stale-history, reordered-set, and old-fixed-handoff adversarial fixtures.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-ReleaseIntent.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -Command "$p=@('release/qualification/phase-08-authority-schema.json','release/qualification/phase-08-authorization-receipt-schema.json','release/qualification/phase-08-handoff-schema.json'); foreach($f in $p){$null=Get-Content -LiteralPath $f -Raw|ConvertFrom-Json -Depth 100 -ErrorAction Stop}"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>All live schemas require r6 plus six histories and preserve receipt-or-exact exclusivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose fresh r6 prepared, index, store, and qualification identity</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r6 root/genesis/prepared/index/store bind six histories and reject every r5 path, locator, active attempt, or store.
    - Duplicate workflow environment keys are rejected structurally while the corrected workflow keeps every required key unique.
    - Equal empty tracked snapshots remain clean, unequal nonempty snapshots fail, and UTC/LF/no-tags semantics remain deterministic.
  </behavior>
  <action>Per D-01, D-07, D-14, and D-18, extend canonical qualification and prepared generation to r6 with all six histories. Require a fresh initial root/genesis/prepared/index/store and prevent r5 StateRoot, locator, index, store, active-attempt, or handoff reuse. Carry the exact r5 PrepareAttempt and absent observation as terminal evidence, but treat the duplicate-key HostedPreflight rejection as having no run or downstream artifact. Add positive r6 fixtures and adversarial r5-current, old-root, reused-index, fake-run, missing/reordered/substituted history, set mismatch, correction-lane, duplicate workflow key, UTC, LF, no-tags, equal-empty, and nonempty-drift cases.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Fresh r6 eligibility is six-history-bound, no-run-aware, snapshot-safe, and workflow-key-unique.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Six prior attempts to r6 | Histories are immutable evidence only and cannot grant current authority. |
| GitHub pre-run rejection to history | Absence of a hosted run must not be replaced by invented run identity. |
| r5 state to r6 preparation | No locator, store, tag, handoff, or StateRoot reuse. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-15-01 | Tampering | attempt family | critical | mitigate | Exact six facts/digests/set and substitution/reorder tests. |
| T-08-15-02 | Spoofing | nonexistent r5 hosted run | critical | mitigate | Closed no-run terminal shape and fake-run rejection. |
| T-08-15-03 | Elevation of Privilege | receipt/handoff | critical | mitigate | Exclusive r6 union bound to six histories. |
| T-08-15-04 | Tampering | r5 state reuse | high | mitigate | Fresh r6 root and old path/index/store rejection. |
| T-08-15-05 | Repudiation | r5 failure | high | mitigate | Exact source/tag/stage/absence/no-downstream facts are digest-bound. |
</threat_model>

<verification>Run intent, prepared, and qualification suites; validate r6, six histories, exact r5 no-run facts, key uniqueness, snapshot behavior, UTC, LF, no-tags, and zero live effects. Accepted scope warning: this plan intentionally names 13 files because the contract must advance atomically across intent/prepared, authority/receipt/handoff, and qualification composition; the three independent tasks remain bounded to 5, 4, and 5 files respectively and share only the existing `Test-ReleaseIntent.ps1` cross-schema fixture.</verification>
<success_criteria>r6 and six terminal histories are closed tested contracts ready for hosted seam integration.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-15-SUMMARY.md` when done.</output>
