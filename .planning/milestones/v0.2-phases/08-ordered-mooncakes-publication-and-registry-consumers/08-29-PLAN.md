---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "29"
type: execute
wave: 29
depends_on: ["08-28"]
files_modified:
  - scripts/quality/Test-Phase08RemoteCloneRef.ps1
  - scripts/quality/Invoke-Phase08R12Boundary.ps1
  - scripts/quality/Test-Phase08R12Boundary.ps1
  - scripts/quality/Invoke-Phase08HostedRun.ps1
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "A disposable no-local/no-tags remote clone proves the canonical policy ref reaches PrepareAttempt’s provider while a derived ref variant fails before provider/state creation."
    - "The r12 boundary invocation derives its release ref only from clone-local policy and passes that canonical literal to HostedRun; callers cannot substitute a ref."
    - "r11 remains immutable evidence: tag object 735ad679 and peel/source 30479a, with the original noncanonical invocation failure distinct from the canonical remote-clone provider path."
  artifacts:
    - path: "scripts/quality/Test-Phase08RemoteCloneRef.ps1"
      provides: "real remote-clone canonical-ref regression"
    - path: "scripts/quality/Invoke-Phase08R12Boundary.ps1"
      provides: "canonical clone-policy boundary invocation contract"
  key_links:
    - from: "clone-local policy.initial_profile.release_ref"
      to: "HostedRun PrepareAttempt"
      via: "non-overridable boundary invocation"
      pattern: "refs/tags/modules-v0.1.0-r12"
---
<objective>Make the canonical remote-clone reference contract executable before any r12 tag is created.</objective>
<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/STATE.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-CONTEXT.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-28-SUMMARY.md
@.planning/debug/phase08-r11-real-ref-mismatch.md
@scripts/quality/Test-Phase08RemoteCloneRef.ps1
@scripts/quality/Invoke-Phase08HostedRun.ps1
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Harden the real remote-clone canonical-ref gate</name>
  <files>scripts/quality/Test-Phase08RemoteCloneRef.ps1, scripts/quality/Test-Phase08R12Boundary.ps1</files>
  <behavior>
    - Against immutable r11, a disposable remote clone fetches the policy-selected tag, verifies object 735ad679 and peel/head/boundary 30479a, and the canonical ref reaches exactly one sentinel provider call.
    - A ref derived by appending tag-peel syntax fails with the pre-PrepareAttempt ref rule before provider, locator, credential, or mutation effects.
    - The test never contacts Mooncakes or reads a credential and emits only disposable local evidence.
  </behavior>
  <action>Start with RED coverage for the boundary invocation contract, then retain and extend the existing real `--no-local --no-tags` remote-clone regression. Require clone policy ref, fetched tag object, tag peel, detached HEAD, and InitializeBoundary SHA to be equal before PrepareAttempt. Exercise canonical policy input and a noncanonical derived input through the actual HostedRun seam. Capture the r11 canonical success as proof that tag resolution is correct and retain the original noncanonical caller failure only as terminal history evidence. Do not modify r11 tags or use a local bare fixture as a substitute for this remote contract.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08RemoteCloneRef.ps1</automated></verify>
  <done>The real remote clone proves canonical ref propagation and rejects substitutions before any provider or state effect.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Create the non-overridable r12 boundary invocation contract</name>
  <files>scripts/quality/Invoke-Phase08R12Boundary.ps1, scripts/quality/Test-Phase08R12Boundary.ps1, scripts/quality/Invoke-Phase08HostedRun.ps1</files>
  <behavior>
    - Given a clean clone and verified immutable tag, the contract reads the clone policy ref and invokes PrepareAttempt with that exact value.
    - A caller cannot supply a release-ref override, a clone whose policy/tag/peel/HEAD disagree stops, and no boundary/publisher action occurs.
    - The contract returns a closed sanitized invocation record containing canonical ref, tag object, peel, head, boundary SHA, and zero mutation.
  </behavior>
  <action>After RED, add an r12 boundary wrapper that accepts only clone/root/state/tag identity inputs, reads `policy/release-control.json` from the clone, verifies the fetched policy-selected tag object and peel against detached HEAD, and supplies the derived canonical ref to InitializeBoundary/PrepareAttempt. Do not expose a caller-controlled ReleaseRef parameter. Make the HostedRun call reject an invocation context whose ref is not the clone policy literal before locator/provider work. The wrapper is credential-free and zero-publish; r12’s later boundary plan must call this wrapper rather than assembling a ref manually.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-Phase08R12Boundary.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08RemoteCloneRef.ps1</automated></verify>
  <done>A future r12 boundary can only use the clone policy’s canonical tag ref, and the test proves no alternative ref reaches PrepareAttempt.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| remote clone tag to PrepareAttempt | Canonical tag identity must survive clone/fetch/checkout/caller propagation. |
| external caller to boundary wrapper | Caller input cannot override the policy-selected release ref. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-08-29-01 | Spoofing | release ref | critical | mitigate | Derive ref from clone policy and reject substitutions before provider/state work. |
| T-08-29-02 | Tampering | fetched tag/peel | critical | mitigate | Check object, peel, head, and boundary against immutable expected identity. |
| T-08-29-03 | Elevation of Privilege | boundary caller | high | mitigate | Remove release-ref override from wrapper interface. |
</threat_model>
<verification>Run real remote-clone and wrapper suites, parser, and diff checks. No tag, push, credential, hosted run, handoff, or registry operation is authorized.</verification>
<success_criteria>The next boundary is structurally unable to repeat r11’s noncanonical caller ref mismatch and proves this against an actual disposable remote clone.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-29-SUMMARY.md` when done.</output>
