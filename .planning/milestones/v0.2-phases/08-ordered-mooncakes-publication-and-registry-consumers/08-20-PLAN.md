---
phase: 08-ordered-mooncakes-publication-and-registry-consumers
plan: "20"
type: execute
wave: 20
depends_on: ["08-19"]
files_modified:
  - scripts/quality/ReleaseQualification.Common.ps1
  - scripts/quality/Invoke-ReleaseQualification.ps1
  - scripts/quality/New-PreparedReleaseBundle.ps1
  - scripts/quality/Test-PreparedReleaseBundle.ps1
  - scripts/quality/Test-ReleaseQualificationNegative.ps1
  - scripts/quality/Test-Phase08Qualification.ps1
  - .gitattributes
  - scripts/quality/Test-CrossPlatformReleaseArchive.ps1
autonomous: true
requirements: [DIST-01]
must_haves:
  truths:
    - "Canonical ZIP preserves original entry order and exact payload bytes while fixing timestamps, Unix made-by/permissions, and stored compression before any archive digest enters r8 identity (D-01, D-18)."
    - "Canonical paths are safe forward-slash relative paths; duplicate, absolute, traversal, payload-drift, and metadata-drift inputs fail closed."
    - "Canonicalization is idempotent, extractable, valid for frozen Moon source consumption, and stable across host-dependent raw ZIP variants."
    - "Committed LF policy and dual-core.autocrlf clean clones remain a separate payload-byte invariant; fresh r8 prepared identity binds eight histories and no r7 state/artifact."
  artifacts:
    - path: "scripts/quality/ReleaseQualification.Common.ps1"
      provides: "Deterministic canonical ZIP implementation"
    - path: "scripts/quality/Test-CrossPlatformReleaseArchive.ps1"
      provides: "Raw-container divergence and canonical convergence regression"
    - path: "scripts/quality/New-PreparedReleaseBundle.ps1"
      provides: "r8 canonical archive prepared identity"
  key_links:
    - from: "raw moon package ZIP"
      to: "release intent archive_sha256"
      via: "canonical archive generation before hashing"
      pattern: "archive_sha256"
    - from: "eight immutable history digests"
      to: "fresh r8 prepared/index/store"
      via: "canonical ordered-set binding"
      pattern: "historical_r7_sha256"
---

<objective>Close r8 canonical archive and prepared-qualification identity before hosted seam integration.

Purpose: Remove host/runtime ZIP-container bytes from release identity while preserving exact source payload provenance.
Output: Canonical ZIP qualification, cross-platform regressions, and fresh r8 prepared identity.</objective>

<execution_context>@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/phase08-cross-platform-intent-components.md
@.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-19-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Canonicalize ZIP container identity without changing ordered source payloads</name>
  <files>scripts/quality/ReleaseQualification.Common.ps1, scripts/quality/Invoke-ReleaseQualification.ps1, scripts/quality/Test-CrossPlatformReleaseArchive.ps1, .gitattributes</files>
  <behavior>
    - Semantically equal raw ZIPs with deliberately different host metadata converge to identical canonical bytes.
    - Original entry order, safe normalized paths, payload SHA/length, fixed timestamp/made-by/permissions, and stored compression are exact.
    - Canonical output is idempotent, extractable, and accepted by frozen Moon source validation.
    - LF dual-clone payload identity remains independently enforced.
  </behavior>
  <action>Per D-01, D-08, D-10, and D-18, require qualification to canonicalize each raw `moon package` archive before any archive digest enters intent/prepared evidence. Preserve original entry order and exact payload bytes; require safe relative forward-slash paths; fix timestamp, Unix made-by/permissions, and stored compression. Reject duplicate/absolute/traversal paths, payload change, missing/extra entry, attribute drift, non-idempotence, extraction failure, or frozen source-consumption failure. Preserve `.gitattributes` `text=auto eol=lf` and dual-core.autocrlf clean-clone regression as the separate payload invariant. Add semantic-equal/raw-different container fixtures and exact canonical byte/digest assertions for all three modules.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-CrossPlatformReleaseArchive.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Invoke-ReleaseQualification.ps1 -Check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>Raw host-container differences cannot affect archive identity, while ordered paths and payload provenance remain exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind canonical archive identity into fresh r8 prepared qualification</name>
  <files>scripts/quality/New-PreparedReleaseBundle.ps1, scripts/quality/Test-PreparedReleaseBundle.ps1, scripts/quality/Test-ReleaseQualificationNegative.ps1, scripts/quality/Test-Phase08Qualification.ps1</files>
  <behavior>
    - Fresh r8 root/genesis/prepared/index/store bind canonical archives and all eight histories/set.
    - Raw r7 container digests, r7 state/artifacts, or recompressed variants cannot satisfy r8 prepared identity.
    - Snapshot, UTC, LF, no-tags, and closed qualification invariants remain fail-closed.
  </behavior>
  <action>Per D-01, D-07, D-14, D-15, and D-18, make canonical archive digests mandatory in r8 prepared generation and qualification. Require fresh r8 root/genesis/prepared/index/store and eight histories/set; reject raw r7 container digests, r7 root/locator/index/store/active attempt/prepared/handoff reuse, alternative recompression, missing canonical metadata, or canonical/source inventory disagreement. Retain equal-empty/nonempty-drift, UTC, LF, no-tags, history order/set, and correction-lane negatives. Ensure the output contract is exactly what 08-21 publisher/hosted/pre-live seams consume.</action>
  <verify><automated>pwsh -NoProfile -File ./scripts/quality/Test-PreparedReleaseBundle.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-ReleaseQualificationNegative.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; pwsh -NoProfile -File ./scripts/quality/Test-Phase08Qualification.ps1; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git diff --check</automated></verify>
  <done>r8 prepared identity is canonical-archive-bound, eight-history-bound, and fresh from all r7 state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Raw ZIP to canonical archive | Host/runtime metadata cannot influence release identity. |
| Canonical archive to prepared evidence | Ordered paths/payloads and canonical bytes must agree before hashing. |
| r7 state to r8 preparation | No old root, store, archive, or handoff may cross. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-20-01 | Tampering | ZIP canonicalizer | critical | mitigate | Fixed attrs/stored/order/path/payload and idempotence tests. |
| T-08-20-02 | Spoofing | prepared archive identity | critical | mitigate | Canonical digest plus source inventory agreement. |
| T-08-20-03 | Tampering | unsafe ZIP path | high | mitigate | Reject absolute, traversal, duplicate, and noncanonical paths. |
| T-08-20-04 | Repudiation | r7 container failure | high | mitigate | Raw and canonical identities remain explicitly distinguished. |
</threat_model>

<verification>Run canonical archive, full release qualification, prepared, negative, and Phase 8 qualification suites; confirm cross-host convergence, exact ordered payload provenance, eight-history r8 identity, and zero live effects.</verification>
<success_criteria>Canonical r8 prepared identity is closed and ready for publisher/workflow/hosted integration in 08-21.</success_criteria>
<output>Create `.planning/phases/08-ordered-mooncakes-publication-and-registry-consumers/08-20-SUMMARY.md` when done.</output>
