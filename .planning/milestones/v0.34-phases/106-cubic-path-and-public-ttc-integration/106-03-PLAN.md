---
phase: 106-cubic-path-and-public-ttc-integration
plan: "03"
type: execute
wave: 3
depends_on:
  - "106-02"
files_modified:
  - modules/mb-font/font/cff_admission.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/collection.mbt
  - modules/mb-font/font/collection_wbtest.mbt
  - modules/mb-font/font/collection_test.mbt
  - modules/mb-font/font/font_test.mbt
autonomous: true
requirements:
  - CFF-04
  - CFF-05
must_haves:
  truths:
    - "A supported CFF1 face selected from TTC/OTC opens through the existing FontCollection::open_face signature and returns the existing opaque Font."
    - "Selected CFF admission reuses the standalone CFF transaction and outline implementation while retaining the collection root, retained opening revision, face directory start, expected table count, and table-local CFF offsets."
    - "Two faces may share CFF bytes while independently retaining face-local cmap, hmtx, kern, and common-table facts."
    - "Standalone and selected forms of equal face data return identical ordered mappings, metrics, kerning, bounds, and cubic PathCommand values."
    - "All previously qualified static-glyf standalone and collection admission, queries, paths, errors, and exact charges remain unchanged."
  artifacts:
    - "modules/mb-font/font/collection.mbt closes open_face dispatch over StaticGlyf and Cff while preserving all existing unsupported-profile outcomes."
    - "modules/mb-font/font/cff_admission.mbt accepts the authoritative collection opening revision and selected-face coordinate facts without copying or reparsing."
    - "collection white-box fixtures prove shared-CFF bytes with face-local common facts, root/table offset authority, exact/one-short charge, and mutation atomicity."
    - "public standalone and collection tests freeze CFF parity plus complete static-glyf compatibility fingerprints."
  key_links:
    - "FontCollection retained face profile Cff -> shared selected CFF admission -> complete AdmittedCff1 -> font_from_admitted_cff1."
    - "Collection root-relative table records -> checked CFF TableWindow -> table-local CFF INDEX/DICT/subroutine offsets."
    - "Shared CFF table -> each selected face's own RequiredTableFacts -> format-neutral Font mapping/metrics/kerning."
    - "Selected CFF Font::outline -> the same cff_decode_outline_atomic used by standalone."
  prohibitions:
    - statement: "No selected face is copied, rebased into a face-local buffer, or parsed by a parallel CFF implementation."
      status: unverified
      verification: flagged
    - statement: "No duplicate or overlapping collection/table range policy changes."
      status: unverified
      verification: flagged
    - statement: "No CFF2, variable, mixed-outline, or otherwise unsupported profile becomes admissible."
      status: unverified
      verification: flagged
    - statement: "No public CFF-specific API or limit type is introduced."
      status: unverified
      verification: flagged
    - statement: "No static-glyf mapping, metric, kerning, outline, error-order, or resource-charge behavior changes."
      status: unverified
      verification: flagged
---

<objective>
Complete CFF-05 by routing selected TTC/OTC CFF1 faces through the same opaque Font and atomic cubic outline implementation as standalone, then freeze all static-glyf compatibility.

Purpose: Preserve collection root/table coordinate authority and face-local common facts without copying, a second parser, or any public-format branching per D-01 through D-03 and D-13 through D-18.
Output: Closed collection profile dispatch, selected-face CFF promotion, shared-CFF/face-local query parity, exact edge/resource evidence, and full standalone/collection glyf fingerprints.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-CONTEXT.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-RESEARCH.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-PATTERNS.md
@.planning/phases/106-cubic-path-and-public-ttc-integration/106-02-SUMMARY.md
@.planning/phases/104-cff1-profile-and-bounded-data-model/104-03-SUMMARY.md
@.planning/phases/104-cff1-profile-and-bounded-data-model/104-VERIFICATION.md
@modules/mb-font/font/collection_parser.mbt
@modules/mb-font/font/collection.mbt
@modules/mb-font/font/cff_admission.mbt
@modules/mb-font/font/font.mbt

<interfaces>
Current collection contracts the executor must preserve:
- `CollectionFaceFacts` retains `directory_start`, `directory_end`, `table_count`, `sfnt_version`, and `FontFaceProfile`.
- `pub fn FontCollection::open_face(index, FontLimits, Budget) -> Result[Font, CoreError]`.
- `FontCollection::open_face_after_preflights` currently validates collection revision and index, then admits only `StaticGlyf`.
- `admit_cff1_structure_at_after_preflight` already accepts root source, directory start, expected table count, checksum mode, collection charge flag, limits, Budget, and final guard.
- `font_open_collection_face` is the existing StaticGlyf adapter and must remain behaviorally unchanged.
</interfaces>
</context>

<edge_assumptions>
1. CFF-05 adjacency uses the existing checked half-open collection-root and table-local windows; exact touching is non-overlap, while duplicate/overlap acceptance or rejection remains the current parser policy.
2. CFF-05 empty behavior uses existing collection semantics: a zero-face collection and an out-of-range index return the established structured error; a one-face collection opens index zero.
3. When standalone and selected inputs compare equal semantically, output order is specified and stable: scalar mapping, numeric GID, metrics fields, kerning, and every PathCommand compare in their existing order.
4. A collection's retained opening revision is the sole selected-face revision identity; selected admission does not recapture a newer revision.
</edge_assumptions>

## Artifacts this phase produces

New files:
- None.

New symbols:
- Private `font_open_cff_collection_face` adapter over the existing selected CFF admission transaction and `font_from_admitted_cff1`.
- Private collection CFF probes for root/table authority, final revision, and exact selected-face charge tests.
- Public black-box test fingerprints for standalone-versus-selected CFF equality and frozen Phase 106 static-glyf behavior.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route selected CFF1 faces through the shared admission and outline implementation</name>
  <files>modules/mb-font/font/cff_admission.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/collection.mbt, modules/mb-font/font/collection_wbtest.mbt, modules/mb-font/font/collection_test.mbt</files>
  <read_first>
    @.planning/phases/104-cff1-profile-and-bounded-data-model/104-VERIFICATION.md
    @.planning/phases/106-cubic-path-and-public-ttc-integration/106-02-SUMMARY.md
    @modules/mb-font/font/collection_parser.mbt
    @modules/mb-font/font/collection.mbt
    @modules/mb-font/font/cff_admission.mbt
    @modules/mb-font/font/font.mbt
    @modules/mb-font/font/collection_wbtest.mbt
    @modules/mb-font/font/collection_test.mbt
  </read_first>
  <behavior>
    - `FontCollection::open_face` matches `FontFaceProfile::Cff` and calls one selected-face adapter with the retained root, collection opening revision, face directory start, and expected table count per D-13.
    - The adapter uses the same complete CFF admission and `font_from_admitted_cff1` as standalone; no bytes are copied and CFF-internal offsets remain table-local per D-14.
    - Two faces sharing one CFF table may return different face-local scalar mapping, hmtx metrics, and kern values while their equal glyph program returns identical cubic commands per D-15.
    - CFF2, variable, mixed, and other unsupported profiles retain the existing collection profile error per D-14 and D-16.
  </behavior>
  <action>
    RED: extend the existing shared-CFF TTC fixture to contain at least two selected face directories that point root-relatively to the same CFF table while using distinct face-local cmap, hmtx, kern, and common-table records. Add public `open_face` assertions for both faces and exact command parity with an equivalent standalone face.

    GREEN: change `FontCollection::open_face_after_preflights` from the StaticGlyf-only check to a closed match. Keep the existing StaticGlyf call intact. For Cff, call `font_open_cff_collection_face`, which forwards the retained collection root, `self.opening_revision`, `face.directory_start`, `face.table_count`, collection checksum mode, limits, Budget, and existing probes to the shared CFF admission transaction, then projects complete AdmittedCff1 through the same constructor and outline dispatch as standalone. Adjust the selected CFF admission seam to accept the authoritative retained revision instead of capturing a fresh one. Return the existing profile capability error for every other face kind.
  </action>
  <acceptance_criteria>
    - CFF-05 selected CFF faces open through the unchanged `FontCollection::open_face` and return the unchanged opaque Font.
    - Root-relative table records and table-local CFF offsets remain checked half-open windows; touching, duplicate, and overlap behavior is unchanged.
    - Shared CFF bytes are not copied and do not erase face-local cmap, hmtx, kern, head, hhea, or OS/2 facts.
    - Standalone and selected instances of equal face data produce identical ordered glyph mapping, metrics, kerning, and PathCommand values.
    - Unsupported collection face profiles keep their established category/code/operation/context behavior.
  </acceptance_criteria>
  <verify>
    <automated>moon test modules/mb-font/font --target native --filter "*Phase 106 selected CFF1*"</automated>
  </verify>
  <done>A selected static CFF1 TTC/OTC face uses the same admission, opaque Font projection, queries, and cubic outline implementation as standalone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze collection edge cases, exact authority, standalone parity, and all glyf behavior</name>
  <files>modules/mb-font/font/collection_wbtest.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/font_test.mbt</files>
  <read_first>
    @modules/mb-font/font/collection_wbtest.mbt
    @modules/mb-font/font/collection_test.mbt
    @modules/mb-font/font/font_test.mbt
    @modules/mb-font/font/collection_parser.mbt
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-VERIFICATION.md
  </read_first>
  <behavior>
    - Zero-face, single-face, and out-of-range collection cases preserve existing parser/query results, and selected CFF adds no null semantics.
    - Exact selected-face admission and outline budgets succeed; each one-short dimension and each caller/ancestor case fails atomically with unchanged counters.
    - Mutation before selection, during shared CFF execution, before selected admission commit, or at the shared outline transaction's post-stage probe before its final source-revision guard yields State without Font/Path2 publication.
    - Existing standalone and collection StaticGlyf fingerprints remain identical for mapping, GID validation, metrics, kerning, ordered paths, errors, and exact charges per D-17.
    - Existing FontLimits and Budget are the only public resource controls per D-18.
  </behavior>
  <action>
    Add the spec-less collection edge matrix for zero faces, one CFF face, out-of-range index, exactly touching half-open ranges, current duplicate/overlap outcomes, and equal standalone/selected ordering. Reuse the existing exact/one-short selected-CFF white-box pattern for admission and extend it through public `open_face` plus `outline`, including nested ancestor Budget windows and mutation probes.

    Expand the Phase 105 public static-glyf standalone and collection fingerprints into Phase 106 fingerprints that compare all common query values, ordered path commands, exact structured errors, and every Budget dimension before and after CFF dispatch exists. Run the full native package suite and all-target check; correct only regressions attributable to Phase 106. Do not change `collection_parser.mbt`, glyf lowering, glyf charge formulas, public FontLimits, or public error types.
  </action>
  <acceptance_criteria>
    - CFF-05 empty behavior is explicit: zero-face/out-of-range results remain existing collection errors, while a single supported face opens at index zero.
    - CFF-05 adjacency is explicit: existing half-open root/table windows and duplicate/overlap policy are unchanged.
    - CFF-05 equality/ordering is explicit: equal standalone and selected inputs return identical ordered mapping, metrics, kerning, and path commands.
    - Exact and every one-short selected admission/query authority outcome is atomic for caller and ancestor Budgets.
    - Static-glyf standalone and collection fingerprints show no semantic, error-order, command, or charge drift.
    - Native tests and `moon check --target all` pass without a new public CFF or Type 2 limit surface.
  </acceptance_criteria>
  <verify>
    <automated>moon test modules/mb-font/font --target native --filter "*Phase 106 preserves*"</automated>
  </verify>
  <done>CFF-05 collection integration and every Phase 106 compatibility/edge boundary are frozen by deterministic automated evidence.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Applicability and Threshold

This plan crosses the untrusted TTC/OTC root and selected-face table-coordinate boundary. Critical/high threats are mitigated and block completion; medium threats on the primary collection boundary are mitigated. No package installation or external service occurs.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained collection root -> selected face directory | Face index, directory start, table count, and root-relative table offsets are attacker controlled. |
| selected table window -> table-local CFF parser | CFF-internal offsets must never be interpreted against the collection root or a copied/rebased face. |
| shared CFF bytes -> face-local common facts | Shared outline storage must not spoof another face's cmap, hmtx, kern, or line metrics. |
| new Cff collection branch -> existing StaticGlyf branch | Dispatch changes must not alter previously qualified behavior or charges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-13 | Tampering | root-relative versus table-local offsets | high | mitigate | Selected adapter passes retained root/directory/table count into the existing checked parser; shared fixtures assert exact root/table coordinates and no rebasing. |
| T-106-14 | Spoofing | shared CFF with face-local common tables | high | mitigate | Each face admits its own RequiredTableFacts while sharing only the CFF TableWindow; divergent cmap/hmtx/kern fixtures prove isolation. |
| T-106-15 | Tampering | selected collection revision identity | high | mitigate | CFF admission receives `self.opening_revision`; admission probes run before its commit, while shared outline probes end post-stage before the final source-revision guard, exactly one charge, and the infallible immediate Path2 return. |
| T-106-16 | Denial of Service | selected admission and outline authority | high | mitigate | Existing FontLimits plus exact caller/ancestor preflight cover directory, CFF, VM, and path allocations/work with one-short tests. |
| T-106-17 | Elevation of Privilege | unsupported collection profiles | medium | mitigate | Closed dispatch accepts only StaticGlyf and Cff; CFF2, variable, mixed, and other cases retain the current capability error. |
| T-106-18 | Tampering | StaticGlyf branch compatibility | medium | mitigate | Existing arm remains intact and expanded standalone/collection fingerprints compare queries, commands, errors, and all charge dimensions. |
</threat_model>

<verification>
Run from the repository root:

1. `moon test modules/mb-font/font --target native --filter "*Phase 106 selected CFF1*"`
2. `moon test modules/mb-font/font --target native --filter "*Phase 106 preserves*"`
3. `moon test modules/mb-font/font --target native --filter "*CFF outline exact path charge*"`
4. `moon test modules/mb-font/font --target native --filter "*CFF outline atomic*"`
5. `moon test --target native`
6. `moon check --target native`
7. `moon check --target all`
</verification>

<success_criteria>
- CFF-04 remains complete for standalone format-neutral Font workflows and atomic native cubic Path2 results.
- CFF-05 opens supported selected CFF1 faces through the same implementation without copying or coordinate-space drift.
- Shared CFF bytes coexist with correctly isolated face-local mapping, metrics, kerning, and common facts.
- Spec-less empty, encoding, adjacency, and equal-order cases have concrete automated acceptance.
- All static-glyf standalone and collection behavior and charges remain unchanged.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Use static CFF1 outlines through existing format-neutral Font and FontCollection workflows | 106-01/02/03 | COVERED | Standalone tracer, atomic hardening, and selected collection integration deliver the full outcome. |
| REQ | CFF-04 | Opaque standalone Font, format-neutral queries/errors, atomic native cubic Path2 | 106-01/02 | COVERED | Wave 1 delivers the path; Wave 2 closes resource, mutation, and error boundaries. |
| REQ | CFF-05 | Same selected TTC/OTC CFF implementation, coordinate authority, face-local facts, frozen glyf | 106-03 | COVERED | Closed profile dispatch and parity/compatibility matrices cover all clauses. |
| RESEARCH | — | Exact-capacity Path2 plus shared Type 2 geometry core and path sink | 106-01 | COVERED | `Path2::with_capacity`, per-GID count, shared VM, pending moveto, and native CubicTo are explicit. |
| RESEARCH | — | Retain complete CFF common/query facts and add closed metric/outline dispatch | 106-01 | COVERED | Outline-neutral common admission and non-fallible Font projection are in the tracer. |
| RESEARCH | A1 | Quotient-plus-remainder rational-to-Double rule | 106-01 | COVERED | The recommended rule is frozen by direct coordinate goldens. |
| RESEARCH | A2 | Conservative per-command backing authority | 106-01/02 | COVERED | The 64-byte unit is explicit, directly tested, and used in exact/one-short transaction evidence. |
| RESEARCH | — | Exact resource, mutation, and zero-publication query transaction | 106-02 | COVERED | Caller/ancestor exact/one-short and all guarded mutation seams are planned. |
| RESEARCH | — | Shared selected CFF route with no copy/rebase and frozen glyf | 106-03 | COVERED | Uses retained collection authority and the same standalone implementation. |
| CONTEXT | D-01 | Existing Font/open_face/outline public workflows only | 106-01/03 | COVERED | Public CFF workflow uses existing signatures; only a format-neutral Path2 capacity constructor is added under D-10. |
| CONTEXT | D-02 | Private Glyf or complete Cff1 dispatch | 106-01 | COVERED | Complete AdmittedCff1 crosses the existing closed source boundary. |
| CONTEXT | D-03 | Opaque receiving-Font numeric GID and existing mapping/order | 106-01/02/03 | COVERED | Standalone, invalid/cross-font, and selected parity tests freeze it. |
| CONTEXT | D-04 | One Type 2 interpreter with path-capable geometry sink | 106-01 | COVERED | The existing operator loop feeds shared bounds/path geometry. |
| CONTEXT | D-05 | Native MoveTo/LineTo/CubicTo/Close only | 106-01 | COVERED | Exact ordered cubic command tests prohibit quadratic conversion/flattening. |
| CONTEXT | D-06 | Exact matrix/design normalization and checked final Double conversion | 106-01 | COVERED | Rational geometry and conversion goldens are part of the tracer. |
| CONTEXT | D-07 | Contour closure and empty/move-only Path2 semantics | 106-01 | COVERED | Pending moveto and non-empty Close rules have direct tests. |
| CONTEXT | D-08 | Selected-GID fresh query; no retained paths | 106-01/02 | COVERED | Compact count retention and fresh one-GID execution are explicit. |
| CONTEXT | D-09 | Guard, stage, preflight, final guard, one commit, publish | 106-02 | COVERED | Authorized pre-execution/in-VM probes lead to the post-stage probe, final source-revision guard, exactly one Budget::charge, and an infallible immediate Path2 return with no post-commit work. |
| CONTEXT | D-10 | Real path/scratch allocation and capacity authority | 106-01/02 | COVERED | Exact-capacity Path2, 64-byte unit, real allocations, and largest allocation are tested. |
| CONTEXT | D-11 | Reuse admitted facts; no reparse/bounds substitution | 106-01/02 | COVERED | Selected descriptor/subroutine/matrix/limits flow directly into the sole VM. |
| CONTEXT | D-12 | Standalone complete CFF transaction before public Font construction | 106-01 | COVERED | Common facts join admission before non-fallible projection. |
| CONTEXT | D-13 | Accept classified selected Cff through shared admission | 106-03 | COVERED | Closed collection dispatch calls the shared selected adapter. |
| CONTEXT | D-14 | Retain root/table coordinate authority, no copy, unsupported profiles closed | 106-03 | COVERED | Shared-root fixtures and closed profile errors are explicit. |
| CONTEXT | D-15 | Common facts and hmtx/retained-bounds metric authority | 106-01/03 | COVERED | Width mismatch and shared-CFF/face-local facts are directly asserted. |
| CONTEXT | D-16 | Existing operations, CoreError, mutation, and GID behavior | 106-01/02/03 | COVERED | Black-box and multi-fault tests cover standalone and selected routes. |
| CONTEXT | D-17 | Static-glyf standalone/collection behavior and charges unchanged | 106-01/02/03 | COVERED | Existing arm remains intact and Phase 106 fingerprints cover every listed observable. |
| CONTEXT | D-18 | Existing FontLimits and Budget only | 106-01/02/03 | COVERED | No CFF/Type 2 public limits are planned. |
| CONTEXT | deferred | Licensed/hostile qualification, CFF2/variation, seac, rendering, authoring, and other excluded features | — | EXCLUDED | Explicitly assigned outside this phase by CONTEXT.md. |

<output>
After completion, create `.planning/phases/106-cubic-path-and-public-ttc-integration/106-03-SUMMARY.md`.
</output>
