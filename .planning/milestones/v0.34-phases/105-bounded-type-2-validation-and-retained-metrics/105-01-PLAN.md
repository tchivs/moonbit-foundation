---
phase: 105-bounded-type-2-validation-and-retained-metrics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-font/font/cff_type2_fixed.mbt
  - modules/mb-font/font/cff_dict.mbt
  - modules/mb-font/font/cff_keying.mbt
  - modules/mb-font/font/limits.mbt
  - modules/mb-font/font/cff_type2_fixed_wbtest.mbt
  - modules/mb-font/font/cff_dict_wbtest.mbt
  - modules/mb-font/font/cff_cid_fixture_wbtest.mbt
autonomous: true
requirements:
  - T2-01
  - T2-02
  - CFF-03
must_haves:
  truths:
    - "Every Type 2 wire number and arithmetic result has one checked, target-identical signed Q16.16 representation."
    - "initialRandomSeed is retained as a checked Private DICT number and produces one deterministic per-GID PRNG stream."
    - "Name-keyed and CID-keyed glyphs expose one non-duplicated effective matrix recipe before VM execution."
    - "All private Type 2 hard ceilings and derived caller limits are explicit and testable."
  artifacts:
    - "cff_type2_fixed.mbt owns signed fixed arithmetic, deterministic random, rational matrix composition, and outward rounding."
    - "cff_dict.mbt retains initialRandomSeed and explicit FD FontMatrix presence."
    - "cff_keying.mbt hands the VM distinct Top and optional FD matrix facts without name-keyed duplication."
    - "limits.mbt derives bounded Type 2 byte, call, work, geometry, and allocation authority."
  key_links:
    - "Private DICT parsing feeds the PRNG seed without converting through Double."
    - "CffGlyphDescriptor feeds exact matrix facts to the fixed transform used by retained bounds."
    - "Type 2 limits reuse FontLimits/ResourceLimits rather than create a second public configuration surface."
---

<objective>
Establish the deterministic numeric and state foundation required by every Phase 105 execution path.

Purpose: Freeze arithmetic, random, matrix, and limit semantics before the operator VM can accumulate target drift or duplicate CFF keying rules.
Output: Private fixed/PRNG/transform helpers, retained Private DICT facts, corrected matrix handoff, and boundary tests.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-CONTEXT.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-RESEARCH.md
@modules/mb-font/font/cff_dict.mbt
@modules/mb-font/font/cff_keying.mbt
@modules/mb-font/font/outline.mbt
@modules/mb-font/font/limits.mbt
</context>

<assumption_delta_decision>
Primary noun: deterministic Type 2 numeric kernel.
Decision: promote as one private source of truth.
Variants: integer and 16.16 wire numbers, arithmetic results, matrix coordinates, random values, and retained-bound rounding.
Rationale: separate implementations would make admission, future path replay, and four targets disagree.
</assumption_delta_decision>

<edge_assumptions>
1. A-01: Type 2 byte 255 is signed 16.16 and byte 28 is signed short; DICT longint/real encodings are not Type 2 encodings.
2. A-02: Every observable VM result fits signed 32-bit Q16.16 raw range; Int64 is only a checked intermediate.
3. A-03: Undefined arithmetic is rejected as Data; multiplication/division scale toward zero and square root rounds down.
4. A-04: xorshift32 resets per GID from checked initialRandomSeed, maps zero to 0x6D2B79F5, and emits raw 1..65536.
5. A-05: Top FontMatrix applies once to name-keyed glyphs; CID applies optional FD-local then Top; omitted FD is identity.
6. A-06: Matrix composition and unitsPerEm normalization are exact rational operations before outward integer rounding.
7. A-07: Stack 48, transient 32, stems 96, frames 10, CharString 65535, and Subrs 65536 are hard private ceilings.
</edge_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the checked Q16.16 arithmetic and deterministic PRNG kernel</name>
  <files>modules/mb-font/font/cff_type2_fixed.mbt, modules/mb-font/font/cff_type2_fixed_wbtest.mbt</files>
  <read_first>
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-CONTEXT.md
    @.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-RESEARCH.md
    @modules/mb-font/font/outline.mbt
  </read_first>
  <behavior>
    - Type 2 integer, shortint, and 255 fixed encodings decode to exact signed Q16.16 raw values and reject truncation/range failure.
    - Checked add/sub/abs/neg/mul/div/sqrt cover zero, signs, toward-zero scaling, floor-root, INT32 edges, division by zero, and overflow.
    - Identical seeds generate identical `(0,1]` Q16.16 streams; per-GID reset is order-independent and zero seed never locks the generator.
  </behavior>
  <action>
    Create the private fixed kernel with explicit signed narrowing and stable Data contexts. Keep Int64 intermediates local, never use Double, and make every returned value a checked signed-32 raw Q16.16. Implement integer square root without floating point. Add xorshift32 with the locked seed normalization/output mapping and direct golden vectors. Write tests first, including positive/negative half-way scaling and one-less/one-more raw boundaries.
  </action>
  <acceptance_criteria>
    - All numeric operations produce hand-derived target-independent raw values.
    - Every undefined or overflowing input returns stable Data without partial state.
    - PRNG vectors prove repeatability, reset isolation, non-zero output, and full endpoint policy.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>The VM has one tested fixed and random kernel with no host numeric dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain seed and correct Top/FD matrix handoff</name>
  <files>modules/mb-font/font/cff_dict.mbt, modules/mb-font/font/cff_keying.mbt, modules/mb-font/font/cff_dict_wbtest.mbt, modules/mb-font/font/cff_cid_fixture_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/cff_dict.mbt
    @modules/mb-font/font/cff_keying.mbt
    @modules/mb-font/font/cff_cid_fixture_wbtest.mbt
  </read_first>
  <behavior>
    - Omitted initialRandomSeed retains exact numeric default zero; explicit integer/real forms retain a checked numeric fact rather than being discarded.
    - Name-keyed descriptors expose Top matrix once and no fabricated FD matrix.
    - CID descriptors preserve whether the selected FD matrix was omitted, and explicit FD then Top composition matches hand-derived vectors.
  </behavior>
  <action>
    Extend the typed Private DICT fact with initialRandomSeed and validate its supported checked conversion. Change Font DICT matrix representation to preserve presence separately from its value. Correct the descriptor construction so name-keyed glyphs cannot duplicate Top FontMatrix and omitted CID FD matrices act as identity. Add exact/default/duplicate/invalid seed tests plus name, CID-explicit, and CID-omitted matrix facts. Do not execute a CharString yet.
  </action>
  <acceptance_criteria>
    - No accepted seed or matrix fact is silently discarded or synthesized ambiguously.
    - Existing Phase 104 keying tests remain green.
    - Hand-derived matrix facts identify the exact transform order the bounds sink will consume.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>Every GID descriptor carries an unambiguous seed and transform recipe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define private Type 2 limits and fixed matrix/bounds primitives</name>
  <files>modules/mb-font/font/cff_type2_fixed.mbt, modules/mb-font/font/limits.mbt, modules/mb-font/font/cff_type2_fixed_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/limits.mbt
    @modules/mb-font/font/metrics.mbt
    @modules/mb-font/font/cff_admission.mbt
  </read_first>
  <behavior>
    - Hard Type 2 ceilings remain enforced even when caller limits are larger.
    - Point/contour/command/instruction/work/allocation authority derives deterministically from existing public limits.
    - Exact rational Top/FD/unitsPerEm transforms and negative-floor/positive-ceil helpers never under-bound fractional geometry.
  </behavior>
  <action>
    Add private Type 2 limit facts and checked derived command/byte/work/allocation ceilings using established checked/budget helpers. Implement exact matrix composition/application and outward rounding primitives in the fixed module. Cover limit 0/1/max, points + 2×contours overflow, matrix identity/order/translation, negative fractional floor, positive fractional ceil, and transform overflow.
  </action>
  <acceptance_criteria>
    - All private hard limits and caller-derived limits have exact/one-short tests.
    - Transform helpers never use Double and never round inward.
    - Limit derivation does not change public FontLimits shape.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>The operator VM can consume one explicit numeric and resource contract.</done>
</task>

</tasks>

<verification>
- `moon test --target native`
- `moon check --target native`
</verification>

<success_criteria>
- T2-01 has deterministic numeric/random primitives.
- T2-02 has explicit hard and caller-derived limits before execution exists.
- CFF-03 has non-duplicated transform facts suitable for truthful retained bounds.
</success_criteria>

<output>
After completion, create `.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-01-SUMMARY.md`.
</output>

