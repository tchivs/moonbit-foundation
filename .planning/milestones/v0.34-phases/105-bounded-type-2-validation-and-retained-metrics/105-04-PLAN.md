---
phase: 105-bounded-type-2-validation-and-retained-metrics
plan: "04"
type: execute
wave: 4
depends_on:
  - "105-03"
files_modified:
  - modules/mb-font/font/cff_admission.mbt
  - modules/mb-font/font/font.mbt
  - modules/mb-font/font/metrics.mbt
  - modules/mb-font/font/cff_admission_wbtest.mbt
  - modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
  - modules/mb-font/font/font_wbtest.mbt
  - modules/mb-font/font/font_test.mbt
  - modules/mb-font/font/collection_test.mbt
autonomous: true
requirements:
  - T2-01
  - T2-02
  - CFF-03
must_haves:
  truths:
    - "CFF structural parsing, all-glyph Type 2 execution, retained bounds, final mutation guard, and budget charge form one transaction."
    - "Any failure publishes no Font, no bounds, and no committed admission charge."
    - "hmtx remains the sole public advance/LSB authority even when CFF width differs or a collection shares CFF bytes."
    - "Native qualification and all-target checking pass without glyf/public API regressions."
  artifacts:
    - "cff_admission.mbt stages structural and VM facts and commits exactly once."
    - "AdmittedCff1 privately retains complete descriptors, compact bounds, and named charge."
    - "Hostile and admission tests freeze exact/one-short, mutation, multi-fault, and atomic publication."
  key_links:
    - "Phase 104 structural ledger and Phase 105 VM ledger combine before FontAdmissionLedger preflight."
    - "metrics.mbt continues to read hmtx while the closed outline source can carry retained CFF bounds for Phase 106."
    - "Final revision validation precedes the only budget commit and private Font promotion."
---

<objective>
Integrate all-glyph Type 2 validation and retained bounds into one atomic CFF admission transaction, then close Phase 105 with regression evidence.

Purpose: Make it impossible to publish or charge a partially validated CFF-backed font while preserving existing metric and glyf contracts.
Output: Complete private AdmittedCff1, atomic admission, hostile/resource/mutation evidence, and target checks.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-CONTEXT.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-RESEARCH.md
@.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-03-SUMMARY.md
@modules/mb-font/font/cff_admission.mbt
@modules/mb-font/font/font.mbt
@modules/mb-font/font/metrics.mbt
@modules/mb-font/font/tables.mbt
</context>

<assumption_delta_decision>
Primary noun: atomic CFF admission transaction.
Decision: promote structure + VM + retained bounds as one preflight/commit boundary.
Variants: standalone and selected collection face adapters.
Rationale: committing Phase 104 structure before Phase 105 execution would violate CFF-03 on any later glyph failure.
</assumption_delta_decision>

<edge_assumptions>
1. A-01: Existing structural admission must be split into staged and committing seams without duplicating parsing.
2. A-02: Combined charge uses unique source bytes, executed bytes/work for replay, true allocation count, and max single allocation size.
3. A-03: The last source revision check occurs after all facts and preflight but before the only commit.
4. A-04: hmtx/CFF width mismatch is legal for this validation boundary and especially for shared-CFF collection faces.
5. A-05: Phase 105 may privately close AdmittedCff1 and retained metrics; public CFF outline/path success remains Phase 106.
6. A-06: Existing static-glyf tests and public API fingerprints must remain unchanged.
</edge_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Refactor CFF structure into a staged transaction and join all-glyph VM facts</name>
  <files>modules/mb-font/font/cff_admission.mbt, modules/mb-font/font/font.mbt, modules/mb-font/font/cff_admission_wbtest.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/cff_admission.mbt
    @modules/mb-font/font/tables.mbt
    @modules/mb-font/font/cff_type2.mbt
    @modules/mb-font/font/cff_type2_bounds.mbt
  </read_first>
  <behavior>
    - Valid structure plus every valid glyph yields complete private AdmittedCff1 with descriptors, per-GID bounds, hmtx facts, and combined named charge.
    - A later glyph failure leaves the same budget and publishes no Font/bounds despite successful structural parsing.
    - Structure and VM charges preflight together and commit exactly once after final revision validation.
  </behavior>
  <action>
    Split the current structural admission's final preflight/commit/publish seam so parsing returns staged facts and structural charge. Invoke the Plan 03 all-GID staging entry point, combine checked structural/VM/bounds allocation facts, preflight once through the existing ledger, recheck revision, commit once, and only then construct complete private AdmittedCff1/closed outline source. Remove the Phase 104 single-glyph tracer field. Add a failing end-to-end fixture first and budget instrumentation proving zero versus exactly one commit.
  </action>
  <acceptance_criteria>
    - No code path commits structure before all glyphs and retained bounds succeed.
    - Complete admitted facts have exact GID/bounds cardinality and immutable source revision.
    - All Phase 104 structural/keying tests continue to pass through the refactored seam.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>CFF admission is one closed structural-plus-execution transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove metrics authority, exact resource boundaries, mutation, and first-error atomicity</name>
  <files>modules/mb-font/font/metrics.mbt, modules/mb-font/font/cff_hostile_fixture_wbtest.mbt, modules/mb-font/font/cff_admission_wbtest.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/metrics.mbt
    @modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
    @modules/mb-font/font/cff_admission.mbt
  </read_first>
  <behavior>
    - Deliberately different Type 2 and hmtx widths admit successfully while public advance/LSB remain the hmtx values.
    - Shared CFF bytes across two collection faces can retain identical geometry but different face-local hmtx metrics.
    - Exact named resource limits succeed; every one-short byte/call/work/point/contour/command/allocation/budget boundary fails Resource with zero charge.
    - Mutation and multi-fault fixtures preserve State → Resource → Capability → Data and smallest failing GID.
  </behavior>
  <action>
    Retain the minimum common metric facts needed for closed future dispatch without exposing a public CFF path. Add width-mismatch and shared-CFF/face-local-hmtx fixtures. Expand hostile fixtures to all named VM and retained-array units, exact/one-short caller budget, mutation before/during/final guard, malformed later GID, seac/CFF2 capability, and numeric Data. Assert no Font, no bounds, unchanged budget, and stable contexts for every failure.
  </action>
  <acceptance_criteria>
    - CFF width never becomes public metric authority or a mismatch rejection.
    - Every T2-02 and CFF-03 authority dimension has exact/one-short evidence.
    - All failure categories are atomic and deterministic.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native</automated>
  </verify>
  <done>The phase's metric, resource, mutation, error, and publication contracts are frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close native and portable regression evidence</name>
  <files>modules/mb-font/font/font_test.mbt, modules/mb-font/font/collection_test.mbt, modules/mb-font/font/font_wbtest.mbt</files>
  <read_first>
    @modules/mb-font/font/font_test.mbt
    @modules/mb-font/font/collection_test.mbt
    @.planning/phases/104-cff1-profile-and-bounded-data-model/104-VERIFICATION.md
  </read_first>
  <behavior>
    - Existing standalone and collection static-glyf public behavior remains byte/semantic compatible.
    - Valid private name-keyed and CID CFF1 admissions complete all-glyph validation without publishing Phase 106 outline APIs.
    - Native tests and all-target checks succeed from a clean worktree except the active auto-chain config marker.
  </behavior>
  <action>
    Add only the minimum public black-box regression coverage needed to prove unchanged APIs and existing glyf behavior; keep detailed CFF facts in white-box tests. Run the entire native suite, package checks, and all-target checks. Fix only Phase 105 regressions and record exact test totals/commands in the summary.
  </action>
  <acceptance_criteria>
    - No public API or glyf compatibility regression is introduced.
    - Phase 105 private name/CID admissions are fully exercised.
    - Native tests and `moon check --target all` pass.
  </acceptance_criteria>
  <verify>
    <automated>moon test --target native &amp;&amp; moon check --target all</automated>
  </verify>
  <done>Phase 105 is implementation-complete and ready for verification/review gates.</done>
</task>

</tasks>

<verification>
- `moon test --target native`
- `moon check --target native`
- `moon check --target all`
</verification>

<success_criteria>
- T2-01 is fully exercised through one production VM.
- T2-02 is bounded across all named authorities with deterministic errors.
- CFF-03 validates every glyph, retains truthful compact bounds, preserves hmtx authority, and publishes/charges nothing on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/105-bounded-type-2-validation-and-retained-metrics/105-04-SUMMARY.md`.
</output>

