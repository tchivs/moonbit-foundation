---
phase: 107-hostile-licensed-and-four-target-qualification
plan: "06"
type: execute
wave: 6
depends_on:
  - 107-05
files_modified:
  - benchmarks/moon.work
  - benchmarks/font-cff/cff_bench.mbt
  - scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1
  - scripts/quality/Test-BenchmarkQualification.ps1
  - policy/foundation.json
  - docs/benchmarks/mb-font-cff-native-release-baseline.md
autonomous: true
requirements:
  - CFF-06
must_haves:
  truths:
    - "Per D-19/D-20, four-target v3 records retain correctness digests only; timing is native release only and ordered Latin admission, CJK admission, Latin outline batch, CJK high-GID multi-FD outline batch."
    - "`benchmarks/moon.work` preserves all existing members and adds tracked local mb-font plus font-cff in one exact order; empty-cache frozen resolution cannot use a registry copy."
    - "Every measured Font open, optional collection open/open-face, and each outline operation constructs a fresh Budget with the same frozen limits; no mutable Budget is reused or reset."
    - "The supported benchmark selector is exactly `moon -C benchmarks bench font-cff/cff_bench.mbt --release --target native --frozen`."
    - "Per D-21/D-22, one excluded warmup plus seven retained complete captures bind exact fixture/workload/correctness/source/git/host/toolchain facts and six statistics under an observation-only claim."
    - "After Task 2 is committed, plan-level verification reruns the complete v3 lane and the sole clean read-only benchmark audit against the final policy/baseline seal."
  artifacts:
    - path: "benchmarks/moon.work"
      provides: "Exact tracked local workspace for benchmark consumers and mb-font."
    - path: "benchmarks/font-cff/cff_bench.mbt"
      provides: "Four ordered same-package native workloads over the single private carrier."
    - path: "scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1"
      provides: "Fixed capture, statistics, canonical render, local resolution, and read-only audit."
    - path: "scripts/quality/Test-BenchmarkQualification.ps1"
      provides: "Workspace, workload, budget, command, sample, statistic, identity, and claim negatives."
    - path: "policy/foundation.json"
      provides: "Final source/workspace/benchmark/baseline inventory and complete Phase 107 seal."
    - path: "docs/benchmarks/mb-font-cff-native-release-baseline.md"
      provides: "Committed observation-only one-warmup/seven-capture native record."
  key_links:
    - from: "benchmarks/moon.work"
      to: "benchmarks/font-cff/moon.mod.json"
      via: "exact local `./font-cff` member and tracked `../modules/mb-font` dependency member"
      pattern: "font-cff"
    - from: "benchmarks/font-cff/generated_cff_evidence.mbt"
      to: "benchmarks/font-cff/cff_bench.mbt"
      via: "same-package private payload, workload identities, fixed GIDs, and correctness facts"
      pattern: "cjk-high-gid-multi-fd-outline-batch"
    - from: "benchmarks/font-cff/cff_bench.mbt"
      to: "scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1"
      via: "exact path-scoped native release benchmark command"
      pattern: "moon -C benchmarks bench font-cff/cff_bench.mbt --release --target native --frozen"
    - from: "scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1"
      to: "docs/benchmarks/mb-font-cff-native-release-baseline.md"
      via: "clean one-plus-seven record and later non-writing reconstruction"
      pattern: "observation_only"
  prohibitions:
    - "No duplicate payload, production API/import/dependency/license change, runtime I/O/FFI, public fixture API, or registry-resolved mb-font."
    - "No caller-selected command/target/workload/order/GID/sample count, benchmark name filter, unscoped workspace bench, mutable Budget reuse/reset, or fixture construction inside measured closures."
    - "No timing in v3 semantic equality, cross-target/library timing comparison, threshold, CI timing gate, regression verdict, ranking, superiority, or marketing claim."
    - "No baseline from dirty/uncommitted sources and no read-only audit before the baseline/policy commit."
    - "No deferred font capability."
---

<objective>
Complete D-23 slice 3b: add the native observation-only benchmark, refresh final policy/source identities, record the clean baseline, and seal complete v3 evidence afterward.

Purpose: Close CFF-06 performance reproducibility without mixing timing into portable semantic equality or creating a second payload owner.
Output: Exact local workspace, four fresh-budget workloads, fixed capture/audit tooling, committed baseline, final policy inventory, complete v3 rerun, and clean read-only audit.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-05-SUMMARY.md
@benchmarks/font-cff/generated_cff_evidence.mbt
@scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1
@scripts/quality/Test-BenchmarkQualification.ps1
@docs/benchmarks/mb-svg-native-release-baseline.md
@policy/foundation.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tracked local workloads, fixed capture/audit contract, and final source policy inventory</name>
  <files>benchmarks/moon.work, benchmarks/font-cff/cff_bench.mbt, scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1, scripts/quality/Test-BenchmarkQualification.ps1, policy/foundation.json</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-05-SUMMARY.md
    - benchmarks/moon.work
    - benchmarks/ppm/moon.mod.json
    - benchmarks/ppm/moon.pkg
    - benchmarks/font-cff/moon.mod.json
    - benchmarks/font-cff/moon.pkg
    - benchmarks/font-cff/generated_cff_evidence.mbt
    - modules/mb-svg/svg/svg_bench.mbt
    - scripts/benchmarks/Invoke-SvgNativeBenchmarkBaseline.ps1
    - scripts/quality/Test-BenchmarkQualification.ps1
    - docs/benchmarks/mb-svg-native-release-baseline.md
    - policy/foundation.json
  </read_first>
  <behavior>
    - "Workspace members are exactly `../modules/mb-core,../modules/mb-color,../modules/mb-image,../modules/mb-font,./ppm,./font-cff`; prior members retain relative order."
    - "Empty-cache frozen verbose resolution proves every mb-font source path is tracked local; removing/substituting the member fails."
    - "Exactly four `@bench.T` tests appear in required order, reuse the private carrier, assert correctness before measurement, and measure only fresh-budget public operations."
    - "Every correctness call and every measured operation invokes one identical budget factory anew; no Budget is reused or reset across operations, iterations, collection stages, or glyphs."
    - "Capture accepts only the fixed path-scoped native release command, one complete excluded warmup, and seven complete retained sequences."
    - "Audit reconstructs from tracked inputs and the committed baseline only and never runs MoonBit or writes."
  </behavior>
  <action>
Per D-19/D-20/D-21, update `benchmarks/moon.work` to exact ordered members `../modules/mb-core`, `../modules/mb-color`, `../modules/mb-image`, `../modules/mb-font`, `./ppm`, `./font-cff`. Preserve prior members and place tracked dependencies before consumers. Add a workspace-resolution check using `--frozen`, verbose resolution, canonical paths, exact module name/version/digest, and a newly created empty external cache. Positive resolution must remain inside tracked `modules/mb-font`; member removal, path/name/version/digest substitution, alias/reparse, or external/cache source fails.

Create exactly four benchmark tests in source order: `bench cff/latin-full-admission`, `bench cff/cjk-full-admission`, `bench cff/latin-fixed-outline-batch`, `bench cff/cjk-high-gid-multi-fd-outline-batch`. Reuse `generated_cff_evidence.mbt`; add no byte literal or runtime file access. Construct immutable fixture bytes and expected digests outside measured closures. Define one exact budget factory with frozen qualification limits. Correctness outside `b.bench` calls this factory anew. Every measured `Font::open` constructs a new Budget inside its operation wrapper. Each outline operation in every measured batch constructs its own new Budget; if collection open/open-face observations remain, each separate operation also constructs a new Budget. Never share/reset one mutable instance. Keep only the public result with `b.keep`; digesting/assertion/formatting stays outside timing.

Implement the fixed PowerShell 5.1/7-compatible command exactly as `moon -C benchmarks bench font-cff/cff_bench.mbt --release --target native --frozen`. The package/file path is the only selector; callers cannot choose command, target, workload/order/GIDs, sample count, output, or policy. Require clean HEAD/tree, exact local resolution, exact moon/moonc/moonrun, PowerShell, native compiler/host facts, and correctness reconstructed from tracked corpus/assets/carrier/module/package/workspace/script sources. Do not read ignored v3 products as benchmark inputs.

Capture one complete warmup and exclude it, then exactly seven complete retained four-workload sequences. Store normalized raw output and SHA-256 per invocation. Compute mean, median, sample standard deviation with denominator six, minimum, maximum, and coefficient of variation. Render canonical LF UTF-8-no-BOM Markdown with closed audit data. `-Audit` reads tracked inputs plus committed baseline only, executes no MoonBit command, writes no file, and recomputes order/count/digests/raw hashes/statistics/claim.

Extend `Test-BenchmarkQualification.ps1` with positives and one-fact negatives for workspace order/local resolution/external fallback, duplicate payload, workload/GID/order, fresh budget for every correctness/measured operation, budget reuse/reset, fixture/workload/correctness/source/toolchain/host/git digest, warmup/capture count/completeness, raw hashes, each statistic, non-finite/negative values, exact command syntax, name-filter/unscoped invocation, ignored-v3 dependency, audit mutation, and forbidden claim fields.

After every source/workspace/script/test change above exists, refresh `policy/foundation.json` with exact final ordered paths/digests while preserving the Wave 5 interface/dependency/import/carrier/fixture/oracle/tool/v3 locks. Add the baseline path/schema as a pending exact Task 2 record, not an unchecked claim. Run policy, workspace, benchmark correctness, and contract-only tests. Commit Task 1 before recording.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1 -CheckWorkspaceResolution; moon -C benchmarks bench font-cff/cff_bench.mbt --release --target native --frozen; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; ./scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1 -ContractOnly; ./scripts/quality/Test-BenchmarkQualification.ps1 -ContractOnly; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-19/D-20 fixed native-only workload/GID/digest order and public-operation closures are exact."
    - "Every correctness/measured operation receives a fresh same-limit Budget; fixtures remain outside timing."
    - "Supported path-scoped bench syntax and tracked local mb-font resolution are executable."
    - "D-21/D-22 capture/audit requires exact identities, one excluded warmup, seven samples, raw hashes, six statistics, and observation-only interpretation."
    - "Final source/workspace/benchmark identities are policy-owned before Task 2 records."
  </acceptance_criteria>
  <done>All benchmark sources, workspace wiring, capture/audit contracts, negatives, and final source policy inventory are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Record the native observation baseline and finalize its policy identity</name>
  <precondition>`git status --porcelain --untracked-files=all` is empty and all five Task 1 files are tracked at HEAD.</precondition>
  <files>docs/benchmarks/mb-font-cff-native-release-baseline.md, policy/foundation.json</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-05-SUMMARY.md
    - scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1
    - docs/benchmarks/mb-svg-native-release-baseline.md
    - policy/foundation.json
  </read_first>
  <behavior>
    - "Clean committed sources record exact fixture/carrier/workload/source/git/host/toolchain identities, one excluded warmup, and seven retained complete sequences."
    - "The document contains raw hashes and mean/median/sample-standard-deviation/minimum/maximum/coefficient-of-variation for every workload."
    - "Policy replaces only the pending baseline record with the exact final path/digest/schema/observation-only identity."
    - "Any dirty state, local-resolution, identity/order/count/hash/statistic/command/claim failure aborts without replacing the document or policy."
  </behavior>
  <action>
Per D-21/D-22, assert the clean tracked precondition before any benchmark process and run `Invoke-CffNativeBenchmarkBaseline.ps1 -Record` exactly once. The script rechecks clean state and tracked-local mb-font immediately before warmup, reconstructs correctness from tracked inputs/carrier and fresh carrier output, records one excluded complete warmup plus seven retained complete sequences, and atomically writes only `docs/benchmarks/mb-font-cff-native-release-baseline.md`. The claim is exactly observation-only.

After the record validates in memory, replace the pending baseline entry in `policy/foundation.json` with its exact path, SHA-256, closed schema/claim identity, workload order, sample contract, and audit owner. Preserve all other policy bytes/order. Run the policy assertion but do not invoke `-Audit` inside this task; commit both files first. The plan-level verification below is the first and sole clean read-only audit and also reruns complete v3 against final policy.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; if(git status --porcelain --untracked-files=all){throw 'clean Task 1 commit required'}; ./scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1 -Record; $p='docs/benchmarks/mb-font-cff-native-release-baseline.md'; if(-not(Test-Path -LiteralPath $p -PathType Leaf)-or(Get-Item -LiteralPath $p).Length-eq 0){throw 'baseline missing'}; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json; git diff --check -- $p policy/foundation.json</automated>
  </verify>
  <acceptance_criteria>
    - "D-19 portable v3 evidence remains correctness-only while native release timing is separate."
    - "D-20 exact workload/GID order matches the tracked carrier."
    - "D-21 one warmup, seven captures, exact identities/raw hashes, and six statistics are present."
    - "D-22 no threshold, comparison, gate, verdict, ranking, superiority, or marketing claim is present."
    - "Task 2 is committed before complete v3 and read-only audit run."
  </acceptance_criteria>
  <done>The baseline and exact policy identity are committed and ready for final clean v3/audit verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries and STRIDE

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-06-01 | Spoofing | command/toolchain/module | high | mitigate | Fixed command, exact toolchain/host, empty-cache tracked-local resolution. |
| T-107-06-02 | Tampering | workload/correctness | high | mitigate | Tracked carrier/assets/GIDs/digests and correctness-before-timing. |
| T-107-06-03 | Tampering | Budget measurements | high | mitigate | One factory and static/dynamic negatives proving fresh construction per operation. |
| T-107-06-04 | Repudiation | dirty provenance | high | mitigate | Clean Task 1 commit and clean Task 2 precondition before capture. |
| T-107-06-05 | Tampering | samples/statistics | high | mitigate | Raw hashes, exact counts/order, six-statistic recomputation, final audit. |
| T-107-06-06 | Repudiation | interpretation | high | mitigate | Closed observation-only claim and forbidden-field negatives. |
| T-107-06-07 | Information disclosure | host facts | low | accept | Approved reproducibility facts only; no secrets or user paths. |

## OWASP ASVS L1 V1-V14

| Family | Applies | Control |
|---|---|---|
| V1 Architecture | Yes | Portable correctness, local workspace, native timing, policy, commit, and audit are separate. |
| V2 Authentication | N/A | No authentication. |
| V3 Session | N/A | No sessions. |
| V4 Access Control | Yes | Fixed tracked members and one baseline destination. |
| V5 Validation | Yes | Exact schemas/orders/counts/digests/numeric finiteness. |
| V6 Cryptography | Yes | SHA-256 binds inputs, sources, commands, raw output, baseline, and policy. |
| V7 Errors | Yes | Failed identity/command/capture aborts before replacement. |
| V8 Data Protection | Yes | Approved host facts only; no secrets/local paths. |
| V9 Communications | N/A | Capture/audit are local and offline. |
| V10 Malicious Code | Yes | No install/runtime dependency; empty-cache frozen local resolution. |
| V11 Business Logic | Yes | Fresh budgets, correctness-before-timing, one-plus-seven, observation-only. |
| V12 Files/Resources | Yes | Fixed workloads/samples, clean tree, atomic outputs. |
| V13 API/Web | N/A | No web API. |
| V14 Configuration | Yes | Exact workspace/command/workload/GID/toolchain/host/sample/claim values. |

No HIGH threat is accepted or left open.
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | Phase 107 goal | Native performance reproducibility and final exactly-four-target closure are sealed. | COVERED |
| REQ | CFF-06 | Declared Latin/CJK baseline, exact local workspace, final policy, v3, and audit are implemented. | COVERED |
| RESEARCH | Six-plan architecture | Wave 6 owns observation-only benchmark and final refresh/seal. | COVERED |
| CONTEXT | D-01/D-02 | Benchmark calls existing public API without runtime/API/dependency expansion. | COVERED |
| CONTEXT | D-03/D-04/D-05/D-06 | Frozen generated/public identities remain tracked correctness inputs. | COVERED |
| CONTEXT | D-07/D-08/D-09/D-10 | Exact assets/oracles and single carrier remain immutable benchmark inputs. | COVERED |
| CONTEXT | D-11/D-12/D-13/D-14 | Hostile/atomicity/glyf evidence remains in complete v3 final rerun. | COVERED |
| CONTEXT | D-15/D-16/D-17/D-18 | Complete v3/policy/CI/source seal is rerun after Task 2 commit. | COVERED |
| CONTEXT | D-19/D-20/D-21/D-22 | Native-only workloads, fresh budgets, one-plus-seven statistics, and observation-only audit are implemented here. | COVERED |
| CONTEXT | D-23 | Strict Wave 6 completes the third slice after all producer/consumer identities are frozen. | COVERED |
</source_audit>

<verification>
After Task 2 and its policy/baseline commit, run this as the plan-level clean gate:
  <automated>$ErrorActionPreference='Stop'; if(git status --porcelain --untracked-files=all){throw 'committed clean baseline and policy required'}; . ./scripts/quality/Assert-Policy.ps1; Assert-FontFoundationPolicy -PolicyPath ./policy/foundation.json; ./scripts/quality.ps1 -Lane FontQualification -EvidenceDirectory artifacts/release-qualification/font-v3; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; ./scripts/benchmarks/Invoke-CffNativeBenchmarkBaseline.ps1 -Audit; if($LASTEXITCODE-ne 0){exit $LASTEXITCODE}; if(git status --porcelain --untracked-files=all){throw 'final v3 and audit must be read-only'}</automated>
</verification>

<success_criteria>
The committed baseline is tracked-local, single-payload, fresh-budget, statistically complete, observation-only, policy-owned, followed by a complete equal v3 rerun, and reconstructible by the sole clean read-only audit.
</success_criteria>

<output>
Create `.planning/phases/107-hostile-licensed-and-four-target-qualification/107-06-SUMMARY.md` when done.
</output>
