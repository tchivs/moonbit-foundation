---
phase: 107-hostile-licensed-and-four-target-qualification
plan: "03"
type: execute
wave: 3
depends_on:
  - 107-02
files_modified:
  - scripts/fixtures/Generate-FontQualification.ps1
  - benchmarks/font-cff/moon.mod.json
  - benchmarks/font-cff/moon.pkg
  - benchmarks/font-cff/generated_cff_evidence.mbt
  - benchmarks/font-cff/cff_qualification_wbtest.mbt
autonomous: true
requirements:
  - CFF-06
must_haves:
  truths:
    - "Per D-10, one non-published evidence module owns exactly one package-private literal body for each licensed payload and reconstructs every standalone/collection wrapper portably."
    - "The evidence module resolves `tchivs/mb-font` only from the tracked local workspace under `--frozen`; a registry/cache copy cannot satisfy any check."
    - "`cff_qualification_wbtest.mbt` can see its package-private carrier but imports and calls only the existing public `tchivs/mb-font/font` API."
    - "The generator implements exact modes `-CheckPrivateOracleFacts` and `-CheckPrivateEvidenceMirrors` before any downstream verification invokes either mode."
    - "Generator-delimited private regions carry exact fact-only FD/oracle and hostile/mutation/glyf rows without licensed payload duplication or public exports."
  artifacts:
    - path: "benchmarks/font-cff/moon.mod.json"
      provides: "Non-published four-target evidence module with local mb-font dependency identity."
    - path: "benchmarks/font-cff/moon.pkg"
      provides: "Single package boundary importing public mb-font and benchmark support without exporting evidence."
    - path: "benchmarks/font-cff/generated_cff_evidence.mbt"
      provides: "Single package-private generated payload/fact/workload carrier."
    - path: "benchmarks/font-cff/cff_qualification_wbtest.mbt"
      provides: "Carrier-visible qualification scaffold that calls only imported public mb-font APIs."
    - path: "scripts/fixtures/Generate-FontQualification.ps1"
      provides: "Canonical rendering, private-region ownership, exact private check modes, single-owner scan, and temporary local-workspace verification."
  key_links:
    - from: "scripts/fixtures/Generate-FontQualification.ps1"
      to: "benchmarks/font-cff/generated_cff_evidence.mbt"
      via: "byte-canonical generated rendering from tracked corpus/assets/provenance"
      pattern: "Generated by scripts/fixtures/Generate-FontQualification.ps1"
    - from: "benchmarks/font-cff/generated_cff_evidence.mbt"
      to: "benchmarks/font-cff/cff_qualification_wbtest.mbt"
      via: "same-package private carrier access followed by public-only font calls"
      pattern: "cff_qualification_wbtest.mbt"
    - from: "scripts/fixtures/Generate-FontQualification.ps1"
      to: "benchmarks/font-cff/generated_cff_evidence.mbt"
      via: "delimited fact-only private projections checked by `-CheckPrivateOracleFacts` and `-CheckPrivateEvidenceMirrors`"
      pattern: "CheckPrivateOracleFacts"
  prohibitions:
    - "No licensed CFF payload or evidence dependency inside `modules/mb-font`."
    - "No second payload literal, public fixture/evidence export, runtime file/network/subprocess/FFI/tool access, or registry fallback."
    - "No consumer-authored expected fact, production-output oracle, or private mb-font call from the evidence package."
    - "No production/API/dependency/license/policy/CI/docs/benchmark-baseline expansion."
---

<objective>
Complete D-23 slice 1c: create the non-published evidence module, single portable mirror, and complete generator-owned private mirror modes.

Purpose: Establish one portable payload owner and all producer-before-consumer seams before public/private assertions are expanded in Wave 4.
Output: A locally resolved evidence module/package, one generated carrier, one carrier-visible public-API test scaffold, and fully implemented generator private checks.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-CONTEXT.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
@.planning/phases/107-hostile-licensed-and-four-target-qualification/107-02-SUMMARY.md
@fixtures/font/cff-qualification-cases.json
@fixtures/font/source-sans-3.052r/qualification.json
@fixtures/font/source-han-serif-2.003r/qualification.json
@scripts/fixtures/Generate-FontQualification.ps1
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the non-published evidence module resolves the tracked public font package</name>
  <files>scripts/fixtures/Generate-FontQualification.ps1, benchmarks/font-cff/moon.mod.json, benchmarks/font-cff/moon.pkg, benchmarks/font-cff/cff_qualification_wbtest.mbt</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-RESEARCH.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-PATTERNS.md
    - scripts/fixtures/Generate-FontQualification.ps1
    - benchmarks/ppm/moon.mod.json
    - benchmarks/ppm/moon.pkg
    - modules/mb-font/moon.mod.json
    - modules/mb-font/font/moon.pkg
  </read_first>
  <behavior>
    - "The module is non-published, supports js/wasm/wasm-gc/native, and declares `tchivs/mb-font@0.1.0` only as the system under test."
    - "A link-free temporary workspace explicitly names tracked mb-core, mb-font, and font-cff roots; frozen resolution proves the mb-font root and module digest."
    - "Empty-cache execution succeeds with the local member and fails when that member is removed, substituted, renamed, or digest-drifted."
    - "The scaffold imports only public font APIs and exports no fixture/evidence symbol."
  </behavior>
  <action>
Per D-01/D-10, create non-published module `moonbit-foundation/font-cff-evidence` at version `0.0.0`, preferred target native, supported targets `+js+wasm+wasm-gc+native`, and named dependency `tchivs/mb-font: 0.1.0`. Create one package importing `tchivs/mb-font/font` plus `moonbitlang/core/bench` for the later native workloads; declare no public exports. Add `cff_qualification_wbtest.mbt` as a concrete compile-and-resolution tracer whose elevated visibility is confined to this package and whose system-under-test references are only imported public font symbols.

Extend `scripts/fixtures/Generate-FontQualification.ps1` with `-CheckEvidencePackage -Target <target>`. It creates a fresh link-free temporary `moon.work` containing explicit canonical paths for the tracked mb-core, mb-font, and font-cff modules, uses a new empty task-specific external module cache, runs `--frozen`, and proves every resolved `tchivs/mb-font` source canonicalizes under this worktree's `modules/mb-font` root with the exact module manifest digest. It removes the overlay afterward. Add negatives for omitted/substituted members, registry/cache fallback, path alias/reparse, wrong name/version/digest, public export, and private mb-font symbol reference.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckEvidencePackage -Target native; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPublicPrivateBoundary; moon fmt --check benchmarks/font-cff/cff_qualification_wbtest.mbt; $fmtExit=$LASTEXITCODE; if ($fmtExit -ne 0) { exit $fmtExit }; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-01 the production module and its exact public/dependency surface remain unchanged."
    - "D-10 the evidence package is non-published and can consume only the tracked public mb-font module."
    - "No registry/cache/private-runtime path can satisfy the tracer."
  </acceptance_criteria>
  <done>The evidence package resolves the tracked public system under test through a deterministic local workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate the single payload carrier and close every private mirror mode before consumption</name>
  <files>scripts/fixtures/Generate-FontQualification.ps1, benchmarks/font-cff/generated_cff_evidence.mbt, benchmarks/font-cff/cff_qualification_wbtest.mbt</files>
  <read_first>
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-01-SUMMARY.md
    - .planning/phases/107-hostile-licensed-and-four-target-qualification/107-02-SUMMARY.md
    - fixtures/font/cff-qualification-cases.json
    - fixtures/font/source-sans-3.052r/qualification.json
    - fixtures/font/source-han-serif-2.003r/qualification.json
    - scripts/fixtures/Generate-FontQualification.ps1
    - benchmarks/font-cff/cff_qualification_wbtest.mbt
    - modules/mb-font/font/generated_font_qualification_test.mbt
    - modules/mb-font/font/cff_cid_fixture_wbtest.mbt
    - modules/mb-font/font/cff_hostile_fixture_wbtest.mbt
  </read_first>
  <behavior>
    - "The carrier contains one literal body per licensed OTF and no other repository file contains either complete payload or generated chunk sequence."
    - "Generated name/CID/shared-CFF bytes, public expected facts, hostile rows, compatibility rows, and workload identities render in canonical producer order."
    - "Private FD/oracle and hostile/mutation/glyf projections are generator-delimited, fact-only, and reject hand edits or consumer-completed fields."
    - "The exact private modes exist and pass before Wave 4 invokes them."
    - "The evidence wbtest sees the carrier privately but calls only public mb-font APIs and exports nothing."
  </behavior>
  <action>
First, before invoking either mode anywhere, implement exact generator switches `-CheckPrivateOracleFacts` and `-CheckPrivateEvidenceMirrors`. `-CheckPrivateOracleFacts` reconstructs the delimited private FD/local-environment oracle projection from hand-derived and two-reader-agreed corpus rows, checks exact carrier region markers and ordering, and rejects production-output-derived expectations. `-CheckPrivateEvidenceMirrors` reconstructs the delimited hostile, mutation, publication, B8, precedence, and static-glyf projections, checks exact source assertion locators, and rejects missing/extra/reordered or consumer-completed fields. Both modes are offline, read-only, byte-compare exact, and implemented before any verification command below calls them. Also close the exact target-path/marker/rendering contract that Wave 4 uses to materialize these same projections into existing `mb-font` white-box files; do not mutate those downstream files in this plan.

Then generate `generated_cff_evidence.mbt` from the tracked canonical assets and facts. Store exactly one package-private byte literal body for Source Sans and one for Source Han; construct every standalone and collection wrapper from those bodies. Include deterministic generated name/CID/shared-CFF bytes and closed expected public facts, exact hostile tables, compatibility facts, target/workload identities, fixed GID lists, and correctness digests. Do not declare a `pub` fixture/evidence symbol.

Finally, add the carrier-boundary assertion to `cff_qualification_wbtest.mbt`. Extend normal generator and `-Check` orchestration to reconstruct assets, provenance, manifest facts, carrier regions, and the closed downstream private-region render contract in memory. Add repository-wide single-owner scans keyed by exact payload digests and generated chunk identities, plus negatives for duplicate payload, hand editing, private mode omission, region-marker drift, public export, runtime I/O/FFI/process seam, and production dependency/import change.
  </action>
  <verify>
    <automated>$ErrorActionPreference='Stop'; ./scripts/fixtures/Generate-FontQualification.ps1; ./scripts/fixtures/Generate-FontQualification.ps1 -Check; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPrivateOracleFacts; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPrivateEvidenceMirrors; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckSinglePayloadOwner; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckEvidencePackage -Target native; ./scripts/fixtures/Generate-FontQualification.ps1 -CheckPublicPrivateBoundary; moon fmt --check benchmarks/font-cff/generated_cff_evidence.mbt; $generatedFmtExit=$LASTEXITCODE; if ($generatedFmtExit -ne 0) { exit $generatedFmtExit }; moon fmt --check benchmarks/font-cff/cff_qualification_wbtest.mbt; $wbtestFmtExit=$LASTEXITCODE; if ($wbtestFmtExit -ne 0) { exit $wbtestFmtExit }; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "D-03-D-06 all generated bytes/facts are rendered from the canonical producer without schema completion."
    - "D-10 one package-private carrier owns licensed payloads and all wrappers reuse it."
    - "The two exact private check modes are implemented and passing before any downstream plan invokes them."
    - "No licensed bytes or fixture API enter production mb-font."
  </acceptance_criteria>
  <done>The single portable carrier and all generator-owned private mirror contracts are complete and ready for Wave 4 assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries and STRIDE

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-03-01 | Spoofing | local module resolution | high | mitigate | Empty-cache frozen explicit workspace and canonical tracked-root/digest checks. |
| T-107-03-02 | Tampering | generated carrier | high | mitigate | Exact generator reconstruction, single-owner digest/chunk scans, no hand edits. |
| T-107-03-03 | Information disclosure | private facts | high | mitigate | Package-private carrier and generator-delimited fact-only mb-font regions. |
| T-107-03-04 | Elevation | public/private boundary | high | mitigate | No exports and no private mb-font reference from the evidence package. |
| T-107-03-05 | Repudiation | expected facts | high | mitigate | Canonical source traces and exact private mode byte comparisons. |

## OWASP ASVS L1 V1-V14

| Family | Applies | Control |
|---|---|---|
| V1 Architecture | Yes | Non-published evidence carrier is separate from production mb-font. |
| V2 Authentication | N/A | No authentication. |
| V3 Session | N/A | No sessions. |
| V4 Access Control | Yes | Package-private symbols and explicit local workspace members. |
| V5 Validation | Yes | Exact regions, schemas, orders, roots, and digests. |
| V6 Cryptography | Yes | SHA-256 binds assets, manifests, carrier, modules, and sources. |
| V7 Errors | Yes | Drift aborts before generated output replacement. |
| V8 Data Protection | Yes | Licensed payloads have one non-production owner. |
| V9 Communications | N/A | Generation/check/runtime are offline. |
| V10 Malicious Code | Yes | No install or ambient executable in portable checks. |
| V11 Business Logic | Yes | Producer-before-consumer modes and public/private separation. |
| V12 Files/Resources | Yes | Link-free temporary workspace and fixed tracked inputs. |
| V13 API/Web | N/A | No web API. |
| V14 Configuration | Yes | Exact module, targets, dependency, package imports, modes, and region IDs. |

No HIGH threat is accepted or left open.
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Coverage | Status |
|---|---|---|---|
| GOAL | Phase 107 goal | Portable evidence now has one reproducible payload/fact owner. | COVERED |
| REQ | CFF-06 | Generated/licensed portable mirror and local public-module resolution are implemented. | COVERED |
| RESEARCH | Six-plan architecture | Wave 3 owns the evidence module, carrier, and complete generator private modes. | COVERED |
| CONTEXT | D-01/D-02 | Qualification-only production/API boundary is enforced. | COVERED |
| CONTEXT | D-03/D-04/D-05/D-06 | Canonical generated contracts become one portable carrier without changing their truth. | COVERED |
| CONTEXT | D-07/D-08/D-09/D-10 | Wave 2 assets/oracles feed one licensed payload owner with no runtime host access. | COVERED |
| CONTEXT | D-11/D-12/D-13/D-14 | Exact private fact regions are generated here and executed in Wave 4. | COVERED |
| CONTEXT | D-15/D-16/D-17/D-18 | Carrier/source identities are frozen for Wave 5 evidence/policy. | COVERED |
| CONTEXT | D-19/D-20/D-21/D-22 | Workload correctness facts enter the carrier; timing remains Wave 6 only. | COVERED |
| CONTEXT | D-23 | Strict Wave 3 completes all producer seams before assertion consumers. | COVERED |
</source_audit>

<verification>
Both exact private modes, generator reconstruction, single-owner scan, public/private boundary, local resolution, formatting, and whitespace checks pass.
</verification>

<success_criteria>
Wave 4 can consume the carrier and private fact regions read-only; no downstream verifier must create a generator mode, mirror schema, payload owner, or module-resolution rule.
</success_criteria>

<output>
Create `.planning/phases/107-hostile-licensed-and-four-target-qualification/107-03-SUMMARY.md` when done.
</output>
