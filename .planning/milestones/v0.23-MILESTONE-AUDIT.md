---
milestone: v0.23
audited: 2026-07-24T01:00:00+08:00
status: passed
scores:
  requirements: 4/4
  phases: 3/3
  integration: 3/3
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
---

# v0.23 Low-Bit Grayscale PNG Encode — Milestone Audit

**Audit verdict: PASS — all v0.23 requirements, integration paths, and the independent four-target final gate are evidenced.**

## Scope and method

The audit covers Phase 73 (explicit eager profiles), Phase 74 (caller-buffered profiles), and Phase 75 (qualification), against GRAYPACK-01 through GRAYPACK-04. It read each PLAN, SUMMARY, and VERIFICATION report, but treated their pass claims as hypotheses. The source, public tests, commit scope, and available runtime evidence were checked directly.

| Check | Result | Direct evidence |
|---|---|---|
| Phase verification artifacts | PASS | `73-VERIFICATION.md`, `74-VERIFICATION.md`, and `75-VERIFICATION.md` exist and each records a completed phase verification. |
| Phase implementation scope | PASS | `53bbfd5`, `713ddf6`, and `f91c3fd` change the planned encoder/stream/test surfaces only; all three patches pass `git diff --check`. |
| Stub/debt scan | PASS | No `TBD`, `FIXME`, `XXX`, placeholder, or low-bit strategy/factory expansion was found in the six modified PNG source/test files. |
| Fresh native package run | PASS | `moon -C modules/mb-image test png --target native --frozen --no-parallelize --quiet` exited `0`. |
| Fresh all-target final gate | PASS | `moon -C modules/mb-image test png --target all --frozen --target-dir D:\\source\\moonbit-foundation-v019\\.moon-audit-v023-264` completed in the isolated build directory: wasm, wasm-gc, js, and native each reported 264/264 passed. |

## Requirement coverage

| Requirement | Assigned phase | Source and test evidence | Verdict |
|---|---:|---|---|
| GRAYPACK-01 | 73 | `PngEncoder::new_gray1/2/4` are fixed Stored/None/non-interlaced selectors in `modules/mb-image/png/png.mbt:226-254`. Exact 1/2/4-bit code mapping, checked packed row arithmetic, and MSB-first packing are implemented in `encode.mbt:55-115` and `541-563`. Phase 73 tests include literal odd-width packed scanlines. | PASS |
| GRAYPACK-02 | 73 | Low-bit admission requires canonical opaque Gray/U8 (`encode.mbt:173-183`), rejects invalid levels in `_png_packed_gray_code`, and occurs at the profile preflight seam before machine construction. Phase 73's eager tests and Phase 74's chunk tests exercise invalid levels and exhausted work budgets. | PASS |
| GRAYPACK-03 | 74 | `PngChunkEncoder::new_gray1/2/4` each calls the single `PngEncodeMachine::new_with_profile` with fixed Stored/None/non-interlaced arguments (`stream_encode.mbt:23-72`). `stream_encode_test.mbt:4764-4898` drives zero/one/ragged leases, compares accepted bytes with eager output, checks unaccepted tails, atomic admission, and replayed typed terminals. | PASS |
| GRAYPACK-04 | 75 | `png_test.mbt:14-108` embeds three complete, CRC-bearing external Type-0 fixtures; public generic decode asserts RGB8 pixels and full source consumption; public eager encoders must equal each full literal. The isolated final four-target gate independently reported 264/264 on wasm, wasm-gc, js, and native. | PASS |

## Cross-phase integration and end-to-end flows

| Flow | Cross-phase wiring checked | Verdict |
|---|---|---|
| Exact eager low-bit PNG | Phase 73 selectors select `Gray1`/`Gray2`/`Gray4`; the profile reaches `PngEncodeMachine::new_with_profile`, whose profile facts drive the one encoder traversal. `stream_encode.mbt:821-845` confirms this construction seam consumes preflight output rather than a parallel transport. | PASS |
| Caller-buffered parity and terminal semantics | Phase 74's public factories use the same profile/machine seam; hostile-drain tests call the public chunk constructors and compare their accumulated output with the public eager selector. | PASS |
| Independent decode/eager qualification | Phase 75 static vectors are not encoder-produced; they first cross `@codec.ImageDecoder::decode`, then are compared byte-for-byte at the public eager writer boundary. This closes the Phase 73/74 self-oracle risk. | PASS |

## Report cross-check

The phase reports claim 260/260 (Phase 73), 263/263 (Phase 74), and 264/264 (Phase 75) for their respective package gates. Those counts are internally plausible as test coverage accumulated, but are not sufficient audit evidence by themselves. The source-backed audit accepts the native exit result and marks the unobserved final all-target count as unresolved rather than promoting it to PASS.

## Conclusion

Phase 73, 74, and 75 are substantively present and correctly wired. There are no observed implementation gaps, cross-phase integration failures, or outstanding evidence gaps. The isolated final package gate independently confirms 264/264 passing tests on every supported target, so v0.23 receives a clean PASS.
