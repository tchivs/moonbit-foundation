---
phase: 21-bounded-png-decode-and-deflate
plan: "02"
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - modules/mb-image/png/png_test.mbt
autonomous: true
requirements: [PNG-04, PNG-05]
must_haves:
  truths:
    - "The generated Phase-20 structural matrix is executed by the public PngDecoder on every portable target, rather than remaining an uncalled helper."
    - "Structural records whose Phase-21 terminal behavior is data truncation assert that exact current error; only the preserved opaque-metadata boundary may assert its capability result."
    - "The current bounded PNG decoder evidence remains green for fixture freshness, all four targets, and the isolated PNG quality lane."
  artifacts:
    - "modules/mb-image/png/png_test.mbt contains a named test that reaches every generated structural record."
  key_links:
    - "The generated structural corpus reaches PngDecoder through the public ImageDecoder contract and verifies deterministic terminal errors without exposing a DecodeResult."
---

<objective>
Close the Phase-21 verification report's remaining evidence gap: the retained structural regression matrix exists but is not invoked by the four-target PNG test suite.

Purpose: Preserve the Phase-20 safety guarantees while reconfirming the active bounded pure-MoonBit decode path without changing PNG scope, the public API, encoding, streaming, release work, or later colour features.
Output: One reachable public regression test and current four-target verification evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-CONTEXT.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-RESEARCH.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-VERIFICATION.md
@.planning/phases/22-canonical-png-encode-and-portable-evidence/22-VERIFICATION.md
@.planning/phases/25-portable-colour-conformance-evidence/25-01-SUMMARY.md

<interfaces>
`PngDecoder` remains the sole decoder surface. The private generated fixture table is consumed only through `@codec.ImageDecoder::decode`; a failing decode returns `Err` and therefore has no `DecodeResult` to expose.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the retained structural corpus a strict public regression test</name>
  <files>modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - Every row from `_generated_png_public_cases()` is invoked through `PngDecoder` by a MoonBit `test`, not an ordinary uncalled function.
    - Rows that fail before DEFLATE retain their declared category, code, context, immutable caller-budget state, and empty diagnostics assertions.
    - Rows inherited from Phase 20 that reach an empty or invalid IDAT assert the active Phase-21 data-truncation outcome exactly; opaque-metadata preservation retains its explicit capability assertion.
  </behavior>
  <action>Promote `_png_legacy_phase20_matrix` in `modules/mb-image/png/png_test.mbt` to a named MoonBit test and retain its public `ImageDecoder::decode` invocation for every generated record. Per D-03 and D-04, keep the test at the public PngDecoder boundary and require the existing exact terminal category/code/context assertions; do not reintroduce the former broad capability fallback or inspect private transport state. Preserve the existing immutable-budget and diagnostics checks for preflight rows, and do not alter fixture bytes, encoder behavior, streaming APIs, colour semantics, or production decoder files.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The current four-target suite reports the named structural-corpus test, and every generated Phase-20 structural record has a strict public Phase-21 terminal assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated hostile PNG bytes → public decoder test | Fixture data must exercise the current PngDecoder result/error boundary, not an uncalled helper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Tampering | structural regression evidence | medium | mitigate | Convert the generated structural-loop helper into a named all-target public test with exact error assertions. |
| T-21-06 | Repudiation | fixture verification | low | mitigate | Run generator freshness, portable package tests, and the isolated PNG lane after the test becomes reachable. |
</threat_model>

<source_audit>

| Source | Required item | Coverage |
|--------|---------------|----------|
| GOAL | Bounded deterministic public PNG decode continues to reject malformed structural input without result visibility. | Task 1 |
| REQ | PNG-04 supported RGB/RGBA filter decode remains protected by reachable public regression evidence. | Task 1 verification |
| REQ | PNG-05 structural/IDAT failure behavior remains deterministic through PngDecoder. | Task 1 |
| RESEARCH | Preserve Phase-20 safety evidence while refactoring the decoder. | Task 1 |
| CONTEXT | D-03 keep the existing eager PngDecoder contract private internally. | Task 1 |
| CONTEXT | D-04 terminal checks precede image-result visibility. | Task 1 |
| CONTEXT | D-06 independently generated hostile evidence runs on js, wasm, wasm-gc, and native. | Task 1 verification |
</source_audit>

<verification>
Run `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`, `moon -C modules/mb-image test png --target all --frozen`, and `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
</verification>

<success_criteria>
The historical Phase-20 structural matrix is a reachable strict public regression test, and current fixture freshness plus four-target PNG evidence pass without adding scope beyond PNG-04 and PNG-05.
</success_criteria>

<output>
Create `.planning/phases/21-bounded-png-decode-and-deflate/21-02-SUMMARY.md` when done.
</output>
