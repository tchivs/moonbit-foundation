---
phase: 21-bounded-png-decode-and-deflate
plan: "03"
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - modules/mb-image/png/png_test.mbt
  - modules/mb-image/png/structural.mbt
autonomous: true
gap_closure: true
requirements: [PNG-04, PNG-05]
must_haves:
  truths:
    - "Every retained Phase-20 structural record has one explicit current terminal outcome at the public PngDecoder boundary; no test chooses its expected error after seeing the decoder result."
    - "Structural failures still win before transport/decode, current semantic ordering and fixed-length failures report their precise PNG context, and empty active IDAT streams report the exact zlib terminal without exposing an image."
    - "The downstream generated decode corpus continues to prove post-DEFLATE output/work resource failures and legal stored/fixed/dynamic RGB/RGBA decoding across js, wasm, wasm-gc, and native."
  artifacts:
    - path: "modules/mb-image/png/png_test.mbt"
      provides: "Explicit Phase-20-to-current decoder outcome classifier and all-target public assertions"
    - path: "modules/mb-image/png/structural.mbt"
      provides: "Ordered PNG framing and post-IDAT chunk terminals used by the public classifier"
  key_links:
    - from: "modules/mb-image/png/png_test.mbt"
      to: "modules/mb-image/png/generated_vectors.mbt"
      via: "Each generated structural record is classified before ImageDecoder::decode(PngDecoder) and compared with its exact current category, code, context, and budget policy."
      pattern: "_generated_png_public_cases"
    - from: "modules/mb-image/png/structural.mbt"
      to: "modules/mb-image/png/png_test.mbt"
      via: "PngIdatSource and structural transport ordering errors are exercised only through the public eager decoder."
      pattern: "PngIdatSource|_png_read_stream_transport"
---

<objective>
Close the Phase-21 audit drift by replacing outcome-dependent legacy assertions with a maintainable, explicit current-outcome map for the generated Phase-20 structural corpus.

Purpose: Retain the Phase-20 safety corpus while accurately distinguishing present structural, semantic, transport, capability, and decoded-resource behavior after Phase 21 and later colour work.
Output: A strict public classifier/test, retained genuine production ordering guards, and fresh generated/all-target PNG evidence.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-CONTEXT.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-RESEARCH.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-VERIFICATION.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-02-PLAN.md
@.planning/phases/20-png-structural-safety-gate/20-VERIFICATION.md
@.planning/phases/20-png-structural-safety-gate/20-02-SUMMARY.md
@.planning/phases/23-png-colour-declaration-and-srgb-semantics/23-CONTEXT.md
@.planning/phases/24-bounded-non-srgb-and-icc-preservation/24-CONTEXT.md
@.planning/phases/25-portable-colour-conformance-evidence/25-CONTEXT.md

<interfaces>
`PngDecoder` remains the only PNG decoder surface. Generated structural records remain private fixture data and are reached only through `@codec.ImageDecoder::decode`; an `Err` has no `DecodeResult` to inspect. The existing generated decode corpus is the separate authority for successful raster output and resource failure after real DEFLATE processing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the current-outcome map before asserting the legacy structural corpus</name>
  <files>modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - Pre-transport framing, CRC, order, and preflight records retain the generated exact category, code, context, immutable-budget, and diagnostics contract.
    - `semantic-crc` has the current semantic terminal (`Data`, `InvalidEncoding`, `png-semantic-chunk`), while other fixed-length/semantic records retain their declared precise PNG context.
    - Exactly `ancillary-preserve` retains the opaque-metadata capability boundary; the legacy records that now enter active IDAT decoding assert `Data`, `InvalidEncoding`, and `zlib-truncated` from an explicitly selected decode-capable budget.
    - At-ceiling structural records that now enter DEFLATE do not claim the old pre-decode immutable-budget invariant; below-limit preflight records still assert `BudgetExceeded`, unchanged caller budget, and empty diagnostics.
  </behavior>
  <action>Create a private expectation record/enum and one classifier in `modules/mb-image/png/png_test.mbt` that is evaluated before `ImageDecoder::decode`. Replace the result-dependent branches and the broad `requires_phase21_decode` condition with this map. The map must have named classes for (1) inherited pre-transport structural errors, (2) current semantic/fixed-length errors, (3) active-IDAT zlib terminals, (4) the opaque-metadata capability boundary, and (5) resource semantics after structural preflight. Per D-03, keep all evidence at `PngDecoder`; do not expose transport internals. Per D-04 and D-05, make each class declare exact category/code/context and whether it uses the fixture budget or a decode-capable budget, and assert no result is exposed on every failure. Explicitly enumerate `semantic-crc`, `ancillary-preserve`, `strict-eof-trailing-at-max`, every former `deflate-and-raster-pending` record, and the paired at-ceiling/below-limit resource records; do not infer the expected class from the error returned by the decoder. Preserve D-01/D-02 coverage by leaving the existing generated valid stored/fixed/dynamic and all-filter raster assertions strict, and preserve D-06 by keeping their existing all-target public test path. Do not alter generated bytes, public API, PNG encoding, streaming API surface, or later colour metadata/capability behavior.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The named structural-corpus test has one precomputed exact expectation per generated record, and neither an observed error context nor a generic capability fallback can make a record pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain only proven structural ordering distinctions and verify generated evidence</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/png_test.mbt</files>
  <behavior>
    - `ihdr-not-first` remains `png-ihdr-first`, `duplicate-ihdr` remains `png-ihdr-order`, and a malformed PLTE semantic route reports `png-semantic-chunk` instead of a generic downstream terminal.
    - A recognised colour declaration after IDAT remains a colour-order error, while legal later Phase-23/24 declarations and their typed non-sRGB capability boundaries retain their existing behavior.
    - The generated decode corpus continues to assert exact post-DEFLATE output/work budget errors and split-IDAT equivalence independently of the legacy empty-IDAT corpus.
  </behavior>
  <action>Review the current uncommitted ordering changes in `modules/mb-image/png/structural.mbt` against the explicit public map, then retain the genuine guards: validate initial IHDR chunk length/type form before accepting it, reject a subsequent IHDR in both pre-IDAT and active-IDAT traversal, and distinguish a post-IDAT PLTE semantic violation from a generic zlib terminal. Keep these guards only with their exact public assertions; correct a guard only if the generated/public test proves its terminal conflicts with the PNG ordering contract. Do not revert these current fixes, weaken an assertion, or roll back later Phase 23–25 colour parsing, retained non-sRGB metadata, typed operation/encoder capability boundaries, or post-decode resource behavior. Per D-04/D-05, retain `PngIdatSource::finish` and the no-result terminal sequence. Per D-06, run both fixture generators, the full public PNG package test on all four targets, and the isolated Png quality lane so generated structural, decode, colour, split-boundary, and post-decode budget evidence are all current.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check; pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The retained production ordering guards have exact public evidence, generated structural/decode fixtures are fresh, and all portable targets plus the isolated Png lane pass without reducing current decode or colour functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated hostile PNG bytes → PngDecoder | Untrusted structural, compressed, and colour-order inputs must produce a deterministic terminal before image visibility. |
| legacy fixture expectation → current decoder | Historical Phase-20 terminal names must not silently broaden or overwrite current Phase-21–25 semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Tampering | legacy structural regression test | high | mitigate | Precompute one exact stage-specific expectation for every record; reject result-dependent expectation selection. |
| T-21-08 | Denial of service | active IDAT / decoded output budgets | high | mitigate | Keep distinct preflight immutable-budget assertions and generated real-DEFLATE output/work limit evidence on all targets. |
| T-21-09 | Tampering | PNG ordering parser | medium | mitigate | Retain verified IHDR, PLTE, and colour-after-IDAT ordering terminals and exercise them through PngDecoder. |
| T-21-10 | Elevation | image result boundary | medium | mitigate | Assert error-only outcomes through ImageDecoder and retain finish-before-result behavior per D-04. |
</threat_model>

<source_audit>

| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | — | Bounded deterministic eager PNG decode preserves structural safety through the real current pipeline. | 21-03 | COVERED | Both tasks retain exact current terminals and no-result behavior. |
| REQ | PNG-04 | Supported RGB/RGBA all-filter decoding remains evidence-backed. | 21-03 | COVERED | Task 1 preserves strict generated valid raster assertions. |
| REQ | PNG-05 | Stored/fixed/dynamic IDAT decoding and deterministic hostile rejection remain bounded. | 21-03 | COVERED | Both tasks distinguish active zlib terminals from structural failures. |
| RESEARCH | — | Streaming IDAT source, terminal IEND/EOF checks, exact resource contracts, and no public streaming API. | 21-03 | COVERED | Tasks retain PngIdatSource/finish and public eager evidence. |
| CONTEXT | D-01 | Legal stored/fixed/dynamic DEFLATE across IDAT boundaries. | 21-03 | COVERED | Task 1 retains generated decode corpus assertions. |
| CONTEXT | D-02 | All five filters and RGB8/RGBA8 contracts. | 21-03 | COVERED | Task 1 retains all-filter raster assertions. |
| CONTEXT | D-03 | Existing eager decoder contract only. | 21-03 | COVERED | Task 1 keeps fixture access at PngDecoder. |
| CONTEXT | D-04 | No result before zlib, raster, IEND, and EOF success. | 21-03 | COVERED | Both tasks assert error-only terminals and preserve finish sequencing. |
| CONTEXT | D-05 | Bounded deterministic DEFLATE error behavior. | 21-03 | COVERED | Task 1 maps exact zlib and resource terminals. |
| CONTEXT | D-06 | Independent fixtures on four targets. | 21-03 | COVERED | Task 2 runs both generators, all-target tests, and Png lane. |
</source_audit>

<verification>
Run `pwsh -NoProfile -File scripts/fixtures/Generate-PngStructuralVectors.ps1 -Check`, `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`, `moon -C modules/mb-image test png --target all --frozen`, and `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`.
</verification>

<success_criteria>
The 89-record legacy structural corpus has a maintained explicit current-outcome classification, production ordering semantics are exact and preserved, and generated public PNG evidence passes on js, wasm, wasm-gc, and native.
</success_criteria>

<output>
Create `.planning/phases/21-bounded-png-decode-and-deflate/21-03-SUMMARY.md` when done.
</output>
