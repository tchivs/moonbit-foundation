---
phase: 27-public-png-chunk-decoder
plan: "03"
type: execute
wave: 3
depends_on: [27-02]
files_modified:
  - modules/mb-image/png/stream_decode_wbtest.mbt
  - modules/mb-image/png/stream_decode.mbt
autonomous: true
gap_closure: true
requirements: [PNGS-02]
must_haves:
  truths:
    - "finish() proves zlib-truncated precedence from real fixed-Huffman-token, dynamic-tree, and dynamic-match pause states even though the containing IDAT CRC and IEND are also incomplete."
    - "After an otherwise complete raster, a zero-length wrong fourth chunk-type byte and a completed zero-length non-IEND type both remain pending until finish(), which returns png-iend-type with exact completed accounting and sticky replay."
    - "The added public/private classifier pairs pass on native, wasm, wasm-gc, and js without changing eager Reader EOF behavior or exposing an image before strict completion."
  artifacts:
    - path: modules/mb-image/png/stream_decode_wbtest.mbt
      provides: "Executable fixed/dynamic inflater and zero-length IEND-type EOF matrix vectors that exercise both PngDecodeMachine::chunk_incomplete_error and PngChunkDecoder::finish."
    - path: modules/mb-image/png/stream_decode.mbt
      provides: "Narrow private active-state handling, only if RED matrix vectors expose the frozen png-iend-type finish classification as unreachable."
  key_links:
    - from: modules/mb-image/png/stream_decode_wbtest.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: "The same byte prefix first interrogates the private classifier and then reaches the public PngChunkDecoder finish route, including exact source total and terminal replay."
      pattern: "PngDecodeMachine::chunk_incomplete_error|PngChunkDecoder::finish|zlib-truncated|png-iend-type"
---

<objective>
Close the remaining Phase 27 EOF-classifier evidence gap with genuine paused-inflater and post-raster type-state vectors.

Purpose: The public API and broad parity tests pass, but the frozen classifier table is not yet executable for fixed-Huffman token, dynamic-Huffman tree/match, and two completed zero-length type rows.
Output: Targeted white-box/public regression tests and only the private classifier-state correction demonstrated necessary by their RED run.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-public-png-chunk-decoder/27-01-PLAN.md
@.planning/phases/27-public-png-chunk-decoder/27-01-SUMMARY.md
@.planning/phases/27-public-png-chunk-decoder/27-02-PLAN.md
@.planning/phases/27-public-png-chunk-decoder/27-02-VERIFICATION.md
@modules/mb-image/png/deflate_inflate.mbt
@modules/mb-image/png/stream_decode.mbt
@modules/mb-image/png/stream_decode_wbtest.mbt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add paired real inflater-pause and zero-length type EOF matrix vectors</name>
  <files>modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - A prefix selected from the existing fixed-Huffman hostile payload stops in an actual fixed literal/distance token decode, and a prefix selected from the existing dynamic hostile payload stops once in dynamic tree construction and once in an actual dynamic match transition; each remains an open IDAT with no IDAT CRC or IEND.
    - For each of those three paused machine states, private chunk_incomplete_error and public finish return the same Data/UnexpectedEndOfStream CoreError with context zlib-truncated and completed equal to the full accepted prefix length. Later public push consumes zero and reports that original error; a second finish returns the same error.
    - Starting immediately after the authenticated final IDAT CRC of the complete one-pixel fixture, the two byte sequences consisting of a zero length plus IEN followed by a wrong fourth type byte, and a zero length plus a complete non-IEND ancillary type, are fully consumed as active pending input. Their private and public finish diagnoses are Data/UnexpectedEndOfStream with context png-iend-type and completed equal to 68 bytes, followed by identical zero-consumption push and finish replay.
  </behavior>
  <action>Begin RED in `stream_decode_wbtest.mbt`, extending the existing `png_wb_assert_incomplete` pair rather than creating a second EOF harness. Derive the three zlib prefixes from the already checked fixed-Huffman stream and dynamic-Huffman stream in this file: feed the candidate bytes one at a time through the private inflater/machine and assert its retained `PngInflatePhase` before using it as an EOF vector. The fixed vector must prove a partially-read `Symbols` token through its cursor; the dynamic-tree vector must prove a retained dynamic code-length/tree phase; the dynamic-match vector must prove retained length/distance/copy work after at least one emitted byte. Wrap each selected exact prefix with the existing valid IHDR/open-IDAT helper so the surrounding IDAT CRC and IEND are absent. Do not label a stored-block prefix as fixed or dynamic, and do not accept a generic non-complete phase as evidence. Route every vector through both the private classifier and `PngChunkDecoder::push` then `finish`; assert category, code, context, operation/requested/limit shape, exact completed count, NeedInput before finish, later push consumed zero, and repeated finish equality.

For the type rows, start from the established 60-byte post-IDAT-CRC fixture boundary and add literal zero-length chunk headers. Include one `IEN?` wrong-fourth-type row and one completed valid-form zero-length ancillary non-IEND type row; both must remain active through their complete eighth header byte so finish, not push, performs the frozen EOF transition. Assert the exact 68-byte total, png-iend-type context, and the same public/private sticky replay checks. Do not alter generated fixtures, policy, public declarations, eager Reader behavior, or unrelated existing classifier coverage in this task. Commit the failing executable matrix before changing production code.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk EOF classifier*'</automated>
  </verify>
  <done>All omitted rows are represented by actual inspected fixed/dynamic/private states and public byte prefixes; RED failures, if any, identify only an unreachable frozen classifier transition rather than an ambiguous test fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the frozen zero-length IEND-type finish state and qualify the completed matrix</name>
  <files>modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - A completed zero-length type following a fully completed IDAT/inflater/raster cannot fall through to generic chunk CRC handling or an immediate unrelated parser error before finish; it records the precise private active state needed for the frozen png-iend-type finish classification.
    - The correction applies after zlib and raster completeness only, so incomplete zlib still takes zlib-truncated precedence and pre-existing nonzero IEND, CRC, trailing-data, semantic, resource, allocation, and budget behavior remains unchanged.
    - Every new vector passes through private classification and public finish with the required exact CoreError and sticky zero-consumption replay on all supported targets.
  </behavior>
  <action>Run Task 1's focused test first. If its two zero-length type rows expose that the current parser advances to a generic CRC state or emits an unrelated terminal error before finish, make the smallest private `PngDecodeMachineState`/accept-transition change in `stream_decode.mbt` that retains an explicit pending-IEND-type condition after a complete zero-length non-IEND type following a complete inflater and raster. Make `chunk_incomplete_error` map only that condition to `png-iend-type`; keep the existing priority order intact so active incomplete zlib wins before framing and a complete raster remains private. A subsequent input before finish must become the same stored terminal condition rather than resume generic chunk parsing. Do not change `PngChunkDecoder` declarations, policy, `Reader`, result transfer, generated vectors, or error contexts outside the targeted state.

Keep Task 1's exact public/private assertions as the regression proof. Add a compact assertion that valid IEND success, nonzero-IEND length failure, trailing-data precedence, and the existing stored/zlib rows still behave as their named tests require. Run the complete PNG package tests on all four targets after the correction; fix only regressions directly caused by this narrowly scoped classifier transition. Commit the implementation only after the RED matrix is green.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The fixed token, dynamic tree, dynamic match, wrong fourth type, and completed non-IEND type rows are all executable and pass with their frozen context, precedence, exact accepted total, and sticky replay on wasm, wasm-gc, js, and native.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller PNG bytes to PngChunkDecoder | Arbitrary byte prefixes select private DEFLATE and framing states before a result is allowed. |
| Private classifier to public finish | A state-derived EOF error becomes a sticky public terminal result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03-01 | DoS | `PngDecodeMachine::chunk_incomplete_error` | high | mitigate | Exercise fixed/dynamic pauses under open IDAT framing, preserving the existing bounded machine and zlib-first EOF classification. |
| T-27-03-02 | Tampering | `PngChunkDecoder::finish` terminal state | medium | mitigate | Paired tests compare the private classifier with public finish and require later caller bytes to consume zero and replay the first CoreError. |
| T-27-03-03 | Repudiation | error accounting | low | mitigate | Assert each terminal error's full shape and exact completed byte total in white-box/public pairs. |
</threat_model>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Public caller chunk decoding completes only through strict explicit EOF. | Task 1 proves finish-only EOF at the omitted active inflater/type states; Task 2 preserves the private pending state required for that route. |
| REQ | PNGS-02 sticky eager-equivalent typed terminal outcomes. | Both tasks require exact `CoreError`, accepted totals, zlib-before-framing precedence, and replay through push and finish. |
| RESEARCH | Thin public wrapper over the Phase 26 private machine; no Reader EOF change or pre-finish image. | Task actions retain the existing paired machine/public harness and forbid facade, Reader, result-transfer, or policy expansion. |
| CONTEXT | No Phase 27 CONTEXT.md or locked D-NN decisions exists. | No additional decision coverage is required. |
| VERIFICATION | Fixed token, dynamic tree/match, wrong fourth type, completed non-IEND zero-length type rows. | Task 1 creates all five executable rows; Task 2 resolves only the state reachability shown by RED. |

<verification>
- `moon -C modules/mb-image test png --target native --frozen -f '*PNG chunk EOF classifier*'`
- `moon -C modules/mb-image test png --target all --frozen`
</verification>

<success_criteria>
- The five formerly omitted classifier rows run from literal caller byte prefixes, not hand-set enum values or stored-block substitutes.
- Each row proves private/public agreement, exact `Data/UnexpectedEndOfStream` context, accepted-byte total, frozen priority, and sticky terminal replay.
- The complete PNG suite passes on js, wasm, wasm-gc, and native.
</success_criteria>

<output>
Create `.planning/phases/27-public-png-chunk-decoder/27-03-SUMMARY.md` when done.
</output>
