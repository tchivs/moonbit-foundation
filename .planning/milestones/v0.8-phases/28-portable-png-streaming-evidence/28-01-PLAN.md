---
phase: 28-portable-png-streaming-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/stream_decode_test.mbt
  - examples/png-portable/main/main.mbt
  - modules/mb-image/README.mbt.md
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
requirements: [PNGS-04]
must_haves:
  truths:
    - "Every one of the 3,850 generated accepted and rejected PNG decode records has executable public PngChunkDecoder evidence under empty-plus-one-byte and deterministic ragged caller packet schedules, including exact admitted progress, finish-only completion, eager parity, and sticky terminal replay."
    - "The existing png-portable executable feeds its fixed 75-byte PNG through public PngChunkDecoder chunks, resizes it bilinearly, eagerly encodes the existing canonical 78-byte PNG, and emits one frozen evidence line on all four supported targets."
    - "The scoped PNG quality lane verifies the new exact portable evidence line together with its established policy, fixture, package-allowlist, README, isolation, and four-target checks without reaching release or registry automation."
  artifacts:
    - path: modules/mb-image/png/stream_decode_test.mbt
      provides: "Black-box all-record hostile public schedule harnesses for PngChunkDecoder."
    - path: examples/png-portable/main/main.mbt
      provides: "The single portable public chunk-decode to bilinear-resize to eager-encode workflow."
    - path: modules/mb-image/README.mbt.md
      provides: "Runnable documentation of explicit PngChunkDecoder input progress and finish-only result ownership."
    - path: scripts/quality/Invoke-MoonQuality.ps1
      provides: "Exact four-target png-portable evidence and scoped PNG-lane isolation trace."
  key_links:
    - from: modules/mb-image/png/stream_decode_test.mbt
      to: modules/mb-image/png/png.mbt
      via: "The black-box harness instantiates PngChunkDecoder, passes fresh caller-owned ByteView slices to push, and obtains DecodeResult only through finish."
      pattern: "PngChunkDecoder::new|PngChunkDecoder::push|PngChunkDecoder::finish"
    - from: examples/png-portable/main/main.mbt
      to: modules/mb-image/png/stream_decode.mbt
      via: "The fixed chunk schedule reaches the public decoder before resize_bilinear and the existing eager PngEncoder."
      pattern: "PngChunkDecoder|resize_bilinear|PngEncoder"
    - from: scripts/quality/Invoke-MoonQuality.ps1
      to: examples/png-portable/main/main.mbt
      via: "Invoke-PngQualityLane executes the one executable once per supported target and exact-matches its sole evidence line."
      pattern: "PNG portable chunk-decode public workflow|example=png-portable"
---

<objective>
Prove the stable public PNG chunk decoder under corpus-wide hostile partitions and make the existing portable PNG workflow visibly use it on every supported target.

Purpose: PNGS-04 is portable public-contract evidence, not another parser or encoder feature. The proof must exercise the actual PngChunkDecoder with caller-owned input and show a real decode-to-process-to-eager-encode consumer.
Output: Corpus-wide public schedule tests, one updated documented png-portable executable, and a narrowed exact PNG quality-lane evidence gate.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-portable-png-streaming-evidence/28-RESEARCH.md
@.planning/phases/27-public-png-chunk-decoder/27-03-SUMMARY.md
@.planning/milestones/v0.5-phases/19-portable-streaming-qoi-evidence/19-01-SUMMARY.md
@modules/mb-image/png/png.mbt
@modules/mb-image/png/stream_decode_test.mbt
@examples/png-portable/main/main.mbt
@modules/mb-image/README.mbt.md
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every generated PNG record through hostile public chunk schedules</name>
  <files>modules/mb-image/png/stream_decode_test.mbt</files>
  <behavior>
    - Every item returned by `_generated_png_decode_cases()` (currently 3,850 accepted and rejected records) first accepts an empty caller-owned view with `consumed()==0` and `NeedInput` while active.
    - The `empty-then-one-byte` lane sends each subsequent byte as a distinct caller-owned ByteView. Accepted records consume exactly one byte and remain `NeedInput` through the authenticated IEND CRC; only `finish()` transfers an eager-equivalent DecodeResult. Rejected records stop at their first typed terminal error, count the terminal input byte, then replay that same error through later zero-consumption push and finish calls.
    - The `ragged-public-packets` lane cycles `[8, 4, 1, 13, 2, 5, 3, 21]`, clips its final packet, and has the same first-terminal prefix, CoreError, diagnostics, remaining-budget, result, and sticky-terminal outcome as the one-byte lane and an independently initialized eager decode.
    - For a rejection that remains active through all supplied source bytes, `finish()` supplies the first typed error; for a success, all source bytes remain non-terminal and `finish()` supplies the sole result. In both cases no push exposes an image.
    - Result parity continues to use the established full descriptor, metadata/profile, disposition, pixel, and bytes-read comparator. Failure parity compares category, code, operation, context, requested, completed, and limit, plus diagnostics rendering and every remaining budget dimension.
  </behavior>
  <action>Start with RED tests in `stream_decode_test.mbt`, extending the existing `png_chunk_vector_*`, `png_chunk_eager_vector`, `png_scheduled_result_matches`, and `png_chunk_error_matches` helpers instead of creating a parser, a second corpus, or private-state assertions. Implement one reusable public schedule runner that always uses freshly owned slices, records each push request/consumed total/outcome, and retains no ByteView after return. Run all accepted and rejected generated records through both named lanes: `empty-then-one-byte` begins with a zero-length push and then emits one byte per turn; `ragged-public-packets` cycles exactly `[8, 4, 1, 13, 2, 5, 3, 21]` and clips the final turn. Derive the required ragged terminal total from the one-byte public route, then assert each active ragged request admits its entire requested prefix until the same terminal byte; when that byte is admitted, assert the exact remaining admitted count, first error shape, and zero-consumption sticky replay. Compare each lane independently with an eager decode using separate limits, budget, and Diagnostics instances. Keep generated fixtures, machine internals, PngDecoder Reader EOF semantics, public API declarations, policy, FFI, and all encoder behavior unchanged. Commit the failing focused schedule tests before making any incidental test-helper correction, then run them green on native and all supported targets.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Both named public schedules execute all generated accepted and rejected records; progress, terminal/error parity, finish-only ownership, diagnostics, and budget assertions pass on js, wasm, wasm-gc, and native.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the sole PNG public example to chunk decode and document the contract</name>
  <files>examples/png-portable/main/main.mbt, modules/mb-image/README.mbt.md</files>
  <behavior>
    - The existing 75-byte source is pushed through one `PngChunkDecoder` with the exact input schedule `[0, 8, 4, 4, 13, 4, 4, 4, 1, 2, 4, 11, 4, 4, 4, 4]`; all 16 calls report their requested consumed count and `NeedInput`, and only the following finish call yields the 2 by 1 decoded image.
    - The existing bilinear resize still produces the same 3 by 1 pixel checks, and the existing eager PngEncoder still produces the frozen 78-byte output with digest `626208771`.
    - The executable prints exactly `example=png-portable input_schedule=workflow-zero-signature-ihdr-idat-deflate-iend pushes=16 bytes_read=75 bytes_written=78 width=3 height=1 resize_bilinear digest=626208771` once on each target.
    - The README gives users a runnable public PngChunkDecoder pattern: caller-owned ByteView input, exact consumed progress, no result from push, and explicit finish before using the decoded image; it preserves the established eager encoder and colour-capability boundaries.
  </behavior>
  <action>Write the example assertion/update RED-first in `examples/png-portable/main/main.mbt`: replace only the eager Reader/PngDecoder decode section with `@png.PngChunkDecoder::new(codec_limits(), budget, diagnostics)` and the fixed sixteen-turn schedule. Count the zero-length call as a push, use source subviews without copying or retaining them, assert each returned `consumed()` equals the requested slice size and its outcome is `@png.PngChunkPushOutcome::NeedInput`, and call `finish()` exactly once after all 75 bytes. Retain the current source bytes, limits, resize, eager PngEncoder, exact writer bytes, digest, semantic pixel assertions, and target-neutral output. Update the printed line exactly as specified in this task. In `modules/mb-image/README.mbt.md`, add a compact `mbt check` public chunk-decode example that demonstrates the same constructor/push/finish ownership rule using portable imports and a small valid PNG; include an assertion that an empty push consumes zero and that finish yields the image. Do not introduce a second workspace/example, a public stream encoder, a buffered alternate decoder, FFI, new dependencies, release work, or colour transformation. Run the README checks and all four `png-portable` target commands after the code is green.</action>
  <verify>
    <automated>moon -C examples/png-portable run main --target js --frozen; moon -C examples/png-portable run main --target wasm --frozen; moon -C examples/png-portable run main --target wasm-gc --frozen; moon -C examples/png-portable run main --target native --frozen; moon -C modules/mb-image check README.mbt.md --target all --frozen</automated>
  </verify>
  <done>The only PNG public executable and its runnable documentation use PngChunkDecoder correctly, preserve the frozen processing/output evidence, and produce the one exact four-target status line.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the scoped PNG quality evidence and isolation trace</name>
  <files>scripts/quality/Invoke-MoonQuality.ps1</files>
  <action>Update only `Invoke-PngQualityLane` and its paired `Assert-PngLaneIsolation` expected stage sequence so the existing public-workflow stage is named `PNG portable chunk-decode public workflow` and exact-matches the Task 2 status line on `js`, `wasm`, `wasm-gc`, and `native`. Preserve the existing policy call, scoped negative fixtures, exact package allowlist, generated structural/decode vector freshness checks, README/colour conformance commands, four-target package test, and the complete ordered isolation trace. Keep `Assert-PngLaneIsolation` fail-closed against broad foundation, QOI, Required, and Release paths; do not add release qualification, registry, credential, publication, or a broader controller. Verify the updated lane directly after the package, executable, and README work is green.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>The isolated PNG lane runs current generated fixtures, all target tests, four exact chunk-decode workflow outputs, and the README check in its asserted order without reaching any release/registry route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated hostile PNG bytes to public `PngChunkDecoder::push` | Untrusted caller-owned chunks select framing, DEFLATE, raster, CRC, and EOF terminal paths. |
| Public chunk decoder to eager decoder oracle | Independent runs must agree on output/error, diagnostics, and resource accounting rather than merely both terminating. |
| Portable example output to quality lane | The executable's exact evidence must remain target-neutral and the scoped lane must not cross into release infrastructure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | fragmented generated PNG input | high | mitigate | Run every accepted and rejected corpus record through empty-plus-one-byte and ragged public schedules, comparing the first terminal prefix and typed observable outcome to independent eager runs. |
| T-28-02 | Denial of service | hostile DEFLATE/raster/resource prefixes | high | mitigate | Preserve corpus limits/budgets and assert exact progress, error replay, Diagnostics, and each remaining resource dimension on every target. |
| T-28-03 | Information disclosure | public result ownership before strict EOF | high | mitigate | Require `NeedInput` through the authenticated IEND and make finish the sole DecodeResult transfer in schedules and executable. |
| T-28-04 | Repudiation | portable workflow evidence | medium | mitigate | Freeze source bytes, schedule identity/count, output byte count, semantic pixels, digest, and an exact single status line for all four target runs. |
| T-28-05 | Elevation of privilege | quality-lane routing | high | mitigate | Retain fail-closed lane-isolation stubs and exact scoped stage trace; do not add release, registry, credential, or publication calls. |
</threat_model>

<verification>
- `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `moon -C modules/mb-image test png --target all --frozen`
- `moon -C examples/png-portable run main --target js --frozen`
- `moon -C examples/png-portable run main --target wasm --frozen`
- `moon -C examples/png-portable run main --target wasm-gc --frozen`
- `moon -C examples/png-portable run main --target native --frozen`
- `moon -C modules/mb-image check README.mbt.md --target all --frozen`
- `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- The 3,850-record generated corpus supplies two executable public schedule proofs that cover accepted and rejected input with deterministic per-push admission, finish behavior, parity, and sticky terminal replay.
- `png-portable` visibly uses public chunks before its established bilinear resize and eager canonical encode, retaining exact output bytes and digest across js, wasm, wasm-gc, and native.
- The README and isolated quality lane make the public workflow reproducible while keeping release/registry and public streaming-encoder scope absent.
</success_criteria>

## Source Coverage Audit

| Source | ID | Required item | Plan coverage | Status |
|--------|----|---------------|---------------|--------|
| GOAL | — | Independently verify public resumable PNG decode through hostile schedules and one portable processing workflow. | Tasks 1-3 exercise the public decoder over corpus input, the in-place public executable, and the scoped cross-target gate. | COVERED |
| REQ | PNGS-04 | Adversarial split schedules and one public chunk-decode workflow unchanged on js, wasm, wasm-gc, and native. | Task 1 provides both all-record schedules; Task 2 provides the workflow and direct four-target commands; Task 3 enforces it in the isolated PNG lane. | COVERED |
| RESEARCH | — | Reuse the generated 3,850-case corpus; test accepted and rejected records under one-byte and ragged partitions. | Task 1 uses exactly the existing corpus and named schedules. | COVERED |
| RESEARCH | — | Use only public PngChunkDecoder and eager oracle; prove exact progress, typed sticky outcomes, result/diagnostic/budget parity, and finish-only ownership. | Task 1's black-box runner compares all listed observables and forbids private parser tests. | COVERED |
| RESEARCH | — | Update existing png-portable in place to chunk decode, bilinear resize, and eager encode with frozen 78-byte/digest evidence. | Task 2 retains the source/output and changes only the public decode route. | COVERED |
| RESEARCH | — | Keep a scoped Png quality lane with exact public evidence and no release/registry expansion. | Task 3 changes only the existing PNG public-workflow stage and its isolation trace. | COVERED |
| CONTEXT | — | No Phase 28 CONTEXT.md or D-NN locked decisions exist. | Roadmap, requirements, and research constraints are reflected in all tasks; no deferred public encoder or release work is planned. | COVERED |

<output>
Create `.planning/phases/28-portable-png-streaming-evidence/28-01-SUMMARY.md` when done.
</output>
