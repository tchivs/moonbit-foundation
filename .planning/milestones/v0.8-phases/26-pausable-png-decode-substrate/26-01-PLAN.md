---
phase: 26-pausable-png-decode-substrate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/structural.mbt
  - modules/mb-image/png/png.mbt
  - modules/mb-image/png/stream_decode.mbt
  - modules/mb-image/png/stream_decode_wbtest.mbt
  - modules/mb-image/png/stream_decode_test.mbt
  - modules/mb-image/png/deflate_bits.mbt
  - modules/mb-image/png/deflate_huffman.mbt
  - modules/mb-image/png/deflate_inflate.mbt
  - modules/mb-image/png/raster_decode.mbt
  - policy/foundation.json
  - scripts/quality/Assert-Policy.ps1
  - scripts/quality/Invoke-MoonQuality.ps1
autonomous: true
requirements: [PNGS-03]
must_haves:
  truths:
    - "The private PNG decode machine can stop and resume at every accepted input-byte boundary without retaining a caller ByteView or a complete IDAT stream."
    - "Framing, per-IDAT CRC, DEFLATE/zlib, filter reconstruction, Adam7, IEND, and EOF all retain enough owned state to continue without replaying a byte or token."
    - "The machine keeps its allocated image and result ingredients private until final framing and integrity checks succeed."
    - "Existing callers receive the same PngDecoder successes and typed failures across the complete supported profile on js, wasm, wasm-gc, and native."
  artifacts:
    - path: "modules/mb-image/png/stream_decode.mbt"
      provides: "Private byte-source adapters and the owned PNG framing, IDAT, inflate, raster, terminal, and result state machine."
    - path: "modules/mb-image/png/stream_decode_wbtest.mbt"
      provides: "White-box byte-boundary pause, ownership, resource, integrity, and no-result-before-completion evidence."
    - path: "modules/mb-image/png/deflate_inflate.mbt"
      provides: "Private resumable stored/fixed/dynamic DEFLATE continuation over the existing pure-MoonBit algorithm."
    - path: "modules/mb-image/png/raster_decode.mbt"
      provides: "Private resumable raster sink retaining filters, packed rows, palette/transparency conversion, and Adam7 cursors."
    - path: "modules/mb-image/png/stream_decode_test.mbt"
      provides: "Full-corpus public-facade versus test-only byte-schedule equivalence evidence."
    - path: "policy/foundation.json"
      provides: "Exact PNG private-source inventory with the unchanged PngDecoder/PngEncoder semantic interface."
  key_links:
    - from: "modules/mb-image/png/stream_decode.mbt"
      to: "modules/mb-image/png/structural.mbt"
      via: "Machine transitions reuse existing PNG grammar, checked sizing, colour, CRC, metadata, and preflight helpers after fields are complete."
      pattern: "PngDecodeMachine|_png_"
    - from: "modules/mb-image/png/stream_decode.mbt"
      to: "modules/mb-image/png/deflate_inflate.mbt"
      via: "PngIdatState supplies one authenticated compressed byte at a time to PngInflateState."
      pattern: "PngIdatState|PngInflateState"
    - from: "modules/mb-image/png/deflate_inflate.mbt"
      to: "modules/mb-image/png/raster_decode.mbt"
      via: "Every inflated byte is emitted through a persisted PngRasterSink before additional compressed input is accepted."
      pattern: "PngRasterSink|emit"
---

<objective>
Build the private, MoonBit-owned PNG decode substrate that safely pauses inside every framing, compression, and raster transition while preserving the eager decoder's established semantics.

Purpose: Make true caller-buffered PNG decode possible in Phase 27 without a second parser, FFI, retained caller buffers, or a public API change in this phase.
Output: Private `PngDecodeMachine` state, the unchanged public PngDecoder facade driven by it, full-profile equivalence evidence, and fail-closed private-source inventories.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-pausable-png-decode-substrate/26-RESEARCH.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-01-SUMMARY.md
@.planning/phases/21-bounded-png-decode-and-deflate/21-03-SUMMARY.md
@.planning/phases/25-portable-colour-conformance-evidence/25-01-SUMMARY.md
@modules/mb-image/png/structural.mbt
@modules/mb-image/png/deflate_bits.mbt
@modules/mb-image/png/deflate_huffman.mbt
@modules/mb-image/png/deflate_inflate.mbt
@modules/mb-image/png/raster_decode.mbt
@modules/mb-image/qoi/stream_decode.mbt
@modules/mb-image/png/png_test.mbt
@modules/mb-image/png/generated_decode_vectors_test.mbt
@policy/foundation.json
@scripts/quality/Assert-Policy.ps1
@scripts/quality/Invoke-MoonQuality.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the private byte-fed framing and preflight machine</name>
  <files>modules/mb-image/png/structural.mbt, modules/mb-image/png/raster_decode.mbt, modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - A test-only owned-slice feeder can pause after each byte of the PNG signature, chunk length/type/payload/CRC, including IHDR, PLTE, tRNS, sRGB/gAMA/cHRM, iCCP, empty IDAT, and IEND, then resume the same machine.
    - The machine accepts and charges a byte exactly once, owns all incomplete fixed fields and required ancillary payload state, and never keeps the feeder's ByteView after returning NeedInput.
    - At the first valid IDAT header, the same metadata, descriptor, child-budget split, image, and row-storage preflight used by eager PNG are created once and held privately; preflight failure leaves the caller budget unchanged wherever the existing contract requires it.
    - The private raster sink owns filter, row, column, packed-row, palette/transparency, 16-bit, and Adam7 pass state independently of the compressed input adapter.
  </behavior>
  <action>Create private-only `PngDecodeMachine`, byte-source result/state, Reader adapter, and test-only slice adapter in `stream_decode.mbt`; no exported chunk-decoder, push result, finish method, or public type is permitted in Phase 26. Move synchronous framing control flow from `structural.mbt` into persisted signature, chunk-header, ancillary, CRC, chunk-order, and IDAT states while retaining existing validators, exact error contexts, checked limits, colour-declaration rules, CRC helpers, and `Reader` EOF interpretation. Extract the current raster emit closure in `raster_decode.mbt` into a private `PngRasterSink` with its owned row and Adam7 state; preserve existing reconstruction/writer helpers and defer compressed-byte wiring to Task 2. On a complete, valid first IDAT header, invoke the existing descriptor/metadata/preflight helpers to allocate the private output and bounded row state exactly once; retain existing ICC and legacy-colour semantics. First write the white-box tests, including a mutation-after-pause assertion that proves bounded copied state is used instead of a borrowed source view.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>The private framing machine is compilable, has no public streaming declaration, pauses at every framing/ancillary/CRC boundary, preserves input and resource accounting, and cannot expose a DecodeResult before final completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert zlib and DEFLATE tokens into owned resumable continuations</name>
  <files>modules/mb-image/png/deflate_bits.mbt, modules/mb-image/png/deflate_huffman.mbt, modules/mb-image/png/deflate_inflate.mbt, modules/mb-image/png/stream_decode.mbt, modules/mb-image/png/stream_decode_wbtest.mbt</files>
  <behavior>
    - Pausing during a zlib header or Adler trailer, a stored-block length/body, a fixed or dynamic Huffman prefix, dynamic code-length repetition, length/distance extra bits, or a match copy produces the same eventual bytes or typed terminal as uninterrupted input.
    - A resumed token neither consumes an already-read bit twice nor loses the active tree, repeat count, history position, length, distance, or copy cursor.
    - IDAT payload bytes feed one zlib stream across arbitrary IDAT partitions; every IDAT CRC is verified before subsequent framing, and existing zlib trailing/error precedence remains intact.
    - The connected PngRasterSink produces the same filter reconstruction and Adam7 pixels while keeping result construction private until final IDAT, zlib, IEND, and EOF validation.
  </behavior>
  <action>Replace `PngIdatSource`-owned synchronous reads and local inflater control variables with private state consumed by `PngDecodeMachine`: preserve the LSB bit accumulator, block phase, stored-block counters, active Huffman prefix/cursor, dynamic-tree arrays and repeat cursor, pending length/distance/match-copy state, 32 KiB history, and Adler accumulator. Refactor bit and Huffman helpers so an unavailable byte yields a resumable pause rather than restarting a partial operation. Feed only the current authenticated IDAT payload byte into the inflater, connect each emitted byte to the `PngRasterSink` created in Task 1, and preserve CRC-trailer, zlib Adler, post-IDAT, IEND, EOF, trailing-data, and no-result-before-terminal transitions. Add RED-first white-box cases that stop at each token, filter/sample, Adam7 pass, CRC, and EOF class and verify restart-free completion plus exact current error category, code, context, budget behavior, and image privacy for malformed input.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>Stored, fixed-Huffman, and dynamic-Huffman DEFLATE—including malformed trees, zlib trailers, and cross-IDAT streams—can pause at every compressed-byte boundary with the existing bounded pure-MoonBit semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch the eager facade, prove full-profile parity, and lock private-source policy</name>
  <files>modules/mb-image/png/png.mbt, modules/mb-image/png/stream_decode_test.mbt, policy/foundation.json, scripts/quality/Assert-Policy.ps1, scripts/quality/Invoke-MoonQuality.ps1</files>
  <behavior>
    - PngDecoder keeps its current constructor, ImageDecoder implementation, Reader-based EOF behavior, and public result/error contract while driving PngDecodeMachine to completion through a Reader adapter.
    - Every accepted generated PNG case, spanning grayscale, indexed/PLTE, tRNS, 16-bit, Adam7, sRGB, retained legacy declarations, and iCCP, matches a test-only machine fed by adversarial internal schedules in dimensions, channels, every pixel byte, descriptor metadata/profile/transfer, disposition, and bytes_read.
    - Every rejected generated case and boundary mutation matches category, code, context, and applicable budget delta; late CRC, zlib, filter, IEND, EOF, and trailing-data failures expose no result.
  </behavior>
  <action>Replace the eager-only `_png_read_stream_transport` plus synchronous inflater hand-off in `PngDecoder::decode` with a compact Reader-adapter driver around the completed private machine. Preserve probe, public signatures, DecodeOptions, Diagnostics handling, and Reader EOF behavior exactly; do not add PngChunkDecoder, a push/finish result, public streaming encoder, FFI, or a second parser. Create `stream_decode_test.mbt` before changing the facade; run the current generated corpus through both the public Reader facade and private slice adapter using one-byte and adversarial multi-byte schedules, comparing all observable success fields and exact failure/resource/no-image behavior. Reuse the corpus without changing fixture JSON or its generator. Add `stream_decode.mbt` after `generated_vectors.mbt` in the PNG production-source sequence and add its two test files to exact directory/package lists in policy and executable checks; update the isolated PNG quality list in the same order. Keep the policy semantic interface unchanged so only PngDecoder and PngEncoder remain public, and keep negative fixtures rejecting prospective public stream decoder declarations. Do not modify QOI inventories, configuration, fixture policy, release work, or public consumer workflows.</action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check; moon -C modules/mb-image test png --target all --frozen; pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png</automated>
  </verify>
  <done>PngDecoder is the only public decoder and is eager-equivalent across the full existing supported profile and hostile terminal/resource cases; exact policy checks recognize only the named private state-machine files and retain the four-target public interface contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader/test-byte feeder → PngDecodeMachine | Untrusted PNG bytes cross into retained parser, DEFLATE, and raster state. |
| PngDecodeMachine → private image/result | Untrusted bytes influence a private allocation that must not become observable before integrity and EOF validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Denial of service | framing, ancillary, DEFLATE, raster continuation | high | mitigate | Reuse checked chunk/image limits, child budgets, declared work, 32 KiB history, bounded retained fields, and generated hostile limit cases at every resumed transition. |
| T-26-02 | Tampering | IDAT/CRC/zlib/state transitions | high | mitigate | Keep one owned chunk/CRC state, authenticate each payload and trailer before transition, and preserve exact zlib/IDAT/IEND ordering/error precedence in white-box boundary tests. |
| T-26-03 | Information disclosure | private OwnedImage and DecodeResult construction | high | mitigate | Allocate once after first-IDAT preflight but retain image, metadata, disposition, and result inside the machine until final IDAT CRC, Adler, IEND CRC, and EOF succeed. |
| T-26-04 | Tampering | borrowed input view after pause | medium | mitigate | Consume bytes into bounded owned state before NeedInput and test post-pause source mutation; adapters retain no ByteView or Reader lease. |
| T-26-05 | Tampering | eager facade and package policy | high | mitigate | Compare the Reader-driven facade with internal hostile schedules over the generated corpus and preserve exact public-interface/source-inventory rejection gates. |
</threat_model>

<verification>
- `pwsh -NoProfile -File scripts/fixtures/Generate-PngDecodeVectors.ps1 -Check`
- `moon -C modules/mb-image test png --target all --frozen`
- `pwsh -NoProfile -File scripts/quality/Invoke-MoonQuality.ps1 -Lane Png`
</verification>

<success_criteria>
- All existing PNG grammar, colour, CRC, DEFLATE, raster, Adam7, limit, and EOF behavior has one private machine continuation path that can pause after any byte.
- The private machine has no public streaming surface, no FFI, no full-IDAT buffering, and no borrowed-input retention.
- Every attempted output remains private until terminal integrity and end-of-input validation completes.
- Existing PngDecoder behavior is parity-proved across js, wasm, wasm-gc, and native; private sources are exact-policy listed while the public PNG interface stays PngDecoder/PngEncoder only.
</success_criteria>

## Source Coverage Audit

| Source | Required item | Coverage | Task |
|--------|---------------|----------|------|
| GOAL | Eager decoding remains compatible while framing, IDAT CRC, DEFLATE, raster, Adam7, IEND, and EOF pause safely at byte boundaries | COVERED | Tasks 1-3 |
| REQ | PNGS-03 eager-profile, semantic, accounting, diagnostics, resource, and no-partial-image parity | COVERED | Tasks 1-3 |
| RESEARCH | One private shared machine plus Reader/test-byte adapters; no buffered false resumability | COVERED | Tasks 1-3 |
| RESEARCH | Preserve grayscale, indexed/PLTE, tRNS, 16-bit, Adam7, sRGB, legacy declarations, and iCCP | COVERED | Tasks 1 and 3 |
| RESEARCH | Exact source/policy inventories and four-target PNG quality lane; reuse corpus without generator or fixture-policy changes | COVERED | Task 3 |
| CONTEXT | No Phase 26 CONTEXT.md exists; roadmap/state/research scope forbids public PngChunkDecoder until Phase 27, FFI, public streaming encode, release work, and changed Reader EOF semantics | COVERED | Tasks 1 and 3 |

<output>
Create `.planning/phases/26-pausable-png-decode-substrate/26-01-SUMMARY.md` when done.
</output>
