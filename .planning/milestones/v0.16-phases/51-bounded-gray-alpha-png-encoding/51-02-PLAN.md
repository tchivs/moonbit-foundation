---
phase: 51-bounded-gray-alpha-png-encoding
plan: "02"
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYA-02
  - GRAYA-03
must_haves:
  truths:
    - "Each bounded strategy/filter pair has a focused caller-buffered GrayAlpha regression that proves ordinary eager/chunk identity and type-4, 8-bit, non-interlaced framing."
    - "A GrayAlpha route rejection leaves eager output empty, leaves the caller budget unchanged, and does not modify a caller-owned sentinel lease before construction returns an error."
    - "The same atomic contract applies to incompatible descriptors and to geometry, output, work, and budget limit failures across all six bounded strategy/filter selections."
  artifacts:
    - "modules/mb-image/png/stream_encode_test.mbt has GrayAlpha strategy-pair and combined atomic-preflight regression helpers."
  key_links:
    - "The public PngChunkEncoder graya8 combined factory reaches the same `PngEncodeMachine::new_with_profile` preflight transaction used by eager encoding."
    - "Each failure assertion compares eager and caller-buffered typed outcomes while examining writer position, budget accounting, and a caller-owned lease sentinel."
---

<objective>
Harden the Gray+Alpha8 bounded contract with focused caller-buffered strategy and atomic-preflight regressions after the production tracer is available.

Purpose: prove that the new profile has inherited the established bounded-encoder safety and ownership rules rather than merely producing a valid PNG.
Output: executable GrayAlpha strategy-grid and atomic-rejection coverage in the existing PNG stream test suite.
</objective>

## Artifacts this phase produces

- A focused GrayAlpha caller-buffered six-pair parity regression.
- A reusable GrayAlpha combined-rejection helper covering typed capability and resource-limit failure states before observable output.

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-CONTEXT.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-RESEARCH.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-PATTERNS.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-01-SUMMARY.md

<interfaces>
From Plan 51-01:
- `PngEncoder::new_graya8_with_strategies(strategy, filter_strategy)` is the eager GrayAlpha entry point.
- `PngChunkEncoder::new_graya8_with_strategies(source, strategy, filter_strategy, limits, budget, diagnostics)` returns `Result[PngChunkEncoder, CoreError]` and must fail before it can expose a callback lease.

From `modules/mb-image/png/stream_encode_test.mbt`:
- `png_chunk_test_drain_encoder` drains a real encoder using ordinary capacities.
- `png_chunk_test_owner` creates a mutable caller-owned byte buffer filled with a sentinel.
- `png_chunk_test_same_error` and `png_fixed_or_stored_same_remaining` compare typed errors and budget states.
- `png_fixed_or_stored_limits` and `png_stream_test_budget` create existing resource-limit and budget test inputs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the GrayAlpha caller-buffered bounded-strategy parity grid</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - For each of the six compression/filter pairs, a newly constructed graya8 chunk encoder drains to the same byte sequence as its eager peer.
    - Each resulting byte sequence carries IHDR depth 8, colour type 4, and interlace method 0.
    - All caller-buffered convenience factory shapes are exercised with a valid non-symmetric packed GrayAlpha source.
  </behavior>
  <action>Per D-01, D-03, and D-04, add a GrayAlpha counterpart to the existing Gray16 caller-buffered strategy test. Reuse the Phase 51 fixture and eager helper from Plan 51-01, construct a fresh `new_graya8_with_strategies` encoder for every Stored/FixedOrStored/DynamicOrFixedOrStored crossed with None/Adaptive, drain it with the established ordinary test helper, and compare the complete result with eager bytes. Inspect the three IHDR fields on each result and invoke the compression-only and filter-only constructors once so the full public family remains compiled and exercised. Keep this test limited to ordinary drains; public hostile capacity schedules and portable qualification remain owned by Phase 52.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>All six GrayAlpha strategy/filter selections and all public caller-buffered factory shapes have focused ordinary-parity coverage using the shared bounded encoder.</done>
</task>

<task type="auto">
  <name>Task 2: Assert atomic GrayAlpha admission before writer output or lease mutation</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <behavior>
    - An incompatible source returns the GrayAlpha typed capability error in eager and caller-buffered construction without writer bytes, budget debit, or sentinel-byte mutation.
    - Valid GrayAlpha sources rejected by width, output, work, or budget envelopes produce matching eager/chunk errors with the same unchanged observable state.
    - The assertions execute for each bounded compression/filter pair.
  </behavior>
  <action>Per D-02 and D-04, mirror `png_gray16_combined_public_reject` as a GrayAlpha helper that iterates the six bounded strategy/filter selections. For each eager attempt, capture the preflight budget and assert failure before the memory writer position advances. For the matching caller-buffered attempt, create the established sentinel owner before construction, compare the typed error and unchanged budget to eager, then inspect every sentinel byte after the `Err` result. Invoke the helper for a channel-order-incompatible source and for a valid GrayAlpha source under tight geometry, output, work, and budget envelopes. Use the exact existing helpers and error contexts; do not add an independent preflight path or substitute a synthetic lease object.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen</automated>
  </verify>
  <done>Every selected GrayAlpha failure is rejected by the shared transaction before eager output or caller-buffered lease mutation, with unchanged resource accounting and matching typed eager/chunk errors.</done>
  <reversibility rating="reversible">These are test-local assertions over an established public contract; their fixture organization can change without migrating consumers.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller image and limits to bounded preflight | Caller-controlled source identity and resource limits determine whether construction is allowed. |
| Encoder construction to caller lease | A caller owns the mutable output lease and must not observe writes when construction is rejected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-05 | Tampering | graya8 eager and chunk strategy factories | high | mitigate | Task 1 proves each public pair selects the same valid framed output through the existing profile-aware machine. |
| T-51-06 | Denial of Service | checked geometry/output/work/budget preflight | high | mitigate | Task 2 runs each resource envelope through all six pairs and asserts unchanged accounting on rejection. |
| T-51-07 | Tampering | callback-scoped mutable output lease | high | mitigate | Task 2 checks all sentinel bytes after rejected chunk construction, proving no pre-exposure mutation. |
| T-51-08 | Repudiation | typed eager/chunk failure comparison | medium | mitigate | Task 2 compares the existing structured CoreError values instead of only checking generic failure. |
</threat_model>

<verification>
Run `moon -C modules/mb-image test png --target native --frozen`. The result must execute the GrayAlpha strategy grid and atomic-preflight regressions together with the established PNG suite.
</verification>

<success_criteria>
- GRAYA-02 retains ordinary eager/caller-buffered byte identity across all bounded strategy selections.
- GRAYA-03 proves typed, atomic rejection before eager output or caller lease exposure for incompatible and resource-limited requests.
- The additions remain test-local to the PNG package and do not create release or target-specific work.
</success_criteria>

<output>
Create `.planning/phases/51-bounded-gray-alpha-png-encoding/51-02-SUMMARY.md` when done.
</output>
