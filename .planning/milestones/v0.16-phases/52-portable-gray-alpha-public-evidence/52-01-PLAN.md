---
phase: 52-portable-gray-alpha-public-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/mb-image/png/encode_test.mbt
  - modules/mb-image/png/stream_encode_test.mbt
autonomous: true
requirements:
  - GRAYA-04
  - GRAYA-05
must_haves:
  truths:
    - "Public GrayAlpha8 evidence proves the literal `(13,A7)/(D2,4C)` Stored/None scanline order, type-4 PNG framing, and public decode to straight RGBA8 with gray replicated through RGB."
    - "All six public compression/filter pairs produce GrayAlpha8 caller-buffered bytes identical to the eager public factory under zero-capacity, one-byte, and deterministic ragged schedules."
    - "Every hostile pull accounts only for accepted bytes, preserves unwritten caller lease sentinels, and returns a zero-byte sticky Finished result after success."
    - "Literal Gray8, Gray16, RGB8, and straight-RGBA8 vectors remain byte-identical in eager and caller-buffered public regression coverage."
    - "The same PNG package test suite passes on wasm, wasm-gc, js, and native through one portable MoonBit invocation."
  artifacts:
    - path: modules/mb-image/png/encode_test.mbt
      provides: Public GrayAlpha8 Stored/None wire, RGBA8 decode canonicalization, six-pair eager, and four-family frozen-vector evidence.
    - path: modules/mb-image/png/stream_encode_test.mbt
      provides: Public GrayAlpha8 hostile-capacity, accepted-progress, terminal, eager-parity, and four-family frozen-vector evidence.
  key_links:
    - from: PngEncoder::new_graya8_with_strategies
      to: PngDecoder::new
      via: Public Stored/None bytes are checked at the type-4 scanline boundary, then decoded through ImageDecoder::decode.
      pattern: new_graya8_with_strategies
    - from: PngChunkEncoder::new_graya8_with_strategies
      to: PngEncoder::new_graya8_with_strategies
      via: Fresh chunk encoders for every schedule must yield the fresh eager oracle bytes for the identical strategy/filter pair.
      pattern: new_graya8_with_strategies
---

<objective>
Close GRAYA-04 and GRAYA-05 with compact, portable public PNG regressions only: prove the Gray+Alpha8 wire/decode contract, hostile caller-buffered ownership, frozen legacy bytes, and all-target execution without changing production behavior.

Purpose: preserve a consumer-visible compatibility and bounded-output contract at the existing `mb-image/png` API seams.
Output: focused public evidence in the two established PNG package test files.
</objective>

<execution_context>
@C:/Users/Admin/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-portable-gray-alpha-public-evidence/52-CONTEXT.md
@.planning/phases/52-portable-gray-alpha-public-evidence/52-RESEARCH.md
@.planning/phases/52-portable-gray-alpha-public-evidence/52-PATTERNS.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-01-SUMMARY.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-02-SUMMARY.md
@.planning/phases/51-bounded-gray-alpha-png-encoding/51-VERIFICATION.md
@.planning/milestones/v0.15-phases/49-portable-gray16-public-evidence/49-01-SUMMARY.md
@.planning/milestones/v0.15-phases/49-portable-gray16-public-evidence/49-VERIFICATION.md
@modules/mb-image/png/encode_test.mbt
@modules/mb-image/png/stream_encode_test.mbt

<interfaces>
- `PngEncoder::new_graya8_with_strategies(strategy, filter_strategy)` is the public eager construction seam and fixes the existing GrayAlpha8 type-4/non-interlaced profile.
- `PngChunkEncoder::new_graya8_with_strategies(source, strategy, filter_strategy, limits, budget, diagnostics)` constructs the public caller-buffered seam and returns `PngChunkPullOutcome` values from `pull`.
- `@codec.ImageDecoder::decode(PngDecoder::new(), ...)` is the public decoder seam; the established result for a type-4 U8 PNG is straight `Rgba` with the gray byte copied to channels 0..2 and alpha in channel 3.
- `png_stream_gray16_public_drain` and the Gray16 public-evidence test supply the exact fresh-encoder, sentinel, accepted-progress, and sticky-terminal pattern to adapt.
</interfaces>
</context>

<source_audit>
| Source | ID | Required item | Coverage |
|---|---|---|---|
| GOAL | Phase 52 | Public GrayAlpha fidelity, caller-buffered semantics, legacy stability, and portable proof | Tasks 1-2 |
| REQ | GRAYA-04 | Exact `(13,A7)/(D2,4C)` wire-pair evidence and straight-RGBA8 canonicalization through public APIs | Task 1 |
| REQ | GRAYA-05 | Six-pair zero/one/ragged parity, accepted-only progress, sticky terminals, four frozen families, and `--target all` | Tasks 1-2 |
| RESEARCH | R-01 | Stored/None scanline `00 13 A7 D2 4C`, type-4/8-bit/non-interlaced framing, and public decoder oracle | Task 1 |
| RESEARCH | R-02 | Reuse the Phase 49 public drain shape for the six GrayAlpha strategy/filter pairs | Task 2 |
| CONTEXT | D-01 | Compact public test fixture, literal wire evidence, and RGBA8 canonicalization | Task 1 |
| CONTEXT | D-02 | Only public eager/chunk constructors and public decoder seam | Tasks 1-2 |
| CONTEXT | D-03 | Zero, one-byte, ragged schedules, accepted-only progress, and sticky terminals for every pair | Task 2 |
| CONTEXT | D-04 | Existing drivers, sentinels, and terminal helpers; no new buffering or retry mechanism | Task 2 |
| CONTEXT | D-05 | Literal frozen Gray8, Gray16, RGB8, and straight-RGBA8 vector comparisons | Tasks 1-2 |
| CONTEXT | D-06 | One portable four-target test command and MoonBit-only test code | Task 2 |
</source_audit>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one public GrayAlpha8 Stored/None wire-to-decode path and freeze eager vectors</name>
  <files>modules/mb-image/png/encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/encode_test.mbt`: reuse `png_encode_graya8_image`, `png_encode_graya8_decode_matches_source`, the Gray16 public wire helpers, and the existing eager frozen-vector test.
    - `52-RESEARCH.md`: use the fixed `(13,A7)/(D2,4C)` source sequence and `00 13 A7 D2 4C` Stored/None scanline expectation.
    - `49-01-SUMMARY.md`: retain its literal-vector policy and public rather than private encoder/decoder evidence boundary.
  </read_first>
  <action>D-01: extend the existing `mb-image/png` eager tests with one compact GrayAlpha8 public evidence case built from `png_encode_graya8_image`, encoded through `PngEncoder::new_graya8_with_strategies(Stored, None)`, and decoded only by `ImageDecoder::decode(PngDecoder::new(), ...)` through the established public helper. Assert PNG signature and IHDR depth/type/interlace bytes, then assert the known Stored/None filtered scanline prefix exactly contains `00 13 A7 D2 4C`; keep the expectation literal and do not derive it with a second encoder. Require the decoded image to be straight `Rgba` and prove `(13,13,13,A7)` and `(D2,D2,D2,4C)` via the public decoder helper. D-02: keep every construction and decode assertion at these public API seams. Extend the established six-pair eager loop so each of Stored, FixedOrStored, and DynamicOrFixedOrStored crossed with None and Adaptive has the public type-4 framing and decode-canonicalization assertion; reserve the exact Stored/None payload check for the fixed vector. D-05: extend the existing literal frozen eager compatibility test with a literal Gray16 Stored vector while retaining literal Gray8, RGB8, and straight-RGBA8 vectors and their legacy/default/configured assertions. Do not modify the encoder, decoder, profiles, scripts, fixtures, or public API.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target native --frozen --filter "GrayAlpha8 public eager evidence"</automated>
  </verify>
  <done>A public eager test proves the non-symmetric GrayAlpha wire order and straight-RGBA8 canonicalization, all six public eager strategy/filter pairs preserve that semantic contract, and literal eager Gray8/Gray16/RGB8/RGBA8 vectors are frozen without production changes.</done>
  <reversibility rating="costly">Per D-01 and D-05, literal public PNG byte vectors become compatibility baselines; changing them later requires an intentional compatibility decision and replacement evidence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove hostile public GrayAlpha8 chunk schedules, frozen chunk vectors, and all-target portability</name>
  <files>modules/mb-image/png/stream_encode_test.mbt</files>
  <read_first>
    - `modules/mb-image/png/stream_encode_test.mbt`: reuse `png_stream_graya8_image`, `png_stream_graya8_eager_with_strategies`, `png_stream_gray16_public_drain`, `PNG Gray16 chunk public evidence`, and the chunk frozen-vector test.
    - `52-CONTEXT.md`: apply D-03's full strategy/schedule matrix and D-04's driver/sentinel constraints exactly.
    - `51-VERIFICATION.md`: retain the completed graya8 public construction/profile path; this task adds evidence only.
  </read_first>
  <action>D-02: construct every oracle through `PngEncoder::new_graya8_with_strategies` and every caller-buffered instance through `PngChunkEncoder::new_graya8_with_strategies`; keep the existing test-local GrayAlpha fixture and no private seam. D-03 and D-04: add one focused GrayAlpha public drain helper by adapting `png_stream_gray16_public_drain`: construct a fresh encoder per schedule, use `Z`-filled caller leases, append only the reported written prefix, require cumulative progress to equal prior accepted bytes plus the reported write, require all lease tails unchanged, require final bytes equal the fresh eager oracle, and verify a later sentinel lease receives zero bytes with sticky `Finished`. Add a public evidence test that iterates all three compression strategies and both filters; for every pair, directly pull a zero-length lease and assert `NeedOutput`, zero current/total progress, and unchanged sentinel, then run independent `[0UL, 1UL]`, `[1UL]`, and `[0UL, 8UL, 4UL, 1UL, 13UL, 2UL, 5UL, 3UL, 21UL]` drains. D-05: add the Gray16 literal Stored vector beside retained literal Gray8, RGB8, and straight-RGBA8 vectors in the chunk frozen-compatibility test; compare normal public drains to literals, never to generated expected values. D-06: keep all additions portable MoonBit and finish with the single four-target package test invocation; do not add target branches, FFI, release work, source copies, staging buffers, or retry behavior.</action>
  <verify>
    <automated>moon -C modules/mb-image test png --target all --frozen</automated>
  </verify>
  <done>All six public GrayAlpha chunk strategy/filter pairs match eager bytes under zero/one/ragged schedules, expose only accepted progress, preserve caller-owned tails, and retain sticky success terminals; four literal legacy format vectors remain frozen and the identical suite passes on all portable targets.</done>
  <reversibility rating="costly">Per D-03 and D-05, the schedule matrix and literal compatibility values protect externally observable caller-buffered ownership and byte stability; weakening them would remove a public regression boundary.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public image/encoder input to PNG bytes | Non-symmetric GrayAlpha source components cross the public `PngEncoder` profile and serialization boundary into a standards-visible type-4 raster. |
| Encoder state to caller-owned mutable lease | The caller chooses lease capacity and owns unwritten bytes while `PngChunkEncoder::pull` reports accepted output and terminal state. |
| Public PNG decoder to decoded image | Type-4 sample pairs cross decoder canonicalization into the public straight-RGBA8 image representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Tampering | GrayAlpha8 eager wire/decode evidence | high | mitigate | Task 1 implements D-01/D-02 with distinct `(13,A7)/(D2,4C)` source pairs, literal Stored/None scanline checks, public type-4 framing, and public RGBA canonicalization assertions. |
| T-52-02 | Tampering | caller-buffered pull accounting and lease ownership | high | mitigate | Task 2 implements D-03/D-04 for each six-pair member: direct zero lease, accepted-prefix-only accounting, untouched tails, eager parity, and later sticky-terminal sentinel checks. |
| T-52-03 | Tampering | legacy PNG compatibility baseline | high | mitigate | Tasks 1-2 implement D-05 with Gray8, Gray16, RGB8, and straight-RGBA8 byte-string literals, preventing the current encoder from becoming its own oracle. |
| T-52-04 | Denial of Service | portable evidence execution | medium | mitigate | Task 2 implements D-06 by running the same bounded package suite through `--target all`, exercising every supported portable target without platform-specific fallback code. |
</threat_model>

<verification>
1. Run `moon -C modules/mb-image test png --target all --frozen`; it must execute the identical public suite on wasm, wasm-gc, js, and native.
2. Run `git diff --check` and inspect the scoped diff: implementation changes must be limited to `modules/mb-image/png/encode_test.mbt` and `modules/mb-image/png/stream_encode_test.mbt`.
3. Confirm the GrayAlpha tests invoke only the public eager/chunk factories and public decoder; expected wire and frozen compatibility data are literals rather than outputs from another current encoder call.
</verification>

<success_criteria>
- [ ] GRAYA-04: Public `(13,A7)/(D2,4C)` GrayAlpha8 evidence proves type-4/8-bit/non-interlaced Stored/None wire ordering and public straight-RGBA8 `(13,13,13,A7)/(D2,D2,D2,4C)` canonicalization.
- [ ] GRAYA-05: Every compression/filter pair has fresh zero-capacity, one-byte, and deterministic ragged caller-buffered drains with eager byte identity, accepted-only progress, untouched lease tails, and sticky `Finished` evidence.
- [ ] Literal Gray8, Gray16, RGB8, and straight-RGBA8 eager and chunk vectors remain byte-identical compatibility baselines.
- [ ] `moon -C modules/mb-image test png --target all --frozen` passes with no platform branch or non-portable test path.
- [ ] No production PNG/model code, API, build/release script, fixture directory, source-tree copy, staging buffer, retry path, or deferred PNG capability is added.
</success_criteria>

<output>
Create `.planning/phases/52-portable-gray-alpha-public-evidence/52-01-SUMMARY.md` when done.
</output>
