---
milestone: v0.10
milestone_name: PNG Compression Optimization
audited: 2026-07-22
status: passed
scores:
  requirements: 4/4
  phases: 3/3
  integration: 5/5
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
observations:
  - "The scoped Png quality lane has no current passing verdict: Phase 34's submitted run exceeded its 120-second executor cap without reporting a test failure. This is incomplete supplemental evidence, not a failed requirement or broken flow."
  - "PngEncoder's general type comment still says it emits a canonical stored-DEFLATE representation, although configured construction may select FixedOrStored. The strategy-specific factory documentation and implementation are accurate; this is a non-blocking documentation-precision follow-up."
---

# v0.10 Milestone Audit — PNG Compression Optimization

## Verdict: Passed

All four v0.10 requirements are satisfied. The Phase 32 public strategy/factory seam is consumed by Phase 33's bounded plan and acknowledgement-aware emitter, and Phase 34 drives that public seam through repeated eager, hostile-capacity chunk, and complete-input decoder evidence. No required cross-phase link is orphaned or broken.

## Scope and phase evidence

| Phase | Requirement(s) | Verification | Summary requirements | Audit result |
|---|---|---|---|---|
| 32 — PNG Compression Strategy and Compatibility | PNGC-01 | `passed`, 4/4 must-haves | `PNGC-01` | Passed |
| 33 — Fixed-or-Stored PNG Planning and Emission | PNGC-02, PNGC-03 | `passed`, 5/5 must-haves | `PNGC-02`, `PNGC-03` in 33-02 closure summary | Passed |
| 34 — Portable PNG Compression Corpus Evidence | PNGC-04 | `passed`, 4/4 must-haves | `PNGC-04` | Passed |

The requirement traceability table marks PNGC-01 through PNGC-04 complete and assigns them to Phases 32–34. No milestone requirement is orphaned.

## Cross-phase wiring

| Connection | Trace | Status |
|---|---|---|
| Phase 32 public strategy → Phase 33 eager route | `PngEncoder::new_with_compression_strategy` retains `strategy`; `ImageEncoder::encode` passes it to `PngEncodeMachine::new_with_compression_strategy`; that invokes `_png_encode_preflight`. | WIRED |
| Phase 32 public strategy → Phase 33 caller-buffered route | `PngChunkEncoder::new_with_compression_strategy` passes the caller selection to the same machine constructor and preflight. | WIRED |
| Compatibility defaults → stored emitter | `PngEncoder::new()` and `PngChunkEncoder::new(...)` explicitly select `Stored`; the private machine preserves the stored plan and frozen byte regressions. | WIRED |
| Phase 33 selected plan → output acknowledgement lifecycle | Preflight chooses `PngDeflatePlan::Fixed` only when its total length is no larger than Stored, charges the selected work before construction, and the machine commits previewed bytes only after writer/lease acknowledgement. | WIRED |
| Phase 33 optimized public route → Phase 34 corpus/decoder | The corpus creates explicit Stored, two FixedOrStored eager, and FixedOrStored chunk outputs; it checks never-larger/strict-win and sends each optimized byte sequence through public complete-input `PngDecoder` component comparison. | WIRED |

## End-to-end flows

| Flow | Evidence | Status |
|---|---|---|
| Legacy compatible PNG encoding | Explicit legacy constructors select Stored and retain frozen RGB8/RGBA8 bytes. | Complete |
| Configured optimized eager/chunk encoding | Public FixedOrStored factories reach exact capability/geometry/output/work/budget admission, then produce eager/chunk parity with acknowledgement-safe progress and sticky terminals. | Complete |
| Compression corpus | Flat 32×1 RGB8 and straight-RGBA8 `0xaa` sources → Stored/FixedOrStored eager plus chunk output under `[0,1,3,2,5]` → deterministic equality/relative size → complete-input decode → per-component source comparison. | Complete |

## Current executable evidence

Executed during this audit:

- The exact corpus test was present exactly once in each filtered outline and passed 1/1 on js, wasm, wasm-gc, and native.
- `moon -C modules/mb-image test png --target all --frozen` passed 114/114 on wasm, wasm-gc, js, and native.

Git history corroborates the intended sequence: Phase 32 contract test → public factories/interface registration; Phase 33 behavior test → bounded fixed-or-stored emitter → closure evidence; Phase 34 corpus helpers → corpus evidence → verification report.

## Requirements Integration Map

| Requirement | Integration path | Status | Issue |
|---|---|---|---|
| PNGC-01 | Phase 32 public `PngCompressionStrategy` and configured factories → retained eager strategy / chunk constructor → shared encode machine; explicit legacy `Stored` routes | WIRED | — |
| PNGC-02 | Phase 32 `FixedOrStored` selection → Phase 33 `_png_encode_preflight` → selected fixed-or-stored plan, checked limits/disposition, single budget charge → machine construction | WIRED | — |
| PNGC-03 | Selected plan → Phase 33 preview/acknowledge → eager writer and caller lease `pull` → four-target configured parity/progress/sticky-terminal tests | WIRED | — |
| PNGC-04 | Phase 34 generated flat corpus → public Stored/FixedOrStored eager and chunk factories → byte-size/determinism assertions → complete-input public decoder/source-component comparison | WIRED | — |

**Requirements with no cross-phase wiring:** None.

## Non-blocking observations

1. `Invoke-MoonQuality.ps1 -Lane Png` has no current passing verdict because the submitted Phase 34 run was terminated at the executor's 120-second limit, with no reported test failure. The Phase 34 verifier correctly records it as **NOT COMPLETED**, not passed. The independent four-target corpus and complete-package rerun above are green, so this does not invalidate PNGC-01…04 or the audited flows. Re-run the lane in an environment with a larger command window for supplemental quality confirmation.
2. The generic `PngEncoder` type comment describes stored-DEFLATE output even though callers using the configured factory may select FixedOrStored. The configured-factory documentation, preflight branch, and tests are accurate; this is documentation precision only.
